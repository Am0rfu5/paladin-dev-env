# Milestone 2-3 cited status ledger

This file **supersedes** `REQUIREMENTS.md`'s `## Milestone 2-3 as-shipped ledger` section (D-21).
That section becomes a pointer to this file. Phases 7, 10 and 13 each add a sibling ledger
(`milestone-04-06.md`, `milestone-07-08.md`, `milestone-09-12.md`) rather than growing
REQUIREMENTS.md further — REQUIREMENTS.md is already ~4,000 lines and five inline sets of
`file:line`-cited verdicts would make it unreadable.

**Primary key: the `REQ-*` requirement ID.** Outstanding task items are nested under the
requirement they belong to, not given their own identifiers — nesting them keeps this ledger
joinable to `REQUIREMENTS.md` and `ROADMAP.md` without inventing new IDs (D-00e).

**Evidence bar.** A `satisfied` verdict requires a `file:line` citation **plus** a named passing
test, example, or command that exercises it. A `file:line` citation with nothing exercising it is
not `satisfied` — it gets its own verdict, `present, unproven` (D-01). This bar applies to all 118
rows below **without exception**, including every row REQUIREMENTS.md's run-2 ledger already
marked `Shipped` or `Shipped (relocated)`: an ingest `Shipped` verdict **is** the bare "the code
exists" claim this bar exists to reject. It is the same bar Phase 1 applied to Milestone 1, and the
same reasoning: "the code exists" has already produced false-positive completions in this corpus
(Milestone 4 Epic 3's task list is fully checked while three CLI-only dependencies remain
unconditional in library builds).

**Path caveat.** Read every row below with two systematic caveats recorded once here, not
repeated per row (D-04): (a) every `src/core|application|infrastructure` path in the run-2 PRDs
predates the Milestone 5 workspace decomposition; the current layout is the ten-library-crate-plus-
facade shape recorded in `.planning/codebase/STRUCTURE.md` and the *Milestone 4-6 as-shipped
ledger*. Citations below use the **current** locations, verified by direct inspection of
`release/v0.7.0` in this worktree. (b) The Milestone-1 benchmark files those PRDs reference have
been relocated into per-crate `benches/` directories. A row whose only divergence from its PRD is
(a) or (b) is `superseded by shipped code` pointing at this note, not a fresh divergence write-up.

## Verdict legend

| Verdict | Meaning |
|---|---|
| `satisfied` | `file:line` citation **and** a named passing test, example, or command exercising it |
| `present, unproven` | `file:line` citation exists, but nothing exercises it |
| `genuinely outstanding` | No shipped code satisfies the requirement |
| `deferred with reason` | Explicitly deferred, with the deferring document and reason cited |
| `superseded by shipped code` | Shipped code answers the requirement differently than the ingested document specified, and the shipped answer is recorded as authoritative |

## Row order and amendment convention

Epic sections below appear in REQUIREMENTS.md's own run-2 order — 11, 12, 13, 20, 14, 15, 16,
17 / 17.5, 18, 19, 21, 22, 23, 24 — and are never re-sorted. Rows within a section appear in the ID
order REQUIREMENTS.md lists them. Later plans replace a row's **Verdict** and **Evidence** cells in
place; they never insert, delete, or reorder rows. The same `file:line` citation may legitimately
appear in more than one row — two requirements citing the same artefact keep separate rows and
separate verdicts, because the `REQ-*` ID, not the citation, is the primary key (D-00e). Amendments
follow D-00g: edit in place, retain superseded text, date every amendment, never a separate
corrections file.

### Epic 11 — Sanctum Memory Foundation (8 IDs)

Epic-level note: `EPIC_11_COMPLETION_SUMMARY.md` claims COMPLETE while recording Qdrant as
DEFERRED and `tasks-sanctum-memory-foundation.md` carries 111 open checkboxes, but
`intel/code-verification.md` records Qdrant as verified shipped, so the 111 count is stale and is
not carried as forward work.

| ID | Verdict | Evidence |
|---|---|---|
| REQ-embedding-port | satisfied | `EmbeddingPort` trait at `crates/paladin-ports/src/output/embedding_port.rs:371` (`Embedding` value type at `:355`, `EmbeddingError` at `:241`); exercised through `OpenAIEmbeddingAdapter`'s trait impl (`crates/paladin-llm/src/openai/embedding.rs:161`) by `cargo test --offline --features openai-embeddings --test openai_embedding_integration` — 8/8 passed, run during this task |
| REQ-openai-embedding-adapter | satisfied | `OpenAIEmbeddingAdapter` struct at `crates/paladin-llm/src/openai/embedding.rs:48`, `impl EmbeddingPort for OpenAIEmbeddingAdapter` at `:161`; exercised by `cargo test --offline --features openai-embeddings --test openai_embedding_integration` — 8/8 passed (mockito-mocked HTTP, no live API key required), run during this task |
| REQ-sanctum-port | satisfied | `SanctumPort` trait at `crates/paladin-ports/src/output/sanctum_port.rs:585`; exercised through `InMemorySanctum`'s trait impl (`crates/paladin-memory/src/sanctum/in_memory_adapter.rs:230`) by `cargo test --offline --test in_memory_sanctum_integration` — 17/17 passed, run during this task |
| REQ-qdrant-sanctum-adapter-v1 | present, unproven | `QdrantSanctumAdapter` struct at `crates/paladin-memory/src/sanctum/qdrant_adapter.rs:59`, `impl SanctumPort for QdrantSanctumAdapter` at `:377`; compiles clean under `cargo build --offline -p paladin-memory --features qdrant` (run during this task, exit 0). Its dedicated exerciser — `tests/integration/qdrant_sanctum_tests.rs`, the `[[test]] name = "qdrant_sanctum_integration"` target at `Cargo.toml:197-199` requiring the `qdrant` feature — carries `#[ignore = "Requires Qdrant running on localhost:6334"]` on all 15 of its `#[tokio::test]` functions and needs a live Qdrant instance; this sandbox has no `docker` binary (`command -v docker` returns nothing), so nothing exercises it here. The Epic 11 "DEFERRED" record is separately confirmed stale — the adapter and its feature wiring are real and compile |
| REQ-in-memory-sanctum | satisfied | `InMemorySanctum` struct at `crates/paladin-memory/src/sanctum/in_memory_adapter.rs:73`, `impl SanctumPort for InMemorySanctum` at `:230`; exercised by `cargo test --offline --test in_memory_sanctum_integration` — 17/17 passed, run during this task |
| REQ-sanctum-domain-model | satisfied | `Memory` struct at `crates/paladin-core/src/platform/container/sanctum.rs:58`, `MemoryType` at `:19`, `MemoryDecayStrategy` at `:38`, `SanctumEntry` at `:204`, `MemoryBuilder` at `:119`; exercised by `cargo test --offline -p paladin-ai-core sanctum::` — 3/3 passed, run during this task (further exercised indirectly by all 17 `in_memory_sanctum_integration` tests and by 10 domain-model unit tests co-located in `qdrant_adapter.rs:587-778`) |
| REQ-sanctum-configuration | satisfied | `SanctumConfig` struct at `crates/paladin-memory/src/config/sanctum.rs:45`, `QdrantSanctumConfig` at `:22`; exercised by `cargo test --offline -p paladin-memory sanctum::` — 13/13 passed, including 6 `config::sanctum::tests::*` cases, run during this task |
| REQ-sanctum-garrison-coexistence | satisfied | Garrison and Sanctum are independently wired module families: `pub mod garrison;` at `crates/paladin-memory/src/lib.rs:42` and `pub mod sanctum;` at `:48`, each with its own directory (`crates/paladin-memory/src/garrison/{in_memory_garrison,sqlite_garrison,token_counter,mod}.rs`, `crates/paladin-memory/src/sanctum/{in_memory_adapter,qdrant_adapter,mod}.rs`) and its own config module (`pub mod garrison;` at `crates/paladin-memory/src/config/mod.rs:3`, `pub mod sanctum;` at `:5`); confirmed by direct listing (`ls crates/paladin-memory/src/{garrison,sanctum,config}`, run during this task) and by the 13/13 pass above, which exercises the Sanctum config path independently of any Garrison test |

### Epic 12 — Sanctum RAG Integration (8 IDs)

| ID | Verdict | Evidence |
|---|---|---|
| REQ-qdrant-sanctum-adapter-v2 | present, unproven | Same shipped adapter as Epic 11's `REQ-qdrant-sanctum-adapter-v1` row — `QdrantSanctumAdapter` struct at `crates/paladin-memory/src/sanctum/qdrant_adapter.rs:59`, `impl SanctumPort for QdrantSanctumAdapter` at `:377`, implementing all five CRUD operations this row's acceptance text requires (store, search, `delete` at `:461`, `update` at `:496`, `count` at `:502`), with payload filtering (`SanctumFilter`/`Condition`/`Filter`) and connection-error mapping into `SanctumError`. The shipped naming and connection shape answer v1's ingested position, not v2's: struct name `QdrantSanctumAdapter` (v1's exact name; v2 specified `QdrantSanctum`), file stem `qdrant_adapter.rs` (v1's path; v2 specified `qdrant_sanctum.rs`), default URL `http://localhost:6334` — v1's port 6334, confirmed at `crates/paladin-memory/src/config/sanctum.rs:24,36` — not v2's specified port 6333. v2's explicit health-check requirement ("MUST implement a health check verifying the collection exists and is accessible") is not met: `grep -n health crates/paladin-memory/src/sanctum/qdrant_adapter.rs` returns nothing. Same verdict and same reasoning as the Epic 11 v1 row, not contradictory: compiles clean (`cargo build --offline -p paladin-memory --features qdrant`, exit 0, run during this task); its dedicated exerciser (`tests/integration/qdrant_sanctum_tests.rs`, the `qdrant_sanctum_integration` `[[test]]` target) is entirely `#[ignore]`d needing a live Qdrant on localhost:6334, and this sandbox has no `docker` binary (`command -v docker` returns nothing, confirmed during this task). |
| REQ-paladin-builder-sanctum-integration | satisfied | `PaladinBuilder::with_sanctum` at `src/application/services/paladin/paladin_builder.rs:751`, `with_embedding_port` at `:783`, `memory_extraction_strategy` at `:817`; fields `sanctum_port`/`embedding_port`/`memory_extraction_strategy` at `:89-91`; builder validation requiring an embedding port whenever a sanctum is configured at `:1153-1156` ("embedding_port is required when sanctum_port is set (needed for RAG operations)"). A minor location divergence from the acceptance text's "stored in `PaladinConfig` or `PaladinData`" phrasing: the three fields live directly on `PaladinBuilder`/`PaladinExecutionService`, not on either domain struct — no behavioural difference. Exercised by `cargo test --offline -p paladin-ai --features cli --lib -- paladin_builder::tests::test_builder_with_sanctum paladin_builder::tests::test_builder_with_embedding_port paladin_builder::tests::test_builder_memory_extraction_strategy paladin_builder::tests::test_builder_validation_sanctum_with_embedding paladin_builder::tests::test_builder_validation_sanctum_without_embedding` — 5/5 passed, run during this task. |
| REQ-memory-extraction-strategy | satisfied | `MemoryExtractionStrategy` enum at `crates/paladin-memory/src/config/rag.rs:96` — `EveryTurn`, `OnCompletion` (`#[default]` at `:100`), `Manual`, `Threshold { importance: u8 }` at `:105`; `MemoryExtractionService::extract_memories` (the `Manual` entry point) at `crates/paladin-memory/src/services/memory_extraction_service.rs:65`; `PaladinExecutionService::should_extract_memories` triggers only for `OnCompletion` at `src/application/services/paladin/paladin_execution_service.rs:1132-1135`. **Divergence**: `Threshold`'s payload is `importance: u8` in shipped code, not the PRD's `importance: f32` — an integer 0-255 scale rather than a 0.0-1.0 float, and never compared against the `Memory.importance: f32` field it is meant to gate anywhere in the tree. Exercised by `cargo test --offline -p paladin-memory` — `config::rag::tests::test_memory_extraction_config_defaults`, `test_memory_extraction_threshold_zero_invalid`, `test_memory_extraction_threshold_nonzero_valid`, `services::memory_extraction_service::tests::test_extraction_strategy_default`, `test_extraction_strategy_equality`, `test_extraction_strategy_threshold`, `test_extraction_strategy_serialization` — 7/7 passed, run during this task. |
| REQ-rag-retrieval-service | satisfied | `RagRetrievalService` at `crates/paladin-memory/src/services/rag_retrieval_service.rs:28` (relocated from the PRD's `src/application/use_cases/sanctum/` path per this ledger's path caveat (a)); `retrieve_context` at `:68`; `format_for_prompt` at `:210`; similarity filter (min 0.7 default) at `:106`, >0.95 dedup at `:117`, relevance ranking at `:158`, token-budget truncation at `:171`; 5-second-timeout graceful-degradation wrapper `retrieve_context_with_timeout` at `:242-263` (returns empty `Vec` on timeout, not an error). Exercised by `cargo test --offline -p paladin-memory rag` — 17/17 passed, run during this task, including `test_successful_retrieval_with_multiple_memories`, `test_filtering_by_min_similarity`, `test_format_for_prompt`, `test_empty_results_graceful_handling`. The three live-Qdrant-gated integration tests in `tests/integration/rag_integration_tests.rs` (`test_rag_retrieval_service_with_real_qdrant`, `test_rag_retrieval_with_token_budget_limiting`, `test_rag_context_formatting`) skip cleanly rather than exercise anything — run during this task via `cargo test --offline --features qdrant --test rag_integration` (9/9 report "ok", but the 3 Qdrant-gated ones print "Skipping test: Qdrant not available on localhost:6334" and return early; no `docker` binary in this sandbox). This `satisfied` verdict rests on the 17 in-memory-backed unit tests, not the 3 skipped ones. |
| REQ-rag-config | satisfied | `RagConfig` struct at `crates/paladin-memory/src/config/rag.rs:31` — `top_k`/`min_similarity`/`max_tokens`/`timeout_seconds`/`retrieval_trigger`; `Default` impl at `:44-54` (`top_k: 5, min_similarity: 0.7, max_tokens: 2000, timeout_seconds: 5, retrieval_trigger: Always`, matching every PRD default including FR-6.1's `timeout_seconds: 5`); `RetrievalTrigger` enum (`Always`/`KeywordBased`/`SemanticThreshold`) at `:15`; `validate()` at `:60-88`. YAML-configurable via `pub rag: Option<RagConfig>` on the application settings struct at `src/config/settings.rs:41`; builder-configurable indirectly by constructing a `RagRetrievalService` with a `RagConfig` and attaching it via `PaladinExecutionService::with_rag_retrieval` at `src/application/services/paladin/paladin_execution_service.rs:234` (no direct `.rag_config()` setter exists on `PaladinBuilder`). Exercised by `cargo test --offline -p paladin-memory` — `config::rag::tests::test_rag_config_defaults`, `test_rag_config_validate_ok`, `test_rag_config_validate_zero_top_k`, `test_rag_config_validate_bad_similarity`, `test_rag_config_validate_zero_timeout` — 5/5 passed, run during this task. |
| REQ-memory-extraction-service | satisfied | `MemoryExtractionService` at `crates/paladin-memory/src/services/memory_extraction_service.rs:41`; `extract_memories` at `:65` (builds an extraction prompt via `build_extraction_prompt` `:199`, calls the LLM, parses a JSON array via `parse_extraction_response` `:212`, generates embeddings and skips duplicates detected at >0.95 similarity via `check_for_duplicates` `:229` — no merging, matching "skip duplicates... (no merging)"), stores via `SanctumPort::store` in `store_memories` `:243`, and logs count/avg_importance/duration at `:186-193`. Exercised by `cargo test --offline -p paladin-memory memory_extraction` — 15/15 passed, run during this task, including `test_successful_extraction_with_multiple_memory_types`, `test_importance_scoring_correctly_assigned`, `test_duplicate_detection_prevents_restorage`, `test_llm_failure_handled_gracefully`, `test_malformed_json_response_handled`, `test_embedding_failure_skips_memory`, `test_empty_conversation_returns_empty`. |
| REQ-execution-service-rag-integration | present, unproven | `PaladinExecutionService` fields `rag_retrieval_service`/`memory_extraction_service` at `src/application/services/paladin/paladin_execution_service.rs:125,128`; `with_rag_retrieval` at `:234`, `with_memory_extraction` at `:249`. `execute_internal` checks `check_sanctum_configured()` and calls `retrieve_context_with_timeout` before the LLM call at `:693-732`; on failure it logs a warning and continues with empty context (non-fatal) at `:721-729`; after successful execution, `OnCompletion` triggers `extract_memories_async` at `:908-911` and `:935-938`. This row's three previously-uninspected criteria, inspected directly against the tree during this task: **(1) prompt's relevant-context section** — present, but titled `## Relevant Context from Memory` (`:971`, `:1027`), not the PRD's exact `## Relevant Context` (heading-text divergence only); directly tested by `cargo test --offline -p paladin-ai --features cli --lib -- paladin_execution_service::tests::test_rag_context_injection paladin_execution_service::tests::test_rag_context_injection_empty` — 2/2 passed, run during this task, asserting `prompt.contains("## Relevant Context from Memory")`. **(2) async non-blocking extraction** — present: `extract_memories_async` (`:1140-1181`) spawns via `tokio::spawn` at `:1152` and never awaits the returned `JoinHandle`, so `execute_internal` returns its `PaladinResult` without waiting for extraction; confirmed by direct code inspection during this task — no test asserts the non-blocking *timing* itself. **(3) three metrics** — partially present: `retrieval_latency_ms` and `memories_retrieved_count` are computed (`:705-706`,`:722`) and logged via `info!`/`warn!` at `:715-718` and `:723-726`; `extraction_triggered` is computed (`:666`) and set `true` at `:909` and `:936` but is **never read, logged, or included in `PaladinResult`** anywhere in the file — a dead write, underscore-prefixed to silence the unused-variable warning (`grep -n "_extraction_triggered"` shows exactly those three lines, no fourth). None of the three metrics is exposed on `PaladinResult` or any structured metrics object returned to the caller. Additional finding beyond the three named criteria: `SanctumError` (`crates/paladin-ports/src/output/sanctum_port.rs:284`) carries `StorageError`/`SearchError`/`InvalidDimension`/`NotFound`/`ConfigError` — the Epic 11 variant set — not this row's specified `ConnectionError`/`QueryError`/`StorageError`/`EmbeddingError`; embedding failures fold into `SearchError` (`rag_retrieval_service.rs:74-76`) rather than a distinct `EmbeddingError` variant. Given the `extraction_triggered` dead-write and the missing `SanctumError` variant names, this row does not clear the `satisfied` bar cleanly on the tested parts alone — `present, unproven`, not rounded up. |
| REQ-rag-performance-targets | deferred with reason | `crates/paladin-memory/benches/sanctum_benchmarks.rs` (8 benchmark fns registered at `:406-416`) benchmarks `InMemorySanctum` store/search/filter/update/delete/count at several vector counts, but exercises neither `RagRetrievalService::retrieve_context` nor `MemoryExtractionService::extract_memories` directly, and records no p95 figure anywhere in `.planning/`. No concurrency benchmark (10+ concurrent Paladins sharing one Sanctum) or memory-overhead benchmark exists. `.planning/STATE.md:642` records this row's non-functional targets explicitly: "Vision and RAG latency targets never measured (single image < 5 s; retrieval < 500 ms p95; extraction < 3 s p95) \| Deferred to v2 — no baseline document exists \| Ingest run 2." Owner: v2 (STATE.md's own "Deferred to v2" disposition; no named individual or team is recorded anywhere in the corpus for this item). |

### Epic 13 — Sentinel Vision System (13 IDs)

| ID | Verdict | Evidence |
|---|---|---|
| REQ-vision-content-model | present, unproven | `VisionContent` enum (`ImageUrl`/`ImageBase64`/`ImageFile`) at `crates/paladin-core/src/platform/container/vision.rs:33`, `ImageDetail` at `:15`, `VisionRequest` at `:131`; exercised by `cargo test --offline -p paladin-ai-core vision::` — 11/11 passed, run during this task (multi-image support confirmed by `test_vision_request_multiple_images`). The acceptance criterion "image metadata MUST include format, size and dimensions when available" has zero citation anywhere in the type — no field carries file size or pixel dimensions on any `VisionContent` variant (`grep -n "dimensions\|width\|height\|image_size" vision.rs` returns nothing), so that sub-clause is unproven rather than shipped, hence `present, unproven` rather than `satisfied` for the row as a whole |
| REQ-vision-format-validation-v1 | superseded by shipped code | Format-type validation is real and framework-side: `VisionContent::validate_format()` at `crates/paladin-core/src/platform/container/vision.rs:70`, returning `VisionError::UnsupportedFormat`; exercised by the same 11/11 `cargo test --offline -p paladin-ai-core vision::` run (`test_vision_content_validation`, `test_vision_content_base64_validation`, `test_vision_content_file_validation`). But this row's size-limit half — `VisionError::FileTooLarge { size, max }` — is never constructed by any validation code path in the tree; it exists only as an enum variant manually built inside a unit test (`vision.rs:504`), and neither vision adapter nor `execute_with_vision` calls `validate_format()`/`validate()` at all before sending (`grep -rn "validate_format\|\.validate()" crates/paladin-llm/src/openai/vision.rs crates/paladin-llm/src/anthropic/vision.rs src/application/services/paladin/paladin_execution_service.rs` returns nothing). The shipped design instead delegates size (and, in practice, format) validation to the provider per Epic 20's `REQ-vision-format-validation-v2` (`docs/src/appendix/sentinel.md:796-801` documents the 20MB/5MB provider limits) — the shipped answer follows v2's delegated approach for the size half of this row, not v1's framework-side `FileTooLarge` check |
| REQ-openai-vision-adapter-v1 | present, unproven | `impl VisionCapableLlm for OpenAIAdapter` at `crates/paladin-llm/src/openai/vision.rs:363` (`generate_with_vision` converts `VisionContent` to OpenAI message format for both URL and base64/file at `:258-298`, vision-model gate at `:332`); helper logic exercised by `cargo test --offline -p paladin-llm --features openai,anthropic,vision vision` — 18/18 passed, run during this task (`openai::vision::tests::test_is_vision_model`, `test_convert_detail`, `test_vision_config_default`). The trait method `generate_with_vision` itself is only exercised by the live-API-gated `tests/integration/vision_integration_test.rs`, skipped in this sandbox (`ENABLE_VISION_TESTS` unset, no API key) — nothing here runs the actual HTTP call, so `present, unproven` rather than `satisfied` |
| REQ-anthropic-vision-adapter-v1 | present, unproven | `impl VisionCapableLlm for AnthropicAdapter` at `crates/paladin-llm/src/anthropic/vision.rs:358` (base64-only images per Anthropic's API; URL/file images auto-downloaded and encoded at `:231-269`); helper logic exercised by the same `cargo test --offline -p paladin-llm --features openai,anthropic,vision vision` run — 18/18 passed (`anthropic::vision::tests::test_is_vision_model`, `test_detect_mime_type`, `test_convert_vision_content_base64`, `test_build_vision_content_blocks_non_vision_model`). As with the OpenAI row, `generate_with_vision` itself is only exercised by the skipped live-API test |
| REQ-vision-capable-llm-trait | satisfied | `VisionCapableLlm: LlmPort` trait at `crates/paladin-ports/src/output/vision_llm_port.rs:52`; exercised by `cargo test --offline -p paladin-ports vision` — 6/6 passed, run during this task (object-safety, `Send`+`Sync`, mock-impl tests). Recorded as the adapter-author surface, coexisting with `REQ-vision-port` (Epic 20) per `.planning/decisions/0011-vision-port-surfaces.md` — not a variant of, or a competitor to, `VisionPort` |
| REQ-paladin-vision-api-v1 | superseded by shipped code | `PaladinBuilder::enable_vision(bool)` at `src/application/services/paladin/paladin_builder.rs:517` is shipped and validated (rejects a vision-enabled config on a non-vision-capable adapter at `:1161-1167`). But `Paladin::run_with_vision(task, images)` — the v1 PRD's exact entry point — was never implemented; the only occurrence of the name anywhere in the tree is a stale placeholder comment at `src/application/services/paladin/paladin_execution_service.rs:2237` ("This is a placeholder until we implement the run_with_vision method"). The shipped entry point is instead `PaladinExecutionService::execute_with_vision` — Epic 20's design, see `REQ-paladin-vision-api-v2` — recorded as authoritative |
| REQ-vision-error-model-v1 | superseded by shipped code | Shipped `VisionError` lives at `crates/paladin-core/src/platform/container/vision.rs:189`, not a v1-specified path (v1 names none) and not the v2-specified `container/sentinel/vision_types.rs` — see this ledger's path caveat. All 7 v1-required variants are present (`UnsupportedFormat`, `FileTooLarge`, `InvalidImage`, `ModelNotSupported`, `NetworkError`, `EncryptionError` at `:210-212`, `IoError`), plus all v2-required variants (`AuthenticationError`, `RateLimitExceeded`, `ProviderError`, `Timeout`, `UnsupportedProvider`, `MaxRetriesExceeded`), merged into one enum rather than either PRD's exact set. `DocumentError` (this row's other half) ships all 5 required variants at `crates/paladin-core/src/platform/container/document.rs:108-131`. Exercised by `cargo test --offline -p paladin-ai-core vision::` (11/11, incl. `test_vision_error_variants`, `test_vision_error_existing_variants`) and `cargo test --offline -p paladin-content document_adapter` (20/20). The merged, relocated shape is recorded as authoritative per the head-note path caveat, matched consistently with the Epic 20 `REQ-vision-error-model-v2` row below |
| REQ-vision-security-encryption | present, unproven | REQUIREMENTS.md's run-2 ledger recorded this capability as absent from the tree on three counts — no encryption-at-rest artefact, no zeroization, no retention policy, and no `VisionError::EncryptionError` variant. All three are verified false by direct inspection during this task: `VisionError::EncryptionError` at `crates/paladin-core/src/platform/container/vision.rs:210-212`; `EncryptionService::encrypt_image_data` at `src/infrastructure/security/encryption.rs:200` and `decrypt_image_data` at `encryption.rs:217`; `SecureData` with `#[derive(Zeroize, ZeroizeOnDrop)]` at `encryption.rs:68` and `SecureData::is_expired` at `encryption.rs:95`; `DataRetentionPolicy::should_retain` at `encryption.rs:131` — two methods on two different types, not one method under two names. The capability compiles and carries its own unit tests, but `grep -rln "EncryptionService\|DataRetentionPolicy\|VisionError::EncryptionError" src/ crates/ \| grep -v "infrastructure/security"` returns no output — zero consumers outside `src/infrastructure/security/` call any of them; `execute_with_vision` never touches `EncryptionService`. The same zero-consumer pattern extends to the module's own audit-logging utility: `infrastructure/security/audit.rs`'s `log_vision_processing` (unit-tested at `:373`) is likewise never called from the vision execution path. Verdicted `present, unproven` rather than `satisfied` for exactly this reason — see `.planning/decisions/0011-vision-port-surfaces.md` (ADR-0011) for the full disposition; Phase 6 CLOSE-03 owns the wiring decision: either wire `EncryptionService::encrypt_image_data` into the vision path, or record the service as a deliberately unimposed consumer-facing utility |
| | | **New finding (plan 05-08):** the run-2 ledger's REQUIREMENTS.md absence claim for `REQ-vision-security-encryption` is superseded by direct inspection performed during this task (2026-08-04) — see ADR-0011, which re-executed the zero-consumer grep independently rather than transcribing an earlier pass |
| REQ-pdf-extraction | present, unproven | `PdfExtractor::extract(path)` at `crates/paladin-content/src/adapters/document/pdf_extractor.rs:40`, `extract_bytes(bytes)` at `:67`, returning `Document { pages, metadata, total_chars }`; exercised by `cargo test --offline -p paladin-content pdf` — 16/16 passed, run during this task, covering metadata extraction, page-parsing helpers (`parse_text_into_pages`, `preserve_structure`), and error paths (`test_extract_bytes_invalid_pdf`, `test_extract_nonexistent_file`). No test exercises the happy-path extraction of a genuine multi-page PDF — `examples/document_processing.rs:35-36` itself falls back to a text-content demo because `./examples/assets/sample_document.pdf` does not exist in the tree — so the `>95% text accuracy` and page-count targets are unproven, not merely unmeasured |
| REQ-document-port | satisfied | `DocumentPort` trait at `crates/paladin-ports/src/input/document_port.rs:114` (`ingest` at `:120`, `chunk` at `:127`); `DocumentSource::{File, Bytes, Url}` at `:14-26`; `ChunkConfig{chunk_size, chunk_overlap, separator}` at `:42-49`. Implemented by `DocumentAdapter` (`crates/paladin-content/src/adapters/document/document_adapter.rs`) for PDF/TXT/MD (`:40-42` format dispatch); exercised by `cargo test --offline -p paladin-content document_adapter` — 20/20 passed, run during this task, including `test_ingest_txt_bytes` and `test_ingest_markdown_bytes` (real `ingest()` happy path, not just format detection), 5 `test_chunk_*` cases, and `test_thread_safety` (`Send`+`Sync`) |
| REQ-vision-cli-and-yaml | present, unproven | Repeatable `--image` flag (`Vec<PathBuf>`) at `src/application/cli/commands/agent.rs:70-71`; `vision_enabled`/`images`/`documents` YAML fields at `src/application/cli/config/paladin_config.rs:79,86-87` with cross-field validation (`vision_enabled` true requires at least one image or document, `:282-288`); exercised by `cargo test --offline -p paladin-ai --features cli,vision,llm-openai,llm-anthropic --lib -- agent::tests` — 12/12 passed (incl. `test_agent_run_args_with_images`, `test_agent_run_args_with_document`) and `-- vision` — 11/11 passed (incl. `test_load_paladin_config_with_vision_fields`), both run during this task. Two divergences from this row's own acceptance text: (a) `--document` is `Option<PathBuf>` (`agent.rs:74-75`), not repeatable, unlike the requirement's "repeatable `--image` and `--document` flags"; (b) `security.encrypt_at_rest` / `security.data_retention_hours` YAML keys do not exist anywhere in the tree (`grep -rn "encrypt_at_rest\|data_retention_hours" src/ crates/ config.example.yml` returns nothing) — consistent with the `REQ-vision-security-encryption` row's zero-consumer finding above |
| REQ-battalion-vision-integration | present, unproven | Formation and Phalanx vision usage is demonstrated end-to-end in `examples/vision_battalion.rs` (sequential Formation pipeline, parallel Phalanx processing at `:96-268`, each Paladin calling `execute_with_vision` independently); the example compiles clean (`cargo check --offline --example vision_battalion --features vision,llm-openai`, exit 0, run during this task). This matches Epic 20 NG-6's narrowing — concurrent/sequential vision handling lives at the Battalion orchestration level via repeated single-image calls rather than new Battalion-type methods (`.planning/INGEST-CONFLICTS.md:626-627`). But `Campaign` conditional branching on vision-analysis results and `ChainOfCommand` delegating vision tasks to sub-agents have zero citation anywhere in the tree: `grep -rn "vision" crates/paladin-battalion/src/formation_service.rs crates/paladin-battalion/src/phalanx_service.rs crates/paladin-core/src/platform/container/battalion/campaign.rs crates/paladin-core/src/platform/container/battalion/chain_of_command.rs` returns only the default `vision_enabled: false` field initializer in each file, no vision-specific logic |
| REQ-vision-performance-and-config | deferred with reason | `VisionConfig{retry, openai, anthropic}` struct at `crates/paladin-llm/src/config/vision.rs:70` with validation (`:81-86`); exercised by the 18/18 `cargo test --offline -p paladin-llm --features openai,anthropic,vision vision` run (`config::vision::tests::*`, 7 cases). Two gaps beyond the unmeasured targets: (a) the config is never actually injected via constructor — both `OpenAIAdapter::generate_with_vision` and `analyze_image` hardcode `VisionConfig::default()` at `crates/paladin-llm/src/openai/vision.rs:369` and `:491` rather than reading any configured value, and `config.example.yml` carries no `vision:` section at all (`grep -n "vision" config.example.yml` returns nothing); (b) the end-to-end latency targets (single image < 5s, batch of 10 < 15s, vision API call < 30s typical, total retry < 60s) have no recorded measurement anywhere in `.planning/`, and `STATE.md:642` records them explicitly deferred: "Vision and RAG latency targets never measured (single image < 5 s; retrieval < 500 ms p95; extraction < 3 s p95) \| Deferred to v2 — no baseline document exists \| Ingest run 2". Owner: Phase 6 (CLOSE-02/CLOSE-03 scope) |

### Epic 20 — Vision Pipeline Completion (6 IDs)

Epic-level note: the authoritative numbering for this section is **20 = Vision Pipeline
Completion**. `RELEASE_NOTES_MILESTONE_3.md` attributing "Epic 20" to the Council pattern is a
documentation defect, not an alternate valid numbering — see
`.planning/decisions/0010-milestone-3-epic-numbering.md` (ADR-0010) for the full disposition and
its downstream consequences. Not restated here.

| ID | Verdict | Evidence |
|---|---|---|
| REQ-vision-format-validation-v2 | present, unproven | Adapters delegate format and size validation to the provider: neither `crates/paladin-llm/src/openai/vision.rs` nor `crates/paladin-llm/src/anthropic/vision.rs` calls `VisionContent::validate_format()`/`VisionRequest::validate()` anywhere (`grep -rn "validate_format\|\.validate()"` across both files and `execute_with_vision` returns nothing — see the `REQ-vision-format-validation-v1` row above), and neither performs resizing, cropping or any image preprocessing (`grep -rn "resize\|preprocess\|crop"` across both adapter files returns nothing) — images are sent as-is, matching this row's "MUST NOT preprocess" clause. Provider size limits are documented at `docs/src/appendix/sentinel.md:796-801` (OpenAI 20MB, Anthropic 5MB, "Recommended: keep images under 2MB"). No test constructs an oversized or malformed image and confirms provider-side rejection (that would require a live API call), so `present, unproven` rather than `satisfied` |
| REQ-openai-vision-adapter-v2 | present, unproven | `impl VisionPort for OpenAIAdapter::analyze_image` at `crates/paladin-llm/src/openai/vision.rs:447-522`, POSTing to `{base_url}/chat/completions` (`:210`, `base_url` defaults to `https://api.openai.com/v1`). Retry handling is genuinely present, not absent: `map_status_to_error` (`:185-196`) maps `400` → `VisionError::InvalidImage`, `401` → `AuthenticationError`, `429` → `RateLimitExceeded`, `5xx` → `ProviderError`; `is_transient_error` (`:174-183`) flags `429`/`500`/`502`/`503`/`504` as retryable; `execute_vision_request` (`:199-256`) retries up to `vision_config.retry.max_retries` (default 3) with exponential backoff via `calculate_backoff_delay` (`:165-172`, default 1000ms × 2.0^attempt) before surfacing `MaxRetriesExceeded`. This matches the row's full 400/401/429+5xx contract. However, unlike the Anthropic adapter below, OpenAI's own test module carries no direct unit test of `is_transient_error` or `calculate_backoff_delay` — only `test_is_vision_model`, `test_convert_detail`, `test_vision_config_default` and `test_vision_retry_config_default` run (part of the 18/18 `cargo test --offline -p paladin-llm --features openai,anthropic,vision vision` pass, run during this task) — and the full `analyze_image` HTTP round-trip is only exercised by the skipped live-API test, so `present, unproven` |
| REQ-anthropic-vision-adapter-v2 | present, unproven | `impl VisionPort for AnthropicAdapter::analyze_image` at `crates/paladin-llm/src/anthropic/vision.rs:441-515`, POSTing to `{base_url}/messages` (`:184`, default `https://api.anthropic.com/v1`) with `x-api-key`/`anthropic-version` headers (`:166-180`). Same retry contract as the OpenAI row — `map_status_to_vision_error` (`:146-157`) maps 400/401/429/5xx identically, `is_transient_error` (`:135-144`) and `execute_vision_request` (`:159-229`) retry with the same default backoff — and here the retry helpers ARE directly unit-tested: `test_calculate_backoff_delay` and `test_is_transient_error` both pass as part of the 18/18 run above, alongside `test_detect_mime_type` and `test_convert_vision_content_base64`. The full `analyze_image` HTTP round-trip remains live-API-gated and skipped in this sandbox, so `present, unproven` rather than `satisfied` |
| REQ-vision-port | satisfied | `VisionPort` trait at `crates/paladin-ports/src/output/vision_port.rs:47`; recorded as the execution-service surface and recommended entry point for application code, reached via `PaladinExecutionService::execute_with_vision` at `src/application/services/paladin/paladin_execution_service.rs:517`, coexisting with `REQ-vision-capable-llm-trait` (Epic 13) per `.planning/decisions/0011-vision-port-surfaces.md` (ADR-0011). Exercised end-to-end (mocked adapter, not live HTTP) by `cargo test --offline -p paladin-ai --features cli,vision,llm-openai,llm-anthropic --lib -- vision` — 11/11 passed, run during this task, including `test_vision_execution_successful` and `test_with_vision_adapter`, both of which construct a `MockVisionPort` implementing this exact trait and drive it through `execute_with_vision` |
| REQ-paladin-vision-api-v2 | satisfied | `PaladinExecutionService::execute_with_vision(paladin, task, images)` at `src/application/services/paladin/paladin_execution_service.rs:517-633`; provider derived from `paladin.node.model` via `extract_provider_from_model` at `:641-652` (`gpt-*`/`o1-*` → openai, `claude-*` → anthropic, else `PaladinError::ConfigurationError`); non-streaming, single `analyze_image` call; respects `stop_words` (`:594-601`) and derives a timeout from `max_loops` (`:552-554`, no distinct `timeout_seconds` field exists on `Paladin` to respect literally). Exercised by `cargo test --offline -p paladin-ai --features cli,vision,llm-openai,llm-anthropic --lib -- vision` — 11/11 passed, run during this task: `test_vision_execution_successful`, `test_execute_with_vision_not_enabled`, `test_execute_with_vision_unsupported_provider`, `test_vision_execution_with_stop_word`, `test_vision_execution_missing_adapter`, `test_with_vision_adapter`, `test_vision_capability_check`. Divergence from the row's literal acceptance text: the method returns `PaladinResult` (`loop_count` hardcoded to 1), not a dedicated `VisionResult` — the shipped answer reuses the existing execution-result type rather than introducing a new one; `VisionError` is converted to `PaladinError` at the boundary (`:566-583`) as required |
| REQ-vision-error-model-v2 | superseded by shipped code | Same shipped `VisionError` as the Epic 13 `REQ-vision-error-model-v1` row above: lives at `crates/paladin-core/src/platform/container/vision.rs:189`, not the v2-specified `container/sentinel/vision_types.rs` — see this ledger's path caveat. This row's required variants (`InvalidImage`, `UnsupportedFormat`, `AuthenticationError`, `RateLimitExceeded`, `ProviderError`, `NetworkError`, `Timeout`, `UnsupportedProvider`, `MaxRetriesExceeded`) are all present, merged with v1's variant set (`EncryptionError`, `FileTooLarge` included, contrary to this row's "no `EncryptionError` or `FileTooLarge` variant is specified" clause) rather than the exact exclusive set either PRD specified. Exercised by the same `cargo test --offline -p paladin-ai-core vision::` run (11/11) as the v1 row. Verdict held consistent with `REQ-vision-error-model-v1`: the merged, relocated shape is authoritative, not a fresh divergence |

### Epic 14 — Autonomous Agent Features (8 IDs)

**Block verdict — `tasks-autonomous-agent-features.md`: partially outstanding.** Eleven of the
twelve parent-task clusters (`0.0`–`11.0`) verify against the current tree, several of them
`satisfied by shipped code` despite unchecked or `DEFERRED`-annotated subtasks — the corpus's
dominant checkbox-understates-reality pattern, repeated here for planning integration, handoff
execution and PaladinResult metadata. One cluster genuinely fails: **`8.0` YAML & CLI Configuration
Support** — the CLI flags (`--auto-plan`, `--auto-prompt`, `--dynamic-temp`, `--enable-handoffs`)
are declared but never read anywhere in the codebase, and the CLI's own YAML loader
(`PaladinYamlConfig`) carries no `autonomous` field at all; `config.example.yml` has no autonomous
section. Per D-06, a block verdicts `satisfied by shipped code` only if every cluster verifies;
that bar is not met here, so the block is `partially outstanding` and **`8.0`'s CLI-facing gap is
exactly Phase 6 CLOSE-02's scope for this block and nothing else** — the domain-level config
surface (`PaladinConfig.autonomous`, `AutonomousConfig` with full serde + validation) is itself
satisfied and tested; only the CLI wiring is missing.

`.planning/intel/task-completion-state.md` records **45 open** items for this task list (the
`Milestone_2-Missing_features` section total and the corpus-wide ranking both cite this figure).
Transcribed here, not re-derived: the 45 open items sit under `5.0` (Handoff Infrastructure, 6
open subtasks), `6.0` (Handoff Tool, 9 open subtasks), `7.0` (Integration, 10 open subtasks), `8.0`
(YAML & CLI Configuration, 3 open subtasks — the genuine gap), and a handful of scattered
`9.18`/`11.x` process-commit checkboxes — every one of them verified below against the tree rather
than accepted at face value.

| Parent task | Verdict | Evidence |
|---|---|---|
| 0.0 Create feature branch | satisfied | Development-process step, not a shipped-code capability. `tasks-autonomous-agent-features.md:96-99` records the branch and its own "Epic 14 Summary" section (`:417-421`) records 12 commits and a completion date of 2026-02-02, corroborated by the CHANGELOG's dedicated "Epic 14: Autonomous Agent Features" section at `CHANGELOG.md:1594` |
| 1.0 Foundation - Domain Models & Configuration | satisfied | `MaxLoops::Auto { max_subtasks: u32 }` / `MaxLoops::Fixed(u32)` at `crates/paladin-core/src/platform/container/paladin.rs:42,72`; `TaskPlan`/`Subtask` at `crates/paladin-core/src/platform/container/planning.rs`; `HandoffDecision`/`HandoffStrategy`/`HandoffContext` at `crates/paladin-core/src/platform/container/handoff.rs:11,61,143`; `PlanningError` at `src/application/errors/planning_error.rs:22`, `PromptError` at `src/application/errors/prompt_error.rs:22`, `HandoffError` at `crates/paladin-core/src/platform/container/arsenal/handoff_error.rs:27`; `AutonomousConfig`/`PlanningConfig`/`PromptConfig`/`TemperatureConfig`/`HandoffConfig` at `crates/paladin-core/src/platform/container/autonomous_config.rs:70,135,172,208,262`; exercised by `cargo test --offline -p paladin-ai-core paladin::` — 8/8 passed, `cargo test --offline -p paladin-ai-core planning::` — 10/10 passed, `cargo test --offline -p paladin-ai-core handoff::` — 16/16 passed, `cargo test --offline -p paladin-ai-core arsenal::handoff` — 14/14 passed, `cargo test --offline -p paladin-ai-core autonomous_config::` — 20/20 passed, `cargo test --offline -p paladin-ai application::errors::planning_error application::errors::prompt_error application::errors::handoff_error` — 21/21 passed, all run during this task |
| 2.0 Planning Service (US-14.1) | satisfied | `PlanningService` at `src/application/services/paladin/planning_service.rs` with `create_plan()`, `execute_subtasks()`, `synthesize_results()`; exercised by `cargo test --offline -p paladin-ai application::services::paladin::planning_service` — 11/11 passed including `test_create_plan_basic`, `test_execute_subtasks_with_dependencies`, `test_synthesize_results`, `test_create_plan_enforces_max_subtasks`, run during this task |
| 3.0 Prompt Generation Service (US-14.2) [PRE-IMPLEMENTED] | satisfied | Verified against the tree, not the annotation: `PromptGenerationService` at `src/application/services/paladin/prompt_generation_service.rs:48`, with caching (`cache: Arc<Mutex<HashMap<String,String>>>` at `:53`), `PaladinBuilder::auto_generate_prompt()`/`agent_description()` at `paladin_builder.rs:311,337`; exercised by `cargo test --offline -p paladin-ai application::services::paladin::prompt_generation_service` — 8/8 passed including `test_prompt_caching`, `test_cache_invalidation`, `test_deterministic_generation`, run during this task. The disabled-test-module question (`tests/unit/mod.rs` line 22 mock, `prompt_generation_service_test`) belongs to `REQ-prompt-generation-test-reenable` (Epic 24, plan 05-07) — not re-verified here |
| 4.0 Temperature Service (US-14.3) [PRE-IMPLEMENTED] | satisfied, with caveat | Verified against the tree, not the annotation: `TemperatureService` at `src/application/services/paladin/temperature_service.rs:51` with `calculate_optimal_temperature()`; exercised by `cargo test --offline -p paladin-ai application::services::paladin::temperature_service` — 17/17 passed, run during this task. **Caveat, not a cluster failure:** the shipped classification is 3 categories (`TaskType::{Creative, Analytical, Standard}`, fixed temps 0.85/0.2/0.6) with LLM-based detection as the *default* (`enable_llm_detection: true`), not the ingested PRD's 4-band heuristic-only spec — see `REQ-dynamic-temperature` below for the requirement-level divergence |
| 5.0 Handoff Infrastructure (US-14.4) | satisfied by shipped code | Checkbox unchecked in the source (`:154`) because subtasks `5.17/5.18` (handoff execution), `5.23/5.24` (PaladinResult handoff history) and `5.27/5.28` (integration test) are marked `DEFERRED` — but every one of these shipped: `HandoffService::execute_handoff()` with retry/backoff at `src/application/services/paladin/handoff_service.rs:466`; `PaladinResult.handoff_history: Vec<HandoffRecord>` at `crates/paladin-core/src/platform/container/execution_result.rs:68`; exercised by `cargo test --offline -p paladin-ai --test lib -- unit::handoff_service_test` — 27/27 passed, including `test_execute_handoff_delegates_to_specialist`, `test_execute_handoff_retries_transient_errors`, `test_execute_handoff_detects_circular_handoff`, `test_execute_handoff_enforces_max_depth`, run during this task. This is the dominant corpus pattern again: the record understates the tree |
| 6.0 Handoff Tool (US-14.5) | satisfied by shipped code, with caveat | `HandoffTool::new()`/`get_schema()`/`validate_parameters()` at `crates/paladin-core/src/platform/container/arsenal/handoff_tool.rs:40,52,112`, emitting `handoff_to_agent` with `agent_name`/`message`; exercised by `cargo test --offline -p paladin-ai --test lib -- unit::arsenal::handoff_tool_test` — 7/7 passed, run during this task. **Real caveat, not a cluster failure:** subtasks `6.9/6.10` (`HandoffTool::execute()`) are correctly marked `DEFERRED` — no `execute()` method exists on this struct — but the end-to-end capability (LLM calls a handoff tool → validated → executed via `HandoffService` → result returned, chain tracked) still ships and is tested: `PaladinBuilder::generate_handoff_tool()` at `paladin_builder.rs:1184` independently emits an inline tool named `handoff_to_specialist` (`specialist_name`/`task_description` — the Epic 21/23 v2 shape, see `REQ-handoff-tool-v1` below), and `PaladinExecutionService::is_handoff_tool_call()`/`execute_handoff()` at `paladin_execution_service.rs:1414,1434` dispatch on that same v2 name, exercised by the same 27/27 `handoff_service_test` run above (`test_execute_handoff_delegates_to_specialist`) plus `test_graceful_degradation_no_prompt_service`/`_no_planning_service` — 2/2 passed. The Epic-14-authored `HandoffTool` struct itself is never invoked by the wired auto-registration or dispatch path; see `REQ-handoff-tool-v1`'s row |
| 7.0 Integration with PaladinExecutionService [LARGELY PRE-IMPLEMENTED] | satisfied by shipped code | Checked in the source (`:224`) despite subtasks `7.11-7.20` (`PaladinResult` planning/handoff metadata, orchestration logic, integration tests) marked `DEFERRED` — all shipped: `PaladinResult.plan: Option<TaskPlan>` at `execution_result.rs:60`, `.handoff_history` at `:68`; orchestration Layers 1-3 at `paladin_execution_service.rs:1231` (`apply_layer1_planning`, graceful degradation on `PlanningError`), `:1300` (`apply_layer1_prompt_generation`, graceful degradation on `PromptError`), `:1379` (`apply_layer2_dynamic_temperature`), `:1414,1434` (Layer 3 handoff detection/execution); exercised by `cargo test --offline -p paladin-ai --test lib -- unit::paladin_execution_service_test::test_graceful_degradation` — 2/2 passed (`test_graceful_degradation_no_planning_service`, `test_graceful_degradation_no_prompt_service`), run during this task |
| 8.0 YAML & CLI Configuration Support | genuinely outstanding | Checkbox unchecked in the source (`:245`), and the tree confirms a real gap, not just a stale checkbox: `src/application/cli/commands/agent.rs:79,83,87,91` declares `--auto-plan`/`--auto-prompt`/`--dynamic-temp`/`--enable-handoffs` on `AgentRunArgs`, but `grep -rn '\.auto_plan\b\|\.auto_prompt\b\|\.dynamic_temp\b\|\.enable_handoffs\b' src/` returns **zero** matches outside the struct's own field declarations and `Default` impls — the flags are read by no code, run during this task. `src/application/cli/config/paladin_config.rs`'s `PaladinYamlConfig` (`:40`) has no `autonomous` field, and `config.example.yml` has zero `autonomous` occurrences (`grep -c autonomous config.example.yml` → 0). This matches the source file's own `8.11`/`8.17`/`8.18` subtask annotations ("NEEDS: Integration test with agent run" / "NEEDS: Config merging implementation" / "NEEDS: Handler code to merge CLI args with YAML config") — the task list correctly records this as unfinished. Domain-level config is separately satisfied: see `REQ-autonomous-configuration` below |
| 9.0 Documentation (AUTONOMOUS.md) | satisfied by shipped code, with caveat | Checked in the source (`:270`) despite `docs/AUTONOMOUS.md` not existing at that path (`find . -iname AUTONOMOUS.md` → none, run during this task) — Milestone 11's docs overhaul relocated this content into the mdbook: `docs/src/user-guides/paladin-agents.md:191-260` carries a full "Autonomous Features" section covering planning, auto-prompts, dynamic temperature and handoffs with code examples, corroborated by `grep -c -i autonomous docs/src/user-guides/paladin-agents.md` → 15. **Caveat:** the handoff code example at `paladin-agents.md:249` calls `.with_specialist(Arc::new(...))`, a method that does not exist (`grep -n 'pub fn with_specialist' paladin_builder.rs` → none; the shipped method is `with_handoffs(Vec<Arc<Paladin>>)` at `paladin_builder.rs:859`) — a real doc-accuracy defect, but the "documentation and examples exist" capability claim is still met via this different artefact, matching the Epic 22 precedent (05-05) for docs relocated into mdbook |
| 10.0 Examples Implementation | satisfied | All 5 named examples present: `examples/{autonomous_planning,autonomous_prompt_generation,dynamic_temperature,agent_handoffs,autonomous_full_config}.rs`; exercised by `cargo build --offline --examples -p paladin-ai` — exit 0, all examples compile clean, run during this task |
| 11.0 Testing & Quality Assurance | satisfied | `CHANGELOG.md:1594` carries a full "Epic 14: Autonomous Agent Features" section; zero `#[ignore]` attributes across the Epic 14 test surface checked during this task (`tests/unit/handoff_service_test.rs`, `tests/unit/arsenal/handoff_tool_test.rs`, and the four `application/services/paladin/*.rs` service modules); every scoped `cargo test`/`cargo build` invocation run during Tasks 1-2 of this plan passed clean (0 failures across ~140 Epic-14-scoped test executions) |

| ID | Verdict | Evidence |
|---|---|---|
| REQ-max-loops-auto | present, unproven | `MaxLoops::Auto { max_subtasks: u32 }` exists and is tested in isolation at `crates/paladin-core/src/platform/container/paladin.rs:42,72` (`cargo test --offline -p paladin-ai-core paladin::` — 8/8 passed, including `test_max_loops_auto`, run during this task) — but the requirement's specific behavioral claims are unproven against the wired system: (1) `PaladinBuilder::max_loops(u32)` at `src/application/services/paladin/paladin_builder.rs:285` only ever constructs `MaxLoops::Fixed(max_loops)` — there is no builder path that produces `MaxLoops::Auto` at all; (2) `MaxLoops::Auto` is never read by `PaladinExecutionService` (`grep -n 'is_auto()\|max_loops.is_auto' src/` returns zero call sites, run during this task) — planning is instead gated by a wholly separate `autonomous_planning: bool` field (`paladin_execution_service.rs:1238`), decoupled from `max_loops`; (3) the planning `max_subtasks` cap is hardcoded to `10` in `apply_layer1_planning`'s call to `create_plan(input, 10, model)` (`:1268`), not read from any `MaxLoops::Auto{max_subtasks}` value. The example meant to demonstrate this (`examples/autonomous_planning.rs:69,163`) says so itself: "`Note: In fully integrated version, would use MaxLoops::Auto`" / "`Full MaxLoops::Auto integration coming soon`" |
| REQ-planning-service | satisfied | `PlanningService` at `src/application/services/paladin/planning_service.rs:56` with `create_plan()` (`:112`), `execute_subtasks()` and `synthesize_results()`; task decomposition, dependency ordering and logging (`tracing`/`log` calls throughout) all present; exercised by `cargo test --offline -p paladin-ai application::services::paladin::planning_service` — 11/11 passed including `test_create_plan_basic`, `test_execute_subtasks_with_dependencies`, `test_synthesize_results`, `test_create_plan_enforces_max_subtasks`, `test_planning_failure_invalid_json`, run during this task |
| REQ-prompt-generation-service | satisfied | `PromptGenerationService` at `src/application/services/paladin/prompt_generation_service.rs:48` with `generate_prompt()` (`:100`, includes role/capability generation from `agent_description`), caching (`cache` field `:53`, `invalidate_cache()`/`clear_cache()` `:186,194`), deterministic generation (`temperature: Some(0.0)` `:140`); `PaladinBuilder::auto_generate_prompt()`/`agent_description()`/`regenerate_prompt()` at `paladin_builder.rs:311,337,362`, and a manual `.system_prompt()` call overrides auto-generation (`manual_temperature_override`-style guard, verified by the builder's own override precedence logic); exercised by `cargo test --offline -p paladin-ai application::services::paladin::prompt_generation_service` — 8/8 passed including `test_prompt_caching`, `test_cache_invalidation`, `test_deterministic_generation`, `test_generate_prompt_empty_description`, run during this task. **Not re-verified here:** the disabled `prompt_generation_service_test` module and its `tests/unit/mod.rs:22` mock are `REQ-prompt-generation-test-reenable`'s scope (Epic 24, plan 05-07) — cross-reference rather than duplicate, per this plan's explicit instruction |
| REQ-dynamic-temperature | superseded by shipped code | `TemperatureService` at `src/application/services/paladin/temperature_service.rs:51` with `calculate_optimal_temperature()` (`:78`); `PaladinBuilder::auto_temperature(bool)` at `paladin_builder.rs:397` (not the ingested `dynamic_temperature(bool)` name), no `temperature_bounds(f32, f32)` builder method exists (`grep -n 'pub fn temperature_bounds' paladin_builder.rs` → none, run during this task); exercised by `cargo test --offline -p paladin-ai application::services::paladin::temperature_service` — 17/17 passed, run during this task. The shipped answer diverges from the ingested spec on three points and is recorded as authoritative: (1) 3 task-type categories (`TaskType::{Creative, Analytical, Standard}` at `temperature_service.rs:14`) with fixed temps 0.85/0.2/0.6, not the PRD's 4 bands (Factual 0.1-0.3, Analytical 0.3-0.5, Conversational 0.5-0.7, Creative 0.7-1.0); (2) classification defaults to an LLM call (`enable_llm_detection: true` at `:45`, `detect_task_type_with_llm` at `:120`), not the PRD's heuristics-only, ≤50ms requirement — a heuristic fallback exists (`detect_task_type_heuristic` at `:179`) but is not the default path; (3) selected temperature is not bounded by any configurable min/max — the category-based fixed values are used as-is. A separate, unrelated mechanism also named "dynamic temperature" ships alongside this: `PaladinBuilder::enable_dynamic_temperature(bool)` at `:607` linearly increases temperature toward 1.0 across `max_loops` iterations (Layer 2, `apply_layer2_dynamic_temperature` at `paladin_execution_service.rs:1379`) — a per-loop escalation, not task-type classification, and not what the PRD or this REQ ID describes |
| REQ-handoff-infrastructure | satisfied | `PaladinBuilder::with_handoffs(Vec<Arc<Paladin>>)` at `paladin_builder.rs:859`, `handoff_config(Arc<HandoffConfig>)` at `:894`; `HandoffStrategy::{Automatic, Explicit, Threshold{confidence}}` at `crates/paladin-core/src/platform/container/handoff.rs:11`; `HandoffService::should_handoff()` (task complexity/confidence-based) at `src/application/services/paladin/handoff_service.rs:115`; circular-delegation and max-depth enforcement in `validate_handoff()`/`can_handoff_to()` at `:319,354`; `HandoffContext` (task, history, metadata) at `handoff.rs:61`; `PaladinResult.handoff_history: Vec<HandoffRecord>` at `crates/paladin-core/src/platform/container/execution_result.rs:68`; all handoff decisions logged (`info!`/`debug!`/`warn!` throughout `handoff_service.rs`); exercised by `cargo test --offline -p paladin-ai --test lib -- unit::handoff_service_test` — 27/27 passed including `test_should_handoff_automatic_low_confidence`, `test_validate_handoff_circular_delegation`, `test_validate_handoff_max_depth`, `test_execute_handoff_tracks_chain_depth`, run during this task |
| REQ-handoff-tool-v1 | present, unproven | `HandoffTool` at `crates/paladin-core/src/platform/container/arsenal/handoff_tool.rs:25`, emitting a `handoff_to_agent` tool schema with required `agent_name` (enum) and `message` at `get_schema()` (`:52,63,68,73,78`); `validate_parameters()` at `:112` checks `agent_name` against specialists. Its own unit tests exercise these methods in isolation and pass: `cargo test --offline -p paladin-ai --test lib -- unit::arsenal::handoff_tool_test` — 7/7 passed, run during this task. **Unproven against the wired system, per this plan's variant-group instruction:** nothing in the actual auto-registration or execution-dispatch path calls this struct. `PaladinBuilder::generate_handoff_tool()` (`paladin_builder.rs:1184`) independently builds its own inline tool definition named `handoff_to_specialist` with `specialist_name`/`task_description` — the competing `REQ-handoff-tool-v2` shape (Epic 21, plan 05-12) — and `PaladinExecutionService::is_handoff_tool_call()` (`paladin_execution_service.rs:1414`) dispatches on that same `handoff_to_specialist` name, never `handoff_to_agent`. The requirement's "tool MUST validate agent_name...execute via HandoffService...return the specialist result...track the chain across invocations" claims are proven for the wired v2 shape (see the `6.0` cluster row above and `test_execute_handoff_delegates_to_specialist`), not for this v1 artefact. Recorded, not pre-empting 05-12's `REQ-handoff-tool-v2` row |
| REQ-autonomous-configuration | present, unproven | Domain-level configuration is satisfied and tested: `PaladinConfig.autonomous: Option<AutonomousConfig>` at `crates/paladin-core/src/platform/container/paladin_config.rs:61`, `PaladinConfigBuilder::autonomous()` at `:141` with build-time validation (`:158`); `AutonomousConfig`/`PlanningConfig`/`PromptConfig`/`TemperatureConfig`/`HandoffConfig` all `Serialize`/`Deserialize` with `enabled: false` opt-in defaults (`autonomous_config.rs:70-284`); exercised by `cargo test --offline -p paladin-ai-core paladin_config::tests::test_paladin_config_with_autonomous paladin_config::tests::test_paladin_config_autonomous_validation` — 2/2 passed, run during this task. **Unproven at the CLI surface** — see the `8.0` cluster row above: the CLI's `PaladinYamlConfig` (`src/application/cli/config/paladin_config.rs:40`) has no `autonomous` field, `config.example.yml` has none either, and the four `--auto-*`/`--dynamic-temp`/`--enable-handoffs` CLI flags on `AgentRunArgs` (`agent.rs:79-92`) are read by no code in the tree. "All autonomous features MUST be configurable via `PaladinConfig`" and "opt-in by default" are proven; "YAML MUST support every autonomous feature flag" and "CLI MUST support flags" are not — the citations exist but nothing exercises the CLI-facing half |
| REQ-autonomous-error-handling | satisfied | All three error enums exist: `PlanningError` at `src/application/errors/planning_error.rs:22` (11 variants: `InvalidPlan`, `MaxSubtasksExceeded`, `ExecutionFailed`, `GenerationFailed`, `CircularDependency`, `InvalidDependency`, `SynthesisFailed`, `LlmError`, `EmptyPlan`, `Timeout`, `ConfigurationError`); `PromptError` at `src/application/errors/prompt_error.rs:22` (10 variants: `GenerationFailed`, `InvalidDescription`, `CacheMiss`, `InvalidPrompt`, `LlmError`, `RegenerationFailed`, `ConfigurationError`, `CacheError`, `Timeout`, `PromptTooLong`); `HandoffError` at `crates/paladin-core/src/platform/container/arsenal/handoff_error.rs:27` (re-exported at `src/application/errors/handoff_error.rs:6`, a single canonical type, not a divergence). Graceful degradation — the specific criterion the run-2 ledger left uninspected — is implemented, not just claimed: `apply_layer1_planning()` at `paladin_execution_service.rs:1231` catches `PlanningError` and falls back to non-planning execution (`Err(e) => { warn!(...); None }` at `:1279`), `apply_layer1_prompt_generation()` at `:1300` catches `PromptError` and falls back to the existing system prompt (`:1349`); exercised by `cargo test --offline -p paladin-ai --test lib -- unit::paladin_execution_service_test::test_graceful_degradation` — 2/2 passed (`test_graceful_degradation_no_planning_service`, `test_graceful_degradation_no_prompt_service`), and `cargo test --offline -p paladin-ai application::errors::planning_error application::errors::prompt_error application::errors::handoff_error` — 21/21 passed, both run during this task. All autonomous decisions are logged at INFO level throughout (`info!`/`warn!`/`debug!` calls in every service touched by this plan) |

### Epic 15 — Conclave / Mixture-of-Agents (5 IDs)

Epic-level note: (1) `.planning/intel/code-verification.md:18` records Conclave as verified shipped against `tasks-conclave-mixture-of-agents.md`'s **129** open checkboxes — the largest open-checkbox concentration in the corpus — so the count is stale and is not carried as forward work. (2) `RELEASE_NOTES_MILESTONE_3.md` attributes Conclave to a Milestone 3 epic number; the authoritative attribution is Milestone 2 Epic 15, per `.planning/decisions/0010-milestone-3-epic-numbering.md` (ADR-0010) — cited here rather than the defect restated.

| ID | Verdict | Evidence |
|---|---|---|
| REQ-conclave-domain-model | satisfied | `Conclave` struct at `crates/paladin-core/src/platform/container/battalion/conclave.rs:210` (`experts: Vec<Paladin>` `:212`, `aggregator: Paladin` `:215`, `config: ConclaveConfig` `:218`); `ConclaveConfig` at `:86` (`name`, `battalion_config`, `timeout_seconds` default 300 `:137`, `retry_attempts` default 2 `:138` clamped 0-5 `:156`, `synthesis_prompt: Option<String>` `:105`, `include_expert_names: bool` `:108`, `max_expert_output_tokens: Option<usize>` `:111`, `observability_level` `:114`); `Conclave::validate` at `:275-327` rejects fewer than 2 experts (`:277-282`) and duplicate names including the aggregator (`:284-303`); `ConclaveResult` at `:384` (`expert_outputs: HashMap<String, PaladinResult>`, `aggregated_output`, `execution_time_ms`, `expert_execution_times`, `retry_counts`, `status`); `ConclaveError` at `:428` — `AllExpertsFailed`, `AggregatorFailed(String)`, `ConfigurationError(String)`, `Timeout(u64)`, `ExpertError(String, String)` — an exact match to the acceptance text's five variants including `ExpertError(name, detail)`'s two-field shape. **Status-enum spelling, read directly from the tree during this task**: `ConclaveStatus` at `:350` has exactly three variants — `Success`, `PartialSuccess`, `Failed` (`:353,356,359`) — matching the PRD's spelling (`prd-conclave-mixture-of-agents.md` US-15.1: "Success (all experts succeeded), PartialSuccess, Failed") exactly. It does **not** match `epic-15-completion-report.md:36`'s own record, which claims `ConclaveStatus enum (Completed, PartialSuccess, Failed)` — "Completed" where the PRD and the shipped tree both say "Success". Since the shipped spelling does not diverge from the PRD's, this row verdicts `satisfied`, not `superseded by shipped code` — see the nested finding below for the completion report's own transcription error. Exercised by `cargo test --offline -p paladin-ai-core conclave::` — 10/10 passed, run during this task (`test_conclave_creation_success`, `test_conclave_insufficient_experts`, `test_conclave_duplicate_expert_names`, `test_conclave_aggregator_name_conflicts`, `test_conclave_config_builder`, `test_conclave_config_retry_attempts_clamping`, `test_conclave_timeout_validation`, `test_observability_level_default`, `test_conclave_status_equality`, `test_conclave_error_conversion`). |
| | | **New finding (plan 05-09):** `epic-15-completion-report.md:36`'s checklist item — "`[x] ConclaveStatus enum (Completed, PartialSuccess, Failed)`" — misstates the shipped/PRD spelling of the first variant as "Completed" instead of "Success". Read directly from `conclave.rs:350-360` during this task: the enum is `Success`/`PartialSuccess`/`Failed`, matching the PRD; the completion report's own transcription is what is wrong, not the code. |
| REQ-conclave-execution-service | satisfied | `ConclaveExecutionService` at `crates/paladin-battalion/src/conclave_execution_service.rs:38`; `execute` at `:90` wraps `execute_internal` in a `timeout()` over the entire Conclave execution (experts + aggregation), not per-agent, at `:96,105`; `execute_experts_parallel` at `:194` spawns one `tokio::spawn` task per expert (`:223-233`) and awaits all of them, so experts run genuinely in parallel; `execute_expert_with_retry` at `:269` implements exponential backoff `2^attempt` capped at 16s (`calculate_retry_delay` `:372-382`; 1s, 2s, 4s, 8s, 16s per the PRD) with ±20% jitter (`:377-379`), retrying only errors `is_retryable_error` (`:343-367`) classifies as transient (timeout, or rate-limit/network/503/429 substrings in `LlmError`); on partial expert failure, `execute_internal` continues with whatever succeeded and only returns `ConclaveError::AllExpertsFailed` when `expert_outputs` is empty (`:146-148`); `format_expert_outputs_for_aggregator` at `:388` builds the aggregator prompt, optionally labelling by expert name (`:409-411`), with a default template when `synthesis_prompt` is unset (`:424-431`); `ConclaveResult.status` set to `Success` only when `successful_experts == total_experts`, else `PartialSuccess`, at `:165-169`. Exercised by `cargo test --offline -p paladin-battalion conclave` — 8/8 passed, run during this task (`test_successful_conclave_execution`, `test_partial_success_with_one_expert_failure`, `test_retry_logic_with_transient_failures`, `test_all_experts_fail`, `test_retry_delay_calculation`, `test_is_retryable_error`, `test_format_expert_outputs`, `test_truncate_output`). The PRD's "total time <= max expert time + aggregation time + 10% overhead" target is not measured or asserted by any test — recorded as unproven, not blocking this row's `satisfied` verdict since it is a non-functional aside rather than one of the row's tested behaviours. |
| REQ-conclave-commander-strategy | present, unproven | `BattalionStrategy::Conclave` handled at `crates/paladin-battalion/src/commander.rs:491-546` (requires an aggregator `:495-499` and at least 2 experts `:503-508`, delegates to `ConclaveExecutionService`, converts `ConclaveResult` into `BattalionResult`); `CommanderBuilder::aggregator(paladin)` at `:1369-1372`; default-last-Paladin-as-aggregator plus the 2-Paladin minimum enforced in `build()` at `:1484-1499` ("No aggregator specified for Conclave, using last Paladin as aggregator"); Auto-strategy keyword scoring for Conclave at `:970-993` (13 keywords, gated on `self.paladins.len() >= 3`, checked first as most specific among the seven strategies). Two divergences from the acceptance text: `CommanderBuilder` exposes only `.aggregator(paladin)` — `grep -n "aggregator_index\|aggregator_name" commander.rs` returns nothing, so "aggregator selection MUST also be possible via `aggregator_index` or `aggregator_name`" is not implemented; and the Auto strategy's Conclave path is a flat keyword-match + Paladin-count gate, not the PRD's point-weighted scoring ("+3 keywords / +2 three-or-more distinct expertises / +1 comprehensive question") — no scoring variable or weight constant exists anywhere in the file. More significantly: **nothing in the tree exercises the Commander-level Conclave wiring at all.** All 4 occurrences of `BattalionStrategy::Conclave` in `commander.rs` are in non-test code; `commander.rs`'s own `mod tests` (`:1558-2704`) has a `test_auto_selects_{formation,phalanx,campaign,chain,council,grove}_for_..._keywords` test for every other strategy but **no** `test_auto_selects_conclave_...` test, and no test anywhere calls `Commander::build()`/`Commander::execute()` with `BattalionStrategy::Conclave` (`grep -rn Conclave tests/integration/commander_integration_tests.rs` returns nothing). The only tested Conclave path in the corpus is `ConclaveExecutionService` called directly, bypassing Commander (see the previous row). A `file:line` citation with nothing exercising it is `present, unproven`, not `satisfied`, per this ledger's evidence bar — even though the domain model and execution service it delegates to are both well-tested. |
| REQ-conclave-cli-and-yaml | satisfied | `paladin battalion run --type conclave --config <file>` dispatches to `execute_conclave` via `BattalionYamlConfig::Conclave` matched at `src/application/cli/commands/battalion.rs:147,189-190`, `execute_conclave` at `:552-751`; `paladin battalion new --type conclave --name <name>` generates a template via `generate_conclave_template` (`src/application/cli/templates/battalion_template.rs:24,297-410`) with 3 example experts of distinct roles (TechnicalExpert, BusinessExpert, SecurityExpert, `:311-350`) plus 1 aggregator (SynthesisAggregator, `:352-374`); `ConclaveConfig` YAML struct at `src/application/cli/config/battalion_config.rs:271-301` supports inline and reference-based `PaladinReference` agent definitions plus `retry_attempts`/`timeout_seconds`/`synthesis_prompt`/`include_expert_names`/`observability_level`, matching the acceptance list; `ConclaveConfig::validate` at `:305-323` returns `CliError::MissingRequiredField`/`ValidationError` with descriptive messages (e.g. "Conclave requires at least 2 expert Paladins"). CLI output: JSON to file when `-o`/`--output` is given (`battalion.rs:702-712`), otherwise a formatted plain-text summary to stdout (`:715-750`, expert-by-expert in `--verbose` mode) — the PRD's third documented option, a distinct Markdown formatter, does not exist; 2 of the 3 named formats (JSON, plain text) are shipped. Exercised by `cargo test --offline -p paladin-ai --features cli --lib battalion_template` — 7/7 passed, run during this task, including `test_generate_conclave_template` (asserts `type: conclave` and the configured name) and `test_all_templates_are_valid_yaml` (asserts the generated document actually parses). |
| REQ-conclave-observability | present, unproven | `ObservabilityLevel` enum at `crates/paladin-core/src/platform/container/battalion/conclave.rs:53` — `Minimal`, `Standard` (`#[default]`), `Verbose` — wired through `ConclaveConfig.observability_level` and read by `ConclaveExecutionService::log_execution_summary` (`crates/paladin-battalion/src/conclave_execution_service.rs:468-509`) and `execute_expert_with_retry`'s per-attempt logging (`:287,323-331`). The run-2 ledger's "three levels implemented with a default, uninspected" claim is only partly true once the actual log content per level is inspected: **Minimal** matches the PRD ("only the final aggregated result and overall status") — logs nothing unless `status != Success` (`:477-486`). **Standard** (PRD: "per-expert execution time, total time, retry counts and success/failure status") logs only the aggregate `successful/total` count, `execution_time_ms`, and the overall status string (`:487-500`) — no per-expert breakdown and no retry counts appear in the log line, even though `ConclaveResult.expert_execution_times`/`retry_counts` are populated regardless of observability level (see the domain-model row) and could supply them. **Verbose** (PRD: additionally "full expert outputs, token usage, LLM provider details, and timestamps") logs only status, expert success count, total time, and the configured retry-attempt limit (`:501-507`) — no expert outputs, token usage, provider details, or timestamps appear anywhere in the log. The three-level *gate* is real and does branch correctly (confirmed structurally; `test_observability_level_default` — the only dedicated observability test — asserts only that the default is `Standard`, `conclave.rs:619-621`), but the *content* each level is specified to carry is a materially reduced subset of what ships, and no test asserts any level's actual log content. Verdict `present, unproven`: the enum and its gating are cited and exist, but nothing in the tree proves the per-level content requirements the acceptance text specifies. |

### Epic 16 — Advanced Battalion Patterns: Council & Grove (11 IDs)

**Epic-level note.** `.planning/intel/code-verification.md` records both Council and Grove as
verified shipped. `RELEASE_NOTES_MILESTONE_3.md` attributes Council and Grove to Milestone 3 epic
numbers ("Epic 20: Council Pattern", "Epic 21: Grove Pattern"); both are in fact Milestone **2**
Epic 16 — see `.planning/decisions/0010-milestone-3-epic-numbering.md`'s mapping table, which
records the corrected attribution once and permanently. Rows below cite that ADR for the
release-notes divergences rather than restating them.

| ID | Verdict | Evidence |
|---|---|---|
| REQ-council-domain-model | satisfied | `Council`/`CouncilData` (name, `participant_ids`, `moderator_id`, `config`) at `crates/paladin-core/src/platform/container/battalion/council.rs:223-236`; `CouncilConfig` (max_rounds, turn_strategy, termination_condition, include_history) at `:186-198`; `CouncilMessage` (speaker, content, round, timestamp) at `:122-135`; Council is a `Node<CouncilData>` aggregate per the module's own doc comment (`:246`). Exercised by `cargo test --offline -p paladin-ai-core council::` — 13/13 passed, run during this task (`test_council_builder_basic`, `test_council_message_creation`, `test_council_config_default`, `test_council_builder_validation_too_few_participants`, `test_council_builder_validation_moderator_required`) |
| REQ-council-turn-strategies | superseded by shipped code | `TurnStrategy` at `council.rs:49-70` ships **all four** variants — `RoundRobin`, `ModeratorDirected`, `Random`, `VoluntaryWithTimeout { timeout_ms }` — not the two the PRD (FR-1.2, NG-6) says ship in this epic, with `Random`/`VoluntaryWithTimeout` explicitly "NOT implemented in this epic" beyond being "prepared for." The shipped code goes further: `determine_next_speaker` at `crates/paladin-battalion/src/council_service.rs:303-355` handles all four arms — `Random` is **fully implemented** (`rand::seq::SliceRandom::choose`, `:338-345`), and `VoluntaryWithTimeout` is a working stub that falls back to RoundRobin behavior with a `warn!("VoluntaryWithTimeout not fully implemented, using RoundRobin")` log line (`:346-353`), not a panic or `unimplemented!()`. **New finding (plan 05-10):** neither `Random` nor `VoluntaryWithTimeout` selection has a dedicated unit or integration test anywhere in the tree (`test_turn_strategy_default`/`test_turn_strategy_serialization` in `council.rs` only cover the enum's `Default` impl and (de)serialization, not `determine_next_speaker`'s branch logic) — so while the shipped surface exceeds NG-6's deferral, that excess is itself unverified by test. RoundRobin and ModeratorDirected (the two MUST variants) are both exercised by `cargo test --offline -p paladin-ai --test lib -- integration::battalion::council_integration_test` — 4/4 passed, run during this task (`test_council_roundrobin_three_paladins_two_rounds`, `test_council_moderator_directed_strategy`) |
| REQ-council-termination-conditions | satisfied | `TerminationCondition` at `council.rs:89-103` ships all four PRD variants as unit/tuple forms matching the PRD exactly: `MaxRounds` (unit), `ModeratorDecision` (unit), `Consensus` (unit), `Keyword(String)` (tuple). `should_terminate` at `council_service.rs:437-450` has one match arm per variant, each backed by a detector (`detect_consensus`, `detect_moderator_conclusion`, `detect_keyword`). Shipped code matches the PRD; the divergence is in `RELEASE_NOTES_MILESTONE_3.md:76,294`, which shows `TerminationCondition::MaxRounds(3)` — a **tuple-variant** form with an embedded round count. The shipped `MaxRounds` is a **unit** variant; the round count lives separately on `CouncilConfig.max_rounds`. Citing `.planning/decisions/0010-milestone-3-epic-numbering.md` for the release-notes Council API-form divergence class rather than re-litigating in prose here. Exercised by `cargo test --offline -p paladin-battalion council_service::` — 10/10 passed (`test_detect_consensus`, `test_detect_consensus_negative`, `test_detect_moderator_conclusion`, `test_detect_keyword`) plus `cargo test --offline -p paladin-ai --test lib -- integration::battalion::council_integration_test::test_council_consensus_termination` — 1/1 passed, both run during this task |
| REQ-council-execution-service | satisfied | `CouncilExecutionService::convene(&self, council: &Council, topic: &str) -> Result<CouncilResult, BattalionError>` at `crates/paladin-battalion/src/council_service.rs:118-122` (not the release notes' `execute(&council, &experts, topic)` — see ADR-0010); tracks `transcript: Vec<CouncilMessage>` (`:164`), implements turn-taking via `determine_next_speaker` and evaluates termination via `should_terminate` before each turn (`:171-191`); `CouncilResult` at `:24-37` carries `transcript`, `conclusion: Option<String>` (not the release notes' `summary` — ADR-0010), `rounds_completed`, `termination_reason`. Empty participants and missing-moderator errors handled at build time (`CouncilBuilder` validation) and at speaker-resolution time (`BattalionError::PaladinNotFound`); participant failures and per-speaker timeouts (`Duration::from_secs(300)`, `:214`) are skipped without aborting the discussion (`:216-236`, `continue` on both `Err` and timeout). Exercised by `cargo test --offline -p paladin-battalion council_service::` — 10/10 passed and `cargo test --offline -p paladin-ai --test lib -- integration::battalion::council_integration_test` — 4/4 passed (`test_council_error_handling`, `test_council_timeout_enforcement` included), both run during this task |
| REQ-council-garrison-integration | present, unproven | `store_in_garrison` at `crates/paladin-battalion/src/council_service.rs:416-432` writes each `CouncilMessage` to `GarrisonPort::remember` as a `GarrisonEntry`, called non-fatally after every turn (`:243-250`) — a real `file:line` citation for the MUST garrison clause. **New finding (plan 05-10):** nothing in the tree exercises this path — every `CouncilExecutionService::new(...)` call across the test suite and `commander.rs:589-592` passes `None` for `garrison_port`, and `commander.rs:588` carries the comment `// Execute Council (pass None for garrison_port - Commander doesn't have one)`. The SHOULD-level Citadel checkpoint-per-round persistence (FR-1.5) has **no shipped code anywhere** — `grep -in citadel crates/paladin-battalion/src/council_service.rs` returns 0 matches. The MUST-level concurrency clause ("multiple Councils MUST be able to run concurrently") is exercised, but only via a Council running concurrently with a Grove, not two Councils: `cargo test --offline -p paladin-ai --test lib -- integration::commander_integration_tests::test_concurrent_council_and_grove_execution` — 1/1 passed, run during this task |
| REQ-grove-domain-model | satisfied | `Grove`/`GroveData` (name, `trees: Vec<Tree>`, `config: GroveConfig`) at `crates/paladin-core/src/platform/container/battalion/grove.rs:258-` (struct definition immediately follows `GroveConfig`); `Tree` (name, `agents: Vec<TreeAgent>`) at `:158-165`; `TreeAgent` (`paladin_id`, `expertise_keywords`, `expertise_embedding: Option<Vec<f32>>`) at `:79-96`; Grove/Tree/routing ubiquitous language used consistently in doc comments. Exercised by `cargo test --offline -p paladin-ai-core grove::` — 15/15 passed, run during this task (`test_tree_creation`, `test_tree_agent_creation`, `test_grove_builder_basic`, `test_grove_serialization`) |
| REQ-grove-routing-strategies | satisfied | `RoutingStrategy` at `grove.rs:52-73` ships exactly the three PRD-required variants — `KeywordMatch` (`#[default]`), `SemanticSimilarity`, `LlmRouting` — implemented at `crates/paladin-battalion/src/grove_service.rs:296` (`route_by_keywords`), `:381` (`route_by_semantic_similarity`), `:479` (`route_by_llm`); highest-score/confidence agent selected in each, `fallback_tree` used on primary-strategy failure (`route_task`, `:240-289`). `RoutingStrategy::PerformanceBased` — the release-notes claim contradicting Epic 16 non-goal NG-3 — is verified absent from the tree (`grep -rn "PerformanceBased" crates/ src/` → 0 matches), citing `.planning/decisions/0010-milestone-3-epic-numbering.md` rather than restating the defect. The `LlmRouting` path carries the hardcoded-model defect recorded on the Epic 22 `REQ-grove-llm-routing` row (`grove_service.rs:537`, `model: "gpt-4".to_string()`), owned by **Phase 6 / CLOSE-01** — cross-referenced here, not duplicated. Exercised by `cargo test --offline -p paladin-battalion grove_service::` — 15/15 passed and `cargo test --offline -p paladin-ai-core grove::` (`test_routing_strategy_default`) — part of the 15/15 run above, both run during this task |
| REQ-grove-config-v1 | satisfied | `GroveConfig` at `crates/paladin-core/src/platform/container/battalion/grove.rs:208-241` is the **same struct** Epic 22's `REQ-grove-config-v2` row cites — it carries v1's fields (`routing_strategy`, `fallback_tree: Option<String>`, `similarity_threshold: f32`) at `:210,217,223` **and** v2's fields (`routing_fallback: String`, `min_confidence: f32`) at `:233,240` side by side in one struct; not two competing implementations. Defaults match the PRD exactly — `RoutingStrategy::default()` (`KeywordMatch`) and `similarity_threshold: 0.7` (`Default` impl, `:243-252`). Validated on Grove creation: `GroveBuilder::build()` (`:484-558`) rejects an out-of-range `similarity_threshold` (`GroveError::InvalidSimilarityThreshold`, `:513-514`) and a `fallback_tree` naming a tree that doesn't exist (`:534-`). This row answers v1's ingested position (routing_strategy/fallback_tree/similarity_threshold); the Epic 22 row answers v2's (routing_fallback/min_confidence) — same code, non-contradictory verdicts, per variant group 14. Exercised by `cargo test --offline -p paladin-ai-core grove::` — 15/15 passed (`test_grove_config_default`, `test_grove_builder_validation_invalid_threshold`, `test_grove_builder_validation_invalid_fallback`, `test_grove_builder_validation_no_trees`, `test_grove_builder_validation_tree_without_agents`), run during this task |
| REQ-grove-execution-service | satisfied | `GroveExecutionService::execute(&self, grove: &Grove, task: &str) -> Result<GroveResult, BattalionError>` at `crates/paladin-battalion/src/grove_service.rs:171-220`, internal `route_task(grove, task)` at `:240-290`; `GroveResult` (`routing_decision`, `execution_result`, `metadata`) at `:44-54`; `RoutingDecision` (`selected_tree`, `selected_agent`, `confidence`, `reasoning`) at `:25-38`. Handles empty trees/no agents (`select_from_tree` returns `BattalionError::RoutingError` when a tree has no agents, `:675-681`; `route_task`'s final fallback errors with "No agents available for routing" when the Grove has no trees at all, `:285-287`), missing embeddings for `SemanticSimilarity`, and LLM routing failure with fallback (`handle_routing_failure`, exercised by `test_route_with_llm_fallback_to_keyword`); always returns a `RoutingDecision` with reasoning even on the fallback path. Multiple Grove executions run concurrently, proven by the same concurrent-execution test as the Council row above. Exercised by `cargo test --offline -p paladin-battalion grove_service::` — 15/15 passed and `cargo test --offline -p paladin-ai --test lib -- integration::battalion::grove_integration_test` — 8/8 passed (`test_grove_fallback_behavior`, `test_grove_no_fallback_default_behavior`, `test_grove_multiple_trees`, `test_grove_semantic_similarity_routing`), both run during this task |
| REQ-grove-arsenal-integration | genuinely outstanding | No shipped code satisfies this SHOULD-level requirement. `TreeAgent` at `grove.rs:79-96` carries only `paladin_id`, `expertise_keywords`, `expertise_embedding` — no tool/Arsenal field of any kind. `grep -in 'tool\|arsenal' crates/paladin-core/src/platform/container/battalion/grove.rs crates/paladin-battalion/src/grove_service.rs` returns exactly one match, an unrelated `supports_tool_calling: false` LLM-capability flag at `grove_service.rs:1110` inside the LLM request path, not an Arsenal/tool-availability check. Routing never validates agent tool access and no tool-availability field appears in `RoutingDecision`. Verified open against the tree during this task — the run-2 ledger left this row uninspected; this task inspects it and records genuine absence, not a stale claim |
| REQ-council-grove-commander-integration | satisfied | `BattalionStrategy::Council`/`Grove` variants exist and Auto-mode logic considers both: `council_keywords`/`grove_keywords` arrays at `crates/paladin-battalion/src/commander.rs:999-1014,1027-1041`; Commander routes to `CouncilExecutionService::convene`/`GroveExecutionService::execute` at `:556-627` and `:627-692`; explicit strategy selection via `CommanderBuilder::strategy()` works; example configs exist at `examples/cli_configs/{council_basic,council_moderated}.yaml` and `examples/cli_configs/{grove_keyword,grove_semantic,grove_llm}.yaml` (PRD names `.yml`, shipped as `.yaml` — cosmetic, both are valid YAML). No regressions: the full Commander end-to-end suite passes. Exercised by `cargo test --offline -p paladin-ai --test lib -- integration::commander_integration_tests` — 19/19 passed, including `test_commander_auto_detects_council_from_input`, `test_commander_auto_detects_grove_from_input`, `test_commander_executes_council_strategy_end_to_end`, `test_commander_executes_grove_strategy_end_to_end`, `test_concurrent_council_and_grove_execution`, run during this task. **Two real gaps, both new findings (plan 05-10):** (1) the PRD's literal `--strategy council`/`--strategy grove` CLI flag does not exist anywhere in the tree — `grep -rn 'BattalionStrategy' src/` returns 0 matches, and `paladin battalion run --type` (`src/application/cli/commands/battalion.rs:142-149`) dispatches only `formation`/`phalanx`/`campaign`/`chain-of-command`/`conclave`/`maneuver`; there is no `council`/`grove` `BattalionYamlConfig` variant. The functional capability is reachable instead via the standalone `paladin council` command (Epic 18, see below) and via direct API examples (`examples/commander_council.rs`, `examples/commander_grove.rs`), not via a battalion-run flag. (2) the PRD's literal auto-detection keywords "expert" and "specialist" are not in Grove's keyword list (`commander.rs:1027-1041` has "expertise"/"expert for"/"specialized in"/"skilled in", not bare "expert"/"specialist"); those exact bare words are instead ChainOfCommand keywords (`:1126-1135`), checked later in the same dispatch chain — so literal input containing only "expert" or "specialist" routes to ChainOfCommand, not Grove, diverging from the PRD's explicit mapping |

### Epic 17 / 17.5 — Flow DSL, Maneuver and CLI consolidation (11 IDs)

**Epic-level note.** `intel/code-verification.md` records Maneuver and the Flow DSL as verified
shipped. Epic 17.5's CLI-location decision — "CLI is an input adapter in the application layer,
not infrastructure" (`Epic_17.5/epic17-5.md`, ADR candidate 2) — is **applied in code**:
`ls src/` confirms `src/cli` is **absent** from the tree, and `src/application/cli/` carries the
full command set (`commands/`, `formatters/`, `interactive/`, `templates/`, entry point
`src/bin/paladin-cli.rs`). It remains at DOC precedence with no ADR promoting it (PROJECT.md
Context, ADR candidate 2).

| ID | Verdict | Evidence |
|---|---|---|
| REQ-flow-dsl-syntax | satisfied | Grammar operators defined in `crates/paladin-battalion/src/maneuver/parser/lexer.rs:9-20` (`Token::Arrow` `->`, `Token::Comma` `,`, `Token::LParen`/`RParen`); precedence and grouping implemented in `crates/paladin-battalion/src/maneuver/parser/mod.rs:122-197` (`parse_sequential` → `parse_parallel` → `parse_primary`, parenthesized grouping at `:161-176`), supporting sequential (`a -> b`), parallel (`a, b`), fan-out (`a -> b, c`, comma binds tighter than arrow), fan-in (`a, b -> c`), grouping (`(a -> b)`) and chains (`a -> b -> c`). Nested grouping (`"planner -> (coder -> tester), docs"`) is exercised directly by the module-level doctest at `parser/mod.rs:15-26` — `cargo test --offline -p paladin-battalion --doc -- maneuver::parser` → 10/10 passed, run during this task (`maneuver::parser (line 15)`). Unit coverage via `cargo test --offline -p paladin-battalion parser::` → 26/26 passed, run during this task |
| REQ-flow-parser | satisfied | `FlowParser::parse` at `crates/paladin-battalion/src/maneuver/parser/mod.rs:78-97` returns `Result<FlowExpression, FlowParseError>`; empty expressions rejected at `:79-81`; unbalanced parentheses rejected via `FlowParseError::UnbalancedParentheses` (`:172-178`); consecutive operators rejected via `FlowParseError::ConsecutiveOperators` (`:189-196`); identifier validation (alphanumeric/underscore/hyphen) enforced by the lexer's `read_identifier`. Every `FlowParseError` variant carries a `.suggestion()` exercised by `error.rs`'s `test_error_suggestion`/`test_consecutive_operators_suggestion`/`test_unbalanced_parens_suggestion`; position reporting confirmed by `test_error_position`/`test_format_with_context`. **New finding (plan 05-11):** the FR-2.4 performance claim ("< 1ms for 99% of flows, 30+ agents") has no benchmark anywhere in the tree — `find crates/paladin-battalion/benches -iname '*flow*' -o -iname '*maneuver*'` returns nothing; correctness is proven, the timing claim is not measured. Exercised by `cargo test --offline -p paladin-battalion parser::` → 26/26 passed, run during this task |
| REQ-flow-expression-ast | satisfied | `FlowExpression` enum at `crates/paladin-battalion/src/maneuver/parser/ast.rs:34-41` with exactly `Agent(String)`, `Sequential(Vec<FlowExpression>)`, `Parallel(Vec<FlowExpression>)`; derives `Serialize`/`Deserialize` (`:33`), round-trip confirmed by `test_serialization` (`ast.rs:255-`). Extensibility is structural (adding enum variants is source-compatible for existing match arms guarded elsewhere by exhaustive handling) rather than proven by a dedicated test — recorded as a caveat, not invented evidence. `agent_names()`/`depth()`/`agent_count()`/`width()` helpers (`:62-`) all doctested and unit-tested. Exercised by `cargo test --offline -p paladin-battalion parser::` → 26/26 passed (`test_agent_names_simple`, `test_agent_names_sequential`, `test_agent_names_deduplication`, `test_depth_simple`, `test_depth_nested`, `test_width_simple`, `test_width_parallel`, `test_agent_count`, `test_serialization`), run during this task |
| REQ-maneuver-domain-model | superseded by shipped code | `Maneuver` struct at `crates/paladin-battalion/src/maneuver/mod.rs:132-144` carries `name: String`, `agents: HashMap<String, Paladin>`, `flow: FlowExpression`, `config: ManeuverConfig` exactly as required; construction validates every flow-referenced agent exists in the agents map (`validate()`, `:173-204`, `ManeuverError::AgentNotFound`) and enforces max depth 5 / max agent count 30 (10-30 agent support). **The constructor argument order is the release-notes divergence ADR-0010 (`.planning/decisions/0010-milestone-3-epic-numbering.md`) records**: the shipped signature at `mod.rs:148-153` is `Maneuver::new(name, agents, flow, config)` — name first, flow third, config fourth — while `RELEASE_NOTES_MILESTONE_3.md:135` (the Epic 22-mislabeled section) shows `Maneuver::new(flow3, paladins, config)` (flow first, config third, no name argument). ADR-0010's Code Locations section already cites this exact divergence and records the shipped signature as authoritative; this row transcribes that citation rather than restating the argument. Exercised by `cargo test --offline -p paladin-battalion maneuver::` → 65/65 passed, run during this task |
| REQ-maneuver-config | superseded by shipped code | `ManeuverConfig` at `crates/paladin-battalion/src/maneuver/mod.rs:40-58` implements `error_strategy: ErrorStrategy`, `output_format: OutputFormat` (`Concatenate`/`JsonArray` matching `concatenate`/`json_array`), `pass_output_as_input: bool`, `timeout: Option<Duration>`, `collect_timing_metrics: bool`; builder methods `with_error_strategy`/`with_output_format`/`with_pass_output_as_input`/`with_timeout`/`without_timeout`/`with_timing_metrics` (`:80-119`); `validate()` at `:122-127`. **New finding (plan 05-11):** three PRD-named fields do not exist anywhere in the tree — `grep -rn 'max_nesting_depth\|max_parallel_branches\|agent_timeout_seconds\|capture_intermediate_outputs' crates/ src/` returns zero matches. `timeout` is workflow-wide only, never split into `agent_timeout_seconds`; `capture_intermediate_outputs` does not exist (only an unrelated `detailed_observability: bool`); no `config.yml`-level `maneuver:` global-defaults section exists at all — `Maneuver::validate()` hardcodes the depth-5 and agent-count-30 bounds directly in `mod.rs:187,196` rather than reading them from any config. The shipped, narrower field set is recorded as authoritative for what the type actually carries. Exercised by `cargo test --offline -p paladin-battalion maneuver::` → 65/65 passed (`test_maneuver_config_builder`, `test_maneuver_config_default`, `test_maneuver_config_validation`), run during this task |
| REQ-maneuver-error-strategy-v2 | satisfied | `crates/paladin-battalion/src/maneuver/mod.rs:17-26` — `ErrorStrategy::FailFast`/`ContinueParallel`/`IgnoreErrors`, matching the v2 (Epic 17) acceptance exactly; `FailFast` stops the workflow (`service.rs:207-209`, returns `Err`), `ContinueParallel` in a sequential context still stops (`service.rs:210-213`, "continue parallel branches but fail the sequence"), `IgnoreErrors` logs and continues (`service.rs:214-218`). **This is the later position on variant group 15**: `REQ-battalion-error-strategy` (run 1, Epic 4) names a differently-shaped `ErrorStrategy` — `FailFast`/`ContinueOnError`/`RetryThenContinue` — which ships as a **separate enum** at `crates/paladin-core/src/platform/container/battalion/mod.rs:239-249`, used by Formation/Phalanx/Campaign, not Maneuver; `grep -rn 'pub enum ErrorStrategy' crates/ src/` returns exactly two hits, one per crate, confirming the two variant sets are structurally distinct rather than competing implementations of one type. Exercised by `cargo test --offline -p paladin-battalion maneuver::` → 65/65 passed (`test_error_strategy_default`, `test_error_strategy_fail_fast`, `test_error_strategy_ignore_errors`, `test_timeout_handling`), run during this task |
| REQ-maneuver-execution-service | superseded by shipped code | `ManeuverExecutionService::execute` at `crates/paladin-battalion/src/maneuver/service.rs:36-63` recursively evaluates `FlowExpression` via `execute_expression` (`:105-127`), with `execute_sequential` (`:180-225`) feeding output into the next step only when `pass_output_as_input` is set (else reusing the original input, `:190-194`) and `execute_parallel` (`:228-`) spawning agents concurrently via `tokio::spawn`, respecting `ErrorStrategy` per-branch. Timeout enforced by `tokio::time::timeout` (`:44-49`), returning `ManeuverError::TimeoutError`; per-agent failures name the failing agent (`ExecutionError(format!("Agent '{}': {}", ...))`, `:151-153`, confirmed by `test_error_strategy_fail_fast` asserting the error message contains `"agent2"`). **The returned `ManeuverResult` diverges from the PRD**: `step_outputs` is `HashMap<String, String>` (`mod.rs:229`), not `HashMap<String, PaladinResult>` as FR-6.5 requires — only the raw output string survives per step, losing per-step `PaladinResult` metadata (token usage, status). `final_output: String`, `execution_order: Vec<String>` and `timing_metrics: Option<HashMap<String, Duration>>` all match exactly. **New finding (plan 05-11):** the <10ms orchestration-overhead / O(n)-memory performance claims have no benchmark anywhere in the tree — `find crates/paladin-battalion/benches -iname '*maneuver*'` returns nothing. Recursive/sequential/parallel/nested/fan-out/timeout/error-strategy behavior is exercised by `cargo test --offline -p paladin-battalion maneuver::service` → included in the 65/65 passing `maneuver::` run (`test_simple_sequential_execution`, `test_simple_parallel_execution`, `test_nested_expression`, `test_fan_out_pattern`, `test_output_passing_disabled`, `test_timing_metrics_collection`, `test_timeout_handling`), run during this task |
| REQ-maneuver-commander-integration | satisfied | `BattalionStrategy::Maneuver` variant exists (`crates/paladin-core/src/platform/container/battalion/mod.rs`, matched at `commander.rs:703`); `CommanderBuilder::flow(&str)` (`commander.rs:1374-1381`) and `.maneuver_config(ManeuverConfig)`/error-strategy setting (`:1397-1436`) are both wired; the dispatch arm at `commander.rs:703-827` constructs a `Maneuver` and executes it via `ManeuverExecutionService`, converting the result to `BattalionResult`. Explicit-only is enforced: Auto-strategy keyword detection for Maneuver was **removed**, per the code comment at `commander.rs:1052-1082` ("Maneuver strategy is EXPLICIT-ONLY and NOT selected by Auto mode... Maneuver keyword detection removed"), and `CommanderBuilder::build()` validates a flow expression is present when `BattalionStrategy::Maneuver` is explicitly selected (`:1501-1505`). Agent-registration validation reuses `Maneuver::new`'s own `validate()` (`AgentNotFound` on construction, `:785-787`). Exercised by `cargo test --offline -p paladin-battalion test_maneuver_strategy_explicit` → 1/1 passed, run during this task |
| REQ-maneuver-cli | superseded by shipped code | `paladin battalion run --type maneuver --config <file>` (`src/application/cli/commands/battalion.rs:56-72`, `BattalionRunArgs`) and `paladin battalion new --type maneuver --output <file>` (`:40-52`, `"maneuver"` in the valid-types list at `:87`) both exist, matching the PRD's `--type maneuver`/YAML-config shape. **The remaining PRD command forms diverge, recorded against ADR-0010 (`.planning/decisions/0010-milestone-3-epic-numbering.md`) rather than restated fresh**, since this is the same defective release-notes section ADR-0010 already corrects: (1) the PRD's inline `--flow "<expr>"` flag on `battalion run` does not exist — `BattalionRunArgs` (`battalion.rs:56-72`) has no `flow` field; the flow expression is read only from the YAML `config` file. (2) `paladin battalion visualize --flow "<expr>"` does not exist anywhere — `BattalionCommands` (`battalion.rs:31-36`) has exactly two variants, `New` and `Run`; visualization instead ships as a **separate top-level command group**, `paladin maneuver visualize -c <file> --format ascii|mermaid` (`src/application/cli/commands/maneuver.rs:29-49`) — the `paladin maneuver ...` surface `RELEASE_NOTES_MILESTONE_3.md:176-177` itself shows as `paladin maneuver visualize "<expr>" --format mermaid`, which is *also* divergent from the shipped form: the shipped command takes `-c/--config <file>`, not a positional flow-expression string. **New finding (plan 05-11):** a third top-level subcommand, `paladin maneuver validate -c <file>`, ships and is named by no ingested document. The shipped, tested command surface (`battalion run`/`battalion new` plus the standalone `maneuver visualize`/`maneuver validate` group) is recorded as authoritative. Exercised by `cargo test --offline --features cli --lib -- application::cli::commands::maneuver` → 4/4 passed, run during this task |
| REQ-flow-visualization | satisfied | `FlowVisualizer::to_ascii(&FlowExpression) -> String` and `FlowVisualizer::to_mermaid(&FlowExpression) -> String` at `crates/paladin-battalion/src/maneuver/visualizer.rs:82,111`; timing overlay via `FlowVisualizer::with_timing` (`:254-`) highlighting the slowest agent, exercised by `test_timing_bottleneck_identification`/`test_timing_total_calculation`; `--output` flag on the CLI (`src/application/cli/commands/maneuver.rs:47-48`, `ManeuverVisualizeArgs.output: Option<PathBuf>`) allows saving diagrams. Exercised by `cargo test --offline -p paladin-battalion maneuver::visualizer` → included in the 65/65 passing `maneuver::` run (`test_ascii_simple_agent`, `test_ascii_sequential`, `test_ascii_parallel`, `test_ascii_nested`, `test_mermaid_simple_agent`, `test_mermaid_sequential`, `test_mermaid_parallel`, `test_mermaid_nested`, `test_mermaid_complex`, `test_mermaid_is_valid`, `test_with_timing_simple`, `test_with_timing_sequential`, `test_with_timing_parallel`, `test_with_timing_nested`, `test_timing_bottleneck_identification`, `test_timing_total_calculation`), run during this task |
| REQ-maneuver-validation | present, unproven | `Maneuver::validate()` at `crates/paladin-battalion/src/maneuver/mod.rs:173-204` checks that every flow-referenced agent exists in the agents map (`ManeuverError::AgentNotFound` naming the missing agent and available set, `:178-182`), rejects flows deeper than 5 levels (`:186-192`) and flows with more than 30 agent nodes (`:195-201`). `ExecutionStatus::PartialSuccess` surfaces partial results when `ErrorStrategy::IgnoreErrors` allows execution to continue after a failure (`service.rs:91-96`), tested by `test_error_strategy_ignore_errors` (asserts `PartialSuccess`, `service.rs:747`); which-agent-failed reporting confirmed by `test_error_strategy_fail_fast`. **New finding (plan 05-11):** `Maneuver::new`'s own rejection paths are entirely untested — every one of the nine `Maneuver::new(...)` call sites in the tree (`grep -rn 'Maneuver::new' crates/ src/ examples/`) constructs a valid Maneuver and either `.unwrap()`s or propagates via `?`; zero test anywhere constructs an invalid flow (missing agent, depth > 5, agent count > 30) and asserts the resulting `Err`. Self-references and circular dependencies specifically are not checked at all — `grep -rn 'self_reference\|circular\|SelfReference\|Circular' crates/paladin-battalion/src/maneuver/` returns zero matches; circular references are structurally unrepresentable in the tree-shaped `FlowExpression` AST (no back-reference mechanism exists), but a literal self-reference such as `"a -> a"` (the same agent name appearing twice) parses and validates successfully with no rejection, contrary to the PRD's literal "reject self-references" clause. Both max-depth (5) and max-agent-count (30) are hardcoded directly in `validate()` rather than driven by any configurable field — the same underlying gap `REQ-maneuver-config`'s missing `max_nesting_depth`/`max_parallel_branches` fields record. The construction-time-rejection code exists and is cited above, but nothing in the tree exercises its rejection behavior, which governs this row's verdict per D-01 (a single row cannot be `satisfied` on the strength of its better-tested half alone) |

### Epic 18 — CLI Enhancement & Polish (7 IDs)

**Epic-level note.** `tasks-epic-18-cli-enhancement.md` carries **12 open checkboxes**, transcribed
from `.planning/intel/task-completion-state.md` and not re-derived. Every command this task list
names exists in the tree — the count is **stale**, not a backlog: `paladin onboarding`,
`paladin setup-check`, `paladin features`, `paladin muster` and `paladin council` all ship, are
wired into `src/bin/paladin-cli.rs`, and are exercised by unit tests below. Epic 18 is explicitly
**not** one of the three blocks flagged for VERIFY-02 (those are Epic 22, Epic 14, Epic 24) — a
reader should not expect a block-level verdict in this section, only the 7 per-requirement rows
below.

| ID | Verdict | Evidence |
|---|---|---|
| REQ-cli-onboarding-wizard | satisfied | `paladin onboarding` wired at `src/bin/paladin-cli.rs` via `onboarding::run_wizard`-style entry; wizard steps (`WelcomeStep`, `ProviderSelectionStep`, `ApiKeyInputStep`, `ApiValidationStep`, `EnvFileStep`, `SampleConfigsStep`, `SummaryStep`) at `src/application/cli/commands/onboarding.rs:240-` implementing `WizardStep`, driven by a resumable `Wizard`/`WizardContext` (`.with_resume()` at `:653`); `.env` generation covers `OPENAI_API_KEY`/`ANTHROPIC_API_KEY`/`DEEPSEEK_API_KEY` (`:42-44`); existing-file conflict handling offers exactly "Overwrite (replace existing file)" / "Skip (keep existing file)" / "Merge (combine configurations)" (`:459-461`); sample configs `basic_paladin.yaml`, `formation.yaml`, `phalanx.yaml`, `paladin_with_rag.yaml` generated at `:163-231`, confirmed written by `test_generate_sample_configs` (`:719-`, asserts all four files exist on disk). Provider API-key validation calls are exercised only as `#[ignore]`d live-network tests (`test_validate_openai_key_invalid`/`_anthropic_key_invalid`/`_deepseek_key_invalid`) — consistent with this corpus's live-API-test convention (VERIFY-06). **New finding (plan 05-10):** the PRD's optional `REDIS_URL`, `QDRANT_URL`, `MINIO_ENDPOINT` `.env` entries are absent — `grep -in 'redis\|qdrant\|minio' src/application/cli/commands/onboarding.rs` returns 0 matches; only the three LLM provider keys are ever written. Exercised by `cargo test --offline --features cli --lib -- application::cli::commands::onboarding` — 6/9 passed, 3 ignored (live-API), run during this task |
| REQ-cli-setup-check | satisfied | `paladin setup-check` at `src/application/cli/commands/setup_check.rs:340` (`run_setup_check`), wired via `src/bin/paladin-cli.rs`; validates CLI/Rust versions and each configured LLM provider plus Redis/Qdrant/MinIO connectivity when configured (`check_cli_version`, `check_rust_version`, provider checks, `check_redis_*`); provider validation makes real API calls (`https://api.openai.com/v1/models` at `:76`, `https://api.deepseek.com/v1/models` at `:135`, Anthropic minimal message request); status indicators are exactly green `✓`/red `✗`/yellow `⚠` via `OutputFormatter` (`src/application/cli/formatters/output.rs:113-127`); exit codes match the PRD exactly — 0 all pass, 1 on failure, 2 on warnings-only (`setup_check.rs:447-455`, confirmed against `error_impl.rs:320-327`'s documented convention). `--verbose` gates extra `Skipped:` output and per-check detail (`:390,405,441`). Exercised by `cargo test --offline --features cli --lib -- application::cli::commands::setup_check` — 6/9 passed, 3 ignored (live-API), run during this task (`test_check_cli_version`, `test_check_rust_version`, `test_check_env_file`, `test_check_redis_invalid_url`, `test_check_redis_valid_url_format`, `test_check_openai_no_env`) |
| REQ-cli-features-discovery | satisfied | `paladin features` at `src/application/cli/commands/features.rs`; `FeatureCategory` groups Agent/Battalion/Orchestration/Memory/Utility; orchestration patterns listed include Formation, Phalanx, Campaign, Conclave, Council, Grove, Maneuver by name (`:136-186`; ChainOfCommand present under a different literal but same category — confirmed by `test_feature_registry_all`); memory systems include Garrison and Sanctum entries; each `Feature` carries name, description, availability status and doc link (`struct Feature`, `:78-96`); `--category <name>` filtering (`:344,360-371`) and `--format json` (`OutputFormat::Json`, `:299,408`) both implemented. Exercised by `cargo test --offline --features cli --lib -- application::cli::commands::features` — 12/12 passed, run during this task (`test_feature_registry_all`, `test_feature_registry_by_category`, `test_run_features_with_category`, `test_run_features_invalid_category`, `test_output_format_from_str`, `test_run_features_invalid_format`) |
| REQ-cli-muster-command | superseded by shipped code | `paladin muster` at `src/application/cli/commands/muster.rs:71` (`run_muster`), accepting `--task`/interactive prompt (`get_task_description`, `:141-153`), `--execute`/`--output`/`--provider`/`--model`/`--no-review` all wired (`run_muster` signature, `:71-77`, matched to CLI flags at `src/bin/paladin-cli.rs:160`); default save path `muster_<battalion_name>_<timestamp>.yaml` (`determine_output_path`, `:389-396`, matches `test_determine_output_path_with_default` asserting the `muster_test_battalion_` prefix); on LLM failure the command falls back to template selection by keyword matching (`test_template_fallback_sequential/_parallel/_discussion/_default` all pass). **The requirement's headline claim — LLM-powered analysis returning a recommended pattern with justification, agents, complexity and token estimate — has no shipped implementation**: `analyze_task_with_llm` at `:156-166` is hardcoded to `Err(CliError::execution("LLM integration in muster - using template fallback"))` on every call, with `_task_description`/`_provider`/`_model` all underscore-prefixed and unused, and the function's own comment reads "For now, return error to trigger template fallback // Full LLM integration would be implemented here." Every `paladin muster` invocation is therefore template-only regardless of `--provider`/`--model`. **New findings (plan 05-10):** (1) stdin input is not supported — `grep -n stdin src/application/cli/commands/muster.rs src/bin/paladin-cli.rs` returns 0 matches, only `--task` and interactive prompt exist; (2) config editing before execution is stubbed — `formatter.warning("Configuration editing not yet implemented. Using generated config.")` at `:381` — only accept/cancel review works, not edit. The template-fallback and flag-wiring portions are exercised by `cargo test --offline --features cli --lib -- application::cli::commands::muster` — 11/11 passed, run during this task; the LLM-analysis capability itself has no passing test because no code path reaches it |
| REQ-cli-council-command | genuinely outstanding | `paladin council` exists at `src/application/cli/commands/council.rs:52` (`run_council`), wired via `src/bin/paladin-cli.rs:170`, and its surrounding mechanics are real: participant-count validation (2-10, `:100-112`), default role assignment matching the PRD's table exactly (`DEFAULT_ROLES_2..5` plus numbered "Expert N" for 6+, `:14-21,146-162`), and transcript save-to-file (`save_transcript`). **The command's core capability — an actual multi-agent discussion — does not exist.** `run_discussion` at `:184` is explicitly commented `// Simulate discussion rounds` (`:203`) and generates the **same templated sentence** for every turn regardless of topic or role — `format!("As the {}, I believe this topic requires careful consideration of multiple perspectives.", participant.role)` (`:209-212`) — with `_model`/`_temperature` parameters underscore-prefixed and never read. `generate_summary` (`:256-287`) is equally templated: fixed "Key Points"/"Consensus" boilerplate referencing only turn/round counts, with **no "areas of disagreement" or "recommended action" section** at all, diverging from the PRD's required four-part summary. This is not a `CouncilExecutionService`/`Council` integration of any kind — `grep -n 'CouncilExecutionService\|paladin_core\|paladin_battalion' src/application/cli/commands/council.rs` returns 0 matches. Real-time formatting (round number, speaker, visual separation) and file-save mechanics are exercised and pass (`cargo test --offline --features cli --lib -- application::cli::commands::council` — 12/12 passed, run during this task), but the requirement's substance — an LLM-driven Council discussion reachable from the CLI — is not shipped by any code in the tree |
| REQ-cli-rich-output | satisfied | `src/application/cli/formatters/` (`mod.rs`, `output.rs`, `progress.rs`, `table.rs`, `tests.rs`) implements progress/color/table/box-drawing formatting; `NO_COLOR` respected at `table.rs:64` and `output.rs:65-67`; `insta = "1.34"` at `Cargo.toml:152`; `tests/cli/snapshots/` holds **86** snapshot files (`ls tests/cli/snapshots/ | wc -l` → 86, run during this task) — the identical count the Epic 24 `REQ-cli-snapshot-testing` row records; cross-referenced rather than re-measured independently to avoid two possibly-differing counts (T-05-17). Status glyphs match the PRD exactly: `✓` success, `✗` error, `⚠` warning (`output.rs:113-127`). Exercised by `cargo test --offline --features cli --test cli -p paladin-ai -- table_output_test` — 8/8 passed, run during this task, matching the count Epic 24's row already cites |
| REQ-cli-core-infrastructure | superseded by shipped code | Every command supports non-interactive flags plus an interactive fallback prompt (confirmed across onboarding/setup-check/features/muster/council rows above); `--help` is clap-generated for all commands; `NO_COLOR`/`TERM` respected (see `REQ-cli-rich-output` row); exit codes 0/1/2 implemented exactly at `src/application/cli/error_impl.rs:320-327`; configuration loads from `.env`, `config.yml` and CLI flags. The PRD's module layout (`commands/`, `formatters/`, `interactive/`, `templates/`, entry point `src/bin/paladin-cli.rs`) exists **verbatim in name** but under `src/application/cli/` rather than the PRD's `src/cli` — confirmed all four subdirectories resolve: `src/application/cli/commands/`, `src/application/cli/formatters/`, `src/application/cli/interactive/`, `src/application/cli/templates/`, plus `src/bin/paladin-cli.rs`. This is the Epic 17.5 CLI-consolidation relocation ("CLI is an input adapter in the application layer, not infrastructure") already applied in code; per this ledger's own head-note path caveat (D-04), this verdicts `superseded by shipped code` pointing at that note, not a fresh divergence write-up. Test coverage: unit tests exist per-command (confirmed by the 9-13-test runs cited on each Epic 18 row above); `tests/cli/integration_tests.rs` is compiled out (`tests/cli/mod.rs:14`, commented per Phase 2's D-09, reported by plan 02-09's D-12 sweep) so the PRD's specific ≥80% line-coverage figure is not independently re-measured here — see VERIFY-05 for the workspace coverage gate this falls under |

### Epic 19 — Herald & Domain Type Consolidation (5 IDs)

**Epic-level note.** 19 = Herald & Domain Type Consolidation is the authoritative numbering
recorded in `.planning/decisions/0010-milestone-3-epic-numbering.md`, which the release notes
contradict (`RELEASE_NOTES_MILESTONE_3.md:21` mislabels this integer "Epic 19: Conclave Pattern" —
that content is in fact Milestone 2 Epic 15). ADR-0010's mapping table is the citation for the
attribution defect; it is not restated here.

| ID | Verdict | Evidence |
|---|---|---|
| REQ-herald-type-consolidation | satisfied | `crates/paladin-core/src/platform/container/herald.rs:25-28` imports `BattalionResult`, `PaladinResult`, `PaladinError` and `TokenUsage` via `pub use` re-export from their single-source-of-truth modules (`battalion::BattalionResult`, `execution_result::PaladinResult`, `paladin_error::PaladinError`, `token_usage::TokenUsage`) rather than defining local placeholder types; `grep -n 'TODO\|FIXME\|placeholder\|Placeholder' crates/paladin-core/src/platform/container/herald.rs` returns zero matches, confirmed during this task. **This is the later position on the run-1 variant group 6 duplication question**: the `Herald` trait (`herald.rs:36-154`) and all three formatters consume exactly these re-exported types, with no local `struct PaladinResult`/`BattalionResult`/`PaladinError` definitions anywhere in the file. `Send + Sync` preserved, confirmed by `test_herald_is_send_sync`. The PRD's `src/application/ports/output/herald_port.rs` path does not exist — the `Herald` trait lives directly in `herald.rs` itself post-workspace-decomposition; this is the ledger's own path caveat (D-04), not a fresh divergence. Exercised by `cargo test --offline -p paladin-ai-core herald::` → 7/7 passed and `cargo test --offline -p paladin-herald` → 70/70 passed, both run during this task |
| REQ-stream-chunk-complete | satisfied | `StreamChunk` at `crates/paladin-core/src/platform/container/herald.rs:239-261` carries all six required fields — `chunk_id: Uuid`, `sequence_number: u64` (0-indexed), `timestamp: DateTime<Utc>`, `content: String`, `token_count: Option<u32>`, `is_final: bool` — plus `metadata: HashMap<String, serde_json::Value>` under `#[serde(flatten)]` (`:259-260`); derives `Debug, Clone, Serialize, Deserialize` (`:238`); `StreamChunk::builder()` at `:282-284` returns `StreamChunkBuilder` (`:292-`) with per-field setters and validation on `.build()`. Serialises for JSON formatting (consumed by `JsonHerald::format_stream_chunk`) and event streaming; Citadel persistence path not independently re-verified here (out of this row's file set). Exercised by `test_format_stream_chunk` (`herald.rs:747-`) and the module's own doctests — `cargo test --offline -p paladin-ai-core herald::` → 7/7 passed, `cargo test --offline -p paladin-ai-core --doc -- herald` → 8/8 passed (3 `ignore`d trait-signature snippets), both run during this task |
| REQ-execution-metadata-complete | satisfied | `ExecutionMetadata` at `crates/paladin-core/src/platform/container/herald.rs:507-527` carries all eight required fields — `execution_id: Uuid`, `start_time: DateTime<Utc>`, `end_time: Option<DateTime<Utc>>`, `duration_ms: Option<u64>`, `model_used: String`, `token_usage: TokenUsage`, `cost_estimate: Option<f64>`, `error_count: u32` — plus `metadata: HashMap<String, serde_json::Value>` under `#[serde(flatten)]` (`:525-526`); `TokenUsage` at `crates/paladin-core/src/platform/container/token_usage.rs:13-20` carries three token fields and `total_tokens`, **named `prompt_tokens`/`completion_tokens`** rather than the PRD's literal `input_tokens`/`output_tokens` — **New finding (plan 05-11):** this naming divergence is the same `TokenUsage` this corpus already tracks as one of three shipped definitions needing consolidation (DEBT-05, Phase 7-8 scope, out of this phase's boundary); recorded here as a fact about the field names, not re-adjudicated. `calculate_duration()` at `:539-544` derives `duration_ms` from `end_time - start_time`, genuinely exercised (not merely called-and-ignored) by the doctest at `herald.rs:424-450`, which `.unwrap()`s `metadata.duration_ms` immediately after calling it — a doctest that would panic if the method left the field `None`. Builder pattern at `ExecutionMetadataBuilder` (`:557-648`) validates required fields on `.build()` (`execution_id`, `start_time`, `model_used`, `token_usage`). Exercised by `test_finalize_stream` (`herald.rs:774-791`, constructs full `ExecutionMetadata` with `token_usage`) and `cargo test --offline -p paladin-ai-core --doc -- herald` → 8/8 passed including `ExecutionMetadata (line 424)` (the `calculate_duration` example) and `ExecutionMetadata (line 454)` (cost/error-tracking example), both run during this task |
| REQ-herald-formatter-autoregistration | satisfied | `HeraldRegistry` at `src/application/services/herald/herald_registry.rs:85-88` implements `Default` (`:216-254`), auto-registering `JsonHerald::new()` as `"json"`, `MarkdownHerald::new()` as `"markdown"` and `TableHerald::default()` as `"table"` (`:248-250`), all retrievable via `registry.get("json")` etc.; the manual `register()`/`new()` API is preserved unchanged (`:90-102, 120-123`); duplicate keys are handled by deliberate overwrite, documented in the `register()` rustdoc ("If a formatter with the same name already exists, it will be replaced") and proven by `test_register_replaces_existing` / `test_default_registry_can_override_builtin_formatters`. Formatter keys and a zero-config usage example are documented in the module-level rustdoc (`:1-66`). Exercised by `cargo test --offline --features cli --lib -- application::services::herald::herald_registry` → 14/14 passed, run during this task (`test_default_registry`, `test_default_registry_can_add_custom_formatters`, `test_default_registry_can_override_builtin_formatters`, `test_new_vs_default_registry`, `test_registry_is_send_sync`) |
| REQ-herald-consolidation-quality-gates | deferred with reason | Per `.planning/decisions/0006-coverage-gate.md`'s Phase 5 amendment module-scoped gate table: Herald (`crates/paladin-core/src/platform/container/herald.rs`) measures **80.49%** line coverage against a **≥ 95%** target, a gap of **~14.5 points**, transcribed byte-identical from `01-coverage-measurement.md:317` via ADR-0006 — **not re-measured or re-derived here**, consistent with the D-16 prohibition on Phase 5 re-measurement and the T-05-18 mitigation against introducing a second Herald coverage figure. Owner: **Phase 15 / PIPE-02**, the same owner ADR-0006 already records. `cargo build`/`cargo clippy zero-warnings`/`examples/herald_*.rs` sub-clauses are not independently re-verified in this row — this row's scope is the transcription itself, per the plan's explicit instruction not to trigger any coverage tool. No `cargo llvm-cov` or `cargo tarpaulin` invocation occurred during this task |

### Epic 21 — Autonomous Agent Completion (7 IDs)

| ID | Verdict | Evidence |
|---|---|---|
| REQ-handoff-tool-v2 | satisfied | Variant group 13, paired with `REQ-handoff-tool-v1` (Epic 14, filled by plan 05-06): this is the shape the wired dispatch path actually uses. `PaladinBuilder::generate_handoff_tool()` at `src/application/services/paladin/paladin_builder.rs:1184` builds an `Armament` named `handoff_to_specialist` with `specialist_name` (string enum of configured specialist names, `:1197-1201`) and `task_description` (`:1202-1205`) — the exact Epic 21/23 shape this ID names. `PaladinBuilder::build()` auto-registers it when `self.handoffs_configured` (set at `:861` by `with_handoffs()`) is true (`:1313-1326`), logging the specialist count; `ArsenalRegistryService`'s `HashMap<String, Armament>` keying (`arsenal_registry_service.rs:44,115`) makes re-registration on a rebuilt Paladin idempotent by construction — a second `register()` call with the same tool name overwrites rather than duplicates. Exercised by `cargo test --offline -p paladin-ai --lib -- handoff_tool` — 5/5 passed (`test_builder_auto_registers_handoff_tool_when_configured`, `test_builder_does_not_register_handoff_tool_when_not_configured`, `test_handoff_tool_schema_includes_all_specialists`, `test_handoff_tool_auto_registration_is_idempotent`, `test_handoff_tool_schema_validation`), run during this task. The wired execution-time dispatch (`PaladinExecutionService::is_handoff_tool_call()` at `paladin_execution_service.rs:1414` matching this same `handoff_to_specialist` name) is exercised by the 27/27 `handoff_service_test` run plan 05-06 already cites for `REQ-handoff-tool-v1`'s row — cross-referenced, not re-run. Not contradictory with `REQ-handoff-tool-v1`'s `present, unproven` verdict: that row already records that the wired path uses this v2 shape instead of the Epic-14 `HandoffTool` struct, exactly what this row confirms from the other side |
| REQ-autonomous-configurable-model | satisfied, with caveat | The defect class the PRD names is closed in both cited files: `PlanningService::create_plan()`/`execute_subtasks()`/`synthesize_results()` take `model: &str` as a parameter (`planning_service.rs:111,173,274`) rather than hardcoding `"gpt-4"`, and `PaladinExecutionService::apply_layer1_planning()` passes the Paladin's actually-configured model (`paladin_execution_service.rs:1265`, `let model = paladin.node.model.as_str();`) into `create_plan(input, 10, model)` at `:1268`; the same pattern closes `PromptGenerationService::generate_prompt()` (`prompt_generation_service.rs:104`) via `paladin_execution_service.rs:1341,1345`. Exercised by `cargo test --offline -p paladin-ai application::services::paladin::planning_service application::services::paladin::prompt_generation_service` — 19/19 passed (11 + 8, the same runs plan 05-06 already cites for cluster `2.0`/`3.0`), run during this task. **The same defect class survives**, unfixed and unrelated to this closure, at `crates/paladin-battalion/src/grove_service.rs:537` (`model: "gpt-4".to_string(), // TODO: Make configurable`, `#[cfg(test)]` begins `:732`) — cross-referenced to the Epic 22 `REQ-grove-llm-routing` row (verdicted `genuinely outstanding`, owner Phase 6/CLOSE-01) rather than duplicated here, per this plan's explicit instruction; not fixed, per this phase's prohibition on editing `.rs` files. **Caveat, not a fresh defect:** two of the acceptance criteria this ID also carries are unmet — no model-compatibility validation-with-fallback exists at the `PlanningService`/`PromptGenerationService` layer (a test comment at `planning_service.rs:989` states outright "In reality, the LlmPort implementation handles fallback" — that layer, not this one), and subtask `expected_output` is still a hardcoded placeholder with its own open TODO: `Subtask::new(llm_subtask.id.clone(), llm_subtask.description, "Expected output from subtask execution".to_string(), // TODO: Ask LLM for expected output` at `planning_service.rs:430-433` |
| REQ-paladin-result-autonomous-metadata | superseded by shipped code | `PaladinResult.plan: Option<TaskPlan>` (`execution_result.rs:60`, `#[serde(default, skip_serializing_if = "Option::is_none")]` at `:58`) and `.handoff_history: Vec<HandoffRecord>` (`:68`, `#[serde(default, ...)]` at `:66`) both default correctly and deserialize tolerant of missing fields — exercised via the same `test_execute_handoff_delegates_to_specialist` (27/27 `handoff_service_test` run cited in Epic 14's cluster `7.0` row) that populates and asserts a real record. **The shipped field shapes diverge from the PRD and are recorded as authoritative:** `TaskPlan` (`planning.rs:15-26`) carries `original_task`/`subtasks`/`dependencies`/`max_subtasks`, not the PRD's `goal`/`subtasks`/`created_at` — no `goal` or `created_at` field exists anywhere in the struct. `HandoffRecord` (`handoff.rs:181-195`) carries `from_agent`/`to_agent`/`task`/`result`/`depth: u32`, not the PRD's `specialist_name`/`task_description`/`timestamp`/`result`/`depth: usize` — no `timestamp` field exists at all, and `depth` is `u32` not `usize`. **MessagePack serialization support does not exist anywhere in the tree** (`grep -rn 'rmp_serde\|MessagePack' --include=*.rs .` → zero matches, run during this task) — only `serde_json`-compatible `Serialize`/`Deserialize` derives ship. **The recommended 100-record cap with oldest-first eviction is not implemented**: `execute_handoff()` at `paladin_execution_service.rs:1492` unconditionally `handoff_history.push(record)`s with no length check anywhere in the call chain, run during this task |
| REQ-autonomous-orchestration-layers | satisfied, with caveat | The layered flow ships: Layer 0 core execution always runs; Layer 1 planning (`apply_layer1_planning`, gated by `paladin.node.autonomous_planning: bool` at `paladin.rs:169`, checked at `paladin_execution_service.rs:1238`) then prompt generation (`apply_layer1_prompt_generation`, gated by `autonomous_prompts: bool` at `paladin.rs:176`, checked `:1306`); Layer 2 dynamic temperature (`apply_layer2_dynamic_temperature`, gated by `dynamic_temperature: bool` at `paladin.rs:191`, checked `:1381`); Layer 3 handoff detection/execution (`is_handoff_tool_call`/`execute_handoff`, `:1414,1434`). Each of the first three layers is toggled by its own named `bool` field on `PaladinData`, matching three of the four names this ID lists; graceful degradation is implemented and tested (`test_graceful_degradation_no_planning_service`/`_no_prompt_service`, 2/2 passed, cited in Epic 14's `REQ-autonomous-error-handling` row). **Caveat, not a fresh defect:** Layer 3 has no equivalent named `handoffs: bool` field on `PaladinData` (`grep -n 'pub handoffs' crates/paladin-core/src/platform/container/paladin.rs` → none, run during this task) — it is instead implicitly gated by whether the arsenal contains the auto-registered `handoff_to_specialist` tool (itself gated by `handoffs_configured`, set only when `with_handoffs()` is called), functionally equivalent but not the fourth named config toggle this ID specifies. **No integration test exercises all four layers enabled together** — `grep -rn` across `tests/` for an all-features-enabled or layer-combination test name returns none; `tests/integration/autonomous_planning_test.rs` (`test_full_planning_workflow_simple_linear`, `_with_parallel_tasks`, `_max_subtasks_enforcement`) exercises Layer 1 planning in isolation only, run during this task |
| REQ-handoff-execution-integration | superseded by shipped code | `HandoffService::execute_handoff()` (`handoff_service.rs:466`) delegates to the specialist via an injected `HandoffExecutor`, tracks the chain in a `HandoffRecord` with `depth` (`:498-502`), and the specialist result flows back as the tool response (`paladin_execution_service.rs:809-816`) — all exercised by the 27/27 `handoff_service_test` run cited in Epic 14's cluster `5.0`/`6.0` rows. Circular-handoff and max-depth are enforced and tested (`test_validate_handoff_circular_delegation`, `test_validate_handoff_max_depth`, part of that same 27/27 run). Retry ships with exactly the PRD's defaults — `HandoffRetryConfig { max_retries: 3, initial_backoff_ms: 1000, backoff_multiplier: 2.0 }` (`autonomous_config.rs:27-34`) — and classifies transient vs. permanent errors before retrying (`is_transient_error`, `handoff_service.rs:531-536`), exercised by the retry loop at `:505-553`. **Two elements of the PRD's spec are absent and recorded as authoritative divergence:** no circuit-breaker integration exists in `handoff_service.rs` (`grep -n 'CircuitBreaker\|circuit_breaker'` → zero matches, run during this task) despite being a MUST; and the specific `E-HANDOFF-001`..`005`/`E-PLAN-001`/`E-PROMPT-001`/`E-CONFIG-001`/`E-CONFIG-002` error-code taxonomy does not exist anywhere in the tree (`grep -rn 'E-HANDOFF\|E-PLAN-\|E-PROMPT-\|E-CONFIG-' --include=*.rs .` → zero matches, run during this task) — the shipped `HandoffError` enum (`handoff_error.rs:26-90`) uses differently-named variants instead (`InvalidAgent` rather than `SpecialistNotFound`; `CircularHandoff`, `MaxDepthExceeded`, `ExecutionFailed` match by behavior but not by code string; no `InvalidResponse` variant exists at all) |
| REQ-autonomous-completion-config-schema | superseded by shipped code | The later position on `REQ-autonomous-configuration` (Epic 14, filled by plan 05-06) — both preserved per this ID's own description. The same shipped config artefact both rows cite is `AutonomousConfig` (`autonomous_config.rs:70-82`): `planning`/`prompt_generation`/`dynamic_temperature`/`handoffs` sub-configs, all opt-in (`enabled: false` defaults, `:69` `#[derive(..., Default)]`), validated at build time (`AutonomousConfig::validate()`, `:96-130`, called from `PaladinBuilder::build()`'s own `self.validate()` per Epic 14's `REQ-autonomous-configuration` citation). `HandoffConfig` (`:261-274`) ships `enabled`/`strategy`/`max_depth` (default 5, matching this ID)/`retry` — but **without** the PRD's `concurrent`, `history.{max_records: 100, eviction: oldest_first}`, `on_specialist_unavailable`, or an inline `specialists` list field (`grep -rn 'concurrent\|on_specialist_unavailable' crates/paladin-core/src/platform/container/autonomous_config.rs` → zero matches, run during this task; specialists are instead supplied out-of-band via `PaladinBuilder::with_handoffs(Vec<Arc<Paladin>>)`, not as config data). `PlanningConfig` (`:135-141`) has no `validate_at` field — validation timing is not configurable, it simply always happens at `AutonomousConfig::validate()`'s call site (build time), which happens to satisfy the *effect* the PRD's `validate_at: planning_time` names without shipping the field itself. Configuration errors do fail fast at build time (`PaladinBuilder::build()` returns `Err` from `self.validate()` before construction completes) — exercised by `cargo test --offline -p paladin-ai-core paladin_config::tests::test_paladin_config_autonomous_validation` — 1/1 passed (part of the 2/2 run cited in Epic 14's `REQ-autonomous-configuration` row) |
| REQ-autonomous-completion-quality-gates | deferred with reason | Transcribed byte-identical from the amended `.planning/decisions/0006-coverage-gate.md` Phase 5 amendment's module-scoped gate table, not re-measured: autonomous components measure **92.80%** line-weighted aggregate coverage (`planning_service.rs` 577 lines/43 missed/92.55%, `prompt_generation_service.rs` 234/26/88.89%, `temperature_service.rs` 394/23/94.16%, `handoff_service.rs` 239/12/94.98%, aggregate `(1444-104)/1444 = 92.80%`) against the **≥ 90%** target — the aggregate clears the target by ~2.8 points, though `prompt_generation_service.rs` individually sits ~1.1 points below it. **Owner: Phase 15 / PIPE-02** (unchanged from ADR-0006). No `cargo llvm-cov` or `cargo tarpaulin` ran during this task. The requirement's "zero remaining TODO comments in autonomous agent code" clause is **contradicted** if Grove counts as in scope: `crates/paladin-battalion/src/grove_service.rs:537` carries `// TODO: Make configurable` on the hardcoded-model defect the Epic 22 `REQ-grove-llm-routing` row already records as `genuinely outstanding` (owner Phase 6/CLOSE-01) — cross-referenced rather than duplicated. **A second TODO not previously recorded anywhere in this ledger** sits inside this epic's own scope: `src/application/services/paladin/planning_service.rs:433` (`// TODO: Ask LLM for expected output`, see `REQ-autonomous-configurable-model`'s row above), so the "zero remaining TODO" clause is unmet even under the narrowest in-scope reading |

### Epic 22 — Battalion & Commander Hardening (10 IDs)

**Block verdict — `tasks-epic22-battalion-commander-hardening.md`: satisfied by shipped code.** All
fifteen parent-task clusters (`0.0`–`14.0`) verify against the current tree, including the three the
source task list still marks open (`3.0`, `4.0`, `5.0`) — Council registry integration, Grove
registry integration and Grove's LLM-based routing all shipped, with passing tests, via commits
`761c49c`, `0cdf8dd` and `5f05db7` respectively; the task list's own checkboxes were simply never
updated. Per D-06, a block verdicts `satisfied by shipped code` only if every cluster verifies; that
bar is met here, so Phase 6's CLOSE-02 has **no work required** for this block.

`.planning/intel/task-completion-state.md` records **81 open** items for this task list (both the
`Milestone_3-Completion` section total and the corpus-wide top-line ranking cite the same figure) —
the largest of the three VERIFY-02 blocks. Transcribed here, not re-derived: every one of the 81 open
checkboxes sits under the three parent-task clusters (`3.0`, `4.0`, `5.0`) verified below, plus the
six explicitly-deferred `14.x` documentation sub-items the source file itself records in its own
"Deferred/Optional Tasks from Task 14.0" section (`tasks-epic22-battalion-commander-hardening.md:446-469`)
— none of the 81 represents unverified forward work once the parent-task capability claims are
checked against the tree.

| Parent task | Verdict | Evidence |
|---|---|---|
| 0.0 Create feature branch | satisfied | Development-process step, not a shipped-code capability. The epic's own commit trail confirms the branch produced this work: `git log --oneline --all --grep "Epic 22"` returns commits `019b1e8`, `bdc63fd`, `b7152df`, `761c49c`, `0cdf8dd`, `5f05db7`, `f9b408d`, all merged into the current tree, run during this task |
| 1.0 Define Paladin Registry trait and implementation (US-22.1 Foundation) | satisfied | `PaladinRegistry` trait (`register`/`get`/`contains`/`list_ids`) at `crates/paladin-ports/src/output/paladin_registry.rs:94`; `RegistryError` at `crates/paladin-core/src/platform/container/registry_error.rs:10`; `HashMapPaladinRegistry` struct at `crates/paladin-battalion/src/in_memory_registry.rs:64`, `impl PaladinRegistry` at `:175`; exercised by `cargo test --offline -p paladin-battalion in_memory_registry::` — 9/9 passed, and `cargo test --offline -p paladin-ports paladin_registry` — 2/2 passed, both run during this task |
| 2.0 Extend error types for Battalion operations | satisfied | `BattalionError::PaladinNotFound`/`GroveRoutingFailed`/`MetadataExportFailed` at `crates/paladin-core/src/platform/container/battalion/mod.rs:813,817,821`, `From<RegistryError>` conversion; exercised by `cargo test --offline -p paladin-ai-core battalion::` — 93/93 passed (including `test_battalion_error_variants`, `test_registry_error_conversion`, `test_new_error_messages_format`), run during this task |
| 3.0 Integrate Paladin Registry into Council service (US-22.1) | satisfied by shipped code | Checkbox unchecked in the source file, but the capability shipped: `registry: Arc<dyn PaladinRegistry>` field at `crates/paladin-battalion/src/council_service.rs:65-66`, resolution logic (`self.registry.get(participant_id)`, returning `PaladinNotFound` on miss) at `:130-160`; shipped via commit `761c49c` "feat: integrate Paladin Registry into Council service"; exercised by `cargo test --offline -p paladin-battalion council_service::` — 10/10 passed including `test_council_resolves_participants` and `test_council_paladin_not_found_error`, run during this task. This is the dominant corpus pattern: the record understates the tree |
| 4.0 Integrate Paladin Registry into Grove service (US-22.1) | satisfied by shipped code | Checkbox unchecked in the source file, but the capability shipped: `registry: Arc<dyn PaladinRegistry>` field at `crates/paladin-battalion/src/grove_service.rs:104-105`, resolution at `:184-190`; Commander populates a per-execution registry before routing to Council (`commander.rs:579-592`) and Grove (`:637-678`); shipped via commit `0cdf8dd` "feat: integrate Paladin Registry into Grove service"; exercised by `cargo test --offline -p paladin-battalion grove_service::` — 15/15 passed including `test_grove_resolves_routed_agent` and `test_grove_paladin_not_found_error`, run during this task |
| 5.0 Implement Grove LLM-based routing (US-22.2) | satisfied by shipped code | Checkbox unchecked in the source file, but the capability shipped in full: `route_by_llm` at `crates/paladin-battalion/src/grove_service.rs:479`, `RoutingResponse` struct at `:60`, `routing_fallback`/`min_confidence` config fields at `crates/paladin-core/src/platform/container/battalion/grove.rs:233,240` with validation exercised by `test_grove_builder_validation_invalid_fallback`/`_invalid_threshold`; shipped via commit `5f05db7` "feat: implement Grove LLM-based routing (Task 5.0)"; exercised by `cargo test --offline -p paladin-battalion grove_service::` — 15/15 passed including all four PRD-named TDD tests (`test_route_with_llm_successful`, `test_route_with_llm_low_confidence`, `test_route_with_llm_invalid_json`, `test_route_with_llm_fallback_to_keyword`), run during this task. **Caveat, not a cluster failure:** `grove_service.rs:537` hardcodes `model: "gpt-4".to_string()` — a real, separately-tracked defect (`codebase/CONCERNS.md` §"Grove Service Model Hardcoded") already owned by **Phase 6 / CLOSE-01** (see the `REQ-grove-llm-routing` row below); it does not defeat this cluster's own capability claim (LLM-based routing with a confidence threshold and configurable fallback), which ships and is fully tested |
| 6.0 Extend BattalionMetadata for enhanced metrics (US-22.3) | satisfied | `TokenUsage` struct at `crates/paladin-core/src/platform/container/battalion/mod.rs:497`; `BattalionMetadata.per_paladin_times: HashMap<String, u64>` at `:584`, `.per_paladin_tokens: HashMap<String, TokenUsage>` at `:587`, `.total_tokens: u64` at `:590` — relocated from the PRD's `battalion/battalion_result.rs` per this ledger's head-note path caveat, not a fresh divergence; exercised by `cargo test --offline -p paladin-ai-core battalion::` (`test_battalion_metadata_serialization`, `test_token_usage_aggregation_calculation`), part of the 93/93 run above |
| 7.0 Implement Phalanx per-paladin metrics collection (US-22.3) | satisfied | Per-Paladin timing/token collection loop at `crates/paladin-battalion/src/phalanx_service.rs:264-300`; exercised by `cargo test --offline -p paladin-battalion phalanx_service::` — 14/14 passed including `test_phalanx_per_paladin_timing`, `test_phalanx_per_paladin_tokens`, `test_phalanx_metrics_with_partial_failures`, run during this task |
| 8.0 Add Commander metadata export configuration (US-22.4) | satisfied | `metadata_output_dir: Option<PathBuf>` field on `BattalionConfig` at `crates/paladin-core/src/platform/container/battalion/mod.rs:54` (the PRD's `CommanderConfig` never existed — already a settled fact per the *Milestone 1 as-shipped ledger*'s RECON-03 resolution, not re-decided here), `validate_metadata_dir()` at `:123`; exercised by `cargo test --offline -p paladin-ai-core battalion::` (`test_battalion_config_with_metadata_dir`, `test_battalion_config_metadata_dir_auto_creates`) and `cargo test --offline -p paladin-battalion commander::` (`test_commander_build_with_valid_metadata_dir`, `test_commander_build_without_metadata_dir`) — all pass |
| 9.0 Implement Commander metadata export logic (US-22.4) | satisfied | `export_metadata()` at `crates/paladin-battalion/src/commander.rs:880`; exercised by `cargo test --offline -p paladin-battalion commander::` — 50/50 passed including `test_metadata_export_creates_file`, `test_metadata_export_correct_naming`, `test_metadata_export_json_structure`, `test_metadata_export_no_dir_configured`, run during this task |
| 10.0 Create MockLlmAdapter test infrastructure (US-22.5) | satisfied | `MockLlmAdapter` struct at `tests/helpers/mock_llm_adapter.rs:66` with `add_response`/`add_success`/`add_failure`/`call_count`/`reset` at `:81,86,91,110,134`, helper functions `create_test_paladin_with_mock`/`create_mock_with_responses` at `:331,354`; exercised by `cargo test --offline -p paladin-ai --test lib -- helpers::mock_llm_adapter` — 10/10 passed, run during this task, including the three PRD-named tests `test_mock_llm_adapter_returns_configured_responses`, `test_mock_llm_adapter_tracks_call_count`, `test_mock_llm_adapter_handles_failures`. **New finding (plan 05-05):** this file carries no explicit `[[test]]` entry in `Cargo.toml`; it is reachable only because `tests/lib.rs` (which declares `pub mod helpers; pub mod integration; pub mod unit;`) is itself auto-discovered by Cargo's default `tests/*.rs` convention as target `lib`. The capability and its tests are real and passing; the wiring is implicit rather than declared, so renaming or deleting `tests/lib.rs` would silently stop compiling roughly 700 tests with no `Cargo.toml` diff to flag it |
| 11.0 Enable and fix Campaign and ChainOfCommand tests (US-22.5 Phase 1) | satisfied | `test_execute_routes_to_campaign_service` and `test_execute_routes_to_chain_service` (renamed from the PRD's `test_execute_campaign`/`test_execute_chain_of_command`) at `crates/paladin-battalion/src/commander.rs:2006,2035`; zero `#[ignore]` attributes remain in the file (`grep -c '#\[ignore\]' crates/paladin-battalion/src/commander.rs` → `0`, run during this task); exercised by `cargo test --offline -p paladin-battalion commander::` — both pass, part of the 50/50 run above |
| 12.0 Enable and fix error handling tests (US-22.5 Phase 2) | satisfied | `test_error_handling_fail_fast`, `test_error_handling_continue_on_error`, `test_error_handling_retry_then_continue`, `test_partial_failure_handling` at `crates/paladin-battalion/src/commander.rs:3041,3077,3110,3150`, none `#[ignore]`d; exercised by `cargo test --offline -p paladin-battalion commander::` — all four pass, part of the 50/50 run above |
| 13.0 Integration testing and final validation | satisfied | Integration suite relocated from the PRD's single `tests/integration/battalion_integration_tests.rs` into `tests/integration/battalion/{campaign,chain_of_command,council,formation,grove,load,phalanx}_integration_test.rs` plus three top-level `tests/integration/battalion_*.rs` files — a path relocation, not a capability gap; wired in via `tests/lib.rs`'s `pub mod integration;`; exercised by `cargo test --offline -p paladin-ai --test lib -- integration::battalion` — 76/76 passed, run during this task |
| 14.0 Update documentation and examples | satisfied | `examples/commander_with_metadata_export.rs` builds clean (`cargo build --offline --example commander_with_metadata_export -p paladin-ai`, exit 0, run during this task) and demonstrates `metadata_output_dir`, `per_paladin_times`, `per_paladin_tokens`; `CHANGELOG.md:1111-1174` carries a full "Epic 22: Battalion & Commander Hardening" section. **Caveat:** the PRD's `docs/BATTALION.md`/`docs/COMMANDER.md` targets don't exist at those paths — Milestone 11's docs overhaul relocated Battalion documentation into `docs/src/{appendix,user-guides,deployment-topologies}/battalion-*.md`, and none of those mdbook pages mention `metadata_output_dir`, `per_paladin_times`, or Paladin Registry by name; the source task list's own "Deferred/Optional Tasks from Task 14.0" section (lines 446-469) records six documentation sub-items as explicitly deferred. The working example plus the CHANGELOG entry clear the D-01 evidence bar; the mdbook content gap is real but does not defeat the "documentation and examples exist" capability claim, which a different, working artefact satisfies |

| ID | Verdict | Evidence |
|---|---|---|
| REQ-paladin-registry-port | satisfied | `PaladinRegistry` trait at `crates/paladin-ports/src/output/paladin_registry.rs:94` (`register`/`get`/`contains`/`list_ids` methods, `Send + Sync` bound); exercised by `cargo test --offline -p paladin-ports paladin_registry` — 2/2 passed (`test_trait_is_object_safe`, `test_registry_error_display`) and by `cargo test --offline -p paladin-battalion in_memory_registry::` — 9/9 passed against the trait's own implementor, both run during this task |
| REQ-paladin-registry-adapter | satisfied | `HashMapPaladinRegistry` struct at `crates/paladin-battalion/src/in_memory_registry.rs:64`, `impl PaladinRegistry for HashMapPaladinRegistry` at `:175` (thread-safe via `RwLock<HashMap<String, Arc<Paladin>>>`); exercised by `cargo test --offline -p paladin-battalion in_memory_registry::` — 9/9 passed including `test_registry_thread_safety`, run during this task |
| REQ-council-grove-registry-resolution | satisfied | Registry resolution shipped in both consumers despite unchecked source-file checkboxes (see the parent-task cluster table above, rows `3.0`/`4.0`): `crates/paladin-battalion/src/council_service.rs:130-160` and `crates/paladin-battalion/src/grove_service.rs:184-190`; exercised by `cargo test --offline -p paladin-battalion council_service::` — 10/10 passed and `cargo test --offline -p paladin-battalion grove_service::` — 15/15 passed, both run during this task |
| REQ-grove-llm-routing | genuinely outstanding | `crates/paladin-battalion/src/grove_service.rs:537` hardcodes `model: "gpt-4".to_string(), // TODO: Make configurable` in production code (the `#[cfg(test)]` boundary begins at `:732`); Grove's LLM-based routing capability itself ships and is fully tested (`route_by_llm` at `:479`, all 4 named TDD tests passing per the cluster table's `5.0` row), but the requirement's specific claim — that routing honours the Paladin's *configured* LLM provider — is not met; the model is hardcoded regardless of configuration. Verified open against the tree during this task (`codebase/CONCERNS.md` §"Grove Service Model Hardcoded"). **Owner: Phase 6 / CLOSE-01.** Not fixed here, per this phase's prohibition on editing `.rs` files |
| REQ-phalanx-per-paladin-metrics | satisfied | Per-Paladin timing/token collection at `crates/paladin-battalion/src/phalanx_service.rs:264-300`, populating `BattalionMetadata.per_paladin_times`/`.per_paladin_tokens`/`.total_tokens`; exercised by `cargo test --offline -p paladin-battalion phalanx_service::` — 14/14 passed including `test_phalanx_per_paladin_timing`, `test_phalanx_per_paladin_tokens`, `test_phalanx_metrics_with_partial_failures`, run during this task |
| REQ-battalion-metadata-extension | satisfied | `TokenUsage` struct at `crates/paladin-core/src/platform/container/battalion/mod.rs:497`, `BattalionMetadata.per_paladin_times`/`.per_paladin_tokens`/`.total_tokens` at `:584,587,590` — relocated from the PRD's `battalion/battalion_result.rs` to `battalion/mod.rs`, treated under this ledger's head-note path caveat rather than a fresh divergence; exercised by `cargo test --offline -p paladin-ai-core battalion::` — 93/93 passed including `test_battalion_metadata_serialization` and `test_token_usage_aggregation_calculation`, run during this task |
| REQ-commander-metadata-export | satisfied | `export_metadata()` at `crates/paladin-battalion/src/commander.rs:880`, called after each Battalion execution; exercised by `cargo test --offline -p paladin-battalion commander::` — 50/50 passed including `test_metadata_export_creates_file`, `test_metadata_export_correct_naming`, `test_metadata_export_json_structure`, run during this task, plus the working `examples/commander_with_metadata_export.rs` (builds clean, exit 0) |
| REQ-commander-config-metadata-dir-v3 | satisfied | `metadata_output_dir: Option<PathBuf>` field and `validate_metadata_dir()` on `BattalionConfig` at `crates/paladin-core/src/platform/container/battalion/mod.rs:54,123` (shipped as `BattalionConfig`, not the PRD's `CommanderConfig`, which never existed — already a settled fact per the *Milestone 1 as-shipped ledger*'s RECON-03 resolution, not re-decided here); exercised by `cargo test --offline -p paladin-ai-core battalion::` (`test_battalion_config_with_metadata_dir`, `test_battalion_config_metadata_dir_auto_creates`) and `cargo test --offline -p paladin-battalion commander::` (`test_commander_build_with_valid_metadata_dir`, `test_commander_build_without_metadata_dir`), both run during this task |
| REQ-commander-test-hardening | satisfied | Zero `#[ignore]` attributes measured in `crates/paladin-battalion/src/commander.rs` (`grep -c '#\[ignore\]' crates/paladin-battalion/src/commander.rs` → **0**, run during this task) — every test the PRD names for enablement is present and un-ignored: `test_execute_routes_to_campaign_service`/`test_execute_routes_to_chain_service` at `commander.rs:2006,2035`, `test_error_handling_fail_fast`/`test_error_handling_continue_on_error`/`test_error_handling_retry_then_continue`/`test_partial_failure_handling` at `:3041,3077,3110,3150`; exercised by `cargo test --offline -p paladin-battalion commander::` — 50/50 passed, 0 ignored, run during this task |
| REQ-grove-config-v2 | satisfied | `GroveConfig.routing_fallback: String` and `.min_confidence: f32` at `crates/paladin-core/src/platform/container/battalion/grove.rs:233,240`, with builder-level validation (not a bare `.validate()` method as the PRD sketched, but functionally equivalent); exercised by `cargo test --offline -p paladin-ai-core grove::` — 15/15 passed including `test_grove_builder_validation_invalid_fallback` and `test_grove_builder_validation_invalid_threshold`, run during this task |

### Epic 23 — CLI, Config & Infrastructure Completion (10 IDs)

**Epic-level note.** Epic 23 is the most reliably complete epic in the run-2 corpus by two
independent signals: the Milestone 3 completion plan was edited in place to mark it done while its
five sibling epics remained unchecked, and its own source task list carries the milestone's lowest
open-item count — `.project/Milestone_3-Completion/Epic_23/tasks-task46-arsenal-tool-integration-tests.md`
records **1 open** item, against 81 (Epic 22), 29 (Epic 24), 12 (Epic 21), 5 (Epic 20) and 4 (Epic
19) for its siblings, transcribed from `.planning/intel/task-completion-state.md`'s Milestone
3-Completion section, not re-derived. This plan's own findings below complicate that reputation in
one respect: four of the ten `tests/cli/` test files this epic's own scope produced (66 test
functions total) are currently disconnected from any compiled test target — see
`REQ-cli-garrison-configuration`, `REQ-cli-arsenal-configuration` and
`REQ-cli-tiered-environment-testing` below.

| ID | Verdict | Evidence |
|---|---|---|
| REQ-cli-garrison-configuration | present, unproven | `instantiate_garrison()` at `src/application/cli/config/loader.rs:113` supports `garrison.type: in_memory|sqlite` (`:122-129` validation) with type-specific settings nested under `garrison.config.{max_entries, path}` (`GarrisonTypeConfig`, `paladin_config.rs:110-119`) — a nested schema, not the PRD's flat `garrison.{type, path, max_entries, ttl_seconds}`, and **no `ttl_seconds` field exists anywhere in the tree** (`grep -rn ttl_seconds src/application/cli/` → zero matches, run during this task). `in_memory` instantiates `InMemoryGarrison::new()` (`:149`); `sqlite` instantiates `SqliteGarrison::connect()` (`:177`) with parent-directory auto-creation (`:163-174`) and a required-path error (`:154-160`, `"garrison.config.path is required for type: sqlite"`, matching the PRD's actionable-message bar). The TODO the PRD names at `agent.rs` line 293 is gone — the call site now reads `let garrison = instantiate_garrison(&config.garrison, &config.name).await?;` at `agent.rs:300`, though the garrison is passed to `PaladinExecutionService::new()` (`:307`) rather than to `PaladinBuilder` as the PRD's own wording specifies — an architectural-layer difference, not a missing wire. **Unproven, per the D-01 bar:** `tests/cli/garrison_config_test.rs`'s 8 tests (`test_in_memory_garrison_config`, `test_sqlite_garrison_config`, `test_invalid_garrison_type`, `test_sqlite_missing_path`, `test_none_garrison_config`, `test_garrison_default_values`, `test_sqlite_directory_creation`, `test_eviction_strategy_config`) are the only tests exercising `instantiate_garrison()` anywhere in the tree, and `tests/cli/mod.rs:11-14` **comments out `mod garrison_config_test;`** — the file is never compiled into the `cli` `[[test]]` binary or any other target (`grep -rln instantiate_garrison tests/ src/` confirms zero other call sites, run during this task). `cargo test --offline --features cli --test cli` — 106/106 passed, none from this file (verified during this task: it does not appear in the 106) |
| REQ-cli-arsenal-configuration | present, unproven | `instantiate_arsenal()` at `loader.rs:216` supports `arsenal.mcp_servers[]` with `type: stdio|streamable_http` (`:236`) — **`sse` is retired** and fails loud with a migration message (`paladin_config.rs:136-137`), not the PRD's `type: stdio|sse`; `streamable_http` uses `endpoint`/`auth_token_env` (an env-var *name*, resolved host-side, `:298-308`), not the PRD's `url`/`auth_token` literal-secret shape — a deliberate, safer divergence (D-02/D-03 per the module's own comment at `:211-215`), recorded as authoritative. Discovered tools ARE registered with name/description/schema via `registry.register(tool)` (`:280`) and a live client wired for invocation (`service.register_client()`, `:282`). **No generic `${VAR}` substitution exists** (`grep -rn '\$\{' loader.rs` → zero matches, run during this task) and **no `arsenal.connection_timeout_seconds` field exists** on `ArsenalConfig` (`paladin_config.rs:122-125`, run during this task). Partially proven: the `streamable_http` error paths ARE exercised by real, currently-compiled tests — `cargo test --offline --features cli --lib -- application::cli::config` — 28/28 passed, including `test_instantiate_arsenal_streamable_http_missing_endpoint_errors`, `_missing_auth_token_env_var_errors`, `_attempts_connect_when_configured` (`loader.rs:564,590,623`), run during this task. **The `stdio` instantiation path, full tool-registration flow and the broader validation surface are unproven**: `tests/cli/arsenal_config_test.rs`'s 10 tests (`test_stdio_mcp_server_config`, `test_sse_mcp_server_config`, `test_multiple_mcp_servers`, `test_missing_name`, `test_invalid_server_type`, `test_stdio_missing_command`, `test_sse_missing_endpoint`, `test_sse_invalid_url_format`, `test_none_arsenal_config`, `test_empty_mcp_servers`) are the only other tests touching `instantiate_arsenal()`, and `tests/cli/mod.rs:13` comments out `mod arsenal_config_test;` — never compiled, same disconnection pattern as `REQ-cli-garrison-configuration` above |
| REQ-mock-llm-adapter | satisfied, with caveat | `MockLlmAdapter` at `tests/helpers/mock_llm_adapter.rs:66` implements `LlmPort` (`generate`/`generate_stream`/`validate_model`) with all four response modes: `MockResponse::{Text, ToolCall, Streaming, Error}` (`:35-46`), configured via `add_success()`/`add_tool_call()`/`add_failure()`/`add_streaming()` (`:86,96,92,104`) — functionally the PRD's `.with_response`/`.with_tool_call`/`.with_error`/`.with_streaming`, but shipped as `add_*` mutating methods on `&self` rather than `.with_*` chainable builders, a naming-convention divergence not a functional gap. Invocation recording captures `prompt`/`model`/`timestamp`/`metadata` per call (`Invocation` struct, `:22-30`) plus `call_count()` (`:110`) and `invocations()` (`:115`). This is the same artefact Epic 22's cluster `10.0` row already cites in full; reused (not duplicated) via `tests/cli/helpers.rs`'s `#[path]` shim (`:22-23`) so the `cli` `[[test]]` target resolves the same file. Exercised by `cargo test --offline --features cli --test cli -- helpers::shared::mock_llm_adapter` — 10/10 passed (`test_mock_llm_adapter_returns_configured_responses`, `_tracks_call_count`, `_handles_failures`, `_tool_call`, `_streaming_chunks`, `_invocation_tracking`, `_reset`, `test_create_mock_with_responses`, `test_create_mock_with_tool_calls`, `test_create_test_paladin_with_mock`), run during this task. Mock-based tests run with no API keys and no network calls by construction (pure in-process `LlmPort` impl) — the broader integration-scenario claim (single-Paladin/Formation/Phalanx/error-handling/tool-integration from YAML) is proven by `tests/cli/{paladin_execution_test,formation_execution_test,phalanx_execution_test,error_handling_test,tool_integration_test}.rs`, all wired and passing as part of the same 106/106 `cli` test run |
| REQ-cli-tiered-environment-testing | present, unproven | The Tier-1 suites under `tests/cli/` exist as real, well-formed test files, but a significant share of them are currently disconnected from any compiled test target — the same orphaning pattern as `REQ-cli-garrison-configuration`/`REQ-cli-arsenal-configuration` above, and a genuine gap this ledger's D-01 bar exists to surface. `tests/cli/mod.rs:6-14` comments out four modules: `arsenal_config_test` (10 tests), `environment_tests` (45 tests), `garrison_config_test` (8 tests), `integration_tests` (3 tests) — 66 written test functions, zero of them compiled into the `cli` `[[test]]` binary or any other target (`grep -c 'fn test_'` per file, run during this task). **`environment_tests.rs` alone carries the PRD's own named edge cases** — `test_config_with_empty_system_prompt`, `test_config_with_very_large_system_prompt`, `test_invalid_yaml_syntax_error`, `test_malformed_yaml_with_tabs`, `test_concurrent_config_loading` — and its non-interactive/NO_COLOR-specific assertions (`test_no_color_environment_detection`, `test_ci_environment_line_buffering`, `test_interactive_prompt_detects_non_tty`, `test_confirm_detects_non_tty`, `test_config_loading_is_non_interactive`) have **no equivalent anywhere in a currently-compiled target** (`grep -rln 'NO_COLOR\|non_interactive' tests/cli/*.rs` returns only `environment_tests.rs` and `help_output_test.rs`'s unrelated `ensure_no_color()` snapshot-determinism helper, run during this task). What IS wired and passing overlaps partially: `error_handling_test.rs` (compiled, part of the 106/106 `cli` run) independently covers empty-input, very-large-input and malformed-YAML edge cases (`test_empty_input_string`, `test_very_large_input`, `test_invalid_yaml_config_structure`) — but not concurrent-loading or NO_COLOR/non-interactive detection, which exist only in the orphaned file. **Tier 2** (Docker-gated) exists and is properly gated: `tests/integration/cli_real_services_test.rs:7` states "All tests are marked `#[ignore]`", confirmed (`test_redis_service_reachable`, `test_setup_check_redis_connection`, `test_setup_check_minio_connection`, verified by inspection during this task — no Docker in this environment to execute them). **Tier 3's contested skip semantics are settled by `.planning/decisions/0012-live-api-test-key-behaviour.md`**: the shipped panic in `require_api_key` stands, the double gate (`#[cfg(feature = "live-api-tests")]` plus 13 `#[ignore]` attributes) is what actually supplies the graceful default-run skip, and the doc-comment correction is named Phase 6 **CLOSE-03**'s scope — not re-litigated here. Coverage-target and CI-duration criteria are not re-measured, consistent with this plan's scope |
| REQ-scheduler-port | superseded by shipped code | `SchedulerPort` (`crates/paladin-ports/src/output/scheduler_port.rs:237`) exposes `schedule_job(JobSpec) -> Result<JobId, SchedulerError>` (`:247`), `cancel_job(&JobId)` (`:253`) and `get_job_status(&JobId)` (`:256`) — the PRD's own trait sketch names the identical three method names, but with owned `JobId` parameters rather than the shipped `&JobId` borrows, and the shipped trait is a larger surface than the PRD's minimal three: it adds `start()`/`shutdown()`/`get_job_info()`/`list_jobs()` (`:239,242,259,262`). **The shipped `tokio-cron-scheduler` version is `0.13`** (`Cargo.toml:136`, locked at `0.13.0` in `Cargo.lock`), not the PRD's pinned `"0.9"` — read directly from the tree, not transcribed from the PRD. **A previously-unrecorded finding: two independent, non-duplicate `TokioCronSchedulerAdapter` implementations ship simultaneously** — `crates/paladin-storage/src/scheduler.rs:178` (feature-gated behind `paladin-storage`'s `scheduler` feature, `engine`/`registrations`/`running` fields, `try_new()` constructor) and `src/infrastructure/adapters/scheduling/tokio_cron_adapter.rs:71` (unconditionally compiled into the root crate, `inner`/`jobs`/`running` fields, `new()` constructor, at the exact path the PRD names) — both real `impl SchedulerPort` blocks, both independently tested. Exercised by `cargo test --offline -p paladin-storage --features scheduler scheduler::` — 16/16 passed (7 unignored + 9 `--ignored`, the latter requiring the real engine), and the `src/infrastructure` copy's own 14 inline tests pass as part of the workspace's default unit-test run. `scheduler.{max_concurrent_jobs, retry_failed_jobs, max_retries, retry_delay_seconds}` from the PRD does not exist — the shipped `SchedulerConfig` (`src/config/scheduler.rs:10-19`) instead carries `enabled`/`default_cron`/`channel_size`, a materially different configuration surface recorded as authoritative |
| REQ-content-deliverer-scheduling | satisfied | The `unimplemented!("Scheduler integration pending")` stub the PRD names is gone; `ApiContentDeliverer::schedule_delivery()` (`crates/paladin-web/src/adapters/api_content_deliverer.rs:309`, re-exported at the PRD's historical path `src/infrastructure/adapters/output/mod.rs:2` — `pub use paladin_web::adapters::api_content_deliverer;` — a relocation-via-re-export, not a divergence, per this ledger's D-04 caveat) creates real jobs through an injected `Arc<dyn SchedulerPort>` (`:20,67`) and returns a `JobId`-tracked response; `cancel_delivery()` (`:375-419`) cancels the underlying scheduler job. Exercised by `tests/integration/scheduler_integration_test.rs` (requires `--features web-server`) — `cargo test --offline -p paladin-ai --test lib --features web-server -- scheduler_full_lifecycle scheduler_job_info deliverer_schedule deliverer_cancel deliverer_without_scheduler` — 5/5 passed (`test_scheduler_full_lifecycle`, `test_scheduler_job_info_persists_metadata`, `test_deliverer_schedule_with_scheduler`, `test_deliverer_cancel_with_scheduler`, `test_deliverer_without_scheduler_still_works`), run during this task, covering job creation, cancellation and the deliverer/scheduler bridge with a mock `SchedulerPort` (not a short-delay real-timing test — the PRD's specific "schedule with a 2-second delay and verify execution timing" scenario is not present, a narrower gap than the stub it replaces). **Re-derived per this task's explicit instruction, not transcribed from the run-2 ledger:** `grep -rn 'unimplemented!' crates/ src/` returns 27 occurrences across 13 files; **every single one sits inside a `#[cfg(test)]` module** (confirmed individually against each file's own `#[cfg(test)]` boundary line — `council_service.rs:521`, `grove_service.rs:732`, `campaign_service.rs:371`, `chain_of_command_service.rs:452`, `maneuver/service.rs:477`, `commander.rs:1557`, `vision_llm_port.rs:141`, `paladin_builder.rs:1354`, `planning_service.rs:569`, `paladin_execution_service.rs:1945`, `prompt_generation_service.rs:227`, `battalion_processor.rs:206`, `battalion_port.rs:808`) or inside a doc-comment code example (`chain_of_command_service.rs:23,24,80`, `//! # let paladin_port = unimplemented!();`) — **zero occurrences in production code paths anywhere in the tree**, confirming this specific requirement's criterion is met, contrary to what the plan's own phrasing suggested might be found |
| REQ-cli-error-types | superseded by shipped code | `CliError` (`src/application/cli/error.rs:21`) has **no `#[from]` attribute on any variant at all** (`grep -c '#\[from\]' error.rs` → 0, run during this task) — none of the requirement's three named variants (`GarrisonError(#[from] GarrisonError)`, `ArsenalError(#[from] ArsenalError)`, `SchedulerError(#[from] SchedulerError)`) exist in that shape. Instead, `CliError` carries `GarrisonConfigError { message: String }` (`:114-115`) and `ArsenalConfigError { message: String }` (`:118-119`) — string-message variants populated by manual `.map_err(\|e\| CliError::GarrisonConfigError { message: format!(...) })` conversions at each call site (`loader.rs:123-128,158-160,179-181,243-248,257-262,268-273,289-296`) rather than a typed `#[from]` conversion — and **no `SchedulerError`-carrying `CliError` variant exists at all** (`grep -rn SchedulerError src/application/cli/` → zero matches, run during this task; scheduler-adjacent CLI errors, if any occur, are not distinguished from other error classes). Configuration errors remain distinguishable from network/validation/user errors by construction (25 distinct variants spanning `ConfigFileNotFound`, `InvalidYaml`, `ValidationError`, `LlmError`, `McpConnectionError`, `Cancelled`, etc., `:21-136`), and all pre-existing variants are preserved — no variant was removed or renamed to accommodate the new config paths |
| REQ-mock-arsenal-port | satisfied | `MockArsenalPort` at `tests/helpers/mock_arsenal_adapter.rs:40` implements `ArsenalPort` (`list_armaments`/`invoke`/`validate_call`, `:185-223` plus the trait's `validate_call` requirement) with a configurable armament list, per-tool keyed `invoke()` responses, `set_error_variant()` for specific `ArsenalError` variants (`:122`), and invocation recording via `call_count()` (`:133`). It is `Send + Sync` and used as `Arc<dyn ArsenalPort>` throughout `tests/cli/tool_integration_test.rs`. It lives exactly at the PRD's named path, `tests/helpers/mock_arsenal_adapter.rs`, alongside `MockLlmAdapter` in the same directory — reused (not duplicated) by the `cli` `[[test]]` target via `tests/cli/helpers.rs`'s `#[path = "../helpers/mod.rs"] mod shared;` re-export shim (`:22-23`), the same pattern `REQ-mock-llm-adapter`'s row above documents. Exercised by `cargo test --offline --features cli --test cli -- helpers::shared::mock_arsenal_adapter` — 9/9 passed (`test_mock_arsenal_default`, `test_mock_arsenal_new_is_empty`, `test_mock_arsenal_add_tool`, `test_mock_arsenal_invoke_success`, `test_mock_arsenal_invoke_error`, `test_mock_arsenal_invoke_not_configured`, `test_mock_arsenal_reusable_response`, `test_mock_arsenal_tracks_invocations`, `test_mock_arsenal_validate_call`), run during this task |
| REQ-tool-call-loop-tests | satisfied | Core tests live exactly at `tests/cli/tool_integration_test.rs` and use only in-process mocks (no network, no Python process — confirmed by imports: `MockLlmAdapter`/`MockArsenalPort` only). All 8 PRD-named test functions are present, verbatim-named, and pass: `test_tool_call_basic_flow` (`:58`), `test_tool_call_result_fed_back_to_llm` (`:120`), `test_tool_call_no_arsenal_available` (`:195`), `test_tool_call_unknown_tool` (`:234`), `test_tool_call_invalid_arguments` (`:277`), `test_tool_call_execution_error` (`:321`), `test_multiple_sequential_tool_calls` (`:388`), `test_tool_call_with_garrison` (`:454`) — a measured count of exactly 8 against the PRD's required 8 (`grep -c 'fn test_' tests/cli/tool_integration_test.rs` → 8, run during this task). Exercised by `cargo test --offline --features cli --test cli -- tool_integration_test` — 8/8 passed, part of the 106/106 full `cli` target run during this task; all error scenarios degrade gracefully with no panic across the full run |
| REQ-mcp-gated-integration-tests | superseded by shipped code | `tests/mcp_test_server.py` exists (confirmed by `find`, run during this task) and is the exact fixture the PRD names, but the gated test file itself does not: `tests/integration/tool_integration_mcp_test.rs` does not exist anywhere in the tree. The equivalent capability ships instead at `tests/integration/mcp_stdio_test.rs` (wired into `tests/integration/mod.rs:36` unconditionally, no `#[cfg(feature)]` gate and **zero `#[ignore]` attributes** — `grep -c '#\[ignore\]' tests/integration/mcp_stdio_test.rs` → 0, run during this task) — the PRD's gating requirement ("MUST live in `tool_integration_mcp_test.rs` and be marked `#[ignore]` or feature-gated") is unmet: these tests run unconditionally in a default `cargo test --workspace`, spawning a `python3` subprocess every run, with **no Python-availability check or graceful skip** anywhere in the file (`grep -n 'which\|available\|skip' tests/integration/mcp_stdio_test.rs` → only the literal `Command::new("python3")` call site, run during this task) — if `python3` is absent, these tests fail rather than skip. The functional capability itself is real and exceeds the PRD's 2-test bar: `test_stdio_invoke_tool_echo` (functionally `test_full_mcp_stdio_tool_call_flow`) and `test_stdio_invoke_tool_calculator` (functionally `test_mcp_calculator_tool_invocation`, asserting `"8"` for `add(5,3)` and `"28"` for `multiply(4,7)` — an exact match to the PRD's own worked example) are two of 9 test functions in the file. Exercised by `cargo test --offline -p paladin-ai --test lib -- integration::mcp_stdio_test` — 10/10 passed (with `python3` present in this environment), run during this task. The MCP WebSocket transport limitation `STATE.md`'s deferred items records is a separate, already-tracked item and not re-raised here |

### Epic 24 — Test Hardening, Benchmarks & QA (9 IDs)

**Block verdict — `tasks-test-hardening-benchmarks-qa.md`: partially outstanding.** Seven of the
nine parent-task clusters (`0.0`–`8.0`) verify against the current tree — several with caveats
that do not defeat the cluster's own claim, matching the corpus's established doc-relocation and
deliberate-deferral patterns. Two clusters genuinely fail: **`1.0` Fix Campaign & ChainOfCommand
Benchmarks** — the shipped `battalion_benchmarks.rs` contains a working Campaign benchmark but
**no ChainOfCommand benchmark at all**, despite `docs/src/appendix/battalion-benchmarks.md:193,223,237`
explicitly and repeatedly claiming "ChainOfCommand Benchmarks: Compiling and enabled" — a
doc-vs-tree contradiction, not a documented, reasoned deferral; and **`8.0` Final Quality
Verification and CI/CD Integration** — the test/lint/build verification half shipped, but the
CI/CD-integration half did not: `.github/workflows/ci.yml` has zero jobs named `cli-tests`,
`bench-check`, or `coverage` (confirmed by listing all fifteen job keys in the file:
`lint`, `security-audit`, `cargo-deny`, `osv-scanner`, `api-surface`, `test`, `examples`,
`crate-isolation`, `integration-tests`, `security`, `docker`, `kubernetes-smoke`, `benchmark`,
`benchmark-regression-signal`, `publish-dry-run`), matching the source file's own unchecked
`8.9`-`8.13` subtasks read-only against `.github/workflows/` per this phase's prohibition. Per
D-06, a block verdicts `satisfied by shipped code` only if every cluster verifies; that bar is not
met here, so the block is `partially outstanding` and **clusters `1.0` and `8.0` are exactly Phase
6 CLOSE-02's scope for this block and nothing else** — cluster `8.0`'s gap overlaps in kind (not in
identity) with Phase 15's already-scoped `PIPE-01`/`PIPE-04` (Deferred-QA Epic 25's `cli-tests` and
`bench-check` jobs), noted for cross-reference, not substituted for the CLOSE-02 assignment this
plan's must_haves require.

`.planning/intel/task-completion-state.md` records **29 open** items for this task list (both the
`Milestone_3-Completion` section total and the corpus-wide top-line ranking cite the same figure) —
the smallest of the three VERIFY-02 blocks. Transcribed here, not re-derived: 5 sit under `5.0`
(Live API tool-calling/rate-limiting subtasks explicitly marked SKIPPED, already shipped in the
adapters or deferred as advanced features — not part of either failing cluster), 4 under `6.0`
(coverage subtasks correctly self-recorded DEFERRED, matching what shipped), 11 under `7.0`'s first
occurrence (demo-recording and architecture-doc subtasks explicitly DEFERRED with a stated,
legitimate blocking reason — "requires live environment", "requires API keys" — tracked separately
by Phase 16 / DOCS-04, not a new CLOSE-02 finding), and the remaining 9 sit under `8.0`'s first
occurrence's unchecked subtasks (`8.6`, `8.7`, `8.9`-`8.13`) plus the two bare parent-header
checkboxes for `7.0`/`8.0`'s first occurrences themselves — none of the 29 represents unverified
forward work outside what the cluster table below already accounts for.

| Parent task | Verdict | Evidence |
|---|---|---|
| 0.0 Create feature branch | satisfied | Development-process step, not a shipped-code capability; the epic's own commit history and `RELEASE_NOTES_MILESTONE_3.md` record Epic 24 as part of the Milestone 3 release, corroborated by `CHANGELOG.md`'s Milestone 3 section |
| 1.0 Fix Campaign & ChainOfCommand Benchmarks (US-24.1) | partially outstanding | `benchmark_campaign_branching_dag` at `crates/paladin-battalion/benches/battalion_benchmarks.rs:122` uses the current `Campaign`/`CampaignEdge::new`/`EdgeCondition::Always`/`.add_edge()` API and compiles clean (`cargo check --offline --bench battalion_benchmarks -p paladin-battalion` — exit 0, run during this task); it is one of exactly 3 functions registered in `criterion_group!(battalion_benches, benchmark_formation_three_agents, benchmark_phalanx_five_agents, benchmark_campaign_branching_dag)` at `:156-161`. **No `benchmark_chain_of_command` function exists anywhere in the file** (`grep -in 'chain_of_command\|chainofcommand' battalion_benchmarks.rs` → zero matches, run during this task), yet `docs/src/appendix/battalion-benchmarks.md:193,223,237` states three times "Campaign and ChainOfCommand benchmarks have been fixed and re-enabled" / "ChainOfCommand Benchmarks: Compiling and enabled". `benches/BENCHMARK_FIXES.md:143` (the pre-fix historical tracking doc) itself anticipated this exact outcome: "If Campaign/ChainOfCommand not yet implemented, comment out those benchmarks for now." Campaign's half of this cluster's claim is real and proven; ChainOfCommand's half was never delivered despite the doc's claim to the contrary. **Owner: Phase 6 / CLOSE-01/CLOSE-02** (either restore the benchmark or correct the stale doc claim) |
| 2.0 Enable Deferred Test Modules (US-24.2, US-24.3) | satisfied | `pub mod prompt_generation_service_test;` uncommented and active at `tests/unit/mod.rs:27`; exercised by `cargo test --offline -p paladin-ai --test lib -- unit::prompt_generation_service_test` — 6/6 passed, run during this task. Zero `#[ignore]` attributes on the timeout tests in `tests/unit/paladin_execution_service_test.rs` (`test_execution_service_enforces_timeout`/`test_execution_completes_before_timeout`/`test_timeout_with_multiple_loops` at `:225,250,274`); exercised by `cargo test --offline -p paladin-ai --test lib -- unit::paladin_execution_service_test::test_execution_service_enforces_timeout unit::paladin_execution_service_test::test_execution_completes_before_timeout unit::paladin_execution_service_test::test_timeout_with_multiple_loops` — 3/3 passed, 0 ignored, run during this task |
| 3.0 Implement Qdrant Integration Tests (US-24.4) | satisfied, with caveat | `tests/integration/rag_integration_tests.rs` (gated `#[cfg(feature = "qdrant")]` at `tests/integration/mod.rs:52-53`) carries `test_rag_retrieval_service_with_real_qdrant`/`test_rag_retrieval_with_token_budget_limiting`/`test_rag_context_formatting` at `:260,331,394`, each using a `setup_qdrant_or_skip()` runtime-availability check (`:182`) rather than `#[ignore]`; compiles clean under `cargo check --offline --features qdrant --test lib -p paladin-ai` — exit 0, run during this task. `tests/integration/qdrant_sanctum_tests.rs` (the file the source task list names `tests/unit/sanctum/qdrant_sanctum_test.rs`, relocated) carries 15 `#[ignore = "Requires Qdrant running on localhost:6334"]` attributes (`grep -c '#\[ignore' qdrant_sanctum_tests.rs` → 15, run during this task), matching the *Epic 11* ledger's `REQ-qdrant-sanctum-adapter-v1` row exactly. **Caveat, not a cluster failure:** this sandbox has no `docker` binary (`command -v docker` → exit 1, run during this task), so none of these tests can execute here — the same environmental limit already recorded for Epic 11, not a fresh gap |
| 4.0 Add CLI Snapshot Testing Infrastructure (US-24.6) | satisfied by shipped code, with caveat | `insta = "1.34"` at `Cargo.toml:152` under `[dev-dependencies]`; `tests/cli/snapshots/` holds **86** `.snap` files (`ls tests/cli/snapshots/ | wc -l` → 86, run during this task, well above the PRD's "at least ten"); `[[test]] name = "cli", path = "tests/cli/mod.rs", required-features = ["cli"]` at `Cargo.toml:211-214`; exercised by `cargo test --offline --features cli --test cli -p paladin-ai -- table_output_test` — 8/8 passed, run during this task. **Caveat:** `docs/QUICKSTART.md`/`docs/INSTALLATION.md`/`docs/cli/README.md` don't exist at those paths — Milestone 11's docs overhaul relocated CLI documentation into `docs/src/appendix/{cli-usage.md (1077 lines, includes an "Installation" section),cli-setup-check.md (498 lines),cli-testing.md (526 lines)}` and `docs/src/contributing/testing-guide.md` (641 lines, includes a "Running Tests" section covering unit/integration/snapshot commands) — matching the Epic 22 (05-05) mdbook-relocation precedent exactly |
| 5.0 Implement Live API Integration Tests (US-24.7) | satisfied by shipped code, with caveat | `live-api-tests = []` feature at `Cargo.toml:265`; `tests/integration/llm_live_api_tests.rs` double-gated behind `#[cfg(feature = "live-api-tests")]` (`tests/integration/mod.rs:34-35`) plus 13 `#[ignore]` attributes on all its tests (`grep -c '#\[ignore' llm_live_api_tests.rs` → 13, run during this task); OpenAI/DeepSeek/Anthropic completion, streaming and error-handling tests present per the source file's own checked subtasks (only tool-calling and rate-limiting/retry subtasks `5.8/5.13/5.18/5.20/5.21` are unchecked, explicitly SKIPPED as advanced/already-handled-in-adapters). **Cross-referenced, not re-litigated:** the "skip gracefully" vs. "fail loudly" contested semantics this cluster's US-24.7 raised are settled by `.planning/decisions/0012-live-api-test-key-behaviour.md` — see `REQ-provider-live-api-tests` below |
| 6.0 Improve Deferred Module Test Coverage (US-24.5) | satisfied | The source file's own annotation — "ANALYSIS COMPLETE - DEFERRED" — matches the tree exactly: `.project/Deferred-QA-CICD-Completion/DEFERRED_COVERAGE.md` (not at the source file's cited `project/DEFERRED_COVERAGE.md` path, which does not exist — the `.project/` rename this ledger's own head-note path caveat already covers) documents `user_service.rs` at 488 LOC / ~4.23% coverage and `listener_service.rs` at 602 LOC / ~57.83% coverage, both explicitly deferred to Epic 28/Epic 29 with a full cost/benefit analysis and a recorded approval/sign-off, read in full during this task. This cluster's own claim was "analyze and document a deferral decision," not "close the coverage gap" — and that is exactly what shipped |
| 7.0 Update Documentation and Create Demo Assets (US-24.8) | satisfied by shipped code, with caveat | **Restated twice in the source: `:182` (unchecked) and `:214` (`**COMPLETE**`, no subtask list). This verdict follows the `:182` occurrence** — its own subtask record of what remains incomplete is still true against the tree, while `:214`'s bare re-label adds no new evidence. `docs/MANEUVER.md`, `docs/COMMANDER.md` and `docs/cli/TESTING.md` don't exist at those paths; `CONTRIBUTING.md` doesn't exist at the repo root either (`find . -iname CONTRIBUTING.md` → none, run during this task) — but its content shipped relocated to `docs/src/appendix/contributing-legacy.md` (verified: carries a "Testing Requirements" section with "Coverage Requirements"/"Test Organization"/"Running Tests" subsections, matching subtasks `7.19`-`7.21`'s claims), Maneuver docs to `docs/src/user-guides/maneuver-flow-dsl.md`, Commander content into `docs/src/user-guides/battalion-patterns.md`/`docs/src/appendix/battalion-patterns-guide.md`, and CLI testing docs to `docs/src/appendix/cli-testing.md`/`docs/src/contributing/testing-guide.md` — the same mdbook-relocation pattern as cluster `4.0` and the Epic 22 precedent. **Real, unresolved gap, but not a new one:** `docs/assets/` does not exist at all; `docs/src/assets/` holds only 6 architecture SVGs, zero demo recordings — subtasks `7.9`-`7.17` are explicitly self-recorded `DEFERRED` in the source ("requires live environment", "requires API keys", "no recordings"), and this exact empty-`docs/assets/` gap is already named forward scope at **Phase 16 / DOCS-04** ("record the demos decision so an empty `docs/assets/` stops implying work in flight"), not a fresh CLOSE-02 finding |
| 8.0 Final Quality Verification and CI/CD Integration | partially outstanding | **Restated twice in the source: `:216` (unchecked) and `:244` (`**SUBSTANTIAL COMPLETION**`, self-qualified, not `**COMPLETE**`). This verdict follows the `:216` occurrence**, corroborated rather than contradicted by `:244`'s own hedged label. The quality-verification half shipped: subtasks `8.1`-`8.5`/`8.8`/`8.14`-`8.26` (test suite, fmt, clippy, check, file review, git/PR/merge process) are checked and plausible against the tree. The **CI/CD-integration half did not ship**: subtasks `8.9`-`8.13` (read `.github/workflows/`, add a CLI-tests CI job, add a benchmark-compilation CI job, add coverage reporting to CI, verify CI syntax) are unchecked and explicitly self-recorded "DEFERRED - CI/CD updates separate"; verified against the tree by reading `.github/workflows/ci.yml` (never edited, per this phase's prohibition): all fifteen job keys are `lint`, `security-audit`, `cargo-deny`, `osv-scanner`, `api-surface`, `test`, `examples`, `crate-isolation`, `integration-tests`, `security`, `docker`, `kubernetes-smoke`, `benchmark`, `benchmark-regression-signal`, `publish-dry-run` (`grep -n '^  [a-z-]*:$' ci.yml`, run during this task) — none named `cli-tests`, `bench-check`, or `coverage`. **Owner: Phase 6 / CLOSE-02** (overlapping in kind, not identity, with Phase 15's already-scoped `PIPE-01`/`PIPE-04`) |

**Note:** this source file restates two parent tasks a second time with a bare completion marker
and no new subtask evidence — `7.0` at `:182` (open) and again at `:214` (`**COMPLETE**`), `8.0` at
`:216` (open) and again at `:244` (`**SUBSTANTIAL COMPLETION**`) — so the nine-row table above is
not missing two rows; each duplicated number occupies exactly one row citing both occurrences.

| ID | Verdict | Evidence |
|---|---|---|
| REQ-battalion-benchmark-repair | present, unproven | Split verdict, per the `1.0` cluster row above: `benchmark_campaign_branching_dag` at `crates/paladin-battalion/benches/battalion_benchmarks.rs:122` uses the current Campaign API and compiles clean (`cargo check --offline --bench battalion_benchmarks -p paladin-battalion` — exit 0, run during this task) — that half is proven. The requirement as stated names **both** Campaign and ChainOfCommand; `grep -in 'chain_of_command' battalion_benchmarks.rs` returns zero matches, run during this task — no citation exists for that half at all, despite `docs/src/appendix/battalion-benchmarks.md:193,223,237` claiming three times that it was "fixed and re-enabled". Nothing in the tree exercises the requirement's full, as-stated claim. **Owner: Phase 6 / CLOSE-02** |
| REQ-prompt-generation-test-reenable | satisfied | The disabled-test-module question this ID names is answered directly against the tree, not the historical claim: `pub mod prompt_generation_service_test;` is uncommented and active at `tests/unit/mod.rs:27` (the source file's own "commented at line 22" note is stale — the module compiles and runs today); exercised by `cargo test --offline -p paladin-ai --test lib -- unit::prompt_generation_service_test` — 6/6 passed (`test_new_prompt_generation_service`, `test_generate_prompt_with_llm`, `test_prompt_caching`, `test_cache_invalidation`, `test_deterministic_generation`, `test_empty_description_error`), run during this task |
| REQ-timeout-test-hardening | satisfied | `test_execution_service_enforces_timeout`/`test_execution_completes_before_timeout`/`test_timeout_with_multiple_loops` at `tests/unit/paladin_execution_service_test.rs:225,250,274`, none carrying `#[ignore]` (`grep -n ignore` over the file returns zero matches, run during this task — the source file's "ignored at lines 237, 239" note is stale); exercised by `cargo test --offline -p paladin-ai --test lib -- unit::paladin_execution_service_test::test_execution_service_enforces_timeout unit::paladin_execution_service_test::test_execution_completes_before_timeout unit::paladin_execution_service_test::test_timeout_with_multiple_loops` — 3/3 passed, 0 ignored, run during this task |
| REQ-qdrant-integration-tests | present, unproven | `tests/integration/rag_integration_tests.rs:260,331,394` (gated `#[cfg(feature = "qdrant")]`, `qdrant` not a default feature per `Cargo.toml:259`) compiles clean under `cargo check --offline --features qdrant --test lib -p paladin-ai` — exit 0, run during this task — and uses a `setup_qdrant_or_skip()` runtime check rather than `#[ignore]`; `tests/integration/qdrant_sanctum_tests.rs` carries 15 `#[ignore = "Requires Qdrant running on localhost:6334"]` attributes (`grep -c '#\[ignore' qdrant_sanctum_tests.rs` → 15, run during this task). This sandbox has no `docker` binary (`command -v docker` → exit 1, run during this task), so nothing exercises either file here — the identical environmental limit the *Epic 11* ledger's `REQ-qdrant-sanctum-adapter-v1` row already recorded, not a fresh gap |
| REQ-deferred-coverage-review | deferred with reason | `.project/Deferred-QA-CICD-Completion/DEFERRED_COVERAGE.md` (not the source's cited `project/DEFERRED_COVERAGE.md`, which does not exist — covered by this ledger's own `.project/` path-rename caveat), read in full during this task: `user_service.rs` (488 LOC) at ~4.23% coverage deferred to "Epic 28: Platform Services Test Coverage" and `listener_service.rs` (602 LOC) at ~57.83% coverage deferred to "Epic 29: Event System Test Coverage & Observability", both with a documented cost/benefit analysis and a recorded approval/sign-off dated 2026-02-14. The coverage clauses this row would otherwise re-litigate are cited, not restated: `.planning/decisions/0006-coverage-gate.md` (as amended by Phase 5 / VERIFY-05) records the single surviving figure — 84.79% measured, 84% floor — superseding every one of the six corpus-wide coverage positions including this Epic's own 80%/70% re-assertion, which ADR-0006's own "Considered Options" section names and rejects by ID (`REQ-epic24-quality-gates`) |
| REQ-cli-snapshot-testing | satisfied | `insta = "1.34"` at `Cargo.toml:152`; `tests/cli/snapshots/` holds **86** snapshot files (`ls tests/cli/snapshots/ | wc -l` → 86, run during this task, well above the "at least ten" bar); QUICKSTART/INSTALLATION/CLI-README content shipped relocated rather than at the named paths — `docs/src/appendix/cli-usage.md` (1077 lines, has an "Installation" section), `docs/src/appendix/cli-setup-check.md` (498 lines), `docs/src/appendix/cli-testing.md` (526 lines) — matching the Epic 22 mdbook-relocation precedent; exercised by `cargo test --offline --features cli --test cli -p paladin-ai -- table_output_test` — 8/8 passed, run during this task |
| REQ-provider-live-api-tests | superseded by shipped code | `.planning/decisions/0012-live-api-test-key-behaviour.md` records the recorded answer this row cites rather than restating the contested skip-vs-panic deadlock: the shipped `require_api_key()` panic (`tests/integration/llm_live_api_tests.rs:71-87`) stands, because the double gate (`#[cfg(feature = "live-api-tests")]` at `tests/integration/mod.rs:34-35` plus 13 `#[ignore]` attributes) already supplies the graceful skip both Epic 23 FR-23.4.4 and this Epic's US-24.7 require for a default, ungated `cargo test --workspace` run — the panic only fires when a developer has explicitly opted into both the feature and `--ignored`, where a silent skip would be a false pass. The shipped harness answers the requirement differently than the ingested PRDs specified, and per the ADR that shipped answer is recorded as authoritative. **Owner: Phase 6 / CLOSE-03** for the one real defect the ADR found — the doc comment at `llm_live_api_tests.rs:61` says "Skip test if API key is not present or empty" while both matched arms panic; a documentation-only fix, no behavioral change |
| REQ-final-documentation-and-demo | superseded by shipped code | Documentation content shipped relocated into the mdbook rather than at the source file's named paths — `docs/src/appendix/contributing-legacy.md` (the relocated `CONTRIBUTING.md`, with a "Testing Requirements" section), `docs/src/user-guides/maneuver-flow-dsl.md` (the relocated `docs/MANEUVER.md`), Commander content folded into `docs/src/user-guides/battalion-patterns.md`/`docs/src/appendix/battalion-patterns-guide.md` (no dedicated `docs/COMMANDER.md`), `docs/src/appendix/cli-testing.md`/`docs/src/contributing/testing-guide.md` (the relocated `docs/cli/TESTING.md`) — the shipped, relocated answer is recorded as authoritative, matching the Epic 22 (05-05) precedent for this exact divergence class. **Not part of this verdict, and not a new finding:** `docs/assets/` remains empty of demo recordings, a gap the source file itself self-recorded `DEFERRED` with legitimate reasons (no live environment, no API keys) and which **Phase 16 / DOCS-04** already owns recording a decision for |
| REQ-epic24-quality-gates | superseded by shipped code | The coverage clauses ("80% all modules / 70% integration, re-asserted") are explicitly named and rejected in `.planning/decisions/0006-coverage-gate.md`'s "Considered Options" section by this exact `REQ-*` ID — ADR-0006's single 84.79%-measured / 84%-floor figure supersedes it, per Phase 5 / VERIFY-05. The "no ignored tests remaining" clause, re-measured directly during this task with the same command plan 05-05 recorded (`grep -c '#\[ignore' crates/paladin-battalion/src/commander.rs` → **0**): commander.rs itself is clean, so that specific claim is **not** contradicted here. The workspace as a whole still carries justified, feature/environment-gated `#[ignore]` attributes elsewhere — 13 in `tests/integration/llm_live_api_tests.rs` (double-gated, per `REQ-provider-live-api-tests` above) and 15 in `tests/integration/qdrant_sanctum_tests.rs` (requires a live Qdrant instance, per `REQ-qdrant-integration-tests` above) — a different, intentional category from an unaddressed regression, not a fresh finding |
