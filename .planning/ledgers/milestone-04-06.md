# Milestone 4-6 cited status ledger

This file **supersedes** `REQUIREMENTS.md`'s `## Milestone 4-6 as-shipped ledger` section (D-26).
That section becomes a pointer to this file. Phases 10 and 13 each add a sibling ledger
(`milestone-07-08.md`, `milestone-09-12.md`) rather than growing REQUIREMENTS.md further —
REQUIREMENTS.md is already ~4,000 lines and five inline sets of `file:line`-cited verdicts would
make it unreadable.

**Primary key: the `REQ-*` requirement ID.** Outstanding task items are nested under the
requirement they belong to, not given their own identifiers — nesting them keeps this ledger
joinable to `REQUIREMENTS.md` and `ROADMAP.md` without inventing new IDs (D-00e). The same
`file:line` citation may legitimately appear in more than one row: two requirements describing the
same shipped artefact keep separate rows and separate verdicts, because the `REQ-*` ID is the
primary key, not the citation. Two rows are never merged because they cite the same artefact.

**Evidence bar.** A `satisfied` verdict requires a `file:line` citation **plus** a named passing
test, example, or command that exercises it. A `file:line` citation with nothing exercising it is
not `satisfied` — it gets its own verdict, `present, unproven` (D-01). This bar applies to all 115
rows below **without exception**, including every row REQUIREMENTS.md's run-3 ledger already marked
with an ingest-era status word (`Shipped`, `Verify`, `Variant`, etc.) — an ingest status word **is**
the bare "the code exists" claim this bar exists to reject.

**Manifest carve-out.** Milestones 4-6 are structural milestones, so a large share of their
requirements *are* manifest declarations — `edition`, feature-flag shapes, dependency lists,
`required-features`, workspace membership. For those, the manifest line **plus** a named CI job or
build leg that consumes it is the exercising artefact. Two exercising artefacts anchor this ledger,
both re-grepped fresh against the tree on 2026-08-06 rather than trusted from an earlier document:
the `crate-isolation` job in `.github/workflows/ci.yml`, confirmed at **line 304**
(`intel/code-verification.md`'s citation of that job at line 228 is stale and must not be copied
forward),
and the workspace matrix build/test steps plus the `cli_isolation` regression step in
`.github/workflows/feature-flags.yml`, confirmed at **lines 115, 118 and 141**. A manifest fact
cited without its consuming CI job is `present, unproven`, not `satisfied` — a bare `Cargo.toml`
feature declaration is the exact false-positive class this bar exists to reject.

**Path caveats.** Read every row below with two systematic caveats recorded once here, not repeated
per row (D-04). (a) The `src/…` paths in the run-3 PRDs are *internally* historical — Milestone 6
moved what Milestone 5 had just placed, and `src/application/use_cases/` no longer exists at all.
Citations in this ledger are **current** locations, resolved through
`.planning/codebase/STRUCTURE.md` or the tree, never through a PRD. (b) `STABLE_API.md`,
`docs/FEATURE_FLAGS.md`, `docs/MIGRATION.md` and `docs/CONFIGURATION.md` ship as mdbook chapters
under `docs/src/api-reference/` and `docs/src/getting-started/installation.md`, not at their PRD
paths. A row whose only divergence from its PRD is caveat (b) is `relocated`, not a gap.

**Workspace shape.** This ledger is authoritative, per this phase (D-05), that the workspace is
**ten library crates** — `paladin-core`, `paladin-ports`, `paladin-battalion`, `paladin-herald`,
`paladin-llm`, `paladin-memory`, `paladin-storage`, `paladin-notifications`, `paladin-content`,
`paladin-web` — plus a `doc-examples` crate plus the root facade package `paladin-ai`. This
replaces both the six crates the Milestone 5/6 overviews assume and the nine-crate figure this
planning set carried before run 3. The same correction lands in REQUIREMENTS.md's pointer (below)
and in `.planning/codebase/STRUCTURE.md`'s Directory Purposes section, corrected by this same plan.

**Per-milestone checkbox corroboration (D-06).** This corpus's dominant record-understates-the-tree
heuristic does not apply uniformly here and must be applied per milestone. Milestone 4 is 93.2%
complete with 20 open items, all in Epic 2, and those 20 are **corroborated** — real remaining work.
Milestone 5 is 96.4% complete with 17 open items, **mostly contradicted** by the tree — the crates,
the CI job and the benchmark report all ship despite the open checkboxes. Milestone 6 is 100%
complete with 0 open items, **corroborated** — all four relocations verifiably complete.

## Verdict legend

| Verdict | Meaning |
| --- | --- |
| `satisfied` | `file:line` citation **and** a named passing test, example, or command exercising it |
| `present, unproven` | `file:line` citation exists, but nothing exercises it |
| `genuinely outstanding` | No shipped code satisfies the requirement |
| `deferred with reason` | Explicitly deferred, with the deferring document and reason cited |
| `superseded by shipped code` | Shipped code answers the requirement differently than the ingested document specified, and the shipped answer is recorded as authoritative |
| `relocated` | The deliverable exists, but at a different path than the requirement names |
| `diverged` | The shipped tree deliberately implements the requirement differently, as distinct from a later milestone replacing it |

**ROADMAP-criterion mapping.** ROADMAP criterion 1 names five verdict words — satisfied, relocated,
superseded, diverged, genuinely outstanding — that map directly onto five of this legend's seven
values (`superseded` → `superseded by shipped code`); the two additional values, `present, unproven`
and `deferred with reason`, are this ledger's own finer-grained vocabulary and are not named by
ROADMAP criterion 1.

**Tie-break rule.** A row that qualifies for both `relocated` and `superseded by shipped code` takes
`relocated`, because D-02 exists to preserve the moved-not-missing signal that `superseded` would
otherwise absorb.

## Row order and amendment convention

The 13 epic sections below appear in REQUIREMENTS.md's own run-3 order — Milestone 4 Epics 1-3,
Milestone 5 Epics 1-6, Milestone 6 Epics 1-4 — and are never re-sorted. Rows within a section appear
in the ID order REQUIREMENTS.md lists them. Later plans replace a row's **Verdict** and **Evidence**
cells in place; they never insert, delete, or reorder rows. Exactly one plan in this phase writes
this file per execution wave (waves 1, 4, 5, 6, 7, 8 and 9 each contain exactly one ledger-writing
plan; waves 2 and 3 contain none), so no two executors edit it concurrently. Amendments follow
D-00f: edit in place, retain superseded text, date every amendment, never a separate corrections
file.

### Milestone 4 Epic 1 — Feature Flag Expansion (7 IDs)

| ID | Verdict | Evidence |
|---|---|---|
| REQ-feature-flag-matrix | PENDING-VERDICT | 07-12 |
| REQ-vision-feature-gating | PENDING-VERDICT | 07-12 |
| REQ-feature-default-set | PENDING-VERDICT | 07-12 |
| REQ-feature-full-flag | PENDING-VERDICT | 07-12 |
| REQ-cfg-guard-discipline | PENDING-VERDICT | 07-12 |
| REQ-feature-flag-docs | PENDING-VERDICT | 07-12 |
| REQ-feature-ci-matrix | PENDING-VERDICT | 07-12 |

### Milestone 4 Epic 2 — Port Trait Hardening & Stable API (9 IDs)

| ID | Verdict | Evidence |
|---|---|---|
| REQ-curated-lib-exports | PENDING-VERDICT | 07-12 |
| REQ-visibility-hardening | PENDING-VERDICT | 07-12 |
| REQ-port-trait-rustdoc | PENDING-VERDICT | 07-12 |
| REQ-stable-api-doc | PENDING-VERDICT | 07-12 |
| REQ-import-path-updates-m4 | PENDING-VERDICT | 07-12 |
| REQ-doc-build-clean | PENDING-VERDICT | 07-12 |
| REQ-api-surface-ci | PENDING-VERDICT | 07-12 |
| REQ-deprecation-warnings | PENDING-VERDICT | 07-12 |
| REQ-api-surface-reduction-target | PENDING-VERDICT | 07-12 |

### Milestone 4 Epic 3 — CLI Isolation (9 IDs)

| ID | Verdict | Evidence |
|---|---|---|
| REQ-cli-feature-gate | PENDING-VERDICT | 07-12 |
| REQ-cli-dependency-isolation | PENDING-VERDICT | 07-12 |
| REQ-binary-target-config | PENDING-VERDICT | 07-12 |
| REQ-cli-test-isolation | PENDING-VERDICT | 07-12 |
| REQ-library-only-build | PENDING-VERDICT | 07-12 |
| REQ-library-only-integration-tests | PENDING-VERDICT | 07-12 |
| REQ-cli-build-time-measurement | PENDING-VERDICT | 07-12 |
| REQ-cli-ci-matrix | PENDING-VERDICT | 07-12 |
| REQ-cli-docs | PENDING-VERDICT | 07-12 |

### Milestone 5 Epic 1 — Workspace Initialization & paladin-core (10 IDs)

| ID | Verdict | Evidence |
|---|---|---|
| REQ-cargo-workspace-root | satisfied | `Cargo.toml:1` (`[workspace]`) and `:2` (`members = [".", "crates/*"]`) — the repository root is a Cargo workspace with the existing `paladin-ai` package as a member alongside every `crates/*` entry, confirmed by direct read during this task. `:5-30` (`[workspace.dependencies]`) declares shared versions for every crate FR-2 names as a minimum: `serde` (`:6`, `features = ["derive"]`), `serde_json` (`:7`), `uuid` (`:8`, `["v4","serde"]`), `chrono` (`:9`, `["serde"]`), `thiserror` (`:10`), `tokio` (`:11`, `["full"]`), `async-trait` (`:12`), `reqwest` (`:13`, `["json"]`), `log` (`:14`). Manifest carve-out (D-01): exercised by the `test` job's `cargo test --workspace --lib --bins` (`ci.yml:223`, re-grepped this task) and the `crate-isolation` matrix (`ci.yml:304-330`), whose ten legs each run `cargo build -p ${{ matrix.crate }}` (`:348`) against this workspace manifest — both re-confirmed passing this task via `cargo check --offline --workspace --lib` (0 errors, `Finished` in 54.29s). |
| REQ-workspace-crate-edition-v1 | superseded by shipped code | Cites `.planning/decisions/0009-workspace-rust-edition-2024.md` (ADR-0009), which already records and applies the answer to ARCH-03(a) — Phase 7's scope for this pair is citation only per CONTEXT.md D-09, not re-adjudicated here. Re-verified fresh during this task (`grep -n "^edition" Cargo.toml crates/*/Cargo.toml`): all twelve edition-carrying manifests declare `edition = "2024"` — `Cargo.toml:35` and `crates/{paladin-core,paladin-ports,paladin-battalion,paladin-llm,paladin-memory,paladin-storage,paladin-notifications,paladin-content,paladin-web,paladin-herald,doc-examples}/Cargo.toml:4` — none remain on the `"2021"` this row's FR-5/§7 text specified. REL-02 (the code fix) is already applied; the `edition = "2021"` position is superseded, not merely stale. |
| REQ-paladin-core-scaffold | satisfied | `crates/paladin-core/Cargo.toml:1-2` (`name = "paladin-ai-core"`), `:14-15` (`[lib] name = "paladin_core"`, satisfying FR-4's "valid Cargo.toml and src/lib.rs" together with the crate's existing `src/lib.rs`), `:18-24` referencing workspace dependencies via `{ workspace = true }` syntax exactly as FR-5 specifies (`serde`, `serde_json`, `uuid`, `chrono`, `thiserror`, `async-trait`, `tokio`). FR-6's exact dependency list is tracked separately by the row below. Exercised by `cargo doc --offline -p paladin-ai-core --no-deps` (run this task) — builds the crate in isolation and completes with `Finished ... Generated .../paladin_core/index.html`, 0 errors — and the `crate-isolation` CI job's `paladin-ai-core` leg (`ci.yml:311-312`, build step `:348`). |
| REQ-paladin-core-dependency-allowlist-v1 | superseded by shipped code | Cites ADR-0015 (`.planning/decisions/0015-core-ports-dependency-allowlist.md`), which rebaselines this row's Appendix B six-crate "complete and exhaustive" claim (`.project/Milestone_5-Workspace-Decomposition/Epic_1/prd-workspace-initialization-and-paladin-core-extraction.md:321-334`) against the measured tree rather than recording the difference as debt. Re-measured fresh this task: `crates/paladin-core/Cargo.toml:17-31` lists **fourteen** `[dependencies]` entries — the six PRD-permitted crates (`serde`, `serde_json`, `uuid`, `chrono`, `thiserror`, `async-trait`) plus `tokio`, `sha2`, `blake3`, `petgraph`, `murmur3`, `url`, `regex`, `futures` — matching ADR-0015's own recount exactly. The enforceable invariant ADR-0015 states — no provider SDK, transport client, storage driver, or web framework — holds: `cargo tree --offline -p paladin-ai-core` (run this task) contains zero matches for `application\|infrastructure\|redis\|sqlx\|mysql\|axum\|actix\|minio\|openai\|anthropic\|deepseek`. |
| REQ-core-base-extraction | satisfied | `crates/paladin-core/src/base/{entity,component,service}/*.rs` (directory listing run this task) contains every FR-9 type: `node.rs` (`Node<T>`), `collection.rs` (`Collection`), `field.rs` (`Field`), `message.rs` (`Message`), `action.rs` (`Action`), `event.rs` (`Event`). `grep -rn "use crate::application::\|use crate::infrastructure::" crates/paladin-core/src/base/` (run this task) returns zero matches, satisfying FR-11. Exercised by `cargo test --offline -p paladin-ai-core --lib base::` — 50 passed, 0 failed, run during this task. |
| REQ-core-container-extraction | satisfied | `crates/paladin-core/src/platform/container/` (directory listing run this task) holds `paladin.rs`, `paladin_config.rs`, the `battalion/` sub-tree, `garrison.rs`, `arsenal/`, `citadel.rs`, `herald.rs`, `sanctum.rs` and the other FR-14 types, module tree preserved. **Known partial supersession**: FR-14's inclusion of the Maneuver lexer/AST/parser inside this extraction was reversed one milestone later — see `REQ-maneuver-files-moved-from-core`'s row (Milestone 6 Epic 3 section above, filled by plan 07-01), which confirms `find crates/paladin-core/src -iname "*maneuver*" -o -iname "*parser*"` returns no results today. Recorded here inline per this plan's instruction, not reopened as a nested outstanding item. Exercised by `cargo test --offline -p paladin-ai-core --lib platform::container::` — 316 passed, 0 failed, run during this task. |
| REQ-core-upward-dependency-resolution | satisfied | `grep -rn "application::" crates/paladin-core/src/` (run this task) returns exactly one hit — a doc-comment cross-reference in `arsenal/handoff_error.rs:6` ("The `application` layer re-exports this type from `application::errors::handoff_error`"), not a `use` statement; `battalion/mod.rs:21-23` imports `PaladinResult`, `PaladinError`, `RegistryError` from crate-local `crate::platform::container::*` paths only. FR-17's hard constraint — `battalion/mod.rs` as shipped must not import from `application::` — holds. SM-10's decision artifact exists at `.project/Milestone_5-Workspace-Decomposition/Epic_1/decisions/battalion-result-upward-dependency-decision.md` (confirmed present this task) and is promoted as ADR-0016, cited by the row below. Exercised by `cargo test --offline -p paladin-ai-core --lib platform::container::` — 316 passed, 0 failed (same run cited above, includes `battalion::` submodule tests). |
| REQ-port-value-type-ownership-v1 | satisfied | Cites ADR-0016 (`.planning/decisions/0016-port-value-type-ownership.md`), which ratifies this row's decision-record position (`paladin-core` owns) as the surviving one — the source record is `Status: Approved`, `2026-05-13`, `Chosen Option: Option A`. Confirmed shipped this task: `crates/paladin-core/src/platform/container/token_usage.rs:13` (canonical `TokenUsage`), `execution_result.rs:38` (`PaladinResult`), `execution_result.rs:76` (`StopReason`), `registry_error.rs:10` (`RegistryError`), `arsenal/handoff_error.rs:27` (`HandoffError`) — all five types the decision record names, all defined in `paladin-core`. Exercised by `cargo test --offline -p paladin-ai-core --lib platform::container::` — 316 passed, 0 failed (includes `token_usage::tests`, `execution_result::tests`, `registry_error::tests`). |
| REQ-facade-core-reexports | satisfied | `src/core/mod.rs:109` (`pub use paladin_core::base;`) and `src/core/platform/mod.rs:6-66` (`pub mod container { ... }`, re-exporting every FR-14 flat module plus `arsenal` and a `battalion` sub-block at `:47-65` that layers `maneuver`/`parser` shims on top of `paladin_core::platform::container::battalion::*`) preserve every pre-extraction `paladin::core::...` import path. Root `paladin` crate lists `paladin-core = { workspace = true }` at `Cargo.toml:51`. `src/core/` (directory listing run this task) contains only `mod.rs`, `platform/{mod.rs,README.md}` and `platform/manager/` (core services outside FR-14's extraction scope) — no relocated source remains, satisfying SM-8. Exercised by `cargo check --offline --workspace --lib` (this task) — `Finished` in 54.29s, 0 errors, satisfying FR-22 — and `cargo test --offline --test unit maneuver` — 21 passed, 0 failed, run during this task, resolving through this exact `container`/`battalion` re-export block via `paladin::core::platform::container::battalion::maneuver`. |
| REQ-core-dependency-validation | satisfied | `cargo tree --offline -p paladin-ai-core` (run this task) contains zero matches for `application\|infrastructure\|redis\|sqlx\|mysql\|axum\|actix\|minio\|openai\|anthropic\|deepseek`, satisfying FR-24/FR-25. `cargo doc --offline -p paladin-ai-core --no-deps` (run this task) completes with `Finished`, 0 errors, 0 broken intra-doc links, satisfying FR-26. The historical `.project/Milestone_5-Workspace-Decomposition/Epic_1/paladin-core-dependency-tree.txt` and `baseline-test-count.txt` artifacts remain locatable (not casualties of the `project/`→`.project/` rename) but are pre-rename-era snapshots (`v0.1.0`, a since-removed `fasthash` dependency) rather than current evidence; this row's verdict rests on the fresh commands above, not on those historical files. |

### Milestone 5 Epic 2 — paladin-ports Extraction (10 IDs)

| ID | Verdict | Evidence |
|---|---|---|
| REQ-paladin-ports-scaffold | superseded by shipped code | Cites ADR-0015, which rebaselines this row's FR-3 seven-crate allowlist (`paladin-core`, `async-trait`, `serde`, `thiserror`, `uuid`, `chrono`, `tokio`) against the measured tree. Re-measured fresh this task: `crates/paladin-ports/Cargo.toml:1-2` (`name = "paladin-ports"`), `:20-31` lists **eleven** `[dependencies]` entries — the base seven plus `serde_json`, `futures`, `md5`, `mime_guess` (`:31`) — not the ten `intel/code-verification.md` records, matching ADR-0015's own recount exactly (`mime_guess` postdates that intel figure). `:14-18` (`[lib] doctest = false`) is tracked separately by `REQ-ports-doctest-compilation` below. Exercised: the `crate-isolation` job's `paladin-ports` leg (`ci.yml:313-314`, build step `:348`) and `cargo test --offline -p paladin-ports --lib` (this task) — 98 passed, 0 failed, which requires a successful isolated build (FR-4). |
| REQ-output-ports-extraction | satisfied | `crates/paladin-ports/src/output/` (directory listing run this task) contains all 18 files FR-7's table names — `arsenal_port.rs`, `battalion_port.rs`, `citadel_port.rs`, `content_delivery_port.rs`, `embedding_port.rs`, `file_storage_port.rs`, `garrison_port.rs`, `llm_port.rs`, `log_port.rs`, `notification_port.rs`, `paladin_executor_port.rs`, `paladin_port.rs`, `paladin_registry.rs`, `queue_port.rs`, `sanctum_port.rs`, `scheduler_port.rs`, `search_engine_port.rs`, `vision_llm_port.rs`, `vision_port.rs` — plus six output ports added since (`auth_port.rs`, `orchestrator_port.rs`, `repository_port.rs`, `streaming_executor_port.rs`, `user_repository_port.rs`, `workflow_repository_port.rs`), a superset rather than a shortfall. `grep -rn "use crate::application::\|use crate::infrastructure::\|use crate::core::" crates/paladin-ports/src/` (run this task) returns zero matches, satisfying FR-9. Exercised by `cargo test --offline -p paladin-ports --lib output::` — 86 passed, 0 failed, run during this task. |
| REQ-input-ports-extraction | satisfied | `crates/paladin-ports/src/input/` (directory listing run this task) contains exactly FR-12's six files: `content_input_port.rs`, `document_port.rs`, `listener_port.rs`, `ml_port.rs`, `nlp_port.rs`, `rpc_port.rs`. The same zero-match `use crate::application::/infrastructure::/core::` grep as the row above applies (`crates/paladin-ports/src/` was checked as a whole). Exercised by `cargo test --offline -p paladin-ports --lib input::` — 12 passed, 0 failed, run during this task. |
| REQ-ports-facade-wiring | satisfied | Confirmed this task: `src/application/ports` does not exist (`ls` returns "No such file or directory"), satisfying FR-16's "no shim files left behind" — `.project/…prd-paladin-ports-extraction.md` §9 Resolved Design Decision 2 ("Backward-compatibility strategy: Full deletion selected (Option B)... produces a cleaner final state with no shim debt") is what shipped, not FR-17's original literal "re-export at old paths" text; the PRD's own later resolution supersedes its own earlier requirement text, and the tree matches the resolution. Root `paladin-ai` package lists `paladin-ports = { workspace = true }` at `Cargo.toml:52` (FR-15). Re-export wiring is centralized in `src/prelude.rs:41-45` (e.g. `pub use paladin_ports::output::llm_port::{LlmError, LlmPort, LlmRequest, LlmResponse};`) rather than `src/lib.rs` directly, and at least twenty source files import `paladin_ports::` at their call sites directly (`grep -rln "paladin_ports::" src/` run this task). Exercised by `cargo check --offline --workspace --lib` (this task) — `Finished`, 0 errors. |
| REQ-ports-import-migration | satisfied | `grep -rn "application::ports::" src/ tests/ examples/ docs/` (run this task) returns exactly one hit — a doc-comment historical cross-reference in `tests/unit/herald_consolidation_test.rs:22`, not a `use` statement — and zero hits inside `crates/paladin-ports/src/` itself, satisfying FR-19/FR-20's "only the `use` statement path strings change" scope. Exercised by the same `cargo check --offline --workspace --lib` run cited above — `Finished`, 0 errors — which would fail to compile on any unmigrated `crate::application::ports::` reference. |
| REQ-ports-doctest-compilation | genuinely outstanding | `crates/paladin-ports/Cargo.toml:14-18` sets `[lib] doctest = false` with an inline comment deferring re-enablement to "Task 7.0"; `.github/workflows/ci.yml:226` (`cargo test --workspace --doc --exclude paladin-ports`, re-grepped this task — the citation elsewhere in this corpus at `:225` is off by one line) excludes the crate from the workspace doctest run entirely. Neither line has changed. Points at Phase 8 / DEBT-03, which re-enables the doctests; no code change is made in this phase per this plan's prohibitions. |
| REQ-ports-docs-markdown-update | satisfied | `grep -rn "application::ports::" docs/` (run this task) returns zero matches. `docs/src/api-reference/stable-api.md:9` states the canonical import path is `crates/paladin-ports/` and gives the example `use paladin_ports::output::llm_port::LlmPort`; the trait-reference table (`:493` onward) lists every port at its `paladin_ports::output::...` path. `docs/src/api-reference/migration-guide.md:67` instructs replacing short paths with `paladin_ports::`/`paladin_core::`/etc. crate-level paths. These are the mdbook-relocated FR-22 deliverable (D-04 caveat (b) governs the file-location half; content itself verified current here). |
| REQ-ports-layering-validation | satisfied | `cargo tree --offline -p paladin-ports` (run this task) shows exactly one workspace-internal dependency, `paladin-ai-core` (satisfying FR-24), and contains zero matches for `redis\|sqlx\|aws-sdk-s3\|minio\|openai\|anthropic` (satisfying FR-23); `reqwest` does not appear at all. `cargo build -p paladin-ports` succeeds independently, confirmed via the same `cargo test --offline -p paladin-ports --lib` run cited above (98 passed — a passing test run requires a successful prior build) and the `crate-isolation` job's dedicated `paladin-ports` matrix leg (`ci.yml:313-314,348`). FR-25/FR-26's specific artifact-save paths (`project/Milestone_5-Workspace-Decomposition/Epic_2/paladin-ports-{isolation-build,dependency-tree}.txt`) are historical `project/`-era paths not reproduced under `.project/` for this crate (unlike Epic 1's dependency-tree file); the underlying commands both pass fresh, which is what FR-23/FR-24 actually gate. |
| REQ-ports-tests-and-rustdoc | present, unproven | FR-27 holds: `cargo test --offline -p paladin-ports --lib` — 98 passed, 0 failed, run during this task. FR-29's rustdoc-preservation claim largely holds: `grep -rc "^///\|^//!"` across `crates/paladin-ports/src/{output,input}/*.rs` (run this task) totals 6683 doc-comment lines, and `cargo doc --offline -p paladin-ports --no-deps` under `RUSTDOCFLAGS="-D warnings"` (run this task) completes with `Finished`, 0 warnings, satisfying FR-30's "zero broken intra-doc links" on the literal check. It does not clear the `satisfied` bar for the row as a whole: FR-29's "no documentation may be lost" clause is undermined by the doctest disablement above — `cargo test --workspace --doc --exclude paladin-ports` never compiles a single one of this crate's embedded doc examples, so any example that silently rotted since extraction would not be caught, which is exactly what "no documentation may be lost" is meant to guard against. Points at Phase 8 / DEBT-03 for the same reason as the row above; recorded `present, unproven` rather than `satisfied` or `genuinely outstanding` because the rustdoc text itself is present and its structural link-integrity is proven, only its example-code correctness is unverified. |
| REQ-port-value-type-ownership-v2 | superseded by shipped code | Cites ADR-0016. This row's FR-7/FR-10 text (`.project/…prd-paladin-ports-extraction.md`, annotated 2026-08-06) would move `PaladinResult`, `StopReason` and `TokenUsage` out of `paladin-core` and into this crate, applied literally reintroducing the exact upward dependency the Epic 1 decision removed. Shipped code instead extends FR-11's `RegistryError` core-re-export carve-out to all three: `crates/paladin-ports/src/output/paladin_port.rs:389` (`pub use paladin_core::platform::container::execution_result::{PaladinResult, StopReason};`) and `crates/paladin-ports/src/output/llm_port.rs:671` (`pub use paladin_core::platform::container::token_usage::TokenUsage;`) — both confirmed thin re-exports, not independent bodies, during this task. The `.project/` source annotation is already in place (top-of-file banner citing ADR-0016 and ADR-0014, FR-7/FR-10 struck through with corrected text inline) — not repeated here per D-00g. Row kept separate from `REQ-port-value-type-ownership-v1`, per this plan's prohibition against merging variant-pair rows. |

### Milestone 5 Epic 3 — paladin-battalion Extraction (9 IDs)

| ID | Verdict | Evidence |
|---|---|---|
| REQ-battalion-crate-scaffold | satisfied | `crates/paladin-battalion/Cargo.toml:1-2` (`name = "paladin-battalion"`) and `:15-29` `[dependencies]` — confirmed this task to contain exactly the FR-3 allowlist plus `paladin-core`/`paladin-ports`: `paladin-core` (`:16`), `paladin-ports` (`:17`), `tokio`, `async-trait`, `serde`, `serde_json`, `thiserror`, `uuid`, `log`, `chrono` via `{ workspace = true }` (`:18-25`), plus `futures = "0.3.31"` (`:26`), `rand = "0.8"` (`:27`), `tokio-util = "0.7"` (`:28`), `petgraph = "0.6"` (`:29`), `regex = "1.11.1"` (`:29`) — no others. `cargo tree --offline -p paladin-battalion` (run this task) contains zero matches for `reqwest\|actix-web\|actix-http\|sqlx\|redis\|qdrant-client\|lettre\|aws-sdk\|minio`. Exercised by `cargo build -p paladin-battalion` (implicit in the `cargo test --offline -p paladin-battalion --lib` run below, which requires a successful prior build) and the `crate-isolation` job's dedicated `paladin-battalion` leg (`ci.yml:315`, build step `:348`, re-grepped this task). |
| REQ-battalion-service-extraction | satisfied | `find crates/paladin-battalion/src -type f` (run this task) lists all nine execution services declared at `crates/paladin-battalion/src/lib.rs:30-40` — `formation_service.rs`, `phalanx_service.rs`, `campaign_service.rs`, `chain_of_command_service.rs`, `conclave_execution_service.rs`, `council_service.rs`, `grove_service.rs`, `commander.rs` — plus the Maneuver service, relocated one milestone later per `REQ-maneuver-files-moved-from-core` (Milestone 6 Epic 3, filled by plan 07-01) into `maneuver/service.rs:15` (`pub struct ManeuverExecutionService`) rather than a flat `maneuver_service.rs`. The three support utilities are present under renamed/relocated names: `error_aggregation.rs`, `retry.rs` at the flat path FR-7 names, and `flow_visualizer.rs` relocated to `maneuver/visualizer.rs:55` (`pub struct FlowVisualizer`) by the same M6 co-location (confirmed via `grep -rln "FlowVisualizer" crates/ src/` this task, which resolves only to that one file). All twelve originals are present in substance; none dropped. `src/application/use_cases/{content,herald,paladin,sanctum,arsenal}/` were not touched by this extraction (confirmed via directory listing — `use_cases/` itself does not exist today, retired by the unrelated M8 Epic 4 rename, not by this epic). Exercised by `cargo test --offline -p paladin-battalion --lib` — 219 passed, 0 failed, run during this task. |
| REQ-battalion-import-migration | satisfied | `grep -rn "use crate::application::\|use crate::infrastructure::" crates/paladin-battalion/src/` (run this task) returns zero matches, satisfying FR-8/FR-9's "no `use` statement referencing `application::`/`infrastructure::`" bar. `PaladinError`'s location (OQ1) resolved to `paladin_core::platform::container::paladin_error::PaladinError`, confirmed at `crates/paladin-battalion/src/phalanx_service.rs:21`. Manifest-plus-job citation under D-01's carve-out: the `crate-isolation` job's `paladin-battalion` leg (`ci.yml:315`, build step `:348`, re-grepped this task at line 304 for the job header — not the stale `:228` figure) exercises this claim directly, since a residual `application::`/`infrastructure::` import would fail that isolated build. Exercised additionally by `cargo test --offline -p paladin-battalion --lib` — 219 passed, 0 failed (same run cited above), which would not compile on any unmigrated reference. |
| REQ-battalion-inline-tests | satisfied | `grep -rc "#\[cfg(test)\]" crates/paladin-battalion/src/**/*.rs` (run this task) finds 18 files carrying at least one `#[cfg(test)]` module — `phalanx_service.rs:24,27,466` carries three — a superset of FR-13's twelve — none dropped or disabled. Exercised by `cargo test --offline -p paladin-battalion --lib` — 219 passed, 0 failed, run during this task, covering every one of those modules. |
| REQ-battalion-facade-shim | superseded by shipped code | Cites ADR-0018 (iv), which records the M5→M6 shim posture flip as history: the shim was correct for Milestone 5 and Milestone 6 retired the *policy* of adding backward-compatible re-export shims. Re-grepped this task: `src/application/use_cases/battalion/` — the literal FR-14 path — does not exist (`ls` returns "No such file or directory"), matching ADR-0018's own citation. **Residual finding, recorded rather than reopened per this plan's prohibition against re-litigating the M5→M6 posture:** the shim mechanism itself was not deleted — it was carried forward under the unrelated Milestone 8 Epic 4 `use_cases`→`services` rename (`REQ-use-cases-services-rename`, already recorded Shipped) and lives today at `src/application/services/battalion/mod.rs`, still a thin `pub use paladin_battalion::<module>;` re-export block per module (confirmed by direct read this task), still declared in `src/application/services/mod.rs:3`, and still consumed by ~19 example/test files (`grep -rln "application::services::battalion\|services::battalion::" --include="*.rs" .`, run this task, excluding `crates/paladin-battalion/`). This does not contradict ADR-0018's no-*new*-shim policy question (Epic 2 OQ4, about orchestration/CircuitBreaker relocations) — it is a narrower fact about this one shim's lifecycle that ADR-0018's clause (iv) states more absolutely than the tree supports. Recorded here as the fresh-grep finding D-06 requires, not as a contradiction to resolve. |
| REQ-battalion-dependency-validation | satisfied | `cargo tree --offline -p paladin-battalion` (run this task, against the `[dependencies]` block at `crates/paladin-battalion/Cargo.toml:15-29`) contains zero matches for `reqwest\|actix-web\|actix-http\|sqlx\|redis\|qdrant-client\|lettre\|aws-sdk\|minio`, satisfying FR-19/FR-21; the `crate-isolation` job's `paladin-battalion` leg (`ci.yml:315`, build step `:348`) exercises the same isolation claim in CI. The FR-20 evidence artifacts exist and are locatable: `.project/Milestone_5-Workspace-Decomposition/Epic_3/paladin-battalion-dependency-tree.txt` and `paladin-battalion-isolation-build.txt` (confirmed present this task, pre-rename-era but not casualties of the `project/`→`.project/` rename). This row's verdict rests on the fresh `cargo tree` run above, not the historical snapshot files. |
| REQ-battalion-example-verification | satisfied | All six named examples exist (`ls examples/` this task): `formation_sequential.rs`, `campaign_workflow.rs`, `chain_of_command_delegation.rs`, `commander_basic.rs`, `commander_auto.rs`, `commander_full_config.rs`. `cargo check --offline --example formation_sequential --example campaign_workflow --example chain_of_command_delegation --example commander_basic --example commander_auto --example commander_full_config` (run this task) — `Finished`, 0 errors. Both named workspace test files exist at `tests/integration/battalion_campaign_integration_test.rs` and `tests/integration/battalion_chain_of_command_integration_test.rs`, declared in `tests/integration/mod.rs:36,38` (confirmed this task); `tests/unit/battalion/` (7 files) and `tests/integration/battalion/` (7 files) remain at workspace level per FR-23's non-migration clause. Exercised by `cargo test --offline --test lib battalion` (run this task) — 177 passed, 0 failed, covering `unit::battalion::*` and `integration::battalion::*`. |
| REQ-battalion-crate-docs | present, unproven | `crates/paladin-battalion/src/lib.rs:1-25` carries a crate-level `//!` doc comment naming all nine patterns (eight orchestration + Commander) and the two utilities, satisfying FR-24's content bar. `cargo clippy --offline -p paladin-battalion --all-targets -- -D warnings` (run this task) — `Finished`, 0 warnings. `cargo fmt --check -p paladin-battalion` (run this task) — exit 0, no diff. Does not clear the `satisfied` bar for the row as a whole: `cargo doc --offline -p paladin-battalion --no-deps` (run this task) completes with `Finished` but **3 warnings** — two unresolved intra-doc links (`[paladin-core]`, `[paladin-ports]` in `in_memory_registry.rs`, missing backticks around the hyphenated crate names) and one unclosed HTML tag (`in_memory_registry.rs:65`, `Paladin ID -> Arc<Paladin>` needing backtick-escaping) — against FR-24's explicit "zero errors and zero warnings" bar. `petgraph` version alignment (FR-25) confirmed: `crates/paladin-core/Cargo.toml:27` and `crates/paladin-battalion/Cargo.toml:28` both pin `petgraph = "0.6"`. Recorded `present, unproven` rather than `satisfied` because the doc-comment content and quality gates (clippy, fmt) all pass but the specific zero-warnings `cargo doc` gate this FR names does not. |
| REQ-paladin-core-dependency-allowlist-v2 | superseded by shipped code | Cites ADR-0015, which rebaselines `paladin-core`'s dependency list (fourteen entries, not the Epic 1 PRD's six) and separately confirms `petgraph` presence as accepted baseline rather than debt — see ADR-0015 (ii). Re-confirmed fresh this task: `crates/paladin-core/Cargo.toml:27` (`petgraph = "0.6"`) and `crates/paladin-battalion/Cargo.toml:28` (`petgraph = "0.6"`) — same workspace-pinned version, satisfying this row's version-alignment clause and avoiding duplicate compilation (confirmed via the single `petgraph v0.6.5` entry, no `(*)`-duplicate marker, in the `cargo tree --offline -p paladin-battalion` run cited by the row above). This later position (2026-05-17 PRD) matches the tree; `REQ-paladin-core-dependency-allowlist-v1` (Milestone 5 Epic 1, filled by plan 07-06) keeps its own row and verdict per this plan's prohibition against merging variant-pair rows. |

### Milestone 5 Epic 4 — paladin-llm Extraction (11 IDs)

| ID | Verdict | Evidence |
|---|---|---|
| REQ-llm-crate-scaffold | satisfied | `crates/paladin-llm/Cargo.toml:1-2` (`name = "paladin-llm"`) and `:17-24` `[features]` — confirmed this task: `default = ["openai", "mock"]` (`:18`), plus `anthropic`, `deepseek`, `vision`, `openai-embeddings` all present. `reqwest` is optional and gated: `:33` (`optional = true`), activated by `openai`/`anthropic`/`deepseek` (`:19-21`, each lists `dep:reqwest`) and not required by `mock` (`:22`, `mock = []`). `cargo build -p paladin-llm --no-default-features` (run this task) — `Finished` in 14.43s (cold), producing an empty-provider-surface crate as FR-4 permits. `paladin-llm` depends on `paladin-core` and `paladin-ports` as non-optional dependencies (`:26-27`) and on no root `paladin` crate, `paladin-battalion`, or `paladin-memory` (confirmed via `Cargo.toml` `[dependencies]` listing, this task). Root workspace `members` includes `crates/paladin-llm` implicitly via the `crates/*` glob (`Cargo.toml:2`, shared with all ten library crates). Exercised further by the `crate-isolation` job's `paladin-llm` leg (`ci.yml:317-318`, `extra_flags: "--all-features"`, build step `:348`, re-grepped this task). |
| REQ-llm-provider-error | diverged | `crates/paladin-llm/src/error.rs:16-52` defines `LlmProviderError` (deriving `thiserror::Error` and `Clone`, not `Debug` explicitly since `#[derive(Debug, Error, Clone)]` at `:15` includes it) with `From<LlmProviderError> for LlmError` at `:54-70`, satisfying this row's structural core — a crate-local error type converting to the shared `LlmError` at the port boundary. The shipped variant set diverges from the row's named minimum: `AuthenticationError(String)` not `Authentication(String)`, `NetworkError(String)` not `Network(String)`, `RateLimitExceeded` not `RateLimit`, `TokenLimitExceeded` (unit variant) not `TokenLimitExceeded { limit, requested }`, `Timeout(String)` not `Timeout(u64)`, `ConfigurationError(String)` not `Configuration(String)`, and no per-provider `OpenAI(String)`/`Anthropic(String)`/`DeepSeek(String)` variants at all (provider-specific HTTP errors stay private within each provider module per this row's own permitted-alternative clause). Recorded `diverged` rather than `satisfied`: the conversion-boundary architecture matches exactly, but the concrete variant shapes are a deliberate, differently-named implementation, not the named minimum set. Exercised by `cargo test --offline -p paladin-llm --all-features --lib error::` (subset of the 80-test run cited below) — all `error::tests::*` pass. |
| REQ-openai-provider-extraction | satisfied | `crates/paladin-llm/src/openai/{adapter.rs,embedding.rs,vision.rs}` (directory listing this task) match FR-11/FR-12's target paths. `OpenAIConfig` (`adapter.rs:26-37`) is public with exactly the named fields — `api_key: String`, `base_url: String`, `organization: Option<String>`, `timeout_seconds: u64`, `max_retries: u32` — and retains `from_env()` (confirmed present). `vision.rs` is gated behind `#[cfg(feature = "vision")]` nested inside the `openai` module, itself gated `#[cfg(feature = "openai")]` at `lib.rs:59`, jointly equivalent to `cfg(all(openai, vision))` (confirmed via `openai/mod.rs:15,19`). `grep -n "crate::core::" crates/paladin-llm/src/openai/*.rs` (run this task) returns zero matches — all such imports already resolve through `paladin_core::`. `cargo build -p paladin-llm --no-default-features --features openai` (run this task) — `Finished` in 17.94s. `rand` is an optional dependency gated by the `openai`/`anthropic`/`deepseek` features (`Cargo.toml:34`), not a shared internal utility, satisfying the row's stated alternative. Exercised by `cargo test --offline -p paladin-llm --all-features --lib openai::` (subset of the 80-test run below) — all `openai::*::tests` pass. |
| REQ-anthropic-provider-extraction | satisfied | `crates/paladin-llm/src/anthropic/{adapter.rs,vision.rs}` exist (directory listing this task). `AnthropicConfig` (`adapter.rs:29-39`) is public with `api_key: String`, `base_url: String`, `model: String`, `max_tokens: u32`, `timeout_seconds: u64` — a superset of the named minimum — and retains `from_env()`. `cargo build -p paladin-llm --no-default-features --features anthropic` (run this task) — `Finished` in 1.79s, independent of `openai`/`deepseek`. Exercised by `cargo test --offline -p paladin-llm --all-features --lib anthropic::` (subset of the 80-test run below) — all `anthropic::*::tests` pass. |
| REQ-deepseek-provider-extraction | satisfied | `crates/paladin-llm/src/deepseek/adapter.rs` exists at the FR-21 sub-module path (a `deepseek/` directory with `adapter.rs` and `mod.rs`, matching the row's alternative form). `DeepSeekConfig` (`adapter.rs:27-36`) is public with `api_key: String`, `base_url: String`, `model: String`, `timeout_seconds: u64`, retaining `from_env()`. `cargo build -p paladin-llm --no-default-features --features deepseek` (run this task) — `Finished` in 1.75s, independent of `openai`/`anthropic`. `cargo tree --offline -p paladin-battalion` and the isolated-build timing above jointly confirm no cross-crate recompilation is triggered by a DeepSeek-only change; the `crate-isolation` job's `paladin-battalion` leg (`ci.yml:315`) builds independently of the `paladin-llm` leg (`:317`), matching the isolation clause structurally. Exercised by `cargo test --offline -p paladin-llm --all-features --lib deepseek::` (subset of the 80-test run below) — all `deepseek::*::tests` pass. |
| REQ-llm-mock-adapters | present, unproven | `crates/paladin-llm/src/mock.rs` exists at the FR-25 path, default-enabled (`Cargo.toml:18`, `mock` in `default`), and compiles with no network dependency (`mock = []` at `:22`, no `dep:reqwest`). `MultiStepMockLlmPort` (`mock.rs:299-`) matches the row's name exactly. The row's `MockLlmPort` name does not exist in the shipped crate — the shipped type is `MockLlmAdapter` (`mock.rs:73`); `grep -n "MockLlmPort" crates/paladin-llm/src/mock.rs crates/paladin-llm/src/lib.rs` (run this task) returns no match for that exact identifier, only the `MultiStepMockLlmPort` compound name. `cargo test --offline -p paladin-llm --features mock --lib mock::` (run this task) — passes with no other feature required. Recorded `present, unproven` rather than `satisfied`: the module, the default-on network-free feature and `MultiStepMockLlmPort` are all exercised and correct, but the row's own named type `MockLlmPort` is not present under that name, so the row as specified is not fully proven by what ships. |
| REQ-llm-provider-factory | diverged | `crates/paladin-llm/src/provider_factory.rs` exists at the co-located FR-28 path; `LlmProviderFactory` and `ProviderFactoryError` are both re-exported at the crate root path (`paladin_llm::provider_factory::{LlmProviderFactory, ProviderFactoryError}`, confirmed via `src/lib.rs:167` consuming the former). `LlmProviderFactory::create()` (`:62-116`) is feature-gated internally and returns `Err(ProviderFactoryError::UnknownProvider(..))` for a disabled-feature provider name rather than failing to compile, satisfying the graceful-degradation core of FR-29 — confirmed by `cargo test --offline -p paladin-llm --all-features --lib provider_factory::` (subset of the 80-test run below), including `test_unknown_provider_returns_error`. Diverges from two of this row's explicit sub-clauses: `ProviderFactoryError` (`:14`) is `pub`, not private to the crate as FR-30 specifies, and `grep -n "From<ProviderFactoryError>" crates/paladin-llm/src/{provider_factory,error}.rs` (run this task) finds no `From<ProviderFactoryError> for LlmProviderError` implementation. The audit clause is satisfied: `grep -n "ApplicationSettings\|application_settings" crates/paladin-llm/src/provider_factory.rs` (run this task) returns zero matches — no direct coupling to remove. Recorded `diverged`: the factory's runtime behaviour matches the requirement, its error-type visibility and conversion boundary do not. |
| REQ-llm-config-bridge-location-v1 | superseded by shipped code | Cites ADR-0017, which records FR-31/FR-32's circular-dependency concern as real at the time it was written but not describing the shipped tree — see the annotated top-of-file banner and the `:205` "Superseded (dated 2026-08-06, ADR-0017)" block in `.project/Milestone_5-Workspace-Decomposition/Epic_4/prd-paladin-llm-extraction.md`, written by plan 07-04. The shipped bridge is `crates/paladin-llm/src/config/bridge.rs`, inside `paladin-llm` — the location FR-31/FR-32 explicitly forbade — confirmed present this task with its own doc comment (`:1-7`) stating the Rust orphan-rule reasoning for why co-location is now correct. This is **not** the shipped shape recorded elsewhere in this row: `-v2` (Milestone 6 Epic 1, filled by plan 07-08) records that shipped shape; this `-v1` row records only that FR-31/FR-32 as originally written is superseded, with the concern real-but-mis-sited per ADR-0017's own framing — the Milestone 6 config-type relocation (not a reversal of the concern) removed the cycle FR-31 feared. |
| REQ-llm-test-architecture | relocated | Unit tests (individual methods, error conversions) are co-located in `#[cfg(test)]` modules inside `crates/paladin-llm/src/**/*.rs` as FR-34 requires — e.g. `error.rs:85-86` (`#[cfg(test)] mod tests`) — confirmed by the per-module `error::tests`, `provider_factory::tests`, `openai::adapter::tests`, etc. exercised in the 80-test run below. Integration tests exist but **not** at FR-35's named path `crates/paladin-llm/tests/` — `find crates/paladin-llm/tests -type f` (run this task) returns nothing; the crate has no `tests/` directory at all. The five named files' substance ships instead at the workspace level: `tests/integration/openai_provider_test.rs`, `anthropic_provider_test.rs`, `deepseek_provider_test.rs` (directory listing this task; no separate `mock_integration.rs`/`provider_factory_test.rs` file names, but `tests/unit/llm/provider_factory_test.rs` covers that surface at the unit layer instead). Network isolation holds regardless of location: `tests/integration/openai_provider_test.rs:24,61,110,148,173` (run this task) carries five `#[ignore]`-annotated live-API tests, each commented "Requires API key and makes real API calls," satisfying FR-36's no-real-network-calls-by-default bar. Recorded `relocated`, not `genuinely outstanding`, per D-02's tie-break: the deliverable exists, at the workspace `tests/integration/` path rather than the crate-local `crates/paladin-llm/tests/` path FR-35 names. |
| REQ-llm-facade-prelude | relocated | Root `Cargo.toml:55` adds `paladin-llm` with `default-features = false` and `features = ["openai", "anthropic", "deepseek", "mock", "vision"]`, satisfying FR-37. `grep -rn "infrastructure::adapters::llm" --include="*.rs" .` (run this task) finds two hits, both inside `rust,ignore`-marked doc-comment examples (`crates/paladin-ports/src/output/llm_port.rs:235`, a doc-comment cross-reference, and `src/application/services/paladin/paladin_execution_service.rs:302`, inside a `` ```rust,ignore ``` `` block, confirmed by direct read — neither is compiled code), not a live `use` statement — satisfying FR-39's sweep in substance, with the doc-comment staleness noted rather than hidden. `src/infrastructure/adapters/llm/` (FR-40) is reduced to exactly what the row's own alternative permits: `mod.rs` is a comment explaining the relocation plus `pub mod config_bridge;`, and `config_bridge.rs` itself is reduced to `#[cfg(test)]`-only integration tests exercising the real bridge in `paladin-llm` (confirmed by direct read this task) — no adapter source remains. The one clause that does **not** hold at its named location: `paladin::prelude` (`src/prelude.rs`, read in full this task) does **not** include `OpenAIAdapter`, `AnthropicAdapter`, `DeepSeekAdapter`, `MockLlmAdapter`, `MultiStepMockLlmPort`, `LlmProviderFactory`, or `LlmProviderError` — `grep -n` for all seven names against `src/prelude.rs` (run this task) returns zero matches. All seven (under their shipped names) are instead re-exported directly at the crate root in `src/lib.rs:167-181`, reachable as `paladin::OpenAIAdapter` etc. but not via `paladin::prelude::*`. Recorded `relocated`: the re-export deliverable exists and is reachable, at `paladin::` crate root rather than `paladin::prelude::` as FR-38 names. |
| REQ-llm-build-validation | present, unproven | Two clauses, addressed separately per this plan's instruction. **Feature-matrix builds (FR-41), against the six flag combinations declared at `crates/paladin-llm/Cargo.toml:18-24`:** all six legs succeed, run fresh this task — `--no-default-features` (`Finished` in 14.43s cold, 0.23s on a warm no-op rebuild), `--features openai` (17.94s), `--features anthropic` (1.79s), `--features deepseek` (1.75s), `--features mock` (0.82s), `--all-features` (5.16s). `cargo tree --offline -p paladin-llm --no-default-features --features openai` (run this task) shows zero `anthropic`/`deepseek` matches, satisfying the dead-provider-code clause. `cargo clippy --offline -p paladin-llm --all-features -- -D warnings` — `Finished`, 0 warnings; `cargo fmt --check -p paladin-llm` — exit 0; `cargo doc --offline -p paladin-llm --all-features --no-deps` — `Finished`, 0 warnings shown. `cargo test --offline -p paladin-llm --all-features --lib` — 80 passed, 0 failed, run during this task (a crate-scoped substitute for the FR-42 workspace-wide "1,487+ tests green" claim, which this plan does not re-run in full per its offline/scoped-command preference). The **no-default-features "<5 seconds" build-time sub-target** is not judgeable against a comparable historical baseline in this sandboxed environment — `build-benchmarks.md:63-71` (cited by ADR-0020) measures workspace-wide scenarios and does not include a `paladin-llm --no-default-features` figure; recorded `present, unproven` for that specific sub-clause rather than asserting a pass or fail on an unfundable comparison. **Incremental-improvement target (FR-45, ≥ 50%):** cites ADR-0020 in full — the target is restated per scenario (ADR-0020 (ii)) rather than one headline figure, with two of the five measured scenarios passing (`paladin-memory` −50.2%, `paladin-battalion` −90.9%) and three failing (clean build −6.6%, `paladin-core` −18.9%, `paladin-llm` adapter change −44.6%), transcribed from `build-benchmarks.md:63-71`'s Summary Table per ADR-0020 (i), with the report's own "Target achieved" conclusion at `build-benchmarks.md:111` recorded as contradicted by its own table per ADR-0020 (iv) and the re-measurement it recommends at `:107` declined per ADR-0020 (v). Overall row recorded `present, unproven`: the matrix-build and quality-gate half is exercised and passing, the build-time sub-target is unmeasurable here, and the incremental target is a restated 2-of-5 per ADR-0020 rather than a single pass. |

### Milestone 5 Epic 5 — paladin-memory Extraction (10 IDs)

| ID | Verdict | Evidence |
|---|---|---|
| REQ-memory-crate-scaffold | PENDING-VERDICT | 07-11 |
| REQ-workspace-crate-edition-v2 | PENDING-VERDICT | 07-11 |
| REQ-memory-module-structure | PENDING-VERDICT | 07-11 |
| REQ-garrison-adapter-extraction | PENDING-VERDICT | 07-11 |
| REQ-sanctum-adapter-extraction | PENDING-VERDICT | 07-11 |
| REQ-memory-services-extraction | PENDING-VERDICT | 07-11 |
| REQ-memory-originals-deletion | PENDING-VERDICT | 07-11 |
| REQ-memory-facade-reexports | PENDING-VERDICT | 07-11 |
| REQ-memory-test-migration | PENDING-VERDICT | 07-11 |
| REQ-memory-build-gates | PENDING-VERDICT | 07-11 |

### Milestone 5 Epic 6 — Workspace Finalization (6 IDs)

| ID | Verdict | Evidence |
|---|---|---|
| REQ-facade-reexport-audit | PENDING-VERDICT | 07-11 |
| REQ-paladin-prelude | PENDING-VERDICT | 07-11 |
| REQ-devcontainer-gh-cli | PENDING-VERDICT | 07-11 |
| REQ-crate-isolation-ci | PENDING-VERDICT | 07-11 |
| REQ-workspace-ci-upgrade | PENDING-VERDICT | 07-11 |
| REQ-build-benchmark-report | PENDING-VERDICT | 07-11 |

### Milestone 6 Epic 1 — application_settings.rs Decomposition (8 IDs)

| ID | Verdict | Evidence |
|---|---|---|
| REQ-config-domain-modules | diverged | The shipped tree is a hybrid of the Epic 1 PRD's map (`.project/Milestone_6-Architectural-Refinements/Epic_1/prd-decompose-application-settings.md` §4.1, `:55-84`) and the milestone overview's map, matching neither exactly. **Present and matching the PRD:** the sub-crate split shipped exactly as specified — `crates/paladin-memory/src/config/{garrison,rag,sanctum}.rs` (listed this task) and `crates/paladin-llm/src/config/{llm,vision,bridge}.rs` (listed this task, `mod.rs:3-5`), plus facade files `arsenal.rs`, `citadel.rs`, `file_storage.rs`, `herald.rs`, `notifications.rs`, `queue.rs`, `scheduler.rs`, `web_server.rs` — all present in `src/config/` (directory listing run this task). **Diverges from the PRD:** the PRD's §4.1 facade table puts the root `Settings` struct directly inside `config/mod.rs`; shipped code instead gives it its own file, `src/config/settings.rs:27` (`pub struct Settings`), with `config/mod.rs:58` doing `pub use settings::Settings;`. The PRD's optional `config/sanctum_app.rs` was correctly never created (Sanctum config moved wholly into `paladin-memory`). **Additions neither document names:** `src/config/agents.rs` (339 lines — the milestone *overview's* filename per `intel/code-verification.md`'s crate-facts table, not the PRD's), `src/config/user_config.rs` (360 lines) and `src/config/setup/` (a sub-directory, not a flat file) all exist in the tree (directory listing run this task) with no PRD or overview table entry naming them. This is a deliberate different implementation — the PRD's sub-crate split is honored, the facade's own internal file layout is not — not a missing deliverable. Manifest carve-out for the file-count claim: `wc -l src/config/*.rs crates/paladin-memory/src/config/*.rs crates/paladin-llm/src/config/*.rs` (run this task) shows every file ≤ 360 lines, satisfying the PRD's "no file over 400 lines" success metric regardless of the naming divergence. |
| REQ-env-overridable-trait | satisfied | `src/config/env_utils.rs:33` (`pub trait EnvOverridable { fn apply_env_overrides(&mut self); }`), matching FR from Epic 1 PRD §4.2's example definition. Five facade config structs implement it, confirmed by grep this task: `citadel.rs:59`, `queue.rs:34`, `file_storage.rs:58`, `notifications.rs:44`, `herald.rs:111`. Exercised by `cargo test --offline -p paladin-ai --lib config::env_utils::` (run this task) — 12 passed, 0 failed, including `test_env_overridable_trait_applies_override` and `test_env_overridable_trait_no_override_when_unset`, which exercise the trait's contract directly, not just its definition. |
| REQ-settings-root-struct | satisfied | `src/config/settings.rs:27` (`pub struct Settings`), `:60-64` (`Settings::new()`), `:76-87` (`Settings::load_from_file()`) — both loaders present as the PRD requires. `config/mod.rs:58` re-exports it (`pub use settings::Settings;`), preserving the `paladin::config::Settings` public path. Exercised indirectly (the struct itself carries no inline `#[test]`s) by `cargo test --offline -p paladin-ai --lib config::setup::` (run this task) — 2 passed (`test_setup_and_run_config_wrapping`, `test_setup_and_run_with_default_config`), both of which construct and deserialize a `Settings` value through `setup_and_run`'s config-loading path. |
| REQ-config-incremental-migration | present, unproven | `find src crates -iname "application_settings*"` (run this task) returns nothing — the monolith is deleted, satisfying the PRD's Success Metric 3 end-state. The migration's own intermediate-state discipline (PRD §4.1's "add new modules with temporary re-exports first, then remove the old file in a follow-up step," Goal 5) is unverifiable after the fact: the tree today only shows the finished state, and this plan does not inspect git history to reconstruct the intermediate commits. No artefact in the current tree exercises "was the migration incremental," so this is `present, unproven` rather than `satisfied` — the deletion is proven, the process claim is not. |
| REQ-config-yml-backcompat | present, unproven | No located before-and-after regression test: `grep -rl "config.yml\|config\.yaml" tests/ src/config/` (run this task) finds CLI help/error-output tests and `config/agents.rs`/`config/arsenal.rs` doc examples, but no test loading a reference `config.test.yml` and asserting field values match pre- and post-migration, which is what Epic 1 PRD §8 Success Metric 6 specifies. `.planning/intel/constraints.md:104` names "the `config.yml` deserialization contract with all `#[serde(default)]`, `#[serde(rename)]` and `#[serde(skip_serializing_if)]` attributes required to survive the move byte-for-byte" as one of the corpus's two strongest SPEC-candidate constraints, and `:143-144` flags it as "already contradicted by shipped code" in the sense that no regression artefact enforces it today. Recorded as a finding in this cell per this plan's instruction, not as a nested outstanding item. |
| REQ-rag-config-dedup | satisfied | `grep -rn "struct RagConfig" crates/` (run this task) returns exactly one result — `crates/paladin-memory/src/config/rag.rs:31` — satisfying Epic 1 PRD §8 Success Metric 7 literally. The module's own doc comment (`rag.rs:5,28`) states the type is "merged from `application_settings` and `rag_retrieval_service`," confirming the dedup happened rather than one copy simply never having existed. Exercised by `cargo test --offline -p paladin-memory --lib config::rag::` (run this task) — 8 passed, 0 failed (`test_rag_config_defaults`, `test_rag_config_validate_ok`, `test_rag_config_validate_zero_top_k`, `test_rag_config_validate_bad_similarity`, `test_rag_config_validate_zero_timeout`, plus 3 `MemoryExtractionConfig` tests co-located in the same file). |
| REQ-config-success-metrics | present, unproven | Epic 1 PRD §8 lists seven success metrics; this task checks each against the tree rather than trusting the ingest-era checkbox. **Confirmed this task:** metric 2 (no file over 400 lines) — `wc -l` across `src/config/*.rs` and both sub-crates' `config/*.rs` (run this task) shows a maximum of 360 lines (`user_config.rs`); metric 3 (`application_settings.rs` deleted) — confirmed absent, same evidence as the row above; metric 7 (`RagConfig` unified) — confirmed exactly one definition, same evidence as the row above. **Not confirmed in this task's scope:** metric 1 (`cargo test --workspace` zero failures) — this plan runs scoped offline commands per its instructions, not a full workspace suite; metric 4 (no new public API surface via `cargo doc --workspace --no-deps` diff) — no pre-migration API snapshot was captured to diff against, and none is locatable in `.project/` for this specific Epic; metric 5 (`cargo clippy --workspace -- -D warnings` clean) — not run, out of this plan's scoped-command instruction; metric 6 (`config.yml` regression test) — confirmed absent, same evidence as `REQ-config-yml-backcompat` above. Four of seven metrics hold on direct evidence and three are unexercised in this task's scope, so the row as a whole is `present, unproven` rather than `satisfied`. |
| REQ-llm-config-bridge-location-v2 | superseded by shipped code | Cites ADR-0017 (`.planning/decisions/0017-llm-config-bridge-location.md`), which accepts the shipped bridge location, `crates/paladin-llm/src/config/bridge.rs`, over Epic 4 FR-31/FR-32's root-crate placement. Confirmed this task: `bridge.rs:1-7` states the orphan-rule reasoning ("Placing the impls here keeps all `paladin-llm` types together and avoids the Rust orphan rule restriction"); `:9-19` shows every import (`LlmProviderConfig`, `OpenAIConfig`, `AnthropicConfig`, `DeepSeekConfig`) resolving to `crate::` paths local to `paladin-llm`, with none reaching up into the root `paladin` crate. ADR-0017 states in one clause exactly what this row's `must_haves` require: Epic 4's FR-31 circular-dependency concern "was real at the time it was written and does not describe the shipped tree" — Milestone 6 moved the *configuration types* down into `paladin-llm` rather than moving the bridge up into the root, which removed the cycle FR-31 feared rather than reversing its reasoning. `crates/paladin-llm/src/config/mod.rs:3-5` confirms `llm`, `vision` and `bridge` are sibling modules in the same crate, the structural fact that makes the orphan-rule argument hold. |

### Milestone 6 Epic 2 — Orchestration Service Relocation (9 IDs)

| ID | Verdict | Evidence |
|---|---|---|
| REQ-orchestration-target-structure | diverged | Epic 2 PRD §4.1 (`.project/Milestone_6-Architectural-Refinements/Epic_2/prd-relocate-orchestration-services.md:61-88`) names `src/application/use_cases/{notification_orchestrator,queue_orchestrator,orchestration,log_orchestrator}/` as the target. `test -d src/application/use_cases` (run this task) fails — the directory does not exist anywhere in the tree. The four orchestrator module groups instead ship under `src/application/services/{notification_orchestrator,queue_orchestrator,orchestration,log_orchestrator}/` — confirmed by directory listing this task, and every sub-file the PRD names is present at that path (`notification_orchestrator/{mod,types}.rs`, `queue_orchestrator/{mod,types}.rs`, `orchestration/{mod,listener,orchestrator_bridge,scheduler,types}.rs` plus a `processors/` sub-directory, `log_orchestrator/{mod,types}.rs`). `src/application/mod.rs` (read this task) declares `pub mod services;` and no `use_cases` module anywhere. This is a deliberate different implementation, not a missing one: the PRD's exact module names, sub-file layout and struct names shipped verbatim, only the parent directory differs (`services` vs `use_cases`) — cross-referencing ARCH-05(5), which plan 07-09 corrects at the PRD source. |
| REQ-six-service-relocation | satisfied | All six services Epic 2 PRD §4.3 names (`notification_service.rs`, `queue_service.rs`, `orchestrator.rs`, `log_service.rs`, `listener_service.rs`, `scheduler.rs`) are confirmed relocated out of `src/core/platform/manager/` — that directory's listing (run this task) contains only `content_service.rs`, `event_manager.rs`, `user_service.rs` and `mod.rs`, none of the six. The relocated targets are confirmed present and renamed (see the row below): `NotificationOrchestrator` (`application/services/notification_orchestrator/mod.rs:64`), `QueueOrchestrator` (`queue_orchestrator/mod.rs:46`), `Orchestrator` (`orchestration/mod.rs:56`), `LogOrchestrator` (`log_orchestrator/mod.rs:47`), `ListenerOrchestrator` (`orchestration/listener.rs:141`), `SchedulerOrchestrator` (`orchestration/scheduler.rs:34`). Exercised by `cargo test --offline -p paladin-ai --lib application::services::` (run this task) — 228 passed, 0 failed, spanning all four module groups including `queue_orchestrator::tests::*`, `log_orchestrator::tests::*`, `orchestration::scheduler::tests::*` and `orchestration::tests::*`. |
| REQ-domain-type-placement-rules | satisfied | Epic 2 PRD §4.2 (`:92-109`) classifies pure value/error types to `paladin-core` and port-holding coordination types to the application layer's `types.rs` files. The five types the PRD names as "already confirmed in `paladin-core`" are all still present (directory listing this task): `orchestration_context.rs`, `queue_config.rs`, `queue_item.rs`, `log.rs`, `notification.rs` under `crates/paladin-core/src/platform/container/`. The coordination-type side of the rule also holds: each of the four relocated module groups carries its own `types.rs` (`notification_orchestrator/types.rs`, `queue_orchestrator/types.rs`, `orchestration/types.rs`, `log_orchestrator/types.rs`, all confirmed present this task), matching the PRD's rule that port-holding coordination types land in the application layer rather than `paladin-core`. Exercised by the same `cargo test --offline -p paladin-ai --lib application::services::` run cited above (228 passed) plus the `crate-isolation` job's `paladin-ai-core` leg (`ci.yml:304,311-312`), which would fail to build `paladin-core` in isolation if a coordination type had been left there needing a port import. |
| REQ-manager-services-retained | satisfied | `src/core/platform/manager/` (directory listing run this task) retains exactly `content_service.rs`, `event_manager.rs`, `user_service.rs` and `mod.rs` — matching Epic 2 PRD §4.4's table exactly: `event_manager.rs` and `content_service.rs` "remain" (confirmed, not merely proposed), and `user_service.rs` "remains with import path update" rather than being relocated. `manager/mod.rs` (read this task) declares only `pub mod content_service; pub mod event_manager; pub mod user_service;` — no stale `pub mod` for any of the six relocated services, satisfying PRD §4.7 point 3. The deferred full `user_service.rs` relocation is folded in here as a finding, not reopened as a nested item: it is Phase 11's FACADE-02 D2 (`ROADMAP.md:624`, "FACADE-02's D2 is coupled forward to DEFER-02"), itself coupled to Phase 15's DEFER-02 (`ROADMAP.md:687`, "DEFER-02 is coupled to FACADE-02's D2 in Phase 11"). |
| REQ-orchestration-consumer-import-updates | satisfied | Epic 2 PRD §4.5 (`:136-149`) names six non-relocated consumer files needing import-path updates. All six confirmed updated to the shipped `application::services::` path this task: `src/config/setup/service_runner.rs:1` (`use crate::application::services::notification_orchestrator::NotificationService;`) and `:2` (`use crate::application::services::orchestration::scheduler::Scheduler;`); `src/config/user_config.rs:8` (same `notification_orchestrator` import); `src/core/platform/manager/user_service.rs:9` (same); `src/application/services/content/content_ingestion_service.rs:22` (`use crate::application::services::orchestration::{`). `NotificationService` and `Scheduler` are confirmed backward-compatible type aliases, not stale names: `notification_orchestrator/mod.rs:47` (`pub type NotificationService = NotificationOrchestrator;`) and `orchestration/scheduler.rs:42` (`pub type Scheduler = SchedulerOrchestrator;`). `grep -rn "core::platform::manager::\(notification_service\|queue_service\|log_service\|orchestrator\|listener_service\|scheduler\)"` across `src/`, `examples/`, `tests/` (run this task) returns zero matches — no consumer still imports from the old path. |
| REQ-orchestrator-renaming | satisfied | Epic 2 PRD §4.6 (`:150-163`) names five renames plus one confirmed no-op. All five confirmed shipped this task: `NotificationOrchestrator` (`notification_orchestrator/mod.rs:64`), `QueueOrchestrator` (`queue_orchestrator/mod.rs:46`), `LogOrchestrator` (`log_orchestrator/mod.rs:47`), `ListenerOrchestrator` (`orchestration/listener.rs:141`), `SchedulerOrchestrator` (`orchestration/scheduler.rs:34`); the sixth, `Orchestrator`, is confirmed unrenamed as the PRD table itself specifies (`orchestration/mod.rs:56`). Exercised by the same `cargo test --offline -p paladin-ai --lib application::services::` run cited above (228 passed) — a stale name would fail to compile against the renamed-consumer imports cited in the row above. |
| REQ-core-isolation-verification | satisfied | Epic 2 PRD §4.7 (`:165-171`) states three isolation checks. (1) `cargo build -p paladin-ai-core` — exercised as part of the `crate-isolation` CI job's build step (`ci.yml:304,311-312,348`), re-grepped this task. (2) `cargo tree -p paladin-ai-core` shows zero references to `paladin_ports`, `application::`, or `infrastructure::` — run fresh this task: `cargo tree --offline -p paladin-ai-core \| grep -icE "paladin.ports\|application\|infrastructure"` returns `0`. (3) `src/core/platform/manager/mod.rs` has no stale `pub mod` for a relocated service — confirmed in the `REQ-manager-services-retained` row above (only `content_service`, `event_manager`, `user_service` declared). All three checks pass on direct evidence gathered this task, clearing the D-01 manifest-plus-job bar without falling back to `present, unproven`. |
| REQ-orchestration-test-coverage | satisfied | Epic 2 PRD §4.8 (`:174-181`) requires existing tests to keep passing through the relocation and relocated domain types to carry inline test coverage. Exercised by `cargo test --offline -p paladin-ai --lib application::services::` (run this task) — 228 passed, 0 failed, spanning `paladin::*`, `orchestration::*`, `queue_orchestrator::*`, `log_orchestrator::*` and their nested `types::tests` modules (e.g. `application::services::queue_orchestrator::types::tests::test_error_variants_display`), confirming the domain-type test blocks the PRD's §4.8 second bullet requires are present and green. No integration test in `tests/` was found still importing a `core::platform::manager::` path for a relocated service (same grep cited in `REQ-orchestration-consumer-import-updates`), satisfying the third bullet's "existing integration tests ... have their import paths updated." |
| REQ-orchestration-no-reexport-shims | superseded by shipped code | Cites ADR-0018 (`.planning/decisions/0018-m6-facade-reexport-policy.md`), which ratifies the no-shim posture. Confirmed this task: `grep -rn "use crate::application::services::" src/application/mod.rs src/prelude.rs src/lib.rs` (run this task) finds no `pub use` re-export of any relocated orchestrator type at an old `use_cases::` path — `src/application/mod.rs` declares only `services` (and feature-gated `cli`, plus `errors`), no facade `pub use` shim. This matches Epic 2 Non-Goal 7 ("no shim re-exports are added," `prd-relocate-orchestration-services.md:192`) and contradicts the milestone overview's Epic 2 Acceptance Criterion 6, which plan 07-05 annotated superseded per ADR-0018. Epic 2's own Open Question 4 (`:330`, "should be confirmed with the team") is recorded by ADR-0018 as now confirmed: no `pub use` re-exports are added at `src/lib.rs` or `src/prelude.rs` from old paths to new. |

### Milestone 6 Epic 3 — Maneuver DSL Co-location (9 IDs)

Epic-level note: this epic carries **0 open checkboxes, corroborated** (D-06) — all four
relocations, including this one, are verifiably complete. All 9 rows below were fully cited and
exercised during this task (2026-08-06): citations re-grepped fresh against the tree, and every
`satisfied` row backed by a scoped offline test run whose output is quoted or summarised inline.

| ID | Verdict | Evidence |
|---|---|---|
| REQ-maneuver-submodule-structure | satisfied | `crates/paladin-battalion/src/maneuver/mod.rs:6-8` declares `pub mod parser; pub mod service; pub mod visualizer;`, with `parser/mod.rs:49` (`FlowParser`), `parser/{lexer,ast,error}.rs`, `service.rs:15` (`ManeuverExecutionService`), and `visualizer.rs:55` (`FlowVisualizer`) all present. Exercised by `cargo test --offline -p paladin-battalion maneuver`, run during this task: 74 passed, 0 failed, spanning `maneuver::parser::{ast,error,lexer}::tests`, `maneuver::parser::tests`, `maneuver::service::tests`, `maneuver::visualizer::tests` and `maneuver::tests` — every submodule the requirement names. |
| REQ-maneuver-files-moved-from-core | satisfied | `find crates/paladin-core/src -iname "*maneuver*" -o -iname "*parser*"` returns no results, and `grep -n "maneuver\|parser" crates/paladin-core/src/platform/container/battalion/mod.rs` returns only the `Maneuver` pattern-type enum variant at `battalion/mod.rs:447` (no lexer/parser/AST code) — both run during this task. The moved content lives at `crates/paladin-battalion/src/maneuver/` (cited in the row above) and is exercised by the same `cargo test --offline -p paladin-battalion maneuver` run (74 passed) plus the `crate-isolation` CI job (`ci.yml:304`), which builds `paladin-core` in isolation with no parser dependency to carry. **Supersedes** the Maneuver clause of `REQ-core-container-extraction`, per REQUIREMENTS.md's own note at that row. |
| REQ-maneuver-files-reorganized | satisfied | The flat `maneuver_service.rs` and `flow_visualizer.rs` are confirmed absent from `crates/paladin-battalion/src/` (directory listing, run during this task), replaced by `crates/paladin-battalion/src/maneuver/service.rs:15` and `maneuver/visualizer.rs:55`. Exercised by `cargo test --offline -p paladin-battalion maneuver`: the `maneuver::service::tests::*` (8 tests) and `maneuver::visualizer::tests::*` (17 tests) all pass, run during this task. |
| REQ-maneuver-inline-tests | satisfied | `grep -rn '#\[test\]' crates/paladin-battalion/src/maneuver/` returns **56** inline test-attribute occurrences (≥ the PRD's 35-test minimum), run during this task. Exercised by `cargo test --offline -p paladin-battalion maneuver` — 74 tests passed, 0 failed, run during this task (the pass count exceeds the raw attribute grep because some cases are table-driven; both figures clear the ≥ 35 bar). |
| REQ-core-maneuver-cleanup | satisfied | `crates/paladin-core/src/platform/container/battalion/mod.rs:447` retains only the `Maneuver` pattern-type enum variant (part of `BattalionStrategy`); `grep -n "maneuver\|parser" battalion/mod.rs` returns no lexer, parser, or AST reference, run during this task. Exercised by the `crate-isolation` CI job (`ci.yml:304`), which builds `paladin-core` standalone — a leftover parser reference or dependency would fail that isolated build. |
| REQ-maneuver-facade-reexports | satisfied | `src/core/platform/mod.rs:6` (`pub mod container { … }`) and `:53-64` (`pub mod battalion { … pub mod maneuver { pub use paladin_battalion::maneuver::*; pub mod parser { … } } pub mod parser { … } }`) — the explicit `container` block plus the `maneuver`/`parser` forwarding sub-modules restoring backward-compatible paths for the moved DSL. Exercised by `cargo test --offline --test unit maneuver` (`tests/unit/maneuver_domain_tests.rs`, imports `paladin::core::platform::container::battalion::maneuver::{…}` and `::parser::FlowParser` at lines 3 and 7) — 21 passed — and `cargo test --offline --test unit parser_tests` (`tests/unit/parser_tests.rs`, same import path) — 57 passed. Both run during this task. |
| REQ-maneuver-battalion-import-updates | satisfied | `crates/paladin-battalion/src/commander.rs:18` (`use crate::maneuver::service::ManeuverExecutionService;`) plus fully-qualified `crate::maneuver::*` references at `:172, 798, 815, 817, 819, 822, 825, 834, 873, 876, 879, 1334, 1467, 1486, 1492, 1568, 2648, 2658`. Exercised by `cargo test --offline -p paladin-battalion commander`, run during this task: 52 passed, 0 failed, including `test_maneuver_execution_through_commander`, `test_maneuver_strategy_explicit`, `test_maneuver_with_nested_pattern`, `test_maneuver_with_parallel_pattern`, `test_maneuver_without_flow_expression_fails`, `test_maneuver_with_invalid_flow_expression_fails`. |
| REQ-maneuver-battalion-lib-exports | satisfied | `crates/paladin-battalion/src/lib.rs:38` (`pub mod maneuver;`). Exercised directly by the two facade-path test files cited under `REQ-maneuver-facade-reexports` (`maneuver_domain_tests.rs` 21 passed, `parser_tests.rs` 57 passed, 78 total, run during this task) and indirectly by `src/core/platform/mod.rs:54` (`pub use paladin_battalion::maneuver::*;`), which compiles only because this crate-root export exists. |
| REQ-maneuver-cargo-dependency-check | satisfied | `crates/paladin-core/Cargo.toml:17-31` lists exactly 14 dependencies (`serde`, `serde_json`, `uuid`, `chrono`, `thiserror`, `async-trait`, `tokio`, `sha2`, `blake3`, `petgraph`, `murmur3`, `url`, `regex`, `futures`) — no parser/lexer crate among them (the Maneuver DSL parser is hand-written, so no dependency pruning was required when it moved to `paladin-battalion`). Manifest carve-out (D-01): the manifest line plus the `crate-isolation` CI job (`ci.yml:304`), which builds and isolates `paladin-core` against exactly this dependency list, is the exercising artefact. |

### Milestone 6 Epic 4 — CircuitBreaker Relocation to Infrastructure (8 IDs)

| ID | Verdict | Evidence |
|---|---|---|
| REQ-resilience-module-structure | satisfied | Epic 4 PRD §4.1 (`.project/Milestone_6-Architectural-Refinements/Epic_4/prd-relocate-circuitbreaker-infra.md:95-111`) specifies `src/infrastructure/resilience/{mod.rs,circuit_breaker.rs}` plus a `pub mod resilience;` declaration in `src/infrastructure/mod.rs`. All three confirmed this task: `src/infrastructure/resilience/mod.rs:22` (`pub mod circuit_breaker;`), `:1-21` carries the module-level rustdoc PRD §4.2 requires (canonical-home statement plus a "Planned Additions" block naming `retry`, `rate_limiter`, `bulkhead` — exactly the three the PRD names), and `src/infrastructure/mod.rs:45` (`pub mod resilience;`), satisfying PRD §4.9. `src/infrastructure/resilience/` directory listing (run this task) contains exactly `mod.rs` and `circuit_breaker.rs`, nothing else — "nothing else in the infrastructure layer changes," per PRD §4.1. |
| REQ-circuitbreaker-relocation | satisfied | `src/application/use_cases/paladin/circuit_breaker.rs` does not exist — `test -d src/application/use_cases` (run this task) fails, confirming the entire old parent tree is gone, not just the file. The moved file is `src/infrastructure/resilience/circuit_breaker.rs` (16,062 bytes, confirmed present this task), matching Epic 4 PRD §4.1's "Moved from `application/use_cases/paladin/`." Exercised by `cargo test --offline -p paladin-ai --lib infrastructure::resilience::` (run this task) — 2 passed, 0 failed (`test_circuit_breaker_initial_state`, `test_state_transitions`) — and `cargo test --offline -p paladin-ai --test unit circuit_breaker` (run this task) — 6 passed, 0 failed, spanning `circuit_breaker_test::*` (5 tests) and `paladin_execution_service_test::test_execution_service_uses_circuit_breaker` (1 test), all resolving through the new path. |
| REQ-circuitbreaker-rustdoc-updates | satisfied | Epic 4 PRD §4.4 (`:138-153`) requires every `use` statement inside `circuit_breaker.rs`'s doc examples to point at the new path. Confirmed this task: `circuit_breaker.rs:22` (module-level `//!` example, `use paladin::infrastructure::resilience::circuit_breaker::CircuitBreaker;`) — the module doc block's example already uses the canonical new path, not the retired one. `grep -n "application::use_cases::paladin::circuit_breaker" src/infrastructure/resilience/circuit_breaker.rs` (run this task) returns zero matches — no stale doc-example import survives. `README.md:28` also carries the updated `use paladin::infrastructure::resilience::circuit_breaker::CircuitBreaker;` import, satisfying PRD §4.8's README doc-example update. |
| REQ-paladin-execution-service-import | satisfied | Epic 4 PRD §4.5 (`:154-166`) requires `paladin_execution_service.rs`'s import and inline doc examples to move to the new path. Confirmed this task: `src/application/services/paladin/paladin_execution_service.rs:68` (`use crate::infrastructure::resilience::circuit_breaker::CircuitBreaker;`), with the struct field at `:110` (`circuit_breaker: Arc<CircuitBreaker>`) and doc-example `use` statements at `:21` (module-level), `:162`, `:271`, `:304`, `:337` (method-level), all pointing at the new path — zero occurrences of the old `application::use_cases::paladin::circuit_breaker` path anywhere in the file (grepped this task). This confirms `src/application/use_cases/` no longer exists and the execution service lives under `src/application/services/paladin/` (directory listing this task), the shipped rename of the PRD's parent directory. Exercised directly, not just indirectly via workspace build: `cargo test --offline -p paladin-ai --test unit circuit_breaker` (run this task) includes `paladin_execution_service_test::test_execution_service_uses_circuit_breaker` — 1 of the 6 passed tests cited in the row above — which constructs a `PaladinExecutionService` wired to a real `CircuitBreaker` through this exact import. |
| REQ-circuitbreaker-example-updates | satisfied | Epic 4 PRD §4.6 (`:168-201`) names 15 example files needing the import update. `grep -rln "circuit_breaker::CircuitBreaker\|CircuitBreaker::new" examples/*.rs` (run this task) finds **16** files at the new path — all 15 the PRD names (`basic_paladin.rs`, `agent_handoffs.rs`, `autonomous_full_config.rs`, `autonomous_planning.rs`, `autonomous_prompt_generation.rs`, `battalion_checkpoint_recovery.rs`, `citadel_autosave.rs`, `citadel_restore.rs`, `dynamic_temperature.rs`, `herald_custom_formatter.rs`, `herald_json_output.rs`, `herald_markdown_output.rs`, `paladin_with_config.rs`, `vision_analysis.rs`, `vision_battalion.rs`) plus one addition not in the PRD's list, `http_service_host.rs` — a superset, not a shortfall. `grep -rln "application::use_cases::paladin::circuit_breaker" examples/` (run this task) returns zero matches — no example retains the old import. |
| REQ-circuitbreaker-test-updates | satisfied | Epic 4 PRD §4.7 (`:203-219`) names three workspace test files needing the import update: `tests/cli/paladin_execution_test.rs`, `tests/cli/tool_integration_test.rs`, `tests/cli/error_handling_test.rs`. All three confirmed this task at the new path (`use paladin::infrastructure::resilience::circuit_breaker::CircuitBreaker;` at `paladin_execution_test.rs:9`, `error_handling_test.rs:12`, `tool_integration_test.rs:14`), zero remaining references to the old path (same grep as the row above extended to `tests/`). Exercised by `cargo test --offline -p paladin-ai --test cli --features cli error_handling` (run this task) — 16 passed, 0 failed, including 4 `error_handling_test::*` cases that construct a `CircuitBreaker` through this exact import and 2 execution tests (`paladin_execution_test::test_paladin_error_handling`, `phalanx_execution_test::test_phalanx_error_handling`) exercising it transitively through `PaladinExecutionService`. `tests/unit/circuit_breaker_test.rs` (5 inline `#[test]` attributes, grepped this task) is the crate's own dedicated CircuitBreaker suite, exercised in the `REQ-circuitbreaker-relocation` row's 6-test run above. |
| REQ-circuitbreaker-old-path-retired | superseded by shipped code | Cites ADR-0018 (`.planning/decisions/0018-m6-facade-reexport-policy.md`), which records the old path as intentionally unresolvable with no re-export. Confirmed this task: `grep -rn "application::use_cases::paladin::circuit_breaker\|application::services::paladin::circuit_breaker" src/ examples/ tests/` returns exactly one hit — `docs/src/api-reference/stable-api.md:619`, which is prose *describing* the historical path as part of a canonical-path-change note, not a live `use` statement or re-export. `src/infrastructure/resilience/circuit_breaker.rs` (the moved file, cited above) contains no `pub use` shim pointing back at the old location, and `src/application/services/paladin/mod.rs` (checked this task) declares no `circuit_breaker` module. This matches Epic 4 Goal 7 / FR-4.11 (`prd-relocate-circuitbreaker-infra.md:50-51,239-244`, "no re-export left behind — old path is intentionally broken") and contradicts the milestone overview's Epic 4 Acceptance Criterion 5, which plan 07-05 annotated superseded per ADR-0018. |
| REQ-circuitbreaker-stable-api-update | relocated | Epic 4 FR-4.12 named updating root-level `STABLE_API.md`; that file does not exist at the root (confirmed this task via `find . -maxdepth 1 -iname "STABLE_API.md"`, no result). The deliverable ships as `docs/src/api-reference/stable-api.md`, confirmed present and current this task: `:614-627` carries a dedicated "Resilience Types" section with a "Canonical path change (Milestone 6, Epic 4)" callout stating `CircuitBreaker` and `CircuitState` "were relocated ... The old path is retired and no longer resolves," and lists both types at `paladin::infrastructure::resilience::circuit_breaker::{CircuitBreaker,CircuitState}` as 🟢 Stable. This is the D-04(b) mdbook-relocation caveat, not a gap — FR-4.12's "update `STABLE_API.md`" now applies to this mdbook chapter per plan 07-05's re-pointing. Where this row could also be argued `superseded by shipped code` (the mdbook content *is* a different, updated answer to the same deliverable), the ledger's tie-break rule is applied and `relocated` is taken, because D-02 exists to preserve the moved-not-missing signal. `final-api.txt` and `api_surface_current.txt` both exist at the repo root (confirmed this task via `test -f`), but their regeneration currency is not confirmed by this task — the `api-surface` CI job that would regenerate them is recorded broken by a stale baseline path (DEBT-01, `.project/current-exports.txt` vs. the job's literal `project/current-exports.txt`), so their presence is not evidence they are current. |
