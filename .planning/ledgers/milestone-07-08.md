# Milestone 7-8 cited status ledger

This file **supersedes** `REQUIREMENTS.md`'s `## Milestone 7-8 as-shipped ledger` section (D-01,
Phase 10 plan 10-01). That section becomes a pointer to this file. Phase 13 adds the fifth sibling
ledger, `milestone-09-12.md`, in this same directory — `.planning/ledgers/milestone-04-06.md`'s own
head note already named this file (`milestone-07-08.md`) as Phase 10's deliverable before this plan
existed.

**Primary key: the `REQ-*` requirement ID.** Outstanding task items are nested under the requirement
they belong to, not given their own identifiers — nesting them keeps this ledger joinable to
`REQUIREMENTS.md` and `ROADMAP.md` without inventing new IDs (D-00f). The same `file:line` citation
may legitimately appear in more than one row: two requirements describing the same shipped artefact
keep separate rows and separate verdicts, because the `REQ-*` ID is the primary key, not the
citation. Two rows are never merged because they cite the same artefact.

**Evidence bar.** A `satisfied` verdict requires a `file:line` citation **plus** a named passing
test, example, or command that exercises it. A `file:line` citation with nothing exercising it is
not `satisfied` — it gets its own verdict, `present, unproven`. This bar applies to **all 86 rows
below without exception**, including every row REQUIREMENTS.md's run-4 ledger already marked with an
ingest-era status word (`Shipped`, `Verify`, `Variant`, `Code diverges`, etc.). An ingest status word
**is** the bare "the code exists" claim this bar exists to reject — it is re-derived, not carried
forward, for every one of the 86 rows.

**Manifest carve-out.** Milestone 7 is a structural milestone like Milestones 4-6, so a large share
of its requirements *are* manifest declarations — crate extraction, feature-flag shape, CI job
coverage. For those, the manifest line **plus** a named CI job or build leg that consumes it is the
exercising artefact. The M7/M8-specific anchors, all re-grepped fresh against the tree on 2026-08-08
rather than trusted from an earlier document:

- The `lint` job's zero-warning `cargo doc` step — `.github/workflows/ci.yml:58`
  (`cargo doc --workspace --no-deps 2>&1 | tee /tmp/doc-output.txt && ! grep -q "warning:" ...`).
- The workspace clippy and test steps — `.github/workflows/ci.yml:55` (`cargo clippy --workspace
  --all-targets --all-features -- -D warnings`), `:235` (`cargo test --workspace --lib --bins`),
  `:238` (`cargo test --workspace --doc`, no `--exclude`).
- The feature-matrix and library-only isolation steps in `.github/workflows/feature-flags.yml` —
  the `no-default-features` matrix leg at `:26`, the `cli-isolation` job at `:121`, and its
  "Check library (no-default-features, no cli)" step at `:138` (`cargo check --lib
  --no-default-features`).
- The per-crate, dependency-ordered publish dry-run leg in `.github/workflows/release.yml:410`
  (`cargo publish --dry-run -p "$crate"`, inside the dependency-ordered loop).

A manifest fact cited without its consuming CI job is `present, unproven`, not `satisfied` — a bare
`Cargo.toml` feature declaration is the exact false-positive class this bar exists to reject.

**Path caveats.** Six Milestone 7-8 deliverables ship as mdbook chapters under `docs/src/` rather
than at their PRD paths: `docs/PERFORMANCE_BASELINE.md`, `docs/RELEASE_CHECKLIST.md`,
`docs/VERSIONING_POLICY.md`, `docs/BUILD_BASELINES.md`, `docs/INTEGRATION_TESTS.md` and the root
`STABLE_API.md`. A row whose only divergence from its PRD is this caveat is `relocated`, not a gap.

**Workspace shape and provenance.** The workspace is, authoritatively, **ten library crates plus
`doc-examples` plus the root facade package `paladin-ai`** — verified this session via
`.planning/codebase/STRUCTURE.md:51-71` (already corrected by Phase 7; this phase inherits a correct
map) and `ls crates/`, which returns exactly: `doc-examples`, `paladin-battalion`, `paladin-content`,
`paladin-core`, `paladin-herald`, `paladin-llm`, `paladin-memory`, `paladin-notifications`,
`paladin-ports`, `paladin-storage`, `paladin-web`. The root facade is confirmed at `Cargo.toml:33`
(`name = "paladin-ai"`). Of the five crates ARCH-01 once marked "provenance pending": `paladin-storage`,
`paladin-notifications`, `paladin-content` and `paladin-web` trace to M7 Epic 1's extraction PRD and
its four-Go cost-benefit gate (`cost-benefit-assessment.md`, self-approved 2026-05-25). **`paladin-herald`
traces to the 2026-06-04 reconciliation, not to any PRD** — it was created by reconciliation commit
`66f6c4e`, inside an Epic (M8 Epic 3) whose own §5 Non-Goals state "No new crates created.
`paladin-herald`, `paladin-ml`, etc. are not in scope." That non-goal is recorded elsewhere in this
ledger (M8 Epic 3) as **overridden for `paladin-herald`, still holding for `paladin-ml`**. This is
why no ingested requirement described `paladin-herald` before ingest run 4, and why this planning
set's earlier "nine-crate workspace" figure was wrong.

**Per-milestone checkbox corroboration.** Per `intel/task-completion-state.md`: Milestone 7 is 98.8%
complete with 3 open items, all in `tasks-production-build-infra-adaptation.md` — **plausible but
uncorroborated** by any single artefact (run 4's finding, restated here rather than re-derived).
Milestone 8 is 99.1% complete with 3 open items (2 in `tasks-remove-dead-shims-empty-modules.md`, 1
in `tasks-relocate-remaining-misplaced-modules.md`) — **contradicted by code**: Epics 2 and 3 are both
verifiably complete in the tree, and Epic 3 went further than its own task list scoped. These counts
are recorded here as inputs to the per-row verdicts below, not trusted as verdicts themselves.

**Provenance.** Phase 10, plan 10-01 (wave 1 scaffold), dated 2026-08-08. This plan writes the head
notes below, the legend, the supersession summary table, all twelve section headings and all 86 row
stubs, and fully derives Milestone 8 Epic 4's four rows end-to-end. Waves 2-3 fan-out plans replace
the remaining 82 rows' Verdict and Evidence cells in place, per the contention table below.

## Verdict legend

| Verdict | Meaning |
| --- | --- |
| `satisfied` | `file:line` citation **and** a named passing test, example, or command exercising it |
| `present, unproven` | `file:line` citation exists, but nothing exercises it |
| `genuinely outstanding` | No shipped code satisfies the requirement |
| `relocated` | The deliverable exists, but at a different path than the requirement names |
| `superseded by outcome` | Shipped code answers the requirement differently than the ingested document specified, and implementing the requirement as written would undo shipped work |
| `deferred with register` | Work was removed deliberately and recorded with a reintroduction condition, distinct from work never done |
| `diverged` | The shipped tree deliberately implements the requirement differently, as distinct from a later milestone replacing it |

This wording differs deliberately from `milestone-04-06.md`'s legend, which uses `superseded by
shipped code` and `deferred with reason` — this ledger's D-02 vocabulary is `superseded by outcome`
and `deferred with register` instead. The shape, the columns, and the two callouts below are copied
from that file; the six-word class names are not.

**HARD-01 mapping callout.** HARD-01 requires four dispositions: `Shipped`, `Superseded by outcome`,
`Relocated`, `Deferred with register`. These map onto this legend as: `Shipped` → `satisfied` (once
re-derived) or `present, unproven` (where nothing exercises it yet); `Superseded by outcome` →
`superseded by outcome`; `Relocated` → `relocated`; `Deferred with register` → `deferred with
register`. `present, unproven`, `genuinely outstanding` and `diverged` are this ledger series' own
finer vocabulary, not named by HARD-01 directly.

**Tie-break rule.** A row that qualifies for both `relocated` and `superseded by outcome` takes
`relocated`, because the mdbook relocations are this corpus's single largest false-gap generator
(six documents, four run-4 requirements — see the summary table below) and collapsing the class
would destroy the moved-not-missing signal that stops a later phase from planning them as missing.

## Superseded by outcome — do not plan these as written

Placed here, at the head of the file, so a reader never has to scan 86 rows to find these.

**Counting command run this session:** `sed -n '365,381p' .planning/intel/code-verification.md |
grep -c '^|'` → **15** (13 data rows + 1 header + 1 separator = 15 lines beginning `| `). The table
holds **13 data rows**, not the "14" figure HARD-01's own text and `REQUIREMENTS.md:3136`/`:3870`
both assert (D-05). That figure is corrected at source by this same plan's Task 3.

**Caveat on this table's contents.** This table transcribes `intel/code-verification.md:365-381`'s
13 rows directly, per Task 1's instruction to build it from the table rather than from a remembered
figure. Three of the 13 rows below (marked †) are **mdbook relocations** — per this ledger's own
tie-break rule (above), their eventual per-row verdict in the epic sections is `relocated`, not
`superseded by outcome`. They appear in this summary table because `code-verification.md`'s own
table lists them under this heading; a future reader should not conclude their epic-section verdict
will read `superseded by outcome`. Two further rows (marked ‡) are HARD-05 and HARD-06 subjects
whose restated-rule ADRs (ADR-0031, ADR-0032 — Phase 10 wave 3) are expected to flip their epic-row
verdict to `satisfied`; this table's "what the tree does instead" column describes the pre-ADR
finding, which is what `code-verification.md` recorded.

| `REQ-*` ID | What the requirement says | What the tree does instead | Citation |
|---|---|---|---|
| `REQ-paladin-web-extraction` | `paladin-web` declares `actix-web` **and** `axum` as direct non-optional dependencies (M7 Epic 1 PRD §4.2.1) | Zero `actix` references in `crates/paladin-web/`; facade `web-server = ["dep:paladin-web", "dep:axum"]`. M8 Epic 7 reversed it deliberately and added a cargo-deny ban | `crates/paladin-web/`; `Cargo.toml:276`; `deny.toml:99-103` |
| `REQ-storage-feature-flags-v1` | Facade `storage-sqlite` flag; `paladin-storage` optional; `storage` alias enables both backends (M7 Epic 1 PRD §4.5.6, §7.2) | `storage-sqlite` retired; `paladin-storage` non-optional with `sqlite` always on; `storage = ["storage-mysql"]` only | root `Cargo.toml` (`paladin-storage` dependency line); commit `897e77e` |
| `REQ-sqlx-workspace-dependency` | `sqlx` workspace declaration includes `mysql` in its feature list (M7 Epic 1 PRD §7.5) | `sqlx = { version = "0.8", default-features = false, features = [...] }` — no `mysql`; `default-features = false` and `migrate` added by the RustSec hardening work | root `Cargo.toml` `[workspace.dependencies]` sqlx line |
| `REQ-ci-publish-dry-run-v1` | `publish-dry-run` runs `cargo publish --dry-run -p <crate>` for ten crates in dependency order (M7 Epic 2 FR-26) | A single `cargo publish --workspace --dry-run` ships at `ci.yml:644`, with an inline rationale that per-crate dry runs cannot work on a version bump — **but** `release.yml:410` also ships the per-crate, dependency-ordered form. The 86-row run-4 ledger's own text records this row `Coexists, not superseded`, not superseded — flagged here as a discrepancy between `code-verification.md`'s run-4 finding and a later re-check; the owning fan-out plan (10-07) re-derives which verdict holds | `.github/workflows/release.yml:410`; `.github/workflows/ci.yml:644` |
| `REQ-tensorflow-ml-feature-gate-v2` | `tensorflow_adapter.rs` gated behind a new `ml = []` feature (M8 Epic 3 PRD §4.3 item 11) | Both the adapter and the flag were deleted outright (commit `3d48768`); neither exists | `git log` commit `3d48768`; `grep -rn "tensorflow\|^ml = " Cargo.toml src/` — zero matches |
| `REQ-m8-epic3-no-extractions` | Every adapter group "Stays in facade", all List B moves deferred to Milestone 9 (M8 Epic 3 PRD §4.3, §5; `infrastructure-adapter-disposition.md`) | The relocations were executed in Milestone 8 by the reconciliation; `paladin-herald` was created despite the explicit non-goal naming it | reconciliation §7 commits `66f6c4e`, `8bd7073`, `ff829e2`, `5a7c901`, `897e77e`, `4c7857e` |
| `REQ-dead-file-batch-deletion` | `find src/ -name "*.rs" \| wc -l` = **163** after Epic 2, **160** after Epic 4 (M8 Epic 2 §7; M8 Epic 4 §4.5 item 9) | Run-4 recorded **136**. The two PRD figures are internally consistent with each other; the further reduction is the reconciliation's Category 1-2 deletions, which the PRDs did not anticipate | `find src/ -name "*.rs" \| wc -l` (run 4, `intel/code-verification.md:375`) |
| `REQ-stable-api-per-crate` † | `STABLE_API.md` at the repository root, updated by four separate run-4 requirements (M7 Epic 4 §4.6; M8 Epics 2, 4, 5) | No `STABLE_API.md` at the root. The equivalent ships as `docs/src/api-reference/stable-api.md` after the Milestone 11 overhaul. `api_surface_current.txt` (881 KB) and `final-api.txt` (198 KB) **do** exist at the root | `docs/src/api-reference/stable-api.md`; also touches `REQ-stable-api-v020-sync` (M8 Epic 5) |
| `REQ-build-baselines-doc`, `REQ-integration-tests-doc`, `REQ-performance-baseline-doc`, `REQ-release-checklist`, `REQ-versioning-policy` † | `docs/PERFORMANCE_BASELINE.md`, `docs/RELEASE_CHECKLIST.md`, `docs/VERSIONING_POLICY.md`, `docs/BUILD_BASELINES.md`, `docs/INTEGRATION_TESTS.md` (M7 Epics 2, 3, 4) | `docs/` holds only `MIGRATION_LOG.md` plus the mdbook. Equivalents ship as `docs/src/appendix/{performance-baseline,release-checklist,release-automation,integration-tests,build-baselines}.md` — the same Milestone 11 relocation run 3 documented. **Do not plan these as missing deliverables** | `docs/src/appendix/*.md` (five files) |
| `REQ-facade-role-lib-docs` | Nine leaf crates named in the facade role documentation (M8 Epic 5 FR-1) | Ten library crates ship; the FR-1 list predates `paladin-herald` | `src/README.md`; `ls crates/` (eleven entries including `doc-examples`) |
| *(no `REQ-*` owner — M7 overview Appendices A/B, not part of the 86-row inventory)* | `crates/paladin-cli/` as a workspace crate, publish Step 5 (M7 overview Appendices A and B) | No `paladin-cli` crate — re-confirming the run-3 finding. `crates/` holds `doc-examples` plus the ten library crates | `ls crates/` |
| `REQ-extracted-crate-dependency-rule` ‡ | Extracted crates depend only on `paladin-ports`, `paladin-core` and workspace-shared deps; "no extracted crate may depend on another extracted crate" (M7 Epic 1 PRD §6.1) | `crates/paladin-content/Cargo.toml` declares `paladin-llm = { ..., optional = true }` behind its `llm` feature — an extracted-to-extracted edge, but non-default and facade-gated. The PRD's own §4.4 complexity note (actually at `cost-benefit-assessment.md:118`, not the same PRD's §4.4) anticipated it without amending the rule. **HARD-05/ADR-0031 (Phase 10 wave 3) is expected to restate the rule and flip this row to `satisfied`** — recorded here as the pre-ADR finding | `crates/paladin-content/Cargo.toml:23,28` |
| `REQ-content-processing-build-gate` ‡ | Facade `content-processing` activates `paladin-content` "with all capability features enabled" (M7 Epic 1 PRD §4.4.6) | `content-processing` enables `web-scraping`, `rss`, `news-api`, `tiktoken`, `llm` — but **not** `pdf`. `paladin-content` does declare `pdf = []`, gating no dependency. **HARD-06/ADR-0032 (Phase 10 wave 3) is expected to record PDF extraction as unconditionally shipped and this feature as inert-and-deleted** — recorded here as the pre-ADR finding | `Cargo.toml:275`; `crates/paladin-content/Cargo.toml:18` |

## Row order and amendment convention

The twelve epic sections below appear in `REQUIREMENTS.md`'s own run-4 order — Milestone 7 Epics 1-4,
Milestone 8 Epics 1-7, then the cross-milestone entries — and are never re-sorted. Rows within a
section appear in the ID order `REQUIREMENTS.md` lists them. Later plans replace a row's **Verdict**
and **Evidence** cells in place; they never insert, delete, or reorder rows. Amendments follow D-00d:
edit in place, retain superseded text, date every amendment, never a separate corrections file.

**Ledger file contention** — the rule every ledger-writing plan in this phase obeys:

| Plan | Wave | Owns | May |
|---|---|---|---|
| 10-01 | 1 | the whole file | create head notes, legend, summary table, twelve section headings, all 86 row stubs; fully derive M8 Epic 4's four rows |
| 10-07 | 3 | M7 Epic 1 (12 rows) + M7 Epic 2 (13 rows) | replace Verdict and Evidence cells **in place** inside its own two sections only |
| 10-08 | 3 | M7 Epic 3 (10 rows) + M7 Epic 4 (12 rows) | same, its own two sections only |
| 10-09 | 3 | M8 Epic 1 (4) + M8 Epic 2 (4) + M8 Epic 3 (6) | same, its own three sections only |
| 10-10 | 3 | M8 Epic 5 (6) + M8 Epic 6 (4) + M8 Epic 7 (6) + Cross-milestone (5) | same, its own four sections only |
| 10-11 | 4 | a dated close-out amendment section appended at the foot | append only |

M8 Epic 4's four rows are derived by plan 10-01 and are owned by no fan-out plan. The four wave-3
plans run in parallel over **disjoint, contiguous** section ranges and perform **cell replacement
only** — never row insertion, deletion or reordering — so the four diffs are non-adjacent hunks in
one file and merge without conflict. `grep -c '^| REQ-'` reads `86` before and after every one of
them, and `git diff --numstat` on this file shows added lines equal to deleted lines for those plans.

For every row this plan does not derive, the Verdict cell reads `pending — plan 10-NN` naming the
owning fan-out plan from the table above, and the Evidence cell carries the run-4 ledger's current
text verbatim, prefixed `run-4 input (not yet re-derived):`. No cell is ever left blank.

### Milestone 7 Epic 1 — Extended Workspace Decomposition (12 IDs)

**Epic note:** All four M7 Epic 1 extraction candidates (`paladin-web`, `paladin-notifications`, `paladin-content`, `paladin-storage`) shipped, in the PRD's recommended storage→notifications→content→web order, confirmed this session by each crate's extraction-commit timestamp (`896ff05`, `2be2f75`, `a9532ca`, `309394f`); this plan flips `REQ-extracted-crate-dependency-rule` from `Code diverges → HARD-05` to `satisfied` on ADR-0031's authority — a wording fix, never a code fix, since the tree has conformed to the restated default-build invariant since the extraction shipped — and the twelve rows land as 4 `satisfied` (cost-benefit gate, notifications extraction, dependency rule, dependency-isolation metrics), 4 `superseded by outcome` (paladin-web-extraction, paladin-storage-extraction, storage-feature-flags-v1, tensorflow-stays-facade-v1), 1 `relocated` (paladin-content-extraction), 1 `diverged` (sqlx-workspace-dependency), and 2 `present, unproven` (facade-workspace-metadata, extraction-order-and-shims — both audited this session and found only partially provable).

| ID | Verdict | Evidence |
|---|---|---|
| REQ-m7-cost-benefit-gate | satisfied | `.project/Milestone_7-Production-Hardening/Epic_1/cost-benefit-assessment.md:3,5,161,167,170` — dated 2026-05-25, "Status: Final — all Go decisions self-approved"; "All four candidates receive a Go decision. No crates are deferred"; approval date 2026-05-25 (re-read this session via `grep -n 'Go/Defer Decision\|self-approved\|2026-05-25'`). Exercising artefact: the four extraction commits the gate authorized — `896ff05` (storage, "Task 2.0", 2026-05-25), `2be2f75` (notifications, "Task 3.0"), `a9532ca` (content, "Task 4.0"), `309394f` (web, "Task 5.0"), all landing within a day of the gate — confirming the Go decisions were acted on, not merely recorded. PRD sub-tasks 1.4/1.5 (mark deferred, create backlog tickets) are correctly N/A since no candidate was deferred. |
| REQ-paladin-web-extraction | superseded by outcome | `crates/paladin-web/` exists (`README.md`, `CHANGELOG.md` confirmed present this session). The PRD's own two-framework clause (§4.2.1: `actix-web` **and** `axum` as direct non-optional dependencies) is reversed by `REQ-actix-removal` (M8 Epic 7, owned by plan 10-10): `grep -rn actix crates/paladin-web/` returns zero matches this session, and `crates/paladin-web/Cargo.toml:17` declares only `axum = { version = "0.8.4" }`. Cross-referenced in this ledger's head-of-file "Superseded by outcome" summary table under this exact `REQ-*` ID. Variant group 21. Citing `REQ-actix-removal` by ID rather than re-deriving its row here, per this plan's instruction. |
| REQ-paladin-notifications-extraction | satisfied | `crates/paladin-notifications/` exists with `README.md` and `CHANGELOG.md` (confirmed this session). `Cargo.toml:18-20` declares all three named features — `email = ["dep:lettre", "dep:handlebars"]`, `push = []`, `system = []` — and `src/{email_notification_adapter,push_notification_adapter,system_notification_adapter}.rs` (confirmed via `ls`) each back one feature; the facade re-exports all three at their original paths (`src/infrastructure/adapters/notifications/mod.rs:9,14,18`). Exercising artefact: the `crate-isolation` CI matrix's `paladin-notifications` leg (`ci.yml:325-326`, build/test steps at `:359-367`), which builds and tests the crate both with and without default features on every push. |
| REQ-paladin-content-extraction | relocated | `crates/paladin-content/` exists (`README.md`, `CHANGELOG.md` present). Its `use_cases/` target directory (PRD §4.4) was renamed to `src/services/` by M8 Epic 6 — confirmed this session via `ls crates/paladin-content/src/services/`, 12 files including `content_llm_analysis_service.rs`. `content_ingestion_service.rs` did not move with the rest: it stays in the facade at `src/application/services/content/content_ingestion_service.rs` (confirmed via `grep -rn content_ingestion_service src/`), tracked as deferred item D4, register `FACADE-02` (`.planning/REQUIREMENTS.md:1558`, Phase 11). Not `superseded by outcome`: per this ledger's tie-break rule, a row eligible for both classes takes `relocated`. |
| REQ-paladin-storage-extraction | superseded by outcome | `crates/paladin-storage/` exists (README, CHANGELOG present); `mysql_content_repository.rs`, `sqlite_{content,user,workflow}_repository.rs`, `minio.rs`, `redis.rs`, `scheduler.rs` all confirmed present via `ls crates/paladin-storage/src/`. The PRD's own §4.5.2/Q4 (`prd-extract-infrastructure-crates.md:219,409`) explicitly directed that `file_content_repository.rs` "stays in the facade crate" (it is a content-delivery adapter, not a SQL repository). The tree instead deletes it outright — `grep -rn file_content_repository src/ crates/` returns zero matches — via commit `2edc031` ("chore(facade): delete unused file_content_repository (M8 Phase 2)", 2026-06-04), a deliberate M8 dead-code deletion, not an omission. Implementing the PRD's "keep it in the facade" clause as written would restore code the M8 reconciliation found and removed as unused. Variant group 28. |
| REQ-storage-feature-flags-v1 | superseded by outcome | Cross-referenced in this ledger's head-of-file "Superseded by outcome" summary table under this exact `REQ-*` ID: `storage-sqlite` is retired, `paladin-storage` is non-optional with `sqlite` always enabled. Re-confirmed this session: root `Cargo.toml:57` `paladin-storage = { workspace = true, features = ["sqlite"] }` (no `optional = true`), `:278-281` documents "SQLite repositories are always available" and only `storage-mysql`/`storage = ["storage-mysql"]` remain as optional flags. Commit `897e77e` (Reconciliation §7) made the change. Variant group 22. |
| REQ-facade-workspace-metadata | present, unproven | All four M7 Epic 1 extraction candidates are present in `[workspace.dependencies]` (`Cargo.toml:25-28`: `paladin-storage`, `paladin-notifications`, `paladin-content`, `paladin-web`) and covered by `[workspace.members] = [".", "crates/*"]` (`Cargo.toml:2`, a glob, re-confirmed this session — no crate is excluded from it). §4.6.3's second clause ("No public API paths may be silently removed") was spot-checked, not exhaustively audited: the notifications adapters remain re-exported at their original facade paths (`src/infrastructure/adapters/notifications/mod.rs:9,14,18`), but a full history diff of every facade re-export path across all four extractions was not performed this session. The manifest half of this row is proven; the no-silent-removal behavioural half is not — hence `present, unproven` rather than `satisfied`, per this plan's instruction not to reach for `satisfied` on an unaudited sub-clause. |
| REQ-extracted-crate-dependency-rule | satisfied | **The divergence was in the rule's wording, not the code.** ADR-0031 (`.planning/decisions/0031-extracted-crate-dependency-rule.md`) restates M7 Epic 1 PRD §6.1's absolute "no extracted crate may depend on another extracted crate" as a default-build invariant: no leaf-to-leaf edge in the default build; a non-default optional feature may declare one only where the facade opts in explicitly and the dependent code is `cfg`-gated. Re-confirmed this session: `crates/paladin-content/Cargo.toml:22` `llm = ["dep:paladin-llm"]` (non-default), `:28`'s dependency line `paladin-llm = { version = "0.7.0", path = "../paladin-llm", optional = true }`, `crates/paladin-content/src/services/mod.rs:7` `#[cfg(feature = "llm")]` gating exactly `content_llm_analysis_service.rs`, and root `Cargo.toml:275` `content-processing` explicitly enabling `paladin-content/llm`. Exercising artefact: `cargo tree -p paladin-content --no-default-features` (re-run this session, offline) shows zero occurrences of `paladin-llm` or any other extracted crate/facade package. The tree has satisfied the restated invariant since the extraction shipped; the PRD's unconditional wording was never amended to reflect the sibling `cost-benefit-assessment.md:118` clause that anticipated exactly this edge. This is a wording fix, not a code fix — the code never changed to comply. |
| REQ-extraction-order-and-shims | present, unproven | **Order confirmed, shim protocol not.** The PRD's recommended storage→notifications→content→web order (`prd-extract-infrastructure-crates.md:299`) was followed exactly: `git log --diff-filter=A` on each crate's `Cargo.toml` shows `896ff05` (paladin-storage, "Task 2.0", 2026-05-25), `2be2f75` (paladin-notifications, "Task 3.0"), `a9532ca` (paladin-content, "Task 4.0"), `309394f` (paladin-web, "Task 5.0"), all 2026-05-26 — re-run this session, commit messages and ordering both match. The "shim protocol" — §4.6.3's backward-compatible re-export requirement, the same clause `REQ-facade-workspace-metadata`'s row audits — was spot-checked (notifications' three adapters remain re-exported at their original paths) but not exhaustively verified across all four extractions' full re-export surfaces. Recorded `present, unproven`: the order half of this row is proven by commit evidence; the shim/re-export half is not exhaustively audited. |
| REQ-tensorflow-stays-facade-v1 | superseded by outcome | Re-confirmed this session: `grep -rn 'tensorflow\|^ml = ' Cargo.toml src/ crates/*/Cargo.toml` returns zero matches — both `tensorflow_adapter.rs` and the `ml` feature this row's PRD (M7 Epic 1's Action-required note, `:364`, "ensure `tensorflow_adapter.rs` is gated behind an `ml` feature flag ... before Task 1.4") specified were deleted outright by commit `3d48768`, not merely gated. **Cross-reference note:** the head-of-file supersession summary table carries this exact fact under `REQ-tensorflow-ml-feature-gate-v2` (M8 Epic 3's later, stricter form of the same underlying requirement, Variant group 24) — this row's own ID, `REQ-tensorflow-stays-facade-v1`, does not appear in the table by name, only its variant-group sibling does. Recorded here as a finding rather than silently forced to match: this plan's contract prohibits editing the summary table, so the cross-reference is by variant group, not literal ID. Variant group 24. |
| REQ-sqlx-workspace-dependency | diverged | Re-read this session, `Cargo.toml:29`: `sqlx = { version = "0.8", default-features = false, features = ["runtime-tokio-rustls", "sqlite", "chrono", "uuid", "json", "migrate"] }`. Two differences from PRD §7.5's explicit form: (1) `mysql` is absent from the workspace feature list — `paladin-storage`'s own manifest now gates it behind its own `mysql` feature instead (`storage-mysql = ["paladin-storage/mysql"]`, root `Cargo.toml:280`); (2) `default-features = false` and `migrate` were both added. Commit `3b65b57` ("chore: begin rustsec remediation hardening", "Tighten sqlx/MySQL exposure in default builds") is the exact diff: `git show 3b65b57 -- Cargo.toml` shows the workspace `sqlx` line changing from `{ version = "0.8", features = ["runtime-tokio-rustls", "sqlite", "mysql", "chrono", "uuid", "json"] }` to the current form, with the facade's own consumer line narrowing from `sqlx = { workspace = true, features = ["mysql"] }` to `sqlx = { workspace = true }` in the same commit. Both changes trace to the RustSec/SEC-01 hardening work, not to Epic 1 itself. Verdict is `diverged`, not `satisfied`: the tree deliberately implements the dependency differently than the PRD specified, for a documented security reason. |
| REQ-dependency-isolation-metrics | satisfied | M7 Epic 1 PRD §8 Success Metrics 7-8 (`prd-extract-infrastructure-crates.md:393-394`) require `cargo tree -p paladin-core --all-features` and `cargo tree -p paladin-battalion --all-features` to contain none of `actix-web`, `axum`, `lettre`, `pdf-extract`, `scraper`, `sqlx`. Both commands re-run this session, offline, against the current lockfile: `cargo tree -p paladin-ai-core --all-features --offline` (193 lines) and `cargo tree -p paladin-battalion --all-features --offline` (291 lines), each piped through `grep -E 'actix-web|axum|lettre|pdf-extract|scraper|sqlx'` — both greps exit 1 (zero matches). The dependency-isolation invariant these two success metrics assert holds today, measured directly rather than inferred from the manifest. |

### Milestone 7 Epic 2 — Production Build Infrastructure (13 IDs)

**Epic note:** Of the thirteen rows, five carried `Verify → HARD-01` stubs (never actually checked) in the run-4 text — `makefile-workspace-targets`, `ci-workflow-triggers`, `ci-per-crate-matrix`, `ci-integration-job`, `integration-test-placement` — and all five were checked this session against the current `ci.yml`, `Makefile`, `feature-flags.yml` and `release.yml` (four resolving to `satisfied`, `ci-integration-job` to `diverged` since it ships via a materially different mechanism than FR-25 specifies); every `ci.yml` citation in this section was re-derived by direct `grep -n` this session rather than carried forward — the run-4 exclusion citation naming the mid-220s of the workflow file and the stale `:644`/`:141` line numbers are absent from every row below — and `REQ-docker-workspace-build` is recorded closed by Phase 9 per D-04 with its citation re-run rather than transcribed, landing the thirteen rows at 7 `satisfied` (docker-workspace-build, makefile-workspace-targets, makefile-per-crate-targets, ci-workflow-triggers, ci-per-crate-matrix, ci-feature-flag-matrix, integration-test-placement), 4 `diverged` (ci-workspace-job, ci-integration-job, and both publish-dry-run rows — v1 and v2 kept as two separate rows and two separate verdicts per D-00f, each shipping via a different mechanism than its own FR specifies), and 2 `relocated` (build-baselines-doc, integration-tests-doc).

| ID | Verdict | Evidence |
|---|---|---|
| REQ-docker-workspace-build | satisfied (closed by Phase 9) | Closed by Phase 9, citing ADR-0027 (`.planning/decisions/0027-dockerfile-chef-planner-stage.md`) and commit `52b1943` — this row is one of Phase 9's seven D-04 hand-off rows (`.planning/REQUIREMENTS.md:1320-1355,1347-1348`); not re-verified from scratch. **Citation re-run this session, not transcribed:** `Dockerfile.chef:10,13` — `FROM rust:1.93-slim-bookworm AS chef`, `RUN cargo install cargo-chef --version 0.1.77 --locked`; `:26-30` — the planner stage's rewritten comment plus `COPY Cargo.toml Cargo.lock ./`, `COPY src ./src`, `COPY crates ./crates`, `COPY benches ./benches`, `RUN cargo chef prepare --recipe-path recipe.json` — no per-crate manifest enumeration anywhere in the file (`grep -c 'COPY crates/paladin' Dockerfile.chef` → `0`, re-run this session). Planner-stage crate coverage is structural (`COPY crates ./crates`), so an eleventh crate needs no edit here — the defect ADR-0027 recorded (a stale nine-manifest enumeration omitting `paladin-herald`) cannot recur by construction. |
| REQ-build-baselines-doc | relocated | `docs/BUILD_BASELINES.md` does not exist at its PRD path (confirmed this session, `test -f` fails). `docs/src/appendix/build-baselines.md` exists (confirmed via `ls docs/src/appendix/`, present alongside 33 sibling mdbook chapters) — the Milestone 11 mdbook relocation this ledger's head-note path caveat names. Do not plan as missing. |
| REQ-makefile-workspace-targets | satisfied | All eight workspace-level FR-10 through FR-17 targets exist with the exact commands the PRD (`prd-production-build-infra-adaptation.md:95-109`) specifies, re-read this session: `test` (`Makefile:115-118`, `cargo test --workspace --lib --bins`), `test-doc` (`:120-123`, `cargo test --workspace --doc`), `test-all` (`:145-146`, `test test-doc test-integration`), `lint` (`:238-239`), `fmt` (`:233-234`), `check` (`:253-256`, `cargo check --workspace --all-targets`), `clean-code` (`:293-294`, `fmt lint lint-shell check`), `doc` (`:288-289`). Exercising artefact: `make check`/`make test` are runnable directly; each target's own `run:`-equivalent recipe line is the command re-grepped above. |
| REQ-makefile-per-crate-targets | satisfied | All ten per-crate targets confirmed present this session, at updated line numbers (the run-4 `Makefile:167-212` citation has shifted +15 lines since): `test-core` (`Makefile:181-184`) through `test-facade` (`:226-229`), covering all ten library crates plus the facade (`test-core`, `test-ports`, `test-battalion`, `test-llm`, `test-memory`, `test-storage`, `test-notifications`, `test-content`, `test-web`, `test-facade`). Each recipe runs `$(CARGO) test -p <crate>` (or bare `paladin` for the facade). |
| REQ-ci-workflow-triggers | satisfied | Never checked in run-4 (`Verify → HARD-01`); checked this session. FR-22 requires triggering on push to `main`/`feature/**` and PR targeting `main`. `ci.yml:9-13` triggers on `push: branches: [main, develop, 'feature/**', 'release/**']`, `pull_request: branches: [main, develop]`, plus `workflow_dispatch` — a strict superset of FR-22's minimum (every FR-22 case still triggers CI, plus `develop`, `release/**` pushes, PRs targeting `develop`, and manual dispatch). Superset satisfies the "must trigger on" requirement; no case FR-22 names is missing. |
| REQ-ci-per-crate-matrix | satisfied | Never checked in run-4; checked this session. The `crate-isolation` job (`ci.yml:316-368`, named differently than FR-23's suggested `test-crate` but functionally matching) runs a `matrix.include` of exactly ten entries — `paladin-ai-core`, `paladin-ports`, `paladin-battalion`, `paladin-llm`, `paladin-memory`, `paladin-storage`, `paladin-notifications`, `paladin-content`, `paladin-web`, `paladin-ai` (the facade) — confirmed via direct read of `:321-343`. Each leg runs `cargo build -p <crate>` (default, `:359-360`), `cargo build -p <crate> --no-default-features` (`:362-363`), then `cargo build`/`cargo test -p <crate> <extra_flags>` (`:365-368`), satisfying FR-23's "Executes `cargo test -p <crate-name>` for each matrix entry" in substance. Cache keying on the OS/toolchain/lockfile hash confirmed at `:350-357`. |
| REQ-ci-workspace-job | diverged | Re-derived this session — the run-4 citation naming a mid-220s line for a `--exclude paladin-ports` clause on the workspace doc-test step is stale by both line number and content and does not appear anywhere in the current file (`grep -c 'exclude paladin-ports' .github/workflows/ci.yml` → `0`). The current bare workspace doc-test step is `ci.yml:238` (`cargo test --workspace --doc`, no `--exclude` of any crate) — DEBT-03's discharge, cited to ADR-0033. The clippy step is `ci.yml:55` (`cargo clippy --workspace --all-targets --all-features -- -D warnings`), the doc-warning gate is `ci.yml:58`, and the unit-test step is `ci.yml:235` (`cargo test --workspace --lib --bins`). Verdict is `diverged`, not `satisfied`: FR-24 specifies one `test-workspace` job containing `cargo test --workspace`, `cargo clippy ...`, and `cargo fmt --all --check` together, gated on the per-crate matrix (`needs: [crate-isolation]`); the shipped tree splits these across two independent jobs instead — `lint` (fmt at `:52`, clippy at `:55`, doc-warnings at `:58`) and `test` (`:203-238`, matrixed over `rust-version: [stable, beta]`) — and neither carries a `needs:` dependency on `crate-isolation` (`grep -n 'needs:' ci.yml` lists `needs: [lint]`, `[lint, test]` ×2, `[docker]`, `[integration-tests]`; `crate-isolation` is never a dependency target). The three required checks all run on every push, just not co-located or ordered as FR-24 specifies. The Makefile residue (`--exclude paladin-ports` in `release-check`) was already deleted by plan 10-06 — confirmed this session, `Makefile:427-430`'s `release-check` target runs the bare `@$(CARGO) test --workspace --doc` with no exclusion and no adjacent stale echo. |
| REQ-ci-integration-job | diverged | Never checked in run-4; checked this session. The `integration-tests` job exists (`ci.yml:371-475`) and runs equivalent test coverage, but by a different mechanism than FR-25 specifies: it starts `redis` and `minio` via native GitHub Actions `services:` blocks (`:374-399`), not `docker-compose -f docker/docker-compose.test.yml up -d`; it carries no MySQL service at all (`services:` lists only `redis` and `minio`); and it has no `needs:` dependency on the `test`/`lint` jobs (`grep -n 'needs:' ci.yml` confirms `integration-tests` is absent from the dependency list). It does run `cargo test redis_queue_integration_tests --release -- --ignored --nocapture` and the file-storage equivalent (`:452-460`), and it tears down/uploads artifacts on failure (`:468-475`). Ships, functions, but via native services + no explicit gating rather than docker-compose + `needs: [test-workspace]`. |
| REQ-ci-publish-dry-run-v1 | diverged | The per-crate, dependency-ordered form FR-26 specifies ships — but in `.github/workflows/release.yml:385-410` (Milestone 10 Epic 3's release pipeline), not in `ci.yml`'s `publish-dry-run` job as FR-26 names it. `release.yml:395-405` lists the ten crates in the same dependency order FR-26 specifies (`paladin-ai-core` … `paladin-ai`), and `:410` runs `cargo publish --dry-run -p "$crate"` inside that per-crate loop when `dry_run=true`. `release.yml`'s own triggers (`:3-17`) are `push: tags: ['v*.*.*']` and `workflow_dispatch`, not "runs only on pushes to `main`" as FR-26 specifies for its `publish-dry-run` job. Cross-referenced in this ledger's head-of-file "Superseded by outcome" summary table (not superseded — coexists, per that table's own caveat on this row). Variant group 23. |
| REQ-ci-publish-dry-run-v2 | diverged | `ci.yml:895-922` (job `publish-dry-run`, re-derived this session — the run-4 `ci.yml:644` citation is stale by 278 lines and does not resolve against the current file). `:898` `needs: [lint, test]`; `:899` `if: github.event_name == 'push' && github.ref == 'refs/heads/main'` — this matches FR-26's trigger/gating shape exactly (push to `main`, gated on the workspace checks). The mechanism diverges from FR-26's per-crate loop: `:922` runs a single `cargo publish --workspace --dry-run`, with an inline comment (`:918-921`) stating the per-crate form "cannot work on a version bump" because a not-yet-published sibling version fails its dependents' `version = "X"` requirement. No PRD or DOC names this v2 form, so it has no precedence standing over v1's PRD-cited mechanism — recorded here as the form CI actually gates on `main`. Variant group 23. |
| REQ-ci-feature-flag-matrix | satisfied | Re-derived this session — the run-4 `feature-flags.yml:115,118,141` citations for the library-only isolation test are off by 3 lines for the last one and confirmed correct for the first two: the `feature-matrix` job's Build/Test command lines are `:115` (`cargo build --workspace ${{ matrix.flags }}`) and `:118` (`cargo test --workspace --lib ${{ matrix.flags }}`), re-confirmed exact; the `cli-isolation` job is at `:121-150`, with its library-only check at `:137-138` (`cargo check --lib --no-default-features`), not `:141` as run-4 recorded — `:141` in the current file is inside the `cli_isolation` regression-test step, a different check. Matches this ledger's own head-note manifest carve-out anchors exactly (`no-default-features` leg `:26`, `cli-isolation` job `:121`, library check `:138`). Fifteen feature-matrix legs confirmed via `matrix.include` (`:24-84`). |
| REQ-integration-test-placement | satisfied | Never checked in run-4; checked this session. FR-29 (cross-crate tests live in workspace-root `tests/`, depend on the facade) and FR-30 (single-crate tests live in that crate's own `tests/`) both hold: workspace-root `tests/integration/` holds the multi-crate integration suite (confirmed via `ls`, dozens of files including `redis_queue_integration_test.rs`-class tests consumed by `ci.yml:463,466`), and `crates/paladin-web/tests/auth_rbac.rs` is a single-crate test living in that crate's own `tests/` directory (confirmed via `ls crates/paladin-web/tests/`) — the one extracted crate with a crate-local integration suite. `scripts/run_integration_tests.sh:307,317,327` runs `cargo test --workspace --features ... <test-name>`, matching FR-31's "appropriate per-test invocation" allowance rather than the literal `--test '*'` form. |
| REQ-integration-tests-doc | relocated | `docs/INTEGRATION_TESTS.md` does not exist at its PRD path (confirmed this session). `docs/src/appendix/integration-tests.md` exists (confirmed via `ls docs/src/appendix/`). Do not plan as missing. |

### Milestone 7 Epic 3 — Benchmark Suite Migration (10 IDs)

**Epic note:** All ten rows are re-derived directly from the tree rather than from `benchmark-assessment.md:167-176`'s own self-asserted "PRD Success Metrics Status" table (all six of its own metrics recorded Satisfied by its own hand — exactly the self-completion claim this ledger's evidence bar rejects); `cargo bench --workspace --no-run` was attempted this session against HEAD (see `REQ-workspace-bench-execution` for the result); the ten rows land at 8 `satisfied` (the five per-crate/root benches, the workspace compile-validation row, the config-ownership finding folded into its own row, and the CI regression-signal job), 1 `superseded by outcome` (the five-file disabled-bench disposition — three deprecated-and-removed, two removed-and-replaced-at-narrower-scope), and 1 `relocated` (the performance-baseline document, which does not close the separate `QUAL-05` runtime-measurement requirement).

| ID | Verdict | Evidence |
|---|---|---|
| REQ-sanctum-bench-migration | satisfied | `crates/paladin-memory/benches/sanctum_benchmarks.rs` (418 lines, re-confirmed this session) — imports rewritten to `paladin_core::platform::container::sanctum::{MemoryBuilder, MemoryType, SanctumEntry}`, `paladin_memory::sanctum::InMemorySanctum`, `paladin_ports::output::sanctum_port::{SanctumFilter, SanctumPort, SanctumQuery}` (`:14-16`, re-grepped this session, matching `.project/Milestone_7-Production-Hardening/Epic_3/benchmark-assessment.md:95-100`'s migration notes). Criterion registration owned by `crates/paladin-memory/Cargo.toml:44-46` (`[[bench]] name = "sanctum_benchmarks"`). Exercising artefact: `cargo bench -p paladin-memory --bench sanctum_benchmarks --no-run`, part of this session's `cargo bench --workspace --no-run` attempt (see `REQ-workspace-bench-execution`). |
| REQ-disabled-bench-disposition | superseded by outcome | Re-confirmed this session via `git log --diff-filter=D --oneline`: commit `10f9a4e` ("chore: deprecate legacy root benchmark files") deleted all five root-owned files in one commit — `benches/arsenal_benchmarks.rs.disabled` (358 lines) and `benches/paladin_benchmarks.rs.disabled` (296 lines), **deprecated and removed outright**, no replacement anywhere in the tree today; `benches/herald_benchmarks.rs` (323 lines), **deprecated and removed outright**, no replacement; `benches/battalion_benchmarks.rs` (982 lines) and `benches/garrison_benchmarks.rs` (299 lines), **removed, then replaced at narrower scope** by commit `d346ab9` ("feat: add crate-local critical path benchmarks"), which created `crates/paladin-battalion/benches/battalion_benchmarks.rs` (232 lines — Formation/Phalanx/Campaign/Chain-of-Command only, not the old file's full orchestration-plus-visualization surface) and `crates/paladin-memory/benches/garrison_benchmarks.rs` (88 lines — read/write at 100/1,000/10,000 entries only, not the old file's mixed `ConversationHistory` operations). `.project/Milestone_7-Production-Hardening/Epic_3/benchmark-assessment.md:104-114` records the per-file rationale. None of the five was directly restored, so this is `superseded by outcome` rather than a gap. |
| REQ-battalion-benchmarks | satisfied | `crates/paladin-battalion/benches/battalion_benchmarks.rs` (232 lines, re-confirmed this session, created by commit `d346ab9`) — `benchmark_formation_three_agents:80`, `benchmark_phalanx_five_agents:103`, `benchmark_campaign_branching_dag:126`, plus `benchmark_chain_of_command:160` added later by Phase 6 plan 06-04. Criterion registration: `crates/paladin-battalion/Cargo.toml:34-36`. Exercising artefact: `cargo bench -p paladin-battalion --bench battalion_benchmarks --no-run` (part of the workspace attempt) and `.github/workflows/ci.yml:809-857`'s `benchmark-regression-signal` job, which runs `cargo bench -p paladin-battalion --bench battalion_benchmarks -- --noplot` on every PR/manual dispatch (non-blocking). |
| REQ-llm-serialization-benchmark | satisfied | `crates/paladin-llm/benches/llm_serialization_benchmarks.rs` (87 lines, re-confirmed this session, created by commit `d346ab9`) — `benchmark_request_serialization:47`, `benchmark_response_deserialization:57`, `benchmark_response_roundtrip:69`, all `serde_json`-only; `grep -n 'reqwest\|http::\|Client::new' crates/paladin-llm/benches/llm_serialization_benchmarks.rs` returns zero matches this session, confirming FR-13's "excludes live HTTP calls and remote provider latency". Criterion registration: `crates/paladin-llm/Cargo.toml:46-48`. Exercising artefact: `cargo bench -p paladin-llm --bench llm_serialization_benchmarks --no-run` and the CI regression-signal job's leg (`ci.yml:809-857`). |
| REQ-garrison-benchmarks | satisfied | `crates/paladin-memory/benches/garrison_benchmarks.rs` (88 lines, re-confirmed this session) — `benchmark_garrison_write:34` and `benchmark_garrison_read:62`, both iterating exactly `[100usize, 1_000, 10_000]` (`:38,66`, re-grepped this session), matching FR-14's three mandated sizes. Criterion registration: `crates/paladin-memory/Cargo.toml:48-50`. Exercising artefact: `cargo bench -p paladin-memory --bench garrison_benchmarks --no-run` and the CI regression-signal job's leg. |
| REQ-config-loading-benchmark | satisfied | `benches/config_benchmarks.rs` (43 lines, re-confirmed this session) — `benchmark_settings_new` and `benchmark_domain_config_accessors`, covering `Settings::new()` plus seven domain accessors. Criterion registration: root `Cargo.toml:254-256`. **Ownership finding closing PRD Open Question 1** (`prd-benchmark-suite-migration.md:210`): `src/config/settings.rs:27` — `pub struct Settings` (re-confirmed this session) — still lives in the root `paladin` facade crate; no extracted crate declares or re-exports it, so the benchmark correctly stays root-owned rather than migrating. Exercising artefact: `cargo bench --bench config_benchmarks --no-run` and the CI regression-signal job's leg (`ci.yml:809-857`). |
| REQ-critical-path-bench-scope | satisfied | Re-derived independently of `benchmark-assessment.md:167-176`'s own "PRD Success Metrics Status" table, which records all six metrics Satisfied by its own hand — exactly the self-asserted-completion claim this ledger's evidence bar rejects. FR-16 (`prd-benchmark-suite-migration.md:106-113`) names four critical-path categories, each checked directly against the bench source this session: Battalion orchestration — Formation/Phalanx/Campaign confirmed at `battalion_benchmarks.rs:80,103,126`; LLM serialization overhead — confirmed HTTP-free in `llm_serialization_benchmarks.rs`; Garrison read/write — confirmed at the three mandated sizes in `garrison_benchmarks.rs:38,66`; Config loading — confirmed in `config_benchmarks.rs`. All four categories are genuinely covered by the shipped bench files; verdict rests on this session's independent check of the source, not on the assessment's own table. |
| REQ-workspace-bench-execution | satisfied | `cargo bench --workspace --no-run` was run this session against HEAD and **completed successfully**: `Finished \`bench\` profile [optimized] target(s) in 10m 41s`, producing executables for all five critical-path benches — `benches/config_benchmarks.rs`, `benches/battalion_benchmarks.rs`, `benches/llm_serialization_benchmarks.rs`, `benches/garrison_benchmarks.rs`, `benches/sanctum_benchmarks.rs` — plus the workspace's other crate-doc bench targets, none of which errored. This is the structural compile-validation command `.project/Milestone_7-Production-Hardening/Epic_3/benchmark-assessment.md:134,158` names; the completed run, not the mere existence of the bench files, is what closes this row `satisfied` rather than `present, unproven`. |
| REQ-performance-baseline-doc | relocated | `docs/PERFORMANCE_BASELINE.md` does not exist at its PRD path (confirmed this session, `test -f` fails). `docs/src/appendix/performance-baseline.md` exists (confirmed via `ls` this session; its most recent run entry, dated 2026-08-05, records only a Chain-of-Command benchmark addition, not a full-suite re-measurement). This row does **not** close `QUAL-05` (`REQUIREMENTS.md:347-354`, Phase 3, Complete — traceability row at `REQUIREMENTS.md:3767`), which owns producing the measured **runtime** numbers themselves (throughput, P50/P95/P99 latency, memory-per-Paladin, startup time) — this row only closes the requirement that a baseline document exists and is populated, and the two are recorded as separate rows so they are never conflated. |
| REQ-bench-regression-signal | satisfied | `.github/workflows/ci.yml:809` — job `benchmark-regression-signal`, re-confirmed this session (line number unchanged from run-4). Threshold `THRESHOLD=3` Criterion `Performance has regressed.` notices, evaluated at `:860-861` and the exceed check at `:871-874` (re-read this session); non-blocking via `continue-on-error: true` at the job level (`:813`) and `if: github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch'` (`:812`, PR/manual-dispatch only). Exercising artefact: the job itself, which runs all four critical-path benches (`config_benchmarks`, `battalion_benchmarks`, `garrison_benchmarks`, `llm_serialization_benchmarks`, `:849-852`) and evaluates the regression count against the threshold. |

### Milestone 7 Epic 4 — API Stabilization & Pre-Release Preparation (12 IDs)

**Epic note:** Twelve rows land at 7 `satisfied` (six closed by Phase 9 — `REQ-crate-metadata-completion`, `REQ-per-crate-changelog`, `REQ-rustsec-risk-acceptance`, `REQ-rustsec-hardening-actions`, `REQ-license-policy-signoff`, `REQ-paladin-ports-publish-verification-closed`, cited per D-04 rather than re-verified, plus `REQ-release-readiness-audit` recorded as closed history citing ADR-0029), 2 `present, unproven` (`REQ-per-crate-readme` — ten-of-ten confirmed with no CI enforcer; `REQ-doc-coverage-audit` — citing ADR-0033's seven-crate doctest-flag list), and 3 `relocated` (`REQ-versioning-policy`, whose `superseded by outcome` half is stated in its own cell per the tie-break rule and cites ADR-0029; `REQ-release-checklist`; `REQ-stable-api-per-crate`, whose two stray root artefacts named by the head-of-file supersession table are both re-confirmed present); all six Phase-9 citations — the register file, the two guard scripts, the manifest licence field, and the crate/lib name split — were re-run this session against `REQUIREMENTS.md:1320-1355`'s hand-off block and none had moved from where it names them.

| ID | Verdict | Evidence |
|---|---|---|
| REQ-crate-metadata-completion | satisfied (closed by Phase 9) | Closed by Phase 9, citing ADR-0025 (`.planning/decisions/0025-licence-posture.md`) per D-04 hand-off item 4 (`REQUIREMENTS.md:1342-1344`, commits `a587e5a`/`7ee741c`). **Citation re-run this session, not transcribed:** root `Cargo.toml:33` `name = "paladin-ai"`, `:46` `[lib] name = "paladin"`; `crates/paladin-core/Cargo.toml:2` `name = "paladin-ai-core"`, `:15` `[lib] name = "paladin_core"` — both renames applied with lib names preserved. `Cargo.toml:40` and all ten `crates/*/Cargo.toml` read `license = "MIT OR Apache-2.0"` (re-grepped this session, zero holdouts) — agrees with the signed checklist per ADR-0025, closing the SEC-02 contest. Not re-opened. |
| REQ-per-crate-readme | present, unproven | All ten library crates have a `README.md` (re-confirmed this session via `ls crates/*/README.md` — ten matches, zero missing). No CI step checks for README existence: `grep -rn README .github/workflows/*.yml scripts/*.sh` returns only an unrelated doc-examples check (`scripts/check-doc-examples.sh:38`); `scripts/check-changelogs.sh` and `scripts/check-crate-names.sh` are both wired into CI (`ci.yml:95,98`) but no equivalent `check-readmes.sh` exists. The file-existence half is proven; nothing exercises it, so `present, unproven` rather than `satisfied`. |
| REQ-per-crate-changelog | satisfied (closed by Phase 9) | Closed by Phase 9, citing commit `0458b6a` per D-04 hand-off item 5 (`REQUIREMENTS.md:1345-1346`). **Citation re-run this session:** `ls crates/*/CHANGELOG.md` returns ten matches including `crates/paladin-herald/CHANGELOG.md` (re-confirmed present, the file the hand-off names `0458b6a` as creating). `scripts/check-changelogs.sh` exists and is wired into CI at `.github/workflows/ci.yml:95` (re-grepped this session), mechanically enforcing the ten-of-ten invariant. Not re-opened. |
| REQ-doc-coverage-audit | present, unproven | Citing ADR-0033 (`.planning/decisions/0033-cargo-doc-warning-bar.md`), written this phase by plan 10-06. The coverage posture is real for the crates whose doctests actually execute — `paladin` (facade, 96 passed/17 ignored), `paladin_core` (49 passed/38 ignored), `paladin_battalion` (28 passed/50 ignored), `paladin_ports` (96 passed/94 ignored), per ADR-0033 Finding 3 — and unmeasured for the **seven** crates that still set `[lib] doctest = false`: `paladin-content`, `paladin-memory`, `paladin-storage`, `paladin-notifications`, `paladin-llm`, `paladin-herald` and `doc-examples` (re-grepped this session, `grep -n doctest crates/*/Cargo.toml`, same seven crates ADR-0033 lists). Two of those seven (`paladin-llm`, `paladin-memory`) actually compile and pass doctests despite the flag — a Cargo-toolchain nuance ADR-0033 records but does not decide. `.project/Milestone_7-Production-Hardening/Epic_4/epic-4-completion-summary.md:63` already carries a dated (2026-08-08) correction annotation citing this same ADR-0033 finding, superseding the row's original "Met" claim. Phase 15 / the coverage-and-CI quality gates owns changing the posture. Not `satisfied`: the number is written, but the claim is not proven across all crates. |
| REQ-versioning-policy | relocated | No document exists at `docs/VERSIONING_POLICY.md` or any other repo path (confirmed this session via `find . -iname '*versioning*polic*'` outside `.planning`/`.project`, zero matches). Under this ledger's tie-break rule, a row eligible for both `relocated` and `superseded by outcome` takes `relocated` — the moved-not-missing signal this class exists to preserve so a later phase does not plan a document search for a page that was never written in the first place. **`superseded by outcome` half, stated per this plan's instruction:** the lockstep `0.2.0` target M7 Epic 4 PRD Appendix C and the M7 overview's own Appendix C both anchored was superseded by what shipped — all ten crates published at `0.1.0` instead, per ADR-0029's `## Trajectory` table (`.planning/decisions/0029-version-trajectory-history.md:79-91`, re-read this session). Citing ADR-0029 for the supersession half. |
| REQ-release-checklist | relocated | `docs/RELEASE_CHECKLIST.md` does not exist at its PRD path (confirmed this session, `test -f` fails). `docs/src/appendix/release-checklist.md` and `docs/src/appendix/release-automation.md` both exist (confirmed via `ls docs/src/appendix/` this session) — the same Milestone 11 mdbook relocation this ledger's head-note path caveat names. Do not plan as missing. |
| REQ-stable-api-per-crate | relocated | No `STABLE_API.md` at the repository root (confirmed this session, `test -f` fails). `docs/src/api-reference/stable-api.md` exists (confirmed via `head` this session — "Version: 0.5.0", "Last Updated: 2026-06-02"). The two stray root artefacts the head-of-file supersession table names as the reason no root document exists are both still present, re-measured this session: `api_surface_current.txt` (900,883 bytes ≈ 881 KB) and `final-api.txt` (202,356 bytes ≈ 198 KB) — the ledger row and the summary table agree. |
| REQ-release-readiness-audit | satisfied (history) | Citing ADR-0029 (`.planning/decisions/0029-version-trajectory-history.md`). Every gate `PASS` — `cargo test --workspace`, `cargo clippy --workspace -- -D warnings`, `cargo fmt --all -- --check`, `cargo doc --workspace --no-deps`, `cargo publish --dry-run` per crate, `cargo audit`, licence compatibility — recorded in `release-readiness-audit-report.md` (dated 2026-05-28), closing `Recommendation: GO for release candidate tagging`; tag `v0.1.0-rc.1` at commit `a9530fc` on 2026-05-28 (re-confirmed this session via `git log -1 --format=%H%n%cs a9530fc`); `post-release-verification-v0.1.0-rc.1.md` confirms all ten crates resolved on docs.rs including the `paladin-ai-core`/`paladin_core` package-lib split, plus an external smoke project compiling against `paladin-ai = "0.1.0"`. **Recorded as closed history, not current state**: the tree is now at `0.7.0` (`Cargo.toml:34`, re-confirmed this session) with tags `v0.7.0` and `v0.7.1` four minors past rc.1 — no reader should treat the rc.1 artefact as live. |
| REQ-rustsec-risk-acceptance | satisfied (closed by Phase 9) | Closed by Phase 9, citing ADR-0024 (`.planning/decisions/0024-rustsec-exception-governance.md`) per D-04 hand-off item 1 (`REQUIREMENTS.md:1332-1334`). **Citation re-run this session, not transcribed:** `SECURITY-EXCEPTIONS.md` holds exactly **10** `id = ` entries (re-counted this session, `grep -c '^id = '`), each carrying `owner = "DF3NDR"` and `review_date = "2026-12-31"` (re-grepped this session) — the one register for all live suppressions, superseding the original two-advisory acceptance's 2026-09-30 expiry. Not re-opened. |
| REQ-rustsec-hardening-actions | satisfied (closed by Phase 9) | Closed by Phase 9, citing ADR-0024, per D-04 hand-off item 2 (`REQUIREMENTS.md:1335-1338`). **Citation re-run this session:** `.cargo/audit.toml` still names five advisories in `[advisories] ignore` (re-read this session, including the corrected `RUSTSEC-2026-0187` parenthetical this phase's plan 10-05 wrote per D-19), each now owner/expiry-governed centrally in `SECURITY-EXCEPTIONS.md` (10 rows, confirmed above) rather than in `audit.toml` itself, mechanically kept in sync per `scripts/check-advisory-register.sh` (`ci.yml:101`, re-grepped this session). Post-mitigation `cargo audit`/`cargo deny check` re-audit evidence remains **CI-only, not run in this environment** — `crates.io` returns HTTP 403 here, unchanged from Phase 9's own scoping. Not re-opened. |
| REQ-license-policy-signoff | satisfied (closed by Phase 9) | Closed by Phase 9, citing ADR-0025, per D-04 hand-off item 3 (`REQUIREMENTS.md:1339-1341`). **Citation re-run this session:** root `Cargo.toml:40` and all ten `crates/*/Cargo.toml` declare `license = "MIT OR Apache-2.0"` (re-grepped this session, zero holdouts), matching the signed 551-package checklist ADR-0025 records. The PRD's single-`MIT` claim is annotated superseded per ADR-0025's own Code Locations. Not re-opened. |
| REQ-paladin-ports-publish-verification-closed | satisfied (closed by Phase 9) | Closed by Phase 9, citing ADR-0026 (`.planning/decisions/0026-crate-name-collision-guard.md`), per D-04 hand-off item 7 (`REQUIREMENTS.md:1349-1351`). **Citation re-run this session:** `scripts/check-crate-names.sh` exists and is wired into CI at `.github/workflows/ci.yml:98` (re-grepped this session, step `run: ./scripts/check-crate-names.sh`), an offline pre-dry-run guard against the collision risk the deferred-verification document flagged. ADR-0026 records the accepted residual cost (a genuinely novel name still needs a human crates.io check). Not re-opened. |

### Milestone 8 Epic 1 — Facade Crate Audit (4 IDs)

**Epic note:** All four rows are re-derived directly against the tree and against ADR-0028 rather than the run-4 ledger's own ingest-era status words — two land `superseded by outcome` (`REQ-facade-file-classification`, `REQ-facade-audit-document`, both citing the same ~4,400 LOC factual mis-classification the head-of-file summary table names under `REQ-facade-file-classification`'s own ID), one lands `present, unproven` (`REQ-facade-file-inventory` — the file-counting act survives, only its List C conclusion is superseded), and one lands `satisfied` (`REQ-shim-consumer-validation` — the orphan test it asked for is the reproducible three-step procedure ADR-0028 preserved verbatim).

| ID | Verdict | Evidence |
|---|---|---|
| REQ-facade-file-inventory | present, unproven | The 189-file inventory happened and still exists at `facade-audit.md` Appendix A — its own Summary (`facade-audit.md:26`) reads "Total files audited: 189", matching `find src/ -name "*.rs" \| wc -l` at audit time (2026-05-29). **The inventory's existence survives; its List C conclusions do not** — ADR-0028 (`.planning/decisions/0028-m8-reconciliation-authoritative.md`) supersedes the classification built on top of this inventory, not the file-counting act itself. Recorded `present, unproven` rather than `satisfied`: the inventory is a `file:line` artefact with no independent test or command exercising its 189-file completeness today, only the original count re-read this session (`facade-audit.md:26-29`, re-confirmed via direct read). See `REQ-facade-file-classification`'s row for the superseded half. |
| REQ-facade-file-classification | superseded by outcome | Citing ADR-0028. `facade-audit.md:26-28` (re-read this session): "Total files audited: 189", "Files staying (List C): 151", "Files to move (List B): 13", "Files to delete (List A): 25" — the classification's own figures. `facade-audit.md:118`'s List C intro line names the 151-file "stays" set as containing "active bridge shims". **The factual reason, not a procedural one:** `facade-cleanup-RECONCILIATION-2026-06-04.md:19-22` (cited in ADR-0028) establishes that approximately **4,400 LOC** of that "stays" set were orphaned, uncompiled duplicate files — `input/file_content_fetcher.rs`, `input/file_content_list_fetcher.rs`, `input/http_content_fetcher.rs`, `input/local_file_fetcher.rs`, `input/news_api_fetcher.rs`, `logs/error_log_adapter.rs`, `document/document_adapter.rs`, `document/pdf_extractor.rs`, `repositories/mysql_content_repository.rs`, `output/api_content_deliverer.rs` (`facade-audit.md:171,175`, ADR-0028 Code Locations) — none of which had a `mod <name>;` declaration anywhere under `src/`, so none was ever compiled, despite being classified "active bridges that stay". The reconciliation (`facade-cleanup-RECONCILIATION-2026-06-04.md`) is the account that replaces this classification. `facade-audit.md:3-14` (re-read this session) carries the dated 2026-08-08 banner plan 10-02 added, naming ADR-0028 and retaining the original text unmodified per D-00c. |
| REQ-shim-consumer-validation | satisfied | The reconciliation's reproducible orphan test is the version to keep, per ADR-0028 `## Decision (i)`: (1) `rg "mod <candidate_file_stem>" src/` returns zero matches for each orphaned file; (2) `cat src/<containing_directory>/mod.rs` shows only a `pub use paladin_<crate>::...` re-export chain with no `mod <name>;` line; (3) `test -f crates/paladin-<crate>/src/<equivalent path>` confirms the destination leaf crate already holds an equivalent, live, compiled file. Citing ADR-0028 `## Decision (i)` by section rather than restating the procedure in this cell, per this plan's instruction — a ledger cell that paraphrases a procedure is how the procedure drifts. Exercising artefact: re-run this session against `output/api_content_deliverer.rs` — `rg "mod api_content_deliverer" src/` returns zero matches, `src/infrastructure/adapters/output/` no longer exists (the whole group was deleted, category 1), confirming the test's own logic still holds against the current, further-cleaned tree. |
| REQ-facade-audit-document | superseded by outcome | Citing ADR-0028. `facade-cleanup-RECONCILIATION-2026-06-04.md:5` (ADR-0028 Code Locations) names `Epic_1/facade-audit.md` by path: "**Supersedes (corrects):** `Epic_1/facade-audit.md` and `Epic_3/infrastructure-adapter-disposition.md`." The document is annotated superseded at source — `facade-audit.md:1-14`, re-read this session — with a dated 2026-08-08 banner naming ADR-0028, describing the ~4,400 LOC mis-classification, and stating "The original text below is retained unmodified" (D-00c), added by plan 10-02. The audit's own read-only-during-Epic-1 gate (`facade-audit.md`'s original §6/§8 clauses) is unaffected; only its List C classification is what the banner and ADR-0028 correct. |

### Milestone 8 Epic 2 — Dead Shim & Empty Module Removal (4 IDs)

**Epic note:** All four rows land `satisfied` on absence evidence re-run this session, including the last, which is upgraded from the run-4 ledger's unactioned `Verify → HARD-01` stub to `satisfied` having actually been checked against `src/lib.rs`'s current ten `pub use` lines. `find src/ -name "*.rs" | wc -l` reads **136** this session (matching `code-verification.md:375`'s run-4 figure and this ledger's own head-of-file supersession-table row for `REQ-dead-file-batch-deletion`), well below both PRD-anticipated figures (163 after Epic 2, 160 after Epic 4) because the reconciliation's Category 1-2 deletions removed further files the PRDs did not anticipate.

| ID | Verdict | Evidence |
|---|---|---|
| REQ-dead-file-batch-deletion | satisfied | All 25 List A files and their five orphaned directories are gone, re-confirmed this session by direct `ls`: `src/application/notifications/` — `No such file or directory`; `src/application/storage/` — `No such file or directory`; `find src -type d -iname subject` — zero matches; `src/core/platform/manager/admin/` and `.../user/` — both `No such file or directory`; `find src/infrastructure -iname "access_log_adapter.rs" -o -iname "push_notification_adapter.rs"` — zero matches. `src/application/` now holds only `cli`, `errors`, `mod.rs`, `services` (re-confirmed via `ls`), matching the batch-deletion PRD's post-state. **Residue, carried per this plan's instruction rather than dropped:** the `email_notifications.rs` (392 LOC) overlap review the Epic 2 PRD's own Open Question 1 required — whether it duplicated the 752-LOC `email_notification_adapter.rs` — is recorded nowhere; the file was deleted outright by commit `cf17559` ("chore(facade): delete dead notification fallback adapters (M8 Phase 3)", re-confirmed via `git show --stat cf17559`) alongside its 320-LOC `system_notification_adapter.rs` fallback sibling, without a written overlap finding. The deletion itself does not change verdict on this residue's account — the batch is gone and the tree builds — but the review's absence is preserved here rather than silently closed. Variant group 27; see `REQ-notification-task-closeout`'s row (M8 Epic 3) for the companion fact that the two deleted fallback files were dead-duplicate, not merely unreviewed. |
| REQ-stale-application-ports-audit | satisfied | `ls src/application/ports/` (re-run this session) returns `No such file or directory` — the directory did not exist even at Epic 1 audit time and was removed in a prior milestone, confirmed by this session's fresh check rather than carried forward. `grep -rn "application::ports::" src/ crates/ tests/ examples/ benches/ --include="*.rs"` (re-run this session) returns zero matches, so no stale import path survives to be updated. Exercising artefact: the same zero-match grep, which would fail to compile any lingering reference — `cargo check --offline --workspace --lib` is the compile-time exerciser this absence claim rests on. |
| REQ-core-minimum-structure | satisfied | `find src/core/ -name "*.rs" \| sort` (re-run this session) returns exactly six files: `src/core/mod.rs`, `src/core/platform/mod.rs`, `src/core/platform/manager/mod.rs`, `src/core/platform/manager/content_service.rs`, `src/core/platform/manager/event_manager.rs`, `src/core/platform/manager/user_service.rs` — matching the PRD's named six exactly, re-confirmed via direct `ls src/core/`, `ls src/core/platform/`, `ls src/core/platform/manager/` this session (no `admin/` or `user/` sub-tree present). `src/core/platform/manager/mod.rs` declares exactly the three modules the requirement names (`content_service`, `event_manager`, `user_service`) and nothing else. Exercising artefact: `cargo check --offline --workspace --lib`, which would fail to compile a dangling `mod` declaration for any file this count omits. |
| REQ-libr-dead-reexport-removal | satisfied | Re-derived this session — never individually checked in run-4 (`Verify → HARD-01`). `grep -c "^pub use" src/lib.rs` (re-run this session) returns **10**, down from the ~50 the requirement's own acceptance criteria describe as the pre-cleanup baseline (`intel/requirements.md:4472`). `src/lib.rs:158-188` (re-read this session, the "Stable Public API — Short-path Aliases" block) carries exactly the five confirmed-consumer exception groups the requirement names and no others: `paladin_llm::provider_factory::LlmProviderFactory` (`:167`), the four provider adapters gated per-feature (`:169-179`), `paladin_llm::mock::{MockLlmAdapter, MultiStepMockLlmPort}` (`:181`), `core::platform::container::paladin::{Paladin, PaladinData, PaladinStatus}` and `paladin_config::PaladinConfig` (`:184-185`), and the Battalion config/error pair (`:188`) — plus the feature-gated `application::cli` re-export (`:156`), which is a module, not a type alias, and is explicitly excluded from the alias-removal scope. Every named alias still resolves at its source path (`paladin_llm::`, `core::platform::container::`), satisfying the requirement's "only the alias is removed, not the type" clause. Exercising artefact: `cargo check --offline --workspace --lib`, which would fail to compile any consumer of a removed alias — none exists in the current tree. |

### Milestone 8 Epic 3 — Relocate Remaining Misplaced Modules (6 IDs)

**Epic note:** All six rows are re-derived against the tree and ADR-0028 rather than the run-4 ledger's own status words, landing at 1 `satisfied` (`REQ-notification-task-closeout`), 1 `satisfied` carrying a do-not-re-delete marker (`REQ-storage-shim-deletion`), 1 `satisfied` carrying both a do-not-re-delete marker and this section's own factual correction (`REQ-garrison-sanctum-bridges-kept`), and 3 `superseded by outcome` (`REQ-adapter-disposition-record`, `REQ-tensorflow-ml-feature-gate-v2`, `REQ-m8-epic3-no-extractions`) — Epic 3 is complete in substance, not punted to Milestone 9: 15 commits (`e5b2011`, `2edc031`, `3d48768`, `ca7e4e8`, `8bd7073`, `66f6c4e`, `897e77e`, `ff829e2`, `5a7c901`, `cf17559`, `6bfcdb7`, `6704807`, `4c7857e`, `74ddf11`, `a1e4901`), ~10,250 net LOC removed (measured 10,252, ADR-0028 `## Decision (iii)`), one new leaf crate (`paladin-herald`).
Per D-09, the §5 non-goal "No new crates created — `paladin-herald`, `paladin-ml`, etc. are not in scope" is **overridden for `paladin-herald`** (created, `crates/paladin-herald/` present) and **still holding for `paladin-ml`** (`test -d crates/paladin-ml` exits 1, re-confirmed this session via `ls crates/` — absent).
**The third correction, epic-wide rather than row-specific:** the remainder of Category 1's orphaned files genuinely were orphaned and were correctly removed — `mysql_content_repository.rs` (780 LOC), the five `input/*` fetchers (`file_content_fetcher.rs`, `file_content_list_fetcher.rs`, `http_content_fetcher.rs`, `local_file_fetcher.rs`, `news_api_fetcher.rs`), `document/*` (`document_adapter.rs`, `pdf_extractor.rs`), `output/api_content_deliverer.rs` (724 LOC), and `logs/error_log_adapter.rs` (875 LOC) — all ten deleted outright in commit `e5b2011` with zero build or test impact, confirming the orphan test's verdict (ADR-0028 `## Decision (ii)`, third bullet). Do not re-audit these ten as if the original "active bridge" classification still applied.

| ID | Verdict | Evidence |
|---|---|---|
| REQ-notification-task-closeout | satisfied | Both halves recorded, per this plan's instruction. **Adapters live in `paladin-notifications` with a facade re-export:** `crates/paladin-notifications/src/` holds `email_notification_adapter.rs`, `push_notification_adapter.rs`, `system_notification_adapter.rs` (re-confirmed this session via `ls`), and `src/infrastructure/adapters/notifications/mod.rs:7-20` (re-read this session) re-exports all three under `#[cfg(feature = "notifications")]`. **The channel files were deleted rather than moved, twice over:** the *application*-layer `src/application/notifications/{email_notifications,push_notifications,system_notifications}.rs` the M8 overview's own success criteria said would be "moved" were instead deleted as orphaned dead code in Epic 2 Batch 1 (`email_notifications.rs` 392 LOC, the other two 0 LOC, `intel/requirements.md:4430`) — `src/application/mod.rs` never declared `pub mod notifications;`, so they were unreachable from the module tree and had zero workspace consumers; and separately, the facade's *infrastructure*-layer fallback duplicates (`email_notification_adapter.rs` 752 LOC, `system_notification_adapter.rs` 320 LOC, compiled only under `#[cfg(not(feature = "notifications"))]`) were deleted outright by commit `cf17559` ("chore(facade): delete dead notification fallback adapters (M8 Phase 3)", re-confirmed via `git show --stat cf17559`: 1,089 lines deleted across the two files plus a 27-line `mod.rs` simplification). Task 3.1's own documentation-only close-out ("no files may be moved, added or deleted") was honoured — this row's requirement is a close-out record, not the deletion act itself — and the deleted half is why a later reader might think a relocation is missing when in fact the source files were dead-duplicate on both layers. Variant group 27; see `REQ-dead-file-batch-deletion`'s row (M8 Epic 2) for the companion residue on the unreviewed overlap. |
| REQ-storage-shim-deletion | satisfied | **DO NOT RE-DELETE:** the `sqlite_*_repository.rs` files were **not** redundant — they were the active default-build implementation, resolved by making `paladin-storage` non-optional rather than deleting shims naively. `crates/paladin-storage/src/` (re-confirmed via `ls` this session) holds `sqlite_content_repository.rs`, `sqlite_user_repository.rs`, `sqlite_workflow_repository.rs` alongside `mysql_content_repository.rs`, `minio.rs`, `redis.rs`, `scheduler.rs` — all present and live. Root `Cargo.toml:57` (re-read this session): `paladin-storage = { workspace = true, features = ["sqlite"] }` — no `optional = true` — with the inline comment at `:278` "SQLite repositories are always available: `paladin-storage` is a non-optional dependency with its `sqlite` feature enabled"; only `storage-mysql = ["paladin-storage/mysql"]` (`:280`) remains optional. Commit `897e77e` ("refactor(storage): make `paladin-storage` non-optional, drop facade sqlite fallbacks (M8 Phase 3)") is the exact commit that executed this resolution — cited and re-confirmed via `git log --oneline` this session. Do not re-introduce a facade-local sqlite fallback on the theory that the leaf crate's sqlite support is optional; it no longer is. |
| REQ-adapter-disposition-record | superseded by outcome | Citing ADR-0028, three separable facts on this one row, per this plan's instruction. **(1)** `infrastructure-adapter-disposition.md:24-44` (re-read this session, post-annotation) runs a 20-row table in which every row reads "**Stays**", and the tree contradicts it directly — row 3 (`document/`), rows 7-11 (`input/*` fetchers) and row 16 (`output/api_content_deliverer.rs`) are all gone, deleted in commit `e5b2011` (see this section's epic note). **(2)** Two rows disagree with the document's own governing PRD: row 1 (`arsenal/`) marks it an M9 extraction candidate targeting `paladin-arsenal`, while `prd-relocate-remaining-misplaced-modules.md:145` states "Stays in facade... **No**" for the same group; row 19 (`sanctum/mod.rs`) invents a target crate "future `paladin-sanctum` (M9)", while the same PRD's §8 Q2 (`:293-299`) resolves sanctum as folding into the Milestone 9 `paladin-memory` extraction, naming no `paladin-sanctum` crate. **(3)** Neither invented crate exists: `ls crates/` (re-run this session) returns `doc-examples`, `paladin-battalion`, `paladin-content`, `paladin-core`, `paladin-herald`, `paladin-llm`, `paladin-memory`, `paladin-notifications`, `paladin-ports`, `paladin-storage`, `paladin-web` — eleven entries, zero named `paladin-arsenal` or `paladin-sanctum`. **`Phase 11 / FACADE-04` owns these two non-existent crate names** (ADR-0028 `Downstream Consumers`), so the two statements do not have to be rediscovered together. The document is annotated superseded at source (`infrastructure-adapter-disposition.md:1-11`, re-read this session, dated 2026-08-08 banner naming ADR-0028, added by plan 10-02, original text retained per D-00c). Its own `Date: 2025-01` header (`:14`) remains inconsistent with every other M8 document, dated but not corrected by this row — a residue noted, not fixed, per D-23's no-`.rs`/no-rewrite boundary. Variant group 26. **Amended 2026-08-08 (plan 11-05):** `.planning/registers/facade-04-m9-candidate-triage.md` triages all 20 rows of the table named above against ADR-0028's executed range and the live tree: **14 `done`, 6 `not a candidate`, 0 `still open`**, summing to 20 — superseding both of `11-RESEARCH.md`'s internal figures ("11 of the 20" and a separate "13 rows" tally that omits row 12 despite row 12's own cell reading `done`). The triage records `paladin-arsenal` and `paladin-sanctum` as artefacts of a mis-written table rather than future crates, on three grounds including `ls crates/` (eleven entries, neither name present, re-confirmed this session) — the same finding this row's fact (3) already states, now with a dedicated register behind it. |
| REQ-tensorflow-ml-feature-gate-v2 | superseded by outcome | Citing the head-of-file summary table's own entry under this exact ID. Both the gate and the adapter were deleted outright rather than gated: `grep -rn "tensorflow\|^ml = " Cargo.toml src/` (re-run this session) returns zero matches — commit `3d48768` ("chore(facade): remove half-built user CLI + tensorflow ML stub (M8)", re-confirmed via `git log --oneline`) removed both `tensorflow_adapter.rs` and the `ml = []` feature the PRD asked to add, rather than adding the feature and gating the module as §4.3 item 11 specified. Cross-referencing `REQ-deferred-tensorflow-ml-adapter-v3` (M8 cross-milestone entries, plan 10-10's range) by ID rather than deriving its row here, per this plan's instruction — that row records the `paladin-ml` leaf-crate placement condition as the live deferred artefact. Variant group 24. |
| REQ-garrison-sanctum-bridges-kept | satisfied | Both bridges remain with consumer evidence: `src/infrastructure/adapters/garrison/mod.rs` and `.../sanctum/mod.rs` (re-read this session) both still exist as `pub use paladin_memory::{garrison,sanctum}::...` re-export shims with backward-compatible sub-module paths (`in_memory_garrison`, `sqlite_garrison`). Consumer evidence re-confirmed this session via `grep -rln "adapters::garrison\|adapters::sanctum"`: `src/application/cli/config/loader.rs`, `src/infrastructure/mod.rs`, five `examples/*.rs` files (`garrison_semantic_search.rs`, `garrison_in_memory.rs`, `garrison_persistent.rs`, `paladin_with_sanctum.rs`, `sanctum_basic_inmemory.rs`, `sanctum_configuration.rs`), and four `tests/integration/*.rs` files (`paladin_garrison_integration_test.rs`, `sqlite_garrison_integration_test.rs`, `rag_integration_tests.rs`, `in_memory_sanctum_tests.rs`, `qdrant_sanctum_tests.rs`) plus `tests/cli/tool_integration_test.rs` — matching the §8 Q1-Q2 resolved-decisions record's own consumer list in substance. **DO NOT RE-DELETE:** this row also carries the registry marker per this plan's instruction — `paladin_registry.rs` was not a duplicate; the facade's 418-LOC implementation was richer than battalion's 67-LOC `pub(crate)` copy (`facade-audit.md:371`'s "stays" disposition), so the richer implementation was **consolidated into `paladin-battalion`** by commit `ca7e4e8` ("refactor(battalion): consolidate `HashMapPaladinRegistry` into `paladin-battalion` (M8 Phase 3)", re-confirmed via `git show --stat ca7e4e8`: the facade's `src/infrastructure/adapters/paladin_registry.rs` (418 lines) was deleted and `crates/paladin-battalion/src/in_memory_registry.rs` gained 379 lines, now `pub struct HashMapPaladinRegistry` at `:64`, re-confirmed via `grep -n` this session) — not deleted blindly. Do not re-delete `paladin_registry.rs`'s logic as if it were orphaned; it is now battalion's own registry implementation. **This row's own factual correction:** `output/api_content_deliverer.rs` is **724 LOC, not 629** — 629 LOC belongs to `tensorflow_adapter.rs` (`intel/requirements.md:4513`'s corrected figure, "corrected from 629") — and the file was later deleted anyway, in commit `e5b2011` (this section's epic note), so the mis-attribution has no live consequence today. |
| REQ-m8-epic3-no-extractions | superseded by outcome | Citing ADR-0028. The relocations **executed inside Milestone 8**: `git log --oneline e5b2011~1..a1e4901 \| wc -l` (ADR-0028 `## Decision (iii)`, re-derived independently of the reconciliation's own prose) returns **15** commits, and `git diff --shortstat e5b2011~1 a1e4901` returns "70 files changed, 1010 insertions(+), 11262 deletions(-)" — **net 10,252 lines removed**, matching the reconciliation's stated "~10,250 net LOC removed" to within rounding — with one new leaf crate, `paladin-herald`, created in this same range (commit `66f6c4e`). This row's ID appears in the head-of-file "Superseded by outcome" summary table with the consistent statement "The relocations were executed in Milestone 8 by the reconciliation; `paladin-herald` was created despite the explicit non-goal naming it", citing reconciliation §7 commits `66f6c4e`, `8bd7073`, `ff829e2`, `5a7c901`, `897e77e`, `4c7857e` (a representative subset of the full 15-commit range this row's own cell measures in full) — the two cells agree in substance. **Must not be planned as Milestone 9 candidates** — they already executed here. Variant group 25. **Amended 2026-08-08 (plan 11-05):** `.planning/registers/facade-04-m9-candidate-triage.md` is the row-level confirmation that no relocation inside ADR-0028's `e5b2011~1..a1e4901` range is re-planned by this phase — every one of the triage's 14 `done` rows resolves inside that range and is marked `done` by outcome, not by claim, and the triage's own tally states zero rows remain `still open`. |

### Milestone 8 Epic 4 — `use_cases` → `services` Rename (4 IDs)

**Epic note:** Fully derived by plan 10-01 (Phase 10 wave 1), dated 2026-08-08 — the end-to-end proof
of this ledger's head-note → legend → section → row → summary-table mechanism, before the 86-row
fan-out runs. Selected because it is the only complete section whose rows cite no ADR this phase has
not yet written, so it proves the mechanism without a forward reference. All citations below were
re-read this session; none is copied from the run-4 ledger text.

| ID | Verdict | Evidence |
|---|---|---|
| REQ-use-cases-services-rename | satisfied | `src/application/services/` exists with eleven sub-modules — `analysis`, `arsenal`, `battalion`, `content`, `herald`, `log_orchestrator`, `notification_orchestrator`, `orchestration`, `paladin`, `queue_orchestrator`, `sanctum` (verified via `ls src/application/services/`, this session). Exercising artefact: the workspace-wide zero-match grep — `grep -rn "use_cases" src/ crates/ tests/ examples/ benches/ --include="*.rs"` returns **zero matches** (re-run this session; confirms the run-4 ledger's own claim of zero matches still holds against the current tree) |
| REQ-rename-clean-break | satisfied | `grep -rn "pub use services as use_cases" src/` returns **zero matches** (re-run this session) — no compatibility shim of any kind exists; the overview's optional Task 4.3 (an alias) was explicitly rejected by the PRD. Exercising artefact: the same zero-match grep above, which would also catch any lingering `use_cases` re-export, since the literal string is what is grepped |
| REQ-rename-doc-updates | present, unproven | Run-4 recorded `Verify → HARD-01` — "the 57 markdown references were not individually re-checked". Re-derived this session: `grep -rln "use_cases" --include="*.md" .` outside `.planning/`, `.git/` and `.project/` returns `CHANGELOG.md`, `docs/src/api-reference/migration-guide.md`, `docs/src/api-reference/stable-api.md`, `benches/BENCHMARK_FIXES.md` and `.github/copilot-instructions.md`. The first three **intentionally** document the old path as migration guidance (correct usage — a migration guide is supposed to name the path being migrated from). `.github/copilot-instructions.md`'s Architecture diagram (imported into `CLAUDE.md`) still shows `application/use_cases/` as the live module structure with no rename annotation — a genuine stale reference this row surfaces, but its fix sits outside this plan's `.planning`/`.project`/three-file config boundary (D-23) and outside `.github/`'s scope entirely. **Not `satisfied`**: the "57 references" claim was not individually re-checked in full, and at least one non-migration stale reference exists |
| REQ-rename-changelog-breaking | satisfied | `CHANGELOG.md:670-686` — "Services Directory Rename (Milestone 8, Epic 4)" carries the full breaking-change migration table (eleven old→new module paths). Exercising artefact: cross-checked this session against `ls src/application/services/` — all eleven documented target module names (`paladin`, `battalion`, `arsenal`, `content`, `herald`, `orchestration`, `log_orchestrator`, `notification_orchestrator`, `queue_orchestrator`, `sanctum`, `analysis`) exist exactly as named, confirming the changelog's table is accurate and not stale |

### Milestone 8 Epic 5 — Facade Role Documentation & v0.2.0 Finalization (6 IDs)

**Epic note:** Six rows land at 2 `superseded by outcome` (`REQ-facade-role-lib-docs`, the exact ID the head-of-file summary table already carries for FR-1's nine-crate list predating `paladin-herald`; `REQ-m8-final-quality-gate`, ADR-0033 ratifying the shipped zero-warning bar over FR-19's weaker one without claiming the tree clears it today), 1 `relocated` (`REQ-stable-api-v020-sync`), and 3 `satisfied` (`REQ-facade-readme`; `REQ-changelog-v020-cut` and `REQ-api-surface-baseline-v020`, both as history) — this section is where three of this phase's own artefacts (ADR-0032, ADR-0033, and Phase 8's DEBT-01 closure) do the resolving work, and where a fresh finding (a stale mdbook citation of the pre-rename `project/current-exports.txt` path, out of DEBT-01's own `.project/`-scoped closure) surfaces without changing any row's verdict.

| ID | Verdict | Evidence |
|---|---|---|
| REQ-facade-role-lib-docs | superseded by outcome | This exact `REQ-*` ID already appears in the head-of-file supersession summary table (built by plan 10-01, byte-unchanged by this plan) — recorded here independently rather than duplicating that text verbatim. `src/lib.rs:25` — `## Facade Crate Role` (re-read this session) — documents the assembly-point/composition-root role (`:27`), `ServiceRunner` (`:32`), and the one-directional dependency rule (`:60-61`, "facade → leaf crates (one direction only)... Leaf crates must never import from the facade"). FR-1 (`prd-document-facade-crate-role.md`) names **nine** leaf crates; `ls crates/` (re-run this session) returns eleven entries — ten library crates plus `doc-examples` — because `paladin-herald` did not exist when FR-1 was written. The documentation's role/dependency-direction content is accurate; only its enumerated crate count is stale by one, which is why the class is `superseded by outcome` rather than `satisfied` outright. |
| REQ-facade-readme | satisfied | `src/README.md` (3,750 bytes, re-confirmed this session via `wc -c`) carries `# Paladin Facade Crate` (`:1`), a `## Role` prose section (`:7`) describing the assembly point/composition root/`ServiceRunner`/application services/CLI/binaries, a `## What lives here` table (`:51-62`) with the required Path/Purpose/Notes columns covering exactly the seven required rows (`src/application/services/`, `src/application/cli/`, `src/config/`, `src/infrastructure/`, `src/core/`, `src/bin/`, `src/main.rs` — cross-checked this session against `ls src/`, all seven present and no others), the dependency-flow rule (`### Dependency direction`, `:32-49`), and a `STABLE_API.md` reference (`:83`). Exercising artefact: this session's direct cross-check of the table's seven paths against the tree, confirming the table's content is accurate, not merely present. **Residual, not this row's own defect:** the same file's `## Leaf Crates` table (`:65-77`) lists **nine** crates — the same staleness `REQ-facade-role-lib-docs` records for `lib.rs`'s FR-1 list — and its `STABLE_API.md` link (`:83`, `../STABLE_API.md`) targets a path absent from the repository root (relocated to `docs/src/api-reference/stable-api.md`, this ledger's own path caveat). FR-2's acceptance criteria require only that the file "reference `STABLE_API.md`", which it does literally, so neither defect changes this row's verdict. |
| REQ-stable-api-v020-sync | relocated | No `STABLE_API.md` exists at the repository root (confirmed this session, `test -f` fails). `docs/src/api-reference/stable-api.md` exists (confirmed via `ls docs/src/api-reference/` this session: `crate-map.md`, `feature-flags.md`, `migration-guide.md`, `stable-api.md`, four files, all present) — the Milestone 11 mdbook relocation this ledger's head-note path caveat names. FR-4 to FR-8's comprehensive-audit-and-update deliverable applies to that relocated chapter rather than to a root file that no longer exists; per this ledger's tie-break rule, a row eligible for both `relocated` and `superseded by outcome` takes `relocated`. Not independently re-audited against the post-Milestone-8 workspace this session — confirmed as the correct relocated target only, per this plan's own instruction. |
| REQ-changelog-v020-cut | satisfied (history) | `CHANGELOG.md:666` — `## [0.2.0] - 2026-05-30` (re-confirmed this session via `grep -n '^## \[0\.'`), carrying the Services Directory Rename breaking-change table FR-9 through FR-12 require. Citing ADR-0029's `## Trajectory` table (`.planning/decisions/0029-version-trajectory-history.md:79-90`) rather than re-deriving the version story: the `v0.2.0` row is recorded `shipped`, and the tree is now four minors past it — `Cargo.toml:34` reads `version = "0.7.0"` (re-confirmed this session), with `v0.7.1`/`v0.7.0` at the head of `git tag --sort=-v:refname` (re-run this session). Recorded as closed history, not current state, matching this ledger's own treatment of `REQ-release-readiness-audit` (M7 Epic 4). |
| REQ-api-surface-baseline-v020 | satisfied (history) | FR-13/FR-14's literal ask — regenerate `api_surface_current.txt` and `final-api.txt` bearing a `v0.2.0 baseline — 2026-05-30` header — was executed at the time: `git log --oneline -- api_surface_current.txt final-api.txt` (re-run this session) shows the most recent touching commit is `a33e22d` ("chore(m8-e5): regenerate API surface baseline files for v0.2.0"), and both files still open with `# v0.2.0 baseline — 2026-05-30` (re-read this session, first line of each) — matching the PRD's own Success Metrics table row-for-row. Sizes re-measured this session: `api_surface_current.txt` 900,883 bytes (≈881 KB), `final-api.txt` 202,356 bytes (≈198 KB) — the same "stray root artefacts" the head-of-file summary table's `REQ-stable-api-per-crate` row already cites. Recorded `satisfied (history)`: FR-13's header is an explicit historical snapshot requirement, not an evergreen freshness bar, and it was met at the time. **Distinct from a different artefact's own defect:** `intel/code-verification.md`'s run-4 (2026-07-30) ties "regenerating the baseline" to the `api-surface` CI job's own `.project/current-exports.txt` mechanism (DEBT-01's subject) — re-checked this session, DEBT-01 is `[x]` Complete (Phase 8, `.planning/REQUIREMENTS.md:828-889`): `ci.yml:184` and both scripts' defaults (`scripts/check-api-surface.sh:6`, `scripts/extract-public-api.sh:6`) already read `.project/current-exports.txt` (446,377 bytes, re-run `ls -la` this session), most recently regenerated by Phase 8 commits `1f7ea39` and `ce38132`, both after this row's own `a33e22d`. **Fresh residual, out of this row's scope:** `docs/src/api-reference/stable-api.md` (`REQ-stable-api-v020-sync`'s subject) still documents the pre-rename `project/current-exports.txt` path in at least four places (`:322,330,349,356`, re-grepped this session) — DEBT-01's own closure record scoped its fix to `.project/` PRD/DOC sources, not the `docs/src/` mdbook, so this chapter was never in its nine-reference count. Recorded rather than fixed, per D-23's boundary; not this row's own requirement. |
| REQ-m8-final-quality-gate | superseded by outcome | Citing ADR-0033. FR-19 (`prd-document-facade-crate-role.md:168`, struck) required `cargo doc --workspace --no-deps` to exit 0 with "warnings acceptable; must not fail" — a strictly weaker bar than M7's zero-warning requirement on the identical command. CI already runs the stricter zero-warning form in the required `lint` job (`.github/workflows/ci.yml:58`, quoted verbatim in ADR-0033 Finding 1), so FR-19's clause is `superseded by outcome` — ratification of what shipped, not a new decision. **The measured state, kept separate from the ratification:** ADR-0033 re-ran the exact CI command against commit `c048938` and it exits 1. The tree produces **20 warnings** across four crates — `paladin-web` 13, `paladin-ai` (facade) 3, `paladin-battalion` 3, `paladin-herald` 1 (13+3+3+1=20; `grep -c '^warning: '` on the captured output returns 24, the 20 individual warnings plus 4 per-crate summary lines). Six of the twenty are pre-existing (recorded in `milestone-04-06.md:129` on 2026-08-06); fourteen are newer (`paladin-web`'s 13 plus `paladin-herald`'s 1 — neither crate existed or was documented to this depth at that measurement). This row's own text states plainly that the tree does not clear this gate today, rather than reading as a claim of present compliance. **Phase 16 / DOCS-03** is ADR-0033's named owner for clearing the warnings, since doing so needs `.rs` doc-comment edits outside this phase's D-23 boundary. FR-19 also now carries a dated (2026-08-08) correction banner at source (`prd-document-facade-crate-role.md:14-18`, re-read this session), citing ADR-0033, added by plan 10-06. |

### Milestone 8 Epic 6 — `paladin-content` Services Rename (4 IDs)

**Epic note:** Four rows land at 3 `satisfied` (`REQ-paladin-content-services-rename`, `REQ-paladin-content-readme-update`, `REQ-content-processing-build-gate`) and 1 `genuinely outstanding` — `REQ-paladin-content-changelog-fix`, a fresh finding this session: its `Verify → HARD-01` stub was never actually checked, and the crate's `CHANGELOG.md` carries no entry describing the rename or the `E0432` errors it fixed at all. The rename itself is real and shipped; only the paperwork FR-8 requires is missing.

| ID | Verdict | Evidence |
|---|---|---|
| REQ-paladin-content-services-rename | satisfied | Citing ADR-0028 (iii). `crates/paladin-content/src/lib.rs:14` — `pub mod services;` (re-confirmed this session); `crates/paladin-content/src/services/` exists (re-confirmed via `ls`, thirteen files — twelve `content_*_service.rs` plus `mod.rs`). Workspace-wide zero-match search, re-run this session: `grep -rn 'use_cases' src/ crates/ tests/ examples/ benches/ --include="*.rs"` → zero matches. This closes the broken bridge the Epic 6 DOC's Root Cause section describes (the Epic 4 rename scope was facade-only; the facade's re-export bridge moved to `services` while `paladin-content` still published `use_cases`, breaking the `content-processing` feature flag). Epic 6 is complete despite the 2026-06-04 reconciliation's own §2 table rating it "Not verified; low priority" (`facade-cleanup-RECONCILIATION-2026-06-04.md:37`) and `deferred-items.md` omitting it entirely — this is the row that stops Epic 6 from being planned as outstanding work. |
| REQ-paladin-content-readme-update | satisfied | `crates/paladin-content/README.md` (re-read this session) carries zero `use_cases` references and uses `services` vocabulary throughout: the module description (`:12`, "`services`: Processing workflows and orchestration logic."), the import example (`:18`, `use paladin_content::services;`), and the type-name example (`:22`, `services::content_processing_service::ContentProcessingService`) — matching FR-7's three named occurrence categories exactly. Exercising artefact: `grep -n 'use_cases\|services' crates/paladin-content/README.md`, re-run this session, returning only `services` hits. **Residual, not this row's own defect:** the same README's "Feature Flags" section (`:27`) still lists `pdf: Enable PDF extraction and parsing support` — the feature ADR-0032/plan 10-05 deleted from the crate's manifest — a staleness FR-7's rename scope does not cover and plan 10-05's PDF-gate fix did not touch; recorded under `REQ-content-processing-build-gate`'s row rather than fixed here, per D-23's boundary. |
| REQ-paladin-content-changelog-fix | genuinely outstanding | FR-8 requires a `### Fixed` sub-section entry under `## [Unreleased]` in `crates/paladin-content/CHANGELOG.md` describing the `use_cases` → `services` rename, the resolution of six `E0432 unresolved import` errors in the facade's `content/mod.rs` re-export bridge, and that the errors were previously masked by the `content-processing` feature gate. Re-read this session in full (26 lines): the `### Fixed` entry that exists (`:25-26`) reads only "Crate metadata and README linkage validated for crates.io release preparation" — it names neither the rename, nor `E0432`, nor the feature-gate masking. `grep -n 'E0432\|rename\|content/mod.rs\|masked' crates/paladin-content/CHANGELOG.md` (re-run this session) returns zero matches anywhere in the file. The rename itself is real and shipped (`REQ-paladin-content-services-rename`'s row, `satisfied`) — only the changelog entry FR-8 requires is missing. Never checked in run-4 (`Verify → HARD-01`); checked this session and found genuinely absent, not merely unaudited. **Owner assigned 2026-08-08 (plan 10-11 close-out follow-up): Phase 16 / DOCS-03** — the same documentation-currency owner ADR-0033 names for the rustdoc-warning residue. This is the ledger's only `genuinely outstanding` row; leaving it unowned would contradict the phase's own standard that nothing outstanding is left without a named owner. |
| REQ-content-processing-build-gate | satisfied | Citing ADR-0032. `crates/paladin-content/Cargo.toml:18-22` (re-read this session, post-plan-10-05 deletion) declares exactly five features — `web-scraping`, `rss`, `news-api`, `tiktoken`, `llm`; `pdf = []` is gone (`grep -cE '^pdf +=' crates/paladin-content/Cargo.toml` → `0`, re-run this session). Root `Cargo.toml:275` `content-processing` enables all five of the crate's surviving features (`paladin-content/web-scraping`, `/rss`, `/news-api`, `/tiktoken`, `/llm`) — full parity now, five of five, where the pre-ADR state was five of six. `pdf-extract` remains an unconditional dependency (`crates/paladin-content/Cargo.toml:40`, no `optional = true`), and `grep -rn 'cfg(feature = "pdf")' crates/paladin-content/src/` (re-run this session) returns zero matches — PDF extraction ships in every build regardless of the deleted feature, exactly ADR-0032's finding. The previously-recorded narrowing (`Shipped, narrowed → HARD-06`) was a defect in the manifest describing a feature that gated nothing, not a capability gap in the facade's feature list — ADR-0032 resolves it by deleting the inert declaration rather than adding it to the list. Exercising artefact: `cargo build --workspace --features content-processing`, re-run this session against HEAD, exits 0 ("Finished `dev` profile ... in 1m 28s", zero errors). `crates/paladin-content/CHANGELOG.md:17-23` records the `pdf` removal under `### Removed`, citing ADR-0032 and the accepted consumer-visible cost. |

### Milestone 8 Epic 7 — `paladin-web` Single Framework (axum) (6 IDs)

**Epic note:** All six rows land `satisfied` — this is the one section in this plan's range where every re-derivation confirms the run-4 status word rather than correcting it, including one row (`REQ-web-api-baseline-changelog`) whose "open defect" framing this session found already fully closed by Phase 8's DEBT-01 (both the tooling half and the requirement-text half), not merely named for future fixing.

| ID | Verdict | Evidence |
|---|---|---|
| REQ-delivery-endpoints-axum | satisfied | The distinction this row exists to make is that the endpoints are **mounted**, not merely ported. `crates/paladin-web/src/delivery_controller.rs:1-12` (re-read this session) documents the three routes — `POST /api/delivery/deliver` → `deliver_content`, `GET /api/delivery/status/{delivery_id}` → `get_delivery_status`, `GET /api/delivery/stats` → `get_delivery_stats` — and `create_delivery_routes` as the router builder. `crates/paladin-web/src/app.rs:24` — `use crate::delivery_controller::create_delivery_routes;` (the import) — and `:63` — `.merge(create_delivery_routes(deliverer))` (the merge, alongside `:60-62`'s `public_routes`/`protected_routes`/`admin_routes`) — both re-read this session. A citation naming only the controller file would lose the distinction; both the import and the merge line are recorded. Exercising artefact: `cargo test -p paladin-web --lib delivery_controller`, re-run this session — 6 tests pass, including `stats_route_is_reachable_through_router` and `status_invalid_uuid_route_returns_400_through_router`, both of which build the router via `create_delivery_routes` and drive it with `tower`'s `oneshot`, proving the routes are reachable through the mounted router. |
| REQ-actix-removal | satisfied | `grep -rn actix crates/paladin-web/` (re-run this session, across `Cargo.toml` and `src/`) returns zero matches. `crates/paladin-web/Cargo.toml:17` (re-read this session) declares only `axum = { version = "0.8.4" }` as the web-framework dependency. Commit `d9cb22e` ("refactor(paladin-web): remove actix-web, standardize on axum (M8 Epic 7, task 3.0)") is the exact commit: `git show --stat d9cb22e` (re-run this session) confirms it deletes the actix `configure()` function, its three handler functions, and the `use actix_web::{...}` import from `api_content_deliverer.rs`, and drops the `actix-web` dependency line. Variant group 21; cross-referenced by `REQ-paladin-web-extraction`'s row (M7 Epic 1). |
| REQ-actix-deny-ban | satisfied | `deny.toml:99-103` (re-read this session): `[bans] deny = [ { crate = "actix-web", reason = "paladin-web standardizes on axum; no second web framework" } ]`, preceded by an explanatory comment (`:100-101`) naming Milestone 8 Epic 7 and requiring reintroduction to be "a deliberate, reviewed decision". Distinct mechanism from `REQ-actix-removal`'s zero-match search — one proves the crate is gone today, the other proves a build leg would reject it coming back — never merged, per D-00f. Commit `6e5ca10` ("build(deny): ban actix-web to keep paladin-web single-framework (M8 Epic 7, task 4.0)") added the entry, re-confirmed via `git show --stat` this session. |
| REQ-delivery-handler-tests | satisfied | FR-9 requires unit tests for the three new axum handlers covering a successful response, the 404 path for an unknown delivery id, and the 400 path for an invalid UUID, mirroring `user_controller.rs`'s test style. `crates/paladin-web/src/delivery_controller.rs:96-199` (re-read this session) carries a `#[cfg(test)] mod tests` block with six `#[tokio::test]` functions: `deliver_content_failure_returns_400`, `get_delivery_status_unknown_id_returns_404`, `get_delivery_status_invalid_uuid_returns_400`, `get_delivery_stats_returns_200`, plus two router-level tests (`stats_route_is_reachable_through_router`, `status_invalid_uuid_route_returns_400_through_router`) — covering the success case (`get_delivery_stats_returns_200`, `200 OK`), the 404 path, and the 400 path, matching FR-9's three named categories. Exercising artefact: `cargo test -p paladin-web --lib delivery_controller`, run this session — "test result: ok. 6 passed; 0 failed; 0 ignored". Never checked in run-4 (`Verify → HARD-01`); checked this session and found genuinely exercised. |
| REQ-web-api-baseline-changelog | satisfied | FR-10's endpoint/deny/test deliverables all shipped (see this section's other rows); its `CHANGELOG.md` clause is met — commit `e27eecb` added the `[Unreleased]` entry, since promoted into `## [0.6.0] - 2026-06-10` (`CHANGELOG.md:139,345`, re-confirmed this session). **The path defect this row's own text names is doubly resolved, not merely open:** FR-10 mandated `./scripts/extract-public-api.sh project/current-exports.txt` — correct when Epic 7 shipped (2026-06-07), stale after commit `928c6d5` renamed the directory (2026-07-30). Phase 8 closed **both halves** of DEBT-01 (`.planning/REQUIREMENTS.md:828-889`, re-read this session): the tooling half (`ci.yml:184`, `scripts/check-api-surface.sh:6`, `scripts/extract-public-api.sh:6` — all now read `.project/current-exports.txt`, re-grepped this session) and the requirement-text half (this exact PRD annotated in place with a dated 2026-08-06 D-00c banner and struck-and-corrected FR-10 text, re-read this session at `prd-paladin-web-single-framework-axum.md:1-12,157-160`). Re-deriving rather than carrying the stub's "open defect" framing forward: DEBT-01 is `[x]` Complete, not merely named as an owner of unfixed work. |
| REQ-web-quality-gate | satisfied | FR-11 requires the workspace to build, lint, format-check and test cleanly on the default feature set and with `web-server` enabled. The `web-server` feature-matrix entry: `.github/workflows/feature-flags.yml:58-60` (re-read this session) — `name: "web-server"`, `flags: "--no-default-features --features web-server"` — confirmed present in the `feature-matrix` job's `matrix.include`. The change-confinement clause (PRD §7, struck at `:147-149` and corrected at `:150-152` by Phase 8's D-00c banner, both re-read this session — "all changes are confined to the `paladin-web` leaf crate, plus `deny.toml`, [baseline], and `CHANGELOG.md`... no facade (`src/`) source changes expected") was independently re-verified this session, not merely re-quoted: `git diff --stat fcdad35^1..fcdad35^2 -- src/ .project/current-exports.txt CHANGELOG.md deny.toml` touches only `CHANGELOG.md` (29 lines) and `deny.toml` (7 lines); zero lines under `src/`. The confinement clause holds exactly as written. Never checked in run-4 (`Verify → HARD-01`); checked this session on both sub-clauses. |

### Cross-milestone entries carried by DOCs rather than PRDs (5 IDs)

**Epic note:** These five entries are carried by DOCs (the reconciliation and its two deferred-item registers) rather than by PRDs — a provenance fact, not a gap, since DOCs are exactly where a milestone's own after-the-fact account belongs in this corpus's D-00b precedence order. Five rows land at 2 `satisfied` (`REQ-storage-nonoptional-v2`, `REQ-m8-reconciliation-relocations`) and 3 `deferred with register`, all three re-verified against the tree this session and all three found to still hold exactly, including D5's `println!`/`eprintln!`/`dbg!` count, unchanged since ingest.

| ID | Verdict | Evidence |
|---|---|---|
| REQ-storage-nonoptional-v2 | satisfied | Citing the reconciliation and commit `897e77e` (§4 Category 2, §7). Root `Cargo.toml:57` (re-confirmed this session): `paladin-storage = { workspace = true, features = ["sqlite"] }` — no `optional = true`. `sqlite_content_repository.rs` (810 LOC) and `sqlite_user_repository.rs` (676 LOC) — the facade's fallback duplicates this requirement names — are confirmed absent from the facade (`grep -rn 'sqlite_content_repository\|sqlite_user_repository' src/` returns zero matches, re-run this session) and live only in `crates/paladin-storage/src/` (confirmed present via `ls`). The `storage-sqlite` feature is retired (`grep -c 'storage-sqlite' Cargo.toml` → `0`, re-run this session). This is the same shipped fact `REQ-storage-feature-flags-v1`'s row (M7 Epic 1) records `superseded by outcome` against the *v1* PRD wording; this *v2* row, sourced from the reconciliation DOC, describes the change correctly and matches the tree exactly, so it lands `satisfied` — the two rows describe the same commit from two different source documents with two different verdicts, per D-00f's primary-key rule (never merged). Exercising artefact: the `crate-isolation` CI matrix leg for `paladin-storage` cited elsewhere in this ledger, which builds and tests the crate under its current non-optional shape on every push. Variant group 22. |
| REQ-m8-reconciliation-relocations | satisfied | Citing ADR-0028 (iii). Re-verified independently this session rather than restating the ADR's figures: `git log --oneline e5b2011~1..a1e4901 \| wc -l` → `15`; `git diff --shortstat e5b2011~1 a1e4901` → "70 files changed, 1010 insertions(+), 11262 deletions(-)" — net 10,252 lines removed, matching ADR-0028 exactly. Every relocation target confirmed present in the tree this session: `crates/paladin-storage/src/minio.rs` and `.../redis.rs` (Category 3), `crates/paladin-memory/src/citadel/file_citadel.rs` (FileCitadel), `crates/paladin-herald/` (the new leaf crate, commit `66f6c4e`). Every Category 1 orphaned-file deletion target confirmed absent (this ledger's M8 Epic 3 epic note). One new leaf crate, `paladin-herald`, created in this range. Must not be planned as Milestone 9 candidates — they already executed here → HARD-02. |
| REQ-m8-deferred-items-register | deferred with register | Register: `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-items.md` (dated 2026-06-07, "Record of intentional non-goals"). All five items re-checked against the tree this session and all five still hold: **D1** (`src/core/` re-export shims, KEEP) — `grep -rln 'crate::core::' src/` (re-run this session) returns **49** files, matching the register's "~49 facade files" exactly. **D2** (`core/platform/manager/` mis-layered services) — `content_service.rs`, `event_manager.rs`, `user_service.rs` all still present (`ls src/core/platform/manager/`, re-run this session). **D3** (entangled Paladin services, KEEP for now) — `planning_service.rs`, `prompt_generation_service.rs`, `temperature_service.rs`, `handoff_service.rs` all still present in `src/application/services/paladin/`. **D4** (`content_ingestion_service.rs` placement) — still present at `src/application/services/content/content_ingestion_service.rs` (also `REQ-paladin-content-extraction`'s subject, M7 Epic 1). **D5** (println/eprintln/dbg residue) — `grep -rn 'println!\|eprintln!\|dbg!' src/application/services/ src/infrastructure/` (re-run this session) returns **17** matches across **6** files, exactly matching the register's count and `intel/code-verification.md`'s run-4 figure, unchanged. No owners are named and no target milestone is assigned beyond the register's own "suggested grouping" — the defect the FACADE requirements exist to close: **FACADE-01** owns D5 (`.planning/REQUIREMENTS.md:1522`), **FACADE-02** owns D1 through D4 (`:1535`), so Phase 11 inherits both by requirement ID rather than by re-reading this register. **Amended 2026-08-08 (plan 11-01):** D5's disposition is now closed. Re-run this date: `grep -rn "println!\|eprintln!\|dbg!" src/application/services/ src/infrastructure/` returns **17** matches across **6** files (unchanged), and the same command filtered through `grep -v '///' | grep -v '//!'` returns **0** — every one of the 17 is a `///`/`//!` doc-comment line inside a fenced rustdoc example block, not runtime library stdout. All 17 occurrences carry a per-occurrence disposition (deliberate rustdoc-example stdout, no conversion) in `.planning/registers/facade-01-rustdoc-stdout-disposition.md`, which also hands the four `herald_registry.rs` `rust,ignore` fences to Phase 15 and records the corpus-level finding that this register's D5 clause was exact about the count and wrong about the kind. `deferred-items.md`'s D5 clause and `.planning/ROADMAP.md:726` criterion 1 both carry dated corrections with their original text retained. **Amended 2026-08-08 (plan 11-05):** D1 through D4 now each carry a disposition, resolving the marker above. `.planning/decisions/0034-d1-d4-facade-relocation-disposition.md` (ADR-0034) records: **D1** — `src/core/` re-export shims DEFER, trigger a facade-wide no-alias sweep, owner unassigned-pending that sweep. **D2** — three independent verdicts: `user_service.rs`'s split WITHDRAWN (three-owner split stated once: the split itself owned by nobody, the full relocation owned by the run-3 v2 tech-debt item, the tests owned by DEFER-02/Phase 15); `content_service.rs` DEFER, trigger the architecture-pass milestone `deferred-items.md`'s own `## Suggested grouping` names, owner unassigned-pending that milestone; `event_manager.rs` DEFER, same trigger plus the still-open `paladin-core`-vs-facade-app-service choice, owner unassigned-pending that milestone. **D3** — entangled Paladin services DEFER, trigger the broader builder/execution refactor, owner unassigned-pending that refactor; HARD-05 is answered (ADR-0031 restated the rule), so the remaining question is per-edge — non-default, facade-gated, `cfg`-scoped — not categorical. **D4** — `content_ingestion_service.rs` placement DEFER, trigger the dependency-coupling review, owner unassigned-pending that review. All five deferred items (D1–D5) now carry a disposition: D5's per-occurrence rustdoc dispositions live at `.planning/registers/facade-01-rustdoc-stdout-disposition.md` (amendment above), and D1 through D4's verdicts, owners and triggers live at ADR-0034. The Verdict cell stays `deferred with register` — every item is deferred or withdrawn with a register or ADR behind it, so no other legend word from the verdict legend fits better. |
| REQ-deferred-cli-user-commands | deferred with register | Register: `deferred-features.md` §1 (dated 2026-06-04). `src/application/cli/commands/` (re-run `ls` this session) holds exactly ten modules — `agent.rs`, `arsenal.rs`, `battalion.rs`, `council.rs`, `features.rs`, `maneuver.rs`, `mod.rs`, `muster.rs`, `onboarding.rs`, `setup_check.rs` — `user.rs` is absent, confirming the register's claim. The backend is intact: `core::platform::manager::user_service::{UserService, UserServiceTrait, ...}` (`src/core/platform/manager/user_service.rs`) and `core::platform::container::user::{User, UserProfile}` (`src/core/platform/container/user.rs`) both still exist, re-confirmed this session. The register states the removed module is "recoverable verbatim from git history at the Milestone 8 removal commit" — that fact is why this row must not read as a loss: the 1,065-LOC command surface plus its eight clap subcommands is mostly re-wiring, not new domain work. Names **FACADE-03(a)** (`.planning/REQUIREMENTS.md:1567`) as the owning requirement. **Amended 2026-08-08 (plan 11-05):** the `.planning`-native home of this record is `.planning/registers/facade-03-removed-features.md` §1, findable without opening `.project/`. Its recovery pointer is given in runnable form — `git show 3d48768^:src/application/cli/commands/user.rs` — recovering the module verbatim from the immutable commit SHA rather than from the branch name this row's own text above still carries (`chore/facade-cleanup-m8-finish`). The register cites the SHA instead because a branch ref — local *or* remote-tracking — is mutable and deletable at any time by anyone with push access, while a commit SHA is immutable once created; that property holds regardless of whether the branch happens to resolve on the day someone reads this row. Re-measured this session: no *local* branch of that name exists (`git branch --list` returns zero matches, confirming the half of this row's claim that holds), but the *remote-tracking* ref `refs/remotes/origin/chore/facade-cleanup-m8-finish` does resolve (`4bf6745…`) and is an ancestor of `3d48768` — the stronger "not present as a local or remote ref" framing this row's own text and `11-CONTEXT.md`'s D-10 both carried is not repeated here. |
| REQ-deferred-tensorflow-ml-adapter-v3 | deferred with register | Register: `deferred-features.md` §2 (dated 2026-06-04). Both halves confirmed absent this session: `grep -rn 'tensorflow\|^ml = ' Cargo.toml src/` returns zero matches (the adapter and the feature flag). The `MlPort` contract remains stable: `crates/paladin-ports/src/input/ml_port.rs` exists (re-confirmed via `find` this session) — the reintroduction condition names a dedicated `paladin-ml` leaf crate as the required home, "consistent with the hexagonal layout — ML inference is an infrastructure adapter, not facade code". Citing ADR-0028 (iv)'s non-goal split, both halves confirmed by `ls crates/` this session (eleven entries: `doc-examples` plus ten library crates): **overridden for `paladin-herald`** (present) and **still holding for `paladin-ml`** (absent, `test -d crates/paladin-ml` exits 1). This distinction is what **FACADE-03(b)** (`.planning/REQUIREMENTS.md:1567`, named in ADR-0028's `Downstream Consumers`) depends on: Phase 11 must not treat `paladin-herald`'s existence as license to create `paladin-ml` without its own decision. Variant group 24; terminal state of the chain `REQ-tensorflow-stays-facade-v1` → `REQ-tensorflow-ml-feature-gate-v2` → this entry. **Amended 2026-08-08 (plan 11-05):** cite `.planning/registers/facade-03-removed-features.md` §2 and `.planning/decisions/0035-paladin-ml-leaf-crate-placement.md` (ADR-0035). The placement condition — any future TensorFlow adapter goes into a dedicated `paladin-ml` leaf crate with the `ml` flag on that crate, never back into the facade, and `MlPort` stays in the workspace — is now promoted out of DOC precedence into an ADR, which is what FACADE-03's "survives outside a single DOC" clause asked for. The asymmetric non-goal split stated in one clause: overridden for `paladin-herald` (created), still holding for `paladin-ml` (absent) — `paladin-herald`'s existence is not licence to create `paladin-ml`. |

## Phase 10 close-out amendments (2026-08-08)

Appended per D-00d — the twelve epic sections above are unmodified by this amendment; nothing is
rewritten or absorbed. Sourced from all ten of the plan SUMMARY files below (plan 10-11), plus a
fresh re-run of every command this section reports. Named individually, one per line, the ten
source files (all in `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/`) are:

- `10-01-SUMMARY.md` — the ledger scaffold, head notes, legend, and the 13-row summary table
- `10-02-SUMMARY.md` — ADR-0028, the M8 reconciliation authority
- `10-03-SUMMARY.md` — ADR-0029 and ADR-0030, version trajectory and self-numbering
- `10-04-SUMMARY.md` — ADR-0031 and the three checkpoint answers (`q1-restate`, `q2-delete`,
  `q3-ratify`)
- `10-05-SUMMARY.md` — ADR-0032, the PDF capability, executing `q2-delete`
- `10-06-SUMMARY.md` — ADR-0033, the `cargo doc` bar, executing `q3-ratify`
- `10-07-SUMMARY.md` — Milestone 7 Epic 1-2 ledger derivation
- `10-08-SUMMARY.md` — Milestone 7 Epic 3-4 ledger derivation
- `10-09-SUMMARY.md` — Milestone 8 Epic 1-3 ledger derivation
- `10-10-SUMMARY.md` — Milestone 8 Epic 5-7 and cross-milestone ledger derivation

**Whole-file integrity check, re-run before writing this section:** `grep -c '^| REQ-'` → `86`;
distinct-ID count (`grep -o '^| REQ-[a-z0-9-]*' | sort -u | wc -l`) → `86`; `grep -c '^### '` → `12`,
in the same run-4 order the twelve section headings have carried since `10-01-SUMMARY.md`; zero rows
carry an unresolved wave-3 stub marker of the `pending`-hyphen-`plan-10-NN` form the scaffold's own
head note (near line 169) describes as its interim-state contract; zero blank Verdict/Evidence
cells. **Every `ADR-NNNN` citation in the ledger resolves to a file that exists** — the nine
distinct numbers cited (`ADR-0024` through
`ADR-0029`, `ADR-0031`, `ADR-0032`, `ADR-0033`; `ADR-0030` is not cited by any row, only by
`10-CONTEXT.md` and `REQUIREMENTS.md`) were extracted and checked individually against
`.planning/decisions/`, zero failures.

**Per-class verdict distribution across all 86 rows**, counted directly from the Verdict column
this session (`awk -F'|' '{print $3}'` over every `^| REQ-` line, trimmed and tallied):

| Verdict | Count |
|---|---|
| `satisfied` | 41 |
| `satisfied (closed by Phase 9)` | 7 |
| `satisfied (history)` | 3 |
| `superseded by outcome` | 12 |
| `relocated` | 8 |
| `present, unproven` | 6 |
| `diverged` | 5 |
| `deferred with register` | 3 |
| `genuinely outstanding` | 1 |
| **Total** | **86** |

The three `satisfied` variants (51 rows total) share one evidentiary bar — a `file:line` citation
plus something that exercises it — and are split into three labels only to record *why* each is
proven: a fresh command run this session (`satisfied`), a citation to a decision closed by an
earlier phase and re-run rather than re-opened (`satisfied (closed by Phase 9)`), or a historical
snapshot requirement that was met at the time and is not an evergreen freshness bar
(`satisfied (history)`).

**Rows cited rather than re-derived.** Seven rows carry `satisfied (closed by Phase 9)`: the six
Phase 9 D-04 hand-off rows in Milestone 7 Epic 4 (`REQ-crate-metadata-completion`,
`REQ-per-crate-changelog`, `REQ-rustsec-risk-acceptance`, `REQ-rustsec-hardening-actions`,
`REQ-license-policy-signoff`, `REQ-paladin-ports-publish-verification-closed`) plus
`REQ-docker-workspace-build` in Milestone 7 Epic 2 — matching D-04's instruction exactly (seven
named rows, no more, no fewer). **Every one of the seven had its citation re-run this session**
(plan 10-08 for the six Epic 4 rows, plan 10-07 for the Epic 2 row) against
`REQUIREMENTS.md:1320-1355`'s hand-off block, and **none had moved** from where that block names
them — the register file (`SECURITY-EXCEPTIONS.md`, still 10 governed rows), both CI-wired guard
scripts (`scripts/check-changelogs.sh`, `scripts/check-crate-names.sh`), the licence field across
all eleven manifests, and the crate/lib name split (`paladin-ai-core` / `paladin_core`) were all
re-verified against the current tree rather than transcribed. No sixth or eighth row was added to
this set; the boundary D-04 drew held exactly.

**Claims that are CI-only and were never measured here**, each with the exact command a CI runner
executes:

- **The RustSec/advisory gates.** `cargo audit --config .cargo/audit.toml` and `cargo deny check
  advisories` — neither tool can reach `crates.io` in this environment (HTTP 403, unchanged since
  Phase 4's own scoping note at `.planning/phases/04-release-coherence/04-ci-gate-deferrals.md`).
  Plan 10-05 corrected `.cargo/audit.toml`'s `RUSTSEC-2026-0187` comment to state the true
  reachability path; the suppression set itself (`ignore = [...]`) was not touched, so both commands
  are expected to behave identically to before this phase's edits once they run in CI.
- **The ADR-0006 84% workspace line-coverage floor.** `cargo-llvm-cov` is not installable here. The
  confirmation this phase records is **reasoning, not measurement**: `git diff --name-only
  6a6f175..HEAD -- '*.rs' | wc -l` → `0` (re-run this session, `6a6f175` being the commit at the head
  of `release/v0.7.0` immediately before Phase 10's first commit) — a phase that modifies no `.rs`
  file cannot move line coverage. No partial substitute was run and presented as the floor.
- **Anything Docker.** `docker` is absent from this environment
  (`.planning/phases/04-release-coherence/04-ci-gate-deferrals.md`, unchanged). No row in this
  ledger's Milestone 7/8 range depends on a Docker-only artefact beyond `REQ-docker-workspace-build`,
  which is cited to Phase 9 rather than re-verified with a live build.

**The measured `cargo doc` state**, as ADR-0033 recorded it and as this session re-confirmed
unchanged: `cargo doc --workspace --no-deps 2>&1 | tee doc-output.txt && ! grep -q "warning:"
doc-output.txt` exits **1** (re-run this session against the current HEAD; `grep -c '^warning:'` on
the captured output returns **24** — the 20 individual warnings plus 4 per-crate summary lines,
exactly ADR-0033's own accounting). The crate split is unchanged: `paladin-web` 13,
`paladin-battalion` 3, `paladin-ai` (facade) 3, `paladin-herald` 1. Dated 2026-08-08 (ADR-0033,
written by plan 10-06). **Phase 16 / DOCS-03** is the named owner of clearing them — doing so needs
`.rs` doc-comment edits outside this phase's D-23 boundary, so this phase does not attempt it and
does not infer a pass.

**The three rows carrying do-not-re-delete markers, and where each sits:**

1. **The sqlite marker** — `REQ-storage-shim-deletion`'s own row (Milestone 8 Epic 3): the
   `sqlite_*_repository.rs` files were the active default-build implementation, not redundant;
   resolved by making `paladin-storage` non-optional (commit `897e77e`), not by deleting a shim.
2. **The registry marker** — inside `REQ-garrison-sanctum-bridges-kept`'s row (Milestone 8 Epic 3):
   `paladin_registry.rs` was not a duplicate; the facade's richer 418-LOC implementation was
   consolidated *into* `paladin-battalion` (commit `ca7e4e8`), not deleted as if orphaned.
3. **The "rest genuinely orphaned" marker** — the Milestone 8 Epic 3 section's own epic note (not a
   single row, since it applies epic-wide): the remaining ten Category 1 files
   (`mysql_content_repository.rs`, the five `input/*` fetchers, `document/*`,
   `output/api_content_deliverer.rs`, `logs/error_log_adapter.rs`) genuinely were orphaned and were
   correctly removed in commit `e5b2011` — do not re-audit them as if the original "active bridge"
   classification still applied.

All three markers were re-confirmed present in the ledger text this session (`grep -ci 'DO NOT
RE-DELETE'` → `2` literal occurrences across the two rows above, plus the epic note's own
unmarked-but-equivalent third correction) rather than assumed carried forward.

**The checkpoint answers plan 10-04 recorded**, so this ledger states on its own page which branch
each flagged decision took, rather than sending a reader to a fourth document to find out:

- **D-15 (the extracted-crate dependency rule) → `q1-restate`.** The rule is a default-build
  invariant, not an absolute "never"; `paladin-content → paladin-llm` stays, legalised by ADR-0031.
  This ledger's `REQ-extracted-crate-dependency-rule` row (Milestone 7 Epic 1) reads `satisfied` on
  that authority.
- **D-18 (the inert `pdf` feature) → `q2-delete`.** `pdf = []` is deleted from
  `crates/paladin-content/Cargo.toml`, not wired or kept. This ledger's
  `REQ-content-processing-build-gate` row (Milestone 8 Epic 6) reads `satisfied` on ADR-0032's
  post-deletion manifest.
- **The `cargo doc` bar → `q3-ratify`.** ADR-0033 ratifies the zero-warning bar and records the
  measured 20-warning debt with Phase 16 / DOCS-03 as owner, rather than clearing it in this phase.
  This ledger's `REQ-m8-final-quality-gate` row (Milestone 8 Epic 5) states plainly that the gate
  does not currently pass.

**The 12-vs-13 supersession-count reconciliation.** The twelve epic sections carry **12** rows
verdicted `superseded by outcome` (see the per-class table above — re-counted this session via
`grep -n '^| REQ-.*| superseded by outcome |'`), while the head-of-file "Superseded by outcome"
summary table (built by plan 10-01 directly from `intel/code-verification.md:365-381`) holds **13**
data rows, expanding to **16** distinct `REQ-*` IDs once the bundled five-document row (5 IDs in one
table row) and the no-`REQ-*`-owner row (0 IDs — the M7 overview's Appendix A/B `paladin-cli` entry,
which is not part of the 86-row inventory at all) are unpacked. The two counts are not the same
population and were never expected to match one-for-one; tracing all 16 IDs to their own
epic-section verdict accounts for the gap completely, with nothing left unexplained:

- **5 of the 16** land `superseded by outcome` in their own epic-section row, exactly as the summary
  table anticipated: `REQ-paladin-web-extraction`, `REQ-storage-feature-flags-v1` (both M7 Epic 1),
  `REQ-tensorflow-ml-feature-gate-v2`, `REQ-m8-epic3-no-extractions` (both M8 Epic 3), and
  `REQ-facade-role-lib-docs` (M8 Epic 5).
- **6 of the 16** land `relocated` instead, per this ledger's own tie-break rule (a row eligible for
  both `relocated` and `superseded by outcome` takes `relocated`): `REQ-stable-api-per-crate` and
  the five-document group (`REQ-build-baselines-doc`, `REQ-integration-tests-doc`,
  `REQ-performance-baseline-doc`, `REQ-release-checklist`, `REQ-versioning-policy` — the last of
  which also states its `superseded by outcome` half explicitly in the same cell, per the tie-break
  rule's own carve-out for that case).
- **2 of the 16** land `satisfied` once their ADRs landed, per the `‡` markers already in the
  summary table: `REQ-extracted-crate-dependency-rule` (ADR-0031) and
  `REQ-content-processing-build-gate` (ADR-0032).
- **2 of the 16** turned out, on independent re-derivation, to be `diverged` rather than
  `superseded by outcome` — the tree implements the requirement differently on purpose, but not in a
  way that would be *undone* by implementing the PRD as written: `REQ-sqlx-workspace-dependency`
  (M7 Epic 1) and `REQ-ci-publish-dry-run-v1` (M7 Epic 2, which this ledger's own row text already
  flagged as "not superseded — coexists" before this reconciliation was written).
- **1 of the 16** turned out to already be `satisfied`, not superseded: `REQ-dead-file-batch-deletion`
  — run 4's own re-count (136 files) matches the tree exactly; the PRD's anticipated post-Epic
  figures (163, then 160) were simply higher than what the reconciliation's further deletions left
  behind, which is a wrong PRD estimate, not shipped code undoing the requirement.
- **0 of the 16** for the M7 overview Appendix A/B `paladin-cli` entry — it carries no `REQ-*` ID and
  is outside the 86-row inventory by the summary table's own parenthetical.

5 (still superseded) + 7 rows found independently during the 82-row fan-out that were **never part
of `code-verification.md`'s original 13-row table at all** — `REQ-paladin-storage-extraction`,
`REQ-tensorflow-stays-facade-v1` (cross-referenced by variant group only, not literal ID, per plan
10-07's own recorded finding), `REQ-disabled-bench-disposition`, `REQ-facade-file-classification`,
`REQ-facade-audit-document`, `REQ-adapter-disposition-record`, and `REQ-m8-final-quality-gate` — sum
to the 12-row epic-section tally exactly (5 + 7 = 12). No table row and no epic-section row is
unaccounted for; the apparent off-by-one is fully explained by re-derivation moving rows between
classes, not by a miscount in either figure.

**Phase gate, run against the whole of Phase 10** (base commit `6a6f175`, the tip of `release/v0.7.0`
immediately before Phase 10's first commit, to current HEAD):

1. `git diff --name-only 6a6f175..HEAD -- '*.rs' | wc -l` → **`0`**. D-23's boundary is proved by a
   command, not only stated.
2. `cargo test --workspace` → **pass** (all reported suites `ok`, `0 failed`, across every crate and
   the facade, including doctests). `cargo fmt --check` → **pass** (exit `0`, no diff). `cargo
   clippy --workspace --all-targets --all-features -- -D warnings` → **pass** (exit `0`, `Finished`
   with no warnings emitted).
3. `cargo metadata --no-deps --format-version 1` → exit `0`. The manifest still parses after plan
   10-05's `pdf` feature deletion.
4. `make -n release-check` → exit `0`; the expanded recipe still runs `clean-code` → `test` →
   (doc-test) → `audit` → `build-release` in original order, with no `--exclude paladin-ports`
   anywhere in it (plan 10-06's fix holds).
5. ADR-0006's coverage floor: recorded as **reasoning**, not measurement, per item 1 above —
   `cargo-llvm-cov` remains uninstallable here.
6. The three Manual-Only rows from `10-VALIDATION.md`, each given an explicit disposition rather
   than left implicit:
   - **The `cargo doc` zero-warning bar** — branch (a) taken: ADR-0033 ratifies the bar and records
     the 20-warning debt against Phase 16 / DOCS-03. The gate itself is confirmed still exiting `1`
     this session (see "The measured `cargo doc` state," above).
   - **`cargo audit` / `cargo deny check`** — CI-only, not run in this environment; the
     `.cargo/audit.toml` comment correction landed (plan 10-05) and the exact commands are recorded
     above. No local pass is claimed for either.
   - **The ADR-0006 84% coverage floor** — CI-only; recorded as reasoning (zero `.rs` files changed)
     rather than as a measurement, per item 5 above.
