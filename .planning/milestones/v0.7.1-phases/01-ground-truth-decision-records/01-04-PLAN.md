---
phase: 01-ground-truth-decision-records
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md
  - .planning/decisions/0006-coverage-gate.md
autonomous: false
requirements: [RECON-07]
user_setup:
  - service: crates.io
    why: "`cargo-llvm-cov` is not installed in the sandbox where this phase was researched and crates.io returned HTTP 403 there; the fresh measurement D-07 requires cannot be produced without registry access or a CI-produced figure"
    dashboard_config:
      - task: "Provide an execution environment that can reach crates.io, OR trigger the existing `.github/workflows/integration-tests.yml` run and supply its coverage output"
        location: "local shell with network access, or GitHub Actions -> integration-tests workflow"

must_haves:
  truths:
    - "The coverage question has exactly one number and exactly one scope, recorded together with the exact command that produced them and the date (RECON-07, D-07, D-08)."
    - "The recorded number was produced by running the recorded command against the current tree in this phase — not copied from any of the six documented stale or contested baselines (D-07)."
    - "The gate is the measured baseline rounded DOWN to a whole percent, hard-fail from the first run, with 80% recorded in the same ADR as the target and a named ratchet trigger (D-09)."
    - "The ADR states the inclusion/exclusion decision for `examples/`, `benches/`, the `doc-examples` crate and doctests, so Phase 15 can reproduce the number in CI."
    - "The two module-scoped gates — Herald >= 95%, autonomous >= 90% — are named in the ADR, recorded as sitting above the global floor, and explicitly not withdrawn, with placement handed to VERIFY-05 in Phase 5 (D-10)."
    - "The ADR records that Phase 3's success criterion 1, which names unit and integration coverage separately, must be amended to the single workspace-wide number (D-08)."
    - "The ADR states the gate as the measured baseline rounded DOWN to a whole percent, states the measured figure it was derived from, and states that the gate is hard-fail from the first run."
    - "The ADR records the measured percentage to the precision the tool reports, names the exact command including every flag, the date, and the inclusion/exclusion decision for `examples/`, `benches/`, the `doc-examples` crate and doctests."
  prohibitions:
    - statement: "MUST NOT record a coverage percentage that was not produced by actually running the recorded command against the current tree — no estimate, no interpolation, and no figure copied from a stale or contested prior baseline. If the measurement cannot be run, the task halts and reports the blocker rather than producing a seventh unverified number."
      status: resolved
      verification: judgment
    - statement: "MUST NOT record a percentage without the exact command, flag set and date that produced it — a number whose scope is implicit is not reproducible and repeats the failure the six existing baselines already demonstrate."
      status: resolved
      verification: judgment
    - statement: "MUST NOT mark an ADR `Code conformance: conforms` where the cited code does not in fact match the chosen variant — a mismatch is recorded as `must change` with its executing requirement named, never smoothed into conformance."
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md"
      provides: "The raw measurement record — exact command, flags, toolchain versions, date, and the tool's own summary output"
      contains: "cargo llvm-cov"
    - path: ".planning/decisions/0006-coverage-gate.md"
      provides: "RECON-07 decision record — one coverage number, one scope, one gate, one target, one ratchet trigger"
      contains: "## Code Conformance"
  key_links:
    - from: ".planning/decisions/0006-coverage-gate.md"
      to: ".planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md"
      via: "the ADR's recorded figure is transcribed from the measurement record, which holds the tool's raw output"
      pattern: "01-coverage-measurement\\.md"
    - from: ".planning/decisions/0006-coverage-gate.md"
      to: ".github/workflows/integration-tests.yml"
      via: "the recorded command's feature scope is stated relative to the project's existing CI invocation"
      pattern: "integration-tests\\.yml"
---

<objective>
Produce the one coverage number this project has never had: measure the current tree, record the
figure with the exact command and date that produced it, and turn it into a single recorded gate
with a single recorded scope.

Purpose: six positions on the coverage gate exist across the corpus (80%, 85%, 75%-layered,
80%-Epic-24, 78%-hard, and a 70/74/78 phased ramp), and every measured baseline is stale or
contested. RECON-07 asks for one number and one scope so Phase 3 can objectively pass or fail and
Phase 15 can wire a CI floor. D-07's premise is that recording a seventh unverified figure would
repeat exactly the problem this phase exists to close.

Output: `.planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md` (the raw
evidence) and `.planning/decisions/0006-coverage-gate.md` (the decision).
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ground-truth-decision-records/01-CONTEXT.md
@.planning/phases/01-ground-truth-decision-records/01-RESEARCH.md
@.planning/phases/01-ground-truth-decision-records/01-PATTERNS.md
@.planning/phases/01-ground-truth-decision-records/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols, files and vocabulary created by Phase 1 (none are pre-existing; the drift-verification pass
must not flag them as unacknowledged):

- **New directories:** `.planning/decisions/`, `.planning/ledgers/`
- **New files:** `.planning/decisions/PROMOTION.md`; `.planning/decisions/0001-battalion-config.md`
  … `0006-coverage-gate.md`; `.planning/ledgers/milestone-01.md`;
  `.planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md`
- **New ADR headings (document vocabulary):** `## Code Conformance`, `## Downstream Consumers`
- **New field values:** `conforms`, `must change` (the `Code conformance` field, D-03)
- **New ledger verdict classes:** `satisfied`, `present, unproven`, `genuinely outstanding`,
  `deferred with reason`, `superseded by shipped code` (D-20)
- **New naming scheme:** flat zero-padded monotonic ADR counter `NNNN-kebab-slug.md`
- **New governance vocabulary introduced by this plan:** the workspace coverage *floor* (the
  hard-fail gate), the coverage *target* (80%), and the named *ratchet trigger* that raises the
  floor toward the target
- **Rust identifiers recorded elsewhere in this phase (NOT implemented — Phase 2 GAP-07 implements
  them):** `BattalionCheckpointConfig`, and the new temperature-range field on `ProviderCapabilities`

<tasks>

<task type="auto">
  <name>Task 1: Measure workspace coverage against the current tree and record the raw evidence</name>
  <precondition>`cargo llvm-cov` is runnable in this environment — either already installed, or `cargo install cargo-llvm-cov` can reach crates.io. RESEARCH.md §Environment Availability verified on 2026-07-30 that the tool was absent and crates.io returned HTTP 403 from the research sandbox, while the `llvm-tools` rustup component was already present. If the install cannot complete, HALT and report the blocker — do not substitute an estimated or previously-documented figure.</precondition>
  <read_first>
    - `.github/workflows/integration-tests.yml` lines 110-130 — the project's own existing `cargo llvm-cov` invocation, including its feature scope and the `codecov` upload; this is the command the recorded one must be stated relative to
    - `.planning/codebase/TESTING.md` — the three-tier test strategy and the (stale) tarpaulin habit, so the ADR can note which document is superseded
    - `Cargo.toml` (workspace root) — the member list, so the exclusion decision names real paths
    - `.planning/phases/01-ground-truth-decision-records/01-RESEARCH.md` §Environment Availability and §Common Pitfalls Pitfall 2 and Pitfall 4
  </read_first>
  <files>.planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md</files>
  <action>
Produce a fresh workspace-wide line-coverage figure and write the evidence down.

Confirm the toolchain first: check whether `cargo llvm-cov --version` responds, and confirm the
`llvm-tools` rustup component is installed. If `cargo llvm-cov` is absent, attempt
`cargo install cargo-llvm-cov` once. That package's legitimacy is already established — RESEARCH.md
§Package Legitimacy Audit records the verdict OK, and the project's own
`.github/workflows/integration-tests.yml` already installs and runs it — so no separate approval is
needed. If the install fails for registry or network reasons, HALT and report; the checkpoint at the
end of this plan is where a human supplies a CI-produced figure instead.

Decide and pin the scope before running, because the number is meaningless without it (this is the
Claude's-discretion item CONTEXT.md hands to the planner, and Phase 15 has to reproduce it):
- `--workspace` is mandatory per D-08; a bare `cargo llvm-cov` covers only the root crate.
- Reproduce the CI invocation's feature scope (`--features integration-tests`), because that is the
  only coverage-generating command this project has evidence of working. If the Docker-backed
  services that suite needs (Redis, MinIO) are unavailable, record that as an explicit caveat on the
  figure rather than silently reporting a lower number.
- Exclude `examples/`, `benches/` and the `doc-examples` crate from the measured denominator, via
  `--ignore-filename-regex`; state the exact regex used.
- Exclude doctests (do not pass `--doctests`), and say so.
- Use `--summary-only` so the output is a table, not an HTML report; do not commit any `.lcov`,
  `.profraw` or HTML artifact.

Write `.planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md` containing: the
full command line verbatim including every flag; the `rustc`, `cargo` and `cargo-llvm-cov` versions;
the date; the git commit SHA and branch the measurement ran against; the tool's own summary output
pasted verbatim; the single workspace line-coverage percentage extracted from it; and any caveat
(unavailable services, skipped suites). This file is the evidence the ADR transcribes and the record
Phase 15 reproduces against — keep it factual, with no interpretation and no gate value.
  </action>
  <acceptance_criteria>
    - `.planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md` exists and contains the string `cargo llvm-cov` and the string `--workspace`
    - The file contains a percentage figure (a number followed by `%`)
    - The file contains a date, a git commit SHA, and the `cargo-llvm-cov` version string
    - The file contains the exact `--ignore-filename-regex` value used, and an explicit statement about doctests
    - The recorded percentage is reproducible: re-running the recorded command on the same commit yields the same figure
    - No `.lcov`, `.profraw`, `.profdata` or coverage HTML artifact is added to the repository
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'cargo llvm-cov' .planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md && grep -q -- '--workspace' .planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md && grep -qE '[0-9]+\.[0-9]+%|[0-9]+%' .planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md && grep -q -- '--ignore-filename-regex' .planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md && git status --porcelain | grep -qE '\.(lcov|profraw|profdata)$' && exit 1; echo MEASUREMENT_OK</automated>
  </verify>
  <done>A single workspace line-coverage percentage exists, produced by a command recorded verbatim with its flags, versions, date and commit, plus any caveat — and no coverage build artifact was committed.</done>
</task>

<task type="auto">
  <name>Task 2: ADR-0006 — one coverage number, one scope, one gate, one target, one ratchet</name>
  <reversibility rating="costly">Phase 3's testing scope and Phase 15's CI floor are both authored against this number and scope; changing either later re-scopes both phases and invalidates the amendment this ADR asks the roadmap to make.</reversibility>
  <read_first>
    - `.planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md` — the figure and command produced by Task 1; transcribe from here, never from memory
    - `.planning/decisions/PROMOTION.md` — the heading set, bullet-list rule and numbering index
    - `.planning/REQUIREMENTS.md` §"Group 1 — project-wide test coverage gate" (line 1657) — the first four competing positions
    - `.planning/REQUIREMENTS.md` §"Group 30 — initial coverage threshold for the CI gate" (line 2174) — the fifth and sixth positions and the parent PRD's Open Question 3
    - `.planning/ROADMAP.md` §"Phase 3" — success criterion 1, which names unit and integration coverage separately and must be amended
    - `.planning/ROADMAP.md` §"Phase 5" VERIFY-05 and §"Phase 15" PIPE-02 — the two downstream owners
  </read_first>
  <files>.planning/decisions/0006-coverage-gate.md</files>
  <action>
Author `# ADR-0006: Project-wide test coverage gate` using the heading set from `PROMOTION.md`.

`## Status` body is the bare word `Accepted`, followed by a `**Date:**` prose line.

`## Context` states the question — one number and one scope for a gate a later phase can objectively
pass or fail — and enumerates the six competing positions with their sources: 80% (nine Milestone-1
PRDs), 85% (the unit-test-improvements workstream), 75% overall with a layered per-tier table (the
Milestone 3 plan), 80%/70% re-asserted (Epic 24), a 78% hard gate (the Deferred-QA parent PRD), and
a phased 70 → 74 → 78 ramp (Epic 25). Record that the parent PRD's Open Question 3 — hard fail or
soft warning initially — is Open in the corpus and is answered here. Record the measured baselines
that are stale or contested: 60.88% unit and 67.79% integration (Milestone 1, predating Milestones
2-12), ~78% overall (Milestone 3 release notes), and 76-77% (Deferred-QA, February 2026, with two
known-stale module paths).

`## Decision` is a bullet list carrying D-07 through D-10, transcribed from the measurement record:
- **The measured figure**, stated to the precision the tool reported, with the date and the git
  commit it was measured against.
- **The exact command**, verbatim, including every flag — `--workspace`, the feature scope, the
  `--ignore-filename-regex` value, and the fact that doctests are excluded. State in words which
  paths the regex removes (`examples/`, `benches/`, the `doc-examples` crate) so a reader does not
  have to parse the regex to know the scope.
- **The scope**: one workspace-wide line-coverage number, all tests combined, from that one
  reproducible command. Not per-tier, not per-crate.
- **The gate (floor)**: the measured baseline rounded **down** to a whole percent, **hard-fail from
  the first run**. State the arithmetic explicitly, showing measured figure and resulting floor, so
  nobody has to re-derive it. Note that this answers Open Question 3 as hard-fail without the
  ramp-up problem it worried about, because the gate cannot be red on day one by construction.
- **The target**: 80%, recorded in this same ADR as the target rather than the gate, so the
  acceptance criteria in nine Milestone-1 PRDs and Epic 24 are neither falsified nor pretended to be
  met.
- **The ratchet trigger** (the Claude's-discretion item D-09 requires be named): the floor is raised
  to the next whole percent below the then-current measured workspace figure whenever a milestone
  closes with measured coverage two or more whole percentage points above the standing floor. The
  raise is applied by amending this ADR in place with the new figure, command and date. The ratchet
  stops at the 80% target and does not pass it.
- **The two module-scoped gates**: Herald >= 95% and autonomous >= 90%. Record them as sitting
  **above** the global floor and **explicitly not withdrawn**; hand their placement to **VERIFY-05
  in Phase 5**, which the roadmap already assigns as their owner.
- **The tool of record**: `cargo llvm-cov`, already the project's CI choice at
  `.github/workflows/integration-tests.yml:117`. Note that `.planning/codebase/TESTING.md`'s
  `cargo tarpaulin` reference is stale relative to that CI file, and that a tarpaulin figure would
  not match CI's own gate.
- **The consequence for the roadmap**: Phase 3's success criterion 1 names unit and integration
  coverage separately and must be amended to this single workspace-wide number. Name plan 01-08 as
  the place that amendment lands.

`## Considered Options` is one bullet per rejected position, naming its REQ-* ID from variant groups
1 and 30 and a one-sentence reason. Include the two explicitly rejected by D-09: an immediate 80%
hard gate (would leave Phase 15's CI gate red until Phase 3 lands) and Epic 25's phased ramp (three
numbers where RECON-07 asked for one).

`## Code Locations` is a bulleted list citing `.github/workflows/integration-tests.yml:117-123`, the
measurement record at
`.planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md`, and
`.planning/codebase/TESTING.md` as the superseded local-tooling reference.

`## Code Conformance` body is the bare word `conforms` if no shipped file contradicts the recorded
number and scope, or the bare phrase `must change` naming the requirement that will execute the
correction if one does. Decide it against what the tree actually shows — no coverage gate currently
exists in `ci.yml`, so state plainly whether that counts as conformance or as pending work, and
name **PIPE-02** in Phase 15 as the requirement that wires the floor into CI either way.

`## Downstream Consumers` names Phase 3 (QUAL-01, which raises real coverage against this floor),
Phase 5 (VERIFY-05, which owns the module-scoped gates and extends this number across the four
earlier positions), and Phase 15 (PIPE-02, which must land the CI threshold on this number or record
why it differs).
  </action>
  <acceptance_criteria>
    - `node .claude/gsd-core/bin/lib/adr-parser.cjs --input .planning/decisions/0006-coverage-gate.md` exits 0 with `"status": "accepted"` and non-empty `decisions`, `options_considered` and `key_files`
    - The ADR contains a percentage figure, the string `cargo llvm-cov`, the string `--workspace` and the string `--ignore-filename-regex`
    - The ADR contains the strings `VERIFY-05`, `PIPE-02`, `QUAL-01`, `95%` and `90%`
    - The ADR states the ratchet trigger as a concrete, checkable condition and states that the ratchet stops at 80%
    - The ADR states that Phase 3's success criterion 1 must be amended, and names plan 01-08 as where
    - The percentage stated in the ADR is byte-identical to the one in `01-coverage-measurement.md`
    - The ADR states the doctest inclusion decision and names the three excluded paths in words
  </acceptance_criteria>
  <verify>
    <automated>node .claude/gsd-core/bin/lib/adr-parser.cjs --input .planning/decisions/0006-coverage-gate.md | grep -q '"status": "accepted"' && for s in 'cargo llvm-cov' 'VERIFY-05' 'PIPE-02' 'QUAL-01' '95%' '90%' '80%'; do grep -q "$s" .planning/decisions/0006-coverage-gate.md || exit 1; done && grep -q -- '--ignore-filename-regex' .planning/decisions/0006-coverage-gate.md && M=$(grep -oE '[0-9]+\.[0-9]+%' .planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md | head -1) && [ -n "$M" ] && grep -qF "$M" .planning/decisions/0006-coverage-gate.md && echo ADR0006_OK</automated>
  </verify>
  <done>The coverage question has one recorded number, one recorded scope, one hard-fail floor derived arithmetically from the measurement, 80% recorded as the target, a concrete ratchet trigger, and both module-scoped gates preserved and handed to VERIFY-05.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the coverage number is reproducible and the gate is acceptable</name>
  <what-built>
    A fresh workspace line-coverage measurement recorded verbatim in
    `.planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md` (command, flags,
    toolchain versions, commit, date, tool output, caveats), and `.planning/decisions/0006-coverage-gate.md`
    recording one number, one scope, a hard-fail floor rounded down from the measurement, 80% as the
    target, a named ratchet trigger, and the two module-scoped gates handed to VERIFY-05.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md`, copy the
       recorded command verbatim, and run it. Confirm the percentage it prints matches the recorded
       figure. If the environment could not run `cargo llvm-cov` at all, supply a figure instead:
       either run the command on a machine with crates.io access, or trigger the existing
       `.github/workflows/integration-tests.yml` workflow and read the coverage output from its run,
       then paste the number, the exact command and the date here so the ADR can be corrected.
    2. Open `.planning/decisions/0006-coverage-gate.md` and confirm three judgements you own:
       (a) the **floor** — the measured figure rounded down to a whole percent, hard-fail from day
       one; (b) the **ratchet trigger** — raise the floor to the next whole percent below the current
       measured figure whenever a milestone closes two or more points above the standing floor,
       stopping at 80%; (c) the **scope exclusions** — `examples/`, `benches/` and the `doc-examples`
       crate excluded, doctests excluded, feature scope as recorded. Phase 15 has to reproduce this
       in CI, so these are the parts that must be right.
    3. Confirm the ADR does not silently reduce the two module-scoped gates: Herald >= 95% and
       autonomous >= 90% must both be present, above the floor, and handed to VERIFY-05.
  </how-to-verify>
  <resume-signal>Type "approved", or state the corrected number/command/date, or name which of the floor, ratchet trigger or exclusions to change.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crates.io → local toolchain | `cargo install cargo-llvm-cov` pulls and compiles a third-party cargo subcommand into the developer's toolchain |
| measured figure → recorded gate | an unreproducible number becomes a hard-fail CI gate in Phase 15 that nobody can diagnose when it disagrees |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | `cargo install cargo-llvm-cov` | high | mitigate | RESEARCH.md §Package Legitimacy Audit ran the legitimacy gate: verdict OK, published 2021-01-22, ~101,781 weekly downloads, repo `github.com/taiki-e/cargo-llvm-cov`, not deprecated, no postinstall. Its provenance is the project's own shipped CI (`.github/workflows/integration-tests.yml:117`), the strongest tier this protocol recognises. No `[ASSUMED]` or `[SUS]` package is involved, so no blocking legitimacy checkpoint is required. |
| T-01-10 | Repudiation | a coverage figure with no recorded command | high | mitigate | The measurement record holds command, flags, toolchain versions, commit SHA and date; the verify command asserts the ADR's percentage is byte-identical to the measurement record's, so a transcription drift fails the gate. The blocking human-verify checkpoint re-runs the command independently. |
| T-01-11 | Spoofing | an estimated or copied figure presented as measured | high | mitigate | The plan's first prohibition forbids it explicitly; Task 1's precondition instructs HALT-and-report rather than substitution, and Task 3's checkpoint is the human path for supplying a CI-produced figure. |
| T-01-12 | Denial of Service | a hard-fail floor set above the measured figure | medium | mitigate | D-09's construction — floor = measured, rounded down — makes a red-on-day-one gate arithmetically impossible; the checkpoint asks the human to confirm the arithmetic. |
| T-01-01 | Information Disclosure | coverage build artifacts | low | accept | `.lcov`/`.profraw`/HTML output is not committed; Task 1's verify asserts none was added. Coverage output contains no secret material. |
</threat_model>

<verification>
- `.planning/phases/.../01-coverage-measurement.md` holds a percentage, the verbatim command, versions, commit SHA and date
- `.planning/decisions/0006-coverage-gate.md` parses via `adr-parser.cjs` and its percentage matches the measurement record byte-for-byte
- The ADR names VERIFY-05, PIPE-02 and QUAL-01, carries 95% and 90% module gates, and states the ratchet trigger and its 80% stop
- The human checkpoint has independently reproduced the command, or supplied a corrected figure with its command and date
</verification>

<success_criteria>
- One workspace coverage number and one scope exist, both reproducible from a recorded command
- The gate is hard-fail and cannot be red on day one by construction
- 80% survives as the recorded target with a named ratchet trigger toward it
- The Herald and autonomous module gates are preserved, not withdrawn, and are owned by VERIFY-05
</success_criteria>

<output>
Create `.planning/phases/01-ground-truth-decision-records/01-04-SUMMARY.md` when done
</output>
