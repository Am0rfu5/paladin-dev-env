---
phase: 01-ground-truth-decision-records
plan: 07
type: execute
wave: 4
depends_on: ["01-06"]
files_modified:
  - .planning/ledgers/milestone-01.md
autonomous: true
requirements: [RECON-01]

must_haves:
  truths:
    - "Every `REQ-*` row from Epics 6-10 and the unit-test-improvements workstream of REQUIREMENTS.md's Milestone 1 as-shipped ledger is present in `.planning/ledgers/milestone-01.md`, keyed on the requirement ID (D-17, D-18)."
    - "The ledger enumerates exactly 39 outstanding Milestone-1 task items — no more, no fewer — and the achieved total is stated in the ledger alongside its source, `intel/task-completion-state.md`."
    - "The 26 outstanding task items belonging to Epics 6, 8, 9 and the unit-test-improvements workstream are nested under the `REQ-*` row they belong to, quoted by literal checkbox text and cited by task-file path and line (D-18)."
    - "Every row carries exactly one of the five legend verdicts and no sixth class is invented (D-20)."
    - "Every `satisfied` row carries a `file:line` citation PLUS a named passing test, example or command that exercises it; a row with a citation and no named exerciser is `present, unproven` (D-19)."
    - "Epic 10's rows are consistent with the Task 7.0 verdict plan 01-05 recorded — the ledger states one answer for Epic 10, not two."
    - "A developer can open one status ledger and see, for every outstanding Milestone-1 task item, whether shipped code already satisfies it, with a `file:line` citation as the evidence."
  prohibitions:
    - statement: "MUST NOT assert a `satisfied` verdict from document text or memory alone — every `satisfied` verdict comes from a citation re-checked against the shipped tree at authoring time, and a claim that cannot be re-checked is recorded as `present, unproven` rather than upgraded."
      status: resolved
      verification: judgment
    - statement: "MUST NOT downgrade genuinely outstanding work to `deferred with reason` in the absence of a document that actually defers it — a verdict of deferral requires a citation to the deferral, not an inference from the work not having been done."
      status: resolved
      verification: judgment
    - statement: "MUST NOT paraphrase an outstanding task item into a tidier description — the literal checkbox text is what the ledger nests, so a reader can join the ledger back to the task file that carries it."
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/ledgers/milestone-01.md"
      provides: "Per-requirement verdicts and nested outstanding task items for Milestone 1 Epics 6-10 and the unit-test-improvements workstream, plus the enumerated outstanding-item total"
      contains: "39"
  key_links:
    - from: ".planning/ledgers/milestone-01.md"
      to: ".planning/intel/task-completion-state.md"
      via: "the enumerated outstanding-item total is stated against the deterministic measurement it must match"
      pattern: "task-completion-state\\.md"
    - from: ".planning/ledgers/milestone-01.md"
      to: ".project/Milestone_1-MVP/Epic_6/tasks-provider-expansion.md"
      via: "the 19 Epic 6 outstanding items are nested by literal text with line citations"
      pattern: "tasks-provider-expansion\\.md:[0-9]+"
---

<objective>
Complete the ledger: the per-requirement rows for Milestone 1 Epics 6 through 10 and the
unit-test-improvements workstream, the 26 outstanding task items those sections own, and the
enumerated outstanding-item total that closes RECON-01.

Purpose: Epic 6 alone holds 19 of the 39 outstanding Milestone-1 task items — half the phase's
outstanding-item universe sits in one file. Epic 10's rows must land on the single Task 7.0 verdict
plan 01-05 already recorded, so the ledger does not contradict itself. When this plan finishes, a
developer can open one file and see every outstanding Milestone-1 task item with a cited verdict.

Output: the Epic 6-10 and unit-test-improvements sections of `.planning/ledgers/milestone-01.md`,
plus the outstanding-item total.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ground-truth-decision-records/01-CONTEXT.md
@.planning/phases/01-ground-truth-decision-records/01-RESEARCH.md
@.planning/phases/01-ground-truth-decision-records/01-PATTERNS.md
@.planning/phases/01-ground-truth-decision-records/01-06-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols, files and vocabulary created by Phase 1 (none are pre-existing; the drift-verification pass
must not flag them as unacknowledged):

- **New directories:** `.planning/decisions/`, `.planning/ledgers/`
- **New files:** `.planning/decisions/PROMOTION.md`; `.planning/decisions/0001-battalion-config.md`
  … `0006-coverage-gate.md`; `.planning/ledgers/milestone-01.md`;
  `.planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md`
- **New ADR headings (document vocabulary):** `## Code Conformance`, `## Downstream Consumers`
- **New field values:** `conforms`, `must change` (the `Code conformance` field, D-03)
- **New ledger verdict classes:** `satisfied`, `present, unproven`, `genuinely outstanding`,
  `deferred with reason`, `superseded by shipped code` (D-20)
- **New ledger structure:** per-epic `### Epic N — …` sections keyed on `REQ-*` IDs with nested
  outstanding-item blocks; this plan adds the closing `## Outstanding item reconciliation` section
- **Rust identifiers recorded elsewhere in this phase (NOT implemented — Phase 2 GAP-07 implements
  them):** `BattalionCheckpointConfig`, and the new temperature-range field on `ProviderCapabilities`

<tasks>

<task type="auto">
  <name>Task 1: Ledger rows for Epic 6 (Provider Expansion) — 19 of the 39 outstanding items</name>
  <read_first>
    - `.planning/ledgers/milestone-01.md` — the Epic 1-5 sections written by plan 01-06, for shape, legend vocabulary and nesting style
    - `.planning/REQUIREMENTS.md` lines 2463-2480 — the existing Epic 6 ledger table (9 rows)
    - `.project/Milestone_1-MVP/Epic_6/tasks-provider-expansion.md` — all 19 open checkboxes, read for their literal item text and line numbers; this is the single largest concentration of outstanding items in Milestone 1
    - `.planning/intel/task-completion-state.md` lines 14-24 — the deterministic per-file open counts these must match exactly
    - `.planning/decisions/0004-temperature-validation.md` — the ADR settling Epic 6 REQ-5's temperature range; Epic 6 rows touching temperature link to it rather than re-deciding
    - `.planning/STATE.md` §Deferred Items — the live-provider-API test suite row, which records that Epic 6 task 7.0 was un-deferred by ingest run 2 and that only the skip-vs-fail semantics remain open (VERIFY-06)
    - `.planning/codebase/TESTING.md` — the three-tier test strategy, for naming the exerciser half of the evidence bar
  </read_first>
  <files>.planning/ledgers/milestone-01.md</files>
  <action>
Append a `### Epic 6 — Provider Expansion` section carrying every `REQ-*` row that epic has in
`REQUIREMENTS.md`'s existing Milestone 1 ledger, in source order, then nest all 19 of its open task
items under the requirements that own them.

Apply the same three-step procedure the Epic 1-5 sections used: re-verdict each row under the D-20
legend, apply the D-19 evidence bar (a `file:line` citation re-verified at authoring time PLUS a
named passing test, example or command; citation-only means `present, unproven`), and nest each open
checkbox by its **literal text** with its `tasks-provider-expansion.md` path and line number and its
own verdict.

Three things are specific to Epic 6:

**The 19 items are the load-bearing part, not the 9 rows.** Read every open checkbox in the task file
and account for each one. Where several belong under the same requirement, nest them all under it
rather than collapsing them into a summary bullet. Where an item belongs under no existing `REQ-*`
row, say so explicitly in the nesting rather than silently dropping it — an unattributable item is a
finding.

**Task 7.0 is not simply deferred.** `STATE.md` records that the live-provider-API integration test
suite was un-deferred by ingest run 2 — the suite ships behind the `live-api-tests` feature, and what
remains open is the skip-versus-fail semantics that VERIFY-06 owns. Verify that against the tree
before classifying: a row that says `deferred with reason` here would be wrong, and the third
prohibition forbids inferring a deferral from work not having been done.

**Temperature rows point at ADR-0004.** Epic 6 REQ-5's DeepSeek 0.0-2.0 range is settled by
`.planning/decisions/0004-temperature-validation.md`, which is `must change`. The row records the code
as it stands today and names the ADR plus GAP-07 as what changes it; it does not restate the ADR's
reasoning and must not reach a different answer.

Use Medieval-military ubiquitous language (Paladin, Herald, Arsenal) as the shipped code names them.
  </action>
  <acceptance_criteria>
    - `.planning/ledgers/milestone-01.md` contains a section heading for Epic 6
    - The number of `REQ-*` rows under that heading equals the number under the Epic 6 heading in `.planning/REQUIREMENTS.md`'s Milestone 1 ledger (9)
    - Exactly 19 nested outstanding-item bullets cite `tasks-provider-expansion.md` with a line number, matching `intel/task-completion-state.md`
    - Each of those 19 bullets' text matches a literal open checkbox line in `.project/Milestone_1-MVP/Epic_6/tasks-provider-expansion.md`
    - No Epic 6 row or nested item classifies the live-provider-API test suite as `deferred with reason` without citing a document that defers it
    - Any Epic 6 row whose subject is the temperature range links to `.planning/decisions/0004-temperature-validation.md`
    - Every `file:line` cited in this section resolves
  </acceptance_criteria>
  <verify>
    <automated>grep -q '### Epic 6 ' .planning/ledgers/milestone-01.md && [ "$(grep -cE 'tasks-provider-expansion\.md:[0-9]+' .planning/ledgers/milestone-01.md)" -eq 19 ] && [ "$(grep -c '^- \[ \]' .project/Milestone_1-MVP/Epic_6/tasks-provider-expansion.md)" -eq 19 ] && [ "$(awk '/^### Epic 6 /,/^### Epic 7 /' .planning/ledgers/milestone-01.md | grep -c 'REQ-')" -ge 9 ] && echo EPIC6_OK</automated>
  </verify>
  <done>All 9 Epic 6 requirements have a citation-backed verdict and all 19 of Epic 6's outstanding task items are nested under them by literal text with resolving line citations — half the phase's outstanding-item universe accounted for in one section.</done>
</task>

<task type="auto">
  <name>Task 2: Ledger rows for Epics 7-10 and the unit-test workstream, plus the outstanding-item reconciliation</name>
  <read_first>
    - `.planning/ledgers/milestone-01.md` — every section written so far, including plan 01-05's Epic 10 Task 7.0 verdict, which Epic 10's rows must agree with
    - `.planning/REQUIREMENTS.md` lines 2477-2541 — the existing Epic 7, 8, 9, 10 and unit-test-improvements ledger tables (10, 10, 12, 9 and 2 rows)
    - `.project/Milestone_1-MVP/Epic_8/tasks-herald-output-formatting.md` — the 2 open checkboxes, read for literal text and line numbers
    - `.project/Milestone_1-MVP/Epic_9/tasks-armory-cli-tools.md` — the 3 open checkboxes, same
    - `.project/Milestone_1-MVP/unit-test-improvements/tasks-improve-unit-test-coverage.md` — the 2 open checkboxes, same
    - `.planning/intel/task-completion-state.md` — the deterministic per-file open counts and the 39-item total
    - `.planning/decisions/0005-herald-trait.md` and `.planning/decisions/0006-coverage-gate.md` — the ADRs settling Epic 8's trait shape and the coverage question the unit-test workstream measures against
    - `.planning/STATE.md` §Deferred Items — the CLI end-to-end test rows, which record that Epic 9 tasks 13.4-13.6 were un-deferred by ingest run 2
  </read_first>
  <files>.planning/ledgers/milestone-01.md</files>
  <action>
Append `### Epic 7 — Citadel State Persistence`, `### Epic 8 — Herald Output Formatting`,
`### Epic 9 — Armory CLI Tools`, `### Epic 10 — Validation & Documentation` and
`### unit-test-improvements workstream` sections, then close the ledger with a reconciliation
section.

**The five epic sections** follow the same three-step procedure: carry every `REQ-*` row forward in
source order, re-verdict under the D-20 legend through the D-19 evidence bar with a re-verified
`file:line` plus a named exerciser, and nest each open checkbox by literal text with its task-file
path and line. Epic 7 and Epic 10 have no open checkboxes, so their rows carry no nested block at all.

Specifics for these epics:
- **Epic 8** rows whose subject is the `Herald` trait method set link to
  `.planning/decisions/0005-herald-trait.md`, which is `conforms`; record the verdict and link,
  do not restate the reasoning.
- **Epic 9** carries the interactive-REPL divergence that plan 01-01 already flagged in the
  divergence section. The Epic 9 rows point at that row rather than repeating it, so the ledger
  states the divergence once. `STATE.md` records that Epic 9 tasks 13.4-13.6 were un-deferred by
  ingest run 2 because the blocking mock provider shipped — verify against the tree before
  classifying any of the 3 open items as deferred.
- **Epic 10** must land on the single Task 7.0 verdict plan 01-05 recorded. Cross-read that section
  before writing these rows; if an Epic 10 row would state a different answer, the row is wrong, not
  the verdict.
- **unit-test-improvements** rows measure against the coverage number recorded in
  `.planning/decisions/0006-coverage-gate.md`. Where a row asserts a coverage figure, it links to the
  ADR rather than carrying its own number, so the phase ships one number and one scope.

**Then add a closing `## Outstanding item reconciliation` section** that closes RECON-01's count:
state the total number of outstanding Milestone-1 task items this ledger nests; state the per-file
breakdown across the 8 task files that carry them; state the source those figures must match
(`intel/task-completion-state.md`'s deterministic GFM-checkbox measurement, which is not an LLM
classification); and state whether the two agree. If they disagree, record both figures and the
difference rather than adjusting either — the same discipline plan 01-05 applied to the requirement
counts. Also state the distribution of the five verdict classes across the whole ledger, so the size
of the `present, unproven` bucket is visible as a number rather than implied.
  </action>
  <acceptance_criteria>
    - `.planning/ledgers/milestone-01.md` contains section headings for Epic 7, Epic 8, Epic 9, Epic 10 and the unit-test-improvements workstream
    - The `REQ-*` row count under each equals the count under the corresponding heading in `.planning/REQUIREMENTS.md`'s Milestone 1 ledger (10, 10, 12, 9 and 2 — 43 in total)
    - Exactly 2 nested bullets cite `tasks-herald-output-formatting.md`, exactly 3 cite `tasks-armory-cli-tools.md`, and exactly 2 cite `tasks-improve-unit-test-coverage.md`
    - A `## Outstanding item reconciliation` section exists stating the total outstanding-item count, the per-file breakdown over 8 files, the `intel/task-completion-state.md` source, and whether the figures agree
    - The stated total equals the number of nested outstanding-item bullets actually present in the file
    - The reconciliation section states a count for each of the five verdict classes
    - Epic 10's rows state the same Task 7.0 answer as the dispute-resolution section written by plan 01-05
    - Epic 8 rows about the `Herald` trait link to `.planning/decisions/0005-herald-trait.md`; coverage-asserting rows link to `.planning/decisions/0006-coverage-gate.md`
  </acceptance_criteria>
  <verify>
    <automated>for h in 'Epic 7 ' 'Epic 8 ' 'Epic 9 ' 'Epic 10 ' 'unit-test-improvements'; do grep -q "### $h" .planning/ledgers/milestone-01.md || exit 1; done && [ "$(grep -cE 'tasks-herald-output-formatting\.md:[0-9]+' .planning/ledgers/milestone-01.md)" -eq 2 ] && [ "$(grep -cE 'tasks-armory-cli-tools\.md:[0-9]+' .planning/ledgers/milestone-01.md)" -eq 3 ] && [ "$(grep -cE 'tasks-improve-unit-test-coverage\.md:[0-9]+' .planning/ledgers/milestone-01.md)" -eq 2 ] && grep -q '## Outstanding item reconciliation' .planning/ledgers/milestone-01.md && grep -q 'task-completion-state.md' .planning/ledgers/milestone-01.md && [ "$(grep -cE 'tasks-[a-z-]+\.md:[0-9]+' .planning/ledgers/milestone-01.md)" -eq 39 ] && grep -q 'decisions/0005-herald-trait.md' .planning/ledgers/milestone-01.md && echo LEDGER_COMPLETE</automated>
  </verify>
  <done>The ledger holds all 112 Milestone-1 requirement rows and all 39 outstanding task items, its outstanding-item count is reconciled against the deterministic measurement, and the five verdict classes are reported as counts so the `present, unproven` bucket is visible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| task-list checkbox state → ledger verdict | checkbox state has been wrong in both directions across three ingest runs; Epic 6 holds 19 of the 39 open items |
| a prior plan's recorded verdict → this plan's rows | Epic 10's rows are written after plan 01-05 recorded a Task 7.0 verdict; two answers in one file would defeat the ledger's purpose |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-16 | Spoofing | a `satisfied` verdict asserted without a re-checked citation | high | mitigate | The D-19 bar is written into both task actions as a two-part requirement; the first prohibition forbids memory-based verdicts; acceptance criteria require every cited `file:line` to resolve. |
| T-01-19 | Tampering | silent loss of an outstanding item | high | mitigate | The verify commands assert exact per-file counts (19 / 2 / 3 / 2) against the task files themselves and a total of 39 across the whole ledger, so a dropped or duplicated item fails the gate rather than passing quietly. |
| T-01-20 | Repudiation | the ledger contradicting itself on Epic 10 | high | mitigate | Task 2's `read_first` requires cross-reading plan 01-05's verdict first, and the acceptance criteria require agreement; a differing row is defined as the error. |
| T-01-15 | Tampering | outstanding work downgraded to deferred | medium | mitigate | The second prohibition requires a citation to an actual deferral; STATE.md's un-deferral records for Epic 6 task 7.0 and Epic 9 tasks 13.4-13.6 are named in `read_first` so the executor sees them before classifying. |
| T-01-01 | Information Disclosure | quoting ingested `.project/` documents | high | mitigate | Nested items quote a single checkbox line, never document bodies. Secret-in-repo handling is canon and owned by `/gsd-secure-phase`. |
| T-01-SC | Tampering | npm/cargo installs | high | mitigate | This plan installs nothing. RESEARCH.md §Package Legitimacy Audit records no `[ASSUMED]`/`[SUS]` packages, so no blocking legitimacy checkpoint is required. |
</threat_model>

<verification>
- Every Milestone-1 epic and the unit-test workstream have a section, with row counts matching the source ledger
- Exactly 39 nested outstanding-item bullets exist across the whole ledger, matching `intel/task-completion-state.md`
- The reconciliation section reports the total, the 8-file breakdown, and a count per verdict class
- Epic 10's rows agree with plan 01-05's Task 7.0 verdict
</verification>

<success_criteria>
- One file answers, for every outstanding Milestone-1 task item, whether shipped code already satisfies it
- Every verdict carries a `file:line` citation rather than a checkbox
- The size of the `present, unproven` bucket is a stated number
</success_criteria>

<output>
Create `.planning/phases/01-ground-truth-decision-records/01-07-SUMMARY.md` when done
</output>
