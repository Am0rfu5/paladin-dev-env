---
phase: 01-ground-truth-decision-records
plan: 08
type: execute
wave: 5
depends_on: ["01-02", "01-03", "01-04", "01-07"]
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [RECON-01, RECON-02, RECON-03, RECON-04, RECON-05, RECON-06, RECON-07]

must_haves:
  truths:
    - "`.planning/PROJECT.md` `## Key Decisions` holds one row per Phase 1 ADR — six rows — each linking to its file in `.planning/decisions/` (D-06)."
    - "The `Empty by evidence, not by omission` framing under the Key Decisions table is replaced by a pointer to `.planning/decisions/` (D-06)."
    - "`.planning/REQUIREMENTS.md`'s expanded `## Milestone 1 as-shipped ledger` body is reduced to a short pointer paragraph directing readers to `.planning/ledgers/milestone-01.md`, and the summary-table row near line 31 is left intact (D-17)."
    - "No Milestone-1 requirement ID that existed in the REQUIREMENTS.md ledger body is lost — every one of them is present in `.planning/ledgers/milestone-01.md` before the body is reduced."
    - "`.planning/ROADMAP.md` Phase 3 success criterion 1 names one workspace-wide coverage number and scope instead of unit and integration coverage separately, and cites the ADR that fixed them (D-08)."
    - "The other four as-shipped ledger sections in REQUIREMENTS.md (Milestones 2-3, 4-6, 7-8, 9-12) are untouched — only the Milestone 1 body is reduced."
  prohibitions:
    - statement: "MUST NOT reduce the REQUIREMENTS.md ledger body to a pointer before verifying that every requirement ID it carried is present in the new ledger file — a pointer to an incomplete destination destroys the record it claims to relocate."
      status: resolved
      verification: judgment
    - statement: "MUST NOT rewrite `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md` or `.planning/ROADMAP.md` wholesale — every change is a scoped in-place replacement, because a whole-file write destroys content outside the diff window that no source artifact would restore."
      status: resolved
      verification: judgment
    - statement: "MUST NOT amend another phase's success criterion silently — the amendment states that it was made by Phase 1, names the ADR that required it, and leaves the phase's scope otherwise unchanged."
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions table with six ADR rows and a pointer to .planning/decisions/"
      contains: "decisions/0006-coverage-gate.md"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Milestone 1 as-shipped ledger body reduced to a pointer"
      contains: "ledgers/milestone-01.md"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 3 success criterion 1 amended to one workspace-wide coverage number"
      contains: "decisions/0006-coverage-gate.md"
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/decisions/"
      via: "each Key Decisions row links to its ADR file"
      pattern: "decisions/000[1-6]-"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ledgers/milestone-01.md"
      via: "the reduced ledger section points at the new ledger file"
      pattern: "ledgers/milestone-01\\.md"
    - from: ".planning/ROADMAP.md"
      to: ".planning/decisions/0006-coverage-gate.md"
      via: "Phase 3's amended criterion cites the ADR that fixed the number and scope"
      pattern: "0006-coverage-gate"
---

<objective>
Correct the record at source: populate PROJECT.md's Key Decisions table with all six Phase 1 ADRs,
reduce REQUIREMENTS.md's Milestone 1 ledger body to a pointer at the new ledger file, and amend
ROADMAP.md's Phase 3 success criterion to the single coverage number ADR-0006 recorded.

Purpose: this phase's whole point is that `.planning/` describes the v0.7.0 code as it actually is.
The six ADRs and the ledger are only authoritative if the documents that readers actually open point
at them. This is the last plan in the phase and runs after every ADR and every ledger row exists, so
nothing points at an incomplete destination.

Output: scoped in-place edits to `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md` and
`.planning/ROADMAP.md`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ground-truth-decision-records/01-CONTEXT.md
@.planning/phases/01-ground-truth-decision-records/01-RESEARCH.md
@.planning/phases/01-ground-truth-decision-records/01-PATTERNS.md
@.planning/phases/01-ground-truth-decision-records/01-07-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols, files and vocabulary created by Phase 1 (none are pre-existing; the drift-verification pass
must not flag them as unacknowledged):

- **New directories:** `.planning/decisions/`, `.planning/ledgers/`
- **New files:** `.planning/decisions/PROMOTION.md`; `.planning/decisions/0001-battalion-config.md`
  … `0006-coverage-gate.md`; `.planning/ledgers/milestone-01.md`;
  `.planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md`
- **New ADR headings (document vocabulary):** `## Code Conformance`, `## Downstream Consumers`
- **New field values:** `conforms`, `must change` (the `Code conformance` field, D-03)
- **New ledger verdict classes:** `satisfied`, `present, unproven`, `genuinely outstanding`,
  `deferred with reason`, `superseded by shipped code` (D-20)
- **New naming scheme:** flat zero-padded monotonic ADR counter `NNNN-kebab-slug.md`
- **Rust identifiers recorded elsewhere in this phase (NOT implemented — Phase 2 GAP-07 implements
  them):** `BattalionCheckpointConfig`, and the new temperature-range field on `ProviderCapabilities`

<tasks>

<task type="auto">
  <name>Task 1: Populate PROJECT.md Key Decisions with all six Phase 1 ADRs</name>
  <read_first>
    - `.planning/PROJECT.md` lines 1015-1080 — the `## Key Decisions` table as plan 01-01 left it (one ADR-0005 row, the framing paragraph already replaced) and the eleven-ADR-candidate inventory below it
    - `.planning/decisions/0001-battalion-config.md`, `0002-battalion-result.md`, `0003-formation-min-paladins.md`, `0004-temperature-validation.md`, `0005-herald-trait.md`, `0006-coverage-gate.md` — each ADR's `## Decision` and `## Code Conformance`, so the table's Rationale and Outcome cells restate them accurately
    - `.planning/decisions/PROMOTION.md` — the numbering index, to confirm no ADR number is missing or duplicated
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
Use scoped `Edit` replacements on `.planning/PROJECT.md`. Never write the whole file — it is ~1,100
lines and holds the corpus-level findings that nothing else records.

Add the five missing rows to `## Key Decisions`, joining the ADR-0005 row plan 01-01 already placed,
so the table holds six rows in ADR-number order. Keep the existing three-column
`Decision | Rationale | Outcome` schema. For each row:
- **Decision** — the question the ADR answers, with a Markdown link to its file under
  `.planning/decisions/`.
- **Rationale** — one sentence naming the chosen variant and the evidence class it rests on, drawn
  from that ADR's `## Decision` section. Do not restate the ADR at length; the link carries the
  detail.
- **Outcome** — the ADR's `## Code Conformance` value verbatim: `conforms` for ADR-0002 and
  ADR-0005, `must change` for ADR-0001, ADR-0003 and ADR-0004 with the executing requirement named,
  and whatever ADR-0006 recorded. Read each ADR's actual field rather than assuming, since ADR-0006's
  value is decided at execution time.

Confirm the framing under the table still points at `.planning/decisions/` (plan 01-01 replaced the
"empty by evidence" paragraph). If any restatement of that emptiness survives elsewhere in
PROJECT.md — the Context section states the same finding in prose — amend those to record that the
finding was true through ingest and that Phase 1 recorded the first six protected decisions, rather
than deleting the finding. It is the corpus's most notable structural result and should read as
history, not be erased.

Leave the eleven-ADR-candidate inventory intact, and add one line noting that
`.planning/decisions/PROMOTION.md` now carries the promotion procedure and owner assignment for all
eleven.
  </action>
  <acceptance_criteria>
    - `.planning/PROJECT.md` `## Key Decisions` table has exactly six data rows
    - The table contains a link to each of `decisions/0001-battalion-config.md`, `0002-battalion-result.md`, `0003-formation-min-paladins.md`, `0004-temperature-validation.md`, `0005-herald-trait.md` and `0006-coverage-gate.md`
    - Each row's Outcome cell matches the `## Code Conformance` value in the ADR it links to
    - `grep -q "\*(none)\*" .planning/PROJECT.md` fails
    - `grep -c "ADR → shipped tree" .planning/PROJECT.md` still returns `3` — plan 01-01's precedence edits survived
    - PROJECT.md still contains its eleven-ADR-candidate inventory and now references `PROMOTION.md`
    - `git diff --stat .planning/PROJECT.md` shows a change of well under 100 lines — evidence of scoped edits rather than a rewrite
  </acceptance_criteria>
  <verify>
    <automated>for n in 0001-battalion-config 0002-battalion-result 0003-formation-min-paladins 0004-temperature-validation 0005-herald-trait 0006-coverage-gate; do grep -q "decisions/$n.md" .planning/PROJECT.md || exit 1; done && [ "$(grep -c 'ADR → shipped tree' .planning/PROJECT.md)" = "3" ] && grep -q 'PROMOTION.md' .planning/PROJECT.md && ! grep -q '\*(none)\*' .planning/PROJECT.md && echo KEYDECISIONS_OK</automated>
  </verify>
  <done>PROJECT.md's Key Decisions table holds six real rows linking to six real ADR files, each Outcome cell matching its ADR's recorded conformance, with the corpus-level emptiness finding preserved as history rather than deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce REQUIREMENTS.md's Milestone 1 ledger body to a pointer</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 2361-2541 — the full `## Milestone 1 as-shipped ledger` body: its status-key legend and eleven per-epic tables, ending where `## Milestone 2-3 as-shipped ledger` begins
    - `.planning/REQUIREMENTS.md` lines 28-40 — the summary table whose Milestone 1 row must be left in place
    - `.planning/ledgers/milestone-01.md` — the destination; read its `## Outstanding item reconciliation` section for the row and verdict counts that prove completeness
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
**Verify before reducing.** Extract every `REQ-*` identifier from the `## Milestone 1 as-shipped
ledger` body (the range between its own heading and the `## Milestone 2-3 as-shipped ledger`
heading), extract every `REQ-*` identifier from `.planning/ledgers/milestone-01.md`, and confirm the
first set is a subset of the second. If any identifier is missing from the new ledger, HALT and
report it — reducing the body to a pointer while a row is missing destroys the record the pointer
claims to relocate, which the first prohibition forbids. Record the two counts in the SUMMARY.

**Then reduce, with a scoped `Edit`.** Replace the body of `## Milestone 1 as-shipped ledger` — the
status-key legend and all eleven per-epic tables — with a short pointer paragraph that states: the
per-requirement verdicts now live in `.planning/ledgers/milestone-01.md`; why they moved (D-17 —
REQUIREMENTS.md is already ~4,000 lines and holds five as-shipped ledger sections, and five sets of
`file:line`-cited verdicts inline would make it unreadable); the number of requirement rows and the
number of nested outstanding task items the destination carries; and that Phases 5, 7, 10 and 13 add
sibling files in the same directory. Keep the `## Milestone 1 as-shipped ledger` heading itself so
every existing cross-reference to that section still resolves.

Do not touch the summary-table row near line 31 — it is already no longer than the pointer treatment
would make it, and PATTERNS.md records it as the shape the pointer imitates. Do not touch the
Milestone 2-3, 4-6, 7-8 or 9-12 ledger sections; their relocation belongs to Phases 5, 7, 10 and 13.

Use scoped `Edit` replacements. `.planning/REQUIREMENTS.md` is ~4,000 lines; a whole-file write would
destroy the forward requirements, the thirty variant groups and four other ledgers.
  </action>
  <acceptance_criteria>
    - Every `REQ-*` identifier that appeared in REQUIREMENTS.md's Milestone 1 ledger body before this task also appears in `.planning/ledgers/milestone-01.md`
    - The `## Milestone 1 as-shipped ledger` heading still exists in `.planning/REQUIREMENTS.md`
    - The section between that heading and `## Milestone 2-3 as-shipped ledger` contains a link to `.planning/ledgers/milestone-01.md` and fewer than 20 lines
    - The section between those two headings contains no `### Epic` sub-heading and no `| REQ-` table row
    - The summary-table row near line 31 naming the Milestone 1 as-shipped ledger is unchanged
    - `grep -c "as-shipped ledger" .planning/REQUIREMENTS.md` still finds the Milestone 2-3, 4-6, 7-8 and 9-12 sections
    - The SUMMARY records both requirement-ID counts and states that the subset check passed
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## Milestone 1 as-shipped ledger' .planning/REQUIREMENTS.md && S=$(awk '/^## Milestone 1 as-shipped ledger/,/^## Milestone 2-3 as-shipped ledger/' .planning/REQUIREMENTS.md) && printf '%s' "$S" | grep -q 'ledgers/milestone-01.md' && [ "$(printf '%s' "$S" | grep -c '^| REQ-')" -eq 0 ] && [ "$(printf '%s' "$S" | grep -c '^### Epic')" -eq 0 ] && [ "$(printf '%s' "$S" | wc -l)" -lt 25 ] && for m in 'Milestone 2-3 as-shipped ledger' 'Milestone 4-6 as-shipped ledger' 'Milestone 7-8 as-shipped ledger' 'Milestone 9-12 as-shipped ledger'; do grep -q "$m" .planning/REQUIREMENTS.md || exit 1; done && echo POINTER_OK</automated>
  </verify>
  <done>REQUIREMENTS.md's Milestone 1 ledger body is a short pointer at a destination proven to carry every requirement ID it held, the heading and summary-table row still resolve, and the four sibling ledger sections are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Amend ROADMAP.md Phase 3's coverage success criterion to one number and one scope</name>
  <read_first>
    - `.planning/ROADMAP.md` §"Phase 3" — success criterion 1 in full, plus the phase's Requirements line, so the amendment changes the criterion and not the scope
    - `.planning/decisions/0006-coverage-gate.md` — the recorded number, scope, command, floor and target the criterion must now name
    - `.planning/ROADMAP.md` §Progress — the coupling note `RECON-07 → VERIFY-05 → PIPE-02`, which the amendment should stay consistent with
    - `.planning/phases/01-ground-truth-decision-records/01-CONTEXT.md` §Deferred Ideas — the boundary: the amendment is in scope here, re-scoping Phase 3's testing work is Phase 3's job
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
Amend Phase 3's success criterion 1 with a scoped `Edit`. It currently names unit and integration
coverage separately; D-08 fixed the scope as one workspace-wide line-coverage number, all tests
combined, from one reproducible command, so the criterion must name that instead.

Rewrite the criterion so it: names the single workspace-wide line-coverage figure and the floor
recorded in ADR-0006; links to `.planning/decisions/0006-coverage-gate.md` as the source of both the
number and the exact command; and states that the measurement scope (feature set, path exclusions,
doctest inclusion) is fixed by that ADR rather than restated here, so the two cannot drift apart.

Add a short parenthetical recording that the criterion was amended by Phase 1 under RECON-07/D-08 —
an amendment to another phase's contract should say who made it and why, which the third prohibition
requires.

Do not change Phase 3's Requirements line, its goal, or any other criterion. Re-scoping Phase 3's
testing work to the new single number is Phase 3's job, not this one. Do not touch Phase 1's own
`**Plans:**` line — the planning workflow maintains it.

Use a scoped `Edit`. `.planning/ROADMAP.md` holds all sixteen phases; a whole-file write would
destroy the fifteen this plan has no business touching.
  </action>
  <acceptance_criteria>
    - Phase 3's success criterion 1 in `.planning/ROADMAP.md` names one workspace-wide coverage number and does not name unit and integration coverage as two separate figures
    - The criterion links to `.planning/decisions/0006-coverage-gate.md`
    - The criterion records that it was amended by Phase 1 under RECON-07
    - Phase 3's Requirements line and goal are byte-identical to before this task
    - All sixteen `### Phase N:` headings are still present in `.planning/ROADMAP.md`
    - `git diff --stat .planning/ROADMAP.md` shows a change of under 20 lines
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -c '^### Phase [0-9]' .planning/ROADMAP.md)" -eq 16 ] && P3=$(awk '/^### Phase 3:/,/^### Phase 4:/' .planning/ROADMAP.md) && printf '%s' "$P3" | grep -q '0006-coverage-gate.md' && printf '%s' "$P3" | grep -q 'RECON-07' && [ "$(git diff --numstat .planning/ROADMAP.md | awk '{print $1+$2}')" -lt 20 ] && echo ROADMAP_OK</automated>
  </verify>
  <done>Phase 3 can objectively pass or fail against one number and one scope owned by ADR-0006, the amendment names Phase 1 as its author, and no other phase in the roadmap was altered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a source-of-truth document → a pointer at another file | reducing REQUIREMENTS.md's ledger body to a pointer destroys the original unless the destination is verified complete first |
| Phase 1 → Phase 3's contract | amending another phase's success criterion changes what that phase is measured against |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-21 | Denial of Service | whole-file rewrite of PROJECT.md / REQUIREMENTS.md / ROADMAP.md | high | mitigate | Every task action mandates scoped `Edit` replacements and names what a whole-file write would destroy; acceptance criteria bound the diff size (`git diff --numstat` under 20 lines for ROADMAP.md) and assert survivors (16 phase headings, 3 precedence restatements, 4 sibling ledger sections). |
| T-01-22 | Tampering | pointer written before the destination is complete | high | mitigate | Task 2 performs an explicit subset check of every `REQ-*` identifier and HALTs on a miss; the plan depends on 01-07, so it cannot run before the ledger is finished. |
| T-01-23 | Repudiation | another phase's criterion amended without attribution | medium | mitigate | The third prohibition and Task 3's acceptance criteria require the amendment to name Phase 1 and RECON-07 as its author and reason. |
| T-01-24 | Tampering | Key Decisions Outcome cells drifting from the ADRs they link to | medium | mitigate | Task 1's action requires reading each ADR's actual `## Code Conformance` field rather than assuming, since ADR-0006's value is decided at execution time; the acceptance criteria require the cells to match. |
| T-01-01 | Information Disclosure | editing planning documents | low | accept | No secret material is introduced; the edits are relocations and links within `.planning/`. |
| T-01-SC | Tampering | npm/cargo installs | high | mitigate | This plan installs nothing. RESEARCH.md §Package Legitimacy Audit records no `[ASSUMED]`/`[SUS]` packages, so no blocking legitimacy checkpoint is required. |
</threat_model>

<verification>
- PROJECT.md's Key Decisions table holds six ADR-linked rows and the precedence edits from plan 01-01 survive
- REQUIREMENTS.md's Milestone 1 ledger body is a pointer under 25 lines with no epic sub-headings or `REQ-*` table rows, and every relocated ID exists in the destination
- ROADMAP.md still holds all sixteen phase headings; Phase 3's criterion 1 names one number and links to ADR-0006
- No document was rewritten wholesale
</verification>

<success_criteria>
- Every reader-facing planning document points at the six ADRs and the ledger rather than at superseded prose
- No Milestone-1 requirement ID was lost in the relocation
- Phase 3 has one number and one scope to pass or fail against
</success_criteria>

<output>
Create `.planning/phases/01-ground-truth-decision-records/01-08-SUMMARY.md` when done
</output>
