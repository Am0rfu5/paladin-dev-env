---
phase: 01-ground-truth-decision-records
plan: 11
type: execute
wave: 6
depends_on: ["01-02", "01-07", "01-08"]
files_modified:
  - .planning/ledgers/milestone-01.md
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
autonomous: true
gap_closure: true
requirements: [RECON-01, RECON-03]

must_haves:
  truths:
    - "`.planning/ledgers/milestone-01.md` carries a row for `REQ-battalion-result-v1` in its Epic 4 table, so the ledger is a true superset of the `REQ-*` IDs REQUIREMENTS.md's Milestone 1 body carried (RECON-01, RECON-03)."
    - "The new row's verdict is one of the five legend classes and its evidence is transcribed from ADR-0002's own analysis with a `file:line` citation — no sixth verdict class is invented and no evidence is authored fresh (D-20, D-19)."
    - "`REQ-battalion-result-v1` and `REQ-battalion-result-v2` remain two separate rows in two separate epic tables (Epic 4 and Epic 5); they are not merged into one row even though a single ADR resolves both (resolves RECON-03 `adjacency`)."
    - "The new row sits at a pinned position in the Epic 4 table — immediately after `REQ-battalion-config-v2` and immediately before `REQ-battalion-error-strategy` — mirroring its relative position in REQUIREMENTS.md's own Milestone 1 body (resolves RECON-03 `ordering`)."
    - "If ADR-0002 carries no `REQ-battalion-result-v1` bullet in its `## Considered Options`, the task halts rather than authoring evidence the ADR does not contain (resolves RECON-03 `empty`)."
    - "The ledger's verdict-distribution table moves by exactly one bucket and exactly one total: `superseded by shipped code` and the `REQ-*` row count each rise by one, the stated total rises by one, and the five bucket counts still sum to that total (resolves RECON-01 `precision`)."
    - "The subset check passes with zero missing identifiers before REQUIREMENTS.md's Milestone 1 ledger body is reduced; a single missing identifier halts the reduction (resolves RECON-01 `boundary`)."
    - "The subset check is set-based and order-independent: an identifier appearing in a different epic table, or in a different position, still counts as present (resolves RECON-01 `ordering`)."
    - "An identifier that appears in two epic tables of the destination counts as present once, and its duplicate rows are left as two rows rather than being deduplicated (resolves RECON-01 `adjacency`)."
    - "An empty extraction from either side is treated as an extraction failure and halts the task — it is never allowed to satisfy the subset check vacuously (resolves RECON-01 `empty`)."
    - "`.planning/REQUIREMENTS.md`'s expanded `## Milestone 1 as-shipped ledger` body is reduced to a short pointer paragraph directing readers to `.planning/ledgers/milestone-01.md`, the heading itself survives so existing cross-references still resolve, and the summary-table row near line 31 is left intact (D-17)."
    - "The other four as-shipped ledger sections in REQUIREMENTS.md (Milestones 2-3, 4-6, 7-8, 9-12) are byte-identical afterwards — only the Milestone 1 body is reduced."
    - "`.planning/WINDOWS.md` open item 1 is marked fixed only after the missing row exists, so `open_count` falls to 0 because the defect was repaired rather than dismissed."
    - statement: "The subset check is re-run inside the same task that performs the reduction, immediately before it, rather than relying on a result computed in an earlier task, an earlier plan, or a parallel wave-6 run — so no window exists in which the destination changed between the check and the reduction."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT resolve the `REQ-battalion-result-v1` gap by deleting, waiving or rewording the `.planning/WINDOWS.md` entry instead of adding the missing row. Closing a defect-register entry without the repair converts a visible gap into an invisible one, which is the precise failure the register exists to prevent."
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/ledgers/milestone-01.md"
      provides: "Epic 4 row for REQ-battalion-result-v1 and a verdict-distribution table consistent with it"
      contains: "REQ-battalion-result-v1"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Milestone 1 as-shipped ledger body reduced to a pointer at the completed ledger"
      contains: "ledgers/milestone-01.md"
    - path: ".planning/WINDOWS.md"
      provides: "Open item 1 marked fixed after the repair"
      contains: "REQ-battalion-result-v1"
  key_links:
    - from: ".planning/ledgers/milestone-01.md"
      to: ".planning/decisions/0002-battalion-result.md"
      via: "the new Epic 4 row cites ADR-0002 as the decision that resolves it, rather than re-deciding per-epic"
      pattern: "0002-battalion-result"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ledgers/milestone-01.md"
      via: "the reduced ledger section points at the new ledger file"
      pattern: "ledgers/milestone-01\\.md"
---

<objective>
Close the one Phase 1 gap that has nothing to do with coverage: add the missing
`REQ-battalion-result-v1` row to the Milestone 1 ledger, then complete the REQUIREMENTS.md pointer
reduction that plan 01-08 correctly refused to perform against an incomplete destination.

Purpose: plan 01-08 Task 2 ran its own mandatory safety check, found `REQ-battalion-result-v1`
present in REQUIREMENTS.md's Milestone 1 body but absent from every table in
`.planning/ledgers/milestone-01.md`, and HALTed rather than reducing a record to a pointer at a
destination missing a row. That halt was correct and preserved the data. The repair is small and the
answer is already written down — ADR-0002 analyses this exact identifier by name in its
`## Considered Options`. Once the row exists the safety check passes and the reduction is safe.

Wave note: this plan depends on plan 01-02 (which authored ADR-0002, the sole source of the new row's
evidence), plan 01-07 (which built the ledger's Epic 4-10 tables) and plan 01-08 (whose halted safety
check found the gap) — so it lands at `wave: 6`. It is entirely independent of the coverage blocker
and runs in parallel with plan 01-09, whose `files_modified` set is disjoint from this one's.

Output: one new ledger row, a corrected verdict-distribution table, a reduced REQUIREMENTS.md
section, and a closed Broken Windows entry.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ground-truth-decision-records/01-CONTEXT.md
@.planning/phases/01-ground-truth-decision-records/01-PATTERNS.md
@.planning/phases/01-ground-truth-decision-records/01-VERIFICATION.md
@.planning/phases/01-ground-truth-decision-records/01-08-PLAN.md
@.planning/phases/01-ground-truth-decision-records/01-08-SUMMARY.md
</context>

## Artifacts this phase produces

Every item below is created by Phase 1 and does not pre-exist. The plan-review-convergence
source-grounding pass must not flag these as unacknowledged drift.

- **New directories:** `.planning/decisions/`, `.planning/ledgers/`
- **New files:** `.planning/decisions/PROMOTION.md`; `.planning/decisions/0001-battalion-config.md`
  … `0006-coverage-gate.md`; `.planning/ledgers/milestone-01.md`;
  `.planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md`;
  `.planning/WINDOWS.md`
- **New ledger row ID created by THIS plan:** `REQ-battalion-result-v1` in
  `.planning/ledgers/milestone-01.md`'s `### Epic 4 — Battalion Orchestration` table
- **Existing ledger section this plan revises:** `## Outstanding item reconciliation (RECON-01)`'s
  verdict-class distribution table
- **New ledger verdict classes (phase vocabulary):** `satisfied`, `present, unproven`,
  `genuinely outstanding`, `deferred with reason`, `superseded by shipped code` (D-20)
- **New ADR headings (document vocabulary):** `## Code Conformance`, `## Downstream Consumers`
- **New field values:** `conforms`, `must change` (the `Code conformance` field, D-03)
- **New naming scheme:** flat zero-padded monotonic ADR counter `NNNN-kebab-slug.md`
- **New governance vocabulary (plans 01-09/01-10):** the workspace coverage *floor*, the coverage
  *target* (80%), and the named *ratchet trigger*
- **Rust identifiers recorded but NOT implemented in this phase (Phase 2 GAP-07 implements them):**
  `BattalionCheckpointConfig`; the new temperature-range field on `ProviderCapabilities`

<tasks>

<task type="auto">
  <name>Task 1: Add the missing REQ-battalion-result-v1 row to the Milestone 1 ledger's Epic 4 table</name>
  <read_first>
    - `.planning/ledgers/milestone-01.md` lines 21-30 — the `## Verdict legend` table, so the new row uses one of exactly five permitted classes and no sixth is invented
    - `.planning/ledgers/milestone-01.md` lines 268-300 — the whole `### Epic 4 — Battalion Orchestration` section: its preamble (which already names ADR-0001/0002/0003 as the ADRs Epic 4 rows link to rather than re-deciding), its three-column `ID | Verdict | Evidence` table, and the exact prose style of its neighbouring rows
    - `.planning/ledgers/milestone-01.md` line 311 — Epic 5's `REQ-battalion-result-v2` row, the closest analog: same subject, same ADR, same verdict class; the new row must read as its sibling, not as a different document
    - `.planning/ledgers/milestone-01.md` lines 500-513 — the verdict-class distribution table and its total label, both of which this task must move
    - `.planning/decisions/0002-battalion-result.md` `## Decision` (the Epic 4 field-set bullet) and `## Considered Options` (the `REQ-battalion-result-v1` bullet) — the ONLY source for this row's evidence
    - `.planning/REQUIREMENTS.md` around line 2578 — the original `| REQ-battalion-result-v1 | Variant (group 4) |` entry and its position relative to its neighbours, which pins where the new ledger row goes
  </read_first>
  <files>.planning/ledgers/milestone-01.md</files>
  <action>
Add one row, then correct the counts that row invalidates. Use scoped `Edit` replacements — this
ledger is ~518 lines and is the deliverable of plans 01-05, 01-06 and 01-07.

**Halt condition, checked first.** Confirm `.planning/decisions/0002-battalion-result.md`'s
`## Considered Options` section contains a bullet naming `REQ-battalion-result-v1`. If it does not,
HALT and report — the whole justification for this row being a small repair rather than new analysis
is that the answer is already recorded in ADR-0002. Do not author evidence the ADR does not contain.

**The row.** Insert it into the `### Epic 4 — Battalion Orchestration` table at a pinned position:
immediately after the `REQ-battalion-config-v2` row and immediately before the
`REQ-battalion-error-strategy` row. That mirrors this identifier's relative position in
REQUIREMENTS.md's own Milestone 1 body, where it sits between the `BattalionConfig` entry and the
error-strategy entry. Use the table's existing three columns, `ID | Verdict | Evidence`.

- **ID:** `REQ-battalion-result-v1`
- **Verdict:** `superseded by shipped code` — the same class Epic 5's `REQ-battalion-result-v2` row
  already carries, and the class D-20 defines for a requirement shipped code answers differently
  than the ingested document specified. Do not invent a class outside the five in the legend.
- **Evidence:** transcribe ADR-0002's own finding rather than re-deriving it. Link to
  `[ADR-0002](../decisions/0002-battalion-result.md)` in the row, exactly as the neighbouring rows
  link to their ADRs. State that the shipped `BattalionResult` at
  `crates/paladin-core/src/platform/container/battalion/mod.rs:549` is a merged superset in which
  every field this Epic 4 FR-4.2 position specified — `battalion_id`, `battalion_name`, timestamps,
  `final_output`, `paladin_results`, `status`, and per-Paladin and overall timing — is present, so
  this position contributed no field the shipped struct dropped and there is **no substitution to
  record**; ADR-0002's phrasing for that is "wholly subsumed". Note that this row is not re-decided
  per-epic, matching the Epic 4 preamble's existing convention. Re-verify the `:549` line citation
  against the live tree before writing it, per this phase's citation discipline, and adjust the line
  number if it has drifted.

Keep `REQ-battalion-result-v1` and `REQ-battalion-result-v2` as two separate rows in two separate
epic tables. One ADR resolves both, but they are two distinct ingested positions from two distinct
epics and the ledger's primary key is the requirement ID (D-18). Do not merge them, and do not move
the Epic 5 row.

**The counts.** Adding one `superseded by shipped code` row invalidates the verdict-class
distribution table under `## Outstanding item reconciliation (RECON-01)`. As recorded on 2026-07-31
that table reads: `satisfied` 100, `present, unproven` 23, `genuinely outstanding` 11,
`superseded by shipped code` 19, `deferred with reason` 1, and a bold total row labelled with
112 `REQ-*` rows plus 39 nested items plus 3 Divergences rows, summing to 154. After this task it
must read: `superseded by shipped code` **20**, the label's `REQ-*` row count **113**, and the total
**155**, with the other four buckets unchanged. Re-count against the actual file before editing — if
the current figures differ from those recorded here, move each by exactly one from whatever it
actually is rather than writing these numbers blindly. Then confirm the five bucket counts sum to
the stated total; a table that no longer adds up is worse than one that is merely out of date.

Do not touch the `## Outstanding item reconciliation` section's **39 nested outstanding items**
figure or its per-file breakdown table. This row is a `REQ-*` requirement row, not a nested task
item, so the 39-item reconciliation against `intel/task-completion-state.md` is unaffected and must
stay exactly as it is.
  </action>
  <acceptance_criteria>
    - `grep -c "REQ-battalion-result-v1" .planning/ledgers/milestone-01.md` returns at least 1
    - The new row is inside the `### Epic 4 — Battalion Orchestration` section, between the `REQ-battalion-config-v2` and `REQ-battalion-error-strategy` rows
    - The row's Verdict cell is exactly one of the five strings in the `## Verdict legend` table
    - The row links to `../decisions/0002-battalion-result.md` and cites `battalion/mod.rs` with a line number that resolves in the live tree
    - `grep -c "REQ-battalion-result-v2" .planning/ledgers/milestone-01.md` is unchanged from before this task, and Epic 5's row for it is byte-identical
    - The verdict-distribution table's `superseded by shipped code` count and the total are each exactly one greater than before this task; the other four bucket counts are unchanged
    - The five bucket counts in the distribution table sum to the stated total
    - The `## Outstanding item reconciliation` section still states 39 nested outstanding items and its per-file breakdown table is byte-identical
    - `git diff --numstat .planning/ledgers/milestone-01.md` shows fewer than 15 changed lines — evidence of a scoped edit, not a rewrite
  </acceptance_criteria>
  <verify>
    <automated>L=.planning/ledgers/milestone-01.md; [ "$(grep -c 'REQ-battalion-result-v1' "$L")" -ge 1 ] && E4=$(awk '/^### Epic 4 — Battalion Orchestration/,/^### Epic 5 —/' "$L") && printf '%s' "$E4" | grep -q 'REQ-battalion-result-v1' && printf '%s' "$E4" | grep -q '0002-battalion-result.md' && grep -q '39 nested outstanding-item bullets' "$L" && [ "$(git diff --numstat -- "$L" | awk '{print $1+$2}')" -lt 15 ] && echo LEDGER_ROW_OK</automated>
  </verify>
  <done>The Milestone 1 ledger carries a row for every `REQ-*` identifier REQUIREMENTS.md's Milestone 1 body carried, the new row reads as a sibling of its Epic 5 counterpart rather than as a patch, and the ledger's own arithmetic still adds up.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the subset check and reduce REQUIREMENTS.md's Milestone 1 ledger body to a pointer</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the whole `## Milestone 1 as-shipped ledger` section, delimited by its own heading and the `## Milestone 2-3 as-shipped ledger` heading that follows it (as of 2026-07-31 these sit near lines 2520 and 2701, but re-derive the range from the headings rather than trusting those numbers): the status-key legend and the per-epic tables it holds
    - `.planning/REQUIREMENTS.md` lines 28-40 — the summary table whose Milestone 1 row must be left exactly as it is
    - `.planning/ledgers/milestone-01.md` `## Outstanding item reconciliation` — the row and verdict counts that evidence completeness, as Task 1 left them
    - `.planning/phases/01-ground-truth-decision-records/01-08-PLAN.md` Task 2 — the reduction specification this task completes; its pointer-paragraph content requirements and its prohibitions still govern
    - `.planning/phases/01-ground-truth-decision-records/01-08-SUMMARY.md` — what the previous run's check found and why it stopped, so this run's SUMMARY can state the before/after clearly
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
**Re-run the safety check inside this task, immediately before the reduction.** Do not rely on
Task 1's result, on plan 01-08's earlier run, or on any figure quoted in a SUMMARY — plan 01-09 runs
in the same wave, and the check must reflect the destination as it is at the moment of reduction.

Extract every `REQ-*` identifier from REQUIREMENTS.md's `## Milestone 1 as-shipped ledger` body (the
range between that heading and the `## Milestone 2-3 as-shipped ledger` heading), extract every
`REQ-*` identifier from `.planning/ledgers/milestone-01.md`, and confirm the first set is a subset of
the second. Treat the comparison as set membership: it is order-independent, an identifier found in a
different epic table than expected still counts as present, and an identifier appearing in two
destination tables counts once without those rows being deduplicated.

Guard against a vacuous pass. If either extraction yields an empty set, or if the source set is
implausibly small against the ~112 identifiers plan 01-08 counted, treat that as an extraction
failure — a broken pattern that matches nothing would make the subset test trivially true and destroy
the record it claims to relocate. HALT and report in that case. Likewise HALT if any identifier is
missing from the destination, exactly as plan 01-08 did. Record both set sizes and the check's verdict
in the SUMMARY either way.

**Then reduce, with a scoped `Edit`.** Replace the body of `## Milestone 1 as-shipped ledger` — its
status-key legend and its per-epic sub-sections and requirement-ID tables — with a short pointer
paragraph stating: that the per-requirement verdicts now live in `.planning/ledgers/milestone-01.md`;
why they moved (D-17 — REQUIREMENTS.md is already ~4,000 lines and holds five as-shipped ledger
sections, and five sets of `file:line`-cited verdicts inline would make it unreadable); the number of
requirement rows and the number of nested outstanding task items the destination carries, taken from
the destination's own reconciliation section as Task 1 left it; and that Phases 5, 7, 10 and 13 add
sibling files in the same directory. Keep the `## Milestone 1 as-shipped ledger` heading itself so
every existing cross-reference to that section still resolves.

Do not touch the summary-table row near line 31 — `01-PATTERNS.md` records it as the shape the pointer
imitates, and it is already no longer than the pointer treatment would make it. Do not touch the
Milestone 2-3, 4-6, 7-8 or 9-12 ledger sections; their relocation belongs to Phases 5, 7, 10 and 13.

Use scoped `Edit` replacements throughout. `.planning/REQUIREMENTS.md` is ~4,000 lines; a whole-file
write would destroy the forward requirements, the thirty variant groups and four other ledgers that
nothing else records.
  </action>
  <acceptance_criteria>
    - Every `REQ-*` identifier that appeared in REQUIREMENTS.md's Milestone 1 ledger body before this task also appears in `.planning/ledgers/milestone-01.md`, and the SUMMARY records both set sizes
    - Both extracted sets are non-empty and the source set size is within one of the count plan 01-08 recorded
    - The `## Milestone 1 as-shipped ledger` heading still exists in `.planning/REQUIREMENTS.md`
    - The section between that heading and `## Milestone 2-3 as-shipped ledger` contains a link to `.planning/ledgers/milestone-01.md` and fewer than 25 lines
    - That section contains no per-epic sub-heading and no requirement-ID table row
    - The summary-table row near line 31 naming the Milestone 1 as-shipped ledger is byte-identical
    - The Milestone 2-3, 4-6, 7-8 and 9-12 as-shipped ledger headings all still exist
    - `.planning/REQUIREMENTS.md`'s thirty variant-group sections are still present
  </acceptance_criteria>
  <verify>
    <automated>R=.planning/REQUIREMENTS.md; grep -q '## Milestone 1 as-shipped ledger' "$R" && S=$(awk '/^## Milestone 1 as-shipped ledger/,/^## Milestone 2-3 as-shipped ledger/' "$R") && printf '%s' "$S" | grep -q 'ledgers/milestone-01.md' && [ "$(printf '%s\n' "$S" | grep -c '^| REQ-')" -eq 0 ] && [ "$(printf '%s\n' "$S" | grep -c '^### ')" -eq 0 ] && [ "$(printf '%s\n' "$S" | wc -l)" -lt 30 ] && for m in 'Milestone 2-3 as-shipped ledger' 'Milestone 4-6 as-shipped ledger' 'Milestone 7-8 as-shipped ledger' 'Milestone 9-12 as-shipped ledger'; do grep -q "$m" "$R" || { echo "MISSING: $m"; exit 1; }; done && echo POINTER_OK</automated>
  </verify>
  <done>REQUIREMENTS.md's Milestone 1 ledger body is a short pointer at a destination freshly proven to carry every requirement identifier it held, the heading and the summary-table row still resolve, and the four sibling ledger sections are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Mark Broken Windows item 1 fixed, now that the repair exists</name>
  <read_first>
    - `.planning/WINDOWS.md` — the full register: its frontmatter counters (`open_count`, `fixed_count`, `total_count`, `last_updated`), the markdown row for item 1, and the trailing JSON block that mirrors it; both representations must end consistent
    - `.planning/phases/01-ground-truth-decision-records/01-08-SUMMARY.md` § Issues Encountered — the finding this entry records, so the closure is verifiably about the same defect
  </read_first>
  <files>.planning/WINDOWS.md</files>
  <action>
Close open item 1 — the `unmet-truth` entry recording that `REQ-battalion-result-v1` has no row
anywhere in `.planning/ledgers/milestone-01.md`.

Run the register's own documented closure command rather than hand-editing: `gsd-tools windows fixed 1`
(resolve the launcher the same way the other plans in this phase do, via
`node .claude/gsd-core/bin/gsd-tools.cjs windows fixed 1` if `gsd-tools` is not on PATH). That keeps
the markdown table, the frontmatter counters and the trailing JSON block consistent with each other,
which a hand-edit would not.

**Order matters and is not negotiable.** Run this only after Task 1's row exists and Task 2's subset
check has passed. The entry is closed because the defect was repaired, not because the phase wants a
clean register. Do not waive it, do not delete it, and do not reword its description to make it read
as resolved — the plan's prohibition forbids all three.

Afterwards, confirm `open_count` reads 0 and `fixed_count` reads 1 in the frontmatter, that item 1's
`status` is `fixed` in both the markdown row and the JSON block, and that `resolved_at` carries a
timestamp. If the command leaves the two representations disagreeing, report it rather than patching
one side by hand.
  </action>
  <acceptance_criteria>
    - `.planning/WINDOWS.md` frontmatter has `open_count: 0` and `fixed_count: 1`
    - Item 1's markdown row shows status `fixed` and a non-empty `resolved_at`
    - The trailing JSON block's item 1 has `"status": "fixed"` and a non-null `"resolved_at"`
    - Item 1's `description` text is unchanged from before this task — the defect record is preserved, not rewritten
    - The `reason` field is empty: this is a repair, not a waiver
    - `grep -c "REQ-battalion-result-v1" .planning/ledgers/milestone-01.md` returns at least 1, confirming closure followed the repair
  </acceptance_criteria>
  <verify>
    <automated>W=.planning/WINDOWS.md; grep -q '^open_count: 0' "$W" && grep -q '^fixed_count: 1' "$W" && grep -q '"status": "fixed"' "$W" && ! grep -q '"resolved_at": null' "$W" && [ "$(grep -c 'REQ-battalion-result-v1' .planning/ledgers/milestone-01.md)" -ge 1 ] && echo WINDOWS_OK</automated>
  </verify>
  <done>The Broken Windows register reports zero open items because the missing ledger row now exists, with the original defect description preserved and no waiver recorded.</done>
</task>

</tasks>

## Edge-probe resolution (this plan's share)

The consolidated 31-item ledger lives in `01-09-PLAN.md`. This plan resolves nine of the items —
eight `covered` and one `backstop`:

| Requirement | Category | Disposition | Predicate |
|---|---|---|---|
| RECON-01 | boundary | covered | Zero missing identifiers passes; one missing halts the reduction |
| RECON-01 | empty | covered | An empty extraction is an extraction failure, never a vacuous pass |
| RECON-01 | ordering | covered | The subset check is set-based and order-independent |
| RECON-01 | adjacency | covered | The same identifier in two epic tables counts once; the rows stay two rows |
| RECON-01 | precision | covered | Exactly one bucket and exactly one total move by exactly one; buckets still sum |
| RECON-01 | concurrency | **backstop** | The check is re-run inside the reducing task, never read from an earlier run |
| RECON-03 | adjacency | covered | `-v1` and `-v2` stay separate rows in separate epic tables |
| RECON-03 | empty | covered | No ADR-0002 bullet for `-v1` means halt, not invented evidence |
| RECON-03 | ordering | covered | The row's position in the Epic 4 table is pinned between two named neighbours |

All nine are authored into `must_haves.truths`; the `backstop` item carries the flat-scalar
`verification: backstop` marker so the verifier branches deterministically on it rather than on prose.

## Prohibition-probe resolution (this plan's share)

One bespoke prohibition from the spec-less §B recall lands here, authored **descriptor-less** in
`must_haves.prohibitions`: the ban on closing the WINDOWS.md entry by waiver, deletion or rewording
instead of by repair. It is `judgment`-tier — "was the register cleaned rather than fixed" is a
transparency judgment, not a mechanical assertion — and disposes flagged-unverified. No canon-referral
breadcrumb applies. The routine-engineering candidates Stage 2 dropped here (whole-file-write hygiene,
pointer-at-incomplete-destination) are already standing prohibitions on executed plan 01-08 and remain
in force via this plan's task actions.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a source-of-truth document → a pointer at another file | reducing REQUIREMENTS.md's ledger body destroys the original unless the destination is verified complete at the moment of reduction |
| a defect register → phase closure | an open Broken Windows item blocks `/gsd-ship`, creating pressure to close the entry rather than fix the defect |
| ADR-0002's recorded analysis → a new ledger row | evidence transcribed loosely becomes evidence authored fresh, which the phase's citation discipline forbids |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-50 | Tampering | pointer written before the destination is complete | high | mitigate | Task 2 re-runs the full identifier subset check inside the reducing task, immediately before the reduction, and HALTs on any miss; the backstop truth records that no earlier result may be reused. |
| T-01-51 | Tampering | a vacuous subset pass from a broken extraction pattern | high | mitigate | Task 2 requires both extracted sets to be non-empty and the source set to be within one of the ~112 count plan 01-08 recorded; an implausibly small set is an extraction failure that HALTs. |
| T-01-52 | Repudiation | closing the Broken Windows entry without the repair | high | mitigate | The plan's prohibition forbids waiver, deletion and rewording; Task 3's verify asserts the ledger row exists as a precondition of the closure, and asserts the `reason` field is empty so a waiver cannot masquerade as a fix. |
| T-01-53 | Denial of Service | whole-file rewrite of REQUIREMENTS.md or the ledger | high | mitigate | Both task actions mandate scoped `Edit` replacements and name what a whole-file write would destroy; acceptance criteria bound the ledger diff to under 15 lines and assert survivors (four sibling ledger sections, thirty variant groups, the summary-table row, the 39-item reconciliation). |
| T-01-54 | Tampering | ledger arithmetic silently inconsistent after the new row | medium | mitigate | Task 1 requires the five bucket counts to sum to the stated total after the edit, and requires the 39 nested-item reconciliation to be left untouched since a `REQ-*` row is not a nested task item. |
| T-01-01 | Information Disclosure | editing planning documents | low | accept | No secret material is introduced; the edits are relocations, citations and links within `.planning/`. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | **This plan installs nothing.** `01-RESEARCH.md` § Package Legitimacy Audit records no `[ASSUMED]`, `[SUS]` or `[SLOP]` package, so no blocking legitimacy checkpoint is required. |
</threat_model>

<verification>
- `.planning/ledgers/milestone-01.md` carries a `REQ-battalion-result-v1` row in Epic 4, citing ADR-0002 and the shipped struct, with a legend-valid verdict
- The ledger's verdict-distribution table moved by exactly one and still sums correctly; the 39-item reconciliation is untouched
- The identifier subset check was re-run inside the reducing task, passed with zero misses, and both set sizes are recorded in the SUMMARY
- REQUIREMENTS.md's Milestone 1 ledger body is a pointer under 30 lines with no per-epic sub-headings and no requirement-ID rows; the heading, the summary-table row and the four sibling ledger sections survive
- `.planning/WINDOWS.md` reports `open_count: 0` with item 1 fixed, its description preserved and no waiver reason recorded
</verification>

<success_criteria>
- The Milestone 1 ledger is a true superset of the requirement identifiers REQUIREMENTS.md's Milestone 1 body carried
- No Milestone-1 requirement identifier was lost in the relocation, and the proof of that is recorded rather than asserted
- The Broken Windows register is clean because a defect was repaired, not because an entry was closed
</success_criteria>

<output>
Create `.planning/phases/01-ground-truth-decision-records/01-11-SUMMARY.md` when done
</output>
