---
phase: 03-verification-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-verification-depth/03-coverage-measurement.md
autonomous: true
requirements: [QUAL-01]

must_haves:
  truths:
    - "`.planning/phases/03-verification-depth/03-coverage-measurement.md` exists and records an entry measurement produced by ADR-0006's verbatim command at the current HEAD SHA, with `rustc -vV`, `cargo --version`, `git rev-parse HEAD` and `date -u` output pasted immediately before the run (D-16)."
    - "The record contains a line of the exact form `Measured workspace line coverage: NN.NN%` whose value is transcribed from the pasted `llvm-cov report` TOTAL row, and that value is at or above 84.00%."
    - "The recorded workspace line-coverage figure is compared at-or-above against ADR-0006's 84% floor: a figure of exactly 84.00% passes and 83.99% fails; the record states explicitly which side the measured figure falls on and by how much."
    - "The floor arithmetic uses truncation toward zero to a whole percent — explicitly neither round-half-up nor round-half-even — shown on the actual measured figure, and the TOTAL-row percentage is transcribed byte-identically from `llvm-cov report` stdout rather than re-derived or re-typed."
    - "If the `--object` discovery command returns zero test-binary paths, or `llvm-profdata merge` produces no `.profdata`, the measurement is recorded as FAILED and no figure is reported — an empty object set never yields a passing coverage figure."
    - "The entry and exit measurements are recorded as two separate dated sections in `03-coverage-measurement.md`; neither overwrites nor merges into the other, and each carries its own commit SHA and UTC timestamp."
    - "The `--object` discovery output is deterministically ordered (`sort -u`) and the discovered object count is recorded verbatim as produced; the count is never adjusted to match the number of test source files (Research Pitfall 3)."
    - "The re-derived first-party zero-coverage set is recorded from this run's own per-file rows, and the QUAL-02 staleness comparison table pairs each of QUAL-02's eleven named files with its measured figure from this run."
    - "The ratchet arithmetic (`measured − 84.00`) is recorded together with the decision that applying the ratchet is a milestone-close action and not Phase 3's — ADR-0006 is not amended by this phase."
  artifacts:
    - path: ".planning/phases/03-verification-depth/03-coverage-measurement.md"
      provides: "Phase 3 coverage measurement record matching the 01-coverage-measurement.md provenance standard"
      contains: "Measured workspace line coverage:"
  key_links:
    - from: ".planning/phases/03-verification-depth/03-coverage-measurement.md"
      to: ".planning/decisions/0006-coverage-gate.md"
      via: "the command, ignore regex, doctest exclusion and default-feature scope are reproduced verbatim from the ADR"
      pattern: "instrument-coverage"
    - from: ".planning/phases/03-verification-depth/03-coverage-measurement.md"
      to: ".planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md"
      via: "section skeleton and provenance-probe ordering copied from the Phase 1 record"
      pattern: "rustc -vV"
  prohibitions:
    - requirement_id: QUAL-01
      category: transparency
      statement: "MUST NOT record any figure in this phase that is not traceable to command stdout pasted in this run on a stated commit — no figure carried over from Phase 1, no figure recalled from memory, no figure re-typed from an ingested document."
    - requirement_id: QUAL-01
      category: transparency
      statement: "MUST NOT alter ADR-0006's command, `--ignore-filename-regex`, feature set or doctest decision in order to make the measured figure clear the floor — the denominator is never tuned toward the answer."
---

<objective>
Reproduce ADR-0006's coverage pipeline end-to-end at current HEAD and record the entry
measurement to the Phase 1 provenance standard, then re-derive this phase's own zero-coverage
target set from that measurement's per-file rows.

Purpose: QUAL-01 is measure-and-hold (D-01). HEAD is 98 commits past the commit ADR-0006's
84.79% was measured on, including all of Phase 2's new tests. This plan proves the 84% floor
still holds and produces the evidence base every other Phase 3 plan and amendment cites. It is
the phase's tracer: it exercises the entire measurement pipeline — instrumented workspace test
run, profdata merge, object discovery, `llvm-cov report`, provenance capture, per-file
re-derivation — end to end before any other plan spends effort on a target set that might be
wrong.

Output: `.planning/phases/03-verification-depth/03-coverage-measurement.md` with an entry
measurement section, a re-derived first-party zero-coverage set, the QUAL-02 staleness
comparison table, the scope exclusions (D-06 `target/`, D-07 `minio.rs`), and the ratchet
arithmetic.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/decisions/0006-coverage-gate.md
@.planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md
@.planning/phases/03-verification-depth/03-CONTEXT.md
@.planning/phases/03-verification-depth/03-RESEARCH.md
@.planning/phases/03-verification-depth/03-PATTERNS.md
</context>

## Artifacts this phase produces

Every symbol, file and doc section Phase 3 creates. Newly-created names are not drift — they do
not exist in the tree before this phase.

| Artifact | Kind | Owning plan | Path |
|---|---|---|---|
| `03-coverage-measurement.md` | new file | 03-01 / 03-07 | `.planning/phases/03-verification-depth/03-coverage-measurement.md` |
| `FaultyPaladinPort` | new struct | 03-02 | `tests/helpers/mock_paladin_port.rs` |
| `FaultyPaladinPort::new`, `::fail_always`, `::fail_paladin`, `::fail_until_attempt`, `::with_delay_ms`, `::call_count`, `::execution_log` | new methods | 03-02 | `tests/helpers/mock_paladin_port.rs` |
| `commander_error_paths_test` | new test module | 03-02 | `tests/integration/commander_error_paths_test.rs` |
| `test_fail_fast_stops_on_first_error`, `test_continue_on_error_collects_all_errors`, `test_retry_then_continue_retries_failed_paladins`, `test_partial_results_returned_with_errors` | relocated test fns (same names, real bodies) | 03-02 | `tests/integration/commander_error_paths_test.rs` |
| `MCPClient::connect_streamable_http_with_timeout` | new pub async fn | 03-03 | `src/infrastructure/adapters/arsenal/mcp_protocol.rs` |
| `spawn_malformed_fixture_server`, `spawn_silent_fixture_server` | new test fns | 03-03 | `tests/integration/mcp_streamable_http_test.rs` |
| `streamable_http_rejects_expired_bearer_token`, `streamable_http_invoke_unknown_tool_maps_to_error`, `streamable_http_invoke_with_missing_message_argument_maps_to_error`, `streamable_http_malformed_tools_list_response_returns_error`, `streamable_http_handshake_timeout_returns_timeout_error` | new test fns | 03-03 | `tests/integration/mcp_streamable_http_test.rs` |
| `muster_baseline` | new example binary | 03-04 | `examples/muster_baseline.rs` |
| `## Run — <date>`, `### Memory-per-Paladin`, `### Startup Time`, `### P50 / P95 / P99 Derivation` | new doc sections | 03-04 | `docs/src/appendix/performance-baseline.md` |
| `queue_key`, `priority_queue_key`, `queue_meta_key`, `processing_key`, `completed_key`, `failed_key`, `serialize_item`, `deserialize_item` | refactored off `&self` (private) | 03-05 | `crates/paladin-storage/src/redis.rs` |
| `#[cfg(test)] mod tests` | new test modules | 03-05 / 03-06 | `crates/paladin-storage/src/redis.rs`, `crates/paladin-ports/src/output/file_storage_port.rs`, `crates/paladin-ports/src/output/arsenal_port.rs`, `crates/paladin-llm/src/error.rs` |

<tasks>

<task type="tracer">
  <name>Task 1: Reproduce the ADR-0006 coverage pipeline end-to-end and record the entry measurement</name>
  <precondition>`llvm-profdata` and `llvm-cov` are present under `$(rustc --print sysroot)/lib/rustlib/x86_64-unknown-linux-gnu/bin/` (the rustup `llvm-tools` component); the cargo subcommand `cargo-llvm-cov` is NOT required and is not installable here.</precondition>
  <reversibility rating="costly">The figure recorded here is cited by VERIFY-05 (Phase 5) and PIPE-02 (Phase 15); a figure produced under a quietly-different command hands both a number they cannot reconcile (CONTEXT.md D-02).</reversibility>
  <files>.planning/phases/03-verification-depth/03-coverage-measurement.md</files>
  <read_first>
    - `.planning/decisions/0006-coverage-gate.md` — the binding command, the `--ignore-filename-regex` in words, the doctest exclusion, the default-feature scope, the truncation rule and the at-or-above comparison. Reproduce all of them; change none of them (D-02).
    - `.planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md` — the section skeleton, the environment-probe ordering, the raw-stdout convention and the human-confirmation section shape this record must match.
    - `.planning/phases/03-verification-depth/03-RESEARCH.md` §Architecture Patterns (coverage pipeline diagram) and §Common Pitfalls, Pitfall 3 (`--object` discovery is per compiled test binary, not per source file).
    - `.planning/phases/03-verification-depth/03-PATTERNS.md` §"Coverage-measurement record for Phase 3" — the exact section skeleton and environment-probe block to reproduce.
  </read_first>
  <action>
    Create `.planning/phases/03-verification-depth/03-coverage-measurement.md` titled
    `# Coverage Measurement — Raw Evidence Record (Phase 3)` with a top-level section
    `## Entry measurement — workspace-wide line coverage`.

    Capture the provenance probes FIRST, in this order, pasting raw stdout for each into a
    `### Environment probes (verbatim)` subsection: `rustc -vV`, `cargo --version`,
    `git rev-parse HEAD`, `git status --porcelain`, `date -u`, `command -v docker` (record the
    exit status and that it is absent), `command -v cargo-llvm-cov` (record that it is absent),
    and `ls "$(rustc --print sysroot)/lib/rustlib/x86_64-unknown-linux-gnu/bin/"`.

    Then run the pipeline, pasting every command and its stdout verbatim under
    `### Pipeline commands (verbatim)`:

    Step 1 — clear and recreate `target/coverage/`, then run the instrumented suite with
    `RUSTFLAGS="-C instrument-coverage"` and
    `LLVM_PROFILE_FILE="$PWD/target/coverage/paladin-%p-%m.profraw"` and
    `cargo test --workspace --offline`. Record the final test-summary lines. Do not add
    `--features`, do not add `--doctests`, do not drop `--workspace`, do not drop `--offline`.

    Step 2 — merge with `llvm-profdata merge -sparse target/coverage/*.profraw -o
    target/coverage/paladin.profdata`, invoking the binary by its absolute path under the
    sysroot `bin/` directory probed above. Record the resulting `.profdata` size.

    Step 3 — discover the test-binary objects with
    `cargo test --workspace --offline --no-run --message-format=json | jq -r 'select(.profile.test==true) | .filenames[]' | sort -u`.
    Export the SAME `RUSTFLAGS="-C instrument-coverage"` for this discovery invocation so cargo
    reuses the instrumented artifacts instead of rebuilding under different flags and emitting a
    different set of object paths. Record the object count exactly as produced (Phase 1 recorded
    31); do not adjust it toward the number of test source files.

    Step 4 — run `llvm-cov report --instr-profile=target/coverage/paladin.profdata
    --ignore-filename-regex='(^|/)(examples|benches)/|crates/doc-examples/|registry/src/|rustlib/src/'`
    with one `--object=<path>` argument per discovered object. Paste the full TOTAL row and the
    per-file rows verbatim inside a fenced block.

    Under `### Measurement of record`, write, on its own line and in exactly this form, the
    sentence `Measured workspace line coverage: NN.NN%` using the line-coverage percentage
    transcribed character-for-character from the pasted TOTAL row. Immediately below it, show the
    arithmetic a reader can re-derive: counted lines, missed lines, and
    `(counted - missed) / counted` resolving to the same two-decimal figure. Then state the
    at-or-above comparison against ADR-0006's 84% floor explicitly — name the measured figure, the
    floor, the margin in percentage points, and the verdict PASS or FAIL — and show the truncation
    arithmetic (truncation toward zero to a whole percent, explicitly neither round-half-up nor
    round-half-even) applied to this run's own figure.

    If the discovery command returns zero object paths, or `llvm-profdata merge` produces no
    `.profdata` file, record the run as FAILED under `### Measurement of record`, state which step
    produced nothing, and report no coverage figure at all.

    Record the sandbox constraints plainly in a `### Recorded scope and constraints` subsection:
    no Docker, crates.io HTTP 403, `cargo-llvm-cov` not installable, workspace default-feature
    scope only, doctests excluded — and that these match ADR-0006's recorded narrowing rather than
    a new one.
  </action>
  <verify>
    <automated>F=.planning/phases/03-verification-depth/03-coverage-measurement.md; grep -q "$(git rev-parse HEAD)" "$F" && grep -qE '^TOTAL[[:space:]]' "$F" && grep -qE 'rustc [0-9]+\.[0-9]+\.[0-9]+' "$F" && grep -qE 'cargo [0-9]+\.[0-9]+\.[0-9]+' "$F" && grep -q '## Entry measurement' "$F" && grep -q 'instrument-coverage' "$F" && grep -q 'ignore-filename-regex' "$F" && LC=$(grep -oE 'Measured workspace line coverage: [0-9]+\.[0-9]{2}%' "$F" | grep -oE '[0-9]+\.[0-9]{2}') && awk -v c="$LC" 'BEGIN{ exit !(c+0 >= 84.00) }'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/03-verification-depth/03-coverage-measurement.md` exists and contains the heading `## Entry measurement — workspace-wide line coverage`.
    - The file contains the output of `git rev-parse HEAD` and that SHA equals the SHA `git rev-parse HEAD` reports at verification time.
    - The file contains a line matching `^Measured workspace line coverage: [0-9]+\.[0-9]{2}%$` and the numeric value is ≥ 84.00.
    - The file contains a line beginning `TOTAL` transcribed from `llvm-cov report` stdout, and the counted/missed line figures on that row re-derive to the recorded percentage at two decimals.
    - The file states the object count produced by the `jq` discovery command as a bare integer, together with the discovery command verbatim.
    - The file contains the literal strings `instrument-coverage` and `ignore-filename-regex`, both inside pasted command text identical to ADR-0006's recorded command.
    - The file records `command -v docker` exit status 1 and `command -v cargo-llvm-cov` as absent.
    - `git status --porcelain` output is recorded, so a reader can tell whether the measurement ran against a clean tree.
  </acceptance_criteria>
  <done>The full ADR-0006 pipeline ran end to end at HEAD, its raw output is pasted into the record, and the recorded line-coverage figure is stated with an explicit PASS/FAIL against the 84% floor.</done>
</task>

<task type="auto">
  <name>Task 2: Re-derive the zero-coverage set, record the QUAL-02 staleness table, scope exclusions and ratchet arithmetic</name>
  <files>.planning/phases/03-verification-depth/03-coverage-measurement.md</files>
  <read_first>
    - The file produced by Task 1 — its per-file rows are the evidence base for everything here.
    - `.planning/phases/03-verification-depth/03-CONTEXT.md` §D-03 (the eleven-file staleness table), §D-04 (the five-file re-derived zero set), §D-06 (`target/` contamination), §D-07 (`minio.rs` out of denominator).
    - `.planning/REQUIREMENTS.md:262-267` — QUAL-02's eleven named offender files, verbatim, so the comparison table pairs each one.
    - `.planning/decisions/0006-coverage-gate.md` §"The ratchet trigger" — the exact trigger condition and the fact that the raise is applied by amending the ADR in place.
  </read_first>
  <action>
    Append three subsections to `03-coverage-measurement.md`, all sourced from Task 1's per-file
    rows and from no other document.

    `### Re-derived first-party zero-coverage set` — a table of every first-party source file whose
    line coverage this run reports as exactly 0.00%, with columns: path, counted lines, missed
    lines, line coverage. Order the rows by counted lines descending; break ties by lexicographic
    path order. Exclude `tests/**` entries and any path under `target/` from this set and list
    those exclusions separately in the next subsection with their reason. State the row count as a
    bare integer. Compare this set against CONTEXT.md D-04's expected five (`redis.rs`,
    `paladin-server.rs`, `file_storage_port.rs`, `error.rs`, `arsenal_port.rs`) and name any file
    that entered or left the set since the Phase 1 run, with no attempt to explain the delta away.

    `### QUAL-02 offender list — claimed versus measured` — a table with one row per file QUAL-02
    names at REQUIREMENTS.md:262-267, columns: file, figure QUAL-02 claims, figure this run
    measures, verdict (`contradicted` / `confirmed` / `not in denominator`). Keep both figures side
    by side on every row; never replace a claimed figure with a measured one. Mark each contradicted
    row as corrected rather than deleting it. Record the count of contradicted rows as a bare
    integer.

    `### Scope exclusions and denominator notes` — record, each with its magnitude: (a) the
    generated file `target/debug/build/utoipa-swagger-ui-*/out/embed.rs` sitting in the denominator
    at 0.00% because ADR-0006's ignore regex does not exclude `target/`, with its counted-line count
    against the total counted lines, stated as immaterial at two decimals, flagged for VERIFY-05 /
    PIPE-02 and explicitly NOT fixed here per D-02; (b) `crates/paladin-storage/src/minio.rs` having
    no denominator entry at all because the `s3` feature is not in the workspace default set, so it
    is out of ADR-0006's recorded scope rather than a 0% file, owner VERIFY-05 / PIPE-02; (c) that
    ROADMAP criterion 2 names the Redis and MinIO adapters together while this run shows they are in
    different positions.

    `### Ratchet readiness` — record `measured − 84.00` to two decimals, state ADR-0006's trigger
    condition (a raise fires when a milestone closes with measured coverage two or more whole
    percentage points above the standing floor), state whether that condition is met by this figure,
    and record this phase's decision: applying the ratchet is a milestone-close action, not Phase
    3's, because ADR-0006's own text scopes the raise to milestone close and amending the ADR
    mid-milestone would produce a floor that a later Phase 4 or Phase 5 measurement could contradict.
    State plainly that Phase 3 does not amend ADR-0006, and name the milestone-close audit as the
    owner of the raise if the condition is met.
  </action>
  <verify>
    <automated>F=.planning/phases/03-verification-depth/03-coverage-measurement.md; grep -q '### Re-derived first-party zero-coverage set' "$F" && grep -q '### QUAL-02 offender list' "$F" && grep -q '### Scope exclusions and denominator notes' "$F" && grep -q '### Ratchet readiness' "$F" && grep -q 'minio.rs' "$F" && grep -q 'utoipa-swagger-ui' "$F" && grep -qE 'arsenal_execution_service\.rs' "$F" && grep -qE 'deepseek_adapter\.rs' "$F"</automated>
  </verify>
  <acceptance_criteria>
    - The record contains all four headings: `### Re-derived first-party zero-coverage set`, `### QUAL-02 offender list — claimed versus measured`, `### Scope exclusions and denominator notes`, `### Ratchet readiness`.
    - The staleness table has exactly eleven data rows, one per file QUAL-02 names, each carrying both a claimed figure and a measured figure in separate columns.
    - Every row of the zero-coverage table carries a counted-line count and a missed-line count copied from Task 1's pasted per-file rows, and the rows are in descending counted-line order.
    - `crates/paladin-storage/src/minio.rs` appears only under scope exclusions with the verdict "no denominator entry", never in the zero-coverage table.
    - The `target/` contamination row names the generated `utoipa-swagger-ui` file, gives its counted-line count against the run's total counted lines, and names VERIFY-05 / PIPE-02 as owner.
    - `### Ratchet readiness` states the numeric delta from 84.00 to two decimals and contains the words "milestone-close" together with an explicit statement that ADR-0006 is not amended by this phase.
  </acceptance_criteria>
  <done>The phase's own target set is derived from its own measurement, QUAL-02's claimed figures are preserved next to the measured ones, both scope exclusions are recorded with magnitude and owner, and the ratchet decision is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| toolchain → recorded artifact | `llvm-cov`/`cargo` stdout crosses into a `.planning/` record that later phases cite as authoritative |
| ingested corpus → this phase | `unit-test-improvements/COVERAGE_ANALYSIS.md`-derived claims in QUAL-02 are untrusted input, superseded by a measurement of the shipped tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | `03-coverage-measurement.md` | medium | mitigate | Every figure is pasted stdout on a stated commit SHA; the TOTAL-row arithmetic is re-derivable by a reader; `git status --porcelain` is recorded so tree cleanliness is visible; the figure is confirmed by a human checkpoint in plan 03-08 |
| T-03-08 | Information Disclosure | environment probe block | low | accept | `rustc -vV`, `cargo --version`, sysroot `bin/` listing and `date -u` are required by D-16's provenance standard and carry no hostname, username, IP or path outside the toolchain and repository |
| T-03-SC | Tampering | crates.io / cargo registry | high | mitigate | This phase installs no packages. `03-RESEARCH.md` §Package Legitimacy Audit records five already-vendored dependencies, all OK, zero ASSUMED/SUS/SLOP. Every cargo command carries `--offline`, so no registry fetch can occur; crates.io returns HTTP 403 in this environment regardless |
</threat_model>

<verification>
- The pipeline ran with ADR-0006's command unchanged: `RUSTFLAGS`, `LLVM_PROFILE_FILE`, `cargo test --workspace --offline`, the same `--ignore-filename-regex`, no `--doctests`, no `--features`.
- The recorded figure is at or above 84.00% and the PASS/FAIL verdict is stated explicitly.
- The zero-coverage set and the staleness table derive from this run's per-file rows only.
- Both scope exclusions (`target/` contamination, `minio.rs` absence) are recorded with magnitude and a named owner.
- Every command in the record carries `--offline` where cargo is invoked.

## Edge coverage (spec-less probe fallback)

No SPEC.md exists for this phase; the fallback probe surfaced 26 edges across QUAL-01…QUAL-05.
This plan authors QUAL-01's **5** edges (`boundary`, `precision`, `empty`, `adjacency`, `ordering`)
into `must_haves.truths` above, all resolved `covered` — none backstopped, none dismissed, none
dropped. The remaining 21 are authored in 03-02 (3), 03-03 (2), 03-04 (3) and 03-07 (13).

## Prohibition probe (spec-less fallback)

Two bespoke transparency prohibitions authored above, descriptor-less. Canon-referral breadcrumb:
secret-in-log handling for the MCP bearer token is canon security — covered by
`/gsd-secure-phase` and by threat T-03-01 in plan 03-03's register; not minted as a prohibition here.
</verification>

<success_criteria>
- `03-coverage-measurement.md` exists with an entry-measurement section carrying full D-16 provenance.
- `Measured workspace line coverage: NN.NN%` is present and ≥ 84.00.
- The re-derived first-party zero-coverage set, the eleven-row QUAL-02 staleness table, the two scope exclusions and the ratchet arithmetic are all recorded.
- No ADR, requirement or roadmap file was edited by this plan.
</success_criteria>

<output>
Create `.planning/phases/03-verification-depth/03-01-SUMMARY.md` when done.
</output>
