---
phase: 03-verification-depth
plan: 07
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-05", "03-06"]
files_modified:
  - .planning/phases/03-verification-depth/03-coverage-measurement.md
  - .planning/phases/03-verification-depth/03-critical-path-exercisers.md
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03]

must_haves:
  truths:
    - "`03-coverage-measurement.md` carries an exit measurement section produced by the same verbatim ADR-0006 command at the post-test-writing HEAD, with its own provenance block, and its recorded line-coverage figure is at or above 84.00%."
    - "A first-party source file counts as closed only when its `llvm-cov report` line-coverage figure is strictly greater than 0.00%; a file at exactly 0.00% is open, and every file still open carries a named owner in the exit record."
    - "A first-party file with no denominator entry at all — zero counted lines, as with `crates/paladin-storage/src/minio.rs` under the non-default `s3` feature — is recorded as outside ADR-0006's measured scope with a named owner, never as a 0% file and never as a closed file."
    - "Every per-file coverage percentage in the exit record is transcribed byte-identically from `llvm-cov report` stdout at the tool's own two-decimal precision; no figure is rounded, re-typed or re-derived."
    - "For each of QUAL-02's eleven originally-named files the record keeps the claimed figure and the re-measured figure side by side; a claimed and a measured figure for the same file are never merged into one number, and each contradicted entry is marked corrected rather than deleted."
    - "The exit record's zero-coverage table is ordered by counted lines descending with ties broken by lexicographic path order."
    - "The `LLVM_PROFILE_FILE` pattern includes `%p-%m` so parallel test binaries never write to the same `.profraw`; if an instrumented run is interrupted, `target/coverage/` is cleared and the run restarted from scratch — a partial `.profraw` set is never merged into a recorded figure."
    - "QUAL-03's `≥ 70% integration coverage` percentage clause is recorded superseded by ADR-0006, and no second coverage percentage under a second scope is produced anywhere in this phase."
    - "If any of the three named critical paths has zero passing non-skipped integration exercisers, it is recorded `genuinely outstanding` with a named owner rather than being credited by an adjacent test."
    - "Where a single integration test exercises more than one of the three named critical paths, it is cited under each path it exercises and the shared citation is stated explicitly — the three paths are never collapsed into one row because their exercisers overlap."
    - "Exerciser identity is compared byte-for-byte on the Rust identifier and on the repository-root-relative `path:line` string (ASCII, `/` separators); a citation differing by case, path separator, or crate-relative versus repo-relative prefix is a different exerciser and is corrected, not accepted as equal."
    - "The three critical paths are recorded in the order QUAL-03 already names them — Paladin execution, Battalion orchestration, tool invocation — rather than reordered by evidence strength."
    - "No numeric coverage percentage is recorded for QUAL-03 in any artifact this phase writes; its substance is satisfied by named exercisers only."
    - "Every cited exerciser passes under the default parallel `cargo test --offline` harness with no `--test-threads=1`, and the command output proving it is pasted into the evidence record."
  artifacts:
    - path: ".planning/phases/03-verification-depth/03-coverage-measurement.md"
      provides: "Exit measurement section proving the floor still holds after this phase's test-writing, plus the post-phase zero-coverage set"
      contains: "## Exit measurement"
    - path: ".planning/phases/03-verification-depth/03-critical-path-exercisers.md"
      provides: "QUAL-03 evidence at the D-19 bar — file:line plus a named passing exerciser per critical path"
      contains: "Battalion orchestration"
  key_links:
    - from: ".planning/phases/03-verification-depth/03-coverage-measurement.md"
      to: ".planning/decisions/0006-coverage-gate.md"
      via: "the exit run reproduces the same command, regex, doctest decision and feature scope as the entry run"
      pattern: "instrument-coverage"
    - from: ".planning/phases/03-verification-depth/03-critical-path-exercisers.md"
      to: "tests/integration/"
      via: "each critical path cites a repo-relative path:line and a named test function verified to pass"
      pattern: "tests/integration/"
---

<objective>
Re-run ADR-0006's command at the phase's ending HEAD to prove the floor still holds after all of this
phase's test-writing, record the post-phase zero-coverage set, and establish QUAL-03's evidence at the
D-19 bar.

Purpose: two coverage runs is the honest shape for this phase. The entry run (plan 03-01) answered
QUAL-01's literal obligation — the floor holds 98 commits past the commit ADR-0006 was measured on.
The exit run answers QUAL-02 — the previously-0.00% files are no longer 0.00% — and re-confirms
QUAL-01 at the commit this phase actually ends on, so no reader inherits a figure that was already
stale by construction within its own phase. QUAL-03's percentage clause is a second number under a
second scope, which ADR-0006 abolished; its surviving substance is named, passing exercisers, and
this plan gathers that evidence so plan 03-08 can amend the requirement at source against something.

Output: an exit measurement section in `03-coverage-measurement.md`, and
`03-critical-path-exercisers.md`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/decisions/0006-coverage-gate.md
@.planning/phases/03-verification-depth/03-CONTEXT.md
@.planning/phases/03-verification-depth/03-RESEARCH.md
</context>

## Artifacts this phase produces

Every symbol, file and doc section Phase 3 creates. Newly-created names are not drift.

| Artifact | Kind | Owning plan | Path |
|---|---|---|---|
| `03-coverage-measurement.md` | new file | 03-01 / 03-07 | `.planning/phases/03-verification-depth/03-coverage-measurement.md` |
| `03-critical-path-exercisers.md` | new file | 03-07 | `.planning/phases/03-verification-depth/03-critical-path-exercisers.md` |
| `FaultyPaladinPort` + its builder/accessor methods | new struct | 03-02 | `tests/helpers/mock_paladin_port.rs` |
| `commander_error_paths_test` and its four relocated test fns | new test module | 03-02 | `tests/integration/commander_error_paths_test.rs` |
| `MCPClient::connect_streamable_http_with_timeout` | new pub async fn | 03-03 | `src/infrastructure/adapters/arsenal/mcp_protocol.rs` |
| `spawn_malformed_fixture_server`, `spawn_silent_fixture_server` and the five MCP failure-mode test fns | new test fns | 03-03 | `tests/integration/mcp_streamable_http_test.rs` |
| `muster_baseline` | new example binary | 03-04 | `examples/muster_baseline.rs` |
| `## Run — <date>`, `### Memory-per-Paladin`, `### Startup Time`, `### P50 / P95 / P99 Derivation` | new doc sections | 03-04 | `docs/src/appendix/performance-baseline.md` |
| `queue_key`, `priority_queue_key`, `queue_meta_key`, `processing_key`, `completed_key`, `failed_key`, `serialize_item`, `deserialize_item` | refactored off `&self` (private) | 03-05 | `crates/paladin-storage/src/redis.rs` |
| `#[cfg(test)] mod tests` | new test modules | 03-05 / 03-06 | `crates/paladin-storage/src/redis.rs`, `crates/paladin-ports/src/output/file_storage_port.rs`, `crates/paladin-ports/src/output/arsenal_port.rs`, `crates/paladin-llm/src/error.rs` |

<tasks>

<task type="auto">
  <name>Task 1: Re-run the pipeline at the phase's ending HEAD and record the exit measurement</name>
  <precondition>All Phase 3 test-writing commits from plans 03-02 through 03-06 are on the current HEAD and `git status --porcelain` is empty — the exit figure must describe a committed tree, not a working copy.</precondition>
  <reversibility rating="costly">This is the figure a future reader will treat as Phase 3's answer; VERIFY-05 (Phase 5) and PIPE-02 (Phase 15) both cite it, and a figure produced under a quietly-different command hands both a number they cannot reconcile (D-02).</reversibility>
  <files>.planning/phases/03-verification-depth/03-coverage-measurement.md</files>
  <read_first>
    - `.planning/phases/03-verification-depth/03-coverage-measurement.md` as produced by plan 03-01 — the entry measurement, its command transcripts, its object count, and its re-derived zero-coverage table. The exit run reproduces the same commands exactly; read them from this file rather than retyping them from the ADR.
    - `.planning/decisions/0006-coverage-gate.md` — to confirm the entry run's transcript still matches the ADR before reusing it.
    - `.planning/phases/03-verification-depth/03-RESEARCH.md` §Common Pitfalls, Pitfall 3 — the object count is per compiled test binary, not per source file, and should stay stable because this phase deliberately added no `[[test]]` target.
  </read_first>
  <action>
    Append `## Exit measurement — workspace-wide line coverage` to
    `.planning/phases/03-verification-depth/03-coverage-measurement.md`. Do not modify, overwrite or
    merge into the entry section; the two runs stand side by side, each with its own commit SHA and
    UTC timestamp.

    Capture the same provenance probes in the same order as the entry run — `rustc -vV`,
    `cargo --version`, `git rev-parse HEAD`, `git status --porcelain`, `date -u` — and paste raw
    stdout. `git status --porcelain` must be empty; if it is not, stop and record why rather than
    measuring a dirty tree.

    Clear `target/coverage/` completely before the run so no `.profraw` from the entry run or from an
    interrupted attempt can be merged into this figure, then re-run the identical four-step pipeline:
    the instrumented `cargo test --workspace --offline` with `RUSTFLAGS="-C instrument-coverage"` and
    the `%p-%m` `LLVM_PROFILE_FILE` pattern; `llvm-profdata merge -sparse`; the
    `--no-run --message-format=json` object discovery piped through `jq` and `sort -u`, run under the
    same `RUSTFLAGS` so cargo reuses the instrumented artifacts; and `llvm-cov report` with the same
    `--ignore-filename-regex` and one `--object` per discovered path. Change no flag, no env var, no
    regex and no feature set (D-02). If the instrumented run is interrupted at any point, clear
    `target/coverage/` and restart from step one — never merge a partial `.profraw` set.

    Record, on its own line and in exactly this form,
    `Measured workspace line coverage: NN.NN%`, transcribed character-for-character from the pasted
    TOTAL row, followed by the re-derivable arithmetic and an explicit at-or-above PASS/FAIL verdict
    against the 84% floor with the margin in percentage points.

    Add `### Post-phase zero-coverage set` — every first-party source file this run reports at exactly
    0.00%, ordered by counted lines descending with ties broken lexicographically by path, each with
    counted lines, missed lines and its figure at the tool's own two-decimal precision. State the row
    count as a bare integer.

    Add `### QUAL-02 closure ledger` — one row per file in the entry run's zero-coverage set, with
    columns: path, entry figure, exit figure, verdict. Use `closed` only where the exit figure is
    strictly greater than 0.00%. Use `deferred with reason` with a named owning phase and requirement
    for anything still at 0.00% — `src/bin/paladin-server.rs` is expected here with owner Phase 5 /
    VERIFY-05, per plan 03-06's recorded decision. Add rows for the two out-of-scope items and mark
    them distinctly: `crates/paladin-storage/src/minio.rs` has no denominator entry at all under the
    default feature set, so it is neither open nor closed but outside ADR-0006's recorded scope, owner
    VERIFY-05 / PIPE-02; the generated `target/` file is denominator contamination, not first-party
    source, owner VERIFY-05 / PIPE-02.

    Add `### Object count and delta` — the exit run's object count next to the entry run's, and a
    one-line statement of why they match or differ. This phase added no `[[test]]` target, so a
    changed count is a finding to record, not an error to correct.

    Add `### Ratchet readiness (exit)` — the exit figure's delta from 84.00 to two decimals, whether
    ADR-0006's two-point milestone-close trigger is met, and a restatement that applying the ratchet
    is a milestone-close action and that Phase 3 does not amend ADR-0006.
  </action>
  <verify>
    <automated>F=.planning/phases/03-verification-depth/03-coverage-measurement.md; [ -z "$(git status --porcelain -- ':!.planning')" ] && grep -q '## Exit measurement' "$F" && grep -q '## Entry measurement' "$F" && grep -q "$(git rev-parse HEAD)" "$F" && grep -q '### QUAL-02 closure ledger' "$F" && grep -q '### Post-phase zero-coverage set' "$F" && grep -q '### Object count and delta' "$F" && LC=$(grep -oE 'Measured workspace line coverage: [0-9]+\.[0-9]{2}%' "$F" | tail -1 | grep -oE '[0-9]+\.[0-9]{2}') && awk -v c="$LC" 'BEGIN{ exit !(c+0 >= 84.00) }' && [ "$(grep -c 'Measured workspace line coverage:' "$F")" -eq 2 ]</automated>
  </verify>
  <acceptance_criteria>
    - The record contains both `## Entry measurement` and `## Exit measurement` as separate sections, each with its own commit SHA and `date -u` stamp, and the entry section's figures are unchanged.
    - `grep -c 'Measured workspace line coverage:' <record>` returns exactly 2 — one per run.
    - The exit run's figure is ≥ 84.00 and its PASS verdict against the 84% floor is stated with the margin in percentage points.
    - The exit section pastes the same four commands as the entry section, character-identical apart from timestamps and output.
    - `### QUAL-02 closure ledger` has one row per entry-run zero-coverage file plus the two out-of-scope rows, and every row whose exit figure is 0.00% carries a named owning phase and requirement.
    - `crates/paladin-storage/src/minio.rs` is marked "no denominator entry / outside recorded scope", not `closed` and not 0%.
    - No per-file percentage in the exit record differs from the pasted `llvm-cov report` stdout by even one digit.
    - `git status --porcelain` output is recorded and, outside `.planning/`, was empty at measurement time.
  </acceptance_criteria>
  <done>The floor is proven at the phase's ending commit, the QUAL-02 closure ledger states a verdict and an owner for every file, and the two runs stand side by side without merging.</done>
</task>

<task type="auto">
  <name>Task 2: Establish QUAL-03's critical-path exerciser evidence at the D-19 bar</name>
  <files>.planning/phases/03-verification-depth/03-critical-path-exercisers.md</files>
  <read_first>
    - `.planning/phases/01-ground-truth-decision-records/01-CONTEXT.md` §D-19 and §D-20 — the evidence bar (`file:line` **plus** a named passing exerciser) and the five verdict classes every citation here must satisfy.
    - `.planning/ledgers/milestone-01.md` §"Verdict legend" — the same five classes at source, and the row-citation style to match.
    - `tests/integration/commander_integration_tests.rs`, `tests/integration/battalion/load_test.rs`, `tests/integration/arsenal_execution_integration_test.rs`, `tests/integration/mcp_streamable_http_test.rs` — the leading candidate exercisers named by research for Battalion orchestration and tool invocation. Verify each by running it, not by reading it.
    - `tests/integration/mod.rs` — the full list of integration modules, so the search for a Paladin-execution exerciser covers the whole tree rather than the first plausible file.
    - `.planning/phases/03-verification-depth/03-CONTEXT.md` §D-08 — that the percentage clause is recorded superseded and the surviving substance is named exercisers.
  </read_first>
  <action>
    Create `.planning/phases/03-verification-depth/03-critical-path-exercisers.md` recording, for each
    of QUAL-03's three named critical paths **in the order QUAL-03 names them** — Paladin execution,
    Battalion orchestration, tool invocation — the evidence required by the D-19 bar.

    For each path, record: the path name; one or more exercisers, each cited as a repository-root
    relative `path:line` using forward slashes plus the exact Rust test-function identifier copied
    byte-for-byte from source; the verbatim `cargo test --offline` command that runs it; and the
    pasted run output showing that test listed as `ok`. Run every command under the default parallel
    harness — never pass `--test-threads=1` — and paste the real output rather than describing it.

    Candidates to verify first, then extend by searching `tests/integration/` if none holds:
    Battalion orchestration is expected to be served by `commander_integration_tests.rs` and
    `battalion/load_test.rs`; tool invocation by `arsenal_execution_integration_test.rs` and by
    `mcp_streamable_http_test.rs`'s `streamable_http_round_trip_with_correct_bearer_token_succeeds`,
    which the milestone ledger already cites. Paladin execution has no pre-named candidate — search
    the integration tree for a test that drives `PaladinExecutionService` or the `PaladinPort`
    execute path end to end.

    An exerciser qualifies only if it is an integration test, passes, and carries no skip attribute.
    If a candidate is skipped or failing, it does not count and must not be cited.

    Where one test genuinely exercises more than one path, cite it under each path it exercises and
    add a one-line note that the citation is shared — do not collapse two paths into one row because
    their exercisers overlap.

    If any path has zero qualifying exercisers, record it `genuinely outstanding` with a named owning
    phase and requirement rather than crediting it with an adjacent or partial test. Do not write a
    test to fill the gap in this plan; record the gap and let plan 03-08's amendment carry it.

    Add a closing `## Percentage clause` section stating that QUAL-03's `at or above 70%, up from the
    67.79% baseline` clause is superseded by ADR-0006, which abolished the second number under a
    second scope, and that this phase therefore records no coverage percentage for QUAL-03 anywhere.
    Name plan 03-08 as where the amendment lands at source in REQUIREMENTS.md.

    Record nothing in this file that is not either a citation verified by a pasted command output or
    an explicitly-labelled gap.
  </action>
  <verify>
    <automated>F=.planning/phases/03-verification-depth/03-critical-path-exercisers.md; grep -q 'Paladin execution' "$F" && grep -q 'Battalion orchestration' "$F" && grep -q 'tool invocation' "$F" && grep -q '## Percentage clause' "$F" && grep -qE 'tests/[a-z_/]+\.rs:[0-9]+' "$F" && [ "$(grep -cE 'cargo test --offline' "$F")" -ge 3 ] && [ "$(grep -cE '\-\-test-threads' "$F")" -eq 0 ] && [ "$(grep -cE '[0-9]+\.[0-9]+%|[0-9]+%' "$F")" -eq 0 ] && grep -qE 'test result: ok\.' "$F"</automated>
  </verify>
  <acceptance_criteria>
    - The file names all three critical paths in QUAL-03's own order: Paladin execution, then Battalion orchestration, then tool invocation.
    - Every cited exerciser carries a repository-root-relative `path:line` matching `tests/[a-z_/]+\.rs:[0-9]+` and an exact Rust identifier.
    - Every citation is accompanied by a pasted `cargo test --offline` invocation and output containing `test result: ok.` with that test listed.
    - `grep -c -- '--test-threads' <file>` returns 0 — every verification ran under the default parallel harness.
    - `grep -cE '[0-9]+%' <file>` returns 0 — no coverage percentage is recorded for QUAL-03 anywhere.
    - Any path with no qualifying exerciser is marked `genuinely outstanding` with a named owning phase and requirement.
    - Any exerciser cited for two paths carries an explicit shared-citation note; no two paths are merged into one row.
    - `## Percentage clause` states the supersession by ADR-0006 and names plan 03-08 as where the source amendment lands.
  </acceptance_criteria>
  <done>Each of the three critical paths has verified, pasted evidence at the D-19 bar or an owned gap, and QUAL-03's percentage clause is recorded superseded with no substitute number invented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| toolchain → recorded artifact | `llvm-cov` stdout crosses into a record that VERIFY-05 and PIPE-02 will cite as authoritative |
| test-suite output → evidence record | Pasted `cargo test` output is the sole proof a cited exerciser passes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | `03-coverage-measurement.md`, `03-critical-path-exercisers.md` | medium | mitigate | Every figure is pasted stdout on a stated commit; `git status --porcelain` proves the tree was clean; the entry section is not editable by this plan; acceptance criteria require exactly two recorded figures and zero percentages in the QUAL-03 record; a human checkpoint in plan 03-08 confirms both figures |
| T-03-18 | Repudiation | exit measurement provenance | medium | mitigate | The exit run records its own commit SHA and UTC timestamp separately from the entry run, so a later reader can tell which tree each figure describes and cannot mistake one for the other |
| T-03-19 | Tampering | partial `.profraw` merge | medium | mitigate | `target/coverage/` is cleared before the run; the `%p-%m` pattern keeps parallel test binaries from colliding on one file; an interrupted run is restarted from scratch rather than merged |
| T-03-08 | Information Disclosure | environment probe block | low | accept | Toolchain versions, commit SHA and UTC timestamp only; no hostname, username, IP or path outside the toolchain and repository |
| T-03-SC | Tampering | crates.io / cargo registry | high | mitigate | No package is installed; every cargo command carries `--offline`. `03-RESEARCH.md` §Package Legitimacy Audit: five already-vendored dependencies, all OK, zero ASSUMED/SUS/SLOP |
</threat_model>

<verification>
- Two coverage figures exist, one per run, neither overwriting the other, both ≥ 84.00%.
- Every file in the entry zero-coverage set has a verdict and, where still open, a named owner.
- `minio.rs` is recorded outside scope rather than as a 0% file.
- All three QUAL-03 critical paths have verified pasted evidence or an owned, labelled gap.
- No second coverage percentage exists anywhere in this phase's artifacts.

## Edge coverage (spec-less probe fallback)

No SPEC.md exists; the fallback probe surfaced 26 edges. This plan authors **13** — all 6 of QUAL-02
(`boundary`: strictly-greater-than-zero closure rule; `empty`: no-denominator-entry files;
`precision`: byte-identical transcription; `adjacency`: claimed and measured kept side by side;
`ordering`: descending counted lines with lexicographic tiebreak; `concurrency`: `%p-%m` isolation and
no partial-profraw merge) and all 7 of QUAL-03 (`boundary`: the superseded percentage clause;
`empty`: a path with zero exercisers; `adjacency`: one test serving two paths; `encoding`: byte-for-byte
citation identity; `ordering`: QUAL-03's own path order preserved; `precision`: no numeric percentage
recorded; `concurrency`: default parallel harness, no `--test-threads=1`) — into `must_haves.truths`
above, all resolved `covered`. None backstopped, none dismissed, none dropped.

## Prohibition probe (spec-less fallback)

No new bespoke prohibition surfaces here; the phase's four kept prohibitions are authored in 03-01 (2),
03-04 (1) and 03-08 (1) and bind this plan's artifacts too. Canon-referral breadcrumb: none applicable
to this plan's surface.
</verification>

<success_criteria>
- The exit measurement is recorded with full provenance and its figure is at or above 84.00%.
- Every previously-0.00% first-party file is either closed with a measured figure or deferred with a named owner.
- QUAL-03's three critical paths each carry D-19-bar evidence or an owned gap, with no percentage invented.
- The entry measurement section is byte-unchanged by this plan.
</success_criteria>

<output>
Create `.planning/phases/03-verification-depth/03-07-SUMMARY.md` when done.
</output>
