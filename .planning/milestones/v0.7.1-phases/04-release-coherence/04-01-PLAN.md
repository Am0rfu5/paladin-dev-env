---
phase: 04-release-coherence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/paladin-ports/Cargo.toml
  - crates/paladin-notifications/Cargo.toml
  - .planning/phases/04-release-coherence/04-release-measurement.md
autonomous: true
requirements: [REL-02]

must_haves:
  truths:
    - "Every workspace manifest declares the 2024 edition: `grep -h '^edition' Cargo.toml crates/*/Cargo.toml | sort -u` returns exactly one line, and that line names 2024."
    - "`cargo build --workspace --offline` exits 0 after the bump."
    - "`cargo build --workspace --no-default-features --offline` exits 0 after the bump (D-06's second proof leg, never run before this phase)."
    - "Exactly zero manifests declare the 2021 edition and exactly twelve declare 2024 (root `Cargo.toml` plus eleven member crates); a crate already on 2024 is untouched, not re-keyed."
    - "`cargo fix --edition` producing zero source rewrites is a pass, not a failure — the proof obligation is the build, not the diff size."
    - "The edition bump lands before the version bump; a crate's `edition` value is independent of every other crate's, so the order the two manifests are processed in does not change the outcome."
    - "`crates/paladin-ports/Cargo.toml` still carries `doctest = false` unchanged — the edition key and the doctest key are orthogonal, and re-enabling doctests is DEBT-03 / Phase 8."
    - "`.planning/phases/04-release-coherence/04-release-measurement.md` exists and each measurement section carries the full D-17 provenance block with raw pasted stdout."
  artifacts:
    - path: "crates/paladin-ports/Cargo.toml"
      provides: "edition = 2024 on the most-depended-on member crate"
      contains: "2024"
    - path: "crates/paladin-notifications/Cargo.toml"
      provides: "edition = 2024 on the second straggler"
      contains: "2024"
    - path: ".planning/phases/04-release-coherence/04-release-measurement.md"
      provides: "The phase's single raw-evidence record; created here, appended by plans 04-04, 04-05 and 04-06"
      min_lines: 60
  key_links:
    - from: "crates/paladin-ports/Cargo.toml"
      to: ".planning/phases/04-release-coherence/04-release-measurement.md"
      via: "the build-proof sections that name this manifest as the change under measurement"
      pattern: "paladin-ports"
    - from: "crates/paladin-ports/Cargo.toml"
      to: "the other nine crates that path-depend on it"
      via: "cargo build --workspace proves the edition change did not alter trait-object inference or macro expansion visible downstream"
      pattern: "cargo build --workspace"
  prohibitions:
    - statement: "MUST NOT silence a compiler diagnostic surfaced by the edition migration with an `#![allow(...)]`, a `#[cfg]` gate, or a disabled test in order to make the build leg pass — a diagnostic is fixed at the file:line the compiler names, or the migration is stopped and reported."
      category: transparency
    - statement: "MUST NOT record a build leg as passing unless its raw stdout was pasted into the measurement record in this run — no figure is carried over from RESEARCH.md, from Phase 2, or from any prior session."
      category: transparency
---

<objective>
Close the `edition = "2024"` / `"2021"` split across the workspace in one direction — forward, to
2024 — and prove it with the two build legs D-06's proof obligation names, recorded to the D-17
provenance standard.

Purpose: REL-02's code fix. Ten of twelve manifests already declare 2024 and the workspace builds
under it at the pinned toolchain (`1.97.1`, where Rust 2024 has been stable since 1.85). Only
`crates/paladin-ports` and `crates/paladin-notifications` lag. This plan is the phase's tracer: it
touches the manifest layer, the build layer and the record layer end to end on the single riskiest
crate first, so an architectural dead end surfaces after one commit instead of after five.

Output: two manifest lines changed, and `04-release-measurement.md` created as the phase's single
raw-evidence record — the file plans 04-04, 04-05 and 04-06 later append to.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-release-coherence/04-CONTEXT.md
@.planning/phases/04-release-coherence/04-RESEARCH.md
@.planning/phases/04-release-coherence/04-PATTERNS.md
@.planning/phases/03-verification-depth/03-coverage-measurement.md
</context>

## Artifacts this phase produces

Newly created by **this plan**:

- `.planning/phases/04-release-coherence/04-release-measurement.md` — the phase's single raw-evidence
  measurement record (D-17). Created here; appended by plans 04-04, 04-05 and 04-06.

Created elsewhere in Phase 4 (listed so drift verification excludes them):

- `.planning/phases/04-release-coherence/04-advisory-findings.md` (plan 04-02)
- `.planning/phases/04-release-coherence/04-ci-gate-deferrals.md` (plan 04-03)
- `.planning/phases/04-release-coherence/COVERAGE.md` (plan 04-03)
- `.planning/decisions/0008-workspace-version-0-7-0.md` (plan 04-07)
- `.planning/decisions/0009-workspace-rust-edition-2024.md` (plan 04-07)
- New `.github/workflows/ci.yml` job keys `examples` and `kubernetes-smoke` (plan 04-03)
- Local annotated git tag `v0.7.0`, unpushed (plan 04-05)

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end edition slice — bump `paladin-ports` and prove the whole workspace still builds</name>
  <precondition>`git status --porcelain` reports no modified or untracked files outside `.planning/`, so `cargo fix --edition --allow-dirty` cannot rewrite an unrelated in-flight change.</precondition>
  <files>crates/paladin-ports/Cargo.toml, .planning/phases/04-release-coherence/04-release-measurement.md</files>
  <read_first>
    - crates/paladin-ports/Cargo.toml (the manifest under change — read the whole file; note the `[lib]` block and the exact pin on line 21 before editing)
    - rust-toolchain.toml (confirm the pinned channel is 1.97.1, the fact that makes 2024 a real edition here)
    - .planning/phases/03-verification-depth/03-coverage-measurement.md (lines 1-60 — the provenance-block shape to copy verbatim, including the sentence that disambiguates unrelated pre-existing dirty files from the measurement's own scope)
    - .planning/phases/04-release-coherence/04-RESEARCH.md (Part A, Q1 — the hazard-by-hazard table and the exact `cargo fix` invocation)
  </read_first>
  <action>
    Change the `edition` key in `crates/paladin-ports/Cargo.toml` from the 2021 value to `2024`,
    per D-04. This is one line. Do not touch any other key in that manifest — in particular
    `doctest = false` in the `[lib]` block and its "Task 7.0" comment stay exactly as they are:
    `edition` and `doctest` are orthogonal Cargo keys, and re-enabling doctests belongs to DEBT-03
    in Phase 8. Do not touch the `=0.6.0` exact pin on line 21 either; the version bump is plan
    04-05's.

    Then run the documented migration route: `cargo fix --edition --offline -p paladin-ports
    --allow-dirty --lib`, followed by `cargo fix --edition --offline -p paladin-ports --allow-dirty
    --tests` if the `--lib` pass leaves the test targets uncompiled. Research verified by grep across
    all 33 files of this crate that four of the seven textbook 2024 hazards are provably absent —
    no `unsafe` blocks or functions at all, no `static mut`, no identifier named `gen`, no
    `no_mangle` — and that the crate's 57 trait methods all go through `#[async_trait]` rather than
    native return-position `impl Trait`, so the lifetime-capture default change is very unlikely to
    bite. The two hazards that remain are semantic and only the compiler can surface them: the
    `if let` / tail-expression temporary drop-order change, and the match-ergonomics tightening. If
    either fires, fix it at the exact `file:line` the compiler names. Do not reach for an
    `#![allow(...)]`, a `#[cfg]` gate, or a disabled test to make the build go green — if a
    diagnostic cannot be fixed inside this task, stop and report it rather than suppressing it.

    Prove the change with both legs of D-06's obligation, in this order:
    `cargo build --workspace --offline`, then `cargo build --workspace --no-default-features
    --offline`. Both exercise the whole workspace deliberately, not just the bumped crate — all nine
    other member crates path-depend on `paladin-ports`, and the no-default-features leg is the one
    that catches a feature-gated path in a dependent crate that only compiled under the old edition's
    rules. That second leg has never been run in this project; treat a failure there as a real
    finding, not as a flaky invocation.

    Create `.planning/phases/04-release-coherence/04-release-measurement.md` following
    `03-coverage-measurement.md`'s shape exactly: a title naming it a raw-evidence record for Phase
    4, the one-paragraph "this file is raw evidence only" preamble, then a section headed
    `## Entry measurement — edition 2024 on paladin-ports` containing the D-17 environment probes
    with raw pasted stdout for `rustc -vV`, `cargo --version`, `git rev-parse HEAD`,
    `git rev-parse --abbrev-ref HEAD`, `git status --porcelain` (with the disambiguating prose
    sentence about pre-existing dirty files) and `date -u`, followed by the verbatim command lines
    and verbatim stdout of both build legs. Every cargo invocation in this record carries `--offline`
    per D-17.
  </action>
  <acceptance_criteria>
    - `grep -c 'edition = "2024"' crates/paladin-ports/Cargo.toml` returns 1
    - `grep -c 'doctest = false' crates/paladin-ports/Cargo.toml` returns 1 (the key is unchanged, not removed)
    - `grep -c '"=0.6.0"' crates/paladin-ports/Cargo.toml` returns 1 (the exact pin is untouched by this plan)
    - `cargo build --workspace --offline` exits 0
    - `cargo build --workspace --no-default-features --offline` exits 0
    - `test -s .planning/phases/04-release-coherence/04-release-measurement.md` succeeds
    - `grep -c 'rustc -vV' .planning/phases/04-release-coherence/04-release-measurement.md` returns at least 1
    - `grep -c 'git rev-parse HEAD' .planning/phases/04-release-coherence/04-release-measurement.md` returns at least 1
    - `grep -c 'no-default-features' .planning/phases/04-release-coherence/04-release-measurement.md` returns at least 1
    - No `#![allow(` line was added to any file under `crates/paladin-ports/src`: `git diff -- crates/paladin-ports/src | grep -c '^+#!\[allow('` returns 0
  </acceptance_criteria>
  <verify>
    <automated>cargo build --workspace --offline && cargo build --workspace --no-default-features --offline && grep -q 'edition = "2024"' crates/paladin-ports/Cargo.toml && grep -q 'doctest = false' crates/paladin-ports/Cargo.toml && test -s .planning/phases/04-release-coherence/04-release-measurement.md</automated>
  </verify>
  <done>`crates/paladin-ports` declares the 2024 edition, both build legs exit 0 across the whole workspace, and `04-release-measurement.md` exists carrying the first measurement section with a full D-17 provenance block and raw stdout for both legs.</done>
</task>

<task type="auto">
  <name>Task 2: Bump `paladin-notifications` and close the split</name>
  <files>crates/paladin-notifications/Cargo.toml, .planning/phases/04-release-coherence/04-release-measurement.md</files>
  <read_first>
    - crates/paladin-notifications/Cargo.toml (the manifest under change)
    - .planning/phases/04-release-coherence/04-release-measurement.md (the record Task 1 created — append, never overwrite)
    - .planning/phases/04-release-coherence/04-RESEARCH.md (Part A, Q1 — the same hazard table covers this crate's 5 files / 1,099 lines)
  </read_first>
  <action>
    Change the `edition` key in `crates/paladin-notifications/Cargo.toml` from the 2021 value to
    `2024`, then run the same migration route: `cargo fix --edition --offline -p
    paladin-notifications --allow-dirty --lib`, plus a `--tests` pass if needed. This crate is the
    smaller of the two (5 files, 1,099 lines) and the same four hazards are provably absent from it,
    but it does use `if let` — the drop-order hazard is live here in exactly the same way, and the
    same no-suppression rule applies.

    Re-run both build legs (`cargo build --workspace --offline` and `cargo build --workspace
    --no-default-features --offline`) so the recorded proof covers the final state of the workspace
    rather than an intermediate one, and append a second section to `04-release-measurement.md`
    headed `## Entry measurement — edition 2024 on paladin-notifications (workspace now uniform)`
    with its own complete D-17 provenance block and raw stdout. Do not edit or fold Task 1's section
    into it; the record accumulates sections, it does not overwrite them.

    Finish by recording the uniformity assertion in the same section: the exact command
    `grep -h '^edition' Cargo.toml crates/*/Cargo.toml | sort -u` and its raw output, which must be
    a single line naming 2024, alongside the count breakdown — twelve manifests total, one root
    package plus eleven member crates. Note in that section that CONTEXT.md and RESEARCH.md say
    "eleven manifests" because they are counting member crates only; the root `Cargo.toml` is the
    twelfth file carrying an `edition` key.
  </action>
  <acceptance_criteria>
    - `grep -c 'edition = "2024"' crates/paladin-notifications/Cargo.toml` returns 1
    - `grep -h '^edition' Cargo.toml crates/*/Cargo.toml | sort -u | wc -l` returns 1
    - `grep -h '^edition' Cargo.toml crates/*/Cargo.toml | grep -c 2024` returns 12
    - `grep -h '^edition' Cargo.toml crates/*/Cargo.toml | grep -c 2021` returns 0
    - `cargo build --workspace --offline` exits 0
    - `cargo build --workspace --no-default-features --offline` exits 0
    - `grep -c '^## Entry measurement' .planning/phases/04-release-coherence/04-release-measurement.md` returns 2
    - No `#![allow(` line was added under `crates/paladin-notifications/src`: `git diff -- crates/paladin-notifications/src | grep -c '^+#!\[allow('` returns 0
  </acceptance_criteria>
  <verify>
    <automated>cargo build --workspace --offline && cargo build --workspace --no-default-features --offline && [ "$(grep -h '^edition' Cargo.toml crates/*/Cargo.toml | sort -u | wc -l)" = "1" ] && [ "$(grep -h '^edition' Cargo.toml crates/*/Cargo.toml | grep -c 2024)" = "12" ] && [ "$(grep -c '^## Entry measurement' .planning/phases/04-release-coherence/04-release-measurement.md)" = "2" ]</automated>
  </verify>
  <done>All twelve manifests declare one edition, 2024; both build legs exit 0 on the final state; and `04-release-measurement.md` carries two provenance-blocked measurement sections plus the uniformity assertion with raw output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `cargo fix --edition` → workspace source tree | An automated rewriter with write access to first-party source; its output is trusted only after a build proves it |
| pinned toolchain (`rust-toolchain.toml`) → build verdict | The 2024 edition is only real because the channel is 1.97.1; a toolchain override would silently change the meaning of a green build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `cargo fix --edition --allow-dirty` writing to first-party source | medium | mitigate | Scope every invocation with `-p <crate>` to one member; the task precondition asserts no uncommitted work exists outside `.planning/`, so `--allow-dirty` cannot absorb an unrelated in-flight edit into the rewrite |
| T-04-02 | Repudiation | `04-release-measurement.md` | high | mitigate | Every figure carries the D-17 provenance block — `rustc -vV`, `cargo --version`, `git rev-parse HEAD`, branch, `git status --porcelain`, `date -u` — with raw pasted stdout, so a later reader re-derives the verdict instead of trusting a summary |
| T-04-03 | Tampering | edition-migration diagnostics | medium | mitigate | An `#![allow(...)]`, `#[cfg]` gate or disabled test that makes a build leg pass is prohibited; the acceptance criteria grep the diff for added crate-level allows |
| T-04-04 | Spoofing | toolchain identity | low | accept | `rust-toolchain.toml` pins `1.97.1` and the provenance block records `rustc -vV` verbatim; a mismatch is visible in the record rather than prevented |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages. `04-RESEARCH.md` § "Package Legitimacy Audit" records the phase-wide verdict as "Not applicable — this phase installs no new external packages" |
</threat_model>

<verification>
- `grep -h '^edition' Cargo.toml crates/*/Cargo.toml | sort -u` returns exactly one line, naming 2024.
- `cargo build --workspace --offline` and `cargo build --workspace --no-default-features --offline` both exit 0.
- `04-release-measurement.md` contains two `## Entry measurement` sections, each with a complete D-17
  provenance block and verbatim stdout.
- `crates/paladin-ports/Cargo.toml` still carries `doctest = false` and the `=0.6.0` exact pin.
</verification>

<success_criteria>
ROADMAP Phase 4 criterion 2 — "Every workspace crate declares the same valid Rust edition and
`cargo build --workspace` succeeds under it" — is true and recorded with reproducible provenance.
The ADR that records *why* 2024 rather than 2021 is plan 04-07's; this plan is the code fix REL-02
names.
</success_criteria>

<output>
Create `.planning/phases/04-release-coherence/04-01-SUMMARY.md` when done.
</output>
