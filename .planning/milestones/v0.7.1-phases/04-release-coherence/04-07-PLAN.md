---
phase: 04-release-coherence
plan: 07
type: execute
wave: 5
depends_on: ["04-02", "04-03", "04-04", "04-05", "04-06"]
files_modified:
  - .planning/decisions/0008-workspace-version-0-7-0.md
  - .planning/decisions/0009-workspace-rust-edition-2024.md
  - .planning/decisions/PROMOTION.md
  - .planning/codebase/CONCERNS.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ledgers/milestone-01.md
autonomous: true
requirements: [REL-01, REL-02, REL-03, REL-04, REL-05]

must_haves:
  truths:
    - "Two ADRs exist at `.planning/decisions/0008-workspace-version-0-7-0.md` and `0009-workspace-rust-edition-2024.md`, each carrying the seven required H2 headings in PROMOTION.md's fixed order."
    - "`## Code Locations` and `## Considered Options` are bulleted lists in both ADRs, never prose paragraphs — `adr-parser.cjs`'s `splitEntries` only yields structured entries from bullet or numbered lines."
    - "Both ADRs carry `## Code Conformance` naming the executing requirement: REL-01 for the version ADR, REL-02 for the edition ADR."
    - "`PROMOTION.md`'s numbering index gains rows 0008 and 0009 and its `Next free ADR number` line reads 0010."
    - "`.planning/PROJECT.md`'s Key Decisions table gains a row per ADR, linking the file — the first entries that table has ever held."
    - "The `CONCERNS.md` edition finding is corrected at source with the dated amendment parenthetical citing the ADR and the toolchain proof; the original claim is retained, not deleted."
    - "Phase 7's ARCH-03(a) and ARCH-04 requirement text cites the ADR that answers it instead of leaving the question open for re-adjudication."
    - "Every one of REL-01 … REL-05 has a ledger row carrying a verdict from the file's own five-verdict legend, and every `deferred with reason` row names a bold owner."
    - "The existing ledger rows that name REL-01 … REL-05 as forward owners are amended in place with the Phase 4 verdict, following the Phase 2 amend-in-place convention rather than being duplicated."
    - "REQUIREMENTS.md's five REL traceability rows move from Pending to a status backed by the ledger rows."
  artifacts:
    - path: ".planning/decisions/0008-workspace-version-0-7-0.md"
      provides: "The recorded version answer that Phase 7's ARCH-04 and Phase 10's HARD-03 inherit"
      contains: "Code Conformance"
    - path: ".planning/decisions/0009-workspace-rust-edition-2024.md"
      provides: "The recorded edition answer that Phase 7's ARCH-03(a) inherits"
      contains: "Code Conformance"
    - path: ".planning/decisions/PROMOTION.md"
      provides: "Updated numbering index and next-free line"
      contains: "Next free ADR number: 0010"
    - path: ".planning/ledgers/milestone-01.md"
      provides: "REL-01..REL-05 verdict rows plus every Phase 4 deferral with a named owner"
      contains: "REL-01"
  key_links:
    - from: ".planning/decisions/0008-workspace-version-0-7-0.md"
      to: ".planning/REQUIREMENTS.md ARCH-04"
      via: "the ADR's Downstream Consumers section names ARCH-04, and ARCH-04's text cites the ADR back"
      pattern: "0008"
    - from: ".planning/decisions/0009-workspace-rust-edition-2024.md"
      to: ".planning/codebase/CONCERNS.md"
      via: "the amendment parenthetical cites the ADR as the authority that voids the stale claim"
      pattern: "0009"
    - from: ".planning/ledgers/milestone-01.md"
      to: ".planning/phases/04-release-coherence/04-release-measurement.md"
      via: "every satisfied REL row cites a measurement section or a file:line, per the D-19 evidence bar"
      pattern: "04-release-measurement"
  prohibitions:
    - statement: "MUST NOT record a `satisfied` verdict for any part of REL-05 that was authored rather than executed. The multi-arch Docker build and the Kubernetes smoke test are `deferred with reason` with a named owner; a row claiming otherwise would make the phase's central honesty constraint fail silently in the one document later phases read as ground truth."
      category: transparency
    - statement: "MUST NOT delete or rewrite a superseded claim. A corrected finding keeps its original text and gains the dated parenthetical; a superseded ADR would keep its file and flip only its Status line. Silent replacement is the failure mode the amend-at-source convention exists to prevent."
      category: transparency
    - statement: "MUST NOT resolve, infer, or record an answer to any question this phase handed to a named owner: the advisory owner/expiry schema and the 2026-09-30 disposition (SEC-01 / SUPPLY-02), the licence three-way (SEC-02), the duplicate CI audit job (SUPPLY-01), the red `api-surface` job (DEBT-01), or `paladin-ports` doctests (DEBT-03)."
      category: governance
---

<objective>
Protect the two answers this phase produced, correct the stale record that argued for the opposite
one, and write the cited verdicts that let Phases 7, 9, 10, 12, 14 and 15 inherit rather than
re-decide.

Purpose: PROJECT.md's own corpus-level finding is that twelve milestones and 554 requirements
produced **not one protected decision** — every technical position sits at DOC precedence and is
auto-overridable by the next document that mentions it. D-02 and D-06 exist to stop that happening
to the version and edition answers. The ADRs top the precedence order, so Phase 7's ARCH-04 and
ARCH-03(a) and Phase 10's HARD-03 cite them instead of re-adjudicating. Alongside them, D-05
corrects the `CONCERNS.md` claim that Rust 2024 "does not exist in Rust's stable channel" — false at
the pinned 1.97.1 toolchain, where the edition has been stable since 1.85.

Output: ADRs 0008 and 0009, a corrected codebase map, updated Phase 7 requirement text, and the
REL-01 … REL-05 ledger with every deferral owned by name.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-release-coherence/04-CONTEXT.md
@.planning/phases/04-release-coherence/04-PATTERNS.md
@.planning/phases/04-release-coherence/04-02-SUMMARY.md
@.planning/phases/04-release-coherence/04-03-SUMMARY.md
@.planning/phases/04-release-coherence/04-04-SUMMARY.md
@.planning/phases/04-release-coherence/04-05-SUMMARY.md
@.planning/phases/04-release-coherence/04-06-SUMMARY.md
</context>

## Artifacts this phase produces

Newly created by **this plan**:

- `.planning/decisions/0008-workspace-version-0-7-0.md`
- `.planning/decisions/0009-workspace-rust-edition-2024.md`

Created elsewhere in Phase 4 (listed so drift verification excludes them):

- `.planning/phases/04-release-coherence/04-release-measurement.md` (plan 04-01, appended by 04-04/05/06)
- `.planning/phases/04-release-coherence/04-advisory-findings.md` (plan 04-02)
- `.planning/phases/04-release-coherence/04-ci-gate-deferrals.md`, `COVERAGE.md` (plan 04-03)
- New `.github/workflows/ci.yml` job keys `examples` and `kubernetes-smoke` (plan 04-03)
- Local annotated git tag `v0.7.0`, unpushed (plan 04-05)

## Decision this plan records (Claude's Discretion item from CONTEXT.md)

**The dual `reqwest` 0.12.28 / 0.13.4 exposure gets a ledger row, not a new document.** It is
already documented at `.planning/codebase/CONCERNS.md:293-303`, `deny.toml` treats duplicates as a
warning by deliberate policy (FR 12 / Open Question 4), and it is not an advisory and does not fail
SC3. It is recorded once in the ledger as a known, policy-accepted duplicate pointing at the
existing CONCERNS.md section, so the discretion item is disposed of visibly rather than dropped.

<tasks>

<task type="auto">
  <name>Task 1: Author ADR-0008 (version) and ADR-0009 (edition), and update the numbering index</name>
  <files>.planning/decisions/0008-workspace-version-0-7-0.md, .planning/decisions/0009-workspace-rust-edition-2024.md, .planning/decisions/PROMOTION.md</files>
  <read_first>
    - .planning/decisions/PROMOTION.md (the whole file — the numbering scheme, the index table, the `Next free ADR number: 0008` line, the seven required headings in fixed order, the bulleted-list constraint on `Code Locations` and `Considered Options`, and the six-step promotion procedure)
    - .planning/decisions/0006-coverage-gate.md (the worked example of a contested-number ADR; lines 211-220 hold the exact `## Code Conformance` phrasing to mirror, naming PIPE-02 as the executing requirement)
    - .planning/decisions/0007-battalion-cancellation-deferral.md (lines 98-104 — the `## Downstream Consumers` convention, naming the specific file and section that inherits the answer)
    - .planning/REQUIREMENTS.md (lines 549-583 for ARCH-03 and ARCH-04, lines 978-991 for HARD-03's version history, lines 1372-1391 for ORCH-05's lockstep chain, and lines 4004-4022 for the cross-phase coupling table — these are the four inputs both ADRs must cite)
  </read_first>
  <action>
    Author two ADRs at `.planning/decisions/0008-workspace-version-0-7-0.md` and
    `.planning/decisions/0009-workspace-rust-edition-2024.md`, both following `PROMOTION.md`'s house
    structure exactly: the seven H2 headings `## Status`, `## Context`, `## Decision`,
    `## Considered Options`, `## Code Locations`, `## Code Conformance`, `## Downstream Consumers`,
    in that order and no other. `## Code Locations` and `## Considered Options` must be bulleted
    lists — the project's `adr-parser.cjs` extracts structured entries only from bullet or numbered
    lines, and a prose paragraph collapses into one opaque blob.

    **ADR-0008 — the workspace version is 0.7.0.** Its `## Context` states the three-way
    disagreement it ends (branch `release/v0.7.0`, all twelve manifests at 0.6.0, latest tag
    `v0.5.1`, and `v0.6.0` never tagged at all) and the fact that the coupling table assigns the
    major-bump question to ARCH-04 in Phase 7 while Phase 4 runs first — so Phase 4 records and
    Phase 7 applies, the same convention REL-02's own text already states for the edition. Its
    `## Decision` states 0.7.0 and the SemVer reasoning plainly: the Milestone 6 facade change *was*
    breaking, but it already shipped inside the 0.x series — `src/application/use_cases/` no longer
    exists in a tree that has been publishing since `v0.1.0-rc.1` — and a pre-1.0 project expresses
    breaking changes as minor bumps. `## Considered Options` carries three bullets, 0.7.0 chosen,
    1.0.0 rejected because it asserts an API-stability guarantee the corpus does not support, and
    blocking on Phase 7 rejected because it is a circular wait. `## Code Locations` cites the twelve
    manifests, `release.toml`'s lockstep settings, `CHANGELOG.md`'s new heading, and the plan 04-05
    measurement section that recorded the bump. `## Code Conformance` is **must change**, naming
    **REL-01** as the executing requirement in the same voice `0006-coverage-gate.md:211-220` names
    PIPE-02 — and stating that REL-01 has already executed it in this phase, so the verdict is
    satisfied at the time of writing rather than pending. `## Downstream Consumers` names ARCH-04
    (Phase 7) and HARD-03 (Phase 10) with the sections that must cite it.

    **ADR-0009 — the workspace edition is 2024.** Its `## Context` states that ten of twelve
    manifests already declared 2024 and two lagged, that `rust-toolchain.toml` pins 1.97.1 where
    Rust 2024 has been stable since 1.85, and — importantly — that neither the code nor the record
    was self-consistent before this: Milestone 5 Epics 1-4 require 2021 while Epic 5 and the
    milestone overview require 2024. Say plainly that those documents are **amended**, not merely
    overridden. Its `## Decision` states 2024 and the reasoning: two manifests forward against a
    38-file surface, versus nine backward plus reconciling any 2024-only syntax already in them.
    `## Considered Options` carries three bullets — 2024 chosen, 2021 rejected (and note that
    `CONCERNS.md`'s recommendation of 2021 rests on a claim that is factually wrong at the pinned
    toolchain, corrected in Task 2), leaving the split rejected because SC2 forbids it.
    `## Code Locations` cites the two bumped manifests, `rust-toolchain.toml`, and plan 04-01's two
    build-proof measurement sections. `## Code Conformance` is **must change** naming **REL-02**,
    likewise already executed. `## Downstream Consumers` names ARCH-03(a) (Phase 7), and states
    explicitly that this ADR does **not** touch `paladin-ports`'s `doctest = false` — that is
    DEBT-03 in Phase 8, governed by HARD-07 in Phase 10, and the two Cargo keys are orthogonal.

    Then update `PROMOTION.md` as its own procedure requires: add rows `0008` and `0009` to the
    numbering index table with their slugs and subjects, and change the `Next free ADR number` line
    from 0008 to 0010. Do not renumber or touch any existing row.
  </action>
  <acceptance_criteria>
    - Both files exist: `test -s .planning/decisions/0008-workspace-version-0-7-0.md` and `test -s .planning/decisions/0009-workspace-rust-edition-2024.md`
    - Each has exactly the seven required headings in order: for each file, `grep -c '^## ' <file>` returns 7, and `grep '^## ' <file> | tr '\n' '|'` equals `## Status|## Context|## Decision|## Considered Options|## Code Locations|## Code Conformance|## Downstream Consumers|`
    - `## Considered Options` is a bulleted list in both: the first non-blank line after that heading begins with `- ` in each file
    - `## Code Locations` is a bulleted list in both: the first non-blank line after that heading begins with `- ` in each file
    - Each `## Code Conformance` body begins with `must change`: `grep -A2 '^## Code Conformance' .planning/decisions/0008-workspace-version-0-7-0.md | grep -c 'must change'` returns at least 1, same for 0009
    - The executing requirement is named: `grep -c 'REL-01' .planning/decisions/0008-workspace-version-0-7-0.md` returns at least 1 and `grep -c 'REL-02' .planning/decisions/0009-workspace-rust-edition-2024.md` returns at least 1
    - Downstream consumers are named: 0008 contains `ARCH-04` and `HARD-03`; 0009 contains `ARCH-03`
    - 0009 states the doctest orthogonality: `grep -c 'DEBT-03' .planning/decisions/0009-workspace-rust-edition-2024.md` returns at least 1
    - `grep -c 'Next free ADR number: 0010' .planning/decisions/PROMOTION.md` returns 1
    - The index gained exactly two rows: `grep -cE '^\| 000[89] \|' .planning/decisions/PROMOTION.md` returns 2
    - No existing ADR was modified: `git diff --name-only .planning/decisions/ | grep -cE '000[1-7]-' ` returns 0
  </acceptance_criteria>
  <verify>
    <automated>for f in .planning/decisions/0008-workspace-version-0-7-0.md .planning/decisions/0009-workspace-rust-edition-2024.md; do test -s "$f" || exit 1; [ "$(grep '^## ' "$f" | tr '\n' '|')" = '## Status|## Context|## Decision|## Considered Options|## Code Locations|## Code Conformance|## Downstream Consumers|' ] || exit 1; done && grep -q 'Next free ADR number: 0010' .planning/decisions/PROMOTION.md && [ "$(grep -cE '^\| 000[89] \|' .planning/decisions/PROMOTION.md)" = "2" ] && [ "$(git diff --name-only .planning/decisions/ | grep -cE '000[1-7]-')" = "0" ]</automated>
  </verify>
  <done>ADR-0008 and ADR-0009 exist with the seven required headings in fixed order, bulleted `Considered Options` and `Code Locations`, `must change` conformance naming REL-01 and REL-02, and Downstream Consumers naming ARCH-04/HARD-03 and ARCH-03(a); `PROMOTION.md`'s index carries both rows and its next-free line reads 0010.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the CONCERNS.md edition finding at source, wire the ADRs into Phase 7's requirement text, and open the Key Decisions table</name>
  <files>.planning/codebase/CONCERNS.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md (lines 1-30 — §"Edition 2024 in Project Manifests", whose Issue line claims the edition "does not exist in Rust's stable channel", whose Impact line hypothesises "possibly via lenient parsing", and whose Fix approach recommends standardizing on 2021)
    - .planning/ROADMAP.md (line 274 — Phase 3's own amendment of its criterion 2; the exact parenthetical prose shape to copy)
    - .planning/REQUIREMENTS.md (lines 549-583 — ARCH-03's (a) clause and ARCH-04's major-bump question, the two texts that must now cite an ADR)
    - .planning/PROJECT.md (the `## Key Decisions` table and the evidence note explaining why it is empty — the note must be updated, not deleted, when the first rows land)
    - .planning/decisions/PROMOTION.md (step 6 of the promotion procedure: adding a row to PROJECT.md's Key Decisions table linking the new ADR)
  </read_first>
  <action>
    Three edits, each a scoped replacement — do not rewrite any file wholesale.

    **`CONCERNS.md`, §"Edition 2024 in Project Manifests" (D-05).** The finding is factually wrong at the
    pinned toolchain: it states Rust defines only editions 2015, 2018 and 2021, but Rust 2024
    stabilised in 1.85 and `rust-toolchain.toml` pins 1.97.1. Its "current build succeeds possibly
    via lenient parsing" hypothesis is therefore void — the build succeeds because the edition is
    real. Append the dated amendment parenthetical to the Issue, Impact and Fix-approach statements
    in the shape `ROADMAP.md:274` established: `(**Amended by Phase 4, dated <date>, citing
    .planning/decisions/0009-workspace-rust-edition-2024.md**: <what was claimed, what is true, and
    why the correction wins under the precedence order>.)`. Cite the ADR and the toolchain proof —
    the `rustc -vV` output recorded in `04-release-measurement.md` — as the evidence. Retain the
    original text; this is the precedence order applied at full strength, shipped tree over
    `.planning/codebase/` map, and the record of what the map said is part of the correction's value.
    Note in the amendment that the split it identified is now closed, in the direction opposite to
    the one it recommended.

    **Phase 7's requirement text.** ARCH-03's (a) clause and ARCH-04 both currently pose questions
    this phase has now answered. Amend each to cite the ADR that carries the answer, so Phase 7
    inherits rather than re-adjudicates: ARCH-03(a) cites ADR-0009 and its remaining scope becomes
    applying/citing rather than deciding; ARCH-04 cites ADR-0008 for the major-bump question, while
    keeping whatever else ARCH-04 owns that this phase did not touch — read the requirement in full
    before editing and preserve every clause the ADRs do not answer. Use the same dated
    parenthetical form.

    **PROJECT.md's Key Decisions table.** Add one row per ADR, linking the file, per step 6 of
    `PROMOTION.md`'s promotion procedure. These are the first entries this table has ever held, so
    the evidence note beneath it — which explains that the table is empty because the ingest found
    zero locked decisions across 263 documents — must be updated rather than removed: it stays as
    the historical finding, with a sentence recording that Phase 4 entered the first two protected
    decisions and naming them. Deleting the note would erase the corpus-level finding that motivated
    the ADR mechanism in the first place.
  </action>
  <acceptance_criteria>
    - `grep -c 'Amended by Phase 4' .planning/codebase/CONCERNS.md` returns at least 1
    - The amendment cites the ADR: `grep -c '0009-workspace-rust-edition-2024' .planning/codebase/CONCERNS.md` returns at least 1
    - The original claim is retained: `grep -ci 'does not exist in Rust' .planning/codebase/CONCERNS.md` returns at least 1
    - The correcting fact is stated: `grep -c '1.85' .planning/codebase/CONCERNS.md` returns at least 1 and `grep -c '1.97.1' .planning/codebase/CONCERNS.md` returns at least 1
    - ARCH-03 and ARCH-04 cite their ADRs: `grep -c '0009-workspace-rust-edition-2024' .planning/REQUIREMENTS.md` returns at least 1 and `grep -c '0008-workspace-version-0-7-0' .planning/REQUIREMENTS.md` returns at least 1
    - PROJECT.md's Key Decisions table has two ADR rows: `grep -cE '\.planning/decisions/000[89]-' .planning/PROJECT.md` returns at least 2
    - The evidence note survives: `grep -ci 'not one protected decision' .planning/PROJECT.md` returns at least 1
    - No file was rewritten wholesale: `git diff --stat -- .planning/codebase/CONCERNS.md .planning/REQUIREMENTS.md .planning/PROJECT.md` shows fewer than 80 changed lines in total
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Amended by Phase 4' .planning/codebase/CONCERNS.md && grep -q '0009-workspace-rust-edition-2024' .planning/codebase/CONCERNS.md && grep -qi 'does not exist in Rust' .planning/codebase/CONCERNS.md && grep -q '0008-workspace-version-0-7-0' .planning/REQUIREMENTS.md && grep -q '0009-workspace-rust-edition-2024' .planning/REQUIREMENTS.md && [ "$(grep -cE '\.planning/decisions/000[89]-' .planning/PROJECT.md)" -ge 2 ] && grep -qi 'not one protected decision' .planning/PROJECT.md</automated>
  </verify>
  <done>`CONCERNS.md`'s edition finding carries the dated amendment citing ADR-0009 with the original claim retained; ARCH-03(a) and ARCH-04 cite ADR-0009 and ADR-0008 respectively; PROJECT.md's Key Decisions table holds its first two rows and its evidence note is updated rather than deleted.</done>
</task>

<task type="auto">
  <name>Task 3: Write the REL-01..REL-05 ledger verdicts and every Phase 4 deferral with a named owner</name>
  <files>.planning/ledgers/milestone-01.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ledgers/milestone-01.md (lines 1-40 for the evidence bar and the Phase 2 amend-in-place convention; lines 88-118 for the row shape and the five-verdict legend; and lines 555-570, the existing rows for `REQ-api-documentation`, `REQ-user-documentation`, `REQ-deployment-artifacts` and `REQ-epic10-quality-gates` that name REL-01..REL-05 as forward owners and must be amended in place rather than duplicated)
    - .planning/REQUIREMENTS.md (lines 3896-3900 — the five REL traceability rows, all currently Pending)
    - .planning/phases/04-release-coherence/04-release-measurement.md (every measurement section — the evidence each satisfied row must cite)
    - .planning/phases/04-release-coherence/04-advisory-findings.md and 04-ci-gate-deferrals.md (the deferral rows already written by plans 04-02 and 04-03, which this ledger consolidates rather than restates from memory)
  </read_first>
  <action>
    Write the phase's verdicts into the ledger using its own conventions, then update the
    traceability rows.

    **Five REL rows.** One row per requirement in the `| ID | Verdict | Evidence |` shape, using only
    the file's five verdict classes and meeting its D-19 evidence bar — a `satisfied` verdict needs a
    `file:line` citation **plus** a named passing test, example or command that exercises it. Cite
    the specific measurement section in `04-release-measurement.md` or `04-advisory-findings.md` that
    produced each verdict, not the phase in general.

    REL-05 is the row that must not be overstated. Five of its seven named gates ran green locally
    and are `satisfied` with cited commands; the multi-arch Docker build and the Kubernetes smoke
    test were **authored and statically validated, never executed** — `docker`, `kind` and `kubectl`
    are absent from this environment. Split REL-05 into facet rows the way the file already splits
    QUAL-02 by file, so the executed clauses carry `satisfied` and the two unexecutable ones carry
    `deferred with reason` with **Owner: Phase 15 / PIPE.** in bold. A single blended REL-05 row
    reading `satisfied` would make this phase's central honesty constraint fail silently in the one
    document later phases read as ground truth.

    **Amend the four existing rows in place.** `REQ-api-documentation` and `REQ-user-documentation`
    name REL-04 as forward owner; `REQ-deployment-artifacts` names REL-05; `REQ-epic10-quality-gates`
    names QUAL-01, QUAL-03, REL-03 and REL-05. Edit each row directly with the Phase 4 verdict, the
    command or citation that produced it, and this date — the Phase 2 convention this file records at
    its own §"Phase 2 amendments" heading. Do not add duplicate rows alongside them.

    **Consolidate every Phase 4 deferral into the ledger**, each with a bold named owner, drawn from
    what plans 04-02, 04-03 and 04-06 already recorded rather than re-derived: the four
    newly-surfaced advisories (**Owner: Phase 9 / SEC-01 and Phase 12 / SUPPLY-02.**); the
    duplicate `Security Audit` CI job, measured non-blocking (**Owner: Phase 12 / SUPPLY-01.**); the
    multi-arch Docker and Kubernetes gates' first execution and the 300-second budget's plausibility
    (**Owner: Phase 15 / PIPE.**); the Kubernetes readiness-probe wiring behind the `sleep 3600`
    placeholder (**Owner: Phase 14 / WEB.**); the clean-machine QUICKSTART timing and the live LLM
    execution (**Owner: Phase 15 / PIPE.**); and one row recording the dual `reqwest` 0.12.28 /
    0.13.4 exposure as a known, policy-accepted duplicate pointing at `CONCERNS.md:293-303`, not an
    advisory and not a SC3 failure.

    Add a short Phase 4 heading above the new rows in the same form the file's Phase 2 amendments
    heading uses, naming the date and the SUMMARY files every row is sourced from, so a reader can
    trace each verdict to the plan that produced it.

    **Traceability.** Update REQUIREMENTS.md's five REL rows from `Pending` to the status the ledger
    supports. Where a requirement is fully discharged, `Complete`; where it carries a live deferral,
    use the same wording the file's other partially-discharged rows use rather than inventing a new
    status word — read the surrounding rows and match them.
  </action>
  <acceptance_criteria>
    - Every REL ID has at least one ledger row: `for r in REL-01 REL-02 REL-03 REL-04 REL-05; do grep -q "| $r" .planning/ledgers/milestone-01.md || { echo "MISSING $r"; exit 1; }; done` produces no `MISSING` output
    - REL-05's unexecutable gates are deferred, not satisfied: the ledger contains a row matching both `REL-05` and `deferred with reason`, and `grep -c 'Owner: Phase 15 / PIPE' .planning/ledgers/milestone-01.md` returns at least 2
    - Every deferral names an owner: `grep -c 'deferred with reason' .planning/ledgers/milestone-01.md` is greater than or equal to `grep -c 'Owner: Phase' .planning/ledgers/milestone-01.md` is false — i.e. every `deferred with reason` row on a Phase 4 line carries a bold `**Owner: …**`, verified by inspecting the Phase 4 section
    - All four named owners appear: the ledger contains `SEC-01`, `SUPPLY-01`, `SUPPLY-02` and `Owner: Phase 14 / WEB`
    - The dual-reqwest disposition is recorded: `grep -c 'reqwest' .planning/ledgers/milestone-01.md` returns at least 1
    - The four pre-existing forward-owner rows were amended, not duplicated: `grep -c 'REQ-user-documentation' .planning/ledgers/milestone-01.md` returns 1, and the same for `REQ-deployment-artifacts`, `REQ-api-documentation` and `REQ-epic10-quality-gates`
    - A Phase 4 heading exists: `grep -c 'Phase 4 amendments' .planning/ledgers/milestone-01.md` returns 1
    - No REL traceability row is still Pending: `sed -n '/| REL-01 |/,/| REL-05 |/p' .planning/REQUIREMENTS.md | grep -c 'Pending'` returns 0
    - Satisfied rows cite evidence: every ledger row in the Phase 4 section carrying `satisfied` also contains either `04-release-measurement.md`, `04-advisory-findings.md`, or a `file:line` citation
  </acceptance_criteria>
  <verify>
    <automated>for r in REL-01 REL-02 REL-03 REL-04 REL-05; do grep -q "| $r" .planning/ledgers/milestone-01.md || exit 1; done && [ "$(grep -c 'Owner: Phase 15 / PIPE' .planning/ledgers/milestone-01.md)" -ge 2 ] && grep -q 'Owner: Phase 14 / WEB' .planning/ledgers/milestone-01.md && grep -q 'SUPPLY-01' .planning/ledgers/milestone-01.md && [ "$(grep -c 'REQ-user-documentation' .planning/ledgers/milestone-01.md)" = "1" ] && [ "$(grep -c 'Phase 4 amendments' .planning/ledgers/milestone-01.md)" = "1" ] && [ "$(sed -n '/| REL-01 |/,/| REL-05 |/p' .planning/REQUIREMENTS.md | grep -c 'Pending')" = "0" ]</automated>
  </verify>
  <done>All five REL requirements carry cited ledger verdicts drawn from the file's own five-verdict legend; REL-05's unexecutable Docker and Kubernetes clauses are `deferred with reason` with a named owner rather than blended into a satisfied row; the four pre-existing forward-owner rows are amended in place; every Phase 4 deferral carries a bold named owner; and no REL traceability row is still Pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a phase's own verdict → the ledger later phases read as ground truth | The ledger is the single document Phases 5-16 join against; a verdict overstated here propagates as fact without re-verification |
| ADR precedence → every later document | ADRs top the precedence order, so an inaccuracy written into one cannot be corrected by a later document merely mentioning the subject |
| an amendment → the claim it corrects | Silent replacement destroys the record of what was believed and why the correction wins |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-31 | Repudiation | the REL-05 ledger row | high | mitigate | REL-05 is split into facet rows so the five executed gates carry `satisfied` with cited commands while the Docker and Kubernetes clauses carry `deferred with reason` with **Owner: Phase 15 / PIPE.**; a blended satisfied row is prohibited and the acceptance criteria assert both a `deferred with reason` REL-05 row and at least two PIPE owners |
| T-04-32 | Tampering | in-place amendment of prior claims | medium | mitigate | Every correction retains its original text and appends the dated parenthetical; `CONCERNS.md`'s original "does not exist in Rust" wording and PROJECT.md's "not one protected decision" evidence note are both asserted still present after the edit |
| T-04-33 | Elevation of privilege | recording an answer this phase does not own | high | mitigate | A plan-level prohibition names each handed-off question — the advisory owner/expiry schema and the 2026-09-30 disposition, the licence three-way, the duplicate CI audit job, the red `api-surface` job, `paladin-ports` doctests — and each appears in the ledger only as a deferral row with an owner, never as a verdict |
| T-04-34 | Spoofing | an ADR that parses as unstructured | medium | mitigate | `## Code Locations` and `## Considered Options` must be bulleted lists because `adr-parser.cjs`'s `splitEntries` yields nothing from prose; the acceptance criteria assert the exact seven-heading sequence and a leading `- ` under both |
| T-04-35 | Tampering | duplicate rather than amended ledger rows | medium | mitigate | Each of the four pre-existing forward-owner rows is asserted to appear exactly once, so a duplicate row carrying a competing verdict cannot land beside the original |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages. `04-RESEARCH.md` § "Package Legitimacy Audit" records the phase-wide verdict as "Not applicable" |
</threat_model>

<verification>
- ADR-0008 and ADR-0009 exist with the seven required headings in fixed order, bulleted
  `Considered Options` and `Code Locations`, `must change` conformance naming REL-01 and REL-02,
  and Downstream Consumers naming ARCH-04, HARD-03 and ARCH-03(a).
- `PROMOTION.md`'s index carries rows 0008 and 0009 and its next-free line reads 0010; no existing
  ADR file is modified.
- `CONCERNS.md`'s edition finding carries the dated amendment citing ADR-0009, with the original
  claim retained and both `1.85` and `1.97.1` stated.
- ARCH-03(a) and ARCH-04 cite their ADRs; PROJECT.md's Key Decisions table holds its first two rows
  and keeps its evidence note.
- All five REL IDs have cited ledger verdicts, REL-05's unexecutable clauses are deferred with a
  named owner, every Phase 4 deferral carries a bold owner, and no REL traceability row is Pending.
</verification>

<success_criteria>
The two answers this phase produced are protected at the top of the precedence order rather than
sitting at DOC precedence where the next document that mentions a version or an edition would
override them — the exact failure mode PROJECT.md's "not one protected decision in twelve
milestones" finding names. Phases 7, 9, 10, 12, 14 and 15 can each read one cited row and know
what they inherit, what they own, and what this phase deliberately did not answer.
</success_criteria>

<output>
Create `.planning/phases/04-release-coherence/04-07-SUMMARY.md` when done.
</output>
