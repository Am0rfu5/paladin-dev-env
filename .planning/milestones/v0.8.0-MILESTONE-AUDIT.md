---
milestone: 0.8.0
audited: 2026-08-24T00:00:00Z
status: tech_debt
scope_note: >
  Audited as the 14 phase directories 05-17 under .planning/phases/, per the operator's scope
  decision at audit time. GSD state (.planning/STATE.md) labels this milestone v0.7.2; every
  Cargo manifest in the workspace says 0.8.0. The operator chose 0.8.0 as the milestone label.
  The ROADMAP `## Milestones` table splits these same 14 phases across four blocks and still
  reads "Not started" for three of them plus the Provider Expansion row — a known stale-cell
  defect already flagged in STATE.md, not a work gap.
scores:
  requirements: 65/65
  phases: 14/14
  integration: 8/8
  flows: 10/10
gaps:
  requirements: []
  integration: []
  flows: []
warnings_status: both_closed_2026_08_24
warnings:
  - id: "INT-01"
    kind: "cross-phase record drift"
    affects: ["SEC-01", "SUPPLY-02"]
    file: "SECURITY-EXCEPTIONS.md:44-49"
    evidence: >
      The register's prose heading reads "The ten live suppressions" and "five unmaintained-class
      advisories". The machine-readable payload actually carries eleven entries — five
      vulnerability-class and six unmaintained-class. Confirmed by `scripts/check-advisory-register.sh`
      (exit 0): "11 register row(s) checked against 11 deny.toml and 5 .cargo/audit.toml ignore
      entries". Phase 9 authored the prose against a genuine 10-entry register (commit a587e5a1,
      plan 09-02); Phase 15.1 added the eleventh, RUSTSEC-2026-0249 (smartstring), and updated the
      payload but not the prose (commit d955998a, plan 15.1-01).
    blocking: false
    why_not_blocking: >
      The enforcement script parses deny.toml and .cargo/audit.toml directly and never reads the
      prose count, so no gate is weakened and CI does not break. This is a truthfulness defect in a
      governance record, which is notable only because it is the exact defect class this milestone
      exists to eliminate.
    resolution: >
      CLOSED 2026-08-24. SECURITY-EXCEPTIONS.md's heading now reads "The eleven live suppressions"
      and its paragraph "six unmaintained-class advisories", with a dated correction note recording
      the Phase 9 / Phase 15.1 provenance and stating explicitly that no gate was ever affected.
      scripts/check-advisory-register.sh re-run after the edit: exit 0, unchanged.
  - id: "TRACE-01"
    kind: "requirements-ledger silence (not an omission)"
    affects: ["Phase 15.1 SC1-SC7"]
    file: ".planning/REQUIREMENTS.md"
    corrected_during_audit: >
      This warning was first recorded as an unclosed bookkeeping gap ("no REQ-IDs were ever
      minted"). That framing was wrong and is corrected here. Phase 15.1's plan 15.1-10 amended the
      phase's ROADMAP entry under D-00f on 2026-08-14, superseding the original "Requirements: TBD"
      line with an explicit decision to mint no identifier, and giving the reasoning: REQUIREMENTS.md
      is corpus-wide rather than phase-local, so retroactive REQ-* entries for an inserted phase
      would have no upstream requirement text and no consumer pointing at them. Minting SC1-SC7
      would have overturned a recorded decision, not closed a gap.
    evidence: >
      `grep -cE '\bSC[1-7]\b' .planning/REQUIREMENTS.md` returned 0. The seven success criteria are
      implemented (.github/workflows/, .github/rulesets/) and all seven verified achieved in
      15.1-VERIFICATION.md. The decision's traceability substitute was checked and holds: both
      .planning/decisions/0043-github-flow-trunk-and-trigger-surface.md and
      0044-branch-protection-posture.md carry populated `Downstream Consumers` sections naming
      concrete dependent artifacts.
    blocking: false
    why_not_blocking: >
      Not an orphaned requirement in the audit's sense — an orphan is a traceability row no
      VERIFICATION covers, and there are none of those. The residual issue was narrower than first
      stated: REQUIREMENTS.md's "Mapped to phases: 90 / Unmapped: 0" coverage claim read as
      complete while being silent about a phase, so a reader could not distinguish a deliberate
      choice from an oversight.
    resolution: >
      CLOSED 2026-08-24 by recording the decision rather than reversing it. REQUIREMENTS.md's
      Coverage block gains a "Phases carrying no requirement ID: 1 — Phase 15.1, deliberately" line
      and a subsection stating the D-00f decision, naming the three places a later reader traces
      instead (the seven ROADMAP success criteria, ADR-0043/0044's Downstream Consumers sections,
      and 15.1-VERIFICATION.md), and warning explicitly against closing it by minting IDs.
tech_debt:
  - phase: "03 (inherited, owner Phase 15)"
    items:
      - "WINDOWS #2: live-server code paths in crates/paladin-storage/src/redis.rs remain uncovered by unit tests; exerciser tests/integration/redis_queue_integration_test.rs requires Docker."
  - phase: 07-workspace-ground-truth-recorded-answers
    items:
      - "WINDOWS #3: ADR-0016 + PRD annotation split into two atomic commits (9e8db80, 71ea46e) rather than the requested single commit. No content impact."
      - "WINDOWS #4: acceptance criterion expecting >=3 strikethrough corrections not met by design — the third correction's target is absent from the live tree."
  - phase: 09-release-security-gate-integrity
    items:
      - "HUMAN: .project/Milestone_7-Production-Hardening/Epic_4/rustsec-remediation-plan.md was never given a dated correction banner pointing at ADR-0024 / SECURITY-EXCEPTIONS.md, unlike the parallel treatment plan 09-05 gave the SEC-02 source documents. A reader opening it directly still sees the superseded owner and the 2026-09-30 date with no forward pointer."
  - phase: 11-facade-residue-deferred-register-disposition
    items:
      - "HUMAN: FACADE-02 backstop truth — confirm user_service.rs resolves to exactly three non-overlapping owners. No single command proves a cross-document non-collision; the verifier found strong supporting evidence and abstained per the backstop protocol."
      - "HUMAN: the plan 11-01 Task 2 ADR-allocation checkpoint was auto-selected under AUTO_MODE=true rather than reasoned by a human reviewer. Transparently recorded, never human-confirmed."
      - "HUMAN: 8 judgment-tier prohibitions across 5 plans were authored descriptor-less with no check_* wiring. LLM-judge disposition is PASS for all 8; the protocol requires human review rather than a silent fold-in."
  - phase: 12-supply-chain-gate-integrity
    items:
      - "CLOSED BY PHASE 15.1 (2026-08-14): SUPPLY-01's required-status-check clause. Phase 12 recorded this open on 2026-08-09 because no CI run existed that postdated Phase 9's 2026-08-08 deletion. One does now — 15.1-CI-EVIDENCE.md:182 records `| 41 | Security Audit | required | success |`, the context resolving green as a required check on 2026-08-14."
      - "CLOSED BY PHASE 15.1 (2026-08-14): the GitHub rulesets were applied. Phase 12's `gh api rulesets` → [] finding was accurate on 2026-08-09 and superseded five days later. Three rulesets are live — 20868126 (refs/heads/main), 20868128 (refs/heads/release/*), 20868099 (refs/tags/v*) — with the full API transcript in 15.1-RULESET-EVIDENCE.md and independent read-back in 15.1-VERIFICATION.md SC2, including `branches/main --jq .protected` → true, 44 required contexts, bypass 0 / can-bypass never on the merge gate, and PR #30 (93 SUCCESS / 6 SKIPPED / 0 failures) as end-to-end proof the gate gates."
  - phase: 14-api-contract-truthfulness
    items:
      - "WINDOWS #5, #6: `cargo test --workspace` never run to completion for plans 14-01 and 14-04 — the /workspace mount hit 99% (0-13G free). Targeted per-plan verify commands all ran and passed."
      - "WINDOWS #7: 14-08 expected >=2 BREAKING lines in the dated 0.8.0 section of the root CHANGELOG.md; 1 is present. Both breaks are documented, split one per file across root and crates/paladin-web changelogs. The grep count was miscalibrated, not the changelog."
  - phase: 15-coverage-ci-quality-gates
    items:
      - "TODO (pending, owner: repo maintainer): .planning/todos/pending/2026-08-13-verify-local-coverage-reproduction.md — the testing guide's local `make services-up` + `make coverage` procedure has never been walked end-to-end on a Docker-capable machine, so the claim that it reproduces CI's 82.39% is unverified. CI's half is confirmed (run 31727496744). Deliberately carries no resolves_phase tag."
      - "INFO: docs claim 97 CLI tests; 106 are actually enumerated. Documentation-accuracy only, no CI effect."
  - phase: 15.1-git-ci-governance
    items:
      - "WINDOWS #8-#11: four acceptance criteria miscalibrated against their own greps (a pre-existing TOML parse error in an inline verify one-liner; a cargo-invocation grep matching step *names*; a survivor assertion scheduled before the deletion plan that satisfies it; a restore-keys grep matching explanatory prose comments). Each was substituted with a correct assertion and recorded."
      - "HUMAN: benchmarks.yml's `schedule: cron '0 6 * * 1'` has not been observed firing. workflow_dispatch proves the job body only; the schedule wiring is unobservable before its first Monday 06:00 UTC tick."
      - "CLOSED BY POLICY: the mandated Snyk scan on scripts/check-workflow-triggers.sh and tests/scripts/check-workflow-triggers_test.sh. Snyk was evaluated and removed 2026-08-18 (no Rust coverage); .github/instructions/security.instructions.md now forbids reintroducing it or recording a phase as blocked on it."
  - phase: 16-documentation-currency-the-architecture-gap
    items:
      - "CARRIED FORWARD: the Charm APT signing-key fingerprint behind the vhs install is owner-authorised with its independent-corroboration gap open."
      - "CARRIED FORWARD: the vhs Chromium-runtime-library devcontainer fix is unproven by a clean image rebuild (Docker unavailable in the verification environment)."
      - "CARRIED FORWARD: FR-26.3's '79 entry points' figure is not reproducible from the tree; 76 is, and the script re-runs at 76/76 exit 0. Attributed to a stale figure in 16-DOCS-03-ENTRY-POINTS.md."
      - "OUT OF SCOPE: the examples gate proves headings, not that all 76 examples compile (8 pre-existing `ignore` fences)."
  - phase: 17-additional-llm-provider-adapters
    items:
      - "WINDOWS #14: docker-compose.test.yml's ollama-test healthcheck uses `ollama list` rather than the plan's preferred curl-based /v1/models probe, because curl availability in ollama/ollama:0.3.14 could not be verified without Docker."
      - "WINDOWS #19: .project/current-exports.txt was regenerated under default features only, so the six new adapters do not appear in it and cannot be checked for public-API drift."
      - "WINDOWS #15-#18 (waived): four Snyk-scan rows across plans 17-10 through 17-16. Correctly waived by the 2026-08-18 policy removal."
      - "STRUCTURAL: the live four-vendor smoke result (Kimi, Qwen, Grok, Gemini) cannot be re-executed by any sandboxed verifier — no vendor credentials, no network egress, ever. The harness was verified to genuinely discriminate a live fetch from the curated fallback constants; the 8/8 PASS itself is accepted on three mutually corroborating internal records."
      - "DEFERRED: evaluate a Rust-capable SAST (CodeQL Rust support, or Semgrep) to replace the removed Snyk. The project has no static taint analysis for first-party Rust; cargo-audit/cargo-deny scan dependencies and clippy is a lint."
nyquist:
  compliant_phases: []
  partial_phases: []
  not_validated_phases: ["05", "07", "08", "09", "10", "11", "12", "13", "14", "15", "15.1", "16", "17"]
  missing_phases: ["06"]
  overall: not_validated
  note: >
    All 13 existing VALIDATION.md files read `status: draft`, which per #2117 means validate-phase
    never reconciled them (or they predate the `status` field). `nyquist_compliant: false` is
    therefore not authoritative on any of them — this is a coverage TODO, not a compliance failure.
    Phase 06 has no VALIDATION.md at all.
---

# Milestone v0.8.0 — Audit

**Scope:** 14 phases (05-17), 149 plans, 1,014 commits (`be2ff05..96c6dadf`).
**Verdict:** `tech_debt` — every requirement is satisfied and no critical blocker exists, but the
accumulated deferred and human-verification items warrant a review before the milestone is sealed.

**Both warnings raised by this audit (INT-01, TRACE-01) were closed on 2026-08-24** and are
retained below with their findings and resolutions intact. TRACE-01's original framing was itself
wrong and is corrected in place — see §3.

## 1. Requirements Coverage (3-source cross-reference)

Every one of the 65 milestone requirements was checked against three independent sources: its
phase's `VERIFICATION.md` status, the `requirements-completed` frontmatter of that phase's plan
SUMMARYs, and the `[x]` checkbox plus traceability row in `.planning/REQUIREMENTS.md`.

| Prefix | Phase | Count | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Status |
|---|---|---|---|---|---|---|
| VERIFY-01..06 | 5 | 6 | passed | listed | `[x]` / Complete | satisfied |
| CLOSE-01..03 | 6 | 3 | passed | listed | `[x]` / Complete | satisfied |
| ARCH-01..07 | 7 | 7 | passed | listed | `[x]` / Complete | satisfied |
| DEBT-01..05 | 8 | 5 | passed | listed | `[x]` / Complete | satisfied |
| SEC-01..05 | 9 | 5 | passed | listed | `[x]` / Complete | satisfied |
| HARD-01..07 | 10 | 7 | passed | listed | `[x]` / Complete | satisfied |
| FACADE-01..04 | 11 | 4 | passed | listed | `[x]` / Complete | satisfied |
| SUPPLY-01..03 | 12 | 3 | passed | listed | `[x]` / Complete | satisfied |
| ORCH-01..05 | 13 | 5 | passed | listed | `[x]` / Complete | satisfied |
| WEB-01..04 | 14 | 4 | passed | listed | `[x]` / Complete | satisfied |
| PIPE-01..05 | 15 | 5 | passed | listed | `[x]` / Complete | satisfied |
| DEFER-01..03 | 15 | 3 | passed | listed | `[x]` / Complete | satisfied |
| DOCS-01..04 | 16 | 4 | passed | listed | `[x]` / Complete | satisfied |
| PROV-01..04 | 17 | 4 | passed | listed | `[x]` / Complete | satisfied |
| **SC1-SC7** | **15.1** | **7** | **passed** | **listed** | **absent** | **untracked — see TRACE-01** |

**65/65 satisfied. Zero unsatisfied. Zero orphaned** — no traceability row lacks a covering
VERIFICATION.

Two entries deserve their footnote rather than a silent tick:

- **PROV-01**'s checkbox was deliberately left unticked during execution ("several Phase 17 plans
  share these IDs — not ticked here per worktree-mode instruction") and ticked at phase close. Its
  `[x]` is now backed by 17-VERIFICATION.md's fourth pass.
- **PIPE-01** was adjudicated at phase close rather than per-plan, and **PIPE-03/PIPE-05**'s
  local-reproduction halves are deferred to the pending todo listed under tech debt. The CI half of
  the coverage claim is confirmed (run 31727496744, 82.39% lines against an 82 floor); the local
  half is documented but unwalked.

**TRACE-01** — Phase 15.1 shipped seven verified success criteria carrying no REQ-IDs, so the
65/65 figure above is accurate for what it counts and silent about them. This is a decision, not an
omission (see §3), and it is now recorded in `REQUIREMENTS.md` rather than reversed.

## 2. Phase Verification

All 14 phases carry a `VERIFICATION.md` with `status: passed`. No unverified phase.

| Phase | Score | Notes |
|---|---|---|
| 05 Milestone 2-3 Ground Truth | 6/6 | One gap found and closed inline (`0c62dff`) — the phase's own VERIFY-01..06 checkboxes were never flipped. |
| 06 Verified Gap Closure | 10/10 | Re-verified from `gaps_found` 8/10. Grove no-fallback guarantee now reachable from `execute()`. |
| 07 Workspace Ground Truth | 7/7 | Zero product code touched, independently confirmed across the full commit range. |
| 08 Verified Defect Closure | 7/7 | One gap closed by `887bd12`. |
| 09 Release & Security Gate Integrity | 5/5 | Guards broken and restored by the verifier, not merely re-run. One doc-consistency item routed to human. |
| 10 Milestone 7-8 Ground Truth | 7/7 | `cargo doc` zero-warning gate honestly recorded as then-failing (20 warnings) rather than concealed. |
| 11 Facade Residue Disposition | 9/9 | Three designed escalation points routed to human; none produced an adverse finding. |
| 12 Supply-Chain Gate Integrity | 8/9 | One clause deliberately not marked passing — it could not be closed from inside the environment. |
| 13 Milestone 9-12 Ground Truth | 7/7 | ORCH-03 / CR-01 scope ratification routed to human — **now moot, see below**. |
| 14 API Contract Truthfulness | 4/4 | Re-verified from `gaps_found` 3/4. Closed by `f9f493e`. |
| 15 Coverage & CI Quality Gates | 6/6 | Coverage floor single-sourced across ci.yml, Makefile and ADR-0006. |
| 15.1 Git & CI Governance | 7/7 | Amended 2026-08-15 after a post-verification escape; fix merged in PR #31 (`6e84972`). |
| 16 Documentation Currency | 4/4 | Re-verified from `gaps_found` 3/4. Fabricated CI-workflow sections removed. |
| 17 Additional LLM Providers | 16/16 | Fourth pass. Re-verified from `human_needed`; all three record gaps independently confirmed closed. |

**Phase 13's open human-verification item is closed by the tree.** It asked a human to ratify
leaving `crates/doc-examples/src/sidecar.rs` building an unprefixed `/agents/{agent}/execute` URL,
which would have made anyone copying the rendered docs page write a client that 404s. This audit
checked directly: `sidecar.rs:25`, `:34` and `:54` all now carry the `/v1/` prefix, and
`docs/src/deployment-topologies/sidecar.md:29` agrees. The defect was fixed after Phase 13 handed
it to Phase 14. No ratification is needed — the thing that would have been ratified no longer
exists.

## 3. Cross-Phase Integration

Eight wiring points were traced across the code-touching phases (06, 08, 09, 12, 14, 15, 15.1, 17).
**All eight are connected. Zero orphaned exports. Zero broken flows.**

| # | Wiring point | Requirements | Evidence |
|---|---|---|---|
| 1 | Grove routing config → `route_task()` → `execute()` | CLOSE-01..03 | `grove_service.rs:185,189,322-324` |
| 2 | `TokenUsage` single canonical definition + re-exports | DEBT-05, ARCH-03(c) | `paladin-core/.../token_usage.rs:13`; re-exports at `battalion/mod.rs:494`, `herald.rs:28`, `llm_analysis_service.rs:51`, `llm_port.rs:742` |
| 3 | `.cargo/audit.toml` ↔ `deny.toml` ↔ register ↔ guard | SEC-01, SUPPLY-01/02 | `check-advisory-register.sh` exit 0; one `security-audit` job at `ci.yml:83`, no duplicate |
| 4 | Sidecar route prefix consistency | ORCH-03, WEB-* | `sidecar.rs:25,34,54` and `sidecar.md:29` agree on `/v1/` |
| 5 | Bearer-token mechanism + multi-replica store warning | WEB-01, WEB-02 | `k8s/server/deployment.yaml:14` replicas=2; `paladin-server.rs:148,190,339` — warning defined, emitted, and asserted by a passing test |
| 6 | Coverage floor → CI job → required status check | PIPE-02, DEFER-* | `coverage.sh:35,101`; `ci.yml:637,735`; ruleset context `"Coverage"` matches the job `name:` exactly |
| 7 | Adapters → `provider_factory` registry → features → docs | PROV-01..04 | `provider_factory.rs:240-296` behind matching `#[cfg(feature)]`; `Cargo.toml:22-27`; defaults match `.env.example`, `config.example.yml`, README |
| 8 | ADR numbering across 48 decision files | all record phases | No duplicate numeric prefix |

Two details worth recording rather than burying:

- **Every required status check in `.github/rulesets/protect-main-branch.json` resolves to a real
  job.** Matrix-derived contexts (`Unit Tests (stable)`, `Unit Tests (beta)`) trace to
  `ci.yml:227`'s `matrix.rust-version`; `Build & Test (*)` traces to `feature-flags.yml`;
  `pre-commit run --all-files` traces to `pre-commit.yml`. The ruleset is correct — it is simply
  not applied (see Phase 12's tech debt).
- **The `cargo doc` warning bar resolved in DOCS-03's favour, and stricter.** `ci.yml:63` enforces
  zero warnings, against the 6-warning state HARD-07 measured and inherited. That is what the
  cross-phase coupling table said should happen — Phase 16, not Phase 10, sets the final number.

### INT-01 — the one integration warning

`SECURITY-EXCEPTIONS.md:44-49` says "The ten live suppressions" and "five unmaintained-class
advisories". The register holds **eleven** — five vulnerability-class and six unmaintained.

This is cross-phase, not a single-phase slip. Phase 9 authored the prose against a register that
genuinely held ten (`a587e5a1`, plan 09-02). Phase 15.1 added the eleventh — `RUSTSEC-2026-0249`,
smartstring, transitive via rxml/minidom under rust-s3's optional `s3` feature — and updated the
machine-readable payload without updating the prose above it (`d955998a`, plan 15.1-01).

Nothing is weakened: `scripts/check-advisory-register.sh` parses the TOML directly and reports
"11 register row(s) checked against 11 deny.toml and 5 .cargo/audit.toml ignore entries; all
clauses satisfied", exit 0. The suppression set is governed correctly. What was wrong is the
register's account of itself — the precise defect class this milestone was convened to eliminate,
present in a security governance record.

**CLOSED 2026-08-24.** The heading now reads "The eleven live suppressions" and the paragraph "six
unmaintained-class advisories", followed by a dated correction note recording the Phase 9 /
Phase 15.1 provenance and stating that no gate was ever affected. The guard was re-run after the
edit and still exits 0 with the same 11/11/5 line.

### TRACE-01 — the traceability silence, and why it was not closed the way first proposed

Phase 15.1 carries no requirement identifier. The first pass of this audit recorded that as an
unclosed bookkeeping gap and proposed minting SC1-SC7 as REQ-IDs. **That was wrong**, and the
correction matters more than the finding did.

Plan `15.1-10` amended the phase's ROADMAP entry under D-00f on 2026-08-14, superseding the
original `Requirements: TBD — minted during /gsd-discuss-phase 15.1` line with a deliberate
decision to mint none, and recording why: `REQUIREMENTS.md` is corpus-wide rather than phase-local,
so retroactive `REQ-*` entries for an inserted phase would carry no upstream requirement text and
have no consumer pointing at them. Minting the IDs would have overturned a recorded decision while
appearing to tidy up after one.

The decision's traceability substitute was checked and holds: `ADR-0043` and `ADR-0044` both carry
populated `Downstream Consumers` sections naming concrete dependents — the trigger-policy register
in `docs/src/contributing/branching-model.md`, `scripts/check-workflow-triggers.sh` and its
44-context `CLAUSE_CONTEXT` assertion, `release.yml`'s `verify-tag-source` ancestry guard, and
`docs/src/appendix/branch-protection.md`.

The residual issue was narrower than first stated: `REQUIREMENTS.md`'s "Mapped to phases: 90 /
Unmapped: 0" read as complete while being silent about an entire phase, so a reader could not tell
a choice from an oversight.

**CLOSED 2026-08-24** by recording the decision rather than reversing it. The Coverage block gains
a `Phases carrying no requirement ID: 1 — Phase 15.1, deliberately` line and a subsection stating
the D-00f decision, naming the three places a later reader traces instead, and warning explicitly
against closing it by minting IDs.

## 4. Nyquist Coverage

| Phase | VALIDATION.md | status | Classification | Action |
|---|---|---|---|---|
| 05, 07, 08, 09, 10, 11, 12, 13, 14, 15, 15.1, 16, 17 | exists | `draft` | NOT-VALIDATED | `/gsd-validate-phase <N>` |
| 06 | missing | — | MISSING | `/gsd-validate-phase 6` |

All 13 existing files read `status: draft`, meaning validate-phase never reconciled them — or they
predate the `status` field entirely (#2117). On a `draft` file `nyquist_compliant: false` is not
authoritative, so **none of these is a compliance failure**; they are a coverage TODO. Re-running
validate-phase promotes each file to `status: validated` and yields the real verdict. Phase 06 has
no VALIDATION.md at all.

## 5. Tech Debt Summary

**25 recorded items across 10 phases.** Full detail in the frontmatter. Several items bundle
more than one underlying row, so the counts below characterise the debt rather than partition it:

| Kind | Count | Nature |
|---|---|---|
| Open WINDOWS.md rows | 12 of 21 | Recorded deviations and unrun verifies, each with a reason |
| Waived WINDOWS.md rows | 4 of 21 | All Snyk rows, correctly closed by the 2026-08-18 policy removal |
| Fixed WINDOWS.md rows | 5 of 21 | Closed during the milestone |
| Human-verification items | 8 | Judgment calls and environment-bound checks routed rather than waved through |
| Environment-bound (Docker / credentials / live CI) | 6 | Structurally unresolvable in a sandbox |
| Pending todos | 1 | Local coverage reproduction, owner: repo maintainer |

### Two Phase 12 items were already closed by Phase 15.1

Corrected 2026-08-24, after this audit's first pass carried both forward as open. Phase 12 verified
on **2026-08-09** and recorded both items accurately for that date. Phase 15.1 executed on
**2026-08-14** and closed both. The audit's first pass read Phase 12's VERIFICATION.md without
checking whether a later phase had overtaken it — the precise failure mode a milestone audit exists
to catch, so it is recorded here rather than quietly fixed.

- **The GitHub rulesets are applied, and main is protected.** Not "committed but unapplied". Three
  rulesets are live: `20868126` (`refs/heads/main`), `20868128` (`refs/heads/release/*`),
  `20868099` (`refs/tags/v*`). `15.1-RULESET-EVIDENCE.md` carries the full creation transcript;
  `15.1-VERIFICATION.md` SC2 re-read them from the API independently of that transcript and
  confirms `branches/main --jq .protected` → `true`, 44 required contexts, `bypass_actors: 0` with
  `current_user_can_bypass: never` on the merge gate, and a deliberate bypass retained on the tag
  ruleset so releases can still be cut. PR #30 merged with a 93-SUCCESS / 6-SKIPPED / 0-failure
  rollup — live proof the gate gates, not just that it exists.
- **SUPPLY-01's required-status-check clause is confirmed.** `15.1-CI-EVIDENCE.md:182` records
  `| 41 | Security Audit | required | success |` — the context Phase 12 could find no qualifying
  run for, resolving green as a required check on 2026-08-14, six days after Phase 9's deletion.

### The one item that genuinely remains

**There is no static taint analysis for first-party Rust.** Snyk was measured to produce 0 findings
on a Rust probe carrying a hardcoded credential, command injection, path traversal and SQL
injection — while the same four in JavaScript returned 3 findings, confirming the scanner worked —
and was removed on 2026-08-18. `cargo-audit` and `cargo-deny` scan dependencies; clippy is a lint.
Evaluating CodeQL's Rust support or Semgrep is open work, and until it lands the manual
credential-handling review in `.github/instructions/security.instructions.md` is the only control.
This is correctly recorded as a known gap rather than papered over.

## 6. Bookkeeping Defects Found by This Audit

Neither blocks the milestone; both should be fixed as part of closing it.

1. **The ROADMAP `## Milestones` table is stale.** The Milestone 4-6 (phases 7-8), Milestone 7-8
   (9-11) and Milestone 9-12 (12-16) blocks, and the Provider Expansion (17-) row, all read
   "Not started" against phases that are every one of them `[x]`. Already flagged in STATE.md as
   the milestone's last outstanding decision.
2. **The milestone carries two version identities.** STATE.md and the ROADMAP say `v0.7.2`; all
   twelve Cargo manifests and the dated CHANGELOG section say `0.8.0`. This audit is filed under
   0.8.0 per the operator's decision; `/gsd-complete-milestone` should be given the same label and
   the ROADMAP/STATE cells reconciled to it.

---

*Audited: 2026-08-24 · Integration check: gsd-integration-checker · Findings INT-01 and the Phase
13 sidecar closure independently re-derived by the auditor before recording.*
