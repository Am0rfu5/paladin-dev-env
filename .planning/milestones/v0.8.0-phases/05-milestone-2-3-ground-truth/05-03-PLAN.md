---
phase: 05-milestone-2-3-ground-truth
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/decisions/0011-vision-port-surfaces.md
  - .planning/decisions/0012-live-api-test-key-behaviour.md
autonomous: true
requirements: [VERIFY-04, VERIFY-06]
must_haves:
  truths:
    - "`.planning/decisions/0011-vision-port-surfaces.md` records both vision surfaces as coexisting and intended long-term, names `VisionPort` (`crates/paladin-ports/src/output/vision_port.rs:47`) as the recommended entry point for application code and `VisionCapableLlm` (`crates/paladin-ports/src/output/vision_llm_port.rs:52`) as the adapter-author surface, and plans no migration."
    - "ADR-0011 records the encryption-at-rest disposition as a third verdict — built, self-tested, and never wired — not as shipped and not as dropped, and states explicitly that `EncryptionService`, `DataRetentionPolicy` and `VisionError::EncryptionError` have zero consumers outside `src/infrastructure/security/`."
    - "ADR-0011's `## Code Locations` cites `SecureData::is_expired` at `src/infrastructure/security/encryption.rs:95` and `DataRetentionPolicy::should_retain` at `src/infrastructure/security/encryption.rs:131` as two methods on two different types — the citation-precision correction 05-RESEARCH.md Pitfall 2 identified against CONTEXT.md D-10."
    - "`.planning/decisions/0012-live-api-test-key-behaviour.md` records that the shipped panic stands and `require_api_key` is not changed to skip, justified by the double gate rather than by overruling either PRD position."
    - "ADR-0012 names both missing-key arms of `require_api_key` (`tests/integration/llm_live_api_tests.rs:63-88`) — environment variable absent, and environment variable present but empty — and records that both panic while the doc comment at :63 says the function skips. [edge: VERIFY-06/empty]"
    - "ADR-0012 records that the double gate — `#[cfg(feature = \"live-api-tests\")]` at `tests/integration/mod.rs:34-35` plus 13 `#[ignore]` attributes in `tests/integration/llm_live_api_tests.rs`, compiled only via `tests/lib.rs:61` — is what makes a default parallel `cargo test --workspace` run never reach `require_api_key`, so the panic cannot abort an unrelated concurrently-running test in a default run. [edge: VERIFY-06/concurrency]"
    - statement: "ADR-0012 states exactly which environment-variable values the shipped `require_api_key` treats as missing — absent and empty-string — and records that a whitespace-only or otherwise non-empty-but-invalid key is treated as present by the shipped helper, without proposing a change to that behaviour. Whether the helper's notion of emptiness is byte-level, character-level or trimmed is read from the shipped body, not assumed."
      verification: backstop
  artifacts:
    - ".planning/decisions/0011-vision-port-surfaces.md — new ADR, Code Conformance `must change`, executed by Phase 6 CLOSE-03 (D-00c: every ADR carries a `Code Conformance` field valued `conforms` or `must change`, and where `must change`, names the requirement that executes it)"
    - ".planning/decisions/0012-live-api-test-key-behaviour.md — new ADR, Code Conformance `must change` (doc comment only), executed by Phase 6 CLOSE-03 (D-00c)"
  key_links:
    - "ADR-0011 → ledger plan 05-08 — the `REQ-vision-security-encryption` row (Epic 13) changes from an absence claim to `present, unproven` with the unwired-consumer finding named; 05-08 cites this ADR"
    - "ADR-0011 and ADR-0012 → Phase 6 CLOSE-03 — the two doc-level code consequences this phase records but does not execute"
    - "ADR-0012 → ledger plan 05-07 — the `REQ-provider-live-api-tests` row (Epic 24) cites this ADR for the contested skip semantics"
  prohibitions:
    - "ADR-0011 must NOT record the encryption capability as shipped or satisfied without stating that it has zero consumers outside `src/infrastructure/security/`. Recording a security capability as present-and-wired when it is unwired would cause a downstream reader to skip real security work."
    - "No plan in this phase may edit any `.rs` file, `Cargo.toml`, or any file under `.github/workflows/`. In particular, do NOT correct `require_api_key`'s doc comment here — that single-line edit is Phase 6 CLOSE-03's, and taking it in this phase would leave CLOSE-03 with an empty scope it was sized against."
    - "Neither ADR may plan or recommend a migration between the two vision surfaces on the strength of the PRD conflict alone (VERIFY-04's own wording)."
---

<objective>
Give VERIFY-04 and VERIFY-06 exactly one recorded answer each, as ADR-0011 and ADR-0012.

Purpose: both questions look like open contradictions in the ingest record and are not. The vision
"conflict" is two surfaces at two layers reached by two entry points, and the ingest record's claim
that Epic 13's encryption requirement is absent from the tree is verified false on all three counts.
The live-API "deadlock" dissolves once the double gate above `require_api_key` is read. Recording
these correctly is what stops Phase 6 doing invented work.
Output: two ADRs in the shipped seven-H2 shape, both `must change`, both naming Phase 6 CLOSE-03 as
the executing requirement.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md
@.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md
@.planning/phases/05-milestone-2-3-ground-truth/05-RESEARCH.md
</context>

<flagged_assumptions>
**VERIFY-04 / unclassified edge — left `unresolved`, surfaced rather than auto-resolved.**

The deterministic edge probe classified one VERIFY-04 row as `unclassified` ("review manually"). Per
the spec-less probe fallback's `--auto` rules an `unclassified` row is never auto-`backstop`ped and
never silently dropped, so it is recorded here as a flagged planner assumption for a human to
confirm or reject:

> **Assumption:** VERIFY-04's remaining open question is a *disposition* question, not a
> *measurement* question — the phase's job is to record that the encryption capability is built and
> unwired and to hand the wiring decision to Phase 6 CLOSE-03 (D-11), rather than to determine
> whether image bytes *ought* to be encrypted at rest. The planner could not derive a defensible
> acceptance criterion for the latter from any source artefact, because no source artefact states a
> security requirement level for vision image data at rest.
>
> **If this assumption is wrong** — i.e. if a reader believes Phase 5 must itself decide whether
> encryption-at-rest is required rather than deferring it — then ADR-0011 needs an additional
> `## Decision` clause and CONTEXT.md D-11 needs revisiting. Flag it at
> `/gsd-verify-work` rather than resolving it inside the executor.

This is the single `unresolved` item of the 20; the other 19 are dispositioned in plan 05-01's
`<probe_fallback_disposition>` table.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Author ADR-0011 — vision port surfaces and the encryption-at-rest disposition</name>
  <files>.planning/decisions/0011-vision-port-surfaces.md</files>
  <read_first>
    - `.planning/decisions/0004-temperature-validation.md` — read in full; the closest existing ADR whose `Code Conformance` is `must change` and which names a downstream executing requirement, and the shape source for all seven H2 headings
    - `.planning/decisions/PROMOTION.md` — the numbering index and required heading set
    - `.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md` §"`.planning/decisions/0010-*.md` … `0012-*.md` (new ADRs)"
    - `crates/paladin-ports/src/output/vision_port.rs` — read around line 47 for the `VisionPort` trait's actual signature
    - `crates/paladin-ports/src/output/vision_llm_port.rs` — read around line 52 for `VisionCapableLlm: LlmPort`
    - `src/application/services/paladin/paladin_builder.rs` around line 517 — `enable_vision`
    - `src/application/services/paladin/paladin_execution_service.rs` around line 517 — `execute_with_vision`
    - `crates/paladin-core/src/platform/container/vision.rs` lines 189-214 — `VisionError`, including the `EncryptionError` variant at :210-212
    - `src/infrastructure/security/encryption.rs` — read the `impl` blocks, not just the module doc comment: `SecureData` (`#[derive(Zeroize, ZeroizeOnDrop)]` at :68, `is_expired` at :95), `DataRetentionPolicy` (struct :106, `should_retain` :131), `EncryptionService` (`encrypt_image_data` :200, `decrypt_image_data` :217)
    - `src/infrastructure/security/mod.rs:44` and `src/infrastructure/mod.rs:47` — the public re-export and the module wiring
    - `Cargo.toml` lines 130-140 — `chacha20poly1305` and `zeroize`, both unconditional
    - `.planning/REQUIREMENTS.md` lines 454-465 — VERIFY-04 in full
    - `.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md` §D-09, D-10, D-11, D-19
    - `.planning/phases/05-milestone-2-3-ground-truth/05-RESEARCH.md` §"Pitfall 2" — the citation-precision correction to apply
  </read_first>
  <precondition>`src/infrastructure/security/encryption.rs` and `crates/paladin-core/src/platform/container/vision.rs` exist at the cited line numbers in the working tree. 05-RESEARCH.md re-verified them on 2026-08-04; if a later commit moved them, re-derive the line numbers with `grep -n` before citing and record the correction in the SUMMARY rather than citing a stale line.</precondition>
  <action>
Create `.planning/decisions/0011-vision-port-surfaces.md` with no YAML frontmatter, H1
`# ADR-0011: Vision port surfaces and the encryption-at-rest disposition`, and exactly the seven H2
headings `## Status`, `## Context`, `## Decision`, `## Considered Options`, `## Code Locations`,
`## Code Conformance`, `## Downstream Consumers` in that order. `## Status` body is `Accepted`
followed by `**Date:** 2026-08-04`.

Before writing any citation, run
`grep -rln "EncryptionService\|DataRetentionPolicy\|VisionError::EncryptionError" src/ crates/ | grep -v "infrastructure/security"`
and record its actual output. The zero-consumer finding is the load-bearing claim of this ADR and
must be re-derived in this task, not transcribed.

`## Context` states two things. First, that the ingest record treats the two vision surfaces as a
PRD conflict requiring a migration decision, when they sit at different layers and are reached by
different entry points. Second, that REQUIREMENTS.md's `REQ-vision-security-encryption` row records
the capability as absent from the tree on three counts — no encryption-at-rest artefact, no
zeroization, no retention policy, and no `EncryptionError` variant — and that all three counts are
verified false, with the fourth finding (zero consumers) being what the ingest record actually
missed.

`## Decision` states, as separate paragraphs:
(1) Both surfaces are intended long-term; neither is legacy; no migration is planned.
(2) The entry-point guidance, stated explicitly rather than left to inference:
`VisionPort` is the recommended entry point for application code, reached via
`PaladinExecutionService::execute_with_vision`; `VisionCapableLlm: LlmPort` is the adapter-author
surface, implemented when adding a vision-capable provider, reached via
`PaladinBuilder::enable_vision`.
(3) The encryption disposition: built, self-tested, and never wired — a third verdict that is neither
shipped nor dropped — with the zero-consumer grep result quoted.
(4) That the consequence is a small, concrete wiring question owned by Phase 6 CLOSE-03 (either wire
`EncryptionService::encrypt_image_data` into the vision path, or record that the service is a
consumer-facing utility the framework deliberately does not impose), and that no new phase and no new
requirement is created for it.

`## Considered Options` must be a bulleted list. Include at least: deprecating `VisionCapableLlm` in
favour of `VisionPort` — rejected, both ship deliberately at different layers and VERIFY-04 forbids
planning a migration on the strength of the PRD conflict alone; deprecating `VisionPort` in favour of
`VisionCapableLlm` — rejected for the same reason; recording the encryption requirement as dropped —
rejected, the capability demonstrably ships; recording it as shipped — rejected, it has zero
consumers so no vision-path image bytes are encrypted at rest and recording it as shipped would cause
a reader to skip real work.

`## Code Locations` must be a bulleted list, one entry per citation with the line number and what is
there. It must include, as separate entries: `SecureData::is_expired` at
`src/infrastructure/security/encryption.rs:95` and `DataRetentionPolicy::should_retain` at
`src/infrastructure/security/encryption.rs:131` — two methods on two different types. Do not conflate
them into a single `DataRetentionPolicy::is_expired` entry; there is no such method in the tree, and
05-RESEARCH.md Pitfall 2 identified this as a defect in CONTEXT.md D-10's own citation. Also cite the
two port files, the two entry points, `VisionError::EncryptionError` at
`crates/paladin-core/src/platform/container/vision.rs:210-212`, `encrypt_image_data` at `:200`,
`decrypt_image_data` at `:217`, the `#[derive(Zeroize, ZeroizeOnDrop)]` at `:68`, the re-export at
`src/infrastructure/security/mod.rs:44`, the module wiring at `src/infrastructure/mod.rs:47`, and the
two unconditional dependency lines in `Cargo.toml`.

`## Code Conformance` is `must change`, followed by a paragraph naming **CLOSE-03 in Phase 6** as the
requirement that executes it, and stating that the vision half is documentation (entry-point guidance
in rustdoc, no removal) and the encryption half is a recorded wiring decision. State that nothing in
this phase edits Rust source.

`## Downstream Consumers` must be a bulleted list naming Phase 6 CLOSE-03, Phase 5 ledger plan 05-08
(which rewrites the `REQ-vision-security-encryption` row), and any adapter author choosing a vision
entry point.

Write the medieval-military ubiquitous language consistently — Paladin, Battalion, Sentinel — per
this project's CLAUDE.md working agreements.
  </action>
  <verify>
    <automated>test -f .planning/decisions/0011-vision-port-surfaces.md && [ "$(grep -c '^## ' .planning/decisions/0011-vision-port-surfaces.md)" = "7" ] && grep -q 'encryption.rs:95' .planning/decisions/0011-vision-port-surfaces.md && grep -q 'encryption.rs:131' .planning/decisions/0011-vision-port-surfaces.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/decisions/0011-vision-port-surfaces.md` succeeds
    - `head -1 .planning/decisions/0011-vision-port-surfaces.md` begins `# ADR-0011: ` and the file's first line is not `---`
    - `grep -n '^## ' .planning/decisions/0011-vision-port-surfaces.md` prints exactly seven headings in the order `## Status`, `## Context`, `## Decision`, `## Considered Options`, `## Code Locations`, `## Code Conformance`, `## Downstream Consumers`
    - `sed -n '/^## Considered Options/,/^## Code Locations/p' .planning/decisions/0011-vision-port-surfaces.md | grep -c '^- '` is at least `4`
    - `sed -n '/^## Code Locations/,/^## Code Conformance/p' .planning/decisions/0011-vision-port-surfaces.md | grep -c '^- '` is at least `10`
    - `grep -c 'encryption.rs:95' .planning/decisions/0011-vision-port-surfaces.md` is at least `1` and the same line names `SecureData`
    - `grep -c 'encryption.rs:131' .planning/decisions/0011-vision-port-surfaces.md` is at least `1` and the same line names `DataRetentionPolicy`
    - `grep -c 'zero consumers' .planning/decisions/0011-vision-port-surfaces.md` is at least `1`
    - `grep -c 'vision_port.rs' .planning/decisions/0011-vision-port-surfaces.md` is at least `1` and `grep -c 'vision_llm_port.rs' .planning/decisions/0011-vision-port-surfaces.md` is at least `1`
    - `grep -c 'execute_with_vision' .planning/decisions/0011-vision-port-surfaces.md` is at least `1` and `grep -c 'enable_vision' .planning/decisions/0011-vision-port-surfaces.md` is at least `1`
    - `sed -n '/^## Code Conformance/,/^## Downstream Consumers/p' .planning/decisions/0011-vision-port-surfaces.md | grep -c 'must change'` equals `1`
    - `sed -n '/^## Code Conformance/,/^## Downstream Consumers/p' .planning/decisions/0011-vision-port-surfaces.md | grep -c 'CLOSE-03'` is at least `1`
    - The `grep -rln` zero-consumer command named in the action was executed during this task and its actual output is what the ADR reports
    - `git diff --stat -- '*.rs'` is empty for this task
  </acceptance_criteria>
  <done>ADR-0011 exists in the seven-H2 shape, records both surfaces as coexisting with explicit entry-point guidance, records the encryption capability as built-self-tested-and-unwired with a freshly derived zero-consumer result, cites `SecureData::is_expired` and `DataRetentionPolicy::should_retain` as two distinct methods, and declares `must change` executed by CLOSE-03.</done>
</task>

<task type="auto">
  <name>Task 2: Author ADR-0012 — live-API-test missing-key behaviour</name>
  <files>.planning/decisions/0012-live-api-test-key-behaviour.md</files>
  <read_first>
    - `.planning/decisions/0004-temperature-validation.md` — the `must change` ADR exemplar and heading shape
    - `.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md` §"`.planning/decisions/0010-*.md` … `0012-*.md` (new ADRs)"
    - `tests/integration/llm_live_api_tests.rs` lines 55-95 — `require_api_key`, its doc comment at :63, and both match arms; read the whole function body, not just the comment
    - `tests/integration/mod.rs` lines 30-40 — the `#[cfg(feature = "live-api-tests")] pub mod llm_live_api_tests;` gate at :34-35
    - `tests/lib.rs` line 61 — `pub mod integration;`, the autodiscovered `lib` test target that makes the module compile at all
    - `Cargo.toml` around line 265 — `live-api-tests = []`
    - `.planning/REQUIREMENTS.md` lines 478-483 — VERIFY-06 in full
    - `.planning/INGEST-CONFLICTS.md` warning 17
    - `.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md` §D-17, D-18, D-19
  </read_first>
  <precondition>`tests/integration/llm_live_api_tests.rs`, `tests/integration/mod.rs` and `tests/lib.rs` exist with the double gate intact. Confirm with `grep -c '#\[ignore\]' tests/integration/llm_live_api_tests.rs` (expected 13) and `grep -n 'cfg(feature = "live-api-tests")' tests/integration/mod.rs` before citing; if either count or line differs from the recorded figure, cite the current one and record the drift in the SUMMARY.</precondition>
  <action>
Create `.planning/decisions/0012-live-api-test-key-behaviour.md` with no YAML frontmatter, H1
`# ADR-0012: Live-API-test missing-key behaviour`, and exactly the seven H2 headings in the standard
order. `## Status` body is `Accepted` followed by `**Date:** 2026-08-04`.

Re-derive the two gate facts in this task before citing them: run
`grep -c '#\[ignore\]' tests/integration/llm_live_api_tests.rs` and
`grep -n 'cfg(feature = "live-api-tests")' tests/integration/mod.rs`, and quote the actual results.

`## Context` states the apparent deadlock — Epic 23 FR-23.4.4 and Epic 24 US-24.7 both require
graceful skip with a clear message; the post-Epic-24 cleanup deliberately reversed this so tests
"properly FAIL when keys are missing"; both positions are defensible and the reversal was conscious,
so precedence cannot settle it — and then states the fact neither PRD's author traced: the suite is
double-gated.

`## Decision` states, as separate paragraphs:
(1) The shipped panic stands. `require_api_key` is not changed to skip.
(2) The justification: the graceful skip both PRDs require is supplied by the gating, not by the
helper. A default `cargo test --workspace` run — no feature flag, no `--ignored` — never compiles or
executes this module, so a run with no API keys already skips cleanly. The panic fires only when a
developer has explicitly opted into both the feature and `--ignored`, at which point a silent skip
would be a false pass, which is exactly the failure the post-Epic-24 cleanup reversed it for. Both
positions' intent is preserved rather than one being overruled.
(3) The missing-key semantics, stated precisely from the shipped body you read: name each arm the
function matches on and what each does. State which environment-variable values the helper treats as
missing — absent, and present-but-empty — and record what the shipped body does with a
whitespace-only or otherwise non-empty-but-invalid value, reading the actual emptiness check rather
than assuming it. Do not propose changing any of this.
(4) The concurrency consequence: because the double gate keeps the module out of a default run, the
panic cannot abort an unrelated test running concurrently in a default parallel `cargo test`
invocation.
(5) The one real defect: the doc comment at `tests/integration/llm_live_api_tests.rs:63` says the
function skips while both arms panic. The panic messages themselves are already correct — they tell
the reader how to skip. The doc comment is the only thing that lies about this harness.

`## Considered Options` must be a bulleted list. Include at least: changing `require_api_key` to skip
per the two PRDs — rejected, it would turn an opted-in live-API run into a false pass; keeping the
panic and leaving the doc comment as-is — rejected, the comment actively misinforms; recording no
answer — rejected, VERIFY-06 requires one recorded answer and the shipped harness matching it.

`## Code Locations` must be a bulleted list citing: `tests/integration/llm_live_api_tests.rs:63` (the
doc comment), the two panic arms with their line numbers as you read them,
`tests/integration/mod.rs:34-35` (the feature gate), `tests/lib.rs:61` (the compiling target),
`Cargo.toml:265` (`live-api-tests = []`), and the `#[ignore]` count you measured.

`## Code Conformance` is `must change`, followed by a paragraph naming **CLOSE-03 in Phase 6** as the
executing requirement and stating that the change is documentation only: correct the doc comment, and
state the double gate as the skip mechanism in the module header. No behavioural change. State
explicitly that Phase 5 does not make this edit.

`## Downstream Consumers` must be a bulleted list naming Phase 6 CLOSE-03, Phase 5 ledger plan 05-07
(which cites this ADR on the `REQ-provider-live-api-tests` and `REQ-cli-tiered-environment-testing`
rows), and any developer running the live-API suite.

Commit both of this plan's ADRs in a single commit at the end, with a Bash timeout of at least
300000 ms — `.pre-commit-config.yaml` sets `always_run: true` on `cargo-fmt` and `cargo-clippy` so
they compile the workspace even for a markdown-only commit. Do not pass `--no-verify`.
  </action>
  <verify>
    <automated>test -f .planning/decisions/0012-live-api-test-key-behaviour.md && [ "$(grep -c '^## ' .planning/decisions/0012-live-api-test-key-behaviour.md)" = "7" ] && grep -q 'tests/lib.rs:61' .planning/decisions/0012-live-api-test-key-behaviour.md && [ "$(grep -c '#\[ignore\]' tests/integration/llm_live_api_tests.rs)" = "13" ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/decisions/0012-live-api-test-key-behaviour.md` succeeds
    - `head -1 .planning/decisions/0012-live-api-test-key-behaviour.md` begins `# ADR-0012: ` and the file's first line is not `---`
    - `grep -n '^## ' .planning/decisions/0012-live-api-test-key-behaviour.md` prints exactly seven headings in the standard order
    - `sed -n '/^## Considered Options/,/^## Code Locations/p' .planning/decisions/0012-live-api-test-key-behaviour.md | grep -c '^- '` is at least `3`
    - `sed -n '/^## Code Locations/,/^## Code Conformance/p' .planning/decisions/0012-live-api-test-key-behaviour.md | grep -c '^- '` is at least `5`
    - `grep -c 'tests/integration/mod.rs:34' .planning/decisions/0012-live-api-test-key-behaviour.md` is at least `1`
    - `grep -c 'tests/lib.rs:61' .planning/decisions/0012-live-api-test-key-behaviour.md` is at least `1`
    - `grep -c 'llm_live_api_tests.rs:63' .planning/decisions/0012-live-api-test-key-behaviour.md` is at least `1`
    - `grep -c '13' .planning/decisions/0012-live-api-test-key-behaviour.md` is at least `1` and the `#[ignore]` count it reports equals the value `grep -c '#\[ignore\]' tests/integration/llm_live_api_tests.rs` returns
    - `sed -n '/^## Code Conformance/,/^## Downstream Consumers/p' .planning/decisions/0012-live-api-test-key-behaviour.md | grep -c 'must change'` equals `1`
    - `sed -n '/^## Code Conformance/,/^## Downstream Consumers/p' .planning/decisions/0012-live-api-test-key-behaviour.md | grep -c 'CLOSE-03'` is at least `1`
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty — in particular `tests/integration/llm_live_api_tests.rs` is unchanged by this phase
    - `git log -1 --name-only` shows both `.planning/decisions/0011-vision-port-surfaces.md` and `.planning/decisions/0012-live-api-test-key-behaviour.md` in one commit
  </acceptance_criteria>
  <done>ADR-0012 exists in the seven-H2 shape, records the shipped panic as standing with the double gate as the justification, names both missing-key arms and the exact emptiness semantics read from the shipped body, and declares `must change` scoped to the doc comment and executed by CLOSE-03 — with no `.rs` file touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/decisions/` ADR → Phase 6 CLOSE-03 implementer | Both ADRs are `must change`. Their `## Code Conformance` text is the entire scope CLOSE-03 receives. An overstated or understated conformance clause becomes a wrong code change. |
| Recorded security finding → public repository | ADR-0011 records, in a version-controlled document, that vision image bytes are not encrypted at rest despite the capability existing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | ADR-0011's encryption finding | medium | **accept** | The finding — `EncryptionService` ships with zero consumers, so no vision-path image bytes are encrypted at rest — is accurate and already derivable from the public tree by a single `grep`. Suppressing it would defeat VERIFY-04's purpose and leave Phase 6 unable to size CLOSE-03. Accepted on the condition that the ADR records the fact and the owner, and includes no key material, no configuration values, and no exploitation steps. |
| T-05-06 | Spoofing (of capability state) | `REQ-vision-security-encryption` disposition | medium | mitigate | Recording a security capability as present-and-wired when it is unwired would cause a downstream reader to skip real security work. Mitigated by the P5 prohibition and by requiring the zero-consumer `grep -rln` to be re-executed in-task rather than transcribed. |
| T-05-07 | Tampering | `tests/integration/llm_live_api_tests.rs` | low | mitigate | The temptation to "just fix" the one-line doc comment while it is open would silently move Phase 6 CLOSE-03's scope. Mitigated by the P4 prohibition and by an acceptance criterion asserting `git diff --stat` over `*.rs` is empty. |
| T-05-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable and not fabricated: this plan installs no packages and touches no manifest. ASVS V6 (Cryptography) applies here only as a citation subject — ADR-0011 records that `EncryptionService` uses ChaCha20-Poly1305 AEAD via the `chacha20poly1305` crate rather than a hand-rolled construction; it does not touch cryptographic code. |

No high or critical threats. Under ASVS L1 with a blocking threshold of `high`, nothing in this plan
blocks. The one genuinely security-adjacent item (T-05-01) is a disclosure-of-an-honest-finding
question, dispositioned `accept` with its reasoning stated rather than mitigated away.
</threat_model>

<artifacts>
## Artifacts this phase produces

Phase-wide artefact set (recorded in full in plan 05-01's `<artifacts>` section). This plan owns two:

| Artefact | Status | This plan's contribution |
|---|---|---|
| `.planning/decisions/0011-vision-port-surfaces.md` | **NEW** ADR, seven H2 headings, no frontmatter, `Code Conformance: must change` executed by Phase 6 CLOSE-03 | created in full (Task 1) |
| `.planning/decisions/0012-live-api-test-key-behaviour.md` | **NEW** ADR, seven H2 headings, no frontmatter, `Code Conformance: must change` (doc comment only) executed by Phase 6 CLOSE-03 | created in full (Task 2) |

New section headings introduced into existing documents: none. New headings created in the two new
ADRs: `## Status`, `## Context`, `## Decision`, `## Considered Options`, `## Code Locations`,
`## Code Conformance`, `## Downstream Consumers` (each file).
</artifacts>

<verification>
- `test -f .planning/decisions/0011-vision-port-surfaces.md && test -f .planning/decisions/0012-live-api-test-key-behaviour.md`
- `grep -c '^## ' .planning/decisions/0011-vision-port-surfaces.md` equals `7`
- `grep -c '^## ' .planning/decisions/0012-live-api-test-key-behaviour.md` equals `7`
- `grep -rln "EncryptionService\|DataRetentionPolicy\|VisionError::EncryptionError" src/ crates/ | grep -v "infrastructure/security"` returns nothing, and that result is what ADR-0011 reports
- `grep -c '#\[ignore\]' tests/integration/llm_live_api_tests.rs` equals `13`, and that count is what ADR-0012 reports
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
- `git status --porcelain` is clean after the plan's single commit
</verification>

<success_criteria>
A developer choosing a vision entry point can read ADR-0011 and see that both surfaces ship
deliberately, which one to use, and that Epic 13's encryption capability was built and never wired
rather than dropped — with the wiring decision named as Phase 6 CLOSE-03's. A developer running the
live-API suite with no keys can read ADR-0012 and see one recorded answer for what should happen, why
the shipped panic is that answer, and that the only defect is a doc comment Phase 6 corrects.
</success_criteria>

<output>
Create `.planning/phases/05-milestone-2-3-ground-truth/05-03-SUMMARY.md` when done. Record: the exact
output of the zero-consumer grep, the measured `#[ignore]` count, the exact emptiness check
`require_api_key` performs, and any line number that had drifted from the figure 05-RESEARCH.md
recorded.
</output>
