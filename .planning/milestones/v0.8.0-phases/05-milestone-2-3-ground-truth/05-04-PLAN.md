---
phase: 05-milestone-2-3-ground-truth
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/decisions/0006-coverage-gate.md
autonomous: true
requirements: [VERIFY-05]
must_haves:
  truths:
    - "`.planning/decisions/0006-coverage-gate.md` is amended in place — no second coverage ADR exists anywhere under `.planning/decisions/`, and the file still carries exactly its original seven H2 headings."
    - "Exactly one coverage number and one scope remain binding after the amendment: the 84% workspace line-coverage floor, default-feature scope. The two run-2 positions (75% overall with a layered per-tier table; 80%/70% re-asserted by Epic 24) are recorded as rejected against the measured 84.79%, and the amendment cross-references ADR-0006's existing rejection bullets rather than duplicating them."
    - "The amendment preserves ADR-0006's existing arithmetic verbatim: a measured figure passes at or above 84% and fails below it — 84.00% passes, 83.99% fails — and the truncation-toward-zero derivation of the floor from the 84.79% measurement is left unmodified. No new comparison operator, rounding mode, or precision rule is introduced. [edges: VERIFY-05/boundary, VERIFY-05/precision]"
    - "Where a module-scoped target and the global floor both apply to the same file, both numbers coexist: the module target is recorded as a target above the floor, never as a replacement for it, and the 84% floor remains the only binding gate. [edge: VERIFY-05/adjacency]"
    - "A module-scoped gate with no per-file row in `01-coverage-measurement.md` is recorded as having no measurement, with Phase 15 / PIPE-02 named as owner — never as a passing or failing number. Herald's 80.49% is transcribed from that file's per-file row, not re-typed from memory and not re-derived. [edge: VERIFY-05/empty]"
    - "The amendment's module-scoped gate table lists Herald before autonomous components, matching the order VERIFY-05's own requirement text names them, and the two new `Considered Options` entries are appended after the existing rejected-position bullets rather than interleaved among them. [edge: VERIFY-05/ordering]"
    - "`src/bin/paladin-server.rs` at 0.00% is dispositioned `deferred with reason` with the `run()`-seam extraction named as the concrete prerequisite and Phase 15 / PIPE-02 as owner; `minio.rs` is recorded as outside the gated denominator by construction, with the feature-scoped-measurement question assigned to Phase 15 / PIPE-02."
    - "The ~78% Milestone-3 release-notes figure is judged, not reconciled: recorded as failing the 84% floor and as predating the measurement that set it, in the same shape ADR-0006 already uses for the 60.88% / 67.79% Milestone-1 baselines."
  artifacts:
    - ".planning/decisions/0006-coverage-gate.md — amended in place with a dated Phase 5 amendment, a module-scoped gate table, two inherited dispositions, a falsifiability statement, and two appended Considered Options entries"
  key_links:
    - "ADR-0006 amendment → Phase 15 / PIPE-02 — receives the `run()` seam prerequisite, the `minio.rs` feature-scope question, and enforcement of both module-scoped gates"
    - "ADR-0006 amendment → ledger plans 05-11 and 05-12 — the `REQ-herald-consolidation-quality-gates` (Epic 19) and `REQ-autonomous-completion-quality-gates` (Epic 21) rows cite the transcribed figures rather than restating an unmeasured target"
    - "`01-coverage-measurement.md` per-file rows → the module-scoped gate table — the transcription source; Herald at line 317, `src/bin/paladin-server.rs` at line 426"
  prohibitions:
    - "The amendment must NOT introduce a second binding coverage floor, a second scope, or a second number presented as operative. RECON-07 exists to eliminate the 'choosing between two numbers' failure and a second coverage ADR — or a second live floor inside this one — would recreate it."
    - "This phase must NOT re-measure coverage. The 84.79% / 84% floor recorded 2026-07-31 stands; re-measurement is Phase 15's, is gated on Docker this environment does not have, and would put a second number in front of a phase whose purpose is to leave exactly one."
    - "No plan in this phase may edit any `.rs` file, `Cargo.toml`, or any file under `.github/workflows/`. In particular, do NOT extract the `run()` seam from `src/bin/paladin-server.rs` — this plan records the disposition only."
    - "Module-scoped gates must NOT be withdrawn. ADR-0006's own standing instruction keeps Herald ≥ 95% and autonomous ≥ 90% explicitly live; this amendment places them, it does not delete them."
---

<objective>
Extend ADR-0006's single coverage answer across the two positions ingest run 2 added, place the two
module-scoped gates against it, and disposition the two items the v0.7.1 close-out left on
VERIFY-05's desk — all by amending ADR-0006 in place.

Purpose: four competing coverage positions (80, 85, 75-layered, 80-Epic-24) plus two module-scoped
targets plus a stale ~78% release-notes figure. RECON-07 exists so exactly one number and one scope
survive. A second coverage ADR would recreate the failure it eliminated, so this is an in-place
amendment and never a new file.
Output: `.planning/decisions/0006-coverage-gate.md`, amended, still the only coverage ADR.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md
@.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md
@.planning/phases/05-milestone-2-3-ground-truth/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend ADR-0006 — the two run-2 positions and the falsifiability statement</name>
  <files>.planning/decisions/0006-coverage-gate.md</files>
  <read_first>
    - `.planning/decisions/0006-coverage-gate.md` — read the whole file (232 lines) before editing. Note specifically: the floor arithmetic at lines 102-106 (measured 84.79%, truncated down to a whole percent, at-or-above comparison, 84% passes and 83.99% fails); the module-scoped-gate stub at 134-140; the function-coverage flag at 142-148; the "accepted and noted, not explained" paragraph at 149-154 whose shape D-15 copies; the existing `Considered Options` rejections at 168-200
    - `.planning/ledgers/milestone-01.md` lines 21-29 and 130-133 — the section-level amendment preamble and the exact inline `**(Amended by Phase N, dated ..., citing ...)**` marker style
    - `.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md` §"`.planning/decisions/0006-coverage-gate.md` (amended in place)" — the four specific amendment instructions
    - `.planning/REQUIREMENTS.md` lines 467-476 — VERIFY-05 in full, including the order in which it names the two module-scoped gates
    - `.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md` §D-12, D-15, D-16
  </read_first>
  <action>
Amend `.planning/decisions/0006-coverage-gate.md` in place using `Edit` with scoped replacements.
Never `Write` this file, and never create a second coverage ADR.

(a) **Amendment preamble.** Insert a `## Phase 5 amendment (2026-08-04)` H2 immediately after the
existing `## Decision` section's last bullet and before `## Considered Options`, modelled on
`milestone-01.md:21-29`'s section-level preamble. It must state: that VERIFY-05 extends this same ADR
rather than writing a second one, per CONTEXT.md D-12 and ADR-0006's own ratchet clause; that
superseded text below is retained, not deleted, per D-00g; that no re-measurement was performed and
the 84.79% figure recorded 2026-07-31 stands (D-16); and that Phase 15 / PIPE-02 remains the
enforcement owner.

(b) **Preserve the arithmetic explicitly.** Inside the amendment preamble, add one sentence
restating — without changing — that the floor is derived by truncating the measured 84.79% toward
zero to a whole percent, that comparison is at-or-above, and that a run measuring exactly 84% passes
while 83.99% fails. Cross-reference the existing lines 102-106 rather than rewriting them. Do not
edit lines 102-106 themselves.

(c) **Two new `Considered Options` entries, appended after the existing rejected-position bullets,
not interleaved.** Add two bullets to the existing `## Considered Options` list:
- The Milestone 3 plan's position — overall ≥ 75% with a layered per-tier table (core ≥ 85%,
  application ≥ 80%, infrastructure ≥ 70%, CLI ≥ 70%) — rejected against the measured 84.79%,
  with the reason stated: the 75% overall figure sits ten points below the measured baseline and
  would under-gate what the tree already achieves, and a four-tier table introduces four numbers
  where RECON-07 requires one. Cross-reference the existing bullet at lines 194-196 that already
  rejects an under-gating floor generically, rather than restating its argument.
- Epic 24's re-assertion of ≥ 80% / ≥ 70% — rejected against the measured 84.79% for the same
  under-gating reason, cross-referencing the existing 80%-rejection bullets at lines 181-200.

(d) **The falsifiability statement (D-15).** Add a bullet inside the Phase 5 amendment section
stating plainly that the ~78% overall figure reported in `RELEASE_NOTES_MILESTONE_3.md` **fails** the
84% floor and **predates** the measurement that set it — a stale historical figure, not a competing
current one — and is not re-derived or explained away. Copy the shape of the existing paragraph at
lines 149-154 ("accepted and noted, not explained"), which gives the 60.88% / 67.79% Milestone-1
baselines exactly this treatment.

Use the inline `**(Amended by Phase 5, dated 2026-08-04, citing ...)**` marker style wherever the
amendment corrects or extends an existing clause in place.
  </action>
  <verify>
    <automated>[ "$(ls .planning/decisions/ | grep -c 'coverage')" = "1" ] && grep -q '^## Phase 5 amendment (2026-08-04)' .planning/decisions/0006-coverage-gate.md && grep -q 'Amended by Phase 5, dated 2026-08-04' .planning/decisions/0006-coverage-gate.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/decisions/ | grep -c 'coverage'` equals `1` — ADR-0006 is still the only coverage ADR
    - `ls .planning/decisions/ | grep -c '^00'` equals `12` after this phase's three new ADRs land (0001-0012); this task adds no new decision file
    - `grep -c '^## Phase 5 amendment (2026-08-04)' .planning/decisions/0006-coverage-gate.md` equals `1`
    - `grep -c 'Amended by Phase 5, dated 2026-08-04' .planning/decisions/0006-coverage-gate.md` is at least `1`
    - `grep -c 'a later run measuring exactly 84%' .planning/decisions/0006-coverage-gate.md` equals `1` — the original at-or-above arithmetic clause is present exactly once and was not duplicated or rewritten
    - `grep -c '84.79' .planning/decisions/0006-coverage-gate.md` is greater than its pre-edit count, and every new occurrence is a comparison against a rejected position, not a new floor
    - `sed -n '/^## Considered Options/,/^## Code Locations/p' .planning/decisions/0006-coverage-gate.md | grep -c '75'` is at least `1`
    - `sed -n '/^## Considered Options/,/^## Code Locations/p' .planning/decisions/0006-coverage-gate.md | grep -c 'Epic 24'` is at least `1`
    - `grep -c '78' .planning/decisions/0006-coverage-gate.md` is at least `1` and the matching line states the figure fails the floor and predates the measurement
    - `grep -n '^## ' .planning/decisions/0006-coverage-gate.md` still shows `## Status`, `## Context`, `## Decision`, `## Considered Options`, `## Code Locations`, `## Code Conformance`, `## Downstream Consumers` in their original relative order, with `## Phase 5 amendment (2026-08-04)` inserted between `## Decision` and `## Considered Options`
    - `git diff -- .planning/decisions/0006-coverage-gate.md` shows only additions and marker insertions in the Decision/Considered Options regions; no deletion of an existing rejected-position bullet
  </acceptance_criteria>
  <done>ADR-0006 carries a dated Phase 5 amendment section, the two run-2 positions appended as rejected `Considered Options` entries cross-referencing the existing rejections, the original 84% arithmetic untouched and restated by reference, and the ~78% figure judged rather than reconciled.</done>
</task>

<task type="auto">
  <name>Task 2: Place the two module-scoped gates and disposition the two inherited items</name>
  <files>.planning/decisions/0006-coverage-gate.md</files>
  <read_first>
    - `.planning/decisions/0006-coverage-gate.md` lines 134-140 — the module-scoped-gate stub this task fills, which already names VERIFY-05 in Phase 5 as the owner and already records Herald at 80.49%
    - `.planning/milestones/v0.7.1-phases/01-ground-truth-decision-records/01-coverage-measurement.md` line 317 — the per-file row for `crates/paladin-core/src/platform/container/herald.rs` (line coverage 80.49%) and lines 336-338 for the `crates/paladin-herald/` files; line 426 — `src/bin/paladin-server.rs` at 0.00%. **Transcribe these figures; never re-type from memory and never re-derive.**
    - `.planning/ROADMAP.md` lines 368-375 — the two inherited v0.7.1 close-out items and the owners already recorded for them
    - `.planning/STATE.md` §"Deferred Items" — the v0.7.1 close-out table
    - `.planning/REQUIREMENTS.md` lines 467-476 — VERIFY-05, which names Herald ≥ 95% before autonomous ≥ 90%
    - `.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md` §D-13, D-14a, D-14b
  </read_first>
  <precondition>`.planning/milestones/v0.7.1-phases/01-ground-truth-decision-records/01-coverage-measurement.md` exists and still contains the per-file coverage rows. It is a Phase 1 artefact archived under the v0.7.1 milestone directory, so its presence is not guaranteed by Phase 5's own plan ordering. Confirm with `grep -c 'herald.rs' <that file>` before transcribing; if the file is absent, halt and report rather than substituting a re-measurement (D-16 forbids re-measuring in this phase).</precondition>
  <action>
Continue amending `.planning/decisions/0006-coverage-gate.md` in place, inside the
`## Phase 5 amendment (2026-08-04)` section Task 1 created. Use `Edit`, never `Write`.

(a) **Module-scoped gate table.** Add a table with header row exactly
`| Module scope | Target | Measured | Gap | Owner |` and exactly two rows, **Herald first, autonomous
components second** — the order VERIFY-05's own text names them. The Herald row transcribes 80.49%
from `01-coverage-measurement.md`'s per-file row for
`crates/paladin-core/src/platform/container/herald.rs`, against its 95% target, with the gap stated
as roughly 15 points, requirement `REQ-herald-consolidation-quality-gates`, owner Phase 15 / PIPE-02.
For the autonomous-components row, look for per-file rows covering the autonomous services
(`planning_service.rs`, `prompt_generation_service.rs`, `temperature_service.rs`,
`handoff_service.rs`) in the same measurement file. If those per-file rows exist, transcribe them and
state the derived scope explicitly. If no per-file row covers the autonomous component set, record
the Measured cell as having no recorded measurement and the Gap cell as unknown, with requirement
`REQ-autonomous-completion-quality-gates` and owner Phase 15 / PIPE-02 — never a guessed or
interpolated figure.

Immediately beneath the table, add a paragraph stating: both module-scoped targets sit **above** the
global 84% floor and are **explicitly not withdrawn** (ADR-0006's own standing instruction); where a
module target and the global floor both apply to the same file, both numbers coexist and the module
target is a target above the floor, never a replacement for it; the 84% floor remains the only
**binding** gate; and Phase 5 records the number, the scope and the gap but does not convert either
target into a hard gate, because Phase 5 has no CI in which to enforce one — enforcement is Phase 15
/ PIPE-02's.

(b) **Disposition `src/bin/paladin-server.rs` (D-14a).** Add an entry recording it at **0.00%**
(transcribed from `01-coverage-measurement.md`'s per-file row), verdict `deferred with reason`, owner
**Phase 15 / PIPE-02**. State the reason concretely: closing it requires extracting a `run()` seam
from `main()`, which is a code change and therefore outside this phase's boundary. Name the seam
extraction as the prerequisite so Phase 15 inherits a task rather than a puzzle. State that a
0%-coverage binary is not allowed to sit silently in the denominator without a record.

(c) **Disposition `minio.rs` (D-14b).** Add an entry recording that ADR-0006's scope stays
default-feature workspace-wide and `minio.rs` is outside the gated denominator **by construction** —
it sits behind the non-default `s3-storage` feature and the recorded 84.79% was measured without it.
State that widening the denominator to non-default features in Phase 5 would move the 84% floor with
no measurement behind it, which is the exact thing ADR-0006 forbids, and that whether to add a
second, feature-scoped measurement is **Phase 15 / PIPE-02's** decision — the owner REQUIREMENTS.md
already records. Restate the RECON-07 constraint: one number, one scope.

(d) **Update `## Downstream Consumers`.** Append entries naming Phase 15 / PIPE-02 as the recipient
of the `run()`-seam prerequisite, the `minio.rs` feature-scope decision, and enforcement of both
module-scoped gates; and naming Phase 5 ledger plans 05-11 and 05-12 as citing the transcribed
figures on the `REQ-herald-consolidation-quality-gates` and `REQ-autonomous-completion-quality-gates`
rows.

Commit this plan's single file in one commit at the end, with a Bash timeout of at least 300000 ms —
`.pre-commit-config.yaml` sets `always_run: true` on `cargo-fmt` and `cargo-clippy` so they compile
the workspace even for a markdown-only commit. Do not pass `--no-verify`.
  </action>
  <verify>
    <automated>grep -q '| Module scope | Target | Measured | Gap | Owner |' .planning/decisions/0006-coverage-gate.md && grep -q '80.49' .planning/decisions/0006-coverage-gate.md && grep -q 'paladin-server.rs' .planning/decisions/0006-coverage-gate.md && grep -q 'minio.rs' .planning/decisions/0006-coverage-gate.md && [ "$(ls .planning/decisions/ | grep -c 'coverage')" = "1" ]</automated>
    <human-check>Read the amended ADR end to end and confirm no second floor or target is stated as operative — a grep can count occurrences of a number but cannot judge bindingness (05-VALIDATION.md §Manual-Only Verifications, row 4).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c '| Module scope | Target | Measured | Gap | Owner |' .planning/decisions/0006-coverage-gate.md` equals `1`
    - The module-scoped gate table has exactly two data rows, and `grep -n 'Herald' .planning/decisions/0006-coverage-gate.md` shows the Herald row at a lower line number than the autonomous-components row
    - `grep -c '80.49' .planning/decisions/0006-coverage-gate.md` is at least `1`, and the figure matches what `grep 'container/herald.rs' .planning/milestones/v0.7.1-phases/01-ground-truth-decision-records/01-coverage-measurement.md` reports
    - `grep -c '95' .planning/decisions/0006-coverage-gate.md` is at least `1` and `grep -c '90' .planning/decisions/0006-coverage-gate.md` is at least `1` — both module targets are stated, neither withdrawn
    - `grep -c 'paladin-server.rs' .planning/decisions/0006-coverage-gate.md` is at least `1` and the same entry contains `0.00`, `deferred with reason`, `run()` and `PIPE-02`
    - `grep -c 'minio.rs' .planning/decisions/0006-coverage-gate.md` is at least `1` and the same entry contains `s3-storage` and `PIPE-02`
    - `sed -n '/^## Downstream Consumers/,$p' .planning/decisions/0006-coverage-gate.md | grep -c 'PIPE-02'` is at least `1`
    - `ls .planning/decisions/ | grep -c 'coverage'` equals `1`
    - `grep -c 'a later run measuring exactly 84%' .planning/decisions/0006-coverage-gate.md` equals `1` — the original arithmetic survived both tasks unmodified
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty — in particular `src/bin/paladin-server.rs` is unchanged
    - `git log -1 --name-only` shows `.planning/decisions/0006-coverage-gate.md` and no other decision file
  </acceptance_criteria>
  <done>ADR-0006 carries a two-row module-scoped gate table with Herald's 80.49% transcribed and the ~15-point gap stated, both inherited items dispositioned with Phase 15 / PIPE-02 named as owner and the `run()` seam named as a concrete prerequisite, and exactly one binding coverage number and scope surviving.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADR-0006 → Phase 15 / PIPE-02 CI implementer | ADR-0006 is what wires a hard-fail coverage gate into CI. A number recorded here becomes a build-breaking threshold. |
| `01-coverage-measurement.md` (archived Phase 1 artefact) → ADR-0006 amendment | The transcription source. A mis-transcribed figure propagates into a CI gate with nothing between it and production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08 | Denial of Service (self-inflicted, build) | the 84% floor and the two module-scoped targets | medium | mitigate | Converting a module-scoped target into a hard gate in this phase, or letting a second floor become operative, would make Phase 15's CI wiring fail on the run that sets it — Herald sits ~15 points below its 95% target today. Mitigated by D-13's record-don't-enforce rule, by keeping 84% the sole binding gate, and by the human-check that reads the amended ADR end to end for a second operative number. |
| T-05-09 | Tampering (of the record) | transcribed coverage figures | medium | mitigate | A mis-transcribed 80.49% or 0.00% becomes a wrong CI threshold. Mitigated by requiring transcription from `01-coverage-measurement.md`'s per-file rows with a `grep`-verifiable match, by the precondition halting if that file is absent, and by forbidding interpolation where no per-file row exists. |
| T-05-10 | Repudiation | superseded coverage positions | low | mitigate | Deleting an existing rejected-position bullet would erase why a number was rejected. Mitigated by D-00g retain-don't-delete, the append-not-interleave rule, and a `git diff` acceptance criterion asserting additions only. |
| T-05-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable and not fabricated: this plan installs no packages and touches no manifest. |

No high or critical threats. Under ASVS L1 with a blocking threshold of `high`, nothing in this plan
blocks. The realistic surface is a build-integrity surface — a wrong number here breaks CI later —
not a runtime security surface.
</threat_model>

<artifacts>
## Artifacts this phase produces

Phase-wide artefact set (recorded in full in plan 05-01's `<artifacts>` section). This plan owns one:

| Artefact | Status | This plan's contribution |
|---|---|---|
| `.planning/decisions/0006-coverage-gate.md` | **AMENDED IN PLACE** — never a second ADR | Task 1 adds the amendment preamble, two appended `Considered Options` entries and the ~78% falsifiability statement; Task 2 adds the module-scoped gate table and the two inherited dispositions, and extends `## Downstream Consumers` |

New section headings introduced into an existing document: exactly one —
`## Phase 5 amendment (2026-08-04)`, inserted between the existing `## Decision` and
`## Considered Options` sections of `.planning/decisions/0006-coverage-gate.md`. The file's original
seven H2 headings are retained in their original relative order. No new files are created by this
plan.
</artifacts>

<verification>
- `ls .planning/decisions/ | grep -c 'coverage'` equals `1`
- `grep -c '^## Phase 5 amendment (2026-08-04)' .planning/decisions/0006-coverage-gate.md` equals `1`
- `grep -c '| Module scope | Target | Measured | Gap | Owner |' .planning/decisions/0006-coverage-gate.md` equals `1`
- `grep -c 'a later run measuring exactly 84%' .planning/decisions/0006-coverage-gate.md` equals `1`
- `grep -c '80.49' .planning/decisions/0006-coverage-gate.md` is at least `1` and matches the source measurement row
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
- `git status --porcelain` is clean after the plan's single commit
</verification>

<success_criteria>
One coverage number and one scope survive across all four competing positions — 84% workspace line
coverage, default-feature scope — with the two module-scoped gates placed above it as unenforced
targets carrying measured figures and named owners, the two v0.7.1-inherited items dispositioned with
Phase 15 / PIPE-02 owning both, and the ~78% Milestone-3 figure judged a stale failing figure rather
than reconciled into a competing one.
</success_criteria>

<output>
Create `.planning/phases/05-milestone-2-3-ground-truth/05-04-SUMMARY.md` when done. Record: the exact
per-file rows transcribed and their line numbers in `01-coverage-measurement.md`, whether an
autonomous-component measurement was found or recorded as absent, and any figure in ADR-0006 that
turned out to disagree with the measurement file.
</output>
