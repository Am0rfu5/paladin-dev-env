---
phase: 05-milestone-2-3-ground-truth
plan: 07
type: execute
wave: 4
depends_on: ["05-01", "05-03", "05-06"]
files_modified:
  - .planning/ledgers/milestone-02-03.md
autonomous: true
requirements: [VERIFY-02, VERIFY-01]
must_haves:
  truths:
    - "`.planning/ledgers/milestone-02-03.md` §Epic 24 carries a written block verdict for `tasks-test-hardening-benchmarks-qa.md`, backed by a `| Parent task | Verdict | Evidence |` table with one row per DISTINCT parent-task number in ascending numeric order."
    - "The source file states parent tasks `7.0` and `8.0` twice each — `7.0` at line 182 (open) and again at line 214 marked complete, `8.0` at line 216 (open) and again at line 244 marked substantially complete. Each collides on one number. The cluster table records ONE row per number, cites BOTH line occurrences in its Evidence cell, and states which occurrence the verdict follows. Neither duplicate is silently deduplicated to the first occurrence, and neither number gets two rows. [edge: VERIFY-02/adjacency]"
    - "The block verdict is `satisfied by shipped code` only if every parent-task cluster verifies; otherwise it is `partially outstanding`, the failing clusters are named, and those named clusters are exactly Phase 6 CLOSE-02's scope for this block and nothing else."
    - "The 29 open-item count is transcribed from `.planning/intel/task-completion-state.md` and attributed to it, never re-derived by a fresh count of checkbox strings."
    - "Epic 24's 9 `REQ-*` ledger rows are filled to the D-01 evidence bar in the same pass, so the epic is read once rather than twice."
    - "`REQ-provider-live-api-tests` and `REQ-epic24-quality-gates` cite the recorded answers rather than restating the contested positions: the live-API skip semantics resolve to ADR-0012, and the coverage clauses resolve to the amended ADR-0006."
  artifacts:
    - ".planning/ledgers/milestone-02-03.md §Epic 24 — 9 cited rows plus a block verdict and parent-task cluster table"
  key_links:
    - "Epic 24 block verdict's named failing clusters → Phase 6 CLOSE-02 — the last of the three inputs sizing CLOSE-02"
    - "`REQ-provider-live-api-tests` row → `.planning/decisions/0012-live-api-test-key-behaviour.md` — the recorded answer this row cites instead of restating the deadlock"
    - "`REQ-epic24-quality-gates` and `REQ-deferred-coverage-review` rows → `.planning/decisions/0006-coverage-gate.md` as amended by plan 05-04 — the single surviving coverage number"
  prohibitions:
    - "A block verdict must NOT be derived from checkbox arithmetic. VERIFY-02 requires a verdict per block, not a task list derived from counting."
    - "Do NOT re-derive the 29 open-item count with a fresh grep. `intel/task-completion-state.md` is authoritative."
    - "A duplicated parent-task number must NOT be silently collapsed to its first occurrence, and must NOT be split into two rows. One row, both citations, an explicit statement of which occurrence the verdict follows."
    - "No plan in this phase may edit any `.rs` file, `Cargo.toml`, or any file under `.github/workflows/`."
    - "The ledger must NOT award a `satisfied` verdict on the strength of a `file:line` citation alone."
---

<objective>
Produce the written block verdict for `tasks-test-hardening-benchmarks-qa.md` and fill Epic 24's 9
ledger rows in the same pass — closing the last of the three blocks
`intel/code-verification.md` leaves unverified.

Purpose: Epic 24 is the smallest of the three blocks by open-item count and the trickiest by
structure — its task list restates two parent tasks a second time with completion markers, so a
naive reading produces either a double-counted cluster or a silently dropped one. Once this block
reports, Phase 6 has its complete CLOSE-02 scope and can be planned at all.
Output: Epic 24's ledger section, complete: 9 cited `REQ-*` rows, a parent-task cluster table
handling the two duplicated numbers explicitly, and one block verdict sentence.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md
@.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md
@.planning/phases/05-milestone-2-3-ground-truth/05-RESEARCH.md
@.planning/phases/05-milestone-2-3-ground-truth/05-01-SUMMARY.md
@.planning/phases/05-milestone-2-3-ground-truth/05-03-SUMMARY.md
@.planning/phases/05-milestone-2-3-ground-truth/05-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Epic 24's parent-task clusters, resolve the two duplicated numbers, and write the block verdict</name>
  <files>.planning/ledgers/milestone-02-03.md</files>
  <read_first>
    - `.project/Milestone_3-Completion/Epic_24/tasks-test-hardening-benchmarks-qa.md` — read in full. Parent tasks run `0.0` through `8.0`, but `7.0` and `8.0` each appear TWICE: `7.0` at line 182 (open) and line 214 (marked complete), `8.0` at line 216 (open) and line 244 (marked substantially complete). Read all four occurrences before writing either row
    - `.planning/intel/task-completion-state.md` — the authoritative 29 open-item count for this file
    - `.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md` §"VERIFY-02 block-verdict table pattern (D-05 parent-task cluster table)"
    - `.planning/phases/05-milestone-2-3-ground-truth/05-05-SUMMARY.md` and `05-06-SUMMARY.md` — the cluster-table shape as the two earlier block plans actually rendered it
    - `.planning/ledgers/milestone-02-03.md` — the scaffold, for the exact `### Epic 24 — Test Hardening, Benchmarks & QA (9 IDs)` heading and the existing stub rows
    - `crates/paladin-battalion/benches/battalion_benchmarks.rs`, `crates/paladin-memory/benches/`, `crates/paladin-llm/benches/`, `benches/BENCHMARK_FIXES.md`, `tests/unit/mod.rs`, `tests/unit/prompt_generation_service_test.rs`, `tests/cli/snapshots/`, `tests/integration/rag_integration_tests.rs`
    - `.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md` §D-05, D-06
  </read_first>
  <action>
Under the existing `### Epic 24 — Test Hardening, Benchmarks & QA (9 IDs)` heading in
`.planning/ledgers/milestone-02-03.md`, add a block-verdict subsection **above** the
`| ID | Verdict | Evidence |` table, using `Edit` with a scoped replacement. Never `Write` the ledger
file. Epic 24 is the ledger's last epic section, so the scoped replacement's anchor is the heading
plus the stub table beneath it, not a following heading.

The subsection contains, in order:

(1) A one-sentence block verdict for `tasks-test-hardening-benchmarks-qa.md`, valued
`satisfied by shipped code`, `partially outstanding`, or `deferred with reason`, following D-06. If
any cluster fails, the verdict is `partially outstanding`, the sentence names the failing clusters
explicitly, and states that those named clusters are exactly Phase 6 CLOSE-02's scope for this block
and nothing else.

(2) A sentence transcribing the open-item count from `.planning/intel/task-completion-state.md` and
citing that file by name.

(3) A table with header row exactly `| Parent task | Verdict | Evidence |`, one row per **distinct**
parent-task number in ascending numeric order (`0.0` through `8.0` — nine rows, not eleven).

For `7.0` and `8.0` specifically: each number appears twice in the source, once open and once
restated with a completion marker. Write ONE row per number. Its Evidence cell must cite both line
occurrences (`tasks-test-hardening-benchmarks-qa.md:182` and `:214` for `7.0`;
`:216` and `:244` for `8.0`), and must state explicitly which occurrence the verdict follows and why
— verified against the tree, not against whichever occurrence appears later in the file. Add a
one-line note under the table recording that this source file restates two parent tasks, so a future
reader does not read the nine-row table as having dropped two.

For every row, read the parent task's subtask lines as one capability claim, verify that claim
against the tree, and record a verdict plus `file:line` evidence. `7.0` covers documentation and demo
assets (verify `docs/MANEUVER.md`, `docs/COMMANDER.md`, `docs/cli/TESTING.md`, `CONTRIBUTING.md` and
the demo asset actually exist); `8.0` covers final quality verification and CI/CD integration (verify
against `.github/workflows/` by reading, never editing).

Nest the cluster table under the Epic 24 block-verdict subsection, matching how plans 05-05 and 05-06
rendered theirs.

Do not commit yet — this plan commits once, after Task 2.
  </action>
  <verify>
    <automated>sed -n '/^### Epic 24 /,$p' .planning/ledgers/milestone-02-03.md | grep -q '| Parent task | Verdict | Evidence |' && [ "$(sed -n '/^### Epic 24 /,$p' .planning/ledgers/milestone-02-03.md | grep -cE '^\| [0-9]+\.0 ')" = "9" ]</automated>
    <human-check>Confirm every parent task in the source file appears in the table with a verdict and evidence, that the two duplicated numbers each occupy exactly one row citing both occurrences, and that a `partially outstanding` verdict names its failing clusters (05-VALIDATION.md §Manual-Only Verifications, row 2).</human-check>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Epic 24 /,$p' .planning/ledgers/milestone-02-03.md | grep -c '| Parent task | Verdict | Evidence |'` equals `1`
    - `sed -n '/^### Epic 24 /,$p' .planning/ledgers/milestone-02-03.md | grep -cE '^\| [0-9]+\.0 '` equals `9` — one row per distinct parent-task number `0.0` through `8.0`
    - `sed -n '/^### Epic 24 /,$p' … | grep -oE '^\| [0-9]+\.0 ' | sort -c` succeeds (rows already in ascending numeric order)
    - `sed -n '/^### Epic 24 /,$p' … | grep -oE '^\| [0-9]+\.0 ' | sort | uniq -d` prints nothing — no parent-task number occupies two rows
    - The `7.0` row's Evidence cell contains both `:182` and `:214`; the `8.0` row's Evidence cell contains both `:216` and `:244`
    - Each of the `7.0` and `8.0` rows states which occurrence its verdict follows
    - `sed -n '/^### Epic 24 /,$p' .planning/ledgers/milestone-02-03.md | grep -c 'restates'` is at least `1` — the duplicate-restatement note is present so the nine-row table is not misread as having dropped two
    - The block-verdict sentence contains exactly one of `satisfied by shipped code`, `partially outstanding`, or `deferred with reason`
    - If the block verdict is `partially outstanding`, the same sentence or the one following it names at least one parent-task number and contains `CLOSE-02`
    - `sed -n '/^### Epic 24 /,$p' .planning/ledgers/milestone-02-03.md | grep -c 'task-completion-state.md'` is at least `1`
    - `sed -n '/^### Epic 24 /,$p' .planning/ledgers/milestone-02-03.md | grep -c '29'` is at least `1`
    - `git diff --stat -- '*.rs' '.github/'` is empty for this task
  </acceptance_criteria>
  <done>Epic 24's ledger section carries one block verdict sentence, a transcribed and attributed open-item count, and a nine-row parent-task cluster table in which the two duplicated numbers each occupy exactly one row citing both occurrences and naming which one the verdict follows.</done>
</task>

<task type="auto">
  <name>Task 2: Fill Epic 24's 9 REQ ledger rows to the D-01 evidence bar</name>
  <files>.planning/ledgers/milestone-02-03.md</files>
  <read_first>
    - `.planning/ledgers/milestone-02-03.md` §`### Epic 24 — Test Hardening, Benchmarks & QA (9 IDs)` — the stub rows this task fills, plus the head notes fixing the evidence bar, verdict vocabulary and path caveats
    - `.planning/decisions/0012-live-api-test-key-behaviour.md` — written by plan 05-03; the recorded answer `REQ-provider-live-api-tests` cites
    - `.planning/decisions/0006-coverage-gate.md` as amended by plan 05-04 — the single surviving coverage number `REQ-epic24-quality-gates` and `REQ-deferred-coverage-review` cite
    - `.planning/phases/05-milestone-2-3-ground-truth/05-01-SUMMARY.md` — the Evidence-cell shape plan 05-01 actually used
    - `.planning/ledgers/milestone-01.md` lines 341-379 — the per-epic row-table and nested outstanding-item patterns
    - `.planning/codebase/STRUCTURE.md` and `.planning/codebase/TESTING.md`
    - `Cargo.toml` around line 152 (`insta`), `tests/cli/snapshots/`, `crates/paladin-battalion/src/commander.rs` (for the `#[ignore]` count), `project/DEFERRED_COVERAGE.md`
    - `.planning/phases/05-milestone-2-3-ground-truth/05-RESEARCH.md` §"Requirement → exercising-artefact search recipe"
  </read_first>
  <action>
Fill all 9 Epic 24 rows in `.planning/ledgers/milestone-02-03.md`, replacing each row's stub verdict
token `PENDING-VERDICT` and its stub Evidence cell. The 9 IDs, in the scaffold's fixed order:
`REQ-battalion-benchmark-repair`, `REQ-prompt-generation-test-reenable`, `REQ-timeout-test-hardening`,
`REQ-qdrant-integration-tests`, `REQ-deferred-coverage-review`, `REQ-cli-snapshot-testing`,
`REQ-provider-live-api-tests`, `REQ-final-documentation-and-demo`, `REQ-epic24-quality-gates`.

For each row: resolve the citation against the current tree; search for an exercising artefact with
the 05-RESEARCH.md recipe; run the scoped command for anything you intend to verdict `satisfied`,
preferring `cargo test --offline -p <crate> <module>::` scoping; assign one of the five legend
verdicts. A citation that resolves with nothing exercising it is `present, unproven`.

Five rows have specific content to record:
- `REQ-prompt-generation-test-reenable` — the open question is whether the module is uncommented in
  `tests/unit/mod.rs`. Read that file and record the actual state, not the recorded claim.
- `REQ-cli-snapshot-testing` — the criteria are `insta` present, `tests/cli/snapshots/` present, at
  least ten snapshots, and QUICKSTART / INSTALLATION / `docs/cli/README.md` updated. Count the
  snapshot files with `ls tests/cli/snapshots/ | wc -l` and record the measured number.
- `REQ-provider-live-api-tests` — cite `.planning/decisions/0012-live-api-test-key-behaviour.md` as
  the recorded answer to the contested skip semantics rather than restating the deadlock. Verdict the
  row against the shipped harness plus that ADR, and name Phase 6 CLOSE-03 as the owner of the
  doc-comment correction.
- `REQ-deferred-coverage-review` — cite the amended `.planning/decisions/0006-coverage-gate.md`.
  Record the `user_service.rs` and `listener_service.rs` figures the run-2 ledger names and whether
  `project/DEFERRED_COVERAGE.md` exists. Do not re-measure coverage.
- `REQ-epic24-quality-gates` — the "no ignored tests remaining" clause is contradicted by the
  `#[ignore]` attributes in `crates/paladin-battalion/src/commander.rs`. Use the measured count plan
  05-05 recorded (read its SUMMARY) rather than re-running the grep, and cite the amended ADR-0006
  for the coverage clauses.

Where the tree reveals something the run-2 ledger missed, add a nested finding row beneath the
`REQ-*` row using the blank-first-two-columns shape from `milestone-01.md:378-379`, prefixed
`**New finding (plan 05-07):**`.

Commit this plan's single file in one commit at the end, with a Bash timeout of at least 300000 ms —
`.pre-commit-config.yaml` sets `always_run: true` on `cargo-fmt` and `cargo-clippy` so they compile
the workspace even for a markdown-only commit. Do not pass `--no-verify`.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Epic 24 /,$p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT')" = "0" ] && [ "$(sed -n '/^### Epic 24 /,$p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-')" = "9" ] && [ "$(grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md)" = "118" ]</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Epic 24 /,$p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `9`
    - `sed -n '/^### Epic 24 /,$p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` still equals `118`
    - Every Epic 24 row's Evidence cell contains a `:` followed by a digit
    - Every Epic 24 row's Verdict cell holds exactly one of `satisfied`, `present, unproven`, `genuinely outstanding`, `deferred with reason`, `superseded by shipped code`
    - The `REQ-provider-live-api-tests` row cites `0012-live-api-test-key-behaviour.md` and names `CLOSE-03`
    - The `REQ-epic24-quality-gates` row and the `REQ-deferred-coverage-review` row each cite `0006-coverage-gate.md`
    - The `REQ-cli-snapshot-testing` row states a snapshot count equal to what `ls tests/cli/snapshots/ | wc -l` returns
    - The `REQ-prompt-generation-test-reenable` row states the actual state of `tests/unit/mod.rs` read during this task
    - Every Epic 24 row verdicted `satisfied` names a test, example, or command, and that command was executed during this task
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
    - `git log -1 --name-only` shows only `.planning/ledgers/milestone-02-03.md`
  </acceptance_criteria>
  <done>All 9 Epic 24 rows carry a resolved `file:line` citation, a legend verdict, and — where `satisfied` — an exerciser that was run; the live-API and coverage rows cite this phase's recorded answers rather than restating the contested positions; the ledger still holds exactly 118 rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Epic 24 block verdict → Phase 6 CLOSE-02 planning | The last of three inputs sizing CLOSE-02. Phase 6 cannot be planned until all three report. |
| Duplicated parent-task statements in the source task list → cluster verdicts | The same number asserts two different completion states in one file. Reading only one occurrence silently picks a completion state without evidence. |
| `.github/workflows/` → cluster `8.0`'s CI/CD verification | This plan READS the CI configuration to verify a claim. Read-only; the P4 prohibition forbids editing it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-14 | Spoofing (of completion state) | duplicated parent tasks `7.0` and `8.0` | medium | mitigate | Reading only the later, completion-marked occurrence would record a cluster as done on the strength of a self-assertion; reading only the earlier would record real work as outstanding. Mitigated by requiring one row per number citing both occurrences, an explicit statement of which occurrence the verdict follows, and a `sort \| uniq -d` acceptance criterion proving no number occupies two rows. |
| T-05-03 | Tampering (of the record) | Epic 24 cluster verdicts and `REQ-*` rows | medium | mitigate | Mitigated by the D-01 evidence bar and the P3 no-checkbox-arithmetic prohibition, both enforced as acceptance criteria. |
| T-05-15 | Tampering | `.github/workflows/` | low | mitigate | Cluster `8.0` verifies a CI/CD claim, which requires reading workflow files — creating the opportunity to "fix" one while open. Mitigated by the P4 prohibition and a `git diff --stat` acceptance criterion covering `.github/`. |
| T-05-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable and not fabricated: this plan installs no packages and touches no manifest. `Cargo.toml:152` (`insta`) is cited as evidence, never modified. |

No high or critical threats. Under ASVS L1 with a blocking threshold of `high`, nothing in this plan
blocks.
</threat_model>

<artifacts>
## Artifacts this phase produces

Phase-wide artefact set (recorded in full in plan 05-01's `<artifacts>` section). This plan
contributes to one:

| Artefact | Status | This plan's contribution |
|---|---|---|
| `.planning/ledgers/milestone-02-03.md` | **NEW** (created by 05-01) | §`### Epic 24 — Test Hardening, Benchmarks & QA (9 IDs)`: one block verdict sentence, a transcribed open-item count, a nine-row `| Parent task | Verdict | Evidence |` cluster table with the two duplicated numbers resolved, a duplicate-restatement note, and 9 cited `REQ-*` rows |

New section headings introduced into existing documents: none. This plan writes inside the
`### Epic 24 — Test Hardening, Benchmarks & QA (9 IDs)` heading the scaffold created.

**Milestone gate:** with this plan complete, all three VERIFY-02 blocks have reported and Phase 6's
CLOSE-02 scope is knowable. Phase 6 must not be planned before this point.
</artifacts>

<verification>
- `sed -n '/^### Epic 24 /,$p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `9`
- `sed -n '/^### Epic 24 /,$p' .planning/ledgers/milestone-02-03.md | grep -cE '^\| [0-9]+\.0 '` equals `9`
- `sed -n '/^### Epic 24 /,$p' .planning/ledgers/milestone-02-03.md | grep -oE '^\| [0-9]+\.0 ' | sort | uniq -d` prints nothing
- `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` equals `118`
- All three block sections (Epic 14, 22, 24) now carry a `| Parent task | Verdict | Evidence |` table: `grep -c '| Parent task | Verdict | Evidence |' .planning/ledgers/milestone-02-03.md` equals `3`
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
- `git status --porcelain` is clean after the plan's single commit
</verification>

<success_criteria>
Asking "is Epic 24 done?" returns a written verdict backed by a per-parent-task cluster table
reproducible from the tree, with the file's two restated parent tasks resolved explicitly rather than
silently. All three VERIFY-02 blocks have now reported, so Phase 6's CLOSE-02 scope is a named list
rather than a range from 0 to 155.
</success_criteria>

<output>
Create `.planning/phases/05-milestone-2-3-ground-truth/05-07-SUMMARY.md` when done. Record: the block
verdict and, if `partially outstanding`, the exact list of failing clusters; which occurrence of
`7.0` and `8.0` each verdict followed and why; the measured snapshot count; the verdict distribution
across the 9 rows; and — for the phase's benefit — the consolidated CLOSE-02 scope across all three
blocks, drawn from this plan's plus 05-05's and 05-06's verdicts.
</output>
