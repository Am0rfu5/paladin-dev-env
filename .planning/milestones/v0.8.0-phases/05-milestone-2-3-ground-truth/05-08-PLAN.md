---
phase: 05-milestone-2-3-ground-truth
plan: 08
type: execute
wave: 5
depends_on: ["05-01", "05-03", "05-07"]
files_modified:
  - .planning/ledgers/milestone-02-03.md
autonomous: true
requirements: [VERIFY-01]
must_haves:
  truths:
    - "Epic 13's 13 rows and Epic 20's 6 rows in `.planning/ledgers/milestone-02-03.md` each carry a `file:line` citation resolved against the shipped tree and a verdict from the five-value legend; every row verdicted `satisfied` names a test, example, or command that was actually run."
    - "`REQ-vision-security-encryption`'s row no longer records the capability as absent from the tree. It is verdicted `present, unproven` and cites `VisionError::EncryptionError` at `crates/paladin-core/src/platform/container/vision.rs:210-212`, `EncryptionService::encrypt_image_data` at `src/infrastructure/security/encryption.rs:200`, the `#[derive(Zeroize, ZeroizeOnDrop)]` on `SecureData` at `:68`, `SecureData::is_expired` at `:95` and `DataRetentionPolicy::should_retain` at `:131` — with the zero-consumer finding named and ADR-0011 cited."
    - "The five paired vision variant groups — format validation (8), OpenAI adapter (9), Anthropic adapter (10), Paladin vision API (11), vision error model (12) — are resolved as pairs in one pass, with the v1 row (Epic 13) and the v2 row (Epic 20) citing the same shipped artefact and stating which ingested position the shipped code answers."
    - "`REQ-vision-capable-llm-trait` (Epic 13) and `REQ-vision-port` (Epic 20) are recorded as coexisting surfaces, both citing ADR-0011, and neither row proposes a migration."
    - "Rows whose only divergence from their PRD is a pre-workspace-decomposition path are verdicted `superseded by shipped code` pointing at the ledger's head-note path caveat, not written up as fresh divergences."
  artifacts:
    - ".planning/ledgers/milestone-02-03.md §Epic 13 — 13 cited rows"
    - ".planning/ledgers/milestone-02-03.md §Epic 20 — 6 cited rows"
  key_links:
    - "`REQ-vision-security-encryption` row → `.planning/decisions/0011-vision-port-surfaces.md` — the ADR that corrects the ingest record's absence claim and hands the wiring decision to Phase 6 CLOSE-03"
    - "`REQ-vision-capable-llm-trait` and `REQ-vision-port` rows → ADR-0011's entry-point guidance — the two surfaces coexist"
    - "Epic 13 / Epic 20 variant pairs (groups 8-12) → `.planning/intel/requirements.md` — the acceptance-criteria source for each ingested position"
  prohibitions:
    - "The ledger must NOT award a `satisfied` verdict on the strength of a `file:line` citation alone."
    - "The `REQ-vision-security-encryption` row must NOT be transcribed from REQUIREMENTS.md's absence claim, which is verified false on all three counts, and must NOT record the capability as shipped without stating it has zero consumers outside `src/infrastructure/security/`."
    - "No plan in this phase may edit any `.rs` file, `Cargo.toml`, or any file under `.github/workflows/`."
    - "Neither vision row may plan or recommend a migration between the two surfaces on the strength of the PRD conflict alone."
---

<objective>
Fill the two vision epics' 19 ledger rows in one pass — Epic 13 (Sentinel Vision System, 13 IDs) and
Epic 20 (Vision Pipeline Completion, 6 IDs).

Purpose: these two epics are deliberately grouped across the Milestone 2 / Milestone 3 boundary
because they share five variant groups (8 through 12) — citing a v1 row without its v2 counterpart
risks recording a divergence that the paired requirement already resolves. They also carry the
phase's single most-wrongly-recorded row: `REQ-vision-security-encryption`, which REQUIREMENTS.md
records as absent from the tree and which is verified present on all three counts.
Output: Epic 13's and Epic 20's ledger sections, fully cited.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md
@.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md
@.planning/phases/05-milestone-2-3-ground-truth/05-RESEARCH.md
@.planning/phases/05-milestone-2-3-ground-truth/05-01-SUMMARY.md
@.planning/phases/05-milestone-2-3-ground-truth/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill Epic 13's 13 rows, including the corrected encryption row</name>
  <files>.planning/ledgers/milestone-02-03.md</files>
  <read_first>
    - `.planning/ledgers/milestone-02-03.md` §`### Epic 13 — Sentinel Vision System (13 IDs)` — the stub rows, plus the head notes fixing the evidence bar, verdict vocabulary and both path caveats
    - `.planning/decisions/0011-vision-port-surfaces.md` — written by plan 05-03; the recorded answer both the coexistence rows and the encryption row cite, and the source of the corrected `SecureData` / `DataRetentionPolicy` citations
    - `.planning/phases/05-milestone-2-3-ground-truth/05-03-SUMMARY.md` — the exact zero-consumer grep output plan 05-03 recorded
    - `.planning/phases/05-milestone-2-3-ground-truth/05-01-SUMMARY.md` — the Evidence-cell shape plan 05-01 established on Epic 11
    - `.planning/ledgers/milestone-01.md` lines 341-379 — the per-epic row-table and nested-finding patterns
    - `.planning/intel/requirements.md` — the acceptance criteria for each Epic 13 requirement; the run-2 ledger carries status only
    - `crates/paladin-core/src/platform/container/vision.rs`, `crates/paladin-llm/src/openai/vision.rs`, `crates/paladin-llm/src/anthropic/vision.rs`, `crates/paladin-ports/src/output/vision_llm_port.rs`, `crates/paladin-ports/src/input/document_port.rs`, `crates/paladin-content/src/adapters/document/pdf_extractor.rs`, `crates/paladin-llm/src/config/vision.rs`, `src/application/cli/commands/agent.rs`, `tests/integration/vision_integration_test.rs`, `examples/vision_battalion.rs`, `examples/document_processing.rs`
    - `.planning/codebase/STRUCTURE.md` and `.planning/codebase/TESTING.md`
    - `.planning/phases/05-milestone-2-3-ground-truth/05-RESEARCH.md` §"Requirement → exercising-artefact search recipe" and §"Pitfall 2"
  </read_first>
  <action>
Fill all 13 Epic 13 rows in `.planning/ledgers/milestone-02-03.md`, replacing each row's stub verdict
token `PENDING-VERDICT` and its stub Evidence cell, using `Edit` with scoped replacements. Never
`Write` the ledger file — other epic sections belong to other plans.

The 13 IDs, in the scaffold's fixed order: `REQ-vision-content-model`,
`REQ-vision-format-validation-v1`, `REQ-openai-vision-adapter-v1`, `REQ-anthropic-vision-adapter-v1`,
`REQ-vision-capable-llm-trait`, `REQ-paladin-vision-api-v1`, `REQ-vision-error-model-v1`,
`REQ-vision-security-encryption`, `REQ-pdf-extraction`, `REQ-document-port`,
`REQ-vision-cli-and-yaml`, `REQ-battalion-vision-integration`, `REQ-vision-performance-and-config`.

For each row: resolve the citation against the current tree and cite a real line number; search for
an exercising artefact with the 05-RESEARCH.md recipe (`grep -rn` the requirement's own type name
across `crates/ src/ tests/ examples/`, then `grep -rn "fn test_.*vision"` inside `tests/`, then
`ls examples/ | grep -i vision`); run the scoped command for anything you intend to verdict
`satisfied`, preferring `cargo test --offline -p paladin-core vision::` and
`cargo test --offline -p paladin-llm vision` style scoping; then assign one of the five legend
verdicts. A citation that resolves with nothing exercising it is `present, unproven`.

Five rows have specific content to record:

- **`REQ-vision-security-encryption`** — this is the row the whole ingest record gets wrong.
  REQUIREMENTS.md recorded it as not found in the tree, on the grounds that no encryption-at-rest,
  zeroization or retention-policy artefact was located and that `VisionError` omits an encryption
  variant. All three grounds are false. Verdict the row `present, unproven` and cite, in the Evidence
  cell: `VisionError::EncryptionError` at `crates/paladin-core/src/platform/container/vision.rs:210-212`;
  `EncryptionService::encrypt_image_data` at `src/infrastructure/security/encryption.rs:200` and
  `decrypt_image_data` at `:217`; `SecureData` with `#[derive(Zeroize, ZeroizeOnDrop)]` at `:68` and
  `SecureData::is_expired` at `:95`; `DataRetentionPolicy::should_retain` at `:131` — note these are
  two methods on two different types, not one method on `DataRetentionPolicy`. State the zero-consumer
  finding (nothing constructs or calls these outside `src/infrastructure/security/`) as the reason the
  verdict is `present, unproven` rather than `satisfied`, and cite
  `.planning/decisions/0011-vision-port-surfaces.md` plus Phase 6 CLOSE-03 as the owner of the wiring
  decision. Add a nested finding row beneath it, prefixed `**New finding (plan 05-08):**`, recording
  that the run-2 ledger's absence claim is superseded by direct inspection and the date.

- **`REQ-vision-capable-llm-trait`** — cite `crates/paladin-ports/src/output/vision_llm_port.rs:52`
  and record it as the adapter-author surface, coexisting with `REQ-vision-port` (Epic 20), citing
  ADR-0011. Do not record it as a variant of, or a competitor to, `VisionPort`.

- **`REQ-vision-format-validation-v1`, `REQ-openai-vision-adapter-v1`,
  `REQ-anthropic-vision-adapter-v1`, `REQ-paladin-vision-api-v1`, `REQ-vision-error-model-v1`** —
  each is one half of a variant pair (groups 8, 9, 10, 11, 12) whose other half sits in Epic 20 and is
  filled by Task 2 of this plan. For each, cite the single shipped artefact and state which ingested
  position the shipped code answers; where the shipped code answers differently from the v1 PRD,
  verdict `superseded by shipped code`. Resolve each pair consistently across the two tasks —
  the v1 and v2 rows must not contradict each other.

- **`REQ-vision-error-model-v1` and `-v2`** specifically: the shipped `VisionError` lives at
  `crates/paladin-core/src/platform/container/vision.rs:189`, not the `container/sentinel/vision_types.rs`
  path the v2 PRD specified. That relocation falls under the ledger's head-note path caveat — verdict
  it `superseded by shipped code` pointing at the head note, not a fresh divergence write-up.

- **`REQ-vision-performance-and-config`** — the end-to-end latency targets have no recorded
  measurement anywhere in `.planning/`, and STATE.md records the vision latency target as deferred to
  v2. Cite the config artefact, verdict the measurement half `deferred with reason` citing STATE.md's
  deferred-items table, and name the owner.

Do not commit yet — this plan commits once, after Task 2.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Epic 13 /,/^### Epic 20 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT')" = "0" ] && [ "$(sed -n '/^### Epic 13 /,/^### Epic 20 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-')" = "13" ] && sed -n '/^### Epic 13 /,/^### Epic 20 /p' .planning/ledgers/milestone-02-03.md | grep -q 'encryption.rs:200'</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Epic 13 /,/^### Epic 20 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `13`
    - `sed -n '/^### Epic 13 /,/^### Epic 20 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` still equals `118`
    - Every Epic 13 row's Evidence cell contains a `:` followed by a digit
    - Every Epic 13 row's Verdict cell holds exactly one of `satisfied`, `present, unproven`, `genuinely outstanding`, `deferred with reason`, `superseded by shipped code`
    - The `REQ-vision-security-encryption` row's Verdict cell is `present, unproven`
    - The `REQ-vision-security-encryption` row's Evidence cell contains all of `vision.rs:210`, `encryption.rs:200`, `encryption.rs:95`, `encryption.rs:131`, `zero consumers`, `0011` and `CLOSE-03`
    - A nested finding row prefixed `**New finding (plan 05-08):**` follows the `REQ-vision-security-encryption` row
    - The `REQ-vision-capable-llm-trait` row cites `vision_llm_port.rs:52` and `0011`
    - Every Epic 13 row verdicted `satisfied` names a test, example, or command, and that command was executed during this task
    - `git diff --stat -- '*.rs'` is empty for this task
  </acceptance_criteria>
  <done>All 13 Epic 13 rows are cited and verdicted; the encryption row records the capability as built-and-unwired with all five corrected citations and the zero-consumer finding, superseding the ingest record's absence claim.</done>
</task>

<task type="auto">
  <name>Task 2: Fill Epic 20's 6 rows and close the five variant pairs</name>
  <files>.planning/ledgers/milestone-02-03.md</files>
  <read_first>
    - `.planning/ledgers/milestone-02-03.md` §`### Epic 20 — Vision Pipeline Completion (6 IDs)` — the stub rows; and §`### Epic 13` as just filled by Task 1, so the paired rows agree
    - `.planning/decisions/0011-vision-port-surfaces.md` — the coexistence answer and the entry-point guidance `REQ-vision-port` cites
    - `.planning/decisions/0010-milestone-3-epic-numbering.md` — the authoritative numbering; the release notes attribute "Epic 20" to Council, and this epic's ledger note must point at the ADR rather than restating the defect
    - `.planning/intel/requirements.md` — the v2 acceptance criteria, including the retry-contract criteria the run-2 ledger left uninspected
    - `crates/paladin-ports/src/output/vision_port.rs`, `crates/paladin-llm/src/openai/vision.rs`, `crates/paladin-llm/src/anthropic/vision.rs`, `src/application/services/paladin/paladin_execution_service.rs` around line 517, `crates/paladin-core/src/platform/container/vision.rs` around line 189
    - `.planning/codebase/STRUCTURE.md` and `.planning/codebase/TESTING.md`
  </read_first>
  <action>
Fill all 6 Epic 20 rows in `.planning/ledgers/milestone-02-03.md`, replacing each row's stub verdict
token `PENDING-VERDICT` and its stub Evidence cell, using `Edit` with scoped replacements.

The 6 IDs, in the scaffold's fixed order: `REQ-vision-format-validation-v2`,
`REQ-openai-vision-adapter-v2`, `REQ-anthropic-vision-adapter-v2`, `REQ-vision-port`,
`REQ-paladin-vision-api-v2`, `REQ-vision-error-model-v2`.

Add the epic-level note above the table stating that the authoritative numbering is
20 = Vision Pipeline Completion and that the release notes' contrary mapping is a documentation
defect recorded in `.planning/decisions/0010-milestone-3-epic-numbering.md` — cite the ADR rather than
restating the defect's details.

For each row: resolve the citation, search for the exercising artefact, run the scoped command for a
`satisfied` verdict, assign a legend verdict. Then close each variant pair against the Epic 13 row
Task 1 wrote — the v1 and v2 rows for groups 8, 9, 10, 11 and 12 must cite the same shipped artefact
and must not assign contradictory verdicts to the same code.

Three rows have specific content to record:
- **`REQ-vision-port`** — cite `crates/paladin-ports/src/output/vision_port.rs:47`, record it as the
  execution-service surface and the recommended entry point for application code, reached via
  `PaladinExecutionService::execute_with_vision` at
  `src/application/services/paladin/paladin_execution_service.rs:517`, coexisting with
  `REQ-vision-capable-llm-trait` (Epic 13), citing ADR-0011.
- **`REQ-openai-vision-adapter-v2`** — the run-2 ledger left the retry-contract criteria uninspected.
  Inspect `crates/paladin-llm/src/openai/vision.rs` for retry handling and record what is actually
  there, including absence.
- **`REQ-vision-error-model-v2`** — the shipped `VisionError` is at `container/vision.rs:189`, not the
  specified `container/sentinel/vision_types.rs`. Verdict `superseded by shipped code` pointing at the
  ledger's head-note path caveat, and keep the verdict consistent with the v1 row.

Where the tree reveals something the run-2 ledger missed, add a nested finding row prefixed
`**New finding (plan 05-08):**`.

Commit this plan's single file in one commit at the end, with a Bash timeout of at least 300000 ms —
`.pre-commit-config.yaml` sets `always_run: true` on `cargo-fmt` and `cargo-clippy` so they compile
the workspace even for a markdown-only commit. Do not pass `--no-verify`. If a hook aborts, unstaged
work is stashed under `~/.cache/pre-commit/patch*`.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Epic 20 /,/^### Epic 14 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT')" = "0" ] && [ "$(sed -n '/^### Epic 20 /,/^### Epic 14 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-')" = "6" ] && [ "$(grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md)" = "118" ]</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Epic 20 /,/^### Epic 14 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `6`
    - `sed -n '/^### Epic 20 /,/^### Epic 14 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` still equals `118`
    - Every Epic 20 row's Evidence cell contains a `:` followed by a digit
    - Every Epic 20 row's Verdict cell holds exactly one of the five legend verdicts
    - `sed -n '/^### Epic 20 /,/^### Epic 14 /p' .planning/ledgers/milestone-02-03.md | grep -c '0010'` is at least `1` — the epic-level note cites the numbering ADR
    - The `REQ-vision-port` row cites `vision_port.rs:47`, `execute_with_vision` and `0011`
    - For each variant group 8, 9, 10, 11 and 12, the Epic 13 row and the Epic 20 row cite the same shipped file, and their verdicts are not mutually contradictory
    - The `REQ-openai-vision-adapter-v2` row states what retry handling `crates/paladin-llm/src/openai/vision.rs` actually contains, including a stated absence if that is the result
    - Every Epic 20 row verdicted `satisfied` names a test, example, or command, and that command was executed during this task
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
    - `git log -1 --name-only` shows only `.planning/ledgers/milestone-02-03.md`
  </acceptance_criteria>
  <done>All 6 Epic 20 rows are cited and verdicted, the five variant pairs are resolved consistently across Epic 13 and Epic 20, the two vision surfaces are recorded as coexisting citing ADR-0011, and the ledger still holds exactly 118 rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `REQ-vision-security-encryption` verdict → Phase 6 CLOSE-03 and the security backlog | This row is the corpus's only security-requirement row. Recording it as satisfied would remove a real, open wiring question from every downstream reader's view. |
| REQUIREMENTS.md's ingest-era verdict text → this plan's rows | The starting hypothesis, verified false on this epic's most important row. Treating it as truth is the failure mode. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Spoofing (of capability state) | `REQ-vision-security-encryption` row | medium | mitigate | Recording the encryption capability as shipped would hide that no vision-path image bytes are encrypted at rest; recording it as absent (the ingest record's claim) would trigger invented new security work the roadmap explicitly feared. Mitigated by pinning the verdict to `present, unproven` with all five corrected citations and the zero-consumer finding required in the Evidence cell as an acceptance criterion. |
| T-05-01 | Information Disclosure | the same row's zero-consumer statement | medium | **accept** | Same disposition and reasoning as ADR-0011's T-05-01: the finding is accurate, already derivable from the public tree by one `grep`, and suppressing it defeats VERIFY-04's purpose. Accepted on the condition the row records the fact and the owner and contains no key material or exploitation steps. |
| T-05-03 | Tampering (of the record) | Epic 13 / Epic 20 rows | medium | mitigate | Mitigated by the D-01 evidence bar enforced as acceptance criteria, and by the cross-pair consistency criterion that catches a v1 and v2 row contradicting each other over the same shipped file. |
| T-05-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable and not fabricated: this plan installs no packages and touches no manifest. `chacha20poly1305` and `zeroize` are cited from `Cargo.toml`, never modified. ASVS V6 applies as a citation subject only. |

No high or critical threats. Under ASVS L1 with a blocking threshold of `high`, nothing in this plan
blocks.
</threat_model>

<artifacts>
## Artifacts this phase produces

Phase-wide artefact set (recorded in full in plan 05-01's `<artifacts>` section). This plan
contributes to one:

| Artefact | Status | This plan's contribution |
|---|---|---|
| `.planning/ledgers/milestone-02-03.md` | **NEW** (created by 05-01) | §`### Epic 13 — Sentinel Vision System (13 IDs)` and §`### Epic 20 — Vision Pipeline Completion (6 IDs)`: 19 cited `REQ-*` rows, an Epic 20 epic-level note citing ADR-0010, and at least one nested finding row recording that the run-2 encryption absence claim is superseded |

New section headings introduced into existing documents: none. This plan writes inside two headings
the scaffold created.
</artifacts>

<verification>
- `sed -n '/^### Epic 13 /,/^### Epic 20 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `13`
- `sed -n '/^### Epic 20 /,/^### Epic 14 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `6`
- `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` equals `118`
- `grep -c 'encryption.rs:131' .planning/ledgers/milestone-02-03.md` is at least `1`
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
- `git status --porcelain` is clean after the plan's single commit
</verification>

<success_criteria>
A developer looking up any of the 19 vision requirement IDs sees a `file:line`-cited verdict against
the current crate layout, with the five variant pairs resolved consistently rather than as ten
independent divergences — and the encryption row tells the truth: the capability is built,
self-tested and unwired, with Phase 6 CLOSE-03 named as the owner of the wiring decision.
</success_criteria>

<output>
Create `.planning/phases/05-milestone-2-3-ground-truth/05-08-SUMMARY.md` when done. Record: the
verdict distribution across the 19 rows, the resolution of each of the five variant pairs, what retry
handling the OpenAI vision adapter actually contains, and every scoped command executed.
</output>
