---
phase: 05-milestone-2-3-ground-truth
plan: 10
type: execute
wave: 7
depends_on: ["05-01", "05-02", "05-09"]
files_modified:
  - .planning/ledgers/milestone-02-03.md
autonomous: true
requirements: [VERIFY-01]
must_haves:
  truths:
    - "Epic 16's 11 rows and Epic 18's 7 rows in `.planning/ledgers/milestone-02-03.md` each carry a `file:line` citation resolved against the shipped tree and a verdict from the five-value legend; every row verdicted `satisfied` names a test, example, or command that was actually run."
    - "`REQ-grove-routing-strategies`'s row records the shipped `RoutingStrategy` variants read from the tree and records `PerformanceBased` as verified absent, citing `.planning/decisions/0010-milestone-3-epic-numbering.md` rather than restating the release-notes defect."
    - "`REQ-council-execution-service` and `REQ-council-termination-conditions` record the shipped method names and result-field names read from the tree against the release-notes forms, citing ADR-0010 for the divergence."
    - "Epic 16's epic-level note records that the release notes attribute Council and Grove to Milestone 3 epic numbers and that both are Milestone 2 Epic 16, citing ADR-0010."
    - "`REQ-grove-config-v1` (Epic 16) and `REQ-grove-config-v2` (Epic 22, filled by plan 05-05) cite the same shipped configuration surface and do not assign contradictory verdicts to the same code."
    - "Epic 18's 12 stale open checkboxes are recorded as stale — every command in the epic exists in the tree — and Epic 18 is explicitly noted as NOT one of the three VERIFY-02 blocks."
  artifacts:
    - ".planning/ledgers/milestone-02-03.md §Epic 16 — 11 cited rows"
    - ".planning/ledgers/milestone-02-03.md §Epic 18 — 7 cited rows"
  key_links:
    - "Epic 16's epic-level note and three divergence rows → `.planning/decisions/0010-milestone-3-epic-numbering.md` — the corrected attribution and the withdrawn `PerformanceBased` claim"
    - "`REQ-grove-config-v1` → the Epic 22 `REQ-grove-config-v2` row plan 05-05 wrote — variant group 14"
    - "`REQ-grove-routing-strategies` → Phase 6 CLOSE-01 — the `LlmRouting` path carries the hardcoded model defect, recorded on the Epic 22 `REQ-grove-llm-routing` row"
  prohibitions:
    - "The ledger must NOT award a `satisfied` verdict on the strength of a `file:line` citation alone."
    - "No plan in this phase may edit any `.rs` file, `Cargo.toml`, or any file under `.github/workflows/`. In particular, do NOT fix the hardcoded routing model while verifying Grove's routing strategies — that fix is Phase 6 CLOSE-01."
    - "Divergence rows must NOT restate the release-notes defect in prose; they cite ADR-0010, which records it once and permanently."
---

<objective>
Fill Epic 16 (Advanced Battalion Patterns: Council & Grove, 11 IDs) and Epic 18 (CLI Enhancement &
Polish, 7 IDs) — 18 ledger rows.

Purpose: Epic 16 carries three of VERIFY-03's four defect classes as ledger rows — the misattributed
Council and Grove epic numbers, the `RoutingStrategy::PerformanceBased` claim verified absent from the
tree, and the Council method-name and result-field divergences. Each of those now has a recorded
answer in ADR-0010, so these rows cite it rather than re-litigating. Epic 18 is the counterweight: a
fully-shipped CLI epic whose 12 open checkboxes are stale and which is deliberately not one of the
three VERIFY-02 blocks.
Output: Epic 16's and Epic 18's ledger sections, fully cited.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md
@.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md
@.planning/phases/05-milestone-2-3-ground-truth/05-RESEARCH.md
@.planning/phases/05-milestone-2-3-ground-truth/05-01-SUMMARY.md
@.planning/phases/05-milestone-2-3-ground-truth/05-02-SUMMARY.md
@.planning/phases/05-milestone-2-3-ground-truth/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill Epic 16's 11 rows — Council and Grove</name>
  <files>.planning/ledgers/milestone-02-03.md</files>
  <read_first>
    - `.planning/ledgers/milestone-02-03.md` §`### Epic 16 — Advanced Battalion Patterns: Council & Grove (11 IDs)` — the stub rows; and §`### Epic 22` as filled by plan 05-05, so variant group 14 agrees
    - `.planning/decisions/0010-milestone-3-epic-numbering.md` — written by plan 05-02; the corrected attribution and the record of the two verified-absent claims
    - `.planning/phases/05-milestone-2-3-ground-truth/05-02-SUMMARY.md` — the shipped `RoutingStrategy` variants and the shipped Council and Maneuver API forms plan 05-02 read from the tree; reuse those readings rather than re-deriving them
    - `.planning/phases/05-milestone-2-3-ground-truth/05-05-SUMMARY.md` — the Epic 22 `REQ-grove-config-v2` verdict
    - `.planning/intel/requirements.md` — the Epic 16 acceptance criteria, including the PRD's deferral of `Random` and `VoluntaryWithTimeout` under NG-6 and non-goal NG-3
    - `crates/paladin-core/src/platform/container/battalion/council.rs`, `crates/paladin-core/src/platform/container/battalion/grove.rs`, `crates/paladin-battalion/src/council_service.rs`, `crates/paladin-battalion/src/grove_service.rs`, `crates/paladin-battalion/src/commander.rs`, `tests/integration/commander_integration_tests.rs`, `examples/council_discussion.rs`, `examples/commander_council.rs`, `examples/grove_routing.rs`, `examples/commander_grove.rs`
    - `.planning/codebase/STRUCTURE.md` and `.planning/codebase/TESTING.md`
    - `.planning/phases/05-milestone-2-3-ground-truth/05-RESEARCH.md` §"Requirement → exercising-artefact search recipe"
  </read_first>
  <action>
Fill all 11 Epic 16 rows in `.planning/ledgers/milestone-02-03.md`, replacing each row's stub verdict
token `PENDING-VERDICT` and its stub Evidence cell, using `Edit` with scoped replacements. Never
`Write` the ledger file.

The 11 IDs, in the scaffold's fixed order: `REQ-council-domain-model`, `REQ-council-turn-strategies`,
`REQ-council-termination-conditions`, `REQ-council-execution-service`,
`REQ-council-garrison-integration`, `REQ-grove-domain-model`, `REQ-grove-routing-strategies`,
`REQ-grove-config-v1`, `REQ-grove-execution-service`, `REQ-grove-arsenal-integration`,
`REQ-council-grove-commander-integration`.

Add an epic-level note above the table stating that `intel/code-verification.md` records both Council
and Grove as verified shipped, and that the release notes attribute them to Milestone 3 epic numbers
while they are Milestone 2 Epic 16 — citing
`.planning/decisions/0010-milestone-3-epic-numbering.md` rather than restating the defect.

For each row: resolve the citation and cite a real line number; search for the exercising artefact
with the 05-RESEARCH.md recipe; run the scoped command for anything you intend to verdict
`satisfied`, preferring `cargo test --offline -p paladin-battalion council::` and
`-p paladin-battalion grove::` scoping; assign one of the five legend verdicts.

Five rows have specific content to record:
- **`REQ-grove-routing-strategies`** — record the shipped `RoutingStrategy` variants read from
  `crates/paladin-core/src/platform/container/battalion/grove.rs` (reuse plan 05-02's reading), and
  record that `PerformanceBased` is verified absent from the tree and contradicts non-goal NG-3,
  citing ADR-0010. Note that the `LlmRouting` path carries the hardcoded-model defect recorded on the
  Epic 22 `REQ-grove-llm-routing` row and owned by Phase 6 CLOSE-01 — cross-reference, do not
  duplicate the finding and do not fix the code.
- **`REQ-council-termination-conditions`** — the release notes show a tuple-variant form against the
  PRD's unit variant. Read the shipped form in `council.rs` and record it, citing ADR-0010 for the
  divergence.
- **`REQ-council-execution-service`** — the method-name and result-field divergences (`convene`
  against `execute`, `conclusion` against `summary`). Read the shipped names from
  `council_service.rs` and record them, citing ADR-0010.
- **`REQ-council-turn-strategies`** — the PRD defers `Random` and `VoluntaryWithTimeout` under NG-6.
  Record which `TurnStrategy` variants actually ship and verdict the deferred pair
  `deferred with reason` citing the non-goal.
- **`REQ-grove-config-v1`** — variant group 14, paired with `REQ-grove-config-v2` (Epic 22, filled by
  plan 05-05). Cite the same shipped configuration surface plan 05-05 cited and state which ingested
  position it answers. The two rows must not assign contradictory verdicts to the same code.

Two rows are recorded as uninspected by the run-2 ledger and must be inspected here:
`REQ-council-garrison-integration` and `REQ-grove-arsenal-integration` (a SHOULD-level requirement).
Record the actual finding for each, including any absence.

Where the tree reveals something the run-2 ledger missed, add a nested finding row prefixed
`**New finding (plan 05-10):**`.

Do not commit yet — this plan commits once, after Task 2.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Epic 16 /,/^### Epic 17 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT')" = "0" ] && [ "$(sed -n '/^### Epic 16 /,/^### Epic 17 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-')" = "11" ] && sed -n '/^### Epic 16 /,/^### Epic 17 /p' .planning/ledgers/milestone-02-03.md | grep -q '0010'</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Epic 16 /,/^### Epic 17 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `11`
    - `sed -n '/^### Epic 16 /,/^### Epic 17 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` still equals `118`
    - `sed -n '/^### Epic 16 /,/^### Epic 17 /p' .planning/ledgers/milestone-02-03.md | grep -c '0010'` is at least `4` — the epic-level note plus the three divergence rows each cite the ADR
    - Every Epic 16 row's Evidence cell contains a `:` followed by a digit
    - Every Epic 16 row's Verdict cell holds exactly one of the five legend verdicts
    - The `REQ-grove-routing-strategies` row names the shipped `RoutingStrategy` variants read from the tree, records `PerformanceBased` as absent, and cross-references `CLOSE-01` without duplicating the Epic 22 finding
    - The `REQ-council-execution-service` row names the shipped method name and result-field name read from `council_service.rs`
    - The `REQ-grove-config-v1` row cites the same shipped configuration surface as the Epic 22 `REQ-grove-config-v2` row, and the two verdicts are not mutually contradictory
    - The `REQ-council-garrison-integration` and `REQ-grove-arsenal-integration` rows each state an actual finding from inspection, including any absence
    - Every Epic 16 row verdicted `satisfied` names a test, example, or command, and that command was executed during this task
    - `git diff --stat -- '*.rs'` is empty for this task
  </acceptance_criteria>
  <done>All 11 Epic 16 rows are cited and verdicted, the three release-notes divergences cite ADR-0010 rather than re-litigating, the two previously uninspected rows are inspected, and variant group 14 is closed consistently against Epic 22.</done>
</task>

<task type="auto">
  <name>Task 2: Fill Epic 18's 7 rows — CLI enhancement and polish</name>
  <files>.planning/ledgers/milestone-02-03.md</files>
  <read_first>
    - `.planning/ledgers/milestone-02-03.md` §`### Epic 18 — CLI Enhancement & Polish (7 IDs)` — the stub rows
    - `.planning/intel/task-completion-state.md` — the 12 open checkboxes recorded for `tasks-epic-18-cli-enhancement.md`
    - `.planning/intel/requirements.md` — the Epic 18 acceptance criteria
    - `src/application/cli/commands/onboarding.rs`, `setup_check.rs`, `features.rs`, `muster.rs`, `council.rs`; `src/application/cli/formatters/`; `tests/cli/snapshots/`; `src/bin/paladin-cli.rs`; `Cargo.toml` around line 152 (`insta`)
    - `.planning/codebase/STRUCTURE.md` and `.planning/codebase/TESTING.md`
  </read_first>
  <action>
Fill all 7 Epic 18 rows in `.planning/ledgers/milestone-02-03.md`, replacing each row's stub verdict
token `PENDING-VERDICT` and its stub Evidence cell, using `Edit` with scoped replacements.

The 7 IDs, in the scaffold's fixed order: `REQ-cli-onboarding-wizard`, `REQ-cli-setup-check`,
`REQ-cli-features-discovery`, `REQ-cli-muster-command`, `REQ-cli-council-command`,
`REQ-cli-rich-output`, `REQ-cli-core-infrastructure`.

Add an epic-level note above the table stating that `tasks-epic-18-cli-enhancement.md` carries 12
open checkboxes (transcribed from `.planning/intel/task-completion-state.md` and cited to it), that
every command below exists in the tree so the count is stale rather than a backlog, and — explicitly
— that Epic 18 is **not** one of the three blocks flagged for VERIFY-02, so a reader does not expect a
block verdict here.

For each row: resolve the citation and cite a real line number; search for the exercising artefact —
for CLI commands the exercisers live under `tests/cli/`, so `grep -rn "<command name>" tests/cli/`
and `ls tests/cli/snapshots/ | grep -i <command>` are the productive searches; run the scoped command
for anything you intend to verdict `satisfied`, preferring `cargo test --offline --test cli` style
scoping; assign one of the five legend verdicts.

Two rows have specific content to record:
- **`REQ-cli-rich-output`** — cite `src/application/cli/formatters/`, the snapshot directory, and
  `insta` in `Cargo.toml`. Count the snapshot files with `ls tests/cli/snapshots/ | wc -l` and record
  the measured number. Note that the snapshot-count criterion also appears on the Epic 24
  `REQ-cli-snapshot-testing` row filled by plan 05-07 — cross-reference it rather than restating a
  second, possibly different count.
- **`REQ-cli-core-infrastructure`** — the PRD's module layout (`commands/`, `formatters/`,
  `interactive/`, `templates/`, entry point `src/bin/paladin-cli.rs`) sits under
  `src/application/cli/` rather than the PRD's `src/cli`, because the Epic 17.5 consolidation is
  applied in code. That relocation falls under the ledger's head-note path caveat — verdict
  `superseded by shipped code` pointing at the head note, not a fresh divergence write-up. Confirm
  each of the four subdirectories actually exists before citing it.

Where the tree reveals something the run-2 ledger missed, add a nested finding row prefixed
`**New finding (plan 05-10):**`.

Commit this plan's single file in one commit at the end, with a Bash timeout of at least 300000 ms —
`.pre-commit-config.yaml` sets `always_run: true` on `cargo-fmt` and `cargo-clippy` so they compile
the workspace even for a markdown-only commit. Do not pass `--no-verify`.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Epic 18 /,/^### Epic 19 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT')" = "0" ] && [ "$(sed -n '/^### Epic 18 /,/^### Epic 19 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-')" = "7" ] && [ "$(grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md)" = "118" ]</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Epic 18 /,/^### Epic 19 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `7`
    - `sed -n '/^### Epic 18 /,/^### Epic 19 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` still equals `118`
    - `sed -n '/^### Epic 18 /,/^### Epic 19 /p' .planning/ledgers/milestone-02-03.md | grep -c 'VERIFY-02'` is at least `1` — the epic-level note states Epic 18 is not one of the three blocks
    - `sed -n '/^### Epic 18 /,/^### Epic 19 /p' .planning/ledgers/milestone-02-03.md | grep -c 'task-completion-state.md'` is at least `1`
    - Every Epic 18 row's Evidence cell contains a `:` followed by a digit
    - Every Epic 18 row's Verdict cell holds exactly one of the five legend verdicts
    - The `REQ-cli-core-infrastructure` row cites four subdirectory paths that each resolve in the tree, and its verdict points at the ledger's head-note path caveat
    - The `REQ-cli-rich-output` row's snapshot count equals what `ls tests/cli/snapshots/ | wc -l` returns and cross-references the Epic 24 `REQ-cli-snapshot-testing` row
    - Every Epic 18 row verdicted `satisfied` names a test, example, or command, and that command was executed during this task
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
    - `git log -1 --name-only` shows only `.planning/ledgers/milestone-02-03.md`
  </acceptance_criteria>
  <done>All 7 Epic 18 rows are cited and verdicted, the 12 stale checkboxes are recorded as stale and attributed, Epic 18 is explicitly marked as outside VERIFY-02's three blocks, and the CLI relocation is handled under the head-note path caveat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/` ledger row → future implementer or agent | The only boundary. A verdict here is consumed downstream as fact. No runtime, network, process, or user-input boundary exists in this plan. |
| ADR-0010's recorded answer → Epic 16's three divergence rows | The rows cite the ADR rather than re-deriving. A row that re-litigates the defect in prose creates a second, drifting account of the same finding. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering (of the record) | Epic 16 / Epic 18 rows | medium | mitigate | A row verdicted `satisfied` on a citation alone sends Phase 6 past real work. Mitigated by the D-01 evidence bar enforced as acceptance criteria requiring a real `file:line` and an executed command behind every `satisfied`. |
| T-05-17 | Tampering (of the record) | duplicated findings across rows | low | mitigate | The snapshot count appears on both this plan's `REQ-cli-rich-output` row and plan 05-07's `REQ-cli-snapshot-testing` row; the hardcoded routing model appears on both `REQ-grove-routing-strategies` and Epic 22's `REQ-grove-llm-routing`. Two independently derived numbers or descriptions would drift. Mitigated by requiring cross-reference rather than restatement, and by pinning the snapshot count to the same `ls … \| wc -l` result. |
| T-05-11 | Elevation of Privilege (scope) | `crates/paladin-battalion/src/grove_service.rs` | low | mitigate | Verifying Grove's routing strategies puts the hardcoded-model defect directly in view. Mitigated by the P4 prohibition and a `git diff --stat` acceptance criterion over `*.rs`. |
| T-05-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable and not fabricated: this plan installs no packages and touches no manifest. `Cargo.toml:152` (`insta`) is cited as evidence, never modified. |

No high or critical threats. Under ASVS L1 with a blocking threshold of `high`, nothing in this plan
blocks.
</threat_model>

<artifacts>
## Artifacts this phase produces

Phase-wide artefact set (recorded in full in plan 05-01's `<artifacts>` section). This plan
contributes to one:

| Artefact | Status | This plan's contribution |
|---|---|---|
| `.planning/ledgers/milestone-02-03.md` | **NEW** (created by 05-01) | §`### Epic 16 — Advanced Battalion Patterns: Council & Grove (11 IDs)` and §`### Epic 18 — CLI Enhancement & Polish (7 IDs)`: 18 cited `REQ-*` rows plus two epic-level notes, one citing ADR-0010 and one recording Epic 18's stale count and its exclusion from VERIFY-02's blocks |

New section headings introduced into existing documents: none. This plan writes inside two headings
the scaffold created.
</artifacts>

<verification>
- `sed -n '/^### Epic 16 /,/^### Epic 17 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `11`
- `sed -n '/^### Epic 18 /,/^### Epic 19 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `7`
- `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` equals `118`
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
- `git status --porcelain` is clean after the plan's single commit
</verification>

<success_criteria>
A developer looking up any of the 18 Council/Grove or CLI requirement IDs sees a `file:line`-cited
verdict against the current crate layout. The three release-notes divergences resolve to ADR-0010
rather than to a second prose account, the `PerformanceBased` absence is recorded against the shipped
variant list, and Epic 18's stale checkbox count is visibly stale rather than a phantom backlog.
</success_criteria>

<output>
Create `.planning/phases/05-milestone-2-3-ground-truth/05-10-SUMMARY.md` when done. Record: the
verdict distribution across the 18 rows, the shipped `RoutingStrategy` and `TurnStrategy` variant
lists, the shipped Council method and result-field names, the measured snapshot count, the findings
for the two previously uninspected Epic 16 rows, and every scoped command executed.
</output>
