---
phase: 05-milestone-2-3-ground-truth
plan: 11
type: execute
wave: 8
depends_on: ["05-01", "05-02", "05-04", "05-10"]
files_modified:
  - .planning/ledgers/milestone-02-03.md
autonomous: true
requirements: [VERIFY-01]
must_haves:
  truths:
    - "Epic 17 / 17.5's 11 rows and Epic 19's 5 rows in `.planning/ledgers/milestone-02-03.md` each carry a `file:line` citation resolved against the shipped tree and a verdict from the five-value legend; every row verdicted `satisfied` names a test, example, or command that was actually run."
    - "`REQ-maneuver-domain-model` records the shipped constructor argument order read from the tree against the two competing ingested orders, and `REQ-maneuver-cli` records the shipped command namespace read from the tree — both citing `.planning/decisions/0010-milestone-3-epic-numbering.md` for the divergence rather than restating it."
    - "`REQ-herald-consolidation-quality-gates` cites the amended `.planning/decisions/0006-coverage-gate.md` for its measured figure and its owner, and does not restate an unmeasured target as if it were a verdict. Its verdict is `deferred with reason` with Phase 15 / PIPE-02 named as owner, matching the disposition ADR-0006 now records."
    - "Epic 17.5's CLI-location decision is recorded as applied in code — `src/cli` is absent from the tree and `src/application/cli/` carries the full command set — with the row verdicted under the ledger's head-note path caveat rather than as a fresh divergence."
    - "The four Epic 19 rows the run-2 ledger left uninspected — `REQ-stream-chunk-complete`, `REQ-execution-metadata-complete`, `REQ-herald-formatter-autoregistration`, and the quality-gates row — are each inspected against the tree and record an actual finding, including any absence."
  artifacts:
    - ".planning/ledgers/milestone-02-03.md §Epic 17 / 17.5 — 11 cited rows"
    - ".planning/ledgers/milestone-02-03.md §Epic 19 — 5 cited rows"
  key_links:
    - "`REQ-maneuver-domain-model` and `REQ-maneuver-cli` → `.planning/decisions/0010-milestone-3-epic-numbering.md` — the constructor-order and namespace divergences"
    - "`REQ-herald-consolidation-quality-gates` → `.planning/decisions/0006-coverage-gate.md` as amended by plan 05-04 — the transcribed 80.49% measured figure, the 95% target, and Phase 15 / PIPE-02 as owner"
    - "`REQ-herald-type-consolidation` → the run-1 variant group 6 later position — this row is the resolution of an earlier milestone's contested type surface"
  prohibitions:
    - "The ledger must NOT award a `satisfied` verdict on the strength of a `file:line` citation alone."
    - "`REQ-herald-consolidation-quality-gates` must NOT record a coverage number other than the one the amended ADR-0006 carries, and must NOT trigger a re-measurement — D-16 keeps Phase 5 out of re-measuring, and a second number would recreate exactly the failure RECON-07 eliminated."
    - "No plan in this phase may edit any `.rs` file, `Cargo.toml`, or any file under `.github/workflows/`."
    - "Divergence rows must NOT restate the release-notes defect in prose; they cite ADR-0010."
---

<objective>
Fill Epic 17 / 17.5 (Flow DSL, Maneuver and CLI consolidation, 11 IDs) and Epic 19 (Herald & Domain
Type Consolidation, 5 IDs) — 16 ledger rows.

Purpose: Epic 17 carries the last two of VERIFY-03's release-notes divergences as ledger rows — the
Maneuver constructor argument order and the CLI command namespace — plus Epic 17.5's CLI-location
decision, which is applied in code but sits at DOC precedence with no ADR. Epic 19 carries the first
of the two module-scoped coverage gates, and its row is where the amended ADR-0006's Herald figure
lands in the ledger rather than being re-derived.
Output: Epic 17 / 17.5's and Epic 19's ledger sections, fully cited.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md
@.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md
@.planning/phases/05-milestone-2-3-ground-truth/05-RESEARCH.md
@.planning/phases/05-milestone-2-3-ground-truth/05-01-SUMMARY.md
@.planning/phases/05-milestone-2-3-ground-truth/05-02-SUMMARY.md
@.planning/phases/05-milestone-2-3-ground-truth/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill Epic 17 / 17.5's 11 rows — Flow DSL, Maneuver and CLI consolidation</name>
  <files>.planning/ledgers/milestone-02-03.md</files>
  <read_first>
    - `.planning/ledgers/milestone-02-03.md` §`### Epic 17 / 17.5 — Flow DSL, Maneuver and CLI consolidation (11 IDs)` — the stub rows
    - `.planning/decisions/0010-milestone-3-epic-numbering.md` — written by plan 05-02; the recorded answer both divergence rows cite
    - `.planning/phases/05-milestone-2-3-ground-truth/05-02-SUMMARY.md` — the shipped Maneuver API form plan 05-02 read from the tree; reuse that reading
    - `.planning/intel/requirements.md` — the Epic 17 and Epic 17.5 acceptance criteria, including the two competing constructor argument orders and the two competing CLI command shapes
    - `crates/paladin-battalion/src/maneuver/mod.rs`, `crates/paladin-battalion/src/maneuver/parser/`, `crates/paladin-battalion/src/maneuver/service.rs`, `crates/paladin-battalion/src/maneuver/visualizer.rs`, `crates/paladin-battalion/src/commander.rs`, `src/application/cli/commands/maneuver.rs`, `examples/maneuver_basic.rs`, `examples/maneuver_nested_flow.rs`, `examples/maneuver_dynamic_flow.rs`
    - `.planning/codebase/STRUCTURE.md` and `.planning/codebase/TESTING.md`
    - `.planning/phases/05-milestone-2-3-ground-truth/05-RESEARCH.md` §"Requirement → exercising-artefact search recipe"
  </read_first>
  <action>
Fill all 11 Epic 17 / 17.5 rows in `.planning/ledgers/milestone-02-03.md`, replacing each row's stub
verdict token `PENDING-VERDICT` and its stub Evidence cell, using `Edit` with scoped replacements.
Never `Write` the ledger file.

The 11 IDs, in the scaffold's fixed order: `REQ-flow-dsl-syntax`, `REQ-flow-parser`,
`REQ-flow-expression-ast`, `REQ-maneuver-domain-model`, `REQ-maneuver-config`,
`REQ-maneuver-error-strategy-v2`, `REQ-maneuver-execution-service`,
`REQ-maneuver-commander-integration`, `REQ-maneuver-cli`, `REQ-flow-visualization`,
`REQ-maneuver-validation`.

Add an epic-level note above the table stating that `intel/code-verification.md` records Maneuver and
the Flow DSL as verified shipped, and that Epic 17.5's CLI-location decision is applied in code —
`src/cli` is absent from the tree and `src/application/cli/` carries the full command set — and that
it remains at DOC precedence with no ADR. Confirm the absence of `src/cli` with `ls src/` before
writing that sentence.

For each row: resolve the citation and cite a real line number; search for the exercising artefact
with the 05-RESEARCH.md recipe; run the scoped command for anything you intend to verdict
`satisfied`, preferring `cargo test --offline -p paladin-battalion maneuver::` scoping; assign one of
the five legend verdicts.

Four rows have specific content to record:
- **`REQ-maneuver-domain-model`** — the constructor argument order differs between the Epic 17 PRD
  and the release notes. Read the shipped constructor signature from
  `crates/paladin-battalion/src/maneuver/mod.rs` and record the actual argument order, citing ADR-0010
  for the divergence rather than restating it. Verdict `superseded by shipped code` where the shipped
  form answers differently from the ingested one.
- **`REQ-maneuver-cli`** — the tree has a top-level `src/application/cli/commands/maneuver.rs` command
  group. Read the actual command shape it registers and record it against the PRD's alternative
  shape, citing ADR-0010.
- **`REQ-maneuver-error-strategy-v2`** — a variant-group member (group 15). Record the shipped error
  strategy and state which ingested position it answers.
- **`REQ-maneuver-validation`** — the run-2 ledger records construction-time validation as
  uninspected. Inspect it and record the actual finding, including any absence.

Three rows — `REQ-flow-dsl-syntax`, `REQ-flow-parser`, `REQ-flow-expression-ast` — all cite the same
`crates/paladin-battalion/src/maneuver/parser/` directory in the run-2 ledger. Resolve each to a
distinct file and line inside that directory so the three rows carry three distinct citations, not
one directory path repeated three times.

Where the tree reveals something the run-2 ledger missed, add a nested finding row prefixed
`**New finding (plan 05-11):**`.

Do not commit yet — this plan commits once, after Task 2.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Epic 17 /,/^### Epic 18 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT')" = "0" ] && [ "$(sed -n '/^### Epic 17 /,/^### Epic 18 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-')" = "11" ] && sed -n '/^### Epic 17 /,/^### Epic 18 /p' .planning/ledgers/milestone-02-03.md | grep -q '0010'</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Epic 17 /,/^### Epic 18 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `11`
    - `sed -n '/^### Epic 17 /,/^### Epic 18 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` still equals `118`
    - `sed -n '/^### Epic 17 /,/^### Epic 18 /p' .planning/ledgers/milestone-02-03.md | grep -c '0010'` is at least `2`
    - Every Epic 17 / 17.5 row's Evidence cell contains a `:` followed by a digit
    - Every Epic 17 / 17.5 row's Verdict cell holds exactly one of the five legend verdicts
    - The `REQ-flow-dsl-syntax`, `REQ-flow-parser` and `REQ-flow-expression-ast` rows cite three distinct `file:line` locations inside `crates/paladin-battalion/src/maneuver/parser/`, not one repeated directory path
    - The `REQ-maneuver-domain-model` row names the constructor argument order actually read from `maneuver/mod.rs`
    - The `REQ-maneuver-cli` row names the command shape actually registered by `src/application/cli/commands/maneuver.rs`
    - The epic-level note's claim that `src/cli` is absent is backed by an `ls src/` run during this task
    - The `REQ-maneuver-validation` row states an actual finding from inspection, including any absence
    - Every Epic 17 / 17.5 row verdicted `satisfied` names a test, example, or command, and that command was executed during this task
    - `git diff --stat -- '*.rs'` is empty for this task
  </acceptance_criteria>
  <done>All 11 Epic 17 / 17.5 rows are cited and verdicted with three distinct parser citations, the two release-notes divergences cite ADR-0010 against forms read from the tree, and Epic 17.5's applied CLI-location decision is recorded with its absence claim verified.</done>
</task>

<task type="auto">
  <name>Task 2: Fill Epic 19's 5 rows — Herald and domain type consolidation</name>
  <files>.planning/ledgers/milestone-02-03.md</files>
  <read_first>
    - `.planning/ledgers/milestone-02-03.md` §`### Epic 19 — Herald & Domain Type Consolidation (5 IDs)` — the stub rows
    - `.planning/decisions/0006-coverage-gate.md` as amended by plan 05-04 — the module-scoped gate table carrying Herald's transcribed measured figure, its target, its gap and its owner. **This row transcribes from the amended ADR; it does not re-measure and does not derive a second number.**
    - `.planning/phases/05-milestone-2-3-ground-truth/05-04-SUMMARY.md` — what plan 05-04 actually transcribed and from which measurement rows
    - `.planning/decisions/0010-milestone-3-epic-numbering.md` — the authoritative numbering; 19 is Herald & Domain Type Consolidation, which the release notes contradict
    - `.planning/intel/requirements.md` — the Epic 19 acceptance criteria, including `StreamChunk`'s six required fields, the `#[serde(flatten)]` metadata and builder, the `ExecutionMetadata` field set and `calculate_duration()`, and `HeraldRegistry: Default`
    - `crates/paladin-core/src/platform/container/herald.rs` (note `token_usage` referenced from around `:736`), `crates/paladin-core/src/platform/container/token_usage.rs`, `crates/paladin-herald/src/lib.rs`, `src/application/services/herald/`
    - `.planning/codebase/STRUCTURE.md` and `.planning/codebase/TESTING.md`
  </read_first>
  <action>
Fill all 5 Epic 19 rows in `.planning/ledgers/milestone-02-03.md`, replacing each row's stub verdict
token `PENDING-VERDICT` and its stub Evidence cell, using `Edit` with scoped replacements.

The 5 IDs, in the scaffold's fixed order: `REQ-herald-type-consolidation`,
`REQ-stream-chunk-complete`, `REQ-execution-metadata-complete`,
`REQ-herald-formatter-autoregistration`, `REQ-herald-consolidation-quality-gates`.

Add an epic-level note above the table stating that 19 = Herald & Domain Type Consolidation is the
authoritative numbering recorded in `.planning/decisions/0010-milestone-3-epic-numbering.md`, which
the release notes contradict — cite the ADR rather than restating the defect.

For each row: resolve the citation and cite a real line number; search for the exercising artefact,
preferring `grep -rn "fn test_.*herald" tests/ crates/` and the in-file `#[cfg(test)]` modules;
run the scoped command for anything you intend to verdict `satisfied`, preferring
`cargo test --offline -p paladin-core herald::` and `-p paladin-herald` scoping; assign one of the
five legend verdicts.

Four rows the run-2 ledger left uninspected must each be inspected here and record an actual finding,
including any absence:
- **`REQ-stream-chunk-complete`** — read `StreamChunk` in the Herald surface and record which of the
  six required fields are present, whether `#[serde(flatten)]` metadata is used, and whether a
  builder exists.
- **`REQ-execution-metadata-complete`** — read the `ExecutionMetadata` field set and check for
  `calculate_duration()`. Cite `token_usage.rs` and the `herald.rs` reference line.
- **`REQ-herald-formatter-autoregistration`** — read `crates/paladin-herald/src/lib.rs` and
  `src/application/services/herald/` and record whether `HeraldRegistry` implements `Default` and what
  auto-registration behaviour actually exists.
- **`REQ-herald-consolidation-quality-gates`** — do **not** re-measure and do **not** derive a
  coverage figure. Transcribe the measured figure, the 95% target, the gap and the owner from the
  amended `.planning/decisions/0006-coverage-gate.md` module-scoped gate table, cite that ADR, and
  verdict the row `deferred with reason` with Phase 15 / PIPE-02 named as owner — the disposition the
  amended ADR now records.

`REQ-herald-type-consolidation` is the later position on run-1 variant group 6. Confirm by grep that
no placeholder types and no `TODO` remain in
`crates/paladin-core/src/platform/container/herald.rs`, cite the result, and record the variant-group
resolution.

Where the tree reveals something the run-2 ledger missed, add a nested finding row prefixed
`**New finding (plan 05-11):**`.

Commit this plan's single file in one commit at the end, with a Bash timeout of at least 300000 ms —
`.pre-commit-config.yaml` sets `always_run: true` on `cargo-fmt` and `cargo-clippy` so they compile
the workspace even for a markdown-only commit. Do not pass `--no-verify`.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Epic 19 /,/^### Epic 21 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT')" = "0" ] && [ "$(sed -n '/^### Epic 19 /,/^### Epic 21 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-')" = "5" ] && [ "$(grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md)" = "118" ]</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Epic 19 /,/^### Epic 21 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `5`
    - `sed -n '/^### Epic 19 /,/^### Epic 21 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` still equals `118`
    - `sed -n '/^### Epic 19 /,/^### Epic 21 /p' .planning/ledgers/milestone-02-03.md | grep -c '0010'` is at least `1`
    - The `REQ-herald-consolidation-quality-gates` row cites `0006-coverage-gate.md`, names `PIPE-02`, and states a coverage figure identical to the one in the amended ADR's module-scoped gate table
    - The `REQ-herald-consolidation-quality-gates` row's Verdict cell is `deferred with reason`
    - No `cargo llvm-cov` or `cargo tarpaulin` invocation appears in this plan's execution record — no re-measurement occurred
    - Every Epic 19 row's Evidence cell contains a `:` followed by a digit
    - Every Epic 19 row's Verdict cell holds exactly one of the five legend verdicts
    - The `REQ-stream-chunk-complete`, `REQ-execution-metadata-complete` and `REQ-herald-formatter-autoregistration` rows each state an actual finding from inspection, including any absence
    - Every Epic 19 row verdicted `satisfied` names a test, example, or command, and that command was executed during this task
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
    - `git log -1 --name-only` shows only `.planning/ledgers/milestone-02-03.md`
  </acceptance_criteria>
  <done>All 5 Epic 19 rows are cited and verdicted, the four previously uninspected rows record actual findings, and the Herald coverage row transcribes the amended ADR-0006's single number rather than deriving a second one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/` ledger row → future implementer or agent | The only boundary. A verdict here is consumed downstream as fact. No runtime, network, process, or user-input boundary exists in this plan. |
| Amended ADR-0006 → `REQ-herald-consolidation-quality-gates` row | The ADR holds the single binding coverage number. A row that derives its own figure creates a second number in a phase whose purpose is to leave exactly one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering (of the record) | Epic 17 / Epic 19 rows | medium | mitigate | A row verdicted `satisfied` on a citation alone, or three rows sharing one directory path as their citation, would produce a ledger that reads complete while proving nothing. Mitigated by the D-01 evidence bar and by the distinct-citation criterion on the three parser rows. |
| T-05-18 | Tampering (of the record) | `REQ-herald-consolidation-quality-gates` | medium | mitigate | Deriving or re-measuring a Herald coverage figure here would put a second number in front of a reader, which is precisely the RECON-07 failure this milestone eliminated. Mitigated by the transcribe-from-ADR-0006 instruction, the no-re-measurement prohibition, and an acceptance criterion asserting no coverage tool was invoked. |
| T-05-12 | Repudiation | scaffold row set | low | mitigate | Inserting or reordering rows breaks the stable order later plans' scoped edits depend on. Mitigated by the 118-row count assertion. |
| T-05-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable and not fabricated: this plan installs no packages and touches no manifest. |

No high or critical threats. Under ASVS L1 with a blocking threshold of `high`, nothing in this plan
blocks.
</threat_model>

<artifacts>
## Artifacts this phase produces

Phase-wide artefact set (recorded in full in plan 05-01's `<artifacts>` section). This plan
contributes to one:

| Artefact | Status | This plan's contribution |
|---|---|---|
| `.planning/ledgers/milestone-02-03.md` | **NEW** (created by 05-01) | §`### Epic 17 / 17.5 — Flow DSL, Maneuver and CLI consolidation (11 IDs)` and §`### Epic 19 — Herald & Domain Type Consolidation (5 IDs)`: 16 cited `REQ-*` rows plus two epic-level notes, one recording Epic 17.5's applied CLI-location decision and one citing ADR-0010's authoritative numbering for Epic 19 |

New section headings introduced into existing documents: none. This plan writes inside two headings
the scaffold created.
</artifacts>

<verification>
- `sed -n '/^### Epic 17 /,/^### Epic 18 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `11`
- `sed -n '/^### Epic 19 /,/^### Epic 21 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `5`
- `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` equals `118`
- The Herald coverage figure in the ledger equals the one in `.planning/decisions/0006-coverage-gate.md`
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
- `git status --porcelain` is clean after the plan's single commit
</verification>

<success_criteria>
A developer looking up any of the 16 Maneuver/Flow-DSL or Herald requirement IDs sees a
`file:line`-cited verdict against the current crate layout, with the two remaining release-notes
divergences resolved against forms read from the tree and cited to ADR-0010, and the Herald coverage
gate carrying exactly the number the amended ADR-0006 records — one number, one scope, one owner.
</success_criteria>

<output>
Create `.planning/phases/05-milestone-2-3-ground-truth/05-11-SUMMARY.md` when done. Record: the
verdict distribution across the 16 rows, the shipped Maneuver constructor signature and CLI command
shape, the three distinct parser citations chosen, the findings for the four previously uninspected
Epic 19 rows, and every scoped command executed.
</output>
