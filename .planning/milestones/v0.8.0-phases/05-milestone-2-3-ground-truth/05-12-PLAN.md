---
phase: 05-milestone-2-3-ground-truth
plan: 12
type: execute
wave: 9
depends_on: ["05-01", "05-03", "05-04", "05-11"]
files_modified:
  - .planning/ledgers/milestone-02-03.md
autonomous: true
requirements: [VERIFY-01]
must_haves:
  truths:
    - "Epic 21's 7 rows and Epic 23's 10 rows in `.planning/ledgers/milestone-02-03.md` each carry a `file:line` citation resolved against the shipped tree and a verdict from the five-value legend; every row verdicted `satisfied` names a test, example, or command that was actually run. With this plan complete, all 118 rows are filled."
    - "`REQ-autonomous-completion-quality-gates` cites the amended `.planning/decisions/0006-coverage-gate.md` for its measured figure (or its recorded absence of one), its 90% target and its owner, verdicted `deferred with reason` with Phase 15 / PIPE-02 named — no coverage figure is derived or re-measured in this plan."
    - "`REQ-autonomous-configurable-model` records that the defect class was closed in `planning_service.rs` and `prompt_generation_service.rs` and that the same class survives in Grove, cross-referencing the Epic 22 `REQ-grove-llm-routing` row and Phase 6 CLOSE-01 rather than duplicating the finding."
    - "`REQ-cli-tiered-environment-testing` cites `.planning/decisions/0012-live-api-test-key-behaviour.md` for the contested Tier-3 skip semantics rather than restating the deadlock."
    - "`REQ-handoff-tool-v2` (Epic 21) and `REQ-handoff-tool-v1` (Epic 14, filled by plan 05-06) cite the same shipped handoff tool and do not assign contradictory verdicts to the same code; likewise `REQ-autonomous-completion-config-schema` (Epic 21) and `REQ-autonomous-configuration` (Epic 14)."
    - "`REQ-scheduler-port`'s version and API-surface divergence is recorded against the shipped `tokio-cron-scheduler` version and the shipped trait method names read from the tree, not transcribed from the PRD's pinned version."
  artifacts:
    - ".planning/ledgers/milestone-02-03.md §Epic 21 — 7 cited rows"
    - ".planning/ledgers/milestone-02-03.md §Epic 23 — 10 cited rows"
  key_links:
    - "`REQ-autonomous-completion-quality-gates` → `.planning/decisions/0006-coverage-gate.md` as amended by plan 05-04 — the second of the two module-scoped gates"
    - "`REQ-cli-tiered-environment-testing` → `.planning/decisions/0012-live-api-test-key-behaviour.md` — the recorded answer for Tier-3 skip semantics"
    - "`REQ-autonomous-configurable-model` → the Epic 22 `REQ-grove-llm-routing` row and Phase 6 CLOSE-01 — the surviving instance of a defect class this requirement closed elsewhere"
  prohibitions:
    - "The ledger must NOT award a `satisfied` verdict on the strength of a `file:line` citation alone."
    - "`REQ-autonomous-completion-quality-gates` must NOT record a coverage number other than the one the amended ADR-0006 carries, and must NOT trigger a re-measurement."
    - "No plan in this phase may edit any `.rs` file, `Cargo.toml`, or any file under `.github/workflows/`. In particular, do NOT fix the hardcoded routing model while recording that the defect class survives in Grove — that fix is Phase 6 CLOSE-01."
---

<objective>
Fill Epic 21 (Autonomous Agent Completion, 7 IDs) and Epic 23 (CLI, Config & Infrastructure
Completion, 10 IDs) — the last 17 ledger rows.

Purpose: this plan closes the fan-out. Epic 21 carries the second module-scoped coverage gate and the
requirement that closed the hardcoded-model defect class everywhere except Grove. Epic 23 is the
most reliably complete epic in the run-2 corpus — a useful counterweight, and the home of the
Tier-3 skip semantics that ADR-0012 now settles. When this plan commits, every one of the 118 rows
carries a verdict.
Output: Epic 21's and Epic 23's ledger sections, fully cited; the ledger has no unfilled rows left.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md
@.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md
@.planning/phases/05-milestone-2-3-ground-truth/05-RESEARCH.md
@.planning/phases/05-milestone-2-3-ground-truth/05-01-SUMMARY.md
@.planning/phases/05-milestone-2-3-ground-truth/05-03-SUMMARY.md
@.planning/phases/05-milestone-2-3-ground-truth/05-04-SUMMARY.md
@.planning/phases/05-milestone-2-3-ground-truth/05-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill Epic 21's 7 rows — autonomous agent completion</name>
  <files>.planning/ledgers/milestone-02-03.md</files>
  <read_first>
    - `.planning/ledgers/milestone-02-03.md` §`### Epic 21 — Autonomous Agent Completion (7 IDs)` — the stub rows; and §`### Epic 14` as filled by plan 05-06, so variant group 13 and the config-schema pair agree
    - `.planning/decisions/0006-coverage-gate.md` as amended by plan 05-04 — the module-scoped gate table's autonomous-components row. **This ledger row transcribes from the amended ADR; it does not re-measure and does not derive a second number.**
    - `.planning/phases/05-milestone-2-3-ground-truth/05-04-SUMMARY.md` — what plan 05-04 transcribed for the autonomous-components scope, including whether a measurement was found or recorded as absent
    - `.planning/phases/05-milestone-2-3-ground-truth/05-06-SUMMARY.md` — the Epic 14 verdicts for `REQ-handoff-tool-v1` and `REQ-autonomous-configuration`
    - `.planning/intel/requirements.md` — the Epic 21 acceptance criteria, including the `PaladinResult` field defaults, MessagePack support, the 100-record cap, the four-layer orchestration flow and per-layer config flags, the retry policy, cycle detection at depth, and the `E-HANDOFF-00x` error codes
    - `crates/paladin-core/src/platform/container/planning.rs`, `crates/paladin-core/src/platform/container/handoff.rs`, `crates/paladin-core/src/platform/container/autonomous_config.rs`, `crates/paladin-core/src/platform/container/arsenal/handoff_tool.rs`, `src/application/services/paladin/handoff_service.rs`, `src/application/services/paladin/paladin_execution_service.rs`, `tests/unit/handoff_service_test.rs`
    - `.planning/codebase/STRUCTURE.md` and `.planning/codebase/TESTING.md`
    - `.planning/phases/05-milestone-2-3-ground-truth/05-RESEARCH.md` §"Requirement → exercising-artefact search recipe"
  </read_first>
  <action>
Fill all 7 Epic 21 rows in `.planning/ledgers/milestone-02-03.md`, replacing each row's stub verdict
token `PENDING-VERDICT` and its stub Evidence cell, using `Edit` with scoped replacements. Never
`Write` the ledger file.

The 7 IDs, in the scaffold's fixed order: `REQ-handoff-tool-v2`,
`REQ-autonomous-configurable-model`, `REQ-paladin-result-autonomous-metadata`,
`REQ-autonomous-orchestration-layers`, `REQ-handoff-execution-integration`,
`REQ-autonomous-completion-config-schema`, `REQ-autonomous-completion-quality-gates`.

For each row: resolve the citation and cite a real line number; search for the exercising artefact
with the 05-RESEARCH.md recipe; run the scoped command for anything you intend to verdict
`satisfied`, preferring `cargo test --offline handoff` and `cargo test --offline -p paladin-core planning::`
scoping; assign one of the five legend verdicts.

Five rows have specific content to record:
- **`REQ-autonomous-completion-quality-gates`** — do **not** re-measure and do **not** derive a
  coverage figure. Transcribe the autonomous-components measured figure (or the recorded absence of
  one) plus the 90% target and the owner from the amended
  `.planning/decisions/0006-coverage-gate.md` module-scoped gate table, cite that ADR, and verdict the
  row `deferred with reason` with Phase 15 / PIPE-02 named. Additionally record that the
  requirement's "zero remaining TODO comments in autonomous agent code" clause is contradicted if
  Grove counts as in scope, cross-referencing the Epic 22 `REQ-grove-llm-routing` row.
- **`REQ-autonomous-configurable-model`** — record that the defect class was closed in
  `planning_service.rs` and `prompt_generation_service.rs` (cite both with line numbers confirming the
  configured model is used), and that the same class survives in
  `crates/paladin-battalion/src/grove_service.rs:537`, owned by Phase 6 CLOSE-01. Cross-reference the
  Epic 22 row rather than duplicating the finding, and do not edit the Rust file.
- **`REQ-handoff-tool-v2`** — variant group 13, paired with `REQ-handoff-tool-v1` (Epic 14, filled by
  plan 05-06). Cite the same shipped handoff tool plan 05-06 cited and state which ingested position
  it answers. The two rows must not assign contradictory verdicts to the same code.
- **`REQ-autonomous-completion-config-schema`** — the later position on `REQ-autonomous-configuration`
  (Epic 14). Cite the same shipped config artefact and record the pairing.
- **Three rows the run-2 ledger left uninspected** — `REQ-paladin-result-autonomous-metadata` (the
  `PaladinResult` field defaults, MessagePack support and the 100-record cap),
  `REQ-autonomous-orchestration-layers` (the four-layer flow and the per-layer config flags in
  `paladin_execution_service.rs`), and `REQ-handoff-execution-integration` (the retry policy, cycle
  detection at depth, and the `E-HANDOFF-00x` error codes). Inspect each with `grep -rn` and record
  the actual finding, including any absence.

Where the tree reveals something the run-2 ledger missed, add a nested finding row prefixed
`**New finding (plan 05-12):**`.

Do not commit yet — this plan commits once, after Task 2.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Epic 21 /,/^### Epic 22 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT')" = "0" ] && [ "$(sed -n '/^### Epic 21 /,/^### Epic 22 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-')" = "7" ]</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Epic 21 /,/^### Epic 22 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `7`
    - `sed -n '/^### Epic 21 /,/^### Epic 22 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` still equals `118`
    - The `REQ-autonomous-completion-quality-gates` row cites `0006-coverage-gate.md`, names `PIPE-02`, states a figure identical to the amended ADR's autonomous-components row (or its recorded absence), and has Verdict `deferred with reason`
    - No `cargo llvm-cov` or `cargo tarpaulin` invocation appears in this plan's execution record
    - The `REQ-autonomous-configurable-model` row cites both `planning_service.rs` and `prompt_generation_service.rs` with line numbers, names `grove_service.rs:537`, and names `CLOSE-01`
    - The `REQ-handoff-tool-v2` row cites the same shipped file as the Epic 14 `REQ-handoff-tool-v1` row, and the two verdicts are not mutually contradictory
    - The `REQ-paladin-result-autonomous-metadata`, `REQ-autonomous-orchestration-layers` and `REQ-handoff-execution-integration` rows each state an actual finding from inspection, including any absence
    - Every Epic 21 row's Evidence cell contains a `:` followed by a digit, and every Verdict cell holds exactly one of the five legend verdicts
    - Every Epic 21 row verdicted `satisfied` names a test, example, or command, and that command was executed during this task
    - `git diff --stat -- '*.rs'` is empty for this task
  </acceptance_criteria>
  <done>All 7 Epic 21 rows are cited and verdicted, the three previously uninspected rows record actual findings, the autonomous coverage row transcribes the amended ADR-0006 without deriving a second number, and the surviving Grove defect is cross-referenced rather than duplicated or fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Fill Epic 23's 10 rows — CLI, config and infrastructure completion</name>
  <files>.planning/ledgers/milestone-02-03.md</files>
  <read_first>
    - `.planning/ledgers/milestone-02-03.md` §`### Epic 23 — CLI, Config & Infrastructure Completion (10 IDs)` — the stub rows
    - `.planning/decisions/0012-live-api-test-key-behaviour.md` — written by plan 05-03; the recorded answer `REQ-cli-tiered-environment-testing` cites for the contested Tier-3 semantics
    - `.planning/intel/requirements.md` — the Epic 23 acceptance criteria, including the three `#[from]` variants on the CLI error type, the eight named tool-call-loop test cases, the scheduler trait's specified method names, and the PRD's pinned `tokio-cron-scheduler` version
    - `.planning/STATE.md` §"Deferred Items" — the MCP WebSocket transport and the Garrison semantic-search CLI-path items, both recorded as Epic 23 known limitations
    - `src/application/cli/commands/agent.rs`, `src/application/cli/error.rs`, `crates/paladin-memory/src/config/garrison.rs`, `crates/paladin-llm/src/mock.rs`, `crates/paladin-ports/src/output/scheduler_port.rs`, `crates/paladin-storage/src/scheduler.rs`, `crates/paladin-content/src/services/content_delivery_service.rs`, `tests/helpers/`, `tests/helpers/mock_arsenal_adapter.rs`, `tests/helpers/mod.rs`, `tests/cli/tool_integration_test.rs`, `tests/mcp_test_server.py`, `Cargo.toml`
    - `.planning/codebase/STRUCTURE.md` and `.planning/codebase/TESTING.md`
  </read_first>
  <action>
Fill all 10 Epic 23 rows in `.planning/ledgers/milestone-02-03.md`, replacing each row's stub verdict
token `PENDING-VERDICT` and its stub Evidence cell, using `Edit` with scoped replacements.

The 10 IDs, in the scaffold's fixed order: `REQ-cli-garrison-configuration`,
`REQ-cli-arsenal-configuration`, `REQ-mock-llm-adapter`, `REQ-cli-tiered-environment-testing`,
`REQ-scheduler-port`, `REQ-content-deliverer-scheduling`, `REQ-cli-error-types`,
`REQ-mock-arsenal-port`, `REQ-tool-call-loop-tests`, `REQ-mcp-gated-integration-tests`.

Add an epic-level note above the table recording that Epic 23 is the most reliably complete epic in
the run-2 corpus — the Milestone 3 plan was edited in place to mark it complete while its siblings
remained unchecked, and `tasks-task46-arsenal-tool-integration-tests.md` has the milestone's lowest
open-item count. Transcribe that count from `.planning/intel/task-completion-state.md` and cite it.

For each row: resolve the citation and cite a real line number; search for the exercising artefact,
preferring `grep -rn` inside `tests/cli/` and `tests/helpers/`; run the scoped command for anything
you intend to verdict `satisfied`, preferring `cargo test --offline --test cli` and
`cargo test --offline -p paladin-storage scheduler` scoping; assign one of the five legend verdicts.

Six rows have specific content to record:
- **`REQ-cli-tiered-environment-testing`** — the Tier-3 skip semantics are contested. Cite
  `.planning/decisions/0012-live-api-test-key-behaviour.md` as the recorded answer rather than
  restating the deadlock, and name Phase 6 CLOSE-03 as the owner of the doc-comment correction. Verify
  the Tier-1 suites under `tests/cli/` exist and cite them.
- **`REQ-scheduler-port`** — read the shipped `tokio-cron-scheduler` version from `Cargo.toml` and the
  shipped trait method names from `crates/paladin-ports/src/output/scheduler_port.rs`. The PRD pinned
  a different version and specified a different status-query method name. Record both the shipped
  version and the shipped method names read from the tree, and verdict
  `superseded by shipped code` where they answer differently from the PRD.
- **`REQ-content-deliverer-scheduling`** — the criterion is that no `unimplemented!()` remains in
  production paths. Re-derive this with `grep -rn 'unimplemented!' crates/ src/` and record which
  occurrences remain and whether each sits inside a `#[cfg(test)]` module or a doc comment. Do not
  transcribe the run-2 ledger's conclusion.
- **`REQ-cli-error-types`** — the three `#[from]` variants were left uninspected. Read
  `src/application/cli/error.rs` and record which variants are actually present.
- **`REQ-tool-call-loop-tests`** — the eight named test cases were not individually inspected. Read
  `tests/cli/tool_integration_test.rs`, count and name the test functions actually present, and record
  the measured count against the required eight.
- **`REQ-mcp-gated-integration-tests`** — the gated MCP STDIO tests and `tests/mcp_test_server.py` were
  left uninspected. Confirm whether that file exists and what gates the MCP tests, and record the
  finding including any absence. Note the MCP WebSocket transport limitation from STATE.md's deferred
  items where relevant.

Where the tree reveals something the run-2 ledger missed, add a nested finding row prefixed
`**New finding (plan 05-12):**`.

Commit this plan's single file in one commit at the end, with a Bash timeout of at least 300000 ms —
`.pre-commit-config.yaml` sets `always_run: true` on `cargo-fmt` and `cargo-clippy` so they compile
the workspace even for a markdown-only commit. Do not pass `--no-verify`. If a hook aborts, unstaged
work is stashed under `~/.cache/pre-commit/patch*` — restore from there rather than re-authoring 17
rows.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Epic 23 /,/^### Epic 24 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT')" = "0" ] && [ "$(sed -n '/^### Epic 23 /,/^### Epic 24 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-')" = "10" ] && [ "$(grep -c 'PENDING-VERDICT' .planning/ledgers/milestone-02-03.md)" = "0" ] && [ "$(grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md)" = "118" ]</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Epic 23 /,/^### Epic 24 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `10`
    - `grep -c 'PENDING-VERDICT' .planning/ledgers/milestone-02-03.md` equals `0` — **every one of the 118 rows is now filled** <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` equals `118`
    - The `REQ-cli-tiered-environment-testing` row cites `0012-live-api-test-key-behaviour.md` and names `CLOSE-03`
    - The `REQ-scheduler-port` row states the `tokio-cron-scheduler` version actually read from `Cargo.toml` and the trait method names actually read from `scheduler_port.rs`
    - The `REQ-content-deliverer-scheduling` row reports the actual result of `grep -rn 'unimplemented!' crates/ src/` run during this task
    - The `REQ-cli-error-types` row names the variants actually present in `src/application/cli/error.rs`
    - The `REQ-tool-call-loop-tests` row states a test-function count measured from `tests/cli/tool_integration_test.rs`
    - The `REQ-mcp-gated-integration-tests` row states whether `tests/mcp_test_server.py` exists, based on a check run during this task
    - Every Epic 23 row's Evidence cell contains a `:` followed by a digit, and every Verdict cell holds exactly one of the five legend verdicts
    - Every Epic 23 row verdicted `satisfied` names a test, example, or command, and that command was executed during this task
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
    - `git log -1 --name-only` shows only `.planning/ledgers/milestone-02-03.md`
  </acceptance_criteria>
  <done>All 10 Epic 23 rows are cited and verdicted, the four previously uninspected rows record measured findings, the Tier-3 semantics cite ADR-0012, and the ledger now carries a filled verdict for every one of its 118 rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/` ledger row → future implementer or agent | The only boundary. A verdict here is consumed downstream as fact. No runtime, network, process, or user-input boundary exists in this plan. |
| Amended ADR-0006 → `REQ-autonomous-completion-quality-gates` row | The ADR holds the single binding coverage number. A row that derives its own figure creates a second number. |
| `unimplemented!()` re-derivation → the production-path claim | The run-2 ledger's conclusion that no `unimplemented!()` remains in production paths is a claim about the shipped tree that this plan must re-derive rather than transcribe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering (of the record) | Epic 21 / Epic 23 rows | medium | mitigate | A row verdicted `satisfied` on a citation alone, or a production-path claim transcribed rather than re-derived, sends Phase 6 past real work. Mitigated by the D-01 evidence bar and by explicit re-derivation instructions on the `unimplemented!()`, test-count, and error-variant rows, each enforced as an acceptance criterion. |
| T-05-18 | Tampering (of the record) | `REQ-autonomous-completion-quality-gates` | medium | mitigate | Deriving or re-measuring an autonomous-components coverage figure would put a second number in front of a reader, recreating the RECON-07 failure. Mitigated by the transcribe-from-ADR-0006 instruction, the no-re-measurement prohibition, and an acceptance criterion asserting no coverage tool was invoked. |
| T-05-11 | Elevation of Privilege (scope) | `crates/paladin-battalion/src/grove_service.rs` | low | mitigate | Recording that the hardcoded-model defect class survives in Grove puts the fix directly in view in a phase that ships no code. Mitigated by the P4 prohibition, the cross-reference-don't-duplicate instruction, and a `git diff --stat` acceptance criterion over `*.rs`. |
| T-05-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable and not fabricated: this plan installs no packages and touches no manifest. `Cargo.toml` is read for the shipped `tokio-cron-scheduler` version and never modified. |

No high or critical threats. Under ASVS L1 with a blocking threshold of `high`, nothing in this plan
blocks.
</threat_model>

<artifacts>
## Artifacts this phase produces

Phase-wide artefact set (recorded in full in plan 05-01's `<artifacts>` section). This plan
contributes to one:

| Artefact | Status | This plan's contribution |
|---|---|---|
| `.planning/ledgers/milestone-02-03.md` | **NEW** (created by 05-01) | §`### Epic 21 — Autonomous Agent Completion (7 IDs)` and §`### Epic 23 — CLI, Config & Infrastructure Completion (10 IDs)`: 17 cited `REQ-*` rows plus an Epic 23 epic-level note. **This plan fills the last of the 118 rows.** |

New section headings introduced into existing documents: none. This plan writes inside two headings
the scaffold created.

**Fan-out gate:** with this plan complete, all 118 ledger rows carry a verdict and all three block
verdicts are written. Plan 05-13 closes the ledger out.
</artifacts>

<verification>
- `sed -n '/^### Epic 21 /,/^### Epic 22 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `7`
- `sed -n '/^### Epic 23 /,/^### Epic 24 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `10`
- `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` equals `118`
- No row in the ledger retains its scaffold stub verdict token
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
- `git status --porcelain` is clean after the plan's single commit
</verification>

<success_criteria>
A developer looking up any of the 17 autonomous-completion or CLI/config/infrastructure requirement
IDs sees a `file:line`-cited verdict against the current crate layout, with the previously
uninspected criteria actually measured, the two contested items resolving to this phase's own ADRs,
and the second module-scoped coverage gate carrying exactly the number the amended ADR-0006 records.
Every one of the 118 rows now carries a verdict.
</success_criteria>

<output>
Create `.planning/phases/05-milestone-2-3-ground-truth/05-12-SUMMARY.md` when done. Record: the
verdict distribution across the 17 rows, the shipped `tokio-cron-scheduler` version and trait method
names, the actual `unimplemented!()` occurrences and their contexts, the measured tool-call-loop test
count, the findings for the three previously uninspected Epic 21 rows, and every scoped command
executed.
</output>
