---
phase: 07-workspace-ground-truth-recorded-answers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ledgers/milestone-04-06.md
  - .planning/REQUIREMENTS.md
  - .planning/codebase/STRUCTURE.md
  - .planning/decisions/0014-milestone-4-6-tier-numbering.md
  - .project/Milestone_4-Refactor-Crates-Features/Milestone-Overview/Milesone-4-Tier-1-High-Value-Low-Risk.md
autonomous: true
requirements: [ARCH-01, ARCH-02]
must_haves:
  truths:
    - "`.planning/ledgers/milestone-04-06.md` exists and contains exactly 115 rows matching `^| REQ-`, one per run-3 requirement ID, distributed across exactly 13 `### Milestone ` epic section headings in REQUIREMENTS.md's own order (M4 Epics 1-3, M5 Epics 1-6, M6 Epics 1-4). [D-00d, D-00e]"
    - "Ledger row order is fixed by this scaffold and never re-sorted by a later plan: epic sections appear in REQUIREMENTS.md's own run-3 order and rows within a section appear in the ID order REQUIREMENTS.md lists them. Later plans replace a row's Verdict and Evidence cells in place; they never insert, delete, or reorder rows. [edge: ARCH-01/ordering]"
    - "A requirement with no locatable exercising artefact is never silently omitted and never left with an empty verdict cell: it receives `genuinely outstanding` when nothing in the tree satisfies it, or `present, unproven` when a citation resolves but nothing exercises it. Row count stays 115 from scaffold to close-out. [edge: ARCH-01/empty]"
    - "Each `REQ-*` ID appears exactly once in the ledger, while the same `file:line` citation MAY appear in more than one row — two requirements describing the same shipped artefact keep separate rows and separate verdicts because the `REQ-*` ID is the primary key (D-00e). A row that qualifies for both `relocated` and `superseded by shipped code` takes `relocated`, because D-02 exists to preserve the moved-not-missing signal. [edge: ARCH-01/adjacency]"
    - "Exactly one plan writes `.planning/ledgers/milestone-04-06.md` in any given wave (waves 1, 4, 5, 6, 7, 8 and 9 each contain exactly one ledger-writing plan; waves 2 and 3 contain none), so no two executors edit it concurrently. A plan interrupted part-way leaves every epic section it does not own still carrying its scaffold stub rows naming the owning plan, so the work is re-runnable without loss. [edge: ARCH-01/concurrency]"
    - "Milestone identity in these three source milestones is compared by directory / task-list number (4 = Tier 1, 5 = Tier 2, 6 = Tier 3), never by the title label a document gives itself. ADR-0014 states this and cites ADR-0010 as the precedent closing the corpus's first numbering defect. [edge: ARCH-02/encoding]"
    - "`.planning/REQUIREMENTS.md`'s `## Milestone 4-6 as-shipped ledger` section body is replaced by a pointer to the new ledger file (D-26 / D-00d), leaving no second diverging copy of the 115 verdicts in the corpus."
    - "`.planning/codebase/STRUCTURE.md`'s `## Directory Purposes` prose section describes all ten library crates plus `doc-examples`, so the third-precedence map cannot outrank the ledger it feeds (D-05)."
    - "Milestone 6 Epic 3's 9 rows each carry a `file:line` citation resolved against the shipped tree at authoring time, and every row verdicted `satisfied` also names a test, example, command, or CI job that was actually run or read this task (D-01)."
    - "`.project/Milestone_4-.../Milesone-4-Tier-1-High-Value-Low-Risk.md` still contains its original `Milestone 1` title text after the correction, alongside a dated correction banner naming ADR-0014 — proving annotation, not rewriting (D-00g)."
  artifacts:
    - ".planning/ledgers/milestone-04-06.md — new file: head notes, seven-value verdict legend, 13 epic sections, 115 keyed rows"
    - ".planning/decisions/0014-milestone-4-6-tier-numbering.md — new ADR, seven canonical headings, no frontmatter"
    - ".planning/REQUIREMENTS.md — `## Milestone 4-6 as-shipped ledger` reduced to a pointer paragraph"
    - ".planning/codebase/STRUCTURE.md — five crate entries added to the Directory Purposes prose section"
    - ".project/Milestone_4-Refactor-Crates-Features/Milestone-Overview/Milesone-4-Tier-1-High-Value-Low-Risk.md — dated correction banner plus inline title annotation"
  key_links:
    - "REQUIREMENTS.md pointer → .planning/ledgers/milestone-04-06.md — the redirect that stops a reader consuming the superseded inline verdicts"
    - "Ledger head note → .planning/codebase/STRUCTURE.md — the D-04(a) path caveat's resolution target for every historical `src/…` PRD path, which is why the map correction rides in this same plan"
    - "Ledger epic section headings → plans 07-06, 07-08, 07-10, 07-11, 07-12 — each later plan locates its rows by the exact `### Milestone N Epic M — ` heading text this scaffold fixes"
    - "ADR-0014 → ADR-0010 — the cited precedent that makes both numbering defects resolve with one convention"
  prohibitions:
    - "The correction to `.project/Milestone_4-.../Milesone-4-Tier-1-High-Value-Low-Risk.md` must NOT delete, replace, or rewrite the original defective text — the corpus's provenance across five ingest runs depends on the original surviving."
    - "The ledger must NOT award a `satisfied` verdict on the strength of a `file:line` citation alone, and must NOT carry REQUIREMENTS.md's ingest-era status words forward as if they were pre-satisfied verdicts."
    - "No task in this plan may edit any `.rs` file, `Cargo.toml`, or any file under `.github/` — every code consequence of this phase is routed to a later phase by name."
    - "The scaffold must NOT invent identifiers for outstanding task items; they nest under the `REQ-*` row they belong to (D-00e)."
---

<objective>
Create the Milestone 4-6 cited status ledger, correct the two records that would otherwise outrank it,
and prove the whole document pipeline end to end — scaffold shape, a row that meets the D-01 evidence
bar, an ADR that parses under `adr-parser.cjs`, and a `.project/` annotation in the D-00g pattern —
before twelve more plans expand into it.

Purpose: every later plan in this phase appends into a fixed shape. If the shape, the verdict
vocabulary, the evidence bar, the row order, the ADR heading set, or the correction markup is wrong,
it is wrong 115 rows and eight ADRs later. This plan lands all five layers once and walks a complete
epic and a complete ADR through them.
Output: `.planning/ledgers/milestone-04-06.md` with head notes, the seven-value legend, 13 epic
sections and 115 keyed rows (Milestone 6 Epic 3's 9 fully cited, 106 carrying stub verdicts);
REQUIREMENTS.md's Milestone 4-6 ledger section reduced to a pointer; `STRUCTURE.md`'s Directory
Purposes section corrected to ten library crates plus `doc-examples`; ADR-0014; and the Milestone 4
overview annotated.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-PATTERNS.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Ledger scaffold plus Milestone 6 Epic 3 fully cited — one epic end-to-end</name>
  <read_first>
    - `.planning/ledgers/milestone-02-03.md` lines 1-52 (head notes, `## Verdict legend`, `## Row order and amendment convention`) — the exact shape to copy; this is the sibling ledger at comparable scale, not a template to reinterpret
    - `.planning/ledgers/milestone-01.md` lines 1-19 and 152-160 — the first instance that established the shape
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-PATTERNS.md` §1 — the quoted head-note order, row format, and nested-item convention
    - `.planning/REQUIREMENTS.md` lines 2830-3068 — the 115 IDs, their 13 epic sections, and their ingest-era status text (the input, not the answer)
    - `.planning/codebase/STRUCTURE.md` — current crate layout, required to resolve every historical `src/…` PRD path
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md` §Decisions D-00d, D-00e, D-00f, D-01, D-02, D-03, D-04, D-05, D-06
  </read_first>
  <files>.planning/ledgers/milestone-04-06.md</files>
  <action>
Create `.planning/ledgers/milestone-04-06.md` with, in this order:

(1) Title `# Milestone 4-6 cited status ledger`.

(2) A supersession head note stating this file supersedes REQUIREMENTS.md's
`## Milestone 4-6 as-shipped ledger` section per D-26, and that Phases 10 and 13 add the sibling
ledgers `milestone-07-08.md` and `milestone-09-12.md`. Model on `milestone-02-03.md`'s own
supersession paragraph.

(3) A primary-key head note (D-00e): the `REQ-*` requirement ID is the primary key; outstanding task
items nest under the requirement they belong to rather than receiving invented identifiers. State
that the same `file:line` citation may legitimately appear in more than one row, and that two rows
are never merged because they cite the same artefact.

(4) An evidence-bar head note stating D-01 in full: a `satisfied` verdict requires a `file:line`
citation plus a named passing test, example, or command that exercises it; a citation with nothing
exercising it is `present, unproven`. State that the bar applies to all 115 rows without exception,
including every row REQUIREMENTS.md's run-3 ledger already marked with an ingest-era status word.
Then state D-01's manifest carve-out explicitly: for structural requirements — edition, feature-flag
shapes, dependency lists, `required-features`, workspace membership — the manifest line plus a named
CI job or build leg that consumes it satisfies the bar. Name the two exercising artefacts and
re-grep both line numbers before writing them: the `crate-isolation` job in
`.github/workflows/ci.yml` (confirmed at line 304 on 2026-08-06; `intel/code-verification.md`'s
citation of 228 is stale and must not be copied forward) and the workspace matrix build/test steps
plus the `cli_isolation` regression step in `.github/workflows/feature-flags.yml` (confirmed at
lines 115, 118 and 141). Add the sentence that a manifest fact cited without its consuming CI job is
`present, unproven`, not `satisfied`.

(5) A path-caveat head note stating both D-04 caveats once. (a) The `src/…` paths in the run-3 PRDs
are internally historical — Milestone 6 moved what Milestone 5 had just placed, and
`src/application/use_cases/` no longer exists at all; citations in this ledger are current
locations, resolved through `.planning/codebase/STRUCTURE.md` or the tree, never through a PRD.
(b) `STABLE_API.md`, `docs/FEATURE_FLAGS.md`, `docs/MIGRATION.md` and `docs/CONFIGURATION.md` ship
as mdbook chapters under `docs/src/api-reference/` and `docs/src/getting-started/installation.md`,
not at their PRD paths. State the consequence: a row whose only divergence is caveat (b) is
`relocated`, not a gap.

(6) A workspace-shape head note (D-05), authoritative for this phase: the workspace is ten library
crates — `paladin-core`, `paladin-ports`, `paladin-battalion`, `paladin-herald`, `paladin-llm`,
`paladin-memory`, `paladin-storage`, `paladin-notifications`, `paladin-content`, `paladin-web` —
plus a `doc-examples` crate plus the root facade package `paladin-ai`. State that this replaces both
the six crates the Milestone 5/6 overviews assume and the nine-crate figure this planning set
carried before run 3, and that the same correction lands in REQUIREMENTS.md's pointer and in
`.planning/codebase/STRUCTURE.md`.

(7) A per-milestone checkbox head note (D-06): Milestone 4 is 93.2% complete with 20 open items all
in Epic 2 and those are corroborated; Milestone 5 is 96.4% with 17 open, mostly contradicted by the
tree; Milestone 6 is 100% with 0 open, corroborated. State that the corpus's dominant
record-understates-the-tree heuristic does not apply uniformly here and must be applied per
milestone.

(8) A `## Verdict legend` H2 with a `| Verdict | Meaning |` table carrying seven rows in this order:
`satisfied`, `present, unproven`, `genuinely outstanding`, `deferred with reason`,
`superseded by shipped code`, `relocated`, `diverged`. Copy the five shared definitions verbatim
from `milestone-02-03.md`'s legend; write the two new definitions from D-02 — `relocated` means the
deliverable exists at a different path than the requirement names, `diverged` means the shipped tree
deliberately implements the requirement differently, as distinct from a later milestone replacing
it. Add a sentence mapping ROADMAP criterion 1's five names (satisfied, relocated, superseded,
diverged, genuinely outstanding) onto this seven-value vocabulary. Add the tie-break rule: a row
qualifying for both `relocated` and `superseded by shipped code` takes `relocated`.

(9) A `## Row order and amendment convention` H2 stating: the 13 epic sections appear in
REQUIREMENTS.md's own run-3 order and are never re-sorted; rows within a section appear in the ID
order REQUIREMENTS.md lists; later plans replace a row's Verdict and Evidence cells in place and
never insert, delete, or reorder rows; exactly one plan in this phase writes this file per execution
wave; amendments follow D-00f — edit in place, retain superseded text, date every amendment, never a
separate corrections file.

(10) Exactly these 13 `### ` epic section headings, in exactly this order and with exactly this
text: `### Milestone 4 Epic 1 — Feature Flag Expansion (7 IDs)`,
`### Milestone 4 Epic 2 — Port Trait Hardening & Stable API (9 IDs)`,
`### Milestone 4 Epic 3 — CLI Isolation (9 IDs)`,
`### Milestone 5 Epic 1 — Workspace Initialization & paladin-core (10 IDs)`,
`### Milestone 5 Epic 2 — paladin-ports Extraction (10 IDs)`,
`### Milestone 5 Epic 3 — paladin-battalion Extraction (9 IDs)`,
`### Milestone 5 Epic 4 — paladin-llm Extraction (11 IDs)`,
`### Milestone 5 Epic 5 — paladin-memory Extraction (10 IDs)`,
`### Milestone 5 Epic 6 — Workspace Finalization (6 IDs)`,
`### Milestone 6 Epic 1 — application_settings.rs Decomposition (8 IDs)`,
`### Milestone 6 Epic 2 — Orchestration Service Relocation (9 IDs)`,
`### Milestone 6 Epic 3 — Maneuver DSL Co-location (9 IDs)`,
`### Milestone 6 Epic 4 — CircuitBreaker Relocation to Infrastructure (8 IDs)`.

(11) Under each heading, a table with header row exactly `| ID | Verdict | Evidence |` and one row
per requirement ID, IDs transcribed in order from REQUIREMENTS.md lines 2830-3068. Every row starts
at column 0 with `| REQ-`. For the 106 rows outside Milestone 6 Epic 3, the Verdict cell holds the
literal stub token `PENDING-VERDICT` and the Evidence cell holds the plan number that will fill it,
per this assignment: Milestone 4 Epics 1, 2 and 3 → plan 07-12; Milestone 5 Epics 1 and 2 → plan
07-06; Milestone 5 Epics 3 and 4 → plan 07-10; Milestone 5 Epics 5 and 6 → plan 07-11; Milestone 6
Epics 1, 2 and 4 → plan 07-08.

(12) Then fill Milestone 6 Epic 3's 9 rows completely: `REQ-maneuver-submodule-structure`,
`REQ-maneuver-files-moved-from-core`, `REQ-maneuver-files-reorganized`, `REQ-maneuver-inline-tests`,
`REQ-core-maneuver-cleanup`, `REQ-maneuver-facade-reexports`,
`REQ-maneuver-battalion-import-updates`, `REQ-maneuver-battalion-lib-exports`,
`REQ-maneuver-cargo-dependency-check`. For each: resolve the citation against the current tree
(REQUIREMENTS.md lines 3035-3048 give the starting path; confirm the file exists and cite a real
line number, not a bare path), then search for an exercising artefact — grep the requirement's own
type, module, or function name across `crates/`, `src/`, `tests/` and `examples/` — then run a
scoped offline command for anything intended to be verdicted `satisfied`, preferring
`cargo test --offline -p paladin-battalion maneuver` over any full-workspace run. Assign one of the
seven legend verdicts. A row whose citation resolves but which nothing exercises is
`present, unproven`, and that is the expected outcome for a substantial fraction of the six
`Verify —` rows in this epic (D-03) — record it plainly rather than stretching for a `satisfied`.
Note above the table that this epic carries 0 open checkboxes, corroborated (D-06).

Write the whole file in one Write call. Do not commit per row — the repo's pre-commit hooks compile
the whole 12-crate workspace on every commit including markdown-only ones, so this plan commits once
at the end with a Bash timeout of at least 300000 ms. `worktree_skip_hooks` is already `true` in
`.planning/config.json`; do not pass `--no-verify`.
  </action>
  <verify>
    <automated>test -f .planning/ledgers/milestone-04-06.md &amp;&amp; [ "$(grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md)" = "115" ] &amp;&amp; [ "$(grep -c '^### Milestone ' .planning/ledgers/milestone-04-06.md)" = "13" ] &amp;&amp; [ "$(sed -n '/^### Milestone 6 Epic 3 /,/^### Milestone 6 Epic 4 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-')" = "9" ]</automated>
    <human-check>Read three Milestone 6 Epic 3 rows end to end and confirm the named exerciser asserts the requirement's behaviour rather than merely importing its symbol (07-VALIDATION.md §Manual-Only Verifications, row 1).</human-check>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/ledgers/milestone-04-06.md` succeeds
    - `grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md` equals `115`
    - `grep -c '^### Milestone ' .planning/ledgers/milestone-04-06.md` equals `13`
    - `grep -o '^| REQ-[a-z0-9-]*' .planning/ledgers/milestone-04-06.md | sort | uniq -d` prints nothing (no duplicate IDs)
    - `grep -n '^### Milestone ' .planning/ledgers/milestone-04-06.md` lists the headings in the order M4 E1, M4 E2, M4 E3, M5 E1, M5 E2, M5 E3, M5 E4, M5 E5, M5 E6, M6 E1, M6 E2, M6 E3, M6 E4
    - `grep -c '| ID | Verdict | Evidence |' .planning/ledgers/milestone-04-06.md` equals `13`
    - `grep -c '^## Verdict legend' .planning/ledgers/milestone-04-06.md` equals `1`
    - `grep -c '^## Row order and amendment convention' .planning/ledgers/milestone-04-06.md` equals `1`
    - `grep -c 'relocated' .planning/ledgers/milestone-04-06.md` is at least `1` and `grep -c 'diverged' .planning/ledgers/milestone-04-06.md` is at least `1` (both new legend rows present)
    - `sed -n '/^## Verdict legend/,/^## Row order/p' .planning/ledgers/milestone-04-06.md | grep -c '^| ' ` is at least `9` (header, separator, seven verdict rows)
    - `sed -n '/^### Milestone 6 Epic 3 /,/^### Milestone 6 Epic 4 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-'` equals `9`
    - `sed -n '/^### Milestone 6 Epic 3 /,/^### Milestone 6 Epic 4 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c 'PENDING-VERDICT' .planning/ledgers/milestone-04-06.md` equals `106`
    - `grep -c 'ci.yml:304' .planning/ledgers/milestone-04-06.md` is at least `1` and `grep -c 'ci.yml:228' .planning/ledgers/milestone-04-06.md` equals `0` (the stale citation is not copied forward)
    - `grep -c 'STRUCTURE.md' .planning/ledgers/milestone-04-06.md` is at least `1` (the D-04(a) caveat resolves to the current layout map)
    - `grep -c 'doc-examples' .planning/ledgers/milestone-04-06.md` is at least `1` (the D-05 workspace-shape note names the twelfth member)
    - Every Milestone 6 Epic 3 row's Evidence cell contains a `:` followed by a digit (a real line number, not a bare path)
    - Every Milestone 6 Epic 3 row verdicted `satisfied` names a test, example, command, or CI job, and that command was executed or that job line was read during this task
  </acceptance_criteria>
  <done>The ledger file exists with 115 keyed rows in 13 fixed-order epic sections, the seven-value verdict legend, both D-04 path caveats and the D-05 workspace shape stated once, the D-01 manifest carve-out named with freshly re-grepped CI line numbers, and Milestone 6 Epic 3's 9 rows carrying resolved citations and, where `satisfied`, an exerciser that was run.</done>
</task>

<task type="auto">
  <name>Task 2: Redirect REQUIREMENTS.md to the ledger and correct the crate map</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 2817-2828 — the `## Milestone 2-3 as-shipped ledger` pointer Phase 5 already wrote; same file, same convention, the exact text to model on
    - `.planning/REQUIREMENTS.md` lines 2830-3068 — the section body being replaced
    - `.planning/codebase/STRUCTURE.md` lines 245-300 — the `## Directory Purposes` prose section, its six existing crate entries, and the insertion gap between the `paladin-storage` entry and the `src/` entry
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-PATTERNS.md` §4 and §5 — the quoted pointer paragraph and the three-bullet per-crate entry shape
    - `.planning/ledgers/milestone-04-06.md` — the file created in Task 1, so the pointer's counts match reality
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/codebase/STRUCTURE.md</files>
  <action>
Two scoped edits, both with the Edit tool. Do not use Write on either file — REQUIREMENTS.md is
~4,000 lines and holds five ledger sections, and a whole-file write would destroy the others.

(A) Replace the body of REQUIREMENTS.md's `## Milestone 4-6 as-shipped ledger` section — everything
between that heading at line 2830 and the `## Milestone 7-8 as-shipped ledger` heading at line 3069
— with a pointer paragraph modelled on the Milestone 2-3 pointer at lines 2817-2828. Keep the
`## Milestone 4-6 as-shipped ledger` heading itself. The replacement body must state: that
per-requirement verdicts for Milestones 4, 5 and 6 now live in `.planning/ledgers/milestone-04-06.md`
written as the relative markdown link `ledgers/milestone-04-06.md`; that the move is per D-26; that
the ledger carries 115 `REQ-*` rows, each with a `file:line` citation and, where the verdict is
`satisfied`, a named passing test, example, command or CI job that exercises it; that the ledger
records the real workspace shape of ten library crates plus `doc-examples` plus the root facade
package `paladin-ai`; and that Phases 10 and 13 add the remaining sibling ledgers
`milestone-07-08.md` and `milestone-09-12.md` in the same directory. Do not state a nested
outstanding-item count — that is not known until every epic section is filled. Instead write the
literal sentence `Nested outstanding-item count is finalised by plan 07-13.` as its own sentence.

(B) In `.planning/codebase/STRUCTURE.md`'s `## Directory Purposes` section, insert five new crate
entries in the gap immediately after the `paladin-storage` entry and immediately before the `src/`
entry — one each for `crates/paladin-herald/`, `crates/paladin-notifications/`,
`crates/paladin-content/`, `crates/paladin-web/` and `crates/doc-examples/`. Each entry copies the
exact three-bullet shape of the existing `paladin-storage` entry: a bold backticked path heading,
then a Purpose bullet, a Contains bullet, and a Key files bullet. Read each crate's own
`Cargo.toml` and `src/lib.rs` for the actual purpose, contents and key files — do not invent generic
text, and use the project's Medieval Military vocabulary where the crate has a domain name (Herald
for output formatting). Do not rewrite or reorder the six existing entries, and do not touch the
ASCII directory tree higher in the file — it already lists all ten crates correctly, so a fix
applied there instead of here would be a no-op that leaves the actual defect unfixed. Add a one-line
dated note under the `## Directory Purposes` heading recording that the five entries were added by
Phase 7 per D-05 and that the section previously described six of the ten library crates.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^## Milestone 4-6 as-shipped ledger/,/^## Milestone 7-8 as-shipped ledger/p' .planning/REQUIREMENTS.md | grep -c '^| REQ-')" = "0" ] &amp;&amp; grep -q 'ledgers/milestone-04-06.md' .planning/REQUIREMENTS.md &amp;&amp; [ "$(sed -n '/^## Directory Purposes/,/^## /p' .planning/codebase/STRUCTURE.md | grep -c '^\*\*`crates/')" -ge "11" ]</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^## Milestone 4-6 as-shipped ledger/,/^## Milestone 7-8 as-shipped ledger/p' .planning/REQUIREMENTS.md | grep -c '^| REQ-'` equals `0` — no inline verdict rows survive in that section
    - `grep -c 'ledgers/milestone-04-06.md' .planning/REQUIREMENTS.md` is at least `1`
    - `grep -c '^## Milestone 4-6 as-shipped ledger' .planning/REQUIREMENTS.md` equals `1` — the heading is retained, only the body replaced
    - `grep -c '^## Milestone 2-3 as-shipped ledger' .planning/REQUIREMENTS.md` equals `1` and `grep -c '^## Milestone 7-8 as-shipped ledger' .planning/REQUIREMENTS.md` equals `1` — no neighbouring section was damaged
    - `sed -n '/^## Milestone 4-6 as-shipped ledger/,/^## Milestone 7-8 as-shipped ledger/p' .planning/REQUIREMENTS.md | grep -c '115'` is at least `1` — the pointer states the row count
    - `grep -c 'finalised by plan 07-13' .planning/REQUIREMENTS.md` equals `1`
    - `sed -n '/^## Directory Purposes/,/^## /p' .planning/codebase/STRUCTURE.md | grep -c '^\*\*`crates/'` is at least `11` (the `crates/` entry, six original crate entries, five new)
    - `sed -n '/^## Directory Purposes/,/^## /p' .planning/codebase/STRUCTURE.md` contains `paladin-herald`, `paladin-notifications`, `paladin-content`, `paladin-web` and `doc-examples`
    - `grep -c '^\*\*`crates/paladin-storage/`' .planning/codebase/STRUCTURE.md` equals `1` — the existing entries were extended, not duplicated
  </acceptance_criteria>
  <done>REQUIREMENTS.md's Milestone 4-6 ledger section is a pointer stating the 115-row count and the ten-crate shape, both neighbouring ledger sections intact; STRUCTURE.md's Directory Purposes prose section describes all ten library crates plus `doc-examples`.</done>
</task>

<task type="auto">
  <name>Task 3: ADR-0014 and the first source correction — proving the decision and annotation layers</name>
  <reversibility rating="costly">The corrected numbering becomes the provenance key every later reader and every Phase 8-16 citation uses; reverting means re-checking which of two numbering schemes each downstream citation meant.</reversibility>
  <read_first>
    - `.planning/decisions/0010-milestone-3-epic-numbering.md` — full read; the structural precedent for a numbering-collision ADR, including its mapping table, its bulleted `## Considered Options`, and its bulleted `## Code Locations`
    - `.planning/decisions/PROMOTION.md` — the required heading set, the numbering rule, and the five-step procedure
    - `.project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` lines 1-8 and 28-31 — the verbatim D-00g banner and inline-correction markup Phase 5 proved
    - `.project/Milestone_4-Refactor-Crates-Features/Milestone-Overview/Milesone-4-Tier-1-High-Value-Low-Risk.md` — the document being annotated, read in full before touching it
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md` §Decisions D-07, D-08, D-00g, D-00h
  </read_first>
  <files>.planning/decisions/0014-milestone-4-6-tier-numbering.md, .project/Milestone_4-Refactor-Crates-Features/Milestone-Overview/Milesone-4-Tier-1-High-Value-Low-Risk.md</files>
  <action>
(A) Create `.planning/decisions/0014-milestone-4-6-tier-numbering.md`. No frontmatter (D-00h).
Exactly seven H2 headings, in this order and with this text: `## Status`, `## Context`,
`## Decision`, `## Considered Options`, `## Code Locations`, `## Code Conformance`,
`## Downstream Consumers`. `## Status` is the bare word `Accepted` followed by a bold `**Date:**`
line, matching ADR-0010.

`## Context` states the collision: the Milestone 4 overview titles itself "Milestone 1: High-Value,
Low-Risk Foundations", the Milestone 5 overview titles itself "Milestone 2: Workspace
Decomposition", the Milestone 6 overview lists its prerequisites as "Completed in Milestones 1 and
2", and two Milestone 5 PRDs cross-reference the port hardening as "Milestone 1 / Epic 2" meaning
Milestone 4 Epic 2. State that this is the corpus's second numbering defect and that ADR-0010 closed
the first, and that `REQ-*` provenance keys only resolve uniformly if both close with the same
convention.

`## Decision` states the convention: the directory and task-list numbering is authoritative —
Milestone 4 is Tier 1, Milestone 5 is Tier 2, Milestone 6 is Tier 3 — and every "Milestone 1/2/3"
reference appearing inside these three milestones is a tier label, not a milestone number. State
explicitly that milestone identity is compared by directory number, never by the title a document
gives itself, and that the tier label is demoted to a descriptive detail. Include a small mapping
table with columns for the tier label, the authoritative milestone number, and the milestone's
subject.

`## Considered Options` is a bulleted list, never prose — one bullet per option with its
accept/reject verdict and reasoning inline: taking the tier labels as authoritative and renumbering
the directories (rejected); leaving both numbering schemes live and disambiguating per citation
(rejected); the chosen convention (accepted), citing ADR-0010 as the precedent it repeats.

`## Code Locations` is a bulleted list, never prose — one `file:line` citation per bullet, each
self-contained: the five materially distinct documents D-08 names (the three overviews plus
`Epic_4/prd-paladin-llm-extraction.md` Non-Goal 2 and `Epic_2/prd-paladin-ports-extraction.md` §1),
each with the line at which its numbering claim appears, re-grepped at authoring time rather than
copied from CONTEXT.md. Add a bullet pointing at `.planning/decisions/0010-milestone-3-epic-numbering.md`
as the cited precedent.

`## Code Conformance` is the bare word `conforms` followed by prose: this is a documentation defect,
not a code defect; no `.rs` file, `Cargo.toml` or workflow changes; the executing work is the source
corrections listed in `## Downstream Consumers`.

`## Downstream Consumers` is a bulleted list naming: plan 07-01 (this plan, the Milestone 4 overview
title correction); plan 07-03 (the eight byte-equivalent extract pointer banners and the
seven-versus-eight count reconciliation); plan 07-02 (`prd-paladin-ports-extraction.md` §1); plan
07-04 (`prd-paladin-llm-extraction.md` Non-Goal 2); plan 07-05 (the Milestone 6 overview
prerequisite line); plan 07-09 (the Milestone 5 overview title); and plan 07-13, which advances
`.planning/decisions/PROMOTION.md`'s next-free ADR number.

(B) Annotate `.project/Milestone_4-Refactor-Crates-Features/Milestone-Overview/Milesone-4-Tier-1-High-Value-Low-Risk.md`
per D-00g — annotation, never rewriting. Add a top-of-file blockquote banner immediately under the
existing H1, dated, naming ADR-0014, stating in one sentence that this document titles itself
"Milestone 1" while the authoritative numbering makes it Milestone 4 (Tier 1), linking to
`.planning/decisions/0014-milestone-4-6-tier-numbering.md` by relative path, and closing with the
sentence that original text is retained below with inline corrections and nothing is deleted. Then
correct the H1 itself with the strikethrough-and-append shape: the original title text struck
through, the corrected title appended after it, and a bold corrected-numbering note on the following
line citing ADR-0014. The original title text must survive inside the strikethrough markers. Do not
touch this document's MCP feature-flag acceptance criterion or Appendix B — plan 07-09 owns that
correction under ARCH-05(2), and this plan and that plan run in different waves precisely so they
never edit this file concurrently.

Commit this plan's five files in a single commit at the end of the plan, with a Bash timeout of at
least 300000 ms. Do not pass `--no-verify`.
  </action>
  <verify>
    <automated>test -f .planning/decisions/0014-milestone-4-6-tier-numbering.md &amp;&amp; [ "$(grep -c '^## ' .planning/decisions/0014-milestone-4-6-tier-numbering.md)" = "7" ] &amp;&amp; grep -q '0010' .planning/decisions/0014-milestone-4-6-tier-numbering.md &amp;&amp; grep -q 'ADR-0014' .project/Milestone_4-Refactor-Crates-Features/Milestone-Overview/Milesone-4-Tier-1-High-Value-Low-Risk.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/decisions/0014-milestone-4-6-tier-numbering.md` exists and `grep -c '^## '` on it equals `7`
    - `grep -n '^## ' .planning/decisions/0014-milestone-4-6-tier-numbering.md` lists the headings in the order Status, Context, Decision, Considered Options, Code Locations, Code Conformance, Downstream Consumers
    - `sed -n '/^## Considered Options/,/^## Code Locations/p' .planning/decisions/0014-milestone-4-6-tier-numbering.md | grep -c '^- '` is at least `3` — the section is a bulleted list, not prose
    - `sed -n '/^## Code Locations/,/^## Code Conformance/p' .planning/decisions/0014-milestone-4-6-tier-numbering.md | grep -c '^- '` is at least `6` — five source documents plus the ADR-0010 precedent, each its own bullet
    - `sed -n '/^## Code Conformance/,/^## Downstream Consumers/p' .planning/decisions/0014-milestone-4-6-tier-numbering.md | grep -cE '^(conforms|must change)$'` equals `1`
    - `head -1 .planning/decisions/0014-milestone-4-6-tier-numbering.md` does not equal `---` (no frontmatter, per D-00h)
    - `grep -c '0010' .planning/decisions/0014-milestone-4-6-tier-numbering.md` is at least `1`
    - `sed -n '/^## Downstream Consumers/,$p' .planning/decisions/0014-milestone-4-6-tier-numbering.md` names plans 07-02, 07-03, 07-04, 07-05, 07-09 and 07-13
    - `grep -c 'ADR-0014' .project/Milestone_4-Refactor-Crates-Features/Milestone-Overview/Milesone-4-Tier-1-High-Value-Low-Risk.md` is at least `2` — the banner and the inline title note
    - `grep -c 'Milestone 1' .project/Milestone_4-Refactor-Crates-Features/Milestone-Overview/Milesone-4-Tier-1-High-Value-Low-Risk.md` is at least `1` — the original claim text survives the correction
    - `git diff --numstat HEAD -- .project/Milestone_4-Refactor-Crates-Features/Milestone-Overview/Milesone-4-Tier-1-High-Value-Low-Risk.md` shows deletions no greater than `2` (only the H1 line rewritten in place; everything else additive)
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty after this plan's commit
  </acceptance_criteria>
  <done>ADR-0014 exists with the seven canonical headings, bulleted Considered Options and Code Locations, a `conforms` verdict, and a Downstream Consumers list naming every plan that executes a correction under it; the Milestone 4 overview carries a dated ADR-0014 banner and an inline title correction with its original text retained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/` record → future implementer or agent | The only boundary this phase has. A verdict, an ADR statement, or a corrected claim recorded here is consumed by the Phase 8-16 planners as fact. A mis-recorded verdict crosses into a real code change or a real omission downstream. |
| `.project/` historical corpus → the correction that annotates it | The corpus is the provenance base five completed ingest runs were built on. An edit that removes rather than annotates destroys evidence no later run can regenerate — the ingest is closed, there is no run 6. |

There is no runtime, network, process, or user-input boundary in this plan. It writes markdown.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering (of the record) | `.planning/ledgers/milestone-04-06.md` verdict cells | high | mitigate | A row recording an unwired or absent capability as `satisfied` would cause Phase 8 or Phase 15 to skip real work. Mitigated by D-01's two-part evidence bar enforced as this plan's acceptance criteria: every Milestone 6 Epic 3 Evidence cell carries a real line number, and every `satisfied` row names an exerciser that was executed. |
| T-07-02 | Repudiation | `.project/Milestone_4-.../Milesone-4-Tier-1-High-Value-Low-Risk.md` | high | mitigate | A correction that deletes the original "Milestone 1" title destroys the provenance five ingest runs cited. Mitigated by D-00g's annotate-never-rewrite rule enforced as a criterion: the original claim text must still be greppable after the edit, and the diff's deletion count is bounded. |
| T-07-03 | Repudiation | `.planning/REQUIREMENTS.md` §Milestone 4-6 ledger | medium | mitigate | Replacing the inline section could destroy the ingest-run-3 record. Mitigated because Task 1 transcribes all 115 IDs into the ledger before Task 2 runs (task order is fixed within this plan), the pointer names the successor file, and git retains the prior body. |
| T-07-04 | Elevation of privilege (of a stale record) | `.planning/codebase/STRUCTURE.md` | medium | mitigate | The map sits third in the precedence order, above `intel/` and every PRD. Left at six crates it would silently outrank the ledger that records ten. Mitigated by correcting the prose section in the same plan and asserting the crate names are present. |
| T-07-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable and not fabricated: this plan installs no packages and touches no manifest. `07-RESEARCH.md` §"Package Legitimacy Audit" records the same, verified. |

No critical threats. Under ASVS L1 with a blocking threshold of `high`, T-07-01 and T-07-02 are the
two that block, and both are mitigated by acceptance criteria in this plan rather than deferred.
</threat_model>

<probe_fallback_disposition>
This phase has no `*-SPEC.md`, so the spec-less probe fallback supplied the edge predicates and the
prohibitions were recalled in-prompt. The deterministic edge probe produced **19 applicable, 0
resolved, 19 unresolved**. The full phase-wide disposition is recorded here once, in the tracer
plan, so the no-silent-drop equality is auditable in one place. Each item is authored into exactly
one plan's `must_haves`.

| # | Requirement | Category | Resolution | Authored in |
|---|---|---|---|---|
| 1 | ARCH-01 | adjacency | covered | 07-01 |
| 2 | ARCH-01 | empty | covered | 07-01 |
| 3 | ARCH-01 | ordering | covered | 07-01 |
| 4 | ARCH-01 | concurrency | covered | 07-01 |
| 5 | ARCH-02 | encoding | covered | 07-01 |
| 6 | ARCH-02 | adjacency | covered | 07-03 |
| 7 | ARCH-02 | empty | covered | 07-03 |
| 8 | ARCH-02 | ordering | covered | 07-03 |
| 9 | ARCH-03 | adjacency | covered | 07-02 |
| 10 | ARCH-03 | empty | **backstop** | 07-02 |
| 11 | ARCH-03 | ordering | covered | 07-04 |
| 12 | ARCH-04 | adjacency | covered | 07-05 |
| 13 | ARCH-04 | empty | covered | 07-05 |
| 14 | ARCH-04 | ordering | covered | 07-05 |
| 15 | ARCH-05 | unclassified | **unresolved — flagged planner assumption** | 07-09 |
| 16 | ARCH-06 | adjacency | covered | 07-07 |
| 17 | ARCH-06 | empty | covered | 07-07 |
| 18 | ARCH-06 | ordering | covered | 07-07 |
| 19 | ARCH-07 | unclassified | **unresolved — flagged planner assumption** | 07-07 |

**No-silent-drop equality: 19 probe-surfaced = 16 covered + 1 backstop + 2 flagged assumptions.**
The two `unclassified` rows are never auto-`backstop`ped and never dropped.

Recalled prohibitions (Stage 2 survivors, authored **descriptor-less** — no `check_*` scalars, each
disposes flagged-unverified):

| # | Prohibition | Authored in |
|---|---|---|
| P1 | A `.project/` correction must not delete, replace, or rewrite the original text | 07-01, 07-02, 07-03, 07-04, 07-05, 07-09 |
| P2 | No `satisfied` verdict on a citation alone; ingest-era status words are not carried forward | 07-01, 07-06, 07-08, 07-10, 07-11, 07-12 |
| P3 | An ADR must not assert a position the shipped tree contradicts without a `must change` verdict naming its executor | 07-02, 07-04, 07-05, 07-07 |
| P4 | No plan in this phase may edit any `.rs`, `Cargo.toml`, or `.github/` file | every plan |
| P5 | No `REQ-*` row may be dropped, reordered, or merged with another ID citing the same artefact | 07-01, 07-06, 07-08, 07-10, 07-11, 07-12, 07-13 |
| P6 | ADR-0019 must not invent a tidy purpose for the `paladin` binary or tidy away the stale `structopt` name | 07-07 |
| P7 | ADR-0020 must not explain away the benchmark report's contradictory conclusion — it judges it | 07-07 |

Canon-referral drops: none. No Stage 2 survivor maps to OWASP, prototype pollution, path traversal,
injection, GDPR, or generic fairness canon — this phase's must-NOT axis is record honesty, which no
other tool owns.
</probe_fallback_disposition>

<assumption_delta_decision>
The detector fired (`{"detected": true}`, one `pluralization` signal on the word "second" in ROADMAP
criterion 5). The identity-model question it raises is real: this is the third ledger in the corpus
to use the `REQ-*` ID as its primary key, and ARCH-02 exists because the corpus carries a second
identity axis for milestones — the tier label versus the directory number.

**Decision: `promote`.** Directory / task-list numbering becomes the primary milestone identity
(Milestone 4 = Tier 1, 5 = Tier 2, 6 = Tier 3); the tier label demotes to a descriptive detail
recorded inside ADR-0014's mapping table. No second identity axis is added alongside, and the
`REQ-*` ID's role as the ledger primary key is unchanged. ADR-0014 states this in `## Decision`, and
edge item 5 (ARCH-02/encoding) is the `must_haves` truth that carries it.
</assumption_delta_decision>

<artifacts>
## Artifacts this phase produces

Every artefact Phase 7 creates or edits, with its owning plan. File paths are this phase's
"symbols". The thirteen-plan shape below follows D-27's suggested decomposition — scaffold, epic
fan-out, decisions, source corrections, bookkeeping — with the fan-out split so that exactly one plan
writes the ledger per wave, and with ADR-0016 pulled forward to wave 2 because ROADMAP records
Phase 8 / DEBT-05 as blocked on it. ADR numbering follows D-00a: one file per decision, flat
sequential, `PROMOTION.md`'s next-free line advanced once at the end by plan 07-13.

| Artefact | Status | Owning plan(s) | Requirement |
|---|---|---|---|
| `.planning/ledgers/milestone-04-06.md` | **NEW** — 115 `REQ-*` rows, 13 epic sections | 07-01 creates + M6 E3; 07-06, 07-08, 07-10, 07-11, 07-12 fill; 07-13 closes out | ARCH-01 |
| `.planning/REQUIREMENTS.md` §`## Milestone 4-6 as-shipped ledger` | **REDUCED TO A POINTER** (D-26) | 07-01 opens, 07-13 finalises counts | ARCH-01 |
| `.planning/codebase/STRUCTURE.md` §`## Directory Purposes` | **CORRECTED IN PLACE** — five crate entries added (D-05) | 07-01 | ARCH-01 |
| `.planning/decisions/0014-milestone-4-6-tier-numbering.md` | **NEW** ADR (ARCH-02) | 07-01 creates; 07-03 appends the count reconciliation | ARCH-02 |
| `.planning/decisions/0015-core-ports-dependency-allowlist.md` | **NEW** ADR (ARCH-03b) | 07-04 | ARCH-03 |
| `.planning/decisions/0016-port-value-type-ownership.md` | **NEW** ADR (ARCH-03c) — Phase 8 DEBT-05 blocker | 07-02 | ARCH-03 |
| `.planning/decisions/0017-llm-config-bridge-location.md` | **NEW** ADR (ARCH-03d) | 07-04 | ARCH-03 |
| `.planning/decisions/0018-m6-facade-reexport-policy.md` | **NEW** ADR (ARCH-04) | 07-05 | ARCH-04 |
| `.planning/decisions/0019-binary-target-architecture.md` | **NEW** ADR (ARCH-06) | 07-07 | ARCH-06 |
| `.planning/decisions/0020-build-benchmark-per-scenario.md` | **NEW** ADR (ARCH-07) | 07-07 | ARCH-07 |
| `.planning/decisions/0021-cli-application-layer-placement.md` | **NEW** ADR (D-25a) | 07-07 | ARCH-06 |
| `.planning/decisions/PROMOTION.md` | **AMENDED** — eight index rows, next-free line 0014 → 0022 | 07-13 | ARCH-02…ARCH-07 |
| `.planning/phases/07-workspace-ground-truth-recorded-answers/COVERAGE.md` | **NEW** — reasoned no-external-API declaration | 07-13 | — |
| `.project/Milestone_4-.../Milestone-Overview/Milesone-4-Tier-1-High-Value-Low-Risk.md` | **ANNOTATED** — title (07-01), MCP flag position (07-09) | 07-01, 07-09 | ARCH-02, ARCH-05 |
| `.project/Milestone_5-.../Epic_2/prd-paladin-ports-extraction.md` | **ANNOTATED** — FR-7/FR-10 and §1 | 07-02 | ARCH-03, ARCH-02 |
| `.project/Milestone_5-.../Epic_4/prd-paladin-llm-extraction.md` | **ANNOTATED** — FR-31/FR-32 and Non-Goal 2 | 07-04 | ARCH-03, ARCH-02 |
| `.project/Milestone_6-.../overview/Milestone_6-Tier_3-Architectural-Refinements.md` | **ANNOTATED** — prerequisite line, Epic 2 AC 6, Epic 4 AC 5, risk register | 07-05 | ARCH-02, ARCH-04 |
| `.project/Milestone_6-.../Epic_4/prd-relocate-circuitbreaker-infra.md` | **ANNOTATED** — Goal 7, FR-4.11, FR-4.12 re-pointing | 07-05 | ARCH-04, ARCH-05 |
| `.project/Milestone_4-.../Epic_1/prd-expand-feature-flags.md` | **ANNOTATED** — FR1 and Design Considerations, three positions | 07-09 | ARCH-05 |
| `.project/Milestone_5-.../overview/Milestone_5-Tier_2-Workspace-Decomposition.md` | **ANNOTATED** — title and the `paladin-cli` target structure / Appendix D | 07-09 | ARCH-02, ARCH-05 |
| `.project/Milestone_6-.../Epic_2/prd-relocate-orchestration-services.md` | **ANNOTATED** — Non-Goal 7, Open Question 4, `use_cases/` target | 07-09 | ARCH-04, ARCH-05 |
| Eight byte-equivalent extract documents under `.project/Milestone_5-.../Epic_{2,3,4,5}/` and `.project/Milestone_6-.../Epic_{1,2,3,4}/` | **ANNOTATED** — one-line dated pointer banner only | 07-03 | ARCH-02 |

New section headings created inside the new ledger file by this plan: `## Verdict legend`,
`## Row order and amendment convention`, and the 13 `### Milestone N Epic M — ` section headings
listed in Task 1. New headings created inside ADR-0014: the seven canonical H2s. No heading is added
to `REQUIREMENTS.md` or `STRUCTURE.md` — both keep their existing headings and only their bodies
change.
</artifacts>

<verification>
- `test -f .planning/ledgers/milestone-04-06.md`
- `grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md` equals `115`
- `grep -c '^### Milestone ' .planning/ledgers/milestone-04-06.md` equals `13`
- `grep -o '^| REQ-[a-z0-9-]*' .planning/ledgers/milestone-04-06.md | sort | uniq -d` prints nothing
- `sed -n '/^### Milestone 6 Epic 3 /,/^### Milestone 6 Epic 4 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-'` equals `9`
- `sed -n '/^## Milestone 4-6 as-shipped ledger/,/^## Milestone 7-8 as-shipped ledger/p' .planning/REQUIREMENTS.md | grep -c '^| REQ-'` equals `0`
- `grep -c '^## ' .planning/decisions/0014-milestone-4-6-tier-numbering.md` equals `7`
- `git status --porcelain` is clean after the plan's single commit
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty — this plan changed no code
</verification>

<success_criteria>
The Milestone 4-6 ledger exists in the shape every later plan appends into: 115 keyed rows in 13
fixed-order epic sections, the seven-value verdict legend, both D-04 path caveats, the D-05
workspace shape and the D-01 manifest carve-out stated once, and the row-order and amend-in-place
conventions written down. Milestone 6 Epic 3 is fully cited to the D-01 bar, proving the citation
workflow end to end. REQUIREMENTS.md no longer holds a second copy of the 115 verdicts, the crate
map no longer outranks the ledger with a six-crate figure, ADR-0014 parses under the seven-heading
convention, and the first `.project/` correction demonstrates the annotate-never-rewrite pattern the
next five correction plans repeat.
</success_criteria>

<output>
Create `.planning/phases/07-workspace-ground-truth-recorded-answers/07-01-SUMMARY.md` when done.
Record: the verdict distribution for Milestone 6 Epic 3, every scoped command actually executed, the
freshly re-grepped line numbers for `crate-isolation` and the `feature-flags.yml` legs, and any
Milestone 6 Epic 3 citation that did not resolve at the path REQUIREMENTS.md recorded.
</output>
