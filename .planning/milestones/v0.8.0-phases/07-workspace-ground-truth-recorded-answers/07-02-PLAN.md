---
phase: 07-workspace-ground-truth-recorded-answers
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - .planning/decisions/0016-port-value-type-ownership.md
  - .project/Milestone_5-Workspace-Decomposition/Epic_2/prd-paladin-ports-extraction.md
autonomous: false
requirements: [ARCH-03]
must_haves:
  truths:
    - "`.planning/decisions/0016-port-value-type-ownership.md` exists with the seven canonical H2 headings in order, no frontmatter, and bulleted `## Considered Options` and `## Code Locations` sections so `adr-parser.cjs`'s `splitEntries` yields structured entries (D-00h)."
    - "ADR-0016 names `crates/paladin-core/src/platform/container/token_usage.rs:13` as the canonical `TokenUsage` and names `crates/paladin-core/src/platform/container/battalion/mod.rs:497` and `crates/paladin-llm/src/llm_analysis_service.rs:51` as the two copies that become re-exports, with all three line numbers re-grepped at authoring time (D-11)."
    - "ADR-0016 carries `## Code Conformance: must change` and names Phase 8 / DEBT-05 as the executing requirement, so the ADR-outranks-code rule (D-00b) resolves to an instruction with an owner rather than a dangling contradiction (D-00c)."
    - "ADR-0016 records that it *is* the promotion of `.project/Milestone_5-Workspace-Decomposition/Epic_1/decisions/battalion-result-upward-dependency-decision.md`, cites that path as its provenance, and states that no `--manifest` re-tagging of the source document was performed — the source decision record is cited, never edited."
    - "The `REQ-port-value-type-ownership-v1` and `-v2` rows both survive in the ledger as separate rows: v1 is the shipped shape and v2 is `superseded by shipped code` pointing at ADR-0016. A losing variant is never deleted and never merged into its winner's row. [edge: ARCH-03/adjacency]"
    - statement: "A competing variant pair whose shipped tree matches neither recorded position is verdicted `diverged` with the divergence stated in the Evidence cell, rather than silently picking one side; and a pair whose answer already exists as an earlier ADR — ARCH-03(a), answered by ADR-0009 — produces a ledger citation and no new ADR. No such neither-position pair is known to exist among the four run-3 pairs, so the first half of this check cannot be positively demonstrated this phase."
      verification: backstop
    - "`.project/Milestone_5-.../Epic_2/prd-paladin-ports-extraction.md` still contains its original FR-7, FR-10 and §1 claim text after the correction, alongside a dated correction banner — proving annotation, not rewriting (D-00g)."
  artifacts:
    - ".planning/decisions/0016-port-value-type-ownership.md — new ADR, `must change`, Phase 8 / DEBT-05 named as executor"
    - ".project/Milestone_5-Workspace-Decomposition/Epic_2/prd-paladin-ports-extraction.md — dated correction banner plus inline annotation of FR-7, FR-10 and §1"
  key_links:
    - "ADR-0016 → Phase 8 / DEBT-05 — the blocked-on relationship ROADMAP records; DEBT-05 consolidates against this ADR's named canonical type"
    - "ADR-0016 → .project/Milestone_5-.../Epic_1/decisions/battalion-result-upward-dependency-decision.md — the DOC-typed Approved decision this ADR promotes without re-running a closed ingest"
    - "prd-paladin-ports-extraction.md FR-11 carve-out → FR-7/FR-10 annotation — the extension from RegistryError alone to PaladinResult, StopReason and TokenUsage"
    - "prd-paladin-ports-extraction.md §1 → ADR-0014 — the Milestone 1/Epic 2 cross-reference resolved by the numbering convention"
  prohibitions:
    - "The correction to `prd-paladin-ports-extraction.md` must NOT delete, replace, or rewrite FR-7, FR-10 or §1 — the original text is retained and marked superseded."
    - "ADR-0016 must NOT be authored as a fresh decision that ignores its `.project/` provenance, and must NOT re-tag that source document via `--manifest` — re-typing a `.project/` file would change how five completed ingest runs classified their corpus."
    - "ADR-0016 must NOT assert the canonical-type answer without a `must change` conformance verdict naming Phase 8 / DEBT-05 as its executor."
    - "This plan must NOT edit any `.rs` file, `Cargo.toml`, or any file under `.github/` — collapsing the two `TokenUsage` copies is Phase 8's work, not this phase's."
---

<objective>
Record the one answer in this corpus where mechanical precedence gives the architecturally wrong
result, and hand Phase 8 the target it is blocked on.

Purpose: the Epic 1 decision record is `Status: Approved` but manifest-typed DOC, so the Epic 2 PRD
published two days later outranks it — and that PRD's FR-10 would pull `PaladinResult`, `StopReason`
and `TokenUsage` back out of `paladin-core`, reintroducing the exact upward dependency the decision
was written to remove. Shipped code implements the decision record. ADR-0016 is the promotion that
puts the right answer at the top of the precedence order by construction, and the source PRD is
annotated so nobody applies FR-10 literally.
Output: `.planning/decisions/0016-port-value-type-ownership.md` and the annotated Epic 2 extraction
PRD. ROADMAP records Phase 8 / DEBT-05 as blocked until this plan lands, which is why it runs in
wave 2 and not at the end of the phase.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-PATTERNS.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the canonical owner of the port value types before ADR-0016 is written</name>
  <decision>Which crate owns `PaladinResult`, `StopReason` and `TokenUsage`, and therefore which of the three shipped `TokenUsage` structs Phase 8 / DEBT-05 collapses the other two into.</decision>
  <context>
This is a one-way door. Once DEBT-05 collapses the two duplicate copies into re-exports of the
canonical type, reversing means re-splitting a public type across two crates and re-doing the
consumer migration. It is also the one place in this corpus where the project's own precedence rule
produces the wrong answer on its own: the Epic 1 decision record that put these types in
`paladin-core` is `Status: Approved` but manifest-typed DOC, so the Epic 2 PRD published two days
later outranks it, and that PRD's FR-10 ("types must not be split across crates") would move them
back out. CONTEXT.md D-11 already resolved this to `paladin-core` — but D-11 was auto-resolved in
`--auto` mode and was never confirmed by a human, and CONTEXT.md's own header asks for review before
planning. This checkpoint exists to walk the door deliberately, not to re-open a settled question.
Shipped code today implements the decision record: `crates/paladin-core/src/platform/container/token_usage.rs:13`
is the canonical definition, with copies at `crates/paladin-core/src/platform/container/battalion/mod.rs:497`
and `crates/paladin-llm/src/llm_analysis_service.rs:51`.
  </context>
  <options>
    <option id="option-a">
      <name>Ratify the shipped answer — `paladin-core` owns (CONTEXT.md D-11)</name>
      <pros>Matches shipped code, matches the Approved Epic 1 decision record, removes the upward dependency, and needs no code change in this phase. ADR-0016 sits above the PRD by construction, so no ingest re-tagging is required.</pros>
      <cons>Phase 8 / DEBT-05 must collapse two duplicate structs, and that consolidation is expensive to reverse.</cons>
    </option>
    <option id="option-b">
      <name>Follow Epic 2 FR-10 — the ports crate owns, types not split across crates</name>
      <pros>Honours the highest-precedence document under the mechanical rule.</pros>
      <cons>Reintroduces the upward dependency the Epic 1 decision removed, contradicts every shipped call site, and requires a real code change this phase is explicitly forbidden from making.</cons>
    </option>
    <option id="option-c">
      <name>Record the conflict without picking a side</name>
      <pros>No one-way door is walked through in this phase.</pros>
      <cons>Leaves Phase 8 / DEBT-05 blocked with no target, which is the exact state ROADMAP records as this phase's only cross-phase dependency.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or option-c</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author ADR-0016 — port value-type ownership</name>
  <reversibility rating="one-way">ADR-0016 is what DEBT-05 consolidates against; once the two duplicate `TokenUsage` copies are collapsed into re-exports, reversing means re-splitting a public type across two crates.</reversibility>
  <read_first>
    - `.planning/decisions/PROMOTION.md` — the required heading set, the numbering rule, and the five-step promotion procedure (Part A) plus the candidate inventory (Part B)
    - `.planning/decisions/0009-workspace-rust-edition-2024.md` — a `must change` exemplar and the `## Downstream Consumers` shape that names another phase's requirement explicitly
    - `.project/Milestone_5-Workspace-Decomposition/Epic_1/decisions/battalion-result-upward-dependency-decision.md` — the Approved-but-DOC decision this ADR promotes; read in full, including its Rationale and Rejected Options sections
    - `.project/Milestone_5-Workspace-Decomposition/Epic_2/prd-paladin-ports-extraction.md` — FR-7, FR-10 and FR-11, the competing position and its existing `RegistryError` carve-out
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md` §Decisions D-11, D-00b, D-00c, D-00h
  </read_first>
  <files>.planning/decisions/0016-port-value-type-ownership.md</files>
  <action>
Create `.planning/decisions/0016-port-value-type-ownership.md`. No frontmatter. Exactly seven H2
headings in this order: `## Status`, `## Context`, `## Decision`, `## Considered Options`,
`## Code Locations`, `## Code Conformance`, `## Downstream Consumers`. `## Status` is the bare word
`Accepted` plus a bold `**Date:**` line.

`## Context` states the precedence trap in full: the Epic 1 decision record carries `Status:
Approved` and `Decision Date: 2026-05-13` but is manifest-typed DOC with `locked: false`, so the
Epic 2 PRD published two days later outranks it under the project's own precedence rule; that PRD's
FR-10 applied literally would move `PaladinResult`, `StopReason` and `TokenUsage` back out of
`paladin-core` and reintroduce the upward dependency the decision was written to remove. State that
the decision record settles the *location* of five types — `PaladinResult`, `StopReason`,
`TokenUsage`, `RegistryError`, `HandoffError` — and that despite its filename it never mentions
`BattalionResult`, so this ADR does not settle that variant. State that shipped code implements the
decision record.

`## Decision` states plainly that `paladin-core` owns these value types and the ports crate holds
thin re-exports; that Epic 2 FR-11's existing `RegistryError` carve-out is extended to
`PaladinResult`, `StopReason` and `TokenUsage`; and that this ADR *is* the promotion of the Epic 1
decision record — it restates the decision inside `.planning/decisions/`, where it sits at the top of
the precedence order by construction, and cites the DOC as its provenance rather than re-tagging it
via `--manifest`. State the reason for that choice in one sentence: re-typing a `.project/` file
would change how five completed ingest runs classified their corpus, for an outcome an ADR achieves
natively, and the ingest is closed with no run 6. Then state the DEBT-05 target explicitly: the
canonical `TokenUsage` is the `paladin-core` definition, and the two other shipped copies become
re-exports of it.

`## Considered Options` is a bulleted list, never prose — one bullet per option with its verdict and
reasoning inline: ratify the shipped answer and promote the decision record (accepted); apply Epic 2
FR-10 literally and move the types into the ports crate (rejected — reintroduces the upward
dependency and contradicts every shipped call site); re-tag the source document via `--manifest` so
mechanical precedence produces the right answer (rejected — changes a closed ingest's classification
for an outcome an ADR achieves natively); record the conflict without an answer (rejected — leaves
Phase 8 / DEBT-05 blocked).

`## Code Locations` is a bulleted list, never prose, one self-contained `file:line` citation per
bullet, every line number re-grepped against the tree at authoring time rather than copied from
CONTEXT.md: the canonical `TokenUsage` at `crates/paladin-core/src/platform/container/token_usage.rs`;
the duplicate in `crates/paladin-core/src/platform/container/battalion/mod.rs`; the duplicate in
`crates/paladin-llm/src/llm_analysis_service.rs`; the `PaladinResult` / `StopReason` definitions in
`crates/paladin-core/src/platform/container/execution_result.rs`; `RegistryError` and
`HandoffError`; the Epic 1 decision record's `.project/` path as this ADR's provenance; and the
Epic 2 PRD's FR-7, FR-10 and FR-11 lines as the corrected source.

`## Code Conformance` is the bare phrase `must change` followed by prose naming Phase 8's DEBT-05 as
the executing requirement, stating that the required change is collapsing the two duplicate
definitions into re-exports of the canonical type, and stating that no code changes in this phase.

`## Downstream Consumers` is a bulleted list naming: Phase 8 / DEBT-05 explicitly as blocked on this
ADR by number; plan 07-06, whose ledger rows for `REQ-port-value-type-ownership-v1` and `-v2` cite
this ADR; and plan 07-13, which adds this ADR's row to `PROMOTION.md` and advances the next-free
number. Do not name Phase 8's CLI-isolation work here — that is ADR-0019's downstream entry.
  </action>
  <verify>
    <automated>test -f .planning/decisions/0016-port-value-type-ownership.md &amp;&amp; [ "$(grep -c '^## ' .planning/decisions/0016-port-value-type-ownership.md)" = "7" ] &amp;&amp; grep -q 'DEBT-05' .planning/decisions/0016-port-value-type-ownership.md &amp;&amp; [ "$(sed -n '/^## Code Locations/,/^## Code Conformance/p' .planning/decisions/0016-port-value-type-ownership.md | grep -c '^- ')" -ge "6" ]</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/decisions/0016-port-value-type-ownership.md` exists and `grep -c '^## '` on it equals `7`
    - `grep -n '^## ' .planning/decisions/0016-port-value-type-ownership.md` lists Status, Context, Decision, Considered Options, Code Locations, Code Conformance, Downstream Consumers in that order
    - `head -1 .planning/decisions/0016-port-value-type-ownership.md` does not equal `---` (no frontmatter)
    - `sed -n '/^## Considered Options/,/^## Code Locations/p' .planning/decisions/0016-port-value-type-ownership.md | grep -c '^- '` is at least `4`
    - `sed -n '/^## Code Locations/,/^## Code Conformance/p' .planning/decisions/0016-port-value-type-ownership.md | grep -c '^- '` is at least `6`
    - `sed -n '/^## Code Locations/,/^## Code Conformance/p' .planning/decisions/0016-port-value-type-ownership.md | grep -c ':[0-9]'` is at least `5` — citations carry real line numbers, not bare paths
    - `sed -n '/^## Code Conformance/,/^## Downstream Consumers/p' .planning/decisions/0016-port-value-type-ownership.md | grep -c '^must change$'` equals `1`
    - `grep -c 'DEBT-05' .planning/decisions/0016-port-value-type-ownership.md` is at least `2` — the conformance executor and the downstream-consumer entry
    - `grep -c 'battalion-result-upward-dependency-decision.md' .planning/decisions/0016-port-value-type-ownership.md` is at least `1` — the provenance is cited
    - `grep -c 'token_usage.rs' .planning/decisions/0016-port-value-type-ownership.md` is at least `1`
    - Every `file:line` in `## Code Locations` resolves: opening each cited path at the cited line shows the named symbol
  </acceptance_criteria>
  <done>ADR-0016 exists with the seven canonical headings, bulleted Considered Options and Code Locations carrying freshly verified `file:line` citations, a `must change` verdict naming Phase 8 / DEBT-05, and an explicit record that it promotes the Epic 1 decision record without re-tagging it.</done>
</task>

<task type="auto">
  <name>Task 3: Annotate the Epic 2 ports-extraction PRD — FR-7, FR-10 and the §1 cross-reference</name>
  <read_first>
    - `.project/Milestone_5-Workspace-Decomposition/Epic_2/prd-paladin-ports-extraction.md` — read in full before touching it; locate FR-7, FR-10, FR-11 and the §1 "Milestone 1 / Epic 2" cross-reference by grep, not by remembered line number
    - `.project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` lines 1-8, 28-31 and 360 — the three verbatim D-00g markup shapes Phase 5 proved: top-of-file banner, inline strikethrough plus bold note, standalone Superseded banner
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-PATTERNS.md` §6 — the FR-level correction shape and the correction-target table row for this document
    - `.planning/decisions/0016-port-value-type-ownership.md` — the ADR just written, so the annotation cites it accurately
    - `.planning/decisions/0014-milestone-4-6-tier-numbering.md` — the numbering ADR the §1 correction cites
  </read_first>
  <files>.project/Milestone_5-Workspace-Decomposition/Epic_2/prd-paladin-ports-extraction.md</files>
  <action>
Annotate this one document per D-00g — annotation, never rewriting. This plan owns the whole file so
no other plan edits it; make all three corrections in one pass.

(1) Add a top-of-file blockquote banner immediately under the existing H1, dated, naming both
ADR-0016 and ADR-0014, stating in two sentences that FR-7 and FR-10 as written would move
`PaladinResult`, `StopReason` and `TokenUsage` out of `paladin-core` and reintroduce the upward
dependency the Epic 1 decision removed, and that §1's "Milestone 1 / Epic 2" cross-reference means
Milestone 4 Epic 2 under the authoritative numbering. Link both ADRs by relative path. Close with
the sentence that original text is retained below with inline corrections and nothing is deleted.

(2) Correct FR-7 and FR-10 inline using the FR-level shape: the original requirement text struck
through, followed on the next line by a bold corrected note citing ADR-0016 and stating that FR-11's
core-re-export carve-out — which already exempts `RegistryError` — extends to `PaladinResult`,
`StopReason` and `TokenUsage`, so the ports crate holds thin re-exports of the `paladin-core`
definitions. State that the original text is retained above and superseded. Do not modify FR-11
itself; cite it.

(3) Correct §1's "Milestone 1 / Epic 2" cross-reference inline with the same shape: the original
phrase struck through, the corrected reference appended, and a bold note citing ADR-0014 stating
that inside these three milestones "Milestone 1" is the Tier 1 label for Milestone 4.

Do not restate this document as authoritative anywhere in the annotation — a `.project/` correction
records what was claimed and what corrects it, and points upward at the ADR. Commit this plan's two
files together in a single commit at the end of the plan, with a Bash timeout of at least
300000 ms.
  </action>
  <verify>
    <automated>grep -q 'ADR-0016' .project/Milestone_5-Workspace-Decomposition/Epic_2/prd-paladin-ports-extraction.md &amp;&amp; grep -q 'ADR-0014' .project/Milestone_5-Workspace-Decomposition/Epic_2/prd-paladin-ports-extraction.md &amp;&amp; [ "$(git diff --numstat HEAD -- .project/Milestone_5-Workspace-Decomposition/Epic_2/prd-paladin-ports-extraction.md | awk '{print $2}')" -le "5" ]</automated>
    <human-check>Open the annotated FR-7 and FR-10 and confirm the original requirement sentences are still readable inside the strikethrough markers and that the corrected note names ADR-0016 (07-VALIDATION.md §Manual-Only Verifications, row 2).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ADR-0016' .project/Milestone_5-Workspace-Decomposition/Epic_2/prd-paladin-ports-extraction.md` is at least `3` — the banner plus the FR-7 and FR-10 notes
    - `grep -c 'ADR-0014' .project/Milestone_5-Workspace-Decomposition/Epic_2/prd-paladin-ports-extraction.md` is at least `2` — the banner plus the §1 note
    - `grep -c 'FR-11' .project/Milestone_5-Workspace-Decomposition/Epic_2/prd-paladin-ports-extraction.md` is at least `2` — the existing FR-11 plus the carve-out citation in the corrections
    - `grep -c '~~' .project/Milestone_5-Workspace-Decomposition/Epic_2/prd-paladin-ports-extraction.md` is at least `3` — three strikethrough corrections, each retaining its original text
    - `git diff --numstat HEAD -- .project/Milestone_5-Workspace-Decomposition/Epic_2/prd-paladin-ports-extraction.md` shows deletions no greater than `5` — the edit is additive apart from the three lines rewritten in place
    - `git log -1 --name-only` shows both `.planning/decisions/0016-port-value-type-ownership.md` and the annotated PRD in a single commit
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
  </acceptance_criteria>
  <done>The Epic 2 ports-extraction PRD carries a dated banner naming ADR-0016 and ADR-0014, FR-7 and FR-10 annotated with the extended FR-11 carve-out, and §1's cross-reference corrected — with every original claim still present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADR → future implementer or agent | An ADR outranks shipped code in this project's precedence order (D-00b), so an ADR is read as an instruction to change code. ADR-0016 is the specific one Phase 8 executes against. |
| `.project/` historical corpus → the correction that annotates it | The Epic 2 PRD is provenance five ingest runs cited. An edit that removes rather than annotates destroys evidence no later run can regenerate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Tampering (of the record) | `.planning/decisions/0016-port-value-type-ownership.md` `## Code Locations` | high | mitigate | An ADR naming the wrong file or a stale line as canonical would send DEBT-05 to consolidate into a type that is not the shipped one. Mitigated by requiring every citation to be re-grepped at authoring time and asserting that each cited line shows the named symbol. |
| T-07-06 | Elevation of privilege (of a contradicted ADR) | ADR-0016 `## Code Conformance` | high | mitigate | An ADR that contradicts shipped code without a `must change` verdict is later executed literally as an instruction with no owner. Mitigated by D-00c enforced as a criterion: the bare phrase `must change` plus Phase 8 / DEBT-05 named twice. |
| T-07-07 | Repudiation | `prd-paladin-ports-extraction.md` FR-7 / FR-10 / §1 | high | mitigate | Deleting the superseded FR text would erase the position ADR-0016 exists to overturn, leaving the ADR's Context unverifiable. Mitigated by the strikethrough-retention criterion and a bounded deletion count in the diff. |
| T-07-08 | Information disclosure | none | n/a | n/a | This plan writes markdown about type ownership. No secret, credential, or user datum crosses any boundary. Recorded explicitly rather than padded with an inapplicable OWASP entry. |
| T-07-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable: no package installed, no manifest touched. `07-RESEARCH.md` §"Package Legitimacy Audit" records the same. |

No critical threats. T-07-05, T-07-06 and T-07-07 are `high` and each is mitigated by an acceptance
criterion inside this plan rather than deferred.
</threat_model>

<artifacts>
## Artifacts this phase produces

The full phase-wide artefact table is recorded once in `07-01-PLAN.md` §Artifacts. This plan
produces two of its rows:

| Artefact | Status | Owning plan | Requirement |
|---|---|---|---|
| `.planning/decisions/0016-port-value-type-ownership.md` | **NEW** ADR — `must change`, Phase 8 / DEBT-05 named as executor | 07-02 | ARCH-03 |
| `.project/Milestone_5-Workspace-Decomposition/Epic_2/prd-paladin-ports-extraction.md` | **ANNOTATED IN PLACE** — banner, FR-7, FR-10, §1 | 07-02 | ARCH-03, ARCH-02 |

New headings created by this plan: the seven canonical H2s inside ADR-0016. No heading is added to
the annotated PRD — its existing structure is preserved and only annotated.
</artifacts>

<verification>
- `grep -c '^## ' .planning/decisions/0016-port-value-type-ownership.md` equals `7`
- `sed -n '/^## Code Conformance/,/^## Downstream Consumers/p' .planning/decisions/0016-port-value-type-ownership.md | grep -c '^must change$'` equals `1`
- `grep -c 'DEBT-05' .planning/decisions/0016-port-value-type-ownership.md` is at least `2`
- `grep -c 'ADR-0016' .project/Milestone_5-Workspace-Decomposition/Epic_2/prd-paladin-ports-extraction.md` is at least `3`
- `git diff --numstat HEAD~1 -- .project/Milestone_5-Workspace-Decomposition/Epic_2/prd-paladin-ports-extraction.md` shows deletions no greater than `5`
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
</verification>

<success_criteria>
Phase 8 is unblocked: ADR-0016 exists, sits at the top of the precedence order by construction, names
`crates/paladin-core/src/platform/container/token_usage.rs` as the canonical `TokenUsage`, carries a
`must change` verdict naming DEBT-05 as its executor, and records that it promotes the Epic 1
decision record without re-tagging a closed ingest. The Epic 2 PRD can no longer be applied literally
without a reader meeting the correction first, and its original FR-7, FR-10 and §1 text is intact.
</success_criteria>

<output>
Create `.planning/phases/07-workspace-ground-truth-recorded-answers/07-02-SUMMARY.md` when done.
Record: the checkpoint decision and who approved it, every `file:line` citation as re-grepped
(including any that had drifted from the value CONTEXT.md recorded), and the exact FR/section line
numbers annotated in the PRD.
</output>
