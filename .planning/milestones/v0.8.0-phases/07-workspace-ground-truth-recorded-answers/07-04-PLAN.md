---
phase: 07-workspace-ground-truth-recorded-answers
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - .planning/decisions/0015-core-ports-dependency-allowlist.md
  - .planning/decisions/0017-llm-config-bridge-location.md
  - .project/Milestone_5-Workspace-Decomposition/Epic_4/prd-paladin-llm-extraction.md
autonomous: true
requirements: [ARCH-03]
must_haves:
  truths:
    - "`.planning/decisions/0015-core-ports-dependency-allowlist.md` separates the enforceable invariant from the list: it states that `paladin-core` and `paladin-ports` may carry no provider SDK, transport client, storage driver or web framework, records that the invariant holds today, and records the measured current dependency sets as the new baseline rather than as debt (D-10)."
    - "ADR-0015's dependency counts are measured from `crates/paladin-core/Cargo.toml` and `crates/paladin-ports/Cargo.toml` during this task, not transcribed — `paladin-ports` carries eleven entries including `mime_guess`, correcting `intel/code-verification.md`'s figure of ten."
    - "ADR-0015 gives `tokio` in `paladin-core` its own written justification, because it is an async runtime inside a crate documented as having zero external dependencies and is the one entry a future purity review would reasonably challenge (D-10 iii)."
    - "`.planning/decisions/0017-llm-config-bridge-location.md` accepts the shipped bridge at `crates/paladin-llm/src/config/bridge.rs` and states that Epic 4's circular-dependency concern was real but mis-sited — Milestone 6 moved the config types down into `paladin-llm` rather than moving the bridge up into the root, removing the need for the boundary rather than breaking it (D-12)."
    - "ADR numbers are allocated monotonically across this phase — 0014 through 0021, one subject per file — and ADR-0016 remains a separately citable file rather than a section inside a combined document, because Phase 8 depends on it by number. No number is reused and no number is skipped. [edge: ARCH-03/ordering]"
    - "`.project/Milestone_5-.../Epic_4/prd-paladin-llm-extraction.md` still contains its original FR-31, FR-32 and Non-Goal 2 claim text after the correction, alongside a dated correction banner — proving annotation, not rewriting (D-00g)."
  artifacts:
    - ".planning/decisions/0015-core-ports-dependency-allowlist.md — new ADR, `conforms`, Phase 15 named as the enforcement candidate"
    - ".planning/decisions/0017-llm-config-bridge-location.md — new ADR, `conforms`"
    - ".project/Milestone_5-Workspace-Decomposition/Epic_4/prd-paladin-llm-extraction.md — dated banner plus a Superseded block on FR-31/FR-32 and an inline correction on Non-Goal 2"
  key_links:
    - "ADR-0015 → Phase 15 — the `cargo tree`-based allowlist check recorded as a candidate, not built here (D-10)"
    - "ADR-0015 → .project/Milestone_5-.../Epic_1/prd-workspace-initialization-and-paladin-core-extraction.md FR-6 + Appendix B — the exhaustive-at-six allowlist this ADR replaces as baseline"
    - "ADR-0017 → crates/paladin-llm/src/config/bridge.rs — the shipped location the ADR ratifies"
    - "prd-paladin-llm-extraction.md Non-Goal 2 → ADR-0014 — the Milestone 1 / Epic 2 cross-reference resolved by the numbering convention"
  prohibitions:
    - "ADR-0015 must NOT manufacture debt items for the twelve extra dependencies — each is accepted as a general-purpose or domain-support crate, and recording twelve debts nobody intends to pay would leave the invariant still unstated."
    - "ADR-0015 must NOT transcribe a dependency count from `intel/code-verification.md` or CONTEXT.md; both counts are measured from the manifests during this task."
    - "ADR-0017 must NOT declare Epic 4 simply wrong — the concern was sound and the resolution was structural, and the ADR says so in those terms."
    - "The correction to `prd-paladin-llm-extraction.md` must NOT delete, replace, or rewrite FR-31, FR-32 or Non-Goal 2."
    - "This plan must NOT edit any `.rs` file, `Cargo.toml`, or any file under `.github/` — no enforcement check is built here."
---

<objective>
Record two of the four competing-variant answers: which dependencies `paladin-core` and
`paladin-ports` may carry, and where the LLM configuration bridge lives.

Purpose: the Epic 1 PRD's Appendix B calls its six-crate allowlist "complete and exhaustive"; the
tree carries fourteen in `paladin-core` and eleven in `paladin-ports`. A list wrong by eight and by
four is unenforceable as written, and the thing it was actually protecting — hexagonal purity — was
never stated separately from the list. ADR-0015 states the invariant, rebaselines the list against
measurement, and justifies the one entry a purity review would challenge. ADR-0017 settles the
bridge location and, more usefully, records *why* Epic 4's cycle concern evaporated rather than
recording Epic 4 as mistaken.
Output: ADR-0015, ADR-0017, and the Epic 4 extraction PRD annotated.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-PATTERNS.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ADR-0015 — the purity invariant and the measured dependency baseline</name>
  <read_first>
    - `.planning/decisions/PROMOTION.md` — the required heading set and the numbering rule
    - `.planning/decisions/0011-vision-port-surfaces.md` — the "record as shipped / record as dropped" balancing act in `## Considered Options`, the closest tonal precedent for accepting shipped extras without manufacturing debt
    - `crates/paladin-core/Cargo.toml` and `crates/paladin-ports/Cargo.toml` — the `[dependencies]` blocks, read and counted during this task
    - `.project/Milestone_5-Workspace-Decomposition/Epic_1/prd-workspace-initialization-and-paladin-core-extraction.md` FR-6 and Appendix B — the exhaustive-at-six allowlist, plus SM-4 / FR-24 / FR-25, the purity clauses the list was protecting
    - `.planning/codebase/ARCHITECTURE.md` and `.planning/codebase/CONVENTIONS.md` — the layering rules the invariant is expressed against
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md` §Decisions D-10, D-00c, D-00h
  </read_first>
  <files>.planning/decisions/0015-core-ports-dependency-allowlist.md</files>
  <action>
First measure. Read `crates/paladin-core/Cargo.toml` and `crates/paladin-ports/Cargo.toml` and count
their `[dependencies]` entries. Research measured fourteen and eleven respectively on 2026-08-06;
confirm both counts and record any drift rather than reusing the figure. Note explicitly that
`intel/code-verification.md` records `paladin-ports` at ten, which predates `mime_guess`.

Then create `.planning/decisions/0015-core-ports-dependency-allowlist.md`. No frontmatter. Exactly
seven H2 headings in the canonical order.

`## Context` states the drift: the Epic 1 PRD's Appendix B calls its allowlist "complete and
exhaustive" at six crates, while the tree carries fourteen in `paladin-core` and eleven in
`paladin-ports`; the allowlist is the stated enforcement mechanism for the whole hexagonal-purity
argument (SM-4, FR-24, FR-25), so leaving it wrong by eight and by four makes that argument
unenforceable as written; and the invariant the list was protecting was never written down
separately from the list.

`## Decision` states three things, in this order and clearly separated. (i) The enforceable
invariant: `paladin-core` and `paladin-ports` may carry no provider SDK, no transport client, no
storage driver and no web framework — and it holds today. (ii) The measured current lists as the new
baseline, with the eight `paladin-core` extras beyond the PRD's six (`tokio`, `sha2`, `blake3`,
`petgraph`, `murmur3`, `url`, `regex`, `futures`) and the four `paladin-ports` extras
(`serde_json`, `futures`, `md5`, `mime_guess`) accepted as general-purpose or domain-support crates
rather than recorded as debt. (iii) `tokio` in `paladin-core` singled out with its own written
justification, because it is an async runtime in a crate documented as having zero external
dependencies and is the one entry a future purity review would reasonably challenge — state what it
is used for, read from the crate's own source, not asserted.

`## Considered Options` is a bulleted list, never prose: state the intended six-crate target and
treat the extra twelve as tracked debt (rejected — manufactures twelve debt items nobody intends to
pay and leaves the invariant still unstated); rebaseline the list without stating an invariant
(rejected — reproduces the same unenforceable shape one measurement later); state the invariant and
rebaseline the list, with `tokio` justified (accepted).

`## Code Locations` is a bulleted list, never prose, one `file:line` citation per bullet, all
re-grepped at authoring time: the `[dependencies]` block of `crates/paladin-core/Cargo.toml`; the
`[dependencies]` block of `crates/paladin-ports/Cargo.toml`; the Epic 1 PRD's FR-6 and Appendix B;
the SM-4 / FR-24 / FR-25 purity clauses; and the `crate-isolation` job in `.github/workflows/ci.yml`
as the build leg that exercises isolation today.

`## Code Conformance` is the bare word `conforms` followed by prose stating that the tree already
satisfies the invariant, that this ADR changes the recorded baseline rather than any code, and that
no enforcement check is built by this phase.

`## Downstream Consumers` is a bulleted list naming: Phase 15, which receives the `cargo tree`-based
allowlist check as a candidate; plans 07-06 and 07-10, whose ledger rows for
`REQ-paladin-core-dependency-allowlist-v1` and `-v2` cite this ADR; and plan 07-13, which adds this
ADR's row to `PROMOTION.md`.
  </action>
  <verify>
    <automated>test -f .planning/decisions/0015-core-ports-dependency-allowlist.md &amp;&amp; [ "$(grep -c '^## ' .planning/decisions/0015-core-ports-dependency-allowlist.md)" = "7" ] &amp;&amp; grep -q 'mime_guess' .planning/decisions/0015-core-ports-dependency-allowlist.md &amp;&amp; grep -q 'tokio' .planning/decisions/0015-core-ports-dependency-allowlist.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## ' .planning/decisions/0015-core-ports-dependency-allowlist.md` equals `7`, in the canonical order
    - `head -1 .planning/decisions/0015-core-ports-dependency-allowlist.md` does not equal `---`
    - `sed -n '/^## Considered Options/,/^## Code Locations/p' .planning/decisions/0015-core-ports-dependency-allowlist.md | grep -c '^- '` is at least `3`
    - `sed -n '/^## Code Locations/,/^## Code Conformance/p' .planning/decisions/0015-core-ports-dependency-allowlist.md | grep -c '^- '` is at least `5`
    - `sed -n '/^## Code Conformance/,/^## Downstream Consumers/p' .planning/decisions/0015-core-ports-dependency-allowlist.md | grep -c '^conforms$'` equals `1`
    - `grep -c 'mime_guess' .planning/decisions/0015-core-ports-dependency-allowlist.md` is at least `1` — the eleventh `paladin-ports` dependency is in the baseline
    - `grep -c 'Phase 15' .planning/decisions/0015-core-ports-dependency-allowlist.md` is at least `1`
    - The measured counts in `## Decision` match `grep -c` over the `[dependencies]` sections of the two manifests as read during this task
    - `sed -n '/^## Decision/,/^## Considered Options/p' .planning/decisions/0015-core-ports-dependency-allowlist.md | grep -ci 'tokio'` is at least `1` — the singled-out justification is in the Decision, not only in a list
  </acceptance_criteria>
  <done>ADR-0015 exists with the seven canonical headings, states the purity invariant separately from the list, records both dependency counts as measured this task, justifies `tokio` explicitly, carries a `conforms` verdict, and names Phase 15 as the enforcement candidate.</done>
</task>

<task type="auto">
  <name>Task 2: ADR-0017 — LLM configuration ownership and the bridge location</name>
  <read_first>
    - `.planning/decisions/PROMOTION.md` — the required heading set
    - `.planning/decisions/0015-core-ports-dependency-allowlist.md` — the ADR just written, for cross-reference consistency
    - `crates/paladin-llm/src/config/bridge.rs` and `crates/paladin-llm/src/config/` — the shipped bridge and the config types Milestone 6 moved down
    - `.project/Milestone_5-Workspace-Decomposition/Epic_4/prd-paladin-llm-extraction.md` FR-31 and FR-32 — the circular-dependency concern, located by grep
    - `.project/Milestone_6-Architectural-Refinements/Epic_1/prd-decompose-application-settings.md` — the Milestone 6 work that moved the config types into `paladin-llm`
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md` §Decisions D-12, D-00h
  </read_first>
  <files>.planning/decisions/0017-llm-config-bridge-location.md</files>
  <action>
Create `.planning/decisions/0017-llm-config-bridge-location.md`. No frontmatter. Exactly seven H2
headings in the canonical order.

`## Context` states the variant pair: Epic 4 FR-31 and FR-32 feared a dependency cycle
`paladin-llm` → root crate if the configuration bridge lived in `paladin-llm`, and specified the
bridge elsewhere; Milestone 6 Epic 1 then decomposed `application_settings.rs` and moved the LLM and
vision config types *down* into `paladin-llm`. State that the shipped bridge is at
`crates/paladin-llm/src/config/bridge.rs`.

`## Decision` accepts the shipped location and states the reasoning in D-12's own terms: the cycle
Epic 4 feared does not exist, because Milestone 6 moved the config types down rather than moving the
bridge up. Milestone 6 did not break the boundary Epic 4 established — it removed the need for it.
State plainly that Epic 4's concern was sound and the resolution was structural, and do not record
Epic 4 as simply wrong.

`## Considered Options` is a bulleted list, never prose: accept the shipped location and record the
concern as real but mis-sited (accepted); relocate the bridge to satisfy FR-31/FR-32 as written
(rejected — a code change this phase is forbidden to make, against a cycle that does not exist);
record Epic 4 as mistaken (rejected — the concern was sound at the time and the record should say
why it stopped applying).

`## Code Locations` is a bulleted list, never prose, one `file:line` citation per bullet, re-grepped
at authoring time: `crates/paladin-llm/src/config/bridge.rs`; the LLM and vision config modules
under `crates/paladin-llm/src/config/`; the Epic 4 PRD's FR-31 and FR-32 lines; and the Milestone 6
Epic 1 PRD clause that moved the config types.

`## Code Conformance` is the bare word `conforms` plus prose stating that shipped code already
implements the decision and no code changes.

`## Downstream Consumers` is a bulleted list naming: plans 07-08 and 07-10, whose ledger rows for
`REQ-llm-config-bridge-location-v1` and `-v2` cite this ADR; and plan 07-13, which adds this ADR's
row to `PROMOTION.md`.
  </action>
  <verify>
    <automated>test -f .planning/decisions/0017-llm-config-bridge-location.md &amp;&amp; [ "$(grep -c '^## ' .planning/decisions/0017-llm-config-bridge-location.md)" = "7" ] &amp;&amp; grep -q 'config/bridge.rs' .planning/decisions/0017-llm-config-bridge-location.md &amp;&amp; test -f crates/paladin-llm/src/config/bridge.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## ' .planning/decisions/0017-llm-config-bridge-location.md` equals `7`, in the canonical order
    - `head -1 .planning/decisions/0017-llm-config-bridge-location.md` does not equal `---`
    - `sed -n '/^## Considered Options/,/^## Code Locations/p' .planning/decisions/0017-llm-config-bridge-location.md | grep -c '^- '` is at least `3`
    - `sed -n '/^## Code Locations/,/^## Code Conformance/p' .planning/decisions/0017-llm-config-bridge-location.md | grep -c '^- '` is at least `4`
    - `sed -n '/^## Code Conformance/,/^## Downstream Consumers/p' .planning/decisions/0017-llm-config-bridge-location.md | grep -c '^conforms$'` equals `1`
    - `test -f crates/paladin-llm/src/config/bridge.rs` succeeds — the cited shipped location exists
    - `sed -n '/^## Decision/,/^## Considered Options/p' .planning/decisions/0017-llm-config-bridge-location.md` states that the config types moved down rather than the bridge moving up
    - `grep -c 'FR-31' .planning/decisions/0017-llm-config-bridge-location.md` is at least `1` and `grep -c 'FR-32' .planning/decisions/0017-llm-config-bridge-location.md` is at least `1`
  </acceptance_criteria>
  <done>ADR-0017 exists with the seven canonical headings, ratifies the shipped bridge location, records Epic 4's concern as real but mis-sited with the structural reason stated, and carries a `conforms` verdict.</done>
</task>

<task type="auto">
  <name>Task 3: Annotate the Epic 4 llm-extraction PRD — FR-31/FR-32 and Non-Goal 2</name>
  <read_first>
    - `.project/Milestone_5-Workspace-Decomposition/Epic_4/prd-paladin-llm-extraction.md` — read in full before touching it; locate FR-31, FR-32 and Non-Goal 2 by grep rather than by remembered line number
    - `.project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` lines 1-8, 28-31 and 360 — the three verbatim D-00g markup shapes, including the standalone Superseded banner used for a whole forward-looking block
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-PATTERNS.md` §6 — the correction-target table row for this document, which assigns shape (a)+(b) to Non-Goal 2 and shape (c) to FR-31/FR-32
    - `.planning/decisions/0017-llm-config-bridge-location.md` and `.planning/decisions/0014-milestone-4-6-tier-numbering.md` — the two ADRs the annotations cite
  </read_first>
  <files>.project/Milestone_5-Workspace-Decomposition/Epic_4/prd-paladin-llm-extraction.md</files>
  <action>
Annotate this one document per D-00g. This plan owns the whole file; make both corrections in one
pass.

(1) Add a top-of-file blockquote banner immediately under the existing H1, dated, naming both
ADR-0017 and ADR-0014, stating in two sentences that FR-31 and FR-32's circular-dependency concern
was resolved structurally by Milestone 6 rather than by relocating the bridge, and that Non-Goal 2's
"hardened in Milestone 1 / Epic 2" cross-reference means Milestone 4 Epic 2 under the authoritative
numbering. Link both ADRs by relative path. Close with the sentence that original text is retained
below with inline corrections and nothing is deleted.

(2) Place a standalone dated Superseded blockquote immediately above the FR-31/FR-32 block, naming
ADR-0017 and stating in one sentence that the concern was real but mis-sited: Milestone 6 moved the
config types down into `paladin-llm` rather than moving the bridge up into the root, so the cycle
these requirements guard against does not exist and the shipped bridge at
`crates/paladin-llm/src/config/bridge.rs` is the accepted answer. Leave FR-31 and FR-32's own text
untouched below the banner.

(3) Correct Non-Goal 2 inline with the strikethrough-and-append shape: the original "Milestone 1 /
Epic 2" phrase struck through, the corrected reference appended, and a bold note citing ADR-0014
stating that inside these three milestones "Milestone 1" is the Tier 1 label for Milestone 4.

Commit this plan's three files together in a single commit at the end of the plan, with a Bash
timeout of at least 300000 ms.
  </action>
  <verify>
    <automated>grep -q 'ADR-0017' .project/Milestone_5-Workspace-Decomposition/Epic_4/prd-paladin-llm-extraction.md &amp;&amp; grep -q 'ADR-0014' .project/Milestone_5-Workspace-Decomposition/Epic_4/prd-paladin-llm-extraction.md &amp;&amp; [ "$(git diff --numstat HEAD -- .project/Milestone_5-Workspace-Decomposition/Epic_4/prd-paladin-llm-extraction.md | awk '{print $2}')" -le "3" ]</automated>
    <human-check>Open the Superseded block above FR-31 and confirm FR-31 and FR-32's own requirement text is still present and unmodified beneath it (07-VALIDATION.md §Manual-Only Verifications, row 2).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ADR-0017' .project/Milestone_5-Workspace-Decomposition/Epic_4/prd-paladin-llm-extraction.md` is at least `2` — the banner and the Superseded block
    - `grep -c 'ADR-0014' .project/Milestone_5-Workspace-Decomposition/Epic_4/prd-paladin-llm-extraction.md` is at least `2` — the banner and the Non-Goal 2 note
    - `grep -c 'FR-31' .project/Milestone_5-Workspace-Decomposition/Epic_4/prd-paladin-llm-extraction.md` is at least `1` and `grep -c 'FR-32' .project/Milestone_5-Workspace-Decomposition/Epic_4/prd-paladin-llm-extraction.md` is at least `1` — both requirement texts survive
    - `grep -c '~~' .project/Milestone_5-Workspace-Decomposition/Epic_4/prd-paladin-llm-extraction.md` is at least `1` — the Non-Goal 2 correction retains its original phrase
    - `git diff --numstat HEAD -- .project/Milestone_5-Workspace-Decomposition/Epic_4/prd-paladin-llm-extraction.md` shows deletions no greater than `3`
    - `git log -1 --name-only` shows both new ADRs and the annotated PRD in a single commit
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
  </acceptance_criteria>
  <done>The Epic 4 llm-extraction PRD carries a dated banner naming ADR-0017 and ADR-0014, a Superseded block above FR-31/FR-32 explaining the structural resolution, and an inline Non-Goal 2 numbering correction — with every original claim still present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADR → future implementer or agent | ADR-0015's rebaselined dependency list becomes what a later purity review is measured against; ADR-0017's ratified location becomes what a later refactor preserves. |
| Measured manifest fact → the recorded baseline | The counts in ADR-0015 are read from `Cargo.toml` at authoring time. A transcribed count would bake an already-known-stale figure (ten `paladin-ports` dependencies) into the new baseline. |
| `.project/` historical corpus → the annotation | Epic 4's FR-31/FR-32 text is the record of a concern that was sound at the time. Deleting it would make ADR-0017's reasoning unverifiable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-12 | Tampering (of the record) | ADR-0015 `## Decision` dependency baseline | high | mitigate | A baseline transcribed from `intel/code-verification.md` would ship a known-stale count (ten, not eleven) as the new authority and a later `cargo tree` check built on it would fail spuriously. Mitigated by requiring both counts to be measured from the manifests during the task and asserted against `grep -c`. |
| T-07-13 | Repudiation | `prd-paladin-llm-extraction.md` FR-31 / FR-32 | high | mitigate | Deleting the superseded FR text erases the concern ADR-0017 exists to explain. Mitigated by the Superseded-banner-above-unmodified-text pattern and a bounded deletion count. |
| T-07-14 | Elevation of privilege (of an unstated invariant) | ADR-0015 | medium | mitigate | Rebaselining the list without stating the invariant would reproduce the unenforceable shape one measurement later. Mitigated by requiring the invariant to appear as its own clause in `## Decision`. |
| T-07-15 | Denial of service | none | n/a | n/a | No runtime surface. Recorded explicitly rather than padded with an inapplicable entry. |
| T-07-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable: this plan reads manifests and installs nothing. |

No critical threats. T-07-12 and T-07-13 are the `high` items and both are mitigated inside this
plan.
</threat_model>

<artifacts>
## Artifacts this phase produces

The full phase-wide artefact table is recorded once in `07-01-PLAN.md` §Artifacts. This plan produces
these rows:

| Artefact | Status | Owning plan | Requirement |
|---|---|---|---|
| `.planning/decisions/0015-core-ports-dependency-allowlist.md` | **NEW** ADR — `conforms`, Phase 15 named as enforcement candidate | 07-04 | ARCH-03 |
| `.planning/decisions/0017-llm-config-bridge-location.md` | **NEW** ADR — `conforms` | 07-04 | ARCH-03 |
| `.project/Milestone_5-.../Epic_4/prd-paladin-llm-extraction.md` | **ANNOTATED IN PLACE** — banner, FR-31/FR-32 Superseded block, Non-Goal 2 | 07-04 | ARCH-03, ARCH-02 |

New headings created by this plan: the seven canonical H2s in each of the two new ADRs. No heading
is added to the annotated PRD.
</artifacts>

<verification>
- `grep -c '^## ' .planning/decisions/0015-core-ports-dependency-allowlist.md` equals `7`
- `grep -c '^## ' .planning/decisions/0017-llm-config-bridge-location.md` equals `7`
- `sed -n '/^## Code Conformance/,/^## Downstream Consumers/p' .planning/decisions/0015-core-ports-dependency-allowlist.md | grep -c '^conforms$'` equals `1`
- `sed -n '/^## Code Conformance/,/^## Downstream Consumers/p' .planning/decisions/0017-llm-config-bridge-location.md | grep -c '^conforms$'` equals `1`
- `grep -c 'ADR-0017' .project/Milestone_5-Workspace-Decomposition/Epic_4/prd-paladin-llm-extraction.md` is at least `2`
- `git diff --numstat HEAD~1 -- .project/Milestone_5-Workspace-Decomposition/Epic_4/prd-paladin-llm-extraction.md` shows deletions no greater than `3`
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
</verification>

<success_criteria>
Two of ARCH-03's four variant pairs have exactly one recorded answer each. The dependency allowlist
is enforceable because the invariant is now stated separately from the list and the list is measured
rather than asserted, with `tokio` justified where a purity review would challenge it. The LLM config
bridge location is settled with the reason Epic 4's concern stopped applying, and the Epic 4 PRD can
no longer be applied literally without a reader meeting that explanation first.
</success_criteria>

<output>
Create `.planning/phases/07-workspace-ground-truth-recorded-answers/07-04-SUMMARY.md` when done.
Record: both measured dependency counts and any drift from the figures research recorded, the
`tokio` justification as written, and the exact FR/Non-Goal line numbers annotated in the PRD.
</output>
