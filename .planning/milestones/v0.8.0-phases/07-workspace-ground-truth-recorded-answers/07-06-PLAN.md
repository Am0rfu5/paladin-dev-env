---
phase: 07-workspace-ground-truth-recorded-answers
plan: 06
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-04"]
files_modified:
  - .planning/ledgers/milestone-04-06.md
autonomous: true
requirements: [ARCH-01, ARCH-03]
must_haves:
  truths:
    - "The 20 `REQ-*` rows under `### Milestone 5 Epic 1` and `### Milestone 5 Epic 2` in `.planning/ledgers/milestone-04-06.md` carry no stub token: each has one of the seven legend verdicts and an Evidence cell with a `file:line` citation resolved against the tree during this plan."
    - "Every row verdicted `satisfied` names a passing test, example, command, or — for a structural requirement under D-01's manifest carve-out — the manifest line plus the CI job or build leg that consumes it, with both citations present in the same cell."
    - "`REQ-workspace-crate-edition-v1` cites `.planning/decisions/0009-workspace-rust-edition-2024.md` and is recorded superseded; `-v2` (in Milestone 5 Epic 5, filled by plan 07-11) is the position the tree follows. Phase 7 cites ADR-0009's answer and does not re-decide ARCH-03(a) (D-09)."
    - "`REQ-paladin-core-dependency-allowlist-v1` cites ADR-0015 and records the six-crate 'exhaustive' list as rebaselined, not as debt; `REQ-port-value-type-ownership-v1` cites ADR-0016 and records the `paladin-core` shape as the shipped and ratified one."
    - "`REQ-port-value-type-ownership-v2` is recorded `superseded by shipped code` pointing at ADR-0016 — it is not deleted, not merged into the `-v1` row, and not left blank, because both halves of a variant pair keep their own row."
    - "`REQ-ports-doctest-compilation` and `REQ-ports-tests-and-rustdoc` are recorded `genuinely outstanding` pointing at Phase 8 / DEBT-03, and no plan in this phase re-enables the doctests."
    - "Row count in the ledger is still exactly 115 and no `REQ-*` ID appears twice after this plan's edits; rows in these two sections appear in the same order the scaffold fixed."
  artifacts:
    - ".planning/ledgers/milestone-04-06.md — 20 rows filled under Milestone 5 Epics 1 and 2"
  key_links:
    - "Ledger rows → ADR-0009, ADR-0015, ADR-0016 — the three recorded answers this block's four variant rows cite"
    - "REQ-ports-doctest-compilation → Phase 8 / DEBT-03 — the outstanding-work pointer, recorded not executed"
    - "Ledger evidence cells → .github/workflows/ci.yml and feature-flags.yml — the exercising artefacts D-01's manifest carve-out relies on, re-grepped at authoring time"
  prohibitions:
    - "No row may be awarded `satisfied` on the strength of a `file:line` citation alone, and REQUIREMENTS.md's ingest-era status words must NOT be carried forward as if they were pre-satisfied verdicts."
    - "No `REQ-*` row may be dropped, reordered, inserted, or merged with another ID that cites the same artefact."
    - "This plan must NOT edit any `.rs` file, `Cargo.toml`, or any file under `.github/` — DEBT-03's doctest re-enablement is Phase 8's work."
    - "A line number must NOT be copied forward from CONTEXT.md, RESEARCH.md, or `intel/code-verification.md` — every citation is re-grepped against the tree in this plan."
---

<objective>
Fill the 20 ledger rows for Milestone 5 Epics 1 and 2 — the workspace initialization block and the
ports extraction — to the D-01 evidence bar.

Purpose: this block hosts two of ARCH-03's four variant pairs (the dependency allowlist and port
value-type ownership) plus the edition pair whose answer Phase 4 already recorded, so it is where the
ledger stops being a list of claims and starts being an index a reader can follow from a requirement
to an ADR to a shipped line. It also holds both DEBT-03 doctest defects, which are recorded here and
fixed in Phase 8.
Output: 20 verdicted, cited rows in `.planning/ledgers/milestone-04-06.md`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-PATTERNS.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Milestone 5 Epic 1 — Workspace Initialization and `paladin-core` (10 rows)</name>
  <read_first>
    - `.planning/ledgers/milestone-04-06.md` — the whole head-note block and the `### Milestone 5 Epic 1` section as the scaffold left it; the evidence bar, the seven-value legend and the manifest carve-out govern every row written here
    - `.planning/REQUIREMENTS.md` lines 2905-2922 — the ten IDs and their ingest-era status text (the input, not the answer)
    - `.planning/intel/code-verification.md` lines 82-260 — the run-3 verification block, including the 22 already-verified claims and the crate-level contradiction table; note that its `ci.yml:228` citation is stale and its `paladin-ports` dependency count of ten predates `mime_guess`
    - `.planning/decisions/0009-workspace-rust-edition-2024.md`, `.planning/decisions/0015-core-ports-dependency-allowlist.md`, `.planning/decisions/0016-port-value-type-ownership.md` — the three ADRs this section's variant rows cite
    - `Cargo.toml` `[workspace]` block and `crates/paladin-core/Cargo.toml` — the manifest facts several rows cite
    - `.planning/ledgers/milestone-02-03.md` lines 230-259 — the Evidence-cell prose shape to reproduce
  </read_first>
  <files>.planning/ledgers/milestone-04-06.md</files>
  <action>
Replace the Verdict and Evidence cells of the ten rows under `### Milestone 5 Epic 1` in place. Do
not insert, delete or reorder rows. The ten IDs, in the order the scaffold fixed, are
`REQ-cargo-workspace-root`, `REQ-workspace-crate-edition-v1`, `REQ-paladin-core-scaffold`,
`REQ-paladin-core-dependency-allowlist-v1`, `REQ-core-base-extraction`,
`REQ-core-container-extraction`, `REQ-core-upward-dependency-resolution`,
`REQ-port-value-type-ownership-v1`, `REQ-facade-core-reexports` and
`REQ-core-dependency-validation`.

For each row: resolve the citation against the current tree and cite a real line number, never a bare
path; then find an exercising artefact. For behavioural requirements that means a test, example or
command — run it, scoped and offline, preferring `cargo test --offline -p paladin-core` over a
full-workspace run, and record the pass count. For structural requirements — workspace membership,
the dependency list, the edition — apply D-01's manifest carve-out: cite the manifest line plus a
named CI job or build leg that consumes it, re-grepping that job's line number in
`.github/workflows/ci.yml` or `.github/workflows/feature-flags.yml` at the moment of writing rather
than copying a figure from any earlier document. A manifest citation with no consuming job is
`present, unproven`, not `satisfied`.

Three rows have prescribed dispositions. `REQ-workspace-crate-edition-v1` records the 2021 position
as superseded, cites `.planning/decisions/0009-workspace-rust-edition-2024.md`, and states that all
twelve workspace manifests declare edition 2024 — confirm that by reading the manifests rather than
asserting it. `REQ-paladin-core-dependency-allowlist-v1` cites ADR-0015 and states that the PRD's
six-crate "exhaustive" list is rebaselined against the measured set rather than recorded as debt.
`REQ-port-value-type-ownership-v1` cites ADR-0016 and states that this is the shipped and ratified
shape, naming the canonical `TokenUsage` location.

`REQ-core-container-extraction` carries a known partial supersession: its clause including the
Maneuver lexer, AST and parser was reversed one milestone later by
`REQ-maneuver-files-moved-from-core`, whose row plan 07-01 already filled. Record that inline in the
Evidence cell, pointing at the later row, rather than opening a nested outstanding item.

Where a row's only evidence is a `.project/` artefact that the `project/` to `.project/` rename made
unlocatable — the Epic 1 dependency-validation evidence artefacts are the likely case — record
`present, unproven` and say so plainly. Do not stretch for `satisfied`.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Milestone 5 Epic 1 /,/^### Milestone 5 Epic 2 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-')" = "10" ] &amp;&amp; [ "$(sed -n '/^### Milestone 5 Epic 1 /,/^### Milestone 5 Epic 2 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT')" = "0" ] &amp;&amp; [ "$(grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md)" = "115" ]</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Milestone 5 Epic 1 /,/^### Milestone 5 Epic 2 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-'` equals `10`
    - `sed -n '/^### Milestone 5 Epic 1 /,/^### Milestone 5 Epic 2 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md` still equals `115`
    - `grep -o '^| REQ-[a-z0-9-]*' .planning/ledgers/milestone-04-06.md | sort | uniq -d` prints nothing
    - Every row in the section has an Evidence cell containing a `:` followed by a digit
    - `sed -n '/^### Milestone 5 Epic 1 /,/^### Milestone 5 Epic 2 /p' .planning/ledgers/milestone-04-06.md | grep -c '0009'` is at least `1` — the edition row cites the ADR
    - `sed -n '/^### Milestone 5 Epic 1 /,/^### Milestone 5 Epic 2 /p' .planning/ledgers/milestone-04-06.md | grep -c 'ADR-0015'` is at least `1`
    - `sed -n '/^### Milestone 5 Epic 1 /,/^### Milestone 5 Epic 2 /p' .planning/ledgers/milestone-04-06.md | grep -c 'ADR-0016'` is at least `1`
    - `sed -n '/^### Milestone 5 Epic 1 /,/^### Milestone 5 Epic 2 /p' .planning/ledgers/milestone-04-06.md | grep -c 'ci.yml:228'` equals `0` — no stale line number was copied forward
    - Every row verdicted `satisfied` names either an executed command with its pass count or a manifest line plus its consuming CI job, both cited in the same cell
  </acceptance_criteria>
  <done>All ten Milestone 5 Epic 1 rows carry a legend verdict and a resolved citation, the three variant rows cite ADR-0009, ADR-0015 and ADR-0016 respectively, and `REQ-core-container-extraction` records its later partial supersession inline.</done>
</task>

<task type="auto">
  <name>Task 2: Milestone 5 Epic 2 — `paladin-ports` Extraction (10 rows)</name>
  <read_first>
    - `.planning/ledgers/milestone-04-06.md` — the `### Milestone 5 Epic 2` section as the scaffold left it, plus the head-note evidence bar
    - `.planning/REQUIREMENTS.md` lines 2923-2937 — the ten IDs and their ingest-era status text
    - `crates/paladin-ports/Cargo.toml` — the `[lib] doctest = false` line and the `[dependencies]` block, both cited by rows in this section
    - `.github/workflows/ci.yml` — the `test` job's `cargo test --workspace --doc` step and its `--exclude paladin-ports` argument, located by grep rather than by the line number any earlier document records
    - `.planning/decisions/0016-port-value-type-ownership.md` — the ADR `REQ-port-value-type-ownership-v2` cites
    - `.project/Milestone_5-Workspace-Decomposition/Epic_2/prd-paladin-ports-extraction.md` — now annotated by plan 07-02; the corrected FR-7/FR-10 position is what the `-v2` row records
  </read_first>
  <files>.planning/ledgers/milestone-04-06.md</files>
  <action>
Replace the Verdict and Evidence cells of the ten rows under `### Milestone 5 Epic 2` in place. The
ten IDs, in scaffold order, are `REQ-paladin-ports-scaffold`, `REQ-output-ports-extraction`,
`REQ-input-ports-extraction`, `REQ-ports-facade-wiring`, `REQ-ports-import-migration`,
`REQ-ports-doctest-compilation`, `REQ-ports-docs-markdown-update`, `REQ-ports-layering-validation`,
`REQ-ports-tests-and-rustdoc` and `REQ-port-value-type-ownership-v2`.

Apply the same citation and exercising-artefact discipline as Task 1, scoping any command to
`-p paladin-ports` and re-grepping every CI line number.

Four rows have prescribed dispositions. `REQ-ports-doctest-compilation` is `genuinely outstanding`
pointing at Phase 8 / DEBT-03, citing both the `[lib] doctest = false` manifest line and the
workflow line that excludes the crate from the doc-test run, each re-grepped. `REQ-ports-tests-and-rustdoc`
is recorded partial for the same reason and also points at DEBT-03 — rustdoc is preserved but the
"no documentation may be lost" clause is undermined by disabled doctests. `REQ-ports-facade-wiring`
records that `src/application/ports/` does not exist at all and that full deletion is what shipped,
which is the §9 Option B resolution, confirmed by listing the directory. `REQ-port-value-type-ownership-v2`
is `superseded by shipped code` pointing at ADR-0016, stating in the Evidence cell that FR-10 applied
literally would reintroduce the upward dependency Epic 1 removed and that the source PRD is now
annotated — cite the annotated FR-7/FR-10 lines.

`REQ-paladin-ports-scaffold`'s ingest-era text records ten dependencies against a declared allowlist
of seven. Record the measured current count instead, taken from the manifest in this task, and cite
ADR-0015 as the rebaselined authority. Do not carry the stale figure forward.

Commit the ledger once at the end of the plan, with a Bash timeout of at least 300000 ms. Do not
commit per row.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Milestone 5 Epic 2 /,/^### Milestone 5 Epic 3 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-')" = "10" ] &amp;&amp; [ "$(sed -n '/^### Milestone 5 Epic 2 /,/^### Milestone 5 Epic 3 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT')" = "0" ] &amp;&amp; [ "$(grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md)" = "115" ]</automated>
    <human-check>Spot-check three rows in this section: open the cited `file:line` and confirm it states what the row says it states (07-VALIDATION.md §Manual-Only Verifications, row 1).</human-check>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Milestone 5 Epic 2 /,/^### Milestone 5 Epic 3 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-'` equals `10`
    - `sed -n '/^### Milestone 5 Epic 2 /,/^### Milestone 5 Epic 3 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c 'PENDING-VERDICT' .planning/ledgers/milestone-04-06.md` equals `86` — 106 stubs minus this plan's 20
    - `grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md` still equals `115` and `grep -c '^### Milestone ' .planning/ledgers/milestone-04-06.md` still equals `13`
    - `sed -n '/^### Milestone 5 Epic 2 /,/^### Milestone 5 Epic 3 /p' .planning/ledgers/milestone-04-06.md | grep -c 'DEBT-03'` is at least `2` — both doctest rows point at their owning phase
    - `sed -n '/^### Milestone 5 Epic 2 /,/^### Milestone 5 Epic 3 /p' .planning/ledgers/milestone-04-06.md | grep -c 'ADR-0016'` is at least `1`
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty after this plan's commit
    - `git log -1 --name-only` shows only `.planning/ledgers/milestone-04-06.md`
  </acceptance_criteria>
  <done>All ten Milestone 5 Epic 2 rows carry a legend verdict and a resolved citation, both DEBT-03 rows point at Phase 8 without any code being changed here, and the `-v2` variant row is superseded by shipped code pointing at ADR-0016 while keeping its own row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ledger verdict → Phase 8 and Phase 15 planners | A verdict recorded here is consumed downstream as fact. A row that records an unwired capability as `satisfied` causes a later phase to skip real work; a row that records shipped work as outstanding causes it to redo work. |
| An earlier document's `file:line` → this plan's Evidence cell | Line numbers drift. `intel/code-verification.md`'s `crate-isolation` citation is already stale by 76 lines, and its `paladin-ports` dependency count predates a crate that has since been added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-20 | Tampering (of the record) | the 20 Evidence cells | high | mitigate | A `satisfied` on a citation alone is the exact false positive the evidence bar exists to reject. Mitigated by requiring every `satisfied` row to name an executed command with its pass count, or a manifest line plus its consuming CI job, both in the same cell. |
| T-07-21 | Spoofing (of a citation) | stale `file:line` values | high | mitigate | A copied line number resolves to the wrong content and makes the row unfalsifiable. Mitigated by the prohibition against copying forward and by asserting that no `ci.yml:228` appears in the section. |
| T-07-22 | Denial of service (of the ledger file) | concurrent edits to `milestone-04-06.md` | medium | mitigate | Two fan-out plans editing the same file in one wave would clobber each other. Mitigated structurally: exactly one ledger-writing plan per wave, and this plan asserts the total row count is unchanged at 115 after its edits. |
| T-07-23 | Elevation of privilege (of an ingest status word) | REQUIREMENTS.md ingest-era text | medium | mitigate | Carrying "Shipped" forward as a verdict re-launders an unverified claim as a verified one. Mitigated by the prohibition and by requiring a fresh citation in every cell. |
| T-07-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable: no package installed, no manifest modified. |

No critical threats. T-07-20 and T-07-21 are the `high` items and both are mitigated inside this plan.
</threat_model>

<artifacts>
## Artifacts this phase produces

The full phase-wide artefact table is recorded once in `07-01-PLAN.md` §Artifacts. This plan produces
one of its rows:

| Artefact | Status | Owning plan | Requirement |
|---|---|---|---|
| `.planning/ledgers/milestone-04-06.md` §`### Milestone 5 Epic 1` and §`### Milestone 5 Epic 2` | **FILLED** — 20 rows verdicted and cited | 07-06 | ARCH-01, ARCH-03 |

No new headings are created by this plan — the scaffold fixed all 13, and this plan only replaces
cell contents.
</artifacts>

<verification>
- `sed -n '/^### Milestone 5 Epic 1 /,/^### Milestone 5 Epic 3 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT'` equals `0`
- `grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md` equals `115`
- `grep -c '^### Milestone ' .planning/ledgers/milestone-04-06.md` equals `13`
- `grep -o '^| REQ-[a-z0-9-]*' .planning/ledgers/milestone-04-06.md | sort | uniq -d` prints nothing
- `git status --porcelain` is clean after the plan's single commit
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
</verification>

<success_criteria>
A developer can look up any of the 20 Milestone 5 Epic 1 and Epic 2 requirement IDs and see a
`file:line`-cited verdict, with the three variant rows in this block resolving to ADR-0009, ADR-0015
and ADR-0016 by number, and both DEBT-03 doctest defects recorded as outstanding with an owning phase
rather than fixed here.
</success_criteria>

<output>
Create `.planning/phases/07-workspace-ground-truth-recorded-answers/07-06-SUMMARY.md` when done.
Record: the verdict distribution across the 20 rows, every scoped command executed with its pass
count, the measured `paladin-ports` dependency count, and any citation that did not resolve at the
path REQUIREMENTS.md recorded.
</output>
