---
phase: 07-workspace-ground-truth-recorded-answers
plan: 07
type: execute
wave: 5
depends_on: ["07-01"]
files_modified:
  - .planning/decisions/0019-binary-target-architecture.md
  - .planning/decisions/0020-build-benchmark-per-scenario.md
  - .planning/decisions/0021-cli-application-layer-placement.md
autonomous: true
requirements: [ARCH-06, ARCH-07]
must_haves:
  truths:
    - "`.planning/decisions/0019-binary-target-architecture.md` ratifies three binary targets — `paladin`, `paladin-cli` and `paladin-server` — and states a distinct purpose for each, which is the deliverable Milestone 4 Epic 3 FR3 asked for and the never-produced architecture review owed (D-19)."
    - "The three binary targets are recorded in `Cargo.toml` declaration order, and that order is the recorded one: `paladin` (no `required-features`), `paladin-cli` (`required-features = [\"cli\"]`), `paladin-server` (`required-features = [\"web-server\"]`). Two targets can never collide on a purpose because each is a distinct `[[bin]]` stanza with a distinct feature gate. [edge: ARCH-06/adjacency, ARCH-06/ordering]"
    - "The `paladin` binary — the one target with no `required-features` and therefore no gate — is given the honest purpose read from `src/main.rs`: the pre-Paladin content-aggregation service runner, declaring `#[structopt(name = \"smartcontent-aggregator\")]`, loading `config.yml` and calling the setup entry point. The stale name is recorded rather than tidied away, and the target is not left without a purpose. [edge: ARCH-06/empty]"
    - "ADR-0019 records the coupling that re-scopes Phase 8: `structopt`'s only consumer in the tree is `src/main.rs`, which is un-gated, so `structopt` cannot be made optional without first deciding that binary's fate; and `paladin-herald` re-introduces `colored` and `comfy-table` into a library-only build through a crate with no `[features]` section at all. The recorded three-line CLI-isolation fix is therefore wrong for one line and incomplete for two (D-20)."
    - "ADR-0019 carries `## Code Conformance: must change` naming Phase 16's documentation-currency work as the executor of the user-facing mdbook page, and naming Phase 8's CLI-isolation requirement as the consumer of the `structopt` precondition (D-21)."
    - "`.planning/decisions/0020-build-benchmark-per-scenario.md` transcribes the report's own five figures verbatim, marks two pass and three fail against the ≥ 50% target, and restates Milestone 5 SM-7 as a per-scenario target so each row can be judged (D-22)."
    - "ADR-0020 resolves the report's internal inconsistency by citing the summary table's −6.6% as authoritative and recording the conclusion's −5% as a transcription error, and it judges the report's \"Overall verdict: Target achieved\" conclusion as contradicted by its own table rather than reconciling it (D-22, D-23)."
    - "ADR-0020 declines the report's own recommended mid-tree re-measurement with a recorded reason — the pre-workspace monolith baseline commit no longer exists in buildable form on this branch and the environment carries offline / no-Docker constraints — so no later phase inherits an unfundable task (D-24)."
    - "`.planning/decisions/0021-cli-application-layer-placement.md` promotes `PROMOTION.md` Part B candidate 2, records that the CLI belongs in `src/application/cli` as an input adapter rather than in infrastructure, and cites the shipped tree that already implements it (D-25a)."
  artifacts:
    - ".planning/decisions/0019-binary-target-architecture.md — new ADR, `must change`, Phase 16 and Phase 8 named"
    - ".planning/decisions/0020-build-benchmark-per-scenario.md — new ADR, `conforms`"
    - ".planning/decisions/0021-cli-application-layer-placement.md — new ADR, `conforms`, closes PROMOTION.md's second Phase-7-owned candidate"
  key_links:
    - "ADR-0019 → Phase 16 — the user-facing binary-architecture mdbook page, flagged not written (D-21)"
    - "ADR-0019 → Phase 8 CLI isolation — the `structopt` precondition and the `paladin-herald` hole that re-scope DEBT-04 (D-20)"
    - "ADR-0020 → .project/Milestone_5-.../Epic_6/build-benchmarks.md — the source transcribed verbatim and judged, never corrected at source"
    - "ADR-0021 → PROMOTION.md Part B candidate 2 — the inventory entry this ADR closes, which is why the next-free line advances to 0022 rather than 0021"
  prohibitions:
    - "ADR-0019 must NOT invent a tidy purpose for the `paladin` binary and must NOT tidy away the stale `smartcontent-aggregator` name — the honest answer is the legacy content-aggregation service runner, and the stale name is part of the record."
    - "ADR-0020 must NOT reconcile or explain away the report's \"Overall verdict: Target achieved\" conclusion — it judges it against the report's own table."
    - "ADR-0020 must NOT re-measure the benchmark or pass a re-measurement task forward; the decline is recorded with its reason."
    - "Neither ADR may change any `.rs` file, `Cargo.toml`, or any file under `.github/` — retiring or migrating `src/main.rs`, gating `structopt`, and feature-gating `paladin-herald`'s formatters are all outside this phase."
---

<objective>
Close the two questions Milestone 4 and Milestone 5 left formally open: what each binary target is
for, and whether the ≥ 50% incremental-rebuild target passed.

Purpose: Milestone 4 Epic 3's Q1 recorded "User selected Option D — requires architecture review" and
made it a blocker for Task 3.3; no architecture-review record exists anywhere in the ingest set,
while the tree answers it de facto with three binary targets. And `build-benchmarks.md` marks four of
five scenarios as failing its own target and then concludes the target was achieved. Both are
recorded answers, not new measurements. ADR-0019 additionally carries the finding that re-scopes
Phase 8's CLI-isolation work, and ADR-0021 closes the second candidate `PROMOTION.md` assigns to this
phase.
Output: ADR-0019, ADR-0020 and ADR-0021.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-PATTERNS.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ADR-0019 — binary-target architecture and per-binary purpose</name>
  <read_first>
    - `.planning/decisions/PROMOTION.md` — the required heading set and the `must change` conformance shape
    - `.planning/decisions/0009-workspace-rust-edition-2024.md` — a `must change` exemplar whose `## Downstream Consumers` names another phase's requirement explicitly
    - `Cargo.toml` lines 240-252 — the three `[[bin]]` stanzas and their `required-features`, re-read at authoring time
    - `src/main.rs` first 40 lines — the `structopt` attribute, the settings load and the setup call that give the `paladin` binary its honest purpose
    - `src/bin/paladin-cli.rs` and `src/bin/paladin-server.rs` — the two gated binaries' entry points
    - `crates/paladin-herald/Cargo.toml` — confirm it declares `colored` and `comfy-table` unconditionally and has no `[features]` section
    - `.project/Milestone_4-Refactor-Crates-Features/Epic_3/prd-cli-isolation.md` FR2.1, FR5.4 and §8.3 — the isolation target D-20 re-scopes, and Q1/FR3/FR9.3, the never-produced deliverable
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md` §Decisions D-19, D-20, D-21
  </read_first>
  <files>.planning/decisions/0019-binary-target-architecture.md</files>
  <action>
Create `.planning/decisions/0019-binary-target-architecture.md`. No frontmatter. Exactly seven H2
headings in the canonical order.

`## Context` states that Milestone 4 Epic 3's Q1 recorded a selected Option D requiring architecture
review, made it a blocker for Task 3.3, and that no architecture-review record exists in the ingest
set; that FR3 asked for each binary's intended use case and FR9.3 asked for the documentation
deliverable, and neither was produced; and that the tree answers the question de facto with three
binary targets, i.e. Option A extended.

`## Decision` ratifies the three targets and gives each a stated purpose, listed in `Cargo.toml`
declaration order. For `paladin` (`src/main.rs`, no `required-features`) state the honest purpose
read from the source during this task: it is the pre-Paladin content-aggregation service runner — it
declares a `structopt` application name of `smartcontent-aggregator`, loads `config.yml` through the
settings loader, and calls the setup entry point. Record the stale application name as a finding
rather than tidying it away, and do not invent an agent-orchestration purpose it does not have. For
`paladin-cli` (`src/bin/paladin-cli.rs`, `required-features = ["cli"]`) state that it is the Armory
developer CLI. For `paladin-server` (`src/bin/paladin-server.rs`,
`required-features = ["web-server"]`) state that it is the Axum HTTP API server. Add a sentence that
the declaration order in `Cargo.toml` is the recorded order and that the three stanzas are distinct
targets with distinct gates, so no two share a purpose.

Then add a second Decision clause recording the coupling this answer exposes, because it re-scopes
Phase 8. State that `structopt`'s only consumer in the entire tree is `src/main.rs` — confirm that by
grepping `src/` and `crates/` during this task and record the hit count — and that because the
default `paladin` binary is not feature-gated, `structopt` cannot be marked optional without first
deciding that binary's fate: gate it, migrate it to `clap`, or retire it. State that the recorded
"three-line fix" for CLI dependency isolation is therefore wrong for one of its three lines. Add the
second half of the finding: `crates/paladin-herald/Cargo.toml` declares `colored` and `comfy-table`
unconditionally and has no `[features]` section at all, so gating them in the root manifest cannot
satisfy FR5.4's zero-CLI-dependencies-in-a-library-only-build criterion — confirm this by reading the
manifest. State plainly that whether `src/main.rs` should survive is new scope this ADR exposes but
does not own.

`## Considered Options` is a bulleted list, never prose: ratify Option A extended with three targets
and a stated purpose each (accepted); collapse to a single binary with subcommands (rejected — a code
change this phase is forbidden to make, against a shape three shipped stanzas already establish);
record the question as still open pending the never-produced architecture review (rejected — the
review has been owed since Milestone 4 and the tree has answered it); invent a forward-looking
purpose for `paladin` (rejected explicitly — the honest answer is the legacy aggregator and the
record is more useful than a tidy one).

`## Code Locations` is a bulleted list, never prose, one citation per bullet, all re-grepped: the
three `[[bin]]` stanzas in `Cargo.toml` with their exact lines; `src/main.rs`'s `structopt`
attribute line; `src/main.rs`'s settings-load and setup-call lines; `src/bin/paladin-cli.rs`;
`src/bin/paladin-server.rs`; the `cli` feature line in `Cargo.toml`; the `web-server` feature line;
`crates/paladin-herald/Cargo.toml`'s `colored` and `comfy-table` lines; and the Epic 3 PRD's FR2.1,
FR5.4, §8.3, Q1, FR3 and FR9.3 lines.

`## Code Conformance` is the bare phrase `must change` followed by prose: the user-facing
binary-architecture page does not exist and is owed; Phase 16's documentation-currency work is the
executor; this ADR plus its ledger rows satisfy FR9.3's record but not its user-facing deliverable;
and no code changes in this phase.

`## Downstream Consumers` is a bulleted list naming: Phase 16, which writes the mdbook page; Phase 8's
CLI-isolation requirement, which receives the `structopt` precondition and the `paladin-herald` hole
and must re-scope its three-line fix accordingly; plan 07-12, whose ledger rows for
`REQ-binary-target-config`, `REQ-cli-docs`, `REQ-cli-dependency-isolation` and
`REQ-library-only-build` cite this ADR; and plan 07-13, which adds this ADR's row to `PROMOTION.md`.
  </action>
  <verify>
    <automated>test -f .planning/decisions/0019-binary-target-architecture.md &amp;&amp; [ "$(grep -c '^## ' .planning/decisions/0019-binary-target-architecture.md)" = "7" ] &amp;&amp; grep -q 'smartcontent-aggregator' .planning/decisions/0019-binary-target-architecture.md &amp;&amp; grep -q 'paladin-herald' .planning/decisions/0019-binary-target-architecture.md &amp;&amp; [ "$(grep -rn structopt src/ crates/ | wc -l)" -ge "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## ' .planning/decisions/0019-binary-target-architecture.md` equals `7`, in the canonical order
    - `head -1 .planning/decisions/0019-binary-target-architecture.md` does not equal `---`
    - `sed -n '/^## Considered Options/,/^## Code Locations/p' .planning/decisions/0019-binary-target-architecture.md | grep -c '^- '` is at least `4`
    - `sed -n '/^## Code Locations/,/^## Code Conformance/p' .planning/decisions/0019-binary-target-architecture.md | grep -c '^- '` is at least `9`
    - `sed -n '/^## Code Conformance/,/^## Downstream Consumers/p' .planning/decisions/0019-binary-target-architecture.md | grep -c '^must change$'` equals `1`
    - `grep -c 'smartcontent-aggregator' .planning/decisions/0019-binary-target-architecture.md` is at least `1` — the stale name is recorded
    - `grep -c 'Phase 16' .planning/decisions/0019-binary-target-architecture.md` is at least `2` — the conformance executor and the downstream entry
    - `grep -c 'Phase 8' .planning/decisions/0019-binary-target-architecture.md` is at least `1`
    - `grep -c 'paladin-herald' .planning/decisions/0019-binary-target-architecture.md` is at least `1`
    - The `structopt` consumer count stated in `## Decision` matches `grep -rln structopt src/ crates/ | wc -l` as run during this task
    - `grep -c 'paladin-server' .planning/decisions/0019-binary-target-architecture.md` is at least `1` and `grep -c 'paladin-cli' .planning/decisions/0019-binary-target-architecture.md` is at least `1`
  </acceptance_criteria>
  <done>ADR-0019 exists with the seven canonical headings, names all three binary targets in declaration order with a distinct purpose each including the honest legacy-aggregator answer for `paladin`, records both halves of the CLI-isolation coupling as verified this task, carries a `must change` verdict, and names Phase 16 and Phase 8 as its consumers.</done>
</task>

<task type="auto">
  <name>Task 2: ADR-0021 — CLI placement in the application layer</name>
  <read_first>
    - `.planning/decisions/PROMOTION.md` Part B — the candidate inventory, including candidate 2 and its "Owner phase: Phase 7" assignment
    - `.project/Milestone_2-Missing_features/Epic_17.5/epic17-5.md` — the source decision being promoted; read the CLI-placement argument in full
    - `src/application/mod.rs` — the `pub mod cli;` declaration, located by grep, and whether it is feature-gated
    - `src/application/cli/` — confirm the directory exists and `src/cli/` does not
    - `.planning/codebase/ARCHITECTURE.md` — the layering rule the decision is expressed against
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md` §Decisions D-25a
  </read_first>
  <files>.planning/decisions/0021-cli-application-layer-placement.md</files>
  <action>
Create `.planning/decisions/0021-cli-application-layer-placement.md`. No frontmatter. Exactly seven
H2 headings in the canonical order.

`## Context` states that `PROMOTION.md` Part B names two promotable candidates owned by Phase 7 —
candidate 1 is promoted by ADR-0016 — and that candidate 2, the Epic 17.5 CLI-placement decision, was
not allocated a number by CONTEXT.md's D-25 and was given one by D-25a during research. State the
candidate's substance: the CLI belongs in `src/application/cli` because a CLI is an input adapter in
the application layer, not infrastructure; and state that shipped code already implements it —
`src/application/cli/` exists, `src/cli/` does not, and `src/application/mod.rs` declares the module.
State that the candidate is outranked in the corpus by a PRD that says otherwise, which is why an ADR
is what protects it.

`## Decision` ratifies the placement, states that `src/application/cli` is the recorded home for the
CLI input adapter, and records the un-gated `pub mod cli;` declaration as a separate observed fact —
the CLI module compiles into the library regardless of the `cli` feature — flagged as an input to
Phase 8's CLI-isolation work rather than corrected here. Cross-reference ADR-0019 for the binary-target
side of the same surface.

`## Considered Options` is a bulleted list, never prose: promote the candidate as its own ADR
(accepted — it is separately citable and `PROMOTION.md` recorded Phase 7 as its owner); record an
explicit deferral in `PROMOTION.md` naming a later owner (rejected — the recorded owner is this
phase, and leaving an unowned candidate is the state D-25a exists to close); treat the candidate as
already covered by ARCH-05's `src/application/use_cases/` correction (rejected — that correction is
about the orchestration home, not the CLI's layer).

`## Code Locations` is a bulleted list, never prose, one citation per bullet, re-grepped: the
`pub mod cli;` line in `src/application/mod.rs`; the `src/application/cli/` directory's entry module;
the `.project/Milestone_2-Missing_features/Epic_17.5/epic17-5.md` source as provenance; and
`.planning/decisions/0019-binary-target-architecture.md` as the sibling answer on the same surface.

`## Code Conformance` is the bare word `conforms` plus prose stating that shipped code already
implements the placement and no code changes; the un-gated module declaration is recorded as a
finding for Phase 8, not as a conformance failure of this decision.

`## Downstream Consumers` is a bulleted list naming: Phase 8's CLI-isolation requirement, which
receives the un-gated `pub mod cli;` observation; plan 07-12, whose Milestone 4 Epic 3 ledger rows may
cite this ADR; and plan 07-13, which adds this ADR's row to `PROMOTION.md`, closes candidate 2 in the
Part B inventory, and advances the next-free number to 0022 rather than 0021.
  </action>
  <verify>
    <automated>test -f .planning/decisions/0021-cli-application-layer-placement.md &amp;&amp; [ "$(grep -c '^## ' .planning/decisions/0021-cli-application-layer-placement.md)" = "7" ] &amp;&amp; test -d src/application/cli &amp;&amp; ! test -d src/cli &amp;&amp; grep -q 'epic17-5.md' .planning/decisions/0021-cli-application-layer-placement.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## ' .planning/decisions/0021-cli-application-layer-placement.md` equals `7`, in the canonical order
    - `head -1 .planning/decisions/0021-cli-application-layer-placement.md` does not equal `---`
    - `sed -n '/^## Considered Options/,/^## Code Locations/p' .planning/decisions/0021-cli-application-layer-placement.md | grep -c '^- '` is at least `3`
    - `sed -n '/^## Code Locations/,/^## Code Conformance/p' .planning/decisions/0021-cli-application-layer-placement.md | grep -c '^- '` is at least `4`
    - `sed -n '/^## Code Conformance/,/^## Downstream Consumers/p' .planning/decisions/0021-cli-application-layer-placement.md | grep -c '^conforms$'` equals `1`
    - `test -d src/application/cli` succeeds and `test -d src/cli` fails — the tree claim holds at authoring time
    - `grep -c 'epic17-5.md' .planning/decisions/0021-cli-application-layer-placement.md` is at least `1` — the provenance is cited
    - `grep -c '0022' .planning/decisions/0021-cli-application-layer-placement.md` is at least `1` — the next-free consequence is recorded
  </acceptance_criteria>
  <done>ADR-0021 exists with the seven canonical headings, promotes `PROMOTION.md`'s second Phase-7-owned candidate, cites the shipped tree that already implements it, records the un-gated CLI module as a Phase 8 input, and states that the next-free ADR number advances to 0022.</done>
</task>

<task type="auto">
  <name>Task 3: ADR-0020 — the build-time benchmark restated per scenario</name>
  <read_first>
    - `.project/Milestone_5-Workspace-Decomposition/Epic_6/build-benchmarks.md` — read in full: the summary table, the conclusion paragraph, the methodology note, the recommended follow-up, and the baseline commit reference. Transcribe the figures from this file, not from any secondary source
    - `.project/Milestone_5-Workspace-Decomposition/Epic_6/prd-workspace-finalization-epic-6.md` FR-3.5 and SM-7 — the target being restated
    - `.planning/decisions/0006-coverage-gate.md` — the precedent for judging a stale or self-contradictory figure rather than reconciling it
    - `.planning/decisions/PROMOTION.md` — the required heading set
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md` §Decisions D-22, D-23, D-24
  </read_first>
  <files>.planning/decisions/0020-build-benchmark-per-scenario.md</files>
  <action>
Create `.planning/decisions/0020-build-benchmark-per-scenario.md`. No frontmatter. Exactly seven H2
headings in the canonical order.

`## Context` states that Milestone 5 SM-7 sets a single ≥ 50% incremental-rebuild improvement target;
that `build-benchmarks.md`'s summary table records five scenarios of which two meet it and three do
not; that the report's conclusion nevertheless declares the target achieved; and that the same
document states the clean-build shortfall as one figure in its table and a different figure in its
conclusion. State the report's own methodology caveat: the monolith baseline incremental was measured
by touching the crate root, which is primarily a module-tree re-export file, so Rust's incremental
pipeline skips most recompilation and the baseline is a best case — making the comparisons
conservative estimates of the workspace advantage.

`## Decision` does four things. (i) Transcribe the report's own five figures verbatim into a table
with a column for the scenario, the monolith median, the workspace median, the improvement, and the
pass or fail verdict against ≥ 50%: clean build at −6.6% fail; incremental core change at −18.9%
fail; incremental LLM adapter change at −44.6% fail; incremental memory adapter change at −50.2%
pass; incremental battalion-only at −90.9% pass. Read every figure from the source file during this
task; do not transcribe from research notes. (ii) Restate SM-7 as a per-scenario target so each row
can be judged independently, and state which scenarios the target is meaningful for given the
methodology caveat. (iii) Resolve the internal inconsistency by citing the summary table's clean-build
figure as authoritative and recording the conclusion's differing figure as a transcription error — do
not attempt to reconcile them as if they measured different things. (iv) Judge the "Overall verdict:
Target achieved" conclusion as contradicted by the report's own table, in the same way ADR-0006
judged a stale coverage figure, without explaining it away.

Then add a fifth clause declining the re-measurement the report itself recommends, with the reason:
the ≥ 50% figure is a comparison against the pre-workspace monolith, that tree no longer exists on
this branch in buildable form, re-measuring means resurrecting a historical commit and running full
clean and incremental builds for a metric about a restructuring that completed three milestones ago,
and this environment carries documented offline and no-Docker constraints. State that the decline is
deliberate and that no re-measurement task is passed forward, so no later phase inherits an
unfundable one.

`## Considered Options` is a bulleted list, never prose: restate SM-7 per scenario and judge the
report against its own table (accepted); re-measure against a mid-tree monolith baseline as the report
recommends (rejected, with the reason above); accept the report's conclusion as written (rejected — it
is contradicted by the table above it); amend `build-benchmarks.md` at source (rejected — the report is
a historical measurement record, transcribed and judged here, not corrected).

`## Code Locations` is a bulleted list, never prose, one citation per bullet: the summary table's
line range in `build-benchmarks.md`; the conclusion paragraph's line range; the methodology note's
line; the recommended-follow-up line; the baseline commit reference; and the Epic 6 PRD's FR-3.5 and
SM-7 lines. Re-grep each at authoring time.

`## Code Conformance` is the bare word `conforms` plus prose: this ADR restates a target and judges a
historical measurement; no code changes and no benchmark is re-run.

`## Downstream Consumers` is a bulleted list naming: plan 07-11, whose `REQ-build-benchmark-report`
and `REQ-llm-build-validation` ledger rows cite this ADR; plan 07-13, which adds this ADR's row to
`PROMOTION.md`; and an explicit entry recording that no phase inherits a re-measurement task.

Commit this plan's three ADR files in a single commit at the end of the plan, with a Bash timeout of
at least 300000 ms.
  </action>
  <verify>
    <automated>test -f .planning/decisions/0020-build-benchmark-per-scenario.md &amp;&amp; [ "$(grep -c '^## ' .planning/decisions/0020-build-benchmark-per-scenario.md)" = "7" ] &amp;&amp; grep -q '6.6' .planning/decisions/0020-build-benchmark-per-scenario.md &amp;&amp; grep -q '90.9' .planning/decisions/0020-build-benchmark-per-scenario.md &amp;&amp; grep -q '50.2' .planning/decisions/0020-build-benchmark-per-scenario.md</automated>
    <human-check>Diff ADR-0020's five figures against `build-benchmarks.md`'s summary table and confirm every number matches the source (07-VALIDATION.md §Manual-Only Verifications, row 3).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## ' .planning/decisions/0020-build-benchmark-per-scenario.md` equals `7`, in the canonical order
    - `head -1 .planning/decisions/0020-build-benchmark-per-scenario.md` does not equal `---`
    - `sed -n '/^## Decision/,/^## Considered Options/p' .planning/decisions/0020-build-benchmark-per-scenario.md | grep -c '^| '` is at least `7` — a header, a separator and five scenario rows
    - The five improvement figures in the ADR's table each appear in `.project/Milestone_5-Workspace-Decomposition/Epic_6/build-benchmarks.md`
    - `sed -n '/^## Considered Options/,/^## Code Locations/p' .planning/decisions/0020-build-benchmark-per-scenario.md | grep -c '^- '` is at least `4`
    - `sed -n '/^## Code Locations/,/^## Code Conformance/p' .planning/decisions/0020-build-benchmark-per-scenario.md | grep -c '^- '` is at least `6`
    - `sed -n '/^## Code Conformance/,/^## Downstream Consumers/p' .planning/decisions/0020-build-benchmark-per-scenario.md | grep -c '^conforms$'` equals `1`
    - `git diff --numstat HEAD -- .project/Milestone_5-Workspace-Decomposition/Epic_6/build-benchmarks.md` is empty — the source report is transcribed, never edited
    - `git log -1 --name-only` shows all three ADR files in a single commit
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
  </acceptance_criteria>
  <done>ADR-0020 exists with the seven canonical headings, transcribes the report's five figures with two pass and three fail verdicts, restates SM-7 per scenario, resolves the internal figure inconsistency by naming the conclusion's number a transcription error, judges the overall verdict against the report's own table, and declines the re-measurement with a recorded reason.</done>
</task>

</tasks>

<flagged_assumptions>
Two edge-probe rows for this phase came back `unclassified`, and per the fallback protocol an
`unclassified` row is never auto-resolved and never dropped. One of the two is surfaced here as an
explicit planner assumption; the other is surfaced in plan 07-09.

- **ARCH-07 / unclassified — "review manually".** The probe's eight-category taxonomy has no reading
  that fits ARCH-07. The planner's assumption, stated so a reviewer can reject it: *ARCH-07's
  falsifiability requirement is satisfied by restatement plus judgement, without any re-measurement,
  because the comparison baseline is a tree that no longer exists in buildable form.* If that
  assumption is wrong — if a reviewer holds that a target is not falsifiable until it is re-measured
  — then D-22's and D-24's decline is the wrong call and ADR-0020 needs a re-measurement task after
  all. The assumption is recorded here rather than silently resolved into a `must_haves` truth.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADR → Phase 8 and Phase 16 planners | ADR-0019 carries a `must change` verdict and two findings that re-scope another phase's requirement. A finding recorded imprecisely sends Phase 8 to make a change that breaks the default build. |
| A transcribed measurement → the recorded target | ADR-0020's five figures become the recorded verdicts for SM-7. A transcription error here would repeat the exact defect the ADR exists to name. |
| `.project/` measurement report → the ADR that judges it | `build-benchmarks.md` is a historical record. Correcting its figures at source would erase the contradiction ADR-0020 is documenting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-24 | Tampering (of the record) | ADR-0020's transcribed figures | high | mitigate | A figure transcribed from research notes rather than the source would put a second wrong number into the corpus, in an ADR whose subject is a wrong number. Mitigated by requiring every figure to be read from `build-benchmarks.md` during the task and asserting each appears in that file. |
| T-07-25 | Tampering (of the record) | ADR-0019's `structopt` and `paladin-herald` findings | high | mitigate | Phase 8 acts on these findings. An overstated or understated consumer count would send it to gate a dependency the default build needs. Mitigated by requiring the grep to be run during the task and the stated count to match it. |
| T-07-26 | Repudiation | `build-benchmarks.md` | medium | mitigate | Editing the source report to remove its contradiction would erase the evidence for ADR-0020's judgement. Mitigated by asserting the file's diff is empty. |
| T-07-27 | Elevation of privilege (of an unfunded task) | the declined re-measurement | low | accept | Recording a decline rather than a task means a future reader may re-propose the measurement. Accepted: the decline carries its reason in the ADR, which is the cheapest durable form, and re-proposing it with the reason in view is a legitimate outcome. |
| T-07-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable: no package installed, no manifest modified. |

No critical threats. T-07-24 and T-07-25 are the `high` items and both are mitigated inside this plan.
</threat_model>

<artifacts>
## Artifacts this phase produces

The full phase-wide artefact table is recorded once in `07-01-PLAN.md` §Artifacts. This plan produces
these rows:

| Artefact | Status | Owning plan | Requirement |
|---|---|---|---|
| `.planning/decisions/0019-binary-target-architecture.md` | **NEW** ADR — `must change`, Phase 16 executor, Phase 8 precondition | 07-07 | ARCH-06 |
| `.planning/decisions/0020-build-benchmark-per-scenario.md` | **NEW** ADR — `conforms` | 07-07 | ARCH-07 |
| `.planning/decisions/0021-cli-application-layer-placement.md` | **NEW** ADR — `conforms`, closes PROMOTION.md candidate 2 | 07-07 | ARCH-06 |

New headings created by this plan: the seven canonical H2s in each of the three new ADRs. No
existing document is edited — `build-benchmarks.md` and the Epic 3 CLI-isolation PRD are cited only.
</artifacts>

<verification>
- `grep -c '^## '` equals `7` for each of the three new ADR files
- `sed -n '/^## Code Conformance/,/^## Downstream Consumers/p' .planning/decisions/0019-binary-target-architecture.md | grep -c '^must change$'` equals `1`
- `grep -c 'smartcontent-aggregator' .planning/decisions/0019-binary-target-architecture.md` is at least `1`
- The five figures in ADR-0020's table each appear in `.project/Milestone_5-Workspace-Decomposition/Epic_6/build-benchmarks.md`
- `git diff --numstat HEAD~1 -- '.project/'` is empty — no `.project/` file was edited by this plan
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
</verification>

<success_criteria>
Every binary target has a documented purpose, closing the architecture review Milestone 4 Epic 3 made
a blocker and never produced — with the `paladin` binary's honest purpose and its stale application
name recorded rather than tidied. Phase 8 receives a corrected CLI-isolation scope by name, and
Phase 16 receives the mdbook page by name. The ≥ 50% incremental-rebuild target can be judged pass or
fail per scenario, its report's self-contradiction is named rather than reconciled, and the
re-measurement is declined with a reason instead of passed forward as an unfundable task. The second
ADR candidate `PROMOTION.md` assigns to this phase is closed rather than left unowned.
</success_criteria>

<output>
Create `.planning/phases/07-workspace-ground-truth-recorded-answers/07-07-SUMMARY.md` when done.
Record: the `structopt` grep result verbatim, the `paladin-herald` manifest facts as read, the five
benchmark figures as transcribed with their source line numbers, and the flagged ARCH-07 assumption
so the phase verifier sees it.
</output>
