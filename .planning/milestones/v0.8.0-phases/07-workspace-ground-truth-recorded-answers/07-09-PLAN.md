---
phase: 07-workspace-ground-truth-recorded-answers
plan: 09
type: execute
wave: 6
depends_on: ["07-01", "07-05"]
files_modified:
  - .project/Milestone_4-Refactor-Crates-Features/Epic_1/prd-expand-feature-flags.md
  - .project/Milestone_4-Refactor-Crates-Features/Milestone-Overview/Milesone-4-Tier-1-High-Value-Low-Risk.md
  - .project/Milestone_5-Workspace-Decomposition/overview/Milestone_5-Tier_2-Workspace-Decomposition.md
  - .project/Milestone_6-Architectural-Refinements/Epic_2/prd-relocate-orchestration-services.md
autonomous: true
requirements: [ARCH-05, ARCH-02, ARCH-04]
must_haves:
  truths:
    - "All five documented positions shipped code contradicts are corrected at source with a dated banner plus an inline annotation, and none of the five receives a new ADR — contested positions get ADRs, code-settled divergences get source corrections plus `diverged` ledger rows (D-17)."
    - "The `vision` feature-gating correction states that `vision` gates no dependency at all, that `chacha20poly1305` and `zeroize` are unconditional, that the Epic 1 `dependency-matrix.md` audit was right and the PRD was wrong, and that applying the PRD literally would break a no-default-features build for user auth and Citadel encryption — with `.planning/decisions/0011-vision-port-surfaces.md` cross-referenced as the decision that dispositioned the same encryption code."
    - "The MCP transport feature-flag correction is applied to both documents that carry the position: the Epic 1 PRD's own dated elimination note is recorded as what shipped, and the Milestone 4 overview's acceptance criterion and appendix are annotated superseded."
    - "The `web-server` correction records the shipped feature definition gating the web crate and axum, and records that actix-web is not a root dependency — both read from `Cargo.toml` during this task rather than transcribed."
    - "The `paladin-cli` correction records that no such workspace crate exists, that the CLI ships as a feature plus a binary target with `required-features`, and that Milestone 5 Epic 6's non-goal was correct while the overview's target structure and appendix were not."
    - "The `src/application/use_cases/` correction records that the four orchestrator modules ship under `src/application/services/` with the PRD's exact module names and that `use_cases/` does not exist — and it does not re-open Non-Goal 7 or Open Question 4, which ADR-0018 already settled."
    - "Every corrected document still contains its original claim text after the correction, alongside a dated correction banner — proving annotation, not rewriting (D-00g)."
  artifacts:
    - ".project/Milestone_4-.../Epic_1/prd-expand-feature-flags.md — dated banner plus three inline corrections (vision gating, MCP transports, web-server)"
    - ".project/Milestone_4-.../Milestone-Overview/Milesone-4-Tier-1-High-Value-Low-Risk.md — MCP acceptance-criterion and appendix annotation, added to the ADR-0014 banner plan 07-01 already placed"
    - ".project/Milestone_5-.../overview/Milestone_5-Tier_2-Workspace-Decomposition.md — dated banner, title numbering correction, and target-structure / Appendix D `paladin-cli` correction"
    - ".project/Milestone_6-.../Epic_2/prd-relocate-orchestration-services.md — dated banner, target-directory correction, and confirmation notes on Non-Goal 7 and Open Question 4"
  key_links:
    - "Each corrected position → its `diverged` ledger row in plan 07-12 (Milestone 4) and plan 07-08 (Milestone 6) — the source correction and the ledger verdict are the two halves of one finding"
    - "vision gating correction → .planning/decisions/0011-vision-port-surfaces.md — the prior decision that dispositioned the same encryption code"
    - "paladin-cli correction → .planning/decisions/0019-binary-target-architecture.md — the shipped binary-target shape that replaces the imagined crate"
    - "use_cases/ correction → .planning/decisions/0018-m6-facade-reexport-policy.md — the settled policy this correction must not re-open"
  prohibitions:
    - "None of the five corrections may delete, replace, or rewrite the original defective text — each original claim is retained and marked superseded."
    - "None of the five positions may be given a new ADR — they are settled by shipped code with no competing defensible position, so they get source corrections and `diverged` ledger rows."
    - "The `use_cases/` correction must NOT re-open Non-Goal 7 or Open Question 4 — ADR-0018 settled the re-export policy and this correction cites it."
    - "This plan must NOT edit any `.rs` file, `Cargo.toml`, or any file under `.github/` — every position is corrected in the document, never in the code it mis-describes."
---

<objective>
Make it impossible to break the build by applying an old PRD literally: correct at source the five
documented positions that shipped code contradicts.

Purpose: four of the five are load-bearing. `vision = []` gates nothing, so a PRD that says it gates
the encryption crates would, if applied, break a no-default-features build for user auth and Citadel.
No MCP feature flag of any kind exists. `web-server` gates the web crate and axum, not actix-web. No
`paladin-cli` workspace crate was ever built. And `src/application/use_cases/` — the Milestone 6
Epic 2 target directory — does not exist at all. None of the five is contested; all five are settled
by the tree, which is why they get source corrections rather than ADRs.
Output: four `.project/` documents annotated, producing the evidence the `diverged` ledger rows in
plans 07-08 and 07-12 cite.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-PATTERNS.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the Epic 1 feature-flag PRD — vision gating, MCP transports, web-server</name>
  <read_first>
    - `.project/Milestone_4-Refactor-Crates-Features/Epic_1/prd-expand-feature-flags.md` — read in full before touching it; locate FR1, the Design Considerations block, and the dated 2026-04-15 MCP elimination note by grep
    - `.project/Milestone_4-Refactor-Crates-Features/Epic_1/dependency-matrix.md` — the audit that was right; cited, never corrected
    - `Cargo.toml` `[features]` block — read the `vision`, `web-server`, `cli` and `full` definitions and their exact line numbers during this task; research recorded `vision` at line 274 and `web-server` at 276 on 2026-08-06, but re-grep rather than trusting those
    - `grep -rn 'chacha20poly1305\|zeroize' Cargo.toml crates/*/Cargo.toml` output — confirm both are unconditional
    - `grep -rn 'actix' Cargo.toml` output — confirm actix-web is not a root dependency
    - `.planning/decisions/0011-vision-port-surfaces.md` — the prior decision that dispositioned the same encryption code
    - `.project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` lines 1-8 and 28-31 — the verbatim D-00g markup shapes
  </read_first>
  <files>.project/Milestone_4-Refactor-Crates-Features/Epic_1/prd-expand-feature-flags.md</files>
  <action>
Annotate this one document per D-00g. This plan owns the whole file; make all three corrections in
one pass.

(1) Add a top-of-file blockquote banner immediately under the existing H1, dated, stating in three
sentences that this PRD's FR1 and Design Considerations describe three feature-flag behaviours the
shipped manifest contradicts — the `vision` gating, the MCP transport flags, and the `web-server`
gating — and that applying the `vision` clause literally would break a no-default-features build.
Point at the Milestone 4-6 ledger's `diverged` rows and at
`.planning/decisions/0011-vision-port-surfaces.md`. Close with the retained-original sentence.

(2) Correct the `vision` gating claim inline with the strikethrough-and-append shape: the original
claim struck through, the shipped behaviour appended, and a bold note stating that the shipped
`vision` feature is an empty feature gating no dependency, that `chacha20poly1305` and `zeroize` are
unconditional root dependencies because `security/encryption.rs` serves user auth and Citadel, that
the Epic 1 `dependency-matrix.md` audit in this same directory classified both correctly and the PRD
did not, and that applying the PRD would break `cargo build --no-default-features`. Cite the exact
`Cargo.toml` line for the `vision` feature as re-grepped in this task, and cross-reference ADR-0011.

(3) Correct the MCP transport flags inline with the same shape: the original flag names struck
through, and a bold note stating that no MCP feature flag of any kind exists in the shipped manifest,
that this PRD's own dated 2026-04-15 elimination note is what shipped, and that Arsenal and its MCP
transport adapters compile unconditionally as core framework components. Cite the elimination note's
own line in this document.

(4) Correct the `web-server` gating claim inline with the same shape: the original actix-web mention
struck through, and a bold note giving the shipped feature definition verbatim from `Cargo.toml` with
its line number, and stating that actix-web is not a root dependency, confirmed by grep in this task.

Do not restate this document as authoritative anywhere in the annotation.
  </action>
  <verify>
    <automated>grep -q 'ADR-0011' .project/Milestone_4-Refactor-Crates-Features/Epic_1/prd-expand-feature-flags.md &amp;&amp; [ "$(grep -c '~~' .project/Milestone_4-Refactor-Crates-Features/Epic_1/prd-expand-feature-flags.md)" -ge "3" ] &amp;&amp; [ "$(grep -rc 'actix' Cargo.toml)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '~~' .project/Milestone_4-Refactor-Crates-Features/Epic_1/prd-expand-feature-flags.md` is at least `3` — three inline corrections, each retaining its original text
    - `grep -c 'ADR-0011' .project/Milestone_4-Refactor-Crates-Features/Epic_1/prd-expand-feature-flags.md` is at least `1`
    - `grep -c 'dependency-matrix.md' .project/Milestone_4-Refactor-Crates-Features/Epic_1/prd-expand-feature-flags.md` is at least `1` — the audit that was right is cited
    - `grep -c 'no-default-features' .project/Milestone_4-Refactor-Crates-Features/Epic_1/prd-expand-feature-flags.md` is at least `1` — the build-breaking consequence is stated
    - `grep -c 'chacha20poly1305' .project/Milestone_4-Refactor-Crates-Features/Epic_1/prd-expand-feature-flags.md` is at least `1`
    - `git diff --numstat HEAD -- .project/Milestone_4-Refactor-Crates-Features/Epic_1/prd-expand-feature-flags.md` shows deletions no greater than `4`
    - The `Cargo.toml` line numbers cited in the annotation match `grep -n` output as run during this task
    - `git diff --numstat HEAD -- .project/Milestone_4-Refactor-Crates-Features/Epic_1/dependency-matrix.md` is empty — the audit is cited, never corrected
  </acceptance_criteria>
  <done>The Epic 1 feature-flag PRD carries a dated banner and three inline corrections — vision gating, MCP transports, web-server — each citing a freshly re-grepped `Cargo.toml` line and each retaining its original claim text.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the two milestone overviews — the MCP position and the `paladin-cli` target structure</name>
  <read_first>
    - `.project/Milestone_4-Refactor-Crates-Features/Milestone-Overview/Milesone-4-Tier-1-High-Value-Low-Risk.md` — read in full; it already carries plan 07-01's ADR-0014 title banner, and this task adds to it without disturbing it; locate acceptance criterion 1, Task 1.5's transport argument, and Appendix B's proposed features block by grep
    - `.project/Milestone_5-Workspace-Decomposition/overview/Milestone_5-Tier_2-Workspace-Decomposition.md` — read in full; locate the H1 title, the target workspace structure listing `crates/paladin-cli/`, Appendix C's dependency-matrix row and Appendix D's members list by grep
    - `Cargo.toml` — the `[[bin]] paladin-cli` stanza with its `required-features`, and the `cli` feature definition; re-grep both line numbers
    - `crates/` directory listing — confirm no `paladin-cli` crate exists and record what is actually there
    - `.project/Milestone_5-Workspace-Decomposition/Epic_6/prd-workspace-finalization-epic-6.md` Non-Goals — the clause that was correct, cited not corrected
    - `.planning/decisions/0014-milestone-4-6-tier-numbering.md` and `.planning/decisions/0019-binary-target-architecture.md` — the two ADRs these annotations cite
  </read_first>
  <files>.project/Milestone_4-Refactor-Crates-Features/Milestone-Overview/Milesone-4-Tier-1-High-Value-Low-Risk.md, .project/Milestone_5-Workspace-Decomposition/overview/Milestone_5-Tier_2-Workspace-Decomposition.md</files>
  <action>
(A) In the Milestone 4 overview, add the MCP correction without disturbing the ADR-0014 title banner
plan 07-01 placed. Extend that banner with one additional sentence noting the MCP feature-flag
position is also corrected below, then correct acceptance criterion 1 and Appendix B's proposed
features block inline with the strikethrough-and-append shape: the MCP flag names struck through, and
a bold note stating that no MCP feature flag of any kind exists in the shipped manifest, that the
Epic 1 PRD's dated 2026-04-15 elimination note is what shipped, and that Arsenal and its MCP transport
adapters compile unconditionally. Cite the Epic 1 PRD's elimination note by relative path. Add a
one-line note beside Task 1.5's transport argument recording that the argument was superseded by that
note rather than by an error in its reasoning.

(B) In the Milestone 5 overview, add a top-of-file blockquote banner immediately under the existing
H1, dated, naming both ADR-0014 and ADR-0019, stating in two sentences that this document titles
itself "Milestone 2" while the authoritative numbering makes it Milestone 5 (Tier 2), and that its
target workspace structure and Appendix D list a `crates/paladin-cli/` member that was never built.
Then correct the H1 inline with the strikethrough-and-append shape citing ADR-0014. Then correct the
target-structure listing, the Appendix C dependency-matrix row and the Appendix D members list — three
separate inline corrections — each striking through the `paladin-cli` crate entry and appending a bold
note stating that no such crate exists, that `crates/` holds the ten library crates plus
`doc-examples`, and that the CLI ships as the `cli` feature plus a `[[bin]] paladin-cli` target with
`required-features`, citing both `Cargo.toml` lines as re-grepped in this task and citing ADR-0019.
Add one clause recording that Milestone 5 Epic 6's non-goal was correct and this overview's target
structure was not.

Neither document may be rewritten. Every original claim survives inside its strikethrough markers.
  </action>
  <verify>
    <automated>grep -q 'ADR-0019' .project/Milestone_5-Workspace-Decomposition/overview/Milestone_5-Tier_2-Workspace-Decomposition.md &amp;&amp; grep -q 'ADR-0014' .project/Milestone_5-Workspace-Decomposition/overview/Milestone_5-Tier_2-Workspace-Decomposition.md &amp;&amp; [ "$(grep -c '~~' .project/Milestone_5-Workspace-Decomposition/overview/Milestone_5-Tier_2-Workspace-Decomposition.md)" -ge "4" ] &amp;&amp; ! test -d crates/paladin-cli</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ADR-0014' .project/Milestone_4-Refactor-Crates-Features/Milestone-Overview/Milesone-4-Tier-1-High-Value-Low-Risk.md` is still at least `2` — plan 07-01's banner and title note survive untouched
    - `grep -ci 'MCP' .project/Milestone_4-Refactor-Crates-Features/Milestone-Overview/Milesone-4-Tier-1-High-Value-Low-Risk.md` is at least `2` and at least one occurrence sits inside a strikethrough correction
    - `grep -c '~~' .project/Milestone_5-Workspace-Decomposition/overview/Milestone_5-Tier_2-Workspace-Decomposition.md` is at least `4` — the title plus three `paladin-cli` corrections
    - `grep -c 'ADR-0019' .project/Milestone_5-Workspace-Decomposition/overview/Milestone_5-Tier_2-Workspace-Decomposition.md` is at least `2`
    - `grep -c 'ADR-0014' .project/Milestone_5-Workspace-Decomposition/overview/Milestone_5-Tier_2-Workspace-Decomposition.md` is at least `2`
    - `test -d crates/paladin-cli` fails — the claim the corrections make holds at authoring time
    - `git diff --numstat HEAD` shows deletions no greater than `2` for the Milestone 4 overview and no greater than `5` for the Milestone 5 overview
    - The `Cargo.toml` line numbers cited for the `cli` feature and the `[[bin]] paladin-cli` stanza match `grep -n` output as run during this task
  </acceptance_criteria>
  <done>Both milestone overviews carry their corrections: the Milestone 4 overview's MCP acceptance criterion and appendix annotated without disturbing plan 07-01's title banner, and the Milestone 5 overview's title plus its three `paladin-cli` target-structure claims corrected with ADR-0019 cited and every original claim retained.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the Milestone 6 Epic 2 PRD — the `use_cases/` target directory</name>
  <read_first>
    - `.project/Milestone_6-Architectural-Refinements/Epic_2/prd-relocate-orchestration-services.md` — read in full; locate the `src/application/use_cases/` target-directory clause, Non-Goal 7 and Open Question 4 by grep
    - `src/application/` and `src/application/services/` directory listings — confirm the four orchestrator module groups and the absence of `use_cases/`
    - `.planning/decisions/0018-m6-facade-reexport-policy.md` — the settled policy this correction cites and must not re-open
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-PATTERNS.md` §6 markup shapes (a) and (b)
  </read_first>
  <files>.project/Milestone_6-Architectural-Refinements/Epic_2/prd-relocate-orchestration-services.md</files>
  <action>
Annotate this one document per D-00g. This plan owns the whole file.

(1) Add a top-of-file blockquote banner immediately under the existing H1, dated, naming ADR-0018,
stating in two sentences that this PRD's target directory `src/application/use_cases/` does not exist
in the shipped tree and that the four orchestrator module groups ship under
`src/application/services/` with this PRD's exact module names, and that the PRD's Non-Goal 7 no-shim
position is the recorded answer confirmed by ADR-0018. Close with the retained-original sentence.

(2) Correct the target-directory clause inline with the strikethrough-and-append shape: the original
`src/application/use_cases/` path struck through, `src/application/services/` appended, and a bold
note stating that the module names are unchanged, that only the parent directory differs, that this
is a deliberate divergence rather than a missing deliverable, and that the ledger records it
`diverged`. Cite the four shipped module paths with line numbers read during this task.

(3) Add a bold confirmation note beneath Non-Goal 7 and beneath Open Question 4 — no strikethrough,
since both are correct — stating that ADR-0018 ratifies the no-shim posture and that Open Question 4
is now confirmed rather than left dangling. Do not re-open either question and do not restate the
policy; cite ADR-0018 and stop.

Commit this plan's four files together in a single commit at the end of the plan, with a Bash timeout
of at least 300000 ms.
  </action>
  <verify>
    <automated>grep -q 'ADR-0018' .project/Milestone_6-Architectural-Refinements/Epic_2/prd-relocate-orchestration-services.md &amp;&amp; grep -q 'src/application/services' .project/Milestone_6-Architectural-Refinements/Epic_2/prd-relocate-orchestration-services.md &amp;&amp; ! test -d src/application/use_cases</automated>
    <human-check>Open each of the four corrected documents and confirm the original claim text is still readable inside its strikethrough markers and that no section was removed (07-VALIDATION.md §Manual-Only Verifications, row 2).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ADR-0018' .project/Milestone_6-Architectural-Refinements/Epic_2/prd-relocate-orchestration-services.md` is at least `4` — the banner, the target-directory note, and the two confirmation notes
    - `grep -c 'src/application/services' .project/Milestone_6-Architectural-Refinements/Epic_2/prd-relocate-orchestration-services.md` is at least `1`
    - `grep -c 'use_cases' .project/Milestone_6-Architectural-Refinements/Epic_2/prd-relocate-orchestration-services.md` is at least `1` — the original path text survives the correction
    - `grep -c '~~' .project/Milestone_6-Architectural-Refinements/Epic_2/prd-relocate-orchestration-services.md` is at least `1`
    - `test -d src/application/use_cases` fails
    - `git diff --numstat HEAD -- .project/Milestone_6-Architectural-Refinements/Epic_2/prd-relocate-orchestration-services.md` shows deletions no greater than `2`
    - `git log -1 --name-only` shows all four annotated documents in a single commit
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
  </acceptance_criteria>
  <done>The Milestone 6 Epic 2 PRD carries a dated ADR-0018 banner, its target directory corrected inline to the shipped path with the module names recorded as unchanged, and confirmation notes on Non-Goal 7 and Open Question 4 that cite rather than re-open the settled policy.</done>
</task>

</tasks>

<flagged_assumptions>
One of the two `unclassified` edge-probe rows for this phase is surfaced here as an explicit planner
assumption rather than silently resolved into a `must_haves` truth. The other is surfaced in plan
07-07.

- **ARCH-05 / unclassified — "review manually".** The probe's eight-category taxonomy has no reading
  that fits ARCH-05. The planner's assumption, stated so a reviewer can reject it: *the five
  positions ARCH-05 enumerates are exactly five, closed, and each is settled by the shipped tree with
  no competing defensible position — so a source correction plus a `diverged` ledger row is the whole
  remedy and no ADR is warranted for any of them.* If a sixth literal-application hazard exists in
  the Milestone 4-6 corpus that this enumeration misses, it will not be corrected by this phase and
  will surface only when someone applies it. The mitigation available inside this phase is the
  ledger's own `diverged` class, which any later fan-out plan may assign to a row it finds in the
  same shape.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.project/` PRD → a future implementer applying it literally | This is the boundary ARCH-05 exists to defend. A PRD clause that survives uncorrected can be executed as an instruction and break the build — the `vision` gating clause specifically would break a no-default-features build for user auth and Citadel. |
| `.project/` historical corpus → the correction that annotates it | Five ingest runs cite these documents. A correction that deletes rather than annotates destroys evidence no later run can regenerate. |
| A cited manifest line → the correction that quotes it | The corrections quote `Cargo.toml` line numbers into historical documents. A stale line makes the correction itself a new defect of the class it is fixing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-32 | Tampering (of the record) | the `vision` gating correction | high | mitigate | An incomplete or wrongly cited correction leaves the build-breaking clause executable. Mitigated by requiring the shipped feature line to be re-grepped and cited, the two encryption crates to be confirmed unconditional by grep, and the build-breaking consequence to be stated in the document. |
| T-07-33 | Repudiation | all four annotated documents | high | mitigate | Deleting the superseded claims erases the positions the `diverged` ledger rows cite as evidence. Mitigated by per-file bounded deletion counts and by asserting the original text is still greppable after each correction. |
| T-07-34 | Spoofing (of a citation) | quoted `Cargo.toml` line numbers | medium | mitigate | Mitigated by requiring every quoted line number to match `grep -n` output as run during the task. |
| T-07-35 | Elevation of privilege (of a settled question) | Non-Goal 7 / Open Question 4 | medium | mitigate | Re-opening a question ADR-0018 settled would put two live answers in the corpus. Mitigated by the prohibition and by restricting those two annotations to confirmation notes that cite the ADR. |
| T-07-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable: this plan reads manifests and installs nothing. |

No critical threats. T-07-32 and T-07-33 are the `high` items and both are mitigated inside this plan.
</threat_model>

<artifacts>
## Artifacts this phase produces

The full phase-wide artefact table is recorded once in `07-01-PLAN.md` §Artifacts. This plan produces
these rows:

| Artefact | Status | Owning plan | Requirement |
|---|---|---|---|
| `.project/Milestone_4-.../Epic_1/prd-expand-feature-flags.md` | **ANNOTATED IN PLACE** — banner plus three inline corrections | 07-09 | ARCH-05 |
| `.project/Milestone_4-.../Milestone-Overview/Milesone-4-Tier-1-High-Value-Low-Risk.md` | **ANNOTATED IN PLACE** — MCP criterion and appendix, added to plan 07-01's ADR-0014 banner | 07-01, 07-09 | ARCH-05, ARCH-02 |
| `.project/Milestone_5-.../overview/Milestone_5-Tier_2-Workspace-Decomposition.md` | **ANNOTATED IN PLACE** — banner, title, three `paladin-cli` corrections | 07-09 | ARCH-02, ARCH-05 |
| `.project/Milestone_6-.../Epic_2/prd-relocate-orchestration-services.md` | **ANNOTATED IN PLACE** — banner, `use_cases/` target, Non-Goal 7 and Open Question 4 confirmations | 07-09 | ARCH-04, ARCH-05 |

Files cited but deliberately not edited by this plan: `dependency-matrix.md` (the audit that was
right) and `prd-workspace-finalization-epic-6.md` (whose non-goal was correct). No new headings are
created in any file.
</artifacts>

<verification>
- `grep -c '~~' .project/Milestone_4-Refactor-Crates-Features/Epic_1/prd-expand-feature-flags.md` is at least `3`
- `grep -c '~~' .project/Milestone_5-Workspace-Decomposition/overview/Milestone_5-Tier_2-Workspace-Decomposition.md` is at least `4`
- `grep -c 'ADR-0018' .project/Milestone_6-Architectural-Refinements/Epic_2/prd-relocate-orchestration-services.md` is at least `4`
- `test -d crates/paladin-cli` fails and `test -d src/application/use_cases` fails
- `grep -c 'actix' Cargo.toml` equals `0`
- `git diff --numstat HEAD~1 -- .project/Milestone_4-Refactor-Crates-Features/Epic_1/dependency-matrix.md` is empty
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
</verification>

<success_criteria>
Someone applying an old PRD literally cannot break the build: all five positions shipped code
contradicts are corrected at source with their originals retained, the `vision` clause carries its
build-breaking consequence in the document itself, and each correction cites a freshly re-grepped
manifest line. No new ADR was created for any of the five, matching the Phase 1 and Phase 5 precedent
that code-settled divergences get source corrections and ledger rows.
</success_criteria>

<output>
Create `.planning/phases/07-workspace-ground-truth-recorded-answers/07-09-SUMMARY.md` when done.
Record: every `Cargo.toml` line number cited and its re-grepped value, the `crates/` listing as read,
the grep results for `actix`, `chacha20poly1305` and `zeroize`, and the flagged ARCH-05 assumption so
the phase verifier sees it.
</output>
