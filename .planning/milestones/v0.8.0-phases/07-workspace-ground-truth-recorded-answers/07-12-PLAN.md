---
phase: 07-workspace-ground-truth-recorded-answers
plan: 12
type: execute
wave: 8
depends_on: ["07-01", "07-07", "07-09"]
files_modified:
  - .planning/ledgers/milestone-04-06.md
autonomous: true
requirements: [ARCH-01, ARCH-05, ARCH-06]
must_haves:
  truths:
    - "The 25 `REQ-*` rows under `### Milestone 4 Epic 1`, `### Milestone 4 Epic 2` and `### Milestone 4 Epic 3` carry no stub token, and after this plan the ledger contains zero stub tokens in total — all 115 rows are verdicted."
    - "Milestone 4's checkbox record — 93.2% complete, 20 open items all in Epic 2 — is treated as corroborated per D-06: Epic 2's open items are real remaining work, and each is verdicted against a fresh grep rather than against the corpus's usual assumption that checkboxes understate the tree."
    - "Milestone 4 Epic 3's checkboxes are treated as the inverse case: the CLI-isolation list is fully checked while CLI-only dependencies remain unconditional, so a checked box is not evidence and every row in that epic is verdicted against the manifest."
    - "`REQ-vision-feature-gating` is verdicted `diverged` citing the corrected Epic 1 PRD and the shipped empty `vision` feature; `REQ-feature-flag-docs` and `REQ-stable-api-doc` are verdicted `relocated` citing their mdbook chapters — none of the three is recorded as a gap."
    - "`REQ-binary-target-config` and `REQ-cli-docs` cite ADR-0019, record the three shipped binary targets with their stated purposes, and record FR9.3's user-facing documentation deliverable as owed to Phase 16 rather than as satisfied here."
    - "`REQ-cli-dependency-isolation` and `REQ-library-only-build` record ADR-0019's corrected scope: `structopt`'s only consumer is the un-gated default binary, and `colored` and `comfy-table` re-enter a library-only build through `paladin-herald`, which declares them unconditionally and has no feature section — so the recorded three-line fix is wrong for one line and cannot satisfy the zero-CLI-dependency criterion for two."
    - "`REQ-api-surface-ci` and `REQ-deprecation-warnings` are recorded `genuinely outstanding` pointing at Phase 8 / DEBT-01 and DEBT-02, with the stale baseline path cited and no attempt to fix either here."
  artifacts:
    - ".planning/ledgers/milestone-04-06.md — 25 rows filled under Milestone 4 Epics 1, 2 and 3; the ledger reaches 115 of 115 verdicted"
  key_links:
    - "REQ-binary-target-config and REQ-cli-docs → ADR-0019 → Phase 16 — the documentation deliverable recorded as owed, not written"
    - "REQ-cli-dependency-isolation → ADR-0019's structopt and paladin-herald findings → Phase 8 / DEBT-04 — the corrected scope the ledger hands forward"
    - "REQ-vision-feature-gating, REQ-feature-flag-matrix → the ARCH-05 corrections in plan 07-09 — the ledger verdict and the source annotation are the two halves of one finding"
    - "REQ-api-surface-ci → Phase 8 / DEBT-01 — the longest-lived unfixed defect in the corpus, recorded not fixed"
  prohibitions:
    - "No row may be awarded `satisfied` on the strength of a `file:line` citation alone, and REQUIREMENTS.md's ingest-era status words must NOT be carried forward as if they were pre-satisfied verdicts."
    - "A checked box in Milestone 4 Epic 3 must NOT be treated as evidence of completion — that epic is the corpus's one verified case of checkboxes overstating completion."
    - "A relocated documentation deliverable must NOT be recorded as a gap or as outstanding work."
    - "No `REQ-*` row may be dropped, reordered, inserted, or merged with another ID that cites the same artefact."
    - "This plan must NOT edit any `.rs` file, `Cargo.toml`, or any file under `.github/` — DEBT-01 through DEBT-04 are Phase 8's work."
---

<objective>
Fill the last 25 ledger rows — Milestone 4 Epics 1, 2 and 3 — and bring the ledger to 115 of 115
verdicted.

Purpose: this is the block where the corpus's usual reading fails in both directions at once. Epic 2
is the one genuinely incomplete epic in the run, with 20 corroborated open items and two DEBT defects
recorded here and fixed in Phase 8. Epic 3 is the inverse: its CLI-isolation checklist is fully
checked while three CLI-only dependencies remain unconditional, and ADR-0019 has since found that the
recorded fix for that is wrong for one of its three lines and incomplete for the other two. Epic 1
holds two of ARCH-05's five divergences and two of the four mdbook relocations.
Output: the ledger with every one of its 115 rows verdicted and cited.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-PATTERNS.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Milestone 4 Epic 1 — Feature Flag Expansion (7 rows)</name>
  <read_first>
    - `.planning/ledgers/milestone-04-06.md` — the head-note block, including the D-04(b) mdbook caveat, the `relocated` and `diverged` legend rows and their tie-break rule, and the `### Milestone 4 Epic 1` section as the scaffold left it
    - `.planning/REQUIREMENTS.md` lines 2855-2869 — the seven IDs and their ingest-era status text
    - `Cargo.toml` `[features]` block — the `vision`, `web-server`, `default`, `full` and `cli` definitions with their current line numbers, re-grepped in this task
    - `.github/workflows/feature-flags.yml` — the workspace feature matrix job and its build and test steps, located by grep
    - `.project/Milestone_4-Refactor-Crates-Features/Epic_1/prd-expand-feature-flags.md` — now annotated by plan 07-09; the three corrections are what the `diverged` rows cite
    - `docs/src/api-reference/feature-flags.md`, `docs/src/api-reference/migration-guide.md` and `docs/src/getting-started/installation.md` — the relocated documentation deliverables
  </read_first>
  <files>.planning/ledgers/milestone-04-06.md</files>
  <action>
Replace the Verdict and Evidence cells of the seven rows under `### Milestone 4 Epic 1` in place. Do
not insert, delete or reorder rows. The seven IDs, in scaffold order, are `REQ-feature-flag-matrix`,
`REQ-vision-feature-gating`, `REQ-feature-default-set`, `REQ-feature-full-flag`,
`REQ-cfg-guard-discipline`, `REQ-feature-flag-docs` and `REQ-feature-ci-matrix`.

Every row here is a structural feature-flag requirement, so D-01's manifest carve-out governs: cite
the `Cargo.toml` feature line plus the feature-matrix job or build leg in
`.github/workflows/feature-flags.yml` that consumes it, both line numbers re-grepped at the moment of
writing. A manifest citation with no consuming job is `present, unproven`.

Three rows have prescribed dispositions. `REQ-vision-feature-gating` is `diverged`, citing the shipped
empty `vision` feature line and the correction plan 07-09 wrote into the Epic 1 PRD, and stating in
the Evidence cell that applying the PRD literally would break a no-default-features build for user
auth and Citadel encryption. `REQ-feature-flag-matrix` is recorded partly `diverged` on two counts —
the `web-server` definition gates the web crate and axum rather than actix-web, and no MCP feature
flag of any kind exists — both citing plan 07-09's corrections. `REQ-feature-flag-docs` is
`relocated`: the named root documents do not exist, equivalent pages ship as mdbook chapters, and the
Evidence cell must cite those chapter paths and confirm they exist, stating that this is the D-04(b)
caveat and not a missing deliverable.

`REQ-cfg-guard-discipline` cites the provider factory at its shipped path in `crates/paladin-llm/`,
noting that the PRD's monolith path is historical per the D-04(a) caveat, and records
`present, unproven` if guard placement and the no-dead-code rule are not individually confirmed.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Milestone 4 Epic 1 /,/^### Milestone 4 Epic 2 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-')" = "7" ] &amp;&amp; [ "$(sed -n '/^### Milestone 4 Epic 1 /,/^### Milestone 4 Epic 2 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT')" = "0" ] &amp;&amp; test -f docs/src/api-reference/feature-flags.md</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Milestone 4 Epic 1 /,/^### Milestone 4 Epic 2 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-'` equals `7`
    - `sed -n '/^### Milestone 4 Epic 1 /,/^### Milestone 4 Epic 2 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `sed -n '/^### Milestone 4 Epic 1 /,/^### Milestone 4 Epic 2 /p' .planning/ledgers/milestone-04-06.md | grep -ci 'diverged'` is at least `2`
    - `sed -n '/^### Milestone 4 Epic 1 /,/^### Milestone 4 Epic 2 /p' .planning/ledgers/milestone-04-06.md | grep -ci 'relocated'` is at least `1`
    - `test -f docs/src/api-reference/feature-flags.md` succeeds — the relocated deliverable the row cites exists
    - Every row in the section has an Evidence cell containing a `:` followed by a digit
    - The `Cargo.toml` line numbers cited match `grep -n` output as run during this task
  </acceptance_criteria>
  <done>All seven Milestone 4 Epic 1 rows carry a legend verdict and a resolved citation, two carry `diverged` verdicts citing plan 07-09's source corrections, and the documentation row is `relocated` with its mdbook chapters confirmed to exist.</done>
</task>

<task type="auto">
  <name>Task 2: Milestone 4 Epic 2 — Port Trait Hardening and Stable API (9 rows)</name>
  <read_first>
    - `.planning/ledgers/milestone-04-06.md` — the `### Milestone 4 Epic 2` section, the head-note evidence bar, and the D-06 note recording Milestone 4's 20 open items as corroborated and all in this epic
    - `.planning/REQUIREMENTS.md` lines 2870-2887 — the nine IDs and their ingest-era status text
    - `.planning/intel/task-completion-state.md` — Milestone 4's completion figure and the location of its 20 open items
    - `src/lib.rs` and `src/prelude.rs` — the curated export surface several rows cite
    - `.github/workflows/ci.yml` — the `api-surface` job and the `doc` step, both located by grep; note that the `api-surface` job references a baseline path that the `project/` to `.project/` rename made stale
    - `scripts/check-api-surface.sh`, `scripts/extract-public-api.sh` and `scripts/check-deprecations.sh` — the tooling and its stale defaults
    - `crates/paladin-ports/Cargo.toml` — the disabled doctest line, cited by the rustdoc row
    - `.project/Milestone_4-Refactor-Crates-Features/Epic_2/DEPRECATIONS.md` and the epic's `api-audit.md` — the self-reported deprecation count and the competing reduction target
  </read_first>
  <files>.planning/ledgers/milestone-04-06.md</files>
  <action>
Replace the Verdict and Evidence cells of the nine rows under `### Milestone 4 Epic 2` in place. The
nine IDs, in scaffold order, are `REQ-curated-lib-exports`, `REQ-visibility-hardening`,
`REQ-port-trait-rustdoc`, `REQ-stable-api-doc`, `REQ-import-path-updates-m4`, `REQ-doc-build-clean`,
`REQ-api-surface-ci`, `REQ-deprecation-warnings` and `REQ-api-surface-reduction-target`.

This is the one genuinely incomplete epic in the run. Apply D-06 deliberately: Milestone 4's 20 open
checkbox items are all in this epic and are corroborated, so do not apply the corpus's usual
assumption that open checkboxes understate the tree. Verdict each row against a fresh grep.

Four rows have prescribed dispositions. `REQ-api-surface-ci` is `genuinely outstanding` pointing at
Phase 8 / DEBT-01: cite the workflow lines and the two script defaults that reference the stale
baseline path, each re-grepped, and record that the job fails on every run so
`check-deprecations.sh` never executes and the named baseline file was never created. Do not fix any
of it. `REQ-deprecation-warnings` is `genuinely outstanding` pointing at Phase 8 / DEBT-02: record the
result of grepping the deprecation attribute across the source and crate trees during this task, and
cite the epic's own self-reported count. `REQ-port-trait-rustdoc` is partial pointing at Phase 8 /
DEBT-03: rustdoc exists but its examples cannot execute, citing the disabled-doctest manifest line and
the workflow exclusion, both re-grepped. `REQ-stable-api-doc` is `relocated`: the deliverable ships as
an mdbook chapter, confirmed to exist during this task, and the Evidence cell records that six run-3
documents cross-reference the old root path and are covered by the ledger's shared D-04(b) head note
rather than by six separate banners.

`REQ-api-surface-reduction-target` has two competing targets — the requirement's own exported-type
ceiling and the epic's `api-audit.md` figure — and no recorded measurement. Record `present,
unproven`, cite both targets, and state that no measurement exists rather than producing one.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Milestone 4 Epic 2 /,/^### Milestone 4 Epic 3 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-')" = "9" ] &amp;&amp; [ "$(sed -n '/^### Milestone 4 Epic 2 /,/^### Milestone 4 Epic 3 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT')" = "0" ] &amp;&amp; test -f docs/src/api-reference/stable-api.md</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Milestone 4 Epic 2 /,/^### Milestone 4 Epic 3 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-'` equals `9`
    - `sed -n '/^### Milestone 4 Epic 2 /,/^### Milestone 4 Epic 3 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `sed -n '/^### Milestone 4 Epic 2 /,/^### Milestone 4 Epic 3 /p' .planning/ledgers/milestone-04-06.md | grep -c 'DEBT-01'` is at least `1`, `grep -c 'DEBT-02'` at least `1`, `grep -c 'DEBT-03'` at least `1` on the same range
    - `sed -n '/^### Milestone 4 Epic 2 /,/^### Milestone 4 Epic 3 /p' .planning/ledgers/milestone-04-06.md | grep -ci 'genuinely outstanding'` is at least `2`
    - `test -f docs/src/api-reference/stable-api.md` succeeds
    - Every row in the section has an Evidence cell containing a `:` followed by a digit
    - The deprecation-attribute grep result recorded in `REQ-deprecation-warnings` matches a live run of that grep during this task
  </acceptance_criteria>
  <done>All nine Milestone 4 Epic 2 rows carry a legend verdict and a resolved citation, the three DEBT defects are recorded as outstanding with their owning phase named and nothing fixed here, and the stable-API row is `relocated` with its mdbook chapter confirmed.</done>
</task>

<task type="auto">
  <name>Task 3: Milestone 4 Epic 3 — CLI Isolation (9 rows), closing the ledger at 115 of 115</name>
  <read_first>
    - `.planning/ledgers/milestone-04-06.md` — the `### Milestone 4 Epic 3` section and the head-note evidence bar
    - `.planning/REQUIREMENTS.md` lines 2888-2904 — the nine IDs and their ingest-era status text
    - `.planning/decisions/0019-binary-target-architecture.md` — the ADR four of these rows cite, including its `structopt` and `paladin-herald` findings and its `must change` verdict
    - `.planning/decisions/0021-cli-application-layer-placement.md` — the sibling answer on the same surface
    - `Cargo.toml` — the `cli` feature definition, the three `[[bin]]` stanzas, and the `structopt`, `colored` and `comfy-table` dependency lines, all re-grepped in this task
    - `crates/paladin-herald/Cargo.toml` — confirm `colored` and `comfy-table` are unconditional and that the crate declares no features
    - `.github/workflows/feature-flags.yml` — the `cli-isolation` job and its regression-test step, located by grep
    - `tests/cli_isolation_test.rs` — the test target the isolation rows cite
  </read_first>
  <files>.planning/ledgers/milestone-04-06.md</files>
  <action>
Replace the Verdict and Evidence cells of the nine rows under `### Milestone 4 Epic 3` in place. The
nine IDs, in scaffold order, are `REQ-cli-feature-gate`, `REQ-cli-dependency-isolation`,
`REQ-binary-target-config`, `REQ-cli-test-isolation`, `REQ-library-only-build`,
`REQ-library-only-integration-tests`, `REQ-cli-build-time-measurement`, `REQ-cli-ci-matrix` and
`REQ-cli-docs`.

Treat this epic as the inverse checkbox case: its isolation checklist is fully checked while CLI-only
dependencies remain unconditional, so a checked box is not evidence. Verdict every row against the
manifest and the workflows, both re-grepped.

Four rows have prescribed dispositions. `REQ-binary-target-config` cites ADR-0019, records the three
shipped binary targets with their stated purposes including the `paladin` binary's honest legacy
purpose, and records that Q1's architecture review never happened and is now closed by the ADR.
`REQ-cli-docs` cites ADR-0019 and records FR9.3's user-facing binary-architecture documentation as
still owed, owned by Phase 16 — the ADR and this ledger row satisfy the record, not the user-facing
page. `REQ-cli-dependency-isolation` and `REQ-library-only-build` both record ADR-0019's corrected
scope: `structopt`'s only consumer is the un-gated default binary so it cannot be made optional
without first deciding that binary's fate, and `colored` and `comfy-table` re-enter a library-only
build through `paladin-herald` which declares them unconditionally with no feature section — so the
zero-CLI-dependency criterion cannot be met by gating the root manifest alone. Point both rows at
Phase 8 / DEBT-04 with the corrected scope stated, and use the verdict class the evidence supports
rather than defaulting to `genuinely outstanding` where the tree has moved past the requirement.

`REQ-cli-build-time-measurement` has no located measurement, and any figure taken today would be
measured against a partly isolated tree. Record `present, unproven` and say so; do not produce a
measurement.

When the section is complete, confirm the whole ledger carries zero stub tokens and still has 115
rows across 13 sections. Commit the ledger once at the end of the plan, with a Bash timeout of at
least 300000 ms.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Milestone 4 Epic 3 /,/^### Milestone 5 Epic 1 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-')" = "9" ] &amp;&amp; [ "$(grep -c 'PENDING-VERDICT' .planning/ledgers/milestone-04-06.md)" = "0" ] &amp;&amp; [ "$(grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md)" = "115" ] &amp;&amp; [ "$(grep -c '^### Milestone ' .planning/ledgers/milestone-04-06.md)" = "13" ]</automated>
    <human-check>Spot-check three rows across the three sections this plan filled: open the cited `file:line` and confirm it states what the row says it states (07-VALIDATION.md §Manual-Only Verifications, row 1).</human-check>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Milestone 4 Epic 3 /,/^### Milestone 5 Epic 1 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-'` equals `9`
    - `grep -c 'PENDING-VERDICT' .planning/ledgers/milestone-04-06.md` equals `0` — every one of the 115 rows is verdicted <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md` equals `115` and `grep -c '^### Milestone ' .planning/ledgers/milestone-04-06.md` equals `13`
    - `grep -o '^| REQ-[a-z0-9-]*' .planning/ledgers/milestone-04-06.md | sort | uniq -d` prints nothing
    - `sed -n '/^### Milestone 4 Epic 3 /,/^### Milestone 5 Epic 1 /p' .planning/ledgers/milestone-04-06.md | grep -c 'ADR-0019'` is at least `3`
    - `sed -n '/^### Milestone 4 Epic 3 /,/^### Milestone 5 Epic 1 /p' .planning/ledgers/milestone-04-06.md | grep -c 'paladin-herald'` is at least `1` — the second half of the corrected CLI-isolation scope is recorded
    - `sed -n '/^### Milestone 4 Epic 3 /,/^### Milestone 5 Epic 1 /p' .planning/ledgers/milestone-04-06.md | grep -c 'Phase 16'` is at least `1`
    - Every row in the section has an Evidence cell containing a `:` followed by a digit
    - `git log -1 --name-only` shows only `.planning/ledgers/milestone-04-06.md`
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
  </acceptance_criteria>
  <done>All nine Milestone 4 Epic 3 rows carry a legend verdict and a resolved citation, four cite ADR-0019 with the corrected CLI-isolation scope and the Phase 16 documentation debt recorded, and the ledger now holds 115 verdicted rows with no stub tokens remaining.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ledger verdict → Phase 8's DEBT plan | Four of Phase 8's five requirements take their scope from rows in this block. An imprecise DEBT-04 row would send Phase 8 to gate a dependency the default build needs. |
| Milestone 4 checkbox state → this plan's verdicts | Epic 2's open boxes are corroborated and Epic 3's checked boxes overstate completion — the two epics fail the corpus heuristic in opposite directions. |
| A relocated deliverable → the ledger's verdict class | Recording an mdbook chapter as a gap sends Phase 16 to write a document that exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-44 | Tampering (of the record) | `REQ-cli-dependency-isolation` and `REQ-library-only-build` | high | mitigate | Recording the uncorrected three-line fix would send Phase 8 to mark `structopt` optional and break the default build. Mitigated by requiring both ADR-0019 findings — the un-gated `structopt` consumer and the `paladin-herald` re-entry — in both rows, each cited to a re-grepped manifest line. |
| T-07-45 | Tampering (of the record) | Milestone 4 Epic 3's checked boxes | high | mitigate | Treating a checked box as evidence in the one epic verified to overstate completion would mark real gaps satisfied. Mitigated by the prohibition and by requiring every row in the epic to cite the manifest or a workflow. |
| T-07-46 | Tampering (of the record) | `REQ-feature-flag-docs` and `REQ-stable-api-doc` verdict class | high | mitigate | Mitigated by requiring `relocated` verdicts and passing existence checks on the mdbook chapters. |
| T-07-47 | Spoofing (of a citation) | stale `file:line` values | high | mitigate | Mitigated by requiring every cited `Cargo.toml` and workflow line to match a live grep run during the task. |
| T-07-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable: no package installed, no manifest modified. |

No critical threats. All four `high` items are mitigated by acceptance criteria in this plan.
</threat_model>

<artifacts>
## Artifacts this phase produces

The full phase-wide artefact table is recorded once in `07-01-PLAN.md` §Artifacts. This plan produces
one of its rows and closes it:

| Artefact | Status | Owning plan | Requirement |
|---|---|---|---|
| `.planning/ledgers/milestone-04-06.md` §`### Milestone 4 Epic 1`, §`### Milestone 4 Epic 2`, §`### Milestone 4 Epic 3` | **FILLED** — 25 rows verdicted and cited; the ledger reaches 115 of 115 | 07-12 | ARCH-01, ARCH-05, ARCH-06 |

No new headings are created by this plan. The `## Summary` section is written by plan 07-13.
</artifacts>

<verification>
- `grep -c 'PENDING-VERDICT' .planning/ledgers/milestone-04-06.md` equals `0`
- `grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md` equals `115`
- `grep -c '^### Milestone ' .planning/ledgers/milestone-04-06.md` equals `13`
- `grep -o '^| REQ-[a-z0-9-]*' .planning/ledgers/milestone-04-06.md | sort | uniq -d` prints nothing
- `test -f docs/src/api-reference/feature-flags.md` and `test -f docs/src/api-reference/stable-api.md` both succeed
- `git status --porcelain` is clean after the plan's single commit
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
</verification>

<success_criteria>
A developer can look up any of the 115 Milestone 4-6 requirement IDs and see a `file:line`-cited
verdict — the ledger carries no stub tokens. Milestone 4's genuinely incomplete epic hands its three
DEBT defects to Phase 8 with their evidence, the CLI-isolation epic hands Phase 8 the corrected scope
ADR-0019 established rather than the wrong three-line fix, the binary-architecture documentation debt
is owned by Phase 16, and the two relocated documentation deliverables are recorded as living in the
mdbook rather than missing.
</success_criteria>

<output>
Create `.planning/phases/07-workspace-ground-truth-recorded-answers/07-12-SUMMARY.md` when done.
Record: the verdict distribution across the 25 rows, the live grep results for the deprecation
attribute and for `structopt`, `colored` and `comfy-table`, every `Cargo.toml` and workflow line
number cited, and confirmation that the ledger reached 115 of 115 verdicted.
</output>
