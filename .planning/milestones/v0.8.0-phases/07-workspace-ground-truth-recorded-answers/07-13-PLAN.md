---
phase: 07-workspace-ground-truth-recorded-answers
plan: 13
type: execute
wave: 9
depends_on: ["07-02", "07-03", "07-04", "07-05", "07-06", "07-07", "07-08", "07-09", "07-10", "07-11", "07-12"]
files_modified:
  - .planning/ledgers/milestone-04-06.md
  - .planning/REQUIREMENTS.md
  - .planning/decisions/PROMOTION.md
  - .planning/PROJECT.md
  - .planning/phases/07-workspace-ground-truth-recorded-answers/COVERAGE.md
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03, ARCH-04, ARCH-05, ARCH-06, ARCH-07]
must_haves:
  truths:
    - "The ledger's `## Summary` section reports a verdict distribution produced by counting the rows in the finished file, not by carrying forward any earlier draft's figures, and the section says so in its own text."
    - "The summary's per-verdict counts sum to exactly 115, and the ledger still contains 115 `REQ-*` rows across 13 epic sections with no duplicate IDs and no stub tokens."
    - "All five forward-scope handoffs this phase produces are recorded in the ledger's summary with their owning requirement named: Phase 8 / DEBT-05 receives the canonical `TokenUsage` target from ADR-0016; Phase 8's CLI-isolation requirement receives ADR-0019's `structopt` precondition and `paladin-herald` hole; Phase 11 / FACADE-02 D1 receives ADR-0018's re-export policy; Phase 15 receives ADR-0015's allowlist-enforcement candidate; Phase 16 receives ADR-0019's binary-architecture mdbook page. Each is recorded here and executed nowhere in this phase."
    - "`.planning/decisions/PROMOTION.md` carries one index row per new ADR (0014 through 0021) under D-00a's flat sequential numbering, its next-free line reads 0022 rather than 0021 because D-25a allocated an eighth number, and its Part B inventory records both Phase-7-owned candidates as closed with the ADR that closed each."
    - "`.planning/REQUIREMENTS.md`'s Milestone 4-6 pointer states the finished nested outstanding-item count and no longer carries the placeholder sentence plan 07-01 wrote."
    - "`.planning/phases/07-workspace-ground-truth-recorded-answers/COVERAGE.md` exists and carries a reasoned no-external-API declaration with its reason line, matching the shape Phases 5 and 6 used — not a fabricated capability matrix for an API this phase does not touch."
    - "Across the whole phase, `git diff --stat` restricted to Rust sources, `Cargo.toml` and `.github/` is empty — the records-phase boundary held."
  artifacts:
    - ".planning/ledgers/milestone-04-06.md — `## Summary` section with the counted verdict distribution and the five forward-scope handoffs"
    - ".planning/REQUIREMENTS.md — Milestone 4-6 pointer finalised with the real nested-item count"
    - ".planning/decisions/PROMOTION.md — eight index rows, next-free line advanced to 0022, both Part B candidates closed"
    - ".planning/PROJECT.md — Key Decisions rows for the eight new ADRs"
    - ".planning/phases/07-workspace-ground-truth-recorded-answers/COVERAGE.md — reasoned no-external-API declaration"
  key_links:
    - "Ledger summary handoffs → Phases 8, 11, 15 and 16 — the five code consequences this phase records and does not execute"
    - "PROMOTION.md next-free line → 0022 — the consequence of D-25a's eighth ADR, which a later phase's numbering depends on"
    - "REQUIREMENTS.md pointer → the finished ledger — the redirect a reader follows, now carrying real counts"
    - "COVERAGE.md → the verify-time API-coverage gate — the declaration that stops a false positive on this phase's incidental API vocabulary"
  prohibitions:
    - "The verdict distribution must NOT be carried forward from any earlier plan's summary or from REQUIREMENTS.md's ingest-era text — it is recomputed by counting the finished rows."
    - "This plan must NOT execute any of the five forward-scope handoffs — each is recorded with its owning phase and requirement and left there."
    - "`COVERAGE.md` must NOT contain a fabricated capability matrix for an API this phase does not integrate; the declaration plus its reason line is the correct artefact."
    - "This plan must NOT edit any `.rs` file, `Cargo.toml`, or any file under `.github/`."
    - "`PROMOTION.md` and `PROJECT.md` must be edited with scoped replacements, never rewritten wholesale."
---

<objective>
Close the phase: count what the ledger actually says, advance the ADR bookkeeping, hand the five code
consequences to their owning phases by name, and record the coverage declaration.

Purpose: a ledger whose summary is assumed rather than counted is the same class of defect as a
verdict without a citation — Phase 6 already had to recompute a distribution that had been carried
forward. And five findings in this phase have real code consequences that belong to Phases 8, 11, 15
and 16; recording each with its owner is what keeps this phase record-only without losing the work.
Output: the ledger's summary, the finalised REQUIREMENTS.md pointer, `PROMOTION.md` advanced to 0022,
PROJECT.md Key Decisions rows, and `COVERAGE.md`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-PATTERNS.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ledger summary — counted distribution and the five forward-scope handoffs</name>
  <read_first>
    - `.planning/ledgers/milestone-04-06.md` — the finished file in full; the summary is written from what it says, not from what earlier plans reported
    - `.planning/ledgers/milestone-02-03.md` lines 53-130 — the `## Summary` section shape to copy, including its `### Verdict distribution`, `### Nested outstanding items` and roll-up sub-sections and its self-discipline sentence that every count is produced by counting the rows
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md` §Decisions D-10, D-11, D-16, D-20, D-21 — the five handoffs and their owning phases
    - `.planning/REQUIREMENTS.md` lines 2830-2845 — the pointer paragraph plan 07-01 wrote, including its placeholder sentence
  </read_first>
  <files>.planning/ledgers/milestone-04-06.md, .planning/REQUIREMENTS.md</files>
  <action>
(A) Append a `## Summary` section to `.planning/ledgers/milestone-04-06.md`, modelled on
`milestone-02-03.md`'s own summary. It must contain, in this order:

A one-sentence self-discipline statement copied in substance from the sibling ledger: every count in
this section was produced by counting the rows in this file, and none is re-judged. Then a
`### Verdict distribution` sub-section with a table of the seven verdict classes and the number of
rows carrying each, produced by grepping the finished file rather than by summing what earlier plans
reported. State the total and confirm it is 115. Then a `### Per-milestone roll-up` sub-section giving
the distribution split by Milestone 4, 5 and 6, with a sentence recording how each milestone's
checkbox claim held up: Milestone 4's 20 open items corroborated and concentrated in Epic 2,
Milestone 5's 17 mostly contradicted by shipped artefacts, Milestone 6's zero corroborated. Then a
`### Nested outstanding items` sub-section listing any nested items the fan-out plans opened, or
recording zero if none were.

Then a `### Forward scope` sub-section recording all five code consequences this phase found, each
with its owning phase and requirement and each explicitly marked as recorded-here-executed-elsewhere:
Phase 8 / DEBT-05 receives the canonical `TokenUsage` target named by ADR-0016; Phase 8's
CLI-isolation requirement receives ADR-0019's finding that `structopt`'s only consumer is the
un-gated default binary and that `paladin-herald` re-introduces two of the three CLI crates, so the
recorded three-line fix has a precondition and a hole; Phase 11 / FACADE-02 D1 receives ADR-0018's
re-export policy; Phase 15 receives ADR-0015's `cargo tree`-based allowlist-enforcement candidate;
Phase 16 receives ADR-0019's user-facing binary-architecture mdbook page. Add a closing line
recording that ADR-0020 declined the benchmark re-measurement with a reason, so no phase inherits it.

Finally a `### Citation resolution` sub-section recording how many citations were re-grepped rather
than transcribed and naming any that had drifted from the value an earlier document recorded — the
`crate-isolation` job's drift is the known case and must appear here if it was confirmed.

(B) In `.planning/REQUIREMENTS.md`, replace the placeholder sentence plan 07-01 wrote in the
Milestone 4-6 pointer with the real nested outstanding-item count, taken from the summary section
just written. Use a scoped Edit; do not rewrite the file.
  </action>
  <verify>
    <automated>[ "$(grep -c '^## Summary' .planning/ledgers/milestone-04-06.md)" = "1" ] &amp;&amp; [ "$(grep -c '^### Verdict distribution' .planning/ledgers/milestone-04-06.md)" = "1" ] &amp;&amp; [ "$(grep -c '^### Forward scope' .planning/ledgers/milestone-04-06.md)" = "1" ] &amp;&amp; [ "$(grep -c 'finalised by plan 07-13' .planning/REQUIREMENTS.md)" = "0" ] &amp;&amp; [ "$(grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md)" = "115" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Summary' .planning/ledgers/milestone-04-06.md` equals `1`
    - `grep -c '^### Verdict distribution' .planning/ledgers/milestone-04-06.md` equals `1` and `grep -c '^### Forward scope' .planning/ledgers/milestone-04-06.md` equals `1`
    - The per-verdict counts in the distribution table sum to `115`, and each count matches a `grep -c` over the finished file for that verdict word
    - `grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md` equals `115` and `grep -c 'PENDING-VERDICT' .planning/ledgers/milestone-04-06.md` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `sed -n '/^### Forward scope/,/^### Citation resolution/p' .planning/ledgers/milestone-04-06.md` names `DEBT-05`, `FACADE-02`, `Phase 15` and `Phase 16`
    - `sed -n '/^### Forward scope/,/^### Citation resolution/p' .planning/ledgers/milestone-04-06.md | grep -c 'ADR-00'` is at least `4` — each handoff cites the ADR that produced it
    - `grep -c 'finalised by plan 07-13' .planning/REQUIREMENTS.md` equals `0` — the placeholder is gone
    - `sed -n '/^## Milestone 4-6 as-shipped ledger/,/^## Milestone 7-8 as-shipped ledger/p' .planning/REQUIREMENTS.md | grep -c '^| REQ-'` still equals `0`
    - `grep -c '^## Milestone 2-3 as-shipped ledger' .planning/REQUIREMENTS.md` equals `1` and `grep -c '^## Milestone 7-8 as-shipped ledger' .planning/REQUIREMENTS.md` equals `1` — neighbouring sections intact
  </acceptance_criteria>
  <done>The ledger carries a `## Summary` whose counts were produced by counting, a per-milestone roll-up recording how each checkbox claim held up, and a `### Forward scope` sub-section naming all five handoffs with their owning phase and the ADR that produced each; REQUIREMENTS.md's pointer states the real nested-item count.</done>
</task>

<task type="auto">
  <name>Task 2: ADR bookkeeping — PROMOTION.md index, next-free number, and PROJECT.md Key Decisions</name>
  <read_first>
    - `.planning/decisions/PROMOTION.md` — full read: the index table's existing rows and their exact phrasing style, the `Next free ADR number` line, the five-step promotion procedure, and Part B's candidate inventory with its two Phase-7-owned entries
    - `.planning/PROJECT.md` §Key Decisions — the table's shape and its existing evidence note, so the new rows match rather than displace it
    - The eight new ADR files under `.planning/decisions/` — read each one's Status, Decision and Code Conformance so the index rows and Key Decisions rows describe them accurately
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md` §Decisions D-25, D-25a
  </read_first>
  <files>.planning/decisions/PROMOTION.md, .planning/PROJECT.md</files>
  <action>
(A) In `.planning/decisions/PROMOTION.md`, append eight rows to the index table — one each for 0014
through 0021 — matching the exact phrasing style of the existing 0010 through 0013 rows: the
zero-padded number, the kebab-case slug, and a short noun-phrase subject with the owning phase in
parentheses. Then change the `Next free ADR number` line from 0014 to 0022. It is 0022 and not 0021
because D-25a allocated an eighth number for the CLI-placement promotion; state that reason in a
short dated note beside the line so a later reader does not treat the jump as an error.

Then update Part B's candidate inventory: mark candidate 1 closed by ADR-0016 and candidate 2 closed
by ADR-0021, each with the date, and leave every other candidate's owner column untouched. Use scoped
Edits throughout; do not rewrite the file.

(B) In `.planning/PROJECT.md`, add one Key Decisions row per new ADR, matching the table's existing
shape. Each row names the ADR by number, states its decision in one clause, and records its
conformance verdict. Do not remove or rewrite the section's existing evidence note about the corpus
carrying zero locked decisions — that note describes the ingested corpus, and these ADRs are
`.planning/` artefacts rather than ingested documents, so both statements stand together. Use a
scoped Edit.
  </action>
  <verify>
    <automated>[ "$(grep -c 'Next free ADR number: 0022' .planning/decisions/PROMOTION.md)" = "1" ] &amp;&amp; [ "$(grep -c 'Next free ADR number: 0014' .planning/decisions/PROMOTION.md)" = "0" ] &amp;&amp; for n in 0014 0015 0016 0017 0018 0019 0020 0021; do grep -q "| $n |" .planning/decisions/PROMOTION.md || exit 1; done; true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Next free ADR number: 0022' .planning/decisions/PROMOTION.md` equals `1`
    - `grep -c 'Next free ADR number: 0014' .planning/decisions/PROMOTION.md` equals `0`
    - Each of `0014`, `0015`, `0016`, `0017`, `0018`, `0019`, `0020`, `0021` appears as an index-table row in `.planning/decisions/PROMOTION.md`
    - `ls .planning/decisions/ | grep -cE '^00(1[4-9]|2[01])-'` equals `8` — eight new ADR files exist matching the eight index rows
    - `grep -L 'conforms\|must change' .planning/decisions/00{14,15,16,17,18,19,20,21}*.md` prints nothing — every new ADR carries a conformance verdict
    - For each new ADR file, `grep -c '^## '` equals `7`
    - `grep -c '0021' .planning/decisions/PROMOTION.md` is at least `2` — the index row and the candidate-2 closure
    - `grep -c 'ADR-0016' .planning/PROJECT.md` is at least `1` and `grep -c 'ADR-0019' .planning/PROJECT.md` is at least `1`
    - `git diff --numstat HEAD -- .planning/PROJECT.md | awk '{print $2}'` is no greater than `2` — the Key Decisions edit is additive and the existing evidence note survives
  </acceptance_criteria>
  <done>`PROMOTION.md` carries eight new index rows, a next-free line reading 0022 with the reason for the jump recorded, and both Phase-7-owned candidates marked closed; PROJECT.md carries a Key Decisions row per new ADR with its existing evidence note intact.</done>
</task>

<task type="auto">
  <name>Task 3: COVERAGE.md declaration and the phase boundary check</name>
  <read_first>
    - `.planning/phases/05-milestone-2-3-ground-truth/COVERAGE.md` and `.planning/phases/06-verified-gap-closure/COVERAGE.md` — the two prior declarations whose shape this one matches, including their reason lines and their explanation of why the detector fired
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-RESEARCH.md` §"Package Legitimacy Audit" and §"Security Domain" — the recorded not-applicable dispositions this declaration is consistent with
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-VALIDATION.md` §"Phase-Gate Checks" — the ten checks this task runs as the phase boundary assertion
  </read_first>
  <files>.planning/phases/07-workspace-ground-truth-recorded-answers/COVERAGE.md</files>
  <action>
(A) Create `.planning/phases/07-workspace-ground-truth-recorded-answers/COVERAGE.md`. Open with an H1
naming the phase, then the required declaration line stating that this phase has no external API
integration because it writes planning records and annotates historical documents, touching no
service, SDK, or transport. Follow it with a short "Why the detector may fire" paragraph naming the
incidental vocabulary in this phase's scope that a keyword detector matches — the ledger discusses an
`api-surface` CI job and a `paladin-web` crate, and several requirement texts use the words API and
integration — and stating that every one of those refers to this project's own shipped Rust surfaces
or to a CI job name, which this phase records rather than calls. Close by noting that this phase
modifies zero Rust source files, and that every code consequence is routed to Phases 8, 11, 15 and 16
by name in the ledger's Forward scope section. Match the length and tone of the Phase 5 and Phase 6
declarations; do not produce a capability matrix.

(B) Run the phase-boundary check across the whole phase and record the result in this plan's SUMMARY:
a diff restricted to Rust sources, `Cargo.toml` and `.github/` across every commit this phase
produced must be empty. Determine the phase's first commit from the git log rather than assuming a
count. If the diff is non-empty, stop and report it as a boundary violation rather than reverting
silently.

(C) Run the remaining phase-gate checks from `07-VALIDATION.md` and record each result in the
SUMMARY: ledger row count, duplicate IDs, stub tokens, per-ADR conformance verdicts, per-ADR heading
counts, the `PROMOTION.md` next-free line, the REQUIREMENTS.md pointer's zero inline rows, additive
`.project/` diffs, and the `STRUCTURE.md` crate list.

Commit this plan's five files in a single commit at the end of the plan, with a Bash timeout of at
least 300000 ms.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-workspace-ground-truth-recorded-answers/COVERAGE.md &amp;&amp; grep -qi 'No external API integration' .planning/phases/07-workspace-ground-truth-recorded-answers/COVERAGE.md &amp;&amp; [ "$(grep -c 'PENDING-VERDICT' .planning/ledgers/milestone-04-06.md)" = "0" ] &amp;&amp; [ "$(grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md)" = "115" ]</automated>
    <human-check>Read the ledger's Forward scope section and confirm each of the five handoffs names a phase, a requirement and the ADR that produced it, and that none of them has been executed inside this phase.</human-check>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/07-workspace-ground-truth-recorded-answers/COVERAGE.md` succeeds
    - `grep -ci 'No external API integration' .planning/phases/07-workspace-ground-truth-recorded-answers/COVERAGE.md` is at least `1` — the declaration line is present
    - `wc -l < .planning/phases/07-workspace-ground-truth-recorded-answers/COVERAGE.md` is at least `6` — a reason paragraph accompanies the declaration, not a bare line
    - `grep -c '|' .planning/phases/07-workspace-ground-truth-recorded-answers/COVERAGE.md` equals `0` — no fabricated capability matrix
    - A diff of the whole phase's commit range restricted to `*.rs`, `Cargo.toml` and `.github/` is empty
    - `grep -c 'PENDING-VERDICT' .planning/ledgers/milestone-04-06.md` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md` equals `115` and `grep -o '^| REQ-[a-z0-9-]*' .planning/ledgers/milestone-04-06.md | sort | uniq -d` prints nothing
    - `sed -n '/^## Directory Purposes/,/^## /p' .planning/codebase/STRUCTURE.md` contains all ten library crate names plus `doc-examples`
    - Every one of the ten phase-gate checks in `07-VALIDATION.md` is recorded in the SUMMARY with its result
    - `git log -1 --name-only` shows all five of this plan's files in a single commit
  </acceptance_criteria>
  <done>`COVERAGE.md` exists with a reasoned declaration and no fabricated matrix, the phase-boundary diff over Rust sources, `Cargo.toml` and `.github/` is empty across the whole phase, and every phase-gate check from `07-VALIDATION.md` is recorded with its result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ledger summary → every later reader | The summary is the figure people quote. A carried-forward distribution is a claim about 115 rows that nobody counted, and Phase 6 already had to recompute one. |
| Forward-scope handoffs → Phases 8, 11, 15 and 16 | These five entries are how a code consequence found in a records phase reaches the phase that executes it. A handoff omitted here is work that silently disappears. |
| `PROMOTION.md` next-free line → every future ADR author | A wrong next-free number causes a collision or a gap in a flat monotonic numbering scheme that has no other index. |
| Phase boundary → the shipped tree | The phase's own hard rule is that it changes no product code. The boundary is only real if it is checked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-48 | Tampering (of the record) | the ledger's verdict distribution | high | mitigate | A carried-forward distribution would misreport what 115 rows say. Mitigated by requiring each count to match a `grep -c` over the finished file and the counts to sum to 115. |
| T-07-49 | Repudiation | the five forward-scope handoffs | high | mitigate | A handoff not written down is a finding that dies with this phase, and one of them is the target Phase 8 / DEBT-05 is blocked on. Mitigated by asserting the Forward scope section names DEBT-05, FACADE-02, Phase 15 and Phase 16 and cites at least four ADRs. |
| T-07-50 | Tampering (of the record) | `PROMOTION.md` next-free line | medium | mitigate | A next-free line left at 0021 would collide with the CLI-placement ADR. Mitigated by asserting the line reads 0022, that the old value is absent, and that eight ADR files exist matching eight index rows. |
| T-07-51 | Spoofing (of scope) | the phase-boundary diff | high | mitigate | A phase that quietly touched product code while claiming to be records-only would break its own precedence contract. Mitigated by running the restricted diff over the whole phase range and requiring it to be empty, with a halt-and-report rule rather than a silent revert. |
| T-07-52 | Information disclosure | `COVERAGE.md` | low | accept | The declaration states what this phase does not integrate. Nothing sensitive crosses it. Accepted with no mitigation, recorded rather than padded. |
| T-07-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable: no package installed, no manifest modified anywhere in this phase. |

No critical threats. T-07-48, T-07-49 and T-07-51 are the `high` items and each is mitigated by an
acceptance criterion in this plan.
</threat_model>

<artifacts>
## Artifacts this phase produces

The full phase-wide artefact table is recorded once in `07-01-PLAN.md` §Artifacts. This plan produces
or closes these rows:

| Artefact | Status | Owning plan | Requirement |
|---|---|---|---|
| `.planning/ledgers/milestone-04-06.md` §`## Summary` | **NEW SECTION** — counted verdict distribution, per-milestone roll-up, forward-scope handoffs, citation-resolution note | 07-13 | ARCH-01 |
| `.planning/REQUIREMENTS.md` §Milestone 4-6 pointer | **FINALISED** — real nested-item count replaces the placeholder | 07-01 opens, 07-13 closes | ARCH-01 |
| `.planning/decisions/PROMOTION.md` | **AMENDED** — eight index rows, next-free 0014 → 0022, both Part B candidates closed | 07-13 | ARCH-02…ARCH-07 |
| `.planning/PROJECT.md` §Key Decisions | **AMENDED** — one row per new ADR, existing evidence note retained | 07-13 | ARCH-02…ARCH-07 |
| `.planning/phases/07-workspace-ground-truth-recorded-answers/COVERAGE.md` | **NEW** — reasoned no-external-API declaration | 07-13 | — |

New headings created by this plan: `## Summary`, `### Verdict distribution`, `### Per-milestone
roll-up`, `### Nested outstanding items`, `### Forward scope` and `### Citation resolution` inside the
ledger, and the H1 inside `COVERAGE.md`. No heading is added to `REQUIREMENTS.md`, `PROMOTION.md` or
`PROJECT.md`.
</artifacts>

<verification>
- `grep -c '^## Summary' .planning/ledgers/milestone-04-06.md` equals `1`
- The per-verdict counts in the summary sum to `115` and each matches a `grep -c` over the file
- `grep -c 'PENDING-VERDICT' .planning/ledgers/milestone-04-06.md` equals `0`
- `grep -c 'Next free ADR number: 0022' .planning/decisions/PROMOTION.md` equals `1`
- `grep -L 'conforms\|must change' .planning/decisions/00{14,15,16,17,18,19,20,21}*.md` prints nothing
- `grep -c 'finalised by plan 07-13' .planning/REQUIREMENTS.md` equals `0`
- `test -f .planning/phases/07-workspace-ground-truth-recorded-answers/COVERAGE.md` succeeds
- A diff of the whole phase's commit range restricted to `*.rs`, `Cargo.toml` and `.github/` is empty
</verification>

<success_criteria>
The phase closes honestly: the ledger's headline numbers were counted rather than assumed, every one
of the 115 rows is verdicted, the ADR numbering index is consistent with the eight files on disk and
points at 0022, both candidates `PROMOTION.md` assigned to this phase are closed, the five code
consequences are recorded with their owning phase and requirement so none of them disappears, and the
records-only boundary is proved rather than asserted.
</success_criteria>

<output>
Create `.planning/phases/07-workspace-ground-truth-recorded-answers/07-13-SUMMARY.md` when done.
Record: the full counted verdict distribution, the per-milestone roll-up, the nested outstanding-item
count, the result of every one of the ten phase-gate checks from `07-VALIDATION.md`, the
phase-boundary diff result and the commit range it covered, and the two flagged planner assumptions
(ARCH-05 in plan 07-09 and ARCH-07 in plan 07-07) carried forward for the phase verifier.
</output>
