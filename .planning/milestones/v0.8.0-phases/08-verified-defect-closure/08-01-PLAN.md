---
phase: 08-verified-defect-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/paladin-core/src/platform/container/token_usage.rs
  - crates/paladin-core/src/platform/container/battalion/mod.rs
  - crates/paladin-llm/src/llm_analysis_service.rs
autonomous: false
requirements: [DEBT-05]

must_haves:
  truths:
    - "`grep -rn 'pub struct TokenUsage' crates src | wc -l` returns exactly 1 (ROADMAP criterion 5), and the surviving definition is `crates/paladin-core/src/platform/container/token_usage.rs` — the canonical site ADR-0016 named (D-17)."
    - "Every existing `TokenUsage::new(..)` / `TokenUsage::from_total(..)` call site keeps resolving without edit, because the canonical type gained those inherent constructors before either duplicate was collapsed (D-17)."
    - "Every existing `use`/path that reached a duplicate — `paladin_core::platform::container::battalion::TokenUsage` and `paladin_llm::llm_analysis_service::TokenUsage` — still resolves, because both duplicates become `pub use` re-exports rather than deletions (D-18)."
    - "Token figures crossing the battalion/ports boundary need no conversion, because both sides name the same type (ROADMAP criterion 5, second clause)."
    - "`cargo test --offline --workspace --lib` passes at or above its pre-change count, proving none of the ~182 `TokenUsage` reference sites broke."
  artifacts:
    - "crates/paladin-core/src/platform/container/token_usage.rs — canonical `TokenUsage` carrying `Debug, Clone, Default, PartialEq, Serialize, Deserialize` plus inherent `new()` and `from_total()`."
    - "crates/paladin-core/src/platform/container/battalion/mod.rs — `pub use crate::platform::container::token_usage::TokenUsage;` in place of the struct body and its `impl` block."
    - "crates/paladin-llm/src/llm_analysis_service.rs — `pub use paladin_core::platform::container::token_usage::TokenUsage;` in place of the struct body."
  key_links:
    - "`crates/paladin-herald/src/table_herald.rs:403` and `markdown_herald.rs:510,514` call `battalion::TokenUsage::from_total(..)` through the battalion path — the re-export must carry the inherent methods, which is why `pub use` (not a type alias) is mandated."
    - "`src/application/services/analysis/llm_analysis_service.rs:6` glob-re-exports `paladin_llm::llm_analysis_service::*` and therefore transitively re-exports whatever `TokenUsage` that module names; the glob must keep resolving to exactly one type after the change."
    - "`crates/paladin-llm/Cargo.toml:27` already declares the `paladin-core` edge (D-19) — the cross-crate re-export adds no new dependency and does not disturb ADR-0015's purity invariant."
  prohibitions:
    - "Do not modify `.github/workflows/ci.yml:148`, `:393` or `:792` (`actions-rs/toolchain@v1`) — Phase 15 / PIPE-04 owns the full eight-reference action-modernization sweep."
    - "Do not converge `VisionTokenUsage` (`crates/paladin-ports/src/output/vision_port.rs:34`) onto the canonical `TokenUsage` — it is a differently-named type outside ADR-0016's five and outside DEBT-05's grep."
    - "Do not audit, re-fence or un-ignore the 87 pre-existing `ignore`/`no_run`/`text` doc fences in `paladin-ports` — Phase 16 / DOCS-03 owns them."
    - "Do not decide which `cargo doc --workspace --no-deps` warning bar governs — Phase 10 / HARD-07 owns that question."
    - "Do not retire or delete `src/main.rs` — ADR-0019 recorded its purpose; only gating and migration are in scope, and neither belongs to this plan."
---

<objective>
Collapse the three shipping `TokenUsage` definitions into one, executing ADR-0016's
`Code Conformance: must change` instruction with Phase 8 / DEBT-05 as its named executor.

Purpose: `BattalionResult.per_paladin_tokens` uses the battalion-local copy while the ports layer
uses the core copy, so token figures crossing that boundary need conversion today. ADR-0016 already
settled which crate owns the type; this plan performs the mechanical consolidation the ADR
authorises (D-17, D-18, D-19).

Output: one `pub struct TokenUsage` in the tree, at
`crates/paladin-core/src/platform/container/token_usage.rs`, with two `pub use` re-exports
preserving every existing import path.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-verified-defect-closure/08-CONTEXT.md
@.planning/phases/08-verified-defect-closure/08-PATTERNS.md
@.planning/decisions/0016-port-value-type-ownership.md
</context>

## Execution notes

- **Workspace gate is mandatory** (CLAUDE.md): `cargo test` → `cargo fmt --check` →
  `cargo clippy -- -D warnings` before any commit. Prefer `--offline` on every `cargo` invocation —
  `crates.io` returns HTTP 403 in this environment and the tree builds and tests offline.
- **`worktree_skip_hooks=true`** — this repository carries `pre-commit` and `pre-push` hooks that
  run `cargo fmt --check` and `cargo clippy --workspace -D warnings`. In a fresh worktree those
  cold-compile the whole workspace on every commit. Run the gate explicitly, then commit with hooks
  skipped.
- **Evidence bar (D-00e / D-21):** no closure claim without the exact command or `file:line` that
  produced it, recorded verbatim in the SUMMARY.
- The package name of `crates/paladin-core` is **`paladin-ai-core`** — use `-p paladin-ai-core`.

## Artifacts this phase produces (this plan's share)

| Symbol / path | Kind | Note |
|---|---|---|
| `TokenUsage::new(prompt_tokens: u32, completion_tokens: u32) -> Self` | new inherent method on the canonical type | moved from `battalion/mod.rs` |
| `TokenUsage::from_total(total_tokens: u32) -> Self` | new inherent method on the canonical type | moved from `battalion/mod.rs` |
| `Default` + `PartialEq` on `paladin_core::platform::container::token_usage::TokenUsage` | new derives | additive for existing consumers |
| `pub use crate::platform::container::token_usage::TokenUsage;` in `battalion/mod.rs` | new re-export path | replaces a struct body |
| `pub use paladin_core::platform::container::token_usage::TokenUsage;` in `llm_analysis_service.rs` | new re-export path | replaces a struct body |

## Flagged planner assumptions — unresolved edge probes (DEBT-05)

No `-SPEC.md` exists for this phase, so no `## Edge Coverage` section governs. The deterministic
edge probe produced 2 applicable items for DEBT-05, both `status: unresolved`,
`verification: null`, `resolution: null`. They are surfaced here rather than dropped. The
category→requirement mapping below is this planner's reconstruction; the probe returned a count
(14 phase-wide), not an item-by-item attribution.

| # | Category | Probe question against DEBT-05 | Disposition |
|---|---|---|---|
| E-05a | `adjacency` | What happens where the canonical type and a duplicate's import path are *both* in scope in one module — does the collapse create an ambiguous-name error? | **unresolved.** Not auto-backstopped, not dismissed. RESEARCH.md checked the one glob re-export (`src/application/services/analysis/llm_analysis_service.rs:6`) and found no collision, and `grep -rn "TokenUsage as"` returns 0 — but no exhaustive two-paths-in-one-scope search was performed. Task 3's workspace build is the practical falsifier. |
| E-05b | `empty` | What is the correct `TokenUsage` for a provider that reports no token accounting at all — is `Default` (all zeros) the intended empty value, or a distinct absent state? | **unresolved.** This plan adds `Default` because the battalion copy already carried it and one call site (`battalion/mod.rs:1151`) uses it; whether all-zeros is semantically "no usage reported" or "zero tokens used" is not settled by ADR-0016 and is not settled here. Recorded, not resolved. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the canonical TokenUsage before anything is collapsed</name>
  <files>crates/paladin-core/src/platform/container/token_usage.rs</files>
  <read_first>
    - `crates/paladin-core/src/platform/container/token_usage.rs` (the file being modified — 20 lines, the canonical definition at `:12-20`)
    - `crates/paladin-core/src/platform/container/battalion/mod.rs:490-525` (the richer duplicate whose derives and inherent methods are being absorbed — the analog PATTERNS.md names)
    - `.planning/phases/08-verified-defect-closure/08-PATTERNS.md` §"`crates/paladin-core/src/platform/container/token_usage.rs` (model, transform — gains derives + inherent impls)"
    - `.planning/decisions/0016-port-value-type-ownership.md` (the ADR whose `Code Conformance: must change` this plan executes)
  </read_first>
  <behavior>
    - `TokenUsage::new(10, 5)` yields `prompt_tokens == 10`, `completion_tokens == 5`, `total_tokens == 15`.
    - `TokenUsage::from_total(263)` yields `prompt_tokens == 0`, `completion_tokens == 0`, `total_tokens == 263`.
    - `TokenUsage::default()` yields all three fields `== 0`.
    - `TokenUsage::new(1, 2) == TokenUsage::new(1, 2)` is `true` and `TokenUsage::new(1, 2) == TokenUsage::from_total(3)` is `false` (the `PartialEq` derive compares all three fields, not just the total).
  </behavior>
  <action>
    Extend the canonical type in place. Change the derive list at
    `crates/paladin-core/src/platform/container/token_usage.rs:12` from
    `Debug, Clone, Serialize, Deserialize` to `Debug, Clone, Default, PartialEq, Serialize, Deserialize`.
    Then append an inherent `impl TokenUsage` block carrying `pub fn new(prompt_tokens: u32,
    completion_tokens: u32) -> Self` (which sets `total_tokens` to `prompt_tokens + completion_tokens`)
    and `pub fn from_total(total_tokens: u32) -> Self` (which sets `prompt_tokens` and
    `completion_tokens` to 0), copied verbatim in body and rustdoc from
    `crates/paladin-core/src/platform/container/battalion/mod.rs:506-523` so the two are provably
    identical before the duplicate is removed. Add a `#[cfg(test)] mod tests` block covering the four
    behaviours above; the crate has no existing test module in this file, so create one following the
    house `#[cfg(test)] mod tests { use super::*; ... }` shape. Do not change the three field names,
    their `u32` types, their `pub` visibility, or the module-level `//!` docs. Do not touch
    `battalion/mod.rs` in this task — D-17's sequence is non-negotiable: extend first, collapse second.
    Keep rustdoc on every new public item (`cargo doc` runs under `-D warnings` in this workspace) and
    use no `unwrap()`/`expect()`/`panic!` outside the test module.
  </action>
  <verify>
    <automated>cargo test --offline -p paladin-ai-core --lib token_usage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Default' crates/paladin-core/src/platform/container/token_usage.rs` is ≥ 1 and the derive line at `:12` contains all six of `Debug`, `Clone`, `Default`, `PartialEq`, `Serialize`, `Deserialize`.
    - `grep -c 'pub fn new\|pub fn from_total' crates/paladin-core/src/platform/container/token_usage.rs` returns `2`.
    - `cargo test --offline -p paladin-ai-core --lib token_usage` exits 0 and reports at least 4 passing tests.
    - `cargo build --offline --workspace` exits 0 — proves the additive derives broke no existing consumer.
    - `grep -rn 'pub struct TokenUsage' crates src | wc -l` still returns `3` at the end of this task (nothing collapsed yet — this task is purely additive).
  </acceptance_criteria>
  <done>The canonical `TokenUsage` carries `Default` + `PartialEq` and both inherent constructors, verified by a passing unit-test module, and the workspace still builds with all three definitions present.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Collapse the two duplicate `TokenUsage` bodies into `pub use` re-exports of the canonical `paladin-core` definition.</decision>
  <context>
CONTEXT.md rates D-18 **one-way**: "once the two bodies are collapsed, re-splitting means
re-dividing a public type across two published crates, which is what ADR-0016 exists to prevent."
`paladin-core` (as `paladin-ai-core`) and `paladin-llm` are both published crates, so this changes
where a published type is defined, not merely where it is used. Task 1 has already landed the
additive half (derives + constructors on the canonical type), which is reversible on its own; this
checkpoint gates the irreversible half. ADR-0016 is `Accepted` with `Code Conformance: must change`
and names Phase 8 / DEBT-05 as the executor, so option-a is the recorded answer — this checkpoint
exists because the reversibility gate requires a human to see a one-way step before it lands, not
because the decision is open.

  </context>
  <options>
    <option id="option-a">
      <name>Proceed — collapse both duplicates to `pub use` re-exports (the ADR-0016 / D-18 path)</name>
      <pros>Satisfies ROADMAP criterion 5 (`grep -rn 'pub struct TokenUsage' crates src` returns exactly one). Preserves all ~182 existing reference sites and both existing import paths, because a `pub use` carries the type's inherent methods with it. Executes an Accepted ADR that names this phase as executor. Two shipped precedents to copy verbatim (`llm_port.rs:671` cross-crate, `herald.rs:28` intra-crate).</pros>
      <cons>One-way: re-splitting later means re-dividing a public type across two published crates. The `battalion::TokenUsage` and `llm_analysis_service::TokenUsage` paths become aliases rather than definitions, which a future reader must follow one hop to find.</cons>
    </option>
    <option id="option-b">
      <name>Halt — leave the two duplicates in place and record DEBT-05 as blocked</name>
      <pros>Nothing irreversible lands. Task 1's additive extension still stands on its own.</pros>
      <cons>ROADMAP criterion 5 stays unmet; token figures continue to need conversion across the battalion/ports boundary; an Accepted ADR carrying `must change` with this phase named as executor is left unexecuted, which is the exact failure mode the precedence rule (D-00b) exists to prevent.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a or option-b</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Collapse both duplicates into pub use re-exports and prove the tree still resolves</name>
  <files>crates/paladin-core/src/platform/container/battalion/mod.rs, crates/paladin-llm/src/llm_analysis_service.rs</files>
  <read_first>
    - `crates/paladin-core/src/platform/container/battalion/mod.rs:490-525` (the intra-crate duplicate: `#[derive(...)] pub struct TokenUsage { .. }` at `:496-504` plus its `impl TokenUsage` block at `:506-523`)
    - `crates/paladin-llm/src/llm_analysis_service.rs:50-55` (the cross-crate duplicate)
    - `crates/paladin-core/src/platform/container/herald.rs:28` (intra-crate `pub use` precedent — copy this shape for `battalion/mod.rs`)
    - `crates/paladin-ports/src/output/llm_port.rs:671` (cross-crate `pub use` precedent — copy this shape for `llm_analysis_service.rs`)
    - `.planning/phases/08-verified-defect-closure/08-PATTERNS.md` §"`battalion/mod.rs` and `llm_analysis_service.rs` — TokenUsage duplicates become `pub use` re-exports"
  </read_first>
  <action>
    Per D-18, replace each duplicate definition with a re-export of the canonical type, preserving
    both existing import paths.

    In `crates/paladin-core/src/platform/container/battalion/mod.rs`: delete the entire
    `#[derive(Debug, Clone, Default, Serialize, Deserialize, PartialEq)] pub struct TokenUsage { .. }`
    block and the `impl TokenUsage { .. }` block that follows it (`:496-523`, both now duplicated on
    the canonical type by task 1), and put in their place the single line
    `pub use crate::platform::container::token_usage::TokenUsage;` — the exact shape at
    `herald.rs:28`. Retain the existing `///` doc paragraph above the struct by converting it to a
    one-line `//` comment above the `pub use`, or drop it; do not leave a `///` doc comment attached
    to a `pub use` if `cargo doc -D warnings` objects. Leave every other item in this file untouched,
    including the 11 `TokenUsage::new(..)` / `TokenUsage::from_total(..)` / `TokenUsage::default()`
    call sites at `:1135,1143,1151,1159,1172,1173,1201,1209,1210,1211,1267` — they must keep resolving
    unchanged through the re-export, which is the point of the mechanism.

    In `crates/paladin-llm/src/llm_analysis_service.rs`: delete the
    `#[derive(Debug, Clone, Serialize, Deserialize)] pub struct TokenUsage { .. }` block at `:50-55`
    and put in its place `pub use paladin_core::platform::container::token_usage::TokenUsage;` — the
    exact shape at `llm_port.rs:671`. Add no dependency: `crates/paladin-llm/Cargo.toml:27` already
    declares `paladin-core = { package = "paladin-ai-core", .. }` (D-19), and the re-export therefore
    introduces no new dependency-graph edge and does not disturb ADR-0015's purity invariant. Place
    the `pub use` where the struct was, so the item order in the module is unchanged for a reader.

    Do not touch `crates/paladin-ports/src/output/vision_port.rs` — `VisionTokenUsage` is explicitly
    out of scope (D-20) and is not among ADR-0016's five named types.
  </action>
  <verify>
    <automated>test "$(grep -rn 'pub struct TokenUsage' crates src | wc -l)" = "1" && cargo test --offline --workspace --lib</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'pub struct TokenUsage' crates src | wc -l` returns exactly `1`, and the single hit is `crates/paladin-core/src/platform/container/token_usage.rs` (ROADMAP criterion 5).
    - `grep -c 'pub use crate::platform::container::token_usage::TokenUsage' crates/paladin-core/src/platform/container/battalion/mod.rs` returns `1`.
    - `grep -c 'pub use paladin_core::platform::container::token_usage::TokenUsage' crates/paladin-llm/src/llm_analysis_service.rs` returns `1`.
    - `cargo test --offline --workspace --lib` exits 0 with a pass count at or above the pre-change baseline (record the pre-change count in the SUMMARY before starting, per D-21).
    - `cargo build --offline --workspace --all-targets` exits 0 — proves the two Herald call sites that reach the type through `battalion::TokenUsage::from_total(..)` (`crates/paladin-herald/src/table_herald.rs:403`, `markdown_herald.rs:510,514`) and the glob re-export at `src/application/services/analysis/llm_analysis_service.rs:6` all still resolve, with zero call-site edits.
    - `grep -rn 'VisionTokenUsage' crates/paladin-ports/src/output/vision_port.rs | wc -l` is unchanged from its pre-plan value — the out-of-scope type was not touched.
    - `cargo fmt --check` exits 0 and `cargo clippy --workspace -- -D warnings` exits 0 (CLAUDE.md workspace gate).
  </acceptance_criteria>
  <done>Exactly one `pub struct TokenUsage` exists in the tree; both former duplicate paths resolve to it through `pub use` re-exports; the whole workspace builds and its library tests pass at or above the pre-change count with zero call-site edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| — | This plan introduces no trust boundary. It edits three first-party Rust source files in the workspace, adds no dependency, opens no network path, reads no user input, and handles no credential. `TokenUsage` is a three-`u32` value type with no I/O. |

## STRIDE Threat Register

Assessed at ASVS level 1, blocking on `high`. RESEARCH.md §"Security Domain" assessed this phase as
introducing no new dependency, network surface, credential or user-input handling; V5 (Input
Validation) and V6 (Cryptography) are not applicable to this plan and are not padded into the table.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01-01 | Tampering | `TokenUsage::new()` arithmetic — `prompt_tokens + completion_tokens` on `u32` | low | accept | The addition is copied verbatim from the shipped `battalion/mod.rs:509-515` body, which has been in production since Milestone 5; this plan changes where it lives, not what it computes. An overflow would require a single LLM call reporting > 4.29 billion tokens across prompt and completion. `[profile.dev] overflow-checks = true` (root `Cargo.toml`) already panics on overflow in debug builds. Introducing a checked-add here would be a behaviour change outside DEBT-05's scope and is recorded as out of scope rather than silently applied. |
| T-08-01-02 | Information Disclosure | `Serialize`/`Deserialize` on the re-exported type | low | accept | Field set, visibility and serde derives are byte-identical to what already ships at all three sites; no field is added, renamed or made newly public, so no previously-private value becomes serialisable. |
| T-08-01-03 | Tampering | Supply chain — package-manager installs | n/a | accept | No `cargo add`, no `cargo install`, no manifest dependency change in this plan. RESEARCH.md's Package Legitimacy Audit records "Not applicable — no new external package is introduced by any DEBT-01…DEBT-05 task." No `[ASSUMED]`/`[SUS]` package exists, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- ROADMAP criterion 5: `grep -rn 'pub struct TokenUsage' crates src | wc -l` returns `1`.
- Regression: `cargo test --offline --workspace --lib` at or above the pre-change pass count.
- Compile-through: `cargo build --offline --workspace --all-targets` exits 0.
- Workspace gate (CLAUDE.md): `cargo test --offline --workspace` → `cargo fmt --check` →
  `cargo clippy --workspace -- -D warnings`, all exit 0.
- ADR-0006 floor is not re-measured here (that is the close-out plan's job), but this plan adds a
  test module and removes none, so its coverage delta is expected non-negative.
</verification>

<success_criteria>
- One `pub struct TokenUsage` in `crates` and `src`, at the ADR-0016 canonical path.
- Both former duplicate import paths still resolve, with zero call-site edits anywhere in the tree.
- The canonical type carries `Default`, `PartialEq`, `new()` and `from_total()`.
- `VisionTokenUsage` untouched.
- Workspace gate green.
- Every closure claim in the SUMMARY carries the verbatim command that produced it (D-00e / D-21).
</success_criteria>

<output>
Create `.planning/phases/08-verified-defect-closure/08-01-SUMMARY.md` when done.
Record verbatim: the pre-change and post-change `cargo test --offline --workspace --lib` pass counts,
the `grep -rn 'pub struct TokenUsage' crates src` output, and the human's checkpoint selection.
</output>
