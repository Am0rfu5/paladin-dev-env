---
phase: 08-verified-defect-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-api-surface.sh
  - scripts/extract-public-api.sh
  - .github/workflows/ci.yml
  - .project/current-exports.txt
  - scripts/check-deprecations.sh
autonomous: true
requirements: [DEBT-01]

must_haves:
  truths:
    - "An unchanged tree makes the `api-surface` guard pass: `bash scripts/check-api-surface.sh .project/current-exports.txt` prints `✅ API surface unchanged` and exits 0 (ROADMAP criterion 1, first clause)."
    - "An intentional public-API change makes the guard fail: adding a throwaway `pub fn` to a library crate makes the same command print `❌ API surface has changed!` and exit 1 (ROADMAP criterion 1, second clause)."
    - "The guard stops exiting 1 with `⚠️  No baseline found` on every run — it resolves `.project/current-exports.txt`, which exists, instead of `project/current-exports.txt`, which does not."
    - "`scripts/check-deprecations.sh` executes as the `api-surface` job's second step and can genuinely fail: a malformed `#[deprecated]` attribute anywhere under `src/` **or** `crates/` produces a non-zero exit."
    - "All five *tooling* references to the stale `project/current-exports.txt` path are gone — `grep -rn 'project/current-exports.txt' scripts/ .github/ | grep -v '\\.project/'` produces no output. (The five *requirement-text* references are plan 08-05's half of DEBT-01.)"
    - "The regenerated baseline is deterministic: two consecutive extractions differ only in their generated-timestamp header line, which `check-api-surface.sh` already filters."
    - statement: "The `api-surface` job's baseline is regenerated from the same commit it guards, so a public-API change landing between regeneration and a CI run is reported as `changed` (exit 1) rather than silently absorbed — the job has no window in which drift passes."
      verification: backstop
  artifacts:
    - "scripts/check-api-surface.sh — `BASELINE` default `.project/current-exports.txt`."
    - "scripts/extract-public-api.sh — `OUTPUT_FILE` default `.project/current-exports.txt`."
    - ".github/workflows/ci.yml — the three `api-surface`-job literals corrected, `:148` untouched."
    - ".project/current-exports.txt — regenerated against HEAD, replacing the 2026-07-06 snapshot."
    - "scripts/check-deprecations.sh — a gate that can fail, scanning `src` and `crates`."
  key_links:
    - "`scripts/check-api-surface.sh` calls `./scripts/extract-public-api.sh` internally — both defaults must move together or the check compares a fresh extraction against a path the extractor would not have written."
    - "`.github/workflows/ci.yml:175` (`Check deprecation warnings`) only executes once `:172` (`Check API surface changes`) stops failing — DEBT-01's path fix is what makes `check-deprecations.sh` reachable at all, which is why D-05 lands in this plan and not in DEBT-02's."
    - "`cargo public-api` requires a nightly toolchain; `rustup toolchain install nightly` reaches `static.rust-lang.org` (HTTP 200) even though `crates.io` returns HTTP 403, and `cargo-public-api` 0.52.0 is already installed at `/usr/local/cargo/bin/cargo-public-api` — no `cargo install` is needed."
  prohibitions:
    - "Do not modify `.github/workflows/ci.yml:148`, `:393` or `:792` (`actions-rs/toolchain@v1`) — Phase 15 / PIPE-04 owns the full eight-reference action-modernization sweep. `:148` sits inside the very `api-surface` job this plan edits; edit `:172`, `:182`, `:187` only."
    - "Do not converge `VisionTokenUsage` (`crates/paladin-ports/src/output/vision_port.rs:34`) onto the canonical `TokenUsage`."
    - "Do not audit, re-fence or un-ignore the 87 pre-existing `ignore`/`no_run`/`text` doc fences in `paladin-ports` — Phase 16 / DOCS-03 owns them."
    - "Do not decide which `cargo doc --workspace --no-deps` warning bar governs — Phase 10 / HARD-07 owns that question."
    - "Do not retire or delete `src/main.rs` — ADR-0019 recorded its purpose; only gating and migration are in scope, and neither belongs to this plan."
    - "Do not add a 'deprecations must exist' gate to `check-deprecations.sh` — that would prejudge DEBT-02 (D-05, explicit)."
---

<objective>
Make the `api-surface` CI job actually guard the public API: correct the five stale
`project/current-exports.txt` tooling references, regenerate the baseline so it matches the tree it
guards, and make `check-deprecations.sh` — which has never executed, and could not fail if it did —
into a gate that can fail.

Purpose: this is the longest-lived unfixed defect in the corpus. Since commit `928c6d5` renamed
`project/` to `.project/`, the single automated guard against unintended public-API changes has
exited 1 with `⚠️  No baseline found` on every run, and the deprecation check that runs after it has
never been reached. Correcting the paths against a five-week-old baseline would produce a job that
fails for a *different* reason, which reads identically from a CI dashboard and is not closure
(D-02).

Output: a guard proven to pass on an unchanged tree and fail on an intentional change, plus a
regenerated baseline and a deprecation check with a meaningful exit status.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-verified-defect-closure/08-CONTEXT.md
@.planning/phases/08-verified-defect-closure/08-RESEARCH.md
@.planning/phases/08-verified-defect-closure/08-PATTERNS.md
</context>

## Execution notes

- **Baseline regeneration is the proven default path, not a best-effort attempt.** CONTEXT.md D-03's
  blocker framing was amended on 2026-08-06 after research ran the full sequence to completion in
  this exact checkout: `cargo-public-api` 0.52.0 is already installed (no `cargo install`, no
  crates.io), and `rustup toolchain install nightly` succeeds via `static.rust-lang.org`. Nightly is
  already present in this container (`rustup toolchain list` shows
  `nightly-x86_64-unknown-linux-gnu`). If it is absent in the executor's environment, install it
  with a timeout of **at least 90 seconds** — research recorded a 15-second timeout looking like a
  hang when the step merely needed more time.
- **The D-03 contingency survives only as a contingency.** If — and only if — extraction genuinely
  cannot run, land task 1's path fix, record the blocker with the exact failing command, and scope
  the DEBT-01 claim honestly to "path corrected; baseline refresh blocked, procedure recorded".
  Never write a baseline by hand and never claim criterion 1 without the both-direction proof.
- **Workspace gate** (CLAUDE.md): `cargo test` → `cargo fmt --check` →
  `cargo clippy -- -D warnings` before any commit. This plan changes no `.rs` file, but the
  pre-commit hook runs the gate regardless. **`worktree_skip_hooks=true`** — run the gate
  explicitly, then commit with hooks skipped, or every commit cold-compiles the workspace.
- **Evidence bar (D-00e / D-21):** record the verbatim command and its output for every claim.

## Artifacts this phase produces (this plan's share)

| Symbol / path | Kind | Note |
|---|---|---|
| `.project/current-exports.txt` (regenerated) | data artefact | replaces the 2026-07-06 / 1,827-item-era snapshot with a HEAD-accurate one (~1,968 items at research time) |
| `BASELINE="${1:-.project/current-exports.txt}"` | changed script default | `scripts/check-api-surface.sh:6` |
| `OUTPUT_FILE="${1:-.project/current-exports.txt}"` | changed script default | `scripts/extract-public-api.sh:6` |
| a failing branch in `scripts/check-deprecations.sh` | changed gate semantics | the script gains a non-zero exit path it did not have |

## Flagged planner assumptions — unresolved edge probes (DEBT-01)

No `-SPEC.md` exists, so no `## Edge Coverage` section governs. The deterministic edge probe
returned 14 phase-wide items, all `status: unresolved`, `verification: null`, `resolution: null`.
The four below are this planner's reconstruction of DEBT-01's share; the probe returned a count, not
an item-by-item attribution. None is auto-backstopped and none is dismissed.

| # | Category | Probe question against DEBT-01 | Disposition |
|---|---|---|---|
| E-01a | `adjacency` | What happens when two baselines exist side by side — the regenerated `.project/current-exports.txt` and the untracked root-level `api_surface_current.txt` / `final-api.txt` the ledger records? Does anything merge or prefer the wrong one? | **unresolved.** `check-api-surface.sh` takes exactly one `$BASELINE` argument and never scans for siblings, so no merge semantics exist — but nothing in this phase *asserts* that the two root-level files are inert, and REQUIREMENTS.md's `REQ-stable-api-per-crate` row records them as present. Recorded, not resolved. |
| E-01b | `empty` | What does the guard do against an empty or truncated baseline file (0 bytes, or a partial write from an interrupted extraction)? | **unresolved.** `check-api-surface.sh:14` tests only `[ ! -f "$BASELINE" ]` — existence, not content — so a 0-byte baseline would report every public item as removed rather than as a tooling fault. Out of DEBT-01's literal scope (its done-condition is the path plus the two directions); recorded here so it is not mistaken for covered. |
| E-01c | `ordering` | Is `cargo public-api`'s item ordering stable across runs and across toolchain versions, or could a reordering alone produce a spurious `changed` verdict? | **PROMOTED to `must_haves.truths`** as the determinism truth — research ran two consecutive extractions and diffed them (0 differing lines excluding the timestamp header). Stability *across nightly versions* is not proven and remains an open risk; the truth as written claims only run-to-run determinism on one toolchain. |
| E-01d | `concurrency` | What does CI guarantee when the baseline regeneration races a concurrent public-API change — can a change land in the window between regeneration and the guard's run? | **PROMOTED to `must_haves.truths` with `verification: backstop`.** The falsifier is indirect: the guard recomputes the current surface on every run and compares against the committed baseline, so any post-regeneration change appears as `changed`. No test in this phase exercises two concurrent branches, hence `backstop` rather than `explicit`. |

<tasks>

<task type="tracer">
  <name>Task 1: Correct all five tooling path literals — the guard reaches its baseline end to end</name>
  <files>scripts/check-api-surface.sh, scripts/extract-public-api.sh, .github/workflows/ci.yml</files>
  <read_first>
    - `scripts/check-api-surface.sh` (full file, 49 lines — `BASELINE` default at `:6`, the existence test at `:14-19`, the filtered diff at `:21-49`)
    - `scripts/extract-public-api.sh` (first 20 lines — `OUTPUT_FILE` default at `:6`)
    - `.github/workflows/ci.yml:140-190` (the whole `api-surface` job — the three literals live at `:172`, `:182`, `:187`; `:148`'s `actions-rs/toolchain@v1` is inside this same job and is PROHIBITED)
    - `.planning/phases/08-verified-defect-closure/08-RESEARCH.md` §"DEBT-01 — the `api-surface` CI job" (the five references re-confirmed verbatim, plus the `ci.yml:148` prohibition)
    - `.planning/ledgers/milestone-04-06.md` row `REQ-api-surface-ci` (line 115 — the ledger evidence this task acts on)
  </read_first>
  <action>
    Change the literal `project/current-exports.txt` to `.project/current-exports.txt` at exactly
    five sites, and nowhere else. Per D-01 this is a literal correction — do not add fallback logic
    that tries both paths, do not introduce an env-var indirection, and do not factor the path into
    a shared variable: a defect that survived three ingest runs because nobody read five literals is
    not fixed by adding a sixth place to look.

    1. `scripts/check-api-surface.sh:6` — `BASELINE="${1:-project/current-exports.txt}"` becomes
       `BASELINE="${1:-.project/current-exports.txt}"`.
    2. `scripts/extract-public-api.sh:6` — `OUTPUT_FILE="${1:-project/current-exports.txt}"` becomes
       `OUTPUT_FILE="${1:-.project/current-exports.txt}"`.
    3. `.github/workflows/ci.yml:172` — the `Check API surface changes` step's
       `run: ./scripts/check-api-surface.sh project/current-exports.txt`.
    4. `.github/workflows/ci.yml:182` — the `Generate API diff on failure` step's
       `diff -u project/current-exports.txt /tmp/current-api.txt || true`.
    5. `.github/workflows/ci.yml:187` — the same step's echoed remediation instruction
       `Update the baseline: ./scripts/extract-public-api.sh project/current-exports.txt`.

    Re-grep for the line numbers before editing — this corpus has a recorded history of citation
    drift (the ledger's `REQ-crate-isolation-ci` row documents a 76-line drift on a neighbouring
    job). Change no other line in the `api-surface` job. In particular leave `:148`
    (`uses: actions-rs/toolchain@v1`) exactly as it is: it belongs to Phase 15 / PIPE-04's
    eight-reference sweep, and DEBT-01 shed it deliberately.

    This is the phase's tracer task: after it, the guard reaches its baseline end to end for the
    first time since commit `928c6d5`. It will now report `❌ API surface has changed!` against the
    stale 2026-07-06 baseline rather than `⚠️  No baseline found` — that is the correct, expected
    intermediate state, and task 2 closes it. Record both the before and after messages verbatim.
  </action>
  <verify>
    <automated>bash scripts/check-api-surface.sh 2>&1 | tee /tmp/gsd-08-02-t1.log; grep -q 'No baseline found' /tmp/gsd-08-02-t1.log && echo TRACER-FAIL && exit 1; grep -qE 'API surface (unchanged|has changed)' /tmp/gsd-08-02-t1.log && echo TRACER-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'project/current-exports.txt' scripts/ .github/ | grep -v '\.project/'` produces **no output**.
    - `grep -c '\.project/current-exports\.txt' scripts/check-api-surface.sh scripts/extract-public-api.sh` returns `1` for each file.
    - `grep -c '\.project/current-exports\.txt' .github/workflows/ci.yml` returns `3`.
    - `bash scripts/check-api-surface.sh` (no argument, exercising the corrected default) prints either `✅ API surface unchanged` or `❌ API surface has changed!` — and **never** `⚠️  No baseline found`. This is the end-to-end assertion: the default path now resolves to a file that exists.
    - `grep -n 'actions-rs/toolchain@v1' .github/workflows/ci.yml` still returns `148`, `393`, `792` — three hits, unchanged (prohibition holds).
    - `git diff --stat .github/workflows/ci.yml` shows 3 changed lines, no more.
  </acceptance_criteria>
  <done>All five tooling literals point at `.project/current-exports.txt`; `check-api-surface.sh` resolves and reads a real baseline end to end instead of exiting on "No baseline found"; `ci.yml:148` is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the baseline and prove the guard in both directions</name>
  <files>.project/current-exports.txt</files>
  <read_first>
    - `scripts/extract-public-api.sh` (full file — the `cargo-public-api` invocation and its install fallback at `:11-20`, which must NOT be exercised because the binary is already present)
    - `scripts/check-api-surface.sh:21-49` (the filtered-diff comparison: the generated-timestamp header and the `Total public items:` line are filtered out before diffing, which is why a fresh extraction compares equal to itself)
    - `.project/current-exports.txt` (first 10 lines only — the header format and the stale `2026-06-10` generation stamp; do not read the whole 442 KB file)
    - `.planning/phases/08-verified-defect-closure/08-RESEARCH.md` §"THE HEADLINE FINDING: baseline regeneration works in this environment, right now" (the verbatim proven sequence, including the 53-line diff against the stale baseline and the "unchanged" confirmation)
    - `.planning/phases/08-verified-defect-closure/08-VALIDATION.md` rows 1 and 1b (the two commands this task must satisfy)
    - `.planning/phases/08-verified-defect-closure/08-CONTEXT.md` D-02 and D-03 (D-02 makes regeneration the deliverable rather than the path edit; D-03 is the contingency — and only the contingency — if extraction genuinely cannot run: land the path fix, record the blocker with its exact failing command, and never claim criterion 1 satisfied)
  </read_first>
  <action>
    Confirm the toolchain first: `cargo public-api --version` must print `cargo-public-api 0.52.0`
    (the binary is already at `/usr/local/cargo/bin/cargo-public-api`; do not run `cargo install`,
    which needs crates.io and would fail with HTTP 403), and `rustup toolchain list` must include
    `nightly-x86_64-unknown-linux-gnu`. If nightly is absent, run
    `rustup toolchain install nightly --profile minimal` with a timeout of at least 90 seconds.

    Prove determinism before committing anything: extract twice to two temp paths
    (`bash scripts/extract-public-api.sh /tmp/api-a.txt` and `.../api-b.txt`), then diff them
    ignoring the generated-timestamp header line. The diff must be empty. Record the item count the
    script prints (research measured 1,968).

    Then regenerate the committed baseline in place:
    `bash scripts/extract-public-api.sh .project/current-exports.txt`. Record the size and character
    of the resulting `git diff` in the SUMMARY (research measured 53 substantive diff lines against
    the stale snapshot, covering `ArsenalExecutionService` methods, `MCPServerConfig.auth_token_env`,
    `PaladinBuilder::add_mcp_sse`, an `MCPTransport` trait, and `mcp_sse_adapter` /
    `mcp_streamable_http_adapter` module churn) — D-02 requires the diff's size and character be
    recorded, not just its existence.

    Now prove the guard in both directions, which is what ROADMAP criterion 1 actually asks for.
    **Positive direction:** `bash scripts/check-api-surface.sh .project/current-exports.txt` must
    print `✅ API surface unchanged` and exit 0. **Negative direction:** add one throwaway public
    item to a library crate that is part of the extracted surface — a `pub fn
    gsd_debt01_negative_probe() {}` in an existing public module of the root facade is sufficient —
    re-run the same command, confirm it prints `❌ API surface has changed!` and exits 1, then
    `git checkout --` the probe file so the tree is clean again. Capture both runs' stdout verbatim.
    Do **not** regenerate the baseline after the negative probe; the baseline must remain the one
    taken from the clean tree.

    Per the discretion D-24 leaves open, commit the regenerated baseline as its own commit — the
    diff is large and reads better alone than folded into task 1's five-line change.
  </action>
  <verify>
    <automated>bash scripts/check-api-surface.sh .project/current-exports.txt</automated>
  </verify>
  <acceptance_criteria>
    - `cargo public-api --version` prints `cargo-public-api 0.52.0`; no `cargo install` was run (confirm the shell history / the script's install branch was not taken).
    - Two consecutive `bash scripts/extract-public-api.sh /tmp/...` runs differ by **0 lines** after filtering the `# Public API Surface - Generated` header — determinism proven.
    - `head -1 .project/current-exports.txt` shows a generation timestamp dated **today**, not `2026-06-10`.
    - `bash scripts/check-api-surface.sh .project/current-exports.txt` prints `✅ API surface unchanged` and exits **0** (ROADMAP criterion 1, positive direction — VALIDATION.md row 1).
    - With a throwaway `pub fn` added, the same command prints `❌ API surface has changed!` and exits **1** (ROADMAP criterion 1, negative direction — VALIDATION.md row 1b).
    - After reverting the probe, `git status --short` shows only `.project/current-exports.txt` as modified — the probe left no residue.
    - The SUMMARY records the regenerated item count and the diff's line count and character (D-02).
  </acceptance_criteria>
  <done>`.project/current-exports.txt` matches HEAD; the guard passes on the unchanged tree and fails on an intentional public-API change, both proven by a command run in this environment and captured verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Make check-deprecations.sh a gate that can actually fail</name>
  <files>scripts/check-deprecations.sh</files>
  <read_first>
    - `scripts/check-deprecations.sh` (full file, 42 lines — the primary `jq` branch at `:9-10`, the fallback branch whose *both* outcomes `exit 0` at `:27` and `:30`, and the only real failure path at `:36-40` which greps `src/` alone)
    - `scripts/check-api-surface.sh:1-19` and `scripts/check-doc-config.sh:1-20` (the house shape for a CI gate that can genuinely fail — `set -euo pipefail`, a real non-zero exit, no swallowed failure)
    - `.planning/phases/08-verified-defect-closure/08-PATTERNS.md` §"`scripts/check-deprecations.sh` (utility script, must become capable of failing)"
    - `.planning/phases/08-verified-defect-closure/08-CONTEXT.md` D-05 (the deliberately narrow scope, and the explicit prohibition on inventing a new gate)
  </read_first>
  <action>
    Two changes, deliberately narrow (D-05).

    **(a) Extend the malformed-attribute grep's file scope.** The check at `:36` scans `src/` only
    and therefore misses all eleven workspace crates. Change it to scan `src/` **and** `crates/`,
    following `check-api-surface.sh`'s pattern of running its check across the full relevant path
    set rather than a subset. Keep the same detection semantics: a `#[deprecated]` occurrence that
    carries neither a `since` nor a `note` field is malformed and must exit 1 with the existing
    message and remediation hint.

    **(b) Make the primary and fallback branches' exit statuses honest.** Today the `jq` primary
    branch pipes its parsed output to `/dev/null` so nothing it finds can ever influence the outcome,
    and the fallback branch's two arms both `exit 0` unconditionally (`:27` and `:30`) — the script
    presents as a gate and, apart from (a), cannot fail. Restructure so that each branch's own
    finding determines its own exit status, and so that reaching the end of the script means the
    malformed-attribute check in (a) actually ran. Concretely: the primary branch must not discard
    its parsed result, and the fallback must not `exit 0` before the (a) check is reached — a
    successful "no deprecation warnings" outcome should fall through to (a), not short-circuit past
    it. Keep `set -euo pipefail` at `:4`. Keep the `🔍` / `✅` echo style; this repository's gate
    scripts are read from CI logs.

    **Do not add a new gate.** Specifically, do not make the script fail when zero `#[deprecated]`
    attributes exist — that would prejudge DEBT-02, whose answer (ADR-0022, plan 08-06) is that zero
    is the correct terminal state. The script's job is: malformed attributes fail; well-formed
    attributes and zero attributes both pass.

    Note that `jq` is present in this environment (`/usr/bin/jq`), so the primary branch is
    exercisable locally, not only in CI.
  </action>
  <verify>
    <automated>bash scripts/check-deprecations.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-deprecations.sh` on the clean tree exits **0** and its final line is the `✅ All deprecation attributes are properly formatted` success message — proving the malformed-attribute check was reached rather than short-circuited.
    - `grep -c 'crates' scripts/check-deprecations.sh` is ≥ 1 and the malformed-attribute grep names both `src/` and `crates/`.
    - **Negative proof:** temporarily insert a bare `#[deprecated]` (no `since`, no `note`) above a `pub fn` in any file under `crates/`, run `bash scripts/check-deprecations.sh`, and confirm it exits **non-zero** with the `❌ Found deprecation without 'since' or 'note' fields!` message; then `git checkout --` the file. This is the proof that the check now covers `crates/`, which it provably did not before (VALIDATION.md row 1d).
    - `grep -vE '^\s*#' scripts/check-deprecations.sh | grep -c 'exit 0'` is ≤ 1 — the two unconditional early `exit 0`s at the old `:27` and `:30` are gone.
    - `grep -c 'set -euo pipefail' scripts/check-deprecations.sh` returns `1`.
    - The script contains no check that fails on a zero-deprecation tree (D-05 prohibition): running it against today's tree, which has `grep -rn '#\[deprecated' src crates` → 0 matches, exits 0.
  </acceptance_criteria>
  <done>`check-deprecations.sh` scans `src` and `crates`, exits non-zero on a malformed attribute anywhere in either, exits 0 on the current zero-deprecation tree, and has no unconditional early `exit 0` that swallows its own findings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → workspace source | `.github/workflows/ci.yml`'s `api-surface` job executes repository-controlled shell scripts against repository-controlled source. Nothing external crosses in; the boundary is listed because this plan edits both sides of it. |
| `rustup` / `static.rust-lang.org` → local toolchain | Task 2 may install a nightly toolchain over the network. This is an existing, already-shipped CI step (`ci.yml:154`, `run: rustup toolchain install nightly`) — this plan does not add it. |

## STRIDE Threat Register

Assessed at ASVS level 1, blocking on `high`. RESEARCH.md §"Security Domain" assessed this phase as
introducing no new dependency, network surface, credential or user-input handling. V5 (Input
Validation) and V6 (Cryptography) are not applicable and are not padded into the table.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02-01 | Tampering | `scripts/check-deprecations.sh` — a CI gate that cannot fail | medium | mitigate | This is the defect, not a hypothetical: the script's two fallback arms `exit 0` unconditionally, so a malformed `#[deprecated]` in any of the eleven crates passes CI today. Task 3 restores a real non-zero exit path and extends coverage from `src/` to `src/ crates/`, proven by the deliberate-malformed-attribute negative test in its acceptance criteria. |
| T-08-02-02 | Tampering | `scripts/check-api-surface.sh` — a public-API guard that is inert | high | mitigate | Also the defect, not a hypothetical: the guard has exited 1 on `No baseline found` since commit `928c6d5`, so an unintended public-API change — including one that widens a surface — has been unreviewable by CI for five weeks. Tasks 1 and 2 restore it and prove it in **both** directions; the negative-direction proof is what distinguishes a working guard from a green-by-accident one. Blocking at ASVS L1 requires this be mitigated, not accepted, and it is. |
| T-08-02-03 | Tampering | Supply chain — `rustup toolchain install nightly` over the network | low | accept | Fetches from `static.rust-lang.org` over TLS using `rustup`'s own signature-checked channel manifests. The identical step already ships at `ci.yml:154`; this plan adds no new fetch. `cargo install cargo-public-api` is explicitly **not** run — the binary is already present at 0.52.0, and `crates.io` is unreachable (HTTP 403) in this environment. |
| T-08-02-04 | Tampering | Supply chain — package-manager installs of workspace dependencies | n/a | accept | No `cargo add`, no manifest dependency change. RESEARCH.md's Package Legitimacy Audit records "Not applicable". No `[ASSUMED]`/`[SUS]` package exists, so no legitimacy checkpoint is required. |
| T-08-02-05 | Repudiation | The regenerated baseline as an audit record | low | accept | `.project/current-exports.txt` carries a generated-timestamp header and is committed, so the diff between any two baselines is attributable through git history. `check-api-surface.sh` filters that header before comparing, so the record does not interfere with the check. |
</threat_model>

<verification>
- ROADMAP criterion 1, positive: `bash scripts/check-api-surface.sh .project/current-exports.txt`
  → `✅ API surface unchanged`, exit 0.
- ROADMAP criterion 1, negative: with a throwaway `pub fn` present, the same command
  → `❌ API surface has changed!`, exit 1. Probe reverted afterwards.
- ROADMAP criterion 1, references: `grep -rn 'project/current-exports.txt' scripts/ .github/ |
  grep -v '\.project/'` → no output (the five *tooling* references; plan 08-05 carries the five
  requirement-text ones).
- `check-deprecations.sh` reachable and failable: exits 0 on the clean tree, non-zero on a
  deliberately malformed `#[deprecated]` placed under `crates/`.
- Prohibition holds: `grep -n 'actions-rs/toolchain@v1' .github/workflows/ci.yml` → `148`, `393`,
  `792`, unchanged.
- Workspace gate (CLAUDE.md): `cargo test --offline --workspace` → `cargo fmt --check` →
  `cargo clippy --workspace -- -D warnings`, all exit 0.
</verification>

<success_criteria>
- Five tooling literals corrected; zero fallback logic, zero indirection added.
- Baseline regenerated from HEAD, determinism proven by a double extraction.
- The guard proven in both directions, both outputs captured verbatim.
- `check-deprecations.sh` can fail, covers `src` and `crates`, and gained no new gate.
- `ci.yml:148` untouched.
- Every closure claim in the SUMMARY carries the verbatim command that produced it (D-00e / D-21).
</success_criteria>

<output>
Create `.planning/phases/08-verified-defect-closure/08-02-SUMMARY.md` when done.
Record verbatim: `cargo public-api --version`; the two-extraction determinism diff result; the
regenerated item count; the `git diff --stat` line count and a one-paragraph characterisation of the
baseline diff (D-02); both directions of the `check-api-surface.sh` proof with their exit codes; and
the `check-deprecations.sh` negative-test output.
</output>
