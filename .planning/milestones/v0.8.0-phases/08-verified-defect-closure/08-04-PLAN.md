---
phase: 08-verified-defect-closure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/decisions/0022-deprecation-requirement-withdrawal.md
  - .planning/decisions/0023-cli-dependency-isolation.md
autonomous: true
requirements: [DEBT-02, DEBT-04]

must_haves:
  truths:
    - "The two decisions this phase changes a shipped surface on are recorded *before* the code that executes them lands — ADR-0022 exists before plan 08-06 reconciles the deprecation documents, and ADR-0023 exists before plan 08-07 gates the `paladin` binary and `paladin-herald`'s formatters (D-24's content dependency)."
    - "The phase goal's second clause — \"no shipped surface is removed without a recorded decision behind it\" — is satisfiable for D-06, D-13 and D-14, because each has a written, numbered, Accepted decision naming its own executor."
    - "Both ADRs match the corpus file shape exactly: no YAML frontmatter, exactly seven `##` headings in the order `Status` / `Context` / `Decision` / `Considered Options` / `Code Locations` / `Code Conformance` / `Downstream Consumers`, with a `**Date:**` line beneath `Status`."
    - "Both ADRs carry `Code Conformance: must change` with **Phase 8 itself** named as the executor — the first ADRs in this corpus whose executing phase is their own (D-22)."
    - "ADR-0022 restates the stale v0.2.0 → v0.3.0 → v1.0.0 removal timeline against the shipped 0.7.0 workspace rather than silently dropping it (D-08), citing ADR-0008."
    - statement: "A future reader who asks \"why does this framework ship zero `#[deprecated]` attributes?\" finds ADR-0022 rather than concluding the work was forgotten."
      verification: backstop
  artifacts:
    - ".planning/decisions/0022-deprecation-requirement-withdrawal.md — Milestone 4 Epic 2 FR-8 withdrawn, with the reason, the evidence and the timeline restatement."
    - ".planning/decisions/0023-cli-dependency-isolation.md — the `structopt`→clap migration, the `required-features = [\"cli\"]` gate on the `paladin` binary, and the `paladin-herald` formatter feature split, recorded as one decision with two sites."
  key_links:
    - "`.planning/decisions/PROMOTION.md:45` records `Next free ADR number: 0022`. This plan consumes 0022 and 0023; plan 08-09 advances the line to 0024 and appends both index rows — this plan does NOT edit PROMOTION.md, per the PATTERNS.md note that the bookkeeping row lands in the close-out plan, not in the ADR itself."
    - "ADR-0023 cites ADR-0019 (binary-target architecture) as its precondition — ADR-0019 established the three-binary architecture and left three doors open (gate, migrate, retire); ADR-0023 records which door was taken and why."
    - "ADR-0022's evidence is `.project/.../Epic_2/DEPRECATIONS.md`'s own ⚠ IMMEDIATE DEPRECATION section, which is the only category that would produce a `#[deprecated]` attribute and which lists no candidates — the epic's own tracking document answering its own requirement."
    - "Plan 08-08 appends the verbatim `cargo tree` proof to ADR-0023's `Code Locations` section once criterion 4 is measured; this plan writes the *command* and leaves the output slot explicitly marked pending."
  prohibitions:
    - "Do not modify `.github/workflows/ci.yml:148`, `:393` or `:792` (`actions-rs/toolchain@v1`) — Phase 15 / PIPE-04 owns the full eight-reference action-modernization sweep."
    - "Do not converge `VisionTokenUsage` (`crates/paladin-ports/src/output/vision_port.rs:34`) onto the canonical `TokenUsage`."
    - "Do not audit, re-fence or un-ignore the 87 pre-existing `ignore`/`no_run`/`text` doc fences in `paladin-ports` — Phase 16 / DOCS-03 owns them."
    - "Do not decide which `cargo doc --workspace --no-deps` warning bar governs — Phase 10 / HARD-07 owns that question."
    - "Do not retire or delete `src/main.rs` — ADR-0019 recorded its purpose, and ADR-0023 must record retirement as an option **rejected**, not taken."
    - "Do not add YAML frontmatter to either ADR — the corpus shape is 0001-0021's, not `adr-parser.cjs`'s schema (D-00a)."
    - "Do not edit `.planning/decisions/PROMOTION.md` in this plan — plan 08-09 owns the bookkeeping."
---

<objective>
Author the two ADRs this phase allocates, before the code they authorise lands: **ADR-0022**
(withdrawing Milestone 4 Epic 2 FR-8's deprecation requirement) and **ADR-0023** (CLI dependency
isolation and the binary/Herald surface).

Purpose: the phase goal's second clause is "no shipped surface is removed without a recorded
decision behind it." Three of this phase's decisions change a shipped, published surface —
withdrawing a published deprecation policy (D-06), gating a binary that `cargo run` builds today
(D-13), and shrinking a crates.io-published crate's default API (D-14). Each needs its decision
written down and reviewable *before* the diff, not reconstructed after it.

Output: two ADR files in the corpus shape, both `Accepted`, both carrying
`Code Conformance: must change` with Phase 8 itself as the named executor.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-verified-defect-closure/08-CONTEXT.md
@.planning/phases/08-verified-defect-closure/08-RESEARCH.md
@.planning/phases/08-verified-defect-closure/08-PATTERNS.md
</context>

## Execution notes

- **ADR shape is fixed and non-negotiable** (D-00a): no YAML frontmatter, exactly seven `##`
  headings in order — `Status` (with a `**Date:**` line beneath), `Context`, `Decision`,
  `Considered Options`, `Code Locations`, `Code Conformance`, `Downstream Consumers`. PATTERNS.md
  carries the verbatim skeleton lifted from ADR-0016 and ADR-0019.
- **Medieval-military ubiquitous language is mandatory** (CLAUDE.md, D-00f) — Herald, Paladin,
  Armory, Battalion, Arsenal, Citadel, Garrison. ADR-0023 in particular is about Herald's surface.
- **Evidence bar (D-00e / D-21):** every factual claim in either ADR carries the command or
  `file:line` that produced it. Re-verify each cited line number before writing it — this corpus has
  a recorded history of citation drift.
- **Both ADRs are ⚠ HUMAN REVIEW items in CONTEXT.md** and none of the three decisions they record
  was human-confirmed. Write them so the reasoning is auditable and the rejected options are
  preserved, not so they read as already-settled.
- This plan changes no `.rs` file and no manifest; the workspace gate still runs via the pre-commit
  hook. **`worktree_skip_hooks=true`** — run the gate explicitly, then commit with hooks skipped.

## Artifacts this phase produces (this plan's share)

| Symbol / path | Kind | Note |
|---|---|---|
| `.planning/decisions/0022-deprecation-requirement-withdrawal.md` | new ADR | slug `deprecation-requirement-withdrawal`; consumed from `PROMOTION.md:45`'s "next free 0022" |
| `.planning/decisions/0023-cli-dependency-isolation.md` | new ADR | slug `cli-dependency-isolation`; records D-13 and D-14 as one decision with two sites (D-15) |

## Flagged planner assumptions — unresolved edge probes (DEBT-02, DEBT-04 — records half)

No `-SPEC.md` exists, so no `## Edge Coverage` section governs. The deterministic edge probe
returned 14 phase-wide items, all `status: unresolved`. The two below are the records-side share;
the rest are carried by plans 08-06 (DEBT-02 reconciliation) and 08-07 / 08-08 (DEBT-04 code).

| # | Category | Probe question | Disposition |
|---|---|---|---|
| E-02a | `adjacency` | If a future ADR re-instates a deprecation policy, what happens where ADR-0022's withdrawal and the new policy are both in force — which wins, and does the corpus have a supersession mechanism for ADRs? | **unresolved.** ADR-0020 demonstrates judging a stale artefact, and `PROMOTION.md` records numbering, but no ADR-supersedes-ADR marker exists in this corpus's shape. ADR-0022 should note that re-instatement means a new ADR that explicitly supersedes it — that is a written instruction, not a proven mechanism. Recorded, not resolved. |
| E-04a | `ordering` | ADR-0023 records a decision whose proof (`cargo tree` output) does not exist until plan 08-08 runs. What is the ADR's status in the window between authoring and proof? | **unresolved.** This plan writes the command and marks the output slot pending; 08-08 fills it. Nothing in this corpus defines a "provisional" ADR status, so the file is `Accepted` from the start with an explicitly-marked pending evidence line. Recorded, not resolved. |

<tasks>

<task type="auto">
  <name>Task 1: Author ADR-0022 — the deprecation requirement is withdrawn</name>
  <files>.planning/decisions/0022-deprecation-requirement-withdrawal.md</files>
  <read_first>
    - `.planning/decisions/0016-port-value-type-ownership.md` (full file — the exact seven-heading shape, and the `Code Conformance: must change` block at `:100-108` that names a phase as executor)
    - `.planning/decisions/0020-build-benchmark-per-scenario.md` (the model for D-08: judge a stale artefact, say why it is stale, state what replaces it — rather than reconciling it)
    - `.planning/decisions/0008-workspace-version-0-7-0.md` (the version story the timeline restatement cites)
    - `.project/Milestone_4-Refactor-Crates-Features/Epic_2/DEPRECATIONS.md` (full file — the ⚠ IMMEDIATE DEPRECATION section at `:81`, its verbatim "None identified yet - managers are currently `pub(crate)` or will be moved to application layer (Epic 3)"; the SOFT DEPRECATION category resolving to `#[doc(hidden)]`; the INTERNAL-ONLY category resolving to `pub(crate)`; §"Current Status" at `:171`, §"Deprecation Log" at `:190`, §"Open Questions" at `:206-211`)
    - `.planning/REQUIREMENTS.md` lines 874-885 (DEBT-02 in full, including the "no third state" clause)
    - `.planning/phases/08-verified-defect-closure/08-PATTERNS.md` §"`.planning/decisions/0022-*.md` and `0023-*.md` (ADR, transform)"
  </read_first>
  <action>
    Write `.planning/decisions/0022-deprecation-requirement-withdrawal.md`, opening with the H1
    `# ADR-0022: Milestone 4 Epic 2 FR-8 deprecation requirement withdrawn` and containing exactly
    the seven `##` headings in the fixed order, no YAML frontmatter.

    `## Status` — `Accepted`, then a blank line, then `**Date:** 2026-08-06`.

    `## Context` — state the question: Milestone 4 Epic 2 FR-8 requires every type leaving the public
    API to carry `#[deprecated(since = …, note = …)]`; `grep -rn '#\[deprecated' src crates` returns
    **0**; DEBT-02's requirement text permits either implementation or withdrawal-with-a-reason and
    explicitly forbids a third state. Present the evidence from the epic's own tracking document, not
    from inference: `DEPRECATIONS.md`'s ⚠ IMMEDIATE DEPRECATION section — the only category that
    would produce a `#[deprecated]` attribute — lists in full "None identified yet - managers are
    currently `pub(crate)` or will be moved to application layer (Epic 3)", with "Migration Path:
    TBD" and "List: TBD based on usage analysis" for its other sub-categories; its SOFT DEPRECATION
    category resolves to `#[doc(hidden)]`, and the tree carries **38** `doc(hidden)` occurrences, so
    that half *was* executed; its INTERNAL-ONLY category resolves to `pub(crate)`, which is an
    annotation-free outcome by design. Re-run each grep and cite the count you get. Frame this in
    the corpus's own precedence language (D-00b): the zero is not an unfinished task, it is what the
    epic's own decisions produce, and the tracking document is the higher-fidelity source.

    `## Decision` — FR-8 is **withdrawn**, with the reason recorded. State plainly that zero
    `#[deprecated]` attributes is the correct terminal state for the 0.7.0 tree, and that
    manufacturing deprecations to satisfy a grep would be the dishonest closure. Include D-08's
    timeline restatement as its own paragraph: the document's `v0.1.0 → v0.2.0 → v0.3.0 → v1.0.0`
    schedule is stale by five minor versions against the shipped `0.7.0` (root `Cargo.toml:34`,
    re-verify), so a `#[deprecated(since = …)]` written today could not honour a removal schedule
    keyed to v0.3.0; per ADR-0008 the pre-1.0 series absorbs API evolution through minor bumps, so
    the policy's version anchors move to **"one minor version"** rather than named releases that
    already shipped. State the target explicitly: the *policy* survives in
    `docs/src/api-reference/stable-api.md` (a future deprecation will follow it); only the claim that
    deprecations exist today is withdrawn. Note that re-instatement requires a new ADR that
    explicitly supersedes this one — the corpus has no ADR-supersedes-ADR marker, so the instruction
    is written rather than mechanised (edge probe E-02a).

    `## Considered Options` — three, with the accepted one first and the rejected ones preserved with
    their reasons: (a) **withdraw with a recorded reason** (accepted); (b) **implement FR-8 by
    deprecating something** (rejected — no named candidate exists anywhere in the 263-document
    corpus, so an implementer would have to invent the list, and the epic's own document says
    "None identified yet"); (c) **leave it open** (rejected — DEBT-02's requirement text forbids the
    third state where a document promises a timeline the tree cannot start).

    `## Code Locations` — cite with `file:line`: `.project/…/Epic_2/DEPRECATIONS.md:81` (⚠ IMMEDIATE
    DEPRECATION), `:171` (Current Status), `:190` (Deprecation Log), `:206-211` (the four Open
    Questions); `docs/src/api-reference/stable-api.md:875` (the single present-tense
    "Current and planned deprecations" claim that is false today — the one line plan 08-06 corrects);
    `scripts/check-deprecations.sh` (the gate, fixed in plan 08-02 so it can fail, deliberately
    without a "deprecations must exist" check that would have prejudged this ADR); root
    `Cargo.toml:34` (`version = "0.7.0"`). Re-verify every line number before writing it.

    `## Code Conformance` — `must change`, then the executor paragraph. **Phase 8 itself** is the
    named executor (D-22): plan 08-06 performs the three-way reconciliation — a dated D-00c banner on
    `DEPRECATIONS.md` recording the withdrawal and closing its four Open Questions, the
    `stable-api.md:875` correction, and the tree left unchanged at zero `#[deprecated]` as the
    recorded correct state. State what "must change" means concretely; no vague "needs updating".

    `## Downstream Consumers` — Phase 8 / plan 08-06 (the reconciliation); Phase 8 / plan 08-09 (the
    `REQ-deprecation-warnings` ledger row at `.planning/ledgers/milestone-04-06.md:116`, the
    DEBT-02 checkbox in REQUIREMENTS.md, the `PROMOTION.md` index row and next-free advance to 0024,
    and the PROJECT.md Key Decisions row); and any future phase that re-opens API-governance policy.
  </action>
  <verify>
    <automated>test "$(grep -c '^## ' .planning/decisions/0022-deprecation-requirement-withdrawal.md)" = "7" && ! head -1 .planning/decisions/0022-deprecation-requirement-withdrawal.md | grep -q '^---$' && grep -q '^must change$' .planning/decisions/0022-deprecation-requirement-withdrawal.md</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 .planning/decisions/0022-deprecation-requirement-withdrawal.md` is `# ADR-0022: Milestone 4 Epic 2 FR-8 deprecation requirement withdrawn` and line 1 is **not** `---` (no YAML frontmatter).
    - `grep -n '^## ' .planning/decisions/0022-deprecation-requirement-withdrawal.md` returns exactly 7 lines, in the order `Status`, `Context`, `Decision`, `Considered Options`, `Code Locations`, `Code Conformance`, `Downstream Consumers`.
    - `grep -A2 '^## Status' .planning/decisions/0022-deprecation-requirement-withdrawal.md` shows `Accepted` and `**Date:** 2026-08-06`.
    - `grep -c '^must change$' .planning/decisions/0022-deprecation-requirement-withdrawal.md` returns `1`, and the paragraph beneath it names **Phase 8** as the executor.
    - The `Considered Options` section lists exactly 3 options, one marked accepted and two marked rejected, each with a reason.
    - `grep -c '0008' .planning/decisions/0022-deprecation-requirement-withdrawal.md` is ≥ 1 — ADR-0008 is cited for the pre-1.0 minor-bump reasoning (D-08).
    - Every `file:line` cited in `## Code Locations` resolves: for each, `sed -n '<line>p' <file>` returns non-empty content matching the ADR's description of it.
    - `grep -rn '#\[deprecated' src crates | wc -l` returns `0` at the time of writing, and the ADR states that count with the command that produced it.
  </acceptance_criteria>
  <done>ADR-0022 exists in the corpus shape, records the withdrawal with the epic's own document as evidence, restates the stale timeline against 0.7.0 per D-08, preserves both rejected options, and names Phase 8 as the executor of its `must change`.</done>
</task>

<task type="auto">
  <name>Task 2: Author ADR-0023 — CLI dependency isolation and the binary/Herald surface</name>
  <files>.planning/decisions/0023-cli-dependency-isolation.md</files>
  <read_first>
    - `.planning/decisions/0019-binary-target-architecture.md` (full file — ADR-0023's stated precondition: the three-binary architecture, the `structopt` / `src/main.rs` coupling, and the three doors it left open)
    - `.planning/decisions/0021-cli-application-layer-placement.md` (the corrected fact that `src/application/mod.rs:57-59`'s `pub mod cli;` **is** `#[cfg(feature = "cli")]`-gated)
    - `.planning/decisions/0016-port-value-type-ownership.md:100-108` (the `Code Conformance: must change` block shape)
    - `.planning/decisions/0015-core-ports-dependency-allowlist.md` (the purity invariant this decision must not disturb)
    - `src/main.rs` (full file, 61 lines — the only `structopt` consumer in the tree)
    - `Cargo.toml` lines 93, 122, 125-126, 240-252, 284 (`structopt = "0.3"`; `clap = { version = "4.5.40", …, optional = true }`; `colored`/`comfy-table` unconditional; the three `[[bin]]` stanzas and their `required-features` asymmetry; the five-crate `cli` feature list)
    - `crates/paladin-herald/Cargo.toml` (no `[features]` section; `comfy-table` and `colored` unconditional)
    - `.planning/phases/08-verified-defect-closure/08-RESEARCH.md` §"DEBT-04 — the library-only build" (the signature check proving `colored`/`comfy_table` appear only in function bodies, and the full downstream-consumer enumeration)
  </read_first>
  <action>
    Write `.planning/decisions/0023-cli-dependency-isolation.md`, opening with the H1
    `# ADR-0023: CLI dependency isolation and the binary/Herald surface`, seven `##` headings in the
    fixed order, no YAML frontmatter. Per D-15 this is **one** ADR recording **one** question — what
    a library-only consumer compiles — with two sites.

    `## Status` — `Accepted`, blank line, `**Date:** 2026-08-06`.

    `## Context` — ROADMAP criterion 4 requires that a downstream project depending on `paladin`
    compile no CLI crates: `cargo tree --lib --no-default-features` must show none of `structopt`,
    `colored` or `comfy-table`. Today the shipped `cli` feature isolates 5 of the 8 dependencies the
    Epic 3 PRD classifies as CLI-only, while `structopt = "0.3"` (`Cargo.toml:93`), `colored = "2.1"`
    (`:125`) and `comfy-table = "7.1"` (`:126`) remain unconditional. Two mechanisms keep them in a
    library-only build, and both must be addressed: (1) the `paladin` `[[bin]]` at `:240-242` has no
    `required-features` while `paladin-cli` (`:247`) and `paladin-server` (`:252`) both do, so
    `src/main.rs` — the sole `structopt` consumer in the entire tree — is compiled by default;
    (2) `paladin-herald` is an **unconditional** root dependency (`Cargo.toml:22,54`) and declares
    `comfy-table` and `colored` unconditionally with no `[features]` section at all, so both re-enter
    through Herald regardless of what the root manifest does. Note that Phase 7's ledger read the
    Herald half as `superseded by shipped code`, but ROADMAP criterion 4 is stricter and names all
    three crates explicitly — the criterion governs, and the ledger row gets amended rather than the
    criterion narrowed. Cite ADR-0019 as the precondition that established the three-binary
    architecture and left three doors open, and ADR-0021 for the already-correct gating of
    `src/application/cli`.

    `## Decision` — two sites, one decision, with a "target, stated explicitly" paragraph naming
    every file the decision touches:
    - **Site 1 (the Armory's legacy entry point).** Migrate `src/main.rs` from `structopt` to
      `clap` v4 derive, and add `required-features = ["cli"]` to the `paladin` `[[bin]]`. Migrate
      *and* gate, because either alone is insufficient: gating without migrating leaves `structopt`
      — a crate whose upstream declares itself superseded by clap 3+ — as an optional dependency
      nobody intends to keep; migrating without gating leaves `clap` unconditional and merely
      renames the leak. `clap` 4.5.40 is already a workspace dependency (`Cargo.toml:122`) gated by
      `cli` (`:284`), so no new dependency line is added — only `structopt = "0.3"` is removed.
      State the user-visible consequence in the ADR itself, not only in `CHANGELOG.md`: after this
      change `cargo run` no longer builds the `paladin` binary without `--features cli`.
    - **Site 2 (Herald's formatters).** Give `paladin-herald` its first `[features]` section, making
      `comfy-table` and `colored` optional. Record the chosen feature names and the split: the
      `TableHerald` formatter (whose entire rendering is `comfy_table`) moves behind a feature; the
      `MarkdownHerald` type stays unconditional with only its *coloured* rendering path behind a
      feature, because `MarkdownHeraldConfig.include_colors` already exists as a runtime switch and
      the uncoloured path is the existing `include_colors: false` behaviour; `JsonHerald` stays
      unconditional, needing neither dependency. Record the signature finding that makes this a
      clean additive split rather than a breaking API change: no `comfy_table::*` or `colored::*`
      type appears in any `pub fn` signature or `pub` struct field — both config structs use only
      `usize`, `String`, `bool` and `u8`, and the dependencies are used only inside function bodies.
      Record that the root `cli` feature enables both `paladin-herald` features and marks
      `colored`/`comfy-table` `optional = true` at the root (they are genuinely needed by
      `src/application/cli/`, which is already `cli`-gated per ADR-0021 — so they become optional,
      not removed). Record that the root-facade consumer sites gate on the root's own
      `feature = "cli"`, since that is the feature DEBT-04's requirement text designates.
    - **Scope statement.** Name the *three* root-facade consumer sites that must gate with it, one of
      which RESEARCH.md did not find: `src/infrastructure/adapters/herald/mod.rs:9-10`,
      `src/application/services/herald/herald_registry.rs:248-250`, and — found during planning —
      `src/config/settings.rs:214-246`, whose `Settings::create_default_herald()` unconditionally
      constructs `MarkdownHerald::with_config(..)` and `TableHerald::new(..)`. Without all three, the
      default library build fails to *compile* and criterion 4 never reaches `cargo tree`. Record the
      behavioural consequence of gating the third: a configuration naming
      `herald.default_formatter = "table"` in a build without `cli` now returns the existing
      `Unknown formatter` error rather than a table Herald.

    `## Considered Options` — at least five, accepted first, rejected preserved with reasons:
    (a) migrate **and** gate, plus the Herald feature split (accepted); (b) gate `src/main.rs`
    without migrating (rejected — keeps a superseded dependency as an optional one nobody intends to
    keep); (c) migrate without gating (rejected — renames the leak, leaves `clap` unconditional);
    (d) **retire `src/main.rs`** (rejected — ADR-0019 has just recorded its purpose as the legacy
    content-aggregator service runner; retiring a binary the previous phase documented as purposeful
    is new scope, not defect closure, and the question of whether the `smartcontent-aggregator`
    runner should survive at all remains deferred); (e) root-manifest gating alone with criterion 4
    restated as root-scoped (rejected — this was D-14's stated fallback, and research **disproved**
    its precondition: `colored`/`comfy_table` are body-only, so the clean additive split is feasible
    and the fallback does not apply).

    `## Code Locations` — every site with `file:line`, re-verified before writing: `Cargo.toml:93`,
    `:122`, `:125`, `:126`, `:22`, `:54`, `:240-242`, `:244-247`, `:249-252`, `:284`;
    `src/main.rs:5,7-12,26,46,52,58`; `crates/paladin-herald/Cargo.toml` `[dependencies]` and the
    absent `[features]`; `crates/paladin-herald/src/lib.rs:19-25`;
    `crates/paladin-herald/src/table_herald.rs:31`; `crates/paladin-herald/src/markdown_herald.rs:17`;
    `src/infrastructure/adapters/herald/mod.rs:9-10`;
    `src/application/services/herald/herald_registry.rs:248-250`; `src/config/settings.rs:214,235,244`;
    `src/application/mod.rs:57-59`. Also record the four downstream build surfaces that assume the
    `paladin` binary builds by default and must move with it: `Dockerfile:33`, `Dockerfile.chef:74`,
    `.github/workflows/feature-flags.yml:144` (a step literally named "Verify paladin binary builds
    without cli feature", whose assertion inverts), and `docs/src/deployment/docker.md:135,146,156`.

    Include the criterion-4 proof command verbatim —
    `cargo build --offline --lib --no-default-features` followed by
    `cargo tree --offline --no-default-features | grep -E 'structopt|colored|comfy-table'` — and mark
    its output slot explicitly, on its own line, as
    `Output: PENDING — filled by plan 08-08 once the code lands.` Plan 08-08 replaces that line with
    the captured output (D-16: criterion 4 is proved by running the command, not by reading the
    manifest).

    `## Code Conformance` — `must change`, with **Phase 8 itself** named as the executor (D-22):
    plan 08-07 performs both sites' manifest and source changes; plan 08-08 performs the downstream
    build-surface sweep, the `CHANGELOG.md` entries and the criterion-4 proof. State concretely what
    must change; no vague "needs updating".

    `## Downstream Consumers` — Phase 8 / plan 08-07 and plan 08-08; Phase 8 / plan 08-09 (the
    `PROMOTION.md` index row and advance to 0024, the PROJECT.md Key Decisions row, the
    REQUIREMENTS.md DEBT-04 checkbox, and the Milestone 4-6 ledger row Phase 7 recorded as
    `superseded by shipped code` on the Herald half, which this decision amends rather than
    narrows); Phase 15 (the `cargo tree` allowlist check in CI, from ADR-0015 — this ADR runs
    `cargo tree` once as proof, and nothing enforces it on every build); Phase 16 (the user-facing
    binary-architecture mdbook page, from ADR-0019).
  </action>
  <verify>
    <automated>test "$(grep -c '^## ' .planning/decisions/0023-cli-dependency-isolation.md)" = "7" && ! head -1 .planning/decisions/0023-cli-dependency-isolation.md | grep -q '^---$' && grep -q '^must change$' .planning/decisions/0023-cli-dependency-isolation.md</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 .planning/decisions/0023-cli-dependency-isolation.md` is `# ADR-0023: CLI dependency isolation and the binary/Herald surface` and line 1 is **not** `---`.
    - `grep -n '^## ' .planning/decisions/0023-cli-dependency-isolation.md` returns exactly 7 lines in the fixed order.
    - `grep -A2 '^## Status'` shows `Accepted` and `**Date:** 2026-08-06`.
    - `grep -c '^must change$'` returns `1`, and the paragraph beneath names **Phase 8** as executor and names plans 08-07 and 08-08 as the executing plans.
    - `grep -c '0019' .planning/decisions/0023-cli-dependency-isolation.md` is ≥ 1 — ADR-0019 is cited as the precondition.
    - The `Considered Options` section lists at least 5 options; one is accepted; **retiring `src/main.rs` appears as an explicitly rejected option with its reason**, satisfying the prohibition against retirement while still recording that it was considered.
    - `grep -c 'src/config/settings.rs' .planning/decisions/0023-cli-dependency-isolation.md` is ≥ 1 — the third facade consumer site (found during planning, absent from RESEARCH.md) is named.
    - `grep -c 'PENDING' .planning/decisions/0023-cli-dependency-isolation.md` returns `1` — exactly one explicitly-marked pending evidence slot, for plan 08-08 to fill.
    - All four downstream build surfaces (`Dockerfile`, `Dockerfile.chef`, `feature-flags.yml`, `docs/src/deployment/docker.md`) are named in `## Code Locations`.
    - Every `file:line` cited in `## Code Locations` resolves: for each, `sed -n '<line>p' <file>` returns non-empty content matching the ADR's description of it.
  </acceptance_criteria>
  <done>ADR-0023 exists in the corpus shape, records D-13 and D-14 as one decision with two sites plus three facade consumer sites, preserves retirement as an explicitly rejected option, carries a single marked-pending evidence slot for the criterion-4 proof, and names Phase 8 as the executor of its `must change`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| — | This plan introduces no trust boundary. It creates two Markdown records under `.planning/decisions/`. No code, manifest, CI file or dependency changes; nothing executes as a result of this plan. |

## STRIDE Threat Register

Assessed at ASVS level 1, blocking on `high`. RESEARCH.md §"Security Domain" assessed this phase as
introducing no new dependency, network surface, credential or user-input handling. V5 (Input
Validation) and V6 (Cryptography) are not applicable and are not padded into the table.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04-01 | Repudiation | The decision record itself — a shipped surface changed without an attributable, dated reason | medium | mitigate | This is the risk the plan exists to close. Both ADRs are dated, numbered, cite their evidence with `file:line`, and preserve every rejected option with its reason, so a future reader can reconstruct *why* a published deprecation policy was withdrawn and *why* a crates.io crate's default API shrank. The acceptance criteria verify the shape mechanically and verify that every cited line resolves. |
| T-08-04-02 | Information Disclosure | ADR content | low | accept | Both ADRs cite only paths, line numbers and public API names that already exist in this repository. No credential, endpoint, token or private identifier is recorded. |
| T-08-04-03 | Tampering | Supply chain — package-manager installs | n/a | accept | No dependency change of any kind. ADR-0023 *describes* a dependency change (`structopt` removed, `clap` already vendored) that plan 08-07 performs; RESEARCH.md's Package Legitimacy Audit records "Not applicable — `clap` is an existing, already-vetted workspace dependency; the migration is a re-point of one binary's dependency, not a new install." No `[ASSUMED]`/`[SUS]` package exists, so no legitimacy checkpoint is required. |
| T-08-04-04 | Elevation of Privilege | `structopt` → `clap` v4 (recorded here, executed in 08-07) | low | mitigate | Recorded as a **net risk reduction**, not a new surface: `structopt`'s upstream publicly declares itself superseded by clap 3+, so the tree currently depends on an unmaintained argument parser for its default-built binary. ADR-0023 records this explicitly so the security benefit is attributable rather than incidental. |
</threat_model>

<verification>
- Both files exist under `.planning/decisions/` with the allocated numbers 0022 and 0023.
- Each has exactly 7 `##` headings in the fixed order and no YAML frontmatter (D-00a).
- Each has `Accepted` + `**Date:** 2026-08-06` under `## Status`.
- Each has `must change` under `## Code Conformance` with Phase 8 named as executor (D-22).
- Every `file:line` in each `## Code Locations` section resolves to content matching its description.
- ADR-0023 carries exactly one `PENDING` evidence slot, for plan 08-08.
- `.planning/decisions/PROMOTION.md` is **unmodified** by this plan (plan 08-09 owns it).
- Workspace gate (CLAUDE.md): `cargo test --offline --workspace` → `cargo fmt --check` →
  `cargo clippy --workspace -- -D warnings`, all exit 0 (no source changed, but the gate is the
  phase's standing requirement).
</verification>

<success_criteria>
- ADR-0022 and ADR-0023 exist, in the corpus shape, both Accepted and dated, both `must change` with
  Phase 8 as executor.
- ADR-0022 records the withdrawal with the epic's own tracking document as evidence and restates the
  stale timeline against 0.7.0 per D-08.
- ADR-0023 records D-13 and D-14 as one decision with two sites and three facade consumer sites,
  retirement rejected rather than silently omitted, and one marked-pending criterion-4 proof slot.
- Every closure claim in the SUMMARY carries the verbatim command or `file:line` that produced it
  (D-00e / D-21).
</success_criteria>

<output>
Create `.planning/phases/08-verified-defect-closure/08-04-SUMMARY.md` when done.
Record verbatim: the `grep -n '^## '` heading listing for both files; the re-run
`grep -rn '#\[deprecated' src crates | wc -l` and `grep -rn 'doc(hidden)' src crates | wc -l` counts
that ADR-0022 cites; and a list of every `file:line` cited in either ADR together with the
verification that it resolves.
</output>
