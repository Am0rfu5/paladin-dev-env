---
phase: 08-verified-defect-closure
plan: 06
type: execute
wave: 2
depends_on: ["08-04"]
files_modified:
  - .project/Milestone_4-Refactor-Crates-Features/Epic_2/DEPRECATIONS.md
  - docs/src/api-reference/stable-api.md
autonomous: true
requirements: [DEBT-02]

must_haves:
  truths:
    - "`grep -rn '#\\[deprecated' src crates` returns 0, and that zero is recorded as the *outcome* of a withdrawn requirement rather than as an unfinished task (ROADMAP criterion 2)."
    - "`DEPRECATIONS.md`, `docs/src/api-reference/stable-api.md` and ADR-0022 tell the same story: zero current deprecations, the withdrawal named, the reason recorded, ADR-0022 cited. There is no third state in which a document promises a deprecation timeline the tree cannot start (D-07)."
    - "The forward-looking deprecation *policy* survives in `stable-api.md` — how a future deprecation will work is not deleted, because the framework will need it. Only the claim that deprecations exist **today** is corrected (D-07(2))."
    - "`DEPRECATIONS.md`'s four Open Questions are each answered or closed with a one-line disposition, not left dangling (D-07(1))."
    - "The tree is unchanged: zero `#[deprecated]` attributes is the recorded correct state, and no deprecation was manufactured to satisfy a grep (D-06)."
    - statement: "Zero `#[deprecated]` attributes is a valid terminal state for DEBT-02 — the empty case is the answer, not a gap — because ADR-0022 withdraws the requirement that would have produced a non-zero count."
      verification: explicit
  artifacts:
    - ".project/Milestone_4-Refactor-Crates-Features/Epic_2/DEPRECATIONS.md — dated D-00c banner citing ADR-0022, the ⚠ IMMEDIATE DEPRECATION / Current Status / Deprecation Log sections annotated as outcome-not-gap, and all four Open Questions closed."
    - "docs/src/api-reference/stable-api.md — the single false present-tense claim corrected; the Deprecation Lifecycle policy and the 🔴 Deprecated tier retained."
  key_links:
    - "ADR-0022 (plan 08-04) is this plan's precondition — the reconciliation cites it by number, and without it the three documents would agree on a claim with no recorded decision behind it. This is a content dependency, not a file dependency: DEBT-02 touches no CI file and no source file."
    - "`scripts/check-deprecations.sh` (repaired in plan 08-02) deliberately gained **no** 'deprecations must exist' gate, precisely so it does not contradict this plan's outcome — a script that failed on a zero-deprecation tree would put the tooling in the third state the requirement forbids."
    - "`stable-api.md:875`'s `[Deprecations Tracking](…/CHANGELOG.md) - Current and planned deprecations` is the ONE line in the file that makes a false present-tense claim; the other ~39 `deprecat` hits are policy prose and a hypothetical `#[deprecated(since = …)]` illustration at `:798-827` explicitly framed as a 'Version 0.2.0' example."
  prohibitions:
    - "Do not modify `.github/workflows/ci.yml:148`, `:393` or `:792` (`actions-rs/toolchain@v1`) — Phase 15 / PIPE-04 owns the full eight-reference action-modernization sweep."
    - "Do not converge `VisionTokenUsage` (`crates/paladin-ports/src/output/vision_port.rs:34`) onto the canonical `TokenUsage`."
    - "Do not audit, re-fence or un-ignore the 87 pre-existing `ignore`/`no_run`/`text` doc fences in `paladin-ports` — Phase 16 / DOCS-03 owns them."
    - "Do not decide which `cargo doc --workspace --no-deps` warning bar governs — Phase 10 / HARD-07 owns that question."
    - "Do not retire or delete `src/main.rs`."
    - "Do not add a single `#[deprecated]` attribute anywhere in the tree — manufacturing deprecations so a grep returns non-zero is the dishonest closure D-06 explicitly rejects."
    - "Do not delete `stable-api.md`'s Deprecation Lifecycle section, its 🔴 Deprecated tier, or its `#[deprecated(since = …)]` illustration — the policy survives; only the present-tense claim is corrected."
    - "Do not rewrite or delete any `.project/` text — annotation only, original retained and marked superseded (D-00c)."
---

<objective>
Make `DEPRECATIONS.md`, the mdbook stable-API page and the tree agree that this framework carries
zero `#[deprecated]` attributes, and that this is the recorded outcome of a withdrawn requirement
rather than an unfinished task.

Purpose: DEBT-02's requirement text permits either implementation or withdrawal-with-a-reason and
explicitly forbids a third state where a document promises a deprecation timeline the tree cannot
start. ADR-0022 (plan 08-04) supplied the decision; **the three-way reconciliation is the
deliverable, not the ADR** (D-07). Today the epic's own tracking document reads as an unfinished
task list ("⏳ Adding deprecation warnings (Task 3.0)"), the mdbook page claims "Current and planned
deprecations" exist, and the tree has none — three documents, three stories.

Output: three artefacts telling one story, with the epic's four Open Questions closed and the
forward-looking policy preserved.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-verified-defect-closure/08-CONTEXT.md
@.planning/phases/08-verified-defect-closure/08-RESEARCH.md
@.planning/phases/08-verified-defect-closure/08-PATTERNS.md
@.planning/decisions/0022-deprecation-requirement-withdrawal.md
</context>

## Execution notes

- **ADR-0022 must exist before this plan runs.** It is authored by plan 08-04 (wave 1). Read it
  first and cite it by number in every annotation; the reconciliation's whole job is to make three
  documents point at one recorded decision.
- **`.project/` is annotated, never rewritten** (D-00c). The verbatim markup analog is
  `.project/Milestone_4-Refactor-Crates-Features/Epic_1/prd-expand-feature-flags.md`.
- **Banner wording and inline markup are Claude's Discretion** — D-00c fixes the pattern, not the
  prose.
- **`docs/src/` is mdbook source and is NOT `.project/`** — it is live documentation, so the
  correction there is a normal prose edit, not a struck-and-annotated one. Do not import `.project/`
  strike-through markup into the published book.
- **`mdbook` and `mdbook-linkcheck` are not installed in this environment** (`command -v mdbook` →
  missing; CI installs `mdbook-linkcheck` 0.7.7 via `cargo install`, which needs crates.io and is
  unavailable here at HTTP 403). VALIDATION.md row 2b is therefore a **backstop**: this plan changes
  no link target and adds no page, so linkcheck risk is structurally low, but it cannot be proven
  locally. Record that honestly rather than claiming a green linkcheck.
- **Evidence bar (D-00e / D-21):** record the verbatim grep counts and the before/after text of every
  corrected line.
- This plan changes no `.rs` file; the pre-commit hook still runs the workspace gate.
  **`worktree_skip_hooks=true`** — run the gate explicitly, then commit with hooks skipped.

## Artifacts this phase produces (this plan's share)

| Symbol / path | Kind | Note |
|---|---|---|
| dated `DEBT-02` / ADR-0022 correction banner | record annotation | top of `DEPRECATIONS.md` |
| four closed Open Questions | record annotation | `DEPRECATIONS.md:206-211`, one-line disposition each |
| corrected "Deprecations Tracking" link description | live docs edit | `docs/src/api-reference/stable-api.md:875` |

## Flagged planner assumptions — unresolved edge probes (DEBT-02)

No `-SPEC.md` exists, so no `## Edge Coverage` section governs. The deterministic edge probe
returned 14 phase-wide items, all `status: unresolved`. The three below are this planner's
reconstruction of DEBT-02's share; the probe returned a count, not an item-by-item attribution. One
is promoted to a `must_haves.truths` entry; the rest stay unresolved, neither backstopped nor
dismissed.

| # | Category | Probe question against DEBT-02 | Disposition |
|---|---|---|---|
| E-02b | `empty` | Is a zero-deprecation tree a valid terminal state, or an unfinished one? | **PROMOTED to `must_haves.truths` with `verification: explicit`.** This is the requirement's entire pivot, and it is now decidable: ADR-0022 withdraws the requirement that would have produced a non-zero count, so zero is the answer. Falsifier: `grep -rn '#\[deprecated' src crates | wc -l` returns `0` **and** all three documents state the same zero-and-why. |
| E-02c | `adjacency` | What happens where the withdrawn requirement (M4 Epic 2 FR-8) and the surviving policy (`stable-api.md`'s Deprecation Lifecycle) sit side by side — does a reader conclude the policy is also withdrawn? | **unresolved.** This plan's mitigation is to keep the policy explicitly forward-looking and to name ADR-0022 in the corrected line, so the reader sees "policy stands, nothing currently deprecated". Whether that reads unambiguously to a first-time reader is a judgement, not a check — and is exactly what the human-judgment verification in task 3 exists to test. Recorded, not resolved. |
| E-02d | `ordering` | If a future deprecation *is* added, which document must be updated first, and does anything enforce the order? | **unresolved.** ADR-0022 records that re-instatement requires a superseding ADR, but nothing in this phase mechanises a document-update order, and `check-deprecations.sh` deliberately does not gate on presence (D-05). Recorded, not resolved. |

<tasks>

<task type="auto">
  <name>Task 1: Annotate DEPRECATIONS.md — the zero is the outcome, and the four Open Questions close</name>
  <files>.project/Milestone_4-Refactor-Crates-Features/Epic_2/DEPRECATIONS.md</files>
  <read_first>
    - `.project/Milestone_4-Refactor-Crates-Features/Epic_2/DEPRECATIONS.md` (full file — the H1 and `**Created:** 2026-04-15` header; the Deprecation Timeline at `:13-18`; the ⚠ IMMEDIATE DEPRECATION section at `:81` whose Manager Services category reads "None identified yet - managers are currently pub(crate) or will be moved to application layer (Epic 3)"; §"Current Status" at `:171` with its "⏳ Adding deprecation warnings (Task 3.0)" / "⏳ Curating explicit exports (Task 6.0)" in-progress markers and "Deprecated Items: 0 (none yet)"; §"Deprecation Log" at `:190` reading "*No deprecations added yet.*"; §"Open Questions" at `:206-211`)
    - `.planning/decisions/0022-deprecation-requirement-withdrawal.md` (the decision this annotation cites — read it in full before writing a word)
    - `.project/Milestone_4-Refactor-Crates-Features/Epic_1/prd-expand-feature-flags.md` lines 1-11 and 122-140 (**the mandatory markup analog** — banner shape, `~~struck~~` + `**Corrected**`, and the `**Confirmed**` variant for a claim that is right-as-written and only needs annotation)
    - `.planning/phases/08-verified-defect-closure/08-PATTERNS.md` §"`.project/` 6 documents — dated correction banners"
  </read_first>
  <action>
    Annotate — do not rewrite. Nothing in this file is deleted.

    **Document-level banner**, immediately below the H1: a dated `2026-08-06` blockquote naming
    DEBT-02 and ADR-0022, recording that Milestone 4 Epic 2 FR-8 is **withdrawn**, that this
    document's zeros ("Deprecated Items: 0 (none yet)", the empty Deprecation Log) are the
    **outcome** of the epic's own decisions rather than a gap, and that the file's original text is
    retained below with inline annotations and nothing is deleted. State the evidence in the banner:
    the ⚠ IMMEDIATE DEPRECATION category — the only one that would produce a `#[deprecated]`
    attribute — names no candidate; the SOFT DEPRECATION category resolves to `#[doc(hidden)]` and
    the tree carries 38 such occurrences, so that half *was* executed; the INTERNAL-ONLY category
    resolves to `pub(crate)`, annotation-free by design. Re-run
    `grep -rn '#\[deprecated' src crates | wc -l` and `grep -rn 'doc(hidden)' src crates | wc -l`
    and cite the counts you get, not the ones written here.

    **Deprecation Timeline (`:13-18`)** — the `v0.1.0 → v0.2.0 → v0.3.0 → v1.0.0` schedule is stale
    by five minor versions against the shipped `0.7.0` (root `Cargo.toml:34`, re-verify). Strike it,
    retain it, and append a dated correction restating it per ADR-0022 / D-08: the pre-1.0 series
    absorbs API evolution through minor bumps per ADR-0008, so a future deprecation's removal window
    is **"one minor version"** rather than a named release that already shipped.

    **⚠ IMMEDIATE DEPRECATION (`:81` onward)** — use the `**Confirmed**` variant, not the struck
    variant: the "None identified yet" text is *correct as written* and needs no strike. Append a
    dated annotation recording that this is now the final answer rather than a pending state, citing
    ADR-0022.

    **§"Current Status" (`:171`)** — the two `⏳` in-progress markers (Task 3.0 "Adding deprecation
    warnings", Task 6.0 "Curating explicit exports") are the sentences that make this document read
    as an unfinished task list. Annotate the deprecation-warnings marker as **closed by withdrawal**
    per ADR-0022. For the Task 6.0 export-curation marker, record its state truthfully rather than
    absorbing it into this withdrawal: ADR-0022 withdraws FR-8's deprecation requirement, not the
    export-curation task, so annotate it as **outside DEBT-02's scope, state unchanged** and name it
    in the SUMMARY as an item this phase did not adjudicate.

    **§"Deprecation Log" (`:190`)** — `**Confirmed**` variant: "*No deprecations added yet.*" is
    accurate; annotate that it is the terminal state, citing ADR-0022, and that the log's format
    block below it is retained for use if a future ADR supersedes 0022.

    **§"Open Questions" (`:206-211`)** — close all four with a one-line disposition each, per
    D-07(1). Suggested dispositions, to be verified before writing: (1) *Adapter Visibility
    Strategy* — resolved, `#[doc(hidden)]` was the approach taken, N occurrences confirmed tree-wide
    by the grep above; (2) *Factory Functions*, (3) *Prelude Module*, (4) *Manager Refactoring* —
    each moot under ADR-0022, because no deprecation timeline exists to schedule them against. If
    question 3 (a `paladin::prelude::*` module) is in fact **answered by shipped code** — the tree
    carries `src/prelude.rs` — record that as its disposition instead of "moot", and cite the file.
    Verify before writing; do not assert either way from this plan's text.
  </action>
  <verify>
    <automated>grep -q 'ADR-0022' .project/Milestone_4-Refactor-Crates-Features/Epic_2/DEPRECATIONS.md && grep -q '2026-08-06' .project/Milestone_4-Refactor-Crates-Features/Epic_2/DEPRECATIONS.md && test "$(grep -rn '#\[deprecated' src crates | wc -l)" = "0" && echo RECONCILED</automated>
  </verify>
  <acceptance_criteria>
    - The file contains a blockquote banner within its first 20 lines naming `2026-08-06`, `DEBT-02` and `ADR-0022`.
    - `grep -c 'ADR-0022' <file>` is ≥ 5 — the banner plus at least one citation at each of the four annotated sections.
    - `grep -c '~~' <file>` is ≥ 1 — the stale Deprecation Timeline is struck and retained.
    - All four Open Questions carry a one-line dated disposition: `sed -n '/^## Open Questions/,/^---/p' <file>` shows four numbered items each followed by a `**Closed`/`**Resolved`/`**Moot` annotation.
    - The original text is intact: `grep -c 'None identified yet' <file>` returns ≥ 1 and `grep -c 'No deprecations added yet' <file>` returns ≥ 1 — the `**Confirmed**` variant annotates without striking.
    - `git diff --numstat <file>` shows **0 deleted lines** that do not reappear struck in the same hunk.
    - `grep -rn '#\[deprecated' src crates | wc -l` returns `0` — the tree is unchanged and no deprecation was manufactured.
    - The SUMMARY names the Task 6.0 export-curation marker as an item annotated but **not** adjudicated by this phase.
  </acceptance_criteria>
  <done>`DEPRECATIONS.md` reads as a completed decision record rather than an unfinished task list: banner, stale timeline struck and restated against 0.7.0, three sections annotated, four Open Questions closed, ADR-0022 cited throughout, zero text deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the one false present-tense claim in stable-api.md, keep the policy</name>
  <files>docs/src/api-reference/stable-api.md</files>
  <read_first>
    - `docs/src/api-reference/stable-api.md:868-880` (the "Related Documentation" list — the defective line is `:875`, `- **[Deprecations Tracking](…/CHANGELOG.md)** - Current and planned deprecations`)
    - `docs/src/api-reference/stable-api.md:75-85` (the policy sentence "Types/methods being removed will be deprecated for at least one minor version before removal" at `:79` — forward-looking, correct, **must survive**)
    - `docs/src/api-reference/stable-api.md:180-190` (the 🔴 Deprecated tier at `:183` — **must survive**)
    - `docs/src/api-reference/stable-api.md:335-342` (the `check-deprecations.sh` reference at `:338` — the script now works, per plan 08-02; verify the sentence is still true and annotate only if it is not)
    - `docs/src/api-reference/stable-api.md:390-400` (the "how long are deprecated APIs supported" FAQ at `:395` — policy, **must survive**)
    - `docs/src/api-reference/stable-api.md:770-830` (the "Deprecation Lifecycle" section at `:772` and the hypothetical `#[deprecated(since = "0.2.0", …)]` illustration at `:798-827`, explicitly framed as a "Version 0.2.0" example — **must survive**; it is an illustration, not a claim about today's tree)
    - `.planning/decisions/0022-deprecation-requirement-withdrawal.md`
    - `.planning/phases/08-verified-defect-closure/08-RESEARCH.md` §"`stable-api.md`'s one concrete 'current state' overclaim"
  </read_first>
  <action>
    This is live mdbook source, not `.project/` — edit it as prose. Do **not** use strike-through
    annotation markup here.

    Run `grep -n 'deprecat' docs/src/api-reference/stable-api.md` first and read every hit. Research
    found ~40 hits, of which exactly **one** makes a present-tense claim that is false today: line
    `:875`'s link description "Current and planned deprecations". Correct that description so it
    states what is true — that the deprecation *process* is documented and that **no deprecation is
    currently active**, with a pointer to ADR-0022 for the reason. Keep the link target
    (`CHANGELOG.md`) unchanged so the mdbook linkcheck target set does not move.

    Then, per D-07(2), add a short forward-looking note where a reader most needs it — at or
    adjacent to the "Deprecation Lifecycle" section (`:772`) — stating that the lifecycle policy
    stands and governs any future deprecation, that Milestone 4 Epic 2 FR-8's requirement to
    deprecate the existing transitional surface was withdrawn by ADR-0022 on 2026-08-06, and that
    the tree therefore carries zero `#[deprecated]` attributes today. Cite the grep and its count.
    Where the section's version anchors name `v0.2.0`/`v0.3.0`, state the ADR-0022 restatement: the
    pre-1.0 series absorbs API evolution through minor bumps per ADR-0008, so the removal window is
    "at least one minor version" rather than a named release that already shipped. Keep the
    `#[deprecated(since = "0.2.0", …)]` code illustration exactly as it is and make clear in the
    surrounding prose that it is an example of the *form*, not a record of an active deprecation.

    Verify the `check-deprecations.sh` reference at `:338` still describes the script accurately
    after plan 08-02's repair (the script now scans `src` and `crates` and can genuinely fail on a
    malformed attribute). If the sentence over- or under-claims, correct it; if it is accurate,
    leave it and record that you checked.

    **Do not delete** the Deprecation Lifecycle section, the 🔴 Deprecated tier, the FAQ entry, or
    the illustration. The policy is not deleted; the framework will need it (D-07(2)).
  </action>
  <verify>
    <automated>test "$(grep -c 'Current and planned deprecations' docs/src/api-reference/stable-api.md)" = "0" && grep -q 'ADR-0022' docs/src/api-reference/stable-api.md && grep -q 'Deprecation Lifecycle' docs/src/api-reference/stable-api.md && echo CORRECTED</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Current and planned deprecations' docs/src/api-reference/stable-api.md` returns `0` — the false present-tense claim is gone.
    - `grep -c 'ADR-0022' docs/src/api-reference/stable-api.md` is ≥ 1 — the reader can reach the recorded reason.
    - `grep -c 'Deprecation Lifecycle' docs/src/api-reference/stable-api.md` is ≥ 1 and `grep -c '🔴 Deprecated' docs/src/api-reference/stable-api.md` is ≥ 1 — the policy and the tier survive.
    - `grep -c 'deprecated(since' docs/src/api-reference/stable-api.md` is unchanged from its pre-task value — the illustration was not deleted.
    - `grep -c '~~' docs/src/api-reference/stable-api.md` is unchanged from its pre-task value — no `.project/` strike-through markup leaked into the published book.
    - The set of link targets is unchanged: `grep -o '](\([^)]*\))' docs/src/api-reference/stable-api.md | sort -u` produces the same list before and after (VALIDATION.md row 2b's structural mitigation, since linkcheck cannot run locally).
    - The SUMMARY records the before/after text of `:875` and states explicitly that `mdbook`/`mdbook-linkcheck` are unavailable in this environment, so row 2b is satisfied structurally (no link target moved, no page added) rather than by execution.
  </acceptance_criteria>
  <done>`stable-api.md` no longer claims deprecations exist today, names ADR-0022 as the reason, keeps its entire forward-looking deprecation policy, and moved no link target.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the three-way agreement</name>
  <files>.planning/phases/08-verified-defect-closure/08-06-SUMMARY.md</files>
  <read_first>
    - `.planning/decisions/0022-deprecation-requirement-withdrawal.md` (all seven sections)
    - `.project/Milestone_4-Refactor-Crates-Features/Epic_2/DEPRECATIONS.md` (as annotated by task 1)
    - `docs/src/api-reference/stable-api.md` (as corrected by task 2 — the `:875` line, the Deprecation Lifecycle section, the 🔴 tier, the FAQ)
    - `.planning/phases/08-verified-defect-closure/08-VALIDATION.md` §"Manual-Only Verifications", row 1 (why this is a reading and not a grep, and the exact instructions)
    - `.planning/REQUIREMENTS.md` lines 874-885 (DEBT-02's "no third state" clause)
  </read_first>
  <action>
    Perform the reconciliation check that no grep can perform, and record it. VALIDATION.md marks
    this **`human_judgment: true`**: the "no third state" clause is about *agreement between prose
    documents*, and only a reading establishes it.

    Read all three documents end to end and confirm, in writing, each of these five points, quoting
    the sentence in each document that establishes it:
    1. Each states that **zero** `#[deprecated]` attributes exist in the tree today.
    2. Each names the withdrawal of Milestone 4 Epic 2 FR-8 and cites **ADR-0022** by number.
    3. Each gives the same reason — the epic's own ⚠ IMMEDIATE DEPRECATION category named no
       candidate, so the zero is the outcome of the epic's decisions, not an unfinished task.
    4. `stable-api.md` **keeps** its forward-looking deprecation policy while carrying no claim that
       deprecations exist today.
    5. None of the three promises a removal timeline the tree cannot start — the stale
       `v0.2.0 → v0.3.0 → v1.0.0` anchors are restated as "at least one minor version" per ADR-0008
       everywhere they appear.

    Then run the mechanical half and capture it verbatim: `grep -rn '#\[deprecated' src crates | wc -l`
    (expect `0`) and `bash scripts/check-deprecations.sh` (expect exit 0 with the success message —
    proving that plan 08-02's repaired gate agrees with this plan's outcome rather than contradicting
    it, which is what would happen if a "deprecations must exist" check had been added).

    If any of the five points fails, fix the document that disagrees before writing the SUMMARY. A
    partial agreement is the third state the requirement forbids.

    Record for plan 08-09: the ledger row this plan's evidence changes is
    `.planning/ledgers/milestone-04-06.md:116` `REQ-deprecation-warnings`, currently
    `genuinely outstanding`. Do not amend the ledger here.
  </action>
  <verify>
    <human-check>
Read `.planning/decisions/0022-deprecation-requirement-withdrawal.md`,
`.project/Milestone_4-Refactor-Crates-Features/Epic_2/DEPRECATIONS.md` and
`docs/src/api-reference/stable-api.md`. Confirm all three state zero current deprecations, name the
withdrawal, and cite ADR-0022; and that `stable-api.md` keeps its forward-looking deprecation policy
while carrying no claim that deprecations exist today. Confirm no document promises a removal
timeline keyed to a release that already shipped.
    </human-check>
    <automated>test "$(grep -rn '#\[deprecated' src crates | wc -l)" = "0" && bash scripts/check-deprecations.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn '#\[deprecated' src crates | wc -l` returns `0` (ROADMAP criterion 2, mechanical half; VALIDATION.md row 2).
    - `bash scripts/check-deprecations.sh` exits 0 — the repaired gate and this plan's outcome agree.
    - The SUMMARY contains a five-point agreement table, each point quoting the establishing sentence from **each** of the three documents by `file:line` (VALIDATION.md Manual-Only row 1).
    - `grep -c 'v0.3.0' docs/src/api-reference/stable-api.md` and the same over `DEPRECATIONS.md`: every remaining occurrence is either inside a struck span, inside the explicitly-hypothetical illustration, or accompanied by the ADR-0022 restatement — enumerate each in the SUMMARY.
    - The SUMMARY names `.planning/ledgers/milestone-04-06.md:116` (`REQ-deprecation-warnings`) as the row plan 08-09 must amend, with the evidence that changes its verdict.
    - The SUMMARY states plainly that `mdbook`/`mdbook-linkcheck` are unavailable in this environment and that VALIDATION.md row 2b is satisfied structurally, not by execution.
  </acceptance_criteria>
  <done>All three documents tell one story about deprecation state, proven by a recorded five-point reading plus the zero-count grep and a passing `check-deprecations.sh`; there is no third state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Published documentation → downstream framework consumer | `docs/src/api-reference/stable-api.md` is mdbook source published to framework consumers. It is the surface on which a consumer decides whether an API they depend on is scheduled for removal. This plan edits that surface. |

## STRIDE Threat Register

Assessed at ASVS level 1, blocking on `high`. RESEARCH.md §"Security Domain" assessed this phase as
introducing no new dependency, network surface, credential or user-input handling. V5 (Input
Validation) and V6 (Cryptography) are not applicable and are not padded into the table.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06-01 | Repudiation | API-governance record — a published deprecation policy withdrawn without an attributable reason | medium | mitigate | Withdrawing a published deprecation requirement is an API-governance change with a downstream consumer audience. Mitigation: ADR-0022 (plan 08-04) records the decision, its evidence and its rejected options with a date; all three artefacts cite it by number; and task 3's five-point reading verifies the citation actually landed in each. |
| T-08-06-02 | Information Disclosure | `docs/src/api-reference/stable-api.md` misleading a consumer into believing an API they depend on is scheduled for removal | low | mitigate | The single false present-tense claim at `:875` is corrected, and the forward-looking policy is retained and explicitly labelled forward-looking, so a consumer reads "the process exists, nothing is currently deprecated" rather than "something is being removed and I do not know what". |
| T-08-06-03 | Tampering | Documentation link integrity (mdbook linkcheck is an error-level gate per M11) | low | accept | Neither task adds a page or moves a link target, and the acceptance criteria assert the link-target set is byte-identical before and after. `mdbook-linkcheck` cannot be run locally (not installed; CI installs it via `cargo install`, which requires crates.io — HTTP 403 here), so this is accepted with a structural argument recorded, not claimed as executed. |
| T-08-06-04 | Tampering | Supply chain — package-manager installs | n/a | accept | No dependency change of any kind, and explicitly **no** `cargo install mdbook-linkcheck` attempt. RESEARCH.md's Package Legitimacy Audit records "Not applicable". No `[ASSUMED]`/`[SUS]` package exists, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- ROADMAP criterion 2, mechanical: `grep -rn '#\[deprecated' src crates | wc -l` → `0`.
- ROADMAP criterion 2, agreement (`human_judgment: true`): the five-point reading recorded in the
  SUMMARY, quoting each establishing sentence by `file:line` from all three documents.
- `bash scripts/check-deprecations.sh` exits 0 — tooling agrees with the outcome.
- `stable-api.md` retains its Deprecation Lifecycle section, its 🔴 Deprecated tier, its FAQ entry
  and its `#[deprecated(since = …)]` illustration; its link-target set is unchanged.
- `DEPRECATIONS.md` has zero net deletions; four Open Questions carry dated dispositions.
- Workspace gate (CLAUDE.md): `cargo test --offline --workspace` → `cargo fmt --check` →
  `cargo clippy --workspace -- -D warnings`, all exit 0.
</verification>

<success_criteria>
- Three documents, one story: zero current deprecations, the withdrawal named, ADR-0022 cited.
- The forward-looking policy preserved; only the false present-tense claim corrected.
- Four Open Questions closed with dated dispositions.
- Zero `#[deprecated]` attributes added; zero `.project/` text deleted.
- The Task 6.0 export-curation marker recorded as annotated-but-not-adjudicated, not silently
  absorbed.
- Every closure claim in the SUMMARY carries the verbatim command or `file:line` that produced it
  (D-00e / D-21).
</success_criteria>

<output>
Create `.planning/phases/08-verified-defect-closure/08-06-SUMMARY.md` when done.
Record verbatim: the `grep -rn '#\[deprecated' src crates | wc -l` and
`grep -rn 'doc(hidden)' src crates | wc -l` counts; the before/after text of `stable-api.md:875`; the
four Open Question dispositions; the five-point three-way agreement table with quoted sentences and
`file:line` for each document; the `check-deprecations.sh` exit; the mdbook-linkcheck unavailability
note; and `.planning/ledgers/milestone-04-06.md:116` as the row plan 08-09 must amend.
</output>
