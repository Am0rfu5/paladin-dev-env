---
phase: 08-verified-defect-closure
plan: 08
type: execute
wave: 3
depends_on: ["08-07"]
files_modified:
  - Dockerfile
  - Dockerfile.chef
  - .github/workflows/feature-flags.yml
  - docs/src/deployment/docker.md
  - CHANGELOG.md
  - .planning/decisions/0023-cli-dependency-isolation.md
autonomous: true
requirements: [DEBT-04]

must_haves:
  truths:
    - "Nothing downstream still assumes the `paladin` binary builds by default: both Dockerfiles pass `--features cli`, the CI step whose assertion inverted is repurposed to assert the gate, and the mdbook deployment page matches the Dockerfile it documents."
    - "ROADMAP criterion 4 is proved by **running the command**, not by reading the manifest (D-16): `cargo tree --offline --no-default-features | grep -E 'structopt|colored|comfy-table'` produces no output, captured verbatim in the SUMMARY and in ADR-0023."
    - "The library-only build **compiles** before `cargo tree` is trusted — `cargo build --offline --lib --no-default-features` exits 0 (RESEARCH.md Pitfall 2: a `cargo tree` that errors instead of printing a tree is not a passing criterion)."
    - "Both user-visible consequences reach `CHANGELOG.md`: `cargo run` no longer builds the `paladin` binary without `--features cli`, and `paladin-herald`'s table formatter and coloured markdown path moved behind features."
    - "ADR-0023's single `PENDING` evidence slot is replaced with the captured `cargo tree` output, so the decision record carries its own proof (D-15)."
    - statement: "No untracked build path still invokes the `paladin` binary unguarded — every `--bin paladin`, `bin/paladin` and `./paladin` reference in tracked automation has been re-read and either updated or explicitly confirmed unaffected."
      verification: backstop
  artifacts:
    - "Dockerfile — `RUN cargo build --release --workspace --bin paladin --features cli`."
    - "Dockerfile.chef — the same on its build stage."
    - ".github/workflows/feature-flags.yml — the step formerly named \"Verify paladin binary builds without cli feature\" repurposed to assert the gate holds."
    - "docs/src/deployment/docker.md — the documented build command matching the shipped Dockerfile."
    - "CHANGELOG.md — `[Unreleased] ### Changed` entries for the binary gate and the Herald feature split."
    - ".planning/decisions/0023-cli-dependency-isolation.md — the `PENDING` slot replaced with the verbatim criterion-4 proof."
  key_links:
    - "`.github/workflows/feature-flags.yml:144` is a step **literally named** \"Verify paladin binary builds without cli feature\" running `cargo build --bin paladin`. Plan 08-07 inverted its assertion; if it is not repaired, CI fails on the first push after that plan lands."
    - "`docs/src/deployment/docker.md:135,146,156` documents the exact `Dockerfile` build/copy/entrypoint commands as source-of-truth prose. `docs/book/html/deployment/docker.html` is a GENERATED artefact and needs no manual edit — it regenerates on `mdbook build`."
    - "ADR-0023's `## Code Locations` section carries the criterion-4 command with an explicitly-marked `PENDING` output line, written by plan 08-04; this plan fills it."
  prohibitions:
    - "Do not modify `.github/workflows/ci.yml:148`, `:393` or `:792` (`actions-rs/toolchain@v1`) — Phase 15 / PIPE-04 owns the full eight-reference action-modernization sweep."
    - "Do not converge `VisionTokenUsage` (`crates/paladin-ports/src/output/vision_port.rs:34`) onto the canonical `TokenUsage`."
    - "Do not audit, re-fence or un-ignore the 87 pre-existing `ignore`/`no_run`/`text` doc fences in `paladin-ports` — Phase 16 / DOCS-03 owns them."
    - "Do not decide which `cargo doc --workspace --no-deps` warning bar governs — Phase 10 / HARD-07 owns that question."
    - "Do not retire or delete `src/main.rs`."
    - "Do not build a `cargo tree` dependency-allowlist check into CI — Phase 15 owns that, from ADR-0015. Criterion 4's proof is a one-off recorded command."
    - "Do not edit `docs/book/html/**` — it is generated output, not source."
    - "Do not fix the four pre-existing unrelated defects (`Dockerfile.chef`'s `health` CMD, `Makefile:307`'s nonexistent `docker/Dockerfile`, `docker-compose.dev.yml:36`'s bare `cargo run`, `docker/redis/Dockerfile`'s orphaned `CMD`). Record each; fix none."
---

<objective>
Close DEBT-04: update every downstream surface that assumed the `paladin` binary builds by default,
record both user-visible changes in `CHANGELOG.md`, and prove ROADMAP criterion 4 by running the
command and capturing its output into ADR-0023.

Purpose: plan 08-07's binary-gate change is a manifest-plus-source edit whose blast radius is Docker
build commands, a CI step name and a documentation page — none of which `cargo test` or
`cargo clippy` will catch. RESEARCH.md flagged this as Pitfall 3: a green `cargo test --workspace`
alongside a red Docker build and a red `feature-flags.yml` run, in the same pull request that
"only" touched `src/main.rs` and `Cargo.toml`. A missed Dockerfile stage is how this change breaks
the release pipeline.

Output: four repaired build surfaces, two changelog entries, and a criterion-4 proof recorded
verbatim in both the SUMMARY and the ADR that authorised the change.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-verified-defect-closure/08-CONTEXT.md
@.planning/phases/08-verified-defect-closure/08-RESEARCH.md
@.planning/phases/08-verified-defect-closure/08-VALIDATION.md
@.planning/decisions/0023-cli-dependency-isolation.md
</context>

## Execution notes

- **Plan 08-07 must have landed.** This plan's whole premise is that the binary gate is already in
  the tree; running it first would repair surfaces that are not yet broken.
- **`docker build` is not runnable in this environment** and is not required. The Dockerfile edits
  are verified by reading the changed lines and by asserting the corresponding `cargo build`
  invocation succeeds locally with the same flags — not by building an image. Say so plainly in the
  SUMMARY rather than implying an image was built.
- **`mdbook` is not installed here** (CI installs `mdbook-linkcheck` via `cargo install`, which needs
  crates.io — HTTP 403 in this environment). The `docs/src/deployment/docker.md` edit changes prose
  inside a fenced code block and moves no link target, so linkcheck risk is structurally nil; record
  that, do not claim a green linkcheck.
- **Workspace gate** (CLAUDE.md) before committing: `cargo test` → `cargo fmt --check` →
  `cargo clippy -- -D warnings`. Prefer `--offline`. **`worktree_skip_hooks=true`** — run the gate
  explicitly, then commit with hooks skipped.
- **Evidence bar (D-00e / D-21):** the criterion-4 output goes into the SUMMARY *and* the ADR
  verbatim, not paraphrased.

## Artifacts this phase produces (this plan's share)

| Symbol / path | Kind | Note |
|---|---|---|
| `--features cli` on the `paladin` build in `Dockerfile` and `Dockerfile.chef` | build-command change | two stages, one line each |
| a repurposed `feature-flags.yml` step asserting the gate | CI assertion inversion | the step's name and body both change |
| `CHANGELOG.md` `[Unreleased] ### Changed` × 2 | release record | the binary gate and the Herald feature split |
| the filled criterion-4 evidence line in ADR-0023 | ADR completion | replaces the single `PENDING` marker |

## Flagged planner assumptions — unresolved edge probes (DEBT-04, downstream half)

No `-SPEC.md` exists, so no `## Edge Coverage` section governs. The deterministic edge probe
returned 14 phase-wide items, all `status: unresolved`. DEBT-04's three reconstructed items are
enumerated in plan 08-07's flagged-assumptions table (E-04b…E-04d) and are not duplicated here. One
additional observation belongs to this plan and is recorded rather than dropped:

| # | Category | Probe question | Disposition |
|---|---|---|---|
| E-04e | `adjacency` (build surfaces) | What happens where an *untracked* build path — a developer's local `docker build --target production`, or a deployment pipeline outside this repository — invokes the binary unguarded? | **unresolved, and matches RESEARCH.md's own `[ASSUMED]` A1/A2.** The sweep covers tracked automation only: `Dockerfile*`, `Makefile`, `docker/`, `k8s/`, `docs/`, `.github/workflows/`. A manual `docker build --target production` against `docker/testserver/Dockerfile`, or a `kubectl apply` against `k8s/deployment.yaml`'s explicitly-labelled `paladin:test` placeholder, would not appear in that grep. Recorded as the `backstop`-verified truth above; not auto-dismissed. |

<tasks>

<task type="auto">
  <name>Task 1: Repair every downstream surface that assumed the binary builds by default</name>
  <files>Dockerfile, Dockerfile.chef, .github/workflows/feature-flags.yml, docs/src/deployment/docker.md</files>
  <read_first>
    - `Dockerfile:28-40` (the builder stage: `RUN cargo build --release --workspace --bin paladin` at `:33`, then `RUN strip target/release/paladin` — and `:52,73`, the `COPY` and `ENTRYPOINT` that are downstream of it and need no change themselves)
    - `Dockerfile.chef:70-80` (the same build command at `:74`, and `:94,113,116` — `COPY`, `HEALTHCHECK CMD [".../paladin","health"]` and `ENTRYPOINT`, all downstream of it)
    - `.github/workflows/feature-flags.yml:135-150` (the `cli-isolation` job: `cargo check --lib --no-default-features` at `:139`, `cargo test --test cli_isolation` at `:142`, the step named "Verify paladin binary builds without cli feature" running `cargo build --bin paladin` at `:143-144`, and "Verify paladin-cli binary builds with cli feature" at `:146-147`)
    - `docs/src/deployment/docker.md:125-160` (the mdbook prose that reproduces the Dockerfile verbatim inside a fenced block — the build line at `:135`, the `COPY` at `:146`, the `CMD` at `:156`)
    - `.planning/phases/08-verified-defect-closure/08-RESEARCH.md` §"Full enumeration: everything that assumes the `paladin` binary builds by default" (the nine-row triage table naming which four break and which five are pre-existing, unrelated defects)
    - `.planning/phases/08-verified-defect-closure/08-VALIDATION.md` §"Manual-Only Verifications", row 2 (why this sweep is `human_judgment: true`)
  </read_first>
  <action>
    Repair the four surfaces RESEARCH.md's triage marks as **will break on merge**, and explicitly
    re-read and confirm the five it marks as pre-existing or inert.

    1. **`Dockerfile:33`** — add `--features cli` to
       `RUN cargo build --release --workspace --bin paladin`. Nothing else in this file changes; the
       `strip`, `COPY` and `ENTRYPOINT` lines are downstream of the build and remain correct.
    2. **`Dockerfile.chef:74`** — the identical addition. Note that this file's `cargo chef cook`
       stage caches dependencies; if that stage passes an explicit feature set, add `cli` there too
       so the cache and the build agree — read the cook stage before deciding, and record what you
       found either way.
    3. **`.github/workflows/feature-flags.yml:143-144`** — this step's assertion has **inverted**:
       "Verify paladin binary builds without cli feature" is now false by design. Do not delete the
       step; repurpose it to assert the gate holds, which is a stronger guard than the one it
       replaces. Rename it to something like "Verify paladin binary requires the cli feature" and
       change its body so the step **fails if the un-featured build succeeds** — for example a shell
       body that runs `cargo build --bin paladin` and exits non-zero when that command exits zero.
       Leave `:139`'s `cargo check --lib --no-default-features` exactly as it is: it now also guards
       plan 08-07's Herald consumer gating, which is precisely the check RESEARCH.md's Pitfall 2
       calls for. Leave `:142`'s `cargo test --test cli_isolation` and `:146-147`'s `paladin-cli`
       step unchanged.
    4. **`docs/src/deployment/docker.md:135`** — update the documented build command inside the
       fenced block so it matches the shipped `Dockerfile` exactly. Then read `:146` and `:156` and
       confirm they still match the shipped `COPY` and `CMD`; correct only if they diverge. Add a
       short sentence of surrounding prose noting that the `paladin` binary requires the `cli`
       feature, so a reader building their own image does not reproduce the old command from memory.
       Do **not** edit `docs/book/html/deployment/docker.html` — it is generated output.

    Then re-read, and record a one-line disposition for, each of the five surfaces RESEARCH.md marks
    as not requiring a change: `docker/testserver/Dockerfile`'s `production` stage (confirmed
    unreferenced by `docker-compose.test.yml`, which targets the `test` stage); `docker/redis/
    Dockerfile:107`'s orphaned `CMD ["./paladin"]`; `k8s/deployment.yaml:65,68`'s explicitly-labelled
    `paladin:test` placeholder; `Makefile:307`'s reference to a nonexistent `docker/Dockerfile`; and
    `docker-compose.dev.yml:36`'s bare `cargo run` against three `[[bin]]` targets with no
    `default-run`. Re-run the enumerating grep yourself —
    `grep -rnE '\-\-bin paladin\b|bin/paladin\b|\./paladin\b' Dockerfile* Makefile docker/ k8s/ docs/src/ .github/workflows/` —
    and reconcile every hit against the triage table. **Fix none of the five.** Record each with its
    `file:line` and the one-line reason it is out of DEBT-04's scope; naming a defect without fixing
    it is this corpus's house style, and fixing them is new scope.
  </action>
  <verify>
    <human-check>
Re-read `Dockerfile:33`, `Dockerfile.chef:74`, `.github/workflows/feature-flags.yml`'s cli-isolation
job, and `docs/src/deployment/docker.md:135,146,156`. Confirm each either passes `--features cli`
where it builds the `paladin` binary, or is a step whose assertion has been deliberately repurposed
to guard the new gate. Confirm the five out-of-scope surfaces are each named with a reason and left
unfixed. Confirm no `docs/book/html/**` file was edited.
    </human-check>
    <automated>grep -c 'cargo build --release --workspace --bin paladin --features cli' Dockerfile Dockerfile.chef && ! grep -qE '^\s+run: cargo build --bin paladin\s*$' .github/workflows/feature-flags.yml && echo SURFACES-REPAIRED</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\-\-bin paladin --features cli' Dockerfile` returns `1` and the same for `Dockerfile.chef`.
    - `grep -rnE '\-\-bin paladin\b' Dockerfile Dockerfile.chef | grep -v 'features cli'` produces **no output** — no build of that binary remains un-featured in either Dockerfile.
    - `.github/workflows/feature-flags.yml` no longer contains a step whose name asserts the binary builds *without* the cli feature: `grep -c 'builds without cli feature' .github/workflows/feature-flags.yml` returns `0`.
    - The replacement step's body fails when `cargo build --bin paladin` succeeds — verify locally by running that body's logic and confirming it exits non-zero against the current (gated) tree only when the build unexpectedly succeeds, and exits zero as the tree stands.
    - `grep -c 'cargo check --lib --no-default-features' .github/workflows/feature-flags.yml` still returns `1` — the Herald-consumer guard is intact.
    - `docs/src/deployment/docker.md`'s build line is byte-identical to `Dockerfile:33`'s command: `diff <(grep -o 'cargo build --release --workspace --bin paladin[^"]*' Dockerfile) <(grep -o 'cargo build --release --workspace --bin paladin[^"]*' docs/src/deployment/docker.md)` produces no output.
    - `git status --short docs/book/` is empty — no generated file was edited.
    - `git status --short docker/ k8s/ Makefile docker-compose*.yml` is empty — none of the five out-of-scope surfaces was touched.
    - The SUMMARY carries the full re-run grep output reconciled row by row against RESEARCH.md's nine-row triage table, with a disposition for every hit.
  </acceptance_criteria>
  <done>Both Dockerfiles build the binary with `--features cli`; the inverted CI step now guards the gate instead of contradicting it; the deployment page matches the Dockerfile it documents; the five out-of-scope surfaces are named, reasoned and untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Record both user-visible changes in CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - `CHANGELOG.md:1-40` (the Keep-a-Changelog header, the `## [Unreleased]` section and its existing `### Added` / `### Changed` subsections — note the house style: a bolded lead clause, then the consequence spelled out for a downstream consumer in prose, as the Grove `routing_model` entries demonstrate)
    - `.planning/decisions/0023-cli-dependency-isolation.md` (both sites, and the user-visible consequences it records)
    - `.planning/phases/08-verified-defect-closure/08-07-SUMMARY.md` (the chosen feature names, the exact gating, and the `Unknown formatter` behaviour change recorded there for this entry)
    - `.planning/phases/08-verified-defect-closure/08-VALIDATION.md` §"Manual-Only Verifications", row 3 (what a downstream consumer needs told)
  </read_first>
  <action>
    Add two entries under `## [Unreleased]` → `### Changed`, in the house style already used in that
    section: a bolded lead clause naming the change, then prose spelling out what a downstream
    consumer must do differently. Do not create a new version heading; this is `[Unreleased]` work.

    **Entry 1 — the `paladin` binary now requires the `cli` feature.** State that
    `cargo run` and `cargo build --bin paladin` no longer build that binary without
    `--features cli`, that this makes all three `[[bin]]` targets consistent with ADR-0019's
    three-binary architecture, and that the remedy is to add `--features cli` (giving the literal
    command). Note that `Dockerfile` and `Dockerfile.chef` were updated accordingly. Mention that
    `structopt` was removed from the workspace entirely and `src/main.rs` migrated to `clap` v4, with
    identical flags (`-c` / `--config`, default `config.yml`) and an unchanged binary name
    (`smartcontent-aggregator`) — a consumer's invocation does not change, only the feature
    requirement does. Cite ADR-0023.

    **Entry 2 — `paladin-herald`'s formatters moved behind features.** State that `paladin-herald`
    gained its first `[features]` section with `default = []`, that `TableHerald` is now behind the
    `table` feature and the coloured Markdown rendering path behind `color`, and that `JsonHerald`
    and `MarkdownHerald` remain available in a default build. State the consumer remedy: add
    `features = ["table", "color"]` to the `paladin-herald` dependency, or depend on the root
    `paladin` crate's `cli` feature, which enables both. State the two facade-level consequences a
    consumer can observe: `paladin::infrastructure::adapters::herald::TableHerald` is available only
    under the root `cli` feature, and `Settings::create_default_herald()` with
    `herald.default_formatter = "table"` returns the existing `Unknown formatter` error in a build
    without `cli`. Note that `paladin-herald` is published on crates.io and this is a default-API
    change. Cite ADR-0023.

    Both entries must be readable by someone who has never seen this planning corpus — name the
    commands and the feature names, not the plan numbers.
  </action>
  <verify>
    <human-check>
Read the two new `CHANGELOG.md` entries as a downstream consumer who has not read this repository's
planning documents. Confirm each states what changed, why it matters to them, and the exact remedy
command or feature flag. Confirm entry 1 covers `cargo run` no longer building `paladin` without
`--features cli`, and entry 2 covers `paladin-herald`'s formatters moving behind features.
    </human-check>
    <automated>grep -c 'ADR-0023' CHANGELOG.md && grep -c 'required-features\|--features cli' CHANGELOG.md && grep -c 'paladin-herald' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `## [Unreleased]` → `### Changed` contains exactly two new entries, added at the section level rather than as a new version heading: `git diff CHANGELOG.md` shows additions only under `[Unreleased]`.
    - `grep -c 'ADR-0023' CHANGELOG.md` is ≥ 1 — a reader can reach the recorded decision.
    - Entry 1 contains the literal `--features cli` and the literal `structopt`, and names `clap`.
    - Entry 2 contains the literals `paladin-herald`, `TableHerald`, `table` and `color`, and states that `JsonHerald` and `MarkdownHerald` remain in the default build.
    - Neither entry references a GSD plan number, phase number or `.planning/` path — the changelog is a consumer-facing document.
    - `head -8 CHANGELOG.md` is unchanged — the Keep-a-Changelog preamble was not disturbed.
  </acceptance_criteria>
  <done>`CHANGELOG.md` `[Unreleased] ### Changed` carries two consumer-readable entries covering the binary gate and the Herald feature split, each with its remedy and a citation to ADR-0023.</done>
</task>

<task type="auto">
  <name>Task 3: Prove criterion 4 by running the command and record it in ADR-0023</name>
  <files>.planning/decisions/0023-cli-dependency-isolation.md</files>
  <read_first>
    - `.planning/decisions/0023-cli-dependency-isolation.md` §"Code Locations" (the criterion-4 command and its single `Output: PENDING — filled by plan 08-08 once the code lands.` line)
    - `.planning/phases/08-verified-defect-closure/08-VALIDATION.md` rows 4, 4b, 4c, 4d (the four commands and their expected outputs)
    - `.planning/REQUIREMENTS.md` lines 910-921 (DEBT-04 in full — its done-condition names `cargo tree --lib --no-default-features` specifically)
    - `.planning/phases/08-verified-defect-closure/08-RESEARCH.md` §"Common Pitfalls" Pitfall 2 (why the build must precede the tree)
    - `.planning/phases/08-verified-defect-closure/08-CONTEXT.md` D-16 (if the flag combination in the requirement text does not exist as written, record the equivalent invocation used and why, rather than silently substituting)
  </read_first>
  <action>
    Run the criterion-4 proof in this order, capturing every command's full output verbatim.

    1. `cargo build --offline --lib --no-default-features` — **must exit 0 first**. RESEARCH.md's
       Pitfall 2: a `cargo tree` that errors instead of printing a tree is not a passing criterion,
       and the failure surfaces two directories away from the manifest change that caused it.
    2. `cargo tree --offline --no-default-features | grep -E 'structopt|colored|comfy-table'` —
       must produce **no output**.
    3. `cargo build --offline --bin paladin --features cli` — must exit 0 (VALIDATION.md row 4d): the
       binary still builds when the feature is on.
    4. `grep -c structopt Cargo.toml` → `0` and `grep -rln structopt src/ crates/` → no files
       (VALIDATION.md row 4c).

    DEBT-04's requirement text names `cargo tree --lib --no-default-features`. Try that literal form
    first. If `cargo tree` rejects `--lib` in the installed cargo version, **do not silently
    substitute** (D-16): record the exact invocation you used, the error the literal form produced,
    and why the substitute is equivalent — `cargo tree --no-default-features` over the root package
    reports the same dependency graph that a `--lib`-only consumer would compile, because the root
    package's non-library targets are gated by `required-features` after plan 08-07. Write that
    reasoning into both the SUMMARY and the ADR.

    Then edit `.planning/decisions/0023-cli-dependency-isolation.md`: replace the single
    `Output: PENDING — filled by plan 08-08 once the code lands.` line with the captured evidence —
    the exact invocation used, the fact that it produced no output, the preceding successful
    `cargo build --offline --lib --no-default-features`, and the date. Keep the surrounding
    `## Code Locations` structure and the seven-heading shape intact; this is a targeted replacement
    of one marked line, not a rewrite. Change nothing else in the ADR.

    Also record, in the SUMMARY only, the two edge-probe items this proof bears on: **E-04d**
    (feature unification — the proof is a dedicated `--no-default-features` invocation precisely so a
    mixed workspace build cannot mask a leak) and **E-04e** (the sweep covers tracked automation
    only). Neither is resolved by this task; both stay flagged.
  </action>
  <verify>
    <automated>cargo build --offline --lib --no-default-features && ! (cargo tree --offline --no-default-features | grep -qE 'structopt|colored|comfy-table') && cargo build --offline --bin paladin --features cli && test "$(grep -c structopt Cargo.toml)" = "0" && echo CRITERION-4-PROVEN</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --offline --lib --no-default-features` exits 0 (VALIDATION.md row 4).
    - `cargo tree --offline --no-default-features | grep -E 'structopt|colored|comfy-table'` produces no output (VALIDATION.md row 4b — ROADMAP criterion 4).
    - `cargo build --offline --bin paladin --features cli` exits 0 (VALIDATION.md row 4d).
    - `grep -c structopt Cargo.toml` returns `0` and `grep -rln structopt src/ crates/` returns no files (VALIDATION.md row 4c).
    - `grep -c 'PENDING' .planning/decisions/0023-cli-dependency-isolation.md` returns `0` — the slot is filled.
    - `grep -c '^## ' .planning/decisions/0023-cli-dependency-isolation.md` still returns `7` and line 1 is still not `---` — the ADR's shape survived the edit.
    - `git diff --numstat .planning/decisions/0023-cli-dependency-isolation.md` shows a single-line replacement plus the added evidence block, with no other section altered.
    - If the literal `cargo tree --lib --no-default-features` form was not used, the SUMMARY and the ADR both state the invocation actually used, the error the literal form produced, and why the substitute is equivalent (D-16).
    - Workspace gate: `cargo test --offline --workspace` → `cargo fmt --check` → `cargo clippy --workspace -- -D warnings`, all exit 0, plus `cargo clippy --workspace --no-default-features -- -D warnings`.
  </acceptance_criteria>
  <done>Criterion 4 is proved by a run command, not a manifest reading: the library-only build compiles, the dependency tree contains none of the three CLI crates, the binary still builds with its feature, and ADR-0023 carries the verbatim evidence in place of its `PENDING` marker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container image build → published runtime image | `Dockerfile` and `Dockerfile.chef` produce the runtime artefact. A build command that no longer produces the binary yields a broken or stale image rather than a failed build if a `COPY` silently picks up a previous layer. |
| CI assertion → merge gate | `.github/workflows/feature-flags.yml`'s cli-isolation job is the automated guard on this phase's own invariant. Its assertion is being inverted deliberately. |
| published documentation → operator building their own image | `docs/src/deployment/docker.md` reproduces the Dockerfile as source-of-truth prose; an operator copying it verbatim gets a build command. |

## STRIDE Threat Register

Assessed at ASVS level 1, blocking on `high`. RESEARCH.md §"Security Domain" assessed this phase as
introducing no new dependency, network surface, credential or user-input handling. V5 (Input
Validation) and V6 (Cryptography) are not applicable and are not padded into the table.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-08-01 | Denial of Service | Release pipeline — a Docker build stage that no longer produces the `paladin` binary | high | mitigate | RESEARCH.md's Pitfall 3 names this exactly: a green `cargo test --workspace` alongside a red Docker build, in the PR that "only" touched `src/main.rs` and `Cargo.toml`. Two stages (`Dockerfile:33`, `Dockerfile.chef:74`) build the binary by name with no `--features cli`. Task 1 repairs both, and the acceptance criteria assert that **no** `--bin paladin` invocation in either file remains un-featured — a stronger check than "the two known lines were edited". Blocking at ASVS L1 requires mitigation, not acceptance, and it is mitigated. |
| T-08-08-02 | Tampering | CI merge gate — a step asserting the opposite of the current invariant | medium | mitigate | `feature-flags.yml:143-144` ("Verify paladin binary builds without cli feature") is now false by design. Deleting it would silently remove a guard; task 1 instead repurposes it to fail if the un-featured build succeeds, which is a *stronger* assertion than the one it replaces — the gate is now itself guarded by CI. `:139`'s `cargo check --lib --no-default-features` is explicitly preserved because it is the automated guard on plan 08-07's Herald-consumer gating. |
| T-08-08-03 | Information Disclosure | Documentation drift — an operator building from a stale documented command | low | mitigate | `docs/src/deployment/docker.md` is mdbook source and is asserted byte-identical to the shipped `Dockerfile` build line by an acceptance criterion, so drift is detectable rather than assumed absent. `docs/book/html/**` is generated and explicitly not edited. |
| T-08-08-04 | Repudiation | A criterion claimed satisfied without recorded evidence | medium | mitigate | D-16 requires criterion 4 be proved by running the command, not by reading the manifest. Task 3 captures the verbatim output into both the SUMMARY and ADR-0023, and requires the preceding `--no-default-features` **build** to succeed first, so a `cargo tree` that errored cannot be mistaken for one that printed an empty grep. |
| T-08-08-05 | Tampering | Supply chain — package-manager installs | n/a | accept | No dependency change in this plan; plan 08-07 performed the manifest edits. No `cargo install` is run, and specifically no `cargo install mdbook-linkcheck` is attempted. RESEARCH.md's Package Legitimacy Audit records "Not applicable". No `[ASSUMED]`/`[SUS]` package exists, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- ROADMAP criterion 4: `cargo build --offline --lib --no-default-features` exits 0, then
  `cargo tree --offline --no-default-features | grep -E 'structopt|colored|comfy-table'` → no
  output, both captured verbatim.
- `cargo build --offline --bin paladin --features cli` exits 0; `grep -c structopt Cargo.toml` → `0`.
- Downstream sweep (`human_judgment: true`, VALIDATION.md Manual-Only row 2): all four repaired
  surfaces re-read; all five out-of-scope surfaces named with a reason and untouched.
- `CHANGELOG.md` (`human_judgment: true`, VALIDATION.md Manual-Only row 3): two consumer-readable
  entries, each with its remedy.
- ADR-0023 has zero `PENDING` markers and still has exactly 7 `##` headings and no frontmatter.
- `git status --short docs/book/ docker/ k8s/ Makefile` is empty.
- Workspace gate (CLAUDE.md): `cargo test --offline --workspace` → `cargo fmt --check` →
  `cargo clippy --workspace -- -D warnings`, plus
  `cargo clippy --workspace --no-default-features -- -D warnings`, all exit 0.
</verification>

<success_criteria>
- Both Dockerfiles, the CI step and the deployment page repaired; the five out-of-scope surfaces
  named and untouched.
- Two consumer-readable `CHANGELOG.md` entries citing ADR-0023.
- Criterion 4 proved by a run command and recorded verbatim in the SUMMARY and in ADR-0023.
- If the requirement's literal `--lib` flag form was unavailable, the substitution is recorded with
  its reason (D-16), never made silently.
- Every closure claim in the SUMMARY carries the verbatim command that produced it (D-00e / D-21).
</success_criteria>

<output>
Create `.planning/phases/08-verified-defect-closure/08-08-SUMMARY.md` when done.
Record verbatim: the `cargo build --offline --lib --no-default-features` result; the full
criterion-4 `cargo tree` invocation and its (empty) grep output; the `cargo build --offline --bin
paladin --features cli` result; the re-run downstream grep reconciled row by row against
RESEARCH.md's nine-row triage table with a disposition for every hit; the two `CHANGELOG.md` entries;
the ADR-0023 `PENDING`→evidence replacement; and the note that neither `docker build` nor `mdbook`
was runnable in this environment, with the structural argument used instead.
</output>
