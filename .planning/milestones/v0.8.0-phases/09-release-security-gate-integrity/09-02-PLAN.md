---
phase: 09-release-security-gate-integrity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - SECURITY-EXCEPTIONS.md
  - .planning/decisions/0024-rustsec-exception-governance.md
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "Asking \"which RustSec advisories does this project suppress, and why?\" returns one answer from one file: `SECURITY-EXCEPTIONS.md` at the repository root"
    - "The register holds exactly ten rows — five vulnerability-class and five unmaintained-class — matching the tree-verified live suppression set after the four Phase-8-orphaned entries are dropped"
    - "Every register row carries all eleven governance fields non-empty: id, class, crate, path, why_present, why_not_fixable, owner, review_date, scope, compensating_control, revisit_condition"
    - "Every row's owner is a named accountable party rather than a closed milestone's team label"
    - "Every row carries its own review date rather than inheriting one blanket date, and no row's review date is in the past"
    - "The register's machine-readable payload is delimited by explicit BEGIN/END marker comments and parses as TOML, so a guard can read it without pipe-table regex"
    - "The three 2026 vulnerability suppressions carry a concretely-stated compensating control naming the actual input path, not a generic risk sentence"
    - "ADR-0024 records the extension of the M10 Epic 2 FR-3 schema as a supersession, so a stricter bar than the governing PRD is written down rather than inferred"
  artifacts:
    - path: "SECURITY-EXCEPTIONS.md"
      provides: "The authoritative RustSec exception governance register (D-01, D-06)"
      contains: "BEGIN MACHINE-READABLE REGISTER"
      min_lines: 60
    - path: ".planning/decisions/0024-rustsec-exception-governance.md"
      provides: "ADR recording the authoritative register, the extended schema, the ratification of the three 2026 advisories, the owner change and the 2026-09-30 disposition"
      contains: "## Downstream Consumers"
  key_links:
    - from: "SECURITY-EXCEPTIONS.md"
      to: ".cargo/audit.toml"
      via: "the register's vulnerability-class rows are the governance record for that file's ignore array"
      pattern: "audit\\.toml"
    - from: "SECURITY-EXCEPTIONS.md"
      to: "deny.toml"
      via: "the register's full row set is the governance record for that file's ignore array"
      pattern: "deny\\.toml"
    - from: ".planning/decisions/0024-rustsec-exception-governance.md"
      to: "SECURITY-EXCEPTIONS.md"
      via: "Code Locations section cites the register as the artifact the decision creates"
      pattern: "SECURITY-EXCEPTIONS"
  prohibitions:
    - "Governance fields must not be filled with placeholder text. A row whose owner reads TBD, whose compensating control reads N/A, or whose revisit condition restates the advisory title is governance theatre — it satisfies a field-count check while telling the next reader nothing, and it is prohibited here even though the guard in plan 09-06 would accept a non-empty string."
    - "A suppression must not be recorded as accepted risk on the strength of a document alone. Each row's reachability claim must trace to a manifest or lockfile fact read in this session; a row justified only by what a prior corpus document asserted is prohibited."
---

<objective>
Author the governance surface before the configuration it governs: one root-level register carrying
all eleven fields for each of the ten live suppressions, plus the ADR that makes its stricter
schema, its ratifications and its owner change a recorded decision rather than an inference.

Purpose: the four governance fields ROADMAP criterion 2 demands cannot live inside
`[advisories] ignore`, which is an array of bare strings — they can only ever be comments there, and
a comment is neither queryable nor gateable. This plan creates the queryable surface. It runs in
wave 1 because plan 09-06's guard asserts bidirectional coverage between the register and both TOML
files, and a guard cannot be written against a register that does not exist.
Output: `SECURITY-EXCEPTIONS.md` and `.planning/decisions/0024-rustsec-exception-governance.md`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-release-security-gate-integrity/09-CONTEXT.md
@.planning/phases/09-release-security-gate-integrity/09-PATTERNS.md
</context>

## Artifacts this phase produces

Newly-created symbols and files across all seven Phase 9 plans. Nothing on this list exists in the
tree today, so none of it can be a drift regression against a prior phase.

| Artifact | Plan | Kind |
|---|---|---|
| `crates/paladin-herald/CHANGELOG.md` | 09-01 | new file |
| `scripts/check-changelogs.sh` | 09-01 | new guard script |
| `Makefile` target `check-changelogs` | 09-01 | new target |
| CI step `Check per-crate changelogs` | 09-01 | new `ci.yml` step |
| `SECURITY-EXCEPTIONS.md` | 09-02 | new root file |
| `.planning/decisions/0024-rustsec-exception-governance.md` | 09-02 | new ADR |
| `.planning/decisions/0027-dockerfile-chef-planner-stage.md` | 09-03 | new ADR |
| `.crate-names.txt` | 09-04 | new allow-list file |
| `scripts/check-crate-names.sh` | 09-04 | new guard script |
| `Makefile` target `check-crate-names` | 09-04 | new target |
| CI step `Check crates.io package names` | 09-04 | new `ci.yml` step |
| `.planning/decisions/0026-crate-name-collision-guard.md` | 09-04 | new ADR |
| `LICENSE-MIT`, `LICENSE-APACHE` | 09-05 | new files (D-11 branch only) |
| `.planning/decisions/0025-licence-posture.md` | 09-05 | new ADR |
| `scripts/check-advisory-register.sh` | 09-06 | new guard script |
| `Makefile` targets `check-advisory-register`, `check-gates` | 09-06 | new targets |
| CI step `Check advisory exception register` | 09-06 | new `ci.yml` step |
| `.planning/phases/09-release-security-gate-integrity/09-COVERAGE.md` | 09-07 | new declaration file |

<tasks>

<task type="auto">
  <name>Task 1: Author SECURITY-EXCEPTIONS.md — ten rows, eleven fields, one machine-readable block</name>
  <read_first>
    - `.cargo/audit.toml` in full — its five-entry `ignore` array and the per-advisory comment block above it, which already carries four of the eleven register fields for each entry and is the text this register lifts rather than re-derives
    - `deny.toml` lines 111-146 — the `[advisories] ignore` array's three labelled classes as they stand today (fourteen entries)
    - `deny.toml` lines 1-8 — the header prose style the register's own header should echo
    - `.project/Milestone_7-Production-Hardening/Epic_4/rustsec-remediation-plan.md` lines 39, 40, 53, 73, 79-81 — the formal risk acceptance: the owner label, the 2026-09-30 target, and the field list a formal acceptance is supposed to carry
    - `crates/paladin-content/Cargo.toml` lines 18 and 41 — the empty `pdf = []` feature and the unconditional `pdf-extract` dependency that make the lopdf suppression warranted on tree evidence (D-17)
    - `.planning/phases/09-release-security-gate-integrity/09-CONTEXT.md` → D-01, D-04, D-05, D-06, D-08, D-09, D-10, D-17
    - `.planning/phases/09-release-security-gate-integrity/09-RESEARCH.md` → "Answering the Open Technical Questions" §2 (the register format recommendation and its Open Question 2 about marker comments) and §3 (the re-verified liveness counts)
  </read_first>
  <files>SECURITY-EXCEPTIONS.md</files>
  <action>
Create `SECURITY-EXCEPTIONS.md` at the repository root — adjacent to the configuration it governs,
so the next person editing `deny.toml` cannot miss it, and visible to anyone who has never run GSD.

Shape it as prose plus exactly one machine-readable payload, per D-01 and the Claude's-Discretion
resolution in 09-RESEARCH.md §2 Option A:

**Header prose.** Echo `deny.toml:1-8`'s header style. State what the file is (the authoritative
governance register for every RustSec advisory this workspace suppresses), which two configuration
files mirror it (`.cargo/audit.toml` as the authoritative suppression surface for `cargo audit`, and
`deny.toml` which mirrors that file's vulnerability class exactly and adds one clearly-labelled
unmaintained class for `cargo deny`), which script enforces the relationship
(`scripts/check-advisory-register.sh`, landing in plan 09-06), and how to run it locally
(`make check-advisory-register`). State the schema contract explicitly: adding an entry to either
TOML without a row here fails CI, and a row here with no live suppression fails CI too.

**The machine-readable payload.** Delimit it with two HTML comment markers on their own lines —
an opening `<!-- BEGIN MACHINE-READABLE REGISTER -->` and a closing
`<!-- END MACHINE-READABLE REGISTER -->` — with a single fenced TOML block between them. Explicit
markers rather than "the first fenced block" so a future editor can add an illustrative snippet
elsewhere in the file without breaking the guard's extraction (09-RESEARCH.md Open Question 2).
Inside the fence, one `[[exception]]` table per advisory with these eleven keys, all non-empty
strings: `id`, `class`, `crate`, `path`, `why_present`, `why_not_fixable`, `owner`, `review_date`,
`scope`, `compensating_control`, `revisit_condition`.

**The ten rows, and only these ten.** Five with `class = "vulnerability"` — `RUSTSEC-2023-0071`
(rsa), `RUSTSEC-2025-0111` (tokio-tar), `RUSTSEC-2026-0187` (lopdf), `RUSTSEC-2026-0194` and
`RUSTSEC-2026-0195` (both quick-xml). Five with `class = "unmaintained"` — `RUSTSEC-2021-0141`
(dotenv), `RUSTSEC-2025-0057` (fxhash), `RUSTSEC-2025-0119` (number_prefix), `RUSTSEC-2025-0134`
(rustls-pemfile), `RUSTSEC-2024-0436` (paste). Do **not** author rows for the four advisories whose
crates Phase 8's clap v4 migration removed from the graph; they return zero hits in `Cargo.lock` and
plan 09-06 deletes them from `deny.toml` in the same phase. Attaching an owner and an expiry to a
suppression that suppresses nothing is precisely the governance theatre D-04 rejects.
<!-- planner-discipline-allow: RUSTSEC-2022-0104 -->
<!-- planner-discipline-allow: RUSTSEC-2021-0139 -->
<!-- planner-discipline-allow: RUSTSEC-2024-0370 -->
<!-- planner-discipline-allow: RUSTSEC-2024-0375 -->

**Field content rules.** `crate` and `path` come from `.cargo/audit.toml`'s comment block and
`deny.toml`'s inline comments — lift that text, do not re-derive it. `owner` is `DF3NDR` on every
row, per D-09: the repository owner is the only named human approver anywhere in this corpus, and a
security exception whose owner is a milestone that ended has no owner. `review_date` is
`2026-12-31` on every row, per D-10 — per-advisory dates that begin aligned so the first review is
one event and diverge afterwards as each upstream moves; this renews the 2026-09-30 acceptance
rather than closing it, because no upstream fix exists for either original advisory.
`compensating_control` must name the concrete reason exposure is bounded, never a generic sentence:
for `RUSTSEC-2026-0187` say that Paladin does not feed attacker-controlled PDFs in a default build;
for `-0194`/`-0195` say the XML the `s3` feature parses is S3 API responses from a configured
endpoint; for the two originals say they are dev/test-scoped and not reachable from a release build.
`why_not_fixable` must state the upstream condition, again lifted from the existing comments.
`revisit_condition` is the specific upstream event that would retire the entry.

**One prose note after the payload**, so a reader learns what the register does not do: record that
`RUSTSEC-2026-0187`'s reachability rests on `crates/paladin-content/Cargo.toml:41` declaring
`pdf-extract` unconditionally while `:18`'s `pdf = []` gates nothing — a live contradiction that is
Phase 10 / HARD-06's subject, handed over rather than answered here.

Also record, in one line of the header prose, the discretionary call this plan makes and does not
silently omit: this file is an exception register only and is **not** a GitHub-facing `SECURITY.md`.
There is none today; adding one is a separate deliverable aimed at a different audience.
  </action>
  <verify>
    <automated>python3 -c "
import re,sys,datetime,tomllib
s=open('SECURITY-EXCEPTIONS.md',encoding='utf-8').read()
m=re.search(r'<!-- BEGIN MACHINE-READABLE REGISTER -->(.*?)<!-- END MACHINE-READABLE REGISTER -->',s,re.S)
assert m, 'markers missing'
b=re.search(r'\`\`\`toml(.*?)\`\`\`',m.group(1),re.S)
assert b, 'fenced toml block missing'
d=tomllib.loads(b.group(1))
rows=d['exception']
assert len(rows)==10, len(rows)
keys=['id','class','crate','path','why_present','why_not_fixable','owner','review_date','scope','compensating_control','revisit_condition']
for r in rows:
    for k in keys: assert isinstance(r.get(k),str) and r[k].strip(), (r.get('id'),k)
    assert datetime.date.fromisoformat(r['review_date']) > datetime.date(2026,8,7), r['id']
cl=[r['class'] for r in rows]
assert cl.count('vulnerability')==5 and cl.count('unmaintained')==5, cl
print('OK', len(rows))
"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command above exits 0 and prints `OK 10`.
    - `grep -c 'BEGIN MACHINE-READABLE REGISTER' SECURITY-EXCEPTIONS.md` returns `1` and `grep -c 'END MACHINE-READABLE REGISTER' SECURITY-EXCEPTIONS.md` returns `1`.
    - `python3 -c "import re,tomllib;s=open('SECURITY-EXCEPTIONS.md','rb').read().decode();b=re.search(r'BEGIN MACHINE-READABLE REGISTER -->(.*?)<!-- END',s,re.S).group(1);ids={r['id'] for r in tomllib.loads(re.search(r'\`\`\`toml(.*?)\`\`\`',b,re.S).group(1))['exception']};import json;print(json.dumps(sorted(ids)))"` prints exactly the ten IDs named in the action and none of the four Phase-8-orphaned ones.
    - `python3 -c "import re,tomllib;s=open('SECURITY-EXCEPTIONS.md','rb').read().decode();b=re.search(r'BEGIN MACHINE-READABLE REGISTER -->(.*?)<!-- END',s,re.S).group(1);rs=tomllib.loads(re.search(r'\`\`\`toml(.*?)\`\`\`',b,re.S).group(1))['exception'];assert all(r['owner']=='DF3NDR' for r in rs);assert all(r['review_date']=='2026-12-31' for r in rs);print('owner+date OK')"` exits 0.
    - For each of the ten rows, `compensating_control` is longer than 40 characters and does not equal any other row's — checkable with a one-line python assertion; a duplicated boilerplate control across unrelated advisories fails this.
    - `grep -c 'pdf-extract' SECURITY-EXCEPTIONS.md` returns at least `1` — the HARD-06 hand-off note is present.
    - `grep -c 'SECURITY.md' SECURITY-EXCEPTIONS.md` returns at least `1` — the not-a-SECURITY.md call is noted rather than silently omitted.
    - `git diff --stat -- '*.rs' | wc -l` returns `0`.
  </acceptance_criteria>
  <done>`SECURITY-EXCEPTIONS.md` exists at the repository root, its machine-readable block parses as TOML into exactly ten fully-populated exception rows, and its prose states what the file governs, what enforces it, and what it deliberately is not.</done>
</task>

<task type="auto">
  <name>Task 2: Write ADR-0024 — RustSec exception governance</name>
  <read_first>
    - `.planning/decisions/0023-cli-dependency-isolation.md` in full — the seven-heading, no-frontmatter shape ADR-0024 must match exactly, including the verbatim terminal-transcript style under `## Code Locations`
    - `.planning/decisions/0022-deprecation-requirement-withdrawal.md` — confirms the same shape on a second sample
    - `.planning/decisions/PROMOTION.md` line 47 — `Next free ADR number: 0024`, and the five-step append procedure (the counter advances to 0028 in plan 09-07, not here)
    - `SECURITY-EXCEPTIONS.md` (the file written in Task 1)
    - `.planning/REQUIREMENTS.md` lines 1055-1146 — SEC-01 in full, including the run-5 correction banner at 1060-1081 that withdraws run 4's out-of-sync finding
    - `.planning/phases/09-release-security-gate-integrity/09-CONTEXT.md` → D-00a, D-00b, D-00g, D-03, D-06, D-08, D-09, D-10, D-18
  </read_first>
  <files>.planning/decisions/0024-rustsec-exception-governance.md</files>
  <action>
Write `.planning/decisions/0024-rustsec-exception-governance.md` matching ADR-0022/0023's shape
exactly: no YAML frontmatter, and these seven headings in this order — `## Status` (body `Accepted`,
then a bold `**Date:**` line), `## Context`, `## Decision`, `## Considered Options`,
`## Code Locations`, `## Code Conformance`, `## Downstream Consumers`. No additional headings.

`## Context` must carry, each with a `file:line` citation read in this session rather than
transcribed from the corpus:
- The four divergent surfaces as they actually stand: `.cargo/audit.toml`'s five-entry ignore array;
  `deny.toml`'s fourteen entries in three labelled classes; the two `ci.yml` `cargo audit` jobs at
  `:61-78` and `:466-482` that both render under the display name `Security Audit`; and
  `rustsec-remediation-plan.md`'s formally-accepted two.
- That the corpus's own arithmetic was wrong twice: the record says fifteen entries and ten
  unmaintained; the file holds fourteen and nine, because `RUSTSEC-2025-0121` (gcc) was removed from
  every surface with no record of its removal anywhere. State that this is the second time a
  suppression set drifted silently, and that it is the argument for the guard — not a footnote.
- That Phase 8's ADR-0023 clap v4 migration removed four suppressions' parent crates from the graph,
  so four `deny.toml` entries suppress nothing today.
- That M10 Epic 2 FR-3's four-field schema requires neither an owner nor an expiry, so ROADMAP
  criterion 2 is a stricter bar than the governing PRD, and under the D-00b precedence order that
  supersession has to be written down rather than assumed.

`## Decision` must state five things as decided: (1) `.cargo/audit.toml` is the authoritative
suppression surface, `deny.toml` mirrors its vulnerability class exactly and adds one labelled
unmaintained class, and `SECURITY-EXCEPTIONS.md` is the authoritative governance register;
(2) the register's schema is the union of FR-3's four fields with owner, review date, affected
scope and compensating control, superseding FR-3; (3) the three 2026 vulnerability advisories are
**ratified**, not removed — they are real, DoS-class, have no clean upstream fix, and removing them
turns CI red on advisories nobody can fix, so ratification converts an unauthorised expansion into
an authorised one, which is what SUPPLY-02 asks for; (4) the owner becomes `DF3NDR`, the repository
owner, replacing a team label attached to a milestone that ended; (5) the 2026-09-30 acceptance is
**renewed** with per-advisory review dates of 2026-12-31, because closing it requires an upstream
fix and none exists for either original advisory.

`## Considered Options` must tag each option `(accepted)` or `(rejected)` with a reason, covering at
minimum: making `deny.toml` authoritative (rejected — it holds a superset `cargo audit` cannot
consume); generating both files from one source document (rejected — a build step nobody maintains,
for two files totalling roughly forty live lines); richer TOML comments instead of a register
(rejected — the four required fields cannot be structured data inside an array of strings, and a
comment is not queryable and cannot be gated); keeping the four dead entries "just in case"
(rejected); removing the three 2026 advisories instead of ratifying them (rejected); a single
blanket renewal date (rejected — that is what produced a state where one date governs two advisories
and nine others have none).

`## Code Locations` must list the register, both TOML files, both `ci.yml` jobs, and
`crates/paladin-content/Cargo.toml:18,41`. Following ADR-0023's precedent, embed the verbatim
liveness transcript as evidence — run the ten-crate `grep -c` loop against `Cargo.lock` in this
session and paste its actual output, rather than restating the table from a prior document. This is
the D-19 bar and this ADR is where it is discharged for SEC-01.

`## Code Conformance` opens with the literal line `must change`, then names which plans execute
what: 09-06 deletes the four orphaned entries, rewrites both files' comment blocks to point at the
register, deletes the duplicate `ci.yml` audit job and lands
`scripts/check-advisory-register.sh`; 09-07 records the closure evidence.

`## Downstream Consumers` must name: Phase 12 / SUPPLY-01 and SUPPLY-02 (which this phase absorbs —
they inherit closed items rather than work); Phase 10 / HARD-06 (which receives the `pdf-extract`
reachability finding as an input, and which this ADR explicitly does not answer); and the
2026-12-31 review event itself.

Do **not** author ADRs 0025, 0026 or 0027 here, and do **not** edit `PROMOTION.md` — the counter
advances once, in the close-out, so four concurrent plans cannot race the same line.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/decisions/0024-rustsec-exception-governance.md; test -f "$f" && head -1 "$f" | grep -q "^# ADR-0024" && ! head -1 "$f" | grep -q "^---" && for h in "## Status" "## Context" "## Decision" "## Considered Options" "## Code Locations" "## Code Conformance" "## Downstream Consumers"; do grep -qx "$h" "$f" || { echo "MISSING $h"; exit 1; }; done && grep -qx "must change" "$f" && grep -c "(rejected)" "$f"'</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 .planning/decisions/0024-rustsec-exception-governance.md` starts with `# ADR-0024`.
    - `sed -n '1p' .planning/decisions/0024-rustsec-exception-governance.md | grep -c '^---'` returns `0` — no YAML frontmatter, matching 0001-0023.
    - All seven headings are present as exact whole lines, in the order listed in the action; `diff <(grep '^## ' .planning/decisions/0024-rustsec-exception-governance.md) <(printf '## Status\n## Context\n## Decision\n## Considered Options\n## Code Locations\n## Code Conformance\n## Downstream Consumers\n')` produces no output.
    - `grep -c '(rejected)' .planning/decisions/0024-rustsec-exception-governance.md` returns at least `6`.
    - `grep -qx 'must change' .planning/decisions/0024-rustsec-exception-governance.md` exits 0.
    - `grep -c 'Cargo.lock' .planning/decisions/0024-rustsec-exception-governance.md` returns at least `1`, and the file contains the verbatim output of the liveness loop run in this session.
    - `grep -c 'HARD-06' .planning/decisions/0024-rustsec-exception-governance.md` returns at least `1` and `grep -c 'SUPPLY-0' .planning/decisions/0024-rustsec-exception-governance.md` returns at least `1`.
    - `git status --porcelain .planning/decisions/PROMOTION.md` is empty — this plan does not touch the numbering index.
  </acceptance_criteria>
  <done>ADR-0024 exists in the exact 0022/0023 shape, records all five decisions with rejected alternatives, carries a verbatim in-session liveness transcript as its evidence, and names its downstream consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor commit → suppression surfaces | Anyone with commit rights can add an ID to `deny.toml` or `.cargo/audit.toml`; today nothing requires a corresponding accountability record. |
| register → downstream consumers of the published crate family | The register is a public repo-root document; whatever it claims about reachability is what a consumer will believe. |
| a closed milestone's team label → present-day accountability | An owner field naming an organisational unit that no longer exists reads as governed while being unowned. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05 | Repudiation | `owner` field, all ten rows | high | mitigate | An exception owned by "Platform Security (Milestone 7)" — a label attached to a milestone that ended — cannot be held to a review. Every row's owner becomes the one named human approver in the corpus, `DF3NDR`, per D-09, and the acceptance criteria assert it on all ten rows rather than sampling. |
| T-09-06 | Tampering | governance fields as free text | high | mitigate | Fields that merely have to be non-empty invite placeholder text that passes the guard while conveying nothing. The acceptance criteria require every `compensating_control` to exceed 40 characters and to be distinct across rows, so one boilerplate sentence cannot be pasted across unrelated advisories. |
| T-09-07 | Spoofing | register rows for dead suppressions | medium | mitigate | Authoring rows for the four Phase-8-orphaned advisories would present four suppressions as governed live risk when their crates are absent from `Cargo.lock`. Those rows are not authored; plan 09-06's liveness clause makes the state permanently unreachable. |
| T-09-08 | Information disclosure | publishing exact unpatched advisory IDs and their reachability at repo root | low | accept | The same IDs are already public in `deny.toml`, `.cargo/audit.toml` and the RustSec advisory database, and the reachability text restates what the manifests declare. The register adds no information an attacker could not derive from the published crates, and D-01's whole argument is that the governance must be visible where the config is. |
| T-09-09 | Elevation of privilege | a stricter-than-PRD schema applied without a recorded supersession | medium | mitigate | Silently enforcing a bar the governing PRD does not set would let any future reader revert it as unauthorised. ADR-0024 records the FR-3 supersession explicitly under the D-00b precedence order. |
| T-09-SC | Tampering | package-manager installs | high | accept | This plan installs nothing and touches no manifest. Two Markdown files are created; `git diff -- Cargo.toml Cargo.lock crates/*/Cargo.toml` is empty for this plan, so the Package Legitimacy Gate does not fire (`09-RESEARCH.md` § "Package Legitimacy Audit"). |
</threat_model>

<verification>
- The Task 1 verify command prints `OK 10`.
- All eleven fields are non-empty on all ten rows; all owners are `DF3NDR`; all review dates are `2026-12-31` and in the future.
- ADR-0024's seven headings match 0023's exactly, in order, with no frontmatter.
- ADR-0024 embeds the verbatim `Cargo.lock` liveness transcript produced in this session.
- `.planning/decisions/PROMOTION.md` is untouched by this plan.
- `git diff --stat 49ad74c..HEAD -- '*.rs' | wc -l` → `0`.
</verification>

<success_criteria>
There is exactly one place to look up which advisories this workspace suppresses and why, it holds
the ten that are live rather than the fifteen the corpus remembers, every one has a named owner and
a dated review, and the decision to hold a stricter bar than the governing PRD is on record.
</success_criteria>

<output>
Create `.planning/phases/09-release-security-gate-integrity/09-02-SUMMARY.md` when done.
</output>
