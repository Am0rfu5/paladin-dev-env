---
phase: 09-release-security-gate-integrity
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile.chef
  - .planning/decisions/0027-dockerfile-chef-planner-stage.md
autonomous: true
requirements: [SEC-05]

must_haves:
  truths:
    - "`Dockerfile.chef`'s planner stage names zero individual crate manifests, so no crate can be omitted from it"
    - "Adding an eleventh crate requires no `Dockerfile.chef` edit — the mechanism cannot go stale on the next crate addition"
    - "After the deletion the planner stage names zero individual crate manifests, so no crate can sit just outside the enumeration"
    - "`COPY crates ./crates` covers zero, one, or eleven crate directories identically; crate count is not encoded anywhere in the planner stage"
    - "No per-name string comparison remains in the planner stage, so no crate-directory naming or encoding question can affect which manifests reach `cargo chef prepare`"
    - "The four surviving planner-stage instructions keep their existing relative order — root manifest and lockfile, then `src`, then `crates`, then `benches`, then the recipe step — because the fix is subtraction only"
    - "The planner-stage comment describes the caching mechanism that actually operates, rather than the isolation the deleted lines never delivered"
    - "`Dockerfile` and `Dockerfile.server` are confirmed free of the same enumeration defect, so SEC-05 closes across the whole Docker surface rather than one file"
    - statement: "Two concurrent or interrupted `docker build -f Dockerfile.chef` invocations produce the same `recipe.json`, because `cargo chef prepare` derives it from the manifests and the lockfile alone and holds no cross-build state. Docker is absent from this environment, so this is established from cargo-chef's documented recipe semantics and cannot be measured here."
      verification: backstop
    - statement: "The builder stage's `cargo chef cook` layer reports CACHED when only `.rs` source changes between two builds. Docker is absent from this environment; this is the measurement that would upgrade ADR-0027's evidence from documentation-cited to verified, and it is recorded as CI-only rather than inferred."
      verification: backstop
  artifacts:
    - path: "Dockerfile.chef"
      provides: "A planner stage whose crate coverage is structural rather than enumerated"
      contains: "cargo chef prepare"
    - path: ".planning/decisions/0027-dockerfile-chef-planner-stage.md"
      provides: "ADR recording the M7 Epic 2 FR-01 supersession, which branch of D-16 was taken, and the cargo-chef evidence behind it"
      contains: "## Considered Options"
  key_links:
    - from: "Dockerfile.chef planner stage"
      to: "Dockerfile.chef builder stage"
      via: "COPY --from=planner /app/recipe.json — the content-addressed cross-stage copy that actually delivers the caching, and which the deletion does not touch"
      pattern: "COPY --from=planner"
    - from: ".planning/decisions/0027-dockerfile-chef-planner-stage.md"
      to: "Dockerfile.chef"
      via: "Code Locations section citing the deleted range and the surviving instructions"
      pattern: "Dockerfile\\.chef"
  prohibitions:
    - "A caching claim must not be recorded as measured when it was not measured. Docker is absent from this environment; the closure evidence must say established-from-upstream-documentation, and any wording that implies a build was observed is prohibited even though it would read as stronger."
    - "The minimal-diff instinct must not be allowed to reproduce the defect. Adding the one missing manifest line satisfies the letter of SEC-05 while leaving an enumerated list that goes stale on the next crate — the done-condition names that outcome as the defect itself, so the additive fix is prohibited here."
---

<objective>
Delete the nine-manifest enumeration from `Dockerfile.chef`'s planner stage so its crate coverage
becomes structural, and record the supersession of M7 Epic 2 FR-01 as an ADR rather than as a silent
Dockerfile edit.

Purpose: SEC-05's done-condition is explicit that "an enumerated list that goes stale on every crate
addition is the defect, not just the one missing line." Adding `paladin-herald`'s line satisfies the
letter and reproduces the defect. The research confirmed the enumeration is inert — a strictly later
`COPY crates ./crates` in the same stage already dominates the cache decision for all ten crates —
so the deletion is equivalent in caching behaviour and structurally eliminates staleness.
Output: a four-instruction planner stage, a corrected comment, and ADR-0027.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-release-security-gate-integrity/09-CONTEXT.md
@.planning/phases/09-release-security-gate-integrity/09-PATTERNS.md
</context>

## Artifacts this phase produces

Newly-created symbols and files across all seven Phase 9 plans. Nothing on this list exists in the
tree today, so none of it can be a drift regression against a prior phase.

| Artifact | Plan | Kind |
|---|---|---|
| `crates/paladin-herald/CHANGELOG.md` | 09-01 | new file |
| `scripts/check-changelogs.sh` | 09-01 | new guard script |
| `Makefile` target `check-changelogs` | 09-01 | new target |
| CI step `Check per-crate changelogs` | 09-01 | new `ci.yml` step |
| `SECURITY-EXCEPTIONS.md` | 09-02 | new root file |
| `.planning/decisions/0024-rustsec-exception-governance.md` | 09-02 | new ADR |
| `.planning/decisions/0027-dockerfile-chef-planner-stage.md` | 09-03 | new ADR |
| `.crate-names.txt` | 09-04 | new allow-list file |
| `scripts/check-crate-names.sh` | 09-04 | new guard script |
| `Makefile` target `check-crate-names` | 09-04 | new target |
| CI step `Check crates.io package names` | 09-04 | new `ci.yml` step |
| `.planning/decisions/0026-crate-name-collision-guard.md` | 09-04 | new ADR |
| `LICENSE-MIT`, `LICENSE-APACHE` | 09-05 | new files (D-11 branch only) |
| `.planning/decisions/0025-licence-posture.md` | 09-05 | new ADR |
| `scripts/check-advisory-register.sh` | 09-06 | new guard script |
| `Makefile` targets `check-advisory-register`, `check-gates` | 09-06 | new targets |
| CI step `Check advisory exception register` | 09-06 | new `ci.yml` step |
| `.planning/phases/09-release-security-gate-integrity/09-COVERAGE.md` | 09-07 | new declaration file |

## Note to plan 09-05 — file contention on this file

`Dockerfile.chef:93` carries `LABEL org.opencontainers.image.licenses="MIT"`. `09-RESEARCH.md` §4
attributes that label to the runtime `Dockerfile`; that attribution is **wrong** — verified this
session, the runtime `Dockerfile` carries no `LABEL` at all and the only OCI label block in the tree
is in `Dockerfile.chef` at `:88-93`. The licence label therefore belongs to plan 09-05 (SEC-02),
which runs in wave 2 and lists `Dockerfile.chef` in its `files_modified` for that reason. This plan
must **not** touch the label; plan 09-05 must re-derive its line number, because deleting nine lines
here moves it from `:93` to approximately `:84`.

<tasks>

<task type="auto">
  <name>Task 1: Delete the planner-stage manifest enumeration and correct its comment</name>
  <read_first>
    - `Dockerfile.chef` lines 18-40 in full — the planner stage as it stands: the three-line comment at `:21-23`, `COPY Cargo.toml Cargo.lock ./` at `:24`, the nine per-crate manifest copies at `:25-33`, `COPY src ./src` at `:35`, `COPY crates ./crates` at `:36`, `COPY benches ./benches` at `:37`, `RUN cargo chef prepare --recipe-path recipe.json` at `:38`
    - `Dockerfile.chef` lines 50-70 — the builder stage, in particular the cross-stage `COPY --from=planner` of `recipe.json` and `RUN cargo chef cook`, which must remain untouched
    - `Dockerfile.chef` lines 85-95 — the OCI `LABEL` block, which this plan must leave exactly as found (see the contention note above)
    - `Dockerfile` and `Dockerfile.server` — confirm by reading that neither carries a per-crate manifest enumeration
    - `.planning/phases/09-release-security-gate-integrity/09-RESEARCH.md` → "Answering the Open Technical Questions" §1 (the CONFIRMED verdict and the mechanism behind it) and "Pitfall 2"
    - `.planning/phases/09-release-security-gate-integrity/09-PATTERNS.md` → the `Dockerfile.chef` section (the exact post-deletion target shape)
    - `.planning/phases/09-release-security-gate-integrity/09-CONTEXT.md` → D-16 in full, including its fallback branch
  </read_first>
  <files>Dockerfile.chef</files>
  <action>
Take D-16's primary branch — deletion — which the research confirmed from cargo-chef's own canonical
documentation.

Delete the nine consecutive per-crate manifest copy instructions in the planner stage, currently at
`:25-33`, covering `paladin-core`, `paladin-ports`, `paladin-battalion`, `paladin-llm`,
`paladin-memory`, `paladin-storage`, `paladin-notifications`, `paladin-content` and `paladin-web`.
Re-derive the exact line range by reading the file before editing rather than trusting these numbers.
<!-- planner-discipline-allow: COPY crates/paladin.*Cargo.toml -->

Leave the four surviving planner-stage instructions exactly as they are and in exactly their current
relative order: the root manifest-and-lockfile copy, then `src`, then `crates`, then `benches`, then
the recipe step. Do not reorder, do not consolidate, do not replace them with the upstream
`COPY . .` form. The fix is subtraction only, and `git diff -- Dockerfile.chef` must show no added
`COPY` or `RUN` instruction.

Rewrite the three-line explanatory comment currently at `:21-23`. Its present text claims each crate
manifest is copied individually so the recipe layer only invalidates when a manifest changes. That
claim is false for this file: the recursive `crates` copy two lines below the enumeration already
brings in real source for every crate, so it busts the recipe layer on any source edit, for all ten
crates and not merely the omitted eleventh. Replace it with the mechanism that actually operates —
`cargo chef prepare` distils a `recipe.json` whose content is derived from the manifests and the
lockfile only, and the builder stage consumes it through a content-addressed cross-stage copy, so
the expensive dependency compilation is skipped whenever `recipe.json`'s bytes are unchanged,
independently of whether the planner stage itself was a cache hit. Say in one line that the stage
copies whole directories deliberately, so a new crate needs no edit here.

Touch nothing else in the file. In particular leave the OCI `LABEL` block alone — plan 09-05 owns
the licence label — and leave the pinned `cargo-chef` version and base image alone.

Finally, confirm and record that the defect is confined to this file: read `Dockerfile` and
`Dockerfile.server` and record in the SUMMARY that each copies the crates tree wholesale with no
per-manifest enumeration, so SEC-05 closes across the whole Docker surface.
  </action>
  <verify>
    <automated>bash -c 'grep -c "COPY crates/paladin" Dockerfile.chef; grep -c "^COPY crates ./crates$" Dockerfile.chef; grep -c "cargo chef prepare" Dockerfile.chef; git diff -- Dockerfile.chef | grep -E "^\+(COPY|RUN)" | wc -l'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'COPY crates/paladin' Dockerfile.chef` returns `0`.
    - `grep -c 'cargo chef prepare' Dockerfile.chef` returns `1` and `grep -c 'cargo chef cook' Dockerfile.chef` returns `1` — the recipe pipeline is intact.
    - `git diff -- Dockerfile.chef | grep -cE '^\+(COPY|RUN)'` returns `0` — no instruction was added, only comment lines and deletions.
    - `git diff --numstat -- Dockerfile.chef` shows at least 9 deleted lines.
    - The four surviving planner-stage instructions appear in their original relative order, verifiable with `grep -n 'COPY \|RUN cargo chef prepare' Dockerfile.chef | sed -n '1,6p'` and comparing against the pre-edit ordering recorded in the SUMMARY.
    - `grep -c 'LABEL org.opencontainers.image.licenses' Dockerfile.chef` returns `1` and `git diff -- Dockerfile.chef | grep -c 'opencontainers'` returns `0` — the label is present and untouched.
    - `grep -c 'COPY crates/paladin' Dockerfile Dockerfile.server` returns `0` for both files, recorded in the SUMMARY as the whole-surface confirmation.
    - `git diff --stat -- '*.rs' | wc -l` returns `0`.
  </acceptance_criteria>
  <done>The planner stage carries four copy instructions and the recipe step, names no crate individually, and its comment describes the mechanism that actually delivers the caching.</done>
</task>

<task type="auto">
  <name>Task 2: Write ADR-0027 — Dockerfile.chef planner-stage supersession</name>
  <read_first>
    - `.planning/decisions/0023-cli-dependency-isolation.md` in full — the seven-heading, no-frontmatter ADR shape to match exactly
    - `.planning/decisions/0020-build-benchmark-per-scenario.md` — the precedent for judging a stale artefact rather than reconciling with it
    - `Dockerfile.chef` (post-edit)
    - `.planning/REQUIREMENTS.md` lines 1191-1204 — SEC-05 in full, including its done-condition
    - `.planning/phases/09-release-security-gate-integrity/09-RESEARCH.md` → §1 in full, including the two upstream citations, the "measured vs. established" paragraph, and the future-measurement recipe
    - `.planning/phases/04-release-coherence/04-ci-gate-deferrals.md` — the standing record that Docker is absent from this environment
    - `.planning/phases/09-release-security-gate-integrity/09-CONTEXT.md` → D-00b, D-00g, D-16, D-18, D-19
  </read_first>
  <files>.planning/decisions/0027-dockerfile-chef-planner-stage.md</files>
  <action>
Write `.planning/decisions/0027-dockerfile-chef-planner-stage.md` in ADR-0022/0023's exact shape: no
YAML frontmatter, seven headings in order — `## Status` (`Accepted`, then a bold `**Date:**` line),
`## Context`, `## Decision`, `## Considered Options`, `## Code Locations`, `## Code Conformance`,
`## Downstream Consumers`.

`## Context` must establish, with citations: that M7 Epic 2 FR-01 requires all crate manifests in
the planner stage and its §6 states the purpose as a dependency layer that only invalidates when a
manifest changes; that the shipped stage enumerated nine and omitted `paladin-herald`, which was
created after that milestone closed; and — the sharper finding — that the enumeration could never
have delivered the stated isolation for **any** crate, because the recursive `crates` copy runs
after it and before the recipe step, dominating the cache decision for all ten. Record what actually
delivers the benefit: `recipe.json`'s manifest-and-lockfile-only content, plus the builder stage's
content-addressed cross-stage copy, neither of which the deletion touches.

`## Decision` must state that M7 Epic 2 FR-01's enumeration requirement is superseded under the
D-00b precedence order (an ADR that contradicts shipped code is an instruction to change the code,
and a PRD requirement contradicted by the tool's own semantics is superseded), that the nine lines
are deleted rather than extended to ten, and — explicitly, because D-16 requires the plan to say
which branch it took — that this is **D-16's primary branch, not its fallback**, because the
researcher confirmed the cargo-chef reading rather than refuting it.

`## Considered Options` must tag each `(accepted)` or `(rejected)` with a reason and must include the
fallback branch as a genuinely considered option: adding the herald line only (rejected — satisfies
the letter and reproduces the defect the done-condition names); keeping the enumeration and adding a
guard script asserting every crate manifest appears in the planner stage (rejected here, but record
that it also closes SEC-05 and would have been correct had the cargo-chef reading been refuted);
adopting cargo-chef's upstream whole-tree planner copy verbatim (rejected — a larger diff with no
behavioural gain over the surviving instructions); deleting the enumeration (accepted).

`## Code Locations` must cite the deleted range, the four surviving instructions, the builder
stage's cross-stage copy, and both upstream sources by name — cargo-chef's own README pattern and
its cache-behaviour claim, and the corroborating third-party Rust-Dockerfile documentation.

`## Code Conformance` opens with the literal line `must change` and names plan 09-03 task 1 as the
executor.

`## Downstream Consumers` must name Phase 10 / HARD-01's ledger row for the Docker workspace build,
and the first CI runner to build this image.

The evidence discipline is the load-bearing part of this ADR. State plainly, in `## Context` and
again in `## Code Locations`, that Docker is absent from this environment, that nothing here was
measured against a real daemon, and that the reasoning is established from upstream documentation.
Include the measurement that would upgrade it — build twice with only a Rust source edit between
runs and confirm the cook layer reports as cached — as an explicitly outstanding, CI-only
verification. Do not write any sentence that implies a build was observed.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/decisions/0027-dockerfile-chef-planner-stage.md; test -f "$f" && head -1 "$f" | grep -q "^# ADR-0027" && ! head -1 "$f" | grep -q "^---" && diff <(grep "^## " "$f") <(printf "## Status\n## Context\n## Decision\n## Considered Options\n## Code Locations\n## Code Conformance\n## Downstream Consumers\n") && grep -qx "must change" "$f" && grep -c "(rejected)" "$f"'</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 .planning/decisions/0027-dockerfile-chef-planner-stage.md` starts with `# ADR-0027`.
    - `diff <(grep '^## ' .planning/decisions/0027-dockerfile-chef-planner-stage.md) <(printf '## Status\n## Context\n## Decision\n## Considered Options\n## Code Locations\n## Code Conformance\n## Downstream Consumers\n')` produces no output.
    - `sed -n '1p' .planning/decisions/0027-dockerfile-chef-planner-stage.md | grep -c '^---'` returns `0`.
    - `grep -c '(rejected)' .planning/decisions/0027-dockerfile-chef-planner-stage.md` returns at least `3` — the fallback branch is recorded as considered, not omitted.
    - `grep -ci 'cargo-chef' .planning/decisions/0027-dockerfile-chef-planner-stage.md` returns at least `2` and both upstream sources are named.
    - `grep -ci 'not measured\|cannot be measured\|absent from this environment' .planning/decisions/0027-dockerfile-chef-planner-stage.md` returns at least `1` — the evidence is scoped honestly per D-19.
    - `grep -qx 'must change' .planning/decisions/0027-dockerfile-chef-planner-stage.md` exits 0.
    - `git status --porcelain .planning/decisions/PROMOTION.md` is empty.
  </acceptance_criteria>
  <done>ADR-0027 records the FR-01 supersession, states which D-16 branch was taken and why, cites both upstream sources, and scopes its evidence as documentation-established rather than measured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository crate set → Docker build inputs | The planner stage decides which manifests reach `cargo chef prepare`; today that decision is a hand-maintained list that drifts silently as crates are added. |
| upstream documentation → a supersession of a completed milestone's PRD requirement | The deletion rests on cargo-chef's documented recipe semantics, which cannot be exercised here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-10 | Tampering | planner-stage manifest enumeration | high | mitigate | A hand-maintained list of crate manifests is a control that silently stops covering the tree the moment a crate is added — it already missed `paladin-herald`. Deleting it makes coverage structural: the surviving directory copy is crate-count-independent, so the drift has no way to recur. Asserted by a zero-count check on per-crate copy instructions. |
| T-09-11 | Repudiation | ADR-0027's evidence claim | high | mitigate | Recording a caching conclusion as measured when Docker is absent would put an unfalsifiable claim into the corpus at ADR precedence, above every PRD. The ADR must state explicitly that nothing was measured here and must carry the outstanding CI-only measurement; the acceptance criteria grep for that disclaimer rather than trusting it. |
| T-09-12 | Denial of service | Docker image build | medium | mitigate | A careless deletion could remove a copy the recipe step needs and break the image for everyone, with no local way to catch it since Docker is absent. The task is constrained to subtraction plus a comment rewrite, and the acceptance criteria assert zero added `COPY`/`RUN` lines and an unchanged relative order for the four survivors. |
| T-09-13 | Tampering | the OCI licence `LABEL` at `Dockerfile.chef:93` | medium | mitigate | Two wave-separated plans edit this one file. Editing the label here would either duplicate or pre-empt the human-gated SEC-02 decision. This plan asserts the label is byte-unchanged in its own diff; plan 09-05 owns it in wave 2 and re-derives its line number. |
| T-09-SC | Tampering | package-manager installs | high | accept | This plan installs nothing and changes no dependency. The pinned `cargo-chef` version and the base image in `Dockerfile.chef` are read but not modified; `git diff -- Cargo.toml Cargo.lock` is empty for this plan, so the Package Legitimacy Gate does not fire. |
</threat_model>

<verification>
- `grep -c 'COPY crates/paladin' Dockerfile.chef` → `0`.
- `git diff -- Dockerfile.chef | grep -cE '^\+(COPY|RUN)'` → `0`.
- `grep -c 'COPY crates/paladin' Dockerfile Dockerfile.server` → `0` for both, recorded in the SUMMARY.
- ADR-0027's seven headings match 0023's exactly, in order, with no frontmatter, and carry the not-measured disclaimer.
- `git diff --stat 49ad74c..HEAD -- '*.rs' | wc -l` → `0`.
- The two backstop truths in `must_haves` remain unconfirmed locally by design; the close-out plan restates them as CI-only.
</verification>

<success_criteria>
`Dockerfile.chef`'s planner stage covers every crate manifest by a mechanism that has no crate list
to go stale, the supersession of a completed milestone's PRD requirement is on record with its
reasoning and both upstream citations, and the one thing that could not be measured here is labelled
as unmeasured instead of inferred.
</success_criteria>

<output>
Create `.planning/phases/09-release-security-gate-integrity/09-03-SUMMARY.md` when done.
</output>
