---
phase: 09-release-security-gate-integrity
plan: 05
type: execute
wave: 2
depends_on: ["09-03"]
files_modified:
  - Cargo.toml
  - crates/paladin-core/Cargo.toml
  - crates/paladin-ports/Cargo.toml
  - crates/paladin-battalion/Cargo.toml
  - crates/paladin-herald/Cargo.toml
  - crates/paladin-llm/Cargo.toml
  - crates/paladin-memory/Cargo.toml
  - crates/paladin-storage/Cargo.toml
  - crates/paladin-notifications/Cargo.toml
  - crates/paladin-content/Cargo.toml
  - crates/paladin-web/Cargo.toml
  - LICENSE
  - LICENSE-MIT
  - LICENSE-APACHE
  - README.md
  - CHANGELOG.md
  - Dockerfile.chef
  - .planning/decisions/0025-licence-posture.md
  - .project/Milestone_7-Production-Hardening/Epic_4/license-compatibility-decision-checklist.md
autonomous: false
requirements: [SEC-02]

must_haves:
  truths:
    - "Asking \"what licence is this project?\" returns exactly one answer, and the root package and all ten library crates declare it"
    - "The answer was chosen by the repository owner at a blocking checkpoint, not inferred by an agent — SEC-02 states in terms that it must not be resolved by inference"
    - "`grep -h '^license = ' Cargo.toml crates/*/Cargo.toml | sort -u | wc -l` returns 1 — eleven equal declarations collapse to one distinct expression, and any single divergent manifest raises the count to 2"
    - "Every one of the eleven manifests still carries exactly one `license` line; a manifest that lost the field returns zero for its own grep and is detectable"
    - "The licence check is order-insensitive: it sorts and de-duplicates, so the order the eleven manifests are listed in cannot change the verdict"
    - "`deny.toml`'s licence allow-list already permits the declared expression under either outcome, and is confirmed unchanged"
    - "Every place in the tree that states a licence — the manifests, the LICENSE file or files, the README badge and section, and the OCI image label — states the same thing"
    - "The signed 551-package compatibility checklist is annotated in place with the outcome and its reason, with the original text retained and marked, rather than rewritten or deleted"
    - "ADR-0025 records which branch was taken, what the alternative would have cost, and why the direction of change is safe for crates already published at 0.1.0"
  artifacts:
    - path: ".planning/decisions/0025-licence-posture.md"
      provides: "ADR recording the licence decision, its approver, and the reasoning for the chosen branch"
      contains: "## Considered Options"
    - path: ".project/Milestone_7-Production-Hardening/Epic_4/license-compatibility-decision-checklist.md"
      provides: "The signed checklist, annotated in place with a dated banner recording the outcome"
      contains: "2026"
  key_links:
    - from: "Cargo.toml and crates/*/Cargo.toml"
      to: "LICENSE files at the repository root"
      via: "the SPDX license field names the licence whose full text the root file or files carry"
      pattern: "^license = "
    - from: "README.md"
      to: "the root LICENSE file or files"
      via: "badge link target and License section link"
      pattern: "LICENSE"
    - from: ".planning/decisions/0025-licence-posture.md"
      to: ".project/Milestone_7-Production-Hardening/Epic_4/license-compatibility-decision-checklist.md"
      via: "the ADR cites the signed checklist as the artefact it either confirms or supersedes"
      pattern: "license-compatibility-decision-checklist"
  prohibitions:
    - "The permission set granted to existing consumers of the published 0.1.0 crates must not be narrowed. Adding a licence branch grants more; removing one revokes something already given to people who have already taken it, and no plan task may execute that direction even if a reviewer asks for tidiness."
    - "The decision must not be taken by inference. SEC-02 names this as a sign-off artefact with a named approver; proceeding on the recommendation without the checkpoint answer is prohibited regardless of how strong the recommendation reads."
---

<objective>
Close the three-way split between a signed `MIT OR Apache-2.0` policy, a PRD that says `MIT`, and
the `license = "MIT"` all eleven manifests actually declare — by putting the choice to the
repository owner at a blocking checkpoint and then executing whichever branch they select.

Purpose: this is the smallest diff in the phase and the largest consequence. The 0.1.0 crates are
published; the change is one-way. SEC-02 says in terms that it "must not be resolved by inference",
and the phase context records that every decision in it was auto-resolved with none confirmed by a
human. Both answers close SEC-02 and both produce the same deliverable shape; only one moves files.
This plan is written so that either branch is executable exactly as specified.
Output: one licence expression across eleven manifests and every other site that states one, an
annotated checklist, and ADR-0025.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-release-security-gate-integrity/09-CONTEXT.md
</context>

## Artifacts this phase produces

Newly-created symbols and files across all seven Phase 9 plans. Nothing on this list exists in the
tree today, so none of it can be a drift regression against a prior phase.

| Artifact | Plan | Kind |
|---|---|---|
| `crates/paladin-herald/CHANGELOG.md` | 09-01 | new file |
| `scripts/check-changelogs.sh` | 09-01 | new guard script |
| `Makefile` target `check-changelogs` | 09-01 | new target |
| CI step `Check per-crate changelogs` | 09-01 | new `ci.yml` step |
| `SECURITY-EXCEPTIONS.md` | 09-02 | new root file |
| `.planning/decisions/0024-rustsec-exception-governance.md` | 09-02 | new ADR |
| `.planning/decisions/0027-dockerfile-chef-planner-stage.md` | 09-03 | new ADR |
| `.crate-names.txt` | 09-04 | new allow-list file |
| `scripts/check-crate-names.sh` | 09-04 | new guard script |
| `Makefile` target `check-crate-names` | 09-04 | new target |
| CI step `Check crates.io package names` | 09-04 | new `ci.yml` step |
| `.planning/decisions/0026-crate-name-collision-guard.md` | 09-04 | new ADR |
| `LICENSE-MIT`, `LICENSE-APACHE` | 09-05 | new files (option-a branch only) |
| `.planning/decisions/0025-licence-posture.md` | 09-05 | new ADR |
| `scripts/check-advisory-register.sh` | 09-06 | new guard script |
| `Makefile` targets `check-advisory-register`, `check-gates` | 09-06 | new targets |
| CI step `Check advisory exception register` | 09-06 | new `ci.yml` step |
| `.planning/phases/09-release-security-gate-integrity/09-COVERAGE.md` | 09-07 | new declaration file |

## Correction inherited by this plan

`09-RESEARCH.md` §4 records `LABEL org.opencontainers.image.licenses="MIT"` as living in the runtime
`Dockerfile` at `:93`. Verified this session: the runtime `Dockerfile` carries no `LABEL` at all —
the only OCI label block in the tree is in **`Dockerfile.chef`**, at `:88-93`. This plan owns that
label, which is why `Dockerfile.chef` is in `files_modified` and why this plan depends on 09-03: the
nine-line deletion in wave 1 moves the label from roughly `:93` to roughly `:84`. Re-derive the line
number by reading the file; do not trust either number.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide the licence posture — this door does not reopen</name>
  <read_first>
    - `.project/Milestone_7-Production-Hardening/Epic_4/license-compatibility-decision-checklist.md` in full — the only signed governance artefact in this corpus: approver `DF3NDR`, date 2026-05-28, 551 packages inventoried with zero unknown, the dual-licence approval rule, and the MPL-2.0 acceptance
    - `.planning/REQUIREMENTS.md` lines 1148-1164 — SEC-02 in full, including the sentence forbidding resolution by inference
    - `Cargo.toml` line 40 and each `crates/*/Cargo.toml` licence line — all eleven currently read the same single-licence expression
    - `LICENSE` — a single root file, standard MIT template text, copyright holder `Am0rfu5`, year 2026; there is no Apache file
    - `deny.toml` lines 24-46 — the licence allow-list, which already permits both candidate branches and needs no change either way
    - `.planning/phases/09-release-security-gate-integrity/09-CONTEXT.md` → D-11 and D-12 in full
  </read_first>
  <decision>
Which licence expression do the root package and all ten library crates declare: the dual expression
recorded in the signed compatibility checklist, or the single expression the manifests and the M7
Epic 4 PRD carry today?
  </decision>
  <context>
This is the one gray area in Phase 9 whose auto-resolution is a placeholder for a human. Everything
else in the phase's context file was auto-resolved; this cannot be, because SEC-02 states that a
sign-off artefact with a named approver "must not be resolved by inference", and the only named
approver in the entire corpus is the repository owner.

It is also a one-way door. All ten library crates are published on crates.io at 0.1.0 under the
single expression. Publishing a later version under the dual expression grants every existing
consumer an additional permission, and that grant cannot be retracted — narrowing back afterwards
would revoke something people have already relied on. The direction of change is safe precisely
once, and only in one direction.

What each answer costs is known and is planned for in task 2, so neither option is more work to
select. Both close SEC-02.

Weighing the two, four things point at the dual expression. The compatibility checklist is the only
signed governance artefact this project has, with a named approver, a date, and 551 packages
inventoried at zero unknown; withdrawing it destroys the project's only completed compliance review,
whereas changing eleven metadata fields does not. The 551-package sign-off leans on the dual-licence
approval rule — "any SPDX expression containing a permissive MIT/Apache branch is acceptable by
default" was the stated basis for accepting one dependency's three-way expression, and under the
single-licence reading that rationale is, in the requirement's own words, weaker than recorded. The
direction of change is additive rather than subtractive. And the dual expression is the Rust
ecosystem norm, giving consumers an explicit patent grant.

Two things point the other way. The M7 Epic 4 PRD and the milestone overview both say the single
expression, and the shipped manifests agree with them — so the tree and its PRD are currently
consistent, and it is only the checklist that dissents. And the single-licence answer moves no
files: no rename, no new legal text, no README or label edits.
  </context>
  <options>
    <option id="option-a">
      <name>Dual expression — adopt what the signed checklist records (the D-11 recommendation)</name>
      <pros>Preserves the only completed compliance review in the corpus and the 551-package sign-off that rests on it; keeps the dual-licence approval rule's rationale intact rather than leaving it weaker than recorded; adds a permission rather than removing one, which is the only direction that is safe on already-published crates; matches the Rust ecosystem norm and gives consumers an explicit patent grant; `deny.toml` already permits it, so the enforcement surface needs no change.</pros>
      <cons>Eleven manifests change; the root licence file is renamed and a second verbatim legal text is added; the README badge, the README licence section and the OCI image label all need matching edits; two source documents need superseded annotations; and once published it cannot be narrowed back.</cons>
    </option>
    <option id="option-b">
      <name>Single expression — confirm what the manifests and the PRD already say (the D-12 fallback)</name>
      <pros>No file moves: manifests, licence file, README, image label and `deny.toml` are all already consistent with it; the tree and its governing PRD stay aligned; nothing is published under a new expression, so nothing becomes irrevocable.</pros>
      <cons>Withdraws the project's only signed governance artefact, which must then be annotated superseded with its reason; the 551-package review's dual-licence acceptance rule has to be re-justified explicitly for the dependency it was invoked on, rather than left implicit; consumers get no explicit patent grant.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - A selection of `option-a` or `option-b` is recorded verbatim in the plan SUMMARY, with the date and the responding party.
    - No file under `Cargo.toml`, `crates/*/Cargo.toml`, `LICENSE*`, `README.md` or `Dockerfile.chef` has been modified before this checkpoint returns — verifiable with `git status --porcelain` showing none of them dirty at the moment the checkpoint is raised.
  </acceptance_criteria>
  <resume-signal>Select: option-a (dual expression) or option-b (single expression)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Execute the selected branch across every site that states a licence</name>
  <reversibility rating="one-way">The ten library crates are published at 0.1.0; a licence expression published to crates.io cannot be retracted, and narrowing it afterwards would revoke a permission consumers already hold.</reversibility>
  <precondition>Task 1's checkpoint has returned an explicit `option-a` or `option-b` selection, recorded in the SUMMARY. Absent that selection this task does not run.</precondition>
  <read_first>
    - The task 1 checkpoint response recorded in this plan's SUMMARY
    - `Cargo.toml` around line 40, and each of the ten `crates/*/Cargo.toml` licence lines (`crates/paladin-core`, `paladin-ports`, `paladin-battalion`, `paladin-herald`, `paladin-llm`, `paladin-memory`, `paladin-storage`, `paladin-notifications`, `paladin-content`, `paladin-web`) — the field appears at `:6` in five of them and `:8` in five, so read each rather than assuming an offset
    - `README.md` line 7 (the shields.io badge) and lines 187-189 (the `## License` section)
    - `Dockerfile.chef` lines 85-95 — the OCI label block; re-derive the exact line, plan 09-03 moved it
    - `CHANGELOG.md` — the root changelog's `## [Unreleased]` section
    - `deny.toml` lines 24-46 — confirm by reading that no allow-list change is required under either branch
  </read_first>
  <files>Cargo.toml, crates/paladin-core/Cargo.toml, crates/paladin-ports/Cargo.toml, crates/paladin-battalion/Cargo.toml, crates/paladin-herald/Cargo.toml, crates/paladin-llm/Cargo.toml, crates/paladin-memory/Cargo.toml, crates/paladin-storage/Cargo.toml, crates/paladin-notifications/Cargo.toml, crates/paladin-content/Cargo.toml, crates/paladin-web/Cargo.toml, LICENSE, LICENSE-MIT, LICENSE-APACHE, README.md, CHANGELOG.md, Dockerfile.chef</files>
  <action>
Execute exactly one of the two branches below, chosen by task 1's answer. Do not blend them, and do
not execute either before the checkpoint returns.

There is no `[workspace.package]` table in the root manifest and no crate uses workspace inheritance
for the licence field — verified this session. Whichever branch runs, there is no single-point
shortcut: the eleven declarations are independent literals.

**If task 1 selected option-a (dual expression):**

1. Set the licence field in all eleven manifests to the SPDX expression `MIT OR Apache-2.0`. Edit
   each file individually. Change nothing else in any manifest — no version, no edition, no
   dependency.
2. Rename the root `LICENSE` to `LICENSE-MIT` using `git mv`, so the rename is recorded as a rename.
   Its content is unchanged, including the copyright holder and year.
3. Add `LICENSE-APACHE` at the repository root containing the canonical Apache License 2.0 text
   verbatim. Do not draft, paraphrase or abridge it, and do not fill in the appendix boilerplate
   with project-specific text — Apache-2.0 permits verbatim redistribution and any deviation is a
   legal risk with no corresponding benefit. If the canonical text cannot be fetched in this
   environment, stop and raise a checkpoint rather than reconstructing it from memory.
4. Update `README.md`: the shields.io badge at `:7` (its label text and its link target, which now
   points at a file that no longer exists under the old name) and the `## License` section at
   `:187-189`. The section should name both licences and say the reader may choose either.
5. Update the OCI image label in `Dockerfile.chef` to the same SPDX expression. Re-derive its line
   number by reading the file — plan 09-03's deletion moved it. Change no other label.
6. Add an entry to the root `CHANGELOG.md` under `## [Unreleased]` → `### Changed`, recording the
   relicensing, naming the approver and the date, and stating that this is an additional grant to
   existing consumers rather than a restriction.
7. Confirm `deny.toml` needs no change by reading its allow-list and recording that both branches of
   the expression are already permitted.

**If task 1 selected option-b (single expression):**

1. Leave all eleven manifests exactly as they are. Confirm with a read, and record the confirmation.
2. Leave `LICENSE` as it is; create no `LICENSE-MIT` and no `LICENSE-APACHE`.
3. Leave `README.md`'s badge and licence section as they are.
4. Leave `Dockerfile.chef`'s OCI label as it is.
5. Add no root `CHANGELOG.md` entry — nothing user-visible changed, and a changelog entry for a
   non-change is noise.
6. Confirm `deny.toml` needs no change, for the same reason as the other branch.
7. Record in the SUMMARY that the deliverable under this branch is entirely documentary and lands
   in task 3: the checklist annotation and the explicit re-justification of the dual-licence
   acceptance rule for the dependency it was invoked on.

Under either branch, run the manifest-parse check before committing, and record which branch ran at
the top of the SUMMARY so every downstream reader can tell without inferring from the diff.
  </action>
  <verify>
    <automated>bash -c 'set -e; N=$(grep -h "^license = " Cargo.toml crates/*/Cargo.toml | sort -u | wc -l); echo "DISTINCT=$N"; test "$N" -eq 1; for f in Cargo.toml crates/*/Cargo.toml; do c=$(grep -c "^license = " "$f"); test "$c" -eq 1 || { echo "BAD $f=$c"; exit 1; }; done; cargo metadata --offline --no-deps --format-version 1 > /dev/null && echo "MANIFESTS PARSE"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -h '^license = ' Cargo.toml crates/*/Cargo.toml | sort -u | wc -l` returns `1` under either branch.
    - `grep -h '^license = ' Cargo.toml crates/*/Cargo.toml | sort -u` prints `license = "MIT OR Apache-2.0"` under option-a, or `license = "MIT"` under option-b — and the SUMMARY records which branch ran, so the two are distinguishable without reading the diff.
    - For each of the eleven manifests, `grep -c '^license = '` returns `1` — no manifest lost the field.
    - `cargo metadata --offline --no-deps --format-version 1 > /dev/null` exits 0, and `cargo check --offline --workspace` exits 0.
    - Under option-a only: `test -f LICENSE-MIT && test -f LICENSE-APACHE && test ! -e LICENSE` exits 0; `git log --follow --oneline -1 -- LICENSE-MIT` shows the rename preserved history; `grep -c 'Apache License' LICENSE-APACHE` returns at least `1` and `wc -l < LICENSE-APACHE` exceeds `150` (the canonical text is long; a short file means it was paraphrased).
    - Under option-a only: `grep -c 'LICENSE-MIT\|LICENSE-APACHE' README.md` returns at least `2`, and `grep -c ']\(LICENSE\)' README.md` returns `0` — no README link still points at the old filename.
    - Under option-a only: `grep -c 'org.opencontainers.image.licenses="MIT OR Apache-2.0"' Dockerfile.chef` returns `1`; `git diff -- Dockerfile.chef | grep -cE '^[-+]LABEL' ` returns `2` (one removed, one added) and no other label line appears in the diff.
    - Under option-b only: `git status --porcelain Cargo.toml crates/ LICENSE README.md CHANGELOG.md Dockerfile.chef` is empty.
    - Under either branch: `git diff -- deny.toml` is empty — the enforcement surface needed no change and did not get one.
    - `git diff --stat -- '*.rs' | wc -l` returns `0`.
  </acceptance_criteria>
  <done>Every site in the tree that states a licence states the selected one, the manifests still parse, `deny.toml` is untouched, and the SUMMARY names the branch that ran.</done>
</task>

<task type="auto">
  <name>Task 3: Annotate the source documents and write ADR-0025</name>
  <read_first>
    - The task 1 checkpoint response and the task 2 branch record in this plan's SUMMARY
    - `.project/Milestone_7-Production-Hardening/Epic_4/license-compatibility-decision-checklist.md` in full — the approver, the date, the 551-package inventory, the dual-licence approval rule, and the specific dependency whose three-way expression that rule was invoked to accept
    - The M7 Epic 4 PRD §4.7.7 and the M7 overview AC 1 — the two places that state the single-licence answer
    - `.planning/decisions/0023-cli-dependency-isolation.md` — the seven-heading, no-frontmatter ADR shape
    - `.planning/phases/09-release-security-gate-integrity/09-CONTEXT.md` → D-00c (annotation, not rewriting), D-00d (amend in place, dated, superseded text retained), D-11, D-12, D-18
  </read_first>
  <files>.project/Milestone_7-Production-Hardening/Epic_4/license-compatibility-decision-checklist.md, .planning/decisions/0025-licence-posture.md</files>
  <action>
Annotate the source documents per D-00c — annotation, never rewriting. Each gets a dated correction
banner naming what changed and pointing at ADR-0025; each affected claim is corrected inline with
the original text retained and explicitly marked superseded. Never a separate corrections file.

Under **option-a**: annotate the compatibility checklist as **confirmed and now declared** — the
policy it records is what the manifests state as of this phase, with the ADR reference and the date.
Annotate the M7 Epic 4 PRD §4.7.7 and the M7 overview AC 1 single-licence claims as **superseded by
ADR-0025**, retaining their original text.

Under **option-b**: annotate the compatibility checklist as **superseded by ADR-0025**, retaining
its original text and stating the reason. Then do the thing D-12 specifically calls out and which is
easy to skip: re-justify the dual-licence approval rule's effect explicitly. That rule — any SPDX
expression containing a permissive MIT/Apache branch is acceptable by default — was the stated basis
for accepting one dependency's three-way expression, and under a single-licence posture that
rationale is weaker than recorded. Write out, in the annotation, the standalone reason that
dependency remains acceptable, so the 551-package review does not silently rest on a rule the
project no longer follows. Leave the PRD and overview claims unannotated; they are now correct.

Then write `.planning/decisions/0025-licence-posture.md` in ADR-0022/0023's exact shape: no YAML
frontmatter, seven headings in order — `## Status` (`Accepted`, then a bold `**Date:**` line),
`## Context`, `## Decision`, `## Considered Options`, `## Code Locations`, `## Code Conformance`,
`## Downstream Consumers`.

`## Context` states the three live positions with citations, notes that the enforcement surface in
`deny.toml` already followed the checklist regardless of which position wins, and records that the
decision was taken at a blocking checkpoint by the repository owner on a stated date — not inferred
— because SEC-02 requires exactly that.

`## Decision` names the selected expression, the approver, the date, and — this is the part a future
reader most needs — that the direction of change was the only safe one available on crates already
published at 0.1.0, or, under option-b, that no change was made and therefore nothing became
irrevocable.

`## Considered Options` records both branches with `(accepted)`/`(rejected)` tags and the reasons
from the checkpoint, including the cost the rejected branch would have carried. Write the rejected
branch fairly and in full; an ADR whose rejected option is a strawman is worse than no ADR.

`## Code Locations` lists all eleven manifest licence lines with their files, the root licence file
or files, the README badge and section, the OCI label in `Dockerfile.chef` with its re-derived line
number, and `deny.toml`'s allow-list with a note that it required no change.

`## Code Conformance` opens with `must change` under option-a or `no change needed` under option-b,
then a paragraph naming this plan's task 2 as the executor.

`## Downstream Consumers` names: the next real release cycle (crates.io accepts or rejects the
expression at publish time, which is out of this phase's scope and cannot be exercised here); Phase
10 / HARD-01's ledger rows for the licence sign-off and crate metadata; and any future consumer of
the published crate family.

Do not edit `PROMOTION.md`; the counter advances once, in the close-out.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/decisions/0025-licence-posture.md; test -f "$f" && head -1 "$f" | grep -q "^# ADR-0025" && ! head -1 "$f" | grep -q "^---" && diff <(grep "^## " "$f") <(printf "## Status\n## Context\n## Decision\n## Considered Options\n## Code Locations\n## Code Conformance\n## Downstream Consumers\n") && grep -cE "^(must change|no change needed)$" "$f" && grep -c "(rejected)" "$f" && grep -ci "supersed\|confirmed" .project/Milestone_7-Production-Hardening/Epic_4/license-compatibility-decision-checklist.md'</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 .planning/decisions/0025-licence-posture.md` starts with `# ADR-0025`.
    - `diff <(grep '^## ' .planning/decisions/0025-licence-posture.md) <(printf '## Status\n## Context\n## Decision\n## Considered Options\n## Code Locations\n## Code Conformance\n## Downstream Consumers\n')` produces no output, and `sed -n '1p' .planning/decisions/0025-licence-posture.md | grep -c '^---'` returns `0`.
    - `grep -c '(rejected)' .planning/decisions/0025-licence-posture.md` returns at least `1`, and the rejected branch's entry is at least three lines long — a strawman fails this.
    - `grep -cE '^(must change|no change needed)$' .planning/decisions/0025-licence-posture.md` returns `1`.
    - `grep -c 'DF3NDR' .planning/decisions/0025-licence-posture.md` returns at least `1` — the approver is named.
    - The compatibility checklist carries a dated banner referencing ADR-0025, and `git diff -- .project/Milestone_7-Production-Hardening/Epic_4/license-compatibility-decision-checklist.md | grep -cE '^\-[^-]'` returns `0` — annotation added, no original line removed.
    - Under option-b only: the checklist annotation contains a standalone re-justification for the dependency the dual-licence rule was invoked on, checkable by grepping the annotation for that dependency's name.
    - Under option-a only: the M7 Epic 4 PRD §4.7.7 and the M7 overview AC 1 each carry a superseded marker referencing ADR-0025, with their original text retained.
    - `git status --porcelain .planning/decisions/PROMOTION.md` is empty.
  </acceptance_criteria>
  <done>ADR-0025 records the decision, its approver and both branches fairly; the signed checklist is annotated in place with its original text retained; and under option-b the dual-licence rule's effect is re-justified explicitly rather than left implicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → crates.io consumers | The SPDX licence field published with a crate is a legal grant to everyone who downloads it; once published it cannot be recalled. |
| agent recommendation → a signed governance artefact | The corpus's only signed compliance review has a named human approver; an agent that resolves it by inference silently substitutes itself for that approver. |
| tree metadata → downstream compliance scanners | Consumers' own `cargo deny` runs read this project's declared expression; a divergence between manifests, licence files and image labels gives different scanners different answers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-19 | Repudiation | the licence decision itself | high | mitigate | Resolving a sign-off artefact by inference produces a decision no human can be shown to have made, on a change that cannot be undone. Task 1 is a blocking `checkpoint:decision` and task 2 carries a `<precondition>` that halts if no selection was recorded; ADR-0025 names the approver and the date. |
| T-09-20 | Elevation of privilege | published permission set | high | mitigate | Narrowing an already-published expression revokes a permission consumers already hold — a change no downstream party consented to. The prohibition block forbids that direction outright; only the additive branch is executable, and option-b changes nothing. |
| T-09-21 | Tampering | `LICENSE-APACHE` text | high | mitigate | A paraphrased or abridged legal text is a licence this project has not actually granted. The action requires verbatim canonical text and forbids reconstruction from memory; the acceptance criteria assert a line count consistent with the full text, and instruct a halt rather than a guess if it cannot be fetched. |
| T-09-22 | Spoofing | divergent licence statements across sites | medium | mitigate | Manifests, licence files, README badge and OCI label each answer "what licence is this?" independently; leaving one stale recreates the exact one-project-three-answers problem SEC-02 exists to close, just in a fourth place. All four sites are in `files_modified` and each has its own acceptance criterion. |
| T-09-23 | Information disclosure | `git mv` versus delete-and-create | low | mitigate | Recreating the licence file rather than renaming it loses the authorship history of a legal document. The action requires `git mv`, and an acceptance criterion checks `git log --follow`. |
| T-09-SC | Tampering | package-manager installs | high | accept | This plan installs nothing and changes no dependency: `git diff -- Cargo.lock` is empty, and the only manifest lines touched are the licence field. `cargo metadata --offline` runs against the existing lockfile with no network. The Package Legitimacy Gate does not fire. |
</threat_model>

<verification>
- The checkpoint response is recorded verbatim in the SUMMARY with its date, and the SUMMARY names which branch ran.
- `grep -h '^license = ' Cargo.toml crates/*/Cargo.toml | sort -u | wc -l` → `1`.
- `cargo metadata --offline --no-deps --format-version 1` exits 0; `cargo check --offline --workspace` exits 0.
- `git diff -- deny.toml` is empty.
- ADR-0025 matches the 0023 shape, names the approver, and records both branches.
- The signed checklist is annotated with no original line removed.
- `git diff --stat 49ad74c..HEAD -- '*.rs' | wc -l` → `0`.
- CI-only and out-of-scope, restated honestly in the close-out rather than inferred: crates.io accepting the declared expression at publish time. No publish happens in this phase.
</verification>

<success_criteria>
Asking "what licence is this project?" returns one answer, chosen by the named approver at a
blocking checkpoint rather than derived by an agent, declared by the root package and all ten
library crates, agreed by `deny.toml`, and stated identically everywhere else in the tree that
mentions a licence. Whichever branch ran, the artefact it superseded says so in its own text.
</success_criteria>

<output>
Create `.planning/phases/09-release-security-gate-integrity/09-05-SUMMARY.md` when done, opening
with the branch that ran and the verbatim checkpoint response.
</output>
