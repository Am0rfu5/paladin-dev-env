---
phase: 10-milestone-7-8-ground-truth-recorded-account
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/decisions/0028-m8-reconciliation-authoritative.md
  - .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_1/facade-audit.md
  - .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md
  - .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/prd-relocate-remaining-misplaced-modules.md
autonomous: true
requirements: [HARD-02]

must_haves:
  truths:
    - "Asking what happened in Milestone 8 returns `facade-cleanup-RECONCILIATION-2026-06-04.md`, because ADR-0028 names it authoritative over `Epic_1/facade-audit.md` and `Epic_3/infrastructure-adapter-disposition.md` (D-07)"
    - "The reason for the supersession is recorded as a fact rather than a procedure: both superseded documents describe ~4,400 LOC of orphaned, uncompiled duplicate files as active bridges that stay (D-07)"
    - "The reconciliation's orphan test is preserved as a runnable three-step procedure — no `mod <name>` declaration anywhere under `src/`, the directory's `mod.rs` re-exporting only from the leaf crate, and the leaf-crate file existing — not as prose about a procedure (D-08)"
    - "The three in-execution corrections survive into the record with do-not-re-delete force: `paladin_registry.rs` was consolidated into battalion rather than deleted, the `sqlite_*_repository.rs` files were the active default-build implementation resolved by commit `897e77e`, and the remainder genuinely were orphaned (D-08)"
    - "Milestone 8 Epic 3 is recorded complete in substance — 15 commits, ~10,250 net LOC removed, one new leaf crate — so no later phase plans its relocations as Milestone 9 candidates (D-09)"
    - "Milestone 8 Epic 6 is recorded complete despite the reconciliation rating it not verified and `deferred-items.md` omitting it, with the zero-match `use_cases` grep as evidence (D-09)"
    - "The Epic 3 §5 non-goal is recorded split — overridden for `paladin-herald`, still holding for `paladin-ml` — and ADR-0028 names Phase 11 / FACADE-03 in `Downstream Consumers` so Phase 11 cannot re-open it (D-09)"
    - "No `.project/` document receives two supersession banners: each of the three documents this plan annotates carries exactly one dated banner, and no other plan in this phase annotates any of them"
    - "Every original word in all three annotated documents is retained — struck or left intact beneath a blockquote — so the superseded claim is still readable (D-00c)"
    - statement: "The reconciliation's ~10,250 net-LOC-removed and 15-commit figures reproduce against the git history of the Milestone 8 range. `git log --numstat` over the six named commits is runnable here, so the executor re-derives both figures; if the commit range cannot be resolved in this checkout, the figures are recorded as reconciliation-stated rather than re-measured, and never as verified."
      verification: backstop
  artifacts:
    - path: ".planning/decisions/0028-m8-reconciliation-authoritative.md"
      provides: "The ADR naming the reconciliation authoritative, holding the orphan test, the three corrections, Epic 3/6 completeness, and the non-goal split"
      contains: "## Downstream Consumers"
    - path: ".project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_1/facade-audit.md"
      provides: "A dated supersession banner pointing at ADR-0028, original text retained"
      contains: "ADR-0028"
    - path: ".project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md"
      provides: "A dated supersession banner pointing at ADR-0028, original text retained"
      contains: "ADR-0028"
  key_links:
    - from: ".planning/decisions/0028-m8-reconciliation-authoritative.md"
      to: ".project/Milestone_8-Facade-Cleanup-Shim-Resolution/facade-cleanup-RECONCILIATION-2026-06-04.md"
      via: "Code Locations citing the reconciliation's §2 orphan test and §7 commit list by file:line"
      pattern: "facade-cleanup-RECONCILIATION-2026-06-04"
    - from: ".planning/decisions/0028-m8-reconciliation-authoritative.md"
      to: ".planning/ledgers/milestone-07-08.md"
      via: "Downstream Consumers naming the HARD-01 ledger rows that cite this ADR, written by plans 10-09 and 10-10"
      pattern: "HARD-01"
  prohibitions:
    - "A `.project/` correction must not delete or rewrite the original text. The superseded claim is the evidence; a clean-looking document that silently loses what it used to assert destroys the record the annotation exists to preserve."
    - "The reason for the supersession must not be softened into a procedural one. Recording that the reconciliation is newer, or more thorough, in place of the factual reason — that both superseded documents classified orphaned uncompiled files as active bridges — is prohibited even though it reads as less accusatory."
    - "A do-not-re-delete marker must not be omitted for brevity. The three in-execution corrections exist so nobody re-executes the original audit against `paladin_registry.rs` or the sqlite repositories; dropping any of them from the record is prohibited."
---

<objective>
Write ADR-0028 recording `facade-cleanup-RECONCILIATION-2026-06-04.md` as the authoritative account of
Milestone 8, and annotate the two documents it supersedes plus the Epic 3 non-goal clause at source.

Purpose: two ingested documents assert the opposite of what the tree shows, which is a contested
position and therefore gets an ADR under D-00g rather than a ledger row alone. Without it, three
completed Epics get re-planned as outstanding and someone re-deletes files the original audit
mis-classified.
Output: ADR-0028, two dated supersession banners, one inline clause correction.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-RESEARCH.md
</context>

## Artifacts this phase produces

| Artifact | Plan | Kind |
|---|---|---|
| `.planning/ledgers/milestone-07-08.md` | 10-01, 10-07…10-10, 10-11 | new file |
| `.planning/decisions/0028-m8-reconciliation-authoritative.md` | **10-02** | new ADR |
| `.planning/decisions/0029-version-trajectory-history.md` | 10-03 | new ADR |
| `.planning/decisions/0030-milestone-7-self-numbering.md` | 10-03 | new ADR |
| `.planning/decisions/0031-extracted-crate-dependency-rule.md` | 10-04 | new ADR |
| `.planning/decisions/0032-pdf-extraction-capability.md` | 10-05 | new ADR |
| `.planning/decisions/0033-cargo-doc-warning-bar.md` | 10-06 | new ADR |
| `.planning/REQUIREMENTS.md` | 10-01, 10-11 | modified |
| `.planning/ROADMAP.md` | 10-01, 10-11 | modified |
| `.planning/PROJECT.md` | 10-11 | modified |
| `.planning/decisions/PROMOTION.md` | 10-11 | modified (next free → 0034) |
| `.project/…/Epic_1/facade-audit.md` | **10-02** | annotated |
| `.project/…/Epic_3/infrastructure-adapter-disposition.md` | **10-02** | annotated |
| `.project/…/Epic_3/prd-relocate-remaining-misplaced-modules.md` | **10-02** | annotated |
| `.project/Milestone_7-Production-Hardening/overview/Milestone_7-Tier_4-Production_Hardening.md` | 10-03 | annotated |
| `.project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md` | 10-04, 10-05 | annotated |
| `.project/…/Epic_5/prd-document-facade-crate-role.md` | 10-06 | annotated |
| `.project/Milestone_7-Production-Hardening/Epic_4/epic-4-completion-summary.md` | 10-06 | annotated |
| `crates/paladin-content/Cargo.toml` | 10-05 | modified |
| `crates/paladin-content/CHANGELOG.md` | 10-05 | modified |
| `.cargo/audit.toml` | 10-05 | modified |
| `Makefile` | 10-06 | modified |

**No `.rs` file appears on this list, and none may be added to it (D-23).**

## Annotation ownership — this plan's three documents are exclusive to it

`facade-audit.md`, `infrastructure-adapter-disposition.md` and
`prd-relocate-remaining-misplaced-modules.md` are annotated by plan 10-02 and by no other plan in this
phase. `prd-extract-infrastructure-crates.md` is shared between 10-04 (§6.1, Goal 2) and 10-05
(§4.4.1, §4.4.6) and is out of this plan's reach entirely. Each annotated document ends this phase
with exactly one banner per annotating plan; two banners never stack on one clause.

<tasks>

<task type="auto">
  <name>Task 1: Write ADR-0028 — the reconciliation as the authoritative account of Milestone 8</name>
  <reversibility rating="costly">Phase 11 plans FACADE-02 and FACADE-03 against this record; reversing it means re-deciding which of the five deferred items and two removed features were already executed (D-09).</reversibility>
  <read_first>
    - `.planning/decisions/0027-dockerfile-chef-planner-stage.md` in full — the seven-heading, no-frontmatter ADR shape to match exactly, including the `## Status` / `**Date:**` block, the precedence-citing opening of `## Decision`, and the `(accepted)`/`(rejected)` tags in `## Considered Options`.
    - `.planning/decisions/0015-core-ports-dependency-allowlist.md` lines 38-92 — the multi-part `## Decision` shape, for a decision carrying several distinct claims.
    - `.planning/decisions/PROMOTION.md` lines 75-106 — the required heading set in exact order, the bullet-list requirement on `## Code Locations` and `## Considered Options`, and the supersession mechanism.
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/facade-cleanup-RECONCILIATION-2026-06-04.md` in full — §2's orphan test, §7's commit list (`66f6c4e`, `8bd7073`, `ff829e2`, `5a7c901`, `897e77e`, `4c7857e`), the three in-execution corrections, and its own header naming the documents it supersedes.
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_1/facade-audit.md` — locate and note the exact `file:line` of the classification claim (151 stay / 13 move / 25 delete) and of the "active bridges" language.
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md` — locate the 20-row all-Stays table, the two rows disagreeing with the governing PRD, and the `paladin-arsenal` / `paladin-sanctum` mentions.
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/prd-relocate-remaining-misplaced-modules.md` line 211 — the §5 non-goal clause naming `paladin-herald` and `paladin-ml`.
    - `.planning/intel/code-verification.md` lines 383-394 — the two favourable-direction contradictions (Epic 6 and Epic 3), which are HARD-02(b) and (d).
    - `.planning/REQUIREMENTS.md` lines 1373-1403 — HARD-02 in full, clauses (a) through (e).
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-00b, D-00c, D-00e, D-00g, D-07, D-08, D-09, D-24.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md` → the six-new-ADRs section, in full.
  </read_first>
  <files>.planning/decisions/0028-m8-reconciliation-authoritative.md</files>
  <action>
Write `.planning/decisions/0028-m8-reconciliation-authoritative.md` with the exact seven headings in
order — `## Status`, `## Context`, `## Decision`, `## Considered Options`, `## Code Locations`,
`## Code Conformance`, `## Downstream Consumers` — with no YAML frontmatter, matching ADR-0027's file
shape. Title line: `# ADR-0028: Milestone 8's authoritative account — the 2026-06-04 reconciliation`.
`## Status` body is `Accepted` followed by a bold `**Date:**` line carrying today's date.

`## Context` states what each document asserts with its `file:line`, then what the tree shows with its
`file:line`. The load-bearing fact, stated plainly: `Epic_1/facade-audit.md` and
`Epic_3/infrastructure-adapter-disposition.md` describe approximately 4,400 LOC of **orphaned,
uncompiled duplicate files** as "active bridges that stay". That is a factual mis-classification, not
a difference of opinion or a matter of one document being newer, and it must be stated that way
(D-07). Record that the reconciliation's own header names both documents by path.

`## Decision` opens by citing the D-00b precedence order verbatim — ADR → shipped tree →
`.planning/codebase/` map → `intel/code-verification.md` → PRD → DOC → task-list checkbox, an ADR that
contradicts shipped code is an instruction to change the code — then states the one-sentence verdict:
`facade-cleanup-RECONCILIATION-2026-06-04.md` is the authoritative account of Milestone 8 and both
named documents are superseded by it. Then four numbered sub-decisions, kept visually separate in
ADR-0015's multi-part shape:

  (i) **The reproducible orphan test**, recorded as a runnable procedure and not as prose about one:
      a ripgrep for the module declaration of the file's name across `src/` returns nothing; the
      containing directory's `mod.rs` only re-exports from the corresponding `paladin_<crate>` leaf
      crate; the leaf-crate file exists. Give the three steps as three numbered, copy-pasteable
      commands with their expected outcomes. State that this is the highest-fidelity verification
      procedure in the run-4 corpus and that it is reusable by any later phase.

  (ii) **The three in-execution corrections**, each with an explicit do-not-re-delete marker:
      `paladin_registry.rs` was **not** a duplicate — the facade's 418-LOC implementation was richer
      than battalion's 67-LOC `pub(crate)` copy, so the richer one was consolidated *into* battalion;
      the `sqlite_*_repository.rs` files were **not** redundant — they were the active default-build
      implementation, resolved by making `paladin-storage` non-optional in commit `897e77e`; the rest
      genuinely were orphaned — `mysql_content_repository.rs`, the `input/*` fetchers, `document/*`,
      `output/api_content_deliverer.rs`, `error_log_adapter.rs`.

  (iii) **Epic 3 is complete in substance and Epic 6 is complete despite its own record.** Epic 3
      executed the relocations it had deferred to Milestone 9 — 15 commits, approximately 10,250 net
      LOC removed, one new leaf crate. Re-derive both figures from `git log --numstat` over the
      reconciliation's §7 commit list and record what you measured alongside what the reconciliation
      states; if the range cannot be resolved in this checkout, say so and label the figures
      reconciliation-stated rather than re-measured. Epic 6 is complete despite the reconciliation
      rating it "Not verified; low priority" and `deferred-items.md` omitting it:
      `crates/paladin-content/src/services/` ships, `lib.rs` declares its services module, and a
      workspace-wide search for the pre-rename module name across `src/`, `crates/`, `tests/`,
      `examples/` and `benches/` returns zero matches. Run that search and record its exact command
      and output.

  (iv) **The Epic 3 §5 non-goal is split.** The clause at
      `prd-relocate-remaining-misplaced-modules.md:211` names the exact crate that was then created in
      the same milestone. Record it **overridden for `paladin-herald` and still holding for
      `paladin-ml`**, and state that this split is what FACADE-03(b) depends on.

`## Considered Options` is a bullet list, each item tagged `(accepted)` or `(rejected)` with a
one-line reason. Include at minimum: naming the reconciliation authoritative and annotating both
documents at source (accepted); leaving the contradiction to a ledger row alone (rejected — D-00g
reserves ledger-only treatment for code-settled defects, and this is two documents asserting the
opposite of the tree); rewriting the two superseded documents to match the tree (rejected — D-00c
forbids rewriting, and the mis-classification is the evidence); recording the non-goal as wholly
overridden (rejected — it still holds for `paladin-ml`, and collapsing the split is precisely what
FACADE-03(b) would then have to re-derive).

`## Code Locations` is a bullet list of exact `file:line` citations covering: the reconciliation's §2
and §7; the two superseded documents' asserting lines; `prd-relocate-remaining-misplaced-modules.md:211`;
`crates/paladin-content/src/lib.rs`'s services declaration; `crates/paladin-herald/` as the crate the
non-goal named; and `intel/code-verification.md:383-394` for the two favourable-direction
contradictions.

`## Code Conformance` is the standalone word `conforms` — the tree already matches this record; nothing
in this ADR instructs a code change.

`## Downstream Consumers` is a bullet list naming, at minimum: **Phase 11 / FACADE-02** (the Epic 3
relocations already executed, so its Milestone 9 candidate list must not re-plan them); **Phase 11 /
FACADE-03(b)** (the non-goal split, overridden for `paladin-herald`, still holding for `paladin-ml`);
**Phase 11 / FACADE-04** (`infrastructure-adapter-disposition.md` is superseded, and it is FACADE-04's
subject for `paladin-arsenal` / `paladin-sanctum`); and **Phase 10 / HARD-01** (the ledger rows written
by plans 10-09 and 10-10 that cite this ADR).

Every `file:line` in this ADR must be one this task re-read. Do not copy a citation from
`10-CONTEXT.md` or `10-RESEARCH.md` without confirming it resolves.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/decisions/0028-m8-reconciliation-authoritative.md; test -f "$f" && head -1 "$f" | grep -q "^# ADR-0028" && ! head -1 "$f" | grep -q "^---" && diff <(grep "^## " "$f") <(printf "## Status\n## Context\n## Decision\n## Considered Options\n## Code Locations\n## Code Conformance\n## Downstream Consumers\n") && grep -qx "conforms" "$f" && echo "rejected=$(grep -c "(rejected)" "$f")" && echo "facade03=$(grep -c "FACADE-03" "$f")"'</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 .planning/decisions/0028-m8-reconciliation-authoritative.md` starts with `# ADR-0028`.
    - `diff <(grep '^## ' .planning/decisions/0028-m8-reconciliation-authoritative.md) <(printf '## Status\n## Context\n## Decision\n## Considered Options\n## Code Locations\n## Code Conformance\n## Downstream Consumers\n')` produces no output.
    - `sed -n '1p' .planning/decisions/0028-m8-reconciliation-authoritative.md | grep -c '^---'` returns `0` — no frontmatter.
    - `grep -qx 'conforms' .planning/decisions/0028-m8-reconciliation-authoritative.md` exits 0.
    - `grep -c '(rejected)' .planning/decisions/0028-m8-reconciliation-authoritative.md` returns at least `3`.
    - `grep -c 'paladin_registry' .planning/decisions/0028-m8-reconciliation-authoritative.md` returns at least `1`, `grep -c 'sqlite_' …` returns at least `1`, and `grep -c '897e77e' …` returns at least `1` — all three in-execution corrections survive.
    - `grep -c 'FACADE-03' .planning/decisions/0028-m8-reconciliation-authoritative.md` returns at least `1` and `grep -c 'FACADE-02' …` returns at least `1`.
    - `grep -c '4,400\|4400' .planning/decisions/0028-m8-reconciliation-authoritative.md` returns at least `1` — the factual reason for the supersession is stated with its figure.
    - The workspace-wide search proving Epic 6 complete is recorded verbatim in `## Decision` and returns zero matches when re-run: `grep -rn 'use_cases' src/ crates/ tests/ examples/ benches/ --include='*.rs' | wc -l` returns `0`.
    - `git status --porcelain -- '*.rs'` is empty.
  </acceptance_criteria>
  <done>ADR-0028 exists in the seven-heading shape, names the reconciliation authoritative on factual grounds with the ~4,400 LOC figure, carries the orphan test as three runnable steps, preserves all three do-not-re-delete corrections, records Epic 3 and Epic 6 complete with re-run evidence, splits the non-goal, and names FACADE-02, FACADE-03(b) and FACADE-04 downstream.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate `facade-audit.md` and `infrastructure-adapter-disposition.md` as superseded at source</name>
  <read_first>
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_1/facade-audit.md` in full — identify the document's own title block and the classification section carrying 151 stay / 13 move / 25 delete, so the banner lands at the top and names the specific claim.
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md` in full — identify the title block, the 20-row all-Stays table, the two rows disagreeing with the governing PRD, and the `2025-01` date inconsistent with every other M8 document.
    - `.planning/decisions/0028-m8-reconciliation-authoritative.md` (post-task-1) — the ADR the banners point at, so the relative link resolves.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md` → the `.project/` annotation banners section, **Analog A / Shape B (full-section supersession)** from commit `74a05fe` — the compact blockquote shape, quoted verbatim there.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-00c, D-07.
    - Run `git show 74a05fe -- '*.md' | head -60` to see the live precedent banner as it was actually committed.
  </read_first>
  <files>.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_1/facade-audit.md, .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md</files>
  <action>
Add one dated supersession banner to the top of each of the two documents, using Shape B from
`10-PATTERNS.md` — a markdown blockquote opening with a bold `SUPERSEDED BY [ADR-0028](<relative path
to the ADR>) — 2026-08-08.` line, followed by two to four sentences of reason, closing with an explicit
statement that the original text below is retained unmodified.

Compute the relative path from each document to
`.planning/decisions/0028-m8-reconciliation-authoritative.md` by counting directory levels, and verify
the link resolves before finishing — `Epic_1/` and `Epic_3/` sit at the same depth, so both links have
the same shape.

For `facade-audit.md`, the banner's reason states: this document classified 189 audited files as 151
stay / 13 move / 25 delete, and approximately 4,400 LOC of the "stay" set were orphaned, uncompiled
duplicate files rather than active bridges. Name `facade-cleanup-RECONCILIATION-2026-06-04.md` as the
account that replaces it, and name the reconciliation's reproducible orphan test as the method that
established the mis-classification. Add the do-not-re-delete warning inline: three of this document's
classifications were corrected during execution, and ADR-0028 §(ii) holds them.

For `infrastructure-adapter-disposition.md`, the banner's reason states: this document's 20-row table
marks every adapter group as staying in the facade and defers all List B moves to Milestone 9, both of
which the tree contradicts — the relocations executed inside Milestone 8. Also name the document's two
further defects recorded by run 4: two rows disagree with the governing PRD, and it names
`paladin-arsenal` and `paladin-sanctum`, neither of which exists in the tree. State that it remains
FACADE-04's subject for those two names.

Both banners are additive. Do not strike, delete, reflow, or re-word one character of the existing
documents. Do not touch the tables, the dates, or the headings. The whole edit in each file is the
inserted blockquote plus one blank line.
  </action>
  <verify>
    <automated>bash -c 'for f in .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_1/facade-audit.md .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md; do echo "$f banners=$(grep -c "SUPERSEDED BY" "$f") adr=$(grep -c "ADR-0028" "$f") removed=$(git diff --numstat -- "$f" | awk "{print \$2}")"; done'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SUPERSEDED BY' .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_1/facade-audit.md` returns exactly `1` — one banner, not two stacked.
    - `grep -c 'SUPERSEDED BY' .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md` returns exactly `1`.
    - `grep -c 'ADR-0028' <each file>` returns at least `1` for both, and the relative link resolves: `test -f "$(dirname <file>)/$(grep -o '](\.\./[^)]*)' <file> | head -1 | tr -d '](' )"` exits 0.
    - `git diff --numstat -- .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_1/facade-audit.md` shows `0` deleted lines — the annotation is purely additive.
    - `git diff --numstat -- .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md` shows `0` deleted lines.
    - `grep -c 'paladin-arsenal' .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md` is unchanged from its pre-edit value plus the banner's own mention, and `FACADE-04` appears at least once.
    - `git status --porcelain -- '*.rs' Cargo.toml Makefile` is empty.
  </acceptance_criteria>
  <done>Both superseded documents open with exactly one dated banner naming ADR-0028 and the factual reason, every original line survives, and the diffs show zero deletions.</done>
</task>

<task type="auto">
  <name>Task 3: Annotate the Epic 3 §5 non-goal clause with the herald / paladin-ml split</name>
  <read_first>
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/prd-relocate-remaining-misplaced-modules.md` — read the whole §5 Non-Goals section around line 211 so the strike lands on the right clause and the surrounding list items are untouched. Re-derive the line number.
    - `.planning/decisions/0028-m8-reconciliation-authoritative.md` (post-task-1) — §(iv) of `## Decision`, so the correction and the ADR say the same thing.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md` → **Analog B / Shape A (inline strike-and-correct)** from commit `94814ff` — the document-head correction blockquote plus the struck clause plus the `**Corrected (dated …, …):**` paragraph.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-00c, D-09.
    - `ls crates/` — confirm `paladin-herald` exists and `paladin-ml` does not, this session, before writing either half of the split.
  </read_first>
  <files>.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/prd-relocate-remaining-misplaced-modules.md</files>
  <action>
Apply Shape A — inline strike-and-correct — to the §5 Non-Goals clause at approximately `:211`, the one
declaring that no new crates are created and naming `paladin-herald` and `paladin-ml` as out of scope.

Two edits, both additive except for the strike markers on the one clause:

1. A dated correction blockquote at the top of the document, in the Shape A head-banner form:
   `**Correction (dated 2026-08-08, HARD-02):**` followed by a statement that this document's §5
   Non-Goals clause (struck below) names the exact crate that was then created inside the same
   milestone, that ADR-0028 records the clause as overridden for `paladin-herald` and still holding for
   `paladin-ml`, and that the original text is retained below with an inline correction and nothing is
   deleted. Link ADR-0028 by relative path.

2. On the clause itself: wrap the existing sentence in `~~` strike markers, preserving it word for
   word, and immediately follow it with a `**Corrected (dated 2026-08-08, HARD-02):**` paragraph
   stating the split — overridden for `paladin-herald`, which exists at `crates/paladin-herald/` and
   was created during Milestone 8 by the reconciliation's own relocation work; still holding for
   `paladin-ml`, which does not exist in the tree and whose reintroduction condition is FACADE-03(b)'s
   subject. Confirm both halves with the `ls crates/` output run in this task and cite it.

Do not modify any other §5 bullet. Do not renumber the list. Do not touch the document's own dates or
headings.
  </action>
  <verify>
    <automated>bash -c 'f=.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/prd-relocate-remaining-misplaced-modules.md; echo "corrections=$(grep -c "Corrected (dated 2026-08-08, HARD-02)" "$f")"; echo "strikes=$(grep -c "~~" "$f")"; echo "adr=$(grep -c "ADR-0028" "$f")"; echo "herald_dir=$(test -d crates/paladin-herald && echo yes || echo no)"; echo "ml_dir=$(test -d crates/paladin-ml && echo yes || echo no)"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Corrected (dated 2026-08-08, HARD-02)' .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/prd-relocate-remaining-misplaced-modules.md` returns exactly `1`.
    - `grep -c '~~' <that file>` returns at least `2` — the original clause is struck, not removed.
    - `grep -c 'ADR-0028' <that file>` returns at least `1` and the relative link resolves to an existing file.
    - `test -d crates/paladin-herald` exits 0 and `test -d crates/paladin-ml` exits 1 — both halves of the split are true of the tree at write time, and the task records the `ls crates/` output that proved it.
    - `grep -c 'FACADE-03' <that file>` returns at least `1`.
    - `git diff --numstat -- <that file>` shows exactly `1` deleted line — the single clause replaced by its struck form; every other line is additive.
    - `git status --porcelain -- '*.rs'` is empty.
  </acceptance_criteria>
  <done>The §5 non-goal clause survives struck and intact, followed by a dated correction recording it overridden for `paladin-herald` and still holding for `paladin-ml`, with the directory listing that proved both halves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingested `.project/` audit documents → `.planning/decisions/` at top precedence | Two documents assert the opposite of the tree; an ADR promoting one of them above the others changes what every later phase treats as true. |
| the reconciliation's stated figures → an ADR's `## Code Locations` | 15 commits and ~10,250 net LOC are the reconciliation's own numbers; recording them as measured without re-deriving them puts an unverified figure at ADR precedence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Repudiation | ADR-0028's `## Code Locations` figures | high | mitigate | A figure copied from a superseded-adjacent document and recorded at ADR precedence is unfalsifiable afterwards. The task requires re-deriving the commit count and net-LOC figure from `git log --numstat` over the reconciliation's own §7 list, and recording measured-versus-stated explicitly when the range will not resolve. |
| T-10-06 | Tampering | the three in-execution corrections | high | mitigate | Losing any of the three do-not-re-delete markers restores the original audit's mis-classification as live guidance, and the next facade-cleanup phase re-deletes `paladin_registry.rs` or the sqlite repositories. The acceptance criteria grep for all three by name and for commit `897e77e`. |
| T-10-07 | Tampering | the two annotated `.project/` documents | medium | mitigate | An annotation that rewrites rather than annotates destroys the evidence of the mis-classification. Both banner tasks assert `git diff --numstat` shows zero deleted lines, and the clause task allows exactly one. |
| T-10-08 | Spoofing | the relative ADR links inside `.project/` | low | mitigate | A banner pointing at a path that does not resolve reads as authoritative and leads nowhere. Each task resolves its own relative link with `test -f` before finishing. |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing and modifies no manifest. `10-RESEARCH.md`'s Package Legitimacy Audit records the phase as not-applicable. `git status --porcelain -- Cargo.toml Cargo.lock` is empty for this plan. |
</threat_model>

<verification>
- ADR-0028's seven headings match ADR-0027's exactly, in order, with no frontmatter, and `## Code Conformance` reads `conforms`.
- All three in-execution corrections and commit `897e77e` appear in ADR-0028.
- `FACADE-02`, `FACADE-03` and `FACADE-04` each appear in `## Downstream Consumers`.
- Each of the two supersession banners appears exactly once in its document, with zero deleted lines in the diff.
- The §5 non-goal clause is struck and followed by a dated correction; `crates/paladin-herald/` exists and `crates/paladin-ml/` does not.
- `git status --porcelain -- '*.rs'` → empty. D-23's boundary holds for this plan.
- The backstop truth about the 15-commit / ~10,250-LOC figures is recorded with its measured-or-stated disposition rather than asserted.
</verification>

<success_criteria>
The question "what happened in Milestone 8?" resolves to the reconciliation through an ADR that states
the factual reason it wins, carries the orphan test as a runnable procedure, and holds all three
do-not-re-delete corrections; both superseded documents say so on their own first screen without
losing a word of what they used to claim; and the non-goal that named the crate it then created is
recorded split rather than wholly overridden.
</success_criteria>

<output>
Create `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-02-SUMMARY.md` when done.
</output>
