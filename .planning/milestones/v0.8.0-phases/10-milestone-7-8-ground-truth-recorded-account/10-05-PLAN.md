---
phase: 10-milestone-7-8-ground-truth-recorded-account
plan: 05
type: execute
wave: 2
depends_on: ["10-04"]
files_modified:
  - .planning/decisions/0032-pdf-extraction-capability.md
  - crates/paladin-content/Cargo.toml
  - crates/paladin-content/CHANGELOG.md
  - .cargo/audit.toml
  - .project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md
autonomous: true
requirements: [HARD-06]

must_haves:
  truths:
    - "Asking whether PDF extraction is supported returns one answer: yes, unconditionally, in every build of `paladin-content`, because `pdf-extract` is an unconditional dependency and `PdfExtractor` is declared, constructed and called with no feature gate (D-17)"
    - "The answer is consistent with the advisory suppression that assumes `pdf-extract` is in the dependency graph: `.cargo/audit.toml`'s `RUSTSEC-2026-0187` entry states the actual reachability path rather than attributing it to a feature that gates nothing (D-19)"
    - "The `RUSTSEC-2026-0187` suppression itself is unchanged — the entry stays in the ignore list, `SECURITY-EXCEPTIONS.md` is untouched, and only the comment's stated reasoning is corrected (D-19)"
    - "The corrected `.cargo/audit.toml` comment states the actual reachability path — `pdf-extract` unconditional inside `paladin-content`, `paladin-content` optional in the facade — rather than merely negating the old wrong parenthetical, so a future security reviewer is not left with a hole where the reasoning was"
    - "The inert `pdf` feature declaration is gone from `crates/paladin-content/Cargo.toml`, and `news-api` — a byte-identical empty declaration that legitimately gates a dependency-free module — is still present, proving the deletion targeted the one inert feature rather than the shape (D-18)"
    - "The five surviving `[features]` entries keep their existing relative order; the manifest fix is subtraction only, with no entry added, reordered, or reformatted"
    - "The consumer-visible cost is recorded: `crates/paladin-content/CHANGELOG.md` states that building with the removed feature begins to fail where it previously succeeded and did nothing (D-18)"
    - "M7 Epic 1 PRD §4.4.1 and §4.4.6 each carry a dated inline correction naming ADR-0032, with the original requirement wording retained struck (D-19)"
    - "Phase 12 / SUPPLY-02 receives this as an answer already delivered rather than a question to re-derive, named in ADR-0032's `Downstream Consumers` (D-19)"
    - statement: "`cargo audit` and `cargo deny check` pass against the reconciled `.cargo/audit.toml`. Neither tool is installable in this environment — `crates.io` returns HTTP 403, unchanged from Phase 9 — so the plan lands the comment correction, records the exact command a CI runner executes, and scopes the claim as CI-only. A pass is never inferred."
      verification: backstop
  artifacts:
    - path: ".planning/decisions/0032-pdf-extraction-capability.md"
      provides: "The PDF capability answer, the inert-feature disposition, and the corrected reachability reasoning"
      contains: "## Code Conformance"
    - path: "crates/paladin-content/Cargo.toml"
      provides: "A features block in which every declared feature gates something"
      contains: "news-api"
    - path: ".cargo/audit.toml"
      provides: "A RUSTSEC-2026-0187 comment whose stated reachability path is true"
      contains: "RUSTSEC-2026-0187"
    - path: "crates/paladin-content/CHANGELOG.md"
      provides: "The consumer-visible record of the removed feature name"
      contains: "paladin-content"
  key_links:
    - from: ".planning/decisions/0032-pdf-extraction-capability.md"
      to: "crates/paladin-content/src/adapters/document/document_adapter.rs"
      via: "Code Locations citing the ungated PdfExtractor struct field, its constructor and its two call sites as the source-level proof"
      pattern: "document_adapter\\.rs"
    - from: ".cargo/audit.toml"
      to: "SECURITY-EXCEPTIONS.md"
      via: "The corrected comment supporting the compensating-control row for RUSTSEC-2026-0187, which is itself unchanged"
      pattern: "RUSTSEC-2026-0187"
  prohibitions:
    - "A published feature must not be removed without recording the consumer-visible cost. Building the crate with the removed feature begins to fail where it previously succeeded and did nothing; omitting that from `CHANGELOG.md` is prohibited even though the change is behaviourally inert."
    - "A security-governance comment must not be left less informative than it was. Deleting the wrong parenthetical and stopping there leaves a future reviewer with no stated reachability path at all, which is worse than a wrong one because it reads as if none was needed."
    - "A closure claim must not be recorded as measured when the tool was unavailable. `cargo audit` and `cargo deny` cannot run in this environment; recording them as passing against the reconciled configuration is prohibited even though CI would likely pass."
    - "The three-file config boundary must not be widened silently. A plan that finds it needs a `.rs` edit must stop and say so rather than absorb the scope — D-23 is the boundary and an unannounced `.rs` change in a ground-truth phase is prohibited."
---

<objective>
Write ADR-0032 answering the PDF capability question, execute the manifest and advisory-comment changes
it records, and annotate the two M7 Epic 1 clauses those changes supersede.

Purpose: HARD-06 lists three facts pointing in two directions, and SEC-01's suppression reasoning rests
on one of them being right. The source-level read settles it — the feature gates nothing, and the
capability ships unconditionally — which makes this the one ADR in this phase whose `Code Conformance`
is `must change`, and makes the change two config lines rather than an architecture question.
Output: ADR-0032, one deleted manifest line, one CHANGELOG entry, one corrected advisory comment, two
dated inline corrections.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-RESEARCH.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-04-SUMMARY.md
</context>

## Artifacts this phase produces

| Artifact | Plan | Kind |
|---|---|---|
| `.planning/ledgers/milestone-07-08.md` | 10-01, 10-07…10-10, 10-11 | new file |
| `.planning/decisions/0028-m8-reconciliation-authoritative.md` | 10-02 | new ADR |
| `.planning/decisions/0029-version-trajectory-history.md` | 10-03 | new ADR |
| `.planning/decisions/0030-milestone-7-self-numbering.md` | 10-03 | new ADR |
| `.planning/decisions/0031-extracted-crate-dependency-rule.md` | 10-04 | new ADR |
| `.planning/decisions/0032-pdf-extraction-capability.md` | **10-05** | new ADR |
| `.planning/decisions/0033-cargo-doc-warning-bar.md` | 10-06 | new ADR |
| `.planning/REQUIREMENTS.md` | 10-01, 10-11 | modified |
| `.planning/ROADMAP.md` | 10-01, 10-11 | modified |
| `.planning/PROJECT.md` | 10-11 | modified |
| `.planning/decisions/PROMOTION.md` | 10-11 | modified (next free → 0034) |
| `.project/…/Epic_1/facade-audit.md` | 10-02 | annotated |
| `.project/…/Epic_3/infrastructure-adapter-disposition.md` | 10-02 | annotated |
| `.project/…/Epic_3/prd-relocate-remaining-misplaced-modules.md` | 10-02 | annotated |
| `.project/Milestone_7-Production-Hardening/overview/Milestone_7-Tier_4-Production_Hardening.md` | 10-03 | annotated |
| `.project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md` | 10-04 (§6.1, Goal 2), **10-05** (§4.4.1, §4.4.6) | annotated |
| `.project/…/Epic_5/prd-document-facade-crate-role.md` | 10-06 | annotated |
| `.project/Milestone_7-Production-Hardening/Epic_4/epic-4-completion-summary.md` | 10-06 | annotated |
| `crates/paladin-content/Cargo.toml` | **10-05** | modified (one feature declaration deleted) |
| `crates/paladin-content/CHANGELOG.md` | **10-05** | modified (one entry) |
| `.cargo/audit.toml` | **10-05** | modified (one comment corrected) |
| `Makefile` | 10-06 | modified |

**No `.rs` file appears on this list, and none may be added to it (D-23).** This plan reads
`crates/paladin-content/src/` extensively and modifies none of it.

## Checkpoint dependency

Plan 10-04's blocking checkpoint answered question 2 — the disposition of the inert feature. Read
`10-04-SUMMARY.md` before task 2 and execute the branch recorded there:

- **`q2-delete`** (the recommended branch): tasks 2 and 3 run as written.
- **`q2-keep`**: task 2 does **not** delete the declaration. Instead it adds the feature to the facade's
  `content-processing` list at root `Cargo.toml`, and ADR-0032 records the feature as a documentation
  marker with no gating effect. `crates/paladin-content/Cargo.toml` and `CHANGELOG.md` are then
  untouched, root `Cargo.toml` joins `files_modified`, and the acceptance criteria for task 2 invert
  accordingly. Both branches close HARD-06; ADR-0032 must state which was taken and why (D-18).
- **`q2-wire`**: stop. Wiring requires `cfg`-gating a struct field, its constructor and two call sites —
  a `.rs` change outside D-23 — and it needs its own plan with the full CLAUDE.md workspace gate. Record
  the selection and halt rather than absorbing the scope.

<tasks>

<task type="auto">
  <name>Task 1: Write ADR-0032 — PDF extraction capability and the inert feature</name>
  <read_first>
    - `.planning/decisions/0027-dockerfile-chef-planner-stage.md` in full — the `must change` ADR shape, including how `## Code Conformance` names its executing plan and task, and the deletion-only fix description with before/after line ranges.
    - `.planning/decisions/PROMOTION.md` lines 75-106 — the heading set in exact order and the bullet-list rule.
    - `crates/paladin-content/Cargo.toml` lines 14-45 in full — the `[features]` block and the `[dependencies]` block. Note which dependency declarations carry `optional = true` and which do not. Re-derive every line number.
    - `crates/paladin-content/src/adapters/document/mod.rs` — the unconditional module declaration and re-export.
    - `crates/paladin-content/src/adapters/document/document_adapter.rs` — the `PdfExtractor` struct field, its construction, and its two call sites. Record all four line numbers.
    - `crates/paladin-content/src/adapters/input/mod.rs` — the `news-api` feature gate, the proof that an empty feature can legitimately gate code.
    - Root `Cargo.toml` — the `paladin-content` dependency declaration and the `content-processing` feature list. Record both line numbers.
    - `.cargo/audit.toml` lines 20-40 — the `RUSTSEC-2026-0187` comment block and the `ignore` array.
    - `SECURITY-EXCEPTIONS.md` — the compensating-control row for `RUSTSEC-2026-0187`, so the ADR states what rests on the corrected reasoning without changing it.
    - `.planning/decisions/0024-rustsec-exception-governance.md` — Phase 9's governance decision, which this ADR must not disturb.
    - `.planning/phases/09-release-security-gate-integrity/09-CONTEXT.md` → its D-17, which reached the manifest-level half of this conclusion and explicitly handed the capability question here.
    - `.planning/REQUIREMENTS.md` lines 1453-1468 — HARD-06 in full.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-00b, D-00g, D-17, D-18, D-19, D-23, D-24.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-RESEARCH.md` → "Security Domain" and the Code Examples section holding the exact current text of both config files.
    - `10-04-SUMMARY.md` — the recorded answer to checkpoint question 2.
  </read_first>
  <files>.planning/decisions/0032-pdf-extraction-capability.md</files>
  <action>
Write `.planning/decisions/0032-pdf-extraction-capability.md`, titled
`# ADR-0032: PDF extraction is unconditional; the inert `pdf` feature and the -0187 reachability path`,
in the exact seven-heading order with no frontmatter. `## Status` is `Accepted` plus a bold `**Date:**`
line.

`## Context` sets out the three facts HARD-06 says point in two directions, each with its `file:line`
re-read this session — the empty feature declaration; the unconditional `pdf-extract` dependency with
no `optional = true`; and the facade's `content-processing` list omitting the feature — and then the
**fourth fact that settles it**, which is source-level rather than manifest-level: a workspace search
for a `pdf` feature gate under `crates/paladin-content/src/` returns zero matches, the document adapter
module declares and re-exports `PdfExtractor` unconditionally, and `document_adapter.rs` holds it as a
plain struct field, constructs it, and calls it at two sites. Record the exact search command and its
zero-match output. Then state the comparator that shows the shape is not the defect: the `news-api`
declaration is byte-identical and legitimately gates a dependency-free module at
`adapters/input/mod.rs`. Conclude: PDF extraction ships, always, in every build of `paladin-content`;
the facade's five-of-six list is not a capability gap; and the feature under discussion is the only one
in the crate inert in both directions.

Record the advisory half in the same section. `.cargo/audit.toml`'s `RUSTSEC-2026-0187` entry is
**right about reachability and wrong about the mechanism**: its stated grounds attribute the
`pdf-extract` path to the facade's content-processing feature, when the actual optionality lives one
level up — `paladin-content` itself is an optional dependency of the facade, and `pdf-extract` is
unconditional inside it. State that the suppression stands, that `SECURITY-EXCEPTIONS.md` and
ADR-0024's governance are untouched, and that only the comment's stated reasoning is corrected.
State plainly that `cargo audit` and `cargo deny check` cannot be run in this environment
(`crates.io` returns HTTP 403, unchanged from Phase 9), that nothing here was measured against either
tool, and give the exact command a CI runner executes as the outstanding verification.

`## Decision` opens by citing the D-00b precedence order verbatim, then states the one-sentence answer:
PDF extraction is a supported capability of `paladin-content` unconditionally, in every build.
Then two sub-decisions:

  (i) **The disposition of the inert feature**, stated as the branch that was actually taken. Read
      `10-04-SUMMARY.md` and record which of `q2-delete`, `q2-keep` or `q2-wire` the human selected,
      quote the reasoning, and state the accepted cost of the selected branch. For the delete branch,
      the accepted cost is that building the crate with that feature name begins to fail where it
      previously succeeded and did nothing — a minor public-contract change on a pre-1.0 crate family,
      recorded in `crates/paladin-content/CHANGELOG.md`. Name the alternative branch and why it lost.

  (ii) **The corrected reachability path**, stated positively: `pdf-extract` is an unconditional
      dependency of `paladin-content`, so nothing inside that crate gates it; reachability is
      determined one level up, by whether the facade's optional `paladin-content` dependency is
      enabled. State that this is the sentence `.cargo/audit.toml` now carries and that
      `SECURITY-EXCEPTIONS.md`'s compensating-control row rests on it.

Add a short note recording an adjacent finding the reader will ask about and this ADR does not fix:
three optional dependencies in the same manifest — `scraper`, `rss` and `tiktoken-rs` — are declared
behind features and consumed by no code in the crate. Confirm it with a search this session and record
the command. State that it is the mirror image of this ADR's defect, that it is outside HARD-06's
scope, and name where it went — the deferred list, candidate for Phase 11's facade residue work or a
Phase 15 dependency-hygiene item.

`## Considered Options` is a bullet list with `(accepted)`/`(rejected)` tags covering all three
dispositions — delete the inert declaration; keep it and add it to the facade's feature list for
literal §4.4.6 compliance; wire it to gate the dependency — each with its one-line reason, plus a
fourth: record the answer and change nothing (rejected — leaves a manifest that lies quietly, which is
the defect class this milestone close-out exists to retire). Tag as `(accepted)` whichever branch the
checkpoint selected.

`## Code Locations` is a bullet list of exact `file:line` citations: the feature declaration and the
`pdf-extract` dependency line; the four `document_adapter.rs` sites; the document module's declaration
and re-export; the `news-api` gate; the root manifest's `paladin-content` declaration and
`content-processing` list; `.cargo/audit.toml`'s comment block and its `ignore` entry; the M7 Epic 1
PRD's §4.4.1 and §4.4.6 lines, re-derived after plan 10-04's insertions moved them.

`## Code Conformance` is the standalone phrase `must change`, followed by a line naming this plan and
the tasks that execute it — plan 10-05 task 2 for the manifest, the CHANGELOG and the advisory comment,
and plan 10-05 task 3 for the PRD annotations.

`## Downstream Consumers` is a bullet list naming: **Phase 12 / SUPPLY-02 and SUPPLY-03** — the
`pdf-extract` reachability question, delivered as an answer rather than deferred, so Phase 12 does not
re-derive it; **Phase 10 / HARD-01** — the ledger row for `REQ-content-processing-build-gate`, written
by plan 10-10, which this ADR resolves; **Phase 11 or Phase 15** — the three consumed-by-nothing
optional dependencies, recorded here and owned there.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/decisions/0032-pdf-extraction-capability.md; test -f "$f" && head -1 "$f" | grep -q "^# ADR-0032" && ! head -1 "$f" | grep -q "^---" && diff <(grep "^## " "$f") <(printf "## Status\n## Context\n## Decision\n## Considered Options\n## Code Locations\n## Code Conformance\n## Downstream Consumers\n") && echo "mustchange=$(grep -cx "must change" "$f") supply=$(grep -c "SUPPLY-0" "$f") rejected=$(grep -c "(rejected)" "$f") newsapi=$(grep -c "news-api" "$f") notmeasured=$(grep -ci "not run in this environment\|cannot be run\|HTTP 403" "$f")"'</automated>
  </verify>
  <acceptance_criteria>
    - `diff <(grep '^## ' .planning/decisions/0032-pdf-extraction-capability.md) <(printf '## Status\n## Context\n## Decision\n## Considered Options\n## Code Locations\n## Code Conformance\n## Downstream Consumers\n')` produces no output, and `sed -n '1p'` does not match `^---`.
    - `grep -cx 'must change' .planning/decisions/0032-pdf-extraction-capability.md` returns `1`, and the following line names plan `10-05` and its executing tasks.
    - `grep -c '(rejected)' .planning/decisions/0032-pdf-extraction-capability.md` returns at least `3` — all three dispositions plus change-nothing appear as considered options, and the selected one is tagged `(accepted)`.
    - `grep -c 'news-api' .planning/decisions/0032-pdf-extraction-capability.md` returns at least `1` — the legitimate empty-feature comparator is recorded, so a later reader does not generalise the deletion to every empty feature.
    - `grep -c 'document_adapter' .planning/decisions/0032-pdf-extraction-capability.md` returns at least `1` — the source-level half of the evidence is cited, not only the manifest.
    - `grep -ci 'HTTP 403\|cannot be run in this environment\|not run in this environment' .planning/decisions/0032-pdf-extraction-capability.md` returns at least `1` — the audit tooling is scoped as unavailable, never inferred as passing.
    - `grep -c 'SUPPLY-0' .planning/decisions/0032-pdf-extraction-capability.md` returns at least `1`.
    - `grep -ci 'scraper' .planning/decisions/0032-pdf-extraction-capability.md` returns at least `1` — the adjacent finding is recorded with its owner rather than left for a reader to rediscover.
    - The ADR names which checkpoint branch was taken, matching the answer recorded in `10-04-SUMMARY.md`.
    - `git status --porcelain -- '*.rs'` is empty.
  </acceptance_criteria>
  <done>ADR-0032 answers the capability question from source rather than manifest evidence, names the selected disposition branch and its accepted cost, states the corrected reachability path positively, scopes the audit tooling as unavailable, records the adjacent three-dependency finding with an owner, and reads `must change` naming its executing tasks.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the two config changes — the manifest declaration and the advisory comment — and record the consumer cost</name>
  <reversibility rating="costly">Removing a declared feature from a published crate is a public-contract change: restorable in one line, but visible to every consumer of `paladin-content 0.1.0`+ (D-18).</reversibility>
  <precondition>`10-04-SUMMARY.md` records the answer to checkpoint question 2. If that answer is `q2-wire`, this task does not run — halt and record why, per the Checkpoint dependency section above.</precondition>
  <read_first>
    - `crates/paladin-content/Cargo.toml` lines 14-45 in full — read the `[features]` block immediately before editing so the deletion targets the right line, and confirm the current line number rather than trusting `:18`.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md` → the `crates/paladin-content/Cargo.toml:18 — delete` section, which quotes the exact current block, and the `.cargo/audit.toml:26-29` section, which quotes the exact current comment and the suggested corrected wording.
    - `.cargo/audit.toml` lines 20-40 in full — the whole 2026-advisories comment block, so the correction changes only the entry it targets and leaves the `-0194` / `-0195` entry alone.
    - `crates/paladin-content/CHANGELOG.md` in full — the existing entry format, heading levels, and whether an Unreleased section already exists.
    - `.planning/decisions/0032-pdf-extraction-capability.md` (post-task-1) — so the CHANGELOG entry and the corrected comment say what the ADR says.
    - `SECURITY-EXCEPTIONS.md` — read it to confirm which row rests on the corrected reasoning; do not edit it.
    - `10-04-SUMMARY.md` — the checkpoint answer.
  </read_first>
  <files>crates/paladin-content/Cargo.toml, crates/paladin-content/CHANGELOG.md, .cargo/audit.toml</files>
  <action>
Three edits, all subtraction or replacement in place, none additive to the build graph.

**Edit 1 — `crates/paladin-content/Cargo.toml`.** Delete the inert feature declaration in the
`[features]` block: the one whose value is an empty list and which gates neither a dependency nor any
code. Its current position is the first entry in the block; re-derive the line number by reading. Leave
every other entry exactly as found, in exactly its current relative order — the fix is subtraction
only, and no entry may be added, reordered, reformatted or re-aligned. In particular leave the
`news-api` entry untouched: it is a byte-identical empty declaration that legitimately gates a
dependency-free module, and it is the comparator that proves the deletion targeted the inert feature
rather than the shape.

**Add no comment to the manifest recording the deletion.** The record lives in ADR-0032 and in
`CHANGELOG.md`. A manifest comment naming the removed feature would both duplicate the record and make
the acceptance grep below meaningless.

**Edit 2 — `crates/paladin-content/CHANGELOG.md`.** Add one entry under `Removed` in the appropriate
unreleased section, following the file's existing heading levels and entry format exactly. It states
the feature name removed, that it gated no dependency and no code, that PDF extraction is unaffected
and remains unconditional in every build, and the consumer-visible cost: building the crate with that
feature name begins to fail where it previously succeeded and did nothing. Cite ADR-0032 by number.

**Edit 3 — `.cargo/audit.toml`.** Correct the reasoning in the `RUSTSEC-2026-0187` comment block only.
The defective clause is the parenthetical attributing the `pdf-extract` path to the facade's
content-processing feature. Replace it with a statement of the actual path, stated positively rather
than as a negation: `pdf-extract` is an unconditional dependency of `paladin-content`, so no feature
inside that crate gates it; reachability is determined one level up, by whether the facade's optional
`paladin-content` dependency is enabled. Cite ADR-0032 in the comment.

Do **not** change which advisories are suppressed. The `ignore` array is untouched, its entry for this
advisory stays, and the remaining sentences of the block — the fix requirement, the breaking-jump note
and the revisit condition — are preserved word for word. Do not touch the `-0194` / `-0195` entry, the
`-0185` / `-0190` note, or any other line of the file. Do not touch `SECURITY-EXCEPTIONS.md`,
`deny.toml`, or ADR-0024.

Neither `cargo audit` nor `cargo deny check` runs in this environment. Record in the SUMMARY the exact
command a CI runner executes against the reconciled configuration, and record that no local pass was
observed. Do not infer one.
  </action>
  <verify>
    <automated>bash -c 'echo "inert=$(grep -cE "^pdf +=" crates/paladin-content/Cargo.toml)"; echo "newsapi=$(grep -cE "^news-api +=" crates/paladin-content/Cargo.toml)"; echo "features=$(awk "/^\[features\]/{f=1;next}/^\[/{f=0}f&&/=/{c++}END{print c+0}" crates/paladin-content/Cargo.toml)"; echo "manifest_added=$(git diff -- crates/paladin-content/Cargo.toml | grep -c "^+[^+]")"; echo "ignore0187=$(grep -c "\"RUSTSEC-2026-0187\"" .cargo/audit.toml)"; echo "oldparen=$(grep -c "optional \`content-processing\`" .cargo/audit.toml)"; echo "changelog=$(grep -c "ADR-0032" crates/paladin-content/CHANGELOG.md)"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^pdf +=' crates/paladin-content/Cargo.toml` returns `0`, and `grep -cE '^pdf-extract' crates/paladin-content/Cargo.toml` returns `1` — the feature declaration is gone and the dependency it never gated is untouched.
    - `grep -cE '^news-api +=' crates/paladin-content/Cargo.toml` returns `1` — the legitimate empty feature survives.
    - The `[features]` block holds exactly `5` entries after the edit, in the same relative order as before, verified by comparing `awk '/^\[features\]/{f=1;next}/^\[/{f=0}f' crates/paladin-content/Cargo.toml` against the pre-edit block recorded in the SUMMARY.
    - `git diff -- crates/paladin-content/Cargo.toml | grep -c '^+[^+]'` returns `0` — the manifest edit is pure deletion, with no added line and no comment.
    - `git diff --numstat -- crates/paladin-content/Cargo.toml` shows exactly `1` deleted line and `0` added.
    - `grep -c '"RUSTSEC-2026-0187"' .cargo/audit.toml` returns `1` — the suppression is unchanged.
    - ``grep -c 'optional `content-processing`' .cargo/audit.toml`` returns `0`, and the corrected block states the actual path: `grep -c 'unconditional' .cargo/audit.toml` returns at least `1` and `grep -c 'ADR-0032' .cargo/audit.toml` returns at least `1`.
    - `grep -c 'RUSTSEC-2026-0194' .cargo/audit.toml` and `grep -c 'RUSTSEC-2026-0195' .cargo/audit.toml` are unchanged from their pre-edit values, and `git diff -- .cargo/audit.toml` touches no line inside their entry.
    - `git status --porcelain -- SECURITY-EXCEPTIONS.md deny.toml` is empty.
    - `grep -c 'ADR-0032' crates/paladin-content/CHANGELOG.md` returns at least `1`, and the entry states the consumer-visible build consequence.
    - `cargo metadata --no-deps --format-version 1 >/dev/null` exits 0 — the manifest still parses.
    - `git status --porcelain -- '*.rs'` is empty.
  </acceptance_criteria>
  <done>The `[features]` block holds five entries, every one of which gates something; the advisory comment states the true reachability path with the suppression itself unchanged; and the CHANGELOG records the consumer-visible cost with the ADR number.</done>
</task>

<task type="auto">
  <name>Task 3: Annotate M7 Epic 1 PRD §4.4.1 and §4.4.6 as superseded by outcome</name>
  <precondition>Plan 10-04 has already annotated §6.1 and Goal 2 in this same file, so the line numbers for §4.4.1 and §4.4.6 have moved. `10-04-SUMMARY.md` records the post-edit numbers; re-derive them anyway before editing.</precondition>
  <read_first>
    - `.project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md` — the whole `## 4. Functional Requirements` region covering §4.4.1 and §4.4.6, plus plan 10-04's existing head blockquote so the new one sits beneath it rather than replacing it. Re-derive both clause line numbers with `grep -n`.
    - `10-04-SUMMARY.md` — the post-edit line numbers plan 10-04 recorded, as a starting point to verify rather than trust.
    - `.planning/decisions/0032-pdf-extraction-capability.md` (post-task-1) — so the corrections state what the ADR states.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md` → **Analog B / Shape A (inline strike-and-correct)**.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-00c, D-19.
  </read_first>
  <files>.project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md</files>
  <action>
Apply the Shape A inline strike-and-correct treatment to two clauses, plus one further dated head
blockquote placed immediately beneath plan 10-04's existing one. Do not merge into, edit, or replace
plan 10-04's blockquote — two plans annotate this document, and each keeps its own dated block so the
provenance of each correction stays readable.

1. **Head blockquote**, dated 2026-08-08, tagged HARD-06, linking ADR-0032 by relative path. It states
   that §4.4.1 and §4.4.6 (both struck below) are superseded by outcome: the feature §4.4.1 requires to
   gate the PDF dependency gates nothing in either direction, and §4.4.6's requirement that the facade
   activate the crate with all capability features enabled describes a completeness that has no
   behavioural effect because the omitted feature gates nothing. State that the original text is
   retained with inline corrections and nothing is deleted.

2. **§4.4.1**: wrap the clause requiring the feature to gate the PDF dependency in `~~` markers, word
   for word, then a `**Corrected (dated 2026-08-08, HARD-06):**` paragraph stating that the dependency
   is declared unconditionally with no `optional = true`, that a workspace search for the feature gate
   under the crate's `src/` returns zero matches, and that ADR-0032 records the outcome. Give the
   command and its result.

3. **§4.4.6**: wrap the "with all capability features enabled" requirement in `~~` markers, word for
   word, then a `**Corrected (dated 2026-08-08, HARD-06):**` paragraph stating that the facade's
   feature list omits one of six capability features and that this is harmless because that feature
   gates nothing; PDF extraction ships in every build of the crate regardless.

Leave §6.1 and Goal 2 untouched — plan 10-04 owns them and has already annotated them. Leave §4.4.4
untouched. Change no other clause, and do not renumber.
  </action>
  <verify>
    <automated>bash -c 'f=".project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md"; echo "hard06=$(grep -c "Corrected (dated 2026-08-08, HARD-06)" "$f")"; echo "hard05=$(grep -c "Corrected (dated 2026-08-08, HARD-05)" "$f")"; echo "adr0032=$(grep -c "ADR-0032" "$f")"; echo "adr0031=$(grep -c "ADR-0031" "$f")"; echo "numstat=$(git diff --numstat -- "$f")"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Corrected (dated 2026-08-08, HARD-06)' .project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md` returns exactly `2` — one per corrected clause.
    - `grep -c 'Corrected (dated 2026-08-08, HARD-05)' <that file>` returns exactly `2` — plan 10-04's corrections are intact and were neither removed nor duplicated.
    - `grep -c 'ADR-0032' <that file>` returns at least `3`, and its relative link resolves to an existing file.
    - `grep -c 'ADR-0031' <that file>` is unchanged from its post-10-04 value, recorded in `10-04-SUMMARY.md`.
    - `git diff --numstat -- <that file>` for this task's commit shows exactly `2` deleted lines — the two struck clauses replaced in place; everything else additive.
    - `grep -c 'SUPERSEDED BY\|Correction (dated' <that file>` shows two separate dated head blocks, one per annotating plan, neither merged into the other.
    - `git status --porcelain -- '*.rs' crates/paladin-content/Cargo.toml .cargo/audit.toml Makefile` is empty for this task's commit.
  </acceptance_criteria>
  <done>§4.4.1 and §4.4.6 survive struck and intact beneath their own dated head blockquote, each followed by a correction naming ADR-0032 and the command that established the outcome, and plan 10-04's two corrections in the same file are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a governance comment in `.cargo/audit.toml` → a future security reviewer's reachability judgement | The comment is the stated reason a live advisory suppression is acceptable; its accuracy is the control, and `SECURITY-EXCEPTIONS.md`'s compensating-control row rests on it. |
| a published crate's declared feature set → downstream consumers | Removing a feature name changes what `cargo build --features` accepts for every consumer of the published crate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-17 | Repudiation | `.cargo/audit.toml`'s `RUSTSEC-2026-0187` reasoning (ASVS V14, configuration) | high | mitigate | A corrected-but-still-imprecise comment misleads a future reviewer more effectively than the wrong one did, because it looks freshly checked. The replacement must state the actual path positively — dependency unconditional inside the crate, crate optional in the facade — rather than negating the old parenthetical; acceptance criteria assert both that the old clause is gone and that a positive statement replaced it. |
| T-10-18 | Tampering | the suppressed-advisory set | high | mitigate | Editing a comment inside an `ignore` block risks disturbing the list itself, which would silently change what `cargo audit` reports. The task forbids touching the array, and an acceptance criterion asserts the advisory's entry is still present and the neighbouring entries' diffs are empty. |
| T-10-19 | Denial of service | `crates/paladin-content`'s public feature surface | medium | mitigate | Removing a declared feature makes a previously-succeeding build invocation fail. The change is gated behind plan 10-04's blocking checkpoint, recorded in `CHANGELOG.md` with its consumer-visible consequence, and `cargo metadata` confirms the manifest still parses. |
| T-10-20 | Tampering | the manifest's surviving feature entries | medium | mitigate | A reformat or reorder during a one-line deletion would show up in every downstream diff and obscure the actual change. The acceptance criteria assert zero added lines, exactly one deleted line, five surviving entries, and an unchanged relative order. |
| T-10-21 | Repudiation | an inferred `cargo audit` pass | high | mitigate | `crates.io` returns HTTP 403 here, so neither audit tool runs. Both the ADR and the SUMMARY must state that nothing was measured locally and record the CI command instead; a backstop truth in `must_haves` carries the same constraint into verification rather than letting it green silently. |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing and adds no dependency. Its only manifest touch **removes** a declaration; `10-RESEARCH.md`'s Package Legitimacy Audit records the phase as not-applicable on exactly that basis. `git diff -- Cargo.lock` is empty. |
</threat_model>

<verification>
- ADR-0032 carries the seven canonical headings in order, no frontmatter, `must change`, and names plan 10-05's tasks as executor.
- `grep -cE '^pdf +=' crates/paladin-content/Cargo.toml` → `0`; `grep -cE '^news-api +=' …` → `1`; five feature entries survive in their original order.
- `git diff -- crates/paladin-content/Cargo.toml | grep -c '^+[^+]'` → `0`; `cargo metadata --no-deps` exits 0.
- `grep -c '"RUSTSEC-2026-0187"' .cargo/audit.toml` → `1`; the old parenthetical is gone and a positive reachability statement replaced it.
- `git status --porcelain -- SECURITY-EXCEPTIONS.md deny.toml` → empty.
- `prd-extract-infrastructure-crates.md` carries exactly two HARD-05 and exactly two HARD-06 corrections, in two separate dated head blocks.
- `git status --porcelain -- '*.rs'` → empty. D-23's boundary holds for this plan.
- `cargo audit` / `cargo deny check` are recorded CI-only with their exact commands; no local pass is claimed.
</verification>

<success_criteria>
Asking whether PDF extraction is supported returns one answer, from source rather than manifest
inference; the advisory suppression that assumes the dependency is in the graph now rests on a true
statement of how it gets there; the manifest declares no feature that gates nothing; the one
consumer-visible consequence is in the CHANGELOG; and Phase 12 inherits the reachability answer instead
of the question.
</success_criteria>

<output>
Create `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-05-SUMMARY.md` when done.
Record the pre-edit `[features]` block verbatim, the CI command for the audit tooling, and the fact
that no local audit pass was observed.
</output>
