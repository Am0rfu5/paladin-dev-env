---
phase: 10-milestone-7-8-ground-truth-recorded-account
plan: 06
type: execute
wave: 2
depends_on: ["10-04"]
files_modified:
  - .planning/decisions/0033-cargo-doc-warning-bar.md
  - Makefile
  - .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_5/prd-document-facade-crate-role.md
  - .project/Milestone_7-Production-Hardening/Epic_4/epic-4-completion-summary.md
autonomous: true
requirements: [HARD-07]

must_haves:
  truths:
    - "`cargo doc --workspace --no-deps` has one bar, applied consistently: ADR-0033 ratifies the zero-warning bar M7 Epic 4 §4.4.3 and M7 Epic 1 §4.6.4/§8.9 set and CI enforces, and records M8 Epic 5 FR-19's warnings-acceptable position as the superseded minority (D-20)"
    - "ADR-0033 records the **measured** state of the gate, not its configured intent: the exact CI command was run against HEAD and exits 1, producing 20 rustdoc warnings across four crates, and the ADR states that alongside the ratification rather than in place of it"
    - "The 20 warnings are recorded per crate with counts and a dated measurement command — never as a bare assertion that some warnings exist — and the entry names an owning phase so the debt is tracked rather than merely noticed"
    - "The `cargo doc` bar and the doctest posture are kept as two separately stated findings even though HARD-07 asks them as one question, because they have different owners and different dispositions"
    - "The seven crates setting `doctest = false` are listed with their `Cargo.toml:LINE` citations in a stated order, and the measured runs-anyway behaviour of two of them is recorded as an observation of this Cargo version rather than inferred from the manifest flag (D-22)"
    - "DEBT-03 is recorded discharged by Phase 8 rather than by this phase: `crates/paladin-ports/Cargo.toml` carries no `[lib]` section, `git log` shows the re-enabling commit, and CI's workspace doc-test step carries no crate exclusion (D-21)"
    - "`release-check` is no longer weaker than CI: the `Makefile` target runs the same workspace doc-test invocation the CI job runs, with no crate excluded and no stale explanatory echo (D-21)"
    - "M8 Epic 5 FR-19 carries a dated inline correction naming ADR-0033, with its original warnings-acceptable wording retained struck (D-20)"
    - "`epic-4-completion-summary.md`'s claim that the documentation quality gates pass carries a dated correction stating the measured warning count, so a record asserting a bar is met does not sit unqualified beside a bar the tree does not clear"
    - statement: "`cargo doc --workspace --no-deps` exits 1 at execution time with the same 20-warning, four-crate profile the planner measured at commit `11e9bdb`. The command runs in this environment, so the executor re-runs it and records the current output; if the count or the crate split has changed, ADR-0033 records what was measured at execution time and not what this plan predicted."
      verification: backstop
    - statement: "The `paladin-ports` doctests that Phase 8 re-enabled still run and pass under the corrected `release-check` target. `make release-check` also invokes `clean-code`, `test`, `audit` and `build-release`, and `audit` cannot run here, so the target is not executed end-to-end; the executor runs the doc-test invocation alone and records its result, and the full-target claim is scoped CI-only rather than inferred."
      verification: backstop
  artifacts:
    - path: ".planning/decisions/0033-cargo-doc-warning-bar.md"
      provides: "The one cargo doc bar, the measured 20-warning debt with an owner, DEBT-03's discharge, and the seven-crate doctest posture handed to Phase 15"
      contains: "## Code Conformance"
    - path: "Makefile"
      provides: "A release-check doc-test step no weaker than the CI job it mirrors"
      contains: "release-check"
    - path: ".project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_5/prd-document-facade-crate-role.md"
      provides: "A dated correction on FR-19, original wording retained struck"
      contains: "ADR-0033"
  key_links:
    - from: ".planning/decisions/0033-cargo-doc-warning-bar.md"
      to: ".github/workflows/ci.yml"
      via: "Code Locations citing the lint job's doc gate and the workspace doc-test step, both re-derived by line number this session"
      pattern: "ci\\.yml"
    - from: "Makefile"
      to: ".github/workflows/ci.yml"
      via: "release-check's doc-test invocation matching the CI job's bare workspace form, so the release gate is not weaker than the push gate"
      pattern: "test --workspace --doc"
  prohibitions:
    - "A record must not claim a bar is met when the measured state says otherwise. ADR-0033 must not assert that the tree passes the workspace doc gate — the command exits 1 — and any phrasing implying the tree already enforces it is prohibited even though CONTEXT.md D-20's text supplies exactly that wording."
    - "A manifest flag must not be cited as proof of behaviour that was not observed. Two of the seven crates run and pass doctests despite setting the flag; recording all seven as unmeasured on the strength of the manifest alone is prohibited."
    - "The three-file config boundary must not be widened silently. A plan that finds it needs a `.rs` edit must stop and say so rather than absorb the scope — D-23 is the boundary and an unannounced `.rs` change in a ground-truth phase is prohibited."
    - "A recorded debt must not be left without an owner. Twenty measured warnings noted with no named owning phase is a finding that decays into folklore; recording the count without naming who closes it is prohibited."
---

<objective>
Write ADR-0033 settling the `cargo doc` bar and the doctest posture against the **measured** state of
the tree, delete the release-gate residue that makes `release-check` weaker than CI, and annotate the
two documents whose claims the measurement contradicts.

Purpose: HARD-07 presents two `cargo doc` bars as an unsettled contest. The tree settled it in M7's
favour and nobody wrote it down — but the gate is also currently red, which CONTEXT.md's D-20 did not
discover because it read the CI job's command text without running it. This plan records what is true
of the configuration and what is true of the build, which are two different claims.
Output: ADR-0033, one Makefile fix, two dated inline corrections.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-RESEARCH.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-04-SUMMARY.md
</context>

## Artifacts this phase produces

| Artifact | Plan | Kind |
|---|---|---|
| `.planning/ledgers/milestone-07-08.md` | 10-01, 10-07…10-10, 10-11 | new file |
| `.planning/decisions/0028-m8-reconciliation-authoritative.md` | 10-02 | new ADR |
| `.planning/decisions/0029-version-trajectory-history.md` | 10-03 | new ADR |
| `.planning/decisions/0030-milestone-7-self-numbering.md` | 10-03 | new ADR |
| `.planning/decisions/0031-extracted-crate-dependency-rule.md` | 10-04 | new ADR |
| `.planning/decisions/0032-pdf-extraction-capability.md` | 10-05 | new ADR |
| `.planning/decisions/0033-cargo-doc-warning-bar.md` | **10-06** | new ADR |
| `.planning/REQUIREMENTS.md` | 10-01, 10-11 | modified |
| `.planning/ROADMAP.md` | 10-01, 10-11 | modified |
| `.planning/PROJECT.md` | 10-11 | modified |
| `.planning/decisions/PROMOTION.md` | 10-11 | modified (next free → 0034) |
| `.project/…/Epic_1/facade-audit.md` | 10-02 | annotated |
| `.project/…/Epic_3/infrastructure-adapter-disposition.md` | 10-02 | annotated |
| `.project/…/Epic_3/prd-relocate-remaining-misplaced-modules.md` | 10-02 | annotated |
| `.project/Milestone_7-Production-Hardening/overview/Milestone_7-Tier_4-Production_Hardening.md` | 10-03 | annotated |
| `.project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md` | 10-04, 10-05 | annotated |
| `.project/…/Epic_5/prd-document-facade-crate-role.md` | **10-06** | annotated |
| `.project/Milestone_7-Production-Hardening/Epic_4/epic-4-completion-summary.md` | **10-06** | annotated |
| `crates/paladin-content/Cargo.toml` | 10-05 | modified |
| `crates/paladin-content/CHANGELOG.md` | 10-05 | modified |
| `.cargo/audit.toml` | 10-05 | modified |
| `Makefile` | **10-06** | modified (one flag and one echo deleted) |

**No `.rs` file appears on this list.** If plan 10-04's checkpoint selected `q3-widen`, that changes,
and the Checkpoint dependency section below says how.

## Checkpoint dependency

Plan 10-04's blocking checkpoint answered question 3 — whether this phase ratifies the bar and records
the warning debt, or widens D-23 to clear the warnings. Read `10-04-SUMMARY.md` before task 1 and
execute the branch recorded there:

- **`q3-ratify`** (the recommended branch): this plan runs exactly as written. ADR-0033 ratifies the
  bar, records the measured 20-warning debt with a named owning phase, and touches no `.rs` file.
- **`q3-widen`**: this plan still runs as written — the ADR and the annotations are unchanged — and the
  warning sweep becomes **a separate added plan**, not extra tasks here. Record that in the SUMMARY,
  name the crates and warning classes, and state that the added plan must run the full CLAUDE.md
  workspace gate (`cargo test` → `cargo fmt --check` → `cargo clippy -- -D warnings`) and re-confirm
  ADR-0006's 84% coverage floor, because it is the only plan in this phase whose `.rs` change count is
  not zero. ADR-0033 then names that plan as the executor of the debt rather than naming a later phase.

## The measurement this plan records

Run by the planner at commit `11e9bdb`, using the exact command from `.github/workflows/ci.yml`'s
`lint` job:

- gate exit code: `1`
- lines matching `^warning:`: `24` — twenty individual warnings plus four per-crate summary lines
- per-crate split: `paladin-web` 13, `paladin-battalion` 3, `paladin-ai` 3, `paladin-herald` 1

Six of the twenty are the same six Phase 7's `milestone-04-06.md` recorded on 2026-08-06 for
`REQ-doc-build-clean` (`paladin-battalion` 3, `paladin-ai` 3). Fourteen are newer. **Re-run the command
at execution time and record what you measure**; these figures are a starting point to confirm, not a
number to transcribe.

<tasks>

<task type="auto">
  <name>Task 1: Write ADR-0033 — one cargo doc bar, the measured warning debt, DEBT-03's discharge, and the doctest posture</name>
  <precondition>`10-04-SUMMARY.md` records the answer to checkpoint question 3. `cargo doc` and `cargo test --doc` both run in this environment; the ADR's claims about the gate must come from commands this task runs, not from figures quoted in this plan.</precondition>
  <read_first>
    - `.planning/decisions/0027-dockerfile-chef-planner-stage.md` in full — the `must change` ADR shape, and in particular how its `## Context` states what is measured versus what is established when something could not be run in this environment. That is the exact shape the warning finding needs.
    - `.planning/decisions/PROMOTION.md` lines 75-106 — the heading set in exact order and the bullet-list rule.
    - `.github/workflows/ci.yml` — the `lint` job's doc gate and the workspace doc-test step. Re-derive both line numbers with `grep -n`; the record's `ci.yml:225` citation is stale by content as well as by number.
    - `crates/paladin-ports/Cargo.toml` in full — confirm there is no `[lib]` section, and run `git log --oneline -- crates/paladin-ports/Cargo.toml` to find the commit that removed it.
    - `Makefile` — the `test-doc` target (already clean) and the `release-check` target, whose doc-test line is the surviving residue. Re-derive both line numbers.
    - `.project/Milestone_7-Production-Hardening/Epic_4/prd-api-stabilization-pre-release-preparation.md` lines 72, 76, 78 and 166 — §4.4.1's `#![warn(missing_docs)]` requirement, §4.4.3's zero-warning bar, §4.4.4's >90% coverage target, and the success metric.
    - `.project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md` lines 225 and 357 — §4.6.4 and §8.9's zero-warning requirements. Read only; plans 10-04 and 10-05 own this file's edits and this plan must not touch it.
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_5/prd-document-facade-crate-role.md` line 159 — FR-19, the minority position, quoted verbatim.
    - `.project/Milestone_7-Production-Hardening/Epic_4/epic-4-completion-summary.md` lines 29-50 — the Task 5.0 quality-gate list that names the doc command among passing gates, and the acceptance-criteria row recording the >=90% coverage posture Met.
    - `.planning/ledgers/milestone-04-06.md` — the `REQ-doc-build-clean` row recording six warnings across two crates on 2026-08-06, so the ADR can state the growth rather than presenting 20 as new.
    - `.planning/REQUIREMENTS.md` lines 1470-1481 — HARD-07 in full, including the `ci.yml:225` citation plan 10-01 corrected in place.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-RESEARCH.md` → Common Pitfall 1 and Common Pitfall 2 in full, and Open Questions 1 and 2.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-00b, D-00e, D-20, D-21, D-22, D-23, D-24, D-25.
    - `10-04-SUMMARY.md` — the recorded answer to checkpoint question 3.
  </read_first>
  <files>.planning/decisions/0033-cargo-doc-warning-bar.md</files>
  <action>
Before writing a word, run three commands and capture their output verbatim for the ADR:

1. The exact doc gate from `.github/workflows/ci.yml`'s `lint` job, reproduced character for character
   from the workflow file rather than paraphrased, plus its exit code.
2. `cargo doc --workspace --no-deps 2>&1 | grep -E '^warning: .* generated .* warnings?$'` — the
   per-crate summary lines, which give the crate split and the counts.
3. `cargo test --workspace --doc` and, for each of the seven crates that set the manifest doctest flag,
   `cargo test -p <crate> --doc`. Record which crates actually ran doctests, how many, and whether they
   passed.

Then write `.planning/decisions/0033-cargo-doc-warning-bar.md`, titled
`# ADR-0033: One `cargo doc` bar — ratified, measured, and its residue`, in the exact seven-heading
order with no frontmatter. `## Status` is `Accepted` plus a bold `**Date:**` line.

`## Context` states **three separate findings**, kept visually distinct because they have different
owners and different dispositions. Do not merge them into one narrative.

  **Finding 1 — the bar is not contested; it was settled and not written down.** M7 Epic 4 §4.4.3 and
  M7 Epic 1 §4.6.4 and §8.9 require zero warnings; M8 Epic 5 FR-19 requires only exit 0 with warnings
  acceptable. CI runs the stricter of the two in the required `lint` job, and all ten library crates
  plus the facade carry `#![warn(missing_docs)]`. Cite each with its `file:line`. So this ADR is
  ratification of a shipped answer, not adjudication of an open one.

  **Finding 2 — the gate's configuration and the gate's current result are two different claims, and
  only the first is what D-20 verified.** State the measurement: the exact CI command, run against
  HEAD at the commit you record, exits 1, producing the warning count and per-crate split you measured
  in step 2 above. Quote the per-crate summary lines verbatim. Name the warning classes you observed —
  broken intra-doc links, and any `redundant_explicit_link` or `private_intra_doc_links` lints — with
  the files they came from. State that six of them are the same six Phase 7's `milestone-04-06.md`
  recorded on 2026-08-06 for `REQ-doc-build-clean`, so this is a pre-existing recorded defect that has
  since grown, not fresh drift. **Do not write any sentence asserting that the workspace clears this
  gate today, or that enforcement is in effect rather than configured.** Record it as a dated, counted, per-crate,
  `file:line`-cited residue with a named owner: Phase 16 / DOCS-03 already scopes settling the doc bar
  as its own success criterion, and clearing the warnings needs `.rs` doc-comment edits that D-23
  places outside this phase. State which checkpoint branch was selected on this question and why, per
  `10-04-SUMMARY.md`; if `q3-widen` was selected, name the added plan as the executor instead of a
  later phase.

  **Finding 3 — DEBT-03 is already discharged, and the doctest posture is measured rather than
  inferred.** `crates/paladin-ports/Cargo.toml` carries no `[lib]` section; give the commit from
  `git log` that re-enabled its doctests, and note that it landed in Phase 8. CI's workspace doc-test
  step carries no crate exclusion; give its current line number and note that the record's `ci.yml:225`
  citation was stale by content as well as by number, corrected at source by plan 10-01.
  `Makefile`'s `test-doc` target is already clean. The one surviving residue is the `release-check`
  target's doc-test line, which excludes `paladin-ports` and carries an adjacent echo explaining the
  exclusion by a reason that stopped being true when the crates published at `0.1.0`. State that the
  exclusion also makes `release-check` weaker than CI, which is the wrong direction for a release gate.
  Then the seven-crate posture: list all seven crates that set the manifest doctest flag, each with its
  `Cargo.toml:LINE`, **in a stated order** (alphabetical by crate name, and say so, so a later reader
  can diff the list). Record what you measured in step 3: which of the seven actually ran doctests and
  passed, which showed none to run, and — where a crate has rustdoc code fences but shows ignored tests
  — say so. State the observation as behaviour of this Cargo version without asserting a mechanism, and
  name the four crates that were believed to run doctests as the positive baseline. Do not spend the
  phase's budget bisecting Cargo versions.

`## Decision` opens by citing the D-00b precedence order verbatim, then states the one-sentence verdict:
the project's one `cargo doc` bar is **zero warnings on `cargo doc --workspace --no-deps`**, ratified
here, with M8 Epic 5 FR-19 recorded superseded by outcome. Then three sub-decisions matching the three
findings: (i) the bar, ratified; (ii) the measured residue, recorded as debt with a named owner and
explicitly **not** claimed as met; (iii) DEBT-03 recorded discharged by Phase 8, the `release-check`
residue deleted by this plan, and the seven-crate doctest posture recorded — not decided — and handed
to Phase 15 / the coverage-and-CI quality gates. State that the ledger row for the documentation
coverage audit is `present, unproven` rather than `satisfied`, because the coverage claim is real for
the crates that run doctests and unmeasured for those that do not, and that leaving the number
unwritten while flipping the row to `satisfied` is the move this ADR exists to prevent.

`## Considered Options` is a bullet list with `(accepted)`/`(rejected)` tags: ratify the zero-warning
bar and record the measured residue with an owner (accepted, if that is the branch selected); ratify
the bar and clear the warnings in this phase (tag per the checkpoint answer, with the reason);
adopt FR-19's warnings-acceptable bar as the single bar (rejected — CI already runs the stricter one in
a required job, and adopting the looser one would weaken a shipped gate to make a record true);
record the bar without stating the measured state (rejected — the gate reads as green and the next
phase inherits a surprise); decide the seven crates' doctest posture here (rejected — it is a
coverage-and-CI-gates question owned by Phase 15, not a ground-truth deliverable).

`## Code Locations` is a bullet list of exact `file:line` citations: the CI doc gate and the workspace
doc-test step; `crates/paladin-ports/Cargo.toml` and its re-enabling commit; both `Makefile` targets;
M7 Epic 4 §4.4.1/§4.4.3/§4.4.4; M7 Epic 1 §4.6.4 and §8.9; M8 Epic 5 FR-19;
`epic-4-completion-summary.md`'s gate list and its coverage-posture row; the seven crates' doctest flag
lines; and `milestone-04-06.md`'s `REQ-doc-build-clean` row.

`## Code Conformance` is the standalone phrase `must change`, followed by a line naming plan 10-06
task 2 as the executor of the `Makefile` fix, and stating that the 20-warning residue is **not**
executed by this phase and names its owner instead.

`## Downstream Consumers` is a bullet list naming: **Phase 15** — the seven-crate doctest posture, to
decide alongside the coverage-and-CI quality gates, and the four-crate positive baseline recorded here;
**Phase 16 / DOCS-03** — the measured warning residue, with its count, crate split and measurement date,
so it is inherited as a known quantity rather than rediscovered; **Phase 10 / HARD-01** — the ledger
rows for the documentation coverage audit, the workspace CI job and the M8 final quality gate, written
by plans 10-07, 10-08 and 10-10.

Every figure in this ADR must be one this task measured. The counts in this plan's "The measurement
this plan records" section were taken at commit `11e9bdb` and are a starting point to confirm, not a
number to transcribe.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/decisions/0033-cargo-doc-warning-bar.md; test -f "$f" && head -1 "$f" | grep -q "^# ADR-0033" && ! head -1 "$f" | grep -q "^---" && diff <(grep "^## " "$f") <(printf "## Status\n## Context\n## Decision\n## Considered Options\n## Code Locations\n## Code Conformance\n## Downstream Consumers\n") && echo "mustchange=$(grep -cx "must change" "$f") phase15=$(grep -c "Phase 15" "$f") phase16=$(grep -c "Phase 16\|DOCS-03" "$f") doctest=$(grep -c "doctest" "$f") rejected=$(grep -c "(rejected)" "$f") ports=$(grep -c "2bffe22\|re-enable paladin-ports doctests" "$f")"'</automated>
  </verify>
  <acceptance_criteria>
    - `diff <(grep '^## ' .planning/decisions/0033-cargo-doc-warning-bar.md) <(printf '## Status\n## Context\n## Decision\n## Considered Options\n## Code Locations\n## Code Conformance\n## Downstream Consumers\n')` produces no output, and `sed -n '1p'` does not match `^---`.
    - `grep -cx 'must change' .planning/decisions/0033-cargo-doc-warning-bar.md` returns `1`, and the following line names plan `10-06` task 2 as the `Makefile` executor and states that the warning residue is not executed here.
    - The ADR records a measured exit code and a per-crate warning breakdown: `grep -ciE 'paladin-web|paladin-battalion|paladin-herald' .planning/decisions/0033-cargo-doc-warning-bar.md` returns at least `3`, and each named crate appears with a numeric count.
    - `grep -ciE 'already enforces|already satisfies|currently passes|the tree already' .planning/decisions/0033-cargo-doc-warning-bar.md` returns `0` — no sentence claims the gate is green.
    - `grep -c 'DOCS-03\|Phase 16' .planning/decisions/0033-cargo-doc-warning-bar.md` returns at least `1` — the residue has a named owner.
    - `grep -c 'Phase 15' .planning/decisions/0033-cargo-doc-warning-bar.md` returns at least `1` — the doctest posture has a named owner.
    - The seven doctest-flag crates each appear with a `Cargo.toml` line citation: `grep -c 'Cargo.toml:15\|Cargo.toml:9' .planning/decisions/0033-cargo-doc-warning-bar.md` returns at least `2`, and re-running `grep -n doctest crates/*/Cargo.toml` reproduces exactly the list the ADR states.
    - `grep -c '(rejected)' .planning/decisions/0033-cargo-doc-warning-bar.md` returns at least `4`.
    - `grep -c 'present, unproven' .planning/decisions/0033-cargo-doc-warning-bar.md` returns at least `1`.
    - The re-enabling commit hash the ADR cites is reproduced by `git log --oneline -- crates/paladin-ports/Cargo.toml`.
    - `git status --porcelain -- '*.rs'` is empty.
  </acceptance_criteria>
  <done>ADR-0033 ratifies the zero-warning bar, records the measured red state with per-crate counts and a named owning phase without claiming the gate is green, keeps the bar and the doctest posture as separate findings, lists all seven doctest-flag crates in a stated order with what was actually observed, and reads `must change` naming only the Makefile fix as its in-phase executor.</done>
</task>

<task type="auto">
  <name>Task 2: Bring `release-check`'s doc-test step up to the CI job's strength</name>
  <read_first>
    - `Makefile` — the whole `release-check` target and, for comparison, the `test-doc` target which is already in the target form. Re-derive both line numbers with `grep -n`; the residue lines are around `:432-433`.
    - `.github/workflows/ci.yml` — the workspace doc-test step, so the `Makefile` line matches the form CI runs. Re-derive its line number.
    - `.planning/decisions/0033-cargo-doc-warning-bar.md` (post-task-1) — the sub-decision this task executes.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md` → the `Makefile:432-433` section, which quotes the exact current target.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-21, D-23.
  </read_first>
  <files>Makefile</files>
  <action>
Two deletions in the `release-check` target, subtraction only.

1. Remove the `--exclude` flag and its crate argument from the target's workspace doc-test invocation,
   leaving the bare workspace doc-test form that `.github/workflows/ci.yml` runs and that the
   `test-doc` target already uses. The flag's argument names `paladin-ports`, whose doctests Phase 8
   re-enabled; the exclusion is the last residue of DEBT-03 and it makes this release gate weaker than
   the push gate.

2. Delete the explanatory echo line immediately above it. Its stated reason — that the excluded crate's
   doctests reference a root crate whose publication had not yet happened — stopped being true when the
   crates published at `0.1.0`, and stopped being implemented when Phase 8 re-enabled them. Read the
   echo's exact wording in the file before deleting it; do not reproduce that wording anywhere else in
   the `Makefile`. Delete the whole echo rather
   than rewording it: the surviving invocation needs no explanation, and `test-doc` carries none.

Change nothing else in the file. Do not touch `clean-code`, `test`, `audit`, `build-release`, the
per-crate test targets, or any other line of `release-check`. Add no replacement comment naming the
removed flag — ADR-0033 holds the record, and a comment here would duplicate it and defeat the
acceptance check below.

Confirm the target still parses and the surviving step behaves: run `make -n release-check` and record
the expanded recipe, and run the doc-test invocation alone — `cargo test --workspace --doc` — and record
its result. Do **not** run `make release-check` end to end: it invokes `audit`, and `cargo audit` cannot
run in this environment (`crates.io` returns HTTP 403). Record that scoping in the SUMMARY rather than
inferring a full-target pass.
  </action>
  <verify>
    <automated>bash -c 'echo "residue=$(grep -c "exclude paladin-ports" Makefile)"; echo "bare=$(grep -c "test --workspace --doc$" Makefile)"; echo "added=$(git diff -- Makefile | grep -cE "^\+[^+]")"; echo "numstat=$(git diff --numstat -- Makefile)"; make -n release-check >/dev/null 2>&1 && echo "parse=ok" || echo "parse=FAIL"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'exclude paladin-ports' Makefile` returns `0`.
    - `grep -c 'not yet published' Makefile` returns `0` — the stale echo is gone, not reworded.
    - `grep -cE 'test --workspace --doc *$' Makefile` returns at least `2` — the `test-doc` target and `release-check` now run the same form.
    - `git diff -- Makefile | grep -cE '^\+[^+]'` returns `1` or fewer — the fix is subtraction plus at most the modified invocation line; no comment was added.
    - `git diff --numstat -- Makefile` shows exactly `2` deleted lines.
    - `make -n release-check` exits 0 and its expanded recipe still contains `clean-code`, `test`, `audit` and `build-release` in their original order, recorded in the SUMMARY.
    - `cargo test --workspace --doc` was run and its result recorded; the SUMMARY states that `make release-check` was **not** run end to end and why.
    - `git status --porcelain -- '*.rs' Cargo.toml crates/paladin-content/Cargo.toml .cargo/audit.toml` is empty.
  </acceptance_criteria>
  <done>`release-check`'s doc-test step runs the same workspace invocation the CI job runs, with no crate excluded and no stale explanation, and the target's other four steps are unchanged and still in order.</done>
</task>

<task type="auto">
  <name>Task 3: Annotate M8 Epic 5 FR-19 and the Epic 4 completion summary's quality-gate claims</name>
  <precondition>ADR-0033 exists, so both annotations' relative links resolve, and the warning figures they cite are the ones task 1 measured.</precondition>
  <read_first>
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_5/prd-document-facade-crate-role.md` around line 159 — FR-19 and the surrounding functional-requirement list, so the strike lands on one clause. Re-derive the line number.
    - `.project/Milestone_7-Production-Hardening/Epic_4/epic-4-completion-summary.md` lines 29-50 — the Task 5.0 "Core quality gates pass" list naming the doc command, and the acceptance-criteria row recording the missing-docs policy and >=90% coverage posture as Met. Re-derive both line numbers.
    - `.planning/decisions/0033-cargo-doc-warning-bar.md` (post-task-1) — the measured figures and the ratified bar, so both annotations state exactly what the ADR states.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md` → **Analog B / Shape A (inline strike-and-correct)** for both documents; these are clause-level fixes, not whole-document supersessions.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-00c, D-20, D-22.
  </read_first>
  <files>.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_5/prd-document-facade-crate-role.md, .project/Milestone_7-Production-Hardening/Epic_4/epic-4-completion-summary.md</files>
  <action>
Two documents, one dated head blockquote and one struck-and-corrected clause each.

**Document 1 — M8 Epic 5's PRD.** Head blockquote dated 2026-08-08, tagged HARD-07, linking ADR-0033 by
relative path: FR-19 (struck below) sets a warnings-acceptable bar on the same command M7 Epic 4 §4.4.3
and M7 Epic 1 §4.6.4/§8.9 require zero warnings on; the required CI `lint` job runs the stricter form;
ADR-0033 ratifies the zero-warning bar and records FR-19 as superseded by outcome; original text
retained, nothing deleted. Then strike FR-19's clause with `~~` markers, word for word, and follow it
with a `**Corrected (dated 2026-08-08, HARD-07):**` paragraph stating the ratified bar, citing the CI
job's line, and stating plainly that the gate is currently red with the count and crate split task 1
measured — so a reader does not take ratification for compliance.

**Document 2 — the Epic 4 completion summary.** Head blockquote dated 2026-08-08, tagged HARD-07,
linking ADR-0033: this document records the workspace documentation gate among the core quality gates
that pass, and records the missing-docs policy and >=90% coverage posture as Met; both claims are
qualified below; ADR-0033 holds the measurement; original text retained. Then two inline corrections:

  (a) On the quality-gate list entry naming the doc command: append a
      `**Corrected (dated 2026-08-08, HARD-07):**` line stating that the gate as CI runs it — command
      output checked for warnings — exits non-zero at this date, with the measured count, the crate
      split, and the measuring command. Do not strike the list entry; the gate did pass at the time
      this summary was written, and the correction is a dated later measurement, not a claim the
      original was false.

  (b) On the acceptance-criteria row recording the coverage posture Met: append a
      `**Corrected (dated 2026-08-08, HARD-07):**` line stating that the posture is real for the crates
      that run doctests and unmeasured for the seven that set the manifest doctest flag, naming the
      count and pointing at ADR-0033 for the list and at Phase 15 as the owner. Again do not strike the
      row; qualify it.

The distinction between (a)/(b) and FR-19 matters and must be respected: FR-19 states a **standard**
that is superseded, so it is struck; the completion summary states a **historical observation** that is
qualified by a later measurement, so it is annotated but not struck. Recording a past measurement as
false because a later one differs would be its own falsification.

Touch no other clause in either document. Do not renumber, reflow, or re-word anything.
  </action>
  <verify>
    <automated>bash -c 'a=".project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_5/prd-document-facade-crate-role.md"; b=".project/Milestone_7-Production-Hardening/Epic_4/epic-4-completion-summary.md"; echo "a_corr=$(grep -c "Corrected (dated 2026-08-08, HARD-07)" "$a") a_strike=$(grep -c "~~" "$a") a_adr=$(grep -c "ADR-0033" "$a") a_numstat=$(git diff --numstat -- "$a")"; echo "b_corr=$(grep -c "Corrected (dated 2026-08-08, HARD-07)" "$b") b_strike=$(grep -c "~~" "$b") b_adr=$(grep -c "ADR-0033" "$b") b_numstat=$(git diff --numstat -- "$b")"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Corrected (dated 2026-08-08, HARD-07)' .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_5/prd-document-facade-crate-role.md` returns exactly `1`, and `grep -c '~~' <that file>` returns at least `2` — FR-19 survives struck.
    - `grep -c 'Corrected (dated 2026-08-08, HARD-07)' .project/Milestone_7-Production-Hardening/Epic_4/epic-4-completion-summary.md` returns exactly `2`, and `grep -c '~~' <that file>` returns `0` — the historical observations are qualified, not struck.
    - `git diff --numstat -- <the Epic 5 PRD>` shows exactly `1` deleted line; `git diff --numstat -- <the completion summary>` shows exactly `2` deleted lines or fewer, all of them the annotated list entry and table row replaced in place.
    - `grep -c 'ADR-0033' <each file>` returns at least `2` for both, and both relative links resolve to an existing file.
    - Both documents state a numeric warning count matching the figure ADR-0033 records: `grep -oE '[0-9]+ (rustdoc )?warnings?' <each file> | head -1` agrees with the count in `.planning/decisions/0033-cargo-doc-warning-bar.md`.
    - `grep -ci 'paladin-web' <the Epic 5 PRD>` returns at least `1` — the crate split is named, not summarised as "some crates".
    - `git status --porcelain -- '*.rs' Makefile Cargo.toml .cargo/audit.toml` is empty for this task's commit.
  </acceptance_criteria>
  <done>FR-19 survives struck beneath a dated banner naming ADR-0033 and the ratified bar with the measured red state; the completion summary's two claims are qualified by dated later measurements without being falsified; and both documents name the crate split rather than gesturing at it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a CI job's command text → a claim about the build's current state | D-20 read the workflow file and concluded the gate passes. Running it gives the opposite answer, and the ADR sits above every PRD in the precedence order. |
| a release gate weaker than the push gate | `release-check` currently excludes a crate that CI includes, so a release can pass a check the mainline already enforces more strictly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-22 | Repudiation | ADR-0033's claim about the doc gate | high | mitigate | Ratifying a bar the tree does not clear, in a document at top precedence, converts a measurable defect into a recorded success. The task requires running the exact CI command and capturing its output before writing; an acceptance criterion greps for zero occurrences of already-enforces / currently-passes phrasing; and the residue is recorded with a count, a crate split, a date and a named owning phase. |
| T-10-23 | Repudiation | the seven-crate doctest posture | high | mitigate | Citing a manifest flag as proof that seven crates' doctests do not run is inference, and two of them run and pass. The task requires per-crate `cargo test -p <crate> --doc` runs and records the observed behaviour without asserting a mechanism. |
| T-10-24 | Elevation of privilege | the `release-check` gate | medium | mitigate | Leaving the exclusion in place means a release artefact can pass a weaker doc-test gate than every push already satisfies. The flag and its stale explanation are deleted, and an acceptance criterion asserts the target now runs the same form as the CI job. |
| T-10-25 | Tampering | `release-check`'s other four steps | medium | mitigate | A careless edit inside a multi-step target could drop `audit` or `build-release` without an obvious symptom, since the target is not run end to end here. `make -n release-check` is run, its expanded recipe recorded, and the four other steps asserted present and in order. |
| T-10-26 | Repudiation | the Epic 4 completion summary's historical claims | medium | mitigate | Striking a past observation because a later measurement differs would falsify a true record. The task distinguishes a superseded **standard** (struck) from a qualified **observation** (annotated, not struck), and the acceptance criteria assert zero strike markers in the completion summary. |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing and modifies no manifest. It runs `cargo doc` and `cargo test --doc` against already-resolved dependencies; `git diff -- Cargo.toml Cargo.lock` is empty. `10-RESEARCH.md`'s Package Legitimacy Audit records the phase as not-applicable. |
</threat_model>

<verification>
- ADR-0033 carries the seven canonical headings in order, no frontmatter, `must change` naming only the Makefile fix as its in-phase executor.
- The ADR's exit code, warning count and crate split were produced by commands this plan ran, and re-running them at verification time reproduces the recorded figures or the ADR records the difference.
- `grep -ciE 'already enforces|already satisfies|currently passes|the tree already' .planning/decisions/0033-cargo-doc-warning-bar.md` → `0`.
- `grep -c 'exclude paladin-ports' Makefile` → `0`; `grep -c 'not yet published' Makefile` → `0`; `make -n release-check` exits 0 with its other four steps intact.
- The Epic 5 PRD carries one struck FR-19 clause; the Epic 4 completion summary carries two qualifications and zero strike markers.
- `git status --porcelain -- '*.rs'` → empty, unless plan 10-04's checkpoint selected `q3-widen`, in which case the SUMMARY names the separate added plan that owns the sweep.
</verification>

<success_criteria>
`cargo doc --workspace --no-deps` has one bar in the record, and the record also says what the command
currently returns, with the count, the crate split, the date and the phase that owns closing it. The
release gate is no longer weaker than the push gate. The doctest question is recorded with what was
measured rather than what a manifest flag implies, and handed to the phase that owns changing it. The
two documents whose claims the measurement touches are qualified or superseded according to which they
are, without either being falsified.
</success_criteria>

<output>
Create `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-06-SUMMARY.md` when done.
Record the measured exit code, warning count and per-crate split; the per-crate doctest results; the
expanded `make -n release-check` recipe; and the fact that `make release-check` was not run end to end.
</output>
