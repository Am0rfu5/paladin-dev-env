---
phase: 10-milestone-7-8-ground-truth-recorded-account
plan: 07
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-03", "10-04", "10-05", "10-06"]
files_modified:
  - .planning/ledgers/milestone-07-08.md
autonomous: true
requirements: [HARD-01, HARD-05, HARD-07]

must_haves:
  truths:
    - "All 25 Milestone 7 Epic 1 and Epic 2 rows carry a `file:line`-cited verdict from one of the seven classes, re-derived from the tree rather than carried forward from an ingest-era status word (D-03)"
    - "No row in this plan's range is `satisfied` without both a `file:line` citation and a named passing test, example, CI job or build leg that exercises it; a citation with nothing exercising it is `present, unproven` (D-03)"
    - "`REQ-extracted-crate-dependency-rule`, currently reading `Code diverges → HARD-05`, is `satisfied` citing ADR-0031, and the row states explicitly that the divergence was in the rule's wording and not in the code — a bare verdict flip with no explanation is the failure this ledger series exists to prevent (D-16)"
    - "`REQ-docker-workspace-build` is recorded already closed by Phase 9 citing ADR-0027 and its commit, with its citation re-run rather than transcribed (D-04)"
    - "`REQ-ci-workspace-job`'s stale `ci.yml:225` reference is replaced by the line the workspace doc-test step actually occupies today, and the row cites ADR-0033 for DEBT-03's discharge (D-21)"
    - "The four `Shipped (relocated)` mdbook rows in this range keep the `relocated` class rather than being collapsed into `superseded by outcome`, preserving the moved-not-missing signal that stops a later phase planning them as gaps (D-02)"
    - "Two requirements citing the same artefact keep separate rows and separate verdicts; the `REQ-*` ID is the primary key, and no two rows are merged for sharing a citation (D-00f)"
    - "Every one of the 25 rows carries a non-empty Verdict cell and a non-empty Evidence cell; a requirement whose evidence cannot be located is recorded `genuinely outstanding` with the search that returned nothing, never as a blank cell"
    - "This plan writes only its own two epic sections and replaces Verdict and Evidence cells in place, so `grep -c '^| REQ-'` still reads 86 and the ledger's added and deleted line counts are equal"
  artifacts:
    - path: ".planning/ledgers/milestone-07-08.md"
      provides: "Milestone 7 Epic 1 (12 rows) and Epic 2 (13 rows) fully derived, with epic notes"
      contains: "REQ-extracted-crate-dependency-rule"
  key_links:
    - from: ".planning/ledgers/milestone-07-08.md"
      to: ".planning/decisions/0031-extracted-crate-dependency-rule.md"
      via: "The REQ-extracted-crate-dependency-rule row citing the ADR as the reason its verdict moves from diverged to satisfied"
      pattern: "ADR-0031"
    - from: ".planning/ledgers/milestone-07-08.md"
      to: ".github/workflows/ci.yml"
      via: "Epic 2's CI rows citing the job and step line numbers that exercise each manifest or workflow claim"
      pattern: "ci\\.yml"
  prohibitions:
    - "A ledger verdict must not be upgraded on the strength of an ingest-era status word. Carrying `Shipped` or `Verify` forward without re-deriving the citation is the exact false-positive class this ledger exists to reject, and it is prohibited even where it would let a plan finish faster."
    - "A verdict must not flip without its reason. Moving a row from diverged to satisfied because an ADR landed, without stating that the rule's wording changed rather than the code, is prohibited — an unexplained status change is what this ledger series exists to prevent."
    - "A cited line number must not be transcribed. Every `file:line` in this range is re-read at execution time; reproducing a stale citation from the run-4 text is prohibited even where the surrounding claim is still true."
---

<objective>
Derive all 25 Milestone 7 Epic 1 and Epic 2 rows in `.planning/ledgers/milestone-07-08.md` — verdict,
`file:line` citation and exercising artefact for each — replacing the scaffold's pending stubs in place.

Purpose: Epic 1 holds the extraction decisions the whole workspace shape rests on, including the
dependency-rule row ADR-0031 resolves, and Epic 2 holds the CI and build-infrastructure claims whose
citations run 4 already caught going stale twice. These are the two densest manifest-and-CI sections in
the ledger, so they carry the manifest carve-out's weight.
Output: 25 cited rows and two epic notes.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-RESEARCH.md
</context>

## Artifacts this phase produces

| Artifact | Plan | Kind |
|---|---|---|
| `.planning/ledgers/milestone-07-08.md` | 10-01 (scaffold), **10-07** (M7 E1-E2), 10-08 (M7 E3-E4), 10-09 (M8 E1-E3), 10-10 (M8 E5-E7 + cross-milestone), 10-11 (close-out amendment) | new file |
| `.planning/decisions/0028-m8-reconciliation-authoritative.md` | 10-02 | new ADR |
| `.planning/decisions/0029-version-trajectory-history.md` | 10-03 | new ADR |
| `.planning/decisions/0030-milestone-7-self-numbering.md` | 10-03 | new ADR |
| `.planning/decisions/0031-extracted-crate-dependency-rule.md` | 10-04 | new ADR |
| `.planning/decisions/0032-pdf-extraction-capability.md` | 10-05 | new ADR |
| `.planning/decisions/0033-cargo-doc-warning-bar.md` | 10-06 | new ADR |
| `.planning/REQUIREMENTS.md` | 10-01, 10-11 | modified |
| `.planning/ROADMAP.md` | 10-01, 10-11 | modified |
| `.planning/PROJECT.md` | 10-11 | modified |
| `.planning/decisions/PROMOTION.md` | 10-11 | modified (next free → 0034) |
| `.project/…/Epic_1/facade-audit.md`, `…/Epic_3/infrastructure-adapter-disposition.md`, `…/Epic_3/prd-relocate-remaining-misplaced-modules.md` | 10-02 | annotated |
| `.project/Milestone_7-Production-Hardening/overview/Milestone_7-Tier_4-Production_Hardening.md` | 10-03 | annotated |
| `.project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md` | 10-04, 10-05 | annotated |
| `.project/…/Epic_5/prd-document-facade-crate-role.md`, `.project/Milestone_7-Production-Hardening/Epic_4/epic-4-completion-summary.md` | 10-06 | annotated |
| `crates/paladin-content/Cargo.toml`, `crates/paladin-content/CHANGELOG.md`, `.cargo/audit.toml` | 10-05 | modified |
| `Makefile` | 10-06 | modified |

**No `.rs` file appears on this list, and none may be added to it (D-23).**

## Ledger contention contract — read before editing

Four plans write this file in wave 3, in parallel, over **disjoint contiguous** section ranges:

| Plan | Sections it owns | Rows |
|---|---|---|
| **10-07 (this plan)** | **M7 Epic 1, M7 Epic 2** | **25** |
| 10-08 | M7 Epic 3, M7 Epic 4 | 22 |
| 10-09 | M8 Epic 1, M8 Epic 2, M8 Epic 3 | 14 |
| 10-10 | M8 Epic 5, M8 Epic 6, M8 Epic 7, Cross-milestone | 21 |

M8 Epic 4's four rows were derived by plan 10-01 and are owned by no fan-out plan.

**Cell replacement only.** Replace the `Verdict` and `Evidence` cells of rows inside your own sections,
and fill your own sections' `**Epic note:**` lines. **Never** insert, delete, reorder or re-key a row.
**Never** add or remove a section heading. **Never** touch a row outside your range, and never edit the
head notes, the verdict legend, the supersession summary table or the row-order section. Keeping every
edit to in-place cell replacement is what makes the four parallel diffs non-adjacent hunks that merge
without conflict, and it is why every plan asserts `grep -c '^| REQ-'` equals `86` and that the ledger's
added and deleted line counts are equal.

If a row genuinely needs more than one line — a long do-not-re-delete marker, for instance — keep it on
the single table row. Markdown table cells hold arbitrary prose; a second line breaks the table and the
merge.

## Requirement IDs in this plan's range

**M7 Epic 1 — Extended Workspace Decomposition (12):** `REQ-m7-cost-benefit-gate`,
`REQ-paladin-web-extraction`, `REQ-paladin-notifications-extraction`, `REQ-paladin-content-extraction`,
`REQ-paladin-storage-extraction`, `REQ-storage-feature-flags-v1`, `REQ-facade-workspace-metadata`,
`REQ-extracted-crate-dependency-rule`, `REQ-extraction-order-and-shims`,
`REQ-tensorflow-stays-facade-v1`, `REQ-sqlx-workspace-dependency`, `REQ-dependency-isolation-metrics`.

**M7 Epic 2 — Production Build Infrastructure (13):** `REQ-docker-workspace-build`,
`REQ-build-baselines-doc`, `REQ-makefile-workspace-targets`, `REQ-makefile-per-crate-targets`,
`REQ-ci-workflow-triggers`, `REQ-ci-per-crate-matrix`, `REQ-ci-workspace-job`, `REQ-ci-integration-job`,
`REQ-ci-publish-dry-run-v1`, `REQ-ci-publish-dry-run-v2`, `REQ-ci-feature-flag-matrix`,
`REQ-integration-test-placement`, `REQ-integration-tests-doc`.

<tasks>

<task type="auto">
  <name>Task 1: Derive Milestone 7 Epic 1's twelve rows</name>
  <read_first>
    - `.planning/ledgers/milestone-07-08.md` — the head notes, the verdict legend, the supersession summary table and the M7 Epic 1 section as the scaffold left it. Read the whole file so the epic note and the rows match the conventions already stated in it.
    - `.planning/ledgers/milestone-04-06.md` lines 108-160 — worked example rows in the target shape, so the Evidence cell's density and phrasing match the series.
    - `.planning/decisions/0031-extracted-crate-dependency-rule.md` — the restated invariant and its `## Downstream Consumers` note about this exact row.
    - `.planning/decisions/0015-core-ports-dependency-allowlist.md` — for the extraction rows that reference the core/ports purity invariant.
    - `.project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md` (post-annotation) and `cost-benefit-assessment.md` — the source requirements and the four-Go gate, both now carrying this phase's corrections.
    - `crates/paladin-web/`, `crates/paladin-notifications/`, `crates/paladin-content/`, `crates/paladin-storage/` — confirm each crate exists and locate its README and CHANGELOG.
    - Root `Cargo.toml` — the `[workspace.members]` and `[workspace.dependencies]` blocks, the `sqlx` declaration and its feature list, and the facade's feature definitions. Record line numbers.
    - `.planning/intel/code-verification.md` lines 263-414 — the run-4 verification block, including the supersession table rows that belong to this epic (`REQ-storage-feature-flags-v1`, `REQ-tensorflow-stays-facade-v1`, and the dependency-rule row).
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-00e, D-00f, D-02, D-03, D-16.
  </read_first>
  <files>.planning/ledgers/milestone-07-08.md</files>
  <action>
Replace the Verdict and Evidence cells of the twelve M7 Epic 1 rows in place, and fill that section's
`**Epic note:**` line.

For each row: choose one of the seven legend classes; give a `file:line` citation re-read at execution
time; and name the artefact that exercises it. Under the manifest carve-out, a manifest declaration
plus a named CI job or build leg that consumes it is the exerciser; a behavioural claim still needs a
test, example or command. Where nothing exercises a citation, the verdict is `present, unproven` and
the cell says what is missing. Where nothing in the tree satisfies the requirement, it is
`genuinely outstanding` and the cell records the search that returned nothing. No cell is left blank.

Specific rows that need more than a mechanical re-derivation:

- **`REQ-extracted-crate-dependency-rule`** — currently `Code diverges → HARD-05`. It becomes
  `satisfied`, citing ADR-0031 and the manifest lines the ADR cites. The cell must state explicitly
  that the divergence was in the **rule's wording** and not in the code: the tree conforms to the
  restated default-build invariant, and ADR-0031 records why the anchor moved. A bare flip with no
  explanation is prohibited.
- **`REQ-storage-feature-flags-v1`** and **`REQ-tensorflow-stays-facade-v1`** — both appear in the
  supersession summary table at the head of the ledger. Their rows take `superseded by outcome`, and
  their Evidence cells must cross-reference the summary table so the two statements cannot drift apart.
- **`REQ-paladin-content-extraction`** — the crate exists but its target directory was renamed by M8
  Epic 6 and one service stayed in the facade, which is deferred item D4. This is `relocated` under the
  tie-break rule, not `superseded by outcome`; state the rename and name FACADE-02 as the register that
  holds D4.
- **`REQ-paladin-web-extraction`** — the crate exists and its two-framework clause is reversed by
  `REQ-actix-removal`, which lives in plan 10-10's range. Cite the reversing requirement by ID; do not
  write its row.
- **`REQ-sqlx-workspace-dependency`** — the workspace declaration ships but with a narrowed feature
  list against the PRD's explicit form, traced to the RustSec hardening work. Re-read the current
  declaration and record the actual feature list you observe, the two differences from the PRD form,
  and the reason. Verdict is `diverged`, not `satisfied`.
- **`REQ-facade-workspace-metadata`**, **`REQ-extraction-order-and-shims`**,
  **`REQ-dependency-isolation-metrics`** — the run-4 text marks each `Verify → HARD-01`, meaning the
  claim was never checked. Check it now. Where the sub-clause genuinely was not audited and nothing in
  the tree exercises it, `present, unproven` is the honest verdict; do not reach for `satisfied`.

The `**Epic note:**` line for this section states, in one sentence, the epic's overall shape: which
extractions shipped, which requirement's verdict this phase changed and on whose authority, and how
many of the twelve carry each verdict class.

Touch nothing outside the M7 Epic 1 section.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/ledgers/milestone-07-08.md; echo "rows=$(grep -c "^| REQ-" "$f")"; echo "pending=$(awk "/^### Milestone 7 Epic 1/{p=1;next}/^### /{p=0}p" "$f" | grep -c "pending — plan")"; echo "blank=$(awk "/^### Milestone 7 Epic 1/{p=1;next}/^### /{p=0}p" "$f" | grep -c "^| REQ-[a-z0-9-]* *| *|")"; echo "adr31=$(grep -c "ADR-0031" "$f")"; echo "numstat=$(git diff --numstat -- "$f")"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| REQ-' .planning/ledgers/milestone-07-08.md` returns exactly `86` — unchanged by this task.
    - Extracting the M7 Epic 1 section with `awk '/^### Milestone 7 Epic 1/{p=1;next}/^### /{p=0}p' .planning/ledgers/milestone-07-08.md` yields exactly `12` `REQ-*` rows, `0` matching `pending — plan`, and `0` with an empty Verdict cell.
    - Every one of the twelve Evidence cells contains a `file:line`-shaped citation: the section's rows matching `[A-Za-z0-9_./-]*:[0-9]` number at least `12`.
    - `grep -c 'ADR-0031' .planning/ledgers/milestone-07-08.md` returns at least `1`, and the `REQ-extracted-crate-dependency-rule` row contains both the word `wording` and the ADR number, so the flip is explained.
    - The `REQ-storage-feature-flags-v1` and `REQ-tensorflow-stays-facade-v1` rows both read `superseded by outcome`, and both `REQ-*` IDs also appear in the supersession summary table at the head of the file.
    - `git diff --numstat -- .planning/ledgers/milestone-07-08.md` shows added lines equal to deleted lines — cell replacement only, with no row inserted or removed.
    - `awk '/^### /{print}' .planning/ledgers/milestone-07-08.md | wc -l` returns `12` — no section heading was added or removed.
    - `git status --porcelain` lists only `.planning/ledgers/milestone-07-08.md` for this task's commit.
  </acceptance_criteria>
  <done>All twelve M7 Epic 1 rows carry a re-derived `file:line` verdict with its exerciser, the dependency-rule row is `satisfied` with the wording-not-code explanation, and the ledger's row count and section count are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Derive Milestone 7 Epic 2's thirteen rows</name>
  <read_first>
    - `.planning/ledgers/milestone-07-08.md` — the head notes' manifest carve-out paragraph, which names the CI jobs and build legs that count as exercisers for this epic, and the M7 Epic 2 section as the scaffold left it.
    - `.github/workflows/ci.yml` in full — the triggers, the per-crate matrix, the workspace job's clippy / doc / test steps, the integration job, the publish dry-run, and the benchmark signal. Record every line number you cite; the run-4 text's `ci.yml` citations have already been caught stale twice.
    - `.github/workflows/feature-flags.yml` — the feature matrix steps and the library-only isolation test.
    - `.github/workflows/release.yml` — the per-crate dependency-ordered publish dry-run.
    - `Makefile` (post-plan-10-06) — the workspace targets and the ten per-crate test targets.
    - `Dockerfile.chef` and `.planning/decisions/0027-dockerfile-chef-planner-stage.md` — for the Docker workspace build row, closed by Phase 9.
    - `.planning/REQUIREMENTS.md` lines 1320-1355 — Phase 9's hand-off block, which names `REQ-docker-workspace-build` among the seven rows this ledger records as already closed, with its ADR and commits.
    - `.planning/decisions/0033-cargo-doc-warning-bar.md` — for the workspace CI job row's DEBT-03 clause.
    - `docs/src/appendix/` — confirm the mdbook chapters that the relocated documentation rows point at, by `ls`.
    - `.planning/intel/code-verification.md` lines 263-414 — the run-4 verification block, including this epic's supersession-table rows.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-02, D-03, D-04, D-21.
  </read_first>
  <files>.planning/ledgers/milestone-07-08.md</files>
  <action>
Replace the Verdict and Evidence cells of the thirteen M7 Epic 2 rows in place, and fill that section's
`**Epic note:**` line. Same rules as task 1: one of the seven classes, a `file:line` re-read at
execution time, and a named exerciser; `present, unproven` where nothing exercises the citation;
`genuinely outstanding` with the failed search where nothing satisfies it; no blank cells.

Specific rows that need more than a mechanical re-derivation:

- **`REQ-docker-workspace-build`** — one of the seven rows Phase 9's hand-off block names as already
  closed (D-04). Record it closed by Phase 9, citing ADR-0027 and the commit named in the hand-off
  block. **Re-run the citation** rather than transcribing it: confirm the planner-stage form in
  `Dockerfile.chef` today and record what you observed. Phase 9's sharpest findings all came from
  re-reading references that had gone stale.
- **`REQ-ci-workspace-job`** — the run-4 text cites a line in the mid-220s of `ci.yml` for a crate
  exclusion on the workspace doc-test step; read the run-4 input cell for the exact stale reference and
  do **not** carry that reference into the new row in any form. It is stale by content as well as by
  number: the step carries no exclusion at all.
  Record the line the step actually occupies today, cite ADR-0033 for DEBT-03's discharge, and note
  that the `Makefile` residue was deleted by plan 10-06. The clippy and doc steps in the same job get
  their own current line numbers.
- **`REQ-build-baselines-doc`**, **`REQ-integration-tests-doc`** — both are `relocated`, not gaps: the
  deliverables ship as mdbook chapters under `docs/src/appendix/` rather than at their PRD paths.
  Confirm each chapter exists by `ls` and cite the actual path. The head note's path caveat covers the
  general case; the row still names its own file.
- **`REQ-ci-publish-dry-run-v1`** and **`REQ-ci-publish-dry-run-v2`** — two requirements describing the
  same subject with different mechanisms, both of which ship. They are **two rows with two verdicts**,
  never merged, because the `REQ-*` ID is the primary key. Record that v1's per-crate
  dependency-ordered form ships in the release workflow and v2's single workspace form ships in CI,
  each with its own citation, and note that v2 has no document carrier and therefore no precedence
  standing.
- **`REQ-ci-workflow-triggers`**, **`REQ-ci-per-crate-matrix`**, **`REQ-ci-integration-job`**,
  **`REQ-integration-test-placement`**, **`REQ-makefile-workspace-targets`** — all marked
  `Verify → HARD-01` in the run-4 text, meaning never checked. Check each now against the workflow and
  `Makefile` and give the honest verdict.

The `**Epic note:**` line states the epic's shape in one sentence, including how many of the thirteen
were `Verify` stubs that this plan resolved and in which direction, and that every `ci.yml` citation in
the section was re-derived at execution time rather than carried forward.

Touch nothing outside the M7 Epic 2 section.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/ledgers/milestone-07-08.md; echo "rows=$(grep -c "^| REQ-" "$f")"; echo "sections=$(grep -c "^### " "$f")"; echo "pending=$(awk "/^### Milestone 7 Epic 2/{p=1;next}/^### /{p=0}p" "$f" | grep -c "pending — plan")"; echo "e2rows=$(awk "/^### Milestone 7 Epic 2/{p=1;next}/^### /{p=0}p" "$f" | grep -c "^| REQ-")"; echo "stale225=$(grep -c "ci.yml:225" "$f")"; echo "numstat=$(git diff --numstat -- "$f")"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| REQ-' .planning/ledgers/milestone-07-08.md` returns exactly `86` and `grep -c '^### '` returns exactly `12`.
    - The M7 Epic 2 section holds exactly `13` `REQ-*` rows, `0` matching `pending — plan`, and `0` with an empty Verdict cell.
    - `grep -c 'ci.yml:225' .planning/ledgers/milestone-07-08.md` returns `0` — the stale citation is not carried forward, and every `ci.yml` line number in the section is reproduced by `grep -n` against the workflow at verification time.
    - The `REQ-docker-workspace-build` row names Phase 9 and ADR-0027, and `grep -c 'ADR-0027' .planning/ledgers/milestone-07-08.md` returns at least `1`.
    - The `REQ-ci-workspace-job` row cites ADR-0033, and `grep -c 'ADR-0033' .planning/ledgers/milestone-07-08.md` returns at least `1`.
    - `REQ-ci-publish-dry-run-v1` and `REQ-ci-publish-dry-run-v2` appear as two distinct rows with two distinct verdicts and two distinct citations.
    - Each mdbook-relocated row cites a path that exists: every `docs/src/` path named in the section satisfies `test -f`.
    - `git diff --numstat -- .planning/ledgers/milestone-07-08.md` shows added lines equal to deleted lines.
    - `git status --porcelain` lists only `.planning/ledgers/milestone-07-08.md` for this task's commit.
  </acceptance_criteria>
  <done>All thirteen M7 Epic 2 rows carry a re-derived verdict with a citation that resolves against the workflow files today, the two publish-dry-run requirements keep separate rows, and no stale `ci.yml` line number survives anywhere in the ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run-4 ledger text → a cited verdict in the new ledger | The input carries ingest-era status words and `file:line` citations this corpus has already caught going stale; copying either forward reproduces a false positive at ledger precedence. |
| four parallel executors → one ledger file | Disjoint contiguous section ranges and in-place cell replacement are the only things keeping four concurrent diffs mergeable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-27 | Repudiation | carried-forward `file:line` citations | high | mitigate | `ci.yml:225` is the documented instance: cited for an exclusion that is not there, stale by thirteen lines and by content. Every citation in this range is re-read at execution time, and an acceptance criterion asserts the stale reference appears nowhere in the ledger and that every workflow line number reproduces under `grep -n`. |
| T-10-28 | Repudiation | an unexplained verdict flip | high | mitigate | Moving the dependency-rule row from diverged to satisfied without saying the rule's wording changed reads as the code having been fixed. The row must contain the explanation, and an acceptance criterion greps the row for it. |
| T-10-29 | Tampering | the ledger's row inventory during parallel writes | high | mitigate | Four executors write one file concurrently. The contract is cell replacement inside disjoint contiguous sections; every task asserts `grep -c '^\| REQ-'` equals `86`, the section count equals `12`, and the diff's added and deleted line counts are equal. |
| T-10-30 | Information disclosure | none | low | accept | This plan reads workflow and manifest files and writes a planning document. No secret, credential or user data is in scope. |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing, adds no dependency and modifies no manifest — it reads them. `10-RESEARCH.md`'s Package Legitimacy Audit records the phase as not-applicable; `git status --porcelain -- Cargo.toml Cargo.lock` is empty. |
</threat_model>

<verification>
- `grep -c '^| REQ-' .planning/ledgers/milestone-07-08.md` → `86`; `grep -c '^### '` → `12`.
- The M7 Epic 1 and Epic 2 sections hold 12 and 13 rows respectively, with zero `pending — plan` markers and zero blank Verdict cells.
- `grep -c 'ci.yml:225' .planning/ledgers/milestone-07-08.md` → `0`; every `ci.yml` line number in this range reproduces under `grep -n`.
- The dependency-rule row is `satisfied`, cites ADR-0031, and states the divergence was in the wording.
- `git diff --numstat` on the ledger shows added lines equal to deleted lines.
- `git status --porcelain -- '*.rs'` → empty. D-23's boundary holds for this plan.
</verification>

<success_criteria>
A developer looking up any of the 25 Milestone 7 Epic 1 or Epic 2 requirement IDs gets a verdict whose
citation resolves today, an exerciser they can run, and — for the one row whose verdict this phase
changed — the reason it changed. The relocated documentation rows keep the moved-not-missing signal,
the two publish-dry-run requirements keep separate answers, and the ledger's shape is byte-for-byte
what the scaffold defined apart from the cells this plan owns.
</success_criteria>

<output>
Create `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-07-SUMMARY.md` when done.
Record the per-class verdict counts for both epics and the `ci.yml` line numbers re-derived.
</output>
