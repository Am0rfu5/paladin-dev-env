---
phase: 10-milestone-7-8-ground-truth-recorded-account
plan: 08
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-03", "10-04", "10-05", "10-06"]
files_modified:
  - .planning/ledgers/milestone-07-08.md
autonomous: true
requirements: [HARD-01, HARD-03, HARD-07]

must_haves:
  truths:
    - "All 22 Milestone 7 Epic 3 and Epic 4 rows carry a `file:line`-cited verdict from one of the seven classes, re-derived from the tree rather than carried forward from an ingest-era status word (D-03)"
    - "The six Phase-9-closed rows in Epic 4 — the RustSec risk acceptance and hardening actions, the licence policy sign-off, the crate metadata completion and the per-crate changelog, plus the paladin-ports publish verification — are recorded closed by Phase 9 citing ADR-0024 through ADR-0027 and the listed commits, and are not re-opened (D-04)"
    - "Each of those six citations is re-run rather than transcribed, because Phase 9's own sharpest findings came from re-reading `file:line` references that had gone stale (D-04)"
    - "`REQ-doc-coverage-audit`, currently reading `Contested → HARD-07`, is `present, unproven` rather than `satisfied` — the coverage claim is real for the crates that run doctests and unmeasured for the seven that set the manifest doctest flag — citing ADR-0033 for the number and the list (D-22)"
    - "`REQ-versioning-policy` and `REQ-release-readiness-audit` cite ADR-0029, and the release-readiness row records the rc.1 trajectory as closed history rather than as current state (D-10)"
    - "The four `Shipped (relocated)` mdbook rows in this range keep the `relocated` class rather than being collapsed into `superseded by outcome`, preserving the moved-not-missing signal (D-02)"
    - "Two requirements citing the same artefact keep separate rows and separate verdicts; the `REQ-*` ID is the primary key (D-00f)"
    - "Every one of the 22 rows carries a non-empty Verdict cell and a non-empty Evidence cell; a requirement whose evidence cannot be located is `genuinely outstanding` with the search that returned nothing, never a blank cell"
    - "This plan writes only its own two epic sections and replaces Verdict and Evidence cells in place, so `grep -c '^| REQ-'` still reads 86 and the ledger's added and deleted line counts are equal"
    - statement: "The benchmark rows' compile-validation claim holds — `cargo bench --workspace --no-run` completes. The command compiles the whole workspace's bench targets and is slow but runnable offline; the executor attempts it and records the result. If it cannot complete in this environment, the affected rows are `present, unproven` with the attempt recorded, never `satisfied` on the strength of the bench files existing."
      verification: backstop
  artifacts:
    - path: ".planning/ledgers/milestone-07-08.md"
      provides: "Milestone 7 Epic 3 (10 rows) and Epic 4 (12 rows) fully derived, with epic notes"
      contains: "REQ-doc-coverage-audit"
  key_links:
    - from: ".planning/ledgers/milestone-07-08.md"
      to: ".planning/decisions/0033-cargo-doc-warning-bar.md"
      via: "The REQ-doc-coverage-audit row citing the ADR for the seven-crate doctest list and the measured posture"
      pattern: "ADR-0033"
    - from: ".planning/ledgers/milestone-07-08.md"
      to: ".planning/REQUIREMENTS.md"
      via: "The six Phase-9-closed rows citing the hand-off block's ADRs and commits"
      pattern: "ADR-002[4-7]"
  prohibitions:
    - "A ledger verdict must not be upgraded on the strength of an ingest-era status word. Carrying `Shipped` or `Verify` forward without re-deriving the citation is the exact false-positive class this ledger exists to reject, and it is prohibited even where it would let a plan finish faster."
    - "A coverage claim must not be recorded as satisfied while its measurement is unwritten. Flipping the documentation coverage row to satisfied without stating the seven-crate doctest number is prohibited — leaving the number unwritten while claiming the target met is the precise move ADR-0033 exists to prevent."
    - "A closed requirement must not be re-opened. The six Phase-9-closed rows are cited, not re-verified into fresh open items; re-deriving them into new work is prohibited even where a citation now resolves differently — a changed citation is an amendment note, not a reopening."
---

<objective>
Derive all 22 Milestone 7 Epic 3 and Epic 4 rows in `.planning/ledgers/milestone-07-08.md`, including
the six rows Phase 9 already closed and the documentation-coverage row ADR-0033 resolves.

Purpose: Epic 4 is where the release history, the security governance and the documentation bar all
land in the same twelve rows, and six of them were closed by the phase that ran immediately before this
one. Getting the citation discipline right here is what stops a later phase re-planning finished
security work or reading a coverage posture as measured.
Output: 22 cited rows and two epic notes.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-RESEARCH.md
</context>

## Artifacts this phase produces

| Artifact | Plan | Kind |
|---|---|---|
| `.planning/ledgers/milestone-07-08.md` | 10-01 (scaffold), 10-07 (M7 E1-E2), **10-08 (M7 E3-E4)**, 10-09 (M8 E1-E3), 10-10 (M8 E5-E7 + cross-milestone), 10-11 (close-out amendment) | new file |
| `.planning/decisions/0028-m8-reconciliation-authoritative.md` | 10-02 | new ADR |
| `.planning/decisions/0029-version-trajectory-history.md` | 10-03 | new ADR |
| `.planning/decisions/0030-milestone-7-self-numbering.md` | 10-03 | new ADR |
| `.planning/decisions/0031-extracted-crate-dependency-rule.md` | 10-04 | new ADR |
| `.planning/decisions/0032-pdf-extraction-capability.md` | 10-05 | new ADR |
| `.planning/decisions/0033-cargo-doc-warning-bar.md` | 10-06 | new ADR |
| `.planning/REQUIREMENTS.md` | 10-01, 10-11 | modified |
| `.planning/ROADMAP.md` | 10-01, 10-11 | modified |
| `.planning/PROJECT.md` | 10-11 | modified |
| `.planning/decisions/PROMOTION.md` | 10-11 | modified (next free → 0034) |
| `.project/…/Epic_1/facade-audit.md`, `…/Epic_3/infrastructure-adapter-disposition.md`, `…/Epic_3/prd-relocate-remaining-misplaced-modules.md` | 10-02 | annotated |
| `.project/Milestone_7-Production-Hardening/overview/Milestone_7-Tier_4-Production_Hardening.md` | 10-03 | annotated |
| `.project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md` | 10-04, 10-05 | annotated |
| `.project/…/Epic_5/prd-document-facade-crate-role.md`, `.project/Milestone_7-Production-Hardening/Epic_4/epic-4-completion-summary.md` | 10-06 | annotated |
| `crates/paladin-content/Cargo.toml`, `crates/paladin-content/CHANGELOG.md`, `.cargo/audit.toml` | 10-05 | modified |
| `Makefile` | 10-06 | modified |

**No `.rs` file appears on this list, and none may be added to it (D-23).**

## Ledger contention contract — read before editing

Four plans write this file in wave 3, in parallel, over **disjoint contiguous** section ranges:

| Plan | Sections it owns | Rows |
|---|---|---|
| 10-07 | M7 Epic 1, M7 Epic 2 | 25 |
| **10-08 (this plan)** | **M7 Epic 3, M7 Epic 4** | **22** |
| 10-09 | M8 Epic 1, M8 Epic 2, M8 Epic 3 | 14 |
| 10-10 | M8 Epic 5, M8 Epic 6, M8 Epic 7, Cross-milestone | 21 |

M8 Epic 4's four rows were derived by plan 10-01 and are owned by no fan-out plan.

**Cell replacement only.** Replace the `Verdict` and `Evidence` cells of rows inside your own sections,
and fill your own sections' `**Epic note:**` lines. **Never** insert, delete, reorder or re-key a row;
**never** add or remove a section heading; **never** touch a row outside your range or edit the head
notes, the legend, the supersession summary table or the row-order section. Keep every row on one line —
markdown table cells hold arbitrary prose, and a second line breaks both the table and the merge. This
is what makes the four parallel diffs non-adjacent hunks, and it is why every plan asserts
`grep -c '^| REQ-'` equals `86` and that added and deleted line counts are equal.

## Requirement IDs in this plan's range

**M7 Epic 3 — Benchmark Suite Migration (10):** `REQ-sanctum-bench-migration`,
`REQ-disabled-bench-disposition`, `REQ-battalion-benchmarks`, `REQ-llm-serialization-benchmark`,
`REQ-garrison-benchmarks`, `REQ-config-loading-benchmark`, `REQ-critical-path-bench-scope`,
`REQ-workspace-bench-execution`, `REQ-performance-baseline-doc`, `REQ-bench-regression-signal`.

**M7 Epic 4 — API Stabilization & Pre-Release Preparation (12):** `REQ-crate-metadata-completion`,
`REQ-per-crate-readme`, `REQ-per-crate-changelog`, `REQ-doc-coverage-audit`, `REQ-versioning-policy`,
`REQ-release-checklist`, `REQ-stable-api-per-crate`, `REQ-release-readiness-audit`,
`REQ-rustsec-risk-acceptance`, `REQ-rustsec-hardening-actions`, `REQ-license-policy-signoff`,
`REQ-paladin-ports-publish-verification-closed`.

Six of the twelve — `REQ-crate-metadata-completion`, `REQ-per-crate-changelog`,
`REQ-rustsec-risk-acceptance`, `REQ-rustsec-hardening-actions`, `REQ-license-policy-signoff`,
`REQ-paladin-ports-publish-verification-closed` — are named in Phase 9's hand-off block as **already
closed**. The seventh, `REQ-docker-workspace-build`, is in plan 10-07's range.

<tasks>

<task type="auto">
  <name>Task 1: Derive Milestone 7 Epic 3's ten benchmark rows</name>
  <read_first>
    - `.planning/ledgers/milestone-07-08.md` — the head notes, the legend, the manifest carve-out and the M7 Epic 3 section as the scaffold left it.
    - `.planning/ledgers/milestone-04-06.md` lines 108-160 — worked example rows in the target shape.
    - `crates/paladin-memory/benches/`, `crates/paladin-battalion/benches/`, `crates/paladin-llm/benches/`, and the root `benches/` directory — confirm every bench file the epic names exists, by `ls`, and record its path.
    - `crates/*/Cargo.toml` — the `[[bench]]` registrations, so each bench row's manifest citation names the crate that owns the Criterion registration.
    - `.github/workflows/ci.yml` — the benchmark regression-signal job, its threshold and its non-blocking flag. Record the current line number.
    - `docs/src/appendix/` — confirm the performance-baseline chapter exists, by `ls`, for the relocated row.
    - `src/config/settings.rs` — the ownership finding that closes the PRD's open question about which crate owns the config type.
    - `.planning/intel/code-verification.md` lines 263-414 — the run-4 verification block.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-00e, D-00f, D-02, D-03.
  </read_first>
  <files>.planning/ledgers/milestone-07-08.md</files>
  <action>
Replace the Verdict and Evidence cells of the ten M7 Epic 3 rows in place, and fill that section's
`**Epic note:**` line.

For each row: one of the seven legend classes, a `file:line` citation re-read at execution time, and a
named exerciser. For a bench file, the exerciser is its Criterion registration in the owning crate's
manifest plus a build or CI leg that compiles it — a bench file that exists but is registered nowhere
is `present, unproven`, not `satisfied`. No cell is left blank.

Specific rows needing more than a mechanical re-derivation:

- **`REQ-workspace-bench-execution`** — the recorded structural compile-validation command is
  `cargo bench --workspace --no-run`. Attempt it and record the exact result. If it completes, that is
  this epic's collective exerciser and the row is `satisfied`. If it cannot complete in this
  environment, the row is `present, unproven`, the cell records the attempt and its failure mode, and
  the other bench rows fall back to their per-crate manifest registration as exerciser.
- **`REQ-disabled-bench-disposition`** — none of the five disabled benches was restored; three were
  deprecated and removed, two were removed and replaced at narrower scope. This is `superseded by
  outcome`, and the cell must name which three and which two, with the evidence that each is absent.
- **`REQ-performance-baseline-doc`** — `relocated`, not a gap: the deliverable ships as an mdbook
  chapter. Confirm the chapter exists and cite its path. The cell must also state that this row does
  **not** close the separate requirement that owns producing measured runtime numbers, so the two are
  not conflated.
- **`REQ-config-loading-benchmark`** — the row records both the bench and the ownership finding that
  closes the PRD's open question. Cite the settings type's current location.
- **`REQ-critical-path-bench-scope`** — the run-4 text says all six PRD success metrics were recorded
  Satisfied in the assessment's own status table. That is a document asserting its own completion,
  which is exactly the claim the evidence bar rejects. Re-derive it: check what the four bench
  categories actually cover and give the honest verdict.

The `**Epic note:**` line states the epic's shape in one sentence, including whether the workspace bench
compile ran and what the per-class verdict counts are.

Touch nothing outside the M7 Epic 3 section.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/ledgers/milestone-07-08.md; echo "rows=$(grep -c "^| REQ-" "$f")"; echo "sections=$(grep -c "^### " "$f")"; echo "e3rows=$(awk "/^### Milestone 7 Epic 3/{p=1;next}/^### /{p=0}p" "$f" | grep -c "^| REQ-")"; echo "pending=$(awk "/^### Milestone 7 Epic 3/{p=1;next}/^### /{p=0}p" "$f" | grep -c "pending — plan")"; echo "numstat=$(git diff --numstat -- "$f")"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| REQ-' .planning/ledgers/milestone-07-08.md` returns exactly `86` and `grep -c '^### '` returns exactly `12`.
    - The M7 Epic 3 section holds exactly `10` `REQ-*` rows, `0` matching `pending — plan`, and `0` with an empty Verdict cell.
    - Every bench path named in the section exists: each `benches/` path in the section satisfies `test -f`.
    - The `REQ-workspace-bench-execution` row records either a completed `cargo bench --workspace --no-run` with its result, or `present, unproven` with the attempt and its failure mode — never `satisfied` on the strength of bench files existing.
    - The `REQ-performance-baseline-doc` row cites an mdbook path that satisfies `test -f`, reads `relocated`, and states that it does not close the separate runtime-measurement requirement.
    - `git diff --numstat -- .planning/ledgers/milestone-07-08.md` shows added lines equal to deleted lines.
    - `git status --porcelain` lists only `.planning/ledgers/milestone-07-08.md` for this task's commit.
  </acceptance_criteria>
  <done>All ten M7 Epic 3 rows carry a re-derived verdict with a bench path that exists and a named registration or CI leg that exercises it, and the workspace bench compile's real result — success or failure — is recorded rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 2: Derive Milestone 7 Epic 4's twelve rows, including the six Phase 9 closed</name>
  <read_first>
    - `.planning/ledgers/milestone-07-08.md` — the head notes and the M7 Epic 4 section as the scaffold left it.
    - `.planning/REQUIREMENTS.md` lines 1320-1355 — Phase 9's hand-off block in full: the six `REQ-*` IDs in this epic that it names already closed, each with its ADR and the commit list. This is the citation source for those rows.
    - `.planning/decisions/0024-rustsec-exception-governance.md`, `0025-licence-posture.md`, `0026-crate-name-collision-guard.md`, `0027-dockerfile-chef-planner-stage.md` — Phase 9's four ADRs. Read enough of each to confirm the claim the row cites it for.
    - `SECURITY-EXCEPTIONS.md` — the register the RustSec rows cite; confirm its live suppression count and its owner and review-date fields.
    - `scripts/check-changelogs.sh` and `scripts/check-crate-names.sh` — the mechanical enforcers two of the closed rows cite; confirm each exists and is invoked by a CI step, and record that step's line.
    - `crates/*/CHANGELOG.md` and `crates/*/README.md` — confirm the ten-of-ten coverage the readme and changelog rows claim, by `ls`.
    - Root `Cargo.toml` and every `crates/*/Cargo.toml` — the `license` field, for the licence and metadata rows.
    - `.planning/decisions/0029-version-trajectory-history.md` — for the versioning-policy and release-readiness rows.
    - `.planning/decisions/0033-cargo-doc-warning-bar.md` — for the documentation-coverage row: the seven-crate doctest list, the measured posture, and why the row is `present, unproven`.
    - `docs/src/appendix/` and `docs/src/api-reference/` — confirm the relocated release-checklist and stable-API chapters exist, by `ls`. Also confirm whether the two large stray root artefacts the run-4 supersession table names are still present, by `ls`, for the stable-API row.
    - `.project/Milestone_7-Production-Hardening/Epic_4/` — the release-readiness audit and post-release verification documents, and `epic-4-completion-summary.md` as annotated by plan 10-06.
    - `.planning/intel/code-verification.md` lines 263-414 — the run-4 verification block and its supersession-table rows for this epic.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-02, D-03, D-04, D-10, D-22.
  </read_first>
  <files>.planning/ledgers/milestone-07-08.md</files>
  <action>
Replace the Verdict and Evidence cells of the twelve M7 Epic 4 rows in place, and fill that section's
`**Epic note:**` line. Same rules as task 1: one of the seven classes, a citation re-read at execution
time, a named exerciser, no blank cells.

**The six Phase-9-closed rows** — `REQ-crate-metadata-completion`, `REQ-per-crate-changelog`,
`REQ-rustsec-risk-acceptance`, `REQ-rustsec-hardening-actions`, `REQ-license-policy-signoff`,
`REQ-paladin-ports-publish-verification-closed`. Record each as closed by Phase 9, citing the ADR and
the commits Phase 9's hand-off block names. Do **not** re-open them and do **not** re-derive them into
fresh open items. But **do re-run each citation**: confirm the register file, the guard script, the
manifest field or the CI step the row rests on is where the hand-off block says it is, and record what
you observed. If a citation has moved, record the current location and add a dated note that the
hand-off block's reference has shifted — an amendment, not a reopening. Two of these rows carry a
CI-only residue the hand-off block itself names: the post-mitigation audit re-run evidence cannot be
produced in this environment, so the cell says so and does not infer a pass.

**`REQ-doc-coverage-audit`** — currently `Contested → HARD-07`. It becomes `present, unproven`, not
`satisfied`, citing ADR-0033. The cell must carry the substance, not just the verdict: the coverage
posture is real for the crates that run doctests and unmeasured for the seven that set the manifest
doctest flag; ADR-0033 holds the list and the measured behaviour; Phase 15 owns changing it. Give the
count. Flipping this row to `satisfied` while the number stays unwritten is prohibited.

**`REQ-versioning-policy`** — `relocated` **and** `superseded by outcome` both apply: no document exists
at the PRD path and the lockstep target was superseded by what shipped. Under the head note's tie-break
rule, `relocated` wins, because the moved-not-missing signal is what stops a later phase planning the
document as missing. State both facts in the cell and cite ADR-0029 for the supersession half.

**`REQ-release-readiness-audit`** — `satisfied` as **history**: every gate PASS, GO sign-off, the tag
and its commit, docs.rs verification of all ten crates, the external smoke project. Cite ADR-0029 and
state explicitly that this is closed history and not current state, so no reader treats an rc.1
artefact as live.

**`REQ-stable-api-per-crate`** — `relocated`: no root document at the PRD path, the equivalent ships as
an mdbook chapter. Confirm by `ls` whether the two large stray root artefacts the supersession table
names are still present and record what you found; the supersession table cites them as the reason no
root document exists, so the ledger row and the summary table must agree.

**`REQ-per-crate-readme`** — confirm ten of ten by `ls` and name the exercising CI step if one exists;
if none does, `present, unproven`.

The `**Epic note:**` line states the epic's shape in one sentence, including that six of the twelve were
closed by Phase 9 and are cited rather than re-verified, how many citations were re-run and how many had
moved, and the per-class verdict counts.

Touch nothing outside the M7 Epic 4 section.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/ledgers/milestone-07-08.md; echo "rows=$(grep -c "^| REQ-" "$f")"; echo "sections=$(grep -c "^### " "$f")"; echo "e4rows=$(awk "/^### Milestone 7 Epic 4/{p=1;next}/^### /{p=0}p" "$f" | grep -c "^| REQ-")"; echo "pending=$(awk "/^### Milestone 7 Epic 4/{p=1;next}/^### /{p=0}p" "$f" | grep -c "pending — plan")"; echo "phase9adrs=$(grep -cE "ADR-002[4-7]" "$f")"; echo "adr33=$(grep -c "ADR-0033" "$f")"; echo "adr29=$(grep -c "ADR-0029" "$f")"; echo "numstat=$(git diff --numstat -- "$f")"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| REQ-' .planning/ledgers/milestone-07-08.md` returns exactly `86` and `grep -c '^### '` returns exactly `12`.
    - The M7 Epic 4 section holds exactly `12` `REQ-*` rows, `0` matching `pending — plan`, and `0` with an empty Verdict cell.
    - `grep -cE 'ADR-002[4-7]' .planning/ledgers/milestone-07-08.md` returns at least `4` — the six closed rows cite Phase 9's ADRs rather than restating their conclusions.
    - The `REQ-doc-coverage-audit` row reads `present, unproven`, cites ADR-0033, and contains a numeric count of the crates setting the doctest flag; it does not read `satisfied`.
    - `grep -c 'ADR-0029' .planning/ledgers/milestone-07-08.md` returns at least `2` — the versioning-policy and release-readiness rows both cite it.
    - The `REQ-versioning-policy` row reads `relocated` and its cell also states the supersession, per the tie-break rule.
    - Every `docs/src/` path named in the section satisfies `test -f`, and every script path named satisfies `test -f`.
    - The two rows whose post-mitigation audit evidence is CI-only say so explicitly; `grep -ci 'CI-only\|HTTP 403\|not run in this environment' .planning/ledgers/milestone-07-08.md` returns at least `1`.
    - `git diff --numstat -- .planning/ledgers/milestone-07-08.md` shows added lines equal to deleted lines.
    - `git status --porcelain` lists only `.planning/ledgers/milestone-07-08.md` for this task's commit.
  </acceptance_criteria>
  <done>All twelve M7 Epic 4 rows carry a re-derived verdict; the six Phase-9-closed rows cite their ADRs and commits with each citation re-run and any movement recorded as a dated amendment; the documentation-coverage row is `present, unproven` with its number written; and the release history is recorded as history.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 9's hand-off block → six ledger rows recorded closed | The block names ADRs and commits for six requirements. Transcribing them without re-running the citations reproduces exactly the failure mode Phase 9's own findings came from. |
| a document asserting its own completion → a ledger verdict | The Epic 4 completion summary records its acceptance criteria Met; the evidence bar exists to reject that as sufficient. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-31 | Repudiation | the six Phase-9-closed rows' citations | high | mitigate | A transcribed citation that has since moved records a closure resting on a reference that no longer resolves. The task requires re-running each — register file, guard script, manifest field, CI step — and recording movement as a dated amendment rather than silently updating or silently keeping the old value. |
| T-10-32 | Repudiation | the documentation-coverage row | high | mitigate | Flipping the row to satisfied while the seven-crate number stays unwritten converts an unmeasured posture into a recorded success at ledger precedence. The row is fixed at `present, unproven`, must carry the count, and an acceptance criterion greps for both. |
| T-10-33 | Repudiation | an inferred audit-tooling pass | high | mitigate | Two of the closed rows carry post-mitigation re-audit evidence that cannot be produced here (`crates.io` returns HTTP 403). Their cells must say CI-only, and an acceptance criterion greps the ledger for that scoping. |
| T-10-34 | Tampering | the ledger's row inventory during parallel writes | high | mitigate | Four executors write one file concurrently. Cell replacement inside disjoint contiguous sections; every task asserts the row count is `86`, the section count is `12`, and added equals deleted. |
| T-10-35 | Repudiation | a self-asserted completion claim | medium | mitigate | The benchmark scope row rests on the assessment's own status table recording its metrics Satisfied. The task requires re-deriving it against what the bench categories actually cover, and the head note's evidence bar already names a self-asserted status word as the claim it rejects. |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing and modifies no manifest. `cargo bench --workspace --no-run` compiles against already-resolved dependencies; `git diff -- Cargo.toml Cargo.lock` is empty. `10-RESEARCH.md`'s Package Legitimacy Audit records the phase as not-applicable. |
</threat_model>

<verification>
- `grep -c '^| REQ-' .planning/ledgers/milestone-07-08.md` → `86`; `grep -c '^### '` → `12`.
- The M7 Epic 3 and Epic 4 sections hold 10 and 12 rows, with zero `pending — plan` markers and zero blank Verdict cells.
- The six Phase-9-closed rows cite ADR-0024 through ADR-0027 and their commits, with each citation re-run and any movement recorded.
- `REQ-doc-coverage-audit` reads `present, unproven`, cites ADR-0033 and states the doctest-flag crate count.
- Every `benches/`, `docs/src/` and `scripts/` path named in this range satisfies `test -f`.
- `git diff --numstat` on the ledger shows added lines equal to deleted lines.
- `git status --porcelain -- '*.rs'` → empty. D-23's boundary holds for this plan.
</verification>

<success_criteria>
A developer looking up any of the 22 Milestone 7 Epic 3 or Epic 4 requirement IDs gets a verdict whose
citation resolves today. The six requirements Phase 9 closed read as closed with their decisions cited,
not as open work and not as unverifiable assertions. The documentation coverage posture is recorded with
its number and its owner rather than flipped green. The release history reads as history.
</success_criteria>

<output>
Create `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-08-SUMMARY.md` when done.
Record the per-class verdict counts, which of the six Phase-9 citations had moved, and the result of
the workspace bench compile attempt.
</output>
