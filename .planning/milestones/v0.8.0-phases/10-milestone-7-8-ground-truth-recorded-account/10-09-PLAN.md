---
phase: 10-milestone-7-8-ground-truth-recorded-account
plan: 09
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-03", "10-04", "10-05", "10-06"]
files_modified:
  - .planning/ledgers/milestone-07-08.md
autonomous: true
requirements: [HARD-01, HARD-02]

must_haves:
  truths:
    - "All 14 Milestone 8 Epic 1, Epic 2 and Epic 3 rows carry a `file:line`-cited verdict from one of the seven classes, re-derived from the tree rather than carried forward from an ingest-era status word (D-03)"
    - "The three in-execution corrections carry a do-not-re-delete marker in the ledger row itself, not only in ADR-0028 — the ADR is where a reader looks after a question, the ledger row is where a planner looks before one (D-08)"
    - "`REQ-facade-file-classification` and `REQ-facade-audit-document` record the supersession with its factual reason — ~4,400 LOC of orphaned, uncompiled duplicate files classified as active bridges that stay — and cite ADR-0028 (D-07)"
    - "`REQ-shim-consumer-validation` preserves the reconciliation's reproducible orphan test as the version to keep, citing ADR-0028's runnable three-step form rather than restating it in prose (D-08)"
    - "`REQ-m8-epic3-no-extractions` records Epic 3 complete in substance — the relocations executed inside Milestone 8, not punted to Milestone 9 — so no later phase plans them as Milestone 9 candidates, citing ADR-0028 (D-09)"
    - "`REQ-adapter-disposition-record` records the document superseded and names FACADE-04 as the phase that owns its two non-existent crate names, so the two statements do not have to be rediscovered together"
    - "Two requirements citing the same artefact keep separate rows and separate verdicts; the `REQ-*` ID is the primary key (D-00f)"
    - "Every one of the 14 rows carries a non-empty Verdict cell and a non-empty Evidence cell; a requirement whose evidence cannot be located is `genuinely outstanding` with the search that returned nothing, never a blank cell"
    - "This plan writes only its own three epic sections and replaces Verdict and Evidence cells in place, so `grep -c '^| REQ-'` still reads 86 and the ledger's added and deleted line counts are equal"
  artifacts:
    - path: ".planning/ledgers/milestone-07-08.md"
      provides: "Milestone 8 Epic 1 (4), Epic 2 (4) and Epic 3 (6) fully derived, with epic notes and the three do-not-re-delete markers"
      contains: "REQ-m8-epic3-no-extractions"
  key_links:
    - from: ".planning/ledgers/milestone-07-08.md"
      to: ".planning/decisions/0028-m8-reconciliation-authoritative.md"
      via: "Epic 1 and Epic 3 rows citing the ADR for the supersession, the orphan test and the three in-execution corrections"
      pattern: "ADR-0028"
  prohibitions:
    - "A ledger verdict must not be upgraded on the strength of an ingest-era status word. Carrying `Shipped` or `Verify` forward without re-deriving the citation is the exact false-positive class this ledger exists to reject, and it is prohibited even where it would let a plan finish faster."
    - "A do-not-re-delete marker must not be omitted for brevity. The three in-execution corrections exist so nobody re-executes the original audit against the registry module or the sqlite repositories; dropping any of them from the ledger rows — on the grounds that ADR-0028 already holds them — is prohibited, because a planner reads the row before the ADR."
    - "A completed Epic must not be recorded as outstanding. Epic 3's relocations executed inside Milestone 8; recording them as Milestone 9 candidates because the Epic's own PRD deferred them is prohibited even though the PRD says so."
---

<objective>
Derive all 14 Milestone 8 Epic 1, Epic 2 and Epic 3 rows in `.planning/ledgers/milestone-07-08.md`,
carrying the three do-not-re-delete markers into the rows themselves.

Purpose: these three epics are where the facade cleanup happened and where the superseded audit
documents' claims live. A planner opening this range must see, before doing anything else, that the
relocations already executed and that three of the original audit's classifications were corrected
during execution — otherwise the next facade phase re-deletes files that are load-bearing.
Output: 14 cited rows and three epic notes.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-RESEARCH.md
</context>

## Artifacts this phase produces

| Artifact | Plan | Kind |
|---|---|---|
| `.planning/ledgers/milestone-07-08.md` | 10-01 (scaffold), 10-07 (M7 E1-E2), 10-08 (M7 E3-E4), **10-09 (M8 E1-E3)**, 10-10 (M8 E5-E7 + cross-milestone), 10-11 (close-out amendment) | new file |
| `.planning/decisions/0028-m8-reconciliation-authoritative.md` | 10-02 | new ADR |
| `.planning/decisions/0029-version-trajectory-history.md` | 10-03 | new ADR |
| `.planning/decisions/0030-milestone-7-self-numbering.md` | 10-03 | new ADR |
| `.planning/decisions/0031-extracted-crate-dependency-rule.md` | 10-04 | new ADR |
| `.planning/decisions/0032-pdf-extraction-capability.md` | 10-05 | new ADR |
| `.planning/decisions/0033-cargo-doc-warning-bar.md` | 10-06 | new ADR |
| `.planning/REQUIREMENTS.md` | 10-01, 10-11 | modified |
| `.planning/ROADMAP.md` | 10-01, 10-11 | modified |
| `.planning/PROJECT.md` | 10-11 | modified |
| `.planning/decisions/PROMOTION.md` | 10-11 | modified (next free → 0034) |
| `.project/…/Epic_1/facade-audit.md`, `…/Epic_3/infrastructure-adapter-disposition.md`, `…/Epic_3/prd-relocate-remaining-misplaced-modules.md` | 10-02 | annotated |
| `.project/Milestone_7-Production-Hardening/overview/Milestone_7-Tier_4-Production_Hardening.md` | 10-03 | annotated |
| `.project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md` | 10-04, 10-05 | annotated |
| `.project/…/Epic_5/prd-document-facade-crate-role.md`, `.project/Milestone_7-Production-Hardening/Epic_4/epic-4-completion-summary.md` | 10-06 | annotated |
| `crates/paladin-content/Cargo.toml`, `crates/paladin-content/CHANGELOG.md`, `.cargo/audit.toml` | 10-05 | modified |
| `Makefile` | 10-06 | modified |

**No `.rs` file appears on this list, and none may be added to it (D-23).**

## Ledger contention contract — read before editing

Four plans write this file in wave 3, in parallel, over **disjoint contiguous** section ranges:

| Plan | Sections it owns | Rows |
|---|---|---|
| 10-07 | M7 Epic 1, M7 Epic 2 | 25 |
| 10-08 | M7 Epic 3, M7 Epic 4 | 22 |
| **10-09 (this plan)** | **M8 Epic 1, M8 Epic 2, M8 Epic 3** | **14** |
| 10-10 | M8 Epic 5, M8 Epic 6, M8 Epic 7, Cross-milestone | 21 |

M8 Epic 4's four rows were derived by plan 10-01 and are owned by no fan-out plan. **M8 Epic 4 sits
between this plan's Epic 3 section and plan 10-10's Epic 5 section — do not edit it.**

**Cell replacement only.** Replace the `Verdict` and `Evidence` cells of rows inside your own sections,
and fill your own sections' `**Epic note:**` lines. **Never** insert, delete, reorder or re-key a row;
**never** add or remove a section heading; **never** touch a row outside your range or edit the head
notes, the legend, the supersession summary table or the row-order section. Keep every row on one line.
This is what makes the four parallel diffs non-adjacent hunks, and it is why every plan asserts
`grep -c '^| REQ-'` equals `86` and that added and deleted line counts are equal.

## Requirement IDs in this plan's range

**M8 Epic 1 — Facade Crate Audit (4):** `REQ-facade-file-inventory`, `REQ-facade-file-classification`,
`REQ-shim-consumer-validation`, `REQ-facade-audit-document`.

**M8 Epic 2 — Dead Shim & Empty Module Removal (4):** `REQ-dead-file-batch-deletion`,
`REQ-stale-application-ports-audit`, `REQ-core-minimum-structure`, `REQ-libr-dead-reexport-removal`.

**M8 Epic 3 — Relocate Remaining Misplaced Modules (6):** `REQ-notification-task-closeout`,
`REQ-storage-shim-deletion`, `REQ-adapter-disposition-record`, `REQ-tensorflow-ml-feature-gate-v2`,
`REQ-garrison-sanctum-bridges-kept`, `REQ-m8-epic3-no-extractions`.

<tasks>

<task type="auto">
  <name>Task 1: Derive Milestone 8 Epic 1's four audit rows and Epic 2's four removal rows</name>
  <read_first>
    - `.planning/ledgers/milestone-07-08.md` — the head notes, the legend, the supersession summary table and the M8 Epic 1 and Epic 2 sections as the scaffold left them.
    - `.planning/ledgers/milestone-04-06.md` lines 108-160 — worked example rows in the target shape.
    - `.planning/decisions/0028-m8-reconciliation-authoritative.md` — the whole ADR: the supersession, the orphan test as three runnable steps, and the three in-execution corrections.
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/facade-cleanup-RECONCILIATION-2026-06-04.md` — §2 and §7, for the citations the rows carry.
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_1/facade-audit.md` (post-annotation) — the 189-file inventory, the 151/13/25 classification, and the banner plan 10-02 added.
    - `src/core/` — confirm by `ls` that it is exactly the six named files, for the minimum-structure row.
    - `src/application/ports/` — confirm by `ls` whether it exists at all, for the stale-ports row.
    - `src/lib.rs` — the alias declarations, for the dead-re-export row; run a search for the removed aliases and record the result.
    - Search the tree for the orphaned directories the batch-deletion row names, and for the notification-overlap file the PRD's open question required a review of, recording each result.
    - `.planning/intel/code-verification.md` lines 263-414 — the run-4 verification block, including this range's supersession-table rows.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-00e, D-00f, D-02, D-03, D-07, D-08.
  </read_first>
  <files>.planning/ledgers/milestone-07-08.md</files>
  <action>
Replace the Verdict and Evidence cells of the four M8 Epic 1 rows and the four M8 Epic 2 rows in place,
and fill both sections' `**Epic note:**` lines.

For each row: one of the seven legend classes, a `file:line` citation re-read at execution time, and a
named exerciser. For a deletion claim, the exerciser is the search that returns nothing — run it and
record the exact command and its zero result. No cell is left blank.

Specific rows needing more than a mechanical re-derivation:

- **`REQ-facade-file-classification`** — `superseded by outcome`, citing ADR-0028. The cell must carry
  the **factual** reason, not a procedural one: approximately 4,400 LOC of the classification's "stay"
  set were orphaned, uncompiled duplicate files described as active bridges that stay. State the
  classification's own figures and name the reconciliation as the account that replaces it.
- **`REQ-facade-audit-document`** — `superseded by outcome`, citing ADR-0028 and the reconciliation's
  own header, which names this document by path. Note that the document now carries a dated banner at
  source, added by plan 10-02.
- **`REQ-shim-consumer-validation`** — the reconciliation's reproducible orphan test is the version to
  keep. Cite ADR-0028's runnable three-step form by section rather than restating the procedure in the
  cell; a ledger cell that paraphrases a procedure is how the procedure drifts. Verdict reflects that
  the method is recorded and reusable.
- **`REQ-facade-file-inventory`** — the 189-file audit happened and is superseded in its conclusions
  but not in its existence. Give the verdict that reflects both, and say which part survives.
- **`REQ-dead-file-batch-deletion`** — the batch deletion is verifiable by absence. Run the searches
  and record them. The cell must also carry the residue the run-4 text names: the overlap review the
  PRD's open question required is recorded nowhere. That residue does not change the row's verdict for
  the deletion itself but must appear in the cell rather than being dropped.
- **`REQ-libr-dead-reexport-removal`** — marked `Verify → HARD-01`, never individually re-checked.
  Check it: search `src/lib.rs` for the aliases the requirement names and record what you found. Where
  a claim genuinely cannot be confirmed, `present, unproven` with the search recorded.

Both `**Epic note:**` lines state their epic's shape in one sentence, including how many rows are
superseded by outcome and that the superseding account is named in the summary table at the head of the
file.

Touch nothing outside the M8 Epic 1 and M8 Epic 2 sections.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/ledgers/milestone-07-08.md; echo "rows=$(grep -c "^| REQ-" "$f")"; echo "sections=$(grep -c "^### " "$f")"; echo "e1=$(awk "/^### Milestone 8 Epic 1/{p=1;next}/^### /{p=0}p" "$f" | grep -c "^| REQ-")"; echo "e2=$(awk "/^### Milestone 8 Epic 2/{p=1;next}/^### /{p=0}p" "$f" | grep -c "^| REQ-")"; echo "pending=$(awk "/^### Milestone 8 Epic 1/{p=1}/^### Milestone 8 Epic 3/{p=0}p" "$f" | grep -c "pending — plan")"; echo "adr28=$(grep -c "ADR-0028" "$f")"; echo "numstat=$(git diff --numstat -- "$f")"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| REQ-' .planning/ledgers/milestone-07-08.md` returns exactly `86` and `grep -c '^### '` returns exactly `12`.
    - The M8 Epic 1 section holds exactly `4` `REQ-*` rows and the M8 Epic 2 section exactly `4`, both with `0` matching `pending — plan` and `0` with an empty Verdict cell.
    - `grep -c 'ADR-0028' .planning/ledgers/milestone-07-08.md` returns at least `2` after this task.
    - The `REQ-facade-file-classification` row contains the numeric LOC figure and the phrase describing what the orphaned files were classified as, so the factual reason is in the row and not only in the ADR.
    - Every absence claim in this range names the search that produced it: each row asserting a file or directory is gone contains a command string, and re-running each returns zero matches.
    - `git diff --numstat -- .planning/ledgers/milestone-07-08.md` shows added lines equal to deleted lines.
    - The M8 Epic 4 section is byte-unchanged by this task: `git diff -- .planning/ledgers/milestone-07-08.md` touches no line between the Epic 3 and Epic 5 headings.
    - `git status --porcelain` lists only `.planning/ledgers/milestone-07-08.md` for this task's commit.
  </acceptance_criteria>
  <done>All eight M8 Epic 1 and Epic 2 rows carry a re-derived verdict, the two superseded audit rows state the factual reason with its LOC figure and cite ADR-0028, and every absence claim names the search that proved it.</done>
</task>

<task type="auto">
  <name>Task 2: Derive Milestone 8 Epic 3's six relocation rows with the three do-not-re-delete markers</name>
  <read_first>
    - `.planning/ledgers/milestone-07-08.md` — the M8 Epic 3 section as the scaffold left it, and the head notes' tie-break rule.
    - `.planning/decisions/0028-m8-reconciliation-authoritative.md` — sub-decisions (ii), (iii) and (iv): the three in-execution corrections, Epic 3's completeness with its commit and LOC figures, and the non-goal split.
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/facade-cleanup-RECONCILIATION-2026-06-04.md` §7 — the commit list, for the relocation citations.
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md` (post-annotation) — the 20-row all-Stays table, the two rows disagreeing with the governing PRD, the two crate names that do not exist, and the banner plan 10-02 added.
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/prd-relocate-remaining-misplaced-modules.md` (post-annotation) — the §5 non-goal clause and its dated correction.
    - `crates/paladin-battalion/src/` — locate the consolidated registry module and record its path and line count, for the first do-not-re-delete marker.
    - `crates/paladin-storage/src/` — locate the sqlite repository modules and confirm they are the active default-build implementation; check the root manifest for whether `paladin-storage` is optional. Record both, for the second marker.
    - `crates/paladin-notifications/` and `src/` — the notification adapters and their facade re-export, and confirm the three channel services are absent.
    - Search the tree for the ML adapter and its feature flag, confirming both are absent, for the feature-gate row.
    - `ls crates/` — confirm which of the two crate names the disposition record invents actually exist.
    - `.planning/intel/code-verification.md` lines 263-414 — the run-4 verification block and its supersession-table row for this epic.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-02, D-03, D-08, D-09.
  </read_first>
  <files>.planning/ledgers/milestone-07-08.md</files>
  <action>
Replace the Verdict and Evidence cells of the six M8 Epic 3 rows in place, and fill that section's
`**Epic note:**` line. Same rules as task 1: one of the seven classes, a citation re-read at execution
time, a named exerciser, no blank cells.

**The three do-not-re-delete markers.** These go in the ledger rows themselves, prefixed with a clear
marker phrase such as `DO NOT RE-DELETE:`, not only in ADR-0028. A reader looks at the ADR after a
question; a planner looks at the ledger row before one.

- **`REQ-storage-shim-deletion`** — carries the sqlite marker: the repository modules were **not**
  redundant, they were the active default-build implementation, and the resolution was making the
  storage crate non-optional rather than deleting shims naively. Cite the commit and the current
  manifest state you observed.
- **`REQ-notification-task-closeout`** — the adapters live in the notifications crate with a facade
  re-export and the three channel services were deleted rather than moved. Record both halves; the
  deleted half is why a later reader might think a relocation is missing.
- **`REQ-garrison-sanctum-bridges-kept`** — carries the registry marker: the facade implementation was
  richer than the leaf-crate copy, so the richer one was consolidated **into** the leaf crate rather
  than deleted blindly. Also record the row's own factual correction: the run-4 text notes that one
  file's line count was mis-attributed in the original document, and that the file was later deleted
  anyway. State the corrected figure and its source.

The third marker — that the remaining files genuinely were orphaned, so the mysql repository, the
input fetchers, the document adapters, the API deliverer and the error-log adapter were correctly
removed — belongs in this section's `**Epic note:**`, because it applies to the epic rather than to one
row.

Other rows needing more than a mechanical re-derivation:

- **`REQ-m8-epic3-no-extractions`** — `superseded by outcome`, citing ADR-0028. The cell states that
  the relocations **executed inside Milestone 8**, with the commit count and net LOC figure ADR-0028
  measured, and that they must not be planned as Milestone 9 candidates. This row also appears in the
  supersession summary table at the head of the ledger; the two statements must agree.
- **`REQ-adapter-disposition-record`** — `superseded by outcome`, citing ADR-0028. The cell must carry
  three separable facts on one row: the 20-row table marks everything as staying and the tree
  contradicts it; two rows disagree with the governing PRD; and it names two crates, of which you must
  record how many exist after checking. Name FACADE-04 as the phase that owns those two names, so a
  reader does not have to find that connection elsewhere.
- **`REQ-tensorflow-ml-feature-gate-v2`** — `superseded by outcome`: both the gate and the adapter were
  deleted. Run the searches, record them, and cross-reference the deferred register row in plan 10-10's
  range by ID without writing it.

The `**Epic note:**` line states the epic's shape in one sentence — that Epic 3 is complete in substance
with its commit and LOC figures, that three of the original audit's classifications were corrected
during execution and where each marker sits, and the per-class verdict counts.

Touch nothing outside the M8 Epic 3 section. In particular, the M8 Epic 4 section immediately below it
was derived by plan 10-01 and must be byte-unchanged.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/ledgers/milestone-07-08.md; echo "rows=$(grep -c "^| REQ-" "$f")"; echo "sections=$(grep -c "^### " "$f")"; echo "e3=$(awk "/^### Milestone 8 Epic 3/{p=1;next}/^### /{p=0}p" "$f" | grep -c "^| REQ-")"; echo "pending=$(awk "/^### Milestone 8 Epic 3/{p=1;next}/^### /{p=0}p" "$f" | grep -c "pending — plan")"; echo "markers=$(grep -ci "DO NOT RE-DELETE" "$f")"; echo "facade04=$(grep -c "FACADE-04" "$f")"; echo "numstat=$(git diff --numstat -- "$f")"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| REQ-' .planning/ledgers/milestone-07-08.md` returns exactly `86` and `grep -c '^### '` returns exactly `12`.
    - The M8 Epic 3 section holds exactly `6` `REQ-*` rows, `0` matching `pending — plan`, and `0` with an empty Verdict cell.
    - `grep -ci 'DO NOT RE-DELETE' .planning/ledgers/milestone-07-08.md` returns at least `2` — both row-level markers are present in the rows, not only in the ADR.
    - The section's `**Epic note:**` line carries the third correction, naming the file groups that genuinely were orphaned.
    - `grep -c 'FACADE-04' .planning/ledgers/milestone-07-08.md` returns at least `1`.
    - The `REQ-m8-epic3-no-extractions` row reads `superseded by outcome`, cites ADR-0028, carries the commit count and net LOC figure, and its `REQ-*` ID also appears in the supersession summary table at the head of the file with a consistent statement.
    - Every absence claim in this range names the search that produced it, and re-running each returns zero matches.
    - `git diff --numstat -- .planning/ledgers/milestone-07-08.md` shows added lines equal to deleted lines.
    - The M8 Epic 4 section is byte-unchanged: `git diff -- .planning/ledgers/milestone-07-08.md` touches no line between the M8 Epic 4 heading and the M8 Epic 5 heading.
    - `git status --porcelain` lists only `.planning/ledgers/milestone-07-08.md` for this task's commit.
  </acceptance_criteria>
  <done>All six M8 Epic 3 rows carry a re-derived verdict, two rows carry explicit do-not-re-delete markers with the observed evidence, the epic note carries the third correction, the completeness row states its measured figures and agrees with the summary table, and plan 10-01's Epic 4 section is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a superseded audit document's classifications → a later cleanup phase's delete list | Three of the original audit's classifications were corrected during execution; a planner reading only the audit would re-delete load-bearing modules. |
| four parallel executors → one ledger file, with a section between two ranges owned by neither | M8 Epic 4 sits between this plan's Epic 3 and plan 10-10's Epic 5 and belongs to plan 10-01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-36 | Tampering | load-bearing modules the original audit mis-classified | high | mitigate | Dropping a do-not-re-delete marker from the ledger row, on the grounds that ADR-0028 already holds it, is how the next facade phase deletes the consolidated registry module or the active sqlite repositories. Both markers are required in the rows, the third in the epic note, and acceptance criteria grep for the marker phrase and for the epic note's content. |
| T-10-37 | Repudiation | Epic 3's completeness claim | high | mitigate | Its own PRD defers the relocations to Milestone 9 while the tree shows them executed. The row must state the executed outcome with the commit count and net LOC figure ADR-0028 measured, and must agree with the supersession summary table so the two statements cannot drift. |
| T-10-38 | Tampering | the M8 Epic 4 section | medium | mitigate | It sits between this plan's last section and the next plan's first, and belongs to plan 10-01. An acceptance criterion asserts the diff touches no line between the Epic 4 and Epic 5 headings. |
| T-10-39 | Repudiation | absence claims | medium | mitigate | Six rows in this range assert that something is gone. Each must name the search that produced the zero result, and every such command is re-runnable at verification time. |
| T-10-40 | Information disclosure | none | low | accept | This plan reads source and planning files and writes a planning document. No secret, credential or user data is in scope. |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing, adds no dependency and modifies no manifest — it reads them. `10-RESEARCH.md`'s Package Legitimacy Audit records the phase as not-applicable; `git status --porcelain -- Cargo.toml Cargo.lock` is empty. |
</threat_model>

<verification>
- `grep -c '^| REQ-' .planning/ledgers/milestone-07-08.md` → `86`; `grep -c '^### '` → `12`.
- The M8 Epic 1, 2 and 3 sections hold 4, 4 and 6 rows, with zero `pending — plan` markers and zero blank Verdict cells.
- `grep -ci 'DO NOT RE-DELETE' .planning/ledgers/milestone-07-08.md` → at least `2`, and the M8 Epic 3 epic note carries the third correction.
- The Epic 3 completeness row and the supersession summary table make the same statement about the relocations having executed.
- `git diff --numstat` on the ledger shows added lines equal to deleted lines, and the M8 Epic 4 section is byte-unchanged.
- `git status --porcelain -- '*.rs'` → empty. D-23's boundary holds for this plan.
</verification>

<success_criteria>
A planner opening the Milestone 8 Epic 1-3 range sees, in the rows rather than in a linked document,
that the relocations already executed, that three of the original audit's classifications were
corrected during execution and which files those were, and that the disposition record's two invented
crate names have an owner. Every absence claim in the range names the command that proved it.
</success_criteria>

<output>
Create `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-09-SUMMARY.md` when done.
Record the per-class verdict counts for all three epics and the exact absence-search commands used.
</output>
