---
phase: 10-milestone-7-8-ground-truth-recorded-account
plan: 11
type: execute
wave: 4
depends_on: ["10-01", "10-02", "10-03", "10-04", "10-05", "10-06", "10-07", "10-08", "10-09", "10-10"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/decisions/PROMOTION.md
  - .planning/ledgers/milestone-07-08.md
autonomous: true
requirements: [HARD-01, HARD-02, HARD-03, HARD-04, HARD-05, HARD-06, HARD-07]

must_haves:
  truths:
    - "Each of HARD-01 through HARD-07 is `[x]` in `REQUIREMENTS.md` only behind evidence: the checkbox flip is accompanied by a dated note naming the artefact that closes it and the command or `file:line` that proves the artefact exists (D-00e, D-25)"
    - "`PROMOTION.md`'s numbering index carries six new rows for ADR-0028 through ADR-0033 and its next-free line reads 0034, with a dated note in the same voice as the existing ones explaining why the line advances by six (D-24)"
    - "Four forward hand-off blocks are written in `REQUIREMENTS.md` in the shape Phase 9 used for this phase: Phase 11 / FACADE-02, Phase 11 / FACADE-03(b), Phase 12 / SUPPLY-02, and Phase 13 / ORCH-05 (D-26)"
    - "Every ADR cross-reference written into the ledger by plans 10-07 through 10-10 resolves to a file that exists, so no row cites a decision that was never written"
    - "The ledger's row inventory survived the parallel wave-3 fan-out intact: 86 rows, 86 distinct `REQ-*` IDs, twelve sections in run-4 order, zero pending stubs, zero blank Verdict cells"
    - "A dated close-out amendment section at the foot of the ledger records what this phase re-derived versus cited, which citations had moved, and which claims are CI-only — appended, never replacing an earlier section (D-00d)"
    - "The phase's `.rs` change count is zero and is asserted rather than assumed, so D-23's boundary is verifiable after the fact rather than only stated before it"
    - "`ROADMAP.md`'s Phase 10 section records the phase complete with its plan list checked, and its criterion-1 row count carries the correction plan 10-01 applied"
    - statement: "ADR-0006's 84% workspace line-coverage floor is unmoved. `cargo-llvm-cov` is not installable in this environment, so the claim rests on the reasoning that a phase modifying no `.rs` file cannot move line coverage — stated as reasoning, with the zero-`.rs`-change assertion as its evidence, and never as a measurement."
      verification: backstop
    - statement: "The CLAUDE.md workspace gate passes: `cargo test`, then `cargo fmt --check`, then `cargo clippy -- -D warnings`. All three run in this environment and the close-out executes them against the final tree. The one config change that could affect a build is a removed feature declaration that gated nothing; if any of the three fails, the failure is recorded and the phase does not close on an inferred pass."
      verification: backstop
  artifacts:
    - path: ".planning/decisions/PROMOTION.md"
      provides: "Six new index rows and the next-free line advanced to 0034, with a dated note"
      contains: "Next free ADR number: 0034"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Seven evidence-backed checkbox flips, updated traceability rows, and four forward hand-off blocks"
      contains: "Hand-off to Phase 11"
    - path: ".planning/ledgers/milestone-07-08.md"
      provides: "A dated Phase 10 close-out amendment section recording what was re-derived, what moved, and what is CI-only"
      contains: "close-out"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/decisions/PROMOTION.md"
      via: "The close-out advancing the next-free line to 0034 after six ADRs were authored across this phase's plans"
      pattern: "0034"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ledgers/milestone-07-08.md"
      via: "HARD-01's closure note citing the ledger's row count and the pointer section that replaced the inline copy"
      pattern: "milestone-07-08\\.md"
  prohibitions:
    - "A checkbox must not be flipped ahead of its evidence. Marking a requirement complete because its plans ran, rather than because the artefact exists and a command proves it, is prohibited — it is the checkbox unreliability this whole close-out series exists to retire."
    - "A closure claim must not be recorded as measured when the tool was unavailable. `cargo-llvm-cov`, `cargo audit` and `cargo deny` cannot run here; recording the coverage floor or the advisory gates as verified is prohibited even though CI would likely pass."
    - "A hand-off must not be written as a summary. Each of the four forward blocks names the specific requirement ID that inherits, the specific artefact it inherits, and what it must not re-derive; a general pointer at this phase is prohibited because it is exactly what the receiving phase cannot act on."
    - "A dated amendment must not replace an earlier one. The ledger's close-out section is appended; rewriting or absorbing a wave-3 epic note into it is prohibited under the amend-in-place convention."
---

<objective>
Close Phase 10: flip HARD-01 through HARD-07 behind evidence, advance the ADR numbering line, write the
four forward hand-off blocks, append the ledger's close-out amendment, and prove the phase's boundary
held.

Purpose: this corpus's dominant defect is a checkbox that means something other than what it says. The
close-out is where this phase either reproduces that defect or does not. Every flip carries its
evidence, every unavailable tool is named as unavailable, and the one assertion that makes D-23
checkable — zero `.rs` files changed — is run rather than assumed.
Output: seven evidence-backed flips, six index rows, four hand-off blocks, one amendment section, one
phase gate.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-VALIDATION.md
</context>

## Artifacts this phase produces

| Artifact | Plan | Kind |
|---|---|---|
| `.planning/ledgers/milestone-07-08.md` | 10-01, 10-07…10-10, **10-11 (close-out amendment)** | new file |
| `.planning/decisions/0028-m8-reconciliation-authoritative.md` | 10-02 | new ADR |
| `.planning/decisions/0029-version-trajectory-history.md` | 10-03 | new ADR |
| `.planning/decisions/0030-milestone-7-self-numbering.md` | 10-03 | new ADR |
| `.planning/decisions/0031-extracted-crate-dependency-rule.md` | 10-04 | new ADR |
| `.planning/decisions/0032-pdf-extraction-capability.md` | 10-05 | new ADR |
| `.planning/decisions/0033-cargo-doc-warning-bar.md` | 10-06 | new ADR |
| `.planning/REQUIREMENTS.md` | 10-01, **10-11** | modified |
| `.planning/ROADMAP.md` | 10-01, **10-11** | modified |
| `.planning/PROJECT.md` | **10-11** | modified |
| `.planning/decisions/PROMOTION.md` | **10-11** | modified (index + next free → 0034) |
| `.project/…/Epic_1/facade-audit.md`, `…/Epic_3/infrastructure-adapter-disposition.md`, `…/Epic_3/prd-relocate-remaining-misplaced-modules.md` | 10-02 | annotated |
| `.project/Milestone_7-Production-Hardening/overview/Milestone_7-Tier_4-Production_Hardening.md` | 10-03 | annotated |
| `.project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md` | 10-04, 10-05 | annotated |
| `.project/…/Epic_5/prd-document-facade-crate-role.md`, `.project/Milestone_7-Production-Hardening/Epic_4/epic-4-completion-summary.md` | 10-06 | annotated |
| `crates/paladin-content/Cargo.toml`, `crates/paladin-content/CHANGELOG.md`, `.cargo/audit.toml` | 10-05 | modified |
| `Makefile` | 10-06 | modified |

**No `.rs` file appears on this list. Task 3 asserts that, for the whole phase, with a command.**

## Operational notes for the executor

- `.planning/config.json` sets `worktree_skip_hooks: true`; a worktree commit does not need to cold-build
  the workspace. Task 3's workspace gate is run deliberately and separately, not as a commit hook.
- If the `gsd-tools` commit helper reports `commit_failed` with output truncated at a clippy line, that
  is a known false negative in this repository. Verify with `git log -1 --stat` before retrying, and
  commit with `git` directly if the commit already landed.

<tasks>

<task type="auto">
  <name>Task 1: Flip HARD-01 through HARD-07 behind evidence and update the traceability rows</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 1357-1482 — HARD-01 through HARD-07 in full, as plan 10-01's corrections left them. Re-derive line numbers.
    - `.planning/REQUIREMENTS.md` around line 3913 — the traceability table, and the rows for the HARD requirements. Locate them by searching for `HARD-0`.
    - `.planning/REQUIREMENTS.md` lines 1320-1355 — Phase 9's hand-off block, as the model for the closure-note voice.
    - The ledger `.planning/ledgers/milestone-07-08.md` in full — the evidence HARD-01's flip rests on.
    - `.planning/decisions/0028-…` through `0033-…` — confirm all six exist, by `ls`, and read each `## Code Conformance` line, because two of them read `must change` and their flips depend on the change having landed.
    - All ten SUMMARY files `10-01-SUMMARY.md` through `10-10-SUMMARY.md` — the evidence each plan recorded. Do not re-derive from memory; each closure note cites a SUMMARY and the artefact it names.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-VALIDATION.md` → the Per-Task Verification Map and the Manual-Only table.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-00e, D-25, D-26.
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
For each of HARD-01 through HARD-07, in order: run the closing evidence, then flip the checkbox, then
write a dated closure note. Never the other way round. A checkbox flipped because its plans ran is the
defect this corpus keeps rediscovering.

Each closure note is one short paragraph appended to the requirement, dated 2026-08-08, naming this
plan, and containing three things: the artefact that closes it, the command or `file:line` that proves
the artefact exists and says what it claims, and the SUMMARY that recorded the work. Specifically:

- **HARD-01** — the ledger. Evidence: the row count, the distinct-ID count, the section count and the
  zero-pending-stub count, all re-run against the file now, plus the `REQUIREMENTS.md` pointer that
  replaced the inline copy. Note the corrected 13-row supersession figure.
- **HARD-02** — ADR-0028 and the three annotated documents. Evidence: the ADR file exists with `conforms`
  and names FACADE-02, FACADE-03 and FACADE-04 downstream; each of the three documents carries its dated
  banner; the ledger's Epic 1 and Epic 3 rows carry the do-not-re-delete markers.
- **HARD-03** — ADR-0029. Evidence: the file exists with its `## Trajectory` table; REL-01 is still `[x]`
  and was not re-opened; the version and tag figures the ADR states reproduce against the tree now.
- **HARD-04** — ADR-0030. Evidence: it cites ADR-0010 and ADR-0014; the Milestone 7 overview carries its
  banner while its self-title survives verbatim; the fifth-instance prediction is recorded discharged.
- **HARD-05** — ADR-0031. Evidence: the restated invariant and its checking command are in the file;
  `prd-extract-infrastructure-crates.md` carries the two HARD-05 corrections; the ledger's
  dependency-rule row reads `satisfied` with the wording-not-code explanation.
- **HARD-06** — ADR-0032 and the config changes. Evidence: the manifest grep returns zero for the removed
  feature and one for the retained comparator; the advisory comment states the true path with the
  suppression intact; the CHANGELOG entry exists.
- **HARD-07** — ADR-0033 and the Makefile fix. Evidence: the Makefile greps return zero for the exclusion
  and for the stale reason; ADR-0033 records the measured warning state with its count, crate split and
  owning phase; FR-19 carries its dated correction. **State in the closure note that the gate itself is
  still red and that the debt has a named owner** — HARD-07 is closed because the bar is now recorded
  once with its measured state, not because the tree clears it.

Then update each HARD requirement's traceability row to name Phase 10 and its status, in the same form
the surrounding rows use. Read the table's existing column shape before editing; do not invent one.

Finally, write the **four forward hand-off blocks**, using Phase 9's block at `REQUIREMENTS.md:1320-1355`
as the exact shape to imitate — heading level `####`, a bold opening sentence naming which requirement
inherits what, a numbered list of specific claims, and a closing evidence paragraph. Place them where a
reader of the receiving requirement will find them, following the placement Phase 9 used.

  1. **Hand-off to Phase 11 / FACADE-02** — ADR-0031's restated default-build invariant, which is what
     legalises D2/D3/D4's leaf-to-leaf relocation targets and under exactly which conditions; and
     ADR-0028's record that the Epic 3 relocations already executed inside Milestone 8, so its Milestone
     9 candidate list must not re-plan them.
  2. **Hand-off to Phase 11 / FACADE-03(b)** — the non-goal split from ADR-0028: overridden for the crate
     that was created, still holding for the one that was not, with the directory evidence for both
     halves.
  3. **Hand-off to Phase 12 / SUPPLY-02 and SUPPLY-03** — D-19's answer to the `pdf-extract`
     reachability question, delivered rather than deferred: the corrected `.cargo/audit.toml` reasoning,
     what it now says, and that the suppression set is unchanged. State that Phase 12 inherits this as an
     answer, not a question, and that the `cargo audit` / `cargo deny check` confirmation remains CI-only.
  4. **Hand-off to Phase 13 / ORCH-05** — ADR-0029's `## Trajectory` table to append v0.3.0 through
     v0.6.0 rows to, in ascending order without re-sorting or re-keying, and the note that REL-01 is
     already converged so ORCH-05 applies rather than re-decides.

Each block names a requirement ID, an artefact and a thing not to re-derive. A block that merely points
at this phase is not a hand-off.
  </action>
  <verify>
    <automated>bash -c 'echo "flipped=$(grep -cE "^- \[x\] \*\*HARD-0[1-7]\*\*" .planning/REQUIREMENTS.md)"; echo "unflipped=$(grep -cE "^- \[ \] \*\*HARD-0[1-7]\*\*" .planning/REQUIREMENTS.md)"; echo "handoffs=$(grep -c "^#### Hand-off to Phase 1[123]" .planning/REQUIREMENTS.md)"; echo "closures=$(grep -c "dated 2026-08-08 (plan 10-11)" .planning/REQUIREMENTS.md)"; echo "rel01=$(sed -n \"358p\" .planning/REQUIREMENTS.md | grep -c \"\\[x\\]\")"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^- \[x\] \*\*HARD-0[1-7]\*\*' .planning/REQUIREMENTS.md` returns `7` and `grep -cE '^- \[ \] \*\*HARD-0[1-7]\*\*' …` returns `0`.
    - `grep -c 'dated 2026-08-08 (plan 10-11)' .planning/REQUIREMENTS.md` returns at least `7` — every flip carries a dated closure note.
    - Each closure note names at least one command or `file:line`: the seven notes collectively contain at least `7` occurrences of a `[A-Za-z0-9_./-]*:[0-9]` citation or a backticked command.
    - `grep -c '^#### Hand-off to Phase 1' .planning/REQUIREMENTS.md` returns at least `4` — the four D-26 blocks exist as separate `####` sections.
    - Each hand-off block names a requirement ID: `grep -c 'FACADE-02' .planning/REQUIREMENTS.md`, `grep -c 'FACADE-03' …`, `grep -c 'SUPPLY-0' …` and `grep -c 'ORCH-05' …` each return at least `1` within the hand-off region.
    - HARD-07's closure note states that the documentation gate is still red and names the owning phase: `grep -ciE 'still red|does not currently clear|remains open' <the HARD-07 note>` returns at least `1`.
    - `sed -n '358p' .planning/REQUIREMENTS.md` still matches `[x]` — REL-01 was not disturbed.
    - Every HARD requirement has a traceability row naming Phase 10: `grep -cE 'HARD-0[1-7].*Phase 10' .planning/REQUIREMENTS.md` returns at least `7`.
    - `git status --porcelain` lists only `.planning/REQUIREMENTS.md` for this task's commit.
  </acceptance_criteria>
  <done>All seven HARD requirements are `[x]`, each behind a dated note naming its artefact, its proving command and its SUMMARY; the traceability rows name Phase 10; and four hand-off blocks each name a receiving requirement ID, an artefact and something not to re-derive.</done>
</task>

<task type="auto">
  <name>Task 2: Advance the ADR numbering line to 0034 and record the phase's decisions in PROJECT.md</name>
  <read_first>
    - `.planning/decisions/PROMOTION.md` in full — the numbering index table at lines 21-49, the next-free line at approximately `:51`, and the three existing dated notes at `:53-70` whose voice the new note must match. Re-derive line numbers.
    - `ls .planning/decisions/` — confirm exactly six new ADR files exist and that their numbers are 0028 through 0033 with no gap and no reuse.
    - Each of the six ADRs' title lines — the slug each row's `Slug` column must carry, taken from the filename.
    - `.planning/PROJECT.md` — the Key Decisions section, its existing row shape and column set. Read before editing; do not invent a format.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md` → the `PROMOTION.md` section, which gives the exact append pattern and the dated-note wording shape.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-00a, D-24.
  </read_first>
  <files>.planning/decisions/PROMOTION.md, .planning/PROJECT.md</files>
  <action>
**`PROMOTION.md`.** Append six rows to the numbering index table, in numeric order, one per new ADR,
each carrying the number, the slug taken from the filename, and a one-line subject ending with
`(Phase 10)` in the same form the existing Phase 9 rows use. Then change the next-free line to read
`0034`. Then add a dated note beneath the existing ones, in their voice, recording that the line
advances by **six** in one phase — from 0028 to 0034 — because Phase 10 authored all six of ADR-0028
through ADR-0033 across its own plans, naming which plan authored which; that this matches the
precedent the Phase 9 note above it established for four ADRs in one phase; and that none of the six
numbers was skipped or reused. Name the two whose `Code Conformance` reads `must change` and the plan
that executed each.

Before writing the note, verify the claim it makes: `ls .planning/decisions/` and confirm the six files
exist with contiguous numbers, and confirm no existing ADR number was reused.

Do not renumber, reword or reorder any existing index row. Do not touch the required-heading-set
section or the supersession-mechanism section.

**`PROJECT.md`.** Add one row per new ADR to the Key Decisions section, following the section's existing
row shape exactly — read it first. Each row names the decision in one line and cites the ADR by number.
Change nothing else in the file.
  </action>
  <verify>
    <automated>bash -c 'echo "nextfree=$(grep -n "Next free ADR number" .planning/decisions/PROMOTION.md)"; echo "newrows=$(grep -cE "^\| 003[0-3] \||^\| 002[89] \|" .planning/decisions/PROMOTION.md)"; echo "adrfiles=$(ls .planning/decisions/003*.md .planning/decisions/002[89]*.md 2>/dev/null | wc -l)"; echo "projrows=$(grep -cE "ADR-00(2[89]|3[0-3])" .planning/PROJECT.md)"; echo "note=$(grep -c "plan 10-11" .planning/decisions/PROMOTION.md)"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Next free ADR number: 0034' .planning/decisions/PROMOTION.md` returns `1`, and `grep -c 'Next free ADR number: 0028' …` returns `0`.
    - `grep -cE '^\| 002[89] \||^\| 003[0-3] \|' .planning/decisions/PROMOTION.md` returns exactly `6` — six new index rows, one per ADR.
    - `ls .planning/decisions/ | grep -cE '^00(2[89]|3[0-3])-.*\.md$'` returns exactly `6`, and the six numbers are contiguous from 0028 to 0033 with none missing.
    - Each of the six index rows' slug matches its filename: for every new row, the slug column equals the filename between the number prefix and `.md`.
    - `grep -c 'plan 10-11' .planning/decisions/PROMOTION.md` returns at least `1`, and the dated note states the line advances by six and names which plan authored each ADR.
    - `git diff -- .planning/decisions/PROMOTION.md | grep -c '^-[^-]'` returns `1` — exactly one line was replaced, the next-free line; every other change is additive and no existing index row was disturbed.
    - `grep -cE 'ADR-00(2[89]|3[0-3])' .planning/PROJECT.md` returns at least `6`.
    - `git status --porcelain -- '*.rs'` is empty.
  </acceptance_criteria>
  <done>`PROMOTION.md` carries six new index rows whose slugs match their filenames, a next-free line reading 0034, and a dated note whose claim about contiguity was verified before it was written; `PROJECT.md` records all six decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Append the ledger close-out amendment, close the ROADMAP section, and run the phase gate</name>
  <read_first>
    - `.planning/ledgers/milestone-07-08.md` in full — the head notes, the twelve sections as the four fan-out plans left them, and `milestone-01.md` lines 21-38 for the dated amendment-section shape.
    - All ten SUMMARY files `10-01-SUMMARY.md` through `10-10-SUMMARY.md` — the per-class verdict counts, the citations that had moved, the absence-search commands, the measured `cargo doc` figures and the checkpoint answers. The amendment section is assembled from these, never re-derived from memory.
    - `.planning/ROADMAP.md` §"Phase 10" around line 653 — the phase section, its plan list as the planner wrote it, and criterion 1 as plan 10-01 corrected it.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-VALIDATION.md` → the Per-Task Verification Map and the three Manual-Only rows, each of which needs an explicit disposition here.
    - `.planning/decisions/0006-coverage-gate.md` — the 84% workspace line-coverage floor, for the confirmation this task records as reasoning rather than measurement.
    - `CLAUDE.md` — the workspace gate order: `cargo test`, then `cargo fmt --check`, then `cargo clippy -- -D warnings`.
    - `.planning/phases/04-release-coherence/04-ci-gate-deferrals.md` — the standing record of which tools are unavailable in this environment.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-00d, D-23, D-25.
  </read_first>
  <files>.planning/ledgers/milestone-07-08.md, .planning/ROADMAP.md</files>
  <action>
**First, run the whole-ledger integrity check** and record every number in the SUMMARY before writing
anything: the `REQ-*` row count, the distinct-ID count, the section count and their order against the
run-4 order, the pending-stub count, the blank-Verdict-cell count, and — the check no earlier plan
could make — that **every ADR number cited anywhere in the ledger resolves to a file that exists**.
Extract every `ADR-[0-9]{4}` occurrence, resolve each against `.planning/decisions/`, and record any
that does not resolve. If one does not, fix the citing row before proceeding; a row citing a decision
that was never written is precisely the defect this phase exists to retire.

**Append a dated close-out amendment section** at the foot of the ledger, headed in the
`## Phase 10 close-out amendments (2026-08-08)` form `milestone-01.md` uses. Append it; do not rewrite
or absorb any wave-3 epic note. It records, sourced from the ten SUMMARY files by name:

  - the per-class verdict distribution across all 86 rows, and the total per class;
  - which rows were **cited rather than re-derived** — the six Phase-9-closed rows in Epic 4 and the one
    in Epic 2 — and how many of their citations had moved since Phase 9's hand-off block was written;
  - which claims are **CI-only** and were never measured here: the advisory tooling, the coverage floor,
    and anything Docker. Name the exact command a runner executes for each;
  - the measured `cargo doc` state as ADR-0033 recorded it, with its count, crate split and date, and
    the phase that owns closing it;
  - the three rows carrying do-not-re-delete markers and where each marker sits;
  - the checkpoint answers plan 10-04 recorded, so the ledger says on its own page which branch each
    flagged decision took.

**Update `ROADMAP.md`'s Phase 10 section**: mark each plan in the plan list complete, and add a dated
completion note in the form the surrounding phase sections use, naming the ledger, the six ADRs and the
three config changes. Leave criterion 1's correction from plan 10-01 in place and do not re-correct it.
Change no other phase section.

**Run the phase gate** and record every result verbatim in the SUMMARY:

  1. `git diff --name-only <phase base>..HEAD -- '*.rs' | wc -l` → must be `0`. This is what makes
     D-23's boundary checkable after the fact rather than only stated before it. If it is not zero,
     stop, name the files, and record why rather than proceeding.
  2. The CLAUDE.md workspace gate, in order: `cargo test`, `cargo fmt --check`,
     `cargo clippy -- -D warnings`. All three run here. Record each result. If any fails, record the
     failure and do not close the phase on an inferred pass.
  3. `cargo metadata --no-deps --format-version 1 >/dev/null` — the manifest still parses after plan
     10-05's deletion.
  4. `make -n release-check` — the target still expands after plan 10-06's deletion, with its other
     four steps intact.
  5. ADR-0006's coverage floor: `cargo-llvm-cov` is not installable here. **Record the confirmation as
     reasoning, not measurement**: a phase that modifies no `.rs` file cannot move line coverage, and
     step 1's zero-change assertion is that reasoning's evidence. Do not run a partial substitute and
     present it as the floor.
  6. Each of the three Manual-Only rows in `10-VALIDATION.md` gets an explicit disposition recorded —
     what was attempted, what was observed, and what remains CI-only. None may be left implicit.

Do not update `10-VALIDATION.md`'s frontmatter status; that belongs to the validation workflow, not to
this plan.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/ledgers/milestone-07-08.md; echo "rows=$(grep -c "^| REQ-" "$f")"; echo "uniq=$(grep -o "^| REQ-[a-z0-9-]*" "$f" | sort -u | wc -l)"; echo "sections=$(grep -c "^### " "$f")"; echo "pending=$(grep -c "pending — plan" "$f")"; echo "blank=$(grep -c "^| REQ-[a-z0-9-]* *| *|" "$f")"; echo "closeout=$(grep -c "^## Phase 10 close-out amendments" "$f")"; echo "danglingADR=$(grep -o "ADR-[0-9]\{4\}" "$f" | sort -u | while read a; do n=${a#ADR-}; ls .planning/decisions/${n}-*.md >/dev/null 2>&1 || echo "$a"; done | wc -l)"; echo "rs=$(git status --porcelain -- "*.rs" | wc -l)"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| REQ-' .planning/ledgers/milestone-07-08.md` returns `86`; the distinct-ID count returns `86`; `grep -c '^### '` returns `12`.
    - `grep -c 'pending — plan' .planning/ledgers/milestone-07-08.md` returns `0` and `grep -c '^| REQ-[a-z0-9-]* *| *|' …` returns `0`.
    - Every `ADR-NNNN` cited in the ledger resolves: extracting each unique occurrence and testing `ls .planning/decisions/NNNN-*.md` yields zero failures.
    - `grep -c '^## Phase 10 close-out amendments' .planning/ledgers/milestone-07-08.md` returns exactly `1`, and it is the last `##` heading in the file.
    - The close-out section names all ten SUMMARY files it sourced from: `grep -cE '10-0[1-9]-SUMMARY|10-10-SUMMARY' .planning/ledgers/milestone-07-08.md` returns at least `10`.
    - `git diff --numstat -- .planning/ledgers/milestone-07-08.md` for this task shows `0` deleted lines — the amendment is appended, and no wave-3 epic note was rewritten.
    - `git diff --name-only <phase base>..HEAD -- '*.rs' | wc -l` returns `0`, and the exact command and its output are recorded in the SUMMARY.
    - `cargo test`, `cargo fmt --check` and `cargo clippy -- -D warnings` each ran and their results are recorded verbatim; the SUMMARY states pass or fail for each rather than asserting the gate held.
    - `cargo metadata --no-deps --format-version 1` exits 0 and `make -n release-check` exits 0.
    - All three Manual-Only rows from `10-VALIDATION.md` have an explicit recorded disposition, and the coverage-floor row is recorded as reasoning with the zero-`.rs`-change evidence, not as a measurement.
    - `.planning/ROADMAP.md`'s Phase 10 plan list has every plan marked complete, and criterion 1 still carries plan 10-01's correction.
  </acceptance_criteria>
  <done>The ledger passes a whole-file integrity check with no dangling ADR citation, carries one appended dated close-out section sourced from all ten SUMMARY files, the ROADMAP section is closed, the phase's zero-`.rs`-change boundary is proved by a command, the workspace gate's three results are recorded verbatim, and every unavailable tool is named rather than inferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan completion → requirement closure | Ten plans ran; that is not the same claim as seven requirements being satisfied, and the checkbox is where this corpus has repeatedly conflated them. |
| unavailable tooling → a recorded gate result | `cargo-llvm-cov`, `cargo audit` and `cargo deny` cannot run here, and CI would probably pass; recording that probability as a result is how an unfalsifiable claim enters the corpus. |
| a ledger citing decisions written in another wave | Rows written by four parallel plans cite six ADRs written by four other plans; nothing before this task checks that every citation resolves. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-46 | Repudiation | the seven HARD checkboxes | high | mitigate | A flip that means "the plans ran" rather than "the artefact exists and a command proves it" reproduces the exact defect five ingest runs kept rediscovering. Task 1 requires running the closing evidence before flipping, and every note must cite a command or `file:line` and the SUMMARY that recorded the work. |
| T-10-47 | Spoofing | ADR cross-references inside the ledger | high | mitigate | Four parallel plans cite six ADRs written elsewhere; a typo or a number that was never allocated reads as authoritative and resolves to nothing. Task 3 extracts every `ADR-NNNN` occurrence and resolves each against the decisions directory, failing closed on any that does not. |
| T-10-48 | Repudiation | the coverage floor and the advisory gates | high | mitigate | Recording an inferred pass for a tool that cannot run puts an unfalsifiable claim above every PRD. The coverage floor is recorded as reasoning backed by the zero-`.rs`-change assertion; the advisory gates are recorded CI-only with their exact commands; both ride into `must_haves` as backstop truths so verification cannot green them silently. |
| T-10-49 | Elevation of privilege | the phase's change boundary | high | mitigate | D-23 is only a claim until something checks it. Task 3 runs a name-only diff across the whole phase restricted to Rust sources and requires zero, halting and naming files if not — which makes the boundary auditable after the fact rather than only asserted before it. |
| T-10-50 | Tampering | the ledger's wave-3 content | medium | mitigate | A close-out that rewrote or absorbed an epic note would destroy the per-wave provenance the amend-in-place convention preserves. The section is appended and an acceptance criterion asserts zero deleted lines in this task's ledger diff. |
| T-10-51 | Tampering | `PROMOTION.md`'s existing index | medium | mitigate | Six appended rows plus one replaced line is the whole edit; a reformat or renumber would break every earlier phase's citation-by-number. An acceptance criterion asserts exactly one deleted line in the diff. |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing and modifies no manifest. It runs `cargo test`, `cargo fmt`, `cargo clippy` and `cargo metadata` against already-resolved dependencies; `git diff -- Cargo.toml Cargo.lock` is empty for this plan. `10-RESEARCH.md`'s Package Legitimacy Audit records the phase as not-applicable, and the phase's only manifest touch removed a declaration. |
</threat_model>

<verification>
- `grep -cE '^- \[x\] \*\*HARD-0[1-7]\*\*' .planning/REQUIREMENTS.md` → `7`; each flip carries a dated note with a command or `file:line`.
- Four `#### Hand-off to Phase 1…` blocks exist, each naming a receiving requirement ID and an artefact.
- `grep -c 'Next free ADR number: 0034' .planning/decisions/PROMOTION.md` → `1`; six new index rows; exactly one deleted line in the diff.
- Ledger: 86 rows, 86 distinct IDs, 12 sections, 0 pending stubs, 0 blank Verdict cells, 0 dangling ADR citations, exactly one appended close-out section and 0 deleted lines in this task's diff.
- `git diff --name-only <phase base>..HEAD -- '*.rs' | wc -l` → `0`, recorded with its command.
- `cargo test`, `cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo metadata --no-deps` and `make -n release-check` all ran, with results recorded verbatim.
- The three Manual-Only rows each carry an explicit disposition; the coverage floor is reasoning, not measurement.
</verification>

<success_criteria>
A developer opening `.planning/` finds seven requirements marked complete, each with the artefact and
the command that closed it; a ledger whose 86 rows all cite something that resolves; six ADRs indexed
and numbered without a gap; four hand-off blocks that tell Phases 11, 12 and 13 exactly what they
inherit and what not to re-derive; and a close-out that says plainly which of its claims were measured
here and which are CI-only — including the one gate that is still red and the phase that owns it.
</success_criteria>

<output>
Create `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-11-SUMMARY.md` when done.
Record the whole-ledger integrity numbers, the zero-`.rs`-change command and its output, the three
workspace-gate results verbatim, and the disposition of each Manual-Only row.
</output>
