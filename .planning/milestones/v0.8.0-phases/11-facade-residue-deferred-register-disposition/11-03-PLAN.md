---
phase: 11-facade-residue-deferred-register-disposition
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - .planning/registers/facade-03-removed-features.md
  - .planning/decisions/0035-paladin-ml-leaf-crate-placement.md
  - .project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-features.md
autonomous: true
requirements: [FACADE-03]

must_haves:
  truths:
    - "Someone asking 'why can I not run `paladin user register`?' finds the answer in `.planning/` without opening `.project/`: 1,065 LOC declared but never dispatched, the backend intact, reintroduction is re-wiring, and the module is recoverable verbatim from a named commit — rather than concluding it was lost (ROADMAP criterion 3)"
    - "The recovery pointer is the immutable commit SHA `3d48768`, given as the runnable form `git show 3d48768^:src/application/cli/commands/user.rs`, not a branch name (D-10)"
    - "The record states that one commit removed both features, correcting the corpus's split attribution which credits the ML removal to `3d48768` and the CLI removal to a branch (D-10)"
    - "The `paladin-ml` placement condition survives outside a single DOC: it is recorded verbatim in a promoted `.planning/decisions/` ADR — any future TensorFlow adapter goes into a dedicated `paladin-ml` leaf crate with the `ml` flag on that crate, never back into the facade, and `paladin_ports::input::ml_port::MlPort` stays in the workspace so the integration point does not move (ROADMAP criterion 4, D-09)"
    - "`paladin-herald`'s existence is recorded as no licence to create `paladin-ml`: the M8 Epic 3 §5 non-goal split is asymmetric — overridden for one crate, still holding for the other (D-00k)"
    - "FACADE-03 closes on recorded deferral with conditions intact; neither feature is reintroduced and no crate is created (D-09, PROJECT.md `### Out of Scope`)"
    - "The record does not imply the CLI surface can be re-wired without a security review — the eight subcommands are registration, login and account-lifecycle operations"
    - "No Rust source file is modified by this plan (D-13)"
  artifacts:
    - path: ".planning/registers/facade-03-removed-features.md"
      provides: "The `.planning`-native record of both removed features: status, why removed, what survives, reintroduction condition, and the SHA recovery pointer"
      contains: "3d48768"
    - path: ".planning/decisions/0035-paladin-ml-leaf-crate-placement.md"
      provides: "The contested `paladin-ml` leaf-crate placement condition, recorded verbatim and promoted out of DOC precedence"
      contains: "## Downstream Consumers"
    - path: ".project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-features.md"
      provides: "A dated pointer to the `.planning/` register and the branch-versus-commit correction, original text retained"
      contains: "2026-08-08"
  key_links:
    - from: ".planning/registers/facade-03-removed-features.md"
      to: ".planning/decisions/0035-paladin-ml-leaf-crate-placement.md"
      via: "the register's ML entry cites the ADR as the durable home of the placement condition rather than restating it as its own authority"
      pattern: "0035-paladin-ml-leaf-crate-placement"
    - from: ".planning/decisions/0035-paladin-ml-leaf-crate-placement.md"
      to: "crates/paladin-ports/src/input/ml_port.rs"
      via: "Code Locations citing the stable workspace home of MlPort, the integration point the condition says must not move"
      pattern: "ml_port"
  prohibitions:
    - "A correction must not delete or rewrite the text it corrects. `deferred-features.md`'s original write-ups — the intended command surface table, the backend-intact list, the reintroduction conditions and the branch reference at `:72` — must all still be readable after this plan. The branch reference is the evidence for the attribution defect being corrected; erasing it removes the reason the correction exists."
    - "The record of the `paladin user …` reintroduction path must not imply it is a pure re-wiring exercise with no security dimension. The eight subcommands are registration, login and account lifecycle; presenting reintroduction as mechanical, while technically true of the wiring, would let a future phase restore an auth-adjacent surface without review. (The review itself is canon and belongs to `/gsd-secure-phase`; what is minted here is only the record's obligation not to understate it.)"
---

<objective>
Record both deliberately removed features — the `paladin user …` CLI surface and the TensorFlow ML
adapter — in `.planning/`, with their reintroduction conditions intact and an immutable recovery
pointer, and promote the contested `paladin-ml` leaf-crate placement condition into an ADR.

Purpose: today the answer to "why can I not run `paladin user register`?" lives only in a `.project/`
DOC, which sits second-from-bottom in the precedence order and is auto-overridable by the next
document that mentions it. The ML placement condition is worse off: it is the surviving half of an
M8 Epic 3 non-goal that `paladin-herald` already overrode, it is carried only by a DOC, and
PROJECT.md lists `paladin-ml` under *Out of Scope*. Neither survives where it is.

Output: one `.planning/` register covering both features, ADR-0035 holding the placement condition,
and a dated pointer plus attribution correction on the source DOC.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-facade-residue-deferred-register-disposition/11-CONTEXT.md
@.planning/phases/11-facade-residue-deferred-register-disposition/11-PATTERNS.md
@.planning/phases/11-facade-residue-deferred-register-disposition/11-RESEARCH.md
</context>

## Artifacts this phase produces

| Artifact | Plan | Kind |
|---|---|---|
| `.planning/registers/facade-01-rustdoc-stdout-disposition.md` | 11-01 | new file |
| `.planning/registers/facade-03-removed-features.md` | **11-03** | new file |
| `.planning/registers/facade-04-m9-candidate-triage.md` | 11-04 | new file |
| `.planning/decisions/0034-d1-d4-facade-relocation-disposition.md` | 11-02 | new ADR |
| `.planning/decisions/0035-paladin-ml-leaf-crate-placement.md` | **11-03** | new ADR |
| `.planning/decisions/PROMOTION.md` | 11-05 | modified — 2 index rows, 1 dated note, next free → 0036 |
| `.planning/ledgers/milestone-07-08.md` | 11-01 (D5 half of one row), 11-05 (all remaining rows) | modified |
| `.planning/ROADMAP.md` | 11-01 (§Phase 11 criterion 1) | modified |
| `.planning/REQUIREMENTS.md` | 11-05 (FACADE-01 and FACADE-03(a) source corrections) | modified |
| `.planning/PROJECT.md` | 11-05 (2 `## Key Decisions` rows) | modified |
| `.project/…/deferred-items.md` | 11-01 (D5 clause), 11-02 (D1–D4 clauses) | annotated |
| `.project/…/deferred-features.md` | **11-03** | annotated |

New headings and rows added to existing records: two `PROMOTION.md` Numbering-index rows (0034, 0035)
plus one dated note plus the `Next free ADR number` line; two `PROJECT.md` `## Key Decisions` rows;
dated amendments to five named ledger rows; a dated amendment to ROADMAP §Phase 11 criterion 1;
source corrections inside REQUIREMENTS.md's FACADE-01 and FACADE-03 blocks.

**No `.rs` file appears on this list, and none may be added to it (D-13).**

## Flagged assumptions carried into this plan

- **FACADE-03's edge probe returned `unclassified`, and it stays unresolved rather than being
  auto-resolved.** The eight-category edge taxonomy found no data-shape axis in FACADE-03's text,
  because the requirement is a recording obligation rather than a computation. The real ambiguity it
  masks is this: FACADE-03's own "Done when" offers two closing branches — *recorded as deliberate
  deferrals with conditions intact* **or** *promoted to scope with a phase* — and either closes it.
  **This plan takes the recorded-deferral branch, per D-09.** Promotion to scope is explicitly not
  chosen and would be a phase of its own. Surfaced here rather than silently assumed.
- **CONTEXT.md's and RESEARCH.md's claim that the branch `chore/facade-cleanup-m8-finish` is "not
  present as a local or remote ref" is measurably wrong today.** `git rev-parse --verify
  refs/remotes/origin/chore/facade-cleanup-m8-finish` resolves (to `4bf6745` at planning time) and
  `git merge-base --is-ancestor 3d48768 refs/remotes/origin/chore/facade-cleanup-m8-finish` succeeds.
  No *local* branch of that name exists, which is the half that holds. D-10's decision — use the SHA,
  not the branch — is unaffected and is if anything better founded, but its stated *reason* must not
  be propagated as written. Task 1 re-measures and records the true state, and justifies the SHA on
  the grounds that actually hold.
- **The ADR number is 0035 only under option-a** of plan 11-01's decision checkpoint. If option-b or
  option-c was selected, re-read `PROMOTION.md`'s `Next free ADR number` line, take the number it
  gives, and record in the SUMMARY which number was actually taken.
- **This plan's `one-way` reversibility gate is satisfied across plans, by design.** `verify
  plan-structure` warns that Task 2 carries `rating="one-way"` with no `checkpoint:decision` earlier
  in *this* file. That is intentional: the whole phase's ADR allocation is one decision, gated once at
  the end of plan 11-01 in wave 1, which this plan depends on. A second gate here would re-ask a
  settled question and, because 11-02 and 11-03 run in parallel, could not stop the sibling plan from
  taking a number first. The rating stays `one-way` because the door is real; only the gate's location
  moved.

<tasks>

<task type="auto">
  <name>Task 1: Write the `.planning/` register for both removed features</name>
  <read_first>
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-features.md` in full — both write-ups, the intended-command-surface table with its eight subcommands and their arguments, the backend-intact symbol list, the reintroduction conditions for each feature, and the branch reference at `:72`.
    - `.planning/REQUIREMENTS.md` lines 1668-1694 — FACADE-03's full text, including the recovery sentence that attributes the CLI removal to a branch and the ML removal to `3d48768`.
    - `.planning/REQUIREMENTS.md` lines 1594-1614 — the Phase 10 hand-off to FACADE-03(b), which is where the asymmetric non-goal split is recorded.
    - `.planning/ledgers/milestone-07-08.md` — the `REQ-deferred-cli-user-commands` and `REQ-deferred-tensorflow-ml-adapter-v3` rows (around lines 346-347), for the ledger's own verdict vocabulary and the `deferred with register` verdict word these two rows already carry.
    - `.planning/phases/11-facade-residue-deferred-register-disposition/11-PATTERNS.md` → the "Recommended shape for FACADE-03" paragraph and the "Evidence bar (D-00e)" shared pattern.
    - `.planning/phases/11-facade-residue-deferred-register-disposition/11-CONTEXT.md` → D-00e, D-00k, D-09, D-10, D-13.
    - `.planning/PROJECT.md` around line 570 — the `### Out of Scope` entry naming `paladin-arsenal`, `paladin-sanctum` and `paladin-ml`, which binds this record.
  </read_first>
  <files>.planning/registers/facade-03-removed-features.md</files>
  <action>
Re-measure before writing, and record each command and its output verbatim per D-00e:
`git log -1 --format="%H %ai %s" 3d48768`; `git show --stat 3d48768` (to confirm both deleted paths
and their LOC in one commit); `ls src/application/cli/commands/` (expect ten modules, no `user.rs`);
`test -d crates/paladin-ml; echo $?` (expect `1`);
`find crates/paladin-ports -iname "*ml_port*"` (expect `crates/paladin-ports/src/input/ml_port.rs`);
`git branch --list '*facade-cleanup-m8-finish*' | wc -l` and
`git rev-parse --verify refs/remotes/origin/chore/facade-cleanup-m8-finish`.

Create `.planning/registers/facade-03-removed-features.md` (the `.planning/registers/` directory
already exists from plan 11-01). Dated header naming FACADE-03 as its requirement and
`REQ-deferred-cli-user-commands` / `REQ-deferred-tensorflow-ml-adapter-v3` as its ledger keys per
D-00f. State in the header that this file is the `.planning`-native home for both records, so the
question can be answered without opening `.project/`.

**Section 1 — the `paladin user …` CLI surface.** Give it these labelled parts: *what it was*
(`src/application/cli/commands/user.rs`, 1,065 LOC, eight clap subcommands — `register`, `login`,
`get`, `update`, `list`, `activate`, `deactivate`, `verify`); *why it was removed* (declared but
never dispatched — no `UserCommands` arm existed in the binary's top-level match, so it compiled and
did nothing); *what survives* (the backend is intact and in use elsewhere:
`core::platform::manager::user_service::{UserService, UserServiceTrait, UserRegistrationRequest,
UserLoginRequest, UserProfileUpdateRequest}` and `core::platform::container::user::{User,
UserProfile}`, with the `config/user_config.rs` DI wiring); *what reintroduction costs* (adding the
enum arm plus a dispatch handler against the existing DI wiring, plus tests — re-wiring, not new
domain work); and *the recovery pointer*, given as the runnable command
`git show 3d48768^:src/application/cli/commands/user.rs`.

State the recovery-pointer rationale on the grounds that actually hold, not on a branch-absence claim:
the SHA is immutable while a branch ref is mutable and deletable, and one commit removed both
features, so the SHA is a single pointer for both records. Record the measured branch state honestly
alongside it — whatever the two branch commands returned this session — and note that at planning
time no *local* branch of that name existed while the remote-tracking ref did, which is a correction
to the framing carried in `deferred-features.md:72` and in `11-CONTEXT.md`'s D-10.

Add one sentence, no more, recording that the eight subcommands are registration, login and
account-lifecycle operations, so a future reintroduction carries a security dimension beyond the
wiring and should not be scheduled as a purely mechanical restore. Do not attempt the review here.

**Section 2 — the TensorFlow ML adapter.** Same labelled parts: *what it was*
(`src/infrastructure/adapters/input/tensorflow_adapter.rs`, 636 LOC, a `#[doc(hidden)]` placeholder
implementing `MlPort` with stub model loading and prediction, plus the `ml = []` feature flag);
*why it was removed* (both deleted outright by commit `3d48768`, rather than feature-gated as the
Epic 3 disposition record's own action cell described); *what survives*
(`paladin_ports::input::ml_port::MlPort` at `crates/paladin-ports/src/input/ml_port.rs`, confirmed
present — the integration point is stable); and *the reintroduction condition*, reproduced verbatim:
any future TensorFlow adapter goes into a dedicated `paladin-ml` **leaf crate** with the `ml` flag on
that crate, **never back into the facade**, and `paladin_ports::input::ml_port::MlPort` **stays in
the workspace** so the integration point does not move.

Cite `.planning/decisions/0035-paladin-ml-leaf-crate-placement.md` as the durable, promoted home of
that condition — this register carries it for findability, the ADR carries it for authority. Record
the asymmetric non-goal split as a stated fact: the M8 Epic 3 §5 non-goal naming both
`paladin-herald` and `paladin-ml` is **overridden for `paladin-herald`** (present, created by
reconciliation commit `66f6c4e`) and **still holding for `paladin-ml`** (absent, `test -d` exits 1),
and `paladin-herald`'s existence is not licence to create `paladin-ml`. Cite PROJECT.md's
`### Out of Scope` entry as binding.

Close with a `## Disposition` line stating that FACADE-03 closes on **recorded deferral with
conditions intact** for both features — neither is reintroduced, no crate is created, and promotion
to scope would be a phase of its own.
  </action>
  <verify>
    <automated>R=.planning/registers/facade-03-removed-features.md; test -f "$R" || { echo "register missing"; exit 1; }; for s in '3d48768' 'git show 3d48768^:src/application/cli/commands/user.rs' 'ml_port' 'paladin-ml' 'leaf crate' '1,065' '636'; do grep -qF "$s" "$R" || { echo "MISSING $s"; exit 1; }; done; echo "register content OK"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format="%H %ai %s" 3d48768` resolves and its output appears verbatim in the register; `git show --stat 3d48768` confirms both `src/application/cli/commands/user.rs` and `src/infrastructure/adapters/input/tensorflow_adapter.rs` were deleted in that one commit, and the register states that one commit did both.
    - `grep -qF 'git show 3d48768^:src/application/cli/commands/user.rs' .planning/registers/facade-03-removed-features.md` succeeds — the recovery pointer is given in runnable form.
    - The register names all eight subcommands: `for s in register login get update list activate deactivate verify; do grep -qw "$s" .planning/registers/facade-03-removed-features.md || exit 1; done` succeeds.
    - `ls src/application/cli/commands/ | wc -l` returns `10` and `ls src/application/cli/commands/ | grep -c '^user.rs$'` returns `0`; both figures appear in the register.
    - `test -d crates/paladin-ml; echo $?` returns `1` and `find crates/paladin-ports -iname "*ml_port*"` returns `crates/paladin-ports/src/input/ml_port.rs`; both appear in the register with the commands that produced them.
    - The register records the measured branch state from `git branch --list '*facade-cleanup-m8-finish*' | wc -l` and `git rev-parse --verify refs/remotes/origin/chore/facade-cleanup-m8-finish`, and does **not** assert that no remote ref exists unless that command actually failed at execution time.
    - `grep -c '0035-paladin-ml-leaf-crate-placement' .planning/registers/facade-03-removed-features.md` is non-zero.
    - `grep -Eic 'security' .planning/registers/facade-03-removed-features.md` is non-zero — the auth-adjacent note is present.
    - `grep -c 'paladin-herald' .planning/registers/facade-03-removed-features.md` is non-zero and the asymmetric split is stated in both directions.
    - `git diff --name-only HEAD~1 HEAD | grep -c 'rs$'` returns `0`.
  </acceptance_criteria>
  <done>`.planning/registers/facade-03-removed-features.md` answers "why can I not run `paladin user register`?" and "what would returning ML support require?" from `.planning/` alone, with an immutable SHA recovery pointer, the honest measured branch state, the verbatim ML placement condition, and the asymmetric non-goal split recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Write ADR-0035 — the `paladin-ml` leaf-crate placement condition</name>
  <reversibility rating="one-way">An ADR number is never reused (`PROMOTION.md:141`) and an ADR tops the precedence order (D-00b), so undoing this means authoring a superseding ADR rather than deleting a file. The allocation was confirmed by plan 11-01's checkpoint.</reversibility>
  <read_first>
    - `.planning/decisions/0018-m6-facade-reexport-policy.md` in full — the posture-ADR shape this one follows: a "this stays out, this condition governs re-entry" ruling rather than an instruction to change code.
    - `.planning/decisions/0028-m8-reconciliation-authoritative.md` — its `## Decision` sub-clause recording the Epic 3 §5 non-goal split, and its `## Downstream Consumers` entry naming FACADE-03(b), which this ADR inherits.
    - `.planning/decisions/PROMOTION.md` lines 57 and 95-128 — **the source of D-00a**, the ADR-shape convention this task is bound by. D-00a: ADRs live in `.planning/decisions/` under flat sequential numbering, with the file shape `Status / Context / Decision / Considered Options / Code Locations / Code Conformance / Downstream Consumers` and **no frontmatter**; line 57 is the `Next free ADR number` record from which 0035 is allocated. Read lines 95-128 for the required heading set in order, the bullet-list requirement on `## Considered Options` and `## Code Locations`, and the supersession mechanism.
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-features.md` §2 — the placement condition's original wording, which must be reproduced verbatim rather than paraphrased.
    - `.planning/PROJECT.md` around line 570 and line 800 — the `### Out of Scope` entry and the existing statement that a future adapter must live in a dedicated leaf crate.
    - `.planning/phases/11-facade-residue-deferred-register-disposition/11-PATTERNS.md` → the `003N-paladin-ml-placement.md` section, which names the analog and the two mandatory `## Code Locations` citations.
    - `.planning/phases/11-facade-residue-deferred-register-disposition/11-CONTEXT.md` → D-00g, D-00k, D-09, D-13, D-14.
  </read_first>
  <files>.planning/decisions/0035-paladin-ml-leaf-crate-placement.md</files>
  <action>
Write `.planning/decisions/0035-paladin-ml-leaf-crate-placement.md` in D-00a's file shape — no YAML
frontmatter, and exactly these seven H2 headings in this order: `## Status`, `## Context`,
`## Decision`, `## Considered Options`, `## Code Locations`, `## Code Conformance`,
`## Downstream Consumers`.
Title line: `# ADR-0035: `paladin-ml` leaf-crate placement condition for a future ML adapter`.
`## Status` body is `Accepted` followed by a bold `**Date:** 2026-08-08` line.

`## Context` states why this is a contested position and therefore ADR material under D-00g, in three
measured facts: it is the surviving half of an M8 Epic 3 §5 non-goal that `paladin-herald` already
overrode inside the same milestone; it is carried today **only by a DOC**
(`deferred-features.md` §2), which sits second-from-bottom in the D-00b precedence order and is
auto-overridable by the next document that mentions it; and PROJECT.md lists `paladin-ml` under
`### Out of Scope`, so a reader could reasonably conclude the condition is moot when it is not. State
what the tree shows: `test -d crates/paladin-ml` exits `1`, `crates/paladin-ports/src/input/ml_port.rs`
exists, and `grep -rn "tensorflow\|^ml = " Cargo.toml src/` returns nothing — the adapter and its
feature flag were deleted outright by commit `3d48768`, not gated.

`## Decision` reproduces the condition **verbatim** as a quotable sentence, following ADR-0031's
single-quotable-sentence convention: *any future TensorFlow adapter goes into a dedicated `paladin-ml`
**leaf crate** with the `ml` flag on that crate, **never back into the facade**, and
`paladin_ports::input::ml_port::MlPort` **stays in the workspace** so the integration point does not
move.* Then state the two clauses this ADR adds around it, numbered:

  **(i) The non-goal split is asymmetric and stays that way.** Overridden for `paladin-herald`
  (created by reconciliation commit `66f6c4e`), still holding for `paladin-ml` (absent). The
  existence of the first is not licence to create the second; each half is independent.

  **(ii) This ADR creates no crate and authorises none.** It records the condition that governs
  re-entry, not a decision to re-enter. `paladin-ml` remains out of scope per PROJECT.md;
  reintroduction would need its own phase and its own decision.

`## Considered Options` is a bullet list, each tagged `(accepted)` or `(rejected)` with a one-clause
reason. Include at minimum: promote the condition into an ADR and leave the crate out of scope
(accepted); record it in a register only (rejected — a register does not outrank the PRD that lists
`paladin-ml` as out of scope, and the condition's whole problem is that it sits at DOC precedence);
promote `paladin-ml` to scope and create the crate (rejected — PROJECT.md lists it out of scope,
D-09 chooses recorded deferral, and there is no adapter to put in it); treat `paladin-herald`'s
creation as having voided the whole non-goal (rejected — D-00k, the split is asymmetric and each half
stands alone).

`## Code Locations` is a bullet list of exact citations covering, at minimum:
`crates/paladin-ports/src/input/ml_port.rs` (the integration point the condition says must not move);
`.planning/PROJECT.md`'s `### Out of Scope` line naming `paladin-ml`;
`.project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-features.md` §2 (the source document
being promoted, per `PROMOTION.md` step 4); the Epic 3 §5 non-goal clause; commit `3d48768`; and the
three re-run commands with their verbatim output.

`## Code Conformance` is the standalone word `conforms` plus one justifying line: the tree already
satisfies the condition — no ML adapter exists anywhere, `MlPort` is already in the workspace, and
the facade carries no `ml` feature — so this ADR instructs no code change (D-13).

`## Downstream Consumers` is a bullet list naming, at minimum: **any future phase that reintroduces
ML support** — inherits the placement condition as binding and must not re-add to the facade;
**`.planning/registers/facade-03-removed-features.md`** — cites this ADR as the condition's
authoritative home; and **plan 11-05** — amends the `REQ-deferred-tensorflow-ml-adapter-v3` ledger row
against this ADR and adds its `PROJECT.md` `## Key Decisions` row.
  </action>
  <verify>
    <automated>A=.planning/decisions/0035-paladin-ml-leaf-crate-placement.md; test -f "$A" || { echo "ADR missing"; exit 1; }; diff <(grep '^## ' "$A") <(printf '## Status\n## Context\n## Decision\n## Considered Options\n## Code Locations\n## Code Conformance\n## Downstream Consumers\n') && grep -qF 'crates/paladin-ports/src/input/ml_port.rs' "$A" && echo "headings + ml_port citation OK"</automated>
  </verify>
  <acceptance_criteria>
    - D-00a: the ADR carries the seven fixed headings `Status / Context / Decision / Considered Options / Code Locations / Code Conformance / Downstream Consumers` in that order and **no frontmatter**. Proven by the `<verify>` diff returning clean against `PROMOTION.md`'s heading set, and by `head -1` of the file being the `# ADR-0035:` title line rather than `---`.
    - `awk '/^## Considered Options/,/^## Code Locations/' "$A" | grep -c '^- '` returns at least `4` and `awk '/^## Code Locations/,/^## Code Conformance/' "$A" | grep -c '^- '` returns at least `5` — both sections are bulleted, as `adr-parser.cjs` requires.
    - The placement condition appears with all four of its load-bearing terms: `grep -qF 'leaf crate' "$A"`, `grep -qF 'never back into the facade' "$A"`, `grep -qF 'MlPort' "$A"` and `grep -qF 'stays in the workspace' "$A"` each succeed.
    - `grep -c 'paladin-herald' "$A"` is non-zero and the asymmetry is stated in both directions — overridden for one crate, still holding for the other.
    - `grep -qF 'deferred-features.md' "$A"` succeeds — the promoted source document is cited in `## Code Locations`, per `PROMOTION.md` step 4.
    - `grep -c '^conforms' "$A"` returns `1`.
    - `test -d crates/paladin-ml; echo $?` returns `1` at execution time and that result is recorded in `## Code Locations`; the ADR creates no directory under `crates/`.
    - `ls crates/ | wc -l` returns the same value before and after this task — no crate was created.
    - `git diff --name-only HEAD~1 HEAD | grep -c 'rs$'` returns `0`.
  </acceptance_criteria>
  <done>ADR-0035 exists with the seven required headings in order, reproduces the placement condition verbatim as a quotable sentence, records the asymmetric non-goal split, cites `ml_port.rs` and the source DOC, carries a `conforms` verdict, and creates no crate.</done>
</task>

<task type="auto">
  <name>Task 3: Annotate `deferred-features.md` with the `.planning/` pointer and the attribution correction</name>
  <read_first>
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-features.md` in full **as it stands now** — confirm whether it already carries any banner (at planning time it did not; it opens directly with its `# Deferred Features …` title and a `**Date:** 2026-06-04` block), and locate the branch reference at `:72` precisely.
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md` lines 1-12 — the live Shape-B blockquote banner Phase 10 wrote, as the visual convention to match.
    - `.planning/registers/facade-03-removed-features.md` and `.planning/decisions/0035-paladin-ml-leaf-crate-placement.md` (just written) — the paths this annotation points at.
    - `.planning/phases/11-facade-residue-deferred-register-disposition/11-CONTEXT.md` → D-00c, D-10.
  </read_first>
  <files>.project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-features.md</files>
  <action>
Add one dated 2026-08-08 Shape-B blockquote banner immediately beneath the document's `# Deferred
Features …` title, matching the visual convention of the banner already live on
`infrastructure-adapter-disposition.md:1-12`. Do not add a second banner if one already exists —
extend the existing one instead. The banner does two things and no more:

1. **Points at the `.planning/` homes** — states that both features' records now live at
   `.planning/registers/facade-03-removed-features.md`, and that the `paladin-ml` placement condition
   is promoted to `.planning/decisions/0035-paladin-ml-leaf-crate-placement.md`, so the answer is
   findable without reading this document. Name FACADE-03 as the requirement that moved them.
2. **Corrects the recovery-pointer framing** — states that the recoverable form is the immutable
   commit `3d48768` and the runnable command `git show 3d48768^:src/application/cli/commands/user.rs`,
   and that **one commit removed both features**, so attributing the CLI removal to a branch and the
   ML removal to a commit splits a single event across two pointer kinds. Record the branch state
   actually measured in Task 1 rather than asserting an absence; a branch ref is mutable and
   deletable, which is the durable reason to cite the SHA, and that reason holds regardless of
   whether the ref happens to resolve today.

End the banner with the sentence `The original text below is retained unmodified.` and change nothing
below it — the intended-command-surface table, the backend-intact symbol list, both reintroduction
conditions and the branch reference at `:72` all stay exactly as they are. The branch reference in
particular is the evidence for the correction and must not be edited away.
  </action>
  <verify>
    <automated>D=.project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-features.md; grep -qF 'facade-cleanup-m8-finish' "$D" || { echo "original branch reference lost"; exit 1; }; grep -qF 'facade-03-removed-features' "$D" || { echo "planning pointer missing"; exit 1; }; grep -qF '3d48768' "$D" || { echo "SHA pointer missing"; exit 1; }; grep -c '^> \*\*' "$D" | grep -qv '^0$' && echo "banner present"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF 'facade-cleanup-m8-finish' .project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-features.md` succeeds — the original branch reference at `:72` is retained, not deleted.
    - `grep -c 'The original text below is retained unmodified' .project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-features.md` returns `1`.
    - `grep -c 'facade-03-removed-features' .project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-features.md` and `grep -c '0035-paladin-ml-leaf-crate-placement' …` are each non-zero — both `.planning/` homes are named.
    - `grep -c '2026-08-08' .project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-features.md` is non-zero and `grep -c 'FACADE-03' …` is non-zero.
    - Exactly one banner exists: `grep -c 'The original text below is retained unmodified' …` returns `1`, not `2` — no duplicate blockquote was stacked.
    - `git diff HEAD~1 HEAD -- .project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-features.md --numstat` shows deletions of `0` — the annotation is purely additive, so no original line was removed or rewritten.
    - `git diff --name-only HEAD~1 HEAD` lists exactly the one `.project/` file, and `| grep -c 'rs$'` returns `0`.
  </acceptance_criteria>
  <done>`deferred-features.md` carries exactly one dated banner pointing at both `.planning/` homes and correcting the branch-versus-commit attribution, with every original line — including the branch reference — retained unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.project/` DOC (lowest-but-one precedence) → `.planning/decisions/` ADR (highest) | The `paladin-ml` placement condition is promoted out of a class of document that the next PRD can silently override |
| git history → a durable recovery instruction | A commit reference recorded now must still resolve to the same 1,065-LOC module when someone acts on it years later |
| this record → a future auth-adjacent reintroduction | The record shapes how the next phase scopes restoring registration, login and account-lifecycle commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-14 | Spoofing | The recovery pointer for `user.rs` | high | mitigate | A pointer that does not resolve makes a 1,065-LOC module unrecoverable in practice, and the corpus currently points at a mutable branch name. Mitigated by recording the immutable SHA `3d48768` in runnable `git show` form, re-verified at execution time, with the true measured branch state recorded rather than the falsified absence claim CONTEXT.md carries. |
| T-11-15 | Tampering | `deferred-features.md` | medium | mitigate | Annotation only (D-00c). The acceptance criteria assert `--numstat` deletions of `0`, so the annotation is provably additive and the original branch reference — the evidence for the correction — survives. |
| T-11-16 | Repudiation | Both feature records | medium | mitigate | D-00e's evidence bar: every claim carries the command that produced it — `git log -1`, `git show --stat`, `ls`, `test -d`, `find` — recorded verbatim, so a later reader can re-run and challenge any of them. |
| T-11-17 | Elevation of Privilege | ADR-0035 versus PROJECT.md's `### Out of Scope` | medium | mitigate | An ADR tops the precedence order, so a carelessly worded one could read as authorising `paladin-ml`. Sub-decision (ii) states explicitly that the ADR creates no crate and authorises none, and the acceptance criteria assert `ls crates/ \| wc -l` is unchanged and `test -d crates/paladin-ml` still exits `1`. |
| T-11-18 | Information Disclosure | The recorded CLI surface | low | mitigate | The eight subcommand names and their argument lists are already public in the checkout and in `deferred-features.md`; nothing new is exposed. What the record does add is one line noting the surface is auth-adjacent, so a future reintroduction is not scoped as a purely mechanical restore. The security review itself is canon and belongs to `/gsd-secure-phase`, not to this record. |
| T-11-19 | Denial of Service | — | low | accept | Not applicable. No runtime surface is touched; execution is file reads, six git/shell queries and three writes. |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable, truthfully: this plan installs, upgrades and removes nothing, and creates no crate. `Cargo.toml`, `Cargo.lock` and every crate manifest are outside its `<files>`; the `ls crates/` and `git diff --name-only` criteria prove it. |
</threat_model>

<verification>
- `git log -1 3d48768` resolves and `git show --stat 3d48768` shows both deletions in one commit.
- The register names all eight subcommands, both LOC figures, and the runnable recovery command.
- ADR-0035 carries the seven required headings in order with bulleted Options and Locations, and reproduces the placement condition verbatim.
- `test -d crates/paladin-ml` still exits `1` and `ls crates/` is unchanged after this plan.
- `deferred-features.md`'s diff has zero deletions and exactly one banner.
- No commit in this plan touches a Rust source file.
</verification>

<success_criteria>
- ROADMAP criterion 3 is met: the `paladin user register` answer is reachable from `.planning/` alone, and it reads as a deliberate deferral with an intact recovery path rather than a loss.
- ROADMAP criterion 4 is met: the ML placement condition lives in a promoted decision, not in a single DOC.
- Neither feature is reintroduced and no crate is created.
- The corpus's branch-versus-commit attribution defect is corrected on evidence measured this session, not on an inherited claim.
</success_criteria>

<output>
Create `.planning/phases/11-facade-residue-deferred-register-disposition/11-03-SUMMARY.md` when done.
</output>
