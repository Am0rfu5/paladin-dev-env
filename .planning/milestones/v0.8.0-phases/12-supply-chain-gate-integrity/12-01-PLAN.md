---
phase: 12-supply-chain-gate-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: false
requirements: [SUPPLY-01, SUPPLY-02]

must_haves:
  truths:
    - "SUPPLY-01 and SUPPLY-02 each carry a dated closure record whose evidence is the literal command and its literal output, captured by re-running the command in this execution — not a citation to 12-CONTEXT.md or 12-RESEARCH.md, which are context files and therefore not evidence (D-00e, D-06)"
    - "`cargo audit`, `cargo deny check` and `./scripts/check-advisory-register.sh` are each re-run and each exits 0, and all three transcripts are recorded verbatim in REQUIREMENTS.md"
    - "Exactly one CI job carries the display name `Security Audit` and exactly one `cargo audit` invocation exists across `.github/workflows/` — the adjacency defect SUPPLY-01 existed to close: two jobs sharing one display name collide in the required-status-check namespace rather than resolving separately"
    - "`./scripts/check-advisory-register.sh` compares identifiers case-sensitively over sets, so a register row differing from its TOML entry only in letter case fails rather than silently matching, and reordering rows or TOML entries does not change the verdict (`check-advisory-register.sh:11-15`)"
    - "The register check's one-sided-empty case is a distinct named failure — zero register rows while either TOML holds a non-empty ignore array fails; zero rows with two empty arrays is a legitimate pass (`check-advisory-register.sh:31-33`) — and the live run reports 10 rows against 10 `deny.toml` and 5 `.cargo/audit.toml` entries"
    - "Running `./scripts/check-advisory-register.sh` twice in succession with no input change produces identical output and the same exit code (`check-advisory-register.sh:44-45`), demonstrated by two consecutive recorded runs rather than asserted"
    - "The register check only reads — it writes nothing and creates no temporary file (`check-advisory-register.sh:43`) — so a concurrent or interrupted invocation cannot corrupt any input it reads"
    - "SUPPLY-01's CI-run-observation clause is recorded as `pending` with the named trigger `next push to release/v0.7.0` and the run-ID boundary `30861568499` (2026-08-03T23:14:24Z, five days before the 2026-08-08 deletion) — never as closed, and never simulated or inferred (D-07)"
    - "The two documents claiming `cargo audit`/`cargo deny` are not installable here carry a dated blocker-lifted banner with their original text retained, and SUPPLY-01's separate CI-run clause is left unbannered because its substance is still correct (D-06 vs D-07)"
    - "The committed-but-unapplied GitHub rulesets are recorded as a finding with its owner named for the milestone close-out, citing the two `gh api` commands and their output — and nothing is applied to the live repository (D-10)"
    - "Every live, uncorrected `ci.yml:389-406` citation in the current canonical governance documents carries a dated correction naming the true location `ci.yml:465-482` and the Phase 9 commit `cb75b2b` that deleted it, with the original citation retained"
    - statement: "With one `Security Audit` job remaining, `.github/rulesets/protect-main-branch.json`'s required context string matches exactly one job and the required status check resolves — unverifiable locally because the committed rulesets are not applied to the live repository (D-10) and no CI run postdates the Phase 9 deletion (D-07)"
      verification: backstop
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Dated closure records for SUPPLY-01 and SUPPLY-02 carrying three verbatim gate transcripts, the D-07 pending record, the D-10 rulesets finding, two checkbox flips and two traceability-row flips"
      contains: "plan 12-01"
    - path: ".planning/ROADMAP.md"
      provides: "A dated blocker-lifted banner on the Phase 12 closure note's `neither tool is installable` clause, original retained"
      contains: "plan 12-01"
    - path: ".planning/PROJECT.md"
      provides: "Dated corrections on the two live stale `ci.yml:389-406` citations"
      contains: "465-482"
    - path: ".planning/STATE.md"
      provides: "Dated corrections on the two live stale `ci.yml:389-406` citations"
      contains: "465-482"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "scripts/check-advisory-register.sh"
      via: "SUPPLY-02's closure record quotes the script's exit-0 transcript verbatim as its evidence"
      pattern: "register row\\(s\\) checked against"
    - from: ".planning/REQUIREMENTS.md"
      to: ".github/workflows/ci.yml"
      via: "SUPPLY-01's closure record cites the surviving `security-audit:` job and the re-run structural greps that prove one invocation and one display name"
      pattern: "Security Audit"
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the ROADMAP closure note's blocker-lifted banner and REQUIREMENTS.md's SUPPLY-02 banner state the same lifted blocker in the same terms and on the same date"
      pattern: "2026-08-09"
  prohibitions:
    - "Do not add, remove, re-date or re-justify any advisory suppression. ADR-0024 owns the exception set, its eleven-field schema, owner `DF3NDR` and the per-advisory `2026-12-31` review dates. `deny.toml`, `.cargo/audit.toml` and `SECURITY-EXCEPTIONS.md` are read-only inputs to this plan (D-00i)."
    - "Do not mark SUPPLY-01's CI-run-observation clause closed, and do not simulate, infer or fabricate a CI run to close it. No run exists that postdates the 2026-08-08 deletion; a closure claim without a `gh run` citation newer than `30861568499` is a false positive (D-07)."
    - "Do not apply the committed GitHub rulesets, enable branch protection, or make any change to live repository administration state. It is a recorded finding with an owner, not work (D-10)."
    - "Do not delete or rewrite any text this plan corrects. Every banner is annotation appended beside the original sentence; the original stays readable so a later auditor can tell a fixed defect from one that never existed (D-00c)."
    - "Do not re-derive `pdf-extract` reachability. ADR-0032 settled it and `.cargo/audit.toml:26-29` already carries the corrected wording (D-00j)."
    - "Do not pick up the `scraper` / `rss` / `tiktoken-rs` dead-dependency finding. Phase 15 is the named owner of record; `ROADMAP.md:714`'s phrasing reads ambiguously but assigns nothing to Phase 12 (D-00k)."
    - "Do not create `.planning/ledgers/milestone-09-12.md` or any stub of it. Its 120-requirement scope is ORCH-01 / Phase 13's stated deliverable (D-09)."
    - "Do not modify any `.rs` file. Phases 10 and 11 each changed zero; a Rust change here is a signal to re-read the phase boundary, not to widen it."
---

<objective>
The tracer: prove the whole record-shaped pipeline of this phase end-to-end on the half that is
already built. Re-run the three supply-chain gates for real, capture their transcripts verbatim, and
carry that evidence all the way through to a dated closure record, a corrected source document and a
flipped traceability row — before any of the expensive ADR or guard work begins.

Purpose: SUPPLY-01 and SUPPLY-02 were executed by Phase 9 and are inherited as **closed items to
verify, not work to re-plan**. Two phases recorded the verification as impossible here because
`crates.io` returned HTTP 403; that blocker has lifted. This plan discharges the verification in-repo
under the D-00e evidence bar, records the one clause that genuinely cannot close in-repo as pending
rather than faking it, and proves the measure → record → correct → close path works before waves 2-4
reuse it for ADR-0036 and the D-08 guard.

Output: SUPPLY-01 and SUPPLY-02 closed with three verbatim gate transcripts, two dated blocker-lifted
banners, a dated pending record for the CI-run observation, the GitHub-rulesets finding, a corrected
stale-line-citation sweep, and a blocking decision checkpoint that gates the ADR-0036 allocation and
surfaces this phase's three unratified `--auto` decisions to a human.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-supply-chain-gate-integrity/12-CONTEXT.md
@.planning/phases/12-supply-chain-gate-integrity/12-RESEARCH.md
@.planning/phases/12-supply-chain-gate-integrity/12-PATTERNS.md
@.planning/phases/12-supply-chain-gate-integrity/12-VALIDATION.md
</context>

## Artifacts this phase produces

Every symbol below is created by Phase 12 and does not exist in the tree before it. A drift check that
flags one of these as an unknown reference is reading a not-yet-written artifact, not a defect.

| Artifact | Plan | Kind |
|---|---|---|
| `scripts/check-workflow-suppressions.sh` | 12-02 | new file — the D-08 regression guard |
| `WORKFLOWS_DIR`, `CARGO_GATE_RE`, `IGNORE_FLAG_RE`, `violates()`, `CLAUSE1_INLINE_SUPPRESSION`, `CLAUSE2_AUDIT_INVOCATION_COUNT` | 12-02 | new identifiers inside that script |
| `check-workflow-suppressions` | 12-02 | new `Makefile` target, added to the `check-gates` prerequisite list |
| `Check workflow files for inline advisory suppressions` | 12-02 | new `ci.yml` step name in the `cargo-deny:` job |
| `.planning/decisions/0036-audit-suppression-single-source-topology.md` | 12-03 | new ADR |
| `#### Hand-off to Phase 13 / ORCH-01 — dated 2026-08-09 (plan 12-04)` | 12-04 | new `REQUIREMENTS.md` heading |
| `PROMOTION.md` index row `0036`, its dated note, `**Next free ADR number: 0037**`, Part B candidate 7 closure note | 12-04 | new rows/lines in an existing record |
| `PROJECT.md` `## Key Decisions` row for ADR-0036 | 12-04 | new row in an existing table |
| `.planning/REQUIREMENTS.md` | **12-01** (SUPPLY-01/02 closure, banners, rows), 12-03 (SUPPLY-03 banners), 12-04 (hand-off, SUPPLY-03 rows) | modified |
| `.planning/ROADMAP.md` | **12-01** (blocker-lifted banner, stale citation) | modified |
| `.planning/PROJECT.md` | **12-01** (stale citations), 12-03 (2 promotion-viability banners), 12-04 (Key Decisions row) | modified |
| `.planning/STATE.md` | **12-01** (stale citations) | modified |

**No `.rs` file appears on this list, and none may be added to it.**

## Executor notes

- `.planning/config.json` sets `worktree_skip_hooks: true`. The `cargo-fmt` and `cargo-clippy`
  pre-commit hooks carry `always_run: true`, which overrides their `types: [rust]` filter and fires
  them on a `.planning/`-only commit — cold-compiling the whole workspace for nothing. Commit with
  `--no-verify`, matching the convention plans 06-01…06-10, 08-01, 08-02 and 08-04 all record.
- Run `cargo audit` and `cargo deny check` **directly**, never through `pre-commit run --all-files`.
- This plan changes zero `.rs` files. `git diff --name-only <base>..HEAD -- '*.rs' | wc -l` is `0`.

## Flagged planner assumptions carried into this plan

1. **The stale `ci.yml:389-406` citation is in far more places than either upstream artifact says.**
   `12-CONTEXT.md` §domain says "three documents"; `12-RESEARCH.md` §B.5 re-grepped and found one, and
   flagged the discrepancy as Open Question 1. The planner re-ran the broad grep this session:
   `grep -rn '389-406' .planning/ .project/` returns **46 hits across 21 files**. Neither upstream
   number survives. Task 2 carries the scoping rule and the measured inventory; the executor re-runs
   the grep and records the live count rather than inheriting any of the three numbers.
2. **SUPPLY-01's traceability cell reads `Complete` while one observation is pending.** The cell
   vocabulary is `Pending` / `Complete` only, and adding prose to it risks breaking a downstream
   parser. The honest record lives where D-07 puts it — in SUPPLY-01's own block and in the Phase 13
   hand-off — so no reader reaches `Complete` without finding the caveat one hop away. Task 1 makes
   the pairing a hard acceptance criterion: the cell may not flip unless the pending record exists.
3. **`09-CONTEXT.md`'s HTTP-403 caveat is deliberately left uncorrected.** `12-RESEARCH.md` §D.12
   item 4 / Assumption A2 found no precedent in this corpus for one phase amending a different
   phase's `NN-CONTEXT.md`. It is cited as provenance, not edited. Record this choice in the SUMMARY.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — re-run all three gates and carry their transcripts through to closed SUPPLY-01 and SUPPLY-02 records</name>
  <precondition>`cargo audit`, `cargo deny check` and `./scripts/check-advisory-register.sh` are all invocable, and `gh` is authenticated. Verify with `command -v cargo-audit cargo-deny` and `gh auth status` before starting. If `cargo audit`'s advisory-database fetch fails at execution time, record the failure transcript verbatim and STOP — do not claim D-06's local verification discharged on a tool that did not run.</precondition>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 1833-1873 — SUPPLY-01's full text, its `**Done when**` clause at `:1853`, and Phase 9's existing `**Closed by Phase 9, dated 2026-08-08**` note at `:1861-1873` including its `**Remaining for Phase 12:**` sentence at `:1871-1873`. This plan appends beneath that note; it does not replace it.
    - `.planning/REQUIREMENTS.md` lines 1875-1927 — SUPPLY-02's full text, its three clauses, and Phase 9's closure note at `:1911-1927` including the `**Remaining for Phase 12:** … CI-only … (crates.io returns HTTP 403)` sentence at `:1925-1927`.
    - `.planning/REQUIREMENTS.md` lines 4004-4010 — the traceability table's `| REQ-ID | Phase | Status |` shape, `FACADE-04`'s already-closed `Complete` row at `:4007` as the exact cell form to copy, and the three `Pending` SUPPLY rows at `:4008-4010`.
    - `scripts/check-advisory-register.sh` lines 1-49 — the header's four stated properties: three clauses, report-every-failure, class-discovery-by-set-equality, never-scrape-comment-prose. Lines 11-15 (case-sensitive set comparison), 31-33 (the one-sided-empty case), 43 (reads only, writes nothing, creates no temporary file), 44-45 (running twice produces identical output and the same exit code). These are the source assertions this task's edge-case truths cite.
    - `.github/workflows/ci.yml` lines 60-78 — the surviving `security-audit:` job, its `name: Security Audit` at `:62`, the "no inline `--ignore` flags are used here" rationale comment at `:76`, and the bare `run: cargo audit` at `:78`.
    - `.planning/phases/12-supply-chain-gate-integrity/12-RESEARCH.md` §D.11 (the transcript shapes to match), §D.13 (the run-ID boundary), §F pitfall 3 (why SUPPLY-01's CI clause and SUPPLY-02's installability clause must not be conflated).
    - `.planning/codebase/CONCERNS.md` lines 9 and 276 — the two verbatim correction-banner examples, for the inline-parenthetical shape used in Task 2.
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Capture evidence first, write second. Nothing in this task may cite `12-CONTEXT.md` or
`12-RESEARCH.md` as the source of a transcript — those are context files and a context file is not
evidence (D-00e). Re-run every command and paste what it actually printed.

**Step 1 — capture the three gate transcripts.** Run each and record the literal invocation, the
literal output and the literal exit code:
  - `cargo audit` — expect exit `0`. Record the `Loaded N security advisories` line, the
    `Scanning Cargo.lock for vulnerabilities (N crate dependencies)` line, and the trailing
    `warning: N allowed warnings found` line. Record the numbers this run produced, not the
    `1190` / `677` / `8` figures the research recorded yesterday.
  - `cargo deny check` — expect exit `0` and the tail line `advisories ok, bans ok, licenses ok, sources ok`.
  - `./scripts/check-advisory-register.sh` — expect exit `0` and the single-line summary beginning
    with a check mark. Run it **twice in succession** and record both invocations, so the header's
    stated idempotence property at `check-advisory-register.sh:44-45` is demonstrated rather than
    quoted.

**Step 2 — capture SUPPLY-01's two structural measurements.** Both must scan every workflow file,
not only `ci.yml`, so a future workflow cannot hide a second invocation:
  - the count of `run: cargo audit` occurrences summed across `.github/workflows/*.yml` — expect `1`;
  - the count of lines reading `name: Security Audit` summed across the same glob — expect `1`.
  Record both commands and both outputs. These are the adjacency measurement: two jobs sharing one
  display name collide in the required-status-check namespace rather than resolving separately, which
  is the defect SUPPLY-01 existed to close.

**Step 3 — capture the CI-run boundary and the rulesets finding.** Run `gh run list --workflow=ci.yml --limit 5`
and `gh run view 30861568499 --json jobs -q '.jobs[] | "\(.name): \(.conclusion)"'`. Confirm the most
recent run is still `30861568499` dated `2026-08-03T23:14:24Z`; if a newer run has appeared since the
research date, record it and re-derive whether D-07's clause can now close. Then run
`gh api repos/:owner/:repo/rulesets` and `gh api repos/:owner/:repo/branches/main/protection`,
recording both outputs. **Read only. Apply nothing, enable nothing.**

**Step 4 — write SUPPLY-01's closure record.** Append a `**Verified by Phase 12, dated 2026-08-09 (plan 12-01):**`
passage immediately beneath Phase 9's existing closure note, retaining that note in full. It carries:
  - the two structural transcripts from step 2 with their commands;
  - the `cargo audit` transcript from step 1;
  - a `**Pending — trigger: the next push to `release/v0.7.0`.**` clause recording that the CI-run
    observation has not failed, it has never had the opportunity to fire: the most recent `ci.yml` run
    is `30861568499`, `2026-08-03T23:14:24Z`, five days before the 2026-08-08 deletion. Name the run ID
    as the boundary that establishes this, and record that in that last pre-deletion run the only
    failing job was `API Surface Tracking` — DEBT-01's, Phase 8's — while `Security Audit` passed;
  - the GitHub-rulesets finding: `.github/rulesets/` is version-controlled but the two `gh api`
    commands from step 3 show the rulesets are not applied and `main` is not protected, so SUPPLY-01's
    "required status check" clause currently has no live enforcement point. Name the milestone
    close-out as owner, flag it as owner-only, and state plainly that this phase applied nothing.

**Step 5 — write SUPPLY-02's closure record.** Append a matching `**Verified by Phase 12, dated 2026-08-09 (plan 12-01):**`
passage beneath Phase 9's closure note, retaining it in full. It carries the `cargo deny check` and
both `check-advisory-register.sh` transcripts, and states the four register properties this run
demonstrates, each citing the script line that asserts it: set-based and case-sensitive comparison so
a row differing only in letter case fails and reordering changes nothing (`:11-15`); the one-sided-empty
case as a distinct named failure (`:31-33`); read-only with no temporary file, so concurrent or
interrupted invocation cannot corrupt an input (`:43`); and identical output and exit code across the
two consecutive runs (`:44-45`).

**Step 6 — flip the checkboxes and the traceability rows.** Change SUPPLY-01's and SUPPLY-02's leading
`- [ ]` to `- [x]`. In the traceability table, change the `Status` cell of the `SUPPLY-01` and
`SUPPLY-02` rows from `Pending` to `Complete`, copying `FACADE-04`'s cell form exactly. Touch no other
column and no other row — SUPPLY-03's row stays `Pending` until plan 12-04. **SUPPLY-01's cell may not
flip unless step 4's pending record exists in the same commit**; the traceability table's vocabulary
cannot express "complete with one pending observation", so the pairing is what keeps the row honest.
  </action>
  <verify>
    <automated>set -e; R=.planning/REQUIREMENTS.md; cargo audit >/tmp/ga.txt 2>&1; echo "cargo audit exit=$?"; cargo deny check >/tmp/gd.txt 2>&1; echo "cargo deny exit=$?"; ./scripts/check-advisory-register.sh >/tmp/gr.txt 2>&1; echo "register exit=$?"; A=$(grep -rhc 'run: cargo audit' .github/workflows/*.yml | awk '{s+=$1} END{print s}'); [ "$A" = 1 ] || { echo "FAIL: cargo audit invocations=$A expected 1"; exit 1; }; N=$(grep -rhc 'name: Security Audit' .github/workflows/*.yml | awk '{s+=$1} END{print s}'); [ "$N" = 1 ] || { echo "FAIL: Security Audit display names=$N expected 1"; exit 1; }; C=$(grep -c 'Verified by Phase 12, dated 2026-08-09 (plan 12-01)' "$R"); [ "$C" -ge 2 ] || { echo "FAIL: expected 2 closure records, found $C"; exit 1; }; grep -qF '30861568499' "$R" || { echo "FAIL: run-ID boundary missing"; exit 1; }; grep -q '^| SUPPLY-01 | Phase 12 | Complete |' "$R" || { echo "FAIL: SUPPLY-01 row not Complete"; exit 1; }; grep -q '^| SUPPLY-02 | Phase 12 | Complete |' "$R" || { echo "FAIL: SUPPLY-02 row not Complete"; exit 1; }; grep -q '^| SUPPLY-03 | Phase 12 | Pending |' "$R" || { echo "FAIL: SUPPLY-03 row must stay Pending in this plan"; exit 1; }; echo "3 gates green, 1 invocation, 1 display name, 2 closure records, rows flipped"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo audit` exits `0` and its recorded transcript in `REQUIREMENTS.md` contains the `Loaded` and `Scanning Cargo.lock for vulnerabilities` lines produced by **this** run, with this run's own numbers.
    - `cargo deny check` exits `0` and the literal string `advisories ok, bans ok, licenses ok, sources ok` appears in `REQUIREMENTS.md`.
    - `./scripts/check-advisory-register.sh` exits `0` on two consecutive invocations, and `REQUIREMENTS.md` records both, showing identical output — the idempotence property at `check-advisory-register.sh:44-45` demonstrated, not quoted.
    - `grep -rhc 'run: cargo audit' .github/workflows/*.yml | awk '{s+=$1} END{print s}'` returns `1`, and `grep -rhc 'name: Security Audit' .github/workflows/*.yml | awk '{s+=$1} END{print s}'` returns `1`; both commands and both outputs appear in SUPPLY-01's record.
    - `grep -c 'Verified by Phase 12, dated 2026-08-09 (plan 12-01)' .planning/REQUIREMENTS.md` returns at least `2` — one closure record per requirement.
    - `grep -qF '30861568499' .planning/REQUIREMENTS.md` succeeds, and the surrounding sentence names the trigger for the pending clause and states the clause is not closed.
    - `grep -qF 'API Surface Tracking' .planning/REQUIREMENTS.md` succeeds within SUPPLY-01's record — the only failing job in the boundary run is named and attributed to DEBT-01, not to SUPPLY.
    - `grep -q '^| SUPPLY-01 | Phase 12 | Complete |'` and `grep -q '^| SUPPLY-02 | Phase 12 | Complete |'` both succeed; `grep -q '^| SUPPLY-03 | Phase 12 | Pending |'` also succeeds, proving no row outside this plan's scope moved.
    - `grep -c '^- \[x\] \*\*SUPPLY-0[12]\*\*' .planning/REQUIREMENTS.md` returns `2`.
    - Phase 9's original closure notes are intact: `grep -qF 'Closed by Phase 9, dated 2026-08-08' .planning/REQUIREMENTS.md` succeeds and `git diff --numstat` for this file shows deletions confined to the two checkbox lines and the two traceability cells — no prose line removed.
    - The rulesets finding names the milestone close-out as owner and records both `gh api` outputs; `git status --porcelain -- .github/` is empty, proving nothing in the repository's GitHub configuration was touched.
    - `git diff --name-only <base>..HEAD -- '*.rs' | wc -l` returns `0`, and `deny.toml`, `.cargo/audit.toml` and `SECURITY-EXCEPTIONS.md` are unchanged (`git status --porcelain -- deny.toml .cargo/audit.toml SECURITY-EXCEPTIONS.md` is empty).
  </acceptance_criteria>
  <done>All three gates re-ran and exited 0 with their transcripts recorded verbatim in SUPPLY-01's and SUPPLY-02's blocks; both structural measurements return 1; the CI-run observation is recorded pending with its trigger and run-ID boundary; the rulesets finding is recorded with an owner and nothing applied; both checkboxes and both traceability rows are flipped and SUPPLY-03's is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Banner the two lifted-blocker sites and sweep the live stale `ci.yml:389-406` citations</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 1925-1927 — SUPPLY-02's `**Remaining for Phase 12:** … CI-only, not run in this environment (crates.io returns HTTP 403)` sentence. **This is the one that gets the blocker-lifted banner.**
    - `.planning/REQUIREMENTS.md` lines 1871-1873 — SUPPLY-01's `**Remaining for Phase 12:** confirming the required status check still resolves … (CI-only, not verifiable in this sandboxed environment)` sentence. **This one does NOT get a blocker-lifted banner** — its substance is still correct per D-07, and bannering it as resolved is `12-RESEARCH.md` §F pitfall 8.
    - `.planning/ROADMAP.md` lines 768-778 — the Phase 12 dated closure note, in particular the `neither tool is installable in Phase 9's sandboxed environment — crates.io returns HTTP 403` clause at `:772-776`. Its CI-run-observation half stays as written; only the installability half is bannered.
    - `.planning/codebase/CONCERNS.md` line 9 and line 276 — the two verbatim banner examples. The shape is a **parenthetical clause appended inside or immediately after the original sentence**, opening `(**Corrected by Phase 12 (plan 12-01), dated 2026-08-09, citing <what>:** …)`, stating the original claim's status explicitly, giving the corrected fact in full. Not a blockquote, not a separate section, and the original is never deleted.
    - `.planning/REQUIREMENTS.md` line 1094 and line 1864 — the two Phase 9 correction banners that **already** correct `:389-406` citations within their own regions. Read both so this task does not double-banner a site a prior phase already fixed.
    - `.planning/PROJECT.md` lines 495-499 (the open `- [ ] Delete ci.yml:389-406` checkbox) and lines 860-864 (the §Context narrative naming `:60-77` and `:389-406`).
    - `.planning/ROADMAP.md` lines 850-854 — the "four cheapest high-value items" list, item 1.
    - `.planning/STATE.md` lines 317-321 and 753-757.
    - `.planning/REQUIREMENTS.md` lines 1186-1190, 3480 and 4059-4063.
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/PROJECT.md, .planning/STATE.md</files>
  <action>
Three correction classes, one shared banner shape, all dated 2026-08-09 and naming plan 12-01. Every
banner is annotation appended beside its original sentence; the original stays readable (D-00c).

**(1) The blocker-lifted banner — two sites, and only two.** Append the parenthetical to
`REQUIREMENTS.md:1925-1927` and to the installability clause of `ROADMAP.md:772-776`. Each states: the
HTTP-403 caveat has lifted, `cargo-audit` and `cargo-deny` are both on `PATH` in this environment, and
both tools plus the register script were re-run and exited `0` on 2026-08-09 by plan 12-01 — pointing
at the transcripts Task 1 wrote into SUPPLY-01's and SUPPLY-02's blocks rather than restating them.
Say **when** the blocker lifted, not merely that it did, so the next reader does not treat the caveat
as permanent: Phases 9 and 10 were both correct at the time they wrote it.

**Do not banner `REQUIREMENTS.md:1871-1873`, and do not banner the CI-run-observation half of the
ROADMAP note.** Those sentences say a CI-run confirmation cannot happen locally, which is still true
(D-07). Conflating the two is this phase's named pitfall.

**(2) The stale `ci.yml:389-406` sweep.** Re-run `grep -rn '389-406' .planning/ .project/` first and
record the live hit count and file list in the SUMMARY — the planner measured **46 hits across 21
files**, against `12-CONTEXT.md`'s "three documents" and `12-RESEARCH.md`'s "one". Do not inherit any
of those three numbers; record what the grep returns today.

Apply this scoping rule and state it in the SUMMARY:

  - **In scope** — live, uncorrected citations in the four current canonical governance documents a
    future reader consults as authoritative for SUPPLY-01's status: `.planning/REQUIREMENTS.md`,
    `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`. The named sites are
    `PROJECT.md:497` and `:862`, `ROADMAP.md:852`, `STATE.md:319` and `:755`, and
    `REQUIREMENTS.md:1188`, `:3480` and `:4061`. Re-derive each line number before editing; earlier
    tasks in this plan shift them.
  - **Out of scope, and each excluded for a stated reason** — `.planning/milestones/v0.7.1-*` (a frozen
    archived-milestone snapshot); `.planning/phases/09-*` (prior-phase context, log and summary files,
    left as historical record per `12-RESEARCH.md` Assumption A2); `.planning/intel/*` and
    `.planning/INGEST-CONFLICTS.md` (closed ingest outputs — there is no run 6);
    `.planning/ledgers/milestone-01.md:144` (a closed prior-milestone ledger row whose deferral record
    is accurate as written and already names Phase 12 / SUPPLY-01 as owner);
    `.planning/decisions/0024-rustsec-exception-governance.md:223` (already annotates the line numbers
    as stale, and ADR-0024 is not edited by this phase under D-00i); and the two
    `REQUIREMENTS.md` regions Phase 9 already corrected at `:1094` and `:1864`.

Each in-scope site gets one parenthetical recording that the cited range never held the duplicate job:
it was re-derived at `ci.yml:465-482` and deleted by Phase 9's plan 09-06 in commit `cb75b2b`, so the
line citation was already stale before Phase 9 touched anything and the job it names no longer exists.
Where the site is a `- [ ]` open checkbox describing work as outstanding — `PROJECT.md:497` — the
banner must also say the work is done, so the checkbox is not read as a live to-do.

**(3) Nothing else moves.** Do not renumber, reorder or reflow any surrounding list. Do not correct a
site outside the scoping rule, and do not delete a site that is already correct.
  </action>
  <verify>
    <automated>set -e; R=.planning/REQUIREMENTS.md; P=.planning/PROJECT.md; M=.planning/ROADMAP.md; S=.planning/STATE.md; grep -qF 'crates.io returns HTTP 403' "$R" || { echo "FAIL: original SUPPLY-02 caveat deleted"; exit 1; }; grep -qF 'returns HTTP 403' "$M" || { echo "FAIL: original ROADMAP caveat deleted"; exit 1; }; grep -qF 'Delete `ci.yml:389-406`' "$P" || { echo "FAIL: original PROJECT.md citation deleted"; exit 1; }; B=$(grep -rc 'Corrected by Phase 12 (plan 12-01), dated 2026-08-09' "$R" "$P" "$M" "$S" | awk -F: '{s+=$2} END {print s}'); [ "$B" -ge 8 ] || { echo "FAIL: only $B dated corrections across the four documents, expected at least 8"; exit 1; }; T=$(grep -rc '465-482' "$R" "$P" "$M" "$S" | awk -F: '{s+=$2} END {print s}'); [ "$T" -ge 8 ] || { echo "FAIL: only $T corrections name the true location, expected at least 8"; exit 1; }; grep -qF 'confirming the required status check still resolves' "$R" || { echo "FAIL: SUPPLY-01 CI clause text lost"; exit 1; }; echo "banners present, originals retained, true location named $T times"</automated>
  </verify>
  <acceptance_criteria>
    - The two lifted-blocker banners exist and their originals are intact: `grep -qF 'crates.io returns HTTP 403' .planning/REQUIREMENTS.md` and `grep -qF 'returns HTTP 403' .planning/ROADMAP.md` both succeed, and each site now also matches `Corrected by Phase 12 (plan 12-01), dated 2026-08-09`.
    - SUPPLY-01's CI-run clause is **not** bannered as resolved: the sentence at `REQUIREMENTS.md:1871-1873` still reads as pending, `grep -qF 'confirming the required status check still resolves' .planning/REQUIREMENTS.md` succeeds, and no `Corrected by Phase 12` parenthetical is attached to it.
    - The live grep count is recorded in the SUMMARY together with its file list, and the SUMMARY states plainly that it agrees with neither `12-CONTEXT.md`'s "three documents" nor `12-RESEARCH.md`'s "one".
    - Every in-scope site carries exactly one dated correction: summed across the four documents, `grep -c 'Corrected by Phase 12 (plan 12-01), dated 2026-08-09'` is at least `8`, and no single site carries two.
    - Every correction names the true location: summed across the four documents, `grep -c '465-482'` is at least `8`, and each occurrence sits inside a correction that also names commit `cb75b2b`.
    - Every original citation is retained: `grep -rc '389-406' .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/ROADMAP.md .planning/STATE.md` summed is no lower than its pre-task value, proving annotation rather than replacement.
    - `PROJECT.md:497`'s open-checkbox correction states the work is done, not merely that the line number is wrong.
    - No excluded path was edited: `git status --porcelain -- .planning/milestones/ .planning/intel/ .planning/INGEST-CONFLICTS.md .planning/ledgers/ .planning/decisions/ .planning/phases/09-release-security-gate-integrity/` is empty.
    - `git diff --numstat` across the four files shows deletions of `0` — every correction is purely additive.
    - `git diff --name-only <base>..HEAD -- '*.rs' | wc -l` returns `0`.
  </acceptance_criteria>
  <done>The two installability caveats carry dated blocker-lifted banners naming when the blocker lifted; SUPPLY-01's CI-run clause is deliberately left unbannered; every in-scope stale `:389-406` citation carries one dated correction naming `ci.yml:465-482` and commit `cb75b2b`; every original is retained; the measured grep inventory and the scoping rule are recorded in the SUMMARY; no excluded path was touched.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Proceed with SUPPLY-03 acting — spend ADR number 0036 promoting `PROMOTION.md` Part B candidate 7, and add the D-08 regression guard to CI — or stop at the verification this plan just delivered.</decision>
  <context>
This phase rests on three decisions that were auto-selected and never confirmed by a human. Waves 2-4
are irreversible in a way this wave was not: an ADR promoted into `.planning/decisions/` tops the
precedence order and can only be retired through the supersession mechanism, never deleted, and
`PROMOTION.md` states that ADR numbers are never reused even if a candidate is later declined. This is
the last point at which the phase can stop having produced only verification.

The three flags, each verbatim from `12-CONTEXT.md`:

  1. **D-01 ⚠ HUMAN REVIEW — this overrides a governing requirement's own explicit self-limitation.**
     `REQUIREMENTS.md:1937-1939` says "**This requirement does not act.** Promotion requires re-tagging
     the source documents and re-running ingest…", and `PROJECT.md` §Out of Scope and
     `REQUIREMENTS.md:103-109` say the same. D-01 overrides all three on the strength of
     `PROMOTION.md` §Part A ("Promotion is now an ordinary write to a directory plus a table row") and
     four prior promotions — candidates 1, 2, 3 and 5, closed by ADR-0016, ADR-0021, ADR-0024 and
     ADR-0025. Three documents say this requirement cannot act; a fourth says it can, and four ADRs
     prove it. Approving option-a accepts that reading.

  2. **D-08 ⚠ HUMAN REVIEW — this adds a CI check no requirement explicitly asks for.** SUPPLY-03's
     text says promotion "would turn the run-5 supply-chain finding from an **observation** into a
     **gate**", and D-08 reads the word *gate* literally. Nothing anywhere currently asserts that a
     workflow file carries no inline advisory suppression — the exact defect SUPPLY-01 existed to fix
     could be reintroduced tomorrow and every gate would stay green. The guard is one offline bash
     script plus two wiring lines; deleting it restores today's behaviour exactly.

  3. **D-00l ⚠ HUMAN REVIEW — this phase's entire scope rests on an unratified `--auto` decision.**
     Phase 9's D-07 re-scoped Phase 12 and is itself flagged "⚠ HUMAN REVIEW — this changes another
     phase's scope"; `09-VERIFICATION.md` scores 5/5 and does not record it as ratified. Phase 12
     proceeds because the re-scope is recorded at source in `ROADMAP.md` §Phase 12 and
     `REQUIREMENTS.md:4085`, which makes it durable and auditable — not because a human approved it. If
     you disagree, SUPPLY-01 and SUPPLY-02 return to Phase 12 as **work** rather than verification, and
     the plan this wave just executed is the wrong shape.

What plan 12-01 has already delivered, and what is not at risk either way: three gate transcripts,
SUPPLY-01 and SUPPLY-02 closed, the CI-run observation recorded pending, the rulesets finding recorded
with an owner, and the stale-citation sweep. None of that depends on this decision.
  </context>
  <options>
    <option id="option-a">
      <name>Proceed as planned — ADR-0036 and the D-08 guard (recommended)</name>
      <pros>Delivers SUPPLY-03's own stated intent, turning the invariant from prose at PRD precedence into an enforced gate. Closes `PROMOTION.md` Part B candidate 7, whose Owner phase is explicitly Phase 12 — leaving it open spends the owning phase without a decision and forces a future reader to re-derive the same contradiction. The tree already satisfies the invariant, which makes this the cheapest possible moment to lock it: the ADR ratifies a true state rather than mandating a change.</pros>
      <cons>Spends ADR number 0036 permanently. Adds a CI check no requirement explicitly asked for. Both consequences follow from decisions no human has ratified.</cons>
    </option>
    <option id="option-b">
      <name>ADR-0036 only — promote the invariant, skip the D-08 guard</name>
      <pros>Honours D-01 and D-03 while leaving CI untouched; adds no check the requirements did not ask for.</pros>
      <cons>Leaves the invariant as prose. Prose did not stop the duplicate job the first time, and SUPPLY-03's own text asks for a gate. Plan 12-02 is dropped and ADR-0036's `## Code Locations` loses its enforcement citation.</cons>
    </option>
    <option id="option-c">
      <name>Stop after verification — record a recommendation, promote nothing</name>
      <pros>Takes `REQUIREMENTS.md:1937-1939` at face value and defers to a human on all three flags. Nothing irreversible happens.</pros>
      <cons>Leaves candidate 7 open with its owning phase spent, leaves ROADMAP criterion 5 half-satisfied, and forces the next reader to re-derive the same "three documents say no, a fourth says yes" contradiction from scratch.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or option-c. If option-b or option-c, say so explicitly — plans 12-02, 12-03 and 12-04 are written against option-a and must be re-scoped before execution continues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a live gate command → a written closure claim | The moment a transcript becomes prose is where a false pass can be introduced; a claim not backed by output run in this execution is unfalsifiable later |
| a stale citation → a governing document a future phase plans against | `PROJECT.md`'s open checkbox and `STATE.md`'s narrative are read as live work queues; a stale entry causes a completed deletion to be planned a second time |
| this phase → live GitHub repository administration state | `gh api` can both read and write; the rulesets finding is one flag away from becoming an unauthorised outward-facing change |
| `.planning/` records → the supply-chain gate configuration | `deny.toml`, `.cargo/audit.toml` and `SECURITY-EXCEPTIONS.md` are ADR-0024's, and are read-only inputs here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Spoofing | SUPPLY-01/02 closure records | high | mitigate | A transcript copied from `12-CONTEXT.md` or `12-RESEARCH.md` would assert a gate verdict nobody re-measured, which is exactly the failure D-06 names. Mitigated by re-running all three gates inside the task's own `<automated>` verify, by requiring this run's own numbers rather than yesterday's `1190`/`677`/`8`, and by a precondition that halts if `cargo audit`'s advisory-db fetch fails rather than degrading to a citation. |
| T-12-02 | Spoofing | SUPPLY-01's CI-run-observation clause | high | mitigate | Marking the clause closed without a `gh run` citation postdating 2026-08-08 is a false positive the verifier cannot distinguish from a real pass. Mitigated by recording it `pending` with the run-ID boundary `30861568499` as a hard acceptance criterion, by pairing the traceability flip to the pending record's existence, and by carrying it forward as a Manual-Only row in `12-VALIDATION.md`. |
| T-12-03 | Tampering | `deny.toml`, `.cargo/audit.toml`, `SECURITY-EXCEPTIONS.md` | high | mitigate | A suppression silently added, removed or re-dated during "verification" would re-enter CI unnoticed and invalidate ADR-0024's register in the same stroke. Mitigated by a prohibition, by these files being read-only inputs, and by an acceptance criterion asserting `git status --porcelain` over all three is empty. |
| T-12-04 | Tampering | Corrected source text | high | mitigate | A correction that overwrites what it corrects leaves a later auditor unable to tell a fixed defect from one that never existed. Mitigated by the paired-grep protocol — banner present **and** original retained — on every site, and by a `--numstat` deletions-of-zero assertion across all four documents. |
| T-12-05 | Elevation of Privilege | live GitHub repository settings | high | mitigate | `gh api` against `repos/:owner/:repo/rulesets` is one HTTP verb from applying a ruleset to a live repository, which is owner-only and outside this phase. Mitigated by restricting the task to two named read-only `GET` invocations, by a prohibition, and by an acceptance criterion asserting `git status --porcelain -- .github/` is empty. |
| T-12-06 | Repudiation | the four corrected documents | medium | mitigate | Each correction is dated, names plan 12-01, states the original claim's status explicitly and cites the true location `ci.yml:465-482` plus commit `cb75b2b`, so a later reader can see what changed, when, and on what evidence (D-00c, D-00e). |
| T-12-07 | Denial of Service | `cargo audit`'s advisory-database fetch | low | accept | The fetch is network-bound and can fail transiently. Accepted rather than mitigated: the precondition halts the task on failure and the honest posture is to record the failure verbatim, not to work around it. A retry costs seconds. |
| T-12-08 | Information Disclosure | — | low | accept | Not applicable. Every value read or written is a repository-local path, a requirement ID, a commit SHA, a workflow-run ID or an advisory identifier already present in the checkout. `gh api` output is public repository metadata. No secret or personal datum is read or written. |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable, truthfully: this plan installs, upgrades and removes nothing. `cargo audit` and `cargo deny` are already on `PATH` and are invoked, not installed; no manifest or lockfile is written. The `git status --porcelain` criteria on both tasks prove it. |
</threat_model>

<verification>
- `cargo audit` → exit 0; `cargo deny check` → exit 0, tail `advisories ok, bans ok, licenses ok, sources ok`; `./scripts/check-advisory-register.sh` → exit 0, twice, identically.
- `run: cargo audit` occurrences across `.github/workflows/*.yml` sum to `1`; `name: Security Audit` occurrences sum to `1`.
- `REQUIREMENTS.md` carries two `Verified by Phase 12, dated 2026-08-09 (plan 12-01)` records containing this run's transcripts.
- SUPPLY-01 and SUPPLY-02 are `- [x]` and read `Complete` in the traceability table; SUPPLY-03 still reads `Pending`.
- The CI-run observation is recorded pending with trigger and run-ID `30861568499`; the rulesets finding is recorded with an owner and nothing applied.
- Every in-scope stale `:389-406` citation carries a dated correction naming `ci.yml:465-482` and commit `cb75b2b`, with the original retained.
- `git status --porcelain -- deny.toml .cargo/audit.toml SECURITY-EXCEPTIONS.md .github/` is empty.
- No `.rs` file is modified.
</verification>

<success_criteria>
- SUPPLY-01 and SUPPLY-02 are closed on evidence produced in this execution, not inherited from a context file.
- The one clause that genuinely cannot close in-repo is recorded pending with a named trigger, not faked.
- A future reader of `PROJECT.md`, `STATE.md`, `ROADMAP.md` or `REQUIREMENTS.md` cannot mistake the already-deleted duplicate job for outstanding work.
- The phase's three unratified `--auto` decisions are put in front of a human before anything irreversible happens.
</success_criteria>

<output>
Create `.planning/phases/12-supply-chain-gate-integrity/12-01-SUMMARY.md` when done. Record in it: the
three gate transcripts verbatim, the live `grep -rn '389-406'` hit count and file list with the
in-scope/out-of-scope split, the checkpoint selection, and the deliberate choice not to edit
`09-CONTEXT.md`.
</output>
