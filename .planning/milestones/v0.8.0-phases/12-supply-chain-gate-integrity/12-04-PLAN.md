---
phase: 12-supply-chain-gate-integrity
plan: 04
type: execute
wave: 4
depends_on: ["12-01", "12-02", "12-03"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/decisions/PROMOTION.md
autonomous: true
requirements: [SUPPLY-01, SUPPLY-02, SUPPLY-03]

must_haves:
  truths:
    - "`#### Hand-off to Phase 13 / ORCH-01 — dated 2026-08-09 (plan 12-04)` exists in `REQUIREMENTS.md` in the established four-part shape: dated heading, bold one-sentence lead, numbered substance list with `file:line` citations, closing `**Evidence:**` line"
    - "The hand-off carries ORCH-01's named verdict class in both halves: Milestone 10 is recorded 100% complete, ships every artefact it promised, and failed one of its own acceptance criteria — and, as of 2026-08-08, no longer does. Phase 12 is the phase that dates the second half"
    - "The hand-off states explicitly that Phase 12 did **not** create `.planning/ledgers/milestone-09-12.md` and that the 120-requirement ledger is ORCH-01's own deliverable, so Phase 13 inherits an unconstrained shape rather than a stub (D-09)"
    - "The hand-off carries SUPPLY-01's still-pending CI-run observation with its trigger and run-ID boundary, so no reader reaches the `Complete` traceability cell without finding the caveat one hop away (D-07)"
    - "The hand-off carries the unapplied-GitHub-rulesets finding with the milestone close-out named as owner, recording that this phase applied nothing (D-10)"
    - "All three SUPPLY requirements are `- [x]` with `Complete` traceability rows, and no other requirement's row changed — every closure and every outstanding item is keyed by its `SUPPLY-0N` requirement ID and nested under the requirement it belongs to, never given an invented identifier (D-00f)"
    - "`PROMOTION.md` is this phase's last edit, in the six-step order its own Part A procedure prescribes, with the `Next free ADR number` line advanced from 0036 to 0037 only after `ls .planning/decisions/0036-*.md` confirms the file exists"
    - "Exactly one plan in this phase writes `PROMOTION.md`, and it runs in the final wave — so two concurrent writers cannot allocate the same ADR number, and the numbering index cannot be corrupted by a partial or interleaved write"
    - "`PROMOTION.md`'s Numbering index gains exactly one row, 0036, appended after 0035 with no existing row renumbered, reworded or reordered — proven by an ascending-order check across all 36 rows and a diff assertion over rows 0001 through 0035"
    - "`PROMOTION.md` Part B candidate 7 gains a `Closed 2026-08-09 by ADR-0036` note in the identical shape candidate 3's closure note uses, and no other candidate's entry is touched"
    - "`PROJECT.md`'s `## Key Decisions` table gains exactly one row for ADR-0036, linked in the table's established convention, with an Outcome cell matching the ADR's own `## Code Conformance` verdict"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "The Phase 13 / ORCH-01 hand-off block, three checkbox flips complete and three Complete traceability rows"
      contains: "Hand-off to Phase 13 / ORCH-01"
    - path: ".planning/decisions/PROMOTION.md"
      provides: "One Numbering-index row, a dated advance note, the Part B candidate 7 closure note, and the advanced next-free line"
      contains: "Next free ADR number"
    - path: ".planning/PROJECT.md"
      provides: "One `## Key Decisions` row for ADR-0036"
      contains: "0036-audit-suppression-single-source-topology"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/decisions/0036-audit-suppression-single-source-topology.md"
      via: "the hand-off block's `**Evidence:**` line cites ADR-0036 by path alongside the three gate transcripts and the guard"
      pattern: "0036-audit-suppression-single-source-topology"
    - from: ".planning/decisions/PROMOTION.md"
      to: ".planning/decisions/0036-audit-suppression-single-source-topology.md"
      via: "a Numbering-index row plus a dated note whose `ls` check confirms the file exists before the next-free line advances"
      pattern: "0036"
    - from: ".planning/PROJECT.md"
      to: ".planning/decisions/0036-audit-suppression-single-source-topology.md"
      via: "a `## Key Decisions` row linking to the ADR file, per promotion-procedure step 6"
      pattern: "0036-audit-suppression-single-source-topology"
  prohibitions:
    - "Do not create `.planning/ledgers/milestone-09-12.md`, or a stub, or a placeholder heading for it. Its 120-requirement-ID scope is ORCH-01 / Phase 13's stated deliverable; creating anything here would either be re-planned by Phase 13 or silently constrain its shape (D-09)."
    - "Do not advance `PROMOTION.md`'s `Next free ADR number` line before `ls .planning/decisions/0036-*.md` confirms the ADR file exists. Numbers are never reused even if a candidate is later declined; advancing early grants a number to an artefact that was never written."
    - "Do not renumber, reword or reorder any existing `PROMOTION.md` Numbering-index row, and do not add a closure note to any Part B candidate other than 7. Candidates 4, 6, 8, 9, 10 and 11 keep their owning phases and their entries unchanged."
    - "Do not mark SUPPLY-01's CI-run observation closed. Its traceability cell may read `Complete` only because the pending record lives in SUPPLY-01's own block and is repeated in this hand-off; a closure claim without a `gh run` citation newer than `30861568499` is a false positive (D-07)."
    - "Do not apply the committed GitHub rulesets or change any live repository setting. The hand-off records the finding and names its owner; it acts on nothing (D-10)."
    - "Do not delete or rewrite any text corrected by plans 12-01 or 12-03. Every banner those plans wrote, and every original beside it, must still be readable after this plan."
    - "Do not modify any `.rs` file, and do not touch `deny.toml`, `.cargo/audit.toml`, `SECURITY-EXCEPTIONS.md` or `.planning/decisions/0024-rustsec-exception-governance.md`."
---

<objective>
Close the phase: write the outbound hand-off to Phase 13 / ORCH-01, flip the last requirement row, add
the `## Key Decisions` row, and advance the ADR numbering index — in that order, with `PROMOTION.md`
last.

Purpose: this phase's evidence is only findable if the record that owns the `REQ-*` keys points at it,
and the ADR numbering line is shared state every later ADR-writing phase reads before it writes. The
promotion procedure makes advancing that line the final step, after the ADR file exists and has been
counted. Phase 12 is also the phase that gets to date the second half of the corpus's only "100%
complete and one acceptance criterion false" verdict — ORCH-01 must carry both halves.

Output: one `#### Hand-off to Phase 13 / ORCH-01` block, three closed requirement rows, one
`## Key Decisions` row, and `PROMOTION.md` carrying a 0036 index row, a dated advance note, candidate
7's closure note and `Next free ADR number: 0037`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-supply-chain-gate-integrity/12-CONTEXT.md
@.planning/phases/12-supply-chain-gate-integrity/12-RESEARCH.md
@.planning/phases/12-supply-chain-gate-integrity/12-PATTERNS.md
@.planning/phases/12-supply-chain-gate-integrity/12-VALIDATION.md
</context>

## Artifacts this phase produces

Every symbol below is created by Phase 12 and does not exist in the tree before it. A drift check that
flags one of these as an unknown reference is reading a not-yet-written artifact, not a defect.

| Artifact | Plan | Kind |
|---|---|---|
| `scripts/check-workflow-suppressions.sh` | 12-02 | new file — the D-08 regression guard |
| `WORKFLOWS_DIR`, `CARGO_GATE_RE`, `IGNORE_FLAG_RE`, `violates()`, `CLAUSE1_INLINE_SUPPRESSION`, `CLAUSE2_AUDIT_INVOCATION_COUNT` | 12-02 | new identifiers inside that script |
| `check-workflow-suppressions` | 12-02 | new `Makefile` target, added to the `check-gates` prerequisite list |
| `Check workflow files for inline advisory suppressions` | 12-02 | new `ci.yml` step name in the `cargo-deny:` job |
| `.planning/decisions/0036-audit-suppression-single-source-topology.md` | 12-03 | new ADR |
| `#### Hand-off to Phase 13 / ORCH-01 — dated 2026-08-09 (plan 12-04)` | **12-04** | new `REQUIREMENTS.md` heading |
| `PROMOTION.md` index row `0036`, its dated note, `**Next free ADR number: 0037**`, Part B candidate 7 closure note | **12-04** | new rows/lines in an existing record |
| `PROJECT.md` `## Key Decisions` row for ADR-0036 | **12-04** | new row in an existing table |

**No `.rs` file appears on this list, and none may be added to it.**

## Executor notes

- `.planning/config.json` sets `worktree_skip_hooks: true`, and both Rust pre-commit hooks carry
  `always_run: true`. This plan touches only `.planning/` markdown — commit with `--no-verify`.
- **Ordering is load-bearing.** Task 1 before task 2, and within task 2 the `Next free ADR number` line
  is the final edit of the task, the plan and the phase.

## `PROMOTION.md` contention

This plan is the only Phase-12 plan that writes `.planning/decisions/PROMOTION.md`, and it runs in the
final wave. That single-writer, final-wave rule is what makes the ADR-number allocation safe: two plans
running concurrently could otherwise both read `0036` from the numbering line and both claim it, and the
file offers no locking of any kind. Plan 12-03 authored ADR-0036 without touching this file precisely so
the allocation and the write could not interleave.

## ⚠ HUMAN REVIEW flags carried into this plan

All three of this phase's flagged decisions converge here, because this is where they become part of the
permanent record:

- **D-01** — SUPPLY-03's own text says it does not act; this plan closes its checkbox on an ADR it
  wrote. Plan 12-03's four correction banners are the audit trail for that override.
- **D-08** — the guard ADR-0036 cites as its enforcement point is a CI check no requirement asked for.
- **D-00l** — the re-scope that made SUPPLY-01 and SUPPLY-02 verification rather than work is Phase 9's
  unratified `--auto` D-07. The hand-off must say so: Phase 13 should inherit the closure **and** the
  provenance, not a bare `Complete`.

All three were gated by the blocking checkpoint at the end of plan 12-01. Confirm that checkpoint
selected option-a by reading `12-01-SUMMARY.md` before starting.

## Flagged assumption carried into this plan

**This plan's `one-way` reversibility gate is satisfied across plans, by design.**
`verify plan-structure` warns that Task 2 carries `rating="one-way"` with no `checkpoint:decision`
earlier in *this* file. That is intentional and matches the precedent plan 11-05 set: the ADR allocation
this task records was gated once, by the blocking checkpoint at the end of plan 12-01 in wave 1, and by
the time this plan runs the number is already spent — ADR-0036 exists on disk, written by plan 12-03.
Re-asking here would gate a door already walked through, which the reversibility taxonomy names as an
anti-pattern. The rating stays `one-way` because advancing the numbering line is genuinely irreversible;
only the gate's location moved.

<tasks>

<task type="auto">
  <name>Task 1: Write the Phase 13 / ORCH-01 hand-off block and close the three requirement rows</name>
  <precondition>Plans 12-01, 12-02 and 12-03 are complete and their SUMMARYs exist. `ls .planning/decisions/0036-*.md` succeeds and `ls .planning/ledgers/milestone-09-12.md` fails — the ledger this hand-off points at must not exist, since creating it is what D-09 forbids.</precondition>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 1790-1825 — the `#### Hand-off to Phase 12 / SUPPLY-02 and SUPPLY-03 — dated 2026-08-08 (plan 10-11)` block Phase 10 wrote *for* this phase. This is the exact four-part shape to imitate: dated heading naming the receiving phase and requirement IDs and the authoring plan; a bold one-sentence lead stating what is inherited and in what posture; a **numbered** list, each item opening with a bold short label and citing exact `file:line`; and a closing `**Evidence:**` paragraph that is a semicolon-separated citation list with no additional prose.
    - `.planning/REQUIREMENTS.md` lines 1943-1959 — the `#### Hand-off to Phase 13 / ORCH-05` block, for a second worked example and for where in the file a Phase-13-bound hand-off sits.
    - `.planning/REQUIREMENTS.md` lines 1833-1941 as plans 12-01 and 12-03 left them — SUPPLY-01's and SUPPLY-02's verification records with their three gate transcripts, the pending CI-run clause with its trigger and run-ID boundary, the rulesets finding, and SUPPLY-03's corrected block. The hand-off cites these; it does not restate their transcripts.
    - `.planning/REQUIREMENTS.md` lines 4004-4010 — the traceability table, and `FACADE-04`'s `Complete` row as the exact cell form. SUPPLY-01 and SUPPLY-02 already read `Complete` after plan 12-01; only SUPPLY-03's row moves here.
    - `.planning/ROADMAP.md` §Phase 13, criterion 1 and criterion 2 — ORCH-01's own words: the 120 Milestone 9-12 requirement IDs each needing a `file:line`-cited verdict, and "asking 'is Milestone 10 done?' returns the honest answer rather than the checkbox answer … **and one of its own acceptance criteria is false** — a verdict class this corpus had not produced before, and the reason Phase 12 exists."
    - `.planning/phases/12-supply-chain-gate-integrity/12-CONTEXT.md` → D-07, D-09, D-10, D-00l.
    - `12-01-SUMMARY.md`, `12-02-SUMMARY.md` and `12-03-SUMMARY.md` — the actual artefacts, transcripts and choices this block must cite accurately rather than paraphrase.
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
**(1) Write the hand-off block.** Place `#### Hand-off to Phase 13 / ORCH-01 — dated 2026-08-09 (plan 12-04)`
in the SUPPLY section, after SUPPLY-03's block and before the existing Phase 13 / ORCH-05 hand-off, so
the two Phase-13-bound blocks sit together. Use the four-part shape exactly.

The bold lead sentence states the posture in one line: ORCH-01 inherits all three SUPPLY requirements
closed with dated `file:line`-cited verdicts, delivered rather than deferred, plus one observation that
is honestly pending and one finding that is honestly owner-only.

Numbered items, each with a bold short label, each citing exact `file:line`:

  1. **The three SUPPLY verdicts and where their evidence lives.** SUPPLY-01 and SUPPLY-02 closed by
     Phase 9 and verified in-repo by plan 12-01 with three re-run gate transcripts; SUPPLY-03 closed by
     ADR-0036. Cite the ADR by path, the guard by path, and the requirement-block line ranges holding
     the transcripts. Do not repeat the transcripts here — point at them.
  2. **ORCH-01's named verdict class, both halves, verbatim.** Milestone 10 is recorded 100% complete,
     ships every artefact it promised, **and failed one of its own acceptance criteria** — and, as of
     2026-08-08, no longer does. The criterion is M10 Epic 2 §8's "`audit.toml` and `deny.toml` are the
     only places policy/exceptions are defined; no inline advisory-ignore flags remain in CI". Phase 9
     made it true; Phase 12 promoted it to an ADR and put a guard behind it. **ORCH-01 must carry both
     halves** — the corpus's only verdict of this class, and Phase 12 is the phase that can date the
     second half.
  3. **What Phase 12 deliberately did not build, and why.** `.planning/ledgers/milestone-09-12.md` does
     not exist and Phase 12 did not create it, not even as a stub. Its 120-requirement-ID scope is
     ORCH-01's own stated deliverable, and a stub here would either be re-planned or would silently
     constrain its shape. Phase 12 wrote its evidence where every prior phase wrote pre-ledger evidence:
     the requirement rows themselves plus this block.
  4. **One observation genuinely pending, with its trigger.** SUPPLY-01's clause "confirming the required
     status check still resolves on the first real CI run after the deletion" has not failed — it has
     never had the opportunity to fire. Cite the run-ID boundary and its timestamp, and the deletion
     date five days later. Trigger: the next push to `release/v0.7.0`. State plainly that a verifier
     marking this done without a `gh run` citation newer than that boundary is reporting a false
     positive. Note that in that last pre-deletion run the only failing job was `API Surface Tracking` —
     DEBT-01's, Phase 8's — while `Security Audit` passed.
  5. **The unapplied GitHub rulesets, a finding with an owner.** `.github/rulesets/` is version-controlled
     but the rulesets are not applied and `main` is not protected, so SUPPLY-01's "required status check"
     clause currently has no live enforcement point. Owner: the milestone close-out. Owner-only, since
     only the repository owner can act. Phase 12 applied nothing. Cite plan 12-01's two `gh api`
     transcripts by their location in SUPPLY-01's block.
  6. **The provenance of the re-scope, carried forward rather than laundered.** Phase 12 treated
     SUPPLY-01 and SUPPLY-02 as verification rather than work because Phase 9's D-07 re-scoped this
     phase — an `--auto` decision flagged "⚠ HUMAN REVIEW — this changes another phase's scope" and never
     ratified. It is durable because it is recorded at source in `ROADMAP.md` §Phase 12's dated closure
     note and `REQUIREMENTS.md:4085`, not because a human approved it. The three re-run gate transcripts
     are strong independent evidence regardless, but ORCH-01 should record the closure **and** its
     provenance rather than a bare `Complete`. Name D-01 and D-08 as this phase's own two unratified
     decisions in the same clause.
  7. **A measurement that contradicts two of this phase's own upstream artefacts.** The stale
     `ci.yml:389-406` citation appears far more widely than either `12-CONTEXT.md` ("three documents")
     or `12-RESEARCH.md` ("one") recorded. Give plan 12-01's measured hit count and file count, the
     scoping rule it applied, and the excluded classes with their reasons — so ORCH-01 inherits a
     measured inventory rather than re-deriving it, and knows which sites are deliberately left as
     historical record.

Close with a single `**Evidence:**` paragraph: a semicolon-separated citation list naming ADR-0036 by
path, `scripts/check-workflow-suppressions.sh`, the `Makefile` and `ci.yml` wiring points, the three
gate commands, the `gh run` and `gh api` invocations, and the SUPPLY-01/02/03 requirement-block ranges.
No additional prose after it.

**(2) Close SUPPLY-03.** Flip its leading `- [ ]` to `- [x]` and change its traceability `Status` cell
from `Pending` to `Complete`, copying `FACADE-04`'s cell form. Confirm SUPPLY-01's and SUPPLY-02's rows
still read `Complete` from plan 12-01 and that no other row in the table moved.
  </action>
  <verify>
    <automated>set -e; R=.planning/REQUIREMENTS.md; ls .planning/ledgers/milestone-09-12.md >/dev/null 2>&1 && { echo "FAIL: the M9-12 ledger must not exist (D-09)"; exit 1; }; grep -q '^#### Hand-off to Phase 13 / ORCH-01 — dated 2026-08-09 (plan 12-04)$' "$R" || { echo "FAIL: hand-off heading missing or malformed"; exit 1; }; grep -qF '**Evidence:**' "$R" || { echo "FAIL: Evidence line missing"; exit 1; }; grep -qF '0036-audit-suppression-single-source-topology' "$R" || { echo "FAIL: hand-off does not cite ADR-0036"; exit 1; }; grep -qF 'check-workflow-suppressions' "$R" || { echo "FAIL: hand-off does not cite the guard"; exit 1; }; grep -qF '30861568499' "$R" || { echo "FAIL: run-ID boundary missing"; exit 1; }; grep -qF 'milestone-09-12' "$R" || { echo "FAIL: hand-off does not state the ledger was not built"; exit 1; }; for r in SUPPLY-01 SUPPLY-02 SUPPLY-03; do grep -q "^| $r | Phase 12 | Complete |" "$R" || { echo "FAIL: $r row not Complete"; exit 1; }; done; C=$(grep -c '^- \[x\] \*\*SUPPLY-0[123]\*\*' "$R"); [ "$C" = 3 ] || { echo "FAIL: $C of 3 SUPPLY checkboxes closed"; exit 1; }; P=$(grep -c '| Phase 12 | Pending |' "$R"); [ "$P" = 0 ] || { echo "FAIL: $P Phase 12 rows still Pending"; exit 1; }; echo "hand-off written, 3 requirements closed, no M9-12 ledger created"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^#### Hand-off to Phase 13 / ORCH-01 — dated 2026-08-09 (plan 12-04)$' .planning/REQUIREMENTS.md` succeeds, and the block sits in the SUPPLY section adjacent to the existing Phase 13 / ORCH-05 hand-off.
    - The block carries all four shape elements: the dated heading, a bold one-sentence lead, a **numbered** list (not bulleted) of at least seven items each opening with a bold label, and one closing `**Evidence:**` paragraph with no prose after it.
    - Item 2 carries ORCH-01's verdict class in both halves — the phrase recording that Milestone 10 failed one of its own acceptance criteria **and** the phrase recording that as of 2026-08-08 it no longer does — and names the M10 Epic 2 §8 criterion.
    - Item 3 states that `.planning/ledgers/milestone-09-12.md` was not created and names ORCH-01 as its owner; `ls .planning/ledgers/milestone-09-12.md` fails, and `ls .planning/ledgers/` returns exactly the four pre-existing files.
    - Item 4 records the CI-run observation as pending, names the trigger `next push to release/v0.7.0`, cites run `30861568499` with its timestamp, and states that a closure claim without a newer `gh run` citation is a false positive.
    - Item 5 names the milestone close-out as the rulesets finding's owner and states that Phase 12 applied nothing; `git status --porcelain -- .github/` is empty.
    - Item 6 names Phase 9's D-07 as unratified and cites where the re-scope is recorded at source, and names D-01 and D-08 as this phase's own two unratified decisions.
    - Item 7 gives plan 12-01's measured `389-406` hit and file counts and the scoping rule, and states that the measurement agrees with neither upstream artefact.
    - The `**Evidence:**` line cites ADR-0036 by path, the guard script by path, and the three gate commands; `grep -qF '0036-audit-suppression-single-source-topology'` and `grep -qF 'check-workflow-suppressions'` both succeed.
    - All three SUPPLY checkboxes read `- [x]` and all three traceability rows read `Complete`; `grep -c '| Phase 12 | Pending |'` returns `0`, and every non-SUPPLY row in the traceability table is byte-identical (`git diff -U0` shows hunks confined to the three SUPPLY rows).
    - Every banner plans 12-01 and 12-03 wrote is intact: `grep -c 'Corrected by Phase 12 (plan 12-01)'` and `grep -c 'Corrected by Phase 12 (plan 12-03)'` are unchanged from their prior values, and `--numstat` deletions for this file are confined to the checkbox and traceability-cell lines.
    - `git diff --name-only <base>..HEAD -- '*.rs' | wc -l` returns `0`.
  </acceptance_criteria>
  <done>The hand-off block exists in the established four-part shape carrying ORCH-01's verdict class in both halves, the not-built ledger, the pending observation with its trigger and boundary, the owner-only rulesets finding, the unratified re-scope provenance and the measured stale-citation inventory; all three SUPPLY requirements are closed with no other row moved.</done>
</task>

<task type="auto">
  <name>Task 2: Advance the ADR numbering index and add the Key Decisions row — the phase's last act</name>
  <reversibility rating="one-way">`PROMOTION.md` §Part A step 1 states that ADR numbers are never reused, even if a candidate is later rejected instead of accepted; advancing the next-free line spends 0036 permanently and a later phase that disagrees with ADR-0036 must supersede it rather than reclaim its number. The gate for this decision fired at the end of plan 12-01, in wave 1 — by the time this task runs the number is already spent and the ADR file already exists, so re-asking here would gate a door already walked through. The rating stays `one-way` because the act is genuinely irreversible; only the gate's location moved.</reversibility>
  <precondition>`ls .planning/decisions/0036-*.md` succeeds — the ADR file exists on disk. Record its output; the dated note quotes it as proof. Read `12-03-SUMMARY.md` for the number and slug actually taken rather than assuming them.</precondition>
  <read_first>
    - `.planning/decisions/PROMOTION.md` in full. The Numbering index table's exact `| Number | Slug | Subject |` row shape and its 35 existing rows at lines 21-57; the `**Next free ADR number: 0036**` line at line 59; the five existing dated notes at lines 61-102 and their voice, in particular the most recent (plan 11-05) at `:61-69` whose shape this task copies — it names the advance size, the authoring phase and plan, the verdict class of each ADR, quotes an `ls` re-verification command, and states explicitly whether any Part B inventory entry was closed. The required heading set at lines 107-128. The supersession mechanism at lines 130-140. The six-step promotion procedure at lines 149-170, with step 5 (updating this line) performed last and step 6 (the `PROJECT.md` row) performed before it. Part B candidate 3's closure note at lines 185-189, the exact shape candidate 7's note must copy. Part B candidate 7's entry at lines 203-205 with its `**Owner phase: Phase 12.**` marker.
    - `.planning/PROJECT.md` lines 1103-1141 — the `## Key Decisions` table's `| Decision | Rationale | Outcome |` column shape, its `[title](.planning/decisions/NNNN-slug.md) (ADR-NNNN)` first-column link convention, and the Outcome column's vocabulary (`conforms`, `must change — …`, `✓ Good — applied in Phase N`). Read the ADR-0034 and ADR-0035 rows at the table's end as the two most recent examples.
    - `.planning/decisions/0036-audit-suppression-single-source-topology.md` — its H1 title line and its `## Code Conformance` verdict, so the index Subject cell and the Key Decisions Outcome cell state what the ADR actually says rather than what this plan predicted it would.
    - `.planning/phases/12-supply-chain-gate-integrity/12-PATTERNS.md` § `.planning/decisions/PROMOTION.md` — the dated-note and closure-note shapes verbatim.
  </read_first>
  <files>.planning/decisions/PROMOTION.md, .planning/PROJECT.md</files>
  <action>
Run `ls .planning/decisions/0036-*.md` first and record its output verbatim — the dated note quotes it
as proof the file exists with a contiguous, unreused number. Then follow the six-step procedure, with
step 6 before step 5 and step 5 last.

**(1) Add one row to the Numbering index table**, appended immediately after the `| 0035 | …` row, in
the identical `| Number | Slug | Subject |` shape: number `0036`, slug
`audit-suppression-single-source-topology`, and a Subject cell naming the audit-suppression
single-source topology invariant promoted out of PRD precedence, with `(Phase 12)` in the trailing
position every other row uses. **Never re-sort, renumber, reword or reorder any existing row.**

**(2) Add a dated note** beneath the next-free line, **above** the existing plan 11-05 note, since the
notes run reverse-chronologically. Match their voice. It states: the line advances by **one**, from 0036
to 0037, because Phase 12 authored ADR-0036 (plan 12-03, the audit-suppression single-source topology
invariant); the ADR carries a `conforms` verdict, so like Phase 11's two it instructs no code change —
this phase changed zero executable Rust; the recorded `ls` output confirms the file exists with a
contiguous number, none skipped or reused, and no existing index row was renumbered, reworded or
reordered. Add two sentences the prior notes' shape calls for: this is the first advancing note since
Phase 7's to cover exactly **one** ADR rather than a multi-ADR batch; and — unlike Phase 11's note,
which stated that neither of its ADRs closed an inventory entry — **this ADR does close a Part B
entry**, candidate 7.

**(3) Add the Part B candidate 7 closure note**, appended to that candidate's existing entry in the
identical shape candidate 3's note uses: `**Owner phase: Phase 12. Closed 2026-08-09 by ADR-0036**`
followed by the ADR's filename in backticks and one clause naming the verdict — that the invariant was
promoted with a `conforms` verdict because the tree already satisfies it, and that a regression guard
now enforces it. Keep the entry's existing `**Owner phase: Phase 12.**` assignment text rather than
rewriting it, exactly as candidate 5's note retains its original owner assignment while recording that
the promotion happened elsewhere. Touch no other candidate.

**(4) Add one row to `.planning/PROJECT.md`'s `## Key Decisions` table** — procedure step 6 — appended
after the ADR-0035 row in the table's own three-column shape and its established link convention. The
Rationale cell states the invariant in one sentence in its two-file framing, names ADR-0024 as the
sibling that owns suppression contents while this one owns topology, and names the guard as what makes
it a gate. The Outcome cell must match the ADR's own `## Code Conformance` verdict — read the file, do
not assume.

**(5) Update the `Next free ADR number` line to read 0037 — last, after every other edit in this task,
this plan and this phase.** This line is the corpus's record of flat sequential numbering: it read 0036
when the phase began, and it must leave the phase reading the first genuinely unused number, with no gap,
no reuse, and every number between the two accounted for by a file on disk carrying the seven required
headings and no frontmatter.
  </action>
  <verify>
    <automated>set -e; P=.planning/decisions/PROMOTION.md; J=.planning/PROJECT.md; ls .planning/decisions/0036-*.md >/dev/null 2>&1 || { echo "FAIL: ADR-0036 file missing"; exit 1; }; N=$(grep -c '^| 00' "$P"); [ "$N" = 36 ] || { echo "FAIL: index has $N rows, expected 36"; exit 1; }; awk -F'|' '/^\| 00/{gsub(/ /,"",$2); print $2}' "$P" | sort -c || { echo "FAIL: index not in ascending order"; exit 1; }; U=$(awk -F'|' '/^\| 00/{gsub(/ /,"",$2); print $2}' "$P" | sort -u | wc -l); [ "$U" = 36 ] || { echo "FAIL: $U unique numbers across 36 rows - a number is reused"; exit 1; }; L=$(grep -c 'Next free ADR number' "$P"); [ "$L" = 1 ] || { echo "FAIL: $L next-free lines, expected 1"; exit 1; }; grep -q 'Next free ADR number: 0037' "$P" || { echo "FAIL: next-free line not advanced to 0037"; exit 1; }; grep -q 'Dated note, 2026-08-09 (plan 12-04)' "$P" || { echo "FAIL: dated note missing"; exit 1; }; grep -q 'Closed 2026-08-09 by ADR-0036' "$P" || { echo "FAIL: Part B candidate 7 closure note missing"; exit 1; }; grep -qF '0036-audit-suppression-single-source-topology' "$J" || { echo "FAIL: Key Decisions row missing"; exit 1; }; K=$(sed -n '/^## Key Decisions/,/^## /p' "$J" | grep -c '^| \['); [ "$K" = 32 ] || { echo "FAIL: Key Decisions has $K rows, expected 32"; exit 1; }; echo "index 36 contiguous unique rows, next free 0037, candidate 7 closed, Key Decisions 32 rows"</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/decisions/0036-*.md` returns exactly one file and its name matches the slug written into the index row; the `ls` output is quoted verbatim inside the dated note.
    - `grep -c '^| 00' .planning/decisions/PROMOTION.md` returns `36` — one row added to the 35 that existed.
    - The index is ascending and contains no duplicate: `awk -F'|' '/^\| 00/{gsub(/ /,"",$2); print $2}' | sort -c` succeeds, and `| sort -u | wc -l` also returns `36`.
    - Every previously existing index row is byte-identical: `git diff` for this file shows no hunk modifying a `| 00` row numbered 0001 through 0035.
    - `grep -c 'Next free ADR number' .planning/decisions/PROMOTION.md` returns `1` and that line reads `**Next free ADR number: 0037**`.
    - `grep -c 'Dated note, 2026-08-09 (plan 12-04)' .planning/decisions/PROMOTION.md` returns `1`, and that note contains the `ls` output, the word `one` for the advance size, the `conforms` verdict, and the sentence recording that this ADR **does** close a Part B entry.
    - `grep -c 'Closed 2026-08-09 by ADR-0036' .planning/decisions/PROMOTION.md` returns `1`, sits inside Part B candidate 7's entry, and that entry's original `**Owner phase: Phase 12.**` text is retained rather than rewritten.
    - No other Part B candidate changed: `git diff -U0 .planning/decisions/PROMOTION.md` shows no hunk touching candidates 1-6 or 8-11.
    - `.planning/PROJECT.md`'s `## Key Decisions` table rises from 31 to 32 rows (`sed -n '/^## Key Decisions/,/^## /p' | grep -c '^| \['` returns `32`), the new row uses the table's `[title](.planning/decisions/NNNN-slug.md) (ADR-NNNN)` link convention, and its Outcome cell matches the string in ADR-0036's own `## Code Conformance` section.
    - The `Next free ADR number` edit is the final change in this plan's commit sequence: the commit advancing it contains no other Phase-12 artefact change.
    - Plan 12-03's corrections in `PROJECT.md` are intact: `grep -c 'Corrected by Phase 12 (plan 12-03)' .planning/PROJECT.md` is unchanged, and `--numstat` deletions for this file are `0`.
    - `git status --porcelain -- .planning/decisions/0024-rustsec-exception-governance.md .planning/decisions/0036-audit-suppression-single-source-topology.md deny.toml .cargo/audit.toml SECURITY-EXCEPTIONS.md` is empty, and `git diff --name-only <base>..HEAD -- '*.rs' | wc -l` returns `0`.
  </acceptance_criteria>
  <done>`PROMOTION.md` carries 36 contiguous ascending unique index rows, a dated plan-12-04 note quoting the `ls` proof and recording that candidate 7 is closed, candidate 7's closure note in candidate 3's shape with its original owner assignment retained, and the next-free line advanced to 0037 as the phase's final edit; `PROJECT.md`'s `## Key Decisions` table carries one new row whose Outcome matches ADR-0036's own verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase → `PROMOTION.md`'s shared numbering state | A single line every future ADR-writing phase reads and trusts without re-deriving; the file offers no locking of any kind |
| this phase's artefacts → the `REQ-*` closure record | An artefact not cited from a requirement row or a hand-off block is effectively unfindable by the phase that inherits it |
| a `Complete` traceability cell → a reader's conclusion | The cell vocabulary cannot express "complete with one pending observation"; the caveat has to be reachable or the cell overstates |
| Phase 12's boundary → Phase 13's deliverable | The M9-12 ledger is one directory entry away, and creating it here would silently constrain a 120-requirement deliverable that is not this phase's |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-25 | Tampering | `PROMOTION.md`'s numbering index | high | mitigate | Renumbering, reusing or skipping a number corrupts shared state for every later phase, and Part A step 1 forbids reuse outright. Mitigated by append-only editing, an `ls` proof quoted in the dated note, an ascending-order `sort -c` check plus a `sort -u` uniqueness check across all 36 rows, and a diff assertion that no row 0001-0035 was modified. |
| T-12-26 | Elevation of Privilege | the advanced next-free line | high | mitigate | Advancing the line before the ADR file exists would grant a permanent number to an artefact that was never written, and no later phase could reclaim it. Mitigated by the task `<precondition>` requiring `ls .planning/decisions/0036-*.md` to succeed first, and by making the advance the final edit of the task, the plan and the phase. |
| T-12-27 | Tampering | ADR-number allocation under concurrency | high | mitigate | Two plans reading `0036` from the numbering line concurrently would both claim it, and the file has no lock. Mitigated structurally: exactly one plan in this phase writes `PROMOTION.md`, it runs alone in the final wave, and plan 12-03 authored the ADR without touching the file so the read and the write cannot interleave. |
| T-12-28 | Spoofing | SUPPLY-01's `Complete` traceability cell | high | mitigate | A `Complete` cell whose pending observation is not reachable overstates the verdict, and the table's vocabulary cannot carry the caveat. Mitigated by requiring the pending record in SUPPLY-01's own block **and** repeating it as hand-off item 4 with its run-ID boundary and false-positive warning, and by carrying it as a Manual-Only row in `12-VALIDATION.md`. |
| T-12-29 | Elevation of Privilege | Phase 13's ledger deliverable | high | mitigate | Creating `.planning/ledgers/milestone-09-12.md` — even as a stub — would pre-empt a 120-requirement deliverable that belongs to ORCH-01. Mitigated by a prohibition, by a task precondition asserting the file does not exist, by a verify-block check that fails if it appears, and by hand-off item 3 recording the non-creation as a deliberate boundary. |
| T-12-30 | Repudiation | the hand-off block | medium | mitigate | A closure handed forward without provenance lets a later reader mistake an unratified `--auto` re-scope for a ratified decision. Mitigated by hand-off item 6, which names Phase 9's D-07 as unratified, cites where the re-scope is recorded at source, and names this phase's own D-01 and D-08 alongside it. |
| T-12-31 | Tampering | prior plans' correction banners | medium | mitigate | An edit to `REQUIREMENTS.md` or `PROJECT.md` that reflows a region could silently drop a banner plans 12-01 or 12-03 wrote. Mitigated by acceptance criteria asserting both banner counts are unchanged and that `--numstat` deletions are confined to the checkbox and traceability-cell lines. |
| T-12-32 | Information Disclosure | — | low | accept | Not applicable. All three files hold repository-local paths, requirement IDs, ADR numbers, commit SHAs and a workflow-run ID already present in the checkout. No secret or personal datum is read or written. |
| T-12-33 | Denial of Service | — | low | accept | Not applicable. No runtime surface is touched; execution is file reads, three in-place edits and a handful of greps. |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable, truthfully: this plan installs, upgrades and removes nothing. Its three files are `.planning/` markdown, so no manifest or lockfile is in reach; the `git status --porcelain` and `git diff --name-only` criteria on both tasks prove it. |
</threat_model>

<verification>
- `#### Hand-off to Phase 13 / ORCH-01 — dated 2026-08-09 (plan 12-04)` exists in the four-part shape with at least seven numbered items and one closing `**Evidence:**` line.
- All three SUPPLY requirements are `- [x]` with `Complete` traceability rows; `grep -c '| Phase 12 | Pending |'` returns `0`.
- `.planning/ledgers/milestone-09-12.md` does not exist, and `.planning/ledgers/` holds exactly its four pre-existing files.
- `PROMOTION.md` holds 36 ascending unique index rows, a dated plan-12-04 note quoting the `ls` proof, candidate 7's closure note, and `Next free ADR number: 0037` written last.
- `PROJECT.md`'s `## Key Decisions` table rises from 31 to 32 rows with an Outcome cell matching ADR-0036's own verdict.
- Every banner written by plans 12-01 and 12-03 is still present.
- No `.rs` file is modified, and none of ADR-0024, ADR-0036, `deny.toml`, `.cargo/audit.toml` or `SECURITY-EXCEPTIONS.md` is touched.
</verification>

<success_criteria>
- Every artefact this phase produced is reachable from a requirement row or the hand-off block by path.
- Phase 13 / ORCH-01 inherits the closure, its evidence, its one honestly pending observation, its one owner-only finding, and the provenance of the re-scope it rests on — not a bare `Complete`.
- The corpus's only "100% complete and one acceptance criterion false" verdict gets its second half dated.
- The ADR numbering line is advanced exactly once, last, with a note explaining the advance rather than a silent renumber, and candidate 7's owning phase closes it.
- Phase 12 ends having changed zero executable Rust — by measurement, not by assertion.
</success_criteria>

<output>
Create `.planning/phases/12-supply-chain-gate-integrity/12-04-SUMMARY.md` when done. Record in it: the
`ls .planning/decisions/0036-*.md` output, the index-row count before and after, the Key Decisions row
count before and after, and confirmation that the next-free line was the final edit of the phase.
</output>
