---
phase: 13-milestone-9-12-ground-truth-recorded-account
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - .planning/ledgers/milestone-09-12.md
autonomous: true
requirements: [ORCH-01]
must_haves:
  truths:
    - "All 23 rows in Milestone 10 Epics 1-5 carry a verdict from the eleven-class key with a `file:line` re-read this session; none reads `Verify` and none reads `pending — plan 13-03`"
    - "The Milestone 10 Epic 2 row for the SUPPLY-01 criterion carries **both** halves — the criterion was false, and as of 2026-08-08 it is not — with commit `cb75b2b` and ADR-0036 cited (D-05)"
    - "The three SUPPLY closures are cited with their provenance rather than re-verified, including that Phase 9's D-07 re-scope was `--auto` and never ratified and that Phase 12's D-01/D-08 were human-selected at plan 12-01's checkpoint on 2026-08-09 (D-06, D-00i)"
    - "SUPPLY-01's previously-pending trigger clause is recorded resolved against a `gh run` newer than `30861568499`, re-listed at write time, with the run id and its `Security Audit` conclusion recorded verbatim"
    - "`REQ-github-rulesets` records the version-controlled-but-unapplied ruleset and unprotected `main` as owner-only, and this phase applies nothing (D-06)"
  artifacts:
    - .planning/ledgers/milestone-09-12.md
  key_links:
    - "Milestone 10 Epic 2 rows -> `.planning/decisions/0036-audit-suppression-single-source-topology.md` and `scripts/check-workflow-suppressions.sh`"
    - "`REQ-m10-v040-release` -> ADR-0029's `v0.4.0` trajectory row appended by plan 13-12 (D-16)"
  prohibitions:
    - "No `.rs` file is modified by this plan (D-19)"
    - "No row outside Milestone 10 Epics 1-5 is touched (D-23)"
    - "The three SUPPLY requirements are not re-opened or re-verified; they are cited (D-06)"
    - "Phase 12's 87-hit stale-citation grep is not re-run and its deliberately excluded classes are not `fixed` (D-07)"
    - "No `Shipped` verdict without a `file:line` plus an exerciser (D-03)"
    - "No bare `Verify` survives (D-02); no row inserted, deleted or reordered (D-00d)"
    - "No item from 13-CONTEXT.md's `<deferred>` block is planned or actioned here"
---

<objective>
Derive real, cited verdicts for the 23 requirement IDs in Milestone 10 Epics 1-5, and write the
corpus's only `Shipped, one acceptance criterion false` row with both of its halves dated.

Purpose: Milestone 10 is why Phase 12 exists. Asking "is Milestone 10 done?" must return the honest
answer — every deliverable ships, the count is 100%, one of its own acceptance criteria was false,
and as of 2026-08-08 it no longer is.

Output: 23 Verdict cells replaced in place in `.planning/ledgers/milestone-09-12.md`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-CONTEXT.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive Milestone 10 Epics 1-2 (12 rows) with the both-halves verdict and the SUPPLY provenance</name>
  <files>.planning/ledgers/milestone-09-12.md</files>
  <read_first>
    - `.planning/ledgers/milestone-09-12.md` — the head notes, the `Shipped, one acceptance criterion false` highlight table plan 13-01 wrote, and the 12 rows this task owns.
    - `.planning/REQUIREMENTS.md:2084-2176` — Phase 12's hand-off block: the three SUPPLY closures, where their evidence lives, the both-halves verdict class verbatim, the pending trigger and the unapplied rulesets.
    - `.planning/decisions/0036-audit-suppression-single-source-topology.md` — the ADR that closed SUPPLY-03 and holds the criterion true.
    - `.planning/decisions/0024-rustsec-exception-governance.md` — Phase 9's exception register, cited by the Epic 2 rows.
    - `.planning/intel/requirements.md` — acceptance criteria for the bare-`Verify` IDs in this range.
  </read_first>
  <action>
Replace the Verdict cell of these 12 rows in place, in
`### Milestone 10 Epic 1 — Pre-Commit & Pre-Push Hooks (4 IDs)` and
`### Milestone 10 Epic 2 — Dependency Security & Licence Compliance (8 IDs)`.

Epic 1 — `REQ-pre-commit-framework`, `REQ-pre-commit-hook-set`, `REQ-pre-push-hook-set`,
`REQ-pre-commit-ci-gate`.

Epic 2 — `REQ-audit-toml-single-source`, `REQ-advisory-exception-process`,
`REQ-osv-scanner-supplementary`, `REQ-snyk-evaluation-decision`, `REQ-deny-license-allowlist`,
`REQ-deny-bans-duplicates`, `REQ-cyclonedx-sbom-release`, `REQ-security-docs-make-target`.

Most of Milestone 10 is structural, so the head note's manifest carve-out applies: a manifest line,
a workflow job from the corrected 15-job list, or a `Makefile` target, **plus a named consumer**
(D-03). Do not write "CI runs it".

`REQ-audit-toml-single-source` is the both-halves row (D-05). Its verdict class is
`Shipped, one acceptance criterion false`, and it must carry, dated:
the criterion — M10 Epic 2 §8's requirement that `audit.toml` and `deny.toml` are the only places
policy or exceptions are defined and that no inline advisory-ignore flags remain in CI;
the failure — a superseded duplicate `security:` job survived at pre-deletion `ci.yml:465-482`
running `cargo audit` with two inline ignores, so two jobs named `Security Audit` were configured to
reach different verdicts on the same tree;
the fix — Phase 9 plan 09-06, commit `cb75b2b`, dated 2026-08-08, and Phase 12's ADR-0036 plus
`scripts/check-workflow-suppressions.sh` wired into `Makefile:171-176` and
`.github/workflows/ci.yml:103-104` so it stays true.
Re-run `grep -n 'cargo audit --ignore' .github/workflows/ci.yml` and record that it returns nothing;
a row recording only the failure, or only the fix, is wrong.

Carry the D-06 provenance on the SUPPLY closures rather than re-verifying them (D-00i): SUPPLY-01
`REQUIREMENTS.md:1855-1940`, SUPPLY-02 `:1941-2046`, SUPPLY-03 `:2047-2082`. Record that Phase 9's
D-07 re-scoped Phase 12 from work to verification under `--auto`, was flagged `⚠ HUMAN REVIEW` and
was never itself ratified, and that Phase 12's own D-01 and D-08 were resolved when a human selected
`option-a` at plan 12-01's blocking checkpoint on 2026-08-09. The ledger records the closure **and**
how it was obtained.

Resolve SUPPLY-01's previously-pending trigger clause. 13-CONTEXT.md's D-06 records it as awaiting a
CI run newer than `30861568499` (2026-08-03); 13-RESEARCH.md found run `31320378772` (2026-08-09,
`success`, one non-duplicated `Security Audit` job). Both citations are timestamped, so re-run
`gh run list --workflow=ci.yml --limit 5 --branch release/v0.7.0` and
`gh run view <newest-id> --json jobs` at write time and record the newest run's id, date and
`Security Audit` conclusion verbatim. Write the resolution with that live citation — this supersedes
D-06's "pending" framing with evidence, which is the update the evidence bar welcomes. If `gh` cannot
reach the API, record the failure verbatim and scope the clause CI-only rather than inferring a pass
(D-21).

Do not re-run Phase 12's 87-hit stale-citation grep or "fix" its deliberately excluded classes
(D-07); cite `12-01-SUMMARY.md` §Grep Inventory instead.
  </action>
  <verify>
    <automated>awk '/^### Milestone 10 Epic 1/,/^### Milestone 10 Epic 3/' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^### Milestone 10 Epic 1/,/^### Milestone 10 Epic 3/' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'` → `12`
    - `awk '/^### Milestone 10 Epic 1/,/^### Milestone 10 Epic 3/' .planning/ledgers/milestone-09-12.md | grep -c 'pending — plan'` → `0`
    - `awk '/^### Milestone 10 Epic 1/,/^### Milestone 10 Epic 3/' .planning/ledgers/milestone-09-12.md | grep -c 'run-5 input (not yet re-derived)'` → `0`
    - `grep -c 'cb75b2b' .planning/ledgers/milestone-09-12.md` → `≥2` (highlight table plus the row)
    - `grep -c '2026-08-08' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c 'ADR-0036' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -cE 'run [0-9]{8,}|31320378772' .planning/ledgers/milestone-09-12.md` → `≥1` (a live `gh run` citation newer than `30861568499`)
    - `grep -c '12-01' .planning/ledgers/milestone-09-12.md` → `≥1` (the checkpoint provenance and the inherited inventory)
  </acceptance_criteria>
  <done>All 12 rows carry a cited verdict; `REQ-audit-toml-single-source` carries both halves dated; the SUPPLY provenance chain and the resolved SUPPLY-01 trigger are recorded with a live run id.</done>
</task>

<task type="auto">
  <name>Task 2: Derive Milestone 10 Epics 3-5 (11 rows)</name>
  <files>.planning/ledgers/milestone-09-12.md</files>
  <read_first>
    - `.planning/ledgers/milestone-09-12.md` — the rows this task owns and the Epic 1-2 rows task 1 just wrote.
    - `Makefile` — the `release`, `audit` and security targets the Epic 3 and Epic 5 rows cite.
    - `.github/workflows/release.yml` and `.github/workflows/ci.yml` — the tag-triggered publish pipeline and the tag-source guard.
    - `.planning/intel/requirements.md` — acceptance criteria for the bare-`Verify` IDs in this range.
  </read_first>
  <action>
Replace the Verdict cell of these 11 rows in place, in
`### Milestone 10 Epic 3 — Release Automation (6 IDs)`,
`### Milestone 10 Epic 4 — v0.4.0 Release (1 ID)` and
`### Milestone 10 Epic 5 — Tag-Source Enforcement (4 IDs)`.

Epic 3 — `REQ-release-tooling-selection`, `REQ-workspace-publish-order`, `REQ-lockstep-versioning`,
`REQ-tag-triggered-publish-pipeline`, `REQ-make-release-target`,
`REQ-contributing-add-dependency-guide`.

Epic 4 — `REQ-m10-v040-release`.

Epic 5 — `REQ-verify-tag-source-guard`, `REQ-make-release-branch-guard`, `REQ-github-rulesets`,
`REQ-branch-protection-doc`.

`REQ-lockstep-versioning` is the mechanism the whole ORCH-05 trajectory rests on: record the
`Makefile` target or script that bumps the root crate and every workspace member together and cuts a
tag, with its `file:line`, and note that plans 13-12's four ADR-0029 rows cite this row rather than
re-deriving a commit hash per tag.

`REQ-m10-v040-release` is the Milestone 10 lockstep gate; record the bump and the tag, and note that
plan 13-12 transcribes it as ADR-0029's `v0.4.0` row (D-16). Do not touch ADR-0029 here.

`REQ-github-rulesets` is owner-only (D-06). Record that `.github/rulesets/` is version-controlled and
**unapplied** and that `main` is unprotected — re-run `gh api repos/:owner/:repo/rulesets` and record
its output verbatim; if it returns `[]`, say so. Only the repository owner can change that, the
finding is recorded against the milestone close-out, and **this phase applies nothing**. If `gh`
cannot reach the API, record the failure and scope the claim rather than inferring.

Apply the head note's manifest carve-out and the D-03 evidence bar to the remaining rows: named job,
target or manifest line plus a named consumer; `Verified open` with its search command where nothing
is found.
  </action>
  <verify>
    <automated>awk '/^### Milestone 10 Epic 3/,/^### Milestone 11 Epics 1-2/' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^### Milestone 10 Epic 3/,/^### Milestone 11 Epics 1-2/' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'` → `11`
    - `awk '/^### Milestone 10 Epic 3/,/^### Milestone 11 Epics 1-2/' .planning/ledgers/milestone-09-12.md | grep -c 'pending — plan'` → `0`
    - `awk '/^### Milestone 10 Epic 3/,/^### Milestone 11 Epics 1-2/' .planning/ledgers/milestone-09-12.md | grep -c 'run-5 input (not yet re-derived)'` → `0`
    - `grep -c 'rulesets' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c 'REQ-lockstep-versioning' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md` → `120`
    - `git diff --numstat -- .planning/ledgers/milestone-09-12.md | awk '{print ($2==$3)?"balanced":"unbalanced"}'` → `balanced` (in-place cell replacement, no rows added or removed)
  </acceptance_criteria>
  <done>All 11 rows in Milestone 10 Epics 3-5 carry a cited verdict, the ruleset finding is recorded as owner-only with nothing applied, and the ledger still holds exactly 120 rows.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Modified by this plan (no new files):
- `.planning/ledgers/milestone-09-12.md` — Verdict cells for 23 IDs

Identifiers a later reader will grep for, finalized here:
- `REQ-pre-commit-framework`, `REQ-pre-commit-hook-set`, `REQ-pre-push-hook-set`, `REQ-pre-commit-ci-gate`
- `REQ-audit-toml-single-source` (the single `Shipped, one acceptance criterion false` row), `REQ-advisory-exception-process`, `REQ-osv-scanner-supplementary`, `REQ-snyk-evaluation-decision`, `REQ-deny-license-allowlist`, `REQ-deny-bans-duplicates`, `REQ-cyclonedx-sbom-release`, `REQ-security-docs-make-target`
- `REQ-release-tooling-selection`, `REQ-workspace-publish-order`, `REQ-lockstep-versioning`, `REQ-tag-triggered-publish-pipeline`, `REQ-make-release-target`, `REQ-contributing-add-dependency-guide`
- `REQ-m10-v040-release`
- `REQ-verify-tag-source-guard`, `REQ-make-release-branch-guard`, `REQ-github-rulesets`, `REQ-branch-protection-doc`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub API (`gh run`, `gh api`) -> ledger | remote, time-varying state crosses into a dated claim |
| prior-phase records -> ledger | Phase 9's and Phase 12's closures cross in by citation rather than re-verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | the both-halves verdict | high | mitigate | Both halves are required by acceptance criteria: `cb75b2b` and `2026-08-08` must both appear, and `grep -n 'cargo audit --ignore' .github/workflows/ci.yml` is re-run to prove the deletion held |
| T-13-07 | Spoofing | the SUPPLY-01 trigger citation | high | mitigate | `gh run list` is re-run at write time rather than transcribing `31320378772` from research; the recorded id must be newer than `30861568499`, and a `gh` failure is recorded verbatim and scoped CI-only, never inferred as a pass (D-21) |
| T-13-03 | Repudiation | `--auto` provenance laundering | medium | mitigate | D-00i: the row records that Phase 9's D-07 was never ratified and that Phase 12's D-01/D-08 were human-selected at 12-01's checkpoint on 2026-08-09 |
| T-13-08 | Tampering | Phase 12's deliberately excluded stale-citation sites | medium | mitigate | D-07: the grep is not re-run; `12-01-SUMMARY.md` §Grep Inventory is cited as the authority for which sites are historical record |
| T-13-09 | Elevation of privilege | live GitHub repository administration | high | mitigate | `REQ-github-rulesets` is read-only: `gh api` reads are permitted, no ruleset is applied and no branch protection is changed by this phase |
| T-13-SC | Tampering | package installs | low | accept | N/A — no package install, no manifest change (D-19) |
</threat_model>

<verification>
- `awk '/^### Milestone 10 Epic 1/,/^### Milestone 11 Epics 1-2/' .planning/ledgers/milestone-09-12.md | grep -c 'pending — plan'` → `0`
- `grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md` → `120`
- `git diff --name-only -- '*.rs' | wc -l` → `0`
</verification>

<success_criteria>
Milestone 10's 23 rows are derived at the D-00e bar, the corpus's only both-halves verdict is written
once with both halves dated, and SUPPLY-01's last pending clause is closed with a live `gh run`
citation.
</success_criteria>

<output>
Create `.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-03-SUMMARY.md` when done.
</output>
