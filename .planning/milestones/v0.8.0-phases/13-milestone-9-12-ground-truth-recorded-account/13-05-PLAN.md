---
phase: 13-milestone-9-12-ground-truth-recorded-account
plan: 05
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - .planning/ledgers/milestone-09-12.md
autonomous: true
requirements: [ORCH-01]
must_haves:
  truths:
    - "All 19 rows in Milestone 12 Epics 1-4 carry a verdict from the eleven-class key with a `file:line` re-read this session; none reads `Verify` and none reads `pending — plan 13-05`"
    - "`REQ-agent-execute-endpoint` records the shipped route surface as `/v1`-prefixed, confirmed against the committed `crates/paladin-web/openapi.json`, with the Epic 1 unprefixed route text marked superseded provenance and ADR-0037 named as the recorded answer (D-11)"
    - "The Milestone 12 Epic 1 row whose requirement text names the undotted `project/current-exports.txt` records the `scripts/check-api-surface.sh:6` finding — the defect is closed in the script and open only in requirement text — and hands it to Phase 15 (D-09)"
    - "`REQ-agent-provisioner-port`, `REQ-concrete-agent-provisioner` and `REQ-registry-from-config-builder` point at ADR-0038 and ADR-0039 as the recorded answers without stating either answer, which is gated on plan 13-09's blocking human checkpoint"
  artifacts:
    - .planning/ledgers/milestone-09-12.md
  key_links:
    - "`REQ-agent-execute-endpoint` -> `crates/paladin-web/openapi.json` and ADR-0037 (plan 13-08)"
    - "`REQ-agent-provisioner-port` -> ADR-0038 (plan 13-09)"
    - "the Epic 1 stale-path row -> `scripts/check-api-surface.sh:6` and Phase 15 / PIPE-01"
  prohibitions:
    - "No `.rs` file is modified by this plan (D-19)"
    - "No row outside Milestone 12 Epics 1-4 is touched (D-23)"
    - "ORCH-03 does not grow to a sixth item: the four Milestone 12 `project/current-exports.txt` references stay ledger rows (D-09)"
    - "No `.project/Milestone_12-Web-API/Epic_{1,5,6,7}` file is edited for the `current-exports.txt` defect; Phase 8 already annotated them on 2026-08-06"
    - "No `Shipped` verdict without a `file:line` plus an exerciser (D-03); no bare `Verify` survives (D-02)"
    - "The ADR-0038 and ADR-0039 answers are not stated here; they are gated on a blocking human checkpoint (D-14, D-15, D-23)"
    - "No item from 13-CONTEXT.md's `<deferred>` block is planned or actioned here"
---

<objective>
Derive real, cited verdicts for the 19 requirement IDs in Milestone 12 Epics 1-4 — the agent registry
and execution API, the configurable host and server binary, streaming and async jobs, and operational
hardening.

Purpose: the ledger must state plainly that the whole Milestone 12 web API ships, and it must record
where its route text is superseded provenance rather than a live contract, so nobody writes to a path
the server does not serve.

Output: 19 Verdict cells replaced in place in `.planning/ledgers/milestone-09-12.md`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-CONTEXT.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive Milestone 12 Epics 1-2 (10 rows) with the route-surface and provisioner pointers</name>
  <files>.planning/ledgers/milestone-09-12.md</files>
  <read_first>
    - `.planning/ledgers/milestone-09-12.md` — the head notes, the `/v1` factual anchor plan 13-01 wrote, and the 10 rows this task owns.
    - `crates/paladin-web/openapi.json` — the committed drift-guard baseline; grep the six agent paths and confirm each carries the `/v1` prefix before citing it.
    - `crates/paladin-web/src/agent_registry.rs` — `AgentSpec` at `:55-79` and the `AgentProvisioner` trait and its `provision` signature at `:103-110`; re-read the line numbers before repeating them.
    - `scripts/check-api-surface.sh` — line 6's `BASELINE` default, the evidence that DEBT-01's consequence clause is closed.
    - `.planning/intel/requirements.md` — acceptance criteria for any bare-`Verify` ID in this range.
  </read_first>
  <action>
Replace the Verdict cell of these 10 rows in place, in
`### Milestone 12 Epic 1 — Agent Registry & Execution API (6 IDs)` and
`### Milestone 12 Epic 2 — Configurable Web Host & Server Binary (4 IDs)`.

Epic 1 — `REQ-agent-registry`, `REQ-agent-execute-endpoint`, `REQ-agent-discovery-endpoints`,
`REQ-agent-runtime-registration`, `REQ-agent-provisioner-port`, `REQ-paladin-web-no-facade-dep`.

Epic 2 — `REQ-host-agents-config-schema`, `REQ-registry-from-config-builder`,
`REQ-concrete-agent-provisioner`, `REQ-paladin-server-binary`.

`REQ-agent-execute-endpoint` and `REQ-agent-discovery-endpoints` carry the route-surface answer
(D-11). Re-run a grep for the agent paths in `crates/paladin-web/openapi.json` and record its output:
all six of `/v1/agents`, `/v1/agents/{id}`, `/v1/agents/{id}/execute`,
`/v1/agents/{id}/execute/stream`, `/v1/agents/{id}/jobs`, `/v1/agents/{id}/jobs/{job_id}` carry the
prefix. Record that Epic 6 §4.3's position — the agent API is served under `/v1` while
operational and docs endpoints remain unversioned — is what shipped, that Epic 1's unprefixed route
text is **superseded provenance, not a live contract**, and that ADR-0037 (plan 13-08) is the
recorded answer. Verdict class `Shipped, superseded` for the route-text half. Do not annotate the
`.project/` Epic documents here — that is plan 13-08's.

`REQ-agent-provisioner-port` and `REQ-concrete-agent-provisioner` and
`REQ-registry-from-config-builder` are ORCH-04's subjects. Record the shipped facts only: the trait
and its `provision(&self, spec: &AgentSpec) -> Result<ProvisionedAgent, ProvisionError>` signature at
`crates/paladin-web/src/agent_registry.rs:103-110`, `AgentSpec`'s `utoipa::ToSchema` derive at
`:55-56`, the public re-export at `crates/paladin-web/src/lib.rs:46`, and the only implementation
outside `paladin-web`'s own tests at `src/infrastructure/web/facade_provisioner.rs:70` behind
`#[cfg(feature = "web-server")]`. Then point at ADR-0038 and ADR-0039 as the phase's recorded answers
**without stating either answer** — both are flagged `⚠ HUMAN REVIEW` and gated on plan 13-09's
blocking checkpoint (D-14, D-15). Re-read each cited line before repeating it.

The Milestone 12 Epic 1 requirement text that names the undotted `project/current-exports.txt` gets
the D-09 treatment in its row: `scripts/check-api-surface.sh:6` reads the dotted
`.project/current-exports.txt` and that file exists, so run-5 finding 8's "the `api-surface` CI job
fails on every run" consequence clause is **no longer true**; what remains is the documentation half
in four Milestone 12 requirement texts, already carrying dated `Corrected (dated 2026-08-06, DEBT-01)`
banners written by Phase 8. Confirm the banner exists with a grep before citing it. Hand the residue
to Phase 15 alongside D-08's job list. **Do not grow ORCH-03 to a sixth item and do not re-annotate
the `.project/` Epic files** — that edit already happened.

Apply the D-03 evidence bar to the remaining rows, with the head note's manifest carve-out where the
claim is a config schema or a binary target.
  </action>
  <verify>
    <automated>awk '/^### Milestone 12 Epic 1/,/^### Milestone 12 Epic 3/' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^### Milestone 12 Epic 1/,/^### Milestone 12 Epic 3/' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'` → `10`
    - `awk '/^### Milestone 12 Epic 1/,/^### Milestone 12 Epic 3/' .planning/ledgers/milestone-09-12.md | grep -c 'pending — plan'` → `0`
    - `awk '/^### Milestone 12 Epic 1/,/^### Milestone 12 Epic 3/' .planning/ledgers/milestone-09-12.md | grep -c 'run-5 input (not yet re-derived)'` → `0`
    - `grep -c '/v1/agents' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c 'ADR-0037' .planning/ledgers/milestone-09-12.md` → `≥1` and `grep -c 'ADR-0038' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c 'check-api-surface.sh:6' .planning/ledgers/milestone-09-12.md` → `≥2` (head note plus this row)
    - `git diff --name-only -- '.project/*' | wc -l` → `0` (this plan edits no source document)
  </acceptance_criteria>
  <done>All 10 rows carry a cited verdict; the `/v1` route surface is confirmed against `openapi.json` with Epic 1's route text marked superseded provenance; the provisioner rows point at ADR-0038/0039 without stating their answers; the D-09 residue is handed to Phase 15.</done>
</task>

<task type="auto">
  <name>Task 2: Derive Milestone 12 Epics 3-4 (9 rows)</name>
  <files>.planning/ledgers/milestone-09-12.md</files>
  <read_first>
    - `.planning/ledgers/milestone-09-12.md` — the rows this task owns and the Epic 1-2 rows task 1 just wrote.
    - `crates/paladin-web/src/` — the streaming, job, error-envelope, health, logging, CORS and rate-limiting modules; locate each with `grep -rn` before citing a line.
    - `.planning/intel/code-verification.md:417-471` — run 5's Verified SHIPPED table for the rows it already covers.
    - `.planning/intel/requirements.md` — acceptance criteria for the bare-`Verify` IDs in this range.
  </read_first>
  <action>
Replace the Verdict cell of these 9 rows in place, in
`### Milestone 12 Epic 3 — Streaming & Async Jobs (4 IDs)` and
`### Milestone 12 Epic 4 — Operational Hardening (5 IDs)`.

Epic 3 — `REQ-execute-stream-service`, `REQ-sse-streaming-endpoint`,
`REQ-execution-timeout-cancellation`, `REQ-async-jobs-api`.

Epic 4 — `REQ-api-error-envelope`, `REQ-health-ready-endpoints`, `REQ-request-logging-request-id`,
`REQ-cors-body-limit-timeout`, `REQ-rate-limiting`.

Every Epic 4 row arrived as a bare `Shipped` — the ingest-era status word D-03 rejects as a claim.
Re-derive each: locate the implementing symbol with `grep -rn` over `crates/paladin-web/src/`, name
its exerciser (a `#[test]`, an integration test under `tests/`, the `server-e2e-tests` route, or a
named `ci.yml` job from the head note's 15-job list), and record both with `file:line`. Remove the
`run-5 input (not yet re-derived)` prefix only after the citation has been re-run.

`REQ-health-ready-endpoints` is the row a Phase 14 reader reaches for when sizing WEB-02: record the
health and readiness handlers with their paths, and cross-reference `REQ-k8s-manifests` (plan 13-06's
row) where the multi-replica token problem is recorded. Do not resolve WEB-02 here; it is Phase 14's.

Where an Epic 3 or Epic 4 claim can only be exercised in CI, apply the head note's manifest carve-out
and name the specific job; do not write "CI runs it". Where no exerciser exists, the verdict is
`Verified open` with the search command that returned nothing.
  </action>
  <verify>
    <automated>awk '/^### Milestone 12 Epic 3/,/^### Milestone 12 Epic 5/' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^### Milestone 12 Epic 3/,/^### Milestone 12 Epic 5/' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'` → `9`
    - `awk '/^### Milestone 12 Epic 3/,/^### Milestone 12 Epic 5/' .planning/ledgers/milestone-09-12.md | grep -c 'pending — plan'` → `0`
    - `awk '/^### Milestone 12 Epic 3/,/^### Milestone 12 Epic 5/' .planning/ledgers/milestone-09-12.md | grep -c 'run-5 input (not yet re-derived)'` → `0`
    - `awk '/^### Milestone 12 Epic 3/,/^### Milestone 12 Epic 5/' .planning/ledgers/milestone-09-12.md | grep -cE '\.rs:[0-9]+'` → `≥9`
    - `grep -c 'REQ-k8s-manifests' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md` → `120`
    - `git diff --numstat -- .planning/ledgers/milestone-09-12.md | awk '{print ($2==$3)?"balanced":"unbalanced"}'` → `balanced`
  </acceptance_criteria>
  <done>All 9 rows in Milestone 12 Epics 3-4 carry a re-derived, line-addressed verdict with a named exerciser, and the ledger still holds exactly 120 rows.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Modified by this plan (no new files):
- `.planning/ledgers/milestone-09-12.md` — Verdict cells for 19 IDs

Identifiers a later reader will grep for, finalized here:
- `REQ-agent-registry`, `REQ-agent-execute-endpoint`, `REQ-agent-discovery-endpoints`, `REQ-agent-runtime-registration`, `REQ-agent-provisioner-port`, `REQ-paladin-web-no-facade-dep`
- `REQ-host-agents-config-schema`, `REQ-registry-from-config-builder`, `REQ-concrete-agent-provisioner`, `REQ-paladin-server-binary`
- `REQ-execute-stream-service`, `REQ-sse-streaming-endpoint`, `REQ-execution-timeout-cancellation`, `REQ-async-jobs-api`
- `REQ-api-error-envelope`, `REQ-health-ready-endpoints`, `REQ-request-logging-request-id`, `REQ-cors-body-limit-timeout`, `REQ-rate-limiting`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `openapi.json` -> ledger route claim | a committed drift-guard baseline crosses into the recorded contract for five Epics |
| ledger -> plans 13-08 / 13-09 | the ADR pointers become the ADRs' own `Code Locations` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | 53-bare-`Shipped` re-derivation, densest in Epic 4 | high | mitigate | The `run-5 input (not yet re-derived)` prefix is removed only after the citation is re-run; the acceptance criteria require a `.rs:NNN` citation on every row in the range |
| T-13-02 | Tampering | the `agent_registry.rs:55-56` and `:103-110` citations | high | mitigate | `read_first` requires re-reading the file before repeating the line numbers; 13-CONTEXT.md documents five separate cases of this corpus citing stale lines |
| T-13-12 | Elevation of privilege | pre-empting the plan 13-09 checkpoint | high | mitigate | The provisioner rows record shipped facts and ADR pointers only; stating D-14's or D-15's answer before the blocking human checkpoint would launder an unratified decision into the ledger |
| T-13-13 | Tampering | re-annotating already-corrected `.project/` files | medium | mitigate | D-09 plus 13-RESEARCH.md Pitfall 2: Phase 8's dated banners already exist; the acceptance criteria assert `git diff --name-only -- '.project/*'` is empty for this plan |
| T-13-14 | Repudiation | ORCH-03 scope creep to a sixth item | medium | mitigate | D-09 is explicit: the four Milestone 12 references stay ledger rows and are handed to Phase 15 |
| T-13-SC | Tampering | package installs | low | accept | N/A — no package install, no manifest change (D-19) |
</threat_model>

<verification>
- `awk '/^### Milestone 12 Epic 1/,/^### Milestone 12 Epic 5/' .planning/ledgers/milestone-09-12.md | grep -c 'pending — plan'` → `0`
- `grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md` → `120`
- `git diff --name-only -- '*.rs' '.project/*' | wc -l` → `0`
</verification>

<success_criteria>
Milestone 12 Epics 1-4 are derived at the D-00e bar; the `/v1` route surface is settled against the
committed baseline; the provisioner seam is recorded as a fact set awaiting a ratified decision.
</success_criteria>

<output>
Create `.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-05-SUMMARY.md` when done.
</output>
