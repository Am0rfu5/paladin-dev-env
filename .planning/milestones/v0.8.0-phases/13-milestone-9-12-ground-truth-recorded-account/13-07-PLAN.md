---
phase: 13-milestone-9-12-ground-truth-recorded-account
plan: 07
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - .planning/ledgers/milestone-09-12.md
autonomous: true
requirements: [ORCH-01, ORCH-02]
must_haves:
  truths:
    - "All 18 rows in Deferred-QA Epics 25, 26, 27, 28-29 and project-management carry a verdict from the eleven-class key with a `file:line` or a search command re-run this session; none reads `Verify` and none reads `pending — plan 13-07`"
    - "The `Verified open` block is complete and each of its rows names its owning phase — Phase 15 for the Epic 25 and coverage-register rows, Phase 16 for the Epic 26 rows, Phase 14 for the Epic 27 rows — so the head-note highlight table resolves against real rows"
    - "The four ORCH-03(b)-(e) requirements — `REQ-listener-service-test-coverage`, `REQ-llm-tool-calling-port`, `REQ-arch-doc-modernization`, `REQ-asciinema-demos` — each record the old path verified absent and the new path verified present, as a relocation rather than a gap (D-13)"
    - "`REQ-arch-doc-modernization` records the relocation and the content gap as two separately labelled facts in its Verdict cell — `Shipped (relocated)` for the move and `Verified open` for the 311-line rewrite — and cross-references `REQ-architecture-docs-update` without merging the two rows (D-00f, D-13d)"
    - statement: "A row for which no exerciser can be found records `Verified open` with the exact search command that returned nothing, rather than being omitted or left with an inherited status word (ORCH-01 empty edge)"
      verification: backstop
    - "`REQ-master-plan-epics-11-18` records project-management's single open item as a formatting example inside a template file and does not convert it into a task (D-10)"
  artifacts:
    - .planning/ledgers/milestone-09-12.md
  key_links:
    - "Deferred-QA Epic 25 rows -> Phase 15 / PIPE-01..PIPE-05"
    - "Deferred-QA Epic 26 rows -> Phase 16 / DOCS-02, DOCS-03, DOCS-04"
    - "Deferred-QA Epic 27 rows -> Phase 14 / WEB-03, WEB-04"
    - "`REQ-listener-service-test-coverage` / `REQ-user-service-test-coverage` -> Phase 15 / DEFER-02, DEFER-03"
  prohibitions:
    - "No `.rs` file is modified by this plan (D-19)"
    - "No row outside the Deferred-QA and project-management sections is touched (D-23)"
    - "No `.project/` or `.planning/intel/` file is edited by this plan; the D-13 source annotations belong to plan 13-11"
    - "project-management's single open item is not converted into a task (D-10)"
    - "The Deferred-QA Epic 25 coverage-threshold variant is recorded with both sides and not resolved; Phase 15 picks one"
    - "No `Shipped` verdict without a `file:line` plus an exerciser (D-03); no bare `Verify` survives (D-02)"
    - "No item from 13-CONTEXT.md's `<deferred>` block is planned or actioned here"
---

<objective>
Derive the 18 rows of the Deferred-QA register and the project-management master plan — the only
ingested epic-set in the corpus verified open item by item, the highest-confidence forward-work
signal it contains, and the direct and only input to Phases 14, 15 and 16.

Purpose: this is the last ground-truth phase that will touch these rows. Whatever they say is what
the next three phases plan against.

Output: 18 Verdict cells replaced in place in `.planning/ledgers/milestone-09-12.md`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-CONTEXT.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive Deferred-QA Epics 25 and 26 (11 rows)</name>
  <files>.planning/ledgers/milestone-09-12.md</files>
  <read_first>
    - `.planning/ledgers/milestone-09-12.md` — the head notes, the `Verified open` highlight table plan 13-01 wrote, and the 11 rows this task owns.
    - `.project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md` — the parent register; its FR-25.3 78% hard gate, its OQ-3 recorded Open, and the asciinema and architecture-document clauses. Read, do not edit.
    - `.project/Deferred-QA-CICD-Completion/Epic_25/prd-cicd-pipeline-enhancement.md` — Epic 25's phased 70 → 74 → 78 ramp, the other side of the variant.
    - `docs/src/appendix/design-and-architecture.md` — re-measure it; run-5 recorded exactly 311 lines with zero mermaid blocks and zero mentions of Commander, Council, Conclave, Grove, Maneuver, Sanctum or Sentinel.
    - `README.md` — 193 lines, the target of `REQ-asciinema-demos`'s embedding clause.
  </read_first>
  <action>
Replace the Verdict cell of these 11 rows in place, in
`### Deferred-QA Epic 25 — CI/CD Pipeline Enhancement (7 IDs)` and
`### Deferred-QA Epic 26 — Documentation & Rustdoc (4 IDs)`.

Epic 25 — `REQ-ci-cli-snapshot-job`, `REQ-ci-bench-check-job`, `REQ-ci-combined-coverage-job`,
`REQ-codecov-config-thresholds`, `REQ-makefile-coverage-targets`, `REQ-modernize-github-actions`,
`REQ-contributing-coverage-docs`.

Epic 26 — `REQ-arch-doc-modernization`, `REQ-rustdoc-zero-warnings`, `REQ-public-api-doc-audit`,
`REQ-asciinema-demos`.

These rows arrived already rich — they are the only rows in the corpus verified open item by item —
so the work is re-confirmation and owner assignment, not derivation. For each, re-run the search that
establishes absence and record its exact output: no `cli-tests` job, no `bench-check` job, no
`coverage` job in `.github/workflows/ci.yml` (check against the head note's measured 15-job list, not
run 5's stale 14), no `.codecov.yml`, no `llvm-cov` reference in the `Makefile`. **An absence claim
needs the command that found nothing, recorded verbatim** — that is the D-00e bar applied to a
negative. Where a search that previously found nothing now finds something, record the change with
its date rather than transcribing the older claim.

`REQ-ci-combined-coverage-job` carries the scoping correction: coverage tooling is **partially
built** — `.github/workflows/integration-tests.yml:117-123` does run `cargo llvm-cov` and
`codecov-action@v3` — so the requirement supersedes the integration-only path rather than filling a
void. Re-read those lines before citing them.

`REQ-codecov-config-thresholds` records both sides of the coverage-threshold variant and resolves
neither: the parent PRD's FR-25.3 78% hard gate, Epic 25's FR-25.6 phased 70 → 74 → 78 ramp, the
parent's own OQ-3 recorded Open, and the measured baseline. Owner **Phase 15 / PIPE-02**.

`REQ-modernize-github-actions` records the eight deprecated references — `actions-rs/toolchain@v1`
×4, `actions/cache@v3` ×3, `codecov/codecov-action@v3` ×1 — and notes that run 5's line numbers for
them are stale because `ci.yml` has since changed shape (D-08). Re-grep for the current locations and
cite those. Owner **Phase 15 / PIPE-04**.

`REQ-arch-doc-modernization` is D-13(d). Record two separately labelled facts in the one Verdict
cell: the **relocation** — `docs/Design/Design_and_Architecture.md` verified absent (`ls` fails) and
`docs/src/appendix/design-and-architecture.md` verified present, Milestone 11 overhaul, class
`Shipped (relocated)`; and the **content gap** — re-measure with `wc -l` and with a grep for the
seven subsystem names and for mermaid fences, and record the outputs, class `Verified open`. State
that the relocation hid the gap rather than closing one, because Milestone 11 Epic 3's non-goals
exempt exactly the appendix chapter its own Epic 2 moved the file into. Cross-reference
`REQ-architecture-docs-update` (plan 13-04's row) and keep the two rows separate (D-00f). Owner
**Phase 16 / DOCS-02**.

`REQ-asciinema-demos` is D-13(e). Record that the clause targets a document that changed shape:
re-run `wc -l README.md` and `grep -ic 'asciinema\|demo' README.md`, confirm `docs/assets/` exists
and is empty and `docs/DEMOS.md` does not exist, and record all four outputs. Owner **Phase 16 /
DOCS-04**. Do not annotate the `.project/` source here — plan 13-11 owns that.
  </action>
  <verify>
    <automated>awk '/^### Deferred-QA Epic 25/,/^### Deferred-QA Epic 27/' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^### Deferred-QA Epic 25/,/^### Deferred-QA Epic 27/' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'` → `11`
    - `awk '/^### Deferred-QA Epic 25/,/^### Deferred-QA Epic 27/' .planning/ledgers/milestone-09-12.md | grep -c 'pending — plan'` → `0`
    - `awk '/^### Deferred-QA Epic 25/,/^### Deferred-QA Epic 27/' .planning/ledgers/milestone-09-12.md | grep -c 'run-5 input (not yet re-derived)'` → `0`
    - `awk '/^### Deferred-QA Epic 25/,/^### Deferred-QA Epic 27/' .planning/ledgers/milestone-09-12.md | grep -c 'Verified open'` → `≥8`
    - `grep -c 'Shipped (relocated)' .planning/ledgers/milestone-09-12.md` → `≥2` (legend plus this row)
    - `grep -c '311' .planning/ledgers/milestone-09-12.md` → `≥1` and `grep -c 'DOCS-02' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c 'integration-tests.yml:117-123' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `git diff --name-only -- '.project/*' '.planning/intel/*' | wc -l` → `0`
  </acceptance_criteria>
  <done>All 11 rows carry a re-confirmed verdict with the absence command recorded verbatim, the architecture-document relocation and content gap are two separately labelled facts, and every row names its owning phase.</done>
</task>

<task type="auto">
  <name>Task 2: Derive Deferred-QA Epics 27, 28-29 and project-management (7 rows)</name>
  <files>.planning/ledgers/milestone-09-12.md</files>
  <read_first>
    - `.planning/ledgers/milestone-09-12.md` — the rows this task owns and the Epic 25-26 rows task 1 just wrote.
    - `crates/paladin-ports/src/output/llm_port.rs` — the current home of the LLM port; the old path `src/application/ports/output/llm_port.rs` and the whole `src/application/ports/` directory are gone.
    - `src/application/services/orchestration/listener.rs` — `ListenerOrchestrator`, the current home of the listener; the old path `src/core/platform/manager/listener_service.rs` is gone.
    - `.project/Deferred-QA-CICD-Completion/DEFERRED_COVERAGE.md` — the coverage register, its stale 602 LOC / 57.83% baseline and its stale path. Read, do not edit.
    - `.project/project-management/tasks-project-management-setup.md` — the single "open" item, ORCH-02's nonexistent verdict.
  </read_first>
  <action>
Replace the Verdict cell of these 7 rows in place, in
`### Deferred-QA Epic 27 — LLM Tool Calling (2 IDs)`,
`### Deferred-QA Epics 28-29 & the coverage register (4 IDs)` and
`### project-management (1 ID)`:
`REQ-llm-tool-calling-port`, `REQ-llm-tool-calling-adapters`, `REQ-mock-infrastructure`,
`REQ-user-service-test-coverage`, `REQ-listener-service-test-coverage`,
`REQ-deferred-coverage-register`, `REQ-master-plan-epics-11-18`.

`REQ-llm-tool-calling-port` is D-13(c). Record the relocation: `ls src/application/ports/output/llm_port.rs`
fails and the whole `src/application/ports/` directory was deleted by Milestone 5 Epic 2;
`crates/paladin-ports/src/output/llm_port.rs` exists. Record both commands and their results. Also
record the capability half as `Verified open`: the port has no `tools` field and greps for
`struct ToolDefinition`, `struct ToolCall` and `tool_calls` return zero across `paladin-ports` and
`paladin-llm` — re-run them and record the output. Note for Phase 14 that this is the same file
WEB-03 and WEB-04 act on. Owner **Phase 14 / WEB-03, WEB-04**.

`REQ-llm-tool-calling-adapters`: record that all three LLM adapters declare tool-calling capability
in `ProviderCapabilities` while hardcoding `function_call: None`, so the flag over-reports. Owner
**Phase 14 / WEB-03**. Do not fix it and do not decide WEB-04's scope question.

`REQ-listener-service-test-coverage` is D-13(b). Record the relocation:
`ls src/core/platform/manager/listener_service.rs` fails,
`src/application/services/orchestration/listener.rs` exists as `ListenerOrchestrator` after the
Milestone 6 Epic 2 relocation. Record that `DEFERRED_COVERAGE.md`'s 602 LOC / ~57.83% baseline dated
2026-02-14 is stale in both path and number because Milestone 9 Epic 2 added tests against the
module — re-measure the line count and record it. Scope real, arithmetic not. Owner
**Phase 15 / DEFER-03**.

`REQ-user-service-test-coverage`: record the collision — Deferred-QA Epic 28 plans to test
`user_service.rs` to ≥80% while Milestone 8's `deferred-items.md` D2 plans to split it, and Phase 11's
ADR-0034 withdrew the split into three non-overlapping owners. Cite ADR-0034 and name
**Phase 15 / DEFER-02**. Do not schedule either action.

`REQ-mock-infrastructure`: record that no `tests/common/` directory exists, that mocks live at
`tests/helpers/{mock_llm_adapter,mock_arsenal_adapter,mock_paladin_port}.rs` — a different location
and a disjoint set — and that none of `MockUserRepository`, `MockLogPort`, `MockNotificationService`,
`MockEventSource` or `MockTriggerExecutor` exists. Re-run each check and record the output. Owner
**Phase 15 / DEFER-01**.

`REQ-deferred-coverage-register`: record the register's own sign-off and its unreached review
trigger, and the two stale paths and stale baselines that weaken it. Owner **Phase 15**.

`REQ-master-plan-epics-11-18` is ORCH-02's fifth verdict. Quote the single "open" item verbatim —
the `- [ ] 1.1 Create template → - [x] 1.1 Create template (after completing)` formatting example
inside a template file — cite its `file:line` in
`.project/project-management/tasks-project-management-setup.md`, and record the verdict as
**nonexistent**. It does not become a task.

Every absence claim in this task carries the command that found nothing, recorded verbatim. Where a
search that run 5 recorded as empty now returns a hit, record the change with its date rather than
transcribing the older claim.
  </action>
  <verify>
    <automated>awk '/^### Deferred-QA Epic 27/,0' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^### Deferred-QA Epic 27/,0' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'` → `7`
    - `awk '/^### Deferred-QA Epic 27/,0' .planning/ledgers/milestone-09-12.md | grep -c 'pending — plan'` → `0`
    - `awk '/^### Deferred-QA Epic 27/,0' .planning/ledgers/milestone-09-12.md | grep -c 'run-5 input (not yet re-derived)'` → `0`
    - `grep -c 'crates/paladin-ports/src/output/llm_port.rs' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c 'src/application/services/orchestration/listener.rs' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c 'ADR-0034' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c 'Create template' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c 'pending — plan' .planning/ledgers/milestone-09-12.md` → `0` (with all six fan-out plans complete, no interim marker survives anywhere)
    - `grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md` → `120`
  </acceptance_criteria>
  <done>All 7 rows carry a re-confirmed verdict with its absence command recorded, the four ORCH-03(b)-(e) relocations are recorded as relocations with both paths checked, and project-management's single open item is recorded nonexistent without becoming a task.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Modified by this plan (no new files):
- `.planning/ledgers/milestone-09-12.md` — Verdict cells for 18 IDs

Identifiers a later reader will grep for, finalized here:
- `REQ-ci-cli-snapshot-job`, `REQ-ci-bench-check-job`, `REQ-ci-combined-coverage-job`, `REQ-codecov-config-thresholds`, `REQ-makefile-coverage-targets`, `REQ-modernize-github-actions`, `REQ-contributing-coverage-docs`
- `REQ-arch-doc-modernization`, `REQ-rustdoc-zero-warnings`, `REQ-public-api-doc-audit`, `REQ-asciinema-demos`
- `REQ-llm-tool-calling-port`, `REQ-llm-tool-calling-adapters`
- `REQ-mock-infrastructure`, `REQ-user-service-test-coverage`, `REQ-listener-service-test-coverage`, `REQ-deferred-coverage-register`
- `REQ-master-plan-epics-11-18`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| absence claims -> ledger | "this does not exist" crosses into a verdict three later phases plan against |
| ledger -> Phases 14, 15, 16 | the entire forward-work signal of the corpus leaves through these 18 rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-17 | Repudiation | negative claims ("no `coverage` job", "no `.codecov.yml`") | high | mitigate | Every absence carries the command that found nothing, recorded verbatim; a search that now returns a hit is recorded as a dated change rather than transcribed from run 5 |
| T-13-02 | Tampering | the four relocation citations | high | mitigate | Both the old and the new path are checked this session with `ls`, and the relocation is recorded as a relocation rather than a gap — the corpus's largest false-gap generator |
| T-13-18 | Tampering | the architecture-document verdict | medium | mitigate | The relocation and the content gap stay two separately labelled facts in one row and cross-reference `REQ-architecture-docs-update`; merging them would let a closed move mask an open rewrite |
| T-13-19 | Elevation of privilege | resolving the coverage-threshold variant | medium | mitigate | Both sides are recorded with the parent PRD's OQ-3 still Open; Phase 15 / PIPE-02 picks, this phase does not |
| T-13-01 | Tampering | Verdict cells for 18 IDs | high | mitigate | D-03 evidence bar; owner phase named per row so the highlight table resolves |
| T-13-SC | Tampering | package installs | low | accept | N/A — no package install, no manifest change (D-19) |
</threat_model>

<verification>
- `grep -c 'pending — plan' .planning/ledgers/milestone-09-12.md` → `0`
- `grep -c 'run-5 input (not yet re-derived)' .planning/ledgers/milestone-09-12.md` → `0`
- `grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md` → `120`
- `git diff --name-only -- '*.rs' '.project/*' | wc -l` → `0`
</verification>

<success_criteria>
The `Verified open` block is complete and owner-assigned; the four ORCH-03(b)-(e) relocations are
recorded with both paths checked; the ledger carries no interim marker anywhere.
</success_criteria>

<output>
Create `.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-07-SUMMARY.md` when done.
</output>
