---
phase: 13-milestone-9-12-ground-truth-recorded-account
plan: 08
type: execute
wave: 3
depends_on: ["13-01", "13-05", "13-06"]
files_modified:
  - .planning/decisions/0037-agent-route-surface-v1.md
  - .project/Milestone_12-Web-API/Epic_1/prd-agent-registry-execution-api.md
  - .project/Milestone_12-Web-API/Epic_1/tasks-agent-registry-execution-api.md
  - .project/Milestone_12-Web-API/Epic_3/prd-streaming-async-execution.md
  - .project/Milestone_12-Web-API/Epic_3/tasks-streaming-async-execution.md
  - .project/Milestone_12-Web-API/Epic_4/prd-api-cross-cutting-concerns.md
  - .project/Milestone_12-Web-API/Epic_4/tasks-api-cross-cutting-concerns.md
  - .project/Milestone_12-Web-API/Epic_5/prd-api-security-authorization.md
  - docs/src/deployment-topologies/sidecar.md
autonomous: true
requirements: [ORCH-03]
must_haves:
  truths:
    - "`.planning/decisions/0037-agent-route-surface-v1.md` exists with the seven required H2 headings in order, no frontmatter, and bulleted `## Code Locations` and `## Considered Options` (D-00a)"
    - "ADR-0037 records the agent route surface as `/v1`-prefixed, confirmed against the committed `crates/paladin-web/openapi.json` baseline, with Milestone 12 Epics 1, 3, 4 and 5's unprefixed route text as superseded provenance rather than a live contract (D-11)"
    - "`docs/src/deployment-topologies/sidecar.md` names `POST /v1/agents/{id}/execute` and no longer names the unprefixed form; the corrected string is character-identical to the form `http-service-host.md` already uses and to the path in `openapi.json` (D-12, ORCH-03 adjacency edge)"
    - "Each of Milestone 12 Epics 1, 3, 4 and 5 carries a dated correction banner naming what was wrong and pointing at ADR-0037, with every superseded route string retained inline and marked superseded (D-00c)"
    - "A requirement whose route text appears in more than one document gets a banner in each document rather than one shared banner, and no two documents are merged (D-00f, ORCH-03 duplicates edge)"
    - "Any unprefixed route site found outside the four Epics D-11 names is recorded in the summary and surfaced, not silently absorbed into this plan's edit set"
  artifacts:
    - .planning/decisions/0037-agent-route-surface-v1.md
  key_links:
    - "ADR-0037 -> `crates/paladin-web/openapi.json` (the settling artefact) and `.project/Milestone_12-Web-API/Epic_6` §4.3 (the position that shipped)"
    - "ADR-0037 `## Code Conformance` -> `docs/src/deployment-topologies/sidecar.md`, corrected by this plan's task 2"
    - "ADR-0037 `## Downstream Consumers` -> Phase 14 and Phase 15"
  prohibitions:
    - "No `.rs` file is modified by this plan (D-19)"
    - "No `.project/` document is rewritten; corrections are dated banners plus inline annotation with the original retained (D-00c)"
    - "No documentation file other than `docs/src/deployment-topologies/sidecar.md` is edited by this plan (D-19)"
    - "No ADR number other than 0037 is taken by this plan; `PROMOTION.md`'s next-free line is advanced once, by plan 13-13 (D-20)"
    - "The run-5 unsettled-position entry for the competing route surface stays in the variants register; the ADR records the answer, the register keeps the disagreement (D-11)"
    - "ORCH-03 is not grown beyond its five named items (D-09)"
    - "No item from 13-CONTEXT.md's `<deferred>` block is planned or actioned here"
---

<objective>
Give the agent route surface one recorded answer — ADR-0037 — and close the single place in the tree
where a reader is told to call a path the server does not serve.

Purpose: ORCH-03's done-when is that anyone applying a run-5 requirement literally cannot write to a
path that does not exist. Four Epics name unprefixed paths in `.project/` PRDs nobody executes
against; one shipped mdbook page names one, and that page is a live contract.

Output: `.planning/decisions/0037-agent-route-surface-v1.md`, one corrected line in
`docs/src/deployment-topologies/sidecar.md`, and dated correction banners in the four Milestone 12
Epic documents.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-CONTEXT.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ADR-0037 — the agent route surface is /v1</name>
  <files>.planning/decisions/0037-agent-route-surface-v1.md</files>
  <read_first>
    - `.planning/decisions/PROMOTION.md` — the numbering index (re-read the `Next free ADR number` line before taking a number), the file-shape rules at `:119-140` including why `## Code Locations` and `## Considered Options` must be bulleted lists rather than prose, and the promotion procedure at `:161-182`.
    - `.planning/decisions/0036-audit-suppression-single-source-topology.md` — the most recent ADR and the exact shape to match: seven H2 headings in order, `## Status` body `Accepted` then a blank line then `**Date:** YYYY-MM-DD`, no frontmatter.
    - `crates/paladin-web/openapi.json` — grep the six agent paths and confirm the `/v1` prefix on each before writing the citation.
    - `.project/Milestone_12-Web-API/Epic_6/prd-openapi-spec-interactive-docs.md` §4.3 — the position that shipped.
    - `.planning/ledgers/milestone-09-12.md` — the `REQ-agent-execute-endpoint`, `REQ-api-v1-versioning` and `REQ-openapi-drift-guard` rows derived by plans 13-05 and 13-06, which this ADR's `## Code Locations` cites.
  </read_first>
  <action>
Create `.planning/decisions/0037-agent-route-surface-v1.md` with exactly the seven H2 headings in
order and no frontmatter (D-00a).

`## Status` — `Accepted`, blank line, `**Date:**` with today's date.

`## Context` — five Milestone 12 Epics disagree about the agent route surface. Epics 1, 3, 4 and 5
write acceptance criteria, test assertions and examples against unprefixed paths; Epic 6 §4.3
requires that the agent API is served under `/v1` while operational and docs endpoints remain
unversioned. Four Epics' requirement text names paths a fifth Epic relocates. State that the
committed drift-guard baseline settles it by construction: `crates/paladin-web/openapi.json` carries
all six agent paths `/v1`-prefixed. State the one live consequence — `docs/src/deployment-topologies/
sidecar.md` tells a reader to call an unprefixed path against a server that serves the prefixed one,
and it is the only such reference anywhere under `docs/src/`, `examples/` or `README.md`.

`## Decision` — the agent API is served under `/v1`; operational and docs endpoints remain
unversioned. Epic 1, 3, 4 and 5 route text is superseded provenance, not a live contract.
`sidecar.md`'s route reference is corrected to the prefixed form.

`## Considered Options` — a bulleted list, one physical line per bullet: adopt Epic 6's `/v1` prefix
as the answer (chosen — the committed baseline locks in whichever form shipped, and it is more
authoritative than five PRDs); treat the unprefixed form as the contract and change the server
(rejected — it would break the shipped `openapi.json` drift guard and every deployed client);
record the disagreement without answering it (rejected — ORCH-03's done-when requires one recorded
answer). Keep each bullet on one physical line: `adr-parser.cjs` splits per line, so a multi-line
bullet inflates the parsed count.

`## Code Locations` — a bulleted list, one physical line per bullet, each with a `file:line` re-read
this session: the six `/v1` agent paths in `crates/paladin-web/openapi.json`; Epic 6 §4.3's position;
`docs/src/deployment-topologies/http-service-host.md`'s existing correct usage;
`docs/src/deployment-topologies/sidecar.md`'s defect site; and the source `.project/` documents for
the four superseded Epics, as `PROMOTION.md` step 4 requires.

`## Code Conformance` — `must change`, followed by a prose paragraph naming plan 13-08 task 2 as the
executor of the one-line `sidecar.md` correction, with the before and after strings quoted and the
verifying grep recorded. This mirrors ADR-0032's precedent of a `must change` ADR whose consequence
is executed by the same phase that writes it.

`## Downstream Consumers` — a bulleted list, each bullet leading with a bold `Phase N / REQ-ID`:
**Phase 14 / WEB-01** inherits the settled route surface so the auth work does not re-derive it, and
**Phase 15 / PIPE-01** inherits it as the versioned surface any future CI route check gates on.

State explicitly in `## Context` that ORCH-03(a)'s instruction to preserve the competing route
surface as a run-5 unsettled position in the variants register still holds: this ADR records the
answer, the register keeps the disagreement. Do not edit the variants register.
  </action>
  <verify>
    <automated>ls .planning/decisions/0037-*.md && grep -c '^## ' .planning/decisions/0037-*.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/decisions/0037-*.md` → exactly one file
    - `grep -c '^## ' .planning/decisions/0037-*.md` → `7`
    - `grep -n '^## ' .planning/decisions/0037-*.md` → `Status`, `Context`, `Decision`, `Considered Options`, `Code Locations`, `Code Conformance`, `Downstream Consumers` in that order
    - `head -1 .planning/decisions/0037-*.md | grep -c '^---'` → `0` (no frontmatter)
    - `awk '/^## Code Locations/,/^## Code Conformance/' .planning/decisions/0037-*.md | grep -c '^- '` → `≥5` and the same range `grep -cv '^- \|^## \|^$'` → `0` (bulleted, no prose paragraphs)
    - `awk '/^## Code Conformance/,/^## Downstream Consumers/' .planning/decisions/0037-*.md | grep -c 'must change'` → `1`
    - `awk '/^## Downstream Consumers/,0' .planning/decisions/0037-*.md | grep -c 'Phase 14'` → `≥1`
    - `grep -c '/v1/agents' .planning/decisions/0037-*.md` → `≥1`
  </acceptance_criteria>
  <done>ADR-0037 exists in the corpus's exact ADR shape, records `/v1` as the answer with the `openapi.json` citation, carries `must change` naming its own executing task, and names Phase 14 and Phase 15 as consumers.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the one live route defect in sidecar.md</name>
  <files>docs/src/deployment-topologies/sidecar.md</files>
  <read_first>
    - `docs/src/deployment-topologies/sidecar.md` — read lines 20-40 and locate the current parenthetical before editing; 13-RESEARCH.md read it at line 29 on 2026-08-10, but re-read rather than trusting the line number.
    - `docs/src/deployment-topologies/http-service-host.md` — the only file under `docs/src/` already using `/v1/agents`; the corrected string must match the form used there exactly.
    - `.planning/decisions/0037-agent-route-surface-v1.md` — the ADR task 1 just wrote, whose `## Code Conformance` names this edit.
  </read_first>
  <action>
In `docs/src/deployment-topologies/sidecar.md`, change the parenthetical route reference in the
"Server side" bullet from the unprefixed `POST /agents/{id}/execute` to the prefixed
`POST /v1/agents/{id}/execute`. The surrounding prose is unaffected — this is a one-string edit.

Confirm before and after that the corrected string is character-identical to the path used in
`docs/src/deployment-topologies/http-service-host.md` and to the corresponding path in
`crates/paladin-web/openapi.json`, so the two pages and the spec name the same route rather than three
near-identical variants (the ORCH-03 adjacency edge).

This file is a published mdbook page, not a `.project/` PRD, so D-00c's retain-the-original
convention does not apply — a live contract is corrected, not annotated. Do not add a banner here.

`docs/book.toml` sets `[output.linkcheck] warning-policy = "error"`, so verify the edit touches no
link target. Then run `mdbook build docs/` and compare its result against the pre-phase baseline
recorded in 13-RESEARCH.md — exit `101` with a broken link at `deployment/docker.md:118` and an
incomplete reference link at `user-guides/tool-integration.md:324`, both pre-existing, both in files
this phase does not touch. The expected outcome is that both errors persist unchanged and no new
error appears. Record the before/after comparison in the summary. Do not attribute the pre-existing
failure to this edit and do not reinterpret it as a pass. If `mdbook` is unavailable, install it
offline at `.github/workflows/docs.yml`'s pinned versions; if that fails, record the failure verbatim
and scope the claim CI-only (D-21).

Do not edit `http-service-host.md` or `overview.md` — those belong to plan 13-09, behind a blocking
human checkpoint.
  </action>
  <verify>
    <automated>grep -c 'POST /v1/agents/{id}/execute' docs/src/deployment-topologies/sidecar.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'POST /v1/agents/{id}/execute' docs/src/deployment-topologies/sidecar.md` → `1`
    - `grep -c 'POST /agents/{id}/execute' docs/src/deployment-topologies/sidecar.md` → `0`
    - `grep -rcE '\(`?POST /agents' docs/src/ examples/ README.md 2>/dev/null | grep -v ':0$' | wc -l` → `0` (no unprefixed agent route remains anywhere in published material)
    - `git diff --numstat -- docs/src/deployment-topologies/sidecar.md` → `1	1	docs/src/deployment-topologies/sidecar.md` (a one-line change)
    - `git diff --name-only -- 'docs/*' | wc -l` → `1`
    - `mdbook build docs/ ; echo "exit=$?"` → `exit=101` with exactly the two pre-existing errors at `deployment/docker.md:118` and `user-guides/tool-integration.md:324`, unchanged from the 2026-08-10 baseline, and no error naming `sidecar.md`
  </acceptance_criteria>
  <done>`sidecar.md` names the prefixed route, no unprefixed agent route survives in published material, the diff is one line, and the mdbook result matches the pre-phase baseline with no new error.</done>
</task>

<task type="auto">
  <name>Task 3: Annotate the four Milestone 12 Epic documents as superseded provenance</name>
  <files>.project/Milestone_12-Web-API/Epic_1/prd-agent-registry-execution-api.md, .project/Milestone_12-Web-API/Epic_1/tasks-agent-registry-execution-api.md, .project/Milestone_12-Web-API/Epic_3/prd-streaming-async-execution.md, .project/Milestone_12-Web-API/Epic_3/tasks-streaming-async-execution.md, .project/Milestone_12-Web-API/Epic_4/prd-api-cross-cutting-concerns.md, .project/Milestone_12-Web-API/Epic_4/tasks-api-cross-cutting-concerns.md, .project/Milestone_12-Web-API/Epic_5/prd-api-security-authorization.md</files>
  <read_first>
    - Each file listed above — locate its unprefixed route strings with `grep -nE '/agents/\{id\}/execute|`/agents`|POST /agents' <file>` and read the surrounding context before annotating. Do not trust a line number from any other document.
    - `.project/Milestone_12-Web-API/Epic_1/prd-agent-registry-execution-api.md` — check whether it already carries a dated correction banner from Phase 8 for the `current-exports.txt` defect; a second banner is appended below the first, never merged into it.
    - `.planning/decisions/0037-agent-route-surface-v1.md` — the ADR each banner points at.
  </read_first>
  <action>
Annotate each document per D-00c: a dated correction banner near the top naming what was wrong and
pointing at ADR-0037, plus each defective route string corrected inline with the **original text
retained and marked superseded**. Never rewrite a document and never delete a superseded string.

Banner content, per document: dated today, stating that the unprefixed agent route paths in this
document are superseded provenance rather than a live contract; that the shipped surface is
`/v1`-prefixed, confirmed against `crates/paladin-web/openapi.json`; and that the recorded answer is
`.planning/decisions/0037-agent-route-surface-v1.md`. Exact banner wording and inline markup are at
your discretion — D-00c fixes the pattern, not the prose — but the pattern must match the dated
banners Phase 8 already wrote in `.project/Milestone_12-Web-API/` so the corpus has one annotation
style. Read one of those banners first and copy its shape.

A document that already carries a Phase 8 banner gets this one appended below it, dated separately.
A requirement whose route text appears in both a `prd-` and a `tasks-` file gets a banner in each —
two documents, two banners, never one shared banner and never a merge (D-00f).

Re-run `grep -rnE '/agents/\{id\}/execute|POST /agents' .project/Milestone_12-Web-API/` before and
after. D-11 names Epics 1, 3, 4 and 5; the grep also reaches Epic 2, Epic 6 and the milestone
overview. **Do not absorb those into this plan's edit set** — Epic 6 is the position that won, and
widening the edit surface beyond D-11's four named Epics is scope this phase did not take. Record
every additional site found, with its `file:line`, in the plan summary so it is surfaced rather than
silently dropped, and state whether it is Epic 6's winning position being quoted or a genuine
additional defect for a later phase.
  </action>
  <verify>
    <automated>grep -rlc 'ADR-0037' .project/Milestone_12-Web-API/Epic_1 .project/Milestone_12-Web-API/Epic_3 .project/Milestone_12-Web-API/Epic_4 .project/Milestone_12-Web-API/Epic_5 | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl 'ADR-0037' .project/Milestone_12-Web-API/ | wc -l` → `7` (one per annotated document)
    - `grep -rl '0037-agent-route-surface-v1.md' .project/Milestone_12-Web-API/ | wc -l` → `7`
    - `grep -rc 'superseded' .project/Milestone_12-Web-API/Epic_1/prd-agent-registry-execution-api.md` → `≥1`
    - `git diff --stat -- .project/Milestone_12-Web-API/ | tail -1` shows deletions far fewer than insertions, and `git diff -- .project/Milestone_12-Web-API/ | grep -c '^-[^-]'` → `0` (no original line removed; annotation only)
    - `git diff --name-only -- '.project/*' | wc -l` → `7`
    - `git diff --name-only -- '*.rs' | wc -l` → `0`
  </acceptance_criteria>
  <done>All seven documents carry a dated banner pointing at ADR-0037 with every superseded route string retained inline, no original line is deleted, and any route site outside D-11's four Epics is recorded in the summary rather than absorbed.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Created by this plan:
- `.planning/decisions/0037-agent-route-surface-v1.md`

Modified by this plan:
- `docs/src/deployment-topologies/sidecar.md` (one route string)
- `.project/Milestone_12-Web-API/Epic_1/prd-agent-registry-execution-api.md`, `Epic_1/tasks-agent-registry-execution-api.md`, `Epic_3/prd-streaming-async-execution.md`, `Epic_3/tasks-streaming-async-execution.md`, `Epic_4/prd-api-cross-cutting-concerns.md`, `Epic_4/tasks-api-cross-cutting-concerns.md`, `Epic_5/prd-api-security-authorization.md` (dated banners)

New identifiers a later reader will grep for:
- `ADR-0037`, `0037-agent-route-surface-v1.md`
- The corrected published route string `POST /v1/agents/{id}/execute`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.project/` PRD -> published mdbook page | one is provenance nobody executes against, the other is a live contract a reader calls |
| ADR -> precedence order | an ADR sits at the top of the precedence order by construction, so a wrong one instructs a code change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-20 | Spoofing | the published route in `sidecar.md` | high | mitigate | The corrected string is checked character-identical against `http-service-host.md` and `openapi.json`; the acceptance criteria assert zero unprefixed agent routes remain anywhere under `docs/src/`, `examples/` or `README.md` |
| T-13-21 | Repudiation | `.project/` annotation destroying provenance | high | mitigate | D-00c: `git diff … grep -c '^-[^-]'` → `0` asserts no original line was removed; banners are appended, never merged into an existing Phase 8 banner |
| T-13-02 | Tampering | stale `file:line` in the ADR's `## Code Locations` | high | mitigate | Every citation is re-read this session; `read_first` requires locating each route string by grep rather than trusting a line number from another document |
| T-13-10 | Repudiation | the `mdbook build` result after the edit | medium | mitigate | The result is compared against the recorded pre-phase baseline and the acceptance criteria require no error naming `sidecar.md` |
| T-13-22 | Elevation of privilege | scope creep past D-11's four Epics | medium | mitigate | Additional route sites are recorded in the summary with `file:line` and surfaced, never absorbed into the edit set |
| T-13-23 | Tampering | ADR numbering collision | medium | mitigate | `PROMOTION.md`'s next-free line is re-read before taking `0037`; the line itself is advanced once, by plan 13-13 (D-20) |
| T-13-SC | Tampering | package installs | low | accept | Only the pre-cached, `docs.yml`-pinned mdbook toolchain may be installed with `--offline`; no manifest is modified and no `.rs` file changes (D-19) |
</threat_model>

<verification>
- `ls .planning/decisions/0037-*.md` → one file with 7 H2 headings and no frontmatter
- `grep -c 'POST /agents/{id}/execute' docs/src/deployment-topologies/sidecar.md` → `0`
- `git diff -- .project/Milestone_12-Web-API/ | grep -c '^-[^-]'` → `0`
- `git diff --name-only -- '*.rs' | wc -l` → `0`
</verification>

<success_criteria>
ORCH-03(a) has one recorded answer with the committed baseline as its evidence, the one live route
defect is closed, and the four superseded Epics say so at source without losing a word of their
original text.
</success_criteria>

<output>
Create `.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-08-SUMMARY.md` when done.
</output>
