---
phase: 14-api-contract-truthfulness
plan: 07
type: execute
wave: 4
depends_on: ["14-05", "14-06"]
files_modified:
  - .planning/ledgers/milestone-09-12.md
  - .planning/REQUIREMENTS.md
  - .planning/decisions/PROMOTION.md
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-04]

must_haves:
  truths:
    - "Every ledger row this phase resolves carries its verdict amended in place, dated, with the superseded text retained and the ADR or commit that settles it cited (D-00d, D-14)"
    - "WEB-03's closure records that its substance shipped in Phase 2 plan 02-02 under --auto, rather than presenting it as a bare human-ratified Complete (D-00i, D-14)"
    - "WEB-02's requirement text carries a dated correction banner naming the right pair of Kubernetes manifests, with the original citation retained and marked superseded (D-08)"
    - "Asking whether WEB-01 through WEB-04 are done returns four checked boxes whose traceability rows read Complete, each behind a named closing plan and a citable artefact"
    - "REQ-fail-closed-auth-posture moves off Verified open behind the test that drives the refusal, not behind a re-read of the source (D-15)"
    - "PROMOTION.md's next-free ADR number advances exactly once, from the value it currently holds past the three records this phase authored, with an index row for each and a dated note explaining the three-step advance"
    - "PROMOTION.md has exactly one writing plan in this phase, and .planning/ledgers/milestone-09-12.md has exactly one writing plan, so no two plans in the same wave contend for either file"
    - "The two Phase 13 hand-off items this phase adopted are recorded closed against the evidence that closed them, not silently absorbed"
    - "Amending the ledger row count does not change it: the row total is recomputed by counting requirement-keyed rows after the amendments, not assumed unchanged"
  artifacts:
    - path: ".planning/ledgers/milestone-09-12.md"
      provides: "Dated in-place amendments closing the six auth and tool-calling rows this phase resolves"
      contains: "ADR-0040"
    - path: ".planning/REQUIREMENTS.md"
      provides: "WEB-01..04 checked with Complete traceability, and WEB-02's manifest citation corrected at source"
      contains: "ADR-0041"
    - path: ".planning/decisions/PROMOTION.md"
      provides: "Index rows for 0040, 0041 and 0042 and the advanced next-free number"
      contains: "0043"
  key_links:
    - from: ".planning/ledgers/milestone-09-12.md"
      to: ".planning/decisions/0040-opaque-bearer-token-mechanism.md"
      via: "the two variant-group-29 rows cite the record that settles the mechanism"
      pattern: "ADR-0040"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/decisions/0041-in-process-token-store-single-replica-scope.md"
      via: "WEB-02's dated banner and its closure both point at the record carrying the deviation reasoning"
      pattern: "ADR-0041"
    - from: ".planning/decisions/PROMOTION.md"
      to: ".planning/decisions/0042-llm-native-tool-calling-deferred.md"
      via: "the index row and the advanced next-free line"
      pattern: "0042"
  prohibitions:
    - requirement_id: "WEB-03"
      category: "transparency"
      statement: "MUST NOT present plan 02-02's closure of WEB-03 as a human-ratified decision — the record must carry forward that it ran under --auto, because laundering the provenance of an unratified decision is exactly the failure D-00i exists to prevent"
---

<objective>
Close the record for all four requirements this phase owns, amending in place rather than annotating
from the side (D-00d).

Six ledger rows carry `Verified open` or `Contract diverges` pointers into this phase; each now has an
answer and needs its verdict amended, dated, with its superseded text retained and the ADR or commit
that settles it cited. One of them — WEB-03's — needs particular care: its substance shipped in
Phase 2 plan 02-02 under `--auto`, and D-00i requires that provenance carried forward rather than
laundered into a bare `Complete`. WEB-02's own requirement text cites the wrong pair of Kubernetes
manifests and gets a dated correction banner at source (D-08). `REQ-fail-closed-auth-posture` moves off
`Verified open` because plan 14-04 gave it the test that drives a real refusal, which is what this
project's evidence bar asks for.

This plan is the **single writer** of all three files it touches, which is why they are gathered here
rather than distributed across the plans that produced their content.

Purpose: leave the corpus describing what this phase actually did, with each claim citable.
Output: six amended ledger rows, four closed requirements, one corrected citation, and an advanced ADR
index.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-api-contract-truthfulness/14-CONTEXT.md
@.planning/phases/14-api-contract-truthfulness/14-01-SUMMARY.md
@.planning/phases/14-api-contract-truthfulness/14-02-SUMMARY.md
@.planning/phases/14-api-contract-truthfulness/14-04-SUMMARY.md
@.planning/phases/14-api-contract-truthfulness/14-05-SUMMARY.md
@.planning/phases/14-api-contract-truthfulness/14-06-SUMMARY.md
</context>

## Artifacts this phase produces

This plan creates **no new files and no new symbols**. It amends three existing planning documents in
place. The three ADR paths it indexes were created by plans 14-05 and 14-06:
`.planning/decisions/0040-opaque-bearer-token-mechanism.md`,
`.planning/decisions/0041-in-process-token-store-single-replica-scope.md`,
`.planning/decisions/0042-llm-native-tool-calling-deferred.md`.

<tasks>

<task type="auto">
  <name>Task 1: Amend the six ledger rows in place, dated, with provenance carried forward</name>
  <read_first>
    - `.planning/ledgers/milestone-09-12.md` — the six rows this phase resolves (`REQ-opaque-bearer-token-adapter-v1`, `REQ-jwt-bearer-auth-v2`, `REQ-fail-closed-auth-posture`, `REQ-k8s-manifests`, `REQ-llm-tool-calling-port`, `REQ-llm-tool-calling-adapters`), the `REQ-health-ready-endpoints` row that cross-references WEB-02, the forward-scope pointer table near the top that lists the two tool-calling rows, the legend that fixes the permitted verdicts, and at least two rows already amended under this convention so the new amendments match their form
    - `.planning/phases/14-api-contract-truthfulness/14-CONTEXT.md` — D-00d (amend in place, dated, superseded text retained), D-00e (no closure claim without the command or `file:line` that produced it), D-00i (carry `--auto` provenance forward), D-14 (WEB-03's already-shipped half)
    - All five sibling SUMMARY files listed in `<context>` — every command quoted in an amendment must come from one of them, run in this phase, not re-derived from memory
    - `.planning/decisions/0040-opaque-bearer-token-mechanism.md`, `0041-...md`, `0042-...md` — the records the amendments cite, for their exact filenames and titles
  </read_first>
  <files>.planning/ledgers/milestone-09-12.md</files>
  <action>
Amend six rows in place — never a separate corrections file, never a new row (D-00d). Each amendment
is dated, names the closing plan, retains the superseded text, and carries the exact command or
`file:line` that produced its claim (D-00e).

`REQ-opaque-bearer-token-adapter-v1` and `REQ-jwt-bearer-auth-v2` — the two halves of variant group 29.
Both currently read as a contract divergence pointing at WEB-01. Amend both to record the group as
settled by ADR-0040 in favour of the Milestone 9 mechanism, with the Milestone 12 vocabulary corrected
to match by plan 14-01. Keep the two rows separate and give each its own verdict, per D-00f — the
mechanism half and the vocabulary half are different requirements and must not be merged. Cite the
commands from `14-01-SUMMARY.md` that prove the correction landed, and record the two consumer breaks.

`REQ-fail-closed-auth-posture` — currently `Verified open` because the code path existed with nothing
exercising it. Amend it behind plan 14-04's new test that drives the `Err` branch and observes a real
refusal, naming the test and quoting the command from `14-04-SUMMARY.md`. Say explicitly what changed:
not the code path, but the proof. Also record that the disabled-mode warning the row notes as
unexercised now has a sibling that is exercised — the in-process-store warning — and state plainly
whether the disabled-mode warning itself remains unexercised, rather than letting the reader infer it.

`REQ-k8s-manifests` — currently shipped with the correctness question handed to WEB-02. Amend it to
record the answer: ADR-0041, the deliberate departure from WEB-02's literal exits, the unconditional
startup warning, and the three artefacts that state the limitation. Record explicitly that the shipped
replica count is unchanged and why that is correct, since the row's own text sets up the opposite
expectation. Cite `git diff --exit-code` on the two Deployment manifests from `14-04-SUMMARY.md`.

`REQ-llm-tool-calling-port` — amend the capability half from `Verified open` to record ADR-0042's
deferral with its named trigger, and cite plan 14-03's reachability rustdoc as the current-functionality
correction on the same file.

`REQ-llm-tool-calling-adapters` — this row already carries a split verdict, and the amendment must
preserve the split rather than collapse it. For the flag half: record it closed by plan 14-02's flip of
the last asymmetric capability flag and the extended correspondence test that now pins both flags,
quoting the test command from `14-02-SUMMARY.md`. **Carry the provenance forward (D-00i, D-14):** the
row's existing text already records that the first half shipped in Phase 2 plan 02-02 under commit
`a2cc1c5`; the amendment must state that plan 02-02 ran under `--auto` and was not human-ratified, so
the closure is recorded with that qualification rather than as a bare `Complete`. For the capability
half: record ADR-0042's deferral, matching the sibling row.

Also update the forward-scope pointer table near the top of the ledger where it routes the two
tool-calling rows to this phase, so a reader arriving there is sent to the ADRs rather than to an open
requirement.

Finally, recompute the ledger's verdict distribution and row total by **counting** requirement-keyed
rows after the amendments rather than assuming the amendments left the counts unchanged — this is the
exact error a prior phase's close-out recorded. Update the summary figures to the counted values, and
record the counting command in the SUMMARY.

Use only verdicts the ledger's own legend permits; invent no new verdict class.
  </action>
  <verify>
    <automated>for r in REQ-opaque-bearer-token-adapter-v1 REQ-jwt-bearer-auth-v2 REQ-fail-closed-auth-posture REQ-k8s-manifests REQ-llm-tool-calling-port REQ-llm-tool-calling-adapters; do grep -q "$r" .planning/ledgers/milestone-09-12.md || exit 1; done &amp;&amp; grep -q 'ADR-0040' .planning/ledgers/milestone-09-12.md &amp;&amp; grep -q 'ADR-0041' .planning/ledgers/milestone-09-12.md &amp;&amp; grep -q 'ADR-0042' .planning/ledgers/milestone-09-12.md &amp;&amp; grep -q 'a2cc1c5' .planning/ledgers/milestone-09-12.md</automated>
  </verify>
  <acceptance_criteria>
    - All six named rows still exist and each appears exactly once: for each requirement key, `grep -c '| \?REQ-...' ` style lookup returns 1 row occurrence in the ledger's row table
    - `grep -c 'ADR-0040' .planning/ledgers/milestone-09-12.md` is at least 2 (both variant-group-29 rows)
    - `grep -c 'ADR-0041' .planning/ledgers/milestone-09-12.md` is at least 1 and appears on the `REQ-k8s-manifests` row
    - `grep -c 'ADR-0042' .planning/ledgers/milestone-09-12.md` is at least 2 (both tool-calling rows)
    - The `REQ-llm-tool-calling-adapters` row names the `--auto` provenance of plan 02-02 alongside commit `a2cc1c5`: `grep 'REQ-llm-tool-calling-adapters' .planning/ledgers/milestone-09-12.md | grep -c 'auto'` is at least 1 (D-00i, D-14)
    - The `REQ-fail-closed-auth-posture` row cites the new test by name and no longer reads `Verified open`
    - `git diff --numstat -- .planning/ledgers/milestone-09-12.md` shows deleted lines only where an amended row's single physical line was rewritten in place — no row is removed and no row is added: the file's requirement-keyed row count is identical before and after, verified by counting and recorded in the SUMMARY
    - The recomputed verdict distribution and row total match a fresh count of requirement-keyed rows, with the counting command recorded verbatim
    - Every verdict used appears in the ledger's own legend — no sixth verdict class was invented
    - Every command quoted in an amendment appears verbatim in one of this phase's SUMMARY files
  </acceptance_criteria>
  <done>Six rows carry dated in-place verdicts citing ADR-0040, ADR-0041 or ADR-0042; WEB-03's closure carries its `--auto` provenance rather than a laundered Complete; and the row counts were recomputed by counting (D-00d, D-00e, D-00i, D-14).</done>
</task>

<task type="auto">
  <name>Task 2: Close WEB-01..04 at source, correct WEB-02's manifest citation, and advance the ADR index</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the WEB-01 through WEB-04 entries in the "API contract truthfulness" section, the "Hand-off to Phase 14" block earlier in the file (whose items 5 and 6 are the two D-15 items this phase adopted), the traceability table rows for the four WEB requirements, and at least two existing dated correction banners elsewhere in the file so the new one matches their form
    - `.planning/decisions/PROMOTION.md` — the ADR index table, the `**Next free ADR number:**` state line, the dated note beneath it explaining the previous multi-step advance (the model for this one), and the procedure section near the end
    - `.planning/phases/14-api-contract-truthfulness/14-CONTEXT.md` — D-08 (correct WEB-02's citation at source; leave the root placeholder alone), D-00c and D-00d (annotation discipline), D-15 (both hand-off items in scope)
    - `.planning/phases/14-api-contract-truthfulness/14-05-SUMMARY.md` and `14-06-SUMMARY.md` — the three ADRs' exact filenames and one-line descriptions for the index rows
    - All five sibling SUMMARY files — the closing evidence each requirement's checkbox stands on
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/decisions/PROMOTION.md</files>
  <action>
Close the four requirements at source and advance the decision index.

WEB-02's citation (D-08): add a dated correction banner on the WEB-02 entry recording that its cited
manifests are the wrong pair — the Milestone 12 Epic 7 artefacts it describes are the ones under the
server subdirectory, not the top-level ones — with the original citation retained and marked
superseded. Record why the top-level placeholder is deliberately out of scope: it runs a placeholder
image under a sleep command with its probes commented out, so nothing in it can issue or verify a
token. Follow the file's existing banner form (D-00c/D-00d): additive, dated, original text retained,
never a rewrite. Point at ADR-0041.

Close the four requirements: check the WEB-01 through WEB-04 boxes and set each traceability row to
`Complete`, each with dated provenance naming its closing plan and its citable artefact — WEB-01 to
plan 14-01 and ADR-0040; WEB-02 to plan 14-04 and ADR-0041; WEB-03 to plan 14-02, with the same
`--auto` provenance qualification the ledger row carries (D-00i, D-14) — do not let the checkbox say
something cleaner than the ledger row; WEB-04 to plans 14-03 and 14-06 and ADR-0042.

Before checking any box, confirm each requirement's done-when is actually met by the phase's evidence
rather than by the plans having run. WEB-02's is the one to check most carefully: its literal
done-when offers two exits and this phase took neither, so its closure rests on ROADMAP criterion 2's
second clause and on ADR-0041's recorded reasoning. Record that basis explicitly in WEB-02's closure
note rather than checking the box silently against text it does not literally satisfy.

Close the hand-off block's two adopted items: item 5 (the compiled example's unversioned route) against
plan 14-02's corrected literal and its assertion tying that literal to the route-prefix constant,
recording that this moves the Phase 13 threat from accepted residual risk to closed and naming the
`/gsd-secure-phase 13` re-run that records it; and item 6 (the fail-closed posture's missing test)
against plan 14-04's test. Amend in place, dated, original text retained.

Advance the ADR index in `PROMOTION.md`: add one index row per record — 0040, 0041, 0042 — each with
its slug, a one-line description and its owning phase and plan, matching the form of the 0037-0039
rows immediately above. Advance the `**Next free ADR number:**` state line past the three, and add a
dated note beneath it explaining the three-step advance and naming the plan that authored each record,
following the form of the existing dated note that explains the previous multi-step advance. Advance
the line **exactly once** — this plan is the phase's only writer of this file. Leave the file's
procedure section untouched: a prior phase recorded that editing that prose to satisfy a grep count is
an unauthorised edit, and the state line is the thing that carries the real state.
  </action>
  <verify>
    <automated>grep -q 'Next free ADR number: 0043' .planning/decisions/PROMOTION.md &amp;&amp; for n in 0040 0041 0042; do grep -q "| $n |" .planning/decisions/PROMOTION.md || exit 1; done &amp;&amp; grep -c '^- \[x\] \*\*WEB-0[1-4]\*\*' .planning/REQUIREMENTS.md | grep -q '^4$' &amp;&amp; grep -q 'ADR-0041' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*WEB-0[1-4]\*\*' .planning/REQUIREMENTS.md` is 4 and `grep -c '^- \[ \] \*\*WEB-0[1-4]\*\*' .planning/REQUIREMENTS.md` is 0
    - The four traceability rows for WEB-01..04 read `Complete`: `grep -E '^\| WEB-0[1-4] ' .planning/REQUIREMENTS.md | grep -c 'Complete'` is 4
    - WEB-02's entry carries a dated correction banner naming the server-subdirectory manifests and pointing at ADR-0041, with the original citation retained — `git diff --numstat -- .planning/REQUIREMENTS.md` shows deleted lines only on the checkbox and traceability-row lines that were rewritten in place
    - WEB-02's closure note states the basis for closure explicitly (ROADMAP criterion 2's second clause plus ADR-0041), rather than implying the literal done-when was satisfied
    - WEB-03's closure note names the `--auto` provenance of plan 02-02: the WEB-03 region matched by `grep -A12 '\*\*WEB-03\*\*' .planning/REQUIREMENTS.md | grep -ci 'auto'` is at least 1
    - The hand-off block's items 5 and 6 are each marked closed with dated provenance naming the closing plan
    - `grep -c 'Next free ADR number: 0043' .planning/decisions/PROMOTION.md` is 1, and no line still declares an earlier next-free value
    - The index table has one new row per record: `grep -c '^| 0040 |' .planning/decisions/PROMOTION.md`, `'^| 0041 |'` and `'^| 0042 |'` each return 1
    - Each new index row names an ADR file that exists: every slug in those three rows resolves under `.planning/decisions/` via `test -e`
    - The PROMOTION.md procedure section is byte-identical to its pre-task state (verified in the diff)
    - `git status --porcelain .planning/` lists only the three files this plan owns as modified, and no new file
  </acceptance_criteria>
  <done>All four WEB requirements are checked with Complete traceability behind named evidence, WEB-02 carries its dated citation correction and an explicit basis-for-closure note, both Phase 13 hand-off items are closed against the work that closed them, and the ADR index advances exactly once with a row per record (D-08, D-14, D-15, D-16).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ledger verdict -> future phase scoping | A row's verdict is what a later phase reads to decide whether work remains; a wrongly-closed row causes shipped-work re-planning or, worse, an unclosed gap recorded as closed |
| Requirement checkbox -> milestone audit | The checkbox and traceability row are what a milestone audit counts; a box checked ahead of its evidence falsifies the audit |
| Decision index -> future ADR numbering | `PROMOTION.md`'s next-free line is the only allocator; a stale or double-advanced value causes a numbering collision or a gap |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-26 | Repudiation | WEB-03's closure record | high | mitigate | Presenting an `--auto` plan's output as a ratified decision manufactures authority the corpus does not contain — the exact failure D-00i names. Both the ledger row and the requirement's closure note must carry the provenance, and the acceptance criteria check for it in both places independently so one cannot be cleaner than the other |
| T-14-27 | Tampering | WEB-02's checkbox | high | mitigate | WEB-02's literal done-when was not satisfied; closing it silently would record a false satisfaction. The closure note must state the basis explicitly (the ROADMAP criterion's second clause plus ADR-0041's reasoning), so a reader can audit the judgement instead of inheriting it |
| T-14-28 | Tampering | ledger row counts | medium | mitigate | A prior phase recorded that assuming counts survive an amendment is wrong. The counts are recomputed by counting requirement-keyed rows and the counting command is recorded, so the summary figures are measured rather than carried |
| T-14-29 | Spoofing | `PROMOTION.md` next-free number | medium | mitigate | Three records were authored against reserved numbers before the allocator advanced. This plan is the phase's single writer of the file, advances the line exactly once, and the acceptance criteria require every indexed slug to resolve on disk so no row points at a record that was never written |
| T-14-30 | Information Disclosure | amended planning records | low | accept | The amendments quote commands, file paths and verdicts; no credential, key name value or token appears in any of them |
| T-14-SC | Tampering | cargo dependency graph | low | accept | Planning-record-only plan; no manifest is touched and no package is installed |
</threat_model>

<verification>
- All six ledger rows carry a dated amendment citing an ADR authored by this phase
- The ledger's requirement-keyed row count is unchanged and was verified by counting, with the command recorded
- All four WEB checkboxes are checked and all four traceability rows read `Complete`
- WEB-02 carries its dated citation correction and its explicit basis-for-closure note
- `grep -c 'Next free ADR number: 0043' .planning/decisions/PROMOTION.md` is 1
- Every ADR slug indexed in `PROMOTION.md`'s three new rows resolves under `.planning/decisions/`
- `git status --porcelain` for this plan lists exactly three modified files and no new file
</verification>

<success_criteria>
The corpus describes what this phase did: six ledger rows amended in place with dated verdicts and
carried-forward provenance; four requirements checked with `Complete` traceability behind named
evidence; WEB-02's manifest citation corrected at source with the original retained and its
non-literal basis for closure stated; both Phase 13 hand-off items closed; and the ADR index advanced
exactly once with a resolving row per record.
</success_criteria>

<output>
Create `.planning/phases/14-api-contract-truthfulness/14-07-SUMMARY.md` when done. Record the
row-counting command and its before/after output, the `git diff --numstat` for all three files, and
the `test -e` sweep over the three indexed ADR slugs.
</output>
