---
phase: 15-coverage-ci-quality-gates
plan: 04
type: execute
wave: 4
depends_on: [15-03]
files_modified:
  - docs/src/contributing/testing-guide.md
  - CLAUDE.md
  - .github/copilot-instructions.md
  - .planning/codebase/TESTING.md
autonomous: false
requirements: [PIPE-03, PIPE-05]

must_haves:
  truths:
    - "The threshold printed in `docs/src/contributing/testing-guide.md` is the same integer `--fail-under-lines` carries in ci.yml and the Makefile — one number in four places, and nowhere a fifth."
    - "The document states the floor arithmetic — truncate the measured percentage toward zero to a whole percent, comparison at-or-above — rather than only the resulting number, so a reader can re-derive the floor after the next measurement."
    - "No file a contributor or an agent session reads before writing a test asserts a coverage target other than ADR-0006's single binding floor."
    - "`cargo-llvm-cov` is named as the tool of record in all three instruction files, and any surviving mention of a different coverage tool is explicitly labelled as not producing the CI-comparable number."
    - statement: "A contributor who has never run coverage locally can follow docs/src/contributing/testing-guide.md end to end and arrive at the same number the CI coverage job reports, with no undocumented prerequisite step."
      verification: backstop
    - statement: "The percentage make coverage reports and the percentage the CI coverage job reports for the same commit agree to the precision the gate compares at (whole percent), or the difference is recorded together with its cause."
      verification: backstop
  artifacts:
    - path: "docs/src/contributing/testing-guide.md"
      provides: "the Code Coverage section a contributor reproduces the CI number from"
      contains: "cargo-llvm-cov"
    - path: "CLAUDE.md"
      provides: "corrected coverage-target statement citing ADR-0006"
      contains: "ADR-0006"
  key_links:
    - from: "docs/src/contributing/testing-guide.md"
      to: ".planning/decisions/0006-coverage-gate.md"
      via: "the documented threshold and its derivation rule cite the ADR as the binding record"
      pattern: "ADR-0006"
    - from: "docs/src/contributing/testing-guide.md"
      to: "Makefile"
      via: "the documented local procedure is the make targets, not a hand-typed cargo invocation"
      pattern: "make coverage"
---

<objective>
Close PIPE-05 and D-14: write the coverage documentation a contributor can reproduce the CI number
from, and remove the rejected coverage figure from the three files every agent session and every
contributor reads before writing a test.

Two requirement premises are corrected rather than followed. PIPE-05 says the section lands in
`CONTRIBUTING.md` — **that file does not exist**; Milestone 11 relocated contributor documentation
into the mdbook, and `docs/src/contributing/testing-guide.md` already exists and is the landing
target (D-13). No root-path file Milestone 11 deliberately removed is reintroduced. PIPE-05 also
says to update `cargo tarpaulin` references "in contributor documentation" — there are none there;
the only ones in the tree are in `.planning/codebase/TESTING.md`, which ADR-0006 already flags.

D-14's scope is **coverage-number claims only**. General content currency is DOCS-01 in Phase 16 —
do not widen into it.

Purpose: the record stops contradicting the gate.
Output: one documented, reproducible procedure and three corrected instruction files.

**Executor note:** `.planning/config.json` sets `workflow.worktree_skip_hooks: true`. Honor it.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/decisions/0006-coverage-gate.md
@.planning/phases/15-coverage-ci-quality-gates/15-CONTEXT.md
@.planning/phases/15-coverage-ci-quality-gates/15-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The Code Coverage section in the contributor testing guide</name>
  <read_first>
    - `docs/src/contributing/testing-guide.md` — full read. Note the existing `## Test Coverage` section (around line 394) with its `### Measuring Coverage`, `### Coverage Configuration` and `### Exclude from Coverage` subsections, and the `### Coverage Requirements` subsection under `## Testing Philosophy` (around line 62). Both carry claims this task replaces. Also note the `## Table of Contents` near line 28, which must stay in sync with any heading change.
    - `docs/src/contributing/development-setup.md` — so the prerequisites section here points at it rather than duplicating it.
    - `.planning/decisions/0006-coverage-gate.md` § Phase 15 amendment — the figure, the floor, the derivation rule and the scope. This is the source of every number in the document.
    - `Makefile` § `##@ Coverage` and the `test-cli` / `bench-check` / `ci-full` targets — the commands the document tells a contributor to run.
    - `.github/workflows/ci.yml` — the `coverage` job, so the documented local procedure is provably the same procedure.
  </read_first>
  <files>docs/src/contributing/testing-guide.md</files>
  <action>
    Rewrite the existing `## Test Coverage` section of `docs/src/contributing/testing-guide.md` so a
    contributor who has never measured coverage can reproduce the CI number from the page alone.
    Extend the page; do not create a new one and do not reintroduce a root `CONTRIBUTING.md`. Keep
    the `## Table of Contents` in sync with any heading you change.

    The section covers, in order:

    **Prerequisites.** `rustup component add llvm-tools-preview` and
    `cargo install cargo-llvm-cov --locked`, with a pointer to `cargo binstall` as the faster path.
    Name `llvm-tools-preview` explicitly and say why: `cargo llvm-cov` cannot instrument without it
    and fails immediately with a missing-component error. State that Redis and MinIO must be running
    for the figure to match CI, and that `make services-up` starts them.

    **Local generation.** `make services-up`, then `make coverage` for the LCOV report and the
    threshold check, then `make coverage-html` for a browsable report at `target/coverage`. Give the
    full underlying `cargo llvm-cov` invocation too, so a reader can see that the make target is a
    thin wrapper and not a different measurement.

    **The scope, and why it is that scope.** `--workspace --features integration-tests`, not
    `--all-features`: `qdrant` needs a live Qdrant service and the vision/embedding suites need real
    provider API keys, so under `--all-features` that code enters the denominator with nothing able
    to exercise it. Note that the three binaries are feature-gated behind `cli` and `web-server` and
    therefore sit outside the denominator by construction, matching `.codecov.yml`'s `src/bin/**`
    ignore entry. Note whether `#[ignore]`-gated tests are in the numerator, matching what the
    ADR-0006 amendment records.

    **The threshold policy.** The floor integer from the ADR-0006 Phase 15 amendment, and the rule
    that produced it: the measured percentage truncated toward zero to a whole percent — explicitly
    neither round-half-up nor round-half-even — compared at-or-above, so a run measuring exactly the
    floor passes and one measuring a hundredth below fails. Cite ADR-0006 as the binding record and
    state that it is the single number: there is no separate unit target and no separate integration
    target. Explain the ratchet: the floor rises at a qualifying milestone close, by amending
    ADR-0006 in place, and never falls.

    **Reading the output.** How to read the LCOV summary line, where the HTML report lands, and what
    the region / function / line columns mean — with the explicit warning that only the **line**
    figure is what the gate compares.

    **Codecov behaviour.** Codecov posts a PR comment and a diff view and **does not gate**;
    `.codecov.yml` sets both status blocks informational. State the reason plainly: without
    `CODECOV_TOKEN` an upload can fail silently, especially on fork PRs, and a gate that silently
    does not run is worse than no gate. The gate is `cargo llvm-cov --fail-under-lines` inside the
    workflow, which is the same flag `make coverage` runs.

    **Troubleshooting.** Four concrete failures with their fixes: the missing-component error when
    `llvm-tools-preview` is absent; a local number lower than CI when the services are not up; low
    patch coverage on a PR and what to do about it; and an upload failure when `CODECOV_TOKEN` is
    unset, which is not a build failure.

    Then update the `### Coverage Requirements` subsection under `## Testing Philosophy` so it states
    ADR-0006's single binding floor instead of a separate unit target and integration target. Where
    the page names a coverage tool other than `cargo-llvm-cov`, keep the mention only if it is
    explicitly labelled as an informal alternative that does not produce the CI-comparable number —
    the two instrumentation approaches are not comparable, which is exactly why ADR-0006 designates
    one tool of record.
  </action>
  <verify>
    <automated>python3 -c "
import re
d=open('docs/src/contributing/testing-guide.md').read()
adr=open('.planning/decisions/0006-coverage-gate.md').read().split('## Phase 15 amendment')[1]
floor=sorted(set(re.findall(r'--fail-under-lines[ =](\d+)',adr)))
assert floor, 'no floor in ADR Phase 15 amendment'
assert floor[0] in d, 'documented threshold does not match ADR floor '+floor[0]
for tok in ['llvm-tools-preview','make coverage','make coverage-html','make services-up','ADR-0006','integration-tests','CODECOV_TOKEN']:
    assert tok in d, 'missing: '+tok
print('ok, floor',floor[0])"</automated>
    <automated>test "$(grep -cE 'unit[^.]{0,30}(≥|&gt;=)[^0-9]{0,4}80|integration[^.]{0,30}(≥|&gt;=)[^0-9]{0,4}70' docs/src/contributing/testing-guide.md)" -eq 0 &amp;&amp; echo ok</automated>
    <automated>python3 -c "
import re
d=open('docs/src/contributing/testing-guide.md').read()
toc=re.search(r'## Table of Contents(.*?)\n## ',d,re.S).group(1)
heads=[m.group(1).strip() for m in re.finditer(r'^## (.+)$',d,re.M) if m.group(1).strip() not in ('Table of Contents',)]
missing=[h for h in heads if h.lower().replace(' ','-') not in toc.lower() and h not in toc]
assert not missing, 'headings absent from ToC: '+str(missing)
print('toc in sync')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/src/contributing/testing-guide.md` contains the floor integer that appears in the ADR-0006 Phase 15 amendment, and contains no other whole-percent coverage threshold presented as binding.
    - The page contains all of: `llvm-tools-preview`, `cargo install cargo-llvm-cov --locked`, `make services-up`, `make coverage`, `make coverage-html`, `--features integration-tests`, `ADR-0006`, `CODECOV_TOKEN`, `src/bin`.
    - The page states the derivation rule using the words "truncat" and "at-or-above".
    - `grep -cE 'unit[^.]{0,30}(≥|>=)[^0-9]{0,4}80|integration[^.]{0,30}(≥|>=)[^0-9]{0,4}70' docs/src/contributing/testing-guide.md` returns `0`.
    - The page states that Codecov does not gate, and names `cargo llvm-cov --fail-under-lines` as what does.
    - Any coverage tool named other than `cargo-llvm-cov` appears within one sentence of a statement that it does not produce the CI-comparable number.
    - The troubleshooting subsection lists at least four distinct failure modes, each with a concrete fix.
    - Every `## ` heading in the file appears in the `## Table of Contents` list.
    - No root `CONTRIBUTING.md` is created: `test ! -f CONTRIBUTING.md` succeeds.
    - `mdbook build docs` (or the project's existing docs build command) exits 0 if that toolchain is available; otherwise the file's Markdown parses without an unterminated code fence — `python3 -c "assert open('docs/src/contributing/testing-guide.md').read().count('\`\`\`') % 2 == 0"` succeeds.
  </acceptance_criteria>
  <done>A contributor can reproduce the CI coverage number from this page alone, and the page's threshold is the gate's threshold.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the rejected coverage figure in the three instruction files</name>
  <read_first>
    - `CLAUDE.md` — run `grep -n '80%\|70%\|Coverage targets' CLAUDE.md` first and work from the live line numbers, not the cited ones. Note that this file is loaded into every agent session in this repository.
    - `.github/copilot-instructions.md` — `grep -n 'coverage' .github/copilot-instructions.md`; the claim sits under a Coverage Requirements heading in the TDD section.
    - `.planning/codebase/TESTING.md` — `grep -n 'coverage\|tarpaulin\|Enforced in CI'`; this file carries three distinct defects, not one.
    - `.planning/decisions/0006-coverage-gate.md` — the Considered Options section, where the separate-unit-and-integration-target position is recorded as explicitly rejected, and § Phase 15 amendment for the current floor.
    - `.github/workflows/ci.yml` — the `coverage` job, which is what makes any "enforced in CI" claim true.
  </read_first>
  <files>CLAUDE.md, .github/copilot-instructions.md, .planning/codebase/TESTING.md</files>
  <action>
    All three files assert a two-number coverage position — a separate unit-test target and a separate
    integration-test target — that is position 1 in ADR-0006's Considered Options and was
    **explicitly rejected**. Replace that claim in each file with ADR-0006's single binding workspace
    floor, naming the integer, citing `ADR-0006` as the record, naming `cargo-llvm-cov` as the tool of
    record, and naming this phase's actual CI mechanism (`cargo llvm-cov --fail-under-lines` in the
    `coverage` job) as what enforces it. Re-grep each file before editing; every cited line number in
    the requirement and context documents predates later edits.

    `CLAUDE.md` is the highest-consequence of the three because it is loaded into every agent session
    in this repository — an agent that reads a rejected target before writing a test reproduces the
    contradiction. Keep the edit to the coverage sentence; leave the surrounding TDD guidance
    (Red-Green-Refactor, doc tests on public APIs) intact.

    `.github/copilot-instructions.md` carries the same claim under its Coverage Requirements heading;
    apply the same replacement. Because `CLAUDE.md` imports this file, the two must not end up
    stating different numbers.

    `.planning/codebase/TESTING.md` carries three defects, and all three are in scope because all
    three are coverage-number or coverage-tool claims. First, the same rejected two-number position.
    Second, it documents `cargo tarpaulin` as the measurement tool — ADR-0006's tool-of-record note
    already flags this; tarpaulin uses ptrace-based instrumentation and produces numbers that are not
    comparable with LLVM source-based instrumentation, so it may be retained only if explicitly
    labelled as an informal alternative that does not produce the CI number. Third, it claims coverage
    is "Enforced in CI pipeline" — a claim that was false when written and becomes **true** because of
    this phase; keep it and make it precise by naming the `coverage` job and the `--fail-under-lines`
    flag.

    Apply D-00c: annotate rather than rewrite where a claim is being corrected in a source document —
    a dated correction naming what was wrong and pointing at ADR-0006, with the original text retained
    and marked superseded. Apply D-00d for `.planning/codebase/TESTING.md` specifically, which is a
    ledger-class document.

    Do not widen beyond coverage-number and coverage-tool claims. General content currency in these
    files is DOCS-01, Phase 16.
  </action>
  <verify>
    <automated>python3 -c "
import re
adr=open('.planning/decisions/0006-coverage-gate.md').read().split('## Phase 15 amendment')[1]
floor=sorted(set(re.findall(r'--fail-under-lines[ =](\d+)',adr)))[0]
for f in ['CLAUDE.md','.github/copilot-instructions.md','.planning/codebase/TESTING.md']:
    t=open(f).read()
    assert 'ADR-0006' in t, f+': missing ADR-0006 citation'
    assert floor in t, f+': missing floor '+floor
    assert 'cargo-llvm-cov' in t or 'cargo llvm-cov' in t, f+': missing tool of record'
print('all three cite ADR-0006, the floor and the tool of record')"</automated>
    <automated>test "$(grep -cE 'Unit tests?:? *(≥|&gt;=) *80|Integration tests?:? *(≥|&gt;=) *70' CLAUDE.md .github/copilot-instructions.md .planning/codebase/TESTING.md | grep -v ':0$' | wc -l)" -eq 0 &amp;&amp; echo ok</automated>
    <automated>grep -q 'coverage' .github/workflows/ci.yml &amp;&amp; grep -q 'fail-under-lines' .github/workflows/ci.yml &amp;&amp; echo "the enforced-in-CI claim is now true"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'Unit tests?:? *(≥|>=) *80|Integration tests?:? *(≥|>=) *70' CLAUDE.md .github/copilot-instructions.md .planning/codebase/TESTING.md` returns `0` for all three files.
    - Each of the three files contains the string `ADR-0006`, the floor integer from the Phase 15 amendment, and a reference to `cargo-llvm-cov` or `cargo llvm-cov`.
    - The three files state the **same** floor integer as each other, as the ADR, as ci.yml and as the Makefile — one number, six places.
    - `.planning/codebase/TESTING.md`'s "Enforced in CI pipeline" claim names the `coverage` job and the `--fail-under-lines` flag.
    - Any `tarpaulin` mention surviving in `.planning/codebase/TESTING.md` appears within one sentence of a statement that it does not produce the CI-comparable number.
    - `.planning/codebase/TESTING.md` carries a dated correction note naming what was wrong and pointing at ADR-0006, with the superseded text retained and marked.
    - `git diff --stat` shows changes confined to the coverage-claim regions: no edit to `CLAUDE.md`'s workspace-layout table, quick-command reference, slash-commands or MCP sections, and none to `.github/copilot-instructions.md` outside its coverage subsection.
    - `CLAUDE.md` still imports `.github/copilot-instructions.md`, and the two do not state different numbers.
  </acceptance_criteria>
  <done>No file an agent or a contributor reads before writing a test contradicts the gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm a developer can reproduce the CI number locally</name>
  <what-built>
    The full PIPE half is now live: a `coverage` job gating on `--fail-under-lines`, `cli-tests` and
    `bench-check` jobs, an `actionlint` job, `.codecov.yml`, five Makefile targets, the ADR-0006
    Phase 15 amendment, a rewritten Code Coverage section in the contributor testing guide, and three
    corrected instruction files.
  </what-built>
  <how-to-verify>
    This is PIPE-05's literal "done when" and PIPE-03's whole purpose, and it needs a machine with
    Docker — which no authoring environment in this phase has.

    1. On a machine with Docker available, check out the phase branch.
    2. Open `docs/src/contributing/testing-guide.md` and follow the Code Coverage section from the
       top, running only the commands it gives you. Do not fill in any step from prior knowledge — if
       you need a command the page does not give, that is a defect in the page.
    3. Record the line-coverage percentage `make coverage` reports.
    4. Compare it against the percentage the CI `coverage` job reports for the same commit:
       `gh run list --workflow=ci.yml --limit 5`, then
       `gh run view <run-id> --log --job <coverage-job-id> | grep -E 'TOTAL|[0-9]+\.[0-9]+%' | tail -10`
    5. State whether the two agree at whole-percent precision. If they differ, state the difference
       and, if you can identify it, the cause — a scope mismatch, a service that was not up, a
       different toolchain version.
    6. Confirm `make ci-full` completes, and that `make test-cli` and `make bench-check` each pass.

    Reply with: the local figure, the CI figure, whether they agree at whole-percent precision, any
    step the page failed to give you, and the result of `make ci-full`.
  </how-to-verify>
  <resume-signal>Type "approved" with the two figures, or describe the discrepancy or the missing documentation step.</resume-signal>
  <acceptance_criteria>
    - A local `make coverage` figure and a CI `coverage` job figure for the same commit are both stated.
    - Whether they agree at whole-percent precision is stated explicitly; if not, the difference and its cause (or an explicit "cause not identified") is recorded.
    - Whether any command needed to reach the number was absent from the documentation page is stated explicitly.
    - The result of `make ci-full`, `make test-cli` and `make bench-check` is stated.
    - Any documentation defect found is either fixed in `docs/src/contributing/testing-guide.md` before this plan closes, or recorded as an open item with an owner.
  </acceptance_criteria>
  <done>The documented procedure is proven to reproduce the CI number, or the gap between them is recorded with its cause.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Instruction files → every agent session | `CLAUDE.md` and `.github/copilot-instructions.md` are read by every agent before it writes code in this repository |
| Published documentation → contributor behaviour | The testing guide is what a new contributor follows instead of reading the workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-11 | Spoofing | A stale coverage target in an instruction file impersonating the binding gate | high | mitigate | All three instruction files are corrected to cite ADR-0006's single floor by integer, with a machine check asserting the same integer appears in all six locations (three instruction files, the ADR, ci.yml, the Makefile) and that the rejected two-number position matches nowhere. `CLAUDE.md` is prioritised because it is loaded into every session. |
| T-15-12 | Repudiation | A correction that erases what was wrong, leaving no audit trail | medium | mitigate | D-00c/D-00d annotation rather than rewriting: a dated correction naming the defect and pointing at ADR-0006, with the superseded text retained and marked, in the ledger-class file. |
| T-15-13 | Information Disclosure | Documenting a `CODECOV_TOKEN` value rather than its name | medium | mitigate | The troubleshooting subsection names the secret and describes the symptom of its absence; an acceptance criterion requires the token be referenced by name only. No token value, and no example value, appears in any file this plan touches. |
| T-15-14 | Tampering | Scope creep past coverage claims into unrelated documentation content | low | mitigate | `git diff --stat` confinement is an acceptance criterion; general content currency is explicitly DOCS-01 in Phase 16. |
</threat_model>

<verification>
- The floor integer from ADR-0006's Phase 15 amendment appears in the testing guide and all three instruction files.
- `grep -cE 'Unit tests?:? *(≥|>=) *80|Integration tests?:? *(≥|>=) *70'` returns 0 across all four files.
- All three instruction files cite `ADR-0006` and `cargo-llvm-cov`.
- No root `CONTRIBUTING.md` exists.
- Every `##` heading in the testing guide appears in its Table of Contents; code fences balance.
- The human-verify checkpoint records both figures and their agreement.

## Manual-only verification

`make coverage` reproducing the CI number requires Docker, which no authoring environment in this
phase has (verified 2026-08-13). Task 3 is that verification, and it is why this plan is not
autonomous. The two `backstop` truths in `must_haves` are exactly what it confirms; absent a reply
they abstain rather than pass.
</verification>

<success_criteria>
- One documented procedure that produces the CI number, with prerequisites, scope, threshold policy, output reading, Codecov behaviour and troubleshooting.
- The rejected two-number coverage position removed from all three instruction files.
- `cargo-llvm-cov` named tool of record in all three.
- The "enforced in CI" claim made true and made precise.
- No root `CONTRIBUTING.md` reintroduced; no widening into DOCS-01's scope.
</success_criteria>

## Artifacts this phase produces

Created by this plan:

| Kind | Symbol |
|---|---|
| Doc subsection | the rewritten `## Test Coverage` body in `docs/src/contributing/testing-guide.md` (prerequisites, local generation, scope, threshold policy, reading the output, Codecov behaviour, troubleshooting) |
| Doc correction note | a dated D-00c correction banner in `.planning/codebase/TESTING.md` |
| Recorded claim | ADR-0006's floor integer restated in `CLAUDE.md`, `.github/copilot-instructions.md` and `.planning/codebase/TESTING.md` |

Deliberately **not** created: no root `CONTRIBUTING.md`, no new docs page, no second threshold value.

## Recorded discretion resolutions

- **The `make coverage` / `make services-up` relationship (Claude's Discretion).** Resolved in plan
  15-01 as **fail loudly with a pointer** rather than a declared Make dependency: `make coverage`
  asserts the services are reachable and exits nonzero naming `make services-up`. A declared
  dependency would silently start containers as a side effect of measuring, which is surprising in a
  target a contributor runs to read a number. The documentation written here states the two-step
  sequence explicitly.

<output>
Create `.planning/phases/15-coverage-ci-quality-gates/15-04-SUMMARY.md` when done. Record the two
figures from the Task 3 checkpoint and any documentation defect it surfaced.
</output>
