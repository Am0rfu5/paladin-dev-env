---
phase: 15.1-git-ci-governance
plan: 03
type: execute
wave: 3
depends_on: ["15.1-02"]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/feature-flags.yml
  - .github/workflows/release.yml
autonomous: true
requirements: [SC5]

must_haves:
  truths:
    - "No job in ci.yml, feature-flags.yml or release.yml restores its cargo dependencies through a cache key with no fallback — the pattern that makes every run after a lockfile change a cold rebuild is gone from the repository, not just from the one job that failed loudest."
    - "The broken pattern is no longer present in the file as the thing to copy: a contributor adding a job by imitating its neighbours now inherits the fallback-capable cache action."
    - "Every migrated job still restores both the cargo home and the build-artefact directory it restored before — the migration does not silently narrow what is cached."
    - "The one cache block in the repository that already carried a fallback and additionally caches a non-cargo path is left intact, because it is not an instance of the defect."
    - statement: "Cache keys remain differentiated per matrix leg, so a ten-crate matrix and a two-toolchain matrix do not collapse onto one shared cache entry."
      verification: backstop
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "fallback-capable dependency caching across every job that caches"
      contains: "Swatinem/rust-cache@v2"
    - path: ".github/workflows/feature-flags.yml"
      provides: "the same migration across the 14-job feature matrix"
      contains: "Swatinem/rust-cache@v2"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: ".github/workflows/feature-flags.yml"
      via: "both files converge on one cache mechanism so a future job copied between them inherits the correct pattern"
      pattern: "Swatinem/rust-cache@v2"
---

<objective>
Widen **D-10** from the single job that hard-fails to the class. Plan `15.1-01` fixed the one job
whose four hermetic build steps turn a cache miss into a download error; this plan replaces the
remaining hand-rolled, key-only cache blocks across `ci.yml`, `feature-flags.yml` and `release.yml`
with the action that handles keys, fallback restoration and artefact pruning correctly.

**Scoping the fix to the failing job alone was explicitly rejected**, and the reasons are worth
restating because they are what makes this an expansion task rather than an optional tidy-up. First,
twenty-plus jobs otherwise keep cold-rebuilding on every dependency change — a cost that stops being
theoretical the moment **D-05** makes most of them required checks standing between every merge and
the merge button. Second, the broken pattern stays in the file as the thing the next contributor
copies. Note also that the action being replaced now emits a Node 20 deprecation warning on every
run, so the migration retires that too.

Purpose: SC5 closed at its class, not at its instance.
Output: the key-only cache pattern is gone from the three files that carry it.

**Executor note:** `workflow.worktree_skip_hooks: true` — commit with the hook bypassed. This plan
changes no Rust; run no `cargo build`, `cargo test` or `cargo clippy`. Every verify below is a
sub-second YAML parse.

**Anchors, not line numbers.** Job offsets in `ci.yml` have already drifted away from the numbers
cited in this phase's own CONTEXT, RESEARCH and PATTERNS documents, and those three disagree with
each other. Locate every cache block by re-grepping for its `uses:` value and its enclosing job id;
do not navigate by any absolute line number carried in from a planning document.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.1-git-ci-governance/15.1-CONTEXT.md
@.planning/phases/15.1-git-ci-governance/15.1-RESEARCH.md
@.planning/phases/15.1-git-ci-governance/15.1-PATTERNS.md
@.planning/phases/15.1-git-ci-governance/15.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate every remaining hand-rolled cache block in ci.yml</name>
  <read_first>
    - `.github/workflows/ci.yml` — enumerate the work before starting: `grep -n "uses: actions/cache@v4" .github/workflows/ci.yml` and, for each hit, read the surrounding step (its `name:`, its `path:` list and its `key:` expression) and identify the enclosing job id. Fourteen blocks are expected after plan `15.1-01` removed three from the `examples` job; if the count differs, reconcile before editing rather than assuming.
    - `.github/workflows/ci.yml` — the `examples` job as plan `15.1-01` left it. This is the in-repository reference shape for the replacement: one step named `Rust cache` using the new action. Copy that shape rather than inventing a second one.
    - `.github/workflows/ci.yml` — the `crate-isolation` job's `strategy.matrix` (ten `crate:` entries) and the `test` job's `strategy.matrix` (`rust-version: [stable, beta]`), plus the cache `key:` expressions inside each, so you can see how those two jobs currently differentiate their cache entries per leg.
    - `.planning/phases/15.1-git-ci-governance/15.1-RESEARCH.md` §2 — the action's input table, in particular which inputs are already correct at their defaults and therefore must not be set redundantly.
    - `.github/workflows/pre-commit.yml` — the step named `Cache pre-commit environments`. Read it only to confirm it is out of scope for this plan and remains untouched.
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    Replace every remaining hand-rolled cache step in `.github/workflows/ci.yml` with a single step
    per job named `Rust cache`, `uses: Swatinem/rust-cache@v2`, positioned where the block it
    replaces sat — after the toolchain installation step and before the first step that invokes
    cargo. Where a job currently carries two or three consecutive cache steps splitting the cargo
    registry, the cargo git checkouts and the build-artefact directory across separate entries,
    collapse them into the one step: the action manages all three paths itself.

    Do not set `path:`, `key:` or `restore-keys:` on the new step — those are the hand-rolled
    concerns the action exists to take over. Its automatic key already folds in the job identity and,
    with its rust-environment hash input at its default, the toolchain version and host triple. That
    default is what keeps the two toolchain matrix legs on distinct cache entries without a manual
    key suffix, and what keeps each of the ten crate-matrix legs distinct. Confirm this by reading
    the input table rather than adding a redundant explicit key "to be safe" — a redundant key is not
    harmful but it reintroduces exactly the hand-maintained surface this decision removes.

    Two per-job inputs are worth setting and nothing else is. On the jobs that run the broad
    service-backed suite and on any job whose failure still leaves a usefully-populated registry, set
    `cache-on-failure: true`, matching what plan `15.1-01` already set on the examples job. Leave
    every other input at its default, including the workspace-crate input: workspace-owned crate
    artefacts are pruned by design, which matches what the hand-rolled blocks already did by never
    special-casing them.

    Preserve each job's existing step ordering and every non-cache step verbatim. Do not rename jobs,
    do not change any `needs:` edge, do not touch the `services:` blocks, and do not alter any cargo
    invocation. The only structural change is cache steps in, cache steps out.

    Leave `.github/workflows/pre-commit.yml` alone in this plan. Its single cache block already
    carries a fallback and additionally caches a pre-commit environment directory the new action does
    not manage, so it is not an instance of the defect **D-10** names; splitting it into two steps
    would add surface for no benefit. That exclusion is recorded here as a decision, not left as a
    silent absence.
  </action>
  <verify>
    <automated>python3 -c "
import yaml
d=yaml.safe_load(open('.github/workflows/ci.yml'))
bad=[]
rc=0
for jid,j in d['jobs'].items():
    for st in j.get('steps',[]) or []:
        u=st.get('uses','') or ''
        if u.startswith('actions/cache@'): bad.append(jid)
        if u.startswith('Swatinem/rust-cache@v2'): rc+=1
assert not bad, 'hand-rolled cache still in jobs: '+str(sorted(set(bad)))
assert rc>0, 'no rust-cache steps found'
print('ci.yml migrated; rust-cache steps =',rc)"</automated>
    <automated>python3 -c "
import yaml
d=yaml.safe_load(open('.github/workflows/ci.yml'))
for jid,j in d['jobs'].items():
    steps=j.get('steps',[]) or []
    idx=[i for i,s in enumerate(steps) if (s.get('uses','') or '').startswith('Swatinem/rust-cache@v2')]
    assert len(idx)<=1, (jid,'more than one rust-cache step')
    for i in idx:
        assert 'key' not in (steps[i].get('with') or {}), (jid,'explicit key set')
        assert 'path' not in (steps[i].get('with') or {}), (jid,'explicit path set')
print('one unparameterised rust-cache step per caching job')"</automated>
    <automated>python3 -c "
import yaml,hashlib
d=yaml.safe_load(open('.github/workflows/ci.yml'))
m=d['jobs']['crate-isolation']['strategy']['matrix']
inc=m.get('include') or []
assert len(inc)==10, len(inc)
t=d['jobs']['test']['strategy']['matrix']
assert t['rust-version']==['stable','beta'], t
print('matrix shapes preserved')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "uses: actions/cache@v4" .github/workflows/ci.yml` returns `0`.
    - `grep -c "uses: Swatinem/rust-cache@v2" .github/workflows/ci.yml` returns a value greater than or equal to `10` — the migration covered the caching jobs rather than deleting caching from most of them.
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0.
    - No job in the parsed `ci.yml` contains more than one step whose `uses` begins `Swatinem/rust-cache@v2`.
    - No `Swatinem/rust-cache@v2` step in `ci.yml` sets a `key` or a `path` input.
    - The `crate-isolation` job's matrix still declares exactly ten `crate:` entries, and the `test` job's matrix still declares exactly `['stable','beta']` — the migration touched caching only.
    - `git diff --stat -- .github/workflows/ci.yml` shows changes confined to `.github/workflows/ci.yml`, and `git diff -- .github/workflows/ci.yml | grep -c "^[+-].*cargo "` returns `0` — no cargo invocation was altered.
    - `grep -c "uses: actions/cache@v4" .github/workflows/pre-commit.yml` returns `1` — the one legitimately-different block is untouched.
  </acceptance_criteria>
  <done>Every job in ci.yml that caches does so through the fallback-capable action, with no hand-maintained key expression left in the file, and no job's steps, services, needs edges or cargo invocations changed.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate feature-flags.yml and release.yml, closing the class</name>
  <read_first>
    - `.github/workflows/feature-flags.yml` — run `grep -n "uses: actions/cache@v4"` and read each of the four blocks with its enclosing job id and `key:` expression. This file drives a fourteen-job feature matrix, so read its `strategy.matrix` too and note how the existing keys differentiate legs.
    - `.github/workflows/release.yml` — run `grep -n "uses: actions/cache@v4"` and read the single block with its enclosing job id.
    - `.github/workflows/ci.yml` as Task 1 leaves it — the reference shape for the replacement step, so all three files end up identical in form.
    - `.planning/phases/15.1-git-ci-governance/15.1-CONTEXT.md` § the D-10 entry — specifically the reason a fallback-only fix was rejected, so the migration is not quietly reduced to adding a fallback key to the existing blocks.
  </read_first>
  <files>.github/workflows/feature-flags.yml, .github/workflows/release.yml</files>
  <action>
    Apply the identical migration to `.github/workflows/feature-flags.yml` (four blocks) and
    `.github/workflows/release.yml` (one block): one step per job named `Rust cache`,
    `uses: Swatinem/rust-cache@v2`, placed after the toolchain step and before the first cargo
    invocation, with no `path`, `key` or `restore-keys` input.

    For the feature matrix in `feature-flags.yml`, do not add a per-leg key suffix. The action's
    automatic key already differentiates by job identity and, at its default rust-environment hash
    setting, by toolchain — which is what the existing hand-written suffixes were approximating. If a
    leg is differentiated by something the action cannot see, such as a feature-set string that
    changes what gets compiled without changing the lockfile or the toolchain, set the action's
    additional-key input to that leg's matrix value rather than reintroducing a full hand-built key
    expression. Decide this by reading the matrix, not by assuming.

    In `release.yml`, migrate the cache block and change nothing else. That file's tag trigger, its
    `verify-tag-source` job and the ancestry assertion that job enforces are outside this plan's
    scope and outside **D-10**'s. In particular do not touch the documentation pointer this file
    carries in a failure message — plan `15.1-10` owns that redirect, and editing it here would put
    two plans in the same string.

    After both files are migrated, confirm the class is closed rather than relocated: across the
    whole workflows directory, the only surviving hand-rolled cache step should be the pre-commit
    environment block deliberately excluded in Task 1, and no file should carry a hand-written
    `restore-keys` list, because the action supersedes that mechanism entirely.
  </action>
  <verify>
    <automated>python3 -c "
import yaml,glob,os
survivors={}
for f in sorted(glob.glob('.github/workflows/*.yml')):
    d=yaml.safe_load(open(f)) or {}
    for jid,j in (d.get('jobs') or {}).items():
        for st in (j.get('steps') or []):
            if (st.get('uses','') or '').startswith('actions/cache@'):
                survivors.setdefault(os.path.basename(f),[]).append(st.get('name'))
assert set(survivors)=={'pre-commit.yml'}, survivors
assert len(survivors['pre-commit.yml'])==1, survivors
print('only the deliberate exclusion survives:',survivors)"</automated>
    <automated>python3 -c "
import yaml
for f in ['.github/workflows/feature-flags.yml','.github/workflows/release.yml']:
    d=yaml.safe_load(open(f))
    n=sum(1 for j in d['jobs'].values() for s in (j.get('steps') or []) if (s.get('uses','') or '').startswith('Swatinem/rust-cache@v2'))
    assert n>0, (f,'no rust-cache step')
    print(f,'rust-cache steps =',n)"</automated>
    <automated>git diff --name-only | sort | python3 -c "import sys;got=[l.strip() for l in sys.stdin if l.strip()];allowed={'.github/workflows/ci.yml','.github/workflows/feature-flags.yml','.github/workflows/release.yml'};extra=[g for g in got if g not in allowed];assert not extra, extra;print('scope ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "uses: actions/cache@v4" .github/workflows/feature-flags.yml` returns `0` and the same command against `.github/workflows/release.yml` returns `0`.
    - `grep -rl "uses: actions/cache@" .github/workflows/` lists exactly one file, `pre-commit.yml`, and that file contains exactly one such step.
    - `grep -rc "restore-keys" .github/workflows/feature-flags.yml .github/workflows/release.yml .github/workflows/ci.yml` reports `0` for all three — the hand-maintained fallback mechanism is superseded, not layered on top.
    - `python3 -c "import yaml,glob; [yaml.safe_load(open(f)) for f in glob.glob('.github/workflows/*.yml')]"` exits 0.
    - `release.yml`'s `verify-tag-source` job still exists in the parsed document and still contains a step invoking `git merge-base`, proving the release-integrity guard was not disturbed.
    - `git diff --name-only` lists no file outside the three this plan declares as modified.
  </acceptance_criteria>
  <done>The key-only cache pattern no longer exists anywhere in the workflows directory except the one block deliberately excluded with a stated reason, so SC5's failure mode is closed at its class and the broken pattern is no longer available to copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → GitHub Actions marketplace | The replacement cache action executes in every migrated job with repository-scoped credentials |
| CI runner → GitHub Actions cache service | Every migrated job now reads and writes cache entries through the action's own key scheme |
| Release pipeline → CI configuration | `release.yml` is edited, and that file also carries the tag-ancestry guard that keeps a release from being cut off unmerged work |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15.1-SC | Tampering | `Swatinem/rust-cache@v2` now present in three workflow files rather than one | high | mitigate | Same single dependency plan `15.1-01` already audited — no additional third-party action is introduced by widening its use. The reference is a moving major tag, which is the repository's existing convention for its other actions; the audit and verdict are recorded in `15.1-RESEARCH.md` § Package Legitimacy Audit. No package-manager install occurs in this phase. |
| T-15.1-10 | Tampering | Accidental edit to `release.yml`'s tag-ancestry guard while migrating its cache block | high | mitigate | The action forbids touching anything in that file beyond the cache step, and an acceptance criterion asserts the `verify-tag-source` job still contains its `git merge-base` step after the edit. A weakened guard here would let a release tag be cut from unmerged work — the precise thing this phase exists to prevent. |
| T-15.1-11 | Tampering | Cache poisoning across matrix legs if keys collapse | medium | mitigate | Verified structurally: the action's rust-environment hash input stays at its default so toolchain and host enter the key, and both matrix shapes are asserted intact after the edit. A collapsed key would let a `beta` toolchain's artefacts be restored into a `stable` leg. |
| T-15.1-12 | Denial of Service | A migration that silently narrows what is cached, making every required check slower | low | accept | The action caches the cargo home and the build-artefact directory, a superset of what most hand-rolled blocks covered. Where a job cached less before, it now caches more; the cost is cache storage, not correctness. Observed job durations are recorded in this phase's CONTEXT and can be re-measured after the fact if merges feel slow. |
| T-15.1-13 | Repudiation | Reintroducing hand-written fallback keys instead of migrating | medium | mitigate | Explicitly forbidden in the action text and asserted by a criterion requiring zero `restore-keys` occurrences in the three migrated files. A fallback-only fix restores an older lockfile generation that by construction lacks a newly-added crate — it looks like a fix and is not one. |
</threat_model>

## Artifacts this phase produces

| Kind | Symbol / path | Created by |
|------|---------------|-----------|
| Shell script | `scripts/check-workflow-triggers.sh` | 15.1-09 |
| Shell test | `tests/scripts/check-workflow-triggers_test.sh` | 15.1-09 |
| Workflow file | `.github/workflows/benchmarks.yml` | 15.1-06 |
| Ruleset JSON | `.github/rulesets/protect-release-branches.json` | 15.1-04 |
| mdbook page | `docs/src/contributing/branching-model.md` | 15.1-09 |
| Evidence record | `.planning/phases/15.1-git-ci-governance/15.1-CI-EVIDENCE.md` | 15.1-02 |
| Evidence record | `.planning/phases/15.1-git-ci-governance/15.1-RULESET-EVIDENCE.md` | 15.1-08 |
| API coverage matrix | `.planning/phases/15.1-git-ci-governance/COVERAGE.md` | plan-phase |
| ADR | `.planning/decisions/0043-github-flow-trunk-and-trigger-surface.md` | 15.1-10 |
| ADR | `.planning/decisions/0044-branch-protection-posture.md` | 15.1-10 |
| Register row id | `RUSTSEC-2026-0249` in `SECURITY-EXCEPTIONS.md` | 15.1-01 |
| Makefile target | `check-workflow-triggers` | 15.1-09 |
| CI step name | `Fetch dependencies` | 15.1-01 |
| CI step name | `Rust cache` | 15.1-01, 15.1-03, 15.1-05 |
| CI step name | `Check workflow trigger policy` | 15.1-09 |
| CI step name | `Run shell-guard regression tests` | 15.1-09 |
| CI job display name | `Unit Tests (stable)` / `Unit Tests (beta)` | 15.1-05 |
| CI job display name | `Docker Integration Tests` (relocated into `ci.yml`) | 15.1-05 |
| CI job display name | `Performance Benchmarks (Weekly)` | 15.1-06 |
| Deleted file | `.github/workflows/integration-tests.yml` | 15.1-05 |

## Recorded discretion resolutions (this plan)

- **`integration-tests.yml`'s three cache blocks are not migrated here.** That file is deleted by
  plan `15.1-05`, which brings its jobs into `ci.yml` already using the new action. Migrating a file
  scheduled for deletion would be work discarded in the next wave, and would put two plans in the
  same file.
- **`pre-commit.yml` is excluded, with a reason.** It is the one block that already has a fallback
  and it additionally caches a directory the new action does not manage. Recorded rather than left
  silent, consistent with this phase's own governance discipline.

<output>
Create `.planning/phases/15.1-git-ci-governance/15.1-03-SUMMARY.md` when done. Record the before and
after counts of hand-rolled cache steps per file, so the class-closure claim is a number rather than
an assertion.
</output>
