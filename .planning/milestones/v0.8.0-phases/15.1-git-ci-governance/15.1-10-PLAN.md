---
phase: 15.1-git-ci-governance
plan: 10
type: execute
wave: 9
depends_on: ["15.1-09"]
files_modified:
  - .planning/decisions/0043-github-flow-trunk-and-trigger-surface.md
  - .planning/decisions/0044-branch-protection-posture.md
  - .planning/decisions/PROMOTION.md
  - docs/src/appendix/branch-protection.md
  - docs/src/contributing/branching-model.md
  - .github/workflows/release.yml
  - Makefile
  - .planning/ROADMAP.md
  - .planning/ledgers/milestone-09-12.md
autonomous: true
requirements: [SC3, SC4, SC7]

must_haves:
  truths:
    - "The three stale pointers at a root-level protection document that does not exist now name the appendix page that does, so a contributor following a failure message or a make-target comment reaches real content — this is how SC3 actually closes, per D-12, and no root-level file is reintroduced."
    - "The appendix enforcement page describes the repository as it now is: the required-check list actually pinned, the applied status of the rulesets, and the deliberate asymmetry whereby the trunk ruleset carries no bypass while the tag ruleset does."
    - "The appendix page and the contributor-facing branching page link to each other, and both links resolve, so neither page is a dead end."
    - "The contested positions this phase settled are recorded as decision records rather than as prose in a phase folder, per D-00g — the branching model and trigger surface in one, the protection posture in the other."
    - "The next-free-decision-number line in the promotion register names a number no file uses, so the next author does not collide."
    - "Every ROADMAP success criterion whose premise this phase proved false carries a dated in-place amendment with its original text retained and marked superseded, per D-00d — the record is corrected where the claim lives, never in a separate corrections file."
    - "The phase's requirement line states plainly that no requirement identifier exists and that the work is keyed by success criterion, so a later reader does not go looking for identifiers that were never minted."
    - statement: "The user-facing documentation carries no dated correction banner and no retained superseded text, because D-00c scopes that convention to project source annotations and D-12 closes the gap by rewriting book pages clean."
      verification: backstop
  artifacts:
    - path: ".planning/decisions/0043-github-flow-trunk-and-trigger-surface.md"
      provides: "the decision record for the branching model, the trigger surface and the register-plus-guard control"
      contains: "Considered Options"
    - path: ".planning/decisions/0044-branch-protection-posture.md"
      provides: "the decision record for the required-check set, the review count, the bypass removal and the ref coverage"
      contains: "Code Conformance"
    - path: "docs/src/appendix/branch-protection.md"
      provides: "the current admin and enforcement page"
      contains: "branching-model.md"
  key_links:
    - from: "Makefile"
      to: "docs/src/appendix/branch-protection.md"
      via: "the release branch guard's comment and its override message point contributors at the page that exists"
      pattern: "appendix/branch-protection.md"
    - from: "docs/src/appendix/branch-protection.md"
      to: "docs/src/contributing/branching-model.md"
      via: "the enforcement page links to the contributor page and the contributor page links back"
      pattern: "branching-model.md"
---

<objective>
Make the record true, and record the decisions. This is the phase's last plan and it closes SC3.

**D-12 governs how.** Book pages are rewritten clean — no dated correction banner, no retained
superseded text. That convention belongs to two other decisions and neither covers user-facing
documentation: **D-00c** scopes annotation-not-rewriting to corrections under the project source
tree, and **D-00d** scopes in-place dated amendment to ledgers and requirement texts. Neither reaches
`docs/src/`, and D-12 closes that gap explicitly. Provenance for a rewritten book page lives in this
phase's decision records, its ledger row and git history — the designated homes.

**SC3 asks for the wrong artifact.** It asks that a root-level protection document exist and match
reality, on the premise that it does not exist. The premise is false in a specific way: the file does
not exist, but its content does, as a long-standing page in the book's appendix that an earlier
milestone relocated there. **D-04 and D-12 therefore decline to reintroduce the root file**, following
the preceding phase's precedent of not restoring root files a milestone deliberately removed. What
actually closes SC3 is redirecting the three surviving pointers — one in the release workflow's
failure message, two in the release make-target's comment and override message — and bringing the
appendix page current. This plan does both.

**D-00g decides that these get decision records.** Contested positions get records; code-settled
defects get ledger rows and no record. Nothing this phase settled was code-settled: choosing a trunk
model over an integration-branch model, choosing which jobs may block a merge, and choosing to remove
an administrative bypass are all positions that were argued and could have gone the other way. Two
records rather than one or five, because the two supersession units are genuinely distinct — someone
revisiting the branching model should not have to supersede a record that also carries the
required-check set.

**D-00d decides where the ROADMAP corrections go.** Amended in place, dated, superseded text
retained, never a separate corrections file. Four of this phase's seven success criteria rest on
premises the phase disproved, and the requirement line records identifiers that were never minted.

Purpose: SC3 closed as the tree actually needs it closed; the phase's contested decisions recorded
where decisions live; the ledger and roadmap made true.
Output: two decision records, one register line updated, one book page brought current and
cross-linked, three pointers redirected, the roadmap amended, one ledger row.

**This plan lands under the gate it helped build.** Protection has been live since plan `15.1-08`, so
this work reaches the trunk through a pull request with every required check green — including the
trigger guard added at plan `15.1-09`. Run the local gates before pushing.

**Executor note:** `workflow.worktree_skip_hooks: true` — bypass the hook when committing. This plan
changes no Rust; run no `cargo build`, `cargo test` or `cargo clippy`. Locate every edit by anchor —
a heading, a target name, an exact string — never by a line number carried from a planning document.
Three separate documents in this phase have already been caught citing drifted line numbers.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15.1-git-ci-governance/15.1-CONTEXT.md
@.planning/phases/15.1-git-ci-governance/15.1-RESEARCH.md
@.planning/phases/15.1-git-ci-governance/15.1-PATTERNS.md
@.planning/phases/15.1-git-ci-governance/15.1-CI-EVIDENCE.md
@.planning/phases/15.1-git-ci-governance/15.1-RULESET-EVIDENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the two decision records and advance the promotion register</name>
  <read_first>
    - `.planning/decisions/PROMOTION.md` — locate the next-free-number state line by anchor (`grep -n "Next free ADR number" .planning/decisions/PROMOTION.md`) and read the promotion procedure's step that requires updating it. Read enough of the surrounding procedure to update only the state line and not the procedural prose around it.
    - `.planning/decisions/0042-llm-native-tool-calling-deferred.md` — the most recent record, read in full for the house shape: no frontmatter, and the section sequence Status, Context, Decision, Considered Options, Code Locations, Code Conformance, Downstream Consumers.
    - `.planning/decisions/0024-rustsec-exception-governance.md` and `.planning/decisions/0036-audit-suppression-single-source-topology.md` — the governance-flavoured records nearest in subject, and the two the advisory suppression in this phase operates under. Neither is superseded here; cite them as siblings.
    - `.planning/phases/15.1-git-ci-governance/15.1-CONTEXT.md` § Implementation Decisions — the full text of D-01 through D-12 with their reversibility notes and their rejected alternatives, which are the substance of both records.
    - `.planning/phases/15.1-git-ci-governance/15.1-RULESET-EVIDENCE.md` — the applied ruleset state, needed for the second record's conformance section.
    - `.planning/phases/15.1-git-ci-governance/15.1-07-SUMMARY.md` — the fast-forward evidence, needed for the first record's conformance section.
  </read_first>
  <files>.planning/decisions/0043-github-flow-trunk-and-trigger-surface.md, .planning/decisions/0044-branch-protection-posture.md, .planning/decisions/PROMOTION.md</files>
  <action>
    Two records, per **D-00g** — contested positions get records, code-settled defects get ledger rows
    and no record. Both follow the house shape exactly: no frontmatter, and the seven sections in
    order. Keep bullets to a single physical line each; a bullet wrapped across lines inflates the
    parsed counts in this project's own record parser, which has bitten a previous phase.

    **The first record covers the branching model and the trigger surface** — the positions taken in
    D-01, D-02, D-03, D-04, D-09 and D-09b. Status is accepted. Context states what was true before:
    a default branch hundreds of commits behind with nothing ahead, a stale midpoint branch nobody
    used, a versioned branch doing integration duty, a push filter naming four branch prefixes while
    origin carried eight, and two workflows rendering the same two job display names. Decision states
    the trunk model, the match-all push filter with its two named exceptions, the consolidation, and
    the register-plus-guard control. Considered Options must preserve what was rejected and why — the
    integration-branch model, rejected because it makes the CI-exclusive suites run later rather than
    earlier and contradicts the invariant the release tag guard already enforces; keeping the release
    branch as the integration point, rejected for the same reason; enumerating sanctioned prefixes
    across six files, rejected because the next unsanctioned prefix is dark again; and a branch-naming
    CI gate, offered and declined in favour of a documented human convention. Code Locations names the
    workflow files, the guard script, its harness, the make targets and the contributor page. Code
    Conformance states, with the recorded evidence behind it, that the trunk now carries the shipped
    work and both retired refs are gone. Downstream Consumers names the trigger register, the guard,
    and the release tag guard whose ancestry assertion this makes meaningful.

    **The second record covers the protection posture** — D-05, D-06, D-07 and D-08b. Status is
    accepted. Context states that two payloads sat committed and unapplied for months while neither the
    trunk nor the integration branch had any protection at all, and that the committed payload was
    self-defeating: a review requirement of one against a single collaborator, satisfiable only through
    an always-on administrative bypass that also skips the required status checks. Decision states the
    required-check set and its two exclusions, the review count of zero with a pull request still
    mandatory, the bypass removal on the trunk payload, the bypass retention on the tag payload, and
    the pre-emptive coverage of the release-branch ref class. Considered Options preserves the rejected
    alternatives: taking up the native-architecture rework so the container build could join the
    required set, offered with its scope cost stated and declined; preparing the payloads without
    applying them, rejected as reproducing the exact failure the phase exists to fix; and narrowing
    the bypass rather than removing it, rejected because the narrowed form still lets the sole
    administrator skip the checks inside a pull request. Code Locations names the three payload files
    and the appendix page. Code Conformance cites the applied-state read-back verbatim by reference to
    the evidence record. Downstream Consumers names the guard clause that keeps pinned contexts
    resolving to real job names.

    Record in both, briefly, that the measurement behind the two exclusions is the container build's
    3762 seconds against a required-set critical path of roughly seven minutes — an exclusion by
    recorded decision with a number, not by omission.

    Then update the promotion register's next-free-number state line to the first unused number after
    these two. Change only that state line; leave the procedural prose untouched, including any
    passage that mentions numbers in passing — editing procedure text to satisfy a count check is how
    a previous phase nearly made an unauthorised fourth edit to this file.
  </action>
  <verify>
    <automated>ls .planning/decisions/0043-github-flow-trunk-and-trigger-surface.md .planning/decisions/0044-branch-protection-posture.md</automated>
    <automated>python3 -c "
import pathlib
for n in ['0043-github-flow-trunk-and-trigger-surface','0044-branch-protection-posture']:
    t=pathlib.Path('.planning/decisions/%s.md'%n).read_text()
    assert not t.lstrip().startswith('---'), n+' must not carry frontmatter'
    for s in ['Status','Context','Decision','Considered Options','Code Locations','Code Conformance','Downstream Consumers']:
        assert s in t, (n,'missing section',s)
print('both records have the house shape')"</automated>
    <automated>grep -n "Next free ADR number: 0045" .planning/decisions/PROMOTION.md</automated>
  </verify>
  <acceptance_criteria>
    - Both record files exist at the paths named in this plan's frontmatter.
    - Neither file begins with a frontmatter delimiter — the house shape carries none.
    - Each file contains all seven section headings: Status, Context, Decision, Considered Options, Code Locations, Code Conformance, Downstream Consumers.
    - Each file's Considered Options section names at least two rejected alternatives with the reason each was rejected.
    - `grep -c "Next free ADR number: 0045" .planning/decisions/PROMOTION.md` returns `1`.
    - `git diff .planning/decisions/PROMOTION.md` shows exactly one changed line — the state line — and no change to the procedural prose.
    - No bullet in either record wraps across more than one physical line: `python3 -c "import re,pathlib;[print(p) for p in []]"` is not required, but the summary records the per-file bullet counts for Code Locations and Considered Options as a structural self-check.
  </acceptance_criteria>
  <done>The two contested positions this phase settled are recorded where decisions live, in the project's own record shape, with their rejected alternatives preserved — and the next author has a free number.</done>
</task>

<task type="auto">
  <name>Task 2: Redirect the three stale pointers and bring the appendix page current</name>
  <read_first>
    - `.github/workflows/release.yml` — find the pointer by anchor with `grep -n "BRANCH_PROTECTION" .github/workflows/release.yml`. It sits inside the tag-source verification job's failure message. Read the whole message so the redirect reads naturally in context.
    - `Makefile` — find both pointers with `grep -n "BRANCH_PROTECTION" Makefile`. One is a comment above the release branch guard; the other is inside the override hint printed when the guard fails. Read both in context.
    - `docs/src/appendix/branch-protection.md` — the whole page. Note in particular: the three-enforcement-layer table; the ruleset import instructions including the direct API invocation; the note explaining the numeric administrator actor identifier; the section describing a one-time reconciliation of an older tag, which describes a *different*, already-completed reconciliation from an earlier milestone; and the mislabelled link near the end whose text names a contributing file that does not exist in this tree.
    - `docs/src/contributing/branching-model.md` as plan `15.1-09` left it — so the cross-link points at a real heading and the two pages divide responsibility rather than duplicating.
    - `.planning/phases/15.1-git-ci-governance/15.1-RULESET-EVIDENCE.md` — the applied ruleset state, the pinned context list and the verified bypass asymmetry, which is the content the page must now carry.
    - `.planning/phases/15.1-git-ci-governance/15.1-04-SUMMARY.md` — the two exclusions and the measurement behind them.
  </read_first>
  <files>.github/workflows/release.yml, Makefile, docs/src/appendix/branch-protection.md, docs/src/contributing/branching-model.md</files>
  <action>
    Per **D-12**, redirect the pointers and rewrite the page clean.

    First the redirect. Three occurrences reference a root-level protection document that does not
    exist in this tree — one in the release workflow's tag-source failure message, two in the release
    make-target. Replace each with the path to the appendix page that does exist. This is mechanical
    string substitution; change nothing else in either file, and in particular do not touch the
    ancestry assertion in the workflow's verification job or the guard logic in the make-target. Leave
    every other occurrence of that old path elsewhere in the repository alone: the changelog entry
    records a historical release and must not be rewritten, the migration log correctly records the
    relocation as history, and the project-source planning documents are governed by **D-00c**, which
    scopes corrections there to annotation rather than rewriting and is out of this phase's scope. Only
    the three live pointers that send a contributor somewhere that does not exist are in scope.

    Then bring the appendix page current, rewritten clean — no dated correction banner, no retained
    superseded text. That convention is scoped by **D-00c** to project-source corrections and by
    **D-00d** to ledgers and requirement texts; **D-12** settles that it does not extend to book pages,
    whose provenance lives in the two records authored in Task 1 and in git history.

    Add what the page lacks. It currently documents the *mechanism* of protection and predates any
    application of it, so it needs the current state: the required-check list actually pinned, taken
    from the evidence record rather than retyped; the two exclusions with the measurement behind them
    and the statement that both still run on every push and pull request and simply do not block the
    merge button; the applied status with the date; and the bypass asymmetry — the trunk ruleset
    carries no administrative bypass, the tag ruleset deliberately retains one because it restricts
    tag creation and removing it would make cutting any release impossible. That asymmetry must be
    stated plainly; it is the single most misreadable thing on the page.

    Remove the section describing the one-time reconciliation of an older tag. It narrates a different,
    already-completed reconciliation from an earlier milestone, and its own prose asserts the
    reconciliation was one-time — which stopped being true when this phase performed a second one.
    Replace it with a short current account of this phase's trunk fast-forward, citing the evidence
    record rather than restating its numbers.

    Fix the mislabelled link whose text names a contributing file absent from this tree: relabel it to
    name the page it actually targets. Then add the cross-links — the appendix page gains a link to the
    contributor-facing branching page for how to branch and what runs when, and the branching page
    gains a link back to the appendix for the enforcement detail. Both links are relative paths between
    book pages; a broken one fails the documentation build's link checker, which is the correct
    automated proxy for this pair resolving.

    Leave alone the question of whether the appendix as a whole is archive material. That is a later
    phase's scope and this plan deliberately does not pre-commit its answer. Likewise leave the stale
    required-check list on the deployment page beyond what is needed here; a broader currency pass on
    that page belongs to a later documentation phase.
  </action>
  <verify>
    <automated>python3 -c "
import pathlib
for f in ['Makefile','.github/workflows/release.yml']:
    t=pathlib.Path(f).read_text()
    assert 'docs/BRANCH_PROTECTION.md' not in t, f+' still points at a file that does not exist'
    assert 'docs/src/appendix/branch-protection.md' in t, f+' has no redirected pointer'
print('all three pointers redirected')"</automated>
    <automated>python3 -c "
import pathlib
a=pathlib.Path('docs/src/appendix/branch-protection.md').read_text()
b=pathlib.Path('docs/src/contributing/branching-model.md').read_text()
assert 'branching-model.md' in a, 'appendix page does not link to the contributor page'
assert 'branch-protection.md' in b, 'contributor page does not link back'
assert 'v0.4.0' not in a, 'the superseded reconciliation section is still present'
print('cross-links present, superseded section removed')"</automated>
    <automated>grep -c "bypass" docs/src/appendix/branch-protection.md</automated>
  </verify>
  <acceptance_criteria>
    - `Makefile` contains no reference to the non-existent root-level protection document, and contains the appendix path exactly twice — once in the guard comment, once in the override hint.
    - `.github/workflows/release.yml` contains no reference to the non-existent root-level protection document, and contains the appendix path once, inside the tag-source failure message.
    - `.github/workflows/release.yml` still contains its `git merge-base` ancestry assertion, unchanged — the redirect touched a message string and nothing else.
    - No root-level protection document was created: `test ! -e docs/BRANCH_PROTECTION.md` succeeds.
    - `docs/src/appendix/branch-protection.md` links to `contributing/branching-model.md`, and `docs/src/contributing/branching-model.md` links back to `appendix/branch-protection.md`.
    - The appendix page contains the pinned required-check list, the two excluded job names with the 3762-second measurement, and an applied-status statement carrying a date.
    - The appendix page states the bypass asymmetry explicitly, naming which ruleset carries a bypass and which does not, with the reason for each.
    - The section narrating the older one-time tag reconciliation is gone; the page contains no reference to that tag version.
    - The appendix page contains no dated correction banner and no text marked superseded — it is rewritten clean.
    - The previously mislabelled link's text names the page it actually targets.
  </acceptance_criteria>
  <done>A contributor following a failed release guard now lands on a page that exists and is current, the two documentation pages reference each other, and no root-level file was reintroduced — which is how SC3 closes against this tree rather than against its own false premise.</done>
</task>

<task type="auto">
  <name>Task 3: Amend the ROADMAP at source and write the ledger row</name>
  <read_first>
    - `.planning/ROADMAP.md` — the Phase 15.1 entry, located by its heading rather than by line number. Read the goal line, the requirements line, the dependency line and all seven success criteria verbatim.
    - `.planning/phases/15.1-git-ci-governance/15.1-CONTEXT.md` § Specific Ideas — the numbered findings, each of which names the criterion whose premise it disproves and states what is actually true. The amendments are transcriptions of these, not new analysis.
    - `.planning/ledgers/milestone-09-12.md` — the row shape used by the surrounding entries and the existing entry recording the declined native-architecture rework, whose reopening condition this phase sharpens.
    - `.planning/phases/15.1-git-ci-governance/15.1-CI-EVIDENCE.md` and `15.1-RULESET-EVIDENCE.md` — the evidence the amendments and the ledger row cite.
    - `.planning/decisions/0043-github-flow-trunk-and-trigger-surface.md` and `0044-branch-protection-posture.md` as Task 1 leaves them — the ledger row points at these rather than restating them.
  </read_first>
  <files>.planning/ROADMAP.md, .planning/ledgers/milestone-09-12.md</files>
  <action>
    Amend the ROADMAP in place per **D-00d** — dated, with the original text retained and marked
    superseded, and never in a separate corrections file. Four criteria carry premises this phase
    disproved, and each amendment states what was claimed, what is actually true, and the evidence.

    The first criterion claims the trigger surface work remains to be done. Its mechanical half was
    already merged before this phase began: all workflows carried deliberate branch lists with comments
    naming the failure each closed. What remained, and what this phase delivered, is the recorded
    policy and the guard.

    The third criterion asks that a root-level protection document exist and match reality, on the
    premise that it does not exist. The file does not exist; its content does, as the appendix page an
    earlier milestone relocated. **D-04** and **D-12** decline to reintroduce the root file, and the
    criterion is satisfied by the redirect and the currency pass performed in Task 2.

    The fourth criterion frames the reconciliation as a hard problem and cites a commit count. The
    branch relationship was a clean fast-forward with nothing on the trunk that the integration branch
    lacked, the count differs from the one recorded, and the reconciliation was one command executed
    behind a checkpoint because it was irreversible, not because it was difficult.

    The sixth criterion names one dark job. Four jobs were non-continuous and only one was genuinely
    dark; two were revived by the branching model with no edit at all, and one was correctly
    conditional rather than dark. Record all four dispositions.

    Also note against the fifth criterion that its framing understates the defect: the failure is not
    confined to the first run of a new branch but occurs after any lockfile change on any branch, and
    the fix was applied at its class rather than at the one job that failed loudest.

    Then amend the phase's requirements line. It records identifiers as pending minting. Per **D-00f**
    no identifier exists and the work is keyed by success criterion. **Resolve the open question by
    declining to mint**: replace the line with a statement that this phase has no requirement
    identifier and is keyed by success criterion, retaining the original text marked superseded and
    dated. State the reasoning in the summary — minting identifiers retroactively for an inserted
    phase would create entries with no upstream requirement text and no consumer, in a requirements
    document that is corpus-wide rather than phase-local, and the success criteria already carry the
    traceability that identifiers would provide. This is a recorded choice, not an omission.

    Finally write the ledger row for the phase in the milestone ledger, in the shape the surrounding
    entries use. It records what shipped, cites the two evidence records rather than restating their
    contents, and points at the two decision records authored in Task 1 rather than duplicating their
    reasoning. Add one line sharpening the reopening condition on the previously-declined
    native-architecture rework: the container build is now the only correctness-relevant job excluded
    from the merge gate, and closing that gap is what the rework buys. Record separately, as a
    code-settled observation rather than a contested position and therefore as a ledger line with no
    decision record, that the two remaining unpinned toolchain action references are correct rather
    than residue — both pass an explicit toolchain input that the pinned tag cannot accept — so a
    future action-modernisation sweep does not "fix" them into breakage.
  </action>
  <verify>
    <automated>python3 -c "
import pathlib,re
t=pathlib.Path('.planning/ROADMAP.md').read_text()
i=t.find('### Phase 15.1')
assert i>=0, 'phase entry not found'
j=t.find('### Phase',i+5)
sec=t[i:j if j>0 else len(t)]
assert 'TBD' not in sec, 'requirements line still records pending identifiers'
assert 'superseded' in sec.lower(), 'no superseded marker: amendments must retain original text'
assert sec.count('2026-') >= 4, 'amendments must be dated'
print('roadmap amended in place')"</automated>
    <automated>python3 -c "
import pathlib
t=pathlib.Path('.planning/ledgers/milestone-09-12.md').read_text()
for s in ['15.1','0043','0044']:
    assert s in t, 'ledger row missing reference: '+s
print('ledger row present and cross-referenced')"</automated>
    <automated>git diff --name-only</automated>
  </verify>
  <acceptance_criteria>
    - The Phase 15.1 section of `.planning/ROADMAP.md` contains no pending-identifier placeholder on its requirements line.
    - That section contains at least four dated amendments, each retaining the original text and marking it superseded.
    - Each of the four amended criteria states what was claimed, what is actually true, and the evidence for the correction.
    - The requirements line states that no requirement identifier exists for this phase and that the work is keyed by success criterion.
    - Criterion five carries a note that its framing understated the defect, without being counted as one of the four premise corrections.
    - `.planning/ledgers/milestone-09-12.md` contains a row for this phase referencing both decision record numbers and both evidence records.
    - The ledger contains the sharpened reopening condition for the declined native-architecture rework, and the observation that the two remaining unpinned toolchain references are deliberate.
    - `git diff --name-only` lists no file outside those declared in this plan's frontmatter.
    - The phase's own trigger guard still passes: `./scripts/check-workflow-triggers.sh` exits 0 after the release-workflow edit in Task 2.
  </acceptance_criteria>
  <done>Every false premise this phase disproved is corrected where the claim lives, the requirements question is answered rather than left open, and the milestone ledger records what shipped with pointers to the evidence and the decision records instead of a retelling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → contributor behaviour | A failure message and a make-target hint direct a contributor to a document; if it does not exist, the guidance dead-ends at the moment someone is already blocked |
| Phase record → future phases | Decision records and ledger rows are the inputs a later phase reads instead of re-deriving; an inaccurate record propagates |
| Release pipeline → string edit | The redirect touches the file that also carries the tag-ancestry guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15.1-51 | Tampering | Editing the release workflow's guard logic while redirecting a string inside its failure message | high | mitigate | The action restricts the change to the message string and an acceptance criterion asserts the `git merge-base` ancestry assertion is still present afterwards. That assertion is the layer preventing a release tag from being cut off unmerged work; weakening it while editing prose would silently remove the invariant this phase exists to reinforce. |
| T-15.1-52 | Repudiation | A documentation page describing a protection posture that is not the one applied | high | mitigate | The page's new content is taken from the applied-state evidence record rather than retyped from the payload files, so the page describes what the server returned. A page describing an intended posture reads as assurance and provides none — the same class of failure as a ruleset committed and never applied. |
| T-15.1-53 | Repudiation | The bypass asymmetry misread as an oversight and "corrected" by a later reader | high | mitigate | Stated plainly on the page, named in the second decision record's Decision section, and asserted by an acceptance criterion. Removing the tag ruleset's bypass by symmetry would make cutting any release impossible; the reason it is deliberate has to be discoverable at the place someone would go to change it. |
| T-15.1-54 | Repudiation | Rewriting historical records while redirecting live pointers | medium | mitigate | Scope is limited to the three pointers that send a contributor somewhere non-existent. The changelog entry, the migration log and the project-source planning documents are explicitly out of scope — the first two are accurate history and the third is governed by D-00c's annotation-not-rewriting rule. Rewriting history to make a grep clean would destroy provenance. |
| T-15.1-55 | Tampering | Editing procedural prose in the promotion register to satisfy a count | medium | mitigate | The action restricts the edit to the single state line and an acceptance criterion requires the diff to show exactly one changed line. A previous phase recorded nearly making an unauthorised edit to this file for precisely this reason. |
| T-15.1-56 | Information Disclosure | Evidence records cited from user-facing documentation | low | accept | The repository is public and the cited content is ruleset configuration and CI job names, already public through the Actions interface. The page cites the evidence records by reference rather than inlining raw command output, so no incidental environment detail crosses into the published book. |
</threat_model>

## Artifacts this phase produces

| Kind | Symbol / path | Created by |
|------|---------------|-----------|
| Shell script | `scripts/check-workflow-triggers.sh` | 15.1-09 |
| Shell test | `tests/scripts/check-workflow-triggers_test.sh` | 15.1-09 |
| Workflow file | `.github/workflows/benchmarks.yml` | 15.1-06 |
| Ruleset JSON | `.github/rulesets/protect-release-branches.json` | 15.1-04 |
| mdbook page | `docs/src/contributing/branching-model.md` | 15.1-09 |
| Evidence record | `.planning/phases/15.1-git-ci-governance/15.1-CI-EVIDENCE.md` | 15.1-02 |
| Evidence record | `.planning/phases/15.1-git-ci-governance/15.1-RULESET-EVIDENCE.md` | 15.1-08 |
| API coverage matrix | `.planning/phases/15.1-git-ci-governance/COVERAGE.md` | plan-phase |
| ADR | `.planning/decisions/0043-github-flow-trunk-and-trigger-surface.md` | 15.1-10 |
| ADR | `.planning/decisions/0044-branch-protection-posture.md` | 15.1-10 |
| Register row id | `RUSTSEC-2026-0249` in `SECURITY-EXCEPTIONS.md` | 15.1-01 |
| Makefile target | `check-workflow-triggers` | 15.1-09 |
| CI step name | `Fetch dependencies` | 15.1-01 |
| CI step name | `Rust cache` | 15.1-01, 15.1-03, 15.1-05 |
| CI step name | `Check workflow trigger policy` | 15.1-09 |
| CI step name | `Run shell-guard regression tests` | 15.1-09 |
| CI job display name | `Unit Tests (stable)` / `Unit Tests (beta)` | 15.1-05 |
| CI job display name | `Docker Integration Tests` (relocated into `ci.yml`) | 15.1-05 |
| CI job display name | `Performance Benchmarks (Weekly)` | 15.1-06 |
| Deleted file | `.github/workflows/integration-tests.yml` | 15.1-05 |

Modified, not created, by this plan: `.planning/decisions/PROMOTION.md` (state line only),
`docs/src/appendix/branch-protection.md`, `docs/src/contributing/branching-model.md` (back-link only),
`.github/workflows/release.yml` (message string only), `Makefile` (two comment/message strings only),
`.planning/ROADMAP.md`, `.planning/ledgers/milestone-09-12.md`.

## Recorded discretion resolutions (this plan)

- **Two decision records, not one and not five.** The branching model and the protection posture are
  distinct supersession units; a single record would force someone revisiting the trunk model to
  supersede the required-check set as well. Five would fragment reasoning that is genuinely joint.
- **The four false roadmap premises are amended now, not deferred to a later minting exercise.** The
  in-place amendment convention says the record is corrected where the claim lives, and deferring
  would leave four criteria reading as open questions the phase already answered.
- **No requirement identifiers are minted.** Recorded as an answer rather than an omission: the
  requirements document is corpus-wide, an inserted phase's retroactive identifiers would have no
  upstream text and no consumer, and success-criterion keying already carries the traceability.
- **The dispositions for the feature-matrix workflow and the pre-commit workflow stay open.** They
  have register rows, which makes the questions visible; answering them is a later phase's scope and
  is already recorded in this phase's deferred list.
- **The stale required-check list on the deployment page is left to a later documentation phase.**
  Reconciling it beyond what the redirect requires belongs with that page's broader currency pass.

<output>
Create `.planning/phases/15.1-git-ci-governance/15.1-10-SUMMARY.md` when done. Record the two decision
record numbers, the promotion register's new state value, the four amended criteria with their dates,
the requirements-minting answer and its reasoning, the three redirected pointer locations, and the
per-file bullet counts from Task 1's structural self-check. Note whether this plan's own pull request
passed every required check on the first attempt — that observation closes the phase's dogfooding
loop.
</output>
