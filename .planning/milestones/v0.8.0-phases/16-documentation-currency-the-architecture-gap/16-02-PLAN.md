---
phase: 16-documentation-currency-the-architecture-gap
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - docs/src/user-guides/orchestration.md
  - docs/src/user-guides/maneuver-flow-dsl.md
  - docs/src/user-guides/memory-management.md
  - .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md
autonomous: true
requirements: [DOCS-01]

must_haves:
  truths:
    - "All eight signal classes of the 16-01 battery are run against each of orchestration.md, maneuver-flow-dsl.md and memory-management.md, and each file's prose is read end to end for statements the 0.8.0 tree contradicts (D-12)"
    - "docs/src/user-guides/maneuver-flow-dsl.md no longer pins paladin-battalion at a version the workspace does not ship — the dependency example matches the shipped 0.8.0 lockstep version"
    - "Every cited crates/*.rs or src/*.rs path in the three files resolves on disk, proven by a recorded per-path `test -f`"
    - "Three verdict rows replace three `pending` rows in 16-DOCS-01-VERDICTS.md, each carrying the producing command or file:line for every finding (D-00e, D-09)"
    - "`mdbook build docs/` exits 0 after the edits, and no file outside the three named pages is modified (D-12: no style rewrite, no collateral edits)"
  artifacts:
    - path: ".planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md"
      provides: "Rows 1-3 of the fourteen — the user-guides group, part A"
      contains: "orchestration.md"
  key_links:
    - from: "docs/src/user-guides/maneuver-flow-dsl.md"
      to: "Cargo.toml"
      via: "the quoted dependency pin must match the workspace's shipped version"
      pattern: "paladin-battalion"
---

<objective>
Settle three of the fourteen by content (D-09, D-12): `docs/src/user-guides/orchestration.md` (315
lines), `docs/src/user-guides/maneuver-flow-dsl.md` (1,156 lines) and
`docs/src/user-guides/memory-management.md` (1,165 lines).

Purpose: DOCS-01 is the only open checkbox count in all 542 that survives verification, and it is
settled by content rather than by the files existing. Presence and mtime are not evidence.
Output: three corrected pages and three evidence-bearing verdict rows.

**Scope guards:** D-12 — mechanical signals exhaustively, prose read for contradictions, **no style
rewrite**. D-00e — every finding carries its producing command. D-00o — commit with `--no-verify`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-documentation-currency-the-architecture-gap/16-CONTEXT.md
@.planning/phases/16-documentation-currency-the-architecture-gap/16-RESEARCH.md
@.planning/phases/16-documentation-currency-the-architecture-gap/16-01-PLAN.md
@.planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md
</context>

## Artifacts this phase produces (plan 16-02 scope)

This plan creates **no new symbol, file or path**. It appends three rows to the phase artifact
`16-DOCS-01-VERDICTS.md` (created in 16-01) and edits three existing mdBook pages in place. The
phase-wide artifact table lives in `16-01-PLAN.md` § "Artifacts this phase produces".

<tasks>

<task type="auto">
  <name>Task 1: Sweep orchestration.md and maneuver-flow-dsl.md against the 0.8.0 tree</name>

  <files>docs/src/user-guides/orchestration.md, docs/src/user-guides/maneuver-flow-dsl.md, .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md</files>

  <read_first>
    - `docs/src/user-guides/orchestration.md` — all 315 lines. The file being modified.
    - `docs/src/user-guides/maneuver-flow-dsl.md` — all 1,156 lines. The file being modified.
      Landmark: the dependency-pin block around line 55.
    - `.planning/phases/16-documentation-currency-the-architecture-gap/16-01-PLAN.md` § "The DOCS-01
      signal battery" — the eight commands and the M-06 crate-name-vs-Kubernetes-object warning.
    - `.planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md` — the
      record's declared header, column shape and row order.
    - `Cargo.toml` (root) — line 34 carries the workspace version (`0.8.0`); all eleven crate
      manifests ship in lockstep at that figure.
    - `crates/paladin-battalion/Cargo.toml` — the crate whose version the DSL guide pins.
  </read_first>

  <action>
Run the full eight-class signal battery from 16-01 against each file, recording every command and its
output as you go — the commands are the evidence, not a summary of them (D-00e).

For each finding, correct the page to the tree's actual value. The known-genuine one, re-derive
before fixing: `maneuver-flow-dsl.md` quotes a `paladin-battalion` dependency pin at a version the
workspace has not shipped since before 0.8.0 — replace the quoted version with the workspace's
current lockstep figure from root `Cargo.toml:34`, keeping the surrounding `path = ` form intact.

For class 3 (crate names), classify every `paladin-*` hit as a **crate name** or a **Kubernetes /
config object name** before touching it. M-06 measured that a naive sweep is mostly false positives;
a wrong "fix" here silently corrupts a working manifest example.

For class 4, run `test -f` against every cited `crates/…/*.rs` and `src/…/*.rs` path and record the
result per path. M-06 measured module-path drift as minimal, so a passing `test -f` is a real
`current` finding and must be recorded as such, not omitted for being uneventful.

Read each file's prose end to end for statements the 0.8.0 tree contradicts — a described behaviour,
a named type, a claimed default. Correct contradictions in place. Do **not** restructure sections,
retitle headings, reorder content or re-tone sentences (D-12): Milestone 11 Epic 3 already rewrote
this corpus once, and a rewrite makes a currency fix indistinguishable from a style change.

Write the two verdict rows into `16-DOCS-01-VERDICTS.md`, replacing those files' `pending` rows in
place and preserving the declared row order. Verdict is `current` (every checked signal matched, with
the commands shown) or `updated → commit` (with each edit's before/after evidence). A `current` row
with an empty Findings cell is invalid.
  </action>

  <acceptance_criteria>
    - `grep -nE 'paladin-battalion *= *\{ *version' docs/src/user-guides/maneuver-flow-dsl.md` shows a version string identical to the `version` value at root `Cargo.toml:34`.
    - Every path matched by `grep -ohE '(crates|src)/[A-Za-z0-9_/.-]+\.rs' docs/src/user-guides/orchestration.md docs/src/user-guides/maneuver-flow-dsl.md | sort -u` exists on disk (a `test -f` loop over that list exits 0).
    - `16-DOCS-01-VERDICTS.md` rows for `orchestration.md` and `maneuver-flow-dsl.md` are no longer `pending`, and each Findings cell contains at least one executable command string.
    - `mdbook build docs/` exits 0.
    - `git diff --name-only` lists only the two guides and the verdict record.
  </acceptance_criteria>

  <verify>
    <automated>for p in $(grep -ohE '(crates|src)/[A-Za-z0-9_/.-]+\.rs' docs/src/user-guides/orchestration.md docs/src/user-guides/maneuver-flow-dsl.md | sort -u); do test -f "$p" || { echo "MISSING $p"; exit 1; }; done &amp;&amp; test "$(grep -c 'pending — not yet checked' .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md)" = 11 &amp;&amp; mdbook build docs/</automated>
  </verify>

  <done>Both guides are checked against every signal class with the commands recorded, their genuine drift is corrected in place, and their two verdict rows carry evidence rather than assertion.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep memory-management.md against the 0.8.0 tree</name>

  <files>docs/src/user-guides/memory-management.md, .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md</files>

  <read_first>
    - `docs/src/user-guides/memory-management.md` — all 1,165 lines. The file being modified.
    - `.planning/phases/16-documentation-currency-the-architecture-gap/16-01-PLAN.md` § "The DOCS-01
      signal battery".
    - `crates/paladin-memory/src/lib.rs` and `crates/paladin-memory/Cargo.toml` — the crate the guide
      documents: its shipped module names, feature flags and error types are the comparison target
      for signal classes 3, 7 and 8.
    - `.planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md`.
  </read_first>

  <action>
Run the same eight-class battery against `memory-management.md`, recording every command.

Pay particular attention to classes 7 and 8 here — this guide is the corpus's densest source of
error-type and feature-flag references. For each `*Error` / `*Error::Variant` token the battery
surfaces, prove it still exists with a recorded `grep -rn` into `crates/` or `src/`, and correct or
remove any that no longer resolves. For each `--features` / `feature = "…"` token, compare against
the `[features]` block of the crate the surrounding text is about, and record the comparison.

Garrison and Sanctum are both memory-side subsystems in this repo's ubiquitous language (D-00h); if
the guide's prose describes one where the tree ships the other, that is a content contradiction and
is corrected — but only the statement, not the surrounding structure (D-12).

Write the third verdict row, replacing this file's `pending` row in place.
  </action>

  <acceptance_criteria>
    - Every `*Error` token matched by `grep -ohE '[A-Z][A-Za-z]*Error' docs/src/user-guides/memory-management.md | sort -u` is found by a recorded `grep -rn` in `crates/` or `src/`, or has been corrected/removed; the verdict row states which, per token.
    - Every feature name the guide names for a crate appears in that crate's `Cargo.toml` `[features]` block, with the comparison command recorded in the row.
    - The `memory-management.md` row in `16-DOCS-01-VERDICTS.md` is no longer `pending` and its Findings cell contains at least one executable command string.
    - `mdbook build docs/` exits 0 and `git diff --name-only` lists only this guide and the verdict record.
  </acceptance_criteria>

  <verify>
    <automated>test "$(grep -c 'pending — not yet checked' .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md)" = 10 &amp;&amp; test "$(grep -c '^| docs/src/' .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md)" = 14 &amp;&amp; mdbook build docs/</automated>
  </verify>

  <done>`memory-management.md` is checked against every signal class with the commands recorded, its error-type and feature-flag references are proven against the tree, and its verdict row carries evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation source → published book | These three pages render to the public GitHub Pages site; a wrong dependency pin or feature flag published here becomes a user's failed build. |
| verdict record → downstream verification | The rows written here are the evidence a later verifier trusts for DOCS-01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05 | Repudiation | `16-DOCS-01-VERDICTS.md` rows 1-3 | medium | mitigate | Each row's Findings cell carries the producing command inline (D-00e); a `current` verdict with an empty Findings cell is defined invalid by the record's own header. |
| T-16-07 | Tampering | published documentation content | medium | mitigate | Every corrected value is taken from the live tree at execute time (`Cargo.toml`, the crate's `[features]`, a `grep -rn` for the error type), never from research prose; `mdbook build docs/` with `warning-policy = "error"` gates each edit. |
| T-16-08 | Tampering | crate-name sweep false positives | medium | mitigate | Class 3 requires each `paladin-*` hit be classified as crate name vs. Kubernetes/config object **before** editing (M-06 measured a naive sweep as mostly false positives); acceptance criteria assert path resolution, not blanket substitution. |
| T-16-SC | Tampering | package-manager installs | low | mitigate | This plan installs nothing. No new dependency enters any manifest; `make audit` / `make deny` posture is unchanged. |
</threat_model>

<verification>
```
mdbook build docs/
git diff --name-only
grep -c 'pending — not yet checked' .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md
```
Commit each task separately with `git commit --no-verify` (D-00o).
</verification>

<success_criteria>
- Three of the fourteen are settled by content, each with all eight signal classes run and recorded.
- The `paladin-battalion` dependency pin in the DSL guide matches the shipped workspace version.
- Three `pending` rows have become evidence-bearing verdict rows; eleven remain explicitly pending.
- No page outside the three named files is modified (D-12).
</success_criteria>

<output>
Create `.planning/phases/16-documentation-currency-the-architecture-gap/16-02-SUMMARY.md` when done.
Record, per D-00e, the verbatim command and output behind each finding, and state for each of the
three files whether its verdict is `current` or `updated → commit` and why.
</output>
</content>
