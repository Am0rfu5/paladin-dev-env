---
phase: 16-documentation-currency-the-architecture-gap
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - docs/src/user-guides/tool-integration.md
  - docs/src/user-guides/paladin-configuration.md
  - docs/src/user-guides/output-formatting.md
  - .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md
autonomous: true
requirements: [DOCS-01]

must_haves:
  truths:
    - "All eight signal classes of the 16-01 battery are run against tool-integration.md, paladin-configuration.md and output-formatting.md, and each file's prose is read end to end for statements the 0.8.0 tree contradicts (D-12)"
    - "Every configuration key the configuration guide documents is proven present in the shipped settings type or its config file, with the proving command recorded per key"
    - "Every Herald formatter and output format the output-formatting guide names resolves in crates/paladin-herald, with the proving command recorded"
    - "Three more `pending` rows become evidence-bearing verdict rows; eight remain pending (D-09)"
    - "`mdbook build docs/` exits 0 after the edits, and no file outside the three named pages is modified (D-12)"
  artifacts:
    - path: ".planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md"
      provides: "Rows 4-6 of the fourteen — the user-guides group, part B"
      contains: "tool-integration.md"
  key_links:
    - from: "docs/src/user-guides/paladin-configuration.md"
      to: "src/config/"
      via: "documented configuration keys must exist in the shipped settings type"
      pattern: "config"
---

<objective>
Settle three more of the fourteen by content (D-09, D-12): `docs/src/user-guides/tool-integration.md`
(1,069 lines), `docs/src/user-guides/paladin-configuration.md` (480 lines) and
`docs/src/user-guides/output-formatting.md` (1,127 lines). This closes the six-file user-guides group
that Milestone 11's task 6.0 left open.

Purpose: these three are the corpus's densest concentration of *checkable configuration surface* —
MCP/Arsenal wiring, `config.yml` keys, and Herald output formats. Each is a claim the tree can
confirm or contradict, and none of them is settled by the file existing.
Output: three corrected pages and three evidence-bearing verdict rows.

**Scope guards:** D-12 — no style rewrite. D-00e — every finding carries its producing command.
D-00o — commit with `--no-verify`. Phase 15.1 already repaired this file's one broken link
(`tool-integration.md:324`, commit `d87d11e`); the book is green on entry and must stay green.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-documentation-currency-the-architecture-gap/16-CONTEXT.md
@.planning/phases/16-documentation-currency-the-architecture-gap/16-RESEARCH.md
@.planning/phases/16-documentation-currency-the-architecture-gap/16-01-PLAN.md
@.planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md
</context>

## Artifacts this phase produces (plan 16-03 scope)

No new symbol, file or path. Three rows appended to the phase artifact `16-DOCS-01-VERDICTS.md`
(created in 16-01) and three existing mdBook pages edited in place. The phase-wide artifact table
lives in `16-01-PLAN.md` § "Artifacts this phase produces".

<tasks>

<task type="auto">
  <name>Task 1: Sweep tool-integration.md and paladin-configuration.md against the 0.8.0 tree</name>

  <files>docs/src/user-guides/tool-integration.md, docs/src/user-guides/paladin-configuration.md, .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md</files>

  <read_first>
    - `docs/src/user-guides/tool-integration.md` — all 1,069 lines. The file being modified.
    - `docs/src/user-guides/paladin-configuration.md` — all 480 lines. The file being modified.
    - `.planning/phases/16-documentation-currency-the-architecture-gap/16-01-PLAN.md` § "The DOCS-01
      signal battery" — the eight commands and the M-06 crate-name warning.
    - `.planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md`.
    - `src/config/` — the shipped configuration surface; the settings type and its serde field names
      are the comparison target for every documented `config.yml` key.
    - `src/infrastructure/adapters/arsenal/` — the shipped Arsenal/MCP adapters (including
      `mcp_streamable_http_adapter.rs`); the comparison target for every transport and adapter name
      the tool-integration guide claims.
  </read_first>

  <action>
Run the full eight-class signal battery from 16-01 against each file, recording every command and its
output (D-00e).

Beyond the battery, both files carry a *structured* claim surface that needs a per-item check:

- **`paladin-configuration.md`** — extract every configuration key the guide documents (the YAML keys
  in its examples and the keys named in prose) and prove each one against the shipped settings type
  in `src/config/`, recording the proving command per key. A documented key with no matching field is
  a content defect: correct the key to the shipped name, or remove the claim if the feature is gone.
  Also check the reverse direction for the sections the guide claims to cover completely — if the
  guide says it documents a section exhaustively and the tree has fields it omits, that claim is the
  defect, not the missing fields.
- **`tool-integration.md`** — extract every MCP transport, adapter name and tool-registration API the
  guide names, and prove each against `src/infrastructure/adapters/arsenal/` and the Arsenal port
  trait, recording the command per item. This guide is the corpus's most likely place for a
  post-M5/M6/M8 module relocation to have gone stale.

Correct only what the checks find. Do not restructure, retitle or re-tone (D-12). Do not "improve"
an example that is merely terse but correct.

Write the two verdict rows, replacing those files' `pending` rows in place and preserving the record's
declared row order.
  </action>

  <acceptance_criteria>
    - Every path matched by `grep -ohE '(crates|src)/[A-Za-z0-9_/.-]+\.rs' docs/src/user-guides/tool-integration.md docs/src/user-guides/paladin-configuration.md | sort -u` exists on disk (a `test -f` loop over that list exits 0).
    - The verdict row for `paladin-configuration.md` enumerates every documented configuration key with a per-key proving command and a per-key result.
    - The verdict row for `tool-integration.md` enumerates every named MCP transport/adapter with its proving command.
    - Both rows are no longer `pending` and neither Findings cell is empty.
    - `mdbook build docs/` exits 0 and `git diff --name-only` lists only the two guides and the verdict record.
  </acceptance_criteria>

  <verify>
    <automated>for p in $(grep -ohE '(crates|src)/[A-Za-z0-9_/.-]+\.rs' docs/src/user-guides/tool-integration.md docs/src/user-guides/paladin-configuration.md | sort -u); do test -f "$p" || { echo "MISSING $p"; exit 1; }; done &amp;&amp; test "$(grep -c 'pending — not yet checked' .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md)" = 8 &amp;&amp; mdbook build docs/</automated>
  </verify>

  <done>Both guides are checked against every signal class plus their own structured claim surface (config keys, MCP transports), with each check's command recorded, and their verdict rows carry evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep output-formatting.md against the 0.8.0 tree</name>

  <files>docs/src/user-guides/output-formatting.md, .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md</files>

  <read_first>
    - `docs/src/user-guides/output-formatting.md` — all 1,127 lines. The file being modified.
    - `crates/paladin-herald/src/lib.rs` — the shipped Herald formatters (`JsonHerald`,
      `MarkdownHerald`, `TableHerald`) and the `color` / `table` feature gates, documented in the
      crate's own module doc at lines 1-18. This is the comparison target for every formatter and
      feature claim in the guide.
    - `crates/paladin-herald/Cargo.toml` — the `[features]` block.
    - `.planning/phases/16-documentation-currency-the-architecture-gap/16-01-PLAN.md` § "The DOCS-01
      signal battery".
    - `.planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md`.
  </read_first>

  <action>
Run the full eight-class battery against `output-formatting.md`, recording every command.

Then run the guide's own claim surface: extract every Herald formatter type name, every output-format
variant, and every feature gate the guide names, and prove each against `crates/paladin-herald`,
recording the proving command per item. A formatter the guide describes that the crate does not ship
— or a feature gate whose name has changed — is a content defect and is corrected in place.

Note for the record (do not act on it here): `crates/paladin-herald/src/lib.rs:20` carries a
crate-level lint attribute that plan 16-07 flips under D-07. Read that line if you need context, but
this plan does **not** modify any `.rs` file — 16-07 owns that crate in wave 2.

Do not restructure, retitle or re-tone (D-12). Write the third verdict row, replacing this file's
`pending` row in place. This completes the six-file user-guides group (Milestone 11 task 6.0); state
that completion explicitly in the row's Findings cell or in a record note so the group's closure is
itself auditable.
  </action>

  <acceptance_criteria>
    - Every formatter type name matched by `grep -ohE '[A-Z][A-Za-z]*Herald' docs/src/user-guides/output-formatting.md | sort -u` is found by a recorded `grep -rn` in `crates/paladin-herald/src/`, or has been corrected/removed, with the per-name result stated in the verdict row.
    - Every feature name the guide attributes to `paladin-herald` appears in `crates/paladin-herald/Cargo.toml`'s `[features]` block, with the comparison command recorded.
    - `grep -c 'pending — not yet checked' .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md` returns 7, and `grep -c '^| docs/src/user-guides/' ...` returns 6 with none of those six rows pending.
    - `git diff --name-only` lists only `docs/src/user-guides/output-formatting.md` and the verdict record — no `.rs` file is touched.
    - `mdbook build docs/` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>for h in $(grep -ohE '[A-Z][A-Za-z]*Herald' docs/src/user-guides/output-formatting.md | sort -u); do grep -rq "$h" crates/paladin-herald/src/ || { echo "UNRESOLVED $h"; exit 1; }; done &amp;&amp; test "$(grep -c 'pending — not yet checked' .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md)" = 7 &amp;&amp; test -z "$(git diff --name-only -- '*.rs')" &amp;&amp; mdbook build docs/</automated>
  </verify>

  <done>`output-formatting.md` is checked against every signal class and every Herald claim it makes, its verdict row carries evidence, and the six-file user-guides group is closed with that closure recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation source → published book | A wrong configuration key or MCP transport name published here becomes a user's broken deployment. |
| verdict record → downstream verification | Rows 4-6 are the evidence a later verifier trusts for the user-guides half of DOCS-01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05 | Repudiation | `16-DOCS-01-VERDICTS.md` rows 4-6 | medium | mitigate | Per-key and per-item proving commands are required in the Findings cell, not a summary; a `current` verdict with an empty Findings cell is invalid by the record's own header (D-00e). |
| T-16-07 | Tampering | published configuration guidance | high | mitigate | Every documented configuration key is proven against the shipped settings type in `src/config/` at execute time; a key with no matching field is corrected or removed rather than left to mislead. This is the phase's highest-impact doc surface — a wrong key silently fails at runtime. |
| T-16-09 | Tampering | cross-plan file collision | low | mitigate | This plan is explicitly forbidden from editing any `.rs` file; `crates/paladin-herald` is owned by plan 16-07 in an earlier wave. An acceptance criterion asserts `git diff --name-only -- '*.rs'` is empty. |
| T-16-SC | Tampering | package-manager installs | low | mitigate | This plan installs nothing and adds no dependency; `make audit` / `make deny` posture is unchanged. |
</threat_model>

<verification>
```
mdbook build docs/
git diff --name-only
grep -c 'pending — not yet checked' .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md
```
Commit each task separately with `git commit --no-verify` (D-00o).
</verification>

<success_criteria>
- Six of the fourteen (the whole `docs/src/user-guides/` group, Milestone 11 task 6.0) are settled by
  content, each with all eight signal classes run and recorded.
- Every documented configuration key and MCP transport is proven against the shipped tree.
- Six verdict rows carry evidence; seven files remain explicitly pending.
- No `.rs` file and no page outside the three named files is modified.
</success_criteria>

<output>
Create `.planning/phases/16-documentation-currency-the-architecture-gap/16-03-SUMMARY.md` when done.
Record, per D-00e, the verbatim command and output behind each finding, and state explicitly that the
six-file user-guides group is closed and by what evidence.
</output>
</content>
