---
phase: 16-documentation-currency-the-architecture-gap
plan: 04
type: execute
wave: 4
depends_on: ["16-03"]
files_modified:
  - docs/src/deployment/docker.md
  - docs/src/deployment/kubernetes.md
  - docs/src/deployment/production.md
  - .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md
autonomous: true
requirements: [DOCS-01]

must_haves:
  truths:
    - "All eight signal classes of the 16-01 battery are run against docker.md, kubernetes.md and production.md, and each file's prose is read end to end for statements the 0.8.0 tree contradicts (D-12)"
    - "No stale release tag survives in the deployment group: the pre-0.8.0 tag that appears 7 times in docker.md and 2 times in kubernetes.md is replaced by the shipped workspace version, and the post-fix count is proven by re-running the recorded counting command"
    - "Every `paladin-*` token in kubernetes.md is classified as a crate name or as a Kubernetes object/Secret/ConfigMap name BEFORE any edit, and the classification is recorded per token — a naive crate-name sweep here is mostly false positives (M-06)"
    - "Every `make` target the deployment group names exists in the repo Makefile, proven by a recorded comparison against the Makefile's own target list"
    - "Three more `pending` rows become evidence-bearing verdict rows; four remain pending (D-09)"
    - "`mdbook build docs/` exits 0 after the edits, and no file outside the three named pages is modified (D-12)"
  artifacts:
    - path: ".planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md"
      provides: "Rows 7-9 of the fourteen — the deployment group (cicd.md was row 1, settled in 16-01)"
      contains: "docker.md"
  key_links:
    - from: "docs/src/deployment/docker.md"
      to: "Cargo.toml"
      via: "image tags and quoted release versions must match the shipped workspace version"
      pattern: "0.8.0"
---

<objective>
Settle three more of the fourteen by content (D-09, D-12): `docs/src/deployment/docker.md` (777
lines), `docs/src/deployment/kubernetes.md` (909 lines) and `docs/src/deployment/production.md` (647
lines). With `cicd.md` already settled in the 16-01 tracer, this closes the four-file deployment half
of Milestone 11's task 7.0.

Purpose: this group carries the phase's densest *measured* drift — nine occurrences of a pre-0.8.0
release tag across two of the three files, left over from an in-place update targeting v0.4.3-era
content that was never re-run against the 0.8.0 tree. It also carries the phase's biggest
false-positive trap: most `paladin-*` tokens here are Kubernetes object names, not crate names.
Output: three corrected pages and three evidence-bearing verdict rows.

**Scope guards:** D-12 — no style rewrite. D-00e — every finding carries its producing command.
D-00o — commit with `--no-verify`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-documentation-currency-the-architecture-gap/16-CONTEXT.md
@.planning/phases/16-documentation-currency-the-architecture-gap/16-RESEARCH.md
@.planning/phases/16-documentation-currency-the-architecture-gap/16-01-PLAN.md
@.planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md
</context>

## Artifacts this phase produces (plan 16-04 scope)

No new symbol, file or path. Three rows appended to the phase artifact `16-DOCS-01-VERDICTS.md`
(created in 16-01) and three existing mdBook pages edited in place. The phase-wide artifact table
lives in `16-01-PLAN.md` § "Artifacts this phase produces".

<!-- planner-discipline-allow: v0.4.3 -->

<tasks>

<task type="auto">
  <name>Task 1: Sweep docker.md and kubernetes.md, clearing the stale release tag and classifying every paladin-* token</name>

  <files>docs/src/deployment/docker.md, docs/src/deployment/kubernetes.md, .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md</files>

  <read_first>
    - `docs/src/deployment/docker.md` — all 777 lines. The file being modified. It carries seven
      occurrences of the stale tag `v0.4.3`.
    - `docs/src/deployment/kubernetes.md` — all 909 lines. The file being modified. Two occurrences of
      the same stale tag, plus the corpus's densest concentration of `paladin-*` Kubernetes object
      names (`paladin-data`, `paladin-secrets`, `paladin-logs`, …) which are **not** crate names.
    - `.planning/phases/16-documentation-currency-the-architecture-gap/16-01-PLAN.md` § "The DOCS-01
      signal battery" — the eight commands and the M-06 warning this task turns on.
    - `.planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md`.
    - `Cargo.toml` (root) — line 34 carries the shipped workspace version; all eleven crate manifests
      ship in lockstep at that figure.
    - `.planning/decisions/0008-workspace-version-0-7-0.md` — the pre-1.0 versioning semantics that
      govern how a stale version string is judged (a `0.x` bump is not a compatibility promise).
    - `docker/` and `k8s/` — the shipped compose/manifest sources the two pages describe; image
      names, service names and object names quoted in the docs are checked against these.
    - `Makefile` — the target list every `make <target>` reference in these pages is checked against.
  </read_first>

  <action>
Run the full eight-class signal battery from 16-01 against each file, recording every command and its
output (D-00e).

**Class 1 (version strings) is the known-genuine defect here.** Replace every occurrence of the stale
pre-0.8.0 release tag with the shipped workspace version from root `Cargo.toml:34`, in both files —
image tags, `helm`/`kubectl` examples, prose references, and any `appVersion`-style field. Record the
before-count and the after-count using the *same* literal counting command in the verdict row, so the
figure can be re-derived. Judge each hit against ADR-0008's pre-1.0 semantics: a version that is
merely *illustrative* in a "for example, tag your own build" sentence is still stale if it names a tag
this project never published at 0.8.0-era, and is corrected.

**Class 3 (crate names) is the known trap.** For `kubernetes.md` especially, classify every
`paladin-*` token before editing anything: a token is a **crate name** only if it matches a directory
under `crates/`; otherwise it is a Kubernetes object, Secret, ConfigMap, PVC, namespace or service
name and must be left exactly as written. Record the classification per distinct token in the verdict
row. Editing a Kubernetes object name to "fix" it silently breaks a working manifest example — this is
the single most likely way to make these pages *worse* while appearing to fix them.

**Classes 4-8** as usual: `test -f` every cited source path; compare every `make <target>` against the
Makefile's own target list; prove every `*Error` token with a `grep -rn`; compare every feature flag
against the owning crate's `[features]`. Also compare the compose/service/image names the pages quote
against `docker/` and `k8s/`, recording the comparison — these pages describe shipped deployment
assets, so a divergence is a content defect, not a stylistic difference.

Read both files' prose end to end for statements the 0.8.0 tree contradicts. Correct contradictions in
place. Do not restructure, retitle, reorder or re-tone (D-12).

Write the two verdict rows, replacing those files' `pending` rows in place.
  </action>

  <acceptance_criteria>
    - `grep -c 'v0\.4\.3' docs/src/deployment/docker.md` returns 0 and the same grep against `docs/src/deployment/kubernetes.md` returns 0.
    - `grep -c '0\.8\.0' docs/src/deployment/docker.md` is at least 1 (the replacement landed rather than the reference simply being deleted).
    - The verdict rows record, per distinct `paladin-*` token in `kubernetes.md`, whether it is a crate name or a Kubernetes/config object name, and `git diff docs/src/deployment/kubernetes.md` shows no edit to any token classified as a Kubernetes object name.
    - Every path matched by `grep -ohE '(crates|src)/[A-Za-z0-9_/.-]+\.rs' docs/src/deployment/docker.md docs/src/deployment/kubernetes.md | sort -u` exists on disk.
    - Every target matched by `grep -ohE 'make [a-z-]+' docs/src/deployment/docker.md docs/src/deployment/kubernetes.md | sed 's/make //' | sort -u` appears as a target in `Makefile`.
    - Both rows are no longer `pending`; neither Findings cell is empty.
    - `mdbook build docs/` exits 0 and `git diff --name-only` lists only the two pages and the verdict record.
  </acceptance_criteria>

  <verify>
    <automated>test "$(grep -c 'v0\.4\.3' docs/src/deployment/docker.md)" = 0 &amp;&amp; test "$(grep -c 'v0\.4\.3' docs/src/deployment/kubernetes.md)" = 0 &amp;&amp; for t in $(grep -ohE 'make [a-z-]+' docs/src/deployment/docker.md docs/src/deployment/kubernetes.md | sed 's/make //' | sort -u); do grep -qE "^${t}:" Makefile || { echo "NO SUCH TARGET $t"; exit 1; }; done &amp;&amp; test "$(grep -c 'pending — not yet checked' .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md)" = 5 &amp;&amp; mdbook build docs/</automated>
  </verify>

  <done>Both deployment pages carry no stale release tag, every `paladin-*` token is classified before being touched, every `make` target and source path they cite resolves, and their verdict rows carry the producing commands.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep production.md against the 0.8.0 tree</name>

  <files>docs/src/deployment/production.md, .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md</files>

  <read_first>
    - `docs/src/deployment/production.md` — all 647 lines. The file being modified.
    - `.planning/phases/16-documentation-currency-the-architecture-gap/16-01-PLAN.md` § "The DOCS-01
      signal battery".
    - `.planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md`.
    - `Makefile` — the target list; production guidance is the likeliest place to name a `make` target
      that has been renamed.
    - `.github/instructions/security.instructions.md` — the repo's binding security posture. Its
      "Snyk was evaluated and removed (2026-08-18)" section is load-bearing for this file: if
      `production.md` recommends a scanner this repo has explicitly removed, that is a content
      contradiction to correct, and the correct replacement set is `make audit` / `make deny` /
      `make security` / `make sbom` as that file lists them.
    - `crates/paladin-web/src/` — the shipped HTTP surface; auth modes, health/readiness endpoints and
      OpenAPI paths quoted in the production guide are checked against it.
  </read_first>

  <action>
Run the full eight-class battery against `production.md`, recording every command.

Two claim surfaces in this file need a per-item check beyond the battery:

1. **Security tooling.** Extract every scanner, gate or command the page recommends for a production
   pipeline and prove each against the repo's actual tooling. The binding set is the table in
   `.github/instructions/security.instructions.md`. **Do not reintroduce, and do not leave standing, a
   recommendation for the scanner this repo evaluated and removed on 2026-08-18 for having no Rust
   coverage** — CLAUDE.md and the security instructions both state this explicitly, and a production
   guide that still recommends it is exactly the kind of stale content this phase exists to catch.
   If the page carries such a recommendation, correct it to the shipped commands and record the
   finding.
2. **Runtime surface.** Extract every endpoint path, health/readiness route, auth mode and
   configuration knob the page names for a production deployment and prove each against
   `crates/paladin-web/src/`, recording the proving command per item.

Read the prose end to end for statements the 0.8.0 tree contradicts. Correct only what the checks
find; do not restructure or re-tone (D-12).

Write the third verdict row, replacing this file's `pending` row in place. This closes the four-file
deployment half of Milestone 11 task 7.0 (`cicd.md` was settled in 16-01) — state that closure
explicitly so it is itself auditable.
  </action>

  <acceptance_criteria>
    - Every target matched by `grep -ohE 'make [a-z-]+' docs/src/deployment/production.md | sed 's/make //' | sort -u` appears as a target in `Makefile`.
    - Every endpoint path the page quotes for the agent API is found by a recorded `grep -rn` in `crates/paladin-web/src/`, or has been corrected, with the per-path result stated in the verdict row.
    - The verdict row states, with its producing command, whether the page recommended the removed scanner and what was done about it.
    - `grep -c 'pending — not yet checked' .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md` returns 4, and the four `docs/src/deployment/` rows are all non-pending.
    - `mdbook build docs/` exits 0 and `git diff --name-only` lists only `docs/src/deployment/production.md` and the verdict record.
  </acceptance_criteria>

  <verify>
    <automated>for t in $(grep -ohE 'make [a-z-]+' docs/src/deployment/production.md | sed 's/make //' | sort -u); do grep -qE "^${t}:" Makefile || { echo "NO SUCH TARGET $t"; exit 1; }; done &amp;&amp; test "$(grep -c 'pending — not yet checked' .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md)" = 4 &amp;&amp; test -z "$(git diff --name-only -- '*.rs')" &amp;&amp; mdbook build docs/</automated>
  </verify>

  <done>`production.md` is checked against every signal class plus its security-tooling and runtime-surface claims, its verdict row carries evidence, and the deployment group is closed with that closure recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation source → published book | These are the pages a reader follows to put the framework into production; a wrong image tag, manifest object name or security recommendation has operational consequences. |
| deployment docs → shipped `docker/` + `k8s/` assets | The pages quote assets that exist in the tree; divergence between the quote and the asset is a defect in the quote. |
| verdict record → downstream verification | Rows 7-9 are the evidence a later verifier trusts for the deployment half of DOCS-01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-07 | Tampering | published deployment guidance | high | mitigate | Every quoted image tag, object name, endpoint and `make` target is proven against the live tree at execute time and corrected to the tree's value; `mdbook build docs/` with `warning-policy = "error"` gates each edit. |
| T-16-08 | Tampering | `paladin-*` token sweep in `kubernetes.md` | high | mitigate | Class 3 requires per-token classification (crate name vs. Kubernetes/config object) **before** any edit, recorded in the verdict row; an acceptance criterion asserts no token classified as a Kubernetes object was edited. Editing one silently breaks a working manifest example (M-06). |
| T-16-10 | Repudiation | stale security-tooling recommendation in `production.md` | high | mitigate | The page is checked against `.github/instructions/security.instructions.md`'s binding table. A recommendation for the scanner this repo removed on 2026-08-18 (no Rust coverage — a "clean" result there means *nothing was analysed*) reads as assurance while providing none; it is corrected to `make audit` / `make deny` / `make security`, with the finding recorded. |
| T-16-05 | Repudiation | `16-DOCS-01-VERDICTS.md` rows 7-9 | medium | mitigate | Producing commands inline per finding, including the before/after counts for the version-tag class re-derived with the same literal command (D-00e). |
| T-16-SC | Tampering | package-manager installs | low | mitigate | This plan installs nothing and adds no dependency; `make audit` / `make deny` posture is unchanged. |
</threat_model>

<verification>
```
grep -c 'v0\.4\.3' docs/src/deployment/docker.md docs/src/deployment/kubernetes.md
mdbook build docs/
git diff --name-only
grep -c 'pending — not yet checked' .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md
```
Commit each task separately with `git commit --no-verify` (D-00o).
</verification>

<success_criteria>
- Nine of the fourteen are settled by content; the whole `docs/src/deployment/` group (Milestone 11
  task 7.0, deployment half) is closed.
- No stale pre-0.8.0 release tag survives in the deployment group, with before/after counts
  re-derivable from the recorded command.
- Every `paladin-*` token in `kubernetes.md` is classified before being touched, and no Kubernetes
  object name was edited.
- `production.md` recommends only tooling this repo actually ships.
- Nine verdict rows carry evidence; four files remain explicitly pending.
</success_criteria>

<output>
Create `.planning/phases/16-documentation-currency-the-architecture-gap/16-04-SUMMARY.md` when done.
Record, per D-00e, the verbatim command and output behind each finding — including the per-token
crate-vs-Kubernetes classification table for `kubernetes.md` — and state explicitly that the
deployment group is closed and by what evidence.
</output>
</content>
