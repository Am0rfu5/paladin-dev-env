---
phase: 16-documentation-currency-the-architecture-gap
plan: 05
type: execute
wave: 5
depends_on: ["16-04"]
files_modified:
  - docs/src/operations/logging.md
  - docs/src/operations/monitoring.md
  - docs/src/operations/performance-tuning.md
  - docs/src/operations/troubleshooting.md
  - .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md
  - .planning/phases/16-documentation-currency-the-architecture-gap/16-LINKCHECK-REPORT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DOCS-01]

must_haves:
  truths:
    - "All eight signal classes of the 16-01 battery are run against logging.md, monitoring.md, performance-tuning.md and troubleshooting.md, and each file's prose is read end to end for statements the 0.8.0 tree contradicts (D-12)"
    - "The stale pre-0.8.0 release tag that appears twice in performance-tuning.md is replaced by the shipped workspace version, with before/after counts re-derived from the same recorded command"
    - "All fourteen rows in 16-DOCS-01-VERDICTS.md carry a verdict of `current` or `updated → commit`; zero rows remain `pending` (D-09)"
    - "A final `mdbook build docs/` is run after the last content edit and its linkcheck section is appended verbatim to 16-LINKCHECK-REPORT.md with a prose re-review — the report is reviewed at the end state, not only at the start (D-10, Milestone 11 task 1.2)"
    - "REQUIREMENTS.md's Hand-off to Phase 16 item 4 is amended in place with a dated note recording that Phase 15.1 commit d87d11e already fixed the two broken mdBook links, superseded text retained (D-00d, M-05)"
    - "REQUIREMENTS.md's DOCS-01 entry carries a pointer to the verdict record, so the ledger names where the fourteen verdicts live without inlining them"
    - "Findings observed outside the fourteen are recorded in a clearly-labelled out-of-scope section and are NOT fixed here — DOCS-01's open count is defined as exactly fourteen"
  artifacts:
    - path: ".planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md"
      provides: "Rows 10-14 of the fourteen, plus the completed record and its out-of-scope observations section"
      contains: "troubleshooting.md"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md"
      via: "the DOCS-01 ledger entry points at the single verdict record rather than inlining fourteen rows"
      pattern: "16-DOCS-01-VERDICTS.md"
---

<objective>
Close DOCS-01. Settle the last four of the fourteen by content (D-09, D-12) —
`docs/src/operations/{logging,monitoring,performance-tuning,troubleshooting}.md`, 2,092 lines — then
re-review the linkcheck report at the phase's end state and amend the two stale statements in
REQUIREMENTS.md in place (D-00d).

Purpose: DOCS-01 is the only open checkbox count in all 542 that survives verification. It closes
when every one of the fourteen carries a content verdict with the command that produced it, and when
the linkcheck report has been *reviewed* — Milestone 11's task 1.2, the fifteenth open item.
Output: four corrected pages, a complete fourteen-row record, an end-state linkcheck review, and two
in-place ledger amendments.

**Scope guards:** D-12 — no style rewrite. D-00e — every finding carries its producing command.
D-00o — commit with `--no-verify`. The five live architecture-chapter pages are explicitly **not**
audited here (CONTEXT.md Deferred Ideas) — the open count is exactly fourteen.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-documentation-currency-the-architecture-gap/16-CONTEXT.md
@.planning/phases/16-documentation-currency-the-architecture-gap/16-RESEARCH.md
@.planning/phases/16-documentation-currency-the-architecture-gap/16-01-PLAN.md
@.planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md
</context>

## Artifacts this phase produces (plan 16-05 scope)

No new symbol, file or path. Five rows appended to `16-DOCS-01-VERDICTS.md`, an appended section in
`16-LINKCHECK-REPORT.md`, four mdBook pages edited in place, and two in-place amendments to
`REQUIREMENTS.md`. The phase-wide artifact table lives in `16-01-PLAN.md` § "Artifacts this phase
produces".

<!-- planner-discipline-allow: v0.4.3 -->

<tasks>

<task type="auto">
  <name>Task 1: Sweep logging.md and monitoring.md against the 0.8.0 tree</name>

  <files>docs/src/operations/logging.md, docs/src/operations/monitoring.md, .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md</files>

  <read_first>
    - `docs/src/operations/logging.md` — all 544 lines. The file being modified.
    - `docs/src/operations/monitoring.md` — all 436 lines. The file being modified.
    - `.planning/phases/16-documentation-currency-the-architecture-gap/16-01-PLAN.md` § "The DOCS-01
      signal battery".
    - `.planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md`.
    - `src/application/services/log_orchestrator/` — the shipped log-orchestration surface; the
      comparison target for every listener, level, sink and metric name the logging guide claims.
    - `crates/paladin-web/src/` — the shipped health/readiness/metrics endpoints the monitoring guide
      quotes.
  </read_first>

  <action>
Run the full eight-class signal battery from 16-01 against each file, recording every command and its
output (D-00e).

Beyond the battery, each file has a structured claim surface to check per item:

- **`logging.md`** — extract every log level, target/sink name, environment variable and orchestrator
  API the page names, and prove each against `src/application/services/log_orchestrator/` and the
  shipped configuration surface, recording the proving command per item.
- **`monitoring.md`** — extract every endpoint path, metric name and health/readiness route and prove
  each against `crates/paladin-web/src/`, recording the proving command per item. `GET /health` and
  `GET /ready` are the shipped unauthenticated probes; a page describing a different probe path or a
  different auth posture for them is a content defect.

Correct only what the checks find. Do not restructure, retitle or re-tone (D-12).

Write the two verdict rows, replacing those files' `pending` rows in place.
  </action>

  <acceptance_criteria>
    - Every path matched by `grep -ohE '(crates|src)/[A-Za-z0-9_/.-]+\.rs' docs/src/operations/logging.md docs/src/operations/monitoring.md | sort -u` exists on disk.
    - Every endpoint path the monitoring guide quotes is found by a recorded `grep -rn` in `crates/paladin-web/src/`, or has been corrected, with the per-path result stated in the verdict row.
    - Both rows are no longer `pending` and neither Findings cell is empty.
    - `mdbook build docs/` exits 0 and `git diff --name-only` lists only the two pages and the verdict record.
  </acceptance_criteria>

  <verify>
    <automated>for p in $(grep -ohE '(crates|src)/[A-Za-z0-9_/.-]+\.rs' docs/src/operations/logging.md docs/src/operations/monitoring.md | sort -u); do test -f "$p" || { echo "MISSING $p"; exit 1; }; done &amp;&amp; test "$(grep -c 'pending — not yet checked' .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md)" = 2 &amp;&amp; mdbook build docs/</automated>
  </verify>

  <done>Both operations pages are checked against every signal class plus their own claim surface, with each check's command recorded, and their verdict rows carry evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep performance-tuning.md and troubleshooting.md, clearing the last stale release tag</name>

  <files>docs/src/operations/performance-tuning.md, docs/src/operations/troubleshooting.md, .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md</files>

  <read_first>
    - `docs/src/operations/performance-tuning.md` — all 569 lines. The file being modified. It carries
      two occurrences of the stale tag `v0.4.3`, the last of the eleven measured across the fourteen.
    - `docs/src/operations/troubleshooting.md` — all 543 lines. The file being modified. This is the
      corpus's densest source of error-type references (signal class 7).
    - `.planning/phases/16-documentation-currency-the-architecture-gap/16-01-PLAN.md` § "The DOCS-01
      signal battery".
    - `.planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md`.
    - `Cargo.toml` (root) — line 34 carries the shipped workspace version.
    - `benches/` and `Makefile` — the shipped benchmark targets and commands the performance guide
      quotes.
  </read_first>

  <action>
Run the full eight-class battery against both files, recording every command.

**`performance-tuning.md`** — replace both occurrences of the stale pre-0.8.0 release tag with the
shipped workspace version from root `Cargo.toml:34`, recording the before-count and after-count with
the same literal counting command. Then check every benchmark name and `make`/`cargo bench` command
the page quotes against `benches/` and the `Makefile`, recording the comparison per item. Numeric
performance figures the page states are **claims about a measurement**, not about the tree: do not
invent new numbers. If a figure names a version or a machine profile the tree no longer matches, mark
the figure as dated in place (a one-clause attribution, not a rewrite) and record that as the finding.

**`troubleshooting.md`** — signal class 7 is the load-bearing one. For every `*Error` /
`*Error::Variant` token the battery surfaces, prove it still exists with a recorded `grep -rn` into
`crates/` or `src/`, and correct or remove any that no longer resolves. A troubleshooting page that
tells a reader to look for an error variant the code cannot emit is worse than no page. Also check
every command the page tells a reader to run against the `Makefile` and the shipped CLI.

Correct only what the checks find; no restructuring or re-toning (D-12).

Write the two verdict rows, replacing those files' `pending` rows in place. After this task **zero**
rows may remain `pending`.
  </action>

  <acceptance_criteria>
    - `grep -c 'v0\.4\.3' docs/src/operations/performance-tuning.md` returns 0, and `grep -rc 'v0\.4\.3' docs/src/user-guides/ docs/src/deployment/ docs/src/operations/` reports 0 for every one of the fourteen files.
    - Every `*Error` token matched by `grep -ohE '[A-Z][A-Za-z]*Error' docs/src/operations/troubleshooting.md | sort -u` is found by a recorded `grep -rn` in `crates/` or `src/`, or has been corrected/removed, with the per-token result stated in the verdict row.
    - `grep -c 'pending — not yet checked' .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md` returns 0, and `grep -c '^| docs/src/' ...` still returns 14.
    - `mdbook build docs/` exits 0 and `git diff --name-only` lists only the two pages and the verdict record.
  </acceptance_criteria>

  <verify>
    <automated>test "$(grep -c 'v0\.4\.3' docs/src/operations/performance-tuning.md)" = 0 &amp;&amp; for e in $(grep -ohE '[A-Z][A-Za-z]*Error' docs/src/operations/troubleshooting.md | sort -u); do grep -rq "$e" crates/ src/ || { echo "UNRESOLVED $e"; exit 1; }; done &amp;&amp; test "$(grep -c 'pending — not yet checked' .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md)" = 0 &amp;&amp; test "$(grep -c '^| docs/src/' .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md)" = 14 &amp;&amp; mdbook build docs/</automated>
  </verify>

  <done>The last stale release tag is gone from the fourteen, every error type the troubleshooting page names resolves in the tree, and all fourteen verdict rows carry a content verdict with its producing command.</done>
</task>

<task type="auto">
  <name>Task 3: Re-review the linkcheck report at the end state and amend the two stale ledger statements in place</name>

  <files>.planning/phases/16-documentation-currency-the-architecture-gap/16-LINKCHECK-REPORT.md, .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md, .planning/REQUIREMENTS.md</files>

  <read_first>
    - `.planning/phases/16-documentation-currency-the-architecture-gap/16-LINKCHECK-REPORT.md` — the
      opening report captured by the 16-01 tracer; this task appends the closing one.
    - `.planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md` — the
      completed fourteen-row record; this task adds the out-of-scope observations section and the
      closure statement.
    - `.planning/REQUIREMENTS.md` — the file being modified. Two landmarks: the `- [ ] **DOCS-01**`
      block (around line 3316) and the section titled "Hand-off to Phase 16 / DOCS-01 … DOCS-04 —
      dated 2026-08-10 (plan 13-13)", whose **item 4** records two broken mdBook links as owned by no
      phase.
    - `.planning/phases/16-documentation-currency-the-architecture-gap/16-CONTEXT.md` § M-05 — the
      measurement that supersedes that item: Phase 15.1 fixed both links in commit `d87d11e`, verified
      `mdbook build` reporting no broken links, exit 0.
    - `docs/book.toml` — `warning-policy = "error"`, `follow-web-links = false`; this is why the local
      report is deterministic and offline.
  </read_first>

  <action>
**Re-review the report at the end state.** After the last content edit of DOCS-01, run
`mdbook build docs/` once more and append its complete stdout+stderr — including every linkcheck line
— verbatim to `16-LINKCHECK-REPORT.md` under a dated "closing run" heading, with the invoked command
above it. Then write the review in prose underneath: how many links were checked, how many were
broken, whether any warning was emitted that `warning-policy = "error"` would escalate, and how the
closing figures compare to the opening run captured in 16-01. Milestone 11's task 1.2 asks for the
report to be **reviewed**; a pass/fail citation is not a review, and neither is an unread paste.

**Complete the verdict record.** Add a closure statement naming the fourteen files, the eight signal
classes, and the fact that every verdict is content-derived. Add a clearly-labelled section
"Out of scope — observed, not fixed" for any currency defect noticed in a file *outside* the fourteen
(the phase's open count is defined as exactly fourteen, and CONTEXT.md's Deferred Ideas explicitly
declines auditing the five live architecture pages). One known candidate to record there without
fixing: `README.md`'s Project Status section states a release figure lower than the shipped workspace
version at root `Cargo.toml:34` — record it with its `file:line` so the observation is not lost, and
do not edit `README.md` here (plan 16-14 adds exactly one line to that file under D-15 and nothing
else).

**Amend REQUIREMENTS.md in place, twice, per D-00d** — dated notes, superseded text retained, never a
separate corrections file:
1. In the "Hand-off to Phase 16" section's **item 4**, append a dated note recording that the two
   broken mdBook links it hands off (`deployment/docker.md:118`,
   `user-guides/tool-integration.md:324`) were already repaired by Phase 15.1 in commit `d87d11e`,
   verified by `mdbook build` reporting no broken links at exit 0, so Phase 16 inherited a green book
   and did not re-fix them. Keep the original text visible above the note (M-05, D-00d).
2. In the `- [ ] **DOCS-01**` block, append a dated pointer naming the verdict record's path as the
   location of the fourteen per-file verdicts and the linkcheck review, so the ledger says where the
   evidence lives without inlining fourteen evidence-bearing rows into it.

Do **not** tick the DOCS-01 checkbox here — requirement ticking is the phase-close step's job, not a
plan's.
  </action>

  <acceptance_criteria>
    - `16-LINKCHECK-REPORT.md` contains two dated run sections (opening and closing), each with its invoked command and a fenced output block, and the closing section is followed by a prose review naming the checked-link and broken-link counts.
    - `16-DOCS-01-VERDICTS.md` contains a section headed with the words `Out of scope` and a closure statement naming all fourteen files.
    - `grep -c 'd87d11e' .planning/REQUIREMENTS.md` is at least 1, and the amendment sits inside the Hand-off to Phase 16 section with the original item-4 text still present above it.
    - `grep -c '16-DOCS-01-VERDICTS.md' .planning/REQUIREMENTS.md` is at least 1.
    - `grep -c '^- \[x\] \*\*DOCS-01\*\*' .planning/REQUIREMENTS.md` returns 0 (the checkbox is not ticked by this plan).
    - `mdbook build docs/` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>mdbook build docs/ &amp;&amp; grep -q 'd87d11e' .planning/REQUIREMENTS.md &amp;&amp; grep -q '16-DOCS-01-VERDICTS.md' .planning/REQUIREMENTS.md &amp;&amp; test "$(grep -c '^- \[x\] \*\*DOCS-01\*\*' .planning/REQUIREMENTS.md)" = 0 &amp;&amp; grep -q 'Out of scope' .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md</automated>
  </verify>

  <done>The linkcheck report is reviewed at the phase's end state as well as its start; the verdict record is complete with its out-of-scope observations recorded rather than silently dropped; and both stale ledger statements are amended in place with dated notes and superseded text retained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation source → published book | Operations pages are what an on-call reader follows; a named error variant the code cannot emit sends them down a dead end. |
| verdict record + ledger → downstream verification | Rows 10-14, the closure statement and the two in-place amendments are the evidence a later verifier trusts for DOCS-01's closure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05 | Repudiation | `16-DOCS-01-VERDICTS.md` closure | high | mitigate | Zero rows may remain `pending`; every row carries its producing command; the closure statement names the method, not just the outcome (D-00e). An acceptance criterion asserts the pending count is 0 and the row count is still 14, so closure cannot be faked by deleting rows. |
| T-16-11 | Repudiation | in-place ledger amendment | medium | mitigate | D-00d requires amendment in place with superseded text retained and a date; an acceptance criterion asserts the original item-4 text is still present above the new note, so a "correction" cannot quietly erase what was previously claimed. |
| T-16-07 | Tampering | published operations guidance | medium | mitigate | Every error type, endpoint and command is proven against the live tree at execute time; `mdbook build docs/` with `warning-policy = "error"` gates each edit. |
| T-16-12 | Repudiation | scope creep past the fourteen | low | mitigate | Out-of-scope findings are recorded in a labelled section and explicitly not fixed; `README.md` is left to plan 16-14's single-line change under D-15. The phase's open count stays exactly fourteen. |
| T-16-SC | Tampering | package-manager installs | low | mitigate | This plan installs nothing and adds no dependency; `make audit` / `make deny` posture is unchanged. |
</threat_model>

<verification>
```
mdbook build docs/
grep -c 'pending — not yet checked' .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md
grep -c '^| docs/src/' .planning/phases/16-documentation-currency-the-architecture-gap/16-DOCS-01-VERDICTS.md
git diff --name-only
```
Commit each task separately with `git commit --no-verify` (D-00o).
</verification>

<success_criteria>
- All fourteen DOCS-01 files carry a content verdict with the command that produced it; zero pending.
- The linkcheck report is captured and reviewed at both the opening and closing state (task 1.2).
- No stale pre-0.8.0 release tag survives anywhere in the fourteen.
- REQUIREMENTS.md's hand-off item 4 and DOCS-01 entry are amended in place, dated, superseded text
  retained (D-00d) — and the DOCS-01 checkbox is *not* ticked by this plan.
- Findings outside the fourteen are recorded, not fixed and not dropped.
</success_criteria>

<output>
Create `.planning/phases/16-documentation-currency-the-architecture-gap/16-05-SUMMARY.md` when done.
Record, per D-00e, the verbatim closing `mdbook build docs/` linkcheck section, the final pending-row
count, and the exact text of both REQUIREMENTS.md amendments.
</output>
</content>
