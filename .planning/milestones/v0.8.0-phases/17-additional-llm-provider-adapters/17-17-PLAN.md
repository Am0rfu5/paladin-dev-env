---
phase: 17-additional-llm-provider-adapters
plan: 17
type: execute
wave: 4
depends_on: [17-12, 17-13, 17-14, 17-15, 17-16]
files_modified:
  - .planning/WINDOWS.md
  - .github/workflows/feature-flags.yml
autonomous: true
gap_closure: true
requirements: [PROV-01, PROV-04]

must_haves:
  truths:
    - "A verification step that did not run is recorded as unrun-verify debt in WINDOWS.md, never omitted and never recorded as passed: plan 17-11's Snyk scan gets the row that 17-VERIFICATION.md flagged as missing alongside ids 15 and 16, and this gap-closure run's own not-run scan gets one covering every file plans 17-12 through 17-16 touched [edge: PROV-04/empty]"
    - "IN-01 is carried forward as a tracked, developer-accepted row rather than silently dropped — .project/current-exports.txt is regenerated under default features only, so the six new adapter types are untracked for public-API drift, and the row records that the exclusion was an explicit human decision on 2026-08-18 rather than an oversight"
    - "The CI blind spot 17-REVIEW.md documented is closed: feature-flags.yml's llm-all matrix leg runs cargo test --workspace --lib, and --lib never executes the tests/unit/mod.rs binary, so this run's provider_factory regression tests would have been invisible to every configuration CI actually runs; a dedicated job now runs that binary under llm-all and is required by the feature-matrix summary gate"
    - "The full crate-scoped suite yields identical pass counts under default thread parallelism and under --test-threads=1, so no test's verdict depends on execution order and the new CI job's result is reproducible locally [edge: PROV-04/ordering]"
    - "D-00d is honoured: WINDOWS.md is amended in place through its own append tool, dated, with no superseded text removed and no separate corrections file"
    - "D-00e is honoured: every closure claim in the SUMMARY carries the exact command and its exact output"
    - "D-06 is honoured: this plan touches no Rust source at all — git diff --stat over crates/ is empty for every commit"
    - statement: "ADR-0045's scored candidate table contains no empty verdict cell: every candidate carries exactly one build, defer or reject disposition, including the Meta/Llama row that has no endpoint behind it, which is dispositioned by naming Ollama as the host it targets rather than by omission"
      verification: backstop
    - statement: "Two ADR-0045 candidates that tie on every recorded criterion still carry one distinct verdict each; a tie never merges two candidate rows into a shared disposition"
      verification: backstop
    - statement: "ADR-0045's candidate table order is presentational; no verdict depends on a candidate's position in the table, and reordering the rows would not change any disposition"
      verification: backstop
  prohibitions:
    - "A verification step that did not run must NOT be recorded as one that passed, and must NOT be omitted. A Snyk scan with no tool available is unrun-verify debt with the reason attached; a coverage measurement with no Docker daemon is unmeasured, not passing. The ledger's value is that it distinguishes what was checked from what was merely not checked."
    - "A finding the developer excluded from scope must NOT disappear from the record. Excluding IN-01 from this run's work is a scheduling decision; deleting it from the register would turn a decision into an omission nobody can audit later."
  artifacts:
    - path: ".planning/WINDOWS.md"
      provides: "three new rows — 17-11's not-run Snyk scan, this gap-closure run's not-run Snyk scan, and IN-01 as carried-forward accepted debt"
      contains: "17-11"
    - path: ".github/workflows/feature-flags.yml"
      provides: "a dedicated job running the tests/unit binary under llm-all, required by the feature-matrix summary gate"
      contains: "--test unit"
  key_links:
    - from: ".github/workflows/feature-flags.yml new job"
      to: "tests/unit/llm/provider_factory_test.rs"
      via: "the job runs the unit test binary with llm-all, which is the only CI configuration that both compiles the nine-row registry and executes this file"
      pattern: "--test unit"
    - from: ".github/workflows/feature-flags.yml feature-matrix-summary"
      to: ".github/workflows/feature-flags.yml new job"
      via: "the summary gate's needs list and its result check, so a failure in the new job fails the workflow rather than being advisory"
      pattern: "needs:"
---

<objective>
Close the two bookkeeping gaps this gap-closure run is obliged to close, and make the run's own
regression tests visible to CI.

**1. The missing ledger row.** `WINDOWS.md` records the not-run Snyk scan for plan 17-09 (id 15) and
plan 17-10 (id 16), but **no matching row exists for 17-11's identical not-run scan**.
`17-VERIFICATION.md` calls this out by name as *"a minor tracking gap worth closing alongside the
code fix."* This plan closes it, and files the equivalent row for this run's own five plans, whose
scans are equally unavailable in an environment with no `snyk_code_scan` tool, no Snyk CLI and no
network egress.

**2. The excluded finding that must stay on the record.** **IN-01** — `.project/current-exports.txt`
regenerated under default features only, so `KimiAdapter`, `QwenAdapter`, `GrokAdapter`,
`OllamaAdapter`, `GeminiAdapter` and `OpenAiCompatibleAdapter` are untracked for public-API drift —
was **excluded from this run's work by explicit developer decision** in the interactive scoping
checkpoint of the `/gsd-plan-phase 17 --gaps` session on 2026-08-18. Excluding it from the work is a
scheduling call; letting it fall out of the register would turn that call into an omission nobody
can audit. It gets a tracked row saying exactly that.

**3. The CI blind spot.** `17-REVIEW.md` documented that CR-01 *"is real and currently invisible to
CI in every configuration CI actually runs"*: `feature-flags.yml`'s `llm-all` matrix leg runs
`cargo test --workspace --lib`, and `--lib` never executes the `tests/unit/mod.rs` integration-test
binary that holds `provider_factory_test.rs`; while `ci.yml:502`'s full-workspace run does execute
that binary but never adds `llm-all`, so it never compiles the six new provider rows into the
registry. Plan 17-12's regression tests would therefore protect nothing in CI.

**The scoping instruction required an explicit decision on this rather than silence. The decision is
to close it**, narrowly: a dedicated job — modelled on the workflow's existing `cli-isolation` job,
which already runs a specific `--test` target outside the matrix — running the unit binary under
`llm-all`, wired into the summary gate so a failure is blocking rather than advisory. It is deferred
out of the tracer plan (17-12) only so a workflow change that cannot be executed in this sandbox
gets its own focused verification, not because it was judged optional.

## Scope guards

- **No Rust source changes.** This plan touches `.planning/WINDOWS.md` and
  `.github/workflows/feature-flags.yml` and nothing else. Every code finding is closed by plans
  17-12 through 17-16, all of which precede this one.
- **Do not modify the fourteen-leg matrix.** Adding `--test unit` to every leg would multiply CI
  time across thirteen feature sets that have no reason to run it. One dedicated job, one feature
  set.
- **Do not touch `ci.yml`.** Its full-workspace run is correct for what it covers; widening its
  feature set is a different change with a different blast radius.
- **Do not mark any existing `WINDOWS.md` row fixed or waived.** Ids 12, 13 and 14 are
  human-accepted debt and stay open. Ids 15 and 16 stay open; this run adds to the record, it does
  not clear it.
- **D-00d** — `WINDOWS.md` is amended in place through its own tool. No hand-editing the JSON block,
  no separate corrections file.
- **D-00o** — commit with `git commit --no-verify`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/WINDOWS.md
@.planning/phases/17-additional-llm-provider-adapters/17-REVIEW.md
@.planning/phases/17-additional-llm-provider-adapters/17-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: File the three ledger rows — two not-run scans and one carried-forward exclusion</name>

  <read_first>
    - `.planning/WINDOWS.md` — **the file being modified**. Read the frontmatter counters
      (`open_count`, `waived_count`, `fixed_count`, `total_count`, `last_updated`), the whole table,
      and the trailing machine-readable JSON block. Note the exact wording and shape of **id 15**
      (17-09's not-run Snyk scan, single file) and **id 16** (17-10's, comma-joined file list) —
      the two new scan rows must read as siblings of those, not as a new genre.
    - `.planning/phases/17-additional-llm-provider-adapters/17-VERIFICATION.md` — the
      `human_verification` block's first item, whose `why_human` names the missing 17-11 row in
      exactly these terms, and the "Human Verification Required" section that restates it.
    - `.planning/phases/17-additional-llm-provider-adapters/17-REVIEW.md` §IN-01 — the finding's own
      wording, the six adapter type names it lists, and its stated non-blocking disposition.
    - `.planning/phases/17-additional-llm-provider-adapters/17-11-SUMMARY.md` — confirm what 17-11
      actually recorded about its Snyk scan, so the new row cites the plan's own words rather than
      an inference.
  </read_first>

  <behavior>
    - Three new rows exist in `WINDOWS.md`, appended through the tool so the table, the JSON block
      and the frontmatter counters all stay consistent.
    - No existing row's `status`, `reason` or text changes.
    - Each row names its phase (`17`), its kind, its file(s), and a description carrying the reason
      the item is open — never a bare label.
  </behavior>

  <files>.planning/WINDOWS.md</files>

  <reversibility rating="reversible">Ledger rows; `gsd-tools windows waive` and `windows fixed` are the sanctioned ways to change their disposition later.</reversibility>

  <action>
Use the ledger's own tool for every row — never hand-edit `WINDOWS.md`, because the table, the JSON
block and the frontmatter counters must move together (D-00d). Resolve the launcher the way every
GSD workflow does and invoke:

  `gsd-tools windows append --kind <K> --phase 17 --file <F> --description "<D>"`

**Run the three appends strictly one at a time, sequentially.** The append implementation reads a
snapshot and rewrites the ledger; two concurrent invocations lose one row silently. **Do not run any
append while a `git commit` is in flight** — concurrent `.planning/` writes trip the pre-commit
hook.

**Row A — plan 17-11's missing not-run scan.**
  `--kind unrun-verify`, `--file crates/paladin-llm/src/gemini/adapter.rs`.
  Description: state that plan 17-11's verification step 7 (Snyk code scan over
  `crates/paladin-llm/src/gemini/adapter.rs`) was not run because no `snyk_code_scan` MCP tool and no
  Snyk CLI were available in the executor's runtime (no network egress); that 17-11's own SUMMARY
  recorded it as not-run and never as passed; and that this row is the sibling of ids 15 and 16,
  filed on 2026-08-18 by plan 17-17 after `17-VERIFICATION.md` flagged its absence.

**Row B — this gap-closure run's own not-run scan.**
  `--kind unrun-verify`, `--file` the comma-joined list, matching id 16's convention:
  `crates/paladin-llm/src/provider_factory.rs,tests/unit/llm/provider_factory_test.rs,crates/paladin-llm/src/openai_compatible/adapter.rs,crates/paladin-llm/src/gemini/adapter.rs,crates/paladin-llm/src/compat/engine.rs`
  Description: state that plans 17-12 through 17-16 each attempted the mandated Snyk scan over the
  files they modified and that none could run — no `snyk_code_scan` MCP tool and no Snyk CLI in this
  environment — so all five are recorded as not-run, never as passed, per
  `.github/instructions/snyk_rules.instructions.md` as imported by `CLAUDE.md`. **Before writing the
  description, read each of the five plans' SUMMARYs and confirm what each actually recorded**; if
  any plan's scan *did* run, say so and name it rather than sweeping it into the row.

**Row C — IN-01 carried forward.**
  `--kind deviation`, `--file .project/current-exports.txt`.
  Description: state that the public-API surface snapshot was regenerated under default features
  only, so `KimiAdapter`, `QwenAdapter`, `GrokAdapter`, `OllamaAdapter`, `GeminiAdapter` and
  `OpenAiCompatibleAdapter` do not appear in it and cannot be checked for public-API drift; that
  this is consistent with D-11's unchanged default feature set and is not itself wrong; that
  `17-REVIEW.md` records it as IN-01, non-blocking, with the suggested follow-up of generating an
  `--features llm-all` variant or documenting its absence; and that it was **excluded from the
  2026-08-18 gap-closure scope by explicit developer decision**, taken in an interactive
  `AskUserQuestion` checkpoint in the orchestrating `/gsd-plan-phase 17 --gaps` session — a recorded
  human choice, not an `--auto` inference — and is therefore carried forward as accepted debt rather
  than dropped.

After all three appends, run `gsd-tools windows status` and confirm the counters moved by exactly
three open rows and that no pre-existing row changed status.

Commit `.planning/WINDOWS.md` as one `docs(17-17):` commit with `git commit --no-verify`.
  </action>

  <verify>
    <automated>grep -c '^| 1[789] | 17 |' .planning/WINDOWS.md</automated>
  </verify>

  <acceptance_criteria>
    - `gsd-tools windows status` → `open_count` is the pre-task value **+ 3** (18 if it was 15); `fixed_count` and `waived_count` unchanged. Record the before and after values.
    - `grep -c 'unrun-verify' .planning/WINDOWS.md` increases by **4** relative to the pre-task value — two new table rows plus their two JSON entries. Record both counts.
    - `grep -c '17-11' .planning/WINDOWS.md` → **≥ 2** (Row A's table entry and its JSON entry) — the gap `17-VERIFICATION.md` named is closed.
    - `grep -c 'IN-01' .planning/WINDOWS.md` → **≥ 2** — the excluded finding is on the record.
    - `grep -c '2026-08-18' .planning/WINDOWS.md` → **≥ 3** — each new row records the human decision's date.
    - `grep -c 'OpenAiCompatibleAdapter' .planning/WINDOWS.md` → **≥ 2** — Row C names the untracked types rather than gesturing at them.
    - `python3 -c "import json,sys,re; s=open('.planning/WINDOWS.md').read(); b=re.search(r'\`\`\`\`json\n(.*)\n\`\`\`\`', s, re.S).group(1); d=json.loads(b); print(len(d))"` → parses without error and prints the pre-task row count **+ 3**. The machine-readable block stayed valid and in step with the table.
    - `python3 -c "import re; s=open('.planning/WINDOWS.md').read(); print(len(re.findall(r'^\| \d+ \| ', s, re.M)))"` → the same number as the JSON block's length. Table and JSON agree.
    - `git diff --stat -- crates/ tests/ .github/` → empty output for this commit. Ledger-only change.
    - No pre-existing row's `status` column changed: `grep -c '| fixed |' .planning/WINDOWS.md` and `grep -c '| open |' .planning/WINDOWS.md` match their pre-task values plus the three new open rows.
    - Exactly one commit, message prefix `docs(17-17):`, created with `--no-verify`.
  </acceptance_criteria>

  <done>
    Every verification this phase could not run is on the ledger with its reason attached — including
    the 17-11 row `17-VERIFICATION.md` flagged as missing — and the one finding the developer
    excluded from scope is recorded as an excluded finding rather than an absent one.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make the registry regression tests visible to CI, in the one configuration that compiles all nine providers</name>

  <read_first>
    - `.github/workflows/feature-flags.yml` — **the file being modified**, in full. Specifically:
      the `on:` triggers and the `concurrency` group (`:10-29`); the fourteen-leg `feature-matrix`
      job's `strategy.matrix.include` list including the `llm-all` entry (`:71-73`) and the shared
      `steps:` block (`:103-129`), whose test step is `cargo test --workspace --lib ${{ matrix.flags }}`
      — **the `--lib` that causes the blind spot**; the `cli-isolation` job (`:131-...`), which is
      the in-file precedent for a dedicated job running a specific `--test` target with its own
      toolchain and cache steps; and `feature-matrix-summary` (`:159-...`), its `needs:` list and the
      shell `if` that reads each dependency's `result`.
    - `Cargo.toml` (workspace root) — the `[[test]] name = "unit" path = "tests/unit/mod.rs"` entry
      (`:170-172`), which is the target the new job runs, and the `llm-all` feature definition.
    - `.planning/phases/17-additional-llm-provider-adapters/17-REVIEW.md` §CR-01, the paragraph
      beginning "Note also that this specific CI leg would not have caught the failure" — the
      finding's own statement of the blind spot and of why `ci.yml:502` does not cover it either.
    - `tests/unit/mod.rs` — confirm which submodules the binary pulls in, so the SUMMARY can state
      what the new job actually executes beyond `provider_factory_test.rs`.
  </read_first>

  <behavior>
    - A new job runs `cargo test --test unit --features llm-all`, exercising the nine-row registry
      against the workspace-level unit binary — the combination no CI job runs today.
    - `feature-matrix-summary` requires it: the job name is in `needs:` and its `result` is checked
      in the summary shell, so a failure fails the workflow.
    - The fourteen-leg matrix is unchanged.
    - The job follows the file's existing conventions: `ubuntu-latest`, `actions/checkout@v4`,
      `dtolnay/rust-toolchain@stable`, `Swatinem/rust-cache@v2` with an explicit `key`.
  </behavior>

  <files>.github/workflows/feature-flags.yml</files>

  <reversibility rating="reversible">A CI job addition; deleting the job and its two summary-gate references restores the previous workflow exactly.</reversibility>

  <action>
Add one job to `.github/workflows/feature-flags.yml`, placed after `cli-isolation` and before
`feature-matrix-summary` so the file's read order stays matrix → dedicated jobs → summary.

Job id `llm-registry-unit-tests`, name `LLM Registry Unit Tests (llm-all)`, `runs-on: ubuntu-latest`.
Steps, mirroring `cli-isolation`'s shape: checkout (`actions/checkout@v4`), toolchain
(`dtolnay/rust-toolchain@stable`), cache (`Swatinem/rust-cache@v2` with
`key: llm-registry-unit-tests` — the file's own comment on the matrix cache explains why an explicit
key is needed when every job uses the same toolchain), then one run step:

  `cargo test --test unit --features llm-all`

Above the job, a comment recording **why it exists** — three sentences, not a label:
  - the fourteen-leg matrix's test step is `cargo test --workspace --lib`, and `--lib` does not
    execute the `tests/unit/mod.rs` integration-test binary at all, so no matrix leg runs
    `tests/unit/llm/provider_factory_test.rs`;
  - `ci.yml`'s full-workspace run does execute that binary but never adds `llm-all`, so it never
    compiles the six new provider rows into the registry;
  - the intersection — the unit binary **and** the nine-row registry — was therefore run by no CI
    job, which is why CR-01 (`17-REVIEW.md`) reproduced locally against a live tree while every CI
    configuration stayed green. Name CR-01 and `17-REVIEW.md` so the comment survives as provenance.

Then wire the gate: add `llm-registry-unit-tests` to `feature-matrix-summary`'s `needs:` list, and
extend the summary step's shell — add the new job's `result` to the success condition, an echo line
on the success path, and its `result` to the failure diagnostic block, matching exactly how
`cli-isolation` is handled in both branches. **Both halves are required**: a job in `needs:` whose
result is not read still gates on job failure, but the summary's own message would be misleading,
and the existing block reads both dependencies explicitly.

Change nothing else. Not the triggers, not the concurrency group, not the matrix, not
`cli-isolation`, not `ci.yml`.

Commit as one `ci(17-17):` commit with `git commit --no-verify`, citing CR-01 and the blind spot
`17-REVIEW.md` documented.
  </action>

  <verify>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/feature-flags.yml')); j=d['jobs']; assert 'llm-registry-unit-tests' in j; assert 'llm-registry-unit-tests' in j['feature-matrix-summary']['needs']; print('ok')"</automated>
  </verify>

  <acceptance_criteria>
    - The `<automated>` command prints `ok` and exits 0 — the workflow still parses as YAML, the new job exists, and the summary gate depends on it. A YAML parse failure here is the whole in-sandbox safety net for a file that cannot be executed locally; do not proceed past a failure.
    - `python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/feature-flags.yml')); print(len(d['jobs']['feature-matrix']['strategy']['matrix']['include']))"` → **14**. The matrix is untouched.
    - `grep -c 'llm-registry-unit-tests' .github/workflows/feature-flags.yml` → **≥ 3** — the job definition, the `needs:` entry, and at least one `result` reference in the summary shell.
    - `grep -c 'cargo test --test unit --features llm-all' .github/workflows/feature-flags.yml` → **1**.
    - **The command the new job runs is green locally, on this tree, right now:** `cargo test --test unit --features llm-all` → 0 failed. Record the pass count. This is the only way to know the job will pass before it ever runs on a runner, and it is exactly the binary-plus-feature-set combination no CI job covered before.
    - The same command with `--test-threads=1` appended → identical pass count, 0 failed. Record both numbers side by side; equality is the ordering/reproducibility criterion, and it is what makes the CI job's default-parallelism run trustworthy.
    - `grep -c 'CR-01' .github/workflows/feature-flags.yml` → **≥ 1** — the job's comment carries its own provenance, so a future maintainer trimming CI can see what the job protects before deleting it.
    - `git diff -- .github/workflows/feature-flags.yml | grep -cE '^[+-].*(on:|concurrency:|cancel-in-progress|branches:)'` → **0**. Triggers and concurrency are untouched.
    - `git diff -- .github/workflows/ci.yml` → empty output. `ci.yml` is not this plan's file.
    - `git diff --stat -- crates/ tests/ .planning/` → empty output for this commit. Workflow-only change.
    - Exactly one commit, message prefix `ci(17-17):`, created with `--no-verify`.
  </acceptance_criteria>

  <done>
    The registry regression tests plan 17-12 added run in CI, in the one configuration that both
    compiles all nine provider rows and executes the workspace unit binary, and a failure there fails
    the feature-flags workflow rather than passing unnoticed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executed work → `WINDOWS.md` | The ledger is the project's record of what was checked and what was not. Its integrity depends entirely on nothing being recorded as checked that was not. |
| pushed branch → `feature-flags.yml` | CI is the automated gate on every branch. A gate that does not run the tests it appears to run gives false assurance, which is worse than no gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-72 | Repudiation | `WINDOWS.md`'s record of unrun verifications | high | mitigate | A mandated security scan that silently disappears from the record reads later as a scan that passed, and the CLAUDE.md-mandated Snyk posture becomes unauditable. Three rows are filed through the ledger's own append tool so the table, the JSON block and the counters stay in step; the JSON-parses-and-agrees-with-the-table criterion catches a partial write; and no existing row's status is touched, verified by before/after status counts. The 17-11 row specifically closes the omission `17-VERIFICATION.md` named. |
| T-17-73 | Spoofing | a CI gate that appears to cover what it does not | high | mitigate | `feature-flags.yml`'s `llm-all` leg reports success for a feature set whose registry-facing tests it never executes — the precise reason CR-01 shipped green through CI while reproducing deterministically against a live tree. The new job runs the real binary under the real feature set and is required by the summary gate. Because the workflow cannot be executed in this environment, the mitigation is verified three ways: the YAML parses and the dependency edge exists (asserted programmatically), the matrix leg count is unchanged, and **the exact command the job will run is executed locally and recorded green**. |
| T-17-SC-17 | Tampering | npm/pip/cargo installs | high | mitigate | **No package-manager install task exists in this plan.** No Rust source changes at all; the only tools invoked are the GSD ledger CLI, `python3`'s bundled `yaml` reader and `cargo test`. `git diff --stat -- crates/` being empty is an acceptance criterion on both tasks, so `make deny` / `make audit` verdicts are provably unchanged. No `[ASSUMED]` / `[SUS]` package; no legitimacy checkpoint required. |

ASVS L1, `security_block_on: high`. All three entries are `high` and each carries a named mitigation
with a named check or a named absence. Nothing blocks.
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**This plan (17-17) creates:**

**New `WINDOWS.md` ids** — three, numbered sequentially by the append tool from the current maximum
(17, 18 and 19 if the ledger's `total_count` is 16 when this plan runs; record the actual ids
assigned):
- one `unrun-verify` row for plan 17-11's not-run Snyk scan over `crates/paladin-llm/src/gemini/adapter.rs`
- one `unrun-verify` row for plans 17-12 through 17-16's not-run Snyk scans over the five files this
  gap-closure run modified
- one `deviation` row carrying IN-01 forward as developer-accepted, explicitly-excluded debt

**New CI job (`.github/workflows/feature-flags.yml`):**
- job id `llm-registry-unit-tests`, name `LLM Registry Unit Tests (llm-all)`, running
  `cargo test --test unit --features llm-all`

**Changed (existing, no rename):** `feature-matrix-summary` — its `needs:` list and its result-check
shell.

**New Rust symbols, functions, constants, module paths, public items or dependencies:** none. This
plan touches no code.
</artifacts_this_phase_produces>

<executor_notes>
## Operational facts for this repository — non-negotiable

1. **Commit with `git commit --no-verify`.** `workflow.worktree_skip_hooks` is `true` in
   `.planning/config.json` (D-00o); without `--no-verify` the pre-commit hook cold-compiles the
   workspace and the executor stalls.
2. **Never run a `windows append` while a commit is in flight**, and never run two appends
   concurrently. Concurrent `.planning/` writes trip the pre-commit hook, and the append
   implementation reads a snapshot before rewriting — a concurrent pair loses a row silently.
   Sequence strictly: append, append, append, verify, commit.
3. **Never run a whole-workspace build or test.** The one workspace-level command this plan runs is
   `cargo test --test unit --features llm-all`, which is a single test binary and is required —
   it is the exact command the new CI job will run.
4. **Never run `make coverage`, `make test-all`, or any Docker-dependent target.** No Docker daemon,
   no network egress, no vendor API keys.
5. **The workflow file cannot be executed here.** Do not attempt `act`, `gh workflow run`, or any
   runner emulation. The verification is: YAML parses, the dependency edge exists, the matrix is
   unchanged, and the job's command is green locally. Say so in the SUMMARY rather than implying the
   job itself was observed passing.
6. **Snyk:** no first-party code changes in this plan, so no scan is applicable. Say that plainly in
   the SUMMARY — "not applicable, no source changed" is a different statement from "not run", and
   Row B already covers the five plans that did change source.
</executor_notes>

<deferred>
## Deferred and explicitly out of scope

- **IN-01's actual fix** — regenerating `.project/current-exports.txt` under `--features llm-all`,
  or adding a sibling snapshot. **Excluded by explicit developer decision**, 2026-08-18. Task 1 files
  the tracking row; the work is not done here. The trigger recorded in `17-REVIEW.md` stands: a
  follow-up that wants public-API drift detection for the six new adapters.
- **The four `human_verification` items** in `17-VERIFICATION.md`'s frontmatter — the Snyk scan over
  plans 17-09/17-10/17-11's files, the `make coverage` 82% floor (ADR-0006, `WINDOWS.md` id 13), the
  Ollama Docker live suite (id 12), and the vendor base-URL/model-ID live smoke test. **None is
  closeable by any plan in this run.** This environment has no Docker daemon, no network egress, no
  vendor API keys and no `snyk_code_scan` tool — confirmed absent from the orchestrating session's
  tool list. This plan records them; it does not pretend to run them, and no acceptance criterion
  anywhere in plans 17-12 through 17-17 depends on any of them.
- **Widening `ci.yml`'s full-workspace run to include `llm-all`** — a second, larger way to close the
  same blind spot, with a much wider blast radius across every job that run gates. Not taken; the
  dedicated job is the narrow fix. Recorded so the alternative is visibly considered rather than
  unnoticed.
- **Adding `--test unit` to all fourteen matrix legs** — rejected: thirteen of them have no registry
  rows to exercise and would pay the compile and run cost for nothing.
- **Marking PROV-01..PROV-04's checkboxes complete in `REQUIREMENTS.md`** — requirement checkboxes
  are adjudicated at phase close, per the Phase 3 precedent PROV-01's own 2026-08-17 amendment cites.
  Not this plan's job.
</deferred>

<probe_resolutions>
## Spec-less probe resolutions — full reconciliation for Phase 17

No `-SPEC.md` exists for Phase 17, so both the edge and prohibition sections were absent and the
planner resolved them per §A/§B/§C of the spec-less probe fallback. This plan holds the
phase-complete reconciliation; each individual plan restates only the rows it carries.

### Edge coverage — all 18 applicable rows resolved

| # | Row | Resolution | Carried by |
|---|---|---|---|
| 1 | PROV-01 / adjacency | backstop | 17-17 — tied ADR-0045 candidates still carry one verdict each |
| 2 | PROV-01 / empty | backstop | 17-17 — no empty verdict cell; the Meta/Llama row is dispositioned by naming a host |
| 3 | PROV-01 / encoding | **unresolved — flagged planner assumption** | 17-17, below |
| 4 | PROV-01 / ordering | backstop | 17-17 — candidate-table order is presentational |
| 5 | PROV-02 / boundary | covered | 17-14 — `min > max` rejected, `min == max` accepted |
| 6 | PROV-02 / adjacency | covered | 17-15 — the truncation guard's two conditions and their adjacent cases |
| 7 | PROV-02 / empty | covered | 17-15 — truncated-to-empty is an error, not an empty-string success |
| 8 | PROV-02 / ordering | covered | 17-16 — only the open is retried, so stream items are never duplicated or reordered |
| 9 | PROV-02 / precision | covered | 17-14 — non-finite bounds rejected before the comparison `NaN` would defeat |
| 10 | PROV-03 / adjacency | covered | 17-13 — two spellings merge to one row; `openai` is not absorbed |
| 11 | PROV-03 / empty | covered | 17-12 — a blank credential variable is treated as absent (CR-01 itself) |
| 12 | PROV-03 / ordering | covered | 17-12 — registry declaration order is preserved and tested |
| 13 | PROV-03 / concurrency | covered | 17-12 — the merged ten-variable guard; identical verdicts serial and parallel |
| 14 | PROV-04 / adjacency | covered | 17-13 — `create()`'s rustdoc names both spellings, so the surface does not imply two providers |
| 15 | PROV-04 / empty | covered | 17-17 — a verification that produced no result is recorded as `unrun-verify`, never omitted or passed |
| 16 | PROV-04 / encoding | covered | 17-12 — `str::trim` uses the Unicode `White_Space` set; U+00A0 regression test |
| 17 | PROV-04 / ordering | covered | 17-17 — identical pass counts under default parallelism and `--test-threads=1` |
| 18 | PROV-04 / concurrency | backstop | 17-16 — cancellation safety of a `generate_stream` future dropped during retry backoff |

**Counts: 18 applicable → 13 covered + 4 backstop + 1 unresolved-and-flagged = 18.** No silent drop
(§C count equality).

Every `covered` row appears verbatim as a plain `must_haves.truths` string in its carrying plan, with
a real acceptance criterion behind it. Every `backstop` row is authored as a flat-scalar
`{ statement, verification: backstop }` marker in `must_haves.truths`, so a verifier that cannot
confirm it abstains to `human_needed` rather than passing silently.

### The one unresolved row — flagged, not dropped

**PROV-01 / encoding** — *"Whose definition of length/equality applies — bytes, code points, grapheme
clusters, or normalized form?"*

**Planner assumption, stated for a human to overturn:** PROV-01 is a recorded provider-selection
study (ADR-0045) — prose, criteria and verdicts. It performs no length or equality operation on any
text, so the probe's question has no referent in this requirement. It was **not auto-dismissed**,
because a dismissal claims the probe was wrong and this planner cannot defend that claim: it is
possible a reader intends "equality" to mean *candidate identity* (whether two differently-spelled
rows name the same provider — a real question that ADR-0045's Groq/Together/Mistral/Fireworks/Bedrock
rejections touch on). Rather than manufacture an acceptance criterion for a reading that may not be
the intended one, the row stays `unresolved` and is surfaced here. **It blocks nothing** and no
acceptance criterion depends on it.

### Prohibitions — kept, descriptor-less, flagged-unverified

Two-stage recall over the gap-closure scope over-produced roughly ten candidates; precision filtering
dropped the routine-engineering ones (owned by the edge probe or by code review) and kept **eight**
values/transparency items across the six plans, each authored descriptor-less in its plan's
`must_haves.prohibitions` block so each disposes flagged-unverified:

| Plan | Prohibition |
|---|---|
| 17-12 | availability must not be reported on a basis weaker than a credential that would construct |
| 17-12 | the emptiness check must never log, echo or embed the credential's value |
| 17-13 | no fuzzy or nearest-name resolution that could misroute to a different vendor |
| 17-14 | never silently repair an operator's configuration — no swap, no clamp, no default |
| 17-15 | never blame the token budget for a refusal |
| 17-15 | never return an empty-string success for a response that produced no usable text |
| 17-16 | never silently multiply a caller's billed prompt tokens |
| 17-16 | never extend the retry over the byte stream itself |
| 17-17 | a verification that did not run is never recorded as passed and never omitted |
| 17-17 | a finding excluded from scope must not disappear from the record |

**Canon-referral breadcrumbs** (surfaced, deliberately not minted — a dedicated tool already owns
each):
- *"re-transmitting a live credential to an endpoint that has already rejected it is canon
  secrets-handling — owned by the phase's STRIDE registers (17-12 T-17-61, 17-16 T-17-69) and
  `/gsd-secure-phase`; not minted as a prohibition."*
- *"URL-path injection via a caller-supplied model identifier is canon injection — already closed by
  plan 17-09's `validate_model_identifier` and owned by `/gsd-secure-phase`; not re-minted here."*
</probe_resolutions>

<verification>
Run after both tasks, from the workspace root:

1. `gsd-tools windows status` — `open_count` up by exactly 3; `fixed_count` and `waived_count` unchanged.
2. The table/JSON agreement check and the four `grep -c` ledger criteria in Task 1.
3. The YAML parse + dependency-edge assertion and the matrix-leg count in Task 2.
4. `cargo test --test unit --features llm-all` — 0 failed. Record the pass count.
5. `cargo test --test unit --features llm-all -- --test-threads=1` — identical pass count, 0 failed.
6. `cargo test -p paladin-llm --no-default-features --features "kimi,qwen,grok,ollama,openai-compatible,gemini"` — 0 failed. The whole gap-closure run's regression surface, in one command, as the final state.
7. `cargo test -p paladin-llm` (default features) — 0 failed.
8. `cargo clippy -p paladin-llm --all-targets --no-default-features --features "kimi,qwen,grok,ollama,openai-compatible,gemini" -- -D warnings` — exit 0.
9. `cargo fmt --check -p paladin-llm` — exit 0.
10. `git diff --stat -- crates/ tests/` across this plan's commits — empty.

**Do not run** `make coverage`, `make test-all`, `cargo test --workspace`, `act`, `gh workflow run`,
or any Docker-dependent target.
</verification>

<success_criteria>
- Every verification this phase could not run is on the `WINDOWS.md` ledger with its reason — including
  plan 17-11's Snyk scan, whose absence `17-VERIFICATION.md` named, and this run's own.
- IN-01 is a tracked, developer-accepted row recording that its exclusion was a human decision on
  2026-08-18, not an omission.
- The CI blind spot is closed by a dedicated job running the unit binary under `llm-all`, required by
  the summary gate, with its provenance in a comment — and the command it runs is proven green
  locally, since the workflow itself cannot be executed here.
- The `.planning/WINDOWS.md` table, its JSON block and its frontmatter counters remain in step.
- No Rust source changed; no existing ledger row's status changed; the fourteen-leg matrix,
  `ci.yml`, and the workflow's triggers are untouched.
- Two commits: `docs(17-17):` (ledger), `ci(17-17):` (workflow).

**On completion, all five in-scope findings — CR-01, WR-01, WR-02, WR-03 (new) and WR-04 (new) — are
closed, the two bookkeeping gaps are closed, and Phase 17 is ready for re-verification.**
</success_criteria>

<output>
Create `.planning/phases/17-additional-llm-provider-adapters/17-17-SUMMARY.md` when done.

Record, per D-00e: the exact command and exact output for every verification command; the three
`WINDOWS.md` ids the append tool actually assigned; the `open_count` before and after; the pass count
for `cargo test --test unit --features llm-all` under both thread settings, side by side; and the
fact that the new CI job's own execution was **not** observed — only its YAML validity, its
dependency edge and its command's local result — stated plainly rather than implied away.

Because this is the last plan of the gap-closure run, also record a closing note for
`/gsd-verify-work`, so it can separate closed work from accepted debt without re-reading the review:

- **Closed by this run (5):** CR-01 (blank-credential availability), WR-01 (`openai_compatible`
  alias), WR-02 (inverted temperature range), WR-03 *new* (Gemini truncated-empty detection), WR-04
  *new* (stream-open retry parity). Note the two ID collisions explicitly — `17-REVIEW.md`'s WR-03
  and WR-04 are **different findings** from the WR-03 and WR-04 plans 17-11 and 17-10 closed, all
  four of which are now closed.
- **Carried forward as accepted debt (1):** IN-01, excluded by developer decision 2026-08-18, now on
  the ledger.
- **Still blocked on the environment (4):** the four `human_verification` items — Snyk scan, the 82%
  coverage floor, the Ollama Docker live suite, and the vendor base-URL/model-ID live smoke test.
- **One flagged, unresolved probe row:** PROV-01 / encoding, surfaced in this plan's
  `<probe_resolutions>` as a planner assumption for a human to overturn. Blocks nothing.
</output>
