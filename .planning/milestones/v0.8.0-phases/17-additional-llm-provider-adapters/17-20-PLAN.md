---
phase: 17-additional-llm-provider-adapters
plan: 20
type: execute
wave: 4
depends_on: [17-21]
files_modified:
  - config.example.yml
  - .env.example
  - docs/src/getting-started/configuration.md
  - crates/paladin-llm/README.md
  - crates/paladin-llm/src/lib.rs
  - .planning/phases/17-additional-llm-provider-adapters/COVERAGE.md
autonomous: true
gap_closure: true
gap_ids: [G-17-4a, G-17-4b, G-17-4d]
requirements: [PROV-04]

must_haves:
  truths:
    - "An operator who copies config.example.yml verbatim and supplies their own credentials gets a working Kimi, a working Grok and a Qwen pointed at the region this project ships as its default — every default_model value names a model the vendor's live catalog actually returns and every base_url names the endpoint the shipped constant names, so the example is a starting point rather than a trap"
    - "The documentation-currency caveat says what is now true per vendor rather than one blanket disclaimer: Gemini, Grok, Kimi and Qwen are each recorded as live-verified with the date and what was verified, and Ollama is recorded as operator-local with no vendor endpoint to verify"
    - "Wherever DASHSCOPE_BASE_URL is described in the operator-facing surface, the region-scoped-credential constraint is described with it and the known regional endpoints are named — the shipped default is right for one region and wrong for the others, and the operator it is wrong for must be able to fix it without reading a planning artifact"
    - "No retired model identifier and no superseded base URL survives anywhere in the shipped surface — not in the crate constants, not in config.example.yml, not in the configuration guide, not in a test fixture in lib.rs"
    - "lib.rs's cross-adapter fixtures reference the adapters' own default-model and default-base-URL constants rather than repeating string literals, so a fixture cannot silently drift from the constant it is meant to exercise the next time a vendor retires a model or this project changes a region default"
    - "COVERAGE.md's verification-status section distinguishes what a live run proved from what remains mock-transport-only, so an INTEGRATE row is not read as a live-exercised claim it never made [edge: PROV-04/empty]"
    - "`.env.example` declares every environment variable the LLM adapters actually read, enumerated from the `env::var` call sites rather than from memory — all four credential names currently absent (MOONSHOT_API_KEY, DASHSCOPE_API_KEY, OPENAI_COMPATIBLE_API_KEY) are present, and each provider's `*_BASE_URL`, `*_MODEL` and `*_TIMEOUT_SECONDS` overrides are declared alongside its key, so an operator discovers what is tunable without reading adapter source"
    - "The declared names are the truth and nothing more: a variable appears in `.env.example` only if an adapter reads it, so the file cannot advertise a knob that does nothing. OPENAI_COMPATIBLE_TEMPERATURE_MIN/MAX and the six OPENAI_COMPATIBLE_SUPPORTS_* flags are declared because that adapter reads them; no per-vendor temperature variable is invented for the vendors that have none [edge: PROV-04/boundary]"
    - "The configuration guide covers the environment-variable surface as a first-class configuration path beside the YAML one, including that devcontainer credentials arrive from `~/.config/paladin/` via `.devcontainer/paladin-env.sh` and that a non-interactive shell must source it explicitly"
    - "The whole workspace still builds, tests and lints clean after the fixture change — a documentation plan that breaks a test is not a documentation plan"
  prohibitions:
    - "A caveat MUST NOT be deleted outright where it is still true. Ollama has no hosted endpoint to verify at all, and several compat surfaces remain mock-transport-only; flattening everything to 'verified' would manufacture exactly the documentation-currency debt PROV-04 exists to prevent."
    - "Qwen MUST NOT be recorded as unverified. That was true when this plan was first written and is false as of 2026-08-22: plan 17-21 read a live model list at the shipped default and the harness reports PASS on both probes."
    - "A value MUST NOT be retyped from a SUMMARY when a shipped constant holds it. Take every model identifier and every base URL from the constant and cross-check against the SUMMARY, not the other way round."
    - "This plan MUST NOT change any adapter's behaviour. It edits documentation, a configuration example, a planning artifact, and test fixtures that assert nothing about the literals they carry."
  artifacts:
    - path: ".env.example"
      provides: "the complete, code-derived environment-variable surface for every LLM adapter — credential, base-URL, model and timeout names per provider, plus the openai-compatible capability and temperature overrides"
      contains: "MOONSHOT_API_KEY"
    - path: "config.example.yml"
      provides: "kimi, grok and qwen blocks carrying live-verified default_model and base_url values, a per-vendor verification note, and the DashScope region constraint"
      contains: "default_model"
    - path: "docs/src/getting-started/configuration.md"
      provides: "the same refreshed values, per-vendor verification status and region constraint in the published configuration guide"
      contains: "default_model"
    - path: "crates/paladin-llm/README.md"
      provides: "a per-vendor verification note replacing the single blanket unverified-endpoint caveat, and the corrected Qwen base URL in the provider table"
      contains: "Providers"
    - path: "crates/paladin-llm/src/lib.rs"
      provides: "cross-adapter fixtures referencing GROK_DEFAULT_MODEL, KIMI_DEFAULT_MODEL and QWEN_DEFAULT_BASE_URL instead of hardcoded literals"
      contains: "DEFAULT_MODEL"
    - path: ".planning/phases/17-additional-llm-provider-adapters/COVERAGE.md"
      provides: "an amended-in-place verification-status section recording which surfaces a live run exercised and which remain mock-transport-only"
      contains: "Verification status"
  key_links:
    - from: ".env.example declared variable names"
      to: "crates/paladin-llm/src/*/adapter.rs env::var call sites"
      via: "the example is the operator-facing index of what the adapters read; it is derived by enumerating the call sites, so a name in the file that no adapter reads, or a read the file omits, is the drift this entry exists to prevent"
      pattern: "env::var"
    - from: "config.example.yml kimi/grok/qwen default_model and base_url"
      to: "crates/paladin-llm/src/{kimi,grok,qwen}/adapter.rs *_DEFAULT_MODEL and QWEN_DEFAULT_BASE_URL"
      via: "the example is the operator-facing copy of the constant; the two disagreeing is how an operator ends up debugging a resource_not_found_error, or a silent three-model fallback, against a config file they were told to copy"
      pattern: "default_model"
    - from: "crates/paladin-llm/src/lib.rs cross-adapter fixtures"
      to: "crates/paladin-llm/src/{kimi,grok,qwen}/adapter.rs constants"
      via: "referencing the constant instead of repeating its value means the next model retirement or region change updates one place, not two — lib.rs:326 is the live example of that drift"
      pattern: "DEFAULT_MODEL"
---

<objective>
Bring the advertised surface back in line with the shipped one after plans 17-18, 17-19 and 17-21
changed Grok's and Kimi's default models and Qwen's default region — PROV-04's second half, and
phase success criterion 5: *"`paladin-llm`'s `Cargo.toml` description/keywords, the crate README,
and the configuration documentation name exactly the providers that exist — so this phase does not
create the same documentation-currency debt Phase 16 is closing."*

Three of the four surfaces still advertise retired identifiers: `config.example.yml:52` and `:66`,
`docs/src/getting-started/configuration.md:54` and `:65`, and a pair of literals in
`crates/paladin-llm/src/lib.rs`'s cross-adapter fixtures. All four also carry a blanket caveat —
*"recorded from vendor documentation but have not been verified against a live endpoint"* — which
is now false for four vendors and never applied to the fifth.

**This plan moved to run after 17-21, not before it.** The earlier revision had it at wave 3,
before the Qwen work, on the assumption that only Kimi's and Grok's model identifiers would
change. Qwen's *base URL* now changes too — `config.example.yml:58`,
`docs/src/getting-started/configuration.md:59`, `crates/paladin-llm/README.md:20` and
`crates/paladin-llm/src/lib.rs:326` all name the superseded Singapore endpoint — and a
documentation plan that predicts a constant instead of quoting it is the drift this plan exists to
remove. So it now runs last among the documentation-bearing plans and quotes what shipped.

One item here is not currency housekeeping but a gap deliverable: **G-17-4d's first `missing`
item** is that the region-scoped-credential constraint be documented wherever `DASHSCOPE_BASE_URL`
is described. Plan 17-21 puts it in the module rustdoc, where a developer reads it. This plan puts
it where an *operator* reads it. With the default now Virginia, the operator who needs it is the
Singapore or mainland one, who was fine yesterday.

**Purpose:** an operator who copies the example gets a working configuration, a mismatched
operator can correct it in one step, and a reader can tell which vendor facts were measured from
which were merely cited.
**Output:** refreshed identifiers and base URLs across every operator-facing surface, the region
constraint stated where the override is described, a per-vendor verification note in place of one
blanket disclaimer, and COVERAGE.md amended in place with what the live run actually proved.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-additional-llm-provider-adapters/17-UAT.md
@.planning/phases/17-additional-llm-provider-adapters/17-18-SUMMARY.md
@.planning/phases/17-additional-llm-provider-adapters/17-19-SUMMARY.md
@.planning/phases/17-additional-llm-provider-adapters/17-21-SUMMARY.md

@config.example.yml
@.env.example
@docs/src/getting-started/configuration.md
@crates/paladin-llm/README.md
@.planning/phases/17-additional-llm-provider-adapters/COVERAGE.md
</context>

<interface_context>
## Facts already established — read these, do not re-derive them

**The refreshed identifiers are in plans 17-18's, 17-19's and 17-21's SUMMARYs**, together with the
live `/models` responses they were chosen from. Take the values from the shipped constants
(`GROK_DEFAULT_MODEL`, `GROK_FALLBACK_MODELS`, `KIMI_DEFAULT_MODEL`, `KIMI_FALLBACK_MODELS`,
`QWEN_DEFAULT_MODEL`, `QWEN_FALLBACK_MODELS`, `QWEN_DEFAULT_BASE_URL`) and cross-check them against
those SUMMARYs. **Do not re-run a live probe for this plan** — nothing here is a measurement, and a
second probe that disagreed with the first would create a discrepancy this plan has no mandate to
adjudicate.

**Verification status after plans 17-18, 17-19 and 17-21, per vendor:**

| Vendor | Status to record |
|---|---|
| Gemini | Live-verified: model list and a `generate()` round trip, dated. The regression control throughout the run. |
| Grok (xAI) | Live-verified by plan 17-18: model list, default model present in it, and a `generate()` round trip with default prompt parameters. |
| Kimi (Moonshot) | Live-verified by plan 17-19: same three, plus the measured temperature constraint. |
| Qwen (DashScope) | **Live-verified by plan 17-21**, at the endpoint that now ships: 92 models in the live list on 2026-08-22, differing from the curated fallback, with the default model present, plus a `generate()` round trip and per-parameter verdicts. Carry the region caveat, which is a different statement from an unverified one — see below. |
| Ollama | Not applicable: self-hosted, no vendor endpoint. Its live exercise is the Docker Tier 2 suite (UAT test 3), which passed on a GitHub Actions runner on 2026-08-19. |

**What replaced Qwen's old caveat, and why it is not simply deleted.** The previous revision of
this plan told you to record Qwen as unverified because "the base URL is confirmed correct and only
the credential is rejected". That diagnosis was **falsified on 2026-08-22**: DashScope API keys are
region-scoped, so a well-formed `401` arrives from every endpoint except the key's own and proves
nothing about the URL. The credential was valid; the shipped default named the wrong region. Plan
17-21 changed the default to US (Virginia) by developer decision and live-verified it. So Qwen's
line becomes verified — and gains a *new*, true caveat: the default is right for one region and
wrong for three, and an operator in Singapore or on the mainland must set `DASHSCOPE_BASE_URL`
because their key will not work anywhere else. The three known compatible-mode endpoints are in
plan 17-21's `<interface_context>` and in the shipped module rustdoc; take them from the rustdoc.

**`crates/paladin-llm/src/lib.rs` around line 320-345** builds one adapter per provider for the
cross-adapter capability invariant tests (`test_every_adapter_declares_a_temperature_range` and
its siblings). Two of those fixtures pass a hardcoded model literal that plans 17-18 and 17-19 have
now retired, and **`lib.rs:326` passes the superseded Qwen base URL as a literal**, which plan
17-21 has now replaced in the constant. None of these tests asserts anything about the literals —
they assert capabilities — so substituting the constants is behaviour-preserving and removes three
live instances of exactly the drift this plan exists to close. The Qwen adapter's own tests already
reference `QWEN_DEFAULT_BASE_URL` and needed no change; `lib.rs` is the one place that did not.

**D-00d governs `.planning/` edits**: amend in place, dated, superseded text preserved rather than
deleted. COVERAGE.md's closing *"Verification status of this surface"* section is the section to
amend, and its existing paragraph is what gets superseded-in-place rather than replaced silently.

**`Cargo.toml`'s `description` and `keywords`** were already brought in line by plan 17-08 and name
no model identifiers. Read them to confirm, and record that they needed no change — success
criterion 5 names them explicitly, so "checked and unchanged" is a different statement from
"not looked at".
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Every operator-facing surface names a model and an endpoint that exist</name>
  <files>config.example.yml, docs/src/getting-started/configuration.md, crates/paladin-llm/README.md, crates/paladin-llm/src/lib.rs</files>
  <action>
Update the `kimi` and `grok` blocks in `config.example.yml` and in
`docs/src/getting-started/configuration.md` so each `default_model` matches the shipped constant
plans 17-18 and 17-19 set, and update the `qwen` block in both — and the Qwen row of
`crates/paladin-llm/README.md`'s provider table — so `base_url` matches `QWEN_DEFAULT_BASE_URL` as
plan 17-21 now sets it. Leave Ollama's, Gemini's and the three pre-existing providers' values
alone; none of them changed.

Replace the blanket unverified-endpoint caveat in all three documents with a per-vendor note
using the status table in `<interface_context>`. State, for each of the five providers this phase
added, whether its base URL and default model were confirmed against the live endpoint, on what
date, and what specifically was confirmed — a model list, a completion round trip, or neither.
Note Ollama as self-hosted with no vendor endpoint to verify, pointing at the Docker suite as its
live exercise.

Qwen's line is the one that changes character rather than just wording. It stops being a caveat
about verification and becomes a caveat about **region**, which is a G-17-4d deliverable rather
than housekeeping. Wherever `DASHSCOPE_BASE_URL` appears or the Qwen `base_url` is shown, say: API
keys are scoped to the Model Studio region that issued them and are rejected by every other
region's endpoint; the shipped default is US (Virginia); an operator whose workspace is in
Singapore or on the mainland must set `DASHSCOPE_BASE_URL` to their own endpoint. Name the three
known compatible-mode endpoints, taking them from the module rustdoc plan 17-21 wrote rather than
from memory. Say it in each of the three documents — an operator reading the config example should
not have to find the README to learn it.

State the **rule and the remedy only**. Do not describe what a mismatch looks like: the symptom is
about to change from a silent plausible model list into an emitted warning, and plan 17-22 owns
saying so once that behaviour exists. A symptom description written here would be stale within one
wave — the exact class of drift this plan is closing.

In `config.example.yml`, the Qwen, Grok and Gemini blocks each say "Same unverified-endpoint
caveat as Kimi above" — that back-reference stops being true once Kimi's caveat changes, so give
each block its own one-line status instead of a pointer to a note that no longer says what it
used to.

In `crates/paladin-llm/src/lib.rs`, replace the two retired model literals and the superseded Qwen
base-URL literal at `:326` in the cross-adapter fixtures with references to `GROK_DEFAULT_MODEL`,
`KIMI_DEFAULT_MODEL` and `QWEN_DEFAULT_BASE_URL`. Check the other fixtures in the same function
while you are there: any that repeats a value a constant already owns should reference the
constant. Do not change what any test asserts.

Read `crates/paladin-llm/Cargo.toml`'s `description` and `keywords` and confirm they still name
exactly the providers that exist. Record the verdict in the SUMMARY either way — success
criterion 5 names them, so an unchanged file still needs to have been looked at.
  </action>
  <verify>
    <automated>cargo test -p paladin-llm --features kimi,qwen,grok,ollama,openai-compatible,gemini && cargo test --test unit --features llm-all && cargo fmt --check && cargo clippy --workspace --all-targets --features llm-all -- -D warnings</automated>
    <human-check>Reading config.example.yml's provider section top to bottom, every default_model names a model the corresponding SUMMARY's live list contains, every base_url matches the shipped constant, each of the five new providers carries its own verification status rather than a back-reference, and the Qwen block states the region-scoping rule and the endpoint an operator outside Virginia must set.</human-check>
  </verify>
  <done>No retired model identifier and no superseded base URL remains in config.example.yml, the configuration guide, the crate README or lib.rs; each of the five new providers carries its own dated verification status; the DashScope region constraint and the known regional endpoints appear in all three operator-facing documents; the workspace builds, tests and lints clean.</done>
</task>

<task type="auto">
  <name>Task 2: COVERAGE.md records what the live run proved and what it did not</name>
  <files>.planning/phases/17-additional-llm-provider-adapters/COVERAGE.md</files>
  <action>
Amend COVERAGE.md's closing *"Verification status of this surface"* section in place, per D-00d —
date the amendment, keep the superseded paragraph visible rather than deleting it, and add what is
now true.

The superseded claim is the whole basis of that section: *"the base URLs, default model IDs and
live-fetch behaviour above are taken from vendor documentation and the shipped code, and have not
been confirmed against a live endpoint — the phase's sandbox has no network egress and no vendor
API keys."* Egress and four credentials now exist, and the live run both confirmed some of those
facts and **falsified others** — Grok's and Kimi's default model IDs were wrong, Qwen's base URL
named a region its credential could not reach, and the `[compat] sampling fields` row's "each is
omitted when unset" claim turned out to be an incomplete description of what a vendor may require,
since xAI rejects a parameter that is set and Moonshot accepts only one value of another.

Record, per surface: which `[compat]` and `[gemini]` INTEGRATE rows a live run has now exercised
end to end (chat completions, the model list, usage mapping), and which remain mock-transport-only
(streaming, error mapping, `reasoning_content`). All four hosted vendors have now been exercised
live, so no row remains blocked on a credential — say that, since the previous text said the
opposite.

Update the `[compat]` sampling-fields row's reason so it reflects the preset-declared parameter
contract plans 17-18, 17-19 and 17-21 added, rather than describing the pre-fix behaviour.

Also correct the stale identifiers in the `## Surfaces` prose near the top: the retired Kimi
default, the absent Grok default, and the superseded Qwen base URL at `COVERAGE.md:34`, each of
which is named when describing its preset.

Do not re-run any probe for this task; every fact comes from plans 17-18's, 17-19's and 17-21's
SUMMARYs.
  </action>
  <verify>
    <automated>test -f .planning/phases/17-additional-llm-provider-adapters/COVERAGE.md && grep -c "Verification status" .planning/phases/17-additional-llm-provider-adapters/COVERAGE.md</automated>
    <human-check>The verification-status section distinguishes live-exercised rows from mock-transport-only rows from credential-blocked rows, carries the amendment date, and preserves the superseded claim rather than deleting it.</human-check>
  </verify>
  <done>COVERAGE.md's verification status reflects the live run: what was confirmed, what was falsified, what remains unexercised, and why — with the superseded text preserved and the amendment dated.</done>
</task>

<task type="auto">
### Task 3: Declare the full LLM environment-variable surface in `.env.example`

**SCOPE AMENDMENT — added by the orchestrator on 2026-08-22 at the developer's explicit
instruction during `/gsd-execute-phase 17 --gaps-only`.** It was not in this plan as
originally written. It belongs here because this is the plan that owns the operator-facing
configuration surface, and `.env.example` is part of that surface.

**The measured gap.** `.env.example` declares 5 of the 46 environment variables the adapters
actually read. Enumerate the real surface FIRST — do not work from this list, re-derive it:

```bash
grep -rhoE 'env::var\("[A-Z_]+"\)' crates/paladin-llm/src | sed 's/.*("//; s/")//' | sort -u
```

At the time of writing that yields 46 names across nine providers. Three credential names are
absent from `.env.example` entirely — `MOONSHOT_API_KEY`, `DASHSCOPE_API_KEY`,
`OPENAI_COMPATIBLE_API_KEY` — as is every `*_BASE_URL`, every `*_MODEL`, every
`*_TIMEOUT_SECONDS`, and the whole `OPENAI_COMPATIBLE_*` capability/temperature block.

**What to write.** Group by provider. For each, declare its key, base URL, model and timeout
with the shipped constant's value as the commented default. Take every default from the
constant, never from this plan or from a SUMMARY. Leave credential values EMPTY — `.env.example`
is committed.

**On temperature.** Only `openai_compatible` reads temperature from the environment
(`OPENAI_COMPATIBLE_TEMPERATURE_MIN` / `_MAX`). Declare those. Do NOT invent a per-vendor
temperature variable for the other eight providers — no adapter reads one, and a knob that
does nothing is worse than an absent knob. Kimi's fixed-temperature constraint is preset data
(plan 17-19), not an environment variable.

**On DashScope.** `DASHSCOPE_BASE_URL` carries the region-scoped-credential constraint from
plan 17-21. State it here too, with the known regional endpoints — this file is where an
operator hitting a region mismatch will look first.

**Credential provenance.** Note in the file's LLM section that in the devcontainer these keys
arrive from `~/.config/paladin/` via `.devcontainer/paladin-env.sh` (one file per secret,
filename = lowercased variable name), that a genuinely-exported non-empty value wins over the
file, and that a NON-INTERACTIVE shell must source `paladin-env.sh` explicitly because
`.bashrc` skips it. Verified 2026-08-22.

**Then mirror it into the guide.** `docs/src/getting-started/configuration.md` gains an
environment-variable section presenting this as a first-class configuration path beside the
YAML one, including the credential-provenance note.

**Verify:** every name you declare appears in the `grep` output above, and every name in that
output appears in `.env.example`. Report both directions in the SUMMARY.
</task>

</tasks>

<executor_notes>
## Operational facts for this repository — non-negotiable

1. **Commit with `git commit --no-verify`.** `workflow.worktree_skip_hooks` is `true` in
   `.planning/config.json` (D-00o).
2. **This plan runs no live probe and needs no credential.** Every vendor fact it records comes
   from plans 17-18's and 17-19's SUMMARYs. If a value there is missing or ambiguous, stop and say
   so rather than probing to fill the gap — a second measurement that disagreed with the first
   would create a discrepancy this plan cannot adjudicate.
3. **`docs/src/` is the mdBook source.** Editing the markdown is sufficient; do not rebuild the
   book as part of this plan.
4. **`cargo clippy` runs `--all-targets`**, which compiles the example and the test fixtures the
   lib.rs change touches.
5. **Never run `make coverage`, `make test-all`, or any Docker-dependent target** as part of this
   plan.
</executor_notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped documentation → operator's configuration | An operator copies `config.example.yml` and trusts it. A value that does not work turns the project's own example into the first bug they debug. |
| verification record → future reader | COVERAGE.md and the README caveats are what a later phase reads to decide whether a fact needs re-checking. A claim of verification that did not happen is worse than no claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-82 | Repudiation | the per-vendor verification claims in README, config example, docs and COVERAGE.md | high | mitigate | The tempting simplification is to replace one blanket "unverified" with one blanket "verified" — which would assert live confirmation for Ollama, which has no vendor endpoint at all, and would quietly drop Qwen's **new** caveat, which is about region rather than verification and is a G-17-4d deliverable. Mitigated by a per-vendor status table supplied in `<interface_context>`, by a prohibition against deleting a caveat that is still true, by a prohibition against still recording Qwen as unverified, and by an acceptance criterion that each of the five providers carries its own status rather than a back-reference. |
| T-17-90 | Spoofing | the DashScope region guidance in operator-facing documents | high | mitigate | **New with this plan.** Naming regional endpoints in three documents makes those documents the source an operator pastes a host from. A wrong or invented host is where an operator's credential goes. Mitigated by requiring the endpoint list to be taken from the module rustdoc plan 17-21 wrote from Alibaba's own Base URL documentation rather than typed from memory, by naming only vendor-documented endpoints, and by the human-check reading the Qwen block against the shipped constant. |
| T-17-83 | Tampering | `crates/paladin-llm/src/lib.rs` cross-adapter fixtures | medium | mitigate | The fixture edit is inside a test module and could quietly change what a test exercises. Mitigated by restricting the change to substituting a constant for the literal it duplicates, asserting nothing new, and gating on the full crate suite plus `--test unit --features llm-all`. |
| T-17-84 | Spoofing | `config.example.yml` as an operator's starting point | medium | mitigate | An example naming a retired model produces a vendor `resource_not_found_error` that reads to a new operator as "this framework does not work". Mitigated by taking every value from the shipped constant rather than retyping it, and by a human-check criterion that reads the provider section top to bottom against the SUMMARYs' live lists. |
| T-17-SC-20 | Tampering | npm/pip/cargo installs | high | mitigate | **No package-manager install task exists in this plan.** No dependency changes; the only Rust edit is a test fixture. `make deny` / `make audit` verdicts are provably unchanged; no `[ASSUMED]`/`[SUS]` package legitimacy checkpoint is required. |

ASVS L1, `security_block_on: high`. T-17-82, T-17-90 and T-17-SC-20 are `high`; each carries a
named mitigation with a named check or a named absence. Nothing blocks.
</threat_model>

<deferred>
## Deferred and explicitly out of scope

- **The engine-side half of G-17-4d** — making a masked auth failure audible in
  `available_models()` — is plan 17-22, which runs after this one. If it adds an operator-facing
  troubleshooting line, it adds it itself; do not pre-announce behaviour that does not exist yet.
- **Automating the currency check** — a test asserting that `config.example.yml`'s `default_model`
  and `base_url` values equal the crate's `*_DEFAULT_MODEL` / `*_DEFAULT_BASE_URL` constants would
  prevent this class of drift mechanically, and this run has now produced three instances of it
  (two model identifiers and one base URL). Genuinely attractive, and out of scope for a
  gap-closure run whose mandate is the UAT gaps. **Trigger:** the next time a vendor retires a
  default model or this project changes a shipped endpoint.
- **Rebuilding or republishing the mdBook** — a release concern, not this plan's.
- **`docs/src` prose beyond the provider configuration section** — untouched; nothing else in the
  guide names a model identifier.
</deferred>

<verification>
- `cargo test -p paladin-llm --features kimi,qwen,grok,ollama,openai-compatible,gemini` — green.
- `cargo test --test unit --features llm-all` — green.
- `cargo fmt --check` and `cargo clippy --workspace --all-targets --features llm-all -- -D warnings` — clean.
- A read-through of `config.example.yml`, `docs/src/getting-started/configuration.md` and
  `crates/paladin-llm/README.md` confirms every model identifier and every base URL matches the
  shipped constant, every one of the five new providers carries its own dated verification status,
  and each document states the DashScope region-scoping rule alongside `DASHSCOPE_BASE_URL`.
- COVERAGE.md's verification-status section is amended in place and dated, with the superseded
  claim preserved.
</verification>

<success_criteria>
1. No retired model identifier and no superseded base URL remains anywhere in the shipped or
   documented surface, `lib.rs:326` included.
2. Each of the five providers this phase added carries its own verification status, dated, saying
   what was confirmed and what was not — Qwen among the verified.
3. The DashScope region-scoped-credential constraint and the three known regional endpoints appear
   in all three operator-facing documents, closing G-17-4d's first `missing` item on the
   operator-facing side.
4. Ollama's caveat survives, because it is still true for its own reason.
5. `lib.rs`'s fixtures reference the constants rather than repeating their values.
6. COVERAGE.md records what the live run confirmed, what it falsified, and what remains
   unexercised.
</success_criteria>

<output>
Create `.planning/phases/17-additional-llm-provider-adapters/17-20-SUMMARY.md` when done.
</output>
