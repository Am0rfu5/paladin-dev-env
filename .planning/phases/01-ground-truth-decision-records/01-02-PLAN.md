---
phase: 01-ground-truth-decision-records
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .planning/decisions/0001-battalion-config.md
  - .planning/decisions/0002-battalion-result.md
autonomous: true
requirements: [RECON-02, RECON-03]

must_haves:
  truths:
    - "`BattalionConfig` has exactly one recorded authoritative definition — `battalion/mod.rs:37` — and the `citadel.rs:280` duplicate is recorded as a different concept with a named replacement identifier (RECON-02, D-12)."
    - "`BattalionResult` has exactly one recorded authoritative definition and the ADR names which field each competing position contributed and which shipped field superseded it (RECON-03, D-13)."
    - "Both ADRs parse under `node .claude/gsd-core/bin/lib/adr-parser.cjs --input <file>` with `status` = `accepted` and non-empty `decisions`, `options_considered` and `key_files`."
    - "`0001-battalion-config.md` carries `Code conformance: must change` and names GAP-07 as the requirement that executes the rename; `0002-battalion-result.md` carries `conforms` (D-03, D-11)."
    - "The ADR names both `BattalionConfig` definitions (`battalion/mod.rs:37` and `citadel.rs:280`) as distinct concepts and states the renamed identifier for the citadel one — it never collapses them into a single entry."
    - "The `BattalionConfig` ADR's `## Considered Options` lists every rejected REQ-* variant from REQUIREMENTS.md variant group 3; where a variant was never built (`REQ-commander-config-metadata-dir-v3`), the entry says so explicitly rather than being omitted."
    - "Every identifier in the `BattalionConfig` ADR (`BattalionConfig`, the chosen rename, `max_concurrency`, `timeout_seconds`, `continue_on_error`) is written exactly as it appears in the Rust source — same case, same underscores — so a grep of the source for the ADR's identifier string matches."
    - "`## Code Locations` bullets in the `BattalionConfig` ADR appear in source-file order (`battalion/mod.rs`, then `citadel.rs`, then `file_citadel.rs`) and each is a bullet, never a prose paragraph."
    - "The `BattalionResult` ADR states which field each of the three source positions contributed and which shipped field superseded it — `per_paladin_times` over `execution_time_ms`, `node_errors: Vec<NodeError>` over `errors: Vec<PaladinError>` — rather than asserting the superset satisfies all three without naming the substitutions."
    - "Where a source position contributed no field the shipped struct kept, the `BattalionResult` ADR records that explicitly instead of omitting the position."
    - "The `BattalionResult` ADR's `## Considered Options` lists `REQ-battalion-result-v1`, `-v2`, `REQ-herald-battalion-result-fields` and `REQ-battalion-metadata-extension` in REQUIREMENTS.md variant-group-4 order."
  prohibitions:
    - statement: "MUST NOT mark an ADR `Code conformance: conforms` where the cited code does not in fact match the chosen variant — a mismatch is recorded as `must change` with its executing requirement named, never smoothed into conformance."
      status: resolved
      verification: judgment
    - statement: "MUST NOT choose a variant for authoring convenience and back-fill a citation that does not support it — the chosen variant is whatever the re-read source shows, and a disagreement between the tree and the intended verdict is written down, not resolved silently."
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/decisions/0001-battalion-config.md"
      provides: "RECON-02 decision record — the authoritative BattalionConfig and the citadel.rs duplicate's disposition"
      contains: "## Code Conformance"
    - path: ".planning/decisions/0002-battalion-result.md"
      provides: "RECON-03 decision record — the merged-superset BattalionResult and what the superset chose"
      contains: "## Code Conformance"
  key_links:
    - from: ".planning/decisions/0001-battalion-config.md"
      to: "crates/paladin-core/src/platform/container/citadel.rs"
      via: "## Code Locations bullet citing the placeholder duplicate and the persistence site"
      pattern: "citadel\\.rs:2(33|80)"
    - from: ".planning/decisions/0002-battalion-result.md"
      to: "crates/paladin-core/src/platform/container/battalion/mod.rs"
      via: "## Code Locations bullet citing the merged-superset struct"
      pattern: "mod\\.rs:549"
---

<objective>
Author the two Battalion-type decision records: RECON-02 (`BattalionConfig`) and RECON-03
(`BattalionResult`).

Purpose: these are the two contested definitions where shipped code has the most to say. Run-3
verification already settled the variant choice for both; what is unrecorded is the *verdict* — and,
for `BattalionConfig`, the disposition of the `citadel.rs:280` duplicate that Epic 4 superseded and
nobody removed. RECON-03 is pure recording; `intel/code-verification.md` explicitly instructs that
no reconciliation task be planned for it.

Output: `.planning/decisions/0001-battalion-config.md`, `.planning/decisions/0002-battalion-result.md`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ground-truth-decision-records/01-CONTEXT.md
@.planning/phases/01-ground-truth-decision-records/01-RESEARCH.md
@.planning/phases/01-ground-truth-decision-records/01-PATTERNS.md
@.planning/phases/01-ground-truth-decision-records/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols, files and vocabulary created by Phase 1 (none are pre-existing; the drift-verification pass
must not flag them as unacknowledged):

- **New directories:** `.planning/decisions/`, `.planning/ledgers/`
- **New files:** `.planning/decisions/PROMOTION.md`; `.planning/decisions/0001-battalion-config.md`
  … `0006-coverage-gate.md`; `.planning/ledgers/milestone-01.md`
- **New ADR headings (document vocabulary):** `## Code Conformance`, `## Downstream Consumers`
- **New field values:** `conforms`, `must change` (the `Code conformance` field, D-03)
- **New ledger verdict classes:** `satisfied`, `present, unproven`, `genuinely outstanding`,
  `deferred with reason`, `superseded by shipped code` (D-20)
- **New naming scheme:** flat zero-padded monotonic ADR counter `NNNN-kebab-slug.md`
- **Rust identifiers recorded (NOT implemented here — Phase 2 GAP-07 implements them):**
  `BattalionCheckpointConfig` (the proposed rename of the `citadel.rs:280` placeholder), and the
  new temperature-range field on `ProviderCapabilities`

<tasks>

<task type="auto">
  <name>Task 1: ADR-0001 — the authoritative BattalionConfig and the citadel.rs duplicate</name>
  <read_first>
    - `.planning/decisions/PROMOTION.md` — the heading set, bullet-list rule and numbering index established by plan 01-01
    - `.planning/decisions/0005-herald-trait.md` — the shape to match, produced by plan 01-01
    - `crates/paladin-core/src/platform/container/battalion/mod.rs` lines 30-60 — the authoritative `BattalionConfig` field set
    - `crates/paladin-core/src/platform/container/citadel.rs` lines 225-295 — the placeholder duplicate at :280 (read its doc comment, not just the signature) and `BattalionState.config` at :233
    - `crates/paladin-memory/src/citadel/file_citadel.rs` lines 495-555 — the other consumers of the persisted shape
    - `.planning/REQUIREMENTS.md` §"Group 3 — `BattalionConfig` field set" (line 1685) — the competing REQ-* variants and the run-3 code-verification note
    - `.planning/intel/code-verification.md` — the run-3 verification this ADR records
  </read_first>
  <files>.planning/decisions/0001-battalion-config.md</files>
  <action>
Author `# ADR-0001: BattalionConfig field set` using the heading set from `PROMOTION.md`.

`## Status` body is the bare word `Accepted`, followed by a `**Date:**` prose line.

`## Context` states the question: two distinct `BattalionConfig` structs ship, and three documented
positions compete for the field set. Record the run-3 finding that settles the variant choice —
`battalion/mod.rs:37` is the Epic 4 field set exactly, and `CommanderConfig` does not exist anywhere
under `crates/` or `src/`, so the third position was never built. Confirm both halves by grep before
writing them.

`## Decision` is a bullet list carrying D-12 in full:
- `crates/paladin-core/src/platform/container/battalion/mod.rs:37` is the one authoritative
  `BattalionConfig`.
- The struct at `crates/paladin-core/src/platform/container/citadel.rs:280` is not a competing
  definition — it is a self-described placeholder whose own doc comment names Epic 4 as the thing
  that would replace it, and Epic 4 did, elsewhere. Quote that doc comment.
- It is a different concept — checkpoint/resume knobs, not orchestration config — so it is
  **renamed** rather than deleted or merged. Record `BattalionCheckpointConfig` as the chosen
  identifier and state that CONTEXT.md left the exact name to the planner's discretion, so this ADR
  is where it is fixed.
- The rename **keeps all three fields — `max_concurrency`, `timeout_seconds`, `continue_on_error` —
  and their serde shape unchanged**. Write each field name exactly as `citadel.rs` declares it.
- **No persisted-schema change and no migration.** State the reason concretely: replacing it with
  the real `BattalionConfig` would change `BattalionState`'s serialized form (`schema_version` is
  `"1.0.0"` at `citadel.rs:233`, consumed by `crates/paladin-memory/src/citadel/file_citadel.rs`)
  and would require a version bump plus a read path for existing checkpoints.

`## Considered Options` is one bullet per rejected variant, each naming its REQ-* ID from
REQUIREMENTS.md variant group 3 and a one-sentence reason it lost. Include
`REQ-commander-config-metadata-dir-v3` with the explicit statement that it was never built — do not
omit a variant merely because no code corresponds to it; its absence is the finding.

`## Code Locations` is a bulleted `file:line` list in source-file order: `battalion/mod.rs:37`, then
`citadel.rs:233` and `citadel.rs:280`, then `file_citadel.rs:507` and `:541`. Re-read each line
before citing it; RESEARCH.md's citations are dated 2026-07-30 and line numbers drift.

`## Code Conformance` body is the bare phrase `must change`, followed by a prose line naming
**GAP-07** in Phase 2 as the requirement that lands the rename, and stating that nothing in this
phase edits Rust source.

`## Downstream Consumers` names Phase 2 GAP-07 and
`crates/paladin-memory/src/citadel/file_citadel.rs` as the consumer whose serialized shape
constrains the decision.

Use Medieval-military ubiquitous language (Battalion, Citadel, Paladin) throughout.
  </action>
  <acceptance_criteria>
    - `node .claude/gsd-core/bin/lib/adr-parser.cjs --input .planning/decisions/0001-battalion-config.md` exits 0 with `"status": "accepted"` and non-empty `decisions`, `options_considered` and `key_files`
    - The ADR contains the literal strings `max_concurrency`, `timeout_seconds`, `continue_on_error`, `BattalionCheckpointConfig`, `GAP-07` and `must change`
    - The ADR contains `REQ-commander-config-metadata-dir-v3` and states that it was never built
    - The ADR cites both `battalion/mod.rs:37` and `citadel.rs:280`
    - `grep -q "pub struct BattalionConfig" crates/paladin-core/src/platform/container/battalion/mod.rs` succeeds and `grep -rq "CommanderConfig" crates/ src/` fails — the two facts the Context section asserts still hold
    - The three field names appear in `crates/paladin-core/src/platform/container/citadel.rs` exactly as spelled in the ADR
  </acceptance_criteria>
  <verify>
    <automated>node .claude/gsd-core/bin/lib/adr-parser.cjs --input .planning/decisions/0001-battalion-config.md | grep -q '"status": "accepted"' && grep -q 'BattalionCheckpointConfig' .planning/decisions/0001-battalion-config.md && grep -q 'GAP-07' .planning/decisions/0001-battalion-config.md && grep -q 'REQ-commander-config-metadata-dir-v3' .planning/decisions/0001-battalion-config.md && grep -q 'citadel.rs:280' .planning/decisions/0001-battalion-config.md && grep -q 'pub struct BattalionConfig' crates/paladin-core/src/platform/container/battalion/mod.rs && for f in max_concurrency timeout_seconds continue_on_error; do grep -q "$f" crates/paladin-core/src/platform/container/citadel.rs || exit 1; done && echo ADR0001_OK</automated>
  </verify>
  <done>One `BattalionConfig` is named authoritative with a re-verified citation, the duplicate has a recorded new identifier and an unchanged serde shape, and the ADR is flagged `must change` with GAP-07 named as its executor.</done>
</task>

<task type="auto">
  <name>Task 2: ADR-0002 — the merged-superset BattalionResult</name>
  <read_first>
    - `.planning/decisions/PROMOTION.md` — the heading set and bullet-list rule
    - `crates/paladin-core/src/platform/container/battalion/mod.rs` lines 540-600 — the shipped `BattalionResult` struct, read field by field
    - `crates/paladin-core/src/platform/container/battalion/mod.rs` lines 490-520 — the `TokenUsage` struct the result aggregates
    - `.planning/REQUIREMENTS.md` §"Group 4 — `BattalionResult` / `BattalionMetadata`" (line 1714) — the competing variants, including the run-2 `REQ-battalion-metadata-extension` position and the explicit *do not plan a reconciliation task* instruction
    - `.project/Milestone_5-Workspace-Decomposition/Epic_1/decisions/battalion-result-upward-dependency-decision.md` — the corpus's only prior decision document; read it so this ADR does not contradict it, and note that despite its filename it never mentions `BattalionResult`
    - `crates/paladin-core/src/platform/container/herald.rs` lines 49-153 — the consumer whose expectation the superset must satisfy
  </read_first>
  <files>.planning/decisions/0002-battalion-result.md</files>
  <action>
Author `# ADR-0002: BattalionResult field set` using the heading set from `PROMOTION.md`.

`## Status` body is the bare word `Accepted`, followed by a `**Date:**` prose line.

`## Context` states the question: three positions competed for the `BattalionResult` shape — two
producer variants and one Herald-consumer expectation that neither producer variant supplied. Note
that run-3 verification found the shipped struct to be a merged **superset** of all three, which is
why this is a recording task and not a reconciliation task, and cite
`intel/code-verification.md`'s standing instruction not to plan the latter.

`## Decision` is a bullet list carrying D-13 in full, and it must name the substitutions rather than
asserting satisfaction in the abstract:
- `crates/paladin-core/src/platform/container/battalion/mod.rs:549` is authoritative.
- The superset chose `per_paladin_times` where Epic 5's position specified `execution_time_ms` —
  state which position lost that field and why the shipped name replaced it.
- The superset chose `node_errors: Vec<NodeError>` where the other position specified
  `errors: Vec<PaladinError>`. State the concrete reason: `BattalionError` does not derive
  `Serialize`/`Deserialize` while `BattalionResult` does. Verify that derive claim against the tree
  before writing it.
- Epic 8's Herald expectation is satisfied: the Battalion type is carried as `strategy_used`, and
  aggregated token usage as `total_tokens` plus `per_paladin_tokens`. Quote each field name exactly
  as the struct declares it.
- Add one bullet per source position that contributed no surviving field, stating that explicitly
  rather than omitting the position.
- Add one bullet recording that this ADR does not decide *where* `PaladinResult`, `StopReason`,
  `TokenUsage`, `RegistryError` and `HandoffError` live — that is the subject of the Milestone 5
  Epic 1 decision document, whose promotion is owned by Phase 7 — so the two records do not
  contradict each other.

`## Considered Options` is one bullet per rejected variant, in REQUIREMENTS.md variant-group-4
order, naming `REQ-battalion-result-v1`, `REQ-battalion-result-v2`,
`REQ-herald-battalion-result-fields` and the run-2 `REQ-battalion-metadata-extension`, each with a
one-sentence reason.

`## Code Locations` is a bulleted `file:line` list: `battalion/mod.rs:549` for the struct,
`battalion/mod.rs:497` for the aggregated `TokenUsage`, and `herald.rs:49` for the consumer. Re-read
each before citing.

`## Code Conformance` body is the bare word `conforms`, followed by a prose line stating that
Phase 2 GAP-07 has no action for this decision.

`## Downstream Consumers` names Phase 2 GAP-07 (no action), Phase 7's ARCH-03(c), and the four
Battalion producers (Formation, Phalanx, Campaign, Chain of Command) plus the Herald consumer.
  </action>
  <acceptance_criteria>
    - `node .claude/gsd-core/bin/lib/adr-parser.cjs --input .planning/decisions/0002-battalion-result.md` exits 0 with `"status": "accepted"` and non-empty `decisions`, `options_considered` and `key_files`
    - The ADR contains the literal strings `per_paladin_times`, `execution_time_ms`, `node_errors`, `strategy_used`, `total_tokens`, `per_paladin_tokens` and `conforms`
    - The ADR contains all four of `REQ-battalion-result-v1`, `REQ-battalion-result-v2`, `REQ-herald-battalion-result-fields`, `REQ-battalion-metadata-extension`
    - The ADR cites `battalion/mod.rs:549`
    - Each of `per_paladin_times`, `node_errors`, `strategy_used` and `total_tokens` is present in `crates/paladin-core/src/platform/container/battalion/mod.rs` — the ADR's field names match the shipped struct
    - The ADR states that it does not decide type ownership, and names Phase 7 as the owner of that question
  </acceptance_criteria>
  <verify>
    <automated>node .claude/gsd-core/bin/lib/adr-parser.cjs --input .planning/decisions/0002-battalion-result.md | grep -q '"status": "accepted"' && for s in per_paladin_times execution_time_ms node_errors strategy_used total_tokens per_paladin_tokens REQ-battalion-metadata-extension; do grep -q "$s" .planning/decisions/0002-battalion-result.md || exit 1; done && for f in per_paladin_times node_errors strategy_used total_tokens; do grep -q "$f" crates/paladin-core/src/platform/container/battalion/mod.rs || exit 1; done && echo ADR0002_OK</automated>
  </verify>
  <done>`BattalionResult` has one recorded authoritative definition that names every field substitution the superset made and the position each substitution displaced, and the ADR is consistent with the Milestone 5 Epic 1 decision document rather than contradicting it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingested `.project/` PRD prose → ADR verdict | competing PRD positions are quoted into a record that now outranks shipped code |
| ADR → shipped code | `Code conformance: must change` on ADR-0001 becomes a Phase 2 GAP-07 code change against a persisted schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Tampering | `## Code Locations` citations in ADR-0001 / ADR-0002 | high | mitigate | Every citation re-read against the tree at authoring time; the verify commands independently re-grep `battalion/mod.rs` and `citadel.rs` for each identifier the ADR quotes, so a drifted or invented field name fails the gate. |
| T-01-05 | Tampering | persisted `BattalionState` schema (`schema_version` `"1.0.0"`) | high | mitigate | D-12's no-migration constraint is written into the ADR as a first-class decision bullet with its reason (`file_citadel.rs` consumes the serialized shape), so GAP-07 cannot execute the rename as a field-set replacement. |
| T-01-01 | Information Disclosure | quoting ingested `.project/` documents | high | mitigate | Cite `file:line`; do not paste source-document bodies. STATE.md records a plaintext API key inside one ingested Milestone 3 document; no Phase 1 artifact quotes that file. |
| T-01-06 | Repudiation | a verdict recorded without its rejected alternatives | medium | mitigate | `## Considered Options` must list every REQ-* variant in its group, including ones with no corresponding code (`REQ-commander-config-metadata-dir-v3`), asserted by the verify command. |
| T-01-SC | Tampering | npm/cargo installs | high | mitigate | This plan installs nothing. RESEARCH.md §Package Legitimacy Audit records no `[ASSUMED]`/`[SUS]` packages, so no blocking legitimacy checkpoint is required. |
</threat_model>

<verification>
- Both ADRs parse via `adr-parser.cjs` with `status` = `accepted` and non-empty `decisions`, `options_considered`, `key_files`
- Every field name quoted in either ADR is present in the shipped struct it cites
- ADR-0001 is `must change` with GAP-07 named; ADR-0002 is `conforms`
- `CommanderConfig` still returns no match anywhere under `crates/` or `src/`
</verification>

<success_criteria>
- RECON-02 and RECON-03 each have exactly one recorded, evidence-cited answer
- The `citadel.rs:280` duplicate has a named replacement identifier, an unchanged serde shape, and a named executing requirement
- No Rust source file is modified by this plan
</success_criteria>

<output>
Create `.planning/phases/01-ground-truth-decision-records/01-02-SUMMARY.md` when done
</output>
