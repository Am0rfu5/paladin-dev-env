---
phase: 01-ground-truth-decision-records
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .planning/ledgers/milestone-01.md
autonomous: true
requirements: [RECON-08, RECON-01]

must_haves:
  truths:
    - "The Epic 10 Task 7.0 dispute is answered with one of exactly two verdicts — the Final Documentation Review is outstanding work with a named owner, or the validation report is recorded as wrong — and the search that produced the verdict is recorded (RECON-08)."
    - "The 102-vs-103 subtask discrepancy is explained in the ledger, with both totals quoted against their source documents (RECON-08)."
    - "All three known divergences are present in the ledger as `superseded by shipped code` rows — MCP Streamable-HTTP in place of SSE, Qdrant/Sanctum in place of `sqlite-vss`, and the interactive REPL against Epic 9 non-goal NG-7 (D-21, RECON-01)."
    - "The code-observed Battalion base module path is recorded as `battalion/mod.rs`, resolving the contradiction between the Epic 4 document and the project completion plan (RECON-01)."
    - "The ingest requirement-count discrepancy is recorded with every figure stated verbatim against its source, and the arithmetic difference explained (RECON-01)."
    - "When an outstanding task item and its owning REQ-* row would both assert a verdict, the ledger records the REQ-* row's verdict and the nested item's verdict separately — never merges them into one."
    - "A REQ-* row with zero nested outstanding task items renders with no nested block at all — not an empty bullet list and not a placeholder."
    - "Ledger rows are ordered by epic, then by REQ-* ID in the order they appear in REQUIREMENTS.md's existing Milestone 1 ledger; ties keep source order."
    - "The two reported Milestone-1 requirement counts are both restated verbatim in the ledger alongside the actual enumerated row count, and the arithmetic difference is explained rather than silently reduced to one figure."
    - "The ledger states both Epic 10 subtask totals (102 and 103) verbatim with their source documents and explains the difference rather than picking one."
    - "Where the Epic 10 task list and the validation report make the same claim about the same parent task, the ledger records both claims side by side with their file citations before stating which the tree corroborates."
    - "If no Final Documentation Review artifact is found anywhere under `.project/Milestone_1-MVP/Epic_10/` or `docs/`, the ledger records the search commands run and the empty result as the evidence, rather than leaving the verdict open."
    - "The RECON-08 entry states the two documents in a fixed order — task list first, validation report second — matching `INGEST-CONFLICTS.md:125-127`."
    - "The RECON-08 verdict is one of exactly two named outcomes, with no third hedged option."
    - statement: "The ledger is authored across three plans in three waves against the same file; after each plan the file is still internally consistent — every section heading it declares either holds rows or states that a later plan fills it, and no verdict cell is left blank."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT assert a `satisfied` verdict from document text or memory alone — every `satisfied` verdict comes from a citation re-checked against the shipped tree at authoring time, and a claim that cannot be re-checked is recorded as `present, unproven` rather than upgraded."
      status: resolved
      verification: judgment
    - statement: "MUST NOT soften, normalize or bury the interactive-REPL divergence — a documented non-goal (Epic 9 NG-7) that shipped anyway is flagged as such, not filed as an ordinary superseded row."
      status: resolved
      verification: judgment
    - statement: "MUST NOT downgrade genuinely outstanding work to `deferred with reason` in the absence of a document that actually defers it — a verdict of deferral requires a citation to the deferral, not an inference from the work not having been done."
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/ledgers/milestone-01.md"
      provides: "Divergence rows, the RECON-08 Epic 10 resolution, and the RECON-01 ingest-bookkeeping corrections"
      contains: "Final Documentation Review"
  key_links:
    - from: ".planning/ledgers/milestone-01.md"
      to: ".planning/INGEST-CONFLICTS.md"
      via: "the RECON-08 entry cites the warning that recorded the conflict"
      pattern: "INGEST-CONFLICTS\\.md:12[0-9]"
    - from: ".planning/ledgers/milestone-01.md"
      to: ".project/Milestone_1-MVP/Epic_10/"
      via: "both disputed Epic 10 documents are cited by path with their quoted claims"
      pattern: "task6\\.0-validation-report\\.md"
---

<objective>
Answer the two questions the ledger exists to settle before any per-epic row is written: the Epic 10
Task 7.0 dispute (RECON-08), and the places where shipped code has already superseded an ingested
requirement (RECON-01, D-21). Also correct the two pieces of ingest bookkeeping RECON-01 names.

Purpose: RECON-08 is the one conflict mechanical precedence cannot resolve — both documents are
DOC-precedence and they disagree about whether a six-subtask task exists at all. The three
divergences matter for the opposite reason: without them recorded, a later phase reads shipped
Streamable-HTTP, shipped Sanctum, and a shipped REPL as defects against the Milestone-1 PRD and
plans work to "fix" things that are correct.

Output: the divergence section, the RECON-08 resolution, and the bookkeeping corrections inside
`.planning/ledgers/milestone-01.md`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ground-truth-decision-records/01-CONTEXT.md
@.planning/phases/01-ground-truth-decision-records/01-RESEARCH.md
@.planning/phases/01-ground-truth-decision-records/01-PATTERNS.md
@.planning/phases/01-ground-truth-decision-records/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols, files and vocabulary created by Phase 1 (none are pre-existing; the drift-verification pass
must not flag them as unacknowledged):

- **New directories:** `.planning/decisions/`, `.planning/ledgers/`
- **New files:** `.planning/decisions/PROMOTION.md`; `.planning/decisions/0001-battalion-config.md`
  … `0006-coverage-gate.md`; `.planning/ledgers/milestone-01.md`;
  `.planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md`
- **New ADR headings (document vocabulary):** `## Code Conformance`, `## Downstream Consumers`
- **New field values:** `conforms`, `must change` (the `Code conformance` field, D-03)
- **New ledger verdict classes:** `satisfied`, `present, unproven`, `genuinely outstanding`,
  `deferred with reason`, `superseded by shipped code` (D-20)
- **New ledger section headings introduced by this plan:** `## Divergences — shipped code superseded
  an ingested requirement`, `## Epic 10 Task 7.0 — dispute resolution`, `## Ingest bookkeeping
  corrections`
- **New naming scheme:** flat zero-padded monotonic ADR counter `NNNN-kebab-slug.md`
- **Rust identifiers recorded elsewhere in this phase (NOT implemented — Phase 2 GAP-07 implements
  them):** `BattalionCheckpointConfig`, and the new temperature-range field on `ProviderCapabilities`

<tasks>

<task type="auto">
  <name>Task 1: Resolve the Epic 10 Task 7.0 dispute and the 102-vs-103 discrepancy</name>
  <read_first>
    - `.planning/ledgers/milestone-01.md` — the header, evidence bar and legend written by plan 01-01, so the new sections match its shape and verdict vocabulary
    - `.planning/INGEST-CONFLICTS.md` lines 118-135 — the warning that recorded this conflict, quoted verbatim as the anchor
    - `.project/Milestone_1-MVP/Epic_10/tasks-epic10-validation-documentation.md` — the task list; confirm its own item count and confirm whether a Task 7.0 heading exists anywhere in it
    - `.project/Milestone_1-MVP/Epic_10/task6.0-validation-report.md` — the validation report; read the passage claiming "101 of 102 subtasks" and "Only Task 7.0 (Final Documentation Review) remains", and read what it says the six subtasks are
    - `.planning/phases/01-ground-truth-decision-records/01-RESEARCH.md` §Common Pitfalls Pitfall 3 and §Open Questions item 2 — the search this task must complete
    - `.planning/intel/task-completion-state.md` — the deterministic per-file checkbox measurement, for Epic 10's own count
  </read_first>
  <files>.planning/ledgers/milestone-01.md</files>
  <action>
Do the search RESEARCH.md flagged as not yet exhaustive, then write the verdict.

**Search first, verdict second.** List the full contents of `.project/Milestone_1-MVP/Epic_10/`.
Grep that directory, the rest of `.project/Milestone_1-MVP/`, and the `docs/` tree for the literal
phrase "Final Documentation Review" and for the looser terms a renamed artifact would carry — a
documentation review, a documentation sign-off, a final review checklist. Also grep for the six
subtask descriptions the validation report attributes to Task 7.0, in case the work exists under a
different heading. Record every command you run and its result, because the absence of a result is
the evidence here, and an unrecorded search is not evidence.

**Then write a `## Epic 10 Task 7.0 — dispute resolution` section** into
`.planning/ledgers/milestone-01.md`, in this fixed order, matching `INGEST-CONFLICTS.md`:
1. The **task list's** claim, quoted with its file path: all of its checklist items complete across
   parent tasks 0.0 through 6.0, and no Task 7.0 present. State its own item total as a number.
2. The **validation report's** claim, quoted with its file path: "101 of 102 subtasks (99%)" and
   "Only Task 7.0 (Final Documentation Review) remains", naming a six-subtask Task 7.0.
3. Where the two make the same claim about the same parent task, put the two claims side by side
   with their citations before stating which one the tree corroborates. Do not summarise them into
   a single sentence.
4. The **search record**: the commands run and their results.
5. The **verdict**, which must be exactly one of two named outcomes — no third hedged option:
   either *"the Final Documentation Review is outstanding work"*, in which case name an owner (the
   phase or requirement that will carry it) and classify the row `genuinely outstanding`; or
   *"the validation report is recorded as wrong"*, in which case classify the row `satisfied` and
   state that the task list is the corroborated document. Choose whichever the search supports.
6. The **102-vs-103 explanation**: state both totals verbatim against their sources and explain the
   arithmetic difference — do not pick one number and drop the other. If the difference is that the
   validation report counts a Task 7.0 the task list never contained, say so in those terms.

Use the verdict vocabulary from the legend plan 01-01 wrote; do not invent a sixth class. Use
Medieval-military ubiquitous language where the subject matter is a shipped subsystem.
  </action>
  <acceptance_criteria>
    - `.planning/ledgers/milestone-01.md` contains a section heading for the Epic 10 Task 7.0 dispute
    - The section contains the literal strings `Final Documentation Review`, `tasks-epic10-validation-documentation.md`, `task6.0-validation-report.md`, `102` and `103`
    - The section contains the search commands that were run and their results
    - The section states exactly one verdict, and that verdict uses one of the five legend classes
    - Where the verdict is "outstanding", an owner is named; where it is "the validation report is wrong", the task list is named as the corroborated document
    - `grep -c "Final Documentation Review" .project/Milestone_1-MVP/Epic_10/tasks-epic10-validation-documentation.md` and the ledger's stated result agree
  </acceptance_criteria>
  <verify>
    <automated>for s in 'Final Documentation Review' 'tasks-epic10-validation-documentation.md' 'task6.0-validation-report.md' '102' '103'; do grep -qF "$s" .planning/ledgers/milestone-01.md || exit 1; done && grep -qE 'genuinely outstanding|satisfied' .planning/ledgers/milestone-01.md && ls .project/Milestone_1-MVP/Epic_10/ >/dev/null && echo RECON08_OK</automated>
  </verify>
  <done>The Epic 10 dispute has one recorded verdict backed by a recorded search, both subtask totals are quoted against their sources, and the arithmetic difference between them is explained rather than resolved by omission.</done>
</task>

<task type="auto">
  <name>Task 2: Record the remaining two divergences and the ingest-bookkeeping corrections</name>
  <read_first>
    - `.planning/ledgers/milestone-01.md` — the divergence section seeded by plan 01-01 with the REPL row, so the two new rows match its shape and the REPL flagging is preserved
    - `.planning/REQUIREMENTS.md` lines 2380-2400 — the existing `Code diverges` rows for `REQ-garrison-longterm-port` and `REQ-garrison-sqlite`, which are the house phrasing for exactly this class
    - `.planning/REQUIREMENTS.md` lines 28-40 — the summary table row asserting "All 115 run-1 requirement IDs"
    - `.planning/REQUIREMENTS.md` lines 2361-2541 — the Milestone 1 ledger body whose REQ-* rows must be counted deterministically
    - `.planning/intel/SYNTHESIS.md` — the reported run-1 requirement count that disagrees with the enumeration
    - `.planning/INGEST-CONFLICTS.md` lines 128-140 — the Battalion base module path conflict
    - `.planning/codebase/ARCHITECTURE.md` and `.planning/codebase/STRUCTURE.md` — for locating the shipped MCP transport and Sanctum adapters to cite
  </read_first>
  <files>.planning/ledgers/milestone-01.md</files>
  <action>
Complete the divergence section and add the bookkeeping corrections. Do not touch the per-epic REQ-*
sections — plans 01-06 and 01-07 own those.

**Two more divergence rows**, both classified `superseded by shipped code`, both in the existing
house phrasing ("shipped as X, not Y; see forward pointer") that `REQUIREMENTS.md:2392,2394` already
uses for this class:
- **MCP transport.** The Milestone-1 PRD specified an SSE transport; the tree ships MCP
  Streamable-HTTP. Cite the shipped adapter by `file:line` — locate it in the tree rather than
  trusting the map — and name the requirement ID(s) the divergence supersedes.
- **Vector search.** The Milestone-1 PRD specified a `sqlite-vss` extension of Garrison; semantic
  retrieval ships as **Sanctum** over Qdrant plus an in-memory backend. Cite the shipped Sanctum
  adapter by `file:line` and name the run-2 requirements that supply the missing coverage
  (`REQ-sanctum-port`, `REQ-embedding-port`, `REQ-sanctum-domain-model`).

Leave the REPL row exactly as plan 01-01 flagged it. All three rows carry a `file:line` citation plus
a named test, example or command that exercises the shipped alternative, per the D-19 evidence bar —
where no such exerciser can be named, the row is `present, unproven` for the exerciser half and says
so, rather than being upgraded on the strength of the code existing.

**Then add a `## Ingest bookkeeping corrections` section** with two entries:
- **Battalion base module path.** `INGEST-CONFLICTS.md` records that the Epic 4 document names the
  base module `battalion/mod.rs` while the project completion plan names it `battalion/battalion.rs`,
  contradicting its own Appendix B. Record the code-observed answer — confirm by listing the
  directory before writing it — and cite the file. State which document is corrected.
- **Requirement-count discrepancy.** Count the `REQ-*` rows in `REQUIREMENTS.md`'s Milestone 1
  as-shipped ledger deterministically (a grep over the section between its own heading and the next
  `##` heading), and record that enumerated total. Then record, verbatim and with citations, every
  other total this corpus reports for the same set: the "All 115 run-1 requirement IDs" claim in the
  REQUIREMENTS.md summary table, and the figure `.planning/intel/SYNTHESIS.md` reports. Explain the
  arithmetic difference between them — which rows are counted by one figure and not the other — and
  state which figure this ledger uses going forward. Do not reduce three numbers to one by choosing
  a favourite; the discrepancy is the finding RECON-01 asks to be recorded.

Every heading this plan declares must either hold content now or say in one line which later plan
fills it, so a partially authored ledger is still internally consistent.
  </action>
  <acceptance_criteria>
    - The divergence section contains exactly three rows and all three carry the `superseded by shipped code` class
    - The section contains the literal strings `Streamable-HTTP`, `sqlite-vss`, `Sanctum`, `Qdrant` and `NG-7`
    - The REPL row's bold flagging written by plan 01-01 is still present
    - Each divergence row carries at least one `file:line` citation that resolves against the tree
    - A `## Ingest bookkeeping corrections` section exists containing `battalion/mod.rs` and `battalion/battalion.rs`
    - The bookkeeping section contains at least three distinct requirement-count figures, each attributed to its source, plus an explanation of the difference and a statement of which figure this ledger uses
    - `grep -q "battalion/mod.rs" ` resolves against a real file: `crates/paladin-core/src/platform/container/battalion/mod.rs` exists and `crates/paladin-core/src/platform/container/battalion/battalion.rs` does not
  </acceptance_criteria>
  <verify>
    <automated>for s in 'Streamable-HTTP' 'sqlite-vss' 'Sanctum' 'Qdrant' 'NG-7' 'battalion/mod.rs' 'battalion/battalion.rs'; do grep -qF "$s" .planning/ledgers/milestone-01.md || exit 1; done && [ "$(grep -c 'superseded by shipped code' .planning/ledgers/milestone-01.md)" -ge 4 ] && [ -f crates/paladin-core/src/platform/container/battalion/mod.rs ] && [ ! -f crates/paladin-core/src/platform/container/battalion/battalion.rs ] && echo DIVERGENCE_OK</automated>
  </verify>
  <done>All three divergences are recorded as `superseded by shipped code` with resolving citations and the REPL row still flagged, the Battalion base module path has a code-observed answer, and every reported Milestone-1 requirement count is recorded verbatim with the difference explained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingested `.project/` documents → ledger verdict | two DOC-precedence documents disagree; the ledger's verdict becomes the record later phases plan against |
| absence of a search result → an asserted verdict | RECON-08 turns on a negative finding, which is only evidence if the search that produced it is recorded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-13 | Repudiation | the RECON-08 verdict | high | mitigate | The verdict must be accompanied by the literal commands run and their results; an unrecorded search cannot support a negative finding. Asserted as an acceptance criterion. |
| T-01-14 | Tampering | divergence rows read as defects by a later phase | high | mitigate | All three rows carry the `superseded by shipped code` class, a `file:line` citation to the shipped alternative, and a named forward pointer, so Phase 2 cannot plan a "fix" for correct code. |
| T-01-01 | Information Disclosure | quoting ingested `.project/` documents | high | mitigate | Cite `file:line` and quote only the specific claim under dispute; never paste document bodies. STATE.md records a plaintext OpenAI API key inside `Milestone_3-Completion/Post-Epic_24-cleanup/LIVE_API_TESTS_FIX.md`; no ledger row quotes that file's body. Secret-in-repo handling is canon and is owned by `/gsd-secure-phase`, not minted as a prohibition here. |
| T-01-15 | Tampering | outstanding work downgraded to deferred | medium | mitigate | The plan's third prohibition requires a citation to an actual deferral; inference from work-not-done is forbidden. |
| T-01-SC | Tampering | npm/cargo installs | high | mitigate | This plan installs nothing. RESEARCH.md §Package Legitimacy Audit records no `[ASSUMED]`/`[SUS]` packages, so no blocking legitimacy checkpoint is required. |
</threat_model>

<verification>
- The ledger contains a resolved Epic 10 Task 7.0 verdict with its search record and both subtask totals
- The divergence section holds exactly three `superseded by shipped code` rows with resolving citations, REPL row still flagged
- The bookkeeping section records the code-observed Battalion base module path and every reported requirement count with the difference explained
- No per-epic REQ-* section is authored by this plan
</verification>

<success_criteria>
- RECON-08 is answered with one of exactly two named outcomes, not left open
- A later phase reading the ledger cannot mistake Streamable-HTTP, Sanctum or the REPL for a defect
- Three reported requirement counts are visible side by side rather than silently reduced to one
</success_criteria>

<output>
Create `.planning/phases/01-ground-truth-decision-records/01-05-SUMMARY.md` when done
</output>
