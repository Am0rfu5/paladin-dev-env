---
phase: 01-ground-truth-decision-records
plan: 10
type: execute
wave: 7
depends_on: ["01-09"]
files_modified:
  - .planning/decisions/0006-coverage-gate.md
autonomous: false
gap_closure: true
requirements: [RECON-07]

must_haves:
  truths:
    - "The coverage question has exactly one number and exactly one scope, recorded in `.planning/decisions/0006-coverage-gate.md` together with the exact command that produced them and the date (RECON-07, D-07, D-08)."
    - "The recorded number is byte-identical to the figure in `01-coverage-measurement.md` and was produced by running that command against the current tree in this phase — not copied from any of the six documented stale or contested baselines (D-07)."
    - "The gate is the measured baseline rounded DOWN to a whole percent, hard-fail from the first run, with 80% recorded in the same ADR as the target and a named ratchet trigger (D-09)."
    - "The floor is derived by truncation toward zero to a whole percent — never round-half-up and never round-half-even — and the ADR shows the arithmetic on the actual measured figure so no reader has to re-derive it (resolves RECON-07 `precision`, derivation half)."
    - "A measured figure exactly equal to the floor PASSES the gate: the comparison is at-or-above, not strictly-above. One hundredth of a percent below the floor FAILS. Because the floor is the measured figure truncated downward, the gate cannot be red on the run that set it (resolves RECON-07 `boundary`)."
    - "The ratchet stops at exactly 80%: once the floor reaches 80% it never rises further, and a measured figure above 80% leaves the floor at 80% rather than tracking upward (resolves RECON-07 `boundary`, upper bound)."
    - "The ADR states the inclusion/exclusion decision for `examples/`, `benches/`, the `crates/doc-examples` crate and doctests in words as well as in the regex, so Phase 15 can reproduce the denominator (D-08, Claude's Discretion on exclusions)."
    - "The ADR states which command produced the recorded figure and how it relates to `cargo llvm-cov`, so Phase 15's CI can either reproduce the denominator or record why it differs — the two are never assumed equivalent by silence."
    - "The two module-scoped gates — Herald >= 95%, autonomous >= 90% — are named in the ADR, recorded as sitting above the global floor, explicitly not withdrawn, with placement handed to VERIFY-05 in Phase 5 (D-10)."
    - "The ADR records that Phase 3's success criterion 1, which names unit and integration coverage separately, must be amended to the single workspace-wide number, and names plan 01-12 as where that amendment lands (D-08)."
    - "The ADR carries a `## Code Conformance` value of `conforms` or `must change` decided against what the tree actually shows, per D-03, naming PIPE-02 in Phase 15 as the requirement that wires the floor into CI."
  prohibitions:
    - statement: "MUST NOT quietly withdraw the two module-scoped gates (Herald >= 95%, autonomous >= 90%) or the 80% target while recording one global number. Reducing a standing commitment is a decision that must be written as one — it must never be absorbed into a simplification, a rounding, or an omission."
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/decisions/0006-coverage-gate.md"
      provides: "RECON-07 decision record — one coverage number, one scope, one floor, one target, one ratchet trigger, both module-scoped gates preserved"
      contains: "## Code Conformance"
  key_links:
    - from: ".planning/decisions/0006-coverage-gate.md"
      to: ".planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md"
      via: "the ADR's recorded figure is transcribed from the measurement record, which holds the tool's raw output"
      pattern: "01-coverage-measurement\\.md"
    - from: ".planning/decisions/0006-coverage-gate.md"
      to: ".github/workflows/integration-tests.yml"
      via: "the recorded command's feature scope is stated relative to the project's existing CI invocation"
      pattern: "integration-tests\\.yml"
---

<objective>
Turn the measurement plan 01-09 produced into the single recorded gate RECON-07 asks for: one number,
one scope, one floor, one target, one ratchet trigger — and gate the one-way part of that decision
behind a human before it binds three later phases.

Purpose: six positions on the coverage gate exist across the corpus (80%, 85%, 75%-layered,
80%-Epic-24, 78%-hard, and a 70/74/78 phased ramp), and every measured baseline in the corpus is
stale or contested. ADR-0006 is the fifth of five ROADMAP success criteria still unmet and the root
blocker for the sixth. Recording it is effectively one-way: Phases 3, 5 and 15 all author work
against this number and scope.

Output: `.planning/decisions/0006-coverage-gate.md`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ground-truth-decision-records/01-CONTEXT.md
@.planning/phases/01-ground-truth-decision-records/01-RESEARCH.md
@.planning/phases/01-ground-truth-decision-records/01-PATTERNS.md
@.planning/phases/01-ground-truth-decision-records/01-04-PLAN.md
@.planning/phases/01-ground-truth-decision-records/01-09-SUMMARY.md
</context>

## Artifacts this phase produces

Every item below is created by Phase 1 and does not pre-exist. The plan-review-convergence
source-grounding pass must not flag these as unacknowledged drift.

- **New directories:** `.planning/decisions/`, `.planning/ledgers/`
- **New file created by THIS plan:** `.planning/decisions/0006-coverage-gate.md`
- **Other new files in the phase:** `.planning/decisions/PROMOTION.md`;
  `.planning/decisions/0001-battalion-config.md` … `0005-herald-trait.md`;
  `.planning/ledgers/milestone-01.md`;
  `.planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md`;
  `.planning/WINDOWS.md`
- **New ADR headings (document vocabulary):** `## Code Conformance`, `## Downstream Consumers`
- **New field values:** `conforms`, `must change` (the `Code conformance` field, D-03)
- **New governance vocabulary introduced by THIS plan:** the workspace coverage *floor* (the hard-fail
  gate), the coverage *target* (80%), and the named *ratchet trigger* that raises the floor toward the
  target
- **New naming scheme:** flat zero-padded monotonic ADR counter `NNNN-kebab-slug.md`
- **New ledger verdict classes:** `satisfied`, `present, unproven`, `genuinely outstanding`,
  `deferred with reason`, `superseded by shipped code` (D-20)
- **New ledger row ID recorded by plan 01-11:** `REQ-battalion-result-v1` in Epic 4's table
- **Rust identifiers recorded but NOT implemented in this phase (Phase 2 GAP-07 implements them):**
  `BattalionCheckpointConfig`; the new temperature-range field on `ProviderCapabilities`

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Lock the recorded coverage scope before ADR-0006 makes it binding</name>
  <decision>
    Which measurement scope does ADR-0006 record as THE scope — the one Phase 3 passes or fails
    against and Phase 15 wires into CI?
  </decision>
  <context>
    D-08 fixed the *shape* of the answer (one workspace-wide line-coverage number, all tests
    combined, from one reproducible command) and D-09 fixed the *arithmetic* (floor = measured figure
    truncated down to a whole percent, hard-fail from the first run, 80% as the target). Neither is
    being re-asked here — those doors are already walked through.

    What is genuinely undecided is narrower and was never put to you: D-08 assumed the scope would be
    the one `.github/workflows/integration-tests.yml` already runs, under
    `--features integration-tests`. That scope **cannot execute in this environment** — `docker` is
    absent, so the Redis- and MinIO-backed suites cannot run. Plan 01-09 therefore measured a
    narrower scope and said so.

    This is a one-way door. ADR-0006's number and scope are inherited by Phase 3's QUAL-01 (which
    raises real coverage against the floor), Phase 5's VERIFY-05 (which owns the module-scoped gates
    and extends this number across the four earlier positions) and Phase 15's PIPE-02 (which lands the
    CI threshold). Changing the scope after those phases are authored re-scopes all three and
    invalidates the ROADMAP amendment plan 01-12 makes on the strength of it.

    Two secondary choices ride along and are yours to confirm rather than choose between — CONTEXT.md
    recorded them as Claude's Discretion and plan 01-09 pinned them: `examples/`, `benches/` and the
    `crates/doc-examples` crate are excluded from the denominator, and doctests are excluded. Say so
    if either should change.
  </context>
  <options>
    <option id="option-a">
      <name>Record the locally-measured workspace scope as binding, with the CI gap named as later work</name>
      <pros>RECON-07 closes now with a real, reproducible, offline-verifiable number. The scope is honest about what ran. ADR-0006 explicitly hands VERIFY-05 and PIPE-02 the job of extending the scope to the Docker-backed suites, so the narrowing is recorded as scheduled work rather than lost. Phase 15's CI can measure a strictly wider scope later and record the delta against a known baseline.</pros>
      <cons>The recorded figure will not equal what CI reports today, so Phase 15 must reconcile them explicitly rather than assume they match. The number is narrower than the phrase "all tests combined" in D-08 literally implies, which the ADR must state plainly rather than gloss.</cons>
    </option>
    <option id="option-b">
      <name>Record a CI-produced figure under the full `--features integration-tests` scope</name>
      <pros>The recorded scope matches what CI already measures, so Phase 15's PIPE-02 wires a threshold against a number CI can reproduce on the first try with no reconciliation step.</pros>
      <cons>Requires an actual CI run to exist or be triggered, and its output read — the number cannot be reproduced or re-checked locally in this environment at all, which weakens exactly the reproducibility RECON-07 was created to establish. Blocks phase closure on an external run.</cons>
    </option>
    <option id="option-c">
      <name>Record both a local number and a CI number</name>
      <pros>Nothing is lost; both figures stay available.</pros>
      <cons>This reproduces the precise failure RECON-07 exists to close. The phase goal is "one number and one scope so Phase 3 can objectively pass or fail instead of choosing between 80% and 85%" — two numbers restores the choosing. Recommended against.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or option-c — and state whether the `examples/` / `benches/` / `crates/doc-examples` and doctest exclusions stand as pinned.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author ADR-0006 — one coverage number, one scope, one floor, one target, one ratchet</name>
  <reversibility rating="one-way">Phase 3's QUAL-01, Phase 5's VERIFY-05 and Phase 15's PIPE-02 are all authored against this number and scope, and plan 01-12 amends ROADMAP.md Phase 3's success criterion to cite it — changing it later re-scopes three phases and invalidates a criterion another phase is measured by.</reversibility>
  <read_first>
    - `.planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md` — the figure, the verbatim command, the toolchain versions, the commit SHA and the scope caveats produced by plan 01-09; transcribe from here, never from memory or from this plan's prose
    - `.planning/decisions/PROMOTION.md` — the required H2 heading set in order, the bullet-list rule for `## Code Locations` and `## Considered Options`, and the numbering index (slot 0006 is already reserved and `Next free ADR number: 0007` is already correct, so this file needs no edit)
    - `.planning/decisions/0005-herald-trait.md` — the shortest worked example of the house ADR shape, including the bare-word `## Status` body and the `**Date:**` prose line
    - `.planning/REQUIREMENTS.md` § "Group 1 — project-wide test coverage gate" (near line 1657) — the first four competing positions
    - `.planning/REQUIREMENTS.md` § "Group 30 — initial coverage threshold for the CI gate" (near line 2174) — the fifth and sixth positions and the parent PRD's Open Question 3
    - `.planning/ROADMAP.md` § "Phase 3" success criterion 1, § "Phase 5" VERIFY-05, and § "Phase 15" PIPE-02 — the criterion to be amended and the two downstream owners
    - `.planning/codebase/TESTING.md` — the `cargo tarpaulin` reference this ADR records as stale relative to the CI file
  </read_first>
  <files>.planning/decisions/0006-coverage-gate.md</files>
  <action>
Author `# ADR-0006: Project-wide test coverage gate` using the exact H2 heading set from
`PROMOTION.md`, in order: `## Status`, `## Context`, `## Decision`, `## Considered Options`,
`## Code Locations`, `## Code Conformance`, `## Downstream Consumers`. `## Code Locations` and
`## Considered Options` must be bulleted lists, never prose paragraphs — `adr-parser.cjs`'s
`splitEntries` yields structured entries only from bullet or numbered lines.

`## Status` body is the bare word `Accepted`, followed by a `**Date:**` prose line.

`## Context` states the question — one number and one scope for a gate a later phase can objectively
pass or fail — and enumerates the six competing positions with their sources: 80% (nine Milestone-1
PRDs), 85% (the unit-test-improvements workstream), 75% overall with a layered per-tier table (the
Milestone 3 plan), 80%/70% re-asserted (Epic 24), a 78% hard gate (the Deferred-QA parent PRD), and a
phased 70 to 74 to 78 ramp (Epic 25). Record that the parent PRD's Open Question 3 — hard fail or soft
warning initially — is Open in the corpus and is answered here. Record the measured baselines that are
stale or contested: 60.88% unit and 67.79% integration (Milestone 1, predating Milestones 2-12), ~78%
overall (Milestone 3 release notes), and 76-77% (Deferred-QA, February 2026, with two known-stale
module paths). Also record, in one short paragraph, that plan 01-04's original measurement path was
blocked (no `cargo-llvm-cov`, crates.io HTTP 403, no Docker) and that the figure recorded here was
produced instead by the offline `rustc` source-based instrumentation path plan 01-09 proved — this is
the provenance a reader needs to understand why the command differs from the CI file's.

`## Decision` is a bullet list carrying D-07 through D-10, transcribed from the measurement record:
- **The measured figure**, stated to exactly the precision the tool reported and byte-identical to
  the figure in `01-coverage-measurement.md`, with the UTC date and the git commit SHA it was measured
  against.
- **The exact command**, verbatim, including every flag and env var — the `RUSTFLAGS` and
  `LLVM_PROFILE_FILE` settings, `--workspace`, the `--ignore-filename-regex` value, and the fact that
  doctests are excluded. State in words which paths the regex removes (`examples/`, `benches/`, the
  `crates/doc-examples` crate, and toolchain/registry paths) so a reader never has to parse the regex
  to learn the scope.
- **The scope**, per whichever option Task 1 selected: one workspace-wide line-coverage number from
  one reproducible command. Not per-tier, not per-crate. Where the recorded scope is narrower than the
  CI file's `--features integration-tests` scope, say so plainly in one sentence, name Docker's absence
  as the reason, and name VERIFY-05 and PIPE-02 as the requirements that must extend it. Do not let the
  narrowing pass unstated.
- **The relationship to `cargo llvm-cov`**: the CI file installs and runs `cargo llvm-cov`, which is a
  wrapper over the same LLVM source-based instrumentation this measurement drove directly. State that
  the denominators are expected to agree only when the ignore regex, the doctest decision and the
  feature set match, and that PIPE-02 must either reproduce those three or record why its figure
  differs. Do not assert the two commands are equivalent by silence.
- **The gate (floor)**: the measured figure truncated **down** to a whole percent, **hard-fail from the
  first run**. Show the arithmetic explicitly on the actual measured figure — the measured value and
  the resulting floor, side by side — so nobody re-derives it. State that the rounding rule is
  truncation toward zero and is explicitly neither round-half-up nor round-half-even, and that a
  measured figure already on a whole percent floors to itself. State that the comparison is
  at-or-above: a later run measuring exactly the floor PASSES, and a run one hundredth of a percent
  below FAILS. Note that this answers Open Question 3 as hard-fail without the ramp-up problem it
  worried about, because the gate cannot be red on the run that set it.
- **The target**: 80%, recorded in this same ADR as the target rather than the gate, so the acceptance
  criteria in nine Milestone-1 PRDs and Epic 24 are neither falsified nor pretended to be met.
- **The ratchet trigger** (the Claude's-Discretion item D-09 requires be named): the floor is raised to
  the next whole percent below the then-current measured workspace figure whenever a milestone closes
  with measured coverage two or more whole percentage points above the standing floor. The raise is
  applied by amending this ADR in place with the new figure, command and date. State that the ratchet
  stops at the 80% target and does not pass it: once the floor reaches 80% it never rises again, and a
  measured figure above 80% leaves the floor at 80%.
- **The two module-scoped gates**: Herald >= 95% and autonomous >= 90%. Record them as sitting
  **above** the global floor and **explicitly not withdrawn**; hand their placement to **VERIFY-05 in
  Phase 5**, which the roadmap already assigns as their owner.
- **The tool-of-record note**: `.planning/codebase/TESTING.md` documents `cargo tarpaulin` as a local
  habit; record that it is stale relative to `.github/workflows/integration-tests.yml` and that a
  tarpaulin figure would not match this gate.
- **The consequence for the roadmap**: Phase 3's success criterion 1 names unit and integration
  coverage separately and must be amended to this single workspace-wide number. Name **plan 01-12** as
  the place that amendment lands.

`## Considered Options` is one bullet per rejected position, naming its `REQ-*` ID from variant groups
1 and 30 and a one-sentence reason. Include the two D-09 explicitly rejects: an immediate 80% hard gate
(would leave Phase 15's CI gate red until Phase 3 lands) and Epic 25's phased ramp (three numbers where
RECON-07 asked for one). Include, as a rejected option, whichever scope alternatives Task 1's
checkpoint did not select, with the human's stated reason.

`## Code Locations` is a bulleted list citing `.github/workflows/integration-tests.yml:117-118` (the
existing CI coverage invocation), the measurement record at
`.planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md`, and
`.planning/codebase/TESTING.md` as the superseded local-tooling reference.

`## Code Conformance` body is the bare word `conforms` or the bare phrase `must change`, decided against
what the tree actually shows rather than assumed. No coverage gate currently exists in `ci.yml`; state
plainly whether that counts as conformance or as pending work, and name **PIPE-02** in Phase 15 as the
requirement that wires the floor into CI either way. Do not smooth a mismatch into conformance.

`## Downstream Consumers` names Phase 3 (QUAL-01, which raises real coverage against this floor),
Phase 5 (VERIFY-05, which owns the module-scoped gates and extends this number across the four earlier
positions), and Phase 15 (PIPE-02, which must land the CI threshold on this number or record why it
differs).

Do not edit `.planning/decisions/PROMOTION.md` — slot 0006 is already reserved in its numbering index
and its `Next free ADR number: 0007` line is already correct.
  </action>
  <acceptance_criteria>
    - `node .claude/gsd-core/bin/lib/adr-parser.cjs --input .planning/decisions/0006-coverage-gate.md` exits 0, reports `"status": "accepted"`, and returns non-empty `decisions`, `options_considered` and `key_files`
    - The ADR contains all seven required H2 headings in `PROMOTION.md`'s order
    - The ADR contains the literal strings `VERIFY-05`, `PIPE-02`, `QUAL-01`, `95%`, `90%`, `80%`, `--workspace`, `--ignore-filename-regex` and `integration-tests.yml`
    - The percentage stated in the ADR as the measured figure is byte-identical to the workspace percentage in `01-coverage-measurement.md`
    - The ADR shows the floor arithmetic with both the measured value and the derived floor present as literal numbers, and states the truncation rule
    - The ADR states that a run measuring exactly the floor passes, and that the ratchet stops at 80%
    - The ADR names `crates/doc-examples`, `examples/` and `benches/` in words, and states the doctest decision
    - The ADR names plan 01-12 as where the ROADMAP Phase 3 amendment lands
    - `## Code Conformance` body is exactly one of `conforms` or `must change`, and the section names `PIPE-02`
    - `## Code Locations` and `## Considered Options` each contain at least two lines beginning with `- ` and contain no bare prose paragraph
  </acceptance_criteria>
  <verify>
    <automated>A=.planning/decisions/0006-coverage-gate.md; node .claude/gsd-core/bin/lib/adr-parser.cjs --input "$A" | grep -q '"status": "accepted"' && for s in 'VERIFY-05' 'PIPE-02' 'QUAL-01' '95%' '90%' '80%' 'crates/doc-examples' '01-12' 'integration-tests.yml'; do grep -qF "$s" "$A" || { echo "MISSING: $s"; exit 1; }; done && grep -qF -- '--workspace' "$A" && grep -qF -- '--ignore-filename-regex' "$A" && for h in '## Status' '## Context' '## Decision' '## Considered Options' '## Code Locations' '## Code Conformance' '## Downstream Consumers'; do grep -qF "$h" "$A" || { echo "MISSING HEADING: $h"; exit 1; }; done && M=$(grep -oE '[0-9]+\.[0-9]+%' .planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md | head -1) && [ -n "$M" ] && grep -qF "$M" "$A" && echo ADR0006_OK</automated>
  </verify>
  <done>The coverage question has one recorded number, one recorded scope, one hard-fail floor derived by stated truncation from the measurement, 80% recorded as the target, a concrete ratchet trigger with an explicit 80% stop, both module-scoped gates preserved and handed to VERIFY-05, and a conformance verdict decided against the tree rather than assumed.</done>
</task>

</tasks>

## Edge-probe resolution (this plan's share)

The consolidated 31-item ledger lives in `01-09-PLAN.md`. This plan resolves two of the items:

- **RECON-07 / `boundary`** → covered. At-or-above comparison; exactly-at-floor passes; one hundredth
  below fails; the floor cannot exceed the measured figure by construction; the ratchet's upper bound
  is exactly 80% and does not pass it. Authored into `must_haves.truths`.
- **RECON-07 / `precision`** (derivation half; plan 01-09 carries the recording half) → covered. The
  floor is derived by truncation toward zero to a whole percent, explicitly not round-half-up and not
  round-half-even; a whole-percent measurement floors to itself. Authored into `must_haves.truths`.

## Prohibition-probe resolution (this plan's share)

One bespoke prohibition from the spec-less §B recall lands here, authored **descriptor-less** in
`must_haves.prohibitions`: the ban on silently withdrawing the Herald >= 95% and autonomous >= 90%
module gates or the 80% target while recording one global number. It is `judgment`-tier — "was a
commitment quietly reduced" is not reducible to a mechanical assertion — and disposes
flagged-unverified. No canon-referral breadcrumb applies.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measurement record → recorded gate | a transcription drift between the evidence file and the ADR turns a real number into an unverifiable one |
| Phase 1 → Phases 3, 5 and 15 | this ADR's number and scope become the contract three later phases are authored against |
| local environment limits → recorded scope | the absence of Docker silently becoming the project's definition of "all tests combined" |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-40 | Tampering | the ADR's percentage drifting from the measurement record | high | mitigate | The verify command extracts the percentage from `01-coverage-measurement.md` and requires a byte-identical match inside the ADR, so a re-typed or rounded figure fails the gate. |
| T-01-41 | Spoofing | a scope narrower than CI's presented as the full "all tests combined" scope | high | mitigate | Task 1's blocking `checkpoint:decision` puts the narrowing to a human as an explicit, one-way choice with the CI alternative offered. The action requires the narrowing to be stated in one plain sentence naming Docker's absence, and requires VERIFY-05 and PIPE-02 to be named as the extenders. |
| T-01-42 | Repudiation | a standing commitment (module gates, 80% target) reduced without a written decision | high | mitigate | The plan's prohibition forbids it; acceptance criteria require the literals `95%`, `90%` and `80%` to be present and the ADR to record the module gates as not withdrawn. |
| T-01-43 | Denial of Service | a hard-fail floor set above the measured figure, red on day one | medium | mitigate | D-09's construction — floor = measured, truncated down — makes this arithmetically impossible; the ADR must show the arithmetic, and the boundary truth records that exactly-at-floor passes. |
| T-01-44 | Tampering | `PROMOTION.md`'s numbering index or next-free line edited unnecessarily | low | accept | Slot 0006 is already reserved and `Next free ADR number: 0007` is already correct; the action states explicitly that this file needs no edit, and it is absent from `files_modified`. |
| T-01-01 | Information Disclosure | authoring a planning document | low | accept | No secret material is introduced; the ADR cites in-repo paths and a locally measured percentage. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | **This plan installs nothing.** `01-RESEARCH.md` § Package Legitimacy Audit records no `[ASSUMED]`, `[SUS]` or `[SLOP]` package, so no blocking legitimacy checkpoint is required. |
</threat_model>

<verification>
- `.planning/decisions/0006-coverage-gate.md` parses via `adr-parser.cjs` with `status: accepted` and non-empty `decisions`, `options_considered` and `key_files`
- Its percentage is byte-identical to the workspace figure in `01-coverage-measurement.md`
- It carries all seven required H2 headings in order, with bulleted `## Code Locations` and `## Considered Options`
- It names VERIFY-05, PIPE-02 and QUAL-01, carries the 95% and 90% module gates as not withdrawn, states the truncation rule with worked arithmetic, and states the ratchet's 80% stop
- The recorded scope states plainly what was measured and what could not be, with Docker's absence named
- The human checkpoint has selected a scope option and confirmed or corrected the exclusions
</verification>

<success_criteria>
- One workspace coverage number and one scope exist as a recorded decision, both reproducible from a recorded command
- The gate is hard-fail and cannot be red on the run that set it, by stated construction
- 80% survives as the recorded target with a named ratchet trigger and an explicit stop
- The Herald and autonomous module gates are preserved, not withdrawn, and are owned by VERIFY-05
- A human has walked through the one-way scope door deliberately rather than inheriting it from an environment limitation
</success_criteria>

<output>
Create `.planning/phases/01-ground-truth-decision-records/01-10-SUMMARY.md` when done
</output>
