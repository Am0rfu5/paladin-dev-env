---
phase: 01-ground-truth-decision-records
plan: 12
type: execute
wave: 8
depends_on: ["01-10", "01-11"]
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [RECON-07]

must_haves:
  truths:
    - "`.planning/PROJECT.md` `## Key Decisions` holds six real ADR rows, each linking to a file that exists in `.planning/decisions/`; no row is a pending placeholder and no row links to a file that is not there (D-06)."
    - "The ADR-0006 row's Outcome cell is the `## Code Conformance` value read out of `.planning/decisions/0006-coverage-gate.md` itself, not assumed — the value was decided at execution time by plan 01-10."
    - "`.planning/ROADMAP.md` Phase 3 success criterion 1 names one workspace-wide coverage number and one scope instead of unit and integration coverage as two separate figures with two stale baselines, and links to the ADR that fixed them (D-08)."
    - "The amended criterion records who amended it and why — Phase 1, under RECON-07 and D-08 — so another phase's contract is never silently altered."
    - "The amended criterion defers the measurement scope (feature set, path exclusions, doctest decision) to ADR-0006 rather than restating it, so the criterion and the ADR cannot drift apart."
    - "Phase 3's Requirements line, its goal, and its other four success criteria are byte-identical afterwards; re-scoping Phase 3's testing work belongs to Phase 3."
    - "All sixteen `### Phase N:` headings survive in `.planning/ROADMAP.md`."
    - "`.planning/REQUIREMENTS.md`'s RECON-07 checkbox is checked only because ADR-0006 exists and parses — the checkbox follows the artifact, never leads it."
  prohibitions: []
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions table with six real ADR rows, the ADR-0006 placeholder replaced"
      contains: "decisions/0006-coverage-gate.md"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 3 success criterion 1 amended to one workspace-wide coverage number citing ADR-0006"
      contains: "0006-coverage-gate.md"
    - path: ".planning/REQUIREMENTS.md"
      provides: "RECON-07 recorded as satisfied, following the ADR rather than preceding it"
      contains: "RECON-07"
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/decisions/0006-coverage-gate.md"
      via: "the sixth Key Decisions row links to the ADR file"
      pattern: "decisions/0006-coverage-gate\\.md"
    - from: ".planning/ROADMAP.md"
      to: ".planning/decisions/0006-coverage-gate.md"
      via: "Phase 3's amended criterion cites the ADR that fixed the number and the scope"
      pattern: "0006-coverage-gate"
---

<objective>
Finish the two source corrections plan 01-08 correctly declined to make without ADR-0006: replace
PROJECT.md's honest "Pending" placeholder with a real Key Decisions row, and amend ROADMAP.md's
Phase 3 success criterion 1 to the single coverage number and scope ADR-0006 now records.

Purpose: this phase's whole point is that `.planning/` describes the code as it actually is. The
ADRs and the ledger are only authoritative if the documents readers actually open point at them.
ROADMAP success criterion 4 — "the coverage question has one number and one scope, so Phase 3 can
objectively pass or fail against it" — is unmet precisely because Phase 3's own criterion still names
two figures. This plan closes it.

Output: three scoped in-place edits, to `.planning/PROJECT.md`, `.planning/ROADMAP.md` and
`.planning/REQUIREMENTS.md`.

Wave note: `wave: 8` places this after plan 01-10 (which authors ADR-0006, wave 7) and plan 01-11
(which edits REQUIREMENTS.md's Milestone 1 body, wave 6), so this plan's REQUIREMENTS.md edit lands
on top of a settled file rather than racing it.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ground-truth-decision-records/01-CONTEXT.md
@.planning/phases/01-ground-truth-decision-records/01-PATTERNS.md
@.planning/phases/01-ground-truth-decision-records/01-08-PLAN.md
@.planning/phases/01-ground-truth-decision-records/01-08-SUMMARY.md
@.planning/phases/01-ground-truth-decision-records/01-10-SUMMARY.md
</context>

## Artifacts this phase produces

Every item below is created by Phase 1 and does not pre-exist. The plan-review-convergence
source-grounding pass must not flag these as unacknowledged drift.

- **New directories:** `.planning/decisions/`, `.planning/ledgers/`
- **New files:** `.planning/decisions/PROMOTION.md`; `.planning/decisions/0001-battalion-config.md`
  … `0006-coverage-gate.md`; `.planning/ledgers/milestone-01.md`;
  `.planning/phases/01-ground-truth-decision-records/01-coverage-measurement.md`;
  `.planning/WINDOWS.md`
- **Existing sections THIS plan revises:** `.planning/PROJECT.md` `## Key Decisions` (the ADR-0006
  row); `.planning/ROADMAP.md` Phase 3 success criterion 1; `.planning/REQUIREMENTS.md`'s RECON-07
  checkbox
- **New ADR headings (document vocabulary):** `## Code Conformance`, `## Downstream Consumers`
- **New field values:** `conforms`, `must change` (the `Code conformance` field, D-03)
- **New governance vocabulary (plans 01-09/01-10):** the workspace coverage *floor* (the hard-fail
  gate), the coverage *target* (80%), and the named *ratchet trigger* that raises the floor toward
  the target
- **New ledger verdict classes:** `satisfied`, `present, unproven`, `genuinely outstanding`,
  `deferred with reason`, `superseded by shipped code` (D-20)
- **New ledger row ID (plan 01-11):** `REQ-battalion-result-v1` in Epic 4's table
- **New naming scheme:** flat zero-padded monotonic ADR counter `NNNN-kebab-slug.md`
- **Rust identifiers recorded but NOT implemented in this phase (Phase 2 GAP-07 implements them):**
  `BattalionCheckpointConfig`; the new temperature-range field on `ProviderCapabilities`

<tasks>

<task type="auto">
  <name>Task 1: Replace PROJECT.md's ADR-0006 pending placeholder with a real Key Decisions row</name>
  <read_first>
    - `.planning/PROJECT.md` lines 1028-1046 — the `## Key Decisions` table as plan 01-08 left it: five real ADR rows (0001-0005) followed by one bold **Pending** placeholder row for ADR-0006 that names its blocker and carries no link, then the framing paragraph, then the eleven-ADR-candidate inventory
    - `.planning/decisions/0006-coverage-gate.md` — specifically its `## Decision` bullets (for the Rationale cell) and its `## Code Conformance` body (for the Outcome cell); read the actual field rather than assuming, because plan 01-10 decided it at execution time
    - `.planning/decisions/0002-battalion-result.md` and `0005-herald-trait.md` — the two `conforms` ADRs, to see how an Outcome cell reads when the verdict is `conforms` versus the `must change` rows above it
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
Use a scoped `Edit` on the single placeholder row. Never write the whole file — PROJECT.md is ~1,100
lines and holds the corpus-level findings nothing else records.

Replace the pending-placeholder ADR-0006 row with a real row in the same three-column
`Decision | Rationale | Outcome` schema the five rows above it use, keeping it in ADR-number order as
the sixth and last row:

- **Decision** — the question ADR-0006 answers, phrased like its siblings (a short noun phrase such
  as the project-wide test coverage gate), as a Markdown link to
  `.planning/decisions/0006-coverage-gate.md`, followed by the `(ADR-0006)` marker the other rows
  carry.
- **Rationale** — one sentence naming the chosen answer and the evidence class it rests on, drawn
  from the ADR's own `## Decision` bullets: the measured workspace line-coverage figure, the floor
  derived from it by truncation, 80% recorded as the target rather than the gate, and the fact that
  the figure was freshly measured against a named commit rather than taken from a prior baseline.
  Do not restate the ADR at length — the link carries the detail, and the five sibling rows are one
  sentence each.
- **Outcome** — the ADR's `## Code Conformance` value **verbatim**. Read it out of the file; plan
  01-10 decided it against what the tree actually shows, and it may be either value. Where it is
  `must change`, name the executing requirement in the cell exactly as the ADR-0001, ADR-0003 and
  ADR-0004 rows already do for GAP-07 — for this ADR the executing requirement is PIPE-02 in Phase 15.

Then reconcile the prose that the placeholder made necessary. The paragraph immediately below the
table states that five of the six competing-variant pairs are recorded above; update that count so it
reads as six recorded, and remove any clause that describes the sixth as not yet authored. Leave the
rest of that paragraph, including its pointer to `.planning/decisions/` for conventions and the
numbering index, exactly as it is.

Leave everything else alone: the eleven-ADR-candidate inventory, the `PROMOTION.md` pointer plan 01-08
added after it, the three restatements of the `ADR → shipped tree` precedence order, and the
"empty by evidence, not by omission" history paragraph. That last one is the corpus's most notable
structural finding and reads as history — it is not to be deleted now that the table is full.
  </action>
  <acceptance_criteria>
    - `.planning/PROJECT.md` `## Key Decisions` table has exactly six data rows
    - The table contains a Markdown link to each of `decisions/0001-battalion-config.md`, `0002-battalion-result.md`, `0003-formation-min-paladins.md`, `0004-temperature-validation.md`, `0005-herald-trait.md` and `0006-coverage-gate.md`
    - The ADR-0006 row's Outcome cell string matches the `## Code Conformance` body of `.planning/decisions/0006-coverage-gate.md`
    - The table contains no bold `**Pending.**` marker and no row lacking a link
    - The paragraph below the table states that six competing-variant pairs are recorded, not five
    - `grep -c "ADR → shipped tree" .planning/PROJECT.md` still returns `3`
    - `grep -q "\*(none)\*" .planning/PROJECT.md` fails, and `PROMOTION.md` is still referenced
    - The eleven-ADR-candidate inventory and the "empty by evidence, not by omission" paragraph are both still present
    - `git diff --numstat .planning/PROJECT.md` shows fewer than 20 changed lines — evidence of a scoped edit
  </acceptance_criteria>
  <verify>
    <automated>P=.planning/PROJECT.md; for n in 0001-battalion-config 0002-battalion-result 0003-formation-min-paladins 0004-temperature-validation 0005-herald-trait 0006-coverage-gate; do grep -q "decisions/$n.md" "$P" || { echo "MISSING: $n"; exit 1; }; done && [ "$(grep -c 'ADR → shipped tree' "$P")" = "3" ] && grep -q 'PROMOTION.md' "$P" && grep -q 'empty by evidence, not by omission' "$P" && ! grep -q '\*\*Pending\.\*\*' "$P" && ! grep -q '\*(none)\*' "$P" && [ "$(git diff --numstat -- "$P" | awk '{print $1+$2}')" -lt 20 ] && echo KEYDECISIONS_OK</automated>
  </verify>
  <done>PROJECT.md's Key Decisions table holds six real rows linking to six real ADR files, the ADR-0006 Outcome cell matches the ADR's own recorded conformance verdict, and the corpus-level emptiness finding survives as history rather than being erased.</done>
</task>

<task type="auto">
  <name>Task 2: Amend ROADMAP.md Phase 3's coverage criterion to one number and one scope</name>
  <read_first>
    - `.planning/ROADMAP.md` § "Phase 3: Verification Depth" in full (from its heading to the `### Phase 4:` heading) — success criterion 1 as it stands, plus the Requirements line, the goal and the other four criteria, all of which must survive byte-identical
    - `.planning/decisions/0006-coverage-gate.md` — the recorded figure, the scope, the exact command, the floor and the target the criterion must now name and defer to
    - `.planning/ROADMAP.md` § Progress — the coupling note `RECON-07 → VERIFY-05 → PIPE-02`, which the amendment must stay consistent with
    - `.planning/phases/01-ground-truth-decision-records/01-CONTEXT.md` § Deferred Ideas — the boundary: making this amendment is in scope for Phase 1; re-scoping Phase 3's testing work is Phase 3's job
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
Amend Phase 3's success criterion 1 with a scoped `Edit` on that one numbered line.

As it stands the criterion names unit coverage and integration coverage as two separate figures, each
carrying a parenthetical stale Milestone-1 baseline percentage, and it cites no decision record. D-08
fixed the scope as one workspace-wide line-coverage number, all tests combined, from one reproducible
command — so the criterion must name that instead. Remove both parenthetical baseline percentages
entirely; they predate Milestones 2 through 12 and are exactly the unresolved-scope statement RECON-07
was created to eliminate.

Rewrite the criterion so that it:
- names the single workspace-wide line-coverage figure and the hard-fail floor recorded in ADR-0006,
  without restating either number inline — cite the ADR as their source so the two documents cannot
  disagree;
- links to `.planning/decisions/0006-coverage-gate.md` as the source of the number, the floor and the
  exact command;
- states that the measurement scope — feature set, path exclusions and the doctest decision — is fixed
  by that ADR rather than restated here, for the same anti-drift reason;
- carries a short parenthetical recording that the criterion was amended by Phase 1 under RECON-07 and
  D-08, so an amendment to another phase's contract names its author and its reason.

Change nothing else in Phase 3: not its Requirements line, not its goal, and none of its other four
success criteria. Re-scoping Phase 3's testing work to the new single number is Phase 3's job, not
this one. Do not touch Phase 1's own `**Plans:**` line or its plan checklist — the planning workflow
maintains those.

Use a scoped `Edit`. `.planning/ROADMAP.md` holds all sixteen phases; a whole-file write would
destroy the fifteen this plan has no business touching.
  </action>
  <acceptance_criteria>
    - Phase 3's success criterion 1 names one workspace-wide coverage number and does not name unit coverage and integration coverage as two separate figures
    - The Phase 3 section contains a link to `.planning/decisions/0006-coverage-gate.md` and the string `RECON-07`
    - The Phase 3 section no longer contains the literal `60.88` or the literal `67.79`
    - Phase 3's `**Goal**` line, `**Depends on**` line and `**Requirements**` line are byte-identical to before this task
    - Phase 3 still has exactly five numbered success criteria, and criteria 2 through 5 are byte-identical
    - All sixteen `### Phase N:` headings are still present: `grep -c '^### Phase [0-9]' .planning/ROADMAP.md` returns `16`
    - `git diff --numstat .planning/ROADMAP.md` shows fewer than 20 changed lines
  </acceptance_criteria>
  <verify>
    <automated>R=.planning/ROADMAP.md; [ "$(grep -c '^### Phase [0-9]' "$R")" -eq 16 ] && P3=$(awk '/^### Phase 3: Verification Depth/,/^### Phase 4:/' "$R") && printf '%s' "$P3" | grep -q '0006-coverage-gate.md' && printf '%s' "$P3" | grep -q 'RECON-07' && ! printf '%s' "$P3" | grep -q '60\.88' && ! printf '%s' "$P3" | grep -q '67\.79' && [ "$(printf '%s\n' "$P3" | grep -c '^  [0-9]\. ')" -eq 5 ] && [ "$(git diff --numstat -- "$R" | awk '{print $1+$2}')" -lt 20 ] && echo ROADMAP_OK</automated>
  </verify>
  <done>Phase 3 can objectively pass or fail against one number and one scope owned by ADR-0006, the amendment names Phase 1 and RECON-07 as its author and reason, and no other phase or criterion in the roadmap was altered.</done>
</task>

<task type="auto">
  <name>Task 3: Record RECON-07 as satisfied in REQUIREMENTS.md, following the artifact</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` around line 186 — the `- [ ] **RECON-07**: One authoritative coverage gate is recorded — a single number and a single …` entry and its surrounding RECON-01 … RECON-08 block, so the checkbox change matches the shape of the seven already-checked siblings
    - `.planning/decisions/0006-coverage-gate.md` — the artifact whose existence is the only justification for checking the box
    - `.planning/phases/01-ground-truth-decision-records/01-VERIFICATION.md` — the record that this checkbox was correctly left unchecked while the ADR did not exist, so the SUMMARY can state what changed
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Check RECON-07's box — but only after proving the artifact it stands for exists and is well-formed.

**Verify first, edit second.** Confirm `.planning/decisions/0006-coverage-gate.md` exists and that
`node .claude/gsd-core/bin/lib/adr-parser.cjs --input .planning/decisions/0006-coverage-gate.md`
exits 0 reporting an accepted status with non-empty decisions. If either check fails, HALT and report
— the checkbox follows the artifact and never leads it, which is the discipline that kept this
requirement honestly open through the whole first execution of this phase.

Then change RECON-07's leading marker from unchecked to checked with a scoped `Edit`, matching the
exact form the seven sibling RECON entries already use. Change nothing else about the entry: its
requirement text, its em-dashed continuation lines and its italic *Derives:* / *Resolves:* provenance
line all stay byte-identical. Do not add a note, a date or a pointer — the sibling entries carry none,
and the record of how it was satisfied lives in the ADR and in this plan's SUMMARY.

Leave every other checkbox in REQUIREMENTS.md alone. In particular do not touch VERIFY-05 near line
368, which references RECON-07 but belongs to Phase 5 and is genuinely still open.

Use a scoped `Edit`. `.planning/REQUIREMENTS.md` is ~4,000 lines and plan 01-11 has just reduced its
Milestone 1 ledger body in the same phase; a whole-file write would undo that and destroy the forward
requirements and the thirty variant groups besides.
  </action>
  <acceptance_criteria>
    - `node .claude/gsd-core/bin/lib/adr-parser.cjs --input .planning/decisions/0006-coverage-gate.md` exits 0 before the edit is made
    - The RECON-07 entry in `.planning/REQUIREMENTS.md` begins with a checked marker
    - All eight RECON-01 … RECON-08 entries are now checked
    - RECON-07's requirement text and its italic provenance line are byte-identical to before this task
    - The VERIFY-05 entry near line 368 is still unchecked and byte-identical
    - Plan 01-11's reduced `## Milestone 1 as-shipped ledger` pointer section is still a pointer — it still links to `.planning/ledgers/milestone-01.md` and still carries no requirement-ID table rows
    - `git diff --numstat .planning/REQUIREMENTS.md` shows fewer than 6 changed lines
  </acceptance_criteria>
  <verify>
    <automated>R=.planning/REQUIREMENTS.md; node .claude/gsd-core/bin/lib/adr-parser.cjs --input .planning/decisions/0006-coverage-gate.md >/dev/null && [ "$(grep -c '^- \[x\] \*\*RECON-0[1-8]\*\*' "$R")" -eq 8 ] && grep -q '^- \[ \] \*\*VERIFY-05\*\*' "$R" && S=$(awk '/^## Milestone 1 as-shipped ledger/,/^## Milestone 2-3 as-shipped ledger/' "$R") && printf '%s' "$S" | grep -q 'ledgers/milestone-01.md' && [ "$(printf '%s\n' "$S" | grep -c '^| REQ-')" -eq 0 ] && [ "$(git diff --numstat -- "$R" | awk '{print $1+$2}')" -lt 6 ] && echo RECON07_OK</automated>
  </verify>
  <done>All eight RECON requirement IDs are recorded as satisfied, RECON-07's among them only because ADR-0006 exists and parses, and plan 01-11's pointer reduction survived intact.</done>
</task>

</tasks>

## Edge-probe resolution (this plan's share)

The consolidated 31-item ledger lives in `01-09-PLAN.md`; the equality accounting for the whole
gap-closure set is stated there. This plan resolves no additional probe items — its three tasks are
scoped edits that consume decisions recorded by plans 01-10 and 01-11 rather than making new ones, so
manufacturing predicates here would be busywork rather than coverage. The twenty flagged assumptions
covering RECON-02, RECON-04, RECON-05, RECON-06 and RECON-08 are recorded in `01-09-PLAN.md` and apply
unchanged.

## Prohibition-probe resolution (this plan's share)

No bespoke prohibition from the spec-less §B recall lands on this plan. The four kept prohibitions are
authored on plans 01-09 (two), 01-10 (one) and 01-11 (one). The candidates Stage 2 dropped that would
otherwise have attached here — the ban on wholesale rewrites of PROJECT.md, ROADMAP.md and
REQUIREMENTS.md, and the ban on amending another phase's criterion without attribution — are routine
engineering hygiene already standing as prohibitions on executed plan 01-08, and they remain in force
through this plan's task actions and diff-size acceptance criteria. `must_haves.prohibitions` is
therefore intentionally empty rather than padded.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 1 → Phase 3's contract | amending another phase's success criterion changes what that phase will be measured against |
| an ADR's recorded verdict → a summary table cell | a Key Decisions Outcome cell restated from memory drifts from the ADR it links to |
| an artifact's existence → a requirement checkbox | a checkbox checked ahead of its artifact converts an honest open item into a false completion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-60 | Spoofing | RECON-07's checkbox checked without ADR-0006 existing | high | mitigate | Task 3 runs `adr-parser.cjs` against the ADR before editing and HALTs on failure; the verify command re-runs the parser as a precondition of the assertion, so a checked box with no parseable ADR fails the gate. |
| T-01-61 | Denial of Service | whole-file rewrite of PROJECT.md, ROADMAP.md or REQUIREMENTS.md | high | mitigate | Every task action mandates scoped `Edit` replacements and names what a whole-file write would destroy; acceptance criteria bound each diff (`< 20`, `< 20`, `< 6` changed lines) and assert survivors — sixteen phase headings, three precedence restatements, the eleven-ADR inventory, and plan 01-11's pointer section. |
| T-01-62 | Tampering | a Key Decisions Outcome cell drifting from the ADR it links to | medium | mitigate | Task 1's action requires reading `## Code Conformance` out of the file rather than assuming, because plan 01-10 decided it at execution time; an acceptance criterion requires the cell to match the ADR body. |
| T-01-63 | Repudiation | another phase's criterion amended without attribution | medium | mitigate | Task 2's action requires the amendment to carry a parenthetical naming Phase 1, RECON-07 and D-08; an acceptance criterion asserts `RECON-07` appears in the Phase 3 section. |
| T-01-64 | Tampering | the amended criterion restating the number and scope inline, then drifting from the ADR | medium | mitigate | The action requires the criterion to cite the ADR as the source rather than restate the figures, and to defer feature set, path exclusions and the doctest decision to it explicitly. |
| T-01-01 | Information Disclosure | editing planning documents | low | accept | No secret material is introduced; the edits are links, a checkbox and one rewritten criterion within `.planning/`. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | **This plan installs nothing.** `01-RESEARCH.md` § Package Legitimacy Audit records no `[ASSUMED]`, `[SUS]` or `[SLOP]` package, so no blocking legitimacy checkpoint is required. |
</threat_model>

<verification>
- PROJECT.md's Key Decisions table holds six ADR-linked rows with no pending placeholder, the ADR-0006 Outcome cell matches its ADR, and plan 01-01's three precedence restatements plus the eleven-ADR inventory survive
- ROADMAP.md still holds all sixteen phase headings; Phase 3 still has five criteria; criterion 1 names one workspace number, links to ADR-0006, cites RECON-07, and carries neither stale baseline percentage
- REQUIREMENTS.md has all eight RECON IDs checked, VERIFY-05 still open, and plan 01-11's Milestone 1 pointer section intact
- No document was rewritten wholesale; every diff is within its stated bound
</verification>

<success_criteria>
- Every reader-facing planning document points at ADR-0006 rather than at a placeholder or a superseded pair of baselines
- Phase 3 has one number and one scope to pass or fail against, and knows which record owns them
- RECON-07 is recorded as satisfied only because the artifact it names exists and parses
- ROADMAP success criteria 2 and 4 — the two this phase failed verification on — are both now true
</success_criteria>

<output>
Create `.planning/phases/01-ground-truth-decision-records/01-12-SUMMARY.md` when done
</output>
