---
phase: 02-functional-gap-closure
plan: 09
type: execute
wave: 4
depends_on: ["02-02", "02-03", "02-04", "02-05", "02-06", "02-07", "02-08"]
files_modified:
  - .planning/phases/02-functional-gap-closure/02-test-wiring-sweep.md
  - .planning/decisions/0007-battalion-cancellation-deferral.md
  - .planning/ledgers/milestone-01.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [GAP-01, GAP-02, GAP-03, GAP-04, GAP-05, GAP-06, GAP-07]

must_haves:
  truths:
    - "Every directory under `tests/` is cross-checked against the `[[test]]` targets in `Cargo.toml` and every barrel module declaration, and anything still never compiled into a target is named in a written sweep record."
    - "`REQ-battalion-cancellation` is recorded Phalanx-only with the other three patterns deferred, and because that overrides the requirement as written it carries an ADR — numbered after 0006, written to ADR-0004's shape, parsing under the project's ADR parser."
    - "The deferred cancellation work has a named forward owner and a named prerequisite, so the deferral is a decision with an address rather than a disappearance."
    - "Every ledger row this phase's measurements contradicted is amended in place with the new verdict, the command that produced it, and the date — the ledger stays one document a reader can trust, not a Phase 1 snapshot plus a Phase 2 correction file."
    - "ROADMAP Phase 2's success criteria no longer assert a premise the tree contradicts: criterion 1's claim that the named Commander test fails today is corrected, and criterion 5 matches what ingest run 3 already narrowed GAP-07 to."
    - "Three corrections this phase's own planning inputs got wrong are recorded: CONTEXT.md D-15's construction-site list was incomplete, ADR-0001's Code Locations omitted the ports-crate consumer sites, and the research document's claim that a missed doc example would fail `cargo test` workspace-wide is false for a crate with doctests disabled."
  prohibitions:
    - statement: "MUST NOT record a measured result that was not produced by running the stated command on the recorded commit — no paraphrased, reconstructed, or summarised-from-memory output. A ledger row's `command that produced it` field must name a command that was actually run."
      status: resolved
      verification: judgment
    - statement: "MUST NOT upgrade a ledger verdict without both a `file:line` citation and a named passing exerciser, and MUST NOT record a deferral without naming both the deferring authority and a named forward owner. A deferral with no owner is a disappearance wearing a verdict class."
      status: resolved
      verification: judgment
    - statement: "MUST NOT downgrade, soften, or omit a ledger row because this phase's work made it inconvenient. Where a measurement contradicts a Phase 1 verdict in either direction, the contradiction is written into the row explicitly — this corpus's own repeated finding is that its evidence has been wrong in both directions."
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/decisions/0007-battalion-cancellation-deferral.md"
      provides: "the ADR recording that battalion-wide cancellation is Phalanx-only and the other three patterns are deferred, with the forward owner and prerequisite named"
      contains: "Code Conformance"
    - path: ".planning/phases/02-functional-gap-closure/02-test-wiring-sweep.md"
      provides: "the D-12 sweep record: every `tests/` directory cross-checked against `[[test]]` targets and barrel declarations"
      min_lines: 40
  key_links:
    - from: ".planning/ledgers/milestone-01.md"
      to: ".planning/phases/02-functional-gap-closure/02-test-baseline.md"
      via: "each amended row cites the command and the commit from the phase's measured baseline record"
      pattern: "02-test-baseline"
    - from: ".planning/decisions/0007-battalion-cancellation-deferral.md"
      to: ".planning/ledgers/milestone-01.md"
      via: "the `REQ-battalion-cancellation` row's `deferred with reason` verdict cites this ADR as the deferring authority"
      pattern: "0007"
---

<objective>
Close the phase's record: sweep for any remaining never-compiled test source, mint the ADR that
records the cancellation deferral, amend every contradicted ledger row in place, and correct the
ROADMAP criteria whose premises the tree has overtaken.

Purpose: this phase's goal is that every Milestone-1 functional requirement is either working and
tested *or explicitly deferred with a recorded reason*. The first half is done by plans 02-02
through 02-08; this plan does the second half and makes the whole thing legible. D-02 sets the
convention four sibling ground-truth phases inherit — amend the ledger in place rather than
keeping a corrections file, because a reader should not need two documents to know a row's
current verdict. D-08 escalates one deferral to an ADR because it overrides a written
requirement.

Output: a sweep record, ADR-0007, an amended ledger, and a ROADMAP whose Phase 2 criteria state
outcomes without stale premises.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-functional-gap-closure/02-CONTEXT.md
@.planning/decisions/PROMOTION.md
@.planning/decisions/0004-temperature-validation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep every `tests/` directory against the compiled test targets (D-12)</name>
  <read_first>
    - `Cargo.toml` lines 165-220 — every `[[test]]` target, its `path`, and its
      `required-features`. This is one half of the cross-check.
    - `tests/lib.rs`, `tests/unit/mod.rs`, `tests/integration/mod.rs`,
      `tests/integration/battalion/mod.rs`, `tests/cli/mod.rs`, `tests/helpers/mod.rs` — the
      barrels, which are the other half. Note that direct children of `tests/` are auto-discovered
      by Cargo and need no `[[test]]` entry, while a subdirectory is reachable only through a
      barrel that a target root declares.
    - `.planning/codebase/TESTING.md` — the documented three-tier strategy and the barrel
      convention, for whether what you find is a deviation or the intended shape.
    - `.planning/phases/02-functional-gap-closure/02-RESEARCH.md` § "Test wiring map" — the
      researcher's already-verified table of which path is wired via what, as a starting point to
      confirm or correct rather than to trust.
    - `.planning/phases/02-functional-gap-closure/02-CONTEXT.md` D-12 — the sweep's scope, and the
      explicit rule that *fixing* anything found beyond the LLM module is a separate decision, not
      automatic.
  </read_first>
  <files>.planning/phases/02-functional-gap-closure/02-test-wiring-sweep.md</files>
  <action>
Enumerate every `.rs` file under `tests/` and determine, for each, whether it is compiled into
some test target — directly as an auto-discovered child of `tests/`, via a `[[test]]` target's
declared path, or via a barrel `mod` declaration reachable from one of those roots. Record the
method for each. `cargo test --workspace --list` and `cargo test --features cli --test cli --list`
give the authoritative "what actually compiled" side; the file listing gives the "what exists"
side; the sweep is the difference between them.

Write the result to `02-test-wiring-sweep.md` as a table: path, how it is reached (or that it is
not), and status. Cover `tests/unit/` and its subdirectories, `tests/integration/` and its
subdirectories, `tests/cli/`, `tests/functional/`, `tests/helpers/`, and any direct child of
`tests/`.

Also extend the sweep to `benches/` and `examples/` — CONTEXT.md leaves this to the planner's
discretion and it is nearly free once the machinery is written. Phase 4's success criterion 5
requires all 22 examples to build in CI, so an orphan there is worth knowing about now rather
than discovering it in a release gate. Record whether each example and bench is declared or
auto-discovered, and note the count of examples found against the 22 that criterion expects.

Record the two instances this phase already knew about and closed — the LLM unit module and the
five CLI modules — as closed, citing the plan that closed each. Anything *new* the sweep finds is
reported only: name it, say what target it should belong to, and assign a forward owner. Do not
fix it. Committing to repair an unknown-size finding sight-unseen is the unbounded trap the
per-file fallback rule exists to avoid.

While the machinery is out, record one related dead-code finding CONTEXT.md leaves to the
planner's discretion so it is not lost: the `LlmProviderError` type and its conversion impl in
`crates/paladin-llm/src/error.rs` have no constructors anywhere outside that file, so the
documented conversion path is dead while each adapter maps status codes at the call site. Verify
that claim against the current tree, record it with a `file:line` citation and the grep that
shows it, and assign Phase 3 as the forward owner. It is a finding, not a fix.

Close with a "Findings requiring a decision" section listing anything new, each with a proposed
forward owner, and a "Nothing further found" statement if the answer is genuinely nothing.
  </action>
  <acceptance_criteria>
    - `.planning/phases/02-functional-gap-closure/02-test-wiring-sweep.md` exists and is at least 40 lines.
    - The sweep table covers `tests/unit/`, `tests/integration/`, `tests/cli/`, `tests/functional/` and `tests/helpers/`, and includes `benches/` and `examples/`.
    - Every row states how the file is reached (auto-discovery, a named `[[test]]` target, or a named barrel `mod` line) or that it is unreachable.
    - The record contains the raw output of `cargo test --workspace --list` summarised by target, so the "what compiled" side is evidence rather than assertion.
    - The example count found is stated alongside the 22 that Phase 4's success criterion 5 expects.
    - The `LlmProviderError` dead-conversion-path finding is recorded with a `file:line` citation and the grep that demonstrates it, and names Phase 3 as forward owner.
    - The two already-closed instances (the LLM unit module, the five CLI modules) are recorded as closed with the closing plan named.
    - A "Findings requiring a decision" section exists, and every entry in it carries a proposed forward owner.
    - No source file was modified by this task: `git diff --name-only` lists nothing outside `.planning/`.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/02-functional-gap-closure/02-test-wiring-sweep.md && [ "$(wc -l < .planning/phases/02-functional-gap-closure/02-test-wiring-sweep.md)" -ge 40 ] && grep -q 'Findings requiring a decision' .planning/phases/02-functional-gap-closure/02-test-wiring-sweep.md</automated>
  </verify>
  <done>Every file under `tests/`, `benches/` and `examples/` is accounted for against the compiled targets, findings are reported with forward owners, and nothing beyond the two already-closed instances was repaired.</done>
</task>

<task type="auto">
  <name>Task 2: Mint ADR-0007 recording the battalion-cancellation deferral (D-05/D-08)</name>
  <read_first>
    - `.planning/decisions/0004-temperature-validation.md` — **the shape to follow exactly**:
      `## Status` (with a `**Date:**` line), `## Context`, `## Decision`, `## Considered Options`,
      `## Code Locations`, `## Code Conformance`, `## Downstream Consumers`. CONTEXT.md names this
      ADR as the template.
    - `.planning/decisions/PROMOTION.md` — the house conventions for a new decision record.
    - `.planning/ledgers/milestone-01.md` § "Epic 4 — Battalion Orchestration", the
      `REQ-battalion-cancellation` row — the requirement text, the Phalanx-only citation
      (`phalanx_service.rs:151`, tested at `:758`), and the recorded fact that the other three
      patterns expose no cancellation entry point at all.
    - `crates/paladin-battalion/src/phalanx_service.rs` around lines 151 and 758 — the shipped
      `execute_with_cancellation` and its test, read from the current tree to confirm the line
      citations before writing them into an ADR.
    - `.planning/phases/02-functional-gap-closure/02-CONTEXT.md` D-03, D-05 and D-08 — the
      deferral, the escalation rule that makes it an ADR rather than a plain ledger row, and the
      four things the ADR must record.
    - `.planning/ROADMAP.md` § Phase 3 — the QUAL requirement set, to confirm for yourself that
      it is verification-depth work rather than new Battalion capability before recording why it
      was rejected as the forward owner.
  </read_first>
  <files>.planning/decisions/0007-battalion-cancellation-deferral.md</files>
  <action>
Write `.planning/decisions/0007-battalion-cancellation-deferral.md` following ADR-0004's section
structure exactly, so the project's ADR parser reads it the way it reads 0001 through 0006.

`## Context` records the requirement as written (battalion-wide cancellation), the shipped
reality verified against the tree (`execute_with_cancellation` exists on Phalanx only, with its
`file:line` and its passing test named), and the fact that Formation, Campaign and
ChainOfCommand expose no cancellation entry point at all.

`## Decision` records that `REQ-battalion-cancellation` is satisfied for Phalanx and
`deferred with reason` for the other three patterns, and states plainly why: building it for
three more patterns is new capability, not gap closure. It needs a cancellation *contract* across
four execution services — including the two genuinely open questions, what a cancelled run
returns mid-DAG in Campaign and mid-delegation in ChainOfCommand — and the ROADMAP's Phase 2
success criteria never mention cancellation, which corroborates that it was never this phase's
scope.

Name the forward owner explicitly. **Owner: the v2 backlog, gated on a recorded
cancellation-contract decision.** Record that Phase 3 was considered and rejected: QUAL-01
through QUAL-04 are verification-depth items — coverage, un-ignoring existing error tests, MCP
failure modes — and none of them builds new Battalion capability, so assigning this there would
be assigning work to a phase whose scope excludes it. Record the prerequisite as its own line:
the mid-run-return contract for Campaign and ChainOfCommand must be decided before any
implementation, and that decision is itself ADR-shaped.

`## Considered Options` records the two alternatives D-05 rejected — implementing cancellation
across all four patterns in this phase, and a Formation-only half-measure — with why each was
rejected.

`## Code Locations` cites the Phalanx entry point and its test, and states that the other three
execution services have no corresponding site, which is the point.

`## Code Conformance` is `conforms` — this ADR records a scope decision and mandates no code
change. Say so explicitly so no later phase reads it as pending work.

`## Downstream Consumers` names the ledger row this ADR authorises, and the v2 backlog owner.

Then confirm the file parses the way the six existing ADRs do, using the project's ADR parser at
`.claude/gsd-core/bin/lib/adr-parser.cjs` — Phase 1 gated every ADR status flip on that parser
exiting 0, and this ADR should meet the same bar.
  </action>
  <acceptance_criteria>
    - `.planning/decisions/0007-battalion-cancellation-deferral.md` exists.
    - It carries all seven ADR-0004 section headings: `grep -cE '^## (Status|Context|Decision|Considered Options|Code Locations|Code Conformance|Downstream Consumers)$' .planning/decisions/0007-battalion-cancellation-deferral.md` returns 7.
    - It carries a `**Date:**` line under `## Status`.
    - `## Code Conformance` states `conforms` and explicitly says no code change is mandated.
    - The forward owner and the prerequisite are each named on their own line, and the record states why Phase 3 was rejected as owner.
    - It cites `phalanx_service.rs` with line numbers verified against the current tree, and names the passing test that exercises the shipped cancellation path.
    - The ADR parses under the project's ADR parser with the same exit status as ADR-0004: running the parser against both files succeeds for both.
    - `.planning/decisions/0001-battalion-config.md` through `0006-coverage-gate.md` are unmodified: none appears in `git diff --name-only`.
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -cE '^## (Status|Context|Decision|Considered Options|Code Locations|Code Conformance|Downstream Consumers)$' .planning/decisions/0007-battalion-cancellation-deferral.md)" = "7" ] && node .claude/gsd-core/bin/lib/adr-parser.cjs .planning/decisions/0007-battalion-cancellation-deferral.md</automated>
  </verify>
  <done>ADR-0007 records the Phalanx-only cancellation reality, the three-pattern deferral with its reasoning, a named forward owner and a named prerequisite — and parses like its six siblings.</done>
</task>

<task type="auto">
  <name>Task 3: Amend the ledger in place and correct the ROADMAP's stale premises</name>
  <read_first>
    - Every SUMMARY this phase produced: `.planning/phases/02-functional-gap-closure/02-01-SUMMARY.md`
      through `02-08-SUMMARY.md`. These carry the measured verdicts, the test names, the commands
      and the per-file counts that every amendment below must cite. Read them before touching the
      ledger.
    - `.planning/phases/02-functional-gap-closure/02-test-baseline.md` — the commit, the commands
      and the agrees/contradicts verdicts from plan 02-01.
    - `.planning/ledgers/milestone-01.md` § "Verdict legend" and the six per-epic sections this
      phase touches: Epic 2 (Garrison), Epic 4 (Battalion, including the cancellation row), Epic 5
      (Commander), Epic 6 (Provider Expansion, including the dead-module finding and the nested
      task rows), Epic 8 (Herald, including task 7.13), Epic 9 (Armory CLI, tasks 13.4-13.6), and
      § "Outstanding item reconciliation".
    - `.planning/ROADMAP.md` § "Phase 2: Functional Gap Closure" — the five success criteria, of
      which criterion 1 asserts a premise the tree contradicts and criterion 5 restates a scope
      ingest run 3 already narrowed.
    - `.planning/phases/02-functional-gap-closure/02-CONTEXT.md` D-02 and D-03 — the amend-in-place
      convention and the deferral-row shape.
  </read_first>
  <files>.planning/ledgers/milestone-01.md, .planning/ROADMAP.md</files>
  <action>
Amend `.planning/ledgers/milestone-01.md` in place. Every amendment carries three things: the new
verdict, the command or `file:line` that produced it, and the date. Use only the five existing
verdict classes; do not invent a sixth. Do not create a separate corrections file — D-02 chose
amend-in-place precisely so a reader never needs two documents to know a row's current verdict,
and four sibling ground-truth phases inherit this convention.

Apply, at minimum, these amendments, each sourced from the named plan's SUMMARY:

- **Epic 8, `REQ-herald-battalion-result-fields`** — the row currently reads `satisfied`. Plan
  02-04 contradicted that by direct code reading: the Table Herald ignored its argument, the JSON
  Herald omitted the strategy and the aggregate tokens, and Formation never populated the
  aggregation at all. Amend the row to record the contradiction and the closure, citing plan
  02-04's changes and plan 02-05's end-to-end test by name.
- **Epic 8, nested task 7.13** — `present, unproven` becomes `satisfied`, citing plan 02-05's
  test name and the exact command that runs it. The task's own note said the gap was "needs
  Battalion execution setup"; record that the closure drove a real execution service.
- **Epic 9, nested tasks 13.4, 13.5 and 13.6** — the rows read `genuinely outstanding` with the
  finding "the test itself was never written". Plan 02-07 contradicted that: the tests were
  written and had been commented out of the barrel. Amend each row with the correction and the
  named passing exerciser.
- **Epic 6, `REQ-provider-testing`** — amend with plan 02-06's outcome: the module is wired, the
  per-file counts of tests present, passing and removed, and each removal's note. Also amend the
  nested rows for tasks 7.10 (now closed by the new provider-switching test, named) and 7.14
  (`deferred with reason`, owner Phase 15 PIPE, blocked in substance on Phase 5's VERIFY-06).
- **Epic 4, `REQ-battalion-cancellation`** — split the row: Phalanx `satisfied` with its existing
  citation preserved; the other three patterns `deferred with reason`, citing ADR-0007 as the
  deferring authority and its named forward owner.
- **Epic 2, `REQ-garrison-testing` and its two nested items** — amend from plan 02-08's review:
  task 11.5 `superseded by shipped code` citing ADR-0006, task 11.6 `satisfied` citing the review
  document by path.
- **Epic 5, `REQ-commander-auto-selection`** — restate the GAP-05 finding with plan 02-01's
  measured command as the evidence, replacing inference with a command that ran.
- **Any other row plan 02-01's baseline recorded as contradicted** — amend each with the same
  three fields. If the baseline found no other contradictions, say so explicitly in the section
  rather than leaving the absence to be inferred.

Then update § "Outstanding item reconciliation" so its counts and verdict distribution match the
amended rows, and show the arithmetic.

Add a short "Phase 2 amendments" note near the top of the ledger recording that this file is now
amended in place per D-02, with the date and the phase, so a reader knows the document has more
than one author phase.

Also record three corrections to this phase's own planning inputs, each in the ledger section
where a future reader would look:
1. CONTEXT.md D-15's `ProviderCapabilities` construction-site list was compiled by grep during
   discussion and was incomplete — plan 02-02's SUMMARY carries the full compiler-derived list.
2. ADR-0001's `Code Locations` omitted the ports-crate consumer sites for the citadel rename —
   plan 02-03's SUMMARY carries the full count.
3. The research document's claim that a missed doc example would fail `cargo test` workspace-wide
   is false for the ports crate, which sets `doctest = false`; the real compile-breaking sites
   there were the ones inside the test module. Note that this makes the existing `doctest = false`
   tech-debt item (DEBT-03) load-bearing for future renames.

Then amend `.planning/ROADMAP.md` § "Phase 2: Functional Gap Closure":
- **Criterion 1** — remove the assertion that the named Commander test "fails today", which the
  measured baseline contradicts. Keep the criterion's outcome (a green workspace test run
  including that test); only the stale premise goes. Phase 1 set the precedent of correcting the
  roadmap at source when it amended Phase 3's criterion 1.
- **Criterion 5** — bring it in line with what ingest run 3 already narrowed GAP-07 to: the
  duplicate checkpoint config in `citadel.rs` resolved and the single-Paladin Formation
  executing. The "one `BattalionResult`" clause was dropped from the requirement because it was
  already true in shipped code, and ADR-0002 records that type as conforming with no code change.
- Add a dated one-line note under the criteria recording that they were amended by Phase 2 and
  why, so the change is visible rather than silent.

Leave every other phase's ROADMAP section untouched. Make these edits as scoped replacements of
the Phase 2 section only — never rewrite the file wholesale.
  </action>
  <acceptance_criteria>
    - `.planning/ledgers/milestone-01.md` contains a dated "Phase 2 amendments" note naming D-02 as the convention.
    - Every verdict token in the ledger is one of the five legend classes: `grep -oE 'satisfied|present, unproven|genuinely outstanding|deferred with reason|superseded by shipped code' .planning/ledgers/milestone-01.md | sort -u` yields only members of that set.
    - The Epic 8 task 7.13 row cites plan 02-05's test name and its command.
    - The Epic 9 rows for tasks 13.4, 13.5 and 13.6 each name a passing exerciser and record the correction to the "never written" finding.
    - The Epic 4 cancellation row cites `0007` as its deferring authority and names a forward owner.
    - The Epic 2 rows cite ADR-0006 for task 11.5 and the review document's path for task 11.6.
    - The ledger's § "Outstanding item reconciliation" counts match the amended rows and the arithmetic is shown.
    - All three planning-input corrections appear in the ledger.
    - `.planning/ROADMAP.md` Phase 2 criterion 1 no longer asserts that the named Commander test fails: `grep -c 'which fails today' .planning/ROADMAP.md` returns 0.
    - The ROADMAP's other phase sections are unchanged: `git diff -U0 .planning/ROADMAP.md` shows changed hunks only within the Phase 2 section and its Plans line.
    - `git diff --name-only` lists no file outside `.planning/`.
    - `cargo test --workspace` still exits 0 (this task changes no code; the check confirms nothing was disturbed).
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Phase 2 amendments' .planning/ledgers/milestone-01.md && [ "$(grep -c 'which fails today' .planning/ROADMAP.md)" = "0" ] && grep -q '0007' .planning/ledgers/milestone-01.md && cargo test --workspace</automated>
  </verify>
  <done>The ledger is amended in place with dated, command-cited verdicts for every row this phase touched or contradicted; the reconciliation arithmetic matches; the three planning-input corrections are recorded; and ROADMAP Phase 2's criteria state outcomes without stale premises.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan SUMMARYs → ledger rows | Verdicts written by seven sibling plans are transcribed into a durable record that four later ground-truth phases inherit as convention and precedent |
| ADR → future phases | ADR-0007 becomes a top-precedence artifact; a mis-stated conformance line would instruct a later phase to change code this decision explicitly does not touch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-35 | Repudiation | A ledger row amended without traceable evidence | high | mitigate | Every amendment carries the new verdict, the command or `file:line` that produced it, and the date; the evidence originates in the sibling plans' SUMMARYs and the measured baseline record, both of which name their commit |
| T-02-36 | Tampering | Editing a shared record wholesale and destroying rows outside the intended change | high | mitigate | Amendments are scoped replacements of named rows and named sections; the ROADMAP edit is confined to the Phase 2 section, with an acceptance criterion requiring `git diff -U0` to show hunks only there. No file is rewritten wholesale |
| T-02-37 | Spoofing | A deferral recorded with no owner, functionally erasing the work | high | mitigate | D-03's rule is enforced by acceptance criterion: every `deferred with reason` row names the deferring authority and a forward owner, and ADR-0007 names both an owner and a prerequisite |
| T-02-38 | Tampering | An ADR whose conformance line reads as pending work | medium | mitigate | `## Code Conformance` is required to state `conforms` and to say explicitly that no code change is mandated; the ADR is required to parse under the same parser Phase 1 gated its six siblings on |
| T-02-39 | Information Disclosure | Sweep output or ledger citations carrying environment content | low | mitigate | The sweep records file paths, target names and test-list output only; no environment dump and no configuration values are pasted |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install and no dependency change. `cargo test --workspace --list` and `cargo test --workspace` resolve only the already-locked workspace graph |
</threat_model>

<verification>
- The sweep record exists, covers every `tests/` directory plus `benches/` and `examples/`, and every finding has a forward owner.
- ADR-0007 exists with all seven sections, states `conforms`, names an owner and a prerequisite, and parses under the project's ADR parser.
- The ledger uses only the five legend verdict classes and carries a dated amendment note.
- ROADMAP Phase 2's criteria no longer carry the stale premise, and no other phase section changed.
- `cargo test --workspace` still green.
- `git diff --name-only` lists nothing outside `.planning/`.
</verification>

<success_criteria>
- Every Milestone-1 requirement this phase touched is either recorded working-and-tested with a named exerciser, or recorded deferred with a reason, an authority and an owner.
- The ledger is one document a reader can trust, amended in place, with arithmetic that checks out.
- ADR-0007 exists and parses.
- The D-12 sweep reports without silently repairing, and its findings carry owners.
- The ROADMAP no longer asserts a premise the tree contradicts.
</success_criteria>

## Artifacts this phase produces

New symbols, files and test names created across all Phase 2 plans (excluded from drift
verification because they do not exist in the tree yet):

- **Fields/types:** `temperature_range` on `ProviderCapabilities`
  (`crates/paladin-ports/src/output/llm_port.rs`); `BattalionCheckpointConfig` (the renamed
  citadel checkpoint config, `crates/paladin-core/src/platform/container/citadel.rs`).
- **New files:** `tests/cli/helpers.rs`;
  `tests/integration/battalion_herald_end_to_end_test.rs`;
  `tests/integration/provider_switching_test.rs`;
  `.planning/phases/02-functional-gap-closure/02-test-baseline.md`;
  `.planning/phases/02-functional-gap-closure/02-garrison-prd-review.md`;
  `.planning/phases/02-functional-gap-closure/02-test-wiring-sweep.md`;
  `.planning/decisions/0007-battalion-cancellation-deferral.md`.
- **New module declarations:** `mod helpers;` and five previously-commented `mod` lines in
  `tests/cli/mod.rs`; `pub mod llm;` in `tests/unit/mod.rs`;
  `pub mod provider_switching_test;` and `pub mod battalion_herald_end_to_end_test;` in
  `tests/integration/mod.rs`.
- **New test names:** `test_provider_switch_preserves_request_contract`,
  `test_deepseek_temperature_range_accepts_two_point_zero`,
  `test_temperature_rejected_above_provider_max`,
  `test_provider_capabilities_default_temperature_range_is_none`,
  `test_every_adapter_declares_a_temperature_range`,
  `test_capabilities_tool_calling_matches_request_surface`,
  `test_formation_accepts_single_paladin`, `test_formation_rejects_zero_paladins`,
  `test_formation_aggregates_per_paladin_times_and_tokens`,
  `test_formation_records_node_errors_on_continue_on_error`,
  `test_json_herald_battalion_includes_strategy_and_total_tokens`,
  `test_markdown_herald_battalion_includes_strategy_and_total_tokens`,
  `test_table_herald_renders_actual_paladin_names`,
  `test_table_herald_renders_empty_paladin_results`,
  `test_table_herald_renders_multibyte_paladin_name`,
  `test_formation_result_through_json_markdown_table_heralds`,
  `test_formation_partial_results_through_all_three_heralds`.
- **Modified test semantics:** `test_formation_validation_minimum_paladins` flips; the OpenAI and
  Anthropic capability tests flip their tool-calling assertion; `test_format_battalion_result` in
  `table_herald.rs` is replaced by three content-asserting tests.
- **Reactivated (not new) test source:** the five modules under `tests/cli/`, and the 25 test
  functions under `tests/unit/llm/`.

<output>
Create `.planning/phases/02-functional-gap-closure/02-09-SUMMARY.md` when done. List every ledger
row amended, with its old verdict, its new verdict and the evidence cited — this is the record
Phases 5, 7, 10 and 13 inherit as the convention for their own ledgers.
</output>
