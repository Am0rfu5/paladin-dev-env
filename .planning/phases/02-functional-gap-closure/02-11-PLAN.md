---
phase: 02-functional-gap-closure
plan: 11
type: execute
wave: 6
gap_closure: true
depends_on: ["02-10"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [GAP-01, GAP-02, GAP-03, GAP-04, GAP-05, GAP-06, GAP-07]

must_haves:
  truths:
    - "All seven GAP requirement checkboxes in `.planning/REQUIREMENTS.md` read `- [x]`, and all seven rows in its bottom-of-file Traceability table read `Complete` — the requirements document matches the state the ledger, the ROADMAP and nine plan SUMMARYs already record, restoring the traceability precedent Phase 1 set when it checked every RECON item on completion."
    - "GAP-03's checkbox is re-checked only because the reason it was reverted is gone: commit `9e5ec04` reverted it to `- [ ]` / `Gaps Found` when 02-VERIFICATION.md found the Table Herald panic, and plan 02-10 closed that panic with tests that demonstrably failed against the previous code."
    - "A reader can get from any checked GAP box to the evidence that closed it without leaving `.planning/` — the flip carries a dated provenance note naming, per requirement, the closing plan SUMMARY and the verification-report row that proved it."
    - "Nothing outside the GAP-01..GAP-07 block changed: the other 71 forward requirement entries, both as-shipped ledgers, and every non-GAP Traceability row are byte-identical to their pre-edit state in a 4,038-line document."
  prohibitions:
    - statement: "MUST NOT check a requirement box or write `Complete` into a Traceability row without naming, in the same change, the plan SUMMARY and the verification-report row that closed it. An unevidenced checkbox is precisely the failure class this milestone exists to end — this corpus's own repeated finding is that its checkbox evidence has been wrong in both directions."
    - statement: "MUST NOT record as evidence any command, test result or verdict that was not actually produced. Every citation must resolve to a file that exists in `.planning/` on this commit."
    - statement: "MUST NOT rewrite `.planning/REQUIREMENTS.md` with a whole-file write. The file is 4,038 lines and holds two as-shipped ledgers plus 78 forward requirements across 16 phases; a regenerated file would silently destroy content this plan never inspected. Every edit is a scoped replacement anchored on literal text."
    - statement: "MUST NOT flip GAP-03 if plan 02-10's tests are not present and passing on the current commit. The dependency is the whole reason this plan is sequenced after it; flipping on the strength of the plan file existing rather than its tests passing repeats the exact error that produced this gap."
    - statement: "MUST NOT edit, soften or re-scope a requirement's descriptive text to make it easier to call complete. The only permitted changes to existing lines are the checkbox character and the Traceability status cell."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "seven checked GAP requirements, seven `Complete` Traceability rows, and a dated provenance note mapping each requirement to its closing evidence"
      contains: "GAP-07"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ledgers/milestone-01.md"
      via: "the provenance note cites the ledger rows plan 02-09 amended in place per D-02, so the requirements document and the ledger tell the same story"
      pattern: "milestone-01"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/02-functional-gap-closure/02-VERIFICATION.md"
      via: "each flip cites the verification report's GAP Requirement Coverage row that proved it"
      pattern: "02-VERIFICATION"
---

<objective>
Close verification gap 2: `.planning/REQUIREMENTS.md` still shows all seven GAP requirements
unchecked, with six marked `Pending` and GAP-03 marked `Gaps Found` in the Traceability table, even
though every closing plan is complete, `.planning/ledgers/milestone-01.md` records all seven
satisfied or deferred-with-reason, and `.planning/ROADMAP.md`'s Phase 2 entry is checked complete.

Purpose: this is documentation and traceability, not shipped behaviour — the verification report
graded it WARNING and confirmed the underlying capabilities work. But it breaks the precedent Phase
1 set for its own RECON items (every one checked `[x]` on completion), and a requirements document
that disagrees with the ledger is exactly the "believed working, actually not" ambiguity this
milestone exists to end. Running in the opposite direction is just as corrosive: a checkbox that
says done while the ledger says pending teaches a reader to distrust both.

Note the file has moved since 02-VERIFICATION.md was written. That report says only GAP-03 was
checked (commit `a5f8c27`); commit `9e5ec04` then reverted it after the report found the Table
Herald panic. **All seven are unchecked on the current commit**, and the Traceability table reads
six `Pending` plus one `Gaps Found`. Plan against the file as it stands, not as the report
describes it.

Output: seven checked boxes, seven `Complete` rows, and a dated provenance note that makes each
flip auditable — with nothing else in a 4,038-line document touched.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-functional-gap-closure/02-VERIFICATION.md
@.planning/phases/02-functional-gap-closure/02-CONTEXT.md
</context>

<assumption_delta_decision>
- **Primary noun:** `fallback`
- **Decision:** `no-change`
- **Rationale:** carried from plan 02-10 and unchanged. This plan edits requirement checkboxes and a
  status table; it introduces no domain noun at all, let alone a change to a core identity model.
</assumption_delta_decision>

<reversibility>
| Decision | Rating | Rationale |
|----------|--------|-----------|
| Marking GAP-01..GAP-07 complete in `REQUIREMENTS.md` | **reversible** | Demonstrated reversible in this very phase: commit `9e5ec04` reverted GAP-03 from `Complete` to `Gaps Found` in a two-line scoped diff when verification found the blocker. Undo is the same scoped edit in the opposite direction, with no downstream artifact regenerated from these values and no migration. |

No decision in this plan rates `one-way` or `costly`, so no `checkpoint:decision` is inserted.
</reversibility>

<tasks>

<task type="auto">
  <name>Task 1: Check off GAP-01..GAP-07 and set their Traceability rows to Complete, with cited evidence</name>
  <precondition>
    Plan 02-10's fix is landed and green on the current commit: `cargo test -p paladin-herald` exits
    0, and `grep -c 'fn test_table_herald_renders_overlong_multibyte_paladin_name' crates/paladin-herald/src/table_herald.rs`
    returns 1. If either check fails, halt — GAP-03 must not be flipped, and this plan has no
    standalone value with GAP-03 held back.
  </precondition>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 195-235 — the `### Gap closure (GAP)` section holding the
      seven requirement entries being edited, and the `### Quality gates (QUAL)` header at line 236
      that bounds it. **Read this range before editing** so you are editing the entries as they
      actually read, not as this plan paraphrases them.
    - `.planning/REQUIREMENTS.md` lines 3789-3812 — the `## Traceability` table header, the eight
      `Complete` RECON rows that establish the exact cell formatting to match, and the seven GAP
      rows being edited.
    - `.planning/phases/02-functional-gap-closure/02-VERIFICATION.md` — the "GAP Requirement
      Coverage" table, which is the per-requirement evidence source for this task, and the "Gaps
      Summary" section's item 2, which is this task's specification.
    - `.planning/phases/02-functional-gap-closure/02-10-SUMMARY.md` — the closing evidence for
      GAP-03's remaining half. Read the recorded pre-fix panic messages; if that section is absent,
      the precondition above has not really been met.
    - `.planning/ledgers/milestone-01.md` — the "Phase 2 amendments" note near the top and the
      per-Epic rows plan 02-09 amended in place. This is the record the requirements document must
      agree with, and the source of the D-19 evidence bar (`satisfied` needs a `file:line` **plus**
      a named passing exerciser).
    - `.planning/phases/02-functional-gap-closure/02-09-SUMMARY.md` — the record-closing plan's own
      account of which ledger rows it amended, so the provenance note cites rows that exist.
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Edit `.planning/REQUIREMENTS.md` with scoped replacements only. Never open this file with a
whole-file write — it is 4,038 lines and contains two as-shipped ledgers plus 78 forward
requirements, almost none of which this plan has read.

Make the seven checkbox edits first, before adding any new lines, so the line numbers below stay
valid. In each case the ONLY character that changes is the checkbox contents; the requirement text,
its `*Derives:*` provenance line and its indentation stay byte-identical.

- Line 197, `- [ ] **GAP-01**` becomes `- [x] **GAP-01**`
- Line 204, `- [ ] **GAP-02**` becomes `- [x] **GAP-02**`
- Line 209, `- [ ] **GAP-03**` becomes `- [x] **GAP-03**`
- Line 214, `- [ ] **GAP-04**` becomes `- [x] **GAP-04**`
- Line 220, `- [ ] **GAP-05**` becomes `- [x] **GAP-05**`
- Line 224, `- [ ] **GAP-06**` becomes `- [x] **GAP-06**`
- Line 228, `- [ ] **GAP-07**` becomes `- [x] **GAP-07**`

Anchor each replacement on the literal `- [ ] **GAP-0N**` prefix together with enough following
text to be unique; treat the line numbers as a cross-check on the anchor, not as the anchor itself.

Then edit the seven Traceability rows near lines 3803-3809. Six of them currently carry the
pending status in their third cell, and GAP-03 carries the gaps-found status that commit `9e5ec04`
wrote there. All seven become `| GAP-0N | Phase 2 | Complete |`, matching the existing RECON rows'
cell formatting exactly — same column widths, same single spaces around the pipes. Do not touch the
QUAL, REL, VERIFY, CLOSE, ARCH or any later rows.

Then add the provenance note. Place it at the END of the `### Gap closure (GAP)` subsection —
after GAP-07's `*Derives:*` line at line 234 and before the blank line preceding the
`### Quality gates (QUAL)` header at line 236 — so it does not shift any of the seven checkbox
lines. Write it as a short italicised block in the same voice as the existing amendment notes in
`ROADMAP.md`'s Phase 2 section and `milestone-01.md`'s "Phase 2 amendments" note. It must carry:

- The date and the authority: closed 2026-08-01 by Phase 2, plans 02-01 through 02-11.
- One line per requirement, naming that requirement's closing plan or plans and the artifact that
  proves it. Take these from 02-VERIFICATION.md's "GAP Requirement Coverage" table rather than
  inventing them: GAP-01 from plans 02-01/02-03/02-06/02-07; GAP-02 from 02-01/02-06/02-07;
  GAP-03 from 02-04/02-05 **and 02-10**, which closed the Table Herald multi-byte truncation panic
  that 02-VERIFICATION.md graded a blocker; GAP-04 from 02-01; GAP-05 from 02-01; GAP-06 from
  02-08; GAP-07 from 02-02/02-03/02-09.
- An explicit statement that GAP-03 was checked at commit `a5f8c27`, reverted at commit `9e5ec04`
  when 02-VERIFICATION.md returned `gaps_found`, and is re-checked here only because plan 02-10
  closed the defect that caused the revert. Naming the round trip is the point — a reader who finds
  the revert in the history must be able to see why the box is checked again.
- A pointer to the two durable records that carry the full evidence rather than restating it:
  `.planning/phases/02-functional-gap-closure/02-VERIFICATION.md` and
  `.planning/ledgers/milestone-01.md`.

Keep the note under about twenty lines. It is a signpost to the evidence, not a second copy of it —
`milestone-01.md` is the ledger of record per D-02 and must not be duplicated here.

Do not edit `.planning/ROADMAP.md`, `.planning/ledgers/milestone-01.md`, `.planning/STATE.md` or any
file other than `.planning/REQUIREMENTS.md` in this task. The ROADMAP's Phase 2 entry is already
checked complete and its criteria were already corrected by plan 02-09; the ledger already records
all seven. This plan closes the one document that disagreed with them.
  </action>
  <acceptance_criteria>
    - All seven GAP boxes are checked: `grep -c '^- \[x\] \*\*GAP-0' .planning/REQUIREMENTS.md` returns `7`.
    - None remain unchecked: `grep -c '^- \[ \] \*\*GAP-0' .planning/REQUIREMENTS.md` returns `0`.
    - All seven Traceability rows read Complete: `grep -c '^| GAP-0. | Phase 2 | Complete |' .planning/REQUIREMENTS.md` returns `7`.
    - No GAP row remains Pending: `grep -c '^| GAP-0. | Phase 2 | Pending |' .planning/REQUIREMENTS.md` returns `0`.
    - No GAP Traceability row carries any non-complete status: `grep -E '^\| GAP-0' .planning/REQUIREMENTS.md | grep -cv 'Complete'` returns `0`. It returns `7` today. This gate is deliberately scoped to Traceability rows rather than matching a status word anywhere in the file, so the provenance note may name the prior status in prose without invalidating it — and it catches any non-complete status, not only the two present today.
    - Scope containment, whole-document counts: `grep -c '^- \[x\] \*\*' .planning/REQUIREMENTS.md` returns `15` (was 8 — exactly the seven GAP items added, no others), and `grep -c '^- \[ \] \*\*' .planning/REQUIREMENTS.md` returns `71` (was 78).
    - Scope containment, Traceability table: `grep -cE '^\| [A-Z]+-[0-9]+ \| Phase [0-9]+ \| Pending \|' .planning/REQUIREMENTS.md` returns `71` (was 77 — exactly the six GAP `Pending` rows removed, no others).
    - Scope containment, diff shape: before committing, `git diff --numstat -- .planning/REQUIREMENTS.md` shows exactly `14` deleted lines — seven checkbox lines plus seven table rows, and nothing else. Additions are those same 14 plus the provenance note. After committing, the equivalent check is `git show --numstat HEAD -- .planning/REQUIREMENTS.md`.
    - Scope containment, files: `git diff --name-only` (pre-commit) lists `.planning/REQUIREMENTS.md` and no other path.
    - The provenance note is present and dated: `grep -c '2026-08-01' .planning/REQUIREMENTS.md` returns `1` or more, and the note names `02-10` — `grep -c '02-10' .planning/REQUIREMENTS.md` returns `1` or more.
    - The note points at both durable records: `grep -c '02-VERIFICATION' .planning/REQUIREMENTS.md` returns `1` or more and `grep -c 'milestone-01' .planning/REQUIREMENTS.md` returns `1` or more.
    - The note records the revert round trip: `grep -c '9e5ec04' .planning/REQUIREMENTS.md` returns `1` or more.
    - Requirement text is unedited: `grep -c 'Derives: Epic 8 task 7.0 and 7.13' .planning/REQUIREMENTS.md` returns `1`, confirming GAP-03's provenance line survived the checkbox edit intact.
    - The code fix this depends on is still green: `cargo test -p paladin-herald` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -c '^- \[x\] \*\*GAP-0' .planning/REQUIREMENTS.md)" = "7" ] && [ "$(grep -c '^- \[ \] \*\*GAP-0' .planning/REQUIREMENTS.md)" = "0" ] && [ "$(grep -c '^| GAP-0. | Phase 2 | Complete |' .planning/REQUIREMENTS.md)" = "7" ] && [ "$(grep -E '^\| GAP-0' .planning/REQUIREMENTS.md | grep -cv 'Complete')" = "0" ] && [ "$(grep -c '^- \[x\] \*\*' .planning/REQUIREMENTS.md)" = "15" ] && [ "$(grep -c '^- \[ \] \*\*' .planning/REQUIREMENTS.md)" = "71" ] && cargo test -p paladin-herald</automated>
  </verify>
  <done>`.planning/REQUIREMENTS.md` records all seven GAP requirements complete, in both the checkbox list and the Traceability table, with a dated provenance note that names each requirement's closing plan and explains GAP-03's check → revert → re-check round trip — and exactly 14 pre-existing lines changed in a 4,038-line file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan execution → requirements record of truth | An automated agent writes completion status into the document four later phases and a milestone audit read as the authoritative forward-requirement state |
| Scoped edit → a 4,038-line multi-ledger document | A single write operation has the reach to destroy two as-shipped ledgers and 78 forward requirements the editor never read |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-45 | Repudiation | A requirement marked complete with no traceable evidence | high | mitigate | Every flip carries a dated provenance note naming the closing plan and the verification-report row, and pointing at `02-VERIFICATION.md` and `milestone-01.md`. Acceptance criteria assert the note's presence, its date, and its citation of `02-10` and commit `9e5ec04`. This is the same control plan 02-09 applied to ledger amendments (T-02-35) |
| T-02-46 | Tampering | A whole-file rewrite of `.planning/REQUIREMENTS.md` silently destroying content outside the edited block | high | mitigate | Scoped, literal-anchored replacements only, explicitly prohibited from whole-file writes. Pinned by four independent containment gates: exactly 14 deleted lines in the diff, whole-document checked-bullet count moving 8 → 15, unchecked-bullet count 78 → 71, and table `Pending` count 77 → 71. Any collateral edit breaks at least one |
| T-02-47 | Spoofing | Citing a plan SUMMARY, ledger row or commit that does not exist, making an unevidenced flip look evidenced | high | mitigate | Every citation named in the action is drawn from `02-VERIFICATION.md`'s coverage table or from files listed in `<read_first>`; the `git show` on commit `9e5ec04` and the `02-10-SUMMARY.md` read in `<read_first>` both resolve on this commit. The prohibition forbids recording any result not actually produced |
| T-02-48 | Elevation of Privilege | Marking GAP-03 complete while the defect that caused its revert is still live | high | mitigate | A `<precondition>` halts the task unless `cargo test -p paladin-herald` exits 0 and plan 02-10's named test exists, plus `depends_on: ["02-10"]` and wave 6 sequencing. The verify gate re-runs the test suite after the edit |
| T-02-49 | Information Disclosure | Secrets or environment content leaking into a planning document | low | accept | This task writes only requirement IDs, plan numbers, file paths, a date and two commit SHAs. No command output is pasted, no environment is dumped, no configuration values are copied |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install, no dependency change, and no source file touched. The only build command run is the pre-existing `cargo test -p paladin-herald` gate |
</threat_model>

<verification>
- The seven checkbox greps and the seven Traceability greps all return their asserted counts.
- The three whole-document containment counts (15 checked, 71 unchecked, 71 `Pending`) hold, proving nothing outside the GAP block moved.
- `git diff --numstat` shows exactly 14 deleted lines in exactly one file.
- The provenance note names each requirement's closing plan, names `02-10`, and explains GAP-03's check → revert → re-check round trip citing commit `9e5ec04`.
- `cargo test -p paladin-herald` still exits 0, so the evidence GAP-03's flip rests on is green at the commit that flips it.
</verification>

<success_criteria>
- All seven GAP requirements read complete in both the checkbox list and the Traceability table.
- Each flip is traceable to its closing plan and to the verification-report row that proved it, without leaving `.planning/`.
- GAP-03's re-check is explained rather than silently reapplied.
- Exactly 14 pre-existing lines changed in a 4,038-line document, and no other file was touched.
- The requirements document, the ledger and the ROADMAP now tell the same story about Phase 2.
</success_criteria>

## Artifacts this phase produces

- **Modified file:** `.planning/REQUIREMENTS.md` — seven checkbox characters (lines 197, 204, 209,
  214, 220, 224, 228), seven Traceability status cells (lines ~3803-3809), and one new dated
  provenance note appended to the end of the `### Gap closure (GAP)` subsection.
- **New symbols:** none. This plan creates no functions, types, tests or source files.
- **New files:** none beyond this plan's own `02-11-SUMMARY.md`.
- **Deliberately unchanged:** `.planning/ROADMAP.md` (Phase 2 already checked complete, criteria
  already corrected by plan 02-09), `.planning/ledgers/milestone-01.md` (already records all seven
  per D-02), and every source file in the workspace.

<output>
Create `.planning/phases/02-functional-gap-closure/02-11-SUMMARY.md` when done. Record the exact
before/after values of all three whole-document containment counts (checked bullets, unchecked
bullets, `Pending` Traceability rows) and the `git diff --numstat` line counts, so a reader can
confirm the edit's blast radius was 14 lines rather than take it on trust.
</output>
