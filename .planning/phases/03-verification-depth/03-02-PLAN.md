---
phase: 03-verification-depth
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/helpers/mock_paladin_port.rs
  - tests/helpers/mod.rs
  - tests/integration/commander_error_paths_test.rs
  - tests/integration/mod.rs
  - crates/paladin-battalion/src/commander.rs
autonomous: true
requirements: [QUAL-04]

must_haves:
  truths:
    - "`FaultyPaladinPort` exists in `tests/helpers/mock_paladin_port.rs`, implements `PaladinPort`, is `Send + Sync`, and supports fail-always, fail-a-named-Paladin, fail-until-the-Nth-attempt with an invocation counter, and a controllable per-execution delay."
    - "`tests/integration/commander_error_paths_test.rs` exists, is reachable through `tests/integration/mod.rs`, and its four tests run and pass under `cargo test --offline --test lib -- commander_error_paths` with none of them skipped."
    - "The four empty stub tests are gone from `crates/paladin-battalion/src/commander.rs` and that file carries zero skip attributes."
    - "Under `ErrorStrategy::ContinueOnError` with two Paladins failing, the returned result carries two distinct error entries — the failures are collected separately and never merged into one aggregate error, and the success/failure metadata counts match the number of distinct entries."
    - "`FaultyPaladinPort`'s execution log records Paladin executions in invocation order, and the fail-fast test asserts on that ordered log that the Paladin following the failing one never appears — the assertion is on presence and absence within an ordered log, not on an unordered set."
    - "`FaultyPaladinPort`'s invocation counter and execution log use `Arc<Mutex<_>>` and never `Rc` or `RefCell`, the type satisfies `Send + Sync`, and the retry-count assertion reads an exact integer from that counter after a concurrent execution rather than accepting a range."
    - "`tests/helpers/mock_paladin_port.rs` has a real consumer for the first time — the file is no longer imported only by its own barrel."
  artifacts:
    - path: "tests/helpers/mock_paladin_port.rs"
      provides: "FaultyPaladinPort — the configurable failing PaladinPort mock (D-09/D-10)"
      contains: "FaultyPaladinPort"
    - path: "tests/integration/commander_error_paths_test.rs"
      provides: "The four relocated Commander error-path tests with real bodies"
      contains: "test_fail_fast_stops_on_first_error"
    - path: "tests/integration/mod.rs"
      provides: "Barrel declaration reaching the new test module"
      contains: "pub mod commander_error_paths_test;"
  key_links:
    - from: "tests/integration/commander_error_paths_test.rs"
      to: "tests/helpers/mock_paladin_port.rs"
      via: "imports FaultyPaladinPort through the tests/lib.rs helpers barrel"
      pattern: "FaultyPaladinPort"
    - from: "tests/integration/mod.rs"
      to: "tests/integration/commander_error_paths_test.rs"
      via: "pub mod declaration — the entire wiring; no Cargo.toml [[test]] entry"
      pattern: "pub mod commander_error_paths_test;"
---

<objective>
Give the four `#[ignore]`d, empty-bodied Commander error-path tests real bodies, in the location
their own TODO comments asked for, driven by a shared failing mock.

Purpose: QUAL-04 and ROADMAP criterion 3 require Commander failure behaviour proven by tests that
actually run — retry counts increment, partial failures are collected and returned, and a timeout
stops sibling agents. The four tests at `commander.rs:2180,2188,2196,2204` are not merely skipped;
they are three comment lines each with no code. They were never written because the in-crate mock at
`commander.rs:1570` always succeeds. This plan builds the missing capability once, in the workspace's
existing shared mock home, and relocates the tests (D-09/D-10).

Output: `FaultyPaladinPort` in `tests/helpers/`, four passing integration tests in
`tests/integration/commander_error_paths_test.rs`, and `commander.rs` free of skip attributes.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-verification-depth/03-CONTEXT.md
@.planning/phases/03-verification-depth/03-RESEARCH.md
@.planning/phases/03-verification-depth/03-PATTERNS.md
</context>

## Artifacts this phase produces

Every symbol, file and doc section Phase 3 creates. Newly-created names are not drift.

| Artifact | Kind | Owning plan | Path |
|---|---|---|---|
| `03-coverage-measurement.md` | new file | 03-01 / 03-07 | `.planning/phases/03-verification-depth/03-coverage-measurement.md` |
| `FaultyPaladinPort` | new struct | 03-02 | `tests/helpers/mock_paladin_port.rs` |
| `FaultyPaladinPort::new`, `::fail_always`, `::fail_paladin`, `::fail_until_attempt`, `::with_delay_ms`, `::call_count`, `::execution_log` | new methods | 03-02 | `tests/helpers/mock_paladin_port.rs` |
| `commander_error_paths_test` | new test module | 03-02 | `tests/integration/commander_error_paths_test.rs` |
| `test_fail_fast_stops_on_first_error`, `test_continue_on_error_collects_all_errors`, `test_retry_then_continue_retries_failed_paladins`, `test_partial_results_returned_with_errors` | relocated test fns (same names, real bodies) | 03-02 | `tests/integration/commander_error_paths_test.rs` |
| `MCPClient::connect_streamable_http_with_timeout` | new pub async fn | 03-03 | `src/infrastructure/adapters/arsenal/mcp_protocol.rs` |
| `spawn_malformed_fixture_server`, `spawn_silent_fixture_server` | new test fns | 03-03 | `tests/integration/mcp_streamable_http_test.rs` |
| `streamable_http_rejects_expired_bearer_token`, `streamable_http_invoke_unknown_tool_maps_to_error`, `streamable_http_invoke_with_missing_message_argument_maps_to_error`, `streamable_http_malformed_tools_list_response_returns_error`, `streamable_http_handshake_timeout_returns_timeout_error` | new test fns | 03-03 | `tests/integration/mcp_streamable_http_test.rs` |
| `muster_baseline` | new example binary | 03-04 | `examples/muster_baseline.rs` |
| `## Run — <date>`, `### Memory-per-Paladin`, `### Startup Time`, `### P50 / P95 / P99 Derivation` | new doc sections | 03-04 | `docs/src/appendix/performance-baseline.md` |
| `queue_key`, `priority_queue_key`, `queue_meta_key`, `processing_key`, `completed_key`, `failed_key`, `serialize_item`, `deserialize_item` | refactored off `&self` (private) | 03-05 | `crates/paladin-storage/src/redis.rs` |
| `#[cfg(test)] mod tests` | new test modules | 03-05 / 03-06 | `crates/paladin-storage/src/redis.rs`, `crates/paladin-ports/src/output/file_storage_port.rs`, `crates/paladin-ports/src/output/arsenal_port.rs`, `crates/paladin-llm/src/error.rs` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build FaultyPaladinPort in the shared helpers home and export it from the barrel</name>
  <files>tests/helpers/mock_paladin_port.rs, tests/helpers/mod.rs</files>
  <read_first>
    - `tests/helpers/mock_paladin_port.rs` (full file, 62 lines) — the existing `MockPaladinPort`, its import set and its local `PaladinStream` alias. The new type is added alongside it, not in place of it.
    - `tests/helpers/mod.rs` — the barrel; note the existing `pub use mock_paladin_port::MockPaladinPort;` line the new export sits beside.
    - `crates/paladin-ports/src/output/paladin_port.rs` around line 631 — the authoritative `PaladinPort` trait signature (`execute`, `execute_stream`, `validate`) and the `PaladinResult` / `PaladinStream` / `StopReason` types the impl must satisfy. Read this before writing the impl; the shape differs from the one the existing `MockPaladinPort` wraps.
    - `crates/paladin-battalion/src/formation_service.rs:379-458` — the `call_count: Arc<Mutex<usize>>` + `fail_until_attempt: Option<usize>` retry-counter idiom to copy.
    - `crates/paladin-battalion/src/phalanx_service.rs:478-550` — the `fail_paladin_names: Arc<Mutex<Vec<String>>>` + `delay_ms: u64` idiom to copy.
    - `tests/integration/commander_integration_tests.rs:20-100` — `IntegrationMockPaladinPort`'s `execution_log: Arc<Mutex<Vec<String>>>` and its `get_execution_log()` accessor, the pattern that makes "Paladin-3 never ran" assertable.
    - `tests/helpers/mock_llm_adapter.rs:16-30,62-78` — this workspace's interior-mutability and invocation-tracking idiom.
  </read_first>
  <behavior>
    - `FaultyPaladinPort::new()` with no configuration succeeds on every execute and records each execution in its log.
    - `.fail_always()` makes every execute return `PaladinError::ExecutionError`.
    - `.fail_paladin("Paladin-2")` fails only executions whose `paladin.node.name` equals `Paladin-2`; other names succeed.
    - `.fail_until_attempt(2)` fails the first two executions and succeeds from the third onward; `call_count()` reports the exact number of executions attempted.
    - `.with_delay_ms(N)` awaits `tokio::time::sleep` for N milliseconds inside `execute` before deciding success or failure.
    - `execution_log()` returns entries in invocation order, one per `execute` call, each naming the Paladin executed.
    - The type is `Send + Sync` — assert it in the test module with a `fn assert_send_sync<T: Send + Sync>() {}` witness call.
  </behavior>
  <action>
    Add `pub struct FaultyPaladinPort` to `tests/helpers/mock_paladin_port.rs`, alongside the existing
    `MockPaladinPort` (do not replace or rename it — D-10 requires extending this file, not adding a
    sixth parallel mock).

    Fields, all using `Arc<Mutex<_>>` for interior mutability and never `Rc` or `RefCell`:
    `call_count: Arc<Mutex<usize>>`, `execution_log: Arc<Mutex<Vec<String>>>`,
    `fail_always: bool`, `fail_paladin_names: Arc<Mutex<Vec<String>>>`,
    `fail_until_attempt: Option<usize>`, `delay_ms: u64`. Derive `Clone`.

    Builder surface, all consuming-self and chainable: `new()`, `fail_always()`,
    `fail_paladin(name: impl Into<String>)`, `fail_until_attempt(n: usize)`,
    `with_delay_ms(ms: u64)`. Accessors: `call_count() -> usize` and
    `execution_log() -> Vec<String>` (returning a clone of the inner vector).

    `#[async_trait] impl PaladinPort for FaultyPaladinPort`: in `execute`, push a log entry naming
    `paladin.node.name` and the input, increment `call_count`, drop the guard before any `.await`,
    sleep `delay_ms` milliseconds, then decide in this precedence order — `fail_until_attempt`
    (fail while the current attempt number is at or below the configured value), then `fail_always`,
    then `fail_paladin_names` membership on `paladin.node.name` — and otherwise return a
    `PaladinResult` with a deterministic output string, `stop_reason: StopReason::Completed`, and
    `..Default::default()` for the remaining fields. Failures return
    `PaladinError::ExecutionError` carrying the Paladin name and the attempt number so a test can
    assert which failure it got. Implement `execute_stream` by returning an error stating streaming
    is not supported in this mock, matching what every existing mock in this workspace does.
    Implement `validate` as `Ok(())`.

    Use the exact `PaladinError` path `commander.rs` imports — confirm it by reading
    `crates/paladin-ports/src/output/paladin_port.rs` rather than copying an import line from a
    different mock, because `tests/helpers/mock_paladin_port.rs`'s existing `MockPaladinPort` uses a
    different `PaladinResult` shape.

    Add a `#[cfg(test)] mod tests` block at the bottom of the file with a `Send + Sync` witness and
    one direct test per behaviour listed above, so the mock's own contract is proven before any
    Commander test relies on it.

    In `tests/helpers/mod.rs`, replace the existing single re-export line with
    `pub use mock_paladin_port::{FaultyPaladinPort, MockPaladinPort};`, keeping the file's existing
    grouping and ordering style. No other barrel changes.

    Medieval-military ubiquitous language is mandatory: the type is `FaultyPaladinPort`, its log
    entries and error strings speak of Paladins, and test Paladin names follow the `Paladin-N` form
    already used in `tests/integration/commander_integration_tests.rs`.
  </action>
  <verify>
    <automated>cargo test --offline --test lib -- mock_paladin_port 2>&1 | grep -E 'test result: ok\.' && cargo fmt --check && cargo clippy --offline --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub struct FaultyPaladinPort' tests/helpers/mock_paladin_port.rs` returns 1.
    - `grep -c 'pub struct MockPaladinPort' tests/helpers/mock_paladin_port.rs` returns 1 — the pre-existing type is still present.
    - `grep -q 'pub use mock_paladin_port::{FaultyPaladinPort, MockPaladinPort};' tests/helpers/mod.rs` succeeds.
    - `grep -cE 'Rc<|RefCell<' tests/helpers/mock_paladin_port.rs` returns 0.
    - The file's own `#[cfg(test)] mod tests` block contains a `Send + Sync` witness call against `FaultyPaladinPort` and it compiles.
    - `cargo test --offline --test lib -- mock_paladin_port` reports `test result: ok.` with a non-zero passed count.
    - `cargo fmt --check` exits 0 and `cargo clippy --offline --all-targets -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>`FaultyPaladinPort` compiles, is `Send + Sync`, proves all six configured behaviours in its own tests, and is exported from the helpers barrel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Relocate the four Commander error-path tests into a real integration module and retire the stubs</name>
  <files>tests/integration/commander_error_paths_test.rs, tests/integration/mod.rs, crates/paladin-battalion/src/commander.rs</files>
  <read_first>
    - `crates/paladin-battalion/src/commander.rs:2176-2208` — the four stub tests, their names, their preceding block comment, and their doc-intent comment lines. Those comment lines are the specification for the bodies you write.
    - `tests/integration/commander_integration_tests.rs` (imports at lines 1-18 and the `CommanderBuilder` construction it already uses successfully) — the exact facade import paths and builder call shape to copy, and how a sibling integration file reaches `tests/helpers/` through the `tests/lib.rs` barrel. Confirm that import form from a real sibling file before writing it.
    - `tests/integration/mod.rs` — the existing alphabetically-sorted `pub mod` list the new line joins.
    - `tests/lib.rs` lines 55-80 — the `pub mod integration;` declaration and the comment documenting why a prior `pub mod cli;` was removed.
    - `crates/paladin-battalion/src/commander.rs` around line 1570 — the in-crate always-succeeding `MockPaladinPort`; leave it untouched, it still serves the passing in-crate tests.
    - `.planning/phases/03-verification-depth/03-PATTERNS.md` §`tests/integration/commander_error_paths_test.rs` — the construction sketch and the `ErrorStrategy` / `RetryPolicy` / `BattalionConfig` surface.
  </read_first>
  <behavior>
    - `test_fail_fast_stops_on_first_error`: with `ErrorStrategy::FailFast` and a mock configured to fail `Paladin-2` out of three Paladins, `execute` returns an error and the execution log contains `Paladin-1` and `Paladin-2` but not `Paladin-3`.
    - `test_continue_on_error_collects_all_errors`: with `ErrorStrategy::ContinueOnError` and two of three Paladins configured to fail, `execute` returns a result carrying two distinct error entries, all three Paladins appear in the execution log, and the success/failure metadata counts read 1 and 2.
    - `test_retry_then_continue_retries_failed_paladins`: with `ErrorStrategy::RetryThenContinue`, a `RetryPolicy` whose attempt budget is 3, and a mock configured to fail until the 2nd attempt, `call_count()` reads the exact expected integer and the final result is a success — proving retries occurred and that the configured attempt budget is respected.
    - `test_partial_results_returned_with_errors`: with a mock failing exactly one of three Paladins, the returned result preserves the two successful Paladin outputs alongside the one failure, and the result metadata's success and failure counts match those numbers exactly.
  </behavior>
  <action>
    Create `tests/integration/commander_error_paths_test.rs` containing the four tests, keeping the
    original function names verbatim so the ledger rows citing them stay traceable:
    `test_fail_fast_stops_on_first_error`, `test_continue_on_error_collects_all_errors`,
    `test_retry_then_continue_retries_failed_paladins`, `test_partial_results_returned_with_errors`.
    Each is a `#[tokio::test]` with a real body and carries no skip attribute.

    Build each test by constructing three Paladins named `Paladin-1`, `Paladin-2`, `Paladin-3`,
    wrapping a configured `FaultyPaladinPort` in an `Arc`, building a Commander through
    `CommanderBuilder` with the `BattalionConfig` and `ErrorStrategy` the behaviour block names, and
    awaiting `execute`. Assert on the mock's `execution_log()` for ordering and non-execution, and on
    `call_count()` for the exact retry integer — never on a range and never on a sleep-based
    approximation. Where the assertion is "Paladin-3 never ran", assert that the ordered log does not
    contain an entry naming it, following the way `IntegrationMockPaladinPort::get_execution_log()`
    is already used in `tests/integration/commander_integration_tests.rs`.

    Add exactly one line to `tests/integration/mod.rs`, inserted at its correct alphabetical
    position: `pub mod commander_error_paths_test;`. Make no change to any `Cargo.toml`. A `[[test]]`
    entry for this file would compile the module tree twice and risk the `clippy::duplicate_mod`
    failure documented in `tests/lib.rs` as having already happened once with `tests/cli/`; this
    single `pub mod` line is the entire wiring, and keeping it that way also keeps the coverage
    object count stable for plan 03-07's exit measurement.

    In `crates/paladin-battalion/src/commander.rs`, delete the four stub test functions at 2176-2208
    together with the two-line block comment immediately above them that explains why they were
    skipped. Leave every other test in that module untouched, and leave the in-crate
    `MockPaladinPort` at line 1570 untouched. After the deletion the file must carry zero skip
    attributes.

    <!-- planner-discipline-allow: #[ignore] -->
  </action>
  <verify>
    <automated>cargo test --offline --test lib -- commander_error_paths 2>&1 | grep -E 'test result: ok\. 4 passed; 0 failed; 0 ignored' && grep -q 'pub mod commander_error_paths_test;' tests/integration/mod.rs && [ "$(grep -c '#\[ignore\]' crates/paladin-battalion/src/commander.rs)" -eq 0 ] && ! git diff --name-only | grep -qx 'Cargo.toml' && cargo test -p paladin-battalion --offline 2>&1 | grep -E 'test result: ok\.' && cargo fmt --check && cargo clippy --offline --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --offline --test lib -- commander_error_paths` reports `test result: ok. 4 passed; 0 failed; 0 ignored`.
    - `grep -c '#\[ignore\]' crates/paladin-battalion/src/commander.rs` returns 0.
    - `grep -c 'test_fail_fast_stops_on_first_error' crates/paladin-battalion/src/commander.rs` returns 0, and the same grep against `tests/integration/commander_error_paths_test.rs` returns 1.
    - `grep -c 'pub mod commander_error_paths_test;' tests/integration/mod.rs` returns 1.
    - `git diff --name-only` does not list `Cargo.toml` — no `[[test]]` entry was added.
    - `grep -c 'struct MockPaladinPort' crates/paladin-battalion/src/commander.rs` is unchanged from before the edit (the in-crate mock survives).
    - `cargo test -p paladin-battalion --offline` passes with zero failures.
    - `cargo fmt --check` exits 0 and `cargo clippy --offline --all-targets -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>The four tests run and pass in `tests/integration/commander_error_paths_test.rs`, `commander.rs` carries no skip attributes, and no Cargo.toml target was added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test scaffolding → library surface | `tests/helpers/` is compiled only into the test binaries; it must never become reachable from `crates/*/src` or the root library |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Elevation of Privilege | `tests/helpers/mock_paladin_port.rs` | low | accept | `tests/` is not compiled into any library or binary target; `FaultyPaladinPort` adds no `pub` product surface, no workspace crate and no feature flag — D-09 chose this siting specifically to avoid both immediately before Phase 4's REL-01 version-coherence work |
| T-03-09 | Denial of Service | `FaultyPaladinPort::with_delay_ms` | low | mitigate | The controllable delay is bounded by the value each test passes; no test uses a delay large enough to hold a test-suite slot, and retry assertions read the exact invocation counter rather than waiting on wall-clock |
| T-03-SC | Tampering | crates.io / cargo registry | high | mitigate | This plan installs no packages; every cargo command carries `--offline`. `03-RESEARCH.md` §Package Legitimacy Audit records all five relevant dependencies OK, zero ASSUMED/SUS/SLOP |
</threat_model>

<verification>
- Four Commander error-path tests exist, run, and pass — none skipped.
- `commander.rs` carries zero skip attributes.
- `FaultyPaladinPort` is the shared asset Phase 2's D-07 asked for, sited in `tests/helpers/`, exported from the barrel, and consumed by a real test.
- No `Cargo.toml` change was made — the `pub mod` line is the entire wiring (Research Pattern 2 / Pitfall 2).
- `cargo fmt --check` and `cargo clippy --offline --all-targets -- -D warnings` both pass.

## Edge coverage (spec-less probe fallback)

No SPEC.md exists; the fallback probe surfaced 26 edges. This plan authors **3** of QUAL-04's 5
edges — `adjacency` (two simultaneous failures collected separately, never merged), `ordering`
(the ordered execution log, not a set) and `concurrency` (`Send + Sync` via `Arc<Mutex<_>>`, exact
retry integer) — into `must_haves.truths` above, all resolved `covered`. QUAL-04's remaining 2
(`empty`, `idempotency`) are authored in 03-03. None backstopped, none dismissed, none dropped.

## Prohibition probe (spec-less fallback)

No bespoke prohibition survives Stage 2 precision for this plan; the phase's four kept
prohibitions are authored in 03-01 (2), 03-04 (1) and 03-08 (1). Canon-referral breadcrumb: test
mocks leaking credentials is canon security — covered by `/gsd-secure-phase`; not minted here, and
this plan's mock handles no secrets.
</verification>

<success_criteria>
- `cargo test --offline --test lib -- commander_error_paths` reports 4 passed, 0 failed, 0 ignored.
- `crates/paladin-battalion/src/commander.rs` has zero skip attributes and no stub bodies.
- `FaultyPaladinPort` exists, is `Send + Sync`, and covers all four fault modes D-09 requires.
- `tests/helpers/mock_paladin_port.rs` has a real external consumer.
- No `Cargo.toml` was modified.
</success_criteria>

<output>
Create `.planning/phases/03-verification-depth/03-02-SUMMARY.md` when done.
</output>
