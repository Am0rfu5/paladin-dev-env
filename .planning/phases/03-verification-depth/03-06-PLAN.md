---
phase: 03-verification-depth
plan: 06
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - crates/paladin-ports/src/output/file_storage_port.rs
  - crates/paladin-ports/src/output/arsenal_port.rs
  - crates/paladin-llm/src/error.rs
autonomous: true
requirements: [QUAL-02]

must_haves:
  truths:
    - "`crates/paladin-ports/src/output/file_storage_port.rs` contains a `#[cfg(test)] mod tests` that calls every default associated function of the `FileStorageUtils` trait directly through a zero-sized test implementor, so the 0% slice is exercised without instantiating any storage backend."
    - "`crates/paladin-ports/src/output/arsenal_port.rs` contains a `#[cfg(test)] mod tests` exercising the specific lines the Phase 3 entry measurement reports as uncovered, identified from that record's per-file rows rather than guessed."
    - "`crates/paladin-llm/src/error.rs` contains a `#[cfg(test)] mod tests` that gives `From<LlmProviderError> for LlmError` its first caller and asserts the mapped variant for every `LlmProviderError` variant, including the payload string where the variant carries one."
    - "All three test modules run under `cargo test -p paladin-ports --offline` and `cargo test -p paladin-llm --offline` with no network, no Docker, and no filesystem writes outside a temporary directory."
    - "`crates/paladin-ports` keeps `[lib] doctest = false`, so coverage for these files comes from `#[cfg(test)]` unit tests and never from doc examples, which ADR-0006 excludes from the measurement anyway."
    - "No `pub` item is added, removed or re-signed in any of the three files — every change is a test module plus, at most, documentation."
  artifacts:
    - path: "crates/paladin-ports/src/output/file_storage_port.rs"
      provides: "First unit tests for the FileStorageUtils default associated functions"
      contains: "#[cfg(test)]"
    - path: "crates/paladin-ports/src/output/arsenal_port.rs"
      provides: "First unit tests for the previously-uncovered lines"
      contains: "#[cfg(test)]"
    - path: "crates/paladin-llm/src/error.rs"
      provides: "First caller for the LlmProviderError to LlmError conversion"
      contains: "#[cfg(test)]"
  key_links:
    - from: "crates/paladin-llm/src/error.rs #[cfg(test)] mod tests"
      to: "impl From<LlmProviderError> for LlmError"
      via: "each test converts a constructed LlmProviderError variant and asserts the resulting LlmError variant"
      pattern: "LlmError::from"
    - from: "crates/paladin-ports/src/output/file_storage_port.rs #[cfg(test)] mod tests"
      to: "trait FileStorageUtils default bodies"
      via: "fully-qualified associated-function calls through a zero-sized test implementor"
      pattern: "FileStorageUtils"
---

<objective>
Close the three remaining first-party zero-coverage files that are genuinely closeable, and record
the disposition of the one that is not.

Purpose: the Phase 1 measurement reported five first-party files at 0.00% (D-04). `redis.rs` is
plan 03-05's. This plan takes the other four and dispositions each one explicitly rather than leaving
CONTEXT.md's discretion items for an executor to decide silently.

Output: three new `#[cfg(test)] mod tests` blocks, and a recorded deferral for the fourth.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-verification-depth/03-CONTEXT.md
@.planning/phases/03-verification-depth/03-RESEARCH.md
@.planning/phases/03-verification-depth/03-PATTERNS.md
</context>

## Recorded decisions this plan carries

CONTEXT.md left four items to Claude's discretion. All four are decided here, not passed through:

- **`crates/paladin-llm/src/error.rs` — exercised, not deleted.** The dead `LlmProviderError`
  conversion path gets its first caller (Task 2). Chosen over removal with a
  `superseded by shipped code` row because the impl is a documented, exhaustive ten-variant match
  that the crate's own module doc names as the port-boundary conversion; deleting shipped, correct,
  documented conversion code is construction-by-subtraction inside a measurement phase, and a later
  adapter that returns `LlmProviderError` would have to rewrite it. A test is cheaper, non-destructive,
  and closes the 0% entry outright.
- **`crates/paladin-ports/src/output/file_storage_port.rs` — a real gap, and closeable.** The 0%
  slice is the `FileStorageUtils` trait's default, no-`&self` associated functions
  (`file_storage_port.rs:1347-1447`), which research confirmed are reachable with fully-qualified
  calls through any implementor. This is not the "trait file with only default bodies nothing
  instantiated" artefact CONTEXT.md asked us to check for — it is genuinely uncovered logic.
- **`crates/paladin-ports/src/output/arsenal_port.rs` — closed.** Two counted lines; closing them is
  trivial once the entry measurement names which two.
- **`src/bin/paladin-server.rs` (185 lines, 0%) — deferred with a named owner: Phase 5, VERIFY-05.**
  Chosen over closing it here because closing it means extracting a testable `run()` seam from
  `#[tokio::main] async fn main()`, which is a refactor of a binary entrypoint — construction inside
  a measurement phase, and a larger change than any other item in this plan. ADR-0006 already assigns
  VERIFY-05 the coverage gap-closure work that sits above this phase's scope, which makes it the
  right owner. The `deferred with reason` ledger row naming that owner is written by plan 03-08; this
  plan writes no source change for it. Recorded here so the deferral is a decision with an owner,
  never a silent omission.

## Artifacts this phase produces

Every symbol, file and doc section Phase 3 creates. Newly-created names are not drift.

| Artifact | Kind | Owning plan | Path |
|---|---|---|---|
| `03-coverage-measurement.md` | new file | 03-01 / 03-07 | `.planning/phases/03-verification-depth/03-coverage-measurement.md` |
| `FaultyPaladinPort` + its builder/accessor methods | new struct | 03-02 | `tests/helpers/mock_paladin_port.rs` |
| `commander_error_paths_test` and its four relocated test fns | new test module | 03-02 | `tests/integration/commander_error_paths_test.rs` |
| `MCPClient::connect_streamable_http_with_timeout` | new pub async fn | 03-03 | `src/infrastructure/adapters/arsenal/mcp_protocol.rs` |
| `spawn_malformed_fixture_server`, `spawn_silent_fixture_server` and the five MCP failure-mode test fns | new test fns | 03-03 | `tests/integration/mcp_streamable_http_test.rs` |
| `muster_baseline` | new example binary | 03-04 | `examples/muster_baseline.rs` |
| `## Run — <date>`, `### Memory-per-Paladin`, `### Startup Time`, `### P50 / P95 / P99 Derivation` | new doc sections | 03-04 | `docs/src/appendix/performance-baseline.md` |
| `queue_key`, `priority_queue_key`, `queue_meta_key`, `processing_key`, `completed_key`, `failed_key`, `serialize_item`, `deserialize_item` | refactored off `&self` (private) | 03-05 | `crates/paladin-storage/src/redis.rs` |
| `#[cfg(test)] mod tests` | new test modules | 03-05 / 03-06 | `crates/paladin-storage/src/redis.rs`, `crates/paladin-ports/src/output/file_storage_port.rs`, `crates/paladin-ports/src/output/arsenal_port.rs`, `crates/paladin-llm/src/error.rs` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cover the FileStorageUtils default associated functions and the arsenal_port gap</name>
  <files>crates/paladin-ports/src/output/file_storage_port.rs, crates/paladin-ports/src/output/arsenal_port.rs</files>
  <read_first>
    - `crates/paladin-ports/src/output/file_storage_port.rs:1347-1447` — the `FileStorageUtils` trait and each of its default associated functions (`detect_content_type`, `calculate_md5`, `validate_path`, `sanitize_filename` and any siblings in that range). Read every default body before writing an assertion; the tests must assert the behaviour the bodies actually implement, not the behaviour their names suggest.
    - `crates/paladin-ports/src/output/arsenal_port.rs` (full file, 877 lines) — locate the specific uncovered lines. Cross-reference against the per-file rows in `.planning/phases/03-verification-depth/03-coverage-measurement.md` produced by plan 03-01, which names the counted and missed line counts for this file. Do not guess which two lines are missed.
    - `crates/paladin-ports/Cargo.toml` — the `[lib] doctest = false` setting at line 18, which is why coverage for these files must come from `#[cfg(test)]` unit tests rather than doc examples.
    - `crates/paladin-storage/src/scheduler.rs:403-482` — the workspace's in-file test-module idiom (banner comment, `use super::*;`, `<function>_<condition>_<expected>` naming, explicit `match` with a `panic!` fallback arm).
    - `.planning/phases/03-verification-depth/03-RESEARCH.md` §Claude's Discretion — the confirmation that these defaults are callable as `<SomeType as FileStorageUtils>::fn_name(...)` and are not a refactor-first item.
  </read_first>
  <behavior>
    - Each `FileStorageUtils` default associated function is called at least once through a zero-sized test implementor and its return value asserted against a literal expectation.
    - `detect_content_type` returns the documented type for a known extension and the documented fallback for an unknown or absent extension.
    - `validate_path` accepts a plain relative path and rejects the traversal and absolute-path forms its body actually guards against, with the rejection asserted on the error value rather than on `is_err()` alone.
    - `sanitize_filename` leaves an already-safe name unchanged and rewrites the specific characters its body handles, asserted with `assert_eq!` against complete expected strings.
    - `calculate_md5` over a fixed byte slice produces a stable digest string, asserted against a literal computed by the function itself and pinned in the test.
    - The `arsenal_port.rs` lines the entry measurement reports as missed are executed by at least one assertion.
  </behavior>
  <action>
    Add a `#[cfg(test)] mod tests` block at the bottom of
    `crates/paladin-ports/src/output/file_storage_port.rs`. Declare a private zero-sized struct in the
    test module — for example `struct TestUtils;` — and give it an empty `impl FileStorageUtils for
    TestUtils {}` so every default body is reachable, then call each default through
    `<TestUtils as FileStorageUtils>::fn_name(...)`. Write one `#[test]` per behaviour above,
    following the crate's `<function>_<condition>_<expected>` naming, with `use super::*;` at the top.

    Where a function under test touches the filesystem, confine it to a path under
    `std::env::temp_dir()` created and removed by the test, and never write outside it. Prefer
    functions that need no filesystem at all where the default body allows it.

    Then add a `#[cfg(test)] mod tests` block to `crates/paladin-ports/src/output/arsenal_port.rs`
    targeting exactly the lines the Phase 3 entry measurement records as missed for that file. Open
    that record first, read the per-file row, locate the corresponding source lines, and write the
    minimum assertions that execute them — typically a `Display`, `Default`, `From` or error-mapping
    body. State in the SUMMARY which lines were covered and how they were identified.

    Do not add, remove or re-sign any `pub` item in either file, and do not touch
    `crates/paladin-ports/Cargo.toml`. Leave `[lib] doctest = false` as it is: changing it is
    DEBT-03's scope, it would not affect this measurement (ADR-0006 excludes doctests), and it is not
    this phase's to decide.

    Test code is exempt from the no-`unwrap`/`expect` rule that binds library code; still prefer an
    explicit `match` where the test is about which error variant is returned.
  </action>
  <verify>
    <automated>timeout 300 cargo test -p paladin-ports --offline 2>&1 | tee /tmp/ports.log | grep -E 'test result: ok\.' && [ "$(grep -cE '^test output::file_storage_port::tests::' /tmp/ports.log)" -ge 4 ] && [ "$(grep -cE '^test output::arsenal_port::tests::' /tmp/ports.log)" -ge 1 ] && grep -q '#\[cfg(test)\]' crates/paladin-ports/src/output/file_storage_port.rs && grep -q '#\[cfg(test)\]' crates/paladin-ports/src/output/arsenal_port.rs && grep -q 'doctest = false' crates/paladin-ports/Cargo.toml && ! git diff --name-only | grep -q 'crates/paladin-ports/Cargo.toml' && cargo fmt --check && cargo clippy --offline --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p paladin-ports --offline` reports `test result: ok.` with zero failures.
    - The run listing shows at least four tests under `file_storage_port::tests::` and at least one under `arsenal_port::tests::`.
    - `grep -c '#\[cfg(test)\]' crates/paladin-ports/src/output/file_storage_port.rs` returns 1, and the same grep on `arsenal_port.rs` returns 1.
    - The `file_storage_port.rs` test module declares a zero-sized implementor of `FileStorageUtils` and calls each default associated function through it.
    - Every default associated function in `file_storage_port.rs:1347-1447` is named in at least one test body.
    - `git diff --name-only` does not list `crates/paladin-ports/Cargo.toml`, and `doctest = false` is still present in it.
    - No test writes to a path outside `std::env::temp_dir()`.
    - `cargo fmt --check` exits 0 and `cargo clippy --offline --all-targets -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>Both `paladin-ports` zero-coverage files have passing unit tests that execute the previously-missed lines, with the `arsenal_port.rs` targets identified from the measurement record rather than guessed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Give the LlmProviderError conversion its first caller</name>
  <files>crates/paladin-llm/src/error.rs</files>
  <read_first>
    - `crates/paladin-llm/src/error.rs` (full file) — the ten `LlmProviderError` variants, their `thiserror` display strings, and the full `impl From<LlmProviderError> for LlmError` match at line 54 onward. Every arm must be exercised, so read every arm.
    - `crates/paladin-ports/src/output/llm_port.rs` — the `LlmError` variants the conversion targets, so the assertions match on the real target variants and payload shapes.
    - `crates/paladin-storage/src/scheduler.rs:403-482` — the in-file test-module idiom to follow.
    - `.planning/phases/03-verification-depth/03-CONTEXT.md` §D-04 — the note that this file's 0% is a symptom of a dead conversion path, and that closing it is a disposition question. The disposition taken is "exercised, not deleted"; it is recorded in this plan's Recorded decisions section.
  </read_first>
  <behavior>
    - Every `LlmProviderError` variant, converted with `LlmError::from(...)`, produces the specific `LlmError` variant its match arm names — asserted one variant per test or one table-driven test with one assertion per variant, never a single catch-all.
    - For each variant carrying a payload string, the payload survives the conversion unchanged and the assertion compares the full string.
    - Each variant's `thiserror` display string renders as documented, so a later change to a message is caught.
    - The `From` impl is total: a compile-time exhaustiveness witness exists, so adding a new `LlmProviderError` variant without a matching arm fails the build rather than silently escaping the tests.
  </behavior>
  <action>
    Append a `#[cfg(test)] mod tests` block to `crates/paladin-llm/src/error.rs` with `use super::*;`
    and plain `#[test]` functions named `<variant>_converts_to_<target_variant>`.

    Cover all ten variants: `AuthenticationError`, `NetworkError`, `RateLimitExceeded`,
    `InvalidPrompt`, `ProcessingError`, `TokenLimitExceeded`, `ModelNotAvailable`, `Timeout`,
    `ConfigurationError`, and any further variant present in the file when you read it. For each,
    construct the `LlmProviderError`, convert it with `LlmError::from(...)`, and `match` the result
    with an explicit arm asserting the expected `LlmError` variant and, where a payload exists,
    `assert_eq!` on the full payload string. Use a `panic!("expected X, got {other:?}")` fallback arm
    so a wrong mapping names what it actually got.

    Add one exhaustiveness witness: a small test-only function taking `LlmProviderError` by value and
    matching every variant explicitly with no wildcard arm, so adding a variant later breaks the
    build in the test module rather than silently reducing coverage. Document in a comment that this
    is why the wildcard is deliberately absent.

    Add a short test-module doc comment recording that this conversion had no caller anywhere in the
    tree before these tests — the reason the file measured 0.00% — and that the disposition taken is
    to exercise the path rather than delete it.

    Change no `pub` item, change no variant, change no display string, and do not touch
    `crates/paladin-llm/Cargo.toml`. The library code in this file must remain free of
    `unwrap`/`expect`/`panic!`; the test module is exempt.
  </action>
  <verify>
    <automated>timeout 300 cargo test -p paladin-llm --offline error 2>&1 | tee /tmp/llmerr.log | grep -E 'test result: ok\.' && [ "$(grep -cE '^test error::tests::' /tmp/llmerr.log)" -ge 9 ] && grep -q '#\[cfg(test)\]' crates/paladin-llm/src/error.rs && grep -q 'impl From<LlmProviderError> for LlmError' crates/paladin-llm/src/error.rs && ! git diff --name-only | grep -q 'crates/paladin-llm/Cargo.toml' && cargo fmt --check && cargo clippy --offline --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p paladin-llm --offline error` reports `test result: ok.` with zero failures and at least nine tests under `error::tests::`.
    - Every `LlmProviderError` variant present in the file appears by name in at least one test body.
    - The test module contains an exhaustiveness witness function that matches every variant with no wildcard arm.
    - `grep -c '#\[cfg(test)\]' crates/paladin-llm/src/error.rs` returns 1.
    - `impl From<LlmProviderError> for LlmError` is textually unchanged — the conversion was exercised, not edited.
    - `git diff --name-only` does not list `crates/paladin-llm/Cargo.toml`.
    - `cargo fmt --check` exits 0 and `cargo clippy --offline --all-targets -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>The `From<LlmProviderError> for LlmError` conversion has its first caller, every arm is asserted, and a compile-time witness prevents a future variant from silently escaping coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted path/filename → storage utilities | `validate_path` and `sanitize_filename` are the port's input-validation surface for caller-supplied paths |
| provider error → port boundary | `LlmProviderError` values originate from third-party LLM HTTP responses and cross into `LlmError` at the port boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-15 | Tampering | `FileStorageUtils::validate_path` / `sanitize_filename` | high | mitigate | These defaults are the port's path-traversal guard and have had zero assertions until now. The new tests assert both the accept and the reject paths, asserting on the returned error value rather than on `is_err()` alone, so a regression that weakens the guard fails the build |
| T-03-16 | Information Disclosure | `LlmError` payload strings | medium | mitigate | The conversion tests assert full payload strings, which pins what crosses the port boundary; the tests construct only synthetic messages and no test constructs an error carrying a real API key or endpoint |
| T-03-17 | Tampering | test filesystem writes | low | mitigate | Any filesystem-touching test confines itself to a directory under `std::env::temp_dir()` that it creates and removes; an acceptance criterion asserts no write outside it |
| T-03-SC | Tampering | crates.io / cargo registry | high | mitigate | No package is installed and no `Cargo.toml` is edited in either crate. Every cargo command carries `--offline`; crates.io returns HTTP 403 here regardless. `03-RESEARCH.md` §Package Legitimacy Audit: all OK, zero ASSUMED/SUS/SLOP |
</threat_model>

<verification>
- Three previously-0.00% first-party files now have passing unit tests.
- The `arsenal_port.rs` targets were identified from the Phase 3 entry measurement's per-file rows, not guessed.
- `src/bin/paladin-server.rs` is deferred with Phase 5 / VERIFY-05 named as owner; the ledger row lands in plan 03-08.
- No public API changed and no `Cargo.toml` changed in either crate.
- `cargo fmt --check` and `cargo clippy --offline --all-targets -- -D warnings` both pass.

## Edge coverage (spec-less probe fallback)

No SPEC.md exists; the fallback probe surfaced 26 edges, of which QUAL-02 carries 6. Those 6 are
authored in plan 03-07, where the "no first-party source file reports 0%" claim is adjudicated against
the exit measurement. This plan carries none of the 26, and none is dropped — the `must_haves.truths`
above are goal-backward criteria for this plan's own deliverable.

## Prohibition probe (spec-less fallback)

No bespoke prohibition survives Stage 2 precision here. Canon-referral breadcrumb: path traversal is
canon security — covered by `/gsd-secure-phase` and by threat T-03-15 above; not minted as a bespoke
prohibition. The phase's four kept prohibitions are authored in 03-01 (2), 03-04 (1) and 03-08 (1).
</verification>

<success_criteria>
- `cargo test -p paladin-ports --offline` and `cargo test -p paladin-llm --offline` both pass with the new modules.
- `file_storage_port.rs`, `arsenal_port.rs` and `error.rs` each contain exactly one `#[cfg(test)] mod tests`.
- All four discretion items in this plan's scope are decided and recorded, none passed through to the executor.
- No public API or `Cargo.toml` change in either crate.
</success_criteria>

<output>
Create `.planning/phases/03-verification-depth/03-06-SUMMARY.md` when done.
</output>
