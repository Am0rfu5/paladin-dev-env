---
phase: 03-verification-depth
plan: 08
type: execute
wave: 4
depends_on: ["03-07"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/ledgers/milestone-01.md
autonomous: false
requirements: [QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05]

must_haves:
  truths:
    - "QUAL-02's offender list is amended in place in `.planning/REQUIREMENTS.md` with dated provenance citing `03-coverage-measurement.md`: the nine contradicted entries are marked corrected with both figures retained, `redis.rs` is confirmed as the one true positive, and `minio.rs` is recorded as outside ADR-0006's measured scope."
    - "QUAL-03's percentage clause is amended in place with the disposition `superseded by shipped code`/decision citing ADR-0006, and the surviving substance is restated as named passing exercisers with no substitute percentage."
    - "ROADMAP.md Phase 3 success criterion 2 is amended in place with dated provenance so it no longer names the Redis and MinIO adapters as being in the same position, and no longer names files the measurement shows are already covered."
    - "The milestone ledger row for `REQ-commander-error-strategy` is amended in place to record the four tests relocated to `tests/integration/commander_error_paths_test.rs` with their new citations, rather than a new duplicate row being added."
    - "New ledger rows exist with verdict `deferred with reason` and a named owning phase and requirement for `src/bin/paladin-server.rs` (Phase 5 / VERIFY-05), `redis.rs`'s live-server paths (Phase 15 / PIPE), `minio.rs` (VERIFY-05 / PIPE-02), the `target/` regex contamination (VERIFY-05 / PIPE-02), and the two absent benchmark suites."
    - "Every amendment retains the superseded text rather than deleting it, and carries a dated note naming the evidence file that justifies it."
    - "Every ledger verdict used is one of the five classes in the ledger's own verdict legend, and every `satisfied` verdict carries a `file:line` citation plus a named passing exerciser."
    - "A human has confirmed both recorded coverage figures and the amendment set through a blocking checkpoint before the phase closes."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "QUAL-02 and QUAL-03 amended at source with dated provenance"
      contains: "03-coverage-measurement.md"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 3 success criterion 2 amended at source"
      contains: "Amended by Phase 3"
    - path: ".planning/ledgers/milestone-01.md"
      provides: "Relocated Commander citations and the phase's deferral rows with named owners"
      contains: "commander_error_paths_test"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/03-verification-depth/03-coverage-measurement.md"
      via: "each QUAL-02 amendment note cites the measurement record as its evidence"
      pattern: "03-coverage-measurement"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/decisions/0006-coverage-gate.md"
      via: "QUAL-03's percentage clause is superseded by the ADR's single-number decision"
      pattern: "ADR-0006"
  prohibitions:
    - requirement_id: QUAL-02
      category: transparency
      statement: "MUST NOT record a gap as closed, satisfied or deferred without the D-19 evidence bar — a `file:line` citation plus a named passing exerciser — or, for a deferral, a named owning phase and requirement; an unowned deferral is not a disposition, and a row is never upgraded merely because code exists."
---

<objective>
Amend the three requirement premises this phase disproved, at their source, with dated provenance —
then have a human confirm the numbers before the phase closes.

Purpose: this phase's largest finding is that QUAL-02's named offender list is stale in nine of eleven
entries, and its second is that QUAL-03 carries a percentage clause ADR-0006 abolished. Substituting a
new list silently would leave a future reader unable to tell correction from drift, which is the exact
failure the milestone exists to end. Phase 1 amended this phase's own criterion 1 and Phase 2 amended
its own criteria 1 and 5; this plan follows the same route (D-03/D-08). The checkpoint exists because
every deliverable in this phase is a number, and numbers in this corpus have repeatedly been cited
rather than measured.

Output: amended QUAL-02 and QUAL-03, amended ROADMAP criterion 2, amended and added ledger rows, and a
recorded human confirmation.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/decisions/0006-coverage-gate.md
@.planning/phases/03-verification-depth/03-CONTEXT.md
</context>

## Recorded decision this plan carries

**Where the amendments physically land.** CONTEXT.md left this to the planner: in-place edits plus a
separate Phase 3 amendments record, or in-place with provenance notes only. **In-place with provenance
notes only** is taken, matching Phase 2's precedent. Each amended clause carries a dated note pointing
at the phase-local evidence file that justifies it — `03-coverage-measurement.md` for the coverage
claims, `03-critical-path-exercisers.md` for QUAL-03's exercisers. A separate amendments record would
fork the account into two places that can drift apart, which is the failure mode this milestone is
closing; one evidence file per measurement plus a dated pointer at the source is the smaller surface.

## Artifacts this phase produces

Every symbol, file and doc section Phase 3 creates. Newly-created names are not drift.

| Artifact | Kind | Owning plan | Path |
|---|---|---|---|
| `03-coverage-measurement.md` | new file | 03-01 / 03-07 | `.planning/phases/03-verification-depth/03-coverage-measurement.md` |
| `03-critical-path-exercisers.md` | new file | 03-07 | `.planning/phases/03-verification-depth/03-critical-path-exercisers.md` |
| `FaultyPaladinPort` + its builder/accessor methods | new struct | 03-02 | `tests/helpers/mock_paladin_port.rs` |
| `commander_error_paths_test` and its four relocated test fns | new test module | 03-02 | `tests/integration/commander_error_paths_test.rs` |
| `MCPClient::connect_streamable_http_with_timeout` | new pub async fn | 03-03 | `src/infrastructure/adapters/arsenal/mcp_protocol.rs` |
| `spawn_malformed_fixture_server`, `spawn_silent_fixture_server` and the five MCP failure-mode test fns | new test fns | 03-03 | `tests/integration/mcp_streamable_http_test.rs` |
| `muster_baseline` | new example binary | 03-04 | `examples/muster_baseline.rs` |
| `## Run — <date>`, `### Memory-per-Paladin`, `### Startup Time`, `### P50 / P95 / P99 Derivation` | new doc sections | 03-04 | `docs/src/appendix/performance-baseline.md` |
| `queue_key`, `priority_queue_key`, `queue_meta_key`, `processing_key`, `completed_key`, `failed_key`, `serialize_item`, `deserialize_item` | refactored off `&self` (private) | 03-05 | `crates/paladin-storage/src/redis.rs` |
| `#[cfg(test)] mod tests` | new test modules | 03-05 / 03-06 | `crates/paladin-storage/src/redis.rs`, `crates/paladin-ports/src/output/file_storage_port.rs`, `crates/paladin-ports/src/output/arsenal_port.rs`, `crates/paladin-llm/src/error.rs` |

<tasks>

<task type="auto">
  <name>Task 1: Amend QUAL-02 and QUAL-03 at source in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md:256-291` — QUAL-01 through QUAL-05 in full, including each one's `Derives:` provenance line, so the amendment sits in the file's own idiom.
    - `.planning/REQUIREMENTS.md:3830-3834` — the QUAL-01…QUAL-05 traceability rows, all currently `Pending`.
    - `.planning/phases/03-verification-depth/03-coverage-measurement.md` — the entry and exit measurements, the eleven-row staleness table and the QUAL-02 closure ledger. Every figure written into REQUIREMENTS.md is transcribed from here; none is retyped from CONTEXT.md.
    - `.planning/phases/03-verification-depth/03-critical-path-exercisers.md` — the verified QUAL-03 citations.
    - `.planning/decisions/0006-coverage-gate.md` — the single-number decision QUAL-03's percentage clause is superseded by.
    - `.planning/ROADMAP.md:273` — how Phase 1's amendment of criterion 1 is worded, since this file's amendments follow the same convention.
  </read_first>
  <action>
    Amend QUAL-02 in place. Retain its original offender list text — do not delete it — and append a
    dated amendment note in the file's existing italic-provenance idiom stating: that the list derives
    from the ingested pre-workspace `unit-test-improvements/COVERAGE_ANALYSIS.md`; that a measurement
    of the shipped tree recorded in `03-coverage-measurement.md` contradicts nine of its eleven
    entries, naming each contradicted file with both its claimed and its measured figure; that
    `crates/paladin-storage/src/redis.rs` is the one confirmed true positive and is now closed with its
    exit figure; and that `crates/paladin-storage/src/minio.rs` never appears in the measurement at all
    because the `s3` feature is not in the workspace default set, so it is outside ADR-0006's recorded
    scope rather than a 0% file, with VERIFY-05 / PIPE-02 named as owner. Then restate the requirement's
    surviving substance against the re-derived set, with each file carrying either a measured non-zero
    figure or a `deferred with reason` disposition and a named owning phase and requirement.

    Amend QUAL-03 in place. Retain its original text. Append a dated note recording that the clause
    "at or above 70%, up from the 67.79% baseline" is `superseded by shipped code`/decision — ADR-0006
    abolished the second number under a second scope, and ROADMAP criterion 1 was already amended by
    plan 01-12 to cite the ADR's single figure while QUAL-03 was not. State that the surviving
    substance is that each of the three named critical paths has a named, passing, non-skipped
    integration exerciser at the D-19 bar, and cite `03-critical-path-exercisers.md` as the evidence.
    Record no coverage percentage for QUAL-03 anywhere in the amendment.

    Update the traceability rows at 3830-3834 for QUAL-01 through QUAL-05 from `Pending` to the status
    this phase's evidence actually supports, using only the ledger's five verdict classes. Do not mark
    a row satisfied without a `file:line` citation plus a named passing exerciser; where the evidence
    supports only `present, unproven` or `deferred with reason`, write that instead — this phase
    shrinks that bucket where it can and records the rest honestly.

    Every amendment note carries the UTC date and names the evidence file. Change no other requirement
    in the file, and do not renumber, reorder or reflow neighbouring entries.
  </action>
  <verify>
    <automated>R=.planning/REQUIREMENTS.md; grep -q '03-coverage-measurement' "$R" && grep -q '03-critical-path-exercisers' "$R" && grep -q 'ADR-0006' "$R" && grep -q 'superseded by shipped code' "$R" && grep -q 'COVERAGE_ANALYSIS.md' "$R" && grep -q 'arsenal_execution_service.rs' "$R" && grep -q 'minio.rs' "$R" && [ "$(grep -cE '^\| QUAL-0[1-5] \| Phase 3 \| Pending \|' "$R")" -eq 0 ] && [ "$(grep -cE '^- \[[ x]\] \*\*QUAL-0[1-5]\*\*' "$R")" -eq 5 ]</automated>
  </verify>
  <acceptance_criteria>
    - All five `**QUAL-0N**` entries still exist in `.planning/REQUIREMENTS.md` — none was deleted or renumbered.
    - QUAL-02's original offender-list sentence is still present verbatim, followed by a dated amendment note naming `03-coverage-measurement.md`.
    - The amendment names all nine contradicted files and, for each, both the claimed figure and the measured figure.
    - `crates/paladin-storage/src/minio.rs` is described as outside ADR-0006's recorded scope with VERIFY-05 / PIPE-02 named as owner, not as a 0% file.
    - QUAL-03's amendment contains the literal `superseded by shipped code` and cites ADR-0006, and contains no percentage figure of its own.
    - `grep -cE '^\| QUAL-0[1-5] \| Phase 3 \| Pending \|' .planning/REQUIREMENTS.md` returns 0 — every traceability row moved off `Pending`.
    - Every traceability status used is one of the five ledger verdict classes.
    - Every amendment note carries a UTC date.
  </acceptance_criteria>
  <done>QUAL-02 and QUAL-03 are corrected at source with both the original and the measured claims visible, and every traceability row reflects evidence this phase actually produced.</done>
</task>

<task type="auto">
  <name>Task 2: Amend ROADMAP criterion 2 and the milestone ledger rows in place</name>
  <files>.planning/ROADMAP.md, .planning/ledgers/milestone-01.md</files>
  <read_first>
    - `.planning/ROADMAP.md:266-279` — the Phase 3 block, and specifically criterion 1's existing amendment parenthetical from plan 01-12, which is the wording convention criterion 2's amendment copies.
    - `.planning/ledgers/milestone-01.md:77-86` — the verdict legend and its five classes.
    - `.planning/ledgers/milestone-01.md:393` — the `REQ-commander-error-strategy` row citing the four skipped, empty-bodied tests at `commander.rs:2180,2188,2196,2204`. This row is amended in place per Phase 2's D-02, not duplicated.
    - `.planning/phases/03-verification-depth/03-coverage-measurement.md` §"QUAL-02 closure ledger" — the verdict and owner for every file, which the new ledger rows transcribe.
    - `docs/src/appendix/performance-baseline.md` §"Not produced by this run" — the two absent benchmark suites and their reason.
    - `.planning/phases/03-verification-depth/03-06-PLAN.md` §"Recorded decisions this plan carries" — the `src/bin/paladin-server.rs` deferral and its named owner.
  </read_first>
  <action>
    Amend ROADMAP.md Phase 3 success criterion 2 in place, in the same parenthetical style criterion 1
    already uses. Retain the criterion's substance — no first-party source file reports 0% coverage —
    and replace its stale file enumeration with a dated amendment note stating that the named list was
    re-derived from a measurement of the shipped tree, that the arsenal execution and registry
    services, the user controller and repository and `main.rs` were already covered when the criterion
    was written, that the Redis and MinIO adapters are **not** in the same position (Redis was a true
    0% and is now closed; MinIO has no denominator entry at all under the default feature set), and
    that `03-coverage-measurement.md` is the evidence. Do not touch any other phase's block and do not
    alter criterion 1's existing amendment.

    Amend `.planning/ledgers/milestone-01.md` in place:

    Update the `REQ-commander-error-strategy` row so its "residual caveat" clause records the
    resolution rather than the old state: the four tests were relocated to
    `tests/integration/commander_error_paths_test.rs` with real bodies and no skip attribute, cite each
    by its new `path:line` and function name, and record the pasted-verified command that runs them.
    Keep the row's original text about the base capability and the 2026-01 count, and add the
    amendment with its UTC date — amend in place, add no duplicate row.

    Add new rows, each using one of the legend's five verdict classes and each carrying a named owning
    phase and requirement where the verdict is `deferred with reason`:
    `src/bin/paladin-server.rs` at 0% — `deferred with reason`, owner Phase 5 / VERIFY-05, reason: a
    binary entrypoint needs a testable `run()` seam extracted, which is a refactor outside a
    measurement phase's charter;
    `crates/paladin-storage/src/redis.rs` live-server paths — `deferred with reason`, owner Phase 15 /
    PIPE, reason: Docker is absent from this environment, and the pure seams are now covered;
    `crates/paladin-storage/src/minio.rs` — `deferred with reason`, owner VERIFY-05 / PIPE-02, reason:
    outside ADR-0006's recorded default-feature scope, no denominator entry;
    ADR-0006's `--ignore-filename-regex` not excluding `target/` — `deferred with reason`, owner
    VERIFY-05 / PIPE-02, with the magnitude recorded and the note that it is immaterial today;
    the absent Paladin-execution-loop and Arsenal-invocation benchmark suites — `deferred with reason`,
    reason recorded from the baseline document, no owner assigned in this phase and that stated
    plainly rather than left implied.

    Add rows recording what this phase closed, each at the D-19 bar with a `file:line` plus a named
    passing exerciser: the five MCP failure modes, the `redis.rs` pure seams, the `paladin-ports` and
    `paladin-llm` zero-coverage closures, and the performance baseline. Use `satisfied` only where both
    halves of the D-19 bar are present; use `present, unproven` where only a citation exists.

    Retain all superseded text; every amendment is additive with a dated note.
  </action>
  <verify>
    <automated>M=.planning/ledgers/milestone-01.md; R=.planning/ROADMAP.md; grep -q 'commander_error_paths_test' "$M" && grep -q 'paladin-server.rs' "$M" && grep -q 'VERIFY-05' "$M" && grep -q 'PIPE' "$M" && [ "$(grep -c 'deferred with reason' "$M")" -ge 5 ] && grep -q '03-coverage-measurement' "$R" && grep -qE 'Amended by Phase 3' "$R" && [ "$(grep -c '### Phase ' "$R")" -eq 16 ] && grep -q 'test_fail_fast_stops_on_first_error' "$M"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` still contains exactly 16 `### Phase ` headings — no phase block was lost or renumbered by the edit.
    - Phase 3 criterion 2 carries a dated `Amended by Phase 3` note citing `03-coverage-measurement.md`, and states that the Redis and MinIO adapters are not in the same position.
    - Criterion 1's existing plan-01-12 amendment is byte-unchanged.
    - The `REQ-commander-error-strategy` ledger row is amended in place — `grep -c 'REQ-commander-error-strategy' .planning/ledgers/milestone-01.md` returns 1, not 2 — and now cites `tests/integration/commander_error_paths_test.rs` with the four function names.
    - `grep -c 'deferred with reason' .planning/ledgers/milestone-01.md` increased by at least 5, and every new such row names an owning phase and requirement, except the benchmark-suite row which states plainly that no owner is assigned.
    - Every new ledger verdict is one of the five classes in the legend at lines 79-86.
    - No previously-existing ledger row text was deleted.
  </acceptance_criteria>
  <done>The stale roadmap criterion and the superseded ledger rows are corrected at source, every deferral names an owner, and nothing was deleted to make room.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human confirmation of both coverage figures, the ratchet decision and the amendment set</name>
  <what-built>
    Phase 3's two coverage figures and the full amendment set.

    - `03-coverage-measurement.md` — an entry measurement at the phase's starting HEAD and an exit measurement at its ending HEAD, both produced by ADR-0006's verbatim command with unchanged regex, feature scope and doctest decision, both with pasted stdout and re-derivable arithmetic.
    - The QUAL-02 closure ledger — a verdict and, where still open, a named owner for every first-party file that measured 0.00%.
    - `03-critical-path-exercisers.md` — QUAL-03's three critical paths cited at the D-19 bar.
    - In-place amendments to QUAL-02, QUAL-03, ROADMAP criterion 2 and the milestone ledger, each with a dated provenance note and the superseded text retained.
    - `docs/src/appendix/performance-baseline.md` — a new dated run with throughput, P50/P95/P99, memory-per-Paladin and startup time; the 2026-05-27 run retained as superseded.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/03-verification-depth/03-coverage-measurement.md`. Confirm there are exactly two `Measured workspace line coverage: NN.NN%` lines, that each sits under its own commit SHA, and that each figure appears character-identically in the `TOTAL` row pasted directly above it.
    2. For each of the two figures, re-derive the arithmetic yourself: `(counted - missed) / counted` should give the stated two-decimal percentage. Confirm both are at or above 84.00%.
    3. Read `### Ratchet readiness (exit)`. Confirm you agree with the recorded decision that applying ADR-0006's ratchet is a milestone-close action rather than Phase 3's, and that ADR-0006 was not amended by this phase. If you want the ratchet applied now instead, say so — it changes the standing floor for Phases 4, 5 and 15.
    4. Read `### QUAL-02 closure ledger`. Confirm every file still at 0.00% carries an owning phase and requirement you accept — in particular `src/bin/paladin-server.rs` deferred to Phase 5 / VERIFY-05 rather than closed here.
    5. Skim the QUAL-02 amendment in `.planning/REQUIREMENTS.md`. Confirm the original list is still visible and the correction sits beside it rather than replacing it.
    6. Open `docs/src/appendix/performance-baseline.md` and confirm the 2026-05-27 run is still present under its superseded callout with its figures intact.
    7. Run `git diff --stat` and confirm nothing outside the expected file set changed.
  </how-to-verify>
  <resume-signal>Type "approved" to record the confirmation, or describe what should change. On approval, append a `## Human confirmation` section to `03-coverage-measurement.md` in the shape of `01-coverage-measurement.md:569-609` — approver, ISO-8601 UTC timestamp, the resume signal given, both confirmed figures, and the arithmetic checks performed before the checkpoint was presented.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingested corpus → planning record | `COVERAGE_ANALYSIS.md`-derived claims in QUAL-02 are untrusted input being corrected against a measurement of the shipped tree |
| this phase's evidence → downstream phases | VERIFY-05, PIPE-02 and the milestone close all read these amended records as authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Tampering | `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/ledgers/milestone-01.md` | medium | mitigate | Every amendment is additive with the superseded text retained and a dated evidence pointer; acceptance criteria assert the ROADMAP still holds 16 phase headings, that all five QUAL entries survive, that the Commander ledger row was amended rather than duplicated, and that no prior row text was deleted |
| T-03-04 | Tampering | recorded coverage figures | medium | mitigate | Both figures are transcribed from pasted stdout, re-derivable by a reader, and confirmed by a blocking human checkpoint that asks the approver to recompute the arithmetic independently |
| T-03-20 | Repudiation | ratchet decision | medium | mitigate | The decision not to amend ADR-0006 in this phase is recorded in the measurement record with its reasoning and is explicitly put to the human at the checkpoint, so a later reader can see it was chosen rather than overlooked |
| T-03-21 | Spoofing | unowned deferrals | medium | mitigate | Every `deferred with reason` row must name an owning phase and requirement, or state plainly that no owner is assigned; the plan's prohibition forbids recording a disposition without the D-19 bar or a named owner |
| T-03-SC | Tampering | crates.io / cargo registry | high | mitigate | This plan runs no package-manager command at all and edits only `.planning/` files. `03-RESEARCH.md` §Package Legitimacy Audit records zero ASSUMED/SUS/SLOP across the phase; any cargo command run for verification carries `--offline` |
</threat_model>

<verification>
- QUAL-02, QUAL-03 and ROADMAP criterion 2 are amended at source with the superseded text retained and a dated evidence pointer.
- The Commander ledger row is amended in place, not duplicated.
- Every deferral names an owner or states plainly that none is assigned.
- Every traceability row moved off `Pending` to a class the evidence supports.
- A human has confirmed both coverage figures and the ratchet decision.

## Edge coverage (spec-less probe fallback)

No SPEC.md exists; the fallback probe surfaced 26 edges across QUAL-01…QUAL-05, and all 26 are authored
into `must_haves` across the plan set: 5 in 03-01 (QUAL-01), 3 in 03-02 and 2 in 03-03 (QUAL-04), 3 in
03-04 (QUAL-05), and 13 in 03-07 (QUAL-02 ×6, QUAL-03 ×7). Total authored 26; backstopped 0; dismissed 0;
unresolved 0; silently dropped 0. This plan authors none of the 26 — its `must_haves.truths` are
goal-backward criteria for the amendment set itself.

## Prohibition probe (spec-less fallback)

One bespoke transparency prohibition authored above, descriptor-less: no gap recorded as closed,
satisfied or deferred without the D-19 evidence bar or a named owner. Across the plan set four kept
prohibitions are authored — 2 in 03-01, 1 in 03-04, 1 here — with three canon-referral breadcrumbs
recorded and dropped (bearer-token-in-logs, credentials-in-source, path traversal, host-fingerprint
disclosure), all covered by `/gsd-secure-phase` and by the per-plan STRIDE registers.
</verification>

<success_criteria>
- QUAL-02's stale offender list and QUAL-03's superseded percentage clause are both amended at source with dated provenance and the original text retained.
- ROADMAP Phase 3 criterion 2 no longer treats the Redis and MinIO adapters as being in the same position.
- The ledger carries the relocated Commander citations and at least five owned deferral rows.
- All five QUAL traceability rows are off `Pending`.
- The human confirmation section exists in `03-coverage-measurement.md`.
</success_criteria>

<output>
Create `.planning/phases/03-verification-depth/03-08-SUMMARY.md` when done.
</output>
