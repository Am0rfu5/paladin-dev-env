---
phase: 04-release-coherence
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .planning/phases/04-release-coherence/04-release-measurement.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [REL-05]

must_haves:
  truths:
    - "`cargo fmt --all -- --check` exits 0 and its verdict is recorded with the D-17 provenance block, re-run in this session rather than cited from the discussion."
    - "`cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0 and is recorded the same way."
    - "`cargo test --workspace` exits 0 and its pass/fail counts are recorded from this run's raw stdout — Phase 2's 2864/0 figure is re-derived, never cited."
    - "`cargo test --workspace --doc --exclude paladin-ports` exits 0, mirroring `ci.yml:225`'s existing exclusion, and the SUMMARY states that the exclusion exists because `paladin-ports` carries `doctest = false` (DEBT-03 / Phase 8), not because this phase disabled anything."
    - "All 47 example targets build across the four-invocation feature matrix, and the gate is 'every example target builds', not a count of 22."
    - "A build invocation that selects zero example targets is a failure, not a pass: the recorded proof is the 47 named binaries present in `target/debug/examples`, not the exit code of a bulk build."
    - "The stale figure of 22 examples is corrected at all five source locations with the dated amendment parenthetical, and the original claim is retained rather than silently replaced."
  artifacts:
    - path: ".planning/phases/04-release-coherence/04-release-measurement.md"
      provides: "Provenance-blocked sections for fmt, clippy, workspace tests, doc tests and the example matrix"
      min_lines: 120
    - path: ".planning/ROADMAP.md"
      provides: "Overview and Phase 4 criterion 5 corrected from 22 examples to the re-derived count, with the dated amendment parenthetical"
      contains: "Amended by Phase 4"
    - path: ".planning/PROJECT.md"
      provides: "Two restatements corrected with the same parenthetical"
      contains: "Amended by Phase 4"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REL-05's own text corrected with the same parenthetical"
      contains: "Amended by Phase 4"
  key_links:
    - from: ".planning/phases/04-release-coherence/04-release-measurement.md"
      to: ".planning/ROADMAP.md"
      via: "the amendment parenthetical cites this measurement record as its evidence file"
      pattern: "04-release-measurement"
  prohibitions:
    - statement: "MUST NOT record any gate verdict that was not executed in this session. Phase 2's test count, RESEARCH.md's build timings, and the discussion session's `cargo fmt` result are all context, not evidence — every figure in the record comes from a command run now, with its raw stdout pasted."
      category: transparency
    - statement: "MUST NOT silently substitute a corrected figure. Every corrected number keeps the original claim in place and appends the dated `(**Amended by Phase 4 …**)` parenthetical naming the evidence file — the convention Phases 1, 2 and 3 all used."
      category: transparency
    - statement: "MUST NOT change a gate command to make it pass — no narrowing `--workspace`, no dropping `-D warnings`, no adding an exclusion beyond the one `ci.yml:225` already carries. If a gate is red, the red is the finding."
      category: transparency
---

<objective>
Run every gate SC5 names that can run locally, record each with the D-17 provenance block, and
replace the stale "22 examples" figure with a measurement of the shipped tree.

Purpose: SC5's five locally-runnable clauses — format, clippy with warnings as errors, workspace
tests, doc tests, and every example target — are provable here today (D-12). The sixth and seventh
(Docker, Kubernetes) are plan 04-03's, authored and deferred. The example clause additionally needs
its own figure corrected: `examples/` holds 47 `.rs` files, not 22, and the 22 traces to a Milestone
1 validation report that has since been restated in five planning documents (D-13). This is Phase
3's D-03 pattern exactly — an ingested count losing to a measurement of the shipped tree.

Output: five new provenance-blocked sections in `04-release-measurement.md`, and five corrected
restatements carrying the dated amendment parenthetical.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-release-coherence/04-CONTEXT.md
@.planning/phases/04-release-coherence/04-RESEARCH.md
@.planning/phases/04-release-coherence/04-PATTERNS.md
@.planning/phases/04-release-coherence/04-01-SUMMARY.md
</context>

## Artifacts this phase produces

Appended to by **this plan** (created by plan 04-01):

- `.planning/phases/04-release-coherence/04-release-measurement.md`

Created elsewhere in Phase 4 (listed so drift verification excludes them):

- `.planning/phases/04-release-coherence/04-advisory-findings.md` (plan 04-02)
- `.planning/phases/04-release-coherence/04-ci-gate-deferrals.md`, `COVERAGE.md` (plan 04-03)
- `.planning/decisions/0008-workspace-version-0-7-0.md`, `0009-workspace-rust-edition-2024.md` (plan 04-07)
- New `.github/workflows/ci.yml` job keys `examples` and `kubernetes-smoke` (plan 04-03)
- Local annotated git tag `v0.7.0`, unpushed (plan 04-05)

<tasks>

<task type="auto">
  <name>Task 1: Run and record the four local gate commands</name>
  <precondition>Plan 04-01's edition bump is committed and `cargo build --workspace --offline` exits 0 — these gates measure the post-bump tree, not the pre-bump one.</precondition>
  <files>.planning/phases/04-release-coherence/04-release-measurement.md</files>
  <read_first>
    - .planning/phases/04-release-coherence/04-release-measurement.md (the record created by plan 04-01 — append new sections, never rewrite existing ones)
    - .github/workflows/ci.yml (lines 20-58 and 190-227 — the `lint` and `test` jobs, so the local commands match what CI will run rather than a paraphrase; note the doc-test step's `--exclude paladin-ports` at line 225)
    - .planning/phases/04-release-coherence/04-VALIDATION.md (the Supplementary gate commands table — the exact command strings this phase committed to)
  </read_first>
  <action>
    Run these four commands, in this order, and append one `## Entry measurement — <name>` section
    per command to `04-release-measurement.md`, each opening with the full D-17 environment-probe
    block (`rustc -vV`, `cargo --version`, `git rev-parse HEAD`, `git rev-parse --abbrev-ref HEAD`,
    `git status --porcelain` with the disambiguating prose sentence, `date -u`) followed by the
    verbatim command line, verbatim stdout, and the exit status:

    - `cargo fmt --all -- --check`
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
    - `cargo test --workspace`
    - `cargo test --workspace --doc --exclude paladin-ports`

    Every one of these must be executed now. Phase 2's recorded 2864 passed / 0 failed and the
    discussion session's clean `cargo fmt` are context; neither is evidence for this phase, and D-12
    says re-run, do not cite. Record the actual pass/fail/ignored counts this run produces, whatever
    they are.

    The doc-test command's `--exclude paladin-ports` is not a concession this phase is making — it
    mirrors the exclusion `ci.yml:225` already carries, which exists because that crate sets
    `doctest = false`. Re-enabling those doctests is DEBT-03 in Phase 8 and the governing `cargo doc`
    bar is HARD-07 in Phase 10. State that in the section so a reader does not read the exclusion as
    this phase weakening a gate.

    If any command exits non-zero, stop and record the failure with its raw output rather than
    adjusting the command. Narrowing `--workspace`, dropping `-D warnings`, or adding an exclusion
    beyond the one `ci.yml` already carries would report a gate as met that was not.

    These four commands run without `--offline` only if they need the network; none of them does, so
    add `--offline` where cargo accepts it, per D-17. `cargo fmt` and `cargo clippy` take their
    flags as shown; note in the record which invocations carried `--offline` and which could not.
  </action>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0
    - `cargo test --workspace` exits 0
    - `cargo test --workspace --doc --exclude paladin-ports` exits 0
    - `grep -c '^## Entry measurement' .planning/phases/04-release-coherence/04-release-measurement.md` returns at least 6 (plan 04-01's two plus this task's four)
    - The record names each command verbatim: `grep -c 'cargo fmt --all -- --check' …`, `grep -c 'D warnings' …`, `grep -c 'cargo test --workspace' …` and `grep -c 'exclude paladin-ports' …` each return at least 1
    - The record contains a test-count line matching `[0-9]\+ passed` taken from this run's stdout: `grep -cE '[0-9]+ passed' .planning/phases/04-release-coherence/04-release-measurement.md` returns at least 1
    - The record explains the doc-test exclusion: `grep -c 'DEBT-03' .planning/phases/04-release-coherence/04-release-measurement.md` returns at least 1
  </acceptance_criteria>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --workspace --all-targets --all-features -- -D warnings && cargo test --workspace && cargo test --workspace --doc --exclude paladin-ports && [ "$(grep -c '^## Entry measurement' .planning/phases/04-release-coherence/04-release-measurement.md)" -ge 6 ]</automated>
  </verify>
  <done>All four gate commands exit 0 in this session and each has its own provenance-blocked section in `04-release-measurement.md` with verbatim command, stdout and exit status; the doc-test exclusion's DEBT-03 provenance is stated.</done>
</task>

<task type="auto">
  <name>Task 2: Prove every example target builds, and re-derive the count</name>
  <files>.planning/phases/04-release-coherence/04-release-measurement.md</files>
  <read_first>
    - Cargo.toml (lines 220-259 — the four `[[example]]` declarations with `required-features`, and `[features] default = ["llm-openai"]`)
    - .planning/phases/04-release-coherence/04-RESEARCH.md (Part B, Q1(b) and Q2 — the verified four-invocation matrix and the silent-skip finding)
    - .planning/phases/04-release-coherence/04-release-measurement.md (append only)
  </read_first>
  <action>
    Run the four-invocation feature matrix and record it as one `## Entry measurement — every
    example target builds (four-invocation feature matrix)` section with the full D-17 provenance
    block:

    - `cargo build --examples --offline`
    - `cargo build --example vision_analysis --example vision_battalion --features "vision,llm-openai" --offline`
    - `cargo build --example document_processing --features "content-processing" --offline`
    - `cargo build --example http_service_host --features "web-server" --offline`

    Record each invocation's verbatim command, stdout and exit status. Then record the count
    re-derivation in the same section, with the commands and their raw output: `find examples -name
    '*.rs' | wc -l` gives the file count; `grep -c '^\[\[example\]\]' Cargo.toml` gives the number of
    explicitly declared targets; and a check that no crate under `crates/` ships an `examples/`
    directory. State the arithmetic a reader can re-derive: the declared targets are the ones that
    gate on non-default features, and the remainder are auto-discovered under the default feature
    set.

    Close the section with the binary-presence proof, which is the part that matters. Exit code 0
    from the bulk invocation is not sufficient evidence: cargo's bulk `--examples` selector silently
    omits targets whose `required-features` are unmet, with no error and no warning, so a bulk build
    alone reports success while covering only the ungated subset. Derive the expected basename list
    from `find examples -name '*.rs'`, then assert each basename exists as an executable file in
    `target/debug/examples`, and record the resulting count. A count below the file count is a
    failure to report, not a number to round.

    Record the verdict as **"every example target builds"** — not as a count. D-13 is explicit that
    the gate is the property, not the figure; the count is recorded as evidence for the amendments
    in Task 3, not as the acceptance criterion.
  </action>
  <acceptance_criteria>
    - All four build invocations exit 0
    - `find examples -name '*.rs' | wc -l` returns 47
    - `grep -c '^\[\[example\]\]' Cargo.toml` returns 4
    - Every example basename has a binary: `find examples -name '*.rs' -exec basename {} .rs \; | sort > /tmp/expected.txt; ls target/debug/examples | grep -vE '\.(d|o)$' | sed 's/-[0-9a-f]\{8,\}$//' | sort -u > /tmp/built.txt; comm -23 /tmp/expected.txt /tmp/built.txt` produces no output
    - `grep -c 'every example target builds' .planning/phases/04-release-coherence/04-release-measurement.md` returns at least 1
    - The section records the silent-skip finding: `grep -ci 'silently' .planning/phases/04-release-coherence/04-release-measurement.md` returns at least 1
    - `grep -c '^## Entry measurement' .planning/phases/04-release-coherence/04-release-measurement.md` returns at least 7
  </acceptance_criteria>
  <verify>
    <automated>cargo build --examples --offline && cargo build --example vision_analysis --example vision_battalion --features "vision,llm-openai" --offline && cargo build --example document_processing --features "content-processing" --offline && cargo build --example http_service_host --features "web-server" --offline && [ "$(find examples -name '*.rs' | wc -l)" = "47" ] && find examples -name '*.rs' -exec basename {} .rs \; | sort > /tmp/expected.txt && ls target/debug/examples | grep -vE '\.(d|o)$' | sed 's/-[0-9a-f]\{8,\}$//' | sort -u > /tmp/built.txt && [ -z "$(comm -23 /tmp/expected.txt /tmp/built.txt)" ]</automated>
  </verify>
  <done>All four invocations exit 0, every one of the 47 example basenames has a built binary, and `04-release-measurement.md` records the matrix, the count re-derivation with its arithmetic, the silent-skip finding, and the verdict phrased as a property rather than a count.</done>
</task>

<task type="auto">
  <name>Task 3: Amend the five stale "22 examples" restatements at source</name>
  <files>.planning/ROADMAP.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md (line 6 in the Overview, and line 313, Phase 4 success criterion 5)
    - .planning/PROJECT.md (line 21 in the opening description, and line 136 in the shipped-artefact list; line 767 records the Milestone 1 origin "22/22 examples compiling" — read it, and decide whether it is a restatement to amend or a historical measurement to leave standing)
    - .planning/REQUIREMENTS.md (line 382 — REL-05's own text)
    - .planning/ROADMAP.md (line 274 — Phase 3's own amendment of its criterion 2; this is the exact prose shape to copy)
  </read_first>
  <action>
    Apply the corpus's amend-at-source convention to each stale restatement of the figure 22. The
    shape, copied from `ROADMAP.md:274`, is a parenthetical appended to the original sentence rather
    than a replacement of it: `(**Amended by Phase 4, dated <YYYY-MM-DD>, citing
    04-release-measurement.md**: <one prose paragraph — what the original figure was, what the
    re-derived figure is, and why the correction wins under the precedence order>.)` The original
    claim stays in place. Never silently substitute the corrected number.

    The prose paragraph should say the same thing at each site: the figure traces to a Milestone 1
    Epic 10 validation report ("22/22 examples compiling"), the shipped tree carries 47 `.rs` files
    under `examples/` of which four are declared `[[example]]` targets gating on non-default
    features, no crate under `crates/` ships examples, and the shipped tree outranks an ingested
    count under the project's precedence order. Add that the gate REL-05 and ROADMAP criterion 5
    express is now "every example target builds" rather than a count, so the figure cannot go stale
    again the same way.

    Four sites are restatements and are amended: `ROADMAP.md:6`, `ROADMAP.md:313`, `PROJECT.md:21`
    and `PROJECT.md:136`, plus `REQUIREMENTS.md:382` — five in total.

    `PROJECT.md:767` is a different case and must be handled differently: it is the Milestone 1
    measurement itself, dated 2026-01-27, sitting in a paragraph explicitly headed as the measured
    quality state of that milestone. A historical measurement is not stale merely because the tree
    moved on. Leave the figure standing and append a short provenance note identifying it as the
    origin of the four restatements and pointing at this phase's re-derivation, in the same
    parenthetical form. Say plainly in the SUMMARY which of the six sites were corrected and which
    was annotated as history, so a later reader can tell the two treatments apart.

    Make every edit with a scoped replacement of the target sentence. Do not rewrite whole files.
  </action>
  <acceptance_criteria>
    - `grep -c 'Amended by Phase 4' .planning/ROADMAP.md` returns at least 2
    - `grep -c 'Amended by Phase 4' .planning/PROJECT.md` returns at least 2
    - `grep -c 'Amended by Phase 4' .planning/REQUIREMENTS.md` returns at least 1
    - Every amendment cites the evidence file: `grep -c '04-release-measurement.md' .planning/ROADMAP.md` returns at least 2, and the same file is cited in `PROJECT.md` and `REQUIREMENTS.md` at least once each
    - The re-derived figure appears at each amended site: `grep -c '47' .planning/ROADMAP.md` returns at least 2
    - The original claims are retained, not replaced: `grep -c '22' .planning/ROADMAP.md` returns at least 2 and `grep -c '22' .planning/REQUIREMENTS.md` returns at least 1
    - `PROJECT.md`'s Milestone 1 measurement paragraph still carries its own date: `grep -c '2026-01-27' .planning/PROJECT.md` returns at least 1
    - No file was rewritten wholesale: `git diff --stat -- .planning/ROADMAP.md .planning/PROJECT.md .planning/REQUIREMENTS.md` shows fewer than 60 changed lines in total
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -c 'Amended by Phase 4' .planning/ROADMAP.md)" -ge 2 ] && [ "$(grep -c 'Amended by Phase 4' .planning/PROJECT.md)" -ge 2 ] && [ "$(grep -c 'Amended by Phase 4' .planning/REQUIREMENTS.md)" -ge 1 ] && grep -q '04-release-measurement.md' .planning/ROADMAP.md && grep -q '04-release-measurement.md' .planning/REQUIREMENTS.md && [ "$(grep -c '22' .planning/ROADMAP.md)" -ge 2 ]</automated>
  </verify>
  <done>All five restatements carry the dated amendment parenthetical citing `04-release-measurement.md`, the original claims are retained beside the corrections, and `PROJECT.md`'s Milestone 1 measurement is annotated as the historical origin rather than corrected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executed command → recorded verdict | The record is the only artefact a later phase reads; a figure that was cited rather than run crosses this boundary undetectably unless raw stdout is present |
| ingested planning document → shipped tree | The "22 examples" figure is an ingested count that outlived the tree it described; the precedence order decides which wins |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-16 | Repudiation | `04-release-measurement.md` gate sections | high | mitigate | Each of the five sections carries the full D-17 probe block plus verbatim stdout and exit status, so no verdict can be traced to a citation rather than a run; the acceptance criteria execute all four gate commands independently |
| T-04-17 | Tampering | gate command strings | high | mitigate | Narrowing `--workspace`, dropping `-D warnings`, or adding an exclusion beyond `ci.yml:225`'s is prohibited; the verify block re-runs the exact commands, so a weakened command in the record cannot pass the gate |
| T-04-18 | Spoofing | bulk `cargo build --examples` exit code | high | mitigate | Cargo silently omits targets with unmet `required-features`, so exit 0 is not evidence of coverage; the acceptance criterion asserts a binary exists for every one of the 47 basenames rather than trusting the exit code |
| T-04-19 | Tampering | in-place amendment of planning records | medium | mitigate | Every correction appends the dated parenthetical and retains the original claim; the diff is asserted under 60 lines so no wholesale rewrite can hide inside the change |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages. `04-RESEARCH.md` § "Package Legitimacy Audit" records the phase-wide verdict as "Not applicable" |
</threat_model>

<verification>
- All four gate commands exit 0 in this session, each with a provenance-blocked section.
- All four example-matrix invocations exit 0, and a binary exists for every one of the 47 example
  basenames.
- `04-release-measurement.md` contains at least seven `## Entry measurement` sections.
- Five restatements carry the dated amendment parenthetical citing `04-release-measurement.md`; the
  Milestone 1 origin measurement is annotated rather than corrected.
</verification>

<success_criteria>
Every clause of ROADMAP Phase 4 criterion 5 that can be executed in this environment has been
executed and recorded reproducibly, and the criterion's own stale figure has been corrected at
source. The two clauses that cannot run here — Docker and Kubernetes — are plan 04-03's, recorded
as authored and deferred with a named owner, and nothing in this plan claims otherwise.
</success_criteria>

<output>
Create `.planning/phases/04-release-coherence/04-04-SUMMARY.md` when done.
</output>
