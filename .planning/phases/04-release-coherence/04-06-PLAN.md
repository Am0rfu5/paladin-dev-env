---
phase: 04-release-coherence
plan: 06
type: execute
wave: 4
depends_on: ["04-05"]
files_modified:
  - docs/src/getting-started/quickstart.md
  - .planning/phases/04-release-coherence/04-release-measurement.md
autonomous: true
requirements: [REL-04]

must_haves:
  truths:
    - "The QUICKSTART code sample imports from a crate the page actually declares as a dependency, using module paths that exist in the shipped tree — verified against `examples/basic_paladin.rs`, the in-tree analog that compiles."
    - "The page's dependency block names every crate its sample imports from, at 0.7.0."
    - "Every `cargo run --example` line on the page names a file that exists under `examples/`."
    - "The elapsed time for the documented steps is recorded once, pass or fail, under stated environment conditions, with the D-17 provenance block."
    - "The recorded figure is explicitly labelled as measured under stated conditions and NOT as a clean-machine claim; the true cold-start timing is filed `deferred with reason` with a named owner."
    - "The page's own timing claim and the < 15-minute gate agree after the measurement, and which number the page carries is decided after measuring rather than before."
    - "REL-04's documentation-final-review clause is discharged by citing RECON-08's recorded verdict, not by inventing a review — the ledger records Epic 10 Task 7.0 as satisfied with no owner needed because no such task ever existed."
  artifacts:
    - path: "docs/src/getting-started/quickstart.md"
      provides: "A structurally correct first-agent walkthrough at 0.7.0 with a reconciled timing claim"
      contains: "0.7.0"
    - path: ".planning/phases/04-release-coherence/04-release-measurement.md"
      provides: "The QUICKSTART timing measurement under stated conditions, and the clean-machine deferral row"
  key_links:
    - from: "docs/src/getting-started/quickstart.md"
      to: "examples/basic_paladin.rs"
      via: "the sample's import lines and the PaladinExecutionService constructor arity are derived from the in-tree example that compiles"
      pattern: "paladin::application::services::paladin"
    - from: "docs/src/getting-started/quickstart.md"
      to: "docs/src/introduction.md"
      via: "the two pages must state the same time-to-first-agent figure"
      pattern: "minutes"
  prohibitions:
    - statement: "MUST NOT tune, shorten, or reorder the QUICKSTART's steps in order to make the measurement hit a target. ROADMAP SC4 says 'measured for the first time, pass or fail' — a fail is a legitimate, recordable outcome, and the steps are corrected for accuracy only."
      category: transparency
    - statement: "MUST NOT present the recorded figure as a clean-machine or cold-registry timing. crates.io returns HTTP 403 here, the local registry and build caches are warm, Docker is absent and no LLM API key exists — the record states each of those conditions and labels the figure accordingly."
      category: transparency
    - statement: "MUST NOT invent, reconstruct, or perform a 'Final Documentation Review'. RECON-08's recorded verdict is that no such task ever existed and none is needed; fabricating one would manufacture an artefact the corpus explicitly determined is absent."
      category: transparency
---

<objective>
Repair the QUICKSTART so a developer following it can actually reach a working agent, then measure
the documented steps once — pass or fail — under conditions this record states plainly.

Purpose: REL-04. Its documentation-review half is already discharged (D-10): RECON-08's recorded
verdict in `.planning/ledgers/milestone-01.md` is that the validation report is wrong and no Task
7.0 exists, classified `satisfied` with no owner needed. The live half is the QUICKSTART
measurement — and research found that the page is not merely stale but structurally broken:
`PaladinBuilder` and `PaladinExecutionService` live in the root `paladin` lib crate, not in
`paladin-ai-core` as the imports claim, and the page's dependency block never lists that crate at
all. A developer with a working network and a valid API key still cannot compile the sample. That
is a real work item, not an environment limitation.

Output: a QUICKSTART whose sample matches the shipped tree, and the first recorded timing for
ROADMAP criterion 4.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-release-coherence/04-CONTEXT.md
@.planning/phases/04-release-coherence/04-RESEARCH.md
@.planning/phases/04-release-coherence/04-PATTERNS.md
@.planning/phases/04-release-coherence/04-05-SUMMARY.md
</context>

## Artifacts this phase produces

Appended to by **this plan** (created by plan 04-01):

- `.planning/phases/04-release-coherence/04-release-measurement.md`

Created elsewhere in Phase 4 (listed so drift verification excludes them):

- `.planning/phases/04-release-coherence/04-advisory-findings.md` (plan 04-02)
- `.planning/phases/04-release-coherence/04-ci-gate-deferrals.md`, `COVERAGE.md` (plan 04-03)
- `.planning/decisions/0008-workspace-version-0-7-0.md`, `0009-workspace-rust-edition-2024.md` (plan 04-07)
- New `.github/workflows/ci.yml` job keys `examples` and `kubernetes-smoke` (plan 04-03)
- Local annotated git tag `v0.7.0`, unpushed (plan 04-05)

## Flagged assumption (unresolved edge probe)

The deterministic edge probe classified REL-04 as `unclassified` and it stays **unresolved** —
carried here as an explicit assumption rather than silently dropped or auto-backstopped. The
assumption is that "a developer following QUICKSTART reaches a working agent" is fully decomposed by
the truths above: the sample compiles against declared dependencies, every referenced example file
exists, and one elapsed-time figure is recorded under stated conditions. Two boundaries this plan
cannot close are named rather than assumed away — whether the sample *runs* (needs an LLM key) and
whether a cold-registry developer hits the same figure (needs network to crates.io). Both are
recorded as deferrals with owners, not as passes.

<!-- planner-discipline-allow: paladin_ai_core::application::services -->
<!-- planner-discipline-allow: phalanx_concurrent -->

<tasks>

<task type="auto">
  <name>Task 1: Repair the QUICKSTART sample so it matches the shipped tree</name>
  <precondition>Plan 04-05's version bump is committed, so `grep -h '^version' Cargo.toml crates/*/Cargo.toml | sort -u` reports 0.7.0 and the dependency block's version can be checked against the manifests rather than transcribed from this plan.</precondition>
  <files>docs/src/getting-started/quickstart.md</files>
  <read_first>
    - docs/src/getting-started/quickstart.md (the whole 127-line file — line 3's timing claim, the dependency block at 22-28, the code sample at 32-69, and the example commands at 96-105)
    - examples/basic_paladin.rs (lines 1-30 — the in-tree analog that actually compiles; its import lines and its `PaladinExecutionService::new` call are the source of truth for the corrected sample)
    - src/application/services/paladin/paladin_execution_service.rs (lines 172-182 — the real `new` signature: four arguments, the second an `Arc<CircuitBreaker>`, not a defaulted config)
    - Cargo.toml (lines 33-46 — the root package is `paladin-ai` and its `[lib] name` is `paladin`, which is why the crate a consumer depends on and the path they import from have different spellings)
    - crates/paladin-core/Cargo.toml (the package that is actually published as `paladin-ai-core`; its `src/` holds only `base/` and `platform/`, with no `application::services` module)
  </read_first>
  <action>
    Three corrections to `docs/src/getting-started/quickstart.md`. Do not restructure the page; edit
    the four regions named below.

    **The imports.** The sample currently imports `PaladinBuilder` and `PaladinExecutionService`
    from the `paladin_ai_core` crate under an `application::services::paladin::…` module path. That
    path does not exist in `paladin-ai-core` on this tree — that crate ships only `base/` and
    `platform/`. Both types live in the root workspace package `paladin-ai`, whose `[lib] name` is
    `paladin`. Replace the two import lines with the forms `examples/basic_paladin.rs` uses, rooted
    at the `paladin` crate rather than `paladin_ai_core`. Keep the `paladin_ports::output::llm_port`
    and `paladin_llm::openai` imports, which are already correct.

    **The dependency block.** It lists `paladin-ai-core`, `paladin-ports` and `paladin-llm` at
    `"0.5.0"` and never lists the crate the sample now imports from. Add `paladin-ai` and set every
    Paladin crate's version to the workspace version plan 04-05 converged on — read it from the
    manifests rather than transcribing it from this plan. Keep `tokio` as it is. If, after the
    import fix, the sample no longer references `paladin-ai-core` at all, remove that line rather
    than leaving an unused dependency in a tutorial.

    **The constructor call.** `PaladinExecutionService::new` takes four arguments and the second is
    an `Arc<CircuitBreaker>`, not a defaulted configuration value. Make the sample construct it the
    way `examples/basic_paladin.rs` does, and add the corresponding import. The page's code fence is
    marked `rust,ignore` so mdbook will not compile it — that is precisely why it drifted, so the
    correctness bar here is structural equivalence to the in-tree example that does compile, checked
    line by line rather than eyeballed.

    **The example commands.** The commented line naming a concurrent-execution example points at a
    file that does not exist; the shipped file is `examples/phalanx_parallel.rs`. Correct the name
    and uncomment it, since the example exists and builds. Verify the other two named examples
    (`basic_paladin`, `formation_sequential`) resolve to real files before leaving them.

    Leave line 3's timing claim alone for now. D-11.1 says the figure is decided **after** measuring,
    and Task 2 owns that edit.
  </action>
  <acceptance_criteria>
    - `grep -c 'paladin_ai_core::application::services' docs/src/getting-started/quickstart.md` returns 0
    - The corrected imports match the in-tree analog: `grep -c 'use paladin::application::services::paladin::paladin_builder::PaladinBuilder;' docs/src/getting-started/quickstart.md` returns 1, and the same for `paladin_execution_service::PaladinExecutionService`
    - Both corrected import lines also appear verbatim in the analog: `grep -c 'use paladin::application::services::paladin::paladin_builder::PaladinBuilder;' examples/basic_paladin.rs` returns 1
    - The dependency block names the crate the sample imports from: `grep -c 'paladin-ai ' docs/src/getting-started/quickstart.md` returns at least 1
    - Every Paladin crate version in the dependency block matches the workspace version: `grep -oE '"0\.[0-9]+\.[0-9]+"' docs/src/getting-started/quickstart.md | sort -u` returns only `"0.7.0"`
    - `grep -c 'phalanx_concurrent' docs/src/getting-started/quickstart.md` returns 0
    - Every example named on the page resolves to a file: `grep -oE 'cargo run --example [a-z_]+' docs/src/getting-started/quickstart.md | awk '{print $4}' | while read e; do test -f "examples/$e.rs" || { echo "MISSING $e"; exit 1; }; done` produces no `MISSING` output and exits 0
    - The constructor arity matches the real signature: `grep -c 'PaladinExecutionService::new(' docs/src/getting-started/quickstart.md` returns 1 and the call passes four arguments
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -c 'paladin_ai_core::application::services' docs/src/getting-started/quickstart.md)" = "0" ] && grep -q 'use paladin::application::services::paladin::paladin_builder::PaladinBuilder;' docs/src/getting-started/quickstart.md && grep -q 'use paladin::application::services::paladin::paladin_builder::PaladinBuilder;' examples/basic_paladin.rs && [ "$(grep -c 'phalanx_concurrent' docs/src/getting-started/quickstart.md)" = "0" ] && [ "$(grep -oE '"0\.[0-9]+\.[0-9]+"' docs/src/getting-started/quickstart.md | sort -u | tr -d '\n')" = '"0.7.0"' ] && grep -oE 'cargo run --example [a-z_]+' docs/src/getting-started/quickstart.md | awk '{print $4}' | while read e; do test -f "examples/$e.rs" || exit 1; done</automated>
  </verify>
  <done>The QUICKSTART sample imports from the crate its dependency block declares, using module paths that exist in the shipped tree and match `examples/basic_paladin.rs` line for line; every Paladin dependency is at the converged workspace version; the constructor call matches the real four-argument signature; and every example command names a file that exists.</done>
</task>

<task type="auto">
  <name>Task 2: Measure the documented steps once, settle the timing claim, and file the deferral</name>
  <files>docs/src/getting-started/quickstart.md, .planning/phases/04-release-coherence/04-release-measurement.md</files>
  <read_first>
    - docs/src/getting-started/quickstart.md (the repaired page from Task 1 — the steps to be timed)
    - docs/src/introduction.md (line 9 — the competing "15 minutes" figure that must end up agreeing with this page)
    - .planning/ledgers/milestone-01.md (the §"Epic 10 Task 7.0 — dispute resolution (RECON-08)" section, roughly lines 142-240 — the recorded verdict this task cites rather than re-deriving; and lines 88-118 for the `deferred with reason` row shape with its bold named-owner convention)
    - .planning/phases/04-release-coherence/04-release-measurement.md (append only)
    - docs/src/appendix/performance-baseline.md (Phase 3's model for adding a dated measurement section without overwriting the prior one)
  </read_first>
  <action>
    Measure, then decide, then record. In that order — deciding the claim before measuring is what
    D-11.3 forbids.

    **Measure.** Walk the repaired page's steps from the current tree and wall-clock them. State up
    front which prefix is reachable here and which is not, because most of the page is not: crates.io
    returns HTTP 403, so the `cargo new` project cannot resolve `paladin-ai`, `paladin-ports` or
    `paladin-llm` from the registry — and the 0.7.0 crates are not published anywhere yet in any
    case. There is no LLM API key, so `OpenAIAdapter::from_env()` cannot make a live call. Docker is
    absent, so `make services-up` cannot run. What *is* measurable is the in-workspace path: from a
    checked-out tree, the elapsed time to build the example the page points a new user at
    (`cargo build --example basic_paladin --offline`), plus the time to read and follow the
    prerequisite and setup steps. Time it with `date -u` before and after and record both stamps
    rather than a computed figure alone, so the arithmetic is re-derivable.

    Additionally, measure the one substitute that makes the new-project path timeable at all: create
    a scratch project outside the repository tree, point its three Paladin dependencies at this
    workspace with `path =` requirements instead of registry versions, paste the repaired sample, and
    time `cargo build`. Record it as a **substitute measurement** with the substitution named
    explicitly — it answers "does the corrected sample compile against the shipped tree", which is
    the defect Task 1 fixed, without pretending to be the documented registry path. Delete the
    scratch project afterwards and confirm nothing under `examples/` or `crates/` changed.

    **Decide.** ROADMAP SC4 and REL-04 both name **< 15 minutes**; `docs/src/introduction.md:9` says
    15 minutes; `quickstart.md:3` says "under five minutes". Settle on 15 minutes as the gate, since
    it is the figure both planning documents carry and two of three doc references support — unless
    the measurement comes in under five, in which case the page keeps its tighter claim and the
    measurement becomes the evidence for it. Edit `quickstart.md:3` to whichever the measurement
    supports, and say in the SUMMARY which branch was taken and why. Do not shorten or reorder the
    page's steps to influence which branch applies; the steps were corrected for accuracy in Task 1
    and are not touched again here.

    **Record.** Append a `## Entry measurement — QUICKSTART elapsed time (first measurement)` section
    to `04-release-measurement.md` with the full D-17 provenance block, the two `date -u` stamps, the
    per-step notes, and the arithmetic. Immediately after the figure, state in one sentence that this
    is **measured under stated conditions, not a clean-machine claim**, and enumerate the conditions:
    warm cargo registry and build cache, no network route to crates.io, no Docker, no LLM API key,
    plus this machine's `uname -a` and CPU model. Then write two `deferred with reason` rows in the
    ledger's shape:

    - the true clean-machine, cold-registry timing — **Owner: Phase 15 / PIPE**, since it needs a
      runner with network and a published 0.7.0 to resolve against;
    - the live LLM execution of the sample (`OpenAIAdapter::from_env()` reaching the OpenAI API) —
      **Owner: Phase 15 / PIPE**, same runner requirement plus a key.

    Close the section by discharging REL-04's other half by citation rather than by work: name
    `.planning/ledgers/milestone-01.md` §"Epic 10 Task 7.0 — dispute resolution (RECON-08)" and its
    verdict — the validation report is wrong, no Task 7.0 heading exists in a 103/103 task list,
    `intel/task-completion-state.md` corroborates independently, and no artefact anywhere in the
    263-document corpus supplies content for a "Final Documentation Review". Classified `satisfied`,
    no owner needed. Do not construct a review to fill the gap; the recorded finding is that there is
    no gap.
  </action>
  <acceptance_criteria>
    - `grep -c 'QUICKSTART elapsed time' .planning/phases/04-release-coherence/04-release-measurement.md` returns at least 1
    - The record carries both raw timestamps and the arithmetic: `grep -c 'date -u' .planning/phases/04-release-coherence/04-release-measurement.md` returns at least 2
    - The stated-conditions label is present verbatim: `grep -c 'measured under stated conditions, not a clean-machine claim' .planning/phases/04-release-coherence/04-release-measurement.md` returns 1
    - All four environment conditions are enumerated: the record contains `crates.io`, `Docker`, `warm` and `API key`
    - Two deferral rows with named owners exist: `grep -c 'Owner: Phase 15 / PIPE' .planning/phases/04-release-coherence/04-release-measurement.md` returns at least 2
    - RECON-08 is cited, not re-derived: `grep -c 'RECON-08' .planning/phases/04-release-coherence/04-release-measurement.md` returns at least 1
    - The two pages agree on one figure: the number of minutes on `docs/src/getting-started/quickstart.md` line 3 matches the number on `docs/src/introduction.md` line 9, or the SUMMARY states that the measurement came in under five minutes and the page's tighter claim is retained with the measurement cited as its evidence
    - The substitute measurement is labelled as such: `grep -ci 'substitute measurement' .planning/phases/04-release-coherence/04-release-measurement.md` returns at least 1
    - No scratch artefacts survive: `git status --porcelain examples crates | wc -l` returns 0
    - The workspace is undisturbed: `cargo build --workspace --offline` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'QUICKSTART elapsed time' .planning/phases/04-release-coherence/04-release-measurement.md && [ "$(grep -c 'measured under stated conditions, not a clean-machine claim' .planning/phases/04-release-coherence/04-release-measurement.md)" = "1" ] && [ "$(grep -c 'Owner: Phase 15 / PIPE' .planning/phases/04-release-coherence/04-release-measurement.md)" -ge 2 ] && grep -q 'RECON-08' .planning/phases/04-release-coherence/04-release-measurement.md && [ "$(git status --porcelain examples crates | wc -l)" = "0" ] && cargo build --workspace --offline</automated>
  </verify>
  <done>The QUICKSTART's elapsed time is recorded once with raw timestamps, re-derivable arithmetic, and an explicit statement that it is not a clean-machine claim; the page and `introduction.md` state one agreed figure decided after measuring; two deferrals carry named owners; and REL-04's documentation-review clause is discharged by citing RECON-08's recorded verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published documentation → a new developer's machine | The QUICKSTART is the project's first-contact surface; a sample that cannot compile is a defect shipped to every reader |
| measured figure → recorded claim | The gap between "what was measured here" and "what a clean machine would see" is where a dishonest number enters the record |
| scratch project → workspace tree | The substitute measurement builds outside the repository; a stray file left behind would silently change the example-target count plan 04-04 asserted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-26 | Repudiation | the recorded QUICKSTART timing | high | mitigate | The verbatim "measured under stated conditions, not a clean-machine claim" label is asserted present exactly once, and all four blocking conditions (crates.io 403, no Docker, warm caches, no API key) must be enumerated in the record |
| T-04-27 | Tampering | the documented steps | high | mitigate | Tuning, shortening or reordering the steps to influence the measurement is prohibited; Task 1 corrects the page for accuracy and Task 2 does not touch the steps again, only line 3's claim |
| T-04-28 | Spoofing | a code sample that looks correct and cannot compile | high | mitigate | The corrected imports are asserted byte-identical to lines present in `examples/basic_paladin.rs`, an example the workspace actually builds, rather than validated by inspection of an `ignore`-fenced block |
| T-04-29 | Tampering | scratch project leaking into the tree | medium | mitigate | The substitute project is created outside the repository and `git status --porcelain examples crates` is asserted empty afterwards, so the 47-target count plan 04-04 recorded cannot be silently altered |
| T-04-30 | Information disclosure | LLM API key handling | medium | mitigate | No key exists in this environment and none is to be obtained; the live-execution path is filed as a deferral rather than attempted, so no credential is written into a scratch project or a record |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | The substitute project resolves its Paladin dependencies by `path =` against this workspace and installs nothing from a registry; `04-RESEARCH.md` § "Package Legitimacy Audit" records the phase-wide verdict as "Not applicable" |
</threat_model>

<verification>
- The QUICKSTART's imports match `examples/basic_paladin.rs` line for line; no reference to the
  non-existent `paladin-ai-core` module path survives.
- Every Paladin dependency version on the page equals the converged workspace version.
- Every `cargo run --example` target named on the page resolves to a file.
- `04-release-measurement.md` carries the timing section with raw timestamps, the verbatim
  stated-conditions label, the four enumerated conditions, two owner-bearing deferral rows, and the
  RECON-08 citation.
- `quickstart.md` and `introduction.md` state one agreed figure.
- `git status --porcelain examples crates` is empty and `cargo build --workspace --offline` exits 0.
</verification>

<success_criteria>
ROADMAP Phase 4 criterion 4 — "A developer following QUICKSTART on a clean machine reaches a working
agent, and the elapsed time is recorded against the documented under-15-minute target (measured for
the first time, pass or fail)" — has its first recorded measurement, the structural defect that
would have blocked the developer regardless of environment is fixed, and the clean-machine clause
that cannot be honoured here is deferred with a named owner rather than claimed.
</success_criteria>

<output>
Create `.planning/phases/04-release-coherence/04-06-SUMMARY.md` when done.
</output>
