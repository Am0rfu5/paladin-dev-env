---
phase: 05-milestone-2-3-ground-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ledgers/milestone-02-03.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [VERIFY-01]
must_haves:
  truths:
    - "`.planning/ledgers/milestone-02-03.md` exists and contains exactly 118 rows matching `^| REQ-`, one per run-2 requirement ID, distributed across exactly 14 `### Epic ` section headings."
    - "Ledger row order is fixed by this scaffold and never re-sorted by a later plan: epic sections appear in REQUIREMENTS.md's own run-2 order (11, 12, 13, 20, 14, 15, 16, 17/17.5, 18, 19, 21, 22, 23, 24), and within each section rows appear in the ID order REQUIREMENTS.md lists them. Later plans replace a row's verdict and evidence cells in place; they never insert, delete, or reorder rows. [edge: VERIFY-01/ordering]"
    - "A requirement with no locatable artefact is never silently omitted and never left with an empty verdict cell: it receives `genuinely outstanding` when nothing in the tree satisfies it, or `present, unproven` when a citation resolves but nothing exercises it. Row count stays 118 from scaffold to close-out. [edge: VERIFY-01/empty]"
    - "Each `REQ-*` ID appears exactly once in the ledger (no duplicate IDs), while the same `file:line` citation MAY appear in more than one row — two requirements citing the same artefact keep separate rows and separate verdicts because the `REQ-*` ID is the primary key (D-00e). Citations are never merged or deduplicated across IDs. [edge: VERIFY-01/adjacency]"
    - "Exactly one plan writes `.planning/ledgers/milestone-02-03.md` in any given wave (waves 1..10 each contain at most one ledger-writing plan), so no two executors edit it concurrently. A plan interrupted part-way leaves every epic section it does not own still carrying its scaffold stub rows, so the work is re-runnable without loss. [edge: VERIFY-01/concurrency]"
    - "`.planning/REQUIREMENTS.md`'s `## Milestone 2-3 as-shipped ledger` section body is replaced by a pointer to the new ledger file (D-21/D-00d), leaving no second diverging copy of the 118 verdicts in the corpus."
    - "Epic 11's 8 rows each carry a `file:line` citation resolved against the shipped tree, and every row verdicted `satisfied` also names a test, example, or command that was actually run (D-01)."
  artifacts:
    - ".planning/ledgers/milestone-02-03.md — new file: head notes, verdict legend, 14 epic sections, 118 keyed rows"
    - ".planning/REQUIREMENTS.md — `## Milestone 2-3 as-shipped ledger` reduced to a pointer paragraph"
  key_links:
    - "REQUIREMENTS.md pointer → .planning/ledgers/milestone-02-03.md — the redirect that stops a reader consuming the superseded inline verdicts"
    - "Ledger head note → .planning/codebase/STRUCTURE.md — the D-04(a) path caveat's resolution target for every historical `src/core|application|infrastructure` PRD path"
    - "Ledger epic section headings → plans 05-05..05-12 — each later plan locates its rows by the exact `### Epic NN — ` heading text this scaffold fixes"
  prohibitions:
    - "The ledger must NOT award a `satisfied` verdict on the strength of a `file:line` citation alone, and must NOT carry REQUIREMENTS.md's ingest-era status words forward as if they were pre-satisfied verdicts."
    - "No plan in this phase may edit any `.rs` file, `Cargo.toml`, or any file under `.github/workflows/` — the code consequences of this phase's findings belong to Phase 6 CLOSE-03 and Phase 15 PIPE-02."
    - "The scaffold must NOT invent identifiers for outstanding task items; they nest under the `REQ-*` row they belong to (D-00e)."
---

<objective>
Create the Milestone 2-3 cited status ledger and prove the whole citation workflow end-to-end on one
epic before eleven more plans fan out into it.

Purpose: every later ledger plan in this phase appends into a fixed shape. If the shape, the verdict
vocabulary, the evidence bar, or the row order is wrong, it is wrong 118 times. This plan lands the
shape and immediately walks one complete epic through it — scaffold, cite, verify, verdict — so the
format is proven by use rather than by inspection.
Output: `.planning/ledgers/milestone-02-03.md` with head notes, verdict legend, 14 epic sections and
118 keyed rows (Epic 11's 8 fully cited, 110 carrying stub verdicts); `.planning/REQUIREMENTS.md`'s
Milestone 2-3 ledger section reduced to a pointer.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md
@.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md
@.planning/phases/05-milestone-2-3-ground-truth/05-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Ledger scaffold plus Epic 11 fully cited — one epic end-to-end</name>
  <files>.planning/ledgers/milestone-02-03.md</files>
  <read_first>
    - `.planning/ledgers/milestone-01.md` lines 1-19 (head notes) and 152-160 (verdict legend) — the exact shape to copy; this is the sibling ledger, not a template to reinterpret
    - `.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md` §"`.planning/ledgers/milestone-02-03.md` (new ledger)" — quoted head-note, legend, row-table and nested-item excerpts
    - `.planning/REQUIREMENTS.md` lines 2641-2890 — the 118 IDs, their epic sections, and their ingest-era status text (the input, not the answer)
    - `.planning/codebase/STRUCTURE.md` — current crate layout, required to resolve every historical `src/core|application|infrastructure` PRD path
    - `.planning/codebase/TESTING.md` — the three-tier test strategy and test-target inventory, used for the exercising-artefact search
    - `.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md` §Decisions D-00d, D-00e, D-00f, D-01, D-02, D-03, D-04
  </read_first>
  <action>
Create `.planning/ledgers/milestone-02-03.md` with, in this order:

(1) Title `# Milestone 2-3 cited status ledger`.

(2) A supersession head note stating this file supersedes `REQUIREMENTS.md`'s
`## Milestone 2-3 as-shipped ledger` section per D-21, modelled on `milestone-01.md:3-7`.

(3) A primary-key head note: the `REQ-*` requirement ID is the primary key; outstanding task items
nest under the requirement they belong to rather than receiving invented identifiers (D-00e).

(4) An evidence-bar head note stating D-01 in full: a `satisfied` verdict requires a `file:line`
citation **plus** a named passing test, example, or command that exercises it; a citation with
nothing exercising it is `present, unproven`. State explicitly that this bar applies to all 118 rows
without exception, including every row REQUIREMENTS.md's run-2 ledger already marked with an
ingest-era status word — an ingest status word is the bare "the code exists" claim the bar exists to
reject.

(5) A path-caveat head note stating both D-04 caveats **once**: (a) every
`src/core|application|infrastructure` path in the run-2 PRDs predates the Milestone 5 workspace
decomposition, current layout in `.planning/codebase/STRUCTURE.md`; (b) the Milestone-1 benchmark
files those PRDs reference have relocated into per-crate `benches/` directories. State the
consequence: a row whose only divergence from its PRD is (a) or (b) is `superseded by shipped code`
pointing at this note, not a fresh divergence write-up.

(6) A `## Verdict legend` H2 with the five-value D-00f table copied verbatim from
`milestone-01.md:154-160`. Column headers exactly `| Verdict | Meaning |`. Do not import
REQUIREMENTS.md's ingest-era status vocabulary into this legend.

(7) A `## Row order and amendment convention` H2 stating: epic sections appear in REQUIREMENTS.md's
own run-2 order and are never re-sorted; rows within a section appear in the ID order
REQUIREMENTS.md lists; later plans replace a row's Verdict and Evidence cells in place and never
insert, delete or reorder rows; the same `file:line` citation may legitimately appear in more than
one row because the `REQ-*` ID — not the citation — is the primary key; amendments follow D-00g
(edit in place, retain superseded text, date every amendment, never a separate corrections file).

(8) Exactly these 14 `### ` epic section headings, in exactly this order and with exactly this text:
`### Epic 11 — Sanctum Memory Foundation (8 IDs)`,
`### Epic 12 — Sanctum RAG Integration (8 IDs)`,
`### Epic 13 — Sentinel Vision System (13 IDs)`,
`### Epic 20 — Vision Pipeline Completion (6 IDs)`,
`### Epic 14 — Autonomous Agent Features (8 IDs)`,
`### Epic 15 — Conclave / Mixture-of-Agents (5 IDs)`,
`### Epic 16 — Advanced Battalion Patterns: Council & Grove (11 IDs)`,
`### Epic 17 / 17.5 — Flow DSL, Maneuver and CLI consolidation (11 IDs)`,
`### Epic 18 — CLI Enhancement & Polish (7 IDs)`,
`### Epic 19 — Herald & Domain Type Consolidation (5 IDs)`,
`### Epic 21 — Autonomous Agent Completion (7 IDs)`,
`### Epic 22 — Battalion & Commander Hardening (10 IDs)`,
`### Epic 23 — CLI, Config & Infrastructure Completion (10 IDs)`,
`### Epic 24 — Test Hardening, Benchmarks & QA (9 IDs)`.

(9) Under each heading, a table with header row exactly `| ID | Verdict | Evidence |` and one row per
requirement ID, IDs transcribed in order from REQUIREMENTS.md lines 2641-2890. Every row starts at
column 0 with `| REQ-`. For the 110 rows outside Epic 11, the Verdict cell holds the literal stub
token `PENDING-VERDICT` and the Evidence cell holds the plan number that will fill it, per this
assignment: Epic 12 and 15 → 05-09; Epic 13 and 20 → 05-08; Epic 14 → 05-06; Epic 16 and 18 → 05-10;
Epic 17/17.5 and 19 → 05-11; Epic 21 and 23 → 05-12; Epic 22 → 05-05; Epic 24 → 05-07.

(10) Then fill Epic 11's 8 rows completely — `REQ-embedding-port`, `REQ-openai-embedding-adapter`,
`REQ-sanctum-port`, `REQ-qdrant-sanctum-adapter-v1`, `REQ-in-memory-sanctum`,
`REQ-sanctum-domain-model`, `REQ-sanctum-configuration`, `REQ-sanctum-garrison-coexistence`. For each:
resolve the citation against the current tree (REQUIREMENTS.md:2668-2675 gives the starting path;
confirm the file exists and cite a real line number, not a bare path), then search for an exercising
artefact using the recipe in 05-RESEARCH.md §"Requirement → exercising-artefact search recipe"
(`grep -rn` for the requirement's own type or function name across `crates/ src/ tests/ examples/`,
then `ls examples/`), then run the scoped command for anything you intend to verdict `satisfied`
(prefer `cargo test --offline -p paladin-memory sanctum` and `-p paladin-ports` style scoping — never
a full-workspace run). Assign one of the five legend verdicts. A row whose citation resolves but
which nothing exercises is `present, unproven`, and that is the expected outcome for a substantial
fraction of rows (D-03) — record it plainly rather than stretching for a `satisfied`. Add the Epic 11
epic-level note from REQUIREMENTS.md:2661-2664 (the stale 111-checkbox count, not carried as forward
work) as a one-line sentence above the table.

Write the whole file in one `Write` call. Do not commit per row — the repo's pre-commit hooks compile
the workspace on every commit including markdown-only ones, so this plan commits once at the end.
  </action>
  <verify>
    <automated>test -f .planning/ledgers/milestone-02-03.md && [ "$(grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md)" = "118" ] && [ "$(grep -c '^### Epic ' .planning/ledgers/milestone-02-03.md)" = "14" ] && [ "$(sed -n '/^### Epic 11 /,/^### Epic 12 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT')" = "0" ]</automated>
    <human-check>Read three Epic 11 rows end to end and confirm the named exerciser asserts the requirement's behaviour rather than merely importing its symbol (05-VALIDATION.md §Manual-Only Verifications, row 1).</human-check>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/ledgers/milestone-02-03.md` succeeds
    - `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` equals `118`
    - `grep -c '^### Epic ' .planning/ledgers/milestone-02-03.md` equals `14`
    - `grep -o '^| REQ-[a-z0-9-]*' .planning/ledgers/milestone-02-03.md | sort | uniq -d` prints nothing (no duplicate IDs)
    - `grep -n '^### Epic ' .planning/ledgers/milestone-02-03.md` lists the headings in the order 11, 12, 13, 20, 14, 15, 16, 17 / 17.5, 18, 19, 21, 22, 23, 24
    - `grep -c '| ID | Verdict | Evidence |' .planning/ledgers/milestone-02-03.md` equals `14`
    - `grep -c '^## Verdict legend' .planning/ledgers/milestone-02-03.md` equals `1`
    - `grep -c '^## Row order and amendment convention' .planning/ledgers/milestone-02-03.md` equals `1`
    - `grep -c 'present, unproven' .planning/ledgers/milestone-02-03.md` is at least `1` (the legend row)
    - `sed -n '/^### Epic 11 /,/^### Epic 12 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `8`
    - `sed -n '/^### Epic 11 /,/^### Epic 12 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c 'PENDING-VERDICT' .planning/ledgers/milestone-02-03.md` equals `110`
    - `grep -c 'STRUCTURE.md' .planning/ledgers/milestone-02-03.md` is at least `1` (the D-04(a) caveat resolves to the current layout map)
    - Every Epic 11 row's Evidence cell contains a `:` followed by a digit (a real line number, not a bare path)
    - Every Epic 11 row verdicted `satisfied` names a test, example, or command, and that command was executed during this task
  </acceptance_criteria>
  <done>The ledger file exists with 118 keyed rows in 14 fixed-order epic sections, the five-value verdict legend, both D-04 path caveats stated once, and Epic 11's 8 rows carrying resolved citations and, where `satisfied`, an exerciser that was run.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce REQUIREMENTS.md's Milestone 2-3 ledger section to a pointer</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 2628-2638 — the `## Milestone 1 as-shipped ledger` pointer Phase 1's D-17 already wrote; this is the same file, same convention, and the exact text to model on
    - `.planning/REQUIREMENTS.md` lines 2641-2890 — the section body being replaced
    - `.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md` §"`.planning/REQUIREMENTS.md` §\"Milestone 2-3 as-shipped ledger\" (reduced to pointer)"
    - `.planning/ledgers/milestone-02-03.md` — the file just created, so the pointer's counts match reality
  </read_first>
  <action>
Replace the body of `.planning/REQUIREMENTS.md`'s `## Milestone 2-3 as-shipped ledger` section —
everything between the `## Milestone 2-3 as-shipped ledger` heading (line 2641) and the `---`
separator that precedes `## Milestone 4-6 as-shipped ledger` (line 2890) — with a pointer paragraph
modelled on the Milestone 1 pointer at lines 2628-2638.

Keep the `## Milestone 2-3 as-shipped ledger` heading itself. The replacement body must state: that
per-requirement verdicts for Milestones 2 and 3 now live in
`.planning/ledgers/milestone-02-03.md`, written as a relative markdown link
`[`.planning/ledgers/milestone-02-03.md`](ledgers/milestone-02-03.md)`; that the move is per D-21;
that the ledger carries **118** `REQ-*` rows, each with a `file:line` citation and, where the verdict
is `satisfied`, a named passing test, example, or command that exercises it; and that Phases 7, 10
and 13 add the remaining sibling ledgers (`milestone-04-06.md`, `milestone-07-08.md`,
`milestone-09-12.md`) in the same directory.

Do **not** state a nested-outstanding-item count in this task — that count is not known until every
epic section is filled. Instead write the literal placeholder sentence `Nested outstanding-item count
is finalised by plan 05-13.` as its own sentence; plan 05-13 replaces it with the real figure.

Use `Edit` with a scoped replacement. Do not use `Write` on REQUIREMENTS.md — the file is ~4,000
lines and a whole-file write would destroy the other four ledger sections.

Commit this plan's two files together in a single commit at the end of the plan, with a Bash timeout
of at least 300000 ms: the repo's `.pre-commit-config.yaml` sets `always_run: true` on the
`cargo-fmt` and `cargo-clippy` hooks, so they compile the 12-crate workspace even for a
markdown-only commit and `cargo fmt` has been observed to exceed two minutes on this machine. Do not
pass `--no-verify`. If a hook aborts, unstaged work is stashed under `~/.cache/pre-commit/patch*` —
restore from there rather than re-authoring.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^## Milestone 2-3 as-shipped ledger/,/^## Milestone 4-6 as-shipped ledger/p' .planning/REQUIREMENTS.md | grep -c '^| REQ-')" = "0" ] &amp;&amp; grep -q 'ledgers/milestone-02-03.md' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^## Milestone 2-3 as-shipped ledger/,/^## Milestone 4-6 as-shipped ledger/p' .planning/REQUIREMENTS.md | grep -c '^| REQ-'` equals `0` — no inline verdict rows survive in that section
    - `grep -c 'ledgers/milestone-02-03.md' .planning/REQUIREMENTS.md` is at least `1`
    - `grep -c '^## Milestone 2-3 as-shipped ledger' .planning/REQUIREMENTS.md` equals `1` — the heading is retained, only the body replaced
    - `grep -c '^## Milestone 1 as-shipped ledger' .planning/REQUIREMENTS.md` equals `1` and `grep -c '^## Milestone 4-6 as-shipped ledger' .planning/REQUIREMENTS.md` equals `1` — no neighbouring section was damaged by the edit
    - `sed -n '/^## Milestone 2-3 as-shipped ledger/,/^## Milestone 4-6 as-shipped ledger/p' .planning/REQUIREMENTS.md | grep -c '118'` is at least `1` — the pointer states the row count
    - `grep -c 'finalised by plan 05-13' .planning/REQUIREMENTS.md` equals `1`
    - `git log -1 --name-only` shows both `.planning/ledgers/milestone-02-03.md` and `.planning/REQUIREMENTS.md` in a single commit
  </acceptance_criteria>
  <done>REQUIREMENTS.md's Milestone 2-3 ledger section is a pointer to the new ledger file with the 118-row count stated, both neighbouring ledger sections intact, and the plan's two files committed together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/` document → future implementer or agent | The only boundary this phase has. A verdict recorded here is consumed by Phase 6's and Phase 15's planners as fact. A mis-recorded verdict crosses into a real code change or a real omission downstream. There is no runtime, network, process, or user-input boundary in this plan — it writes markdown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering (of the record) | `.planning/ledgers/milestone-02-03.md` verdict cells | medium | mitigate | A row that records an unwired or absent capability as `satisfied` would cause Phase 6 to skip real work. Mitigated by D-01's two-part evidence bar enforced as this plan's acceptance criteria: every `satisfied` row names an exerciser that was executed, and every Evidence cell carries a real line number. |
| T-05-04 | Repudiation | `.planning/REQUIREMENTS.md` §Milestone 2-3 ledger | low | mitigate | Replacing the inline section could destroy the ingest-run-2 record. Mitigated because the ledger file transcribes every one of the 118 IDs before the pointer reduction runs (task order is fixed within this plan), the pointer names the successor file, and git retains the prior body. |
| T-05-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable and not fabricated: this plan installs no packages and touches no manifest. `05-RESEARCH.md` §"Package Legitimacy Audit" records the same, verified. |

No high or critical threats. Under ASVS L1 with a blocking threshold of `high`, nothing in this plan
blocks. The honest surface is a documentation-integrity surface, not a security surface.
</threat_model>

<probe_fallback_disposition>
This phase has no `*-SPEC.md`, so the spec-less probe fallback supplied the edge predicates. The
deterministic probe produced **20 applicable edges, 0 pre-resolved**. Full phase-wide disposition —
recorded here once, in the tracer plan, so the no-silent-drop equality is auditable in one place.
Each item is authored into exactly one plan's `must_haves`.

| # | Requirement | Category | Resolution | Authored in |
|---|---|---|---|---|
| 1 | VERIFY-01 | adjacency | covered | 05-01 |
| 2 | VERIFY-01 | empty | covered | 05-01 |
| 3 | VERIFY-01 | ordering | covered | 05-01 |
| 4 | VERIFY-01 | concurrency | covered | 05-01 |
| 5 | VERIFY-02 | adjacency | covered | 05-05 |
| 6 | VERIFY-02 | empty | **backstop** | 05-05 |
| 7 | VERIFY-02 | ordering | covered | 05-05 |
| 8 | VERIFY-03 | adjacency | covered | 05-02 |
| 9 | VERIFY-03 | empty | covered | 05-02 |
| 10 | VERIFY-03 | ordering | covered | 05-02 |
| 11 | VERIFY-03 | concurrency | covered | 05-02 |
| 12 | VERIFY-04 | unclassified | **unresolved — flagged planner assumption** | 05-03 |
| 13 | VERIFY-05 | boundary | covered | 05-04 |
| 14 | VERIFY-05 | adjacency | covered | 05-04 |
| 15 | VERIFY-05 | empty | covered | 05-04 |
| 16 | VERIFY-05 | ordering | covered | 05-04 |
| 17 | VERIFY-05 | precision | covered | 05-04 |
| 18 | VERIFY-06 | empty | covered | 05-03 |
| 19 | VERIFY-06 | encoding | **backstop** | 05-03 |
| 20 | VERIFY-06 | concurrency | covered | 05-03 |

**No-silent-drop equality: 20 probe-surfaced = 17 covered + 2 backstop + 1 flagged assumption.**
The single `unclassified` row (VERIFY-04) is never auto-`backstop`ped and never dropped; it is
surfaced as a flagged assumption in plan 05-03.

Recalled prohibitions (Stage 2 survivors, authored **descriptor-less** — no `check_*` scalars, each
disposes flagged-unverified):

| # | Prohibition | Authored in |
|---|---|---|
| P1 | No `satisfied` verdict on a citation alone; ingest-era status words are not carried forward as pre-satisfied | 05-01 (governs all ledger plans) |
| P2 | The release-notes correction must not delete, replace, or rewrite the original defective text | 05-02 |
| P3 | A VERIFY-02 block verdict must not be derived from checkbox arithmetic | 05-05, 05-06, 05-07 |
| P4 | No plan in this phase may edit any `.rs`, `Cargo.toml`, or CI workflow file | every plan |
| P5 | ADR-0011 must not record the encryption capability as shipped without stating it has zero consumers | 05-03 |
| P6 | ADR-0006's amendment must not introduce a second binding coverage floor or re-measure coverage | 05-04 |

No canon-referral drops were needed — none of the survivors maps to OWASP, prototype pollution,
path traversal, injection, GDPR, or generic fairness canon.
</probe_fallback_disposition>

<artifacts>
## Artifacts this phase produces

Six artefacts plus one source reduction. This plan produces the first and the last of them.

| Artefact | Status | Owning plan | Requirement |
|---|---|---|---|
| `.planning/ledgers/milestone-02-03.md` | **NEW** — 118 `REQ-*` rows with `file:line`-cited verdicts, 14 epic sections, three VERIFY-02 block verdicts | 05-01 creates; 05-05..05-12 fill; 05-13 closes out | VERIFY-01, VERIFY-02 |
| `.planning/decisions/0010-milestone-3-epic-numbering.md` | **NEW** ADR | 05-02 | VERIFY-03 |
| `.planning/decisions/0011-vision-port-surfaces.md` | **NEW** ADR | 05-03 | VERIFY-04 |
| `.planning/decisions/0012-live-api-test-key-behaviour.md` | **NEW** ADR | 05-03 | VERIFY-06 |
| `.planning/decisions/0006-coverage-gate.md` | **AMENDED IN PLACE** — never a second ADR | 05-04 | VERIFY-05 |
| `.project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` | **CORRECTED IN PLACE** — annotated, superseded text retained | 05-02 | VERIFY-03 |
| `.planning/REQUIREMENTS.md` §`## Milestone 2-3 as-shipped ledger` | **REDUCED TO A POINTER** (D-21) | 05-01 opens, 05-13 finalises counts | VERIFY-01 |
| `.planning/decisions/PROMOTION.md` "Next free ADR number" line | **UPDATED** 0010 → 0013 | 05-13 | VERIFY-03, VERIFY-04, VERIFY-06 |

New section headings introduced into existing documents by this plan: none in
`REQUIREMENTS.md` (the existing `## Milestone 2-3 as-shipped ledger` heading is retained, only its
body replaced). New headings created inside the new ledger file: `## Verdict legend`,
`## Row order and amendment convention`, and the 14 `### Epic NN — ` section headings listed in
Task 1.
</artifacts>

<verification>
- `test -f .planning/ledgers/milestone-02-03.md`
- `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` equals `118`
- `grep -c '^### Epic ' .planning/ledgers/milestone-02-03.md` equals `14`
- `grep -o '^| REQ-[a-z0-9-]*' .planning/ledgers/milestone-02-03.md | sort | uniq -d` prints nothing
- `sed -n '/^### Epic 11 /,/^### Epic 12 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `8`
- `sed -n '/^## Milestone 2-3 as-shipped ledger/,/^## Milestone 4-6 as-shipped ledger/p' .planning/REQUIREMENTS.md | grep -c '^| REQ-'` equals `0`
- `git status --porcelain` is clean after the plan's single commit
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty — this plan changed no code
</verification>

<success_criteria>
The Milestone 2-3 ledger exists in the shape every later plan appends into: 118 keyed rows in 14
fixed-order epic sections, the five-value verdict legend, both D-04 path caveats stated once, and the
row-order and amend-in-place conventions written down. Epic 11 is fully cited to the D-01 bar,
proving the citation workflow end-to-end. REQUIREMENTS.md no longer holds a second copy of the 118
verdicts.
</success_criteria>

<output>
Create `.planning/phases/05-milestone-2-3-ground-truth/05-01-SUMMARY.md` when done. Record: the
verdict distribution for Epic 11, every scoped command actually executed, and any Epic 11 citation
that did not resolve at the path REQUIREMENTS.md recorded.
</output>
