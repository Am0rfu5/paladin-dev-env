---
phase: 05-milestone-2-3-ground-truth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/decisions/0010-milestone-3-epic-numbering.md
  - .project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md
autonomous: true
requirements: [VERIFY-03]
must_haves:
  truths:
    - "`.planning/decisions/0010-milestone-3-epic-numbering.md` exists and records the authoritative Milestone 3 numbering — 19 Herald & Domain Type Consolidation, 20 Vision Pipeline Completion, 21 Autonomous Agent Completion, 22 Battalion & Commander Hardening, 23 CLI/Config/Infrastructure Completion, 24 Test Hardening — in the shipped seven-H2 ADR shape with no YAML frontmatter."
    - "Where the release-notes number and the authoritative number collide on the same integer (release notes assert 20 = Council; authoritative 20 = Vision Pipeline Completion), ADR-0010's mapping table carries one row per integer listing BOTH the release-notes-asserted content and the authoritative content. The collision is recorded, never silently resolved by picking one side. [edge: VERIFY-03/adjacency]"
    - "Every one of the five defective `### Epic 19..23` headings in the release notes receives an inline correction, and an authoritative epic with no corresponding release-notes section is listed in ADR-0010's mapping table as not covered by the release notes rather than omitted from the table. [edge: VERIFY-03/empty]"
    - "ADR-0010's mapping table is ordered by authoritative epic number ascending (19, 20, 21, 22, 23, 24). The release-notes edits are applied bottom-up by line number — 320, 160, 147, 111, 106, 76, 48, 21 — so that each cited line number is still valid at the moment its edit is made and no earlier insertion shifts a later target. [edge: VERIFY-03/ordering]"
    - "`.project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` is written by exactly one plan in exactly one wave. The top-of-file correction banner is written LAST, so its presence is the completion sentinel: a banner with fewer than the full set of inline corrections below it is detectable, and a partially applied correction leaves no banner at all. [edge: VERIFY-03/concurrency]"
    - "The release-notes correction is annotation, not rewriting: the original defective heading text and the original `RoutingStrategy::PerformanceBased` claim remain readable in the file, marked superseded, alongside the correction (D-08, D-00g)."
    - "The forward-look section at line 320 is marked a superseded point-in-time forward-look, with the reason stated (vision shipped — `intel/code-verification.md` records Sentinel vision as verified shipped)."
  artifacts:
    - ".planning/decisions/0010-milestone-3-epic-numbering.md — new ADR, Code Conformance `conforms`"
    - ".project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md — corrected in place with a dated banner and per-claim inline corrections"
  key_links:
    - "ADR-0010 → RELEASE_NOTES_MILESTONE_3.md — the ADR is what downstream phases cite; the document edit is what stops the defect propagating to the next human reader"
    - "ADR-0010's authoritative numbering → ledger plans 05-08..05-12 — Epic 15 (Conclave), Epic 16 (Council/Grove) and Epic 17 (Maneuver) ledger rows cite this corrected mapping when recording the release-notes attribution defect"
    - "ADR-0010 → `.planning/decisions/PROMOTION.md` numbering index — plan 05-13 advances the next-free number past 0010"
  prohibitions:
    - "The release-notes correction must NOT delete, replace, or rewrite the original defective text. `.project/` is this project's historical ingest corpus and five ingest runs were built on its provenance; silently rewriting history destroys the audit trail that makes every downstream citation checkable."
    - "No plan in this phase may edit any `.rs` file, `Cargo.toml`, or any file under `.github/workflows/` — the code consequences of this phase's findings belong to Phase 6 CLOSE-03 and Phase 15 PIPE-02."
    - "ADR-0010 must NOT assert that any Rust source file is wrong. The defect is documentary; `Code Conformance` is `conforms` and the executing work is this plan's own document edit."
---

<objective>
Record the Milestone 3 epic-numbering defect once and permanently as ADR-0010, and correct the one
document that propagates it.

Purpose: `RELEASE_NOTES_MILESTONE_3.md` teaches 19=Conclave / 20=Council / 21=Grove / 22=Maneuver /
23=Commander. Eight of nine Milestone-3 documents and every task list say otherwise. Every citation
Phases 6-16 make against a Milestone 3 epic number depends on which of the two schemes the citer
meant — so this correction is the provenance key for the rest of the milestone, and it lands early
(wave 1) so later ledger plans can cite it.
Output: ADR-0010; an annotated, non-destructive correction to the release notes.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md
@.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md
@.planning/phases/05-milestone-2-3-ground-truth/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ADR-0010 — Milestone 3 epic numbering</name>
  <files>.planning/decisions/0010-milestone-3-epic-numbering.md</files>
  <read_first>
    - `.planning/decisions/0004-temperature-validation.md` — read in full; the worked ADR example this repo ships, including how `Considered Options` is bulleted and how `Downstream Consumers` names phases and requirements rather than files
    - `.planning/decisions/0001-battalion-config.md` — the shape source for the seven-H2 sequence
    - `.planning/decisions/PROMOTION.md` — the numbering index (`Next free ADR number: 0010` at line 33) and the required heading set
    - `.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md` §"`.planning/decisions/0010-*.md` … `0012-*.md` (new ADRs)" — the confirmed heading sequence and the exact Status-block shape
    - `.planning/REQUIREMENTS.md` lines 440-452 — VERIFY-03 in full, including the authoritative six-epic mapping
    - `.planning/intel/code-verification.md` §"Milestone 3 epic numbering" and §"Release-notes forward-look is stale"
    - `.planning/INGEST-CONFLICTS.md` warnings 1, 2, 12, 13, 14
    - `.project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` — the document under correction; note its epic headings at lines 21, 48, 76, 111, 147, 160
  </read_first>
  <action>
Create `.planning/decisions/0010-milestone-3-epic-numbering.md` with no YAML frontmatter and exactly
these seven H2 headings in this order: `## Status`, `## Context`, `## Decision`,
`## Considered Options`, `## Code Locations`, `## Code Conformance`, `## Downstream Consumers`. The
H1 is `# ADR-0010: Milestone 3 epic numbering`. `## Status` body is `Accepted` followed by a
`**Date:** 2026-08-04` line, matching `0004-temperature-validation.md:1-7`.

`## Context` states the conflict: `RELEASE_NOTES_MILESTONE_3.md` asserts 19=Conclave, 20=Council,
21=Grove, 22=Maneuver, 23=Commander Enhancement; the plan/epic-definition set, agreed by eight of
nine Milestone-3 documents and every task list, is 19=Herald & Domain Type Consolidation, 20=Vision
Pipeline Completion, 21=Autonomous Agent Completion, 22=Battalion & Commander Hardening,
23=CLI/Config/Infrastructure Completion, 24=Test Hardening. State the real-world consequence: the
content the release notes attributes to Milestone 3 Epics 19-22 (Conclave, Council, Grove, Maneuver)
is Milestone **2** Epic 15, Epic 16 and Epic 17/17.5 work, so a reader following the release notes
looks for those features in the wrong milestone's ledger.

`## Decision` states that the plan/epic-definition set is authoritative and is the numbering every
Phase 5-16 citation uses, and includes a mapping table with header row exactly
`| Epic | Authoritative content | Release-notes assertion | Actual home of the release-notes content |`
and one row per integer 19, 20, 21, 22, 23, 24 in ascending order. Each row carries BOTH sides of the
collision — an integer where both schemes assert content gets both recorded on the one row, never a
silent choice of one. Epic 24 (Test Hardening) has no colliding release-notes assertion; record its
Release-notes assertion cell as `— (release notes' Epic 24 heading at :160 already matches)` rather
than omitting the row.

`## Considered Options` must be a bulleted list (the parser's `splitEntries` yields structured
entries only from `-`/`*`/`+`/numbered lines; a prose paragraph collapses into one opaque blob).
Include at least: the release-notes numbering as the authoritative set — rejected, one document
against eight plus every task list; renumbering the plan/epic-definition set to match the release
notes — rejected, it would falsify every task-list filename and the ROADMAP's own phase text;
recording the conflict without choosing — rejected, VERIFY-03 requires the defect recorded "once and
permanently" and a recorded non-answer leaves every downstream citation ambiguous.

`## Code Locations` must be a bulleted list. Cite the defective document's own lines:
`.project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md:21`, `:48`, `:76`, `:106`, `:111`,
`:147`, `:320`, naming what sits at each. Include the two verified-absent claims: the
`RoutingStrategy::PerformanceBased` bullet at `:106`, which contradicts Epic 16 non-goal NG-3 and is
absent from `crates/paladin-battalion/src/grove_service.rs` and
`crates/paladin-core/src/platform/container/battalion/grove.rs`; and the Council and Maneuver API
forms that disagree with the shipped surfaces (`convene` versus `execute` and `conclusion` versus
`summary` on `crates/paladin-battalion/src/council_service.rs`; the Maneuver constructor argument
order on `crates/paladin-battalion/src/maneuver/mod.rs`). Confirm each absence against the tree with
`grep -rn` before writing it as a citation — do not transcribe the claim from REQUIREMENTS.md
without checking.

`## Code Conformance` is `conforms`, with one paragraph explaining that this is a documentation
defect: no Rust source is wrong, and the executing work is this plan's own Task 2 edit to the release
notes.

`## Downstream Consumers` must be a bulleted list naming Phase 5 ledger plans 05-08 through 05-12
(which cite this mapping when recording the Epic 15 / 16 / 17 attribution defect), plan 05-13 (which
advances PROMOTION.md's next-free-ADR line), and any Phase 6-16 reader citing a Milestone 3 epic
number.
  </action>
  <verify>
    <automated>test -f .planning/decisions/0010-milestone-3-epic-numbering.md && [ "$(grep -c '^## ' .planning/decisions/0010-milestone-3-epic-numbering.md)" = "7" ] && head -1 .planning/decisions/0010-milestone-3-epic-numbering.md | grep -q '^# ADR-0010: '</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/decisions/0010-milestone-3-epic-numbering.md` succeeds
    - `head -1 .planning/decisions/0010-milestone-3-epic-numbering.md` is exactly `# ADR-0010: Milestone 3 epic numbering`
    - `head -1 .planning/decisions/0010-milestone-3-epic-numbering.md` does not begin with `---` (no YAML frontmatter, matching 0001-0009)
    - `grep -n '^## ' .planning/decisions/0010-milestone-3-epic-numbering.md` prints exactly seven headings in the order `## Status`, `## Context`, `## Decision`, `## Considered Options`, `## Code Locations`, `## Code Conformance`, `## Downstream Consumers`
    - The first non-blank line under `## Status` is `Accepted`
    - `sed -n '/^## Considered Options/,/^## Code Locations/p' .planning/decisions/0010-milestone-3-epic-numbering.md | grep -c '^- '` is at least `3` (bulleted, not prose — parser requirement)
    - `sed -n '/^## Code Locations/,/^## Code Conformance/p' .planning/decisions/0010-milestone-3-epic-numbering.md | grep -c '^- '` is at least `5` (bulleted, not prose)
    - The mapping table under `## Decision` contains exactly six rows matching `^| 19 \|^| 20 \|^| 21 \|^| 22 \|^| 23 \|^| 24 ` — one per authoritative epic, ascending
    - `sed -n '/^## Code Conformance/,/^## Downstream Consumers/p' .planning/decisions/0010-milestone-3-epic-numbering.md | grep -c 'conforms'` equals `1`
    - `grep -c 'Herald & Domain Type Consolidation' .planning/decisions/0010-milestone-3-epic-numbering.md` is at least `1`
    - `grep -c 'PerformanceBased' .planning/decisions/0010-milestone-3-epic-numbering.md` is at least `1`
    - `grep -rn 'PerformanceBased' crates/ src/` was run during this task and its result (present or absent) is what the ADR records
    - `node .claude/gsd-core/bin/lib/adr-parser.cjs` is not required to run; the seven-heading shape is the contract
  </acceptance_criteria>
  <done>ADR-0010 exists in the shipped seven-H2 ADR shape, records the authoritative 19-24 mapping with both sides of every colliding integer, cites the defective document's exact lines, and declares `conforms`.</done>
</task>

<task type="auto">
  <name>Task 2: Correct RELEASE_NOTES_MILESTONE_3.md in place — annotate, never rewrite</name>
  <files>.project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md</files>
  <read_first>
    - `.project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` — read in full (349 lines) before editing; the correction sites are at lines 21, 48, 76, 106, 111, 147, 160 and 320
    - `.planning/ROADMAP.md` lines 8-13 — this project's own inline `**Amended by Phase N, dated ..., citing ...**` self-amendment banner; the structural pattern to reproduce
    - `.planning/ledgers/milestone-01.md` lines 21-29 — the section-level amendment preamble, the closer fit for a document-level banner
    - `.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md` §"`.project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` (corrected in place)" — the five structural features to reproduce, quoted
    - `.planning/decisions/0010-milestone-3-epic-numbering.md` — the ADR this correction cites, written by Task 1
    - `.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md` §D-08
  </read_first>
  <reversibility rating="costly">
    The corrected epic numbering becomes the provenance key every Phase 6-16 citation uses. Reverting
    means re-checking which of two numbering schemes each downstream citation meant. Not one-way —
    the original text is retained in place and git holds the prior revision — so this proceeds
    without a decision checkpoint, but it is recorded as costly rather than free.
  </reversibility>
  <action>
Apply three correction classes to `.project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md`
using `Edit` with scoped replacements. Never `Write` this file.

**Apply the edits bottom-up by line number — 320, 160, 147, 111, 106, 76, 48, 21 — then the banner
last.** Editing top-down shifts every subsequent line number and invalidates the remaining targets.
The banner is written last so that its presence in the file means the full correction set landed.

(a) **Epic renumbering, at lines 21, 48, 76, 111, 147.** For each of the five `### Epic NN: <title>`
headings, keep the original heading text visible using markdown strikethrough and follow it with the
authoritative title on the same heading line, then add a bold `**Corrected numbering (ADR-0010):**`
line immediately beneath naming where that section's actual content lives. Concretely: the section at
:21 (Conclave) is Milestone 2 Epic 15; :48 (Council) is Milestone 2 Epic 16; :76 (Grove) is Milestone
2 Epic 16; :111 (Maneuver) is Milestone 2 Epic 17 / 17.5; :147 (Commander Enhancement) is Milestone 3
Epic 22, Battalion & Commander Hardening. The authoritative titles for the integers themselves are
19 Herald & Domain Type Consolidation, 20 Vision Pipeline Completion, 21 Autonomous Agent Completion,
22 Battalion & Commander Hardening, 23 CLI/Config/Infrastructure Completion. Leave the heading at
:160 (Epic 24, Test Hardening) uncorrected — it already matches — but add a one-line note there
confirming it is the one heading that agrees with the authoritative set, so a reader does not read
its absence from the correction list as an oversight.

(b) **The `PerformanceBased` claim at line 106.** Keep the original bullet text visible, struck
through, and follow it with a bold correction stating that `RoutingStrategy::PerformanceBased` is
verified absent from the tree and contradicts Epic 16 non-goal NG-3, citing the shipped
`RoutingStrategy` variants you read from
`crates/paladin-core/src/platform/container/battalion/grove.rs`. Read that file and name the actual
shipped variants — do not write "the shipped variants" as a placeholder.

Apply the same treatment to the Council and Maneuver API-form claims wherever they appear in the
Epic 20 (:48) and Epic 22 (:111) sections: strike the divergent form, state the shipped form, and
cite it from `crates/paladin-battalion/src/council_service.rs` and
`crates/paladin-battalion/src/maneuver/mod.rs` respectively.

(c) **The forward-look at line 320.** Prefix the `## 🔮 What's Next (Milestone 4)` section with a
bold superseded marker stating it is a point-in-time forward-look, dated 2026-08-04, superseded
because Sentinel vision shipped — cite `.planning/intel/code-verification.md`'s record of vision as
verified shipped. Retain the section's body unchanged beneath it.

(d) **The banner, written last**, as a blockquote immediately after the H1 at line 1: a
`> **Correction (dated 2026-08-04, ADR-0010):**` opener naming what was wrong (this document's Epic
19-24 numbering does not match the authoritative plan/epic-definition set, and two further claims are
verified absent from the tree), pointing at
`.planning/decisions/0010-milestone-3-epic-numbering.md`, and stating that original text is retained
below with inline corrections and nothing is deleted.

Commit both of this plan's files in a single commit at the end, with a Bash timeout of at least
300000 ms: `.pre-commit-config.yaml` sets `always_run: true` on the `cargo-fmt` and `cargo-clippy`
hooks so they compile the workspace even for a markdown-only commit. Do not pass `--no-verify`. If a
hook aborts, unstaged work is stashed under `~/.cache/pre-commit/patch*`.
  </action>
  <verify>
    <automated>grep -q 'Correction (dated 2026-08-04, ADR-0010)' .project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md && [ "$(grep -c 'Corrected numbering (ADR-0010)' .project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md)" = "5" ] && [ "$(grep -c 'Conclave Pattern (Multi-Expert Synthesis)' .project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md)" -ge "1" ]</automated>
    <human-check>Confirm the original Epic 19-23 numbering is still readable in the file, marked superseded, alongside the corrected set (05-VALIDATION.md §Manual-Only Verifications, row 3).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Correction (dated 2026-08-04, ADR-0010)' .project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` equals `1` and the match sits above every `### Epic ` heading
    - `grep -c 'Corrected numbering (ADR-0010)' .project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` equals `5`
    - `grep -c 'Conclave Pattern (Multi-Expert Synthesis)' .project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` is at least `1` — the original defective heading text is retained, not deleted
    - `grep -c 'Council Pattern (Iterative Discussion)' .project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` is at least `1`
    - `grep -c 'Grove Pattern (Contextual Routing)' .project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` is at least `1`
    - `grep -c 'Maneuver Pattern (Flow DSL)' .project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` is at least `1`
    - `grep -c 'Commander Enhancement' .project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` is at least `1`
    - `grep -c 'PerformanceBased' .project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` is at least `1` — the original claim is retained, struck through, with the correction beside it
    - `grep -c "What's Next (Milestone 4)" .project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` is at least `1` and a superseded marker appears within the three lines preceding it
    - `wc -l .project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` is greater than `349` — the file grew; a shrunken file means text was deleted rather than annotated
    - `grep -c 'Herald & Domain Type Consolidation' .project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` is at least `1`
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
    - `git log -1 --name-only` shows both `.planning/decisions/0010-milestone-3-epic-numbering.md` and `.project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` in one commit
  </acceptance_criteria>
  <done>The release notes carry a dated top-of-file correction banner citing ADR-0010, five inline heading corrections, corrections for the `PerformanceBased` and Council/Maneuver API claims, and a superseded marker on the Milestone 4 forward-look — with every original claim still readable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.project/` historical ingest corpus → future reader or ingest run | `.project/` is the provenance five ingest runs were built on. An edit that removes rather than annotates breaks the ability to audit what was originally claimed. |
| `.planning/decisions/` ADR → Phase 6-16 planners | ADR-0010 sits at the top of this project's precedence order (D-00b). Every Milestone 3 epic citation downstream resolves through it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02 | Repudiation | `.project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` | low | mitigate | A rewrite that deletes the superseded text destroys the audit trail for what the document originally asserted. Mitigated by D-08's annotate-don't-rewrite rule enforced as acceptance criteria: each original heading string must still be greppable after the edit, and the file's line count must grow. |
| T-05-05 | Tampering (of the record) | ADR-0010 mapping table | medium | mitigate | An ADR that silently picks one side of a colliding integer would make half the downstream citations resolve wrong without anyone noticing. Mitigated by requiring both sides recorded on one row per integer, and by requiring the two verified-absent claims be re-confirmed with `grep -rn` against the tree rather than transcribed from REQUIREMENTS.md. |
| T-05-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable and not fabricated: this plan installs no packages and touches no manifest. |

No high or critical threats. Under ASVS L1 with a blocking threshold of `high`, nothing in this plan
blocks. The realistic surface is documentary provenance, not a runtime security surface.
</threat_model>

<artifacts>
## Artifacts this phase produces

Phase-wide artefact set (recorded in full in plan 05-01's `<artifacts>` section). This plan owns two:

| Artefact | Status | This plan's contribution |
|---|---|---|
| `.planning/decisions/0010-milestone-3-epic-numbering.md` | **NEW** ADR, seven H2 headings, no frontmatter, `Code Conformance: conforms` | created in full (Task 1) |
| `.project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` | **CORRECTED IN PLACE** — annotated, superseded text retained | banner + 5 heading corrections + `PerformanceBased` correction + Council/Maneuver API-form corrections + forward-look superseded marker (Task 2) |

New section headings introduced into existing documents: none — the release-notes correction adds a
blockquote banner and inline bold correction lines, not new headings. New headings created in the new
ADR: `## Status`, `## Context`, `## Decision`, `## Considered Options`, `## Code Locations`,
`## Code Conformance`, `## Downstream Consumers`.
</artifacts>

<verification>
- `test -f .planning/decisions/0010-milestone-3-epic-numbering.md`
- `grep -c '^## ' .planning/decisions/0010-milestone-3-epic-numbering.md` equals `7`
- `grep -c 'Corrected numbering (ADR-0010)' .project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` equals `5`
- `wc -l .project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` is greater than `349`
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
- `git status --porcelain` is clean after the plan's single commit
</verification>

<success_criteria>
Reading `RELEASE_NOTES_MILESTONE_3.md` no longer teaches the wrong epic numbers: the authoritative
19-24 set is visible at every defective heading, the `RoutingStrategy::PerformanceBased` and
Council/Maneuver API claims are corrected against the shipped surfaces, and the Milestone 4
forward-look is marked superseded — with every original claim retained and marked, and the whole
correction traceable to ADR-0010.
</success_criteria>

<output>
Create `.planning/phases/05-milestone-2-3-ground-truth/05-02-SUMMARY.md` when done. Record: the
shipped `RoutingStrategy` variants actually read from the tree, the shipped Council and Maneuver API
forms actually read from the tree, and any release-notes claim found defective that D-08 did not
anticipate.
</output>
