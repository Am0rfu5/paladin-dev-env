---
phase: 05-milestone-2-3-ground-truth
plan: 05
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - .planning/ledgers/milestone-02-03.md
autonomous: true
requirements: [VERIFY-02, VERIFY-01]
must_haves:
  truths:
    - "`.planning/ledgers/milestone-02-03.md` carries a written block verdict for `tasks-epic22-battalion-commander-hardening.md`, backed by a parent-task cluster table with one row per parent task, each row carrying a verdict and evidence."
    - "The block verdict is `satisfied by shipped code` only if every parent-task cluster verifies; otherwise it is `partially outstanding` and the failing clusters are named — and those named clusters are exactly Phase 6 CLOSE-02's scope for this block and nothing else."
    - "The cluster table records one row per parent-task number. Where a task-list file states the same parent-task number twice (a later restatement marking it complete), both line occurrences are cited on the one row and the row states which occurrence the verdict follows — duplicates are never silently deduplicated to the first occurrence, and two clusters making the same capability claim are never merged. [edge: VERIFY-02/adjacency]"
    - "Cluster-table rows appear in the source task-list file's own parent-task numeric order (0.0, 1.0, 2.0 …). Ties are impossible within a row set because each row is keyed by a distinct parent-task number. [edge: VERIFY-02/ordering]"
    - statement: "Each parent-task cluster's verdict is justified against the cluster's distinct capability claim rather than against its checkbox count. A cluster whose open items are entirely stale — the capability ships — is recorded `satisfied by shipped code` with the shipping artefact cited, and a block whose clusters all verify records `satisfied by shipped code` rather than being omitted or left blank."
      verification: backstop
    - "Epic 22's 10 `REQ-*` ledger rows are filled to the D-01 evidence bar in the same pass, so the epic is read once rather than twice."
    - "`REQ-grove-llm-routing`'s row records the hardcoded `model: \"gpt-4\"` at `crates/paladin-battalion/src/grove_service.rs:537` as verified open and assigns it to Phase 6 / CLOSE-01 — the defect is recorded, not fixed."
  artifacts:
    - ".planning/ledgers/milestone-02-03.md §Epic 22 — 10 cited rows plus a block verdict and parent-task cluster table"
  key_links:
    - "Epic 22 block verdict's named failing clusters → Phase 6 CLOSE-02 — this is the scope-setting output; CLOSE-02's size is unknowable until this plan reports"
    - "`intel/task-completion-state.md` → the 81 open-item count — authoritative, transcribed, never re-derived by a fresh grep"
    - "`REQ-grove-llm-routing` row → Phase 6 CLOSE-01 — the one run-2 defect verified open against the tree"
  prohibitions:
    - "A block verdict must NOT be derived from checkbox arithmetic. VERIFY-02's own wording requires a verdict per block, not a task list derived from counting — run 1 and run 2 both proved checkbox state understates reality."
    - "Do NOT re-derive the 81 open-item count with a fresh grep. `intel/task-completion-state.md` is authoritative; a naive count of literal checkbox strings picks up the shared 'Instructions for Completing Tasks' boilerplate examples that are not real task checkboxes."
    - "No plan in this phase may edit any `.rs` file, `Cargo.toml`, or any file under `.github/workflows/`. In particular, do NOT fix the hardcoded routing model in `grove_service.rs` while verifying this epic — record the row and move on; that fix is Phase 6 CLOSE-01."
    - "The ledger must NOT award a `satisfied` verdict on the strength of a `file:line` citation alone."
---

<objective>
Produce the written block verdict for `tasks-epic22-battalion-commander-hardening.md` — the deepest
of the three unverified blocks, and the one with the strongest corroboration — and fill Epic 22's 10
ledger rows in the same pass.

Purpose: this block's verdict sets Phase 6's CLOSE-02 scope. REQUIREMENTS.md and ROADMAP.md both say
Phase 6's size is unknowable until VERIFY-02 reports, and the roadmap's own range is "0 to 155 items".
A verdict of `partially outstanding` with named clusters gives Phase 6 a named scope; a checkbox
count gives it a mood. This block runs first among the three because it is the largest and the most
corroborated — `codebase/CONCERNS.md` records a real open TODO inside its scope.
Output: Epic 22's ledger section, complete: 10 cited `REQ-*` rows, a parent-task cluster table, and
one block verdict sentence.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md
@.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md
@.planning/phases/05-milestone-2-3-ground-truth/05-RESEARCH.md
@.planning/phases/05-milestone-2-3-ground-truth/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Epic 22's parent-task clusters and write the block verdict</name>
  <files>.planning/ledgers/milestone-02-03.md</files>
  <read_first>
    - `.project/Milestone_3-Completion/Epic_22/tasks-epic22-battalion-commander-hardening.md` — read in full. Its parent tasks are numbered `0.0` through `14.0`; three carry open state at lines 123 (`3.0 Integrate Paladin Registry into Council service (US-22.1)`), 142 (`4.0 Integrate Paladin Registry into Grove service (US-22.1)`) and 164 (`5.0 Implement Grove LLM-based routing (US-22.2)`)
    - `.planning/intel/task-completion-state.md` — the authoritative 81 open-item count for this file; transcribe it, do not re-derive it
    - `.planning/ledgers/milestone-01.md` lines 140-150 — the `ID | Verdict | Evidence`-with-named-owner table shape, the closest existing analog for a cluster table
    - `.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md` §"VERIFY-02 block-verdict table pattern (D-05 parent-task cluster table)"
    - `.planning/ledgers/milestone-02-03.md` — the scaffold, for the exact `### Epic 22 — Battalion & Commander Hardening (10 IDs)` heading and the existing stub rows
    - `crates/paladin-battalion/src/council_service.rs`, `crates/paladin-battalion/src/grove_service.rs`, `crates/paladin-battalion/src/in_memory_registry.rs`, `crates/paladin-ports/src/output/paladin_registry.rs` — the tree the three open clusters claim capability over
    - `.planning/codebase/CONCERNS.md` §"Grove Service Model Hardcoded"
    - `.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md` §D-05, D-06
  </read_first>
  <action>
Under the existing `### Epic 22 — Battalion & Commander Hardening (10 IDs)` heading in
`.planning/ledgers/milestone-02-03.md`, add a block-verdict subsection **above** the `| ID | Verdict |
Evidence |` table, using `Edit` with a scoped replacement. Never `Write` the ledger file — other
epic sections belong to other plans.

The subsection contains, in order:

(1) A one-sentence block verdict for `tasks-epic22-battalion-commander-hardening.md`, valued
`satisfied by shipped code`, `partially outstanding`, or `deferred with reason`. Per D-06, it is
`satisfied by shipped code` only if every parent-task cluster verifies; if any cluster fails, it is
`partially outstanding` and the sentence names the failing clusters explicitly, followed by the
statement that those named clusters are exactly Phase 6 CLOSE-02's scope for this block and nothing
else.

(2) A sentence transcribing the open-item count from `.planning/intel/task-completion-state.md` and
citing that file. Do not produce this number with a fresh count of checkbox strings.

(3) A table with header row exactly `| Parent task | Verdict | Evidence |`, one row per parent task
in the source file, in ascending parent-task numeric order (`0.0`, `1.0`, `2.0`, … `14.0`). For each
row: read the parent task's open subtask lines as **one capability claim**, then verify that claim
against the tree, then record a verdict and the `file:line` evidence that supports it. The three
clusters carrying open state — `3.0` Council registry integration, `4.0` Grove registry integration,
`5.0` Grove LLM-based routing — get first-pass depth and the deepest evidence. Clusters already
marked complete still get a row, verdicted against the tree rather than against their checkbox state,
because the dominant corpus pattern is the record understating the tree in both directions.

If the source file states the same parent-task number more than once (a later restatement marking an
earlier open block complete), record ONE row for that number, cite both line occurrences in the
Evidence cell, and state which occurrence the verdict follows. Do not silently take the first
occurrence and do not emit two rows for one number.

Where a cluster's open items are entirely stale because the capability ships, verdict it
`satisfied by shipped code` and cite the shipping artefact — the verdict follows the capability
claim, never the checkbox count.

Format the cluster table nested under the Epic 22 block-verdict subsection rather than as a separate
appendix, so a reader following the ledger's `### Epic 22` heading finds the verdict and its backing
in one place.

Do not commit yet — this plan commits once, after Task 2.
  </action>
  <verify>
    <automated>sed -n '/^### Epic 22 /,/^### Epic 23 /p' .planning/ledgers/milestone-02-03.md | grep -q '| Parent task | Verdict | Evidence |' && [ "$(sed -n '/^### Epic 22 /,/^### Epic 23 /p' .planning/ledgers/milestone-02-03.md | grep -cE '^\| [0-9]+\.0 ')" -ge "12" ]</automated>
    <human-check>Confirm every parent task in the source file appears in the table with a verdict and evidence, and that a `partially outstanding` verdict names its failing clusters (05-VALIDATION.md §Manual-Only Verifications, row 2).</human-check>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Epic 22 /,/^### Epic 23 /p' .planning/ledgers/milestone-02-03.md | grep -c '| Parent task | Verdict | Evidence |'` equals `1`
    - The cluster table's data-row count equals the number of distinct parent-task numbers in `.project/Milestone_3-Completion/Epic_22/tasks-epic22-battalion-commander-hardening.md`, which `grep -cE '^- \[[ x]\] [0-9]+\.0 '` on that file reports (expected `15`, for `0.0` through `14.0`)
    - The cluster-table rows appear in ascending parent-task numeric order — `sed -n '/^### Epic 22 /,/^### Epic 23 /p' … | grep -oE '^\| [0-9]+\.0 '` is already sorted numerically
    - Rows for parent tasks `3.0`, `4.0` and `5.0` each carry an Evidence cell containing a `:` followed by a digit (a real `file:line`, not a bare path or a restatement of the checkbox text)
    - The block-verdict sentence contains exactly one of `satisfied by shipped code`, `partially outstanding`, or `deferred with reason`
    - If the block verdict is `partially outstanding`, the same sentence or the one following it names at least one parent-task number and contains `CLOSE-02`
    - `sed -n '/^### Epic 22 /,/^### Epic 23 /p' .planning/ledgers/milestone-02-03.md | grep -c 'task-completion-state.md'` is at least `1` — the open-item count is transcribed and attributed
    - `sed -n '/^### Epic 22 /,/^### Epic 23 /p' .planning/ledgers/milestone-02-03.md | grep -c '81'` is at least `1`
    - `git diff --stat -- '*.rs'` is empty for this task
  </acceptance_criteria>
  <done>Epic 22's ledger section carries one block verdict sentence, a transcribed and attributed open-item count, and a parent-task cluster table covering every parent task in ascending order with `file:line` evidence — and, where the verdict is `partially outstanding`, the failing clusters are named as CLOSE-02's scope.</done>
</task>

<task type="auto">
  <name>Task 2: Fill Epic 22's 10 REQ ledger rows to the D-01 evidence bar</name>
  <files>.planning/ledgers/milestone-02-03.md</files>
  <read_first>
    - `.planning/ledgers/milestone-02-03.md` §`### Epic 22 — Battalion & Commander Hardening (10 IDs)` — the stub rows this task fills, and the head notes fixing the evidence bar, verdict vocabulary and path caveats
    - `.planning/REQUIREMENTS.md` §"Milestone 2-3 as-shipped ledger" as it stood before plan 05-01's pointer reduction (recoverable with `git show HEAD~N:.planning/REQUIREMENTS.md`, or read plan 05-01's SUMMARY) — the Epic 22 starting citations
    - `.planning/ledgers/milestone-01.md` lines 341-379 — the per-epic row-table pattern and the nested outstanding-item row pattern
    - `.planning/codebase/STRUCTURE.md` and `.planning/codebase/TESTING.md` — for resolving historical PRD paths and for the exercising-artefact search
    - `crates/paladin-ports/src/output/paladin_registry.rs`, `crates/paladin-core/src/platform/container/registry_error.rs`, `crates/paladin-battalion/src/in_memory_registry.rs`, `crates/paladin-battalion/src/phalanx_service.rs`, `crates/paladin-battalion/src/commander.rs`, `crates/paladin-battalion/src/grove_service.rs`, `crates/paladin-core/src/platform/container/battalion/mod.rs`
    - `.planning/phases/05-milestone-2-3-ground-truth/05-RESEARCH.md` §"Requirement → exercising-artefact search recipe"
  </read_first>
  <action>
Fill all 10 Epic 22 rows in `.planning/ledgers/milestone-02-03.md`, replacing each row's stub verdict
token `PENDING-VERDICT` and its stub Evidence cell. The 10 IDs, in the scaffold's fixed order:
`REQ-paladin-registry-port`, `REQ-paladin-registry-adapter`, `REQ-council-grove-registry-resolution`,
`REQ-grove-llm-routing`, `REQ-phalanx-per-paladin-metrics`, `REQ-battalion-metadata-extension`,
`REQ-commander-metadata-export`, `REQ-commander-config-metadata-dir-v3`,
`REQ-commander-test-hardening`, `REQ-grove-config-v2`.

For each row, in this order:
1. Resolve the citation against the current tree. The run-2 ledger's path is the starting point, not
   the answer — confirm the file exists and cite a real line number. Where the PRD path is a
   pre-workspace-decomposition `src/core|application|infrastructure` path, resolve it via
   `.planning/codebase/STRUCTURE.md` and verdict the row `superseded by shipped code` pointing at the
   ledger's head-note path caveat rather than writing a fresh divergence note (D-04).
2. Search for an exercising artefact using the recipe in 05-RESEARCH.md: `grep -rn` the requirement's
   own type or function name across `crates/ src/ tests/ examples/`, then `grep -rn "fn test_.*<keyword>"`
   inside `tests/`, then `ls examples/ | grep -i <keyword>`.
3. Run the scoped command for anything you intend to verdict `satisfied` — prefer
   `cargo test --offline -p paladin-battalion <module>::` scoping. Never run the full workspace
   suite. Batch the scoped runs for this epic into as few invocations as the evidence allows.
4. Assign one of the five legend verdicts. A citation that resolves with nothing exercising it is
   `present, unproven`; that is the expected outcome for a substantial fraction of rows (D-03).

Three rows have known specific content to record:
- `REQ-grove-llm-routing` — record the hardcoded `model: "gpt-4"` at
  `crates/paladin-battalion/src/grove_service.rs:537` in production code (the `#[cfg(test)]` boundary
  begins near line 732) as verified open, verdict it against the D-00f vocabulary, and name **Phase 6
  / CLOSE-01** as the owner. Do not edit the Rust file.
- `REQ-commander-test-hardening` — the PRD requires six named tests enabled; count the `#[ignore]`
  attributes actually present in `crates/paladin-battalion/src/commander.rs` with
  `grep -c '#\[ignore\]'` and record the measured number, not the recorded one.
- `REQ-battalion-metadata-extension` — the shipped fields sit at
  `crates/paladin-core/src/platform/container/battalion/mod.rs` rather than the PRD's
  `battalion/battalion_result.rs`; confirm the current line numbers and treat the relocation under the
  head-note path caveat.

Where the tree reveals something the run-2 ledger missed, add a nested finding row beneath the
`REQ-*` row using the blank-first-two-columns shape from `milestone-01.md:378-379`, prefixed
`**New finding (plan 05-05):**`.

Commit this plan's single file in one commit at the end, with a Bash timeout of at least 300000 ms —
`.pre-commit-config.yaml` sets `always_run: true` on `cargo-fmt` and `cargo-clippy` so they compile
the workspace even for a markdown-only commit. Do not pass `--no-verify`. If a hook aborts, unstaged
work is stashed under `~/.cache/pre-commit/patch*` — restore from there rather than re-authoring.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Epic 22 /,/^### Epic 23 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT')" = "0" ] && [ "$(sed -n '/^### Epic 22 /,/^### Epic 23 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-')" = "10" ] && [ "$(grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md)" = "118" ]</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Epic 22 /,/^### Epic 23 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `10`
    - `sed -n '/^### Epic 22 /,/^### Epic 23 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` still equals `118` — no row was inserted, deleted, or reordered
    - Every Epic 22 row's Evidence cell contains a `:` followed by a digit
    - Every Epic 22 row's Verdict cell holds exactly one of `satisfied`, `present, unproven`, `genuinely outstanding`, `deferred with reason`, `superseded by shipped code`
    - `sed -n '/^### Epic 22 /,/^### Epic 23 /p' .planning/ledgers/milestone-02-03.md | grep -c 'grove_service.rs:537'` is at least `1` and the same row contains `CLOSE-01`
    - The `REQ-commander-test-hardening` row states an `#[ignore]` count equal to what `grep -c '#\[ignore\]' crates/paladin-battalion/src/commander.rs` returns
    - Every Epic 22 row verdicted `satisfied` names a test, example, or command, and that command was executed during this task
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty — in particular `grove_service.rs` is unchanged
    - `git log -1 --name-only` shows only `.planning/ledgers/milestone-02-03.md`
  </acceptance_criteria>
  <done>All 10 Epic 22 rows carry a resolved `file:line` citation, a legend verdict, and — where `satisfied` — an exerciser that was run; the hardcoded routing model is recorded against CLOSE-01 without being fixed; the ledger still holds exactly 118 rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Epic 22 block verdict → Phase 6 CLOSE-02 planning | This verdict is the sole input sizing CLOSE-02 for this block. An overstated verdict creates invented work; an understated one leaves real gaps unclosed. There is no runtime, network, or user-input boundary — this plan reads Rust and writes markdown. |
| `intel/task-completion-state.md` → the transcribed open-item count | The authoritative measurement. Re-deriving it introduces a known counting defect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering (of the record) | Epic 22 cluster verdicts and `REQ-*` rows | medium | mitigate | A cluster verdicted `satisfied by shipped code` on a stale checkbox reading, or a row verdicted `satisfied` on a citation alone, sends Phase 6 past real work. Mitigated by the D-01 evidence bar and the P3 no-checkbox-arithmetic prohibition, both enforced as acceptance criteria requiring a real `file:line` in every Evidence cell and an executed command behind every `satisfied`. |
| T-05-11 | Elevation of Privilege (scope) | `crates/paladin-battalion/src/grove_service.rs:537` | low | mitigate | Fixing the hardcoded routing model while it is open would silently consume Phase 6 CLOSE-01's entire scope inside a phase that ships no code. Mitigated by the P4 prohibition and by a `git diff --stat` acceptance criterion over `*.rs`. |
| T-05-12 | Repudiation | scaffold row set | low | mitigate | Inserting or reordering rows would break the ledger's stable order and make later plans' scoped edits land in the wrong section. Mitigated by the phase-wide 118-row count assertion in this plan's own acceptance criteria. |
| T-05-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable and not fabricated: this plan installs no packages and touches no manifest. |

No high or critical threats. Under ASVS L1 with a blocking threshold of `high`, nothing in this plan
blocks. The honest surface is record integrity feeding a downstream scoping decision.
</threat_model>

<artifacts>
## Artifacts this phase produces

Phase-wide artefact set (recorded in full in plan 05-01's `<artifacts>` section). This plan
contributes to one:

| Artefact | Status | This plan's contribution |
|---|---|---|
| `.planning/ledgers/milestone-02-03.md` | **NEW** (created by 05-01) | §`### Epic 22 — Battalion & Commander Hardening (10 IDs)`: one block verdict sentence, a transcribed open-item count, a `| Parent task | Verdict | Evidence |` cluster table, and 10 cited `REQ-*` rows |

New section headings introduced into existing documents: none. This plan writes inside the
`### Epic 22 — Battalion & Commander Hardening (10 IDs)` heading the scaffold created; it introduces
one nested `| Parent task | Verdict | Evidence |` table there and no new heading.
</artifacts>

<verification>
- `sed -n '/^### Epic 22 /,/^### Epic 23 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `10`
- `sed -n '/^### Epic 22 /,/^### Epic 23 /p' .planning/ledgers/milestone-02-03.md | grep -c '| Parent task | Verdict | Evidence |'` equals `1`
- `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` equals `118`
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
- `git status --porcelain` is clean after the plan's single commit
</verification>

<success_criteria>
Asking "is Epic 22 done?" returns a written verdict backed by a per-parent-task cluster table
reproducible from the tree, not a checkbox count — and if the answer is `partially outstanding`, the
named clusters are exactly what Phase 6 CLOSE-02 has to close. Epic 22's 10 requirement rows each
carry a resolved citation and, where `satisfied`, an exerciser that was actually run.
</success_criteria>

<output>
Create `.planning/phases/05-milestone-2-3-ground-truth/05-05-SUMMARY.md` when done. Record: the block
verdict and, if `partially outstanding`, the exact list of failing clusters that becomes CLOSE-02's
scope for this block; the measured `#[ignore]` count in `commander.rs`; the verdict distribution
across the 10 rows; and every scoped command executed.
</output>
