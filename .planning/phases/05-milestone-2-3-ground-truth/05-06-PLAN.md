---
phase: 05-milestone-2-3-ground-truth
plan: 06
type: execute
wave: 3
depends_on: ["05-01", "05-05"]
files_modified:
  - .planning/ledgers/milestone-02-03.md
autonomous: true
requirements: [VERIFY-02, VERIFY-01]
must_haves:
  truths:
    - "`.planning/ledgers/milestone-02-03.md` §Epic 14 carries a written block verdict for `tasks-autonomous-agent-features.md`, backed by a `| Parent task | Verdict | Evidence |` table with one row per parent task in ascending numeric order."
    - "The block verdict is `satisfied by shipped code` only if every parent-task cluster verifies; otherwise it is `partially outstanding`, the failing clusters are named, and those named clusters are exactly Phase 6 CLOSE-02's scope for this block and nothing else."
    - "The 45 open-item count is transcribed from `.planning/intel/task-completion-state.md` and attributed to it, never re-derived by a fresh count of checkbox strings."
    - "Epic 14's 8 `REQ-*` ledger rows are filled to the D-01 evidence bar in the same pass, so the epic is read once rather than twice."
    - "`REQ-autonomous-error-handling`'s row records whether `PlanningError` and `PromptError` are present and whether the graceful-degradation paths exist — the specific criteria the run-2 ledger left uninspected — rather than restating the ingest-era status word."
  artifacts:
    - ".planning/ledgers/milestone-02-03.md §Epic 14 — 8 cited rows plus a block verdict and parent-task cluster table"
  key_links:
    - "Epic 14 block verdict's named failing clusters → Phase 6 CLOSE-02 — one of the three inputs sizing CLOSE-02"
    - "`intel/task-completion-state.md` → the 45 open-item count — authoritative, transcribed"
    - "`REQ-prompt-generation-service` row → `REQ-prompt-generation-test-reenable` (Epic 24, plan 05-07) — the test-module re-enablement is verified there, not here; cross-reference rather than duplicate"
  prohibitions:
    - "A block verdict must NOT be derived from checkbox arithmetic. VERIFY-02 requires a verdict per block, not a task list derived from counting."
    - "Do NOT re-derive the 45 open-item count with a fresh grep. `intel/task-completion-state.md` is authoritative; a naive count picks up the shared 'Instructions for Completing Tasks' boilerplate examples."
    - "No plan in this phase may edit any `.rs` file, `Cargo.toml`, or any file under `.github/workflows/`."
    - "The ledger must NOT award a `satisfied` verdict on the strength of a `file:line` citation alone."
---

<objective>
Produce the written block verdict for `tasks-autonomous-agent-features.md` and fill Epic 14's 8
ledger rows in the same pass.

Purpose: Epic 14 is the second of the three blocks `intel/code-verification.md` leaves unverified.
Shipped artefacts exist for every one of its eight requirements, so its 45 open checkboxes are a
claim to verify rather than a backlog — but "shipped artefacts exist" is exactly the bare
existence claim the D-01 evidence bar rejects. This plan settles which of the two readings is true,
cluster by cluster.
Output: Epic 14's ledger section, complete: 8 cited `REQ-*` rows, a parent-task cluster table, and
one block verdict sentence.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md
@.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md
@.planning/phases/05-milestone-2-3-ground-truth/05-RESEARCH.md
@.planning/phases/05-milestone-2-3-ground-truth/05-01-SUMMARY.md
@.planning/phases/05-milestone-2-3-ground-truth/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Epic 14's parent-task clusters and write the block verdict</name>
  <files>.planning/ledgers/milestone-02-03.md</files>
  <read_first>
    - `.project/Milestone_2-Missing_features/Epic_14/tasks-autonomous-agent-features.md` — read in full. Its parent tasks are numbered `0.0` through `11.0`; two carry open state, at lines 154 (`5.0 Handoff Infrastructure (US-14.4: Agent Handoff Infrastructure)`) and 245 (`8.0 YAML & CLI Configuration Support`). Note that `3.0`, `4.0` and `7.0` carry `[PRE-IMPLEMENTED]` / `[LARGELY PRE-IMPLEMENTED]` annotations — verify those claims against the tree rather than accepting the annotation
    - `.planning/intel/task-completion-state.md` — the authoritative 45 open-item count for this file
    - `.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md` §"VERIFY-02 block-verdict table pattern (D-05 parent-task cluster table)"
    - `.planning/phases/05-milestone-2-3-ground-truth/05-05-SUMMARY.md` — the cluster-table shape as plan 05-05 actually rendered it, so Epic 14's table matches rather than diverges
    - `.planning/ledgers/milestone-02-03.md` — the scaffold, for the exact `### Epic 14 — Autonomous Agent Features (8 IDs)` heading and the existing stub rows
    - `src/application/services/paladin/handoff_service.rs`, `src/application/services/paladin/planning_service.rs`, `src/application/services/paladin/prompt_generation_service.rs`, `src/application/services/paladin/temperature_service.rs`, `crates/paladin-core/src/platform/container/handoff.rs`, `crates/paladin-core/src/platform/container/autonomous_config.rs`, `src/application/cli/commands/agent.rs`
    - `.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md` §D-05, D-06
  </read_first>
  <action>
Under the existing `### Epic 14 — Autonomous Agent Features (8 IDs)` heading in
`.planning/ledgers/milestone-02-03.md`, add a block-verdict subsection **above** the
`| ID | Verdict | Evidence |` table, using `Edit` with a scoped replacement. Never `Write` the ledger
file.

The subsection contains, in order:

(1) A one-sentence block verdict for `tasks-autonomous-agent-features.md`, valued
`satisfied by shipped code`, `partially outstanding`, or `deferred with reason`, following D-06: it
is `satisfied by shipped code` only if every parent-task cluster verifies. If any cluster fails, the
verdict is `partially outstanding`, the sentence names the failing clusters explicitly, and states
that those named clusters are exactly Phase 6 CLOSE-02's scope for this block and nothing else.

(2) A sentence transcribing the open-item count from `.planning/intel/task-completion-state.md` and
citing that file by name.

(3) A table with header row exactly `| Parent task | Verdict | Evidence |`, one row per parent task
in the source file, in ascending numeric order (`0.0` through `11.0`). For each row, read the parent
task's open subtask lines as **one capability claim**, verify that claim against the tree, and record
a verdict plus `file:line` evidence. Give first-pass depth to `5.0` (handoff infrastructure — cite
`handoff_service.rs`, `container/handoff.rs`, `HandoffStrategy` in `autonomous_config.rs`, and the
`examples/agent_handoffs.rs` example) and `8.0` (YAML and CLI configuration support — cite the
autonomous config surface in `src/application/cli/commands/agent.rs` and the YAML schema wiring in
`paladin_config.rs`).

For clusters annotated `[PRE-IMPLEMENTED]` or `[LARGELY PRE-IMPLEMENTED]`, verdict against the tree
rather than against the annotation. The dominant corpus pattern is the record disagreeing with the
tree in both directions, and an annotation is a claim like any other.

If the source file states the same parent-task number more than once, record ONE row for that number,
cite both line occurrences in the Evidence cell, and state which occurrence the verdict follows.

Nest the cluster table under the Epic 14 block-verdict subsection, matching how plan 05-05 rendered
Epic 22's.

Do not commit yet — this plan commits once, after Task 2.
  </action>
  <verify>
    <automated>sed -n '/^### Epic 14 /,/^### Epic 15 /p' .planning/ledgers/milestone-02-03.md | grep -q '| Parent task | Verdict | Evidence |' && [ "$(sed -n '/^### Epic 14 /,/^### Epic 15 /p' .planning/ledgers/milestone-02-03.md | grep -cE '^\| [0-9]+\.0 ')" -ge "10" ]</automated>
    <human-check>Confirm every parent task in the source file appears in the table with a verdict and evidence, and that a `partially outstanding` verdict names its failing clusters (05-VALIDATION.md §Manual-Only Verifications, row 2).</human-check>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Epic 14 /,/^### Epic 15 /p' .planning/ledgers/milestone-02-03.md | grep -c '| Parent task | Verdict | Evidence |'` equals `1`
    - The cluster table's data-row count equals what `grep -cE '^- \[[ x]\] [0-9]+\.0 ' .project/Milestone_2-Missing_features/Epic_14/tasks-autonomous-agent-features.md` reports (expected `12`, for `0.0` through `11.0`)
    - `sed -n '/^### Epic 14 /,/^### Epic 15 /p' … | grep -oE '^\| [0-9]+\.0 '` is already sorted in ascending numeric order
    - Rows for parent tasks `5.0` and `8.0` each carry an Evidence cell containing a `:` followed by a digit
    - The block-verdict sentence contains exactly one of `satisfied by shipped code`, `partially outstanding`, or `deferred with reason`
    - If the block verdict is `partially outstanding`, the same sentence or the one following it names at least one parent-task number and contains `CLOSE-02`
    - `sed -n '/^### Epic 14 /,/^### Epic 15 /p' .planning/ledgers/milestone-02-03.md | grep -c 'task-completion-state.md'` is at least `1`
    - `sed -n '/^### Epic 14 /,/^### Epic 15 /p' .planning/ledgers/milestone-02-03.md | grep -c '45'` is at least `1`
    - Every cluster row annotated `[PRE-IMPLEMENTED]` in the source carries an Evidence cell citing a tree artefact rather than the annotation text
    - `git diff --stat -- '*.rs'` is empty for this task
  </acceptance_criteria>
  <done>Epic 14's ledger section carries one block verdict sentence, a transcribed and attributed open-item count, and a parent-task cluster table covering every parent task in ascending order with `file:line` evidence — with the `[PRE-IMPLEMENTED]` annotations verified against the tree rather than accepted.</done>
</task>

<task type="auto">
  <name>Task 2: Fill Epic 14's 8 REQ ledger rows to the D-01 evidence bar</name>
  <files>.planning/ledgers/milestone-02-03.md</files>
  <read_first>
    - `.planning/ledgers/milestone-02-03.md` §`### Epic 14 — Autonomous Agent Features (8 IDs)` — the stub rows this task fills, plus the head notes fixing the evidence bar, verdict vocabulary and path caveats
    - `.planning/phases/05-milestone-2-3-ground-truth/05-01-SUMMARY.md` — plan 05-01's record of how Epic 11's rows were rendered, including the Evidence-cell shape actually used
    - `.planning/ledgers/milestone-01.md` lines 341-379 — the per-epic row-table pattern and the nested outstanding-item row pattern
    - `.planning/codebase/STRUCTURE.md` and `.planning/codebase/TESTING.md`
    - `crates/paladin-core/src/platform/container/paladin.rs` around line 42 (`MaxLoops`), `crates/paladin-core/src/platform/container/planning.rs`, `crates/paladin-core/src/platform/container/arsenal/handoff_tool.rs`, `crates/paladin-core/src/platform/container/arsenal/handoff_error.rs`, `src/application/errors/handoff_error.rs`
    - `.planning/phases/05-milestone-2-3-ground-truth/05-RESEARCH.md` §"Requirement → exercising-artefact search recipe"
  </read_first>
  <action>
Fill all 8 Epic 14 rows in `.planning/ledgers/milestone-02-03.md`, replacing each row's stub verdict
token `PENDING-VERDICT` and its stub Evidence cell. The 8 IDs, in the scaffold's fixed order:
`REQ-max-loops-auto`, `REQ-planning-service`, `REQ-prompt-generation-service`,
`REQ-dynamic-temperature`, `REQ-handoff-infrastructure`, `REQ-handoff-tool-v1`,
`REQ-autonomous-configuration`, `REQ-autonomous-error-handling`.

For each row: resolve the citation against the current tree (confirm the file exists and cite a real
line number); search for an exercising artefact with the 05-RESEARCH.md recipe; run the scoped
command for anything you intend to verdict `satisfied`, preferring
`cargo test --offline -p paladin-core <module>::` and `cargo test --offline handoff` style scoping
over a full-workspace run; then assign one of the five legend verdicts. A citation that resolves with
nothing exercising it is `present, unproven`.

Four rows have specific content to record:
- `REQ-max-loops-auto` — the `MaxLoops` enum supersedes the run-1 scalar; this is a variant-group
  resolution, so verdict it `superseded by shipped code` if the shipped enum answers the requirement
  differently from the ingested text, citing the enum's actual location and its exercising test.
- `REQ-prompt-generation-service` — the test-module re-enablement question belongs to
  `REQ-prompt-generation-test-reenable` (Epic 24, verified by plan 05-07). Cross-reference that row
  rather than verifying the same thing twice.
- `REQ-handoff-tool-v1` — a variant-group member paired with `REQ-handoff-tool-v2` (Epic 21, plan
  05-12). Record the shipped form and cite it; note the pairing without pre-empting 05-12's row.
- `REQ-autonomous-error-handling` — the run-2 ledger explicitly left `PlanningError` / `PromptError`
  presence and the graceful-degradation paths uninspected. Inspect them: `grep -rn "PlanningError\|PromptError" crates/ src/`
  and record what is actually there, including absence.

Where the tree reveals something the run-2 ledger missed, add a nested finding row beneath the
`REQ-*` row using the blank-first-two-columns shape from `milestone-01.md:378-379`, prefixed
`**New finding (plan 05-06):**`.

Commit this plan's single file in one commit at the end, with a Bash timeout of at least 300000 ms —
`.pre-commit-config.yaml` sets `always_run: true` on `cargo-fmt` and `cargo-clippy` so they compile
the workspace even for a markdown-only commit. Do not pass `--no-verify`.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Epic 14 /,/^### Epic 15 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT')" = "0" ] && [ "$(sed -n '/^### Epic 14 /,/^### Epic 15 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-')" = "8" ] && [ "$(grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md)" = "118" ]</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Epic 14 /,/^### Epic 15 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `8`
    - `sed -n '/^### Epic 14 /,/^### Epic 15 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` still equals `118`
    - Every Epic 14 row's Evidence cell contains a `:` followed by a digit
    - Every Epic 14 row's Verdict cell holds exactly one of `satisfied`, `present, unproven`, `genuinely outstanding`, `deferred with reason`, `superseded by shipped code`
    - The `REQ-autonomous-error-handling` row reports the actual result of `grep -rn "PlanningError\|PromptError" crates/ src/`, including a stated absence if that is the result
    - The `REQ-prompt-generation-service` row references `REQ-prompt-generation-test-reenable` rather than restating its verification
    - Every Epic 14 row verdicted `satisfied` names a test, example, or command, and that command was executed during this task
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
    - `git log -1 --name-only` shows only `.planning/ledgers/milestone-02-03.md`
  </acceptance_criteria>
  <done>All 8 Epic 14 rows carry a resolved `file:line` citation, a legend verdict, and — where `satisfied` — an exerciser that was run; the two previously uninspected error types are inspected and recorded; the ledger still holds exactly 118 rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Epic 14 block verdict → Phase 6 CLOSE-02 planning | One of three inputs sizing CLOSE-02. An overstated verdict creates invented work; an understated one leaves real gaps unclosed. No runtime, network, or user-input boundary exists — this plan reads Rust and writes markdown. |
| `[PRE-IMPLEMENTED]` annotations in the source task list → cluster verdicts | Self-asserted completion claims inside the document being verified. Treating them as evidence would make the verification circular. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering (of the record) | Epic 14 cluster verdicts and `REQ-*` rows | medium | mitigate | A cluster verdicted from a `[PRE-IMPLEMENTED]` annotation rather than from the tree makes the verification circular and sends Phase 6 past real work. Mitigated by an explicit action instruction and an acceptance criterion requiring every annotated cluster's Evidence cell to cite a tree artefact rather than the annotation text. |
| T-05-13 | Spoofing (of completion state) | the 45 open-item count | low | mitigate | Re-deriving the count with a naive checkbox grep imports a known counting defect and would let checkbox arithmetic leak back into a verdict. Mitigated by the P3 prohibition and by requiring the count be transcribed and attributed to `intel/task-completion-state.md`. |
| T-05-12 | Repudiation | scaffold row set | low | mitigate | Inserting or reordering rows breaks the stable order later plans' scoped edits depend on. Mitigated by the 118-row count assertion. |
| T-05-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable and not fabricated: this plan installs no packages and touches no manifest. |

No high or critical threats. Under ASVS L1 with a blocking threshold of `high`, nothing in this plan
blocks.
</threat_model>

<artifacts>
## Artifacts this phase produces

Phase-wide artefact set (recorded in full in plan 05-01's `<artifacts>` section). This plan
contributes to one:

| Artefact | Status | This plan's contribution |
|---|---|---|
| `.planning/ledgers/milestone-02-03.md` | **NEW** (created by 05-01) | §`### Epic 14 — Autonomous Agent Features (8 IDs)`: one block verdict sentence, a transcribed open-item count, a `| Parent task | Verdict | Evidence |` cluster table, and 8 cited `REQ-*` rows |

New section headings introduced into existing documents: none. This plan writes inside the
`### Epic 14 — Autonomous Agent Features (8 IDs)` heading the scaffold created.
</artifacts>

<verification>
- `sed -n '/^### Epic 14 /,/^### Epic 15 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `8`
- `sed -n '/^### Epic 14 /,/^### Epic 15 /p' .planning/ledgers/milestone-02-03.md | grep -c '| Parent task | Verdict | Evidence |'` equals `1`
- `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` equals `118`
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
- `git status --porcelain` is clean after the plan's single commit
</verification>

<success_criteria>
Asking "is Epic 14 done?" returns a written verdict backed by a per-parent-task cluster table
reproducible from the tree, not a checkbox count — with the `[PRE-IMPLEMENTED]` claims verified
rather than accepted, and, if the verdict is `partially outstanding`, the named clusters forming
exactly Phase 6 CLOSE-02's scope for this block.
</success_criteria>

<output>
Create `.planning/phases/05-milestone-2-3-ground-truth/05-06-SUMMARY.md` when done. Record: the block
verdict and, if `partially outstanding`, the exact list of failing clusters; which
`[PRE-IMPLEMENTED]` annotations proved accurate and which did not; the verdict distribution across
the 8 rows; and every scoped command executed.
</output>
