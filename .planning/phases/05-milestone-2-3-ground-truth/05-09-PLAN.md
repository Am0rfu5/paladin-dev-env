---
phase: 05-milestone-2-3-ground-truth
plan: 09
type: execute
wave: 6
depends_on: ["05-01", "05-02", "05-08"]
files_modified:
  - .planning/ledgers/milestone-02-03.md
autonomous: true
requirements: [VERIFY-01]
must_haves:
  truths:
    - "Epic 12's 8 rows and Epic 15's 5 rows in `.planning/ledgers/milestone-02-03.md` each carry a `file:line` citation resolved against the shipped tree and a verdict from the five-value legend; every row verdicted `satisfied` names a test, example, or command that was actually run."
    - "`REQ-execution-service-rag-integration`'s three previously uninspected criteria — the prompt's context section, async non-blocking extraction, and the three metrics — are inspected against the tree and recorded, including any stated absence."
    - "`REQ-conclave-domain-model`'s row records the shipped status-enum spelling read from the tree against the PRD's spelling, verdicted `superseded by shipped code` where they differ."
    - "Epic 15's epic-level note records that its release-notes attribution is corrected by ADR-0010 — the release notes place Conclave under a Milestone 3 epic number, and Conclave is Milestone 2 Epic 15 — by citing the ADR rather than restating the defect."
    - "Epic 15's stale 129-open-checkbox count is recorded as stale with `intel/code-verification.md` cited, and is not carried as forward work."
    - "`REQ-qdrant-sanctum-adapter-v2` (Epic 12) and `REQ-qdrant-sanctum-adapter-v1` (Epic 11, filled by plan 05-01) cite the same shipped adapter and do not assign contradictory verdicts to the same code."
  artifacts:
    - ".planning/ledgers/milestone-02-03.md §Epic 12 — 8 cited rows"
    - ".planning/ledgers/milestone-02-03.md §Epic 15 — 5 cited rows"
  key_links:
    - "Epic 15's epic-level note → `.planning/decisions/0010-milestone-3-epic-numbering.md` — the corrected attribution"
    - "`REQ-qdrant-sanctum-adapter-v2` → the Epic 11 v1 row plan 05-01 wrote — variant group 7, one shipped adapter, two ingested positions"
    - "`REQ-rag-performance-targets` → `.planning/STATE.md` §Deferred Items — the RAG latency targets are recorded there as deferred to v2 with no baseline document"
  prohibitions:
    - "The ledger must NOT award a `satisfied` verdict on the strength of a `file:line` citation alone."
    - "Epic 15's 129 open checkboxes must NOT be carried as forward work — `intel/code-verification.md` records Conclave as verified shipped and the count as stale."
    - "No plan in this phase may edit any `.rs` file, `Cargo.toml`, or any file under `.github/workflows/`."
---

<objective>
Fill Epic 12 (Sanctum RAG Integration, 8 IDs) and Epic 15 (Conclave / Mixture-of-Agents, 5 IDs) — 13
ledger rows.

Purpose: Epic 12 continues the Sanctum lineage plan 05-01 opened with Epic 11, so its variant-group-7
row closes a pair already half-written. Epic 15 carries the corpus's largest stale open-checkbox
concentration — 129 open against a capability `intel/code-verification.md` records as verified
shipped — and is one of the epics the release notes misattribute to a Milestone 3 number.
Output: Epic 12's and Epic 15's ledger sections, fully cited.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md
@.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md
@.planning/phases/05-milestone-2-3-ground-truth/05-RESEARCH.md
@.planning/phases/05-milestone-2-3-ground-truth/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill Epic 12's 8 rows — Sanctum RAG integration</name>
  <files>.planning/ledgers/milestone-02-03.md</files>
  <read_first>
    - `.planning/ledgers/milestone-02-03.md` §`### Epic 12 — Sanctum RAG Integration (8 IDs)` — the stub rows; and §`### Epic 11` as filled by plan 05-01, so the variant-group-7 pair agrees
    - `.planning/phases/05-milestone-2-3-ground-truth/05-01-SUMMARY.md` — the Evidence-cell shape and the Epic 11 verdicts
    - `.planning/ledgers/milestone-01.md` lines 341-379 — the per-epic row-table and nested-finding patterns
    - `.planning/intel/requirements.md` — the acceptance criteria for each Epic 12 requirement
    - `.planning/STATE.md` §"Deferred Items" — the RAG latency targets recorded as deferred to v2 with no baseline document
    - `crates/paladin-memory/src/services/rag_retrieval_service.rs`, `crates/paladin-memory/src/services/memory_extraction_service.rs`, `crates/paladin-memory/src/config/rag.rs`, `crates/paladin-memory/benches/sanctum_benchmarks.rs`, `src/application/services/paladin/paladin_builder.rs` (fields at `:90`, `:135`; methods documented at `:727-745`), `src/application/services/paladin/paladin_execution_service.rs`
    - `.planning/codebase/ARCHITECTURE.md` — documents RAG context injection as a step in the execution flow
    - `.planning/codebase/STRUCTURE.md` and `.planning/codebase/TESTING.md`
    - `.planning/phases/05-milestone-2-3-ground-truth/05-RESEARCH.md` §"Requirement → exercising-artefact search recipe"
  </read_first>
  <action>
Fill all 8 Epic 12 rows in `.planning/ledgers/milestone-02-03.md`, replacing each row's stub verdict
token `PENDING-VERDICT` and its stub Evidence cell, using `Edit` with scoped replacements. Never
`Write` the ledger file.

The 8 IDs, in the scaffold's fixed order: `REQ-qdrant-sanctum-adapter-v2`,
`REQ-paladin-builder-sanctum-integration`, `REQ-memory-extraction-strategy`,
`REQ-rag-retrieval-service`, `REQ-rag-config`, `REQ-memory-extraction-service`,
`REQ-execution-service-rag-integration`, `REQ-rag-performance-targets`.

For each row: resolve the citation against the current tree and cite a real line number; search for
the exercising artefact with the 05-RESEARCH.md recipe; run the scoped command for anything you
intend to verdict `satisfied`, preferring `cargo test --offline -p paladin-memory rag` and
`-p paladin-memory sanctum` scoping; assign one of the five legend verdicts. A citation that resolves
with nothing exercising it is `present, unproven`.

Three rows have specific content to record:
- **`REQ-qdrant-sanctum-adapter-v2`** — variant group 7. The same shipped adapter answers both v1
  (Epic 11, filled by plan 05-01) and v2. Read plan 05-01's SUMMARY for the v1 verdict, cite the same
  adapter file and line, and state which ingested position the shipped naming and connection shape
  answers. The two rows must not assign contradictory verdicts to the same code.
- **`REQ-execution-service-rag-integration`** — the run-2 ledger explicitly left three criteria
  uninspected: the prompt's relevant-context section, async non-blocking extraction, and three
  metrics. Inspect each against `paladin_execution_service.rs` with `grep -rn` and record what is
  actually there, including any stated absence. Do not verdict this row from
  `codebase/ARCHITECTURE.md`'s prose alone — that document describes the flow, it does not exercise it.
- **`REQ-rag-performance-targets`** — `crates/paladin-memory/benches/sanctum_benchmarks.rs` exists but
  no p95 retrieval or extraction baseline is recorded anywhere in `.planning/`. Cite the bench file,
  verdict the measurement half `deferred with reason` citing STATE.md's deferred-items row, and name
  the owner recorded there.

Where the tree reveals something the run-2 ledger missed, add a nested finding row prefixed
`**New finding (plan 05-09):**`.

Do not commit yet — this plan commits once, after Task 2.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Epic 12 /,/^### Epic 13 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT')" = "0" ] && [ "$(sed -n '/^### Epic 12 /,/^### Epic 13 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-')" = "8" ]</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Epic 12 /,/^### Epic 13 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `8`
    - `sed -n '/^### Epic 12 /,/^### Epic 13 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` still equals `118`
    - Every Epic 12 row's Evidence cell contains a `:` followed by a digit
    - Every Epic 12 row's Verdict cell holds exactly one of the five legend verdicts
    - The `REQ-execution-service-rag-integration` row names all three previously uninspected criteria and states the actual finding for each, including any absence
    - The `REQ-qdrant-sanctum-adapter-v2` row cites the same adapter file as the Epic 11 `REQ-qdrant-sanctum-adapter-v1` row, and the two verdicts are not mutually contradictory
    - The `REQ-rag-performance-targets` row cites `.planning/STATE.md` and names an owner
    - Every Epic 12 row verdicted `satisfied` names a test, example, or command, and that command was executed during this task
    - `git diff --stat -- '*.rs'` is empty for this task
  </acceptance_criteria>
  <done>All 8 Epic 12 rows are cited and verdicted, the three previously uninspected RAG-integration criteria are inspected and recorded, and variant group 7 is closed consistently against Epic 11.</done>
</task>

<task type="auto">
  <name>Task 2: Fill Epic 15's 5 rows — Conclave</name>
  <files>.planning/ledgers/milestone-02-03.md</files>
  <read_first>
    - `.planning/ledgers/milestone-02-03.md` §`### Epic 15 — Conclave / Mixture-of-Agents (5 IDs)` — the stub rows
    - `.planning/decisions/0010-milestone-3-epic-numbering.md` — written by plan 05-02; the corrected attribution Epic 15's epic-level note cites
    - `.planning/intel/code-verification.md` — the record of Conclave as verified shipped against the 129 open checkboxes
    - `.planning/intel/requirements.md` — the Epic 15 acceptance criteria, including the PRD's status-enum spelling
    - `crates/paladin-core/src/platform/container/battalion/conclave.rs`, `crates/paladin-battalion/src/conclave_execution_service.rs`, `crates/paladin-battalion/src/commander.rs`, `crates/paladin-core/src/platform/container/battalion/mod.rs`, `src/application/cli/commands/battalion.rs`, `examples/conclave_expert_panel.rs`
    - `.planning/codebase/STRUCTURE.md` and `.planning/codebase/TESTING.md`
  </read_first>
  <action>
Fill all 5 Epic 15 rows in `.planning/ledgers/milestone-02-03.md`, replacing each row's stub verdict
token `PENDING-VERDICT` and its stub Evidence cell, using `Edit` with scoped replacements.

The 5 IDs, in the scaffold's fixed order: `REQ-conclave-domain-model`,
`REQ-conclave-execution-service`, `REQ-conclave-commander-strategy`, `REQ-conclave-cli-and-yaml`,
`REQ-conclave-observability`.

Add an epic-level note above the table with two sentences: (1) `intel/code-verification.md` records
Conclave as verified shipped against `tasks-conclave-mixture-of-agents.md`'s 129 open checkboxes —
the largest open concentration in the corpus and stale — so the count is not carried as forward work,
citing that file; (2) the release notes attribute Conclave to a Milestone 3 epic number, and the
authoritative attribution is Milestone 2 Epic 15, recorded in
`.planning/decisions/0010-milestone-3-epic-numbering.md` — cite the ADR rather than restating the
defect.

For each row: resolve the citation, search for the exercising artefact, run the scoped command for a
`satisfied` verdict — prefer `cargo test --offline -p paladin-battalion conclave` scoping — and
assign a legend verdict.

Two rows have specific content to record:
- **`REQ-conclave-domain-model`** — read the shipped status enum in
  `crates/paladin-core/src/platform/container/battalion/conclave.rs` and record its actual variant
  names. The Epic 15 completion report records a spelling that differs from the PRD's. Where the
  shipped spelling differs from the PRD's, verdict the row `superseded by shipped code` and cite the
  shipped variant names read from the tree — do not transcribe either recorded spelling without
  checking.
- **`REQ-conclave-observability`** — the run-2 ledger records three observability levels as
  implemented with a default, uninspected. Inspect and record what is actually there, including any
  absence.

Where the tree reveals something the run-2 ledger missed, add a nested finding row prefixed
`**New finding (plan 05-09):**`.

Commit this plan's single file in one commit at the end, with a Bash timeout of at least 300000 ms —
`.pre-commit-config.yaml` sets `always_run: true` on `cargo-fmt` and `cargo-clippy` so they compile
the workspace even for a markdown-only commit. Do not pass `--no-verify`.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Epic 15 /,/^### Epic 16 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT')" = "0" ] && [ "$(sed -n '/^### Epic 15 /,/^### Epic 16 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-')" = "5" ] && [ "$(grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md)" = "118" ]</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Epic 15 /,/^### Epic 16 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `5`
    - `sed -n '/^### Epic 15 /,/^### Epic 16 /p' .planning/ledgers/milestone-02-03.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` still equals `118`
    - `sed -n '/^### Epic 15 /,/^### Epic 16 /p' .planning/ledgers/milestone-02-03.md | grep -c '0010'` is at least `1`
    - `sed -n '/^### Epic 15 /,/^### Epic 16 /p' .planning/ledgers/milestone-02-03.md | grep -c '129'` is at least `1` and the same sentence marks the count stale and cites `code-verification.md`
    - Every Epic 15 row's Evidence cell contains a `:` followed by a digit
    - Every Epic 15 row's Verdict cell holds exactly one of the five legend verdicts
    - The `REQ-conclave-domain-model` row names the status-enum variants actually read from `conclave.rs` during this task
    - The `REQ-conclave-observability` row states the actual observability implementation found, including any absence
    - Every Epic 15 row verdicted `satisfied` names a test, example, or command, and that command was executed during this task
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
    - `git log -1 --name-only` shows only `.planning/ledgers/milestone-02-03.md`
  </acceptance_criteria>
  <done>All 5 Epic 15 rows are cited and verdicted, the stale 129 count is recorded as stale rather than carried forward, the shipped status-enum spelling is read from the tree, and the release-notes attribution is corrected by reference to ADR-0010.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/` ledger row → future implementer or agent | The only boundary. A verdict here is consumed downstream as fact. No runtime, network, process, or user-input boundary exists in this plan. |
| Epic 15's 129 open checkboxes → forward-work scope | The largest open concentration in the corpus. Carrying it forward would manufacture 129 items of invented work against a capability that ships. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering (of the record) | Epic 12 / Epic 15 rows | medium | mitigate | A row verdicted `satisfied` from a citation alone, or from `codebase/ARCHITECTURE.md`'s prose description of the RAG flow rather than from an exerciser, sends Phase 6 past real work. Mitigated by the D-01 evidence bar enforced as acceptance criteria, and by an explicit instruction that architecture prose does not exercise a requirement. |
| T-05-16 | Spoofing (of completion state) | Epic 15's 129 open checkboxes | low | mitigate | Carrying a stale count forward would create invented scope. Mitigated by requiring the count be recorded as stale with `intel/code-verification.md` cited, enforced as an acceptance criterion. |
| T-05-12 | Repudiation | scaffold row set | low | mitigate | Inserting or reordering rows breaks the stable order later plans' scoped edits depend on. Mitigated by the 118-row count assertion. |
| T-05-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable and not fabricated: this plan installs no packages and touches no manifest. |

No high or critical threats. Under ASVS L1 with a blocking threshold of `high`, nothing in this plan
blocks.
</threat_model>

<artifacts>
## Artifacts this phase produces

Phase-wide artefact set (recorded in full in plan 05-01's `<artifacts>` section). This plan
contributes to one:

| Artefact | Status | This plan's contribution |
|---|---|---|
| `.planning/ledgers/milestone-02-03.md` | **NEW** (created by 05-01) | §`### Epic 12 — Sanctum RAG Integration (8 IDs)` and §`### Epic 15 — Conclave / Mixture-of-Agents (5 IDs)`: 13 cited `REQ-*` rows plus an Epic 15 epic-level note citing ADR-0010 and the stale-count record |

New section headings introduced into existing documents: none. This plan writes inside two headings
the scaffold created.
</artifacts>

<verification>
- `sed -n '/^### Epic 12 /,/^### Epic 13 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `8`
- `sed -n '/^### Epic 15 /,/^### Epic 16 /p' .planning/ledgers/milestone-02-03.md | grep -c '^| REQ-'` equals `5`
- `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` equals `118`
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
- `git status --porcelain` is clean after the plan's single commit
</verification>

<success_criteria>
A developer looking up any of the 13 Sanctum-RAG or Conclave requirement IDs sees a
`file:line`-cited verdict against the current crate layout, with the three previously uninspected RAG
criteria actually inspected, the Conclave status enum read from the tree rather than from either
recorded spelling, and Conclave's 129 stale checkboxes recorded as stale rather than carried as
forward work.
</success_criteria>

<output>
Create `.planning/phases/05-milestone-2-3-ground-truth/05-09-SUMMARY.md` when done. Record: the
verdict distribution across the 13 rows, the actual findings for the three RAG-integration criteria,
the shipped Conclave status-enum variant names, and every scoped command executed.
</output>
