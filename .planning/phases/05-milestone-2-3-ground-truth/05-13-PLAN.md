---
phase: 05-milestone-2-3-ground-truth
plan: 13
type: execute
wave: 10
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05", "05-06", "05-07", "05-08", "05-09", "05-10", "05-11", "05-12"]
files_modified:
  - .planning/ledgers/milestone-02-03.md
  - .planning/REQUIREMENTS.md
  - .planning/decisions/PROMOTION.md
autonomous: true
requirements: [VERIFY-01, VERIFY-02]
must_haves:
  truths:
    - "`.planning/ledgers/milestone-02-03.md` opens with a `## Summary` section stating the verdict distribution across all five legend values, the nested-outstanding-item count, and the three block verdicts — and the five bucket counts sum to exactly 118."
    - "Every one of the 118 rows carries a verdict from the five-value legend; no row retains the scaffold's stub token, and no row has an empty Verdict cell. A requirement with no locatable artefact was recorded as `genuinely outstanding` or `present, unproven`, never omitted. [edge: VERIFY-01/empty, closed]"
    - "The summary carries a consolidated Phase 6 CLOSE-02 scope: the named parent-task clusters from all three block verdicts and nothing else. If all three blocks verified clean, that verdict is recorded explicitly so CLOSE-02 can close with a recorded \"no work required\" rather than being deleted."
    - "`.planning/REQUIREMENTS.md`'s Milestone 2-3 pointer states the ledger's actual final counts — 118 `REQ-*` rows plus the real nested-outstanding-item count — with plan 05-01's placeholder sentence replaced."
    - "D-00a: `.planning/decisions/PROMOTION.md`'s numbering index records the next free ADR number as 0013, and its numbering index lists 0010, 0011 and 0012 with their titles — the flat sequential single-file-per-decision scheme continues unbroken from 0001-0009, so Phases 7, 10 and 13 allocate from 0013 without collision."
    - "Zero ledger rows cite a `file:line` that does not resolve in the tree."
  artifacts:
    - ".planning/ledgers/milestone-02-03.md — a `## Summary` section with bucket counts, the three block verdicts, and the consolidated CLOSE-02 scope"
    - ".planning/REQUIREMENTS.md — the Milestone 2-3 pointer's counts finalised"
    - ".planning/decisions/PROMOTION.md — next free ADR number advanced to 0013; 0010-0012 added to the numbering index"
  key_links:
    - "Ledger `## Summary` → Phase 6 CLOSE-02 planning — the consolidated named-cluster scope, the single artefact Phase 6's planner reads to size itself"
    - "REQUIREMENTS.md pointer → `.planning/ledgers/milestone-02-03.md` — the redirect, now carrying true counts"
    - "PROMOTION.md numbering index → Phases 7, 10 and 13 — the next phases to allocate ADR numbers take 0013 from this line"
  prohibitions:
    - "The summary must NOT restate a verdict that differs from the row it summarises. Bucket counts are derived by counting the rows, not by re-judging them."
    - "The consolidated CLOSE-02 scope must NOT include anything the three block verdicts did not name. D-06 is explicit: the named clusters are exactly CLOSE-02's scope and nothing else."
    - "No plan in this phase may edit any `.rs` file, `Cargo.toml`, or any file under `.github/workflows/`."
    - "REQUIREMENTS.md must be edited with a scoped `Edit`, never a whole-file `Write` — the file is ~4,000 lines and holds four other ledger sections."
---

<objective>
Close the ledger out: summarise it honestly, finalise the counts REQUIREMENTS.md's pointer promises,
and advance the ADR numbering index past this phase's three new records.

Purpose: twelve plans wrote into one ledger. Without a summary, answering "how much of Milestone 2-3
actually holds up?" means reading 118 rows. Without a consolidated CLOSE-02 scope, Phase 6's planner
has to re-read three block verdicts and assemble the list itself. Without the PROMOTION.md
increment, Phase 7 allocates ADR numbers that collide with this phase's.
Output: a `## Summary` section on the ledger; true counts in REQUIREMENTS.md's pointer; a numbering
index that says 0013.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md
@.planning/phases/05-milestone-2-3-ground-truth/05-PATTERNS.md
@.planning/phases/05-milestone-2-3-ground-truth/05-05-SUMMARY.md
@.planning/phases/05-milestone-2-3-ground-truth/05-06-SUMMARY.md
@.planning/phases/05-milestone-2-3-ground-truth/05-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the ledger summary, roll up the three block verdicts, and consolidate CLOSE-02's scope</name>
  <files>.planning/ledgers/milestone-02-03.md</files>
  <read_first>
    - `.planning/ledgers/milestone-02-03.md` — read the whole file. This is the first pass that sees all 118 rows together; read it as a reader would, not just as a counter
    - `.planning/ledgers/milestone-01.md` lines 1-160 — the sibling ledger's own head-note and legend region, for where a summary sits relative to them
    - `.planning/phases/05-milestone-2-3-ground-truth/05-05-SUMMARY.md`, `05-06-SUMMARY.md`, `05-07-SUMMARY.md` — the three block verdicts and, where `partially outstanding`, each block's named failing clusters. 05-07's SUMMARY already carries a consolidated CLOSE-02 scope drawn from all three
    - `.planning/REQUIREMENTS.md` lines 498-502 — CLOSE-02's own text, including its instruction that a clean result closes the requirement with a recorded verdict rather than deleting it
    - `.planning/phases/05-milestone-2-3-ground-truth/05-CONTEXT.md` §D-03, D-06
  </read_first>
  <action>
Add a `## Summary` H2 to `.planning/ledgers/milestone-02-03.md`, placed immediately after the
`## Row order and amendment convention` section and before the first `### Epic ` heading, using
`Edit` with a scoped replacement.

The section contains, in order:

(1) **A verdict distribution table** with header row exactly `| Verdict | Count |` and exactly five
data rows, one per legend value in the legend's own order: `satisfied`, `present, unproven`,
`genuinely outstanding`, `deferred with reason`, `superseded by shipped code`. Derive each count by
counting the rows in the file — do not re-judge any row, and do not adjust a count to make a
narrative work. Add a total row or a sentence stating the sum, which must be exactly 118. State the
`present, unproven` figure as a headline number: CONTEXT.md D-03 predicted this bucket would be large
and budgeted for it, so reporting it plainly is the honest outcome, not an admission.

(2) **A nested-outstanding-item count** — the number of nested rows the twelve plans added beneath
`REQ-*` rows (the blank-first-two-columns rows prefixed `**Nested outstanding item:**` or
`**New finding (plan 05-NN):**`). Count them; this figure is what Task 2 writes into REQUIREMENTS.md's
pointer.

(3) **A block-verdict roll-up table** with header row exactly `| Block | Open items | Verdict |` and
exactly three rows — `tasks-epic22-battalion-commander-hardening.md`,
`tasks-autonomous-agent-features.md`, `tasks-test-hardening-benchmarks-qa.md` — carrying the open-item
counts already transcribed into each epic section and the verdict each block plan wrote. Each row
links to the epic section holding that block's cluster table.

(4) **A `### Phase 6 CLOSE-02 scope` H3** listing every parent-task cluster the three block verdicts
named as failing, and nothing else. Preface it with one sentence stating that this list is exactly
CLOSE-02's scope per D-06, and that anything not on it is out of CLOSE-02's scope. If all three
blocks verified clean, write instead an explicit sentence recording that verdict and stating that
CLOSE-02 therefore closes with a recorded "no work required" rather than being deleted — REQUIREMENTS.md
requires this outcome be recorded, not dropped.

(5) **A `### Findings the ingest record did not contain` H3** listing the nested `New finding` rows by
`REQ-*` ID with a one-line description each — the corpus's own evidence for why a verdict is never
transcribed from the ingest record.

Then run a citation resolution sweep: for every `file:line` cited anywhere in the ledger, confirm the
file exists in the tree. Extract the cited paths, check each with `test -f`, and record the result. If
any citation does not resolve, fix that row in place per D-00g — edit it directly with the corrected
citation and the date, retaining the superseded text — rather than leaving a broken citation or
opening a separate corrections file.

Do not commit yet — this plan commits once, after Task 2.
  </action>
  <verify>
    <automated>grep -q '^## Summary' .planning/ledgers/milestone-02-03.md && grep -q '| Verdict | Count |' .planning/ledgers/milestone-02-03.md && grep -q '| Block | Open items | Verdict |' .planning/ledgers/milestone-02-03.md && [ "$(grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md)" = "118" ]</automated>
    <human-check>Read a sample of ten rows across different epics and confirm each cited test asserts its requirement's behaviour rather than merely importing the symbol, and that the summary's bucket counts match what those rows actually say (05-VALIDATION.md §Manual-Only Verifications, rows 1 and 2).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Summary' .planning/ledgers/milestone-02-03.md` equals `1`, and `grep -n '^## Summary\|^### Epic 11 '` shows the summary at a lower line number than the first epic heading
    - `grep -c '| Verdict | Count |' .planning/ledgers/milestone-02-03.md` equals `1`, and that table has exactly five data rows whose counts sum to `118`
    - The `satisfied` count in the table equals the number of ledger rows whose Verdict cell is `satisfied`, verified by counting the rows
    - `grep -c '| Block | Open items | Verdict |' .planning/ledgers/milestone-02-03.md` equals `1`, and that table has exactly three data rows naming the three block files
    - `grep -c '^### Phase 6 CLOSE-02 scope' .planning/ledgers/milestone-02-03.md` equals `1`
    - The CLOSE-02 scope section either lists at least one named parent-task cluster, or contains an explicit sentence recording that all three blocks verified clean and that CLOSE-02 closes with a recorded "no work required"
    - Every parent-task cluster listed under `### Phase 6 CLOSE-02 scope` appears in one of the three block cluster tables with a failing verdict — nothing appears that no block verdict named
    - `grep -c '^### Findings the ingest record did not contain' .planning/ledgers/milestone-02-03.md` equals `1`
    - `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` equals `118`
    - Every `file:line` cited in the ledger resolves: the citation sweep ran, and its result — zero unresolved, or a list of rows fixed in place — is recorded in this plan's SUMMARY
    - `git diff --stat -- '*.rs'` is empty for this task
  </acceptance_criteria>
  <done>The ledger opens with a summary carrying a five-bucket verdict distribution summing to 118, a nested-item count, a three-row block roll-up, an explicit CLOSE-02 scope, and a findings list — and every citation in the file resolves.</done>
</task>

<task type="auto">
  <name>Task 2: Finalise REQUIREMENTS.md's pointer counts and advance PROMOTION.md to 0013</name>
  <files>.planning/REQUIREMENTS.md, .planning/decisions/PROMOTION.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` §`## Milestone 2-3 as-shipped ledger` — the pointer plan 05-01 wrote, including its placeholder sentence about the nested count
    - `.planning/REQUIREMENTS.md` lines 2628-2638 — the Milestone 1 pointer, which states its own real counts; the shape to match
    - `.planning/decisions/PROMOTION.md` — the numbering index at line 16 onward and the `Next free ADR number: 0010` line at line 33; also the promotion procedure's step 5, which requires updating that line
    - `.planning/decisions/0010-milestone-3-epic-numbering.md`, `0011-vision-port-surfaces.md`, `0012-live-api-test-key-behaviour.md` — the three new ADRs and their exact titles
    - `.planning/ledgers/milestone-02-03.md` §`## Summary` — the nested-outstanding-item count written by Task 1
  </read_first>
  <action>
Two scoped edits, both with `Edit`. Never `Write` either file.

(a) **REQUIREMENTS.md.** Replace plan 05-01's placeholder sentence — the one stating that the nested
outstanding-item count is finalised by plan 05-13 — with the real figure taken from the ledger's
`## Summary` section. The finished pointer must state both counts explicitly: 118 `REQ-*` rows and the
actual nested-outstanding-item count, matching the Milestone 1 pointer's shape at lines 2628-2638,
which states 113 rows and 39 nested items. Do not copy the Milestone 1 figures.

(b) **PROMOTION.md.** Two edits in this file:
- Add `0010`, `0011` and `0012` to the numbering index with their exact H1 titles read from the three
  ADR files, matching however entries `0001` through `0009` are formatted in that index.
- Change the `Next free ADR number: 0010` line to read `0013`. This is step 5 of PROMOTION.md's own
  promotion procedure, and Phases 7, 10 and 13 take their numbers from this line — leaving it at 0010
  guarantees a collision.

Then run the phase's final verification sweep and record the results in the SUMMARY: confirm the
ledger holds 118 rows with no unfilled verdicts, that the three new ADR files exist with seven H2
headings each, that `.planning/decisions/` contains exactly one coverage ADR, and that
`git diff --stat` over the phase's full commit range shows no `.rs`, `Cargo.toml`, or
`.github/workflows/` file changed.

Commit this plan's three files in a single commit at the end, with a Bash timeout of at least
300000 ms — `.pre-commit-config.yaml` sets `always_run: true` on `cargo-fmt` and `cargo-clippy` so
they compile the workspace even for a markdown-only commit. Do not pass `--no-verify`. If a hook
aborts, unstaged work is stashed under `~/.cache/pre-commit/patch*`.
  </action>
  <verify>
    <automated>grep -q 'Next free ADR number: 0013' .planning/decisions/PROMOTION.md && grep -q 'ledgers/milestone-02-03.md' .planning/REQUIREMENTS.md && [ "$(grep -c 'finalised by plan 05-13' .planning/REQUIREMENTS.md)" = "0" ] && [ "$(grep -c '^## Milestone 2-3 as-shipped ledger' .planning/REQUIREMENTS.md)" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Next free ADR number: 0013' .planning/decisions/PROMOTION.md` equals `1`
    - `grep -c 'Next free ADR number: 0010' .planning/decisions/PROMOTION.md` equals `0` <!-- planner-discipline-allow: Next free ADR number: 0010 -->
    - `grep -c '0010' .planning/decisions/PROMOTION.md`, `grep -c '0011' …` and `grep -c '0012' …` are each at least `1`, and each appears in the numbering index alongside the ADR's title
    - `grep -c 'finalised by plan 05-13' .planning/REQUIREMENTS.md` equals `0` — the placeholder sentence is gone <!-- planner-discipline-allow: finalised by plan 05-13 -->
    - `sed -n '/^## Milestone 2-3 as-shipped ledger/,/^## Milestone 4-6 as-shipped ledger/p' .planning/REQUIREMENTS.md | grep -c '118'` is at least `1`
    - The nested-item count stated in REQUIREMENTS.md's pointer equals the count stated in the ledger's `## Summary` section
    - `grep -c '^## Milestone 1 as-shipped ledger' .planning/REQUIREMENTS.md` equals `1` and `grep -c '^## Milestone 4-6 as-shipped ledger' .planning/REQUIREMENTS.md` equals `1` — no neighbouring section was damaged
    - `ls .planning/decisions/00*.md | wc -l` equals `12`
    - `for f in .planning/decisions/001[012]-*.md; do grep -c '^## ' "$f"; done` prints `7` three times
    - `ls .planning/decisions/ | grep -c 'coverage'` equals `1`
    - `git diff --stat $(git log --format=%H --grep='05-01' -1)^..HEAD -- '*.rs' 'Cargo.toml' '.github/'` is empty across the whole phase
    - `git log -1 --name-only` shows all three of `.planning/ledgers/milestone-02-03.md`, `.planning/REQUIREMENTS.md` and `.planning/decisions/PROMOTION.md`
  </acceptance_criteria>
  <done>REQUIREMENTS.md's pointer states the ledger's true counts, PROMOTION.md's numbering index lists 0010-0012 and reads 0013 as next free, and the phase-wide sweep confirms no product code changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ledger `## Summary` → Phase 6 CLOSE-02 planning | The consolidated scope list is what Phase 6's planner reads to size itself. An item added here that no block verdict named becomes invented work; one dropped becomes a missed gap. |
| PROMOTION.md numbering index → Phases 7, 10, 13 | A stale next-free-number line causes two phases to author ADRs with the same number, and the corpus's flat sequential numbering has no mechanism to detect the collision after the fact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-19 | Tampering (of the record) | the `### Phase 6 CLOSE-02 scope` list | medium | mitigate | A summary that adds to or subtracts from what the three block verdicts named silently re-scopes Phase 6. Mitigated by the D-06 prohibition and an acceptance criterion requiring every listed cluster to trace back to a failing verdict in one of the three cluster tables. |
| T-05-20 | Spoofing (of aggregate state) | the verdict distribution table | medium | mitigate | Bucket counts adjusted to make a narrative work would misrepresent the phase's honest outcome — CONTEXT.md D-03 explicitly predicts a large `present, unproven` bucket. Mitigated by requiring counts be derived by counting rows, by the sum-to-118 criterion, and by the human-check sampling rows against the table. |
| T-05-21 | Tampering | `.planning/decisions/PROMOTION.md` numbering index | medium | mitigate | Leaving the index at 0010 guarantees a future ADR-number collision that flat sequential numbering cannot detect. Mitigated by an acceptance criterion asserting both that `0013` is present and that `0010` no longer appears on that line. |
| T-05-04 | Repudiation | `.planning/REQUIREMENTS.md` | low | mitigate | A whole-file write would destroy four other ledger sections. Mitigated by the scoped-`Edit` prohibition and by acceptance criteria asserting the Milestone 1 and Milestone 4-6 headings both survive. |
| T-05-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable and not fabricated: this plan installs no packages and touches no manifest. |

No high or critical threats. Under ASVS L1 with a blocking threshold of `high`, nothing in this plan
blocks. The phase closes having recorded one security-relevant finding (the unwired
`EncryptionService`, ADR-0011 / T-05-01, dispositioned `accept`) and having changed no security code
path.
</threat_model>

<artifacts>
## Artifacts this phase produces

Phase-wide artefact set (recorded in full in plan 05-01's `<artifacts>` section). This plan closes
three of them:

| Artefact | Status | This plan's contribution |
|---|---|---|
| `.planning/ledgers/milestone-02-03.md` | **NEW** (created by 05-01, filled by 05-05..05-12) | a `## Summary` section: verdict distribution table, nested-item count, three-row block roll-up, `### Phase 6 CLOSE-02 scope`, `### Findings the ingest record did not contain`; plus any citation fixed in place by the resolution sweep |
| `.planning/REQUIREMENTS.md` §`## Milestone 2-3 as-shipped ledger` | **REDUCED TO A POINTER** (by 05-01) | the pointer's counts finalised — 118 rows plus the real nested-item count |
| `.planning/decisions/PROMOTION.md` | **UPDATED** | 0010, 0011 and 0012 added to the numbering index; next free ADR number advanced to 0013 |

New section headings introduced into existing documents by this plan: `## Summary`,
`### Phase 6 CLOSE-02 scope` and `### Findings the ingest record did not contain`, all inside
`.planning/ledgers/milestone-02-03.md`. No new headings in `REQUIREMENTS.md` or `PROMOTION.md` — both
receive scoped in-place edits only.
</artifacts>

<verification>
- `grep -c '^## Summary' .planning/ledgers/milestone-02-03.md` equals `1`
- The five bucket counts in the verdict distribution table sum to `118`
- `grep -c '^| REQ-' .planning/ledgers/milestone-02-03.md` equals `118`
- `grep -c '| Parent task | Verdict | Evidence |' .planning/ledgers/milestone-02-03.md` equals `3`
- `grep -c 'Next free ADR number: 0013' .planning/decisions/PROMOTION.md` equals `1`
- `ls .planning/decisions/00*.md | wc -l` equals `12`
- `ls .planning/decisions/ | grep -c 'coverage'` equals `1`
- Every `file:line` cited in the ledger resolves in the tree
- `git diff --stat` across the phase's full commit range shows no `.rs`, `Cargo.toml`, or `.github/workflows/` file changed
- `git status --porcelain` is clean after the plan's single commit
</verification>

<success_criteria>
A developer opening `.planning/` gets a truthful account of Milestones 2 and 3 in one read: a summary
that says how many of the 118 requirements the tree actually satisfies, three block verdicts
reproducible from the tree, and a named CLOSE-02 scope Phase 6 can plan against. REQUIREMENTS.md
redirects to it with true counts, and the ADR numbering index is safe for Phase 7 to use.
</success_criteria>

<output>
Create `.planning/phases/05-milestone-2-3-ground-truth/05-13-SUMMARY.md` when done. Record: the five
bucket counts and their sum, the nested-item count, the three block verdicts verbatim, the
consolidated CLOSE-02 scope list, the citation-resolution sweep result (zero unresolved, or the rows
fixed in place), and the phase-wide confirmation that no product code changed.
</output>
