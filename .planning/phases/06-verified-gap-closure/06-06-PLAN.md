---
phase: 06-verified-gap-closure
plan: 06
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - .planning/decisions/0013-grove-routing-model.md
  - .planning/decisions/PROMOTION.md
  - CHANGELOG.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - CLOSE-01

must_haves:
  truths:
    - "The Grove routing-model break is recorded three ways, as D-03 requires: a new ADR-0013, a CHANGELOG entry under the current version, and rustdoc on `GroveConfig.routing_model` (the rustdoc half ships in plan 06-01)."
    - "`.planning/decisions/0013-grove-routing-model.md` exists, carries the seven required H2 headings in PROMOTION.md's order, and records: the chosen option, the two alternatives D-01 rejected (a `String` with a serde default, and a constructor argument on `GroveExecutionService::new`), the hard-error choice from D-02 with its three rejected alternatives, and the deliberate runtime break (D-03)."
    - "`## Considered Options` and `## Code Locations` in ADR-0013 are bulleted lists, never prose paragraphs, so `adr-parser.cjs`'s `splitEntries` yields structured entries (PROMOTION.md)."
    - "`## Code Conformance` in ADR-0013 carries a `conforms` verdict naming CLOSE-01 as the requirement that executed it, since plan 06-01 lands the code in the same phase (PROMOTION.md step 3, D-03)."
    - "`.planning/decisions/PROMOTION.md`'s `Next free ADR number` line reads `0014`, and its numbering-index table carries a row for `0013 | grove-routing-model` (PROMOTION.md step 5)."
    - "`CHANGELOG.md` carries an entry under `## [Unreleased]` describing the change and, in a `### Changed` or `### Breaking` sub-heading, stating plainly that a Grove using `RoutingStrategy::LlmRouting` must now set `routing_model` and will return `BattalionError::RoutingError` until it does (D-03)."
    - "`.planning/PROJECT.md`'s `## Key Decisions` table carries a row linking to ADR-0013 (PROMOTION.md step 6)."
    - "The record does not overstate the blast radius or understate it: `GroveConfig::default()` uses `RoutingStrategy::KeywordMatch`, so only Groves that explicitly select LLM routing are affected — the CHANGELOG and the ADR both say exactly that."
  artifacts:
    - "`.planning/decisions/0013-grove-routing-model.md` — the new ADR"
    - "`.planning/decisions/PROMOTION.md` — the numbering-index row and the bumped next-free-number line"
    - "`CHANGELOG.md` — the `## [Unreleased]` entry"
    - "`.planning/PROJECT.md` — the `## Key Decisions` row"
  key_links:
    - "ADR-0013 -> PROMOTION.md's numbering index -> the next phase that mints an ADR — a stale next-free-number line hands the same number to two decisions"
    - "CHANGELOG `## [Unreleased]` -> a consumer upgrading across this change -> their `paladin.yaml` / `GroveBuilder` call — the CHANGELOG is the only surface an external consumer reads before the break reaches them"
    - "ADR-0013 `## Code Conformance` -> plan 06-01's shipped code — the verdict is only `conforms` because 06-01 landed first; this plan's `depends_on` is what makes that true"
  prohibitions:
    - statement: "The routing-model change must never be presented to a reader as backward-compatible or as a pure improvement. Every record of it — ADR-0013, the CHANGELOG entry and the `GroveConfig.routing_model` rustdoc — must state that an existing Grove using `RoutingStrategy::LlmRouting` stops working at runtime until its configuration names a model, and must state what the operator has to do about it. Softening the wording, burying it under an 'Added' heading alone, or omitting the migration instruction would leave a shipped break undiscoverable until it fires in a user's run."
      status: unverified
      verification: flagged
---

<objective>
Record CLOSE-01's deliberate runtime break the three ways D-03 requires.

Purpose: D-02 is the phase's only code-behaviour decision and it is a one-way door — a Grove using
`RoutingStrategy::LlmRouting` works today (silently, against an OpenAI model) and starts returning
`BattalionError::RoutingError` after plan 06-01 until its configuration names a model. Its siblings
ADR-0010, ADR-0011 and ADR-0012 all got an ADR; this one gets ADR-0013 plus a CHANGELOG entry, and
plan 06-01 already shipped the rustdoc half.

Output: `.planning/decisions/0013-grove-routing-model.md`, an updated PROMOTION.md numbering index,
a `## [Unreleased]` CHANGELOG entry, and a `## Key Decisions` row in PROJECT.md.

**Dependency.** This plan depends on 06-01 so ADR-0013's `## Code Locations` can cite the shipped
line numbers rather than predicted ones, and so `## Code Conformance` can honestly read `conforms`
rather than `must change`.

**Executor note:** `.planning/config.json` sets `workflow.worktree_skip_hooks: true`. Do not
disable it — without it every commit cold-compiles the whole workspace inside the worktree.

**Spec-less probe fallback.** This plan carries none of the eight edge-probe rows (they land in
06-01, 06-03, 06-05 and 06-07). One prohibition survived Stage-2 precision and is authored
descriptor-less above.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/decisions/PROMOTION.md
@.planning/phases/06-verified-gap-closure/06-CONTEXT.md
@.planning/phases/06-verified-gap-closure/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ADR-0013 and update the numbering index</name>
  <files>.planning/decisions/0013-grove-routing-model.md, .planning/decisions/PROMOTION.md</files>
  <read_first>
    - `.planning/decisions/PROMOTION.md` — the whole file: the flat zero-padded numbering scheme, the numbering-index table, the `**Next free ADR number: 0013**` line, the required heading set and its order, the rule that `## Considered Options` and `## Code Locations` must be bulleted lists (never prose paragraphs) because `adr-parser.cjs`'s `splitEntries` only yields structured entries from bullet or numbered lines, the note that `## Code Conformance` and `## Downstream Consumers` land in `unmapped_headers` and are still required, the supersession mechanism, and the six-step promotion procedure.
    - `.planning/decisions/0012-live-api-test-key-behaviour.md` — the most recent ADR, as the shape and density to match: how it states its Context with re-derived evidence, how it writes `## Decision` as a position plus its justification, how `## Considered Options` records each rejected alternative with its reason, how `## Code Locations` cites `file:line` with a description per bullet, and how `## Downstream Consumers` names who reads it next.
    - `.planning/decisions/0011-vision-port-surfaces.md` — the sibling ADR from the same Phase 5 batch, for the `## Code Conformance` verdict format.
    - `.planning/phases/06-verified-gap-closure/06-CONTEXT.md` — D-01 (the chosen option and its two rejected alternatives), D-02 (the hard-error choice, its three rejected alternatives, and the one-way reversibility rating with its rationale), D-03 (what the ADR must record), and the `<domain>` section, which reproduces the pre-change defect line verbatim.
    - `.planning/phases/06-verified-gap-closure/06-01-SUMMARY.md` — what 06-01 actually shipped, including the final line numbers, the checkpoint outcome, and any deviation recorded there.
    - `crates/paladin-core/src/platform/container/battalion/grove.rs` and `crates/paladin-battalion/src/grove_service.rs` — the post-06-01 state, to read the shipped line numbers for `## Code Locations` rather than predicting them.
  </read_first>
  <action>
    Create `.planning/decisions/0013-grove-routing-model.md` using the next free number from
    PROMOTION.md, with the seven required H2 headings in PROMOTION.md's exact order: `## Status`,
    `## Context`, `## Decision`, `## Considered Options`, `## Code Locations`,
    `## Code Conformance`, `## Downstream Consumers`.

    `## Status`: `Accepted`, with the dated line in the same form ADR-0011 and ADR-0012 use.

    `## Context`: state the defect as Phase 5 verified it — `grove_service.rs` built its routing
    `LlmRequest` with a hardcoded provider model literal in production code, with the file's
    `#[cfg(test)]` module beginning well below it, so a Grove configured for Anthropic or DeepSeek
    silently routed through OpenAI. Quote the pre-change line verbatim as it stood before plan
    06-01, sourcing it from `06-CONTEXT.md`'s `<domain>` section or from
    `git show <pre-change-commit>:crates/paladin-battalion/src/grove_service.rs`. Record that this
    was the only defect in run-2 scope verified open against the tree, that it is the same defect
    class Epic 21 removed from `planning_service.rs` and `prompt_generation_service.rs`, and that
    it meant Epic 22's "all inline TODOs in Battalion and Commander files resolved" criterion was
    unmet. Also record that the two sibling occurrences in `council_service.rs` and
    `conclave_execution_service.rs` are inside `#[cfg(test)]` blocks and are therefore out of scope
    — re-derive that yourself against the tree rather than transcribing it.

    `## Decision`: record both halves. First, the routing model comes from a new
    `routing_model: Option<String>` field on `GroveConfig`, threaded through `GroveBuilder`, sitting
    alongside the five knobs already there and additive to the YAML surface, with
    `#[serde(skip_serializing_if = "Option::is_none")]` matching `fallback_tree`. Second, when
    `routing_model` is absent (or blank) and `routing_strategy` is `RoutingStrategy::LlmRouting`,
    routing returns `BattalionError::RoutingError` naming the missing configuration, with no
    fallback of any kind — `routing_fallback` is not consulted, `get_available_models()` is not
    called, and no per-provider default is guessed. State the deliberate consequence plainly: this
    is a runtime behaviour break for any Grove already using LLM routing, and it is scoped by the
    fact that `RoutingStrategy::default()` is `KeywordMatch`, so a default-constructed Grove is
    unaffected. Record the reversibility ratings carried forward from `06-CONTEXT.md` — D-01
    `costly` (a public serialized type), D-02 `one-way` (a shipped behaviour break) — and record the
    outcome of plan 06-01's `checkpoint:decision` gate as `06-01-SUMMARY.md` states it.

    `## Considered Options`: bulleted, one bullet per rejected alternative with its reason —
    a `String` field with a serde default naming a provider model (moves the vendor literal into the
    config default rather than eliminating it, leaving ROADMAP criterion 1 half-met); a constructor
    argument on `GroveExecutionService::new` (breaking signature, and puts a routing knob where no
    other Grove knob lives, invisible to YAML); resolving the model from
    `llm_port.get_available_models()` (async, fallible, arbitrary ordering); and a per-provider
    default-model table inside `paladin-battalion` (goes stale as providers rename models, and puts
    provider knowledge in the orchestration crate).

    `## Code Locations`: bulleted, one `file:line` citation per bullet with a description, read from
    the post-06-01 tree: the `routing_model` field on `GroveConfig`, its `Default` initialiser, the
    `GroveBuilder` field and setter, the missing-model guard in `route_by_llm`, the config-sourced
    `LlmRequest.model` construction, and each test named in 06-01's `must_haves` — the named passing
    test is what clears this project's D-19 evidence bar.

    `## Code Conformance`: `conforms`, naming CLOSE-01 as the requirement that executed it and
    naming plan 06-01 as where the code landed.

    `## Downstream Consumers`: the CHANGELOG entry and PROJECT.md row this plan's Task 2 writes; the
    Phase 6 close-out plan 06-07 that marks CLOSE-01 satisfied in REQUIREMENTS.md; and any operator
    running a Grove with `RoutingStrategy::LlmRouting`.

    Then update `.planning/decisions/PROMOTION.md` with a scoped edit: add a row to the numbering
    index for `0013 | grove-routing-model | Grove routing model from configuration, per D-01/D-02/D-03 (Phase 6)`
    in the same format as the existing rows, and change the `**Next free ADR number: 0013**` line to
    read `0014`. Use `Edit`, never a whole-file `Write`.
  </action>
  <acceptance_criteria>
    - `.planning/decisions/0013-grove-routing-model.md` exists.
    - `grep -c '^## ' .planning/decisions/0013-grove-routing-model.md` outputs `7`, and `grep -n '^## ' .planning/decisions/0013-grove-routing-model.md` lists Status, Context, Decision, Considered Options, Code Locations, Code Conformance, Downstream Consumers in that order.
    - Every non-blank line in ADR-0013's `## Considered Options` and `## Code Locations` sections begins with `- ` or a numbered-list marker (no prose paragraph in either section).
    - `grep -c 'RoutingError' .planning/decisions/0013-grove-routing-model.md` outputs a number greater than or equal to `1`.
    - `grep -c 'get_available_models' .planning/decisions/0013-grove-routing-model.md` outputs a number greater than or equal to `1` (the rejected alternative is recorded, not omitted).
    - `grep -c 'CLOSE-01' .planning/decisions/0013-grove-routing-model.md` outputs a number greater than or equal to `1`.
    - `grep -c 'Next free ADR number: 0014' .planning/decisions/PROMOTION.md` outputs `1`.
    - `grep -c '| 0013 |' .planning/decisions/PROMOTION.md` outputs `1`.
    - `grep -c 'Next free ADR number: 0013' .planning/decisions/PROMOTION.md` outputs `0`.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/decisions/0013-grove-routing-model.md && grep -c '^## ' .planning/decisions/0013-grove-routing-model.md && grep -c 'Next free ADR number: 0014' .planning/decisions/PROMOTION.md</automated>
  </verify>
  <done>ADR-0013 exists with all seven required headings in order, both list sections are bulleted, the chosen option and all four rejected alternatives are recorded with reasons, `## Code Conformance` reads `conforms` naming CLOSE-01, and PROMOTION.md's index and next-free-number line are updated. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Write the CHANGELOG entry and the PROJECT.md Key Decisions row</name>
  <files>CHANGELOG.md, .planning/PROJECT.md</files>
  <read_first>
    - `CHANGELOG.md` — the whole `## [Unreleased]` and `## [0.7.0]` regions: the Keep a Changelog format declared at the top, whether `## [Unreleased]` currently has any content, the `### Added` / `### Changed` / `### Fixed` sub-heading convention the `## [0.7.0]` section uses, and how that section writes a provenance note.
    - `.planning/PROJECT.md` around `## Key Decisions` — the table's three columns (`Decision`, `Rationale`, `Outcome`), the markdown-link-plus-`(ADR-NNNN)` format each row's Decision cell uses, the way the Outcome column distinguishes `conforms`, `✓ Good — applied in Phase N (...)` and `— Pending — ...`, and the explanatory paragraphs beneath the table.
    - `.planning/decisions/0013-grove-routing-model.md` — the state after Task 1, so the CHANGELOG and the table row say the same thing the ADR says.
    - `.planning/phases/06-verified-gap-closure/06-01-SUMMARY.md` — the shipped behaviour, so the migration instruction is accurate.
    - `.planning/decisions/PROMOTION.md` — step 6 of the promotion procedure, which requires the PROJECT.md row.
  </read_first>
  <action>
    Add a CHANGELOG entry under `## [Unreleased]`, using the file's existing Keep a Changelog
    sub-heading convention. Under `### Added`, record the new `GroveConfig.routing_model` field and
    its `GroveBuilder::routing_model(..)` setter. Under `### Changed` (or a `### Breaking` heading if
    the file already uses one — read it and follow whichever convention is there), record the
    behaviour break in operator-facing terms: a Grove whose `routing_strategy` is
    `RoutingStrategy::LlmRouting` must now set `routing_model`; until it does, routing returns
    `BattalionError::RoutingError` instead of silently using a provider default. Include the
    migration instruction — set `routing_model` in the Grove's YAML/JSON configuration or pass it to
    `GroveBuilder::routing_model(..)` — and the scoping fact that a default-constructed Grove is
    unaffected because `RoutingStrategy::default()` is `KeywordMatch`. Link to ADR-0013. Do not put
    the break under `### Added` alone and do not soften it: this is the surface an external consumer
    reads before the break reaches them.

    Add a row to `.planning/PROJECT.md`'s `## Key Decisions` table following the existing format:
    the Decision cell a markdown link to `.planning/decisions/0013-grove-routing-model.md` with the
    `(ADR-0013)` suffix; the Rationale cell a one-sentence statement of the chosen option and the
    hard-error choice; the Outcome cell in the `✓ Good — applied in Phase 6 (...)` form the table
    already uses for executed decisions, citing plan 06-01. Use `Edit` with a scoped replacement
    anchored on the last existing table row — never a whole-file `Write` on PROJECT.md.

    One observation to record, not to act on: `## Key Decisions` currently carries rows for
    ADR-0001 through ADR-0006, ADR-0008 and ADR-0009, but **not** for ADR-0010, ADR-0011 or
    ADR-0012 — Phase 5 minted three ADRs without adding their rows, against PROMOTION.md step 6.
    Back-filling those three is outside this phase's scope. Record the gap explicitly in this plan's
    SUMMARY as an observation with a named next owner, so it is carried rather than dropped.
  </action>
  <acceptance_criteria>
    - `awk '/^## \[Unreleased\]/{f=1;next} /^## \[/{f=0} f' CHANGELOG.md | grep -c 'routing_model'` outputs a number greater than or equal to `2`.
    - `awk '/^## \[Unreleased\]/{f=1;next} /^## \[/{f=0} f' CHANGELOG.md | grep -c 'RoutingError'` outputs a number greater than or equal to `1`.
    - `awk '/^## \[Unreleased\]/{f=1;next} /^## \[/{f=0} f' CHANGELOG.md | grep -c 'ADR-0013'` outputs a number greater than or equal to `1`.
    - `awk '/^## \[Unreleased\]/{f=1;next} /^## \[/{f=0} f' CHANGELOG.md | grep -cE '^### (Changed|Breaking)'` outputs a number greater than or equal to `1`.
    - `grep -c '0013-grove-routing-model.md' .planning/PROJECT.md` outputs `1`.
    - `grep -c 'ADR-0013' .planning/PROJECT.md` outputs a number greater than or equal to `1`.
    - `git diff --stat .planning/PROJECT.md` shows an additive change of a small number of lines, not a whole-file rewrite.
    - `git diff CHANGELOG.md` shows no change to any line inside the `## [0.7.0]` section.
  </acceptance_criteria>
  <verify>
    <automated>awk '/^## \[Unreleased\]/{f=1;next} /^## \[/{f=0} f' CHANGELOG.md | grep -c 'routing_model' && grep -c 'ADR-0013' .planning/PROJECT.md</automated>
  </verify>
  <done>The CHANGELOG records the change and the break with a migration instruction under a Changed/Breaking heading, and PROJECT.md's Key Decisions table links to ADR-0013. The missing ADR-0010/0011/0012 rows are recorded as an observation, not silently fixed or silently ignored. Committed.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (this plan's share)

| Symbol / path | Kind | Location |
|---|---|---|
| `.planning/decisions/0013-grove-routing-model.md` | new ADR file | `.planning/decisions/` |
| `0013 | grove-routing-model` numbering-index row | table row | `.planning/decisions/PROMOTION.md` |
| `Next free ADR number: 0014` | index state change | same |
| `## [Unreleased]` entry for the Grove routing model | CHANGELOG entry | `CHANGELOG.md` |
| ADR-0013 row in `## Key Decisions` | table row | `.planning/PROJECT.md` |

No code symbols are created by this plan.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the project's own record -> a future planner or operator | the ADR, CHANGELOG and Key Decisions table are what a later reader trusts instead of re-deriving from the tree; a wrong or missing record is acted on as though it were true |
| CHANGELOG -> an external consumer upgrading | the only surface an external consumer reads before a shipped behaviour break reaches their running system |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06-01 | Repudiation | `CHANGELOG.md` `## [Unreleased]` | high | mitigate | A shipped runtime break that is not discoverable before it fires costs a consumer a production failure with no explanation. Mitigated by requiring the entry under a `### Changed`/`### Breaking` heading, with the affected configuration named, the resulting error type named, and the migration instruction stated — enforced by four separate acceptance greps scoped to the `## [Unreleased]` section. |
| T-06-06-02 | Tampering | `.planning/decisions/PROMOTION.md` next-free-number line | medium | mitigate | A stale next-free-number line hands `0013` to a second decision in Phase 7, producing two ADRs with one number and a silently-lost decision. Mitigated by the paired acceptance criteria asserting the line reads `0014` and no longer reads `0013`. |
| T-06-06-03 | Tampering | `.planning/PROJECT.md` | medium | mitigate | A whole-file `Write` on PROJECT.md would destroy content outside the diff window. The action mandates a scoped `Edit` anchored on the last table row, and the acceptance criteria require an additive, small-line-count diff. |
| T-06-06-04 | Information Disclosure | ADR-0013 `## Context` | low | accept | The ADR quotes a pre-change source line and cites `file:line` locations. It contains no credential, endpoint or key material — a model name is not a secret. |
| T-06-06-SC | Tampering | npm/pip/cargo installs | low | accept | This plan touches no source and no manifest; no package-manager install task exists, so the package-legitimacy gate does not apply. |
</threat_model>

<verification>
- `test -f .planning/decisions/0013-grove-routing-model.md` succeeds and the file has exactly seven `## ` headings in PROMOTION.md's order.
- `grep -c 'Next free ADR number: 0014' .planning/decisions/PROMOTION.md` outputs `1`.
- The `## [Unreleased]` CHANGELOG section names `routing_model`, `RoutingError`, ADR-0013 and a migration instruction under a Changed/Breaking heading.
- `grep -c 'ADR-0013' .planning/PROJECT.md` is at least `1` and `git diff --stat .planning/PROJECT.md` is small and additive.
</verification>

<success_criteria>
D-03 is satisfied: the deliberate break is recorded in an ADR, in the CHANGELOG under the current
version, and in rustdoc (the last shipped in plan 06-01). PROMOTION.md's shared numbering state is
correct for the next phase that mints an ADR, and PROJECT.md links to the new decision.
</success_criteria>

<output>
Create `.planning/phases/06-verified-gap-closure/06-06-SUMMARY.md` when done.
</output>
