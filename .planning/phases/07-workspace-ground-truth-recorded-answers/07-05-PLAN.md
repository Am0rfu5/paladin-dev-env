---
phase: 07-workspace-ground-truth-recorded-answers
plan: 05
type: execute
wave: 4
depends_on: ["07-01"]
files_modified:
  - .planning/decisions/0018-m6-facade-reexport-policy.md
  - .project/Milestone_6-Architectural-Refinements/overview/Milestone_6-Tier_3-Architectural-Refinements.md
  - .project/Milestone_6-Architectural-Refinements/Epic_4/prd-relocate-circuitbreaker-infra.md
autonomous: true
requirements: [ARCH-04, ARCH-02, ARCH-05]
must_haves:
  truths:
    - "`.planning/decisions/0018-m6-facade-reexport-policy.md` records the no-shim posture as policy: Milestone 6 added no backward-compatible re-export shims, `src/application/use_cases/` does not exist, and the two PRDs' position is the recorded answer (D-13)."
    - "ADR-0018 records the version consequence by citing `.planning/decisions/0008-workspace-version-0-7-0.md` rather than re-deriving it: Milestone 6 removed publicly reachable import paths, which is breaking under ordinary semver reasoning, and pre-1.0 Cargo semantics make `0.7.0` the correct expression of it, so REL-01's single-version story is unaffected (D-14). This is ROADMAP criterion 4's one recorded answer. [edge: ARCH-04/ordering]"
    - "The milestone overview's Epic 2 AC 6, Epic 4 AC 5 and risk-register re-export language and the two PRDs' Non-Goal 7 / FR-4.11 describe the same policy from opposite sides. The PRD position wins and the overview's clauses are annotated superseded rather than deleted — the two positions are never merged into one restated clause. [edge: ARCH-04/adjacency]"
    - "`REQ-battalion-facade-shim` — a Milestone 5 requirement that was satisfied and then retired by Milestone 6 — is recorded `superseded by shipped code` pointing at ADR-0018, never `genuinely outstanding`, because a requirement whose deliverable was deliberately removed is not an unmet requirement (D-15). [edge: ARCH-04/empty]"
    - "Milestone 6 Epic 2's Open Question 4 (\"this should be confirmed with the team before implementation begins\") is recorded as now confirmed by ADR-0018 rather than left dangling."
    - "Milestone 6 Epic 4's FR-4.12 is re-pointed: its \"update `STABLE_API.md`\" clause now applies to `docs/src/api-reference/stable-api.md`, the mdbook chapter the deliverable relocated into (D-18)."
    - "Both annotated `.project/` documents still contain their original claim text after the corrections, alongside dated correction banners — proving annotation, not rewriting (D-00g)."
  artifacts:
    - ".planning/decisions/0018-m6-facade-reexport-policy.md — new ADR, `conforms`, cites ADR-0008, names Phase 11 / FACADE-02 D1"
    - ".project/Milestone_6-Architectural-Refinements/overview/Milestone_6-Tier_3-Architectural-Refinements.md — dated banner, prerequisite-line numbering correction, and Superseded blocks on Epic 2 AC 6, Epic 4 AC 5 and the risk register"
    - ".project/Milestone_6-Architectural-Refinements/Epic_4/prd-relocate-circuitbreaker-infra.md — dated banner, Goal 7 / FR-4.11 confirmation note, FR-4.12 re-pointed to the mdbook path"
  key_links:
    - "ADR-0018 → ADR-0008 — the version half already answered by Phase 4; ADR-0018 cites it and adds only the re-export policy"
    - "ADR-0018 → Phase 11 / FACADE-02 D1 — the downstream consumer that stops Phase 11 re-opening the policy (D-16)"
    - "ADR-0018 → REQ-battalion-facade-shim, REQ-orchestration-no-reexport-shims, REQ-circuitbreaker-old-path-retired — the three ledger rows across two milestones this one answer settles"
    - "FR-4.12 → docs/src/api-reference/stable-api.md — the re-pointed deliverable path (D-18)"
  prohibitions:
    - "ADR-0018 must NOT re-derive the major-version-bump question — Phase 4 answered it as ADR-0008, and re-deciding it would produce a second version story."
    - "The corrections to the Milestone 6 overview and the Epic 4 PRD must NOT delete, replace, or rewrite the superseded acceptance criteria or the risk-register language."
    - "The M5→M6 posture flip must NOT be recorded as a contradiction to resolve — `REQ-battalion-facade-shim` was correct for Milestone 5 and was retired by Milestone 6, and the ledger records that as history."
    - "This plan must NOT edit any `.rs` file, `Cargo.toml`, or any file under `.github/`."
---

<objective>
Give "was Milestone 6 a breaking change?" one recorded answer with its version consequence, and stop
the milestone overview and its two PRDs disagreeing about whether the old import paths still resolve.

Purpose: the Milestone 6 overview requires the facade to re-export relocated types at their original
paths; both PRDs forbid exactly that; and shipped code follows the PRDs — `src/application/use_cases/`
does not exist at all. Epic 2's own Open Question 4 undercuts its own Non-Goal, so this is an open
decision rather than a precedence conflict, and it is the one ARCH-04 answer three ledger rows across
two milestones point at. The version half is already answered by ADR-0008; ADR-0018 cites it rather
than producing a second version story.
Output: ADR-0018, the Milestone 6 overview annotated, and the Epic 4 CircuitBreaker PRD annotated
with its FR-4.12 deliverable re-pointed into the mdbook.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-PATTERNS.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ADR-0018 — the Milestone 6 facade re-export policy and its version consequence</name>
  <read_first>
    - `.planning/decisions/0008-workspace-version-0-7-0.md` — full read; its `## Context` framing of the three-way version disagreement and its cross-phase coupling note are the structural template for ADR-0018's version half
    - `.planning/decisions/PROMOTION.md` — the required heading set and the numbering rule
    - `.project/Milestone_6-Architectural-Refinements/overview/Milestone_6-Tier_3-Architectural-Refinements.md` — Epic 2 AC 6, Epic 4 AC 5, and the risk register's "facade crate re-exports absorb the change" language, located by grep
    - `.project/Milestone_6-Architectural-Refinements/Epic_2/prd-relocate-orchestration-services.md` — Non-Goal 7 and Open Question 4
    - `.project/Milestone_6-Architectural-Refinements/Epic_4/prd-relocate-circuitbreaker-infra.md` — Goal 7 and FR-4.11
    - `src/application/` — confirm by listing that it holds `cli`, `errors`, `mod.rs` and `services` and no `use_cases`
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md` §Decisions D-13, D-14, D-15, D-16
  </read_first>
  <files>.planning/decisions/0018-m6-facade-reexport-policy.md</files>
  <action>
Create `.planning/decisions/0018-m6-facade-reexport-policy.md`. No frontmatter. Exactly seven H2
headings in the canonical order.

`## Context` opens by citing ADR-0008's framing of the version question almost verbatim — the
three-way disagreement, the cross-phase coupling, the rule that whichever phase runs first records
the answer — then states what ADR-0008 did not touch: the re-export policy itself. State the
disagreement: the overview's Epic 2 AC 6, Epic 4 AC 5 and risk register require backward-compatible
facade re-exports; Epic 2 Non-Goal 7 and Epic 4 FR-4.11 forbid them; shipped code follows the PRDs.
State that `src/application/` holds `cli`, `errors`, `mod.rs` and `services` and that
`src/application/use_cases/` does not exist, confirmed by listing the directory during this task.
State that Epic 2's Open Question 4 asked for team confirmation and never received it.

`## Decision` states three things. (i) The no-shim posture stands as policy: relocated types are not
re-exported at their original paths, and the old paths are intentionally retired. (ii) Epic 2's Open
Question 4 is now confirmed by this ADR rather than left dangling. (iii) The version consequence,
citing ADR-0008 by number: Milestone 6 removed publicly reachable import paths, which is a breaking
change under ordinary semver reasoning, and pre-1.0 Cargo semantics make `0.7.0` the correct
expression of it — so REL-01's single-version story is unaffected and no major bump is required.
State explicitly that this clause cites Phase 4's answer and does not re-derive it. Add a fourth
clause recording the Milestone 5 to Milestone 6 posture flip as history: `REQ-battalion-facade-shim`
required a re-export shim and was correct for Milestone 5; Milestone 6 retired it; the ledger records
that row as superseded by shipped code pointing here, not as a contradiction to reopen.

`## Considered Options` is a bulleted list, never prose: ratify the no-shim posture the PRDs and the
tree agree on (accepted); reintroduce facade re-exports to satisfy the overview's acceptance criteria
(rejected — a code change this phase is forbidden to make, against a posture two PRDs and the shipped
tree already settled); leave the overview and the PRDs both live and disambiguate per reader
(rejected — that is the state ARCH-04 exists to end); re-derive the version consequence independently
of ADR-0008 (rejected — produces a second version story that REL-01 would then have to reconcile).

`## Code Locations` is a bulleted list, never prose, one citation per bullet, all re-grepped at
authoring time: `src/application/mod.rs` showing the modules that do exist; `src/application/services/`
as the shipped orchestration home; the overview's Epic 2 AC 6, Epic 4 AC 5 and risk-register lines;
Epic 2 Non-Goal 7 and Open Question 4; Epic 4 Goal 7 and FR-4.11; and
`.planning/decisions/0008-workspace-version-0-7-0.md` as the cited version answer.

`## Code Conformance` is the bare word `conforms` plus prose stating that shipped code already
implements the no-shim posture and no code changes; the executing work is the source annotations in
Task 2 and Task 3 of this plan.

`## Downstream Consumers` is a bulleted list naming: Phase 11 / FACADE-02 D1 explicitly, stating
that D1's re-export question is answered here and Phase 11 applies rather than re-opens it (D-16);
plans 07-08 and 07-10, whose ledger rows for `REQ-orchestration-no-reexport-shims`,
`REQ-circuitbreaker-old-path-retired` and `REQ-battalion-facade-shim` cite this ADR; Phase 4 /
REL-01, which is unaffected; and plan 07-13, which adds this ADR's row to `PROMOTION.md`.
  </action>
  <verify>
    <automated>test -f .planning/decisions/0018-m6-facade-reexport-policy.md &amp;&amp; [ "$(grep -c '^## ' .planning/decisions/0018-m6-facade-reexport-policy.md)" = "7" ] &amp;&amp; grep -q '0008' .planning/decisions/0018-m6-facade-reexport-policy.md &amp;&amp; grep -q 'FACADE-02' .planning/decisions/0018-m6-facade-reexport-policy.md &amp;&amp; ! test -d src/application/use_cases</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## ' .planning/decisions/0018-m6-facade-reexport-policy.md` equals `7`, in the canonical order
    - `head -1 .planning/decisions/0018-m6-facade-reexport-policy.md` does not equal `---`
    - `sed -n '/^## Considered Options/,/^## Code Locations/p' .planning/decisions/0018-m6-facade-reexport-policy.md | grep -c '^- '` is at least `4`
    - `sed -n '/^## Code Locations/,/^## Code Conformance/p' .planning/decisions/0018-m6-facade-reexport-policy.md | grep -c '^- '` is at least `7`
    - `sed -n '/^## Code Conformance/,/^## Downstream Consumers/p' .planning/decisions/0018-m6-facade-reexport-policy.md | grep -c '^conforms$'` equals `1`
    - `grep -c '0008' .planning/decisions/0018-m6-facade-reexport-policy.md` is at least `2` — cited in Context and in Code Locations
    - `grep -c 'FACADE-02' .planning/decisions/0018-m6-facade-reexport-policy.md` is at least `1`
    - `grep -c 'REQ-battalion-facade-shim' .planning/decisions/0018-m6-facade-reexport-policy.md` is at least `1` — the M5→M6 posture flip is recorded
    - `test -d src/application/use_cases` fails — the claim the ADR makes about the tree holds at authoring time
  </acceptance_criteria>
  <done>ADR-0018 exists with the seven canonical headings, records the no-shim posture as policy, confirms Epic 2's Open Question 4, states the version consequence by citing ADR-0008 rather than re-deriving it, records the M5→M6 posture flip as history, and names Phase 11 / FACADE-02 D1 as its downstream consumer.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate the Milestone 6 overview — prerequisite numbering and the three re-export clauses</name>
  <read_first>
    - `.project/Milestone_6-Architectural-Refinements/overview/Milestone_6-Tier_3-Architectural-Refinements.md` — read in full before touching it; locate the "Completed in Milestones 1 and 2" prerequisite line, Epic 2 AC 6, Epic 4 AC 5 and the risk-register re-export row by grep
    - `.project/Milestone_3-Completion/RELEASE_NOTES_MILESTONE_3.md` lines 1-8, 28-31 and 360 — the three verbatim D-00g markup shapes
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-PATTERNS.md` §6 — the correction-target table row for this document, which assigns shapes (a)+(b) and (c)
    - `.planning/decisions/0018-m6-facade-reexport-policy.md` and `.planning/decisions/0014-milestone-4-6-tier-numbering.md` — the two ADRs the annotations cite
  </read_first>
  <files>.project/Milestone_6-Architectural-Refinements/overview/Milestone_6-Tier_3-Architectural-Refinements.md</files>
  <action>
Annotate this one document per D-00g. This plan owns the whole file; make all four corrections in one
pass.

(1) Add a top-of-file blockquote banner immediately under the existing H1, dated, naming both
ADR-0018 and ADR-0014, stating in two sentences that this document's backward-compatible re-export
requirement is the minority position that shipped code and both Epic PRDs contradict, and that its
"Completed in Milestones 1 and 2" prerequisite line means Milestones 4 and 5 under the authoritative
numbering. Link both ADRs by relative path. Close with the retained-original sentence.

(2) Correct the prerequisite line inline with the strikethrough-and-append shape, citing ADR-0014.

(3) Place a standalone dated Superseded blockquote immediately above each of Epic 2 AC 6, Epic 4 AC 5
and the risk-register re-export row — three separate banners — each naming ADR-0018 and stating in
one sentence that the no-shim posture is the recorded answer and that `src/application/use_cases/`
does not exist in the shipped tree. Leave the acceptance-criteria and risk-register text itself
untouched beneath each banner.

Do not restate this document as authoritative anywhere in the annotation. Do not touch anything else
in this file.
  </action>
  <verify>
    <automated>grep -q 'ADR-0018' .project/Milestone_6-Architectural-Refinements/overview/Milestone_6-Tier_3-Architectural-Refinements.md &amp;&amp; grep -q 'ADR-0014' .project/Milestone_6-Architectural-Refinements/overview/Milestone_6-Tier_3-Architectural-Refinements.md &amp;&amp; [ "$(git diff --numstat HEAD -- .project/Milestone_6-Architectural-Refinements/overview/Milestone_6-Tier_3-Architectural-Refinements.md | awk '{print $2}')" -le "3" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ADR-0018' .project/Milestone_6-Architectural-Refinements/overview/Milestone_6-Tier_3-Architectural-Refinements.md` is at least `4` — the banner plus three Superseded blocks
    - `grep -c 'ADR-0014' .project/Milestone_6-Architectural-Refinements/overview/Milestone_6-Tier_3-Architectural-Refinements.md` is at least `2` — the banner plus the prerequisite-line note
    - `grep -ci 'Superseded' .project/Milestone_6-Architectural-Refinements/overview/Milestone_6-Tier_3-Architectural-Refinements.md` is at least `3`
    - `grep -c '~~' .project/Milestone_6-Architectural-Refinements/overview/Milestone_6-Tier_3-Architectural-Refinements.md` is at least `1` — the prerequisite line retains its original text
    - `git diff --numstat HEAD -- .project/Milestone_6-Architectural-Refinements/overview/Milestone_6-Tier_3-Architectural-Refinements.md` shows deletions no greater than `3`
    - The original acceptance-criterion sentences for Epic 2 AC 6 and Epic 4 AC 5 are still greppable in the file after the edit
  </acceptance_criteria>
  <done>The Milestone 6 overview carries a dated banner naming ADR-0018 and ADR-0014, its prerequisite line corrected inline, and three Superseded banners above the re-export clauses — with every original claim still present.</done>
</task>

<task type="auto">
  <name>Task 3: Annotate the Epic 4 CircuitBreaker PRD — confirm Goal 7 / FR-4.11 and re-point FR-4.12</name>
  <read_first>
    - `.project/Milestone_6-Architectural-Refinements/Epic_4/prd-relocate-circuitbreaker-infra.md` — read in full; locate Goal 7, FR-4.11 and FR-4.12 by grep
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md` §Decisions D-13, D-18 — the confirmation and the re-pointing
    - `docs/src/api-reference/stable-api.md` — confirm the relocated deliverable exists at the path FR-4.12 is re-pointed to; if it does not, stop and report rather than writing a path that does not resolve
    - `.planning/decisions/0018-m6-facade-reexport-policy.md` — the ADR the confirmation cites
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-PATTERNS.md` §6 markup shapes (a) and (b)
  </read_first>
  <files>.project/Milestone_6-Architectural-Refinements/Epic_4/prd-relocate-circuitbreaker-infra.md</files>
  <action>
Annotate this one document per D-00g. This plan owns the whole file.

(1) Add a top-of-file blockquote banner immediately under the existing H1, dated, naming ADR-0018,
stating in two sentences that this PRD's Goal 7 and FR-4.11 no-shim position is the recorded answer
and has been confirmed against the milestone overview's contrary acceptance criterion, and that
FR-4.12's `STABLE_API.md` deliverable has relocated into the mdbook. Close with the retained-original
sentence.

(2) Add a bold inline confirmation note beneath Goal 7 and beneath FR-4.11 — no strikethrough, since
these clauses are correct — stating that ADR-0018 ratifies this position and that the milestone
overview's contrary acceptance criterion is annotated superseded.

(3) Correct FR-4.12 inline with the strikethrough-and-append shape: the original root `STABLE_API.md`
path struck through, `docs/src/api-reference/stable-api.md` appended, and a bold note citing D-18 and
ADR-0018 stating that the deliverable relocated into the mdbook and is not missing. Confirm the new
path exists on disk before writing it.

Commit this plan's three files together in a single commit at the end of the plan, with a Bash
timeout of at least 300000 ms.
  </action>
  <verify>
    <automated>grep -q 'ADR-0018' .project/Milestone_6-Architectural-Refinements/Epic_4/prd-relocate-circuitbreaker-infra.md &amp;&amp; grep -q 'docs/src/api-reference/stable-api.md' .project/Milestone_6-Architectural-Refinements/Epic_4/prd-relocate-circuitbreaker-infra.md &amp;&amp; test -f docs/src/api-reference/stable-api.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ADR-0018' .project/Milestone_6-Architectural-Refinements/Epic_4/prd-relocate-circuitbreaker-infra.md` is at least `3` — the banner plus the Goal 7 and FR-4.11 notes
    - `grep -c 'docs/src/api-reference/stable-api.md' .project/Milestone_6-Architectural-Refinements/Epic_4/prd-relocate-circuitbreaker-infra.md` is at least `1`
    - `test -f docs/src/api-reference/stable-api.md` succeeds — the re-pointed path resolves
    - `grep -c 'STABLE_API.md' .project/Milestone_6-Architectural-Refinements/Epic_4/prd-relocate-circuitbreaker-infra.md` is at least `1` — the original path text survives the correction
    - `git diff --numstat HEAD -- .project/Milestone_6-Architectural-Refinements/Epic_4/prd-relocate-circuitbreaker-infra.md` shows deletions no greater than `2`
    - `git log -1 --name-only` shows ADR-0018 and both annotated documents in a single commit
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
  </acceptance_criteria>
  <done>The Epic 4 CircuitBreaker PRD carries a dated ADR-0018 banner, confirmation notes on Goal 7 and FR-4.11, and FR-4.12 re-pointed to a verified mdbook path with the original path text retained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADR → Phase 11 / FACADE-02 D1 | ADR-0018's policy is what stops Phase 11 re-opening the re-export question. An ADR that does not name D1 explicitly leaves the question live in two places. |
| ADR-0018 → ADR-0008 | The version consequence is Phase 4's answer, cited. A re-derivation here would create a second version story that REL-01 must then reconcile. |
| `.project/` historical corpus → the annotation | The overview's acceptance criteria are the record of the minority position. Deleting them makes ADR-0018's Context unverifiable. |
| A re-pointed deliverable path → the filesystem | FR-4.12's new path is written into a historical document. A path that does not resolve is a new defect of the exact class ARCH-05 exists to remove. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-16 | Tampering (of the record) | `prd-relocate-circuitbreaker-infra.md` FR-4.12 re-pointed path | high | mitigate | Writing a mdbook path that does not exist would replace one stale path with another and re-create the defect. Mitigated by requiring `test -f docs/src/api-reference/stable-api.md` to pass before the path is written and again as an acceptance criterion. |
| T-07-17 | Repudiation | Milestone 6 overview Epic 2 AC 6 / Epic 4 AC 5 / risk register | high | mitigate | Deleting the superseded acceptance criteria erases the minority position ADR-0018 exists to overturn. Mitigated by the Superseded-banner-above-unmodified-text pattern and a bounded deletion count. |
| T-07-18 | Elevation of privilege (of a duplicate answer) | ADR-0018 version clause | medium | mitigate | Re-deriving the version consequence would create a second, independently maintainable version story. Mitigated by requiring ADR-0008 to be cited twice and by the prohibition against re-derivation. |
| T-07-19 | Spoofing (of a settled question) | Epic 2 Open Question 4 | medium | mitigate | Leaving OQ4 dangling lets a later reader treat the policy as still open. Mitigated by requiring the ADR to record it as now confirmed. |
| T-07-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable: no package installed, no manifest touched. |

No critical threats. T-07-16 and T-07-17 are the `high` items and both are mitigated inside this
plan.
</threat_model>

<artifacts>
## Artifacts this phase produces

The full phase-wide artefact table is recorded once in `07-01-PLAN.md` §Artifacts. This plan produces
these rows:

| Artefact | Status | Owning plan | Requirement |
|---|---|---|---|
| `.planning/decisions/0018-m6-facade-reexport-policy.md` | **NEW** ADR — `conforms`, cites ADR-0008, names Phase 11 / FACADE-02 D1 | 07-05 | ARCH-04 |
| `.project/Milestone_6-.../overview/Milestone_6-Tier_3-Architectural-Refinements.md` | **ANNOTATED IN PLACE** — banner, prerequisite line, three Superseded blocks | 07-05 | ARCH-02, ARCH-04 |
| `.project/Milestone_6-.../Epic_4/prd-relocate-circuitbreaker-infra.md` | **ANNOTATED IN PLACE** — banner, Goal 7 / FR-4.11 confirmation, FR-4.12 re-pointed | 07-05 | ARCH-04, ARCH-05 |

New headings created by this plan: the seven canonical H2s inside ADR-0018. No heading is added to
either annotated document.
</artifacts>

<verification>
- `grep -c '^## ' .planning/decisions/0018-m6-facade-reexport-policy.md` equals `7`
- `grep -c '0008' .planning/decisions/0018-m6-facade-reexport-policy.md` is at least `2`
- `grep -c 'FACADE-02' .planning/decisions/0018-m6-facade-reexport-policy.md` is at least `1`
- `grep -c 'ADR-0018' .project/Milestone_6-Architectural-Refinements/overview/Milestone_6-Tier_3-Architectural-Refinements.md` is at least `4`
- `test -f docs/src/api-reference/stable-api.md` succeeds
- `git diff --numstat HEAD~1` shows deletions no greater than `3` in each annotated `.project/` file
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
</verification>

<success_criteria>
Asking "was Milestone 6 a breaking change?" returns one recorded answer with its version consequence
citing ADR-0008, instead of a milestone overview and two PRDs that flatly disagree about whether the
old import paths still resolve. Epic 2's Open Question 4 is confirmed rather than dangling, the M5→M6
posture flip is history rather than a contradiction, Phase 11's FACADE-02 D1 has its answer by name,
and FR-4.12 points at a documentation deliverable that actually exists.
</success_criteria>

<output>
Create `.planning/phases/07-workspace-ground-truth-recorded-answers/07-05-SUMMARY.md` when done.
Record: the exact clause line numbers annotated in both documents, the confirmation that
`src/application/use_cases/` is absent as listed during the task, and the confirmed existence of the
re-pointed mdbook path.
</output>
