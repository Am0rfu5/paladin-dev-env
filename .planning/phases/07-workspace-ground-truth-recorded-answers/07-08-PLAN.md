---
phase: 07-workspace-ground-truth-recorded-answers
plan: 08
type: execute
wave: 5
depends_on: ["07-01", "07-04", "07-05"]
files_modified:
  - .planning/ledgers/milestone-04-06.md
autonomous: true
requirements: [ARCH-01, ARCH-03, ARCH-04, ARCH-05]
must_haves:
  truths:
    - "The 25 `REQ-*` rows under `### Milestone 6 Epic 1`, `### Milestone 6 Epic 2` and `### Milestone 6 Epic 4` carry no stub token: each has one of the seven legend verdicts and an Evidence cell with a `file:line` citation resolved against the tree during this plan."
    - "Milestone 6's checkbox record — 100% complete, 0 open — is treated as corroborated per D-06, and each row still carries its own citation rather than inheriting the milestone-level claim."
    - "`REQ-orchestration-no-reexport-shims` and `REQ-circuitbreaker-old-path-retired` both cite ADR-0018 and both record that the shipped no-shim posture matches the PRDs and contradicts the milestone overview, whose clauses are annotated superseded."
    - "`REQ-orchestration-target-structure` is verdicted `diverged`, not `genuinely outstanding`: the four orchestrator module groups ship under `src/application/services/` with the PRD's exact module names and `src/application/use_cases/` does not exist, which is a deliberate different implementation rather than a missing one (ARCH-05(5))."
    - "`REQ-circuitbreaker-stable-api-update` is verdicted `relocated`, not `diverged` and not a gap: its deliverable ships as an mdbook chapter under `docs/src/api-reference/`, and where a row qualifies for both `relocated` and `superseded by shipped code` the scaffold's tie-break rule selects `relocated` (D-04(b), D-18)."
    - "`REQ-llm-config-bridge-location-v2` cites ADR-0017 and is recorded as the shipped shape, while its `-v1` counterpart (in Milestone 5 Epic 4, filled by plan 07-10) keeps its own row."
    - "Row count in the ledger is still exactly 115, no `REQ-*` ID appears twice, and rows in these three sections appear in the order the scaffold fixed."
  artifacts:
    - ".planning/ledgers/milestone-04-06.md — 25 rows filled under Milestone 6 Epics 1, 2 and 4"
  key_links:
    - "Ledger rows → ADR-0017 and ADR-0018 — the two recorded answers this block's contested rows cite"
    - "REQ-orchestration-target-structure and REQ-circuitbreaker-stable-api-update → the ARCH-05 corrections in plan 07-09 — the ledger row and the source annotation are the two halves of one finding"
    - "Ledger evidence cells → .github/workflows/ci.yml and feature-flags.yml — the exercising artefacts under D-01's manifest carve-out, re-grepped at authoring time"
  prohibitions:
    - "No row may be awarded `satisfied` on the strength of a `file:line` citation alone, and REQUIREMENTS.md's ingest-era status words must NOT be carried forward as if they were pre-satisfied verdicts."
    - "A relocated documentation deliverable must NOT be recorded as a gap or as outstanding work — the four mdbook relocations are `relocated`, which is the signal that stops Phase 16 planning them as missing."
    - "No `REQ-*` row may be dropped, reordered, inserted, or merged with another ID that cites the same artefact."
    - "This plan must NOT edit any `.rs` file, `Cargo.toml`, or any file under `.github/`."
---

<objective>
Fill the 25 ledger rows for Milestone 6 Epics 1, 2 and 4 — the configuration decomposition, the
orchestration relocation and the CircuitBreaker relocation.

Purpose: Milestone 6 is the one block in this corpus whose checkbox record is both complete and
correct, so it is the cheapest 25 rows in the phase — but it also hosts both of ARCH-04's contested
re-export rows, one of ARCH-05's five divergences, one of the four mdbook relocations, and the
shipped half of the LLM config bridge variant. Getting the verdict *class* right here is what stops a
later phase planning a relocation as a gap.
Output: 25 verdicted, cited rows in `.planning/ledgers/milestone-04-06.md`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-PATTERNS.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Milestone 6 Epic 1 — `application_settings.rs` Decomposition (8 rows)</name>
  <read_first>
    - `.planning/ledgers/milestone-04-06.md` — the head-note block (evidence bar, legend, manifest carve-out, D-06 per-milestone note) and the `### Milestone 6 Epic 1` section as the scaffold left it
    - `.planning/REQUIREMENTS.md` lines 3005-3020 — the eight IDs and their ingest-era status text
    - `src/config/` — the shipped configuration modules, listed during this task; note which of the PRD's and the overview's named files are present and which are not
    - `crates/paladin-memory/src/config/` and `crates/paladin-llm/src/config/` — the config modules that moved down into the leaf crates
    - `.planning/decisions/0017-llm-config-bridge-location.md` — the ADR `REQ-llm-config-bridge-location-v2` cites
    - `.planning/intel/code-verification.md` lines 82-260 — the run-3 verification block, treating its line numbers as needing a fresh check
  </read_first>
  <files>.planning/ledgers/milestone-04-06.md</files>
  <action>
Replace the Verdict and Evidence cells of the eight rows under `### Milestone 6 Epic 1` in place. Do
not insert, delete or reorder rows. The eight IDs, in scaffold order, are
`REQ-config-domain-modules`, `REQ-env-overridable-trait`, `REQ-settings-root-struct`,
`REQ-config-incremental-migration`, `REQ-config-yml-backcompat`, `REQ-rag-config-dedup`,
`REQ-config-success-metrics` and `REQ-llm-config-bridge-location-v2`.

Apply the ledger's evidence bar to every row: a real line number, never a bare path, plus an
exercising artefact — a scoped offline command for behavioural requirements, preferring
`cargo test --offline -p paladin-memory config` and equivalents over a full-workspace run; or the
manifest line plus a named CI job for structural ones, with that job's line re-grepped at the moment
of writing.

Three rows have prescribed handling. `REQ-config-domain-modules` ships in a hybrid shape that matches
neither the PRD map nor the overview map exactly — record `diverged` with the specific differences in
the Evidence cell, listing which module names are present, which are additions and which named files
are absent, all read from the tree in this task. `REQ-config-incremental-migration` records that
`application_settings.rs` is deleted and that the migration's intermediate-state discipline is
unverifiable after the fact — that is `present, unproven` if nothing exercises it, stated plainly.
`REQ-llm-config-bridge-location-v2` cites ADR-0017, records this as the shipped shape, and states in
one clause that Epic 4's cycle concern was real but mis-sited.

`REQ-config-yml-backcompat` has no located before-and-after regression test. Record it
`present, unproven` and note that `intel/constraints.md` names this deserialization contract one of
the corpus's strongest specification candidates — as a finding folded into the Evidence cell, not as
a nested outstanding item.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Milestone 6 Epic 1 /,/^### Milestone 6 Epic 2 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-')" = "8" ] &amp;&amp; [ "$(sed -n '/^### Milestone 6 Epic 1 /,/^### Milestone 6 Epic 2 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT')" = "0" ] &amp;&amp; [ "$(grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md)" = "115" ]</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Milestone 6 Epic 1 /,/^### Milestone 6 Epic 2 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-'` equals `8`
    - `sed -n '/^### Milestone 6 Epic 1 /,/^### Milestone 6 Epic 2 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `sed -n '/^### Milestone 6 Epic 1 /,/^### Milestone 6 Epic 2 /p' .planning/ledgers/milestone-04-06.md | grep -c 'ADR-0017'` is at least `1`
    - Every row in the section has an Evidence cell containing a `:` followed by a digit
    - `grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md` still equals `115`
    - Every row verdicted `satisfied` names either an executed command with its pass count or a manifest line plus its consuming CI job, both cited in the same cell
  </acceptance_criteria>
  <done>All eight Milestone 6 Epic 1 rows carry a legend verdict and a resolved citation, the hybrid config shape is recorded as `diverged` with its specific differences listed from the tree, and the bridge row cites ADR-0017.</done>
</task>

<task type="auto">
  <name>Task 2: Milestone 6 Epic 2 — Orchestration Service Relocation (9 rows)</name>
  <read_first>
    - `.planning/ledgers/milestone-04-06.md` — the `### Milestone 6 Epic 2` section and the head-note evidence bar
    - `.planning/REQUIREMENTS.md` lines 3021-3034 — the nine IDs and their ingest-era status text
    - `src/application/services/` — the four orchestrator module groups, listed during this task, and the absence of `src/application/use_cases/`
    - `src/core/platform/manager/` — confirm which services remain there after the six-service move
    - `.planning/decisions/0018-m6-facade-reexport-policy.md` — the ADR `REQ-orchestration-no-reexport-shims` cites
    - `.project/Milestone_6-Architectural-Refinements/Epic_2/prd-relocate-orchestration-services.md` — the PRD's target directory and its Non-Goal 7 / Open Question 4, for the divergence write-up
  </read_first>
  <files>.planning/ledgers/milestone-04-06.md</files>
  <action>
Replace the Verdict and Evidence cells of the nine rows under `### Milestone 6 Epic 2` in place. The
nine IDs, in scaffold order, are `REQ-orchestration-target-structure`, `REQ-six-service-relocation`,
`REQ-domain-type-placement-rules`, `REQ-manager-services-retained`,
`REQ-orchestration-consumer-import-updates`, `REQ-orchestrator-renaming`,
`REQ-core-isolation-verification`, `REQ-orchestration-test-coverage` and
`REQ-orchestration-no-reexport-shims`.

Apply the same citation and exercising-artefact discipline as Task 1.

Two rows have prescribed dispositions. `REQ-orchestration-target-structure` is `diverged`: the four
orchestrator module groups ship under `src/application/services/` with the PRD's exact module names
and `src/application/use_cases/` does not exist — cite the shipped module paths with line numbers and
state that this is a deliberate different implementation, not a missing one, cross-referencing
ARCH-05(5) and noting that plan 07-09 corrects the source PRD. `REQ-orchestration-no-reexport-shims`
cites ADR-0018 and records that no shims exist, matching the PRD and contradicting the milestone
overview whose acceptance criterion plan 07-05 annotated superseded, and that Epic 2's Open Question 4
is now confirmed.

`REQ-manager-services-retained` records exactly which files remain in `src/core/platform/manager/`,
read by listing the directory, and folds the deferred full `user_service` relocation into the Evidence
cell as a finding with its owning phase named — it is Phase 11's FACADE-02 D2, coupled to Phase 15's
DEFER-02 — rather than opening a nested outstanding item here.

`REQ-core-isolation-verification` has no located `cargo tree` evidence artefact, but the
`crate-isolation` CI job builds `paladin-core` independently, which is strong indirect evidence.
Under D-01's carve-out that is a manifest-plus-job citation, so it may be `satisfied` if the job's
line is re-grepped and cited; if the job does not cover the specific claim, record
`present, unproven`.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Milestone 6 Epic 2 /,/^### Milestone 6 Epic 3 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-')" = "9" ] &amp;&amp; [ "$(sed -n '/^### Milestone 6 Epic 2 /,/^### Milestone 6 Epic 3 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT')" = "0" ] &amp;&amp; ! test -d src/application/use_cases</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Milestone 6 Epic 2 /,/^### Milestone 6 Epic 3 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-'` equals `9`
    - `sed -n '/^### Milestone 6 Epic 2 /,/^### Milestone 6 Epic 3 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `sed -n '/^### Milestone 6 Epic 2 /,/^### Milestone 6 Epic 3 /p' .planning/ledgers/milestone-04-06.md | grep -c 'ADR-0018'` is at least `1`
    - `sed -n '/^### Milestone 6 Epic 2 /,/^### Milestone 6 Epic 3 /p' .planning/ledgers/milestone-04-06.md | grep -ci 'diverged'` is at least `1`
    - `test -d src/application/use_cases` fails — the claim the `diverged` row makes holds at authoring time
    - Every row in the section has an Evidence cell containing a `:` followed by a digit
  </acceptance_criteria>
  <done>All nine Milestone 6 Epic 2 rows carry a legend verdict and a resolved citation, the target-structure row is `diverged` with the shipped module paths cited, and the no-shim row cites ADR-0018 with Open Question 4 recorded as confirmed.</done>
</task>

<task type="auto">
  <name>Task 3: Milestone 6 Epic 4 — `CircuitBreaker` Relocation to Infrastructure (8 rows)</name>
  <read_first>
    - `.planning/ledgers/milestone-04-06.md` — the `### Milestone 6 Epic 4` section, the head-note evidence bar, and the D-04(b) mdbook-relocation caveat plus the `relocated` legend row and its tie-break rule
    - `.planning/REQUIREMENTS.md` lines 3049-3068 — the eight IDs and their ingest-era status text
    - `src/infrastructure/resilience/` — the shipped module and the relocated `CircuitBreaker`
    - `docs/src/api-reference/stable-api.md` — the relocated deliverable `REQ-circuitbreaker-stable-api-update` now applies to
    - `.planning/decisions/0018-m6-facade-reexport-policy.md` — the ADR `REQ-circuitbreaker-old-path-retired` cites
    - `.project/Milestone_6-Architectural-Refinements/Epic_4/prd-relocate-circuitbreaker-infra.md` — now annotated by plan 07-05, including the re-pointed FR-4.12
  </read_first>
  <files>.planning/ledgers/milestone-04-06.md</files>
  <action>
Replace the Verdict and Evidence cells of the eight rows under `### Milestone 6 Epic 4` in place. The
eight IDs, in scaffold order, are `REQ-resilience-module-structure`, `REQ-circuitbreaker-relocation`,
`REQ-circuitbreaker-rustdoc-updates`, `REQ-paladin-execution-service-import`,
`REQ-circuitbreaker-example-updates`, `REQ-circuitbreaker-test-updates`,
`REQ-circuitbreaker-old-path-retired` and `REQ-circuitbreaker-stable-api-update`.

Apply the same citation and exercising-artefact discipline as Tasks 1 and 2, scoping commands to the
resilience module and the CLI test files rather than running the workspace suite.

Two rows have prescribed dispositions. `REQ-circuitbreaker-old-path-retired` cites ADR-0018 and
records that the old path is unresolvable, no re-export exists, and this matches the PRD while
contradicting the milestone overview whose criterion plan 07-05 annotated superseded.
`REQ-circuitbreaker-stable-api-update` is `relocated`: the deliverable ships as
`docs/src/api-reference/stable-api.md` rather than a root `STABLE_API.md`, confirmed to exist during
this task, and the Evidence cell must state that this is the D-04(b) caveat and not a gap, citing the
FR-4.12 re-pointing plan 07-05 wrote. Where this row could also be argued `superseded by shipped
code`, apply the scaffold's tie-break rule and take `relocated`, stating that the tie-break was
applied. Note also whether `final-api.txt` and `api_surface_current.txt` exist and whether
regeneration was confirmed, pointing at DEBT-01 for the api-surface job that cannot run today.

`REQ-paladin-execution-service-import` records that `src/application/use_cases/` no longer exists,
that the execution service lives under `src/application/services/paladin/`, and cites the workspace
build as the exercising evidence — if that is only indirect, record `present, unproven`.

Commit the ledger once at the end of the plan, with a Bash timeout of at least 300000 ms. Do not
commit per row.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Milestone 6 Epic 4 /,$p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-')" = "8" ] &amp;&amp; [ "$(sed -n '/^### Milestone 6 Epic 4 /,$p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT')" = "0" ] &amp;&amp; [ "$(grep -c 'PENDING-VERDICT' .planning/ledgers/milestone-04-06.md)" = "61" ] &amp;&amp; test -f docs/src/api-reference/stable-api.md</automated>
    <human-check>Spot-check three rows across the three sections this plan filled: open the cited `file:line` and confirm it states what the row says it states (07-VALIDATION.md §Manual-Only Verifications, row 1).</human-check>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Milestone 6 Epic 4 /,$p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-'` equals `8`
    - `sed -n '/^### Milestone 6 Epic 4 /,$p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c 'PENDING-VERDICT' .planning/ledgers/milestone-04-06.md` equals `61` — 86 remaining before this plan minus this plan's 25
    - `grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md` still equals `115` and `grep -c '^### Milestone ' .planning/ledgers/milestone-04-06.md` still equals `13`
    - `grep -o '^| REQ-[a-z0-9-]*' .planning/ledgers/milestone-04-06.md | sort | uniq -d` prints nothing
    - `sed -n '/^### Milestone 6 Epic 4 /,$p' .planning/ledgers/milestone-04-06.md | grep -ci 'relocated'` is at least `1`
    - `sed -n '/^### Milestone 6 Epic 4 /,$p' .planning/ledgers/milestone-04-06.md | grep -c 'docs/src/api-reference/stable-api.md'` is at least `1`
    - `test -f docs/src/api-reference/stable-api.md` succeeds
    - `git log -1 --name-only` shows only `.planning/ledgers/milestone-04-06.md`
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
  </acceptance_criteria>
  <done>All eight Milestone 6 Epic 4 rows carry a legend verdict and a resolved citation, the stable-API row is `relocated` with the tie-break stated and the mdbook path confirmed to exist, and the old-path row cites ADR-0018.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ledger verdict class → Phase 16's documentation plan | A relocated deliverable recorded as a gap sends Phase 16 to write a document that already exists under a different path. The `relocated` class exists specifically to prevent that. |
| Ledger verdict → Phase 8 and Phase 11 planners | The `diverged` rows and the `user_service` finding in this block are read downstream as scope. |
| An earlier document's `file:line` → this plan's Evidence cell | Line numbers drift; `intel/code-verification.md`'s CI citation is already stale by 76 lines. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-28 | Tampering (of the record) | `REQ-circuitbreaker-stable-api-update` verdict class | high | mitigate | Recording a relocated mdbook chapter as a gap would put phantom work into Phase 16's plan. Mitigated by requiring the `relocated` verdict, the tie-break statement, and a passing existence check on the mdbook path. |
| T-07-29 | Tampering (of the record) | the 25 Evidence cells | high | mitigate | A `satisfied` on a citation alone is the false positive the evidence bar rejects. Mitigated by requiring an executed command with a pass count or a manifest-plus-job pair in every `satisfied` cell. |
| T-07-30 | Spoofing (of a citation) | stale `file:line` values | high | mitigate | Mitigated by the prohibition against copying line numbers forward and by asserting every Evidence cell carries a colon-plus-digit citation. |
| T-07-31 | Denial of service (of the ledger file) | concurrent edits to `milestone-04-06.md` | medium | mitigate | Mitigated structurally by one ledger-writing plan per wave, and asserted by the unchanged 115-row and 13-heading counts plus the exact remaining stub count. |
| T-07-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable: no package installed, no manifest modified. |

No critical threats. The three `high` items are each mitigated by acceptance criteria in this plan.
</threat_model>

<artifacts>
## Artifacts this phase produces

The full phase-wide artefact table is recorded once in `07-01-PLAN.md` §Artifacts. This plan produces
one of its rows:

| Artefact | Status | Owning plan | Requirement |
|---|---|---|---|
| `.planning/ledgers/milestone-04-06.md` §`### Milestone 6 Epic 1`, §`### Milestone 6 Epic 2`, §`### Milestone 6 Epic 4` | **FILLED** — 25 rows verdicted and cited | 07-08 | ARCH-01, ARCH-03, ARCH-04, ARCH-05 |

No new headings are created by this plan.
</artifacts>

<verification>
- `grep -c 'PENDING-VERDICT' .planning/ledgers/milestone-04-06.md` equals `61`
- `grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md` equals `115`
- `grep -c '^### Milestone ' .planning/ledgers/milestone-04-06.md` equals `13`
- `grep -o '^| REQ-[a-z0-9-]*' .planning/ledgers/milestone-04-06.md | sort | uniq -d` prints nothing
- `test -f docs/src/api-reference/stable-api.md` succeeds
- `git status --porcelain` is clean after the plan's single commit
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
</verification>

<success_criteria>
All 34 Milestone 6 requirement IDs now carry a `file:line`-cited verdict — the 9 plan 07-01 filled
plus the 25 here. Both ARCH-04 contested rows resolve to ADR-0018, the orchestration target structure
is `diverged` rather than outstanding, and the relocated stable-API deliverable is recorded as living
in the mdbook so no later phase plans it as a gap.
</success_criteria>

<output>
Create `.planning/phases/07-workspace-ground-truth-recorded-answers/07-08-SUMMARY.md` when done.
Record: the verdict distribution across the 25 rows, every scoped command executed with its pass
count, the exact contents of `src/config/` and `src/core/platform/manager/` as listed, and any
citation that did not resolve at the path REQUIREMENTS.md recorded.
</output>
