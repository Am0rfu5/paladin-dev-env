---
phase: 07-workspace-ground-truth-recorded-answers
plan: 10
type: execute
wave: 6
depends_on: ["07-01", "07-04", "07-05", "07-07"]
files_modified:
  - .planning/ledgers/milestone-04-06.md
autonomous: true
requirements: [ARCH-01, ARCH-03, ARCH-04, ARCH-07]
must_haves:
  truths:
    - "The 20 `REQ-*` rows under `### Milestone 5 Epic 3` and `### Milestone 5 Epic 4` carry no stub token: each has one of the seven legend verdicts and an Evidence cell with a `file:line` citation resolved against the tree during this plan."
    - "`REQ-battalion-facade-shim` is recorded `superseded by shipped code` pointing at ADR-0018, with the Evidence cell stating that the shim was correct for Milestone 5 and was retired by Milestone 6 — recorded as history, not as a contradiction to reopen (D-15)."
    - "`REQ-paladin-core-dependency-allowlist-v2` cites ADR-0015 and is recorded as the later position that matches the tree, while its `-v1` counterpart (Milestone 5 Epic 1, filled by plan 07-06) keeps its own row and its own verdict."
    - "`REQ-llm-config-bridge-location-v1` cites ADR-0017 and is recorded `superseded by shipped code` — not the shipped shape — while `-v2` (Milestone 6 Epic 1, filled by plan 07-08) records the shipped one."
    - "`REQ-llm-build-validation` cites ADR-0020 for its ≥ 50% incremental-improvement clause and records that the target is now restated per scenario rather than left as a single unjudgeable figure; the no-default-features build-time clause is recorded separately with its own evidence or its own `present, unproven`."
    - "Milestone 5's checkbox record — 96.4% complete, 17 open, mostly contradicted by the tree — is applied per row, not uniformly: a row whose open checkbox is contradicted by a shipped artefact is verdicted on the artefact, and each such contradiction is cited rather than asserted (D-06)."
    - "Row count in the ledger is still exactly 115, no `REQ-*` ID appears twice, and rows in these two sections appear in the order the scaffold fixed."
  artifacts:
    - ".planning/ledgers/milestone-04-06.md — 20 rows filled under Milestone 5 Epics 3 and 4"
  key_links:
    - "Ledger rows → ADR-0015, ADR-0017, ADR-0018, ADR-0020 — the four recorded answers this block's contested rows cite"
    - "REQ-battalion-facade-shim → ADR-0018 → Phase 11 / FACADE-02 D1 — the chain that stops Phase 11 reopening the re-export policy"
    - "Ledger evidence cells → the `crate-isolation` CI job — the exercising artefact for the extraction rows under D-01's manifest carve-out, re-grepped at authoring time"
  prohibitions:
    - "No row may be awarded `satisfied` on the strength of a `file:line` citation alone, and REQUIREMENTS.md's ingest-era status words must NOT be carried forward as if they were pre-satisfied verdicts."
    - "The M5→M6 posture flip must NOT be recorded as a contradiction to resolve — `REQ-battalion-facade-shim` is history pointing at ADR-0018."
    - "A Milestone 5 open checkbox must NOT be treated as evidence of outstanding work without a fresh grep, and must NOT be assumed stale without one either."
    - "No `REQ-*` row may be dropped, reordered, inserted, or merged with another ID that cites the same artefact."
    - "This plan must NOT edit any `.rs` file, `Cargo.toml`, or any file under `.github/`."
---

<objective>
Fill the 20 ledger rows for Milestone 5 Epics 3 and 4 — the battalion extraction and the LLM
extraction.

Purpose: this block holds the second half of two variant pairs (the dependency allowlist and the LLM
config bridge), the facade shim whose retirement one milestone later is ARCH-04's history, and the
build-validation row whose ≥ 50% clause ARCH-07 restates. It is also where D-06's per-milestone
heuristic bites hardest: Milestone 5's open checkboxes are mostly contradicted by the tree, and a
verifier who assumes that uniformly will get some rows wrong in the other direction.
Output: 20 verdicted, cited rows in `.planning/ledgers/milestone-04-06.md`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-PATTERNS.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Milestone 5 Epic 3 — `paladin-battalion` Extraction (9 rows)</name>
  <read_first>
    - `.planning/ledgers/milestone-04-06.md` — the head-note block (evidence bar, seven-value legend, manifest carve-out, D-06 per-milestone note) and the `### Milestone 5 Epic 3` section as the scaffold left it
    - `.planning/REQUIREMENTS.md` lines 2938-2953 — the nine IDs and their ingest-era status text
    - `crates/paladin-battalion/src/` — the nine execution services and three support utilities, listed during this task
    - `.github/workflows/ci.yml` — the `crate-isolation` job, located by grep; research confirmed it at line 304 on 2026-08-06 and `intel/code-verification.md`'s citation of 228 is stale
    - `.planning/decisions/0015-core-ports-dependency-allowlist.md` and `.planning/decisions/0018-m6-facade-reexport-policy.md` — the two ADRs this section's contested rows cite
    - `.planning/intel/task-completion-state.md` — Milestone 5's 17 open items, to be checked per row rather than applied uniformly
  </read_first>
  <files>.planning/ledgers/milestone-04-06.md</files>
  <action>
Replace the Verdict and Evidence cells of the nine rows under `### Milestone 5 Epic 3` in place. Do
not insert, delete or reorder rows. The nine IDs, in scaffold order, are
`REQ-battalion-crate-scaffold`, `REQ-battalion-service-extraction`, `REQ-battalion-import-migration`,
`REQ-battalion-inline-tests`, `REQ-battalion-facade-shim`, `REQ-battalion-dependency-validation`,
`REQ-battalion-example-verification`, `REQ-battalion-crate-docs` and
`REQ-paladin-core-dependency-allowlist-v2`.

Apply the ledger's evidence bar to every row: a real line number, never a bare path, plus an
exercising artefact — a scoped offline command for behavioural requirements, preferring
`cargo test --offline -p paladin-battalion` over a full-workspace run and recording the pass count;
or the manifest line plus a named CI job for structural ones, with that job's line re-grepped at the
moment of writing.

Two rows have prescribed dispositions. `REQ-battalion-facade-shim` is `superseded by shipped code`
pointing at ADR-0018: the shim lived under `src/application/use_cases/battalion/`, that directory no
longer exists, and the Evidence cell must state that the shim was correct for Milestone 5 and was
retired by Milestone 6 — history, not a contradiction to reopen. `REQ-paladin-core-dependency-allowlist-v2`
cites ADR-0015 and records that this later position matches the tree, confirming `petgraph`'s presence
in `paladin-core` by reading the manifest.

`REQ-battalion-import-migration` has no direct artefact but the crate compiles in isolation in the
`crate-isolation` job, which under D-01's carve-out is a manifest-plus-job citation. Cite the job's
re-grepped line and verdict accordingly; if the job does not cover the specific claim, record
`present, unproven`.

Four rows carry `Verify —` in the ingest record (inline tests, dependency validation, example
verification, crate docs). Apply D-03: these get first-pass depth, meaning an actual search for the
artefact — count the inline test modules, look for the named evidence artefacts, check the six named
examples exist — and record `present, unproven` plainly where nothing exercises the claim rather than
stretching for `satisfied`.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Milestone 5 Epic 3 /,/^### Milestone 5 Epic 4 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-')" = "9" ] &amp;&amp; [ "$(sed -n '/^### Milestone 5 Epic 3 /,/^### Milestone 5 Epic 4 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT')" = "0" ] &amp;&amp; [ "$(grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md)" = "115" ]</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Milestone 5 Epic 3 /,/^### Milestone 5 Epic 4 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-'` equals `9`
    - `sed -n '/^### Milestone 5 Epic 3 /,/^### Milestone 5 Epic 4 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `sed -n '/^### Milestone 5 Epic 3 /,/^### Milestone 5 Epic 4 /p' .planning/ledgers/milestone-04-06.md | grep -c 'ADR-0018'` is at least `1`
    - `sed -n '/^### Milestone 5 Epic 3 /,/^### Milestone 5 Epic 4 /p' .planning/ledgers/milestone-04-06.md | grep -c 'ADR-0015'` is at least `1`
    - `sed -n '/^### Milestone 5 Epic 3 /,/^### Milestone 5 Epic 4 /p' .planning/ledgers/milestone-04-06.md | grep -c 'ci.yml:228'` equals `0`
    - Every row in the section has an Evidence cell containing a `:` followed by a digit
    - Every row verdicted `satisfied` names either an executed command with its pass count or a manifest line plus its consuming CI job, both cited in the same cell
  </acceptance_criteria>
  <done>All nine Milestone 5 Epic 3 rows carry a legend verdict and a resolved citation, the facade-shim row is superseded by shipped code pointing at ADR-0018 with the M5→M6 flip recorded as history, and the allowlist `-v2` row cites ADR-0015.</done>
</task>

<task type="auto">
  <name>Task 2: Milestone 5 Epic 4 — `paladin-llm` Extraction (11 rows)</name>
  <read_first>
    - `.planning/ledgers/milestone-04-06.md` — the `### Milestone 5 Epic 4` section and the head-note evidence bar
    - `.planning/REQUIREMENTS.md` lines 2954-2972 — the eleven IDs and their ingest-era status text
    - `crates/paladin-llm/src/` — the provider modules, the mock adapter, the provider factory and the config bridge, listed during this task
    - `.github/workflows/feature-flags.yml` — the workspace feature matrix build and test steps, located by grep; research confirmed them at lines 115 and 118 on 2026-08-06 and found no drift
    - `.planning/decisions/0017-llm-config-bridge-location.md` and `.planning/decisions/0020-build-benchmark-per-scenario.md` — the two ADRs this section's contested rows cite
    - `.project/Milestone_5-Workspace-Decomposition/Epic_4/prd-paladin-llm-extraction.md` — now annotated by plan 07-04, including the FR-31/FR-32 Superseded block
  </read_first>
  <files>.planning/ledgers/milestone-04-06.md</files>
  <action>
Replace the Verdict and Evidence cells of the eleven rows under `### Milestone 5 Epic 4` in place. The
eleven IDs, in scaffold order, are `REQ-llm-crate-scaffold`, `REQ-llm-provider-error`,
`REQ-openai-provider-extraction`, `REQ-anthropic-provider-extraction`,
`REQ-deepseek-provider-extraction`, `REQ-llm-mock-adapters`, `REQ-llm-provider-factory`,
`REQ-llm-config-bridge-location-v1`, `REQ-llm-test-architecture`, `REQ-llm-facade-prelude` and
`REQ-llm-build-validation`.

Apply the same citation and exercising-artefact discipline as Task 1, scoping commands to
`-p paladin-llm` and preferring offline runs. For the three provider-extraction rows, the per-provider
feature legs of the workspace feature matrix in `.github/workflows/feature-flags.yml` are the
exercising artefacts under D-01's carve-out — re-grep their line numbers.

Two rows have prescribed dispositions. `REQ-llm-config-bridge-location-v1` is `superseded by shipped
code` pointing at ADR-0017, with the Evidence cell stating that this is not the shipped shape and that
Epic 4's cycle concern was real but mis-sited; cite the annotated FR-31/FR-32 block plan 07-04 wrote.
`REQ-llm-build-validation` splits into two clauses and the Evidence cell must address both: the
no-default-features build-time target gets its own evidence or its own `present, unproven`, and the
≥ 50% incremental-improvement clause cites ADR-0020 and records that the target is now restated per
scenario with two of five scenarios passing — do not verdict the whole row on one clause.

`REQ-llm-mock-adapters` and `REQ-llm-test-architecture` carry `Verify —` in the ingest record. Search
for the named artefacts — the mock module, the multi-step mock type, the five per-provider integration
test files, the no-network rule — and record what is actually there, with `present, unproven` where
nothing exercises the claim.

Commit the ledger once at the end of the plan, with a Bash timeout of at least 300000 ms. Do not
commit per row.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Milestone 5 Epic 4 /,/^### Milestone 5 Epic 5 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-')" = "11" ] &amp;&amp; [ "$(sed -n '/^### Milestone 5 Epic 4 /,/^### Milestone 5 Epic 5 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT')" = "0" ] &amp;&amp; [ "$(grep -c 'PENDING-VERDICT' .planning/ledgers/milestone-04-06.md)" = "41" ]</automated>
    <human-check>Spot-check three rows across the two sections this plan filled: open the cited `file:line` and confirm it states what the row says it states (07-VALIDATION.md §Manual-Only Verifications, row 1).</human-check>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Milestone 5 Epic 4 /,/^### Milestone 5 Epic 5 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-'` equals `11`
    - `sed -n '/^### Milestone 5 Epic 4 /,/^### Milestone 5 Epic 5 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c 'PENDING-VERDICT' .planning/ledgers/milestone-04-06.md` equals `41` — 61 remaining before this plan minus this plan's 20
    - `grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md` still equals `115` and `grep -c '^### Milestone ' .planning/ledgers/milestone-04-06.md` still equals `13`
    - `grep -o '^| REQ-[a-z0-9-]*' .planning/ledgers/milestone-04-06.md | sort | uniq -d` prints nothing
    - `sed -n '/^### Milestone 5 Epic 4 /,/^### Milestone 5 Epic 5 /p' .planning/ledgers/milestone-04-06.md | grep -c 'ADR-0017'` is at least `1`
    - `sed -n '/^### Milestone 5 Epic 4 /,/^### Milestone 5 Epic 5 /p' .planning/ledgers/milestone-04-06.md | grep -c 'ADR-0020'` is at least `1`
    - `git log -1 --name-only` shows only `.planning/ledgers/milestone-04-06.md`
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
  </acceptance_criteria>
  <done>All eleven Milestone 5 Epic 4 rows carry a legend verdict and a resolved citation, the bridge `-v1` row is superseded pointing at ADR-0017, and the build-validation row addresses both its clauses with the incremental target citing ADR-0020's per-scenario restatement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ledger verdict → Phase 8, Phase 11 and Phase 15 planners | Verdicts here are consumed downstream as scope. The facade-shim row in particular feeds Phase 11 through ADR-0018. |
| Milestone 5 checkbox state → this plan's verdicts | Milestone 5's open checkboxes are mostly contradicted by the tree, which makes both a credulous and a uniformly sceptical reading wrong. |
| An earlier document's `file:line` → this plan's Evidence cell | Line numbers drift; the `crate-isolation` citation is already stale by 76 lines in `intel/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-36 | Tampering (of the record) | the 20 Evidence cells | high | mitigate | A `satisfied` on a citation alone is the false positive the evidence bar rejects. Mitigated by requiring an executed command with a pass count or a manifest-plus-job pair in every `satisfied` cell. |
| T-07-37 | Spoofing (of a citation) | stale `file:line` values | high | mitigate | Mitigated by the prohibition against copying line numbers forward and by asserting no `ci.yml:228` appears in the filled sections. |
| T-07-38 | Tampering (of the record) | `REQ-llm-build-validation` split clauses | medium | mitigate | Verdicting a two-clause requirement on one clause hides the unmet half. Mitigated by requiring both clauses to be addressed in the Evidence cell with the incremental half citing ADR-0020. |
| T-07-39 | Elevation of privilege (of a checkbox) | Milestone 5's 17 open items | medium | mitigate | Extrapolating the "checkboxes are stale" pattern uniformly would mark real gaps satisfied. Mitigated by the prohibition requiring a fresh grep in both directions. |
| T-07-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable: no package installed, no manifest modified. |

No critical threats. T-07-36 and T-07-37 are the `high` items and both are mitigated inside this plan.
</threat_model>

<artifacts>
## Artifacts this phase produces

The full phase-wide artefact table is recorded once in `07-01-PLAN.md` §Artifacts. This plan produces
one of its rows:

| Artefact | Status | Owning plan | Requirement |
|---|---|---|---|
| `.planning/ledgers/milestone-04-06.md` §`### Milestone 5 Epic 3` and §`### Milestone 5 Epic 4` | **FILLED** — 20 rows verdicted and cited | 07-10 | ARCH-01, ARCH-03, ARCH-04, ARCH-07 |

No new headings are created by this plan.
</artifacts>

<verification>
- `grep -c 'PENDING-VERDICT' .planning/ledgers/milestone-04-06.md` equals `41`
- `grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md` equals `115`
- `grep -c '^### Milestone ' .planning/ledgers/milestone-04-06.md` equals `13`
- `grep -o '^| REQ-[a-z0-9-]*' .planning/ledgers/milestone-04-06.md | sort | uniq -d` prints nothing
- `git status --porcelain` is clean after the plan's single commit
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
</verification>

<success_criteria>
A developer can look up any of the 20 Milestone 5 Epic 3 and Epic 4 requirement IDs and see a
`file:line`-cited verdict. The facade-shim row records the M5→M6 posture flip as history pointing at
ADR-0018, both remaining halves of two variant pairs resolve to ADR-0015 and ADR-0017 by number, and
the build-validation row's ≥ 50% clause is judgeable because ADR-0020 restated it per scenario.
</success_criteria>

<output>
Create `.planning/phases/07-workspace-ground-truth-recorded-answers/07-10-SUMMARY.md` when done.
Record: the verdict distribution across the 20 rows, every scoped command executed with its pass
count, the re-grepped `crate-isolation` and feature-matrix line numbers, and any Milestone 5 open
checkbox this plan found corroborated rather than contradicted.
</output>
