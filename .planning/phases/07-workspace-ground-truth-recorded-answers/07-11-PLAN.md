---
phase: 07-workspace-ground-truth-recorded-answers
plan: 11
type: execute
wave: 7
depends_on: ["07-01", "07-07"]
files_modified:
  - .planning/ledgers/milestone-04-06.md
autonomous: true
requirements: [ARCH-01, ARCH-03, ARCH-07]
must_haves:
  truths:
    - "The 16 `REQ-*` rows under `### Milestone 5 Epic 5` and `### Milestone 5 Epic 6` carry no stub token: each has one of the seven legend verdicts and an Evidence cell with a `file:line` citation resolved against the tree during this plan."
    - "`REQ-crate-isolation-ci`'s Evidence cell cites the `crate-isolation` job at its current line in `.github/workflows/ci.yml`, re-grepped during this plan, and does not carry forward the stale line number REQUIREMENTS.md's ingest-era text records — this row is the phase's canonical demonstration that a transcribed citation is not evidence."
    - "`REQ-workspace-crate-edition-v2` cites `.planning/decisions/0009-workspace-rust-edition-2024.md` and records that this is the position the tree follows for every workspace manifest, confirmed by reading the manifests rather than asserted; Phase 7 cites ADR-0009's answer and does not re-decide ARCH-03(a) (D-09)."
    - "`REQ-build-benchmark-report` cites ADR-0020 and records the contested verdict resolved: the report and the script both exist, its summary table fails the target in three of five scenarios, its conclusion declares the target achieved, and ADR-0020 restates the target per scenario and judges the conclusion (ARCH-07)."
    - "`REQ-workspace-ci-upgrade` is recorded partial pointing at Phase 8 / DEBT-01 and at Phase 15 / PIPE-04, with the workspace-scoping half cited as shipped and the deprecated-action half cited as outstanding — the row is not verdicted on one half."
    - "Row count in the ledger is still exactly 115, no `REQ-*` ID appears twice, and rows in these two sections appear in the order the scaffold fixed."
  artifacts:
    - ".planning/ledgers/milestone-04-06.md — 16 rows filled under Milestone 5 Epics 5 and 6"
  key_links:
    - "REQ-crate-isolation-ci → .github/workflows/ci.yml — the citation whose drift from 228 to its current line is the phase's own proof that line numbers must be re-grepped"
    - "REQ-build-benchmark-report → ADR-0020 — the restated per-scenario target that makes this row judgeable"
    - "REQ-workspace-ci-upgrade → Phase 8 / DEBT-01 and Phase 15 / PIPE-04 — the two owners of its two outstanding halves"
  prohibitions:
    - "No row may be awarded `satisfied` on the strength of a `file:line` citation alone, and REQUIREMENTS.md's ingest-era status words must NOT be carried forward as if they were pre-satisfied verdicts."
    - "The `ci.yml` line number recorded for `crate-isolation` must NOT be copied from REQUIREMENTS.md, CONTEXT.md, RESEARCH.md, or `intel/code-verification.md` — it is re-grepped in this plan."
    - "No `REQ-*` row may be dropped, reordered, inserted, or merged with another ID that cites the same artefact."
    - "This plan must NOT edit any `.rs` file, `Cargo.toml`, or any file under `.github/` — the deprecated GitHub Action references are Phase 15's work."
---

<objective>
Fill the 16 ledger rows for Milestone 5 Epics 5 and 6 — the memory extraction and the workspace
finalization.

Purpose: Epic 5 is the only epic in this run verified line-for-line against its own PRD, so it is
cheap. Epic 6 is not: it holds the `crate-isolation` citation that has already gone stale once, the
CI-upgrade row with one shipped half and two outstanding halves owned by two different later phases,
and the build-benchmark row whose verdict ARCH-07 exists to make judgeable.
Output: 16 verdicted, cited rows in `.planning/ledgers/milestone-04-06.md`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-PATTERNS.md
@.planning/phases/07-workspace-ground-truth-recorded-answers/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Milestone 5 Epic 5 — `paladin-memory` Extraction (10 rows)</name>
  <read_first>
    - `.planning/ledgers/milestone-04-06.md` — the head-note block and the `### Milestone 5 Epic 5` section as the scaffold left it
    - `.planning/REQUIREMENTS.md` lines 2973-2990 — the ten IDs and their ingest-era status text
    - `crates/paladin-memory/Cargo.toml` and `crates/paladin-memory/src/` — the manifest facts and the shipped module structure, read during this task
    - `Cargo.toml` `[workspace.dependencies]` — the hoisted `sqlx` and vector-store client pins several rows cite
    - `.planning/decisions/0009-workspace-rust-edition-2024.md` — the ADR `REQ-workspace-crate-edition-v2` cites
    - `.github/workflows/feature-flags.yml` and `.github/workflows/ci.yml` — the feature matrix and `crate-isolation` jobs, both located by grep at authoring time
  </read_first>
  <files>.planning/ledgers/milestone-04-06.md</files>
  <action>
Replace the Verdict and Evidence cells of the ten rows under `### Milestone 5 Epic 5` in place. Do not
insert, delete or reorder rows. The ten IDs, in scaffold order, are `REQ-memory-crate-scaffold`,
`REQ-workspace-crate-edition-v2`, `REQ-memory-module-structure`, `REQ-garrison-adapter-extraction`,
`REQ-sanctum-adapter-extraction`, `REQ-memory-services-extraction`, `REQ-memory-originals-deletion`,
`REQ-memory-facade-reexports`, `REQ-memory-test-migration` and `REQ-memory-build-gates`.

Apply the ledger's evidence bar to every row: a real line number, never a bare path, plus an
exercising artefact — a scoped offline command for behavioural requirements, preferring
`cargo test --offline -p paladin-memory` and recording the pass count; or the manifest line plus a
named CI job for structural ones, with that job's line re-grepped at the moment of writing.

Two rows have prescribed handling. `REQ-workspace-crate-edition-v2` cites ADR-0009, records this as
the position every workspace manifest follows, and confirms that by reading the manifests during this
task rather than asserting it — state the number of manifests checked. `REQ-memory-originals-deletion`
records that the monolith garrison and sanctum adapter directories and the sanctum use-case directory
are gone, confirmed by attempting to list them.

`REQ-memory-facade-reexports`, `REQ-memory-test-migration` and `REQ-memory-build-gates` carry
`Verify —` in the ingest record. Apply D-03 first-pass depth: grep for the named re-export paths,
check inline-versus-workspace test placement, and map the five per-feature build combinations onto the
feature-matrix legs and the `crate-isolation` job. Record `present, unproven` where a claim is covered
only in spirit rather than individually confirmed, and say which part is unconfirmed.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Milestone 5 Epic 5 /,/^### Milestone 5 Epic 6 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-')" = "10" ] &amp;&amp; [ "$(sed -n '/^### Milestone 5 Epic 5 /,/^### Milestone 5 Epic 6 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT')" = "0" ] &amp;&amp; [ "$(grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md)" = "115" ]</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Milestone 5 Epic 5 /,/^### Milestone 5 Epic 6 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-'` equals `10`
    - `sed -n '/^### Milestone 5 Epic 5 /,/^### Milestone 5 Epic 6 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `sed -n '/^### Milestone 5 Epic 5 /,/^### Milestone 5 Epic 6 /p' .planning/ledgers/milestone-04-06.md | grep -c '0009'` is at least `1` — the edition `-v2` row cites the ADR
    - Every row in the section has an Evidence cell containing a `:` followed by a digit
    - `test -d src/infrastructure/adapters/garrison` fails and `test -d src/infrastructure/adapters/sanctum` fails — the deletion row's claim holds at authoring time
    - Every row verdicted `satisfied` names either an executed command with its pass count or a manifest line plus its consuming CI job, both cited in the same cell
  </acceptance_criteria>
  <done>All ten Milestone 5 Epic 5 rows carry a legend verdict and a resolved citation, the edition `-v2` row cites ADR-0009 with the manifest count stated, and the originals-deletion row's claim is confirmed against the tree.</done>
</task>

<task type="auto">
  <name>Task 2: Milestone 5 Epic 6 — Workspace Finalization (6 rows)</name>
  <read_first>
    - `.planning/ledgers/milestone-04-06.md` — the `### Milestone 5 Epic 6` section, the head-note evidence bar, and the manifest carve-out's own warning about the drifted `crate-isolation` citation
    - `.planning/REQUIREMENTS.md` lines 2991-3004 — the six IDs and their ingest-era status text, which records `crate-isolation` at a line number that has since drifted
    - `.github/workflows/ci.yml` — grep for the `crate-isolation` job key and record its current line; research confirmed 304 on 2026-08-06 against `intel/`'s stale 228
    - `.github/workflows/integration-tests.yml` — the remaining deprecated toolchain and cache action references, counted by grep during this task
    - `src/prelude.rs` and `Cargo.toml` `[workspace.dependencies]` — the prelude and facade-audit rows' citations
    - `.planning/decisions/0020-build-benchmark-per-scenario.md` — the ADR `REQ-build-benchmark-report` cites
    - `.project/Milestone_5-Workspace-Decomposition/Epic_6/build-benchmarks.md` and `scripts/benchmark-builds.sh` — the two artefacts the benchmark row cites
  </read_first>
  <files>.planning/ledgers/milestone-04-06.md</files>
  <action>
Replace the Verdict and Evidence cells of the six rows under `### Milestone 5 Epic 6` in place. The
six IDs, in scaffold order, are `REQ-facade-reexport-audit`, `REQ-paladin-prelude`,
`REQ-devcontainer-gh-cli`, `REQ-crate-isolation-ci`, `REQ-workspace-ci-upgrade` and
`REQ-build-benchmark-report`.

Three rows have prescribed handling.

`REQ-crate-isolation-ci` is this phase's canonical citation-drift case. Grep
`.github/workflows/ci.yml` for the `crate-isolation` job key, record the line it is on now, and cite
that. Do not copy the number REQUIREMENTS.md's ingest-era text records. Add a short note in the
Evidence cell stating that the earlier citation had drifted and by how much, so a later reader sees
why the ledger re-verifies rather than transcribes.

`REQ-workspace-ci-upgrade` has three clauses with different answers and the Evidence cell must address
all three: workspace scoping shipped, and cite the workflow lines that show it; the toolchain-action
upgrade is incomplete, and cite each remaining deprecated action reference by file and line as counted
in this task, pointing at Phase 15 / PIPE-04 which owns the full sweep; and the doc-test exclusion for
the ports crate remains, pointing at Phase 8 / DEBT-03. Verdict the row on the whole, not on the
shipped clause.

`REQ-build-benchmark-report` cites ADR-0020. Record that both `scripts/benchmark-builds.sh` and the
report exist, that the report's summary table fails the ≥ 50% target in three of five scenarios while
its conclusion declares the target achieved, and that ADR-0020 restates the target per scenario and
judges the conclusion rather than reconciling it. The verdict is `satisfied` for the deliverable —
the report exists and is exercised by the script — with the contested verdict recorded in the same
cell, or `present, unproven` if the script cannot be shown to have produced it.

For the remaining three rows apply the standard bar. `REQ-devcontainer-gh-cli` requires reading
`.devcontainer/Dockerfile.dev`; if it is absent or does not carry the named tooling, record that
plainly.

Commit the ledger once at the end of the plan, with a Bash timeout of at least 300000 ms.
  </action>
  <verify>
    <automated>[ "$(sed -n '/^### Milestone 5 Epic 6 /,/^### Milestone 6 Epic 1 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-')" = "6" ] &amp;&amp; [ "$(sed -n '/^### Milestone 5 Epic 6 /,/^### Milestone 6 Epic 1 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT')" = "0" ] &amp;&amp; [ "$(grep -c 'PENDING-VERDICT' .planning/ledgers/milestone-04-06.md)" = "25" ] &amp;&amp; [ "$(grep -c 'ci.yml:228' .planning/ledgers/milestone-04-06.md)" = "0" ]</automated>
    <human-check>Open the `crate-isolation` job at the line this plan cited and confirm it is the per-crate isolated build matrix the row claims (07-VALIDATION.md §Manual-Only Verifications, row 1).</human-check>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Milestone 5 Epic 6 /,/^### Milestone 6 Epic 1 /p' .planning/ledgers/milestone-04-06.md | grep -c '^| REQ-'` equals `6`
    - `sed -n '/^### Milestone 5 Epic 6 /,/^### Milestone 6 Epic 1 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PENDING-VERDICT'` equals `0` <!-- planner-discipline-allow: PENDING-VERDICT -->
    - `grep -c 'PENDING-VERDICT' .planning/ledgers/milestone-04-06.md` equals `25` — 41 remaining before this plan minus this plan's 16
    - `grep -c 'ci.yml:228' .planning/ledgers/milestone-04-06.md` equals `0` across the whole file
    - The `crate-isolation` line number cited in `REQ-crate-isolation-ci` matches `grep -n 'crate-isolation:' .github/workflows/ci.yml` as run during this task
    - `sed -n '/^### Milestone 5 Epic 6 /,/^### Milestone 6 Epic 1 /p' .planning/ledgers/milestone-04-06.md | grep -c 'PIPE-04'` is at least `1` and `grep -c 'DEBT-03'` on the same range is at least `1`
    - `sed -n '/^### Milestone 5 Epic 6 /,/^### Milestone 6 Epic 1 /p' .planning/ledgers/milestone-04-06.md | grep -c 'ADR-0020'` is at least `1`
    - `grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md` still equals `115` and `grep -o '^| REQ-[a-z0-9-]*' .planning/ledgers/milestone-04-06.md | sort | uniq -d` prints nothing
    - `git log -1 --name-only` shows only `.planning/ledgers/milestone-04-06.md`
    - `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
  </acceptance_criteria>
  <done>All six Milestone 5 Epic 6 rows carry a legend verdict and a resolved citation, the `crate-isolation` row cites a freshly grepped line with the drift noted, the CI-upgrade row addresses all three clauses with two owning phases named, and the benchmark row cites ADR-0020's per-scenario restatement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| An earlier document's `file:line` → this plan's Evidence cell | This block contains the corpus's known drift case. REQUIREMENTS.md's own ingest-era text for `REQ-crate-isolation-ci` records a line number that is now wrong by 76. |
| Ledger verdict → Phase 8 and Phase 15 planners | The CI-upgrade row splits across DEBT-03 and PIPE-04. A row verdicted on its shipped clause alone would hide two real gaps from two different phases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-40 | Spoofing (of a citation) | `REQ-crate-isolation-ci` line number | high | mitigate | Copying the stale value would put a citation in the ledger that resolves to the wrong job, in the one row where the drift is already documented. Mitigated by asserting the whole file contains zero occurrences of the stale citation and that the recorded line matches a live grep. |
| T-07-41 | Tampering (of the record) | `REQ-workspace-ci-upgrade` multi-clause verdict | high | mitigate | Verdicting on the shipped clause alone hides the deprecated-action sweep and the doctest exclusion from their owning phases. Mitigated by requiring all three clauses in the Evidence cell and both owning requirements named. |
| T-07-42 | Tampering (of the record) | the 16 Evidence cells | high | mitigate | Mitigated by requiring an executed command with a pass count or a manifest-plus-job pair in every `satisfied` cell. |
| T-07-43 | Denial of service (of the ledger file) | concurrent edits to `milestone-04-06.md` | medium | mitigate | Mitigated structurally by one ledger-writing plan per wave, and asserted by the unchanged 115-row count and the exact remaining stub count. |
| T-07-SC | Tampering (supply chain) | package installs | n/a | n/a | Not applicable: no package installed, no manifest modified. |

No critical threats. The three `high` items are each mitigated by acceptance criteria in this plan.
</threat_model>

<artifacts>
## Artifacts this phase produces

The full phase-wide artefact table is recorded once in `07-01-PLAN.md` §Artifacts. This plan produces
one of its rows:

| Artefact | Status | Owning plan | Requirement |
|---|---|---|---|
| `.planning/ledgers/milestone-04-06.md` §`### Milestone 5 Epic 5` and §`### Milestone 5 Epic 6` | **FILLED** — 16 rows verdicted and cited | 07-11 | ARCH-01, ARCH-03, ARCH-07 |

No new headings are created by this plan.
</artifacts>

<verification>
- `grep -c 'PENDING-VERDICT' .planning/ledgers/milestone-04-06.md` equals `25`
- `grep -c 'ci.yml:228' .planning/ledgers/milestone-04-06.md` equals `0`
- `grep -c '^| REQ-' .planning/ledgers/milestone-04-06.md` equals `115`
- `grep -c '^### Milestone ' .planning/ledgers/milestone-04-06.md` equals `13`
- `grep -o '^| REQ-[a-z0-9-]*' .planning/ledgers/milestone-04-06.md | sort | uniq -d` prints nothing
- `git status --porcelain` is clean after the plan's single commit
- `git diff --stat HEAD~1 -- '*.rs' 'Cargo.toml' '.github/'` is empty
</verification>

<success_criteria>
A developer can look up any of the 16 Milestone 5 Epic 5 and Epic 6 requirement IDs and see a
`file:line`-cited verdict. The `crate-isolation` citation resolves to the job it names rather than to
whatever now sits at the old line, the CI-upgrade row hands its two outstanding halves to Phase 8 and
Phase 15 by name, and the build-benchmark row is judgeable because ADR-0020 restated its target per
scenario.
</success_criteria>

<output>
Create `.planning/phases/07-workspace-ground-truth-recorded-answers/07-11-SUMMARY.md` when done.
Record: the verdict distribution across the 16 rows, the re-grepped `crate-isolation` line and its
drift from the recorded value, the count and locations of remaining deprecated action references, and
every scoped command executed with its pass count.
</output>
