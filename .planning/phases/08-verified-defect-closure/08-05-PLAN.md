---
phase: 08-verified-defect-closure
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_7/prd-paladin-web-single-framework-axum.md
  - .project/Milestone_12-Web-API/Epic_1/prd-agent-registry-execution-api.md
  - .project/Milestone_12-Web-API/Epic_5/prd-api-security-authorization.md
  - .project/Milestone_12-Web-API/Epic_6/prd-openapi-spec-interactive-docs.md
  - .project/Milestone_12-Web-API/Epic_7/prd-deployment-artifacts-examples-docs.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DEBT-01]

must_haves:
  truths:
    - "All nine stale `project/current-exports.txt` references are gone — the five *tooling* ones in plan 08-02, and the four FR clauses plus one `cross_refs`-style bullet across five requirement documents here (ROADMAP criterion 1, final clause)."
    - "A future implementer reading any of the five source PRDs sees the corrected `.project/current-exports.txt` path, so the defect stops propagating forward — it was written into requirements in June 2026, months after commit `928c6d5` renamed the directory."
    - "A future *planner* reading REQUIREMENTS.md's traceability sees the corrected path in the five `REQ-*` rows that carry it, so both the implementer-facing and planner-facing surfaces agree (D-04's both-sides requirement)."
    - "No `.project/` text is deleted or rewritten: every defective clause keeps its original wording, struck and retained, with a dated correction appended beneath it and a document-level banner at the top (D-00c)."
    - statement: "No sixth propagation site exists — a repository-wide grep for the stale literal outside `.project/` task-list history and outside the corrected sites returns nothing new."
      verification: backstop
  artifacts:
    - "Five `.project/` PRDs, each carrying a dated `> **Correction (dated 2026-08-06, DEBT-01):**` banner immediately below its H1 and an inline `~~struck~~` + `**Corrected**` block at each defective clause."
    - ".planning/REQUIREMENTS.md — the five `REQ-*` traceability rows updated to name `.project/current-exports.txt`."
  key_links:
    - "The five requirement documents and the five tooling references are two halves of DEBT-01's nine; the ROADMAP criterion counts all nine, so this plan and plan 08-02 must both land before criterion 1's final clause is true."
    - "`.project/` is the historical ingest corpus — annotation, never rewriting (D-00c). Phase 7's `prd-expand-feature-flags.md` is the verbatim markup analog."
    - "REQUIREMENTS.md rows `REQ-web-api-baseline-changelog` (:3028), `REQ-agent-registry` (:3245), `REQ-per-agent-role-authorization` (:3292), `REQ-openapi-drift-guard` (:3303) and `REQ-m12-v060-release` (:3313) each already point at DEBT-01; they gain the corrected path, not a new pointer."
  prohibitions:
    - "Do not modify `.github/workflows/ci.yml:148`, `:393` or `:792` (`actions-rs/toolchain@v1`) — Phase 15 / PIPE-04 owns the full eight-reference action-modernization sweep."
    - "Do not converge `VisionTokenUsage` (`crates/paladin-ports/src/output/vision_port.rs:34`) onto the canonical `TokenUsage`."
    - "Do not audit, re-fence or un-ignore the 87 pre-existing `ignore`/`no_run`/`text` doc fences in `paladin-ports` — Phase 16 / DOCS-03 owns them."
    - "Do not decide which `cargo doc --workspace --no-deps` warning bar governs — Phase 10 / HARD-07 owns that question."
    - "Do not retire or delete `src/main.rs`."
    - "Do not rewrite, reflow or delete any `.project/` text — annotation only, original retained and marked superseded (D-00c)."
    - "Do not flip any DEBT-01 checkbox or traceability *status* in REQUIREMENTS.md — plan 08-09 owns checkbox and status changes behind evidence. This plan corrects path literals only."
---

<objective>
Close the requirement-text half of DEBT-01: annotate the five `.project/` source documents that
mandate writing to `project/current-exports.txt` — a path that has not existed since commit
`928c6d5` — and correct the corresponding `REQ-*` traceability rows in REQUIREMENTS.md.

Purpose: four of the five were written between 2026-06-07 and 2026-06-09, months *after* the
rename. The defect is propagating forward, not decaying: anyone implementing M8 Epic 7 FR-10 or M12
Epic 7 FR-13 literally would write to a path that does not exist and re-introduce the same inert
guard plan 08-02 just repaired. Both sides get corrected because the `.project/` documents are what a
future *implementer* reads and REQUIREMENTS.md is what a future *planner* reads (D-04).

Output: five annotated source documents and five corrected traceability rows, with every original
claim retained and marked superseded.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-verified-defect-closure/08-CONTEXT.md
@.planning/phases/08-verified-defect-closure/08-PATTERNS.md
</context>

## Execution notes

- **`.project/` is annotated, never rewritten** (D-00c). The verbatim markup analog is
  `.project/Milestone_4-Refactor-Crates-Features/Epic_1/prd-expand-feature-flags.md` — a
  document-level banner immediately below the H1, then `~~struck~~` original text with a bolded
  dated correction directly beneath. Read it before writing a single banner.
- **Banner wording and inline markup are Claude's Discretion** (CONTEXT.md) — D-00c fixes the
  *pattern*, not the prose.
- **Scope discipline:** `grep -rn 'project/current-exports.txt' .project/` returns roughly twenty
  hits. Most are **past-tense task-list checkboxes** recording that a baseline *was* regenerated
  during Milestone 12 (e.g. "7.2 Regenerated `project/current-exports.txt` — version-header bump
  only"). Those are historical records of an action already taken, not forward-looking instructions,
  and DEBT-01's scope is the **five requirement documents** named in REQUIREMENTS.md and CONTEXT.md
  D-04. Annotate those five. Record the remaining hits in the SUMMARY as historical task-list entries
  left intact by design, so a later reader does not mistake them for missed sites.
- **Evidence bar (D-00e / D-21):** record the before/after `file:line` for each corrected clause.
- This plan changes no `.rs` file; the pre-commit hook still runs the workspace gate.
  **`worktree_skip_hooks=true`** — run the gate explicitly, then commit with hooks skipped.

## Artifacts this phase produces (this plan's share)

| Symbol / path | Kind | Note |
|---|---|---|
| dated `DEBT-01` correction banner × 5 | record annotation | one per `.project/` PRD, immediately below the H1 |
| inline `~~struck~~` + `**Corrected (dated 2026-08-06, DEBT-01)**` block × 6 clauses | record annotation | M12 Epic 7 carries two clauses (a success metric and an FR) |
| corrected path in 5 `REQ-*` traceability rows | record edit | `.planning/REQUIREMENTS.md` |

## Flagged planner assumptions — unresolved edge probes (DEBT-01, records half)

No `-SPEC.md` exists, so no `## Edge Coverage` section governs. The deterministic edge probe
returned 14 phase-wide items, all `status: unresolved`. DEBT-01's four are enumerated in plan
08-02's flagged-assumptions table (E-01a…E-01d) and are not duplicated here. One additional
observation belongs to this plan and is recorded rather than dropped:

| # | Category | Probe question | Disposition |
|---|---|---|---|
| E-01e | `empty` (records) | What is the correct treatment of a *historical* stale-path reference — a task-list checkbox recording an action already completed against the old path? Is leaving it a gap or the correct empty case? | **unresolved.** This plan leaves them intact on the reasoning that a past-tense record of what was done is not an instruction and correcting it would rewrite history, which D-00c forbids. That reasoning is written down, not proven, and no requirement adjudicates it. The SUMMARY enumerates every such hit so the choice is visible. |

<tasks>

<task type="auto">
  <name>Task 1: Annotate the five .project/ requirement-text sources</name>
  <files>.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_7/prd-paladin-web-single-framework-axum.md, .project/Milestone_12-Web-API/Epic_1/prd-agent-registry-execution-api.md, .project/Milestone_12-Web-API/Epic_5/prd-api-security-authorization.md, .project/Milestone_12-Web-API/Epic_6/prd-openapi-spec-interactive-docs.md, .project/Milestone_12-Web-API/Epic_7/prd-deployment-artifacts-examples-docs.md</files>
  <read_first>
    - `.project/Milestone_4-Refactor-Crates-Features/Epic_1/prd-expand-feature-flags.md` lines 1-11 and 122-140 (**the mandatory analog** — the document-level banner shape and both inline-correction variants, `~~struck~~` + `**Corrected**` and the `**Confirmed**` variant)
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_7/prd-paladin-web-single-framework-axum.md` lines 1-5 (H1, for banner placement) and 95-102 (FR-10, the defective clause at `:99`)
    - `.project/Milestone_12-Web-API/Epic_1/prd-agent-registry-execution-api.md` lines 1-5 and 280-290 (the §7 "API surface" bullet at `:285`)
    - `.project/Milestone_12-Web-API/Epic_5/prd-api-security-authorization.md` lines 1-5 and 216-224 (the §7 "API surface" bullet at `:221`)
    - `.project/Milestone_12-Web-API/Epic_6/prd-openapi-spec-interactive-docs.md` lines 1-5 and 190-200 (the §7 "API surface" bullet at `:198` — recorded in the corpus as the "`cross_refs`" reference)
    - `.project/Milestone_12-Web-API/Epic_7/prd-deployment-artifacts-examples-docs.md` lines 1-5, 48-53 and 126-131 (**two** defective clauses: success metric 6 at `:51` and FR-13 at `:129`, the pair the corpus records as "FR-4.6")
    - `.planning/phases/08-verified-defect-closure/08-PATTERNS.md` §"`.project/` 6 documents — dated correction banners (record, annotation not rewrite)"
    - `.planning/REQUIREMENTS.md` lines 849-872 (DEBT-01's run-5 extension banner enumerating all nine references and their provenance)
  </read_first>
  <action>
    For each of the five documents, add a document-level correction banner immediately below the H1,
    then correct each defective clause inline. Re-grep every line number before editing — this corpus
    has a recorded history of citation drift.

    **Banner** — a blockquote in the shape the analog uses, carrying: the date `2026-08-06`, the ID
    `DEBT-01`, what is wrong (the document mandates writing to `project/current-exports.txt`, a path
    that has not existed since commit `928c6d5` renamed `project/` to `.project/`), where the file
    actually lives (`.project/current-exports.txt`), a pointer to this phase and to the corrected
    tooling (`scripts/check-api-surface.sh`, `scripts/extract-public-api.sh` and
    `.github/workflows/ci.yml`, all corrected in plan 08-02), and the closing sentence that original
    text is retained below with inline corrections and nothing is deleted. Wording is yours; the
    pattern is not.

    **Inline corrections** — at each site below, strike the original clause with `~~…~~`, retain it
    in full, and append directly beneath it a bolded
    `**Corrected (dated 2026-08-06, DEBT-01):**` paragraph stating the correct path and citing the
    evidence (`.project/current-exports.txt` exists at ~442 KB; `project/current-exports.txt` does
    not — re-run `ls` on both and cite the result). Where the clause is prose containing the literal
    inside a longer sentence, strike the whole bullet or numbered item rather than surgically
    editing the literal inside it, so the original reads intact.

    1. `prd-paladin-web-single-framework-axum.md:99` — FR-10's
       `./scripts/extract-public-api.sh project/current-exports.txt`.
    2. `prd-agent-registry-execution-api.md:285` — §7's "new public items will change
       `project/current-exports.txt`; regenerate the baseline…".
    3. `prd-api-security-authorization.md:221` — §7's "will change `project/current-exports.txt` —
       regenerate (expected additive…)".
    4. `prd-openapi-spec-interactive-docs.md:198` — §7's "will change
       `project/current-exports.txt` — regenerate (additive expected)". Note in the correction that
       the corpus records this reference as a `cross_refs` field; it is in fact the §7 "API surface"
       bullet, and record that label drift rather than silently reconciling it.
    5. `prd-deployment-artifacts-examples-docs.md` — **two** clauses: success metric 6 at `:51`
       ("`CHANGELOG.md` and `project/current-exports.txt` reflect the release") and FR-13 at `:129`
       ("`project/current-exports.txt` **must** be regenerated"). Both get the same treatment; a
       single banner at the top covers both.

    Leave every past-tense task-list checkbox under `.project/Milestone_12-Web-API/*/tasks-*.md`
    untouched — those record actions already taken and are historical record, not instruction. Do not
    reflow, re-wrap or re-order any surrounding text. Do not delete anything.
  </action>
  <verify>
    <automated>for f in .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_7/prd-paladin-web-single-framework-axum.md .project/Milestone_12-Web-API/Epic_1/prd-agent-registry-execution-api.md .project/Milestone_12-Web-API/Epic_5/prd-api-security-authorization.md .project/Milestone_12-Web-API/Epic_6/prd-openapi-spec-interactive-docs.md .project/Milestone_12-Web-API/Epic_7/prd-deployment-artifacts-examples-docs.md; do grep -q 'DEBT-01' "$f" || { echo "MISSING BANNER: $f"; exit 1; }; done; echo ALL-FIVE-ANNOTATED</automated>
  </verify>
  <acceptance_criteria>
    - Each of the five files contains a blockquote banner within the first 20 lines that names the date `2026-08-06` and the identifier `DEBT-01`.
    - `grep -c '~~' <file>` is ≥ 1 for each of the five — the original text is struck, not removed.
    - `grep -c 'Corrected (dated 2026-08-06' <file>` is ≥ 1 for each of the five, and `= 2` (or the banner plus two) for `prd-deployment-artifacts-examples-docs.md`, which carries two defective clauses.
    - For each of the five, `git diff --stat <file>` shows **zero deleted lines** other than lines that reappear struck in the same hunk — verify by confirming the original literal `project/current-exports.txt` still appears in each file inside a `~~…~~` span: `grep -c 'project/current-exports.txt' <file>` is unchanged or higher, never lower.
    - `grep -rn 'project/current-exports.txt' .project/ --include='prd-*.md' | grep -v '~~' | grep -v '\.project/current-exports'` produces **no output** — every remaining live (unstruck) occurrence in a PRD is gone.
    - The past-tense task-list hits are untouched: `git status --short .project/` lists exactly the five PRD files and no `tasks-*.md`.
    - The SUMMARY enumerates every `.project/` hit of the stale literal that was deliberately left intact, with its `file:line` and the one-line reason.
  </acceptance_criteria>
  <done>All five requirement-text sources carry a dated DEBT-01 banner and an inline struck-and-corrected clause at each defective site; no `.project/` text was deleted or rewritten; the historical task-list entries are intact and enumerated.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the five REQUIREMENTS.md traceability rows</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` line 3028 (`REQ-web-api-baseline-changelog` — M8 Epic 7 FR-10)
    - `.planning/REQUIREMENTS.md` line 3245 (`REQ-agent-registry` — M12 Epic 1 §7)
    - `.planning/REQUIREMENTS.md` line 3292 (`REQ-per-agent-role-authorization` — M12 Epic 5 §7)
    - `.planning/REQUIREMENTS.md` line 3303 (`REQ-openapi-drift-guard` — M12 Epic 6 `cross_refs`)
    - `.planning/REQUIREMENTS.md` line 3313 (`REQ-m12-v060-release` — M12 Epic 7 FR-4.6)
    - `.planning/REQUIREMENTS.md` lines 849-872 (DEBT-01's run-5 extension banner — the authoritative enumeration of the nine references)
    - `.planning/REQUIREMENTS.md` lines 3659-3663 (the DEBT-01…DEBT-05 traceability status rows — **do not touch these here**; plan 08-09 flips them behind evidence)
  </read_first>
  <action>
    In each of the five traceability rows above, update the reference to the corrected path so a
    future planner reading REQUIREMENTS.md sees `.project/current-exports.txt`, not the stale
    literal. Re-grep the line numbers before editing.

    Each row already carries a `→ DEBT-01` pointer and a sentence naming the stale path (for
    example, `REQ-agent-registry`'s "**§7 names `project/current-exports.txt`** → DEBT-01"). Change
    the sentence so it records both facts in past/present form: that the source clause named the
    stale path, and that it was corrected in place by Phase 8 / DEBT-01 on 2026-08-06 with the
    `.project/` annotation. Keep the `→ DEBT-01` pointer — the requirement's ownership is unchanged.
    Do not delete any existing evidence text in these rows; append.

    Do **not** change the DEBT-01…DEBT-05 status rows at `:3659-3663` (all `Pending`), do not tick
    the `- [ ] **DEBT-01**` checkbox at `:826`, and do not alter DEBT-01's own requirement text at
    `:826-872`. Those are plan 08-09's, behind the full closure evidence. This task corrects path
    literals in traceability rows and nothing else.
  </action>
  <verify>
    <automated>grep -n 'project/current-exports.txt' .planning/REQUIREMENTS.md | grep -v '\.project/current-exports' | grep -vE '^(8[2-7][0-9]|2[0-9]{3}):' | head</automated>
  </verify>
  <acceptance_criteria>
    - Each of the five named traceability rows contains the literal `.project/current-exports.txt` and a dated note attributing the correction to Phase 8 / DEBT-01, 2026-08-06.
    - Each of the five rows still contains its `→ DEBT-01` pointer — ownership was appended to, not replaced.
    - `sed -n '3659,3663p' .planning/REQUIREMENTS.md` is byte-identical to its pre-task content (the DEBT status rows are plan 08-09's).
    - `sed -n '826p' .planning/REQUIREMENTS.md` still begins `- [ ] **DEBT-01**` — the checkbox is not ticked here.
    - `git diff --stat .planning/REQUIREMENTS.md` shows changes confined to the five traceability rows (≤ 5 hunks).
    - The SUMMARY records the before/after text of each of the five rows.
  </acceptance_criteria>
  <done>The five `REQ-*` traceability rows name the corrected `.project/current-exports.txt` path with a dated Phase 8 attribution, their DEBT-01 pointers intact, and no checkbox or status row touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| — | This plan introduces no trust boundary. It annotates Markdown records under `.project/` and `.planning/`. No code, manifest, CI file or dependency changes; nothing executes as a result of this plan. |

## STRIDE Threat Register

Assessed at ASVS level 1, blocking on `high`. RESEARCH.md §"Security Domain" assessed this phase as
introducing no new dependency, network surface, credential or user-input handling. V5 (Input
Validation) and V6 (Cryptography) are not applicable and are not padded into the table.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05-01 | Tampering | Requirement text as an instruction to a future implementer | medium | mitigate | This is the defect, not a hypothetical: five ingested requirements instruct a future implementer to write the public-API baseline to a path that does not exist, which would silently re-disable the only automated public-API guard in the project — the same inert state plan 08-02 just repaired. Mitigation is the annotation itself, verified by the acceptance criterion that no live (unstruck) occurrence of the stale literal remains in any `.project/` PRD. |
| T-08-05-02 | Repudiation | Annotation destroying the historical record | low | mitigate | D-00c's annotation-not-rewrite rule is the mitigation, and it is enforced mechanically: the acceptance criteria require the original literal to still appear in each file inside a `~~…~~` span and require zero net deletions, so the historical claim remains auditable alongside its correction. |
| T-08-05-03 | Tampering | Supply chain — package-manager installs | n/a | accept | No dependency change of any kind. RESEARCH.md's Package Legitimacy Audit records "Not applicable". No `[ASSUMED]`/`[SUS]` package exists, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `grep -rn 'project/current-exports.txt' .project/ --include='prd-*.md' | grep -v '~~' |
  grep -v '\.project/current-exports'` → no output.
- Each of the five PRDs carries a `2026-08-06` / `DEBT-01` banner and at least one struck-and-
  corrected clause; `prd-deployment-artifacts-examples-docs.md` carries two.
- Each of the five REQUIREMENTS.md traceability rows names `.project/current-exports.txt` with a
  dated Phase 8 attribution and keeps its `→ DEBT-01` pointer.
- No DEBT checkbox and no DEBT status row changed (`sed -n '826p'` and `sed -n '3659,3663p'`
  unchanged).
- `git status --short .project/` lists exactly five files, none of them a `tasks-*.md`.
- Workspace gate (CLAUDE.md): `cargo test --offline --workspace` → `cargo fmt --check` →
  `cargo clippy --workspace -- -D warnings`, all exit 0.
</verification>

<success_criteria>
- Five source documents annotated per D-00c, originals retained and struck, nothing deleted.
- Five traceability rows corrected, ownership pointers intact.
- Historical task-list references left intact by design and enumerated in the SUMMARY.
- Combined with plan 08-02, all nine of DEBT-01's stale references are closed.
- Every closure claim in the SUMMARY carries the verbatim command or `file:line` that produced it
  (D-00e / D-21).
</success_criteria>

<output>
Create `.planning/phases/08-verified-defect-closure/08-05-SUMMARY.md` when done.
Record verbatim: the before/after `file:line` for each of the six corrected clauses; the before/after
text of each of the five traceability rows; the full list of `.project/` stale-literal hits
deliberately left intact with the reason; and the final
`grep -rn 'project/current-exports.txt' .project/ --include='prd-*.md'` output.
</output>
