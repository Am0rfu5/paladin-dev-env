---
phase: 08-verified-defect-closure
plan: 09
type: execute
wave: 4
depends_on: ["08-01", "08-02", "08-03", "08-04", "08-05", "08-06", "08-07", "08-08"]
files_modified:
  - .planning/ledgers/milestone-04-06.md
  - .planning/REQUIREMENTS.md
  - .planning/decisions/PROMOTION.md
  - .planning/PROJECT.md
  - .planning/phases/08-verified-defect-closure/COVERAGE.md
autonomous: false
requirements: [DEBT-01, DEBT-02, DEBT-03, DEBT-04, DEBT-05]

must_haves:
  truths:
    - "Every DEBT-01…DEBT-05 checkbox and traceability status in REQUIREMENTS.md is flipped **behind evidence** — each carries the verbatim command or `file:line` from the plan that closed it, never an inferred pass (D-00e / D-21)."
    - "The five Milestone 4-6 ledger rows this phase closes are amended **in place**, dated 2026-08-06, with superseded verdict text retained: `REQ-api-surface-ci` (:115), `REQ-deprecation-warnings` (:116), `REQ-ports-doctest-compilation` (:157), `REQ-ports-tests-and-rustdoc` (:160), `REQ-workspace-ci-upgrade` clause 3 (:225) (D-23, D-00d)."
    - "The ledger's verdict distribution is **recounted**, not adjusted arithmetically — the Phase 7 / 07-13 lesson."
    - "`.planning/decisions/PROMOTION.md` records 0022 and 0023 in its index and its next-free line reads **0024** (D-22)."
    - "`.planning/phases/08-verified-defect-closure/COVERAGE.md` exists and carries a reasoned no-external-API declaration with its reason, satisfying the `verify:pre` seal-time gate."
    - "ADR-0006's **84% workspace line-coverage floor** is re-measured with the ADR's own verbatim pipeline and is not regressed — or, if the pipeline cannot run in this environment, the blocker is recorded with the exact failing command and no coverage claim is made."
    - statement: "The three `#[test]` functions plan 08-07 gated behind `cli` are the only change to the default-feature coverage denominator this phase introduces, and their removal does not take workspace line coverage below 84.00%."
      verification: explicit
    - statement: "All 14 unresolved edge probes from the specless fallback are carried forward visibly rather than silently dropped, and each is either promoted with a falsifier or recorded as an open assumption with an owner."
      verification: backstop
  artifacts:
    - ".planning/ledgers/milestone-04-06.md — a `## Phase 8 amendments (2026-08-06)` section and five amended rows."
    - ".planning/REQUIREMENTS.md — five ticked DEBT checkboxes, five updated traceability status rows, each behind cited evidence."
    - ".planning/decisions/PROMOTION.md — index rows 0022 and 0023, next free 0024."
    - ".planning/PROJECT.md — Key Decisions rows for ADR-0022 and ADR-0023."
    - ".planning/phases/08-verified-defect-closure/COVERAGE.md — the reasoned no-external-API declaration."
  key_links:
    - "Every checkbox flip depends on a SUMMARY from a prior plan carrying the evidence: 08-02 + 08-05 for DEBT-01 (all nine references), 08-06 for DEBT-02, 08-03 for DEBT-03, 08-07 + 08-08 for DEBT-04, 08-01 for DEBT-05. A checkbox with no SUMMARY behind it is not flipped."
    - "`PROMOTION.md:45` currently reads `**Next free ADR number: 0022**` with a dated note explaining why the jump from thirteen prior ADRs landed on 0022 rather than 0021. Advancing it must preserve that note — it exists so a reader does not mistake 0022 for a skipped number."
    - "Plan 08-07 gates three `#[test]` functions behind `cli`, which changes the ADR-0006 default-feature coverage denominator. This is the phase's one plausible coverage-floor risk and is why the re-check is a first-class task, not a formality."
  prohibitions:
    - "Do not modify `.github/workflows/ci.yml:148`, `:393` or `:792` (`actions-rs/toolchain@v1`) — Phase 15 / PIPE-04 owns the full eight-reference action-modernization sweep."
    - "Do not converge `VisionTokenUsage` (`crates/paladin-ports/src/output/vision_port.rs:34`) onto the canonical `TokenUsage`."
    - "Do not audit, re-fence or un-ignore the 87 pre-existing `ignore`/`no_run`/`text` doc fences in `paladin-ports` — Phase 16 / DOCS-03 owns them."
    - "Do not decide which `cargo doc --workspace --no-deps` warning bar governs — Phase 10 / HARD-07 owns that question; record the seam DEBT-03 hands it."
    - "Do not retire or delete `src/main.rs`."
    - "Do not adjust the ledger's verdict distribution arithmetically — recount it (the Phase 7 / 07-13 lesson)."
    - "Do not `git rm` REQUIREMENTS.md or treat it as milestone-scoped — it is corpus-wide."
    - "Do not fabricate an API-coverage capability matrix in COVERAGE.md — the detector returned `{\"detected\": false, \"signals\": []}`; write the reasoned declaration instead."
---

<objective>
Close Phase 8: amend the five Milestone 4-6 ledger rows in place, flip the DEBT-01…DEBT-05
checkboxes and traceability statuses behind evidence, advance the ADR numbering bookkeeping to 0024,
record the two new ADRs in PROJECT.md's Key Decisions table, write `COVERAGE.md`, and re-measure
ADR-0006's 84% workspace line-coverage floor against the phase's changes.

Purpose: this phase's whole premise is that a claim without a command behind it is not closure. The
close-out is where that discipline is either honoured or quietly abandoned — five checkboxes and
five ledger verdicts are the most tempting things in the corpus to flip on the strength of "the plan
ran". Each one gets the verbatim command that produced it, lifted from the SUMMARY of the plan that
earned it.

Output: a records surface that matches the tree, with the phase's one real coverage risk measured
rather than assumed.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-verified-defect-closure/08-CONTEXT.md
@.planning/phases/08-verified-defect-closure/08-VALIDATION.md
@.planning/phases/08-verified-defect-closure/08-PATTERNS.md
@.planning/decisions/0006-coverage-gate.md
</context>

## Execution notes

- **Read every prior SUMMARY in this phase before touching anything**: `08-01` through `08-08`.
  Every flip in this plan is a transcription of a command already run and recorded, not a new
  judgement.
- **Ledgers are amended in place, dated, superseded text retained** (D-00d). Never a separate
  corrections file. PATTERNS.md carries the verbatim row-amendment markup from `milestone-01.md`.
- **Evidence bar (D-00e / D-21):** every amended verdict and every ticked checkbox cites the exact
  command or `file:line` that produced it.
- **`cargo-llvm-cov` is NOT installed in this environment** (`command -v cargo-llvm-cov` → missing).
  ADR-0006 anticipated exactly this and records a pipeline built on `RUSTFLAGS="-C
  instrument-coverage"` plus `llvm-profdata`/`llvm-cov` directly, which needs no crates.io fetch. Use
  the ADR's verbatim command. If `llvm-profdata`/`llvm-cov` are also unavailable, record the blocker
  with the failing command and make **no** coverage claim — a fabricated figure would be worse than
  a recorded gap, and `cargo tarpaulin` is explicitly not comparable (ADR-0006's tool-of-record note).
- **Workspace gate** (CLAUDE.md) before committing. **`worktree_skip_hooks=true`** — run the gate
  explicitly, then commit with hooks skipped.
- STATE.md and the ROADMAP phase-status line are the orchestrator's, not this plan's.

## Artifacts this phase produces (this plan's share)

| Symbol / path | Kind | Note |
|---|---|---|
| `## Phase 8 amendments (2026-08-06)` section | ledger section header | `.planning/ledgers/milestone-04-06.md` |
| five amended ledger rows | record edit | `:115`, `:116`, `:157`, `:160`, `:225` |
| five ticked `- [x] **DEBT-0N**` checkboxes | record edit | `.planning/REQUIREMENTS.md:826,874,887,910,923` |
| five updated DEBT traceability status rows | record edit | `.planning/REQUIREMENTS.md:3659-3663` |
| `PROMOTION.md` index rows `0022`, `0023` and `**Next free ADR number: 0024**` | bookkeeping | `.planning/decisions/PROMOTION.md` |
| PROJECT.md Key Decisions rows for ADR-0022 and ADR-0023 | record edit | `.planning/PROJECT.md` |
| `.planning/phases/08-verified-defect-closure/COVERAGE.md` | new record | reasoned no-external-API declaration |

## Flagged planner assumptions — the full 14-item unresolved edge-probe register

No `-SPEC.md` exists for this phase, so neither `## Edge Coverage` nor `## Prohibitions` is present
and the specless fallback fires. The deterministic edge probe reported
`coverage: {"applicable": 14, "resolved": 0, "unresolved": 14, "byVerification": {"explicit": 0,
"backstop": 0}}` — every item `status: unresolved`, `verification: null`, `resolution: null`. Its
category vocabulary (`adjacency`, `empty`, `ordering`, `concurrency`) is algorithmic and none of it
classified cleanly against a phase about CI job wiring, manifest feature flags, doctest
configuration and struct de-duplication.

**Per §A/§C of the fallback contract: an `unresolved` row stays `unresolved`. None is auto-
backstopped, none is auto-dismissed.** Three were promotable to a defensible acceptance criterion
and were promoted into `must_haves.truths` on the plan that owns them; the rest are carried as
visible planner assumptions. This is the phase-level register so that none is dropped; the
per-requirement detail lives in each owning plan.

**The category→requirement attribution below is this planner's reconstruction.** The probe returned
a count (14) rather than an item-by-item attribution, and that reconstruction is itself an
assumption, recorded here rather than presented as probe output.

| # | Category | Requirement | Owning plan | Disposition |
|---|---|---|---|---|
| E-01a | `adjacency` | DEBT-01 | 08-02 | unresolved — two side-by-side baselines (`.project/current-exports.txt` vs the root-level `api_surface_current.txt` / `final-api.txt`); no merge semantics exist, but nothing asserts the root-level files are inert |
| E-01b | `empty` | DEBT-01 | 08-02 | unresolved — `check-api-surface.sh:14` tests existence, not content; a 0-byte baseline reports every item as removed rather than as a tooling fault |
| E-01c | `ordering` | DEBT-01 | 08-02 | **promoted** to the determinism truth (two extractions, 0 differing lines) — stability *across* nightly versions remains open |
| E-01d | `concurrency` | DEBT-01 | 08-02 | **promoted** with `verification: backstop` — the guard recomputes on every run, so post-regeneration drift reports `changed`; no two-branch test exercises it |
| E-01e | `empty` (records) | DEBT-01 | 08-05 | unresolved — historical past-tense task-list references to the stale path left intact by reasoning, not by rule |
| E-02a | `adjacency` | DEBT-02 | 08-04 | unresolved — no ADR-supersedes-ADR mechanism exists in this corpus's shape; ADR-0022 writes the instruction instead |
| E-02b | `empty` | DEBT-02 | 08-06 | **promoted** with `verification: explicit` — zero `#[deprecated]` is the valid terminal state; falsifier is the grep plus the three-document agreement |
| E-02c | `adjacency` | DEBT-02 | 08-06 | unresolved — whether a first-time reader distinguishes "policy stands" from "policy withdrawn" is a judgement, tested by 08-06's human-judgment reading |
| E-02d | `ordering` | DEBT-02 | 08-06 | unresolved — nothing mechanises a document-update order if a future deprecation is added |
| E-03a | `empty` | DEBT-03 | 08-03 | unresolved — a zero-doctest crate passes indistinguishably from one not run |
| E-03b | `ordering` | DEBT-03 | 08-03 | unresolved — doctest execution order is not contractual; no exhaustive inter-example dependency check was made |
| E-03c | `concurrency` | DEBT-03 | 08-03 | unresolved — 96/96 under default parallelism is evidence, not proof of no race |
| E-04b | `adjacency` | DEBT-04 | 08-07 | unresolved — `paladin-herald/table` and the root `cli` gate can be enabled independently, yielding a crate-level type with no facade path |
| E-04c | `ordering` | DEBT-04 | 08-07 | **promoted** — task ordering is deliberate and each task's criteria require a building workspace at its end |
| E-04d | `concurrency` | DEBT-04 | 08-07 | unresolved — cargo feature unification across a mixed target set; mitigated by measuring criterion 4 with a dedicated `--no-default-features` invocation |
| E-04e | `adjacency` (build) | DEBT-04 | 08-08 | unresolved — the downstream sweep covers tracked automation only; matches RESEARCH.md's own `[ASSUMED]` A1/A2 |
| E-05a | `adjacency` | DEBT-05 | 08-01 | unresolved — no exhaustive two-import-paths-in-one-scope search; the workspace build is the practical falsifier |
| E-05b | `empty` | DEBT-05 | 08-01 | unresolved — whether `TokenUsage::default()`'s all-zeros means "no usage reported" or "zero tokens used" is not settled by ADR-0016 |

*(Eighteen rows are listed because DEBT-01 and DEBT-04 each acquired one records-side / build-side
observation during planning beyond the probe's reconstructed 14; the probe's own count of 14 is
recorded above verbatim, and the four additional rows are labelled as planner findings, not probe
output.)*

<tasks>

<task type="auto">
  <name>Task 1: Amend the five Milestone 4-6 ledger rows in place</name>
  <files>.planning/ledgers/milestone-04-06.md</files>
  <read_first>
    - `.planning/ledgers/milestone-04-06.md` lines 113-118 (`REQ-api-surface-ci` at `:115` `genuinely outstanding`; `REQ-deprecation-warnings` at `:116` `genuinely outstanding`)
    - `.planning/ledgers/milestone-04-06.md` lines 155-162 (`REQ-ports-doctest-compilation` at `:157` `genuinely outstanding`; `REQ-ports-tests-and-rustdoc` at `:160` `present, unproven`)
    - `.planning/ledgers/milestone-04-06.md` lines 223-227 (`REQ-workspace-ci-upgrade` at `:225` `deferred with reason`, whose clause 3 is the `paladin-ports` doc-test exclusion)
    - `.planning/ledgers/milestone-04-06.md` §"Summary" and its verdict-distribution table (the counts this plan must **recount**, not adjust)
    - `.planning/ledgers/milestone-01.md` §"Phase 4 amendments (2026-08-03)" (**the mandatory analog** — the section-header framing and the row-amendment markup PATTERNS.md quotes verbatim)
    - `.planning/phases/08-verified-defect-closure/08-PATTERNS.md` §"`.planning/ledgers/milestone-04-06.md` (record, in-place amendment)"
    - the SUMMARYs of plans `08-02`, `08-03`, `08-05`, `08-06` (the evidence each amendment cites)
  </read_first>
  <action>
    Open a `## Phase 8 amendments (2026-08-06)` section using the same "amended in place, per Phase
    N's own convention: every row below is additive, retains any superseded text, and carries a dated
    note naming its evidence" framing the `milestone-01.md` analog uses. Then amend each of the five
    rows **in the row itself** (D-00d — amend in place, never a separate corrections file), wrapping
    the verdict cell as `**<new verdict>** (was: <old verdict>)` and appending the amendment prose in
    bold parentheses citing the exact command or `file:line`, exactly as the `milestone-01.md`
    `REL-05` rows do.

    1. **`REQ-api-surface-ci` (:115)** — `genuinely outstanding` → the verdict the evidence supports.
       Cite from `08-02`'s SUMMARY: the five corrected literals with their `file:line`, the
       regenerated baseline's item count and generation date, and **both** directions of the guard
       proof with their exit codes. Note that the four requirement-text references were closed
       separately by `08-05`.
    2. **`REQ-deprecation-warnings` (:116)** — `genuinely outstanding` → the verdict the evidence
       supports. Cite ADR-0022 by path, the `grep -rn '#\[deprecated' src crates | wc -l` → 0 result,
       and `08-06`'s three-way agreement reading. Record that this row closes by **withdrawal**, not
       by implementation, so a later reader does not look for deprecation attributes that were
       decided against.
    3. **`REQ-ports-doctest-compilation` (:157)** — `genuinely outstanding` → the verdict the
       evidence supports. Cite `08-03`'s `cargo test --offline -p paladin-ports --doc` result line
       and the removal of `ci.yml`'s `--exclude paladin-ports`, both in one commit.
    4. **`REQ-ports-tests-and-rustdoc` (:160)** — `present, unproven`. Its FR-29 "no documentation
       may be lost" clause was undermined *specifically* by the doctest disablement DEBT-03 removed,
       and the row's own text says so. Amend it to the verdict that evidence now supports, citing the
       96-passing doctest run as the exerciser its "example-code correctness is unverified" caveat
       was waiting for.
    5. **`REQ-workspace-ci-upgrade` (:225)** — `deferred with reason`, three clauses. Clause 3 (the
       `paladin-ports` doc-test exclusion, owner Phase 8 / DEBT-03) closes here. Clause 2 (the
       `actions-rs/toolchain@v1` upgrade) remains **open, owner Phase 15 / PIPE-04** — this phase is
       prohibited from touching it. Amend the row to reflect two of three clauses shipped with clause
       2 still deferred, and keep the Phase 15 pointer explicit. Do **not** flip this row to a fully
       satisfied verdict.

    Additionally, check whether the Herald half of `REQ-cli-dependency-isolation` /
    `REQ-library-only-build` (or whichever row Phase 7 recorded as `superseded by shipped code` on
    the `colored`/`comfy-table` question) exists in this ledger. ADR-0023 records that ROADMAP
    criterion 4 is stricter than that verdict and that **the row gets amended rather than the
    criterion narrowed** (D-14). If such a row exists, amend it in the same section citing
    `08-08`'s criterion-4 proof; if it does not, record in the SUMMARY that you searched and it was
    not present, so the D-14 instruction is visibly discharged either way.

    Finally, **recount** the ledger's verdict distribution by counting `REQ-*`-keyed rows — do not
    adjust the previous totals arithmetically. This is the Phase 7 / 07-13 lesson: count only
    `REQ-*`-keyed rows, and treat cluster-table verdicts as a separate bookkeeping layer. Record the
    counting command and the before/after distribution in the SUMMARY.
  </action>
  <verify>
    <automated>grep -c 'Amended by Phase 8, dated 2026-08-06' .planning/ledgers/milestone-04-06.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Phase 8 amendments (2026-08-06)' .planning/ledgers/milestone-04-06.md` returns `1`.
    - `grep -c 'Amended by Phase 8, dated 2026-08-06' .planning/ledgers/milestone-04-06.md` is ≥ 5 — one per amended row.
    - Each of the five rows contains `(was: ` followed by its original verdict string — superseded text retained (D-00d).
    - `git diff --numstat .planning/ledgers/milestone-04-06.md` shows **0 deleted lines** — every amendment is additive within its row.
    - `REQ-workspace-ci-upgrade`'s amended row still names Phase 15 / PIPE-04 as the owner of its clause 2, and its verdict is **not** a fully-satisfied one.
    - The verdict distribution in the ledger's `## Summary` was produced by a recount: the SUMMARY records the counting command, the row total, and the before/after distribution, and the row total is unchanged from its pre-plan value.
    - The SUMMARY states whether a Phase 7 `superseded by shipped code` Herald row exists and, if so, that it was amended per D-14.
  </acceptance_criteria>
  <done>Five ledger rows amended in place with retained superseded text and dated evidence; the `REQ-workspace-ci-upgrade` clause-2 deferral to Phase 15 preserved; the verdict distribution recounted rather than adjusted.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the DEBT checkboxes and traceability statuses behind evidence</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 821-937 (DEBT-01…DEBT-05 in full, including all three of DEBT-01's extension banners and its scope-split note)
    - `.planning/REQUIREMENTS.md` lines 3659-3663 (the DEBT-01…DEBT-05 traceability status rows, all `Pending`)
    - `.planning/REQUIREMENTS.md` lines 3745-3762 (the cross-phase coupling table: ARCH-03(c) → DEBT-05, HARD-07 → DEBT-03, DEBT-03 → DOCS-03)
    - the SUMMARYs of plans `08-01` through `08-08` — **all of them**; each checkbox flip transcribes evidence from a specific one
    - `.planning/phases/08-verified-defect-closure/08-VALIDATION.md` (the five criterion commands and their expected outputs)
  </read_first>
  <action>
    Tick each DEBT checkbox and update each traceability status **only** where a prior SUMMARY
    carries the evidence, and write that evidence into the requirement text as a dated closure note.
    A checkbox with no SUMMARY behind it is not flipped; record why instead.

    - **DEBT-01** (`:826`) — needs **both** halves: `08-02`'s five tooling literals plus the
      regenerated baseline and the both-direction guard proof, **and** `08-05`'s five requirement-text
      annotations. The requirement's own done-condition is explicit that all nine references close.
      Cite the `grep -rn 'project/current-exports.txt' scripts/ .github/` result, the
      `.project/`-PRD grep result, and both `check-api-surface.sh` exit codes. Also record that the
      adjacent M4 Epic 2 FR-7.3 `.public-api-baseline.txt` item named in the requirement text is
      closed as *superseded by naming*, per the requirement's own sentence — do not silently drop it.
    - **DEBT-02** (`:874`) — cite ADR-0022 by path, the zero grep, and `08-06`'s three-way agreement.
      Record explicitly that this closes by **withdrawal**.
    - **DEBT-03** (`:887`) — cite `08-03`'s crate-scoped and workspace-scoped doctest results and the
      one-commit removal of both halves of the guard. Record the HARD-07 seam in the closure note:
      the `cargo doc --workspace --no-deps` warning bar is **not** decided by this phase (D-12), and
      Phase 10 inherits the measured state.
    - **DEBT-04** (`:910`) — cite `08-08`'s verbatim criterion-4 proof (the successful
      `--no-default-features` build followed by the empty `cargo tree` grep), the `structopt`
      removal greps, and the four repaired downstream surfaces.
    - **DEBT-05** (`:923`) — cite `08-01`'s `grep -rn 'pub struct TokenUsage' crates src | wc -l` → 1
      and the workspace library-test pass count. Record that `VisionTokenUsage` remains deliberately
      out of scope per D-20, so a later reader does not treat it as a missed site.

    Update the five traceability rows at `:3659-3663` from `Pending` to the status the evidence
    supports, keeping the `Phase 8` column.

    Then check the cross-phase coupling table at `:3745-3762` and update only what this phase
    settled: ARCH-03(c) → DEBT-05 is now discharged; HARD-07 → DEBT-03 remains a live coupling with
    HARD-07 still owning the `cargo doc` bar (D-12 declined to block on it — say so); DEBT-03 →
    DOCS-03 (Phase 16) now has executable port-trait examples as its input.

    Do not tick, alter or re-verdict any requirement outside DEBT-01…DEBT-05. Do not touch the five
    traceability rows plan `08-05` corrected for path literals beyond what this task's status change
    requires. REQUIREMENTS.md is corpus-wide — never `git rm` it and never scope it to a milestone.
  </action>
  <verify>
    <automated>test "$(grep -cE '^- \[x\] \*\*DEBT-0[1-5]\*\*' .planning/REQUIREMENTS.md)" = "5" && test "$(sed -n '3655,3670p' .planning/REQUIREMENTS.md | grep -c 'Pending')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^- \[x\] \*\*DEBT-0[1-5]\*\*' .planning/REQUIREMENTS.md` returns `5`, and `grep -cE '^- \[ \] \*\*DEBT-0[1-5]\*\*' .planning/REQUIREMENTS.md` returns `0`.
    - Each of the five requirement blocks contains a dated `2026-08-06` closure note naming at least one verbatim command and its result.
    - The five traceability rows in the `3655-3670` region contain no `Pending` value.
    - DEBT-01's closure note cites **both** the tooling half (08-02) and the requirement-text half (08-05), and mentions the FR-7.3 `.public-api-baseline.txt` adjacent item.
    - DEBT-03's closure note states that the `cargo doc --workspace --no-deps` warning bar is not decided by this phase and names Phase 10 / HARD-07 as its owner.
    - DEBT-05's closure note states that `VisionTokenUsage` is deliberately out of scope per D-20.
    - `git diff .planning/REQUIREMENTS.md | grep -cE '^\-.*\[x\]'` returns `0` — no previously-ticked checkbox was unticked or disturbed.
    - The number of `- [ ]` checkboxes elsewhere in the file is unchanged: compare `grep -c '^- \[ \]' .planning/REQUIREMENTS.md` before and after; the delta is exactly `-5`.
  </acceptance_criteria>
  <done>All five DEBT checkboxes and traceability statuses reflect the tree, each behind a dated, cited command; the three cross-phase couplings are updated with only what this phase settled; nothing outside DEBT-01…DEBT-05 changed.</done>
</task>

<task type="auto">
  <name>Task 3: ADR bookkeeping, PROJECT.md, COVERAGE.md, and the ADR-0006 coverage-floor re-check</name>
  <files>.planning/decisions/PROMOTION.md, .planning/PROJECT.md, .planning/phases/08-verified-defect-closure/COVERAGE.md</files>
  <read_first>
    - `.planning/decisions/PROMOTION.md` lines 30-55 (the index table's last rows through `0021`, the `**Next free ADR number: 0022**` line at `:45`, its dated explanatory note, and the five-step append procedure below it)
    - `.planning/PROJECT.md` §"Key Decisions" (the table's column shape and the rows Phase 7 added for its eight ADRs — copy that shape)
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/COVERAGE.md` (**the exact analog** — the one-line no-external-API declaration plus a "Why the detector may fire" paragraph and a closing evidence paragraph)
    - `.planning/decisions/0006-coverage-gate.md` lines 40-105 (the verbatim measurement pipeline: `RUSTFLAGS="-C instrument-coverage"`, `LLVM_PROFILE_FILE`, `cargo test --workspace --offline`, then `llvm-profdata merge -sparse`, then `llvm-cov report` with the exact `--ignore-filename-regex`; the recorded 84.79% figure; the 84% hard floor and its "84.00% passes, 83.99% fails" arithmetic; the tool-of-record note ruling out `cargo tarpaulin`)
    - `.planning/phases/08-verified-defect-closure/08-07-SUMMARY.md` (the three `#[test]` functions gated behind `cli` — the only change to the default-feature coverage denominator this phase makes)
  </read_first>
  <action>
    **(a) `PROMOTION.md`.** Append two index rows in the existing table shape —
    `| 0022 | deprecation-requirement-withdrawal | Milestone 4 Epic 2 FR-8 deprecation requirement
    withdrawn (Phase 8) |` and `| 0023 | cli-dependency-isolation | CLI dependency isolation and the
    binary/Herald surface (Phase 8) |`, matching the wording style of the `0014`-`0021` rows — and
    advance the next-free line to `**Next free ADR number: 0024**`. **Preserve the existing dated
    note beneath that line**, which explains why the jump from thirteen prior ADRs landed on 0022
    rather than 0021; it exists so a reader does not treat 0022 as a skipped number. Add a short
    dated note of your own recording that Phase 8 consumed 0022 and 0023 and that both are the first
    ADRs in this corpus whose executing phase is their own (D-22). Follow the file's own five-step
    append procedure rather than improvising.

    **(b) `PROJECT.md` Key Decisions.** Add one row per new ADR in the table's existing shape, each
    naming the decision in one line and pointing at its `.planning/decisions/` path. Keep the
    existing evidence note about the corpus carrying zero *ingested* locked decisions intact and
    unaltered — that note is about the 263 ingested documents, not about ADRs this project has
    authored, and the two claims must not be conflated.

    **(c) `COVERAGE.md`.** Write
    `.planning/phases/08-verified-defect-closure/COVERAGE.md` in the Phase 7 analog's shape. The
    deterministic detector returned `{"detected": false, "signals": []}` for this phase, so this is a
    **reasoned declaration, not a capability matrix** — do not fabricate one. Open with the required
    form: `No external API integration: <one-line reason — what the phase touches instead>.` The
    reason is mandatory. Then, as the analog does, explain honestly why a keyword detector might fire
    on this phase's vocabulary anyway — "the `api-surface` CI job", `cargo public-api`,
    `REQ-web-api-baseline-changelog`, `REQ-openapi-drift-guard`, the `paladin-web` HTTP surface named
    in the traceability rows this phase edits — and state that every one of those refers to this
    project's own already-shipped Rust surfaces or to a CI job name, not to a network call, provider
    client or transport this phase adds or exercises. Close with the evidence: this phase adds zero
    dependencies (`git diff Cargo.lock` shows removals only, per `08-07`), makes no network call
    other than `rustup toolchain install nightly` against `static.rust-lang.org` for the existing
    `cargo public-api` tooling, and handles no credential or user input.

    **(d) The ADR-0006 coverage-floor re-check.** Run ADR-0006's pipeline **verbatim** — the
    `RUSTFLAGS`/`LLVM_PROFILE_FILE`/`cargo test --workspace --offline` invocation, then
    `llvm-profdata merge -sparse`, then `llvm-cov report` with the exact `--ignore-filename-regex`
    the ADR records. Do **not** substitute `cargo tarpaulin` (ADR-0006's tool-of-record note states a
    tarpaulin figure would not match this gate and must not be compared) and do **not** attempt
    `cargo install cargo-llvm-cov` (not installed; crates.io returns HTTP 403 here).

    Report the workspace line-coverage figure against the **84.00%** hard floor: 84.00% passes,
    83.99% fails. Pay specific attention to this phase's one plausible regression vector — plan
    08-07 gated three `#[test]` functions behind `cli`, removing them from the default-feature run
    that ADR-0006's scope binds. State the measured figure, the delta from the last recorded
    measurement, and whether the three gated tests account for any part of it.

    If the figure is **below 84.00%**, do not silently proceed: stop, record the measurement, and
    surface the options rather than choosing one — (i) un-gate the tests by giving them a
    `required-features` test target that the coverage run enables, (ii) add equivalent
    default-feature coverage for the lines they exercised, or (iii) amend ADR-0006's scope, which is
    a recorded decision requiring its own ADR amendment. If `llvm-profdata`/`llvm-cov` are
    unavailable in this environment, record the exact failing command as a blocker and make **no**
    coverage claim at all.
  </action>
  <verify>
    <automated>grep -q 'Next free ADR number: 0024' .planning/decisions/PROMOTION.md && grep -q 'deprecation-requirement-withdrawal' .planning/decisions/PROMOTION.md && grep -q 'cli-dependency-isolation' .planning/decisions/PROMOTION.md && test -f .planning/phases/08-verified-defect-closure/COVERAGE.md && grep -q 'No external API integration:' .planning/phases/08-verified-defect-closure/COVERAGE.md && echo CLOSEOUT-BOOKKEEPING-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Next free ADR number: 0024' .planning/decisions/PROMOTION.md` returns `1` and `grep -c 'Next free ADR number: 0022' .planning/decisions/PROMOTION.md` returns `0`.
    - The `PROMOTION.md` index table contains one row for `0022` and one for `0023`, each naming its slug and `(Phase 8)`, in the shape of the `0014`-`0021` rows.
    - The pre-existing dated note explaining the 0021→0022 jump is still present: `grep -c 'D-25a' .planning/decisions/PROMOTION.md` is unchanged from its pre-task value.
    - `.planning/PROJECT.md`'s Key Decisions table gains exactly two rows, one per new ADR, each pointing at its `.planning/decisions/` path; the existing zero-ingested-locked-decisions evidence note is byte-identical before and after.
    - `.planning/phases/08-verified-defect-closure/COVERAGE.md` exists and its declaration line matches the required form `No external API integration: <reason>.` with a non-empty reason.
    - `COVERAGE.md` contains no capability matrix and no per-endpoint table — `grep -c '^|' .planning/phases/08-verified-defect-closure/COVERAGE.md` returns `0`.
    - The coverage re-check is recorded in one of exactly two forms, never a third: **(i)** a measured workspace line-coverage figure with the verbatim ADR-0006 command that produced it, its comparison against the 84.00% floor, and the delta from the last recorded measurement (85.92% at HEAD `1ad8be5`, per Phase 3 plan 07); or **(ii)** a blocker naming the exact failing command, with no coverage figure asserted anywhere in the SUMMARY.
    - If a figure was measured and it is `>= 84.00`, the SUMMARY states whether the three `cli`-gated tests moved it and by how much. If it is `< 84.00`, the plan HALTS at the checkpoint below rather than proceeding.
    - `command -v cargo-tarpaulin` output is irrelevant and no tarpaulin figure appears anywhere — `grep -ci tarpaulin` over the SUMMARY returns `0` except in a sentence explaining why it was not used.
  </acceptance_criteria>
  <done>`PROMOTION.md` indexes 0022 and 0023 and points at 0024 with its explanatory note intact; PROJECT.md records both decisions; `COVERAGE.md` carries a reasoned no-external-API declaration; and the ADR-0006 floor is either re-measured with the ADR's own pipeline or recorded as blocked with the failing command.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase seal — human review of the three shipped-surface changes and the coverage floor</name>
  <what-built>
Phase 8 is closed on paper: five ledger rows amended in place with retained superseded text, five
DEBT checkboxes and traceability statuses flipped behind cited evidence, `PROMOTION.md` advanced to
0024 with ADR-0022 and ADR-0023 indexed, PROJECT.md's Key Decisions extended, `COVERAGE.md` written,
and ADR-0006's 84% workspace line-coverage floor re-measured against a denominator this phase
deliberately changed by gating three `#[test]` functions behind `cli`.

Three of this phase's decisions changed a shipped, published surface and were flagged
⚠ HUMAN REVIEW in `08-CONTEXT.md` without ever being human-confirmed: **D-06** (withdrawing a
published deprecation policy via ADR-0022), **D-13** (`cargo run` no longer builds the `paladin`
binary without `--features cli`), and **D-14** (shrinking the crates.io-published `paladin-herald`
crate's default API). This checkpoint is the first and only point at which a human sees all three
together, with their evidence, before the phase is sealed.
  </what-built>
  <how-to-verify>
1. **The three shipped-surface changes.** Read `.planning/decisions/0022-deprecation-requirement-withdrawal.md`
   and `.planning/decisions/0023-cli-dependency-isolation.md` end to end. Confirm you accept each
   decision, its rejected options, and the user-visible consequences recorded in `CHANGELOG.md`'s
   two `[Unreleased] ### Changed` entries.
2. **The coverage floor.** Read this plan's SUMMARY §coverage. Confirm it reports either a measured
   figure `>= 84.00%` with the verbatim ADR-0006 command, or a blocker with the exact failing
   command and no figure. If it reports a figure `< 84.00%`, do not approve — the SUMMARY should
   present the three options (un-gate the tests via a `required-features` test target the coverage
   run enables; add equivalent default-feature coverage; or amend ADR-0006's scope by ADR amendment)
   and you choose one.
3. **The five criterion commands, spot-checked.** Run, from the repository root:
   `grep -rn 'pub struct TokenUsage' crates src | wc -l` (expect `1`);
   `bash scripts/check-api-surface.sh .project/current-exports.txt` (expect `✅ API surface unchanged`, exit 0);
   `grep -rn '#\[deprecated' src crates | wc -l` (expect `0`);
   `cargo test --offline -p paladin-ports --doc` (expect `96 passed; 0 failed` or better);
   `cargo build --offline --lib --no-default-features && cargo tree --offline --no-default-features | grep -E 'structopt|colored|comfy-table'` (expect the build to succeed and the grep to print nothing).
4. **The records discipline.** Open `.planning/ledgers/milestone-04-06.md` §"Phase 8 amendments
   (2026-08-06)". Confirm each of the five rows carries `(was: <old verdict>)` with its prior text
   retained and a dated evidence citation, and that `REQ-workspace-ci-upgrade` still names Phase 15 /
   PIPE-04 as owner of its clause-2 `actions-rs` deferral.
5. **The prohibitions held.** `grep -n 'actions-rs/toolchain@v1' .github/workflows/ci.yml` still
   returns `148`, `393`, `792`; `git diff --stat crates/paladin-ports/src/output/vision_port.rs` is
   empty; `src/main.rs` still exists.
  </how-to-verify>
  <resume-signal>Type "approved" to seal the phase, or describe the issues (including a coverage-floor option if the measured figure is below 84.00%)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning records → every future phase's planner | `.planning/REQUIREMENTS.md`, the Milestone 4-6 ledger and `PROMOTION.md` are what the next planner reads instead of re-verifying the tree. A wrong verdict here misroutes work in Phases 9-16. |
| coverage measurement → the ADR-0006 hard gate | The 84% floor is a hard-fail gate. A figure produced by the wrong pipeline, or asserted without one, would let a real regression through. |

## STRIDE Threat Register

Assessed at ASVS level 1, blocking on `high`. RESEARCH.md §"Security Domain" assessed this phase as
introducing no new dependency, network surface, credential or user-input handling. V5 (Input
Validation) and V6 (Cryptography) are not applicable and are not padded into the table.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-09-01 | Repudiation | Verdict flips asserted without the command that produced them | high | mitigate | This corpus's central failure mode: five ingest runs found checkbox state wrong in *both* directions, and Milestone 10 is recorded 100% complete while carrying a false acceptance criterion. Mitigation is structural — every flip in tasks 1 and 2 must transcribe a verbatim command from a named prior SUMMARY, the acceptance criteria require a dated citation in each amended row and each closure note, and a checkbox with no SUMMARY behind it is explicitly not flipped. Blocking at ASVS L1 requires mitigation, not acceptance. |
| T-08-09-02 | Tampering | Coverage gate — a figure from the wrong tool, or none at all, passing as a re-check | medium | mitigate | ADR-0006 records a verbatim pipeline and an explicit tool-of-record note ruling out `cargo tarpaulin` as non-comparable. Task 3 requires that pipeline verbatim and permits exactly two recorded outcomes — a measured figure with its command, or a named blocker with no figure. The blocking checkpoint gives a human the third case (a figure below the floor) rather than letting the plan choose. |
| T-08-09-03 | Denial of Service | Coverage regression from this phase's own change | medium | mitigate | Plan 08-07 gates three `#[test]` functions behind `cli`, removing them from the default-feature run ADR-0006's scope binds. This is a known, named, quantified vector rather than a surprise: task 3 must state whether those three account for any part of the delta, and the checkpoint blocks on a sub-floor figure. |
| T-08-09-04 | Tampering | `.planning/REQUIREMENTS.md` treated as milestone-scoped | medium | mitigate | REQUIREMENTS.md is corpus-wide, not milestone-scoped; deleting or truncating it at a milestone close would destroy the traceability every later phase reads. Prohibited explicitly in this plan's `must_haves.prohibitions`, and the acceptance criteria assert the file's non-DEBT checkbox count is unchanged and no previously-ticked box was disturbed. |
| T-08-09-05 | Tampering | Supply chain — package-manager installs | n/a | accept | No dependency change, and `cargo install cargo-llvm-cov` is explicitly **not** attempted (crates.io returns HTTP 403 here). RESEARCH.md's Package Legitimacy Audit records "Not applicable". No `[ASSUMED]`/`[SUS]` package exists, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- Ledger: `## Phase 8 amendments (2026-08-06)` exists; ≥ 5 rows carry
  `Amended by Phase 8, dated 2026-08-06`; each carries `(was: …)`; zero deleted lines;
  `REQ-workspace-ci-upgrade` keeps its Phase 15 / PIPE-04 clause-2 pointer; the verdict distribution
  was recounted with the counting command recorded.
- REQUIREMENTS.md: `grep -cE '^- \[x\] \*\*DEBT-0[1-5]\*\*'` → `5`;
  `grep -cE '^- \[ \] \*\*DEBT-0[1-5]\*\*'` → `0`; no `Pending` in the DEBT traceability rows; each
  closure note dated and cited; the non-DEBT checkbox delta is exactly `-5`.
- Bookkeeping: `PROMOTION.md` next-free reads `0024` with 0022/0023 indexed and the pre-existing
  dated note intact; PROJECT.md gains exactly two Key Decisions rows.
- `COVERAGE.md` exists, opens with `No external API integration: <reason>.`, and contains no table.
- Coverage: either a measured figure `>= 84.00%` with the verbatim ADR-0006 command, or a recorded
  blocker with the exact failing command and no figure asserted.
- Human checkpoint approved.
- Prohibitions: `ci.yml` `actions-rs` refs unchanged at `148/393/792`; `vision_port.rs` untouched;
  `src/main.rs` present.
- Workspace gate (CLAUDE.md): `cargo test --offline --workspace` → `cargo fmt --check` →
  `cargo clippy --workspace -- -D warnings`, all exit 0.
</verification>

<success_criteria>
- Five ledger rows amended in place, dated, superseded text retained; distribution recounted.
- Five DEBT checkboxes and traceability statuses flipped, each behind a verbatim command from a
  named prior SUMMARY.
- `PROMOTION.md` at 0024 with both new ADRs indexed and its explanatory note preserved; PROJECT.md
  Key Decisions extended.
- `COVERAGE.md` written as a reasoned declaration, not a fabricated matrix.
- ADR-0006's 84% floor re-measured with the ADR's own pipeline, or blocked and recorded — never
  inferred.
- All 18 flagged edge-probe rows (the probe's reconstructed 14 plus the four planner findings) are
  carried in this plan's register; none silently dropped.
- The blocking human checkpoint approved after review of the three ⚠ HUMAN REVIEW shipped-surface
  changes.
- Every closure claim in the SUMMARY carries the verbatim command or `file:line` that produced it
  (D-00e / D-21).
</success_criteria>

<output>
Create `.planning/phases/08-verified-defect-closure/08-09-SUMMARY.md` when done.
Record verbatim: the five before/after ledger verdicts with their evidence citations; the ledger
recount command and the before/after distribution; the five DEBT closure notes; the `PROMOTION.md`
before/after next-free line; the two PROJECT.md rows; the `COVERAGE.md` declaration line; the full
ADR-0006 coverage pipeline invocation and its `llvm-cov report` TOTAL row (or the exact failing
command if blocked); the spot-checked outputs of all five ROADMAP criterion commands; and the
human's checkpoint response.
</output>
