---
phase: 09-release-security-gate-integrity
plan: 04
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - .crate-names.txt
  - scripts/check-crate-names.sh
  - Makefile
  - .github/workflows/ci.yml
  - .planning/decisions/0026-crate-name-collision-guard.md
autonomous: true
requirements: [SEC-03]

must_haves:
  truths:
    - "A crates.io package-name collision is detectable on every pull request, rather than at main-branch dry-run time or inside the release job"
    - "Adding a crate with a name not on the committed allow-list fails a required CI context, so a new name reaches crates.io only after a deliberate one-line addition"
    - "The guard compares in both directions — an unlisted tree name fails, and an allow-list entry with no corresponding crate in the tree also fails — so it cannot pass vacuously"
    - "The name guard treats an exact string match between a tree package name and an allow-list entry as satisfied; the comparison is exact rather than prefix or substring, so `paladin-core` does not satisfy an allow-list entry of `paladin-ai-core`"
    - "With an empty allow-list file the guard exits non-zero naming every unlisted publishable crate; a `crates/*/` directory without a manifest is skipped without error"
    - "Crate-name comparison is exact byte equality over ASCII; a name differing only in letter case or in hyphen-versus-underscore fails the guard rather than matching"
    - "Reordering the allow-list file leaves the guard's verdict unchanged — it is a set comparison, not a sequence comparison"
    - "Crates declaring `publish = false` are excluded from the guarded set by reading that field, not by matching a directory name"
    - "The guard has been observed failing as well as passing, with the exit codes recorded verbatim"
    - "ADR-0026 records the accepted residual cost in terms: a genuinely novel name is still checked by a human against crates.io, not by CI"
    - statement: "Two concurrent invocations of the name guard reach the same verdict and cannot interfere, because the script only reads repository files and writes nothing outside its own stdout."
      verification: backstop
  artifacts:
    - path: ".crate-names.txt"
      provides: "The committed allow-list of the eleven package names this project already owns on crates.io"
      min_lines: 11
    - path: "scripts/check-crate-names.sh"
      provides: "Bidirectional set-equality guard over publishable workspace package names (D-13)"
      contains: "exit 1"
    - path: "Makefile"
      provides: "check-crate-names target"
      contains: "check-crate-names"
    - path: ".github/workflows/ci.yml"
      provides: "CI step invoking the name guard inside a required-context job"
      contains: "check-crate-names.sh"
    - path: ".planning/decisions/0026-crate-name-collision-guard.md"
      provides: "ADR recording the offline-guard choice and its accepted residual cost"
      contains: "## Considered Options"
  key_links:
    - from: "scripts/check-crate-names.sh"
      to: ".crate-names.txt"
      via: "reads the allow-list as the authoritative owned-name set"
      pattern: "crate-names\\.txt"
    - from: "scripts/check-crate-names.sh"
      to: "Cargo.toml and crates/*/Cargo.toml"
      via: "python3 + tomllib read of each [package] table's name and publish fields"
      pattern: "tomllib"
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-crate-names.sh"
      via: "run: step inside the cargo-deny job, whose display name is a required status-check context"
      pattern: "check-crate-names.sh"
  prohibitions:
    - "The guard must not be written so that it can only ever pass. A one-directional check — every allow-listed name still exists — passes vacuously against the exact failure this guard exists to catch, a new colliding name; writing only that direction is prohibited even though it is simpler."
    - "The allow-list must not be auto-generated from the tree at check time. A guard that derives its own expected set from the thing it is checking asserts nothing; the list is committed, reviewed, and edited by hand precisely so that adding a name is a visible decision."
---

<objective>
Add the offline crates.io name-collision guard SEC-03 asks for: a committed allow-list of the eleven
package names this project already owns, a bidirectional set-equality guard over it, a `make`
target, a CI step in a required context, and the ADR that records the residual cost this design
accepts.

Purpose: collisions were not hypothetical — they cost Epic 4 two package renames and a full NO-GO
cycle, and are the reason every crate's package name and lib name now diverge. Today the earliest
guard is a main-branch-only workspace dry run, with per-crate dry runs happening inside the release
job itself. This plan moves detection to every pull request. A live crates.io query was rejected
because crates.io returns HTTP 403 in this environment, so a network check could be written but
never demonstrated — the Phase 8 D-03 failure mode.
Output: `.crate-names.txt`, `scripts/check-crate-names.sh`, a `Makefile` target, a `ci.yml` step,
and ADR-0026.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-release-security-gate-integrity/09-CONTEXT.md
@.planning/phases/09-release-security-gate-integrity/09-PATTERNS.md
@.planning/phases/09-release-security-gate-integrity/09-01-SUMMARY.md
</context>

## Artifacts this phase produces

Newly-created symbols and files across all seven Phase 9 plans. Nothing on this list exists in the
tree today, so none of it can be a drift regression against a prior phase.

| Artifact | Plan | Kind |
|---|---|---|
| `crates/paladin-herald/CHANGELOG.md` | 09-01 | new file |
| `scripts/check-changelogs.sh` | 09-01 | new guard script |
| `Makefile` target `check-changelogs` | 09-01 | new target |
| CI step `Check per-crate changelogs` | 09-01 | new `ci.yml` step |
| `SECURITY-EXCEPTIONS.md` | 09-02 | new root file |
| `.planning/decisions/0024-rustsec-exception-governance.md` | 09-02 | new ADR |
| `.planning/decisions/0027-dockerfile-chef-planner-stage.md` | 09-03 | new ADR |
| `.crate-names.txt` | 09-04 | new allow-list file |
| `scripts/check-crate-names.sh` | 09-04 | new guard script |
| `Makefile` target `check-crate-names` | 09-04 | new target |
| CI step `Check crates.io package names` | 09-04 | new `ci.yml` step |
| `.planning/decisions/0026-crate-name-collision-guard.md` | 09-04 | new ADR |
| `LICENSE-MIT`, `LICENSE-APACHE` | 09-05 | new files (D-11 branch only) |
| `.planning/decisions/0025-licence-posture.md` | 09-05 | new ADR |
| `scripts/check-advisory-register.sh` | 09-06 | new guard script |
| `Makefile` targets `check-advisory-register`, `check-gates` | 09-06 | new targets |
| CI step `Check advisory exception register` | 09-06 | new `ci.yml` step |
| `.planning/phases/09-release-security-gate-integrity/09-COVERAGE.md` | 09-07 | new declaration file |

<tasks>

<task type="auto">
  <name>Task 1: Author the allow-list and the bidirectional name guard</name>
  <precondition>`scripts/check-changelogs.sh` exists and exits 0 — plan 09-01's tracer proved the guard idiom this script copies.</precondition>
  <read_first>
    - `scripts/check-changelogs.sh` (written by plan 09-01) — the proven shape to copy: shebang, `set -euo pipefail`, python3 presence-check, `python3 - <<'PY'` heredoc, exactly one `exit 0` branch
    - `scripts/check-api-surface.sh` lines 15-19 — the missing-input guard shape, for the missing-allow-list case
    - `Cargo.toml` lines 30-50 — the root `[package] name` is `paladin-ai` at `:33`; note that `[[bin]]`, `[[test]]` and `[[bench]]` tables in the same file also carry `name` keys, so the guard must read `[package].name` specifically and never grep for a name line
    - `crates/paladin-core/Cargo.toml` — `[package] name` is `paladin-ai-core` at `:2` while `[lib] name` is `paladin_core` at `:15`; the guard reads the former only
    - `crates/doc-examples/Cargo.toml` lines 2-6 — `paladin-doc-examples` with `publish = false` at `:5`
    - `.planning/phases/09-release-security-gate-integrity/09-RESEARCH.md` → §5 (the eleven-name table, the `publish = false` twelfth, and the both-directions argument)
    - `.planning/phases/09-release-security-gate-integrity/09-CONTEXT.md` → D-13
  </read_first>
  <files>.crate-names.txt, scripts/check-crate-names.sh</files>
  <action>
(a) Create `.crate-names.txt` at the repository root: one crates.io package name per line, no other
syntax, with a leading comment block explaining what the file is, that a line is added only when a
new crate's name has been confirmed available on crates.io by a human, and that the guard reads it.
Populate it with exactly the eleven publishable package names verified in the tree — `paladin-ai`
(the root package), `paladin-ai-core`, `paladin-ports`, `paladin-battalion`, `paladin-herald`,
`paladin-llm`, `paladin-memory`, `paladin-storage`, `paladin-notifications`, `paladin-content`,
`paladin-web`. Do not include `paladin-doc-examples`; it declares itself unpublishable and the guard
excludes it by reading that declaration.

(b) Create `scripts/check-crate-names.sh`, copying plan 09-01's proven guard shape. It must:

- Fail with a non-zero exit and a legible message if `.crate-names.txt` is missing, following
  `check-api-surface.sh:15-19`'s missing-input shape.
- Fail with a non-zero exit if `python3` is unavailable, following the presence-check idiom.
- Read `.crate-names.txt` into a set, ignoring blank lines and lines whose first non-whitespace
  character starts a comment, and stripping surrounding whitespace from each name. Order is
  irrelevant — the file is a set, and reordering it must not change the verdict.
- Walk the root `Cargo.toml` and every `crates/*/Cargo.toml`, parse each with `tomllib`, and take
  the value of `[package].name`. Never grep for a name line: the root manifest carries many
  `[[bin]]`, `[[test]]` and `[[bench]]` tables with their own `name` keys, and one crate's `[lib]`
  name differs from its package name. Skip any manifest whose `[package].publish` is `false`. Skip a
  `crates/*/` directory that has no `Cargo.toml` without raising an error.
- Assert **set equality in both directions**. Unlisted tree names are the collision case the guard
  exists for and must fail with a message naming each one and telling the reader to confirm the name
  is available on crates.io before adding it to the allow-list. Allow-list entries with no
  corresponding publishable crate must also fail, with their own distinct message — a stale entry is
  a different integrity signal but the same "an assertion nobody enforces" pattern, and checking one
  direction only would let the guard pass vacuously against exactly the failure it exists to catch.
- Comparison is exact string equality on the raw name; do not case-fold, do not normalise hyphens
  and underscores, do not substring-match. A name differing only in case or in separator is a
  different crates.io package and must fail.
- Fail if zero publishable crates were discovered, with its own message, so a broken glob cannot
  read as success.
- Close with the two-branch conditional: exactly one branch exits 0, neither falls through.

(c) Demonstrate the guard failing before considering the task done. Exercise, restoring the tree
after each: an unlisted tree name (temporarily rename the root package, or add a scratch crate
directory with a manifest declaring an unlisted name); a stale allow-list entry (append a name for a
crate that does not exist); an emptied `.crate-names.txt`; a removed `.crate-names.txt`; a
case-only-different allow-list entry; and a reordered `.crate-names.txt`, which must still exit 0.
Record every invocation and its exact exit code verbatim in the SUMMARY, per D-19.
  </action>
  <verify>
    <automated>bash -c 'set -e; chmod +x scripts/check-crate-names.sh; ./scripts/check-crate-names.sh; cp .crate-names.txt /tmp/cn.bak; echo "paladin-nonexistent" >> .crate-names.txt; set +e; ./scripts/check-crate-names.sh > /tmp/cn.out 2>&1; echo "STALE_EXIT=$?"; set -e; cp /tmp/cn.bak .crate-names.txt; grep -q "paladin-nonexistent" /tmp/cn.out && echo "NAMED=yes"; sort -r /tmp/cn.bak > .crate-names.txt; ./scripts/check-crate-names.sh && echo "REORDER_OK"; cp /tmp/cn.bak .crate-names.txt; ./scripts/check-crate-names.sh'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -vc '^\s*#' .crate-names.txt` returns `11` after blank lines are excluded, and the eleven names match the tree exactly.
    - `./scripts/check-crate-names.sh` exits 0 against the unmodified tree.
    - Appending a name for a nonexistent crate makes the guard exit `1` and name that entry; the transcript is recorded in the SUMMARY.
    - Emptying `.crate-names.txt` makes the guard exit non-zero and name every one of the eleven tree crates; removing the file makes it exit non-zero with the missing-input message.
    - Replacing an allow-list entry with a case-only variant makes the guard exit `1` — the comparison is exact.
    - `sort -r` on the allow-list leaves the guard exiting 0 — the verdict is order-insensitive.
    - `python3 -c "import tomllib;d=tomllib.load(open('crates/doc-examples/Cargo.toml','rb'));assert d['package']['publish'] is False;print('exempt-by-field')"` exits 0, and the guard's exclusion is proven to follow that field by temporarily flipping it and observing exit `1`, then restoring.
    - `bash -n scripts/check-crate-names.sh` exits 0 and `grep -c 'exit 0' scripts/check-crate-names.sh` returns `1`.
    - `git status --porcelain` shows no leftover scratch crate directory or modified manifest after the demonstrations.
  </acceptance_criteria>
  <done>The allow-list holds the eleven owned names, the guard enforces set equality both ways with exact comparison, and it has been observed failing six different ways and passing under reordering.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the name guard into make and CI</name>
  <read_first>
    - `Makefile` lines 149-160 — the `check-doc-examples` / `check-doc-config` shape plus the `check-changelogs` target plan 09-01 added
    - `.github/workflows/ci.yml` — the `cargo-deny:` job, including the `Make scripts executable` step and the `Check per-crate changelogs` step plan 09-01 inserted; re-derive all line numbers by reading the file, they have moved
    - `.github/rulesets/protect-main-branch.json` lines 31-46 — the three required status-check contexts, one of which is this job's display name
    - `.planning/phases/09-release-security-gate-integrity/09-01-SUMMARY.md` — where plan 09-01 recorded the exact step placement it used
  </read_first>
  <files>Makefile, .github/workflows/ci.yml</files>
  <action>
Add a `Makefile` target named `check-crate-names`, matching the shape already established: a
`.PHONY:` line, the target with a one-line `## ` help comment describing it as the crates.io
package-name collision guard, and a body of `@./scripts/check-crate-names.sh`. Place it immediately
after the `check-changelogs` target so the three guards read as a group.

Add one step to `.github/workflows/ci.yml`'s `cargo-deny:` job — the job whose display name is
`License & Dependency Policy`, a required status-check context. Append it immediately after the
`Check per-crate changelogs` step that plan 09-01 added, named `Check crates.io package names`,
running `./scripts/check-crate-names.sh`. Do **not** add a second `Make scripts executable` step;
plan 09-01's already covers `scripts/*.sh`. Re-derive every line number by reading the file first —
plan 09-01 inserted steps into this job and the numbers in any earlier document are stale.

Change nothing else in either file. In particular do not touch the `dtolnay/rust-toolchain` or
`actions/cache` references in this job, and do not modify the existing `audit`, `deny` or `security`
Makefile targets — Phase 15 / PIPE-04 owns the action sweep, and plan 09-06 owns the aggregate
target.
  </action>
  <verify>
    <automated>bash -c 'make -n check-crate-names && grep -c "check-crate-names.sh" .github/workflows/ci.yml && python3 -c "
import sys,re
s=open(\".github/workflows/ci.yml\",encoding=\"utf-8\").read()
i=s.index(\"  cargo-deny:\"); j=s.index(\"\n  \", s.index(\"cargo deny check\"))
blk=s[i:]
nxt=re.search(r\"\n  [a-z][a-z0-9-]*:\n\", blk[10:])
blk=blk[:nxt.start()+10] if nxt else blk
assert \"check-crate-names.sh\" in blk, \"guard step not inside cargo-deny job\"
assert blk.count(\"Make scripts executable\")==1, blk.count(\"Make scripts executable\")
print(\"wired OK\")
"'</automated>
  </verify>
  <acceptance_criteria>
    - `make -n check-crate-names` prints the script invocation and exits 0.
    - `grep -c 'check-crate-names.sh' .github/workflows/ci.yml` returns `1`.
    - The verify command's python assertion confirms the step is inside the `cargo-deny` job block and that the job contains exactly one `Make scripts executable` step.
    - `git diff -- .github/workflows/ci.yml | grep -c 'actions-rs\|dtolnay\|actions/cache'` returns `0` — no deprecated-action reference was opportunistically bumped.
    - `git diff -- Makefile | grep -cE '^\-'` returns `0` — the Makefile change is additive only.
    - `./scripts/check-crate-names.sh && ./scripts/check-changelogs.sh` exits 0 — both wired guards still pass together.
  </acceptance_criteria>
  <done>`make check-crate-names` runs the guard locally and the guard runs on every pull request inside a required status-check context.</done>
</task>

<task type="auto">
  <name>Task 3: Write ADR-0026 — crates.io name-collision guard</name>
  <read_first>
    - `.planning/decisions/0023-cli-dependency-isolation.md` in full — the seven-heading, no-frontmatter shape and its verbatim-transcript evidence style
    - `.planning/REQUIREMENTS.md` lines 1166-1179 — SEC-03 in full, including its two permitted answers
    - `.project/Milestone_7-Production-Hardening/Epic_4/deferred-paladin-ports-publish-verification.md` — Task 5.5 Resolved, and the single residue about keeping CI guardrails that detect collisions early
    - `.github/workflows/ci.yml` — the `publish-dry-run` job (main-branch only) and its workspace dry run; re-derive its line numbers, they moved when plan 09-01 and task 2 above inserted steps
    - `.github/workflows/release.yml` lines 353-427 — the per-crate dry run inside the release job
    - `.planning/phases/09-release-security-gate-integrity/09-CONTEXT.md` → D-13, D-18, D-19
  </read_first>
  <files>.planning/decisions/0026-crate-name-collision-guard.md</files>
  <action>
Write `.planning/decisions/0026-crate-name-collision-guard.md` in ADR-0022/0023's exact shape: no
YAML frontmatter, seven headings in order — `## Status` (`Accepted`, then a bold `**Date:**` line),
`## Context`, `## Decision`, `## Considered Options`, `## Code Locations`, `## Code Conformance`,
`## Downstream Consumers`.

`## Context` must record: the residue the publish-verification document left behind; that collisions
already cost this project two package renames and a full NO-GO cycle, which is why every crate's
package name and lib name diverge today; and where the earliest existing guard actually sits —
a main-branch-only workspace dry run in `ci.yml`, plus per-crate dry runs inside `release.yml`'s
publish job, both late and neither name-specific. Cite the current line numbers, re-derived after
this plan's own `ci.yml` edit.

`## Decision` must state that the project adopts an offline allow-list guard running on every pull
request, that `.crate-names.txt` is the authoritative owned-name list, and that adding a crate is a
deliberate one-line addition at which point the release runbook requires a human availability check.

`## Considered Options` must tag each `(accepted)` or `(rejected)`: a live crates.io sparse-index
availability query (rejected — crates.io returns HTTP 403 in this environment, so the check could be
written but never demonstrated, which is the Phase 8 D-03 failure mode, and it would be flaky in CI
besides); pure acceptance of dry-run reliance, which SEC-03 permits as its second answer (rejected —
roughly twenty lines of guard buys detection on every pull request instead of at main-branch or
release time); a one-directional guard checking only that allow-listed names still exist (rejected —
passes vacuously against a new colliding name); the offline bidirectional allow-list guard
(accepted).

`## Code Locations` must cite `.crate-names.txt`, `scripts/check-crate-names.sh`, the new `Makefile`
target, the new `ci.yml` step and the job it sits in, and the two existing dry-run sites. Following
ADR-0023's precedent, embed the verbatim negative-path transcript from task 1 as evidence that this
guard can fail — this ADR is where the D-19 bar is discharged for SEC-03.

`## Code Conformance` opens with the literal line `must change` and names this plan's tasks 1 and 2.

`## Downstream Consumers` must name the release runbook (which gains the manual availability check
for a genuinely novel name) and Phase 10 / HARD-01's ledger row for the publish-verification
closure.

State the accepted residual cost explicitly and in terms, in both `## Decision` and
`## Downstream Consumers`: the eleven existing names are already owned so their collision risk is
zero, and a genuinely novel name is still checked by a human against crates.io rather than by CI.
That is the cost this design accepts, and it must be recorded rather than left implicit.

Do not edit `PROMOTION.md`; the counter advances once, in the close-out.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/decisions/0026-crate-name-collision-guard.md; test -f "$f" && head -1 "$f" | grep -q "^# ADR-0026" && ! head -1 "$f" | grep -q "^---" && diff <(grep "^## " "$f") <(printf "## Status\n## Context\n## Decision\n## Considered Options\n## Code Locations\n## Code Conformance\n## Downstream Consumers\n") && grep -qx "must change" "$f" && grep -c "(rejected)" "$f"'</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 .planning/decisions/0026-crate-name-collision-guard.md` starts with `# ADR-0026`.
    - `diff <(grep '^## ' .planning/decisions/0026-crate-name-collision-guard.md) <(printf '## Status\n## Context\n## Decision\n## Considered Options\n## Code Locations\n## Code Conformance\n## Downstream Consumers\n')` produces no output.
    - `sed -n '1p' .planning/decisions/0026-crate-name-collision-guard.md | grep -c '^---'` returns `0`.
    - `grep -c '(rejected)' .planning/decisions/0026-crate-name-collision-guard.md` returns at least `3`.
    - `grep -ci 'residual cost' .planning/decisions/0026-crate-name-collision-guard.md` returns at least `1`.
    - `grep -c '403' .planning/decisions/0026-crate-name-collision-guard.md` returns at least `1` — the reason the network option was rejected is stated concretely.
    - The file contains the verbatim negative-path transcript captured in task 1.
    - `git status --porcelain .planning/decisions/PROMOTION.md` is empty.
  </acceptance_criteria>
  <done>ADR-0026 records the offline-guard decision, the rejected live-query and dry-run-only alternatives with concrete reasons, the verbatim proof the guard can fail, and the residual cost this design accepts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor commit → crates.io publish | A new crate name added to the workspace becomes a publish attempt against a public registry; the name is outside this project's control until it is claimed. |
| `.crate-names.txt` → CI verdict | The allow-list is the guard's only notion of what this project owns; anything that lets it be derived from the tree it checks collapses the control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-14 | Spoofing | crates.io package name | high | mitigate | An unowned name reaching a publish attempt is how Epic 4 lost two renames and a NO-GO cycle. The forward direction of the set-equality check fails any tree name not on the committed allow-list, on every pull request rather than at main-branch dry-run time. |
| T-09-15 | Repudiation | a one-directional guard | high | mitigate | Checking only that allow-listed names still exist passes vacuously against a newly added colliding name — the guard would report green while never having examined the risk. Both directions are asserted, and task 1 demonstrates each failing separately. |
| T-09-16 | Tampering | `.crate-names.txt` provenance | medium | mitigate | An allow-list regenerated from the tree at check time would rubber-stamp whatever was just added. The list is committed and hand-edited; the prohibition block forbids auto-generation, and the guard reads the file rather than deriving it. |
| T-09-17 | Spoofing | near-miss name matching | medium | mitigate | Case-folding or hyphen-underscore normalisation would let `paladin_web` satisfy an entry for `paladin-web`, which are different crates.io packages. Comparison is exact byte equality; the acceptance criteria prove a case-only variant fails. |
| T-09-18 | Elevation of privilege | guard placed in a non-required CI job | medium | mitigate | An advisory-only guard can be merged past. The step goes into the job whose display name is one of the three contexts required by `.github/rulesets/protect-main-branch.json`. |
| T-09-SC | Tampering | package-manager installs | high | accept | This plan installs nothing. The guard is `bash` plus stdlib `tomllib`; `git diff -- Cargo.toml Cargo.lock crates/*/Cargo.toml` is empty for this plan once the demonstrations are reverted, so the Package Legitimacy Gate does not fire. |
</threat_model>

<verification>
- `./scripts/check-crate-names.sh` exits 0; the six recorded negative-path transcripts show non-zero exits with the offending names printed.
- `grep -vc '^\s*#' .crate-names.txt` reflects exactly the eleven publishable names in the tree.
- `make -n check-crate-names` resolves; `grep -c 'check-crate-names.sh' .github/workflows/ci.yml` → `1`, inside the `cargo-deny` job.
- `git diff -- .github/workflows/ci.yml | grep -c 'actions-rs\|dtolnay\|actions/cache'` → `0`.
- ADR-0026 matches 0023's seven-heading shape and carries the verbatim failing transcript.
- `git diff --stat 49ad74c..HEAD -- '*.rs' | wc -l` → `0`.
</verification>

<success_criteria>
A crates.io name collision is caught on the pull request that introduces it rather than at dry-run
or release time, the guard cannot pass vacuously in either direction, and the one case it
deliberately does not cover — a genuinely novel name's availability — is recorded as an accepted
cost with a named human step rather than left as an implicit gap.
</success_criteria>

<output>
Create `.planning/phases/09-release-security-gate-integrity/09-04-SUMMARY.md` when done, including
the verbatim negative-path transcripts.
</output>
