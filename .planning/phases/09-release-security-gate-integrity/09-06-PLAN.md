---
phase: 09-release-security-gate-integrity
plan: 06
type: execute
wave: 3
depends_on: ["09-02", "09-04"]
files_modified:
  - deny.toml
  - .cargo/audit.toml
  - scripts/check-advisory-register.sh
  - Makefile
  - .github/workflows/ci.yml
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "`make audit` and the one surviving CI audit job cannot pass different advisory sets, because there is one configuration rather than three"
    - "Exactly one `cargo audit` invocation remains in CI and it passes no advisory identifiers on its command line"
    - "The four suppressions whose crates Phase 8's clap migration removed from the dependency graph are gone from `deny.toml`, not backfilled with governance"
    - "The advisory-register guard exits non-zero when a register row's identifier appears in neither configuration file, and when a row's class disagrees with the file set that identifier appears in"
    - "With an empty ignore array in both configuration files and zero register rows the guard exits 0; with an empty array in one file only, it exits non-zero"
    - "Advisory-identifier comparison is exact byte equality over ASCII; an identifier differing only in letter case fails the guard rather than matching"
    - "Reordering the entries of either ignore array without changing the set leaves the guard's exit code at 0 — it is a set comparison, not a sequence comparison"
    - "Running the guard twice in succession produces identical output and the same exit code, and modifies no file — it is read-only"
    - "No suppression can outlive the dependency it suppresses: every register row's affected crate is asserted present in `Cargo.lock`"
    - "Both configuration files point the reader at the register rather than duplicating its governance prose"
    - statement: "Two concurrent or interrupted invocations of the advisory-register guard cannot corrupt any repository file and reach the same verdict on the same inputs, because the script only reads and writes nothing outside its own stdout."
      verification: backstop
    - statement: "`cargo audit` and `cargo deny check` pass against the reconciled configuration. Neither tool is installed and neither can be installed here, because crates.io returns HTTP 403 in this environment; this is verifiable only on a CI runner and must be recorded as such rather than inferred."
      verification: backstop
  artifacts:
    - path: "scripts/check-advisory-register.sh"
      provides: "The three-clause sync, coverage and liveness guard (D-02)"
      contains: "exit 1"
    - path: "deny.toml"
      provides: "Ten live suppressions in two labelled classes, with a pointer to the register"
      contains: "SECURITY-EXCEPTIONS"
    - path: ".cargo/audit.toml"
      provides: "Five vulnerability-class suppressions with a pointer to the register"
      contains: "SECURITY-EXCEPTIONS"
    - path: "Makefile"
      provides: "check-advisory-register and check-gates targets"
      contains: "check-advisory-register"
    - path: ".github/workflows/ci.yml"
      provides: "One audit job and a guard step, in a required-context job"
      contains: "check-advisory-register.sh"
  key_links:
    - from: "scripts/check-advisory-register.sh"
      to: "SECURITY-EXCEPTIONS.md"
      via: "extracts the delimited fenced TOML block and parses it with tomllib as the source of class information"
      pattern: "MACHINE-READABLE REGISTER"
    - from: "scripts/check-advisory-register.sh"
      to: "Cargo.lock"
      via: "anchored per-crate grep proving each suppressed crate is still in the graph"
      pattern: "Cargo\\.lock"
    - from: "deny.toml"
      to: ".cargo/audit.toml"
      via: "the vulnerability class is mirrored exactly; the guard, not the comment header, enforces it"
      pattern: "audit\\.toml"
  prohibitions:
    - "The guard must not recover class information by scraping either configuration file's inline comments. The register is the single source of class information; a script that depends on the exact wording of a human-readable comment breaks silently the next time someone rewords it, and the breakage looks like a passing gate."
    - "Deleting the duplicate audit job must not reduce what CI actually scans. The surviving job reads five advisories from the authoritative file while the deleted one hardcoded two; any edit that leaves fewer advisories scanned than before, or leaves no audit job at all, is prohibited even though it would still satisfy a naive one-invocation count."
---

<assumption_delta_decision>
The advisory delta detector fired with two `pluralization` signals, on the words "second" and
"alongside" in the ROADMAP criterion sentence "…`ci.yml:406` (2 inline, alongside a second
bare-`cargo audit` job at `ci.yml:77`)".

**Noun now primary:** `.cargo/audit.toml` as the single suppression surface, and one `cargo audit`
invocation in CI.

**Decision: `no-change`.** The detector fired on prose that *describes the defect this plan
deletes*, not on a genuine generalisation. There was never an intended second audit job — Milestone
10 Epic 2 added the compliant one without removing its predecessor, and Epic 4's non-goals then
froze the area. The correct move is to restore the monopoly, which is exactly what task 3 does; a
`promote` or `add-alongside` disposition would enshrine an accident.

**Suggested (not required) invariant:** the one-invocation assertion in task 3's acceptance criteria
already encodes the generalised intent — CI holds exactly one `cargo audit` invocation and it passes
no advisory identifiers inline. Keeping that assertion in CI is what stops a third surface appearing
the next time someone needs an exception in a hurry.
</assumption_delta_decision>

<objective>
Reconcile the four divergent advisory surfaces into one configuration, land the guard that keeps
them reconciled, and delete the duplicate CI audit job that is the reason two jobs both named
"Security Audit" could reach different verdicts on the same lockfile.

Purpose: ROADMAP criterion 3 cannot be satisfied without the deletion, and criterion 1 cannot be
satisfied by a comment. This repository has now shipped three invariants asserted in comments and
enforced by nothing — the sync header in `deny.toml`, a guard script whose branches both exited
zero, and a per-crate changelog criterion that missed a crate. Every clause this plan closes ends in
a script or a CI step.
Output: ten live suppressions in `deny.toml`, pointer comments in both configuration files,
`scripts/check-advisory-register.sh`, one audit job, and two new `make` targets.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-release-security-gate-integrity/09-CONTEXT.md
@.planning/phases/09-release-security-gate-integrity/09-PATTERNS.md
@.planning/phases/09-release-security-gate-integrity/09-02-SUMMARY.md
</context>

## Artifacts this phase produces

Newly-created symbols and files across all seven Phase 9 plans. Nothing on this list exists in the
tree today, so none of it can be a drift regression against a prior phase.

| Artifact | Plan | Kind |
|---|---|---|
| `crates/paladin-herald/CHANGELOG.md` | 09-01 | new file |
| `scripts/check-changelogs.sh` | 09-01 | new guard script |
| `Makefile` target `check-changelogs` | 09-01 | new target |
| CI step `Check per-crate changelogs` | 09-01 | new `ci.yml` step |
| `SECURITY-EXCEPTIONS.md` | 09-02 | new root file |
| `.planning/decisions/0024-rustsec-exception-governance.md` | 09-02 | new ADR |
| `.planning/decisions/0027-dockerfile-chef-planner-stage.md` | 09-03 | new ADR |
| `.crate-names.txt` | 09-04 | new allow-list file |
| `scripts/check-crate-names.sh` | 09-04 | new guard script |
| `Makefile` target `check-crate-names` | 09-04 | new target |
| CI step `Check crates.io package names` | 09-04 | new `ci.yml` step |
| `.planning/decisions/0026-crate-name-collision-guard.md` | 09-04 | new ADR |
| `LICENSE-MIT`, `LICENSE-APACHE` | 09-05 | new files (option-a branch only) |
| `.planning/decisions/0025-licence-posture.md` | 09-05 | new ADR |
| `scripts/check-advisory-register.sh` | 09-06 | new guard script |
| `Makefile` targets `check-advisory-register`, `check-gates` | 09-06 | new targets |
| CI step `Check advisory exception register` | 09-06 | new `ci.yml` step |
| `.planning/phases/09-release-security-gate-integrity/09-COVERAGE.md` | 09-07 | new declaration file |

<!-- planner-discipline-allow: RUSTSEC-2022-0104 -->
<!-- planner-discipline-allow: RUSTSEC-2021-0139 -->
<!-- planner-discipline-allow: RUSTSEC-2024-0370 -->
<!-- planner-discipline-allow: RUSTSEC-2024-0375 -->
<!-- planner-discipline-allow: cargo audit --ignore -->
<!-- planner-discipline-allow: ^  security:$ -->

<tasks>

<task type="auto">
  <name>Task 1: Reconcile deny.toml and .cargo/audit.toml against the register</name>
  <precondition>`SECURITY-EXCEPTIONS.md` exists at the repository root and its delimited fenced block parses into ten fully-populated exception rows — plan 09-02's deliverable. Verify by running plan 09-02's register-parse check before touching either configuration file.</precondition>
  <read_first>
    - `deny.toml` lines 108-150 in full — the `[advisories]` table, the `ignore` array's fourteen entries in three labelled comment classes, and the surrounding prose
    - `deny.toml` lines 1-20 — the header, including the sync assertion and `[graph] all-features = true` at `:15`, both of which stay
    - `.cargo/audit.toml` in full — its five-entry array and the per-advisory comment block above it
    - `SECURITY-EXCEPTIONS.md` (written by plan 09-02) — the ten rows and their class assignments
    - `.planning/REQUIREMENTS.md` lines 1060-1081 — the run-5 correction banner, which withdraws run 4's out-of-sync finding; read it before touching `deny.toml`
    - `.planning/phases/09-release-security-gate-integrity/09-CONTEXT.md` → D-01, D-03, D-04, D-05
  </read_first>
  <files>deny.toml, .cargo/audit.toml</files>
  <action>
Bring both configuration files into exact agreement with the register that now governs them.

(a) In `deny.toml`'s `[advisories] ignore` array, delete the four entries whose crates Phase 8's
clap v4 migration removed from the graph — the entries for `structopt`, `ansi_term`, `atty` and
`proc-macro-error`. Re-verify liveness first by running the anchored per-crate count against
`Cargo.lock` for all ten crate names named in the register plus those four, and paste the transcript
into the SUMMARY; do not delete on the strength of a prior document. Delete each entry's line
together with any comment that exists solely to justify it. Leave the other ten entries exactly as
they are.

(b) Keep the two classes separated and labelled, per D-03. Vulnerability advisories and
unmaintained notices are different objects with different governance — `cargo audit` does not fail
on unmaintained crates and `cargo deny` does, and ten of the surviving entries are explicitly
sanctioned by their originating requirement. Do not merge the class comment headers, do not
re-order the array across class boundaries, and do not reopen the sync question the run-5 banner
settled.

(c) Rewrite the class comment headers in `deny.toml` so they stop duplicating governance prose and
instead point at `SECURITY-EXCEPTIONS.md` as the authoritative record of owner, review date,
affected scope and compensating control for every identifier in the array. Keep one short
human-readable line per entry naming the crate and the reason it is here — a reader scanning the
TOML should still learn what an entry is for — but the full governance lives in one place now.
Update the file's top-of-file header to name the register and the guard.

(d) In `.cargo/audit.toml`, change no entries. All five are live and all five stay. Add a pointer
line to the comment header naming `SECURITY-EXCEPTIONS.md` as the governance record and
`scripts/check-advisory-register.sh` as the enforcement, in the same style as the existing header
prose.

(e) Leave `[graph] all-features = true`, the licence allow-list, the bans section and the sources
section untouched. This task's diff must be confined to the advisories block and the two files'
comment headers.
  </action>
  <verify>
    <automated>python3 -c "
import tomllib
d=tomllib.load(open('deny.toml','rb'))['advisories']['ignore']
a=tomllib.load(open('.cargo/audit.toml','rb'))['advisories']['ignore']
ds,as_=set(d),set(a)
assert len(d)==len(ds)==10, (len(d),len(ds))
assert len(a)==len(as_)==5, (len(a),len(as_))
assert as_ <= ds, as_-ds
dead={'RUSTSEC-2022-0104','RUSTSEC-2021-0139','RUSTSEC-2024-0375','RUSTSEC-2024-0370'}
assert not (dead & ds), dead & ds
print('deny=%d audit=%d mirrored=%s' % (len(ds), len(as_), as_ <= ds))
"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 and prints `deny=10 audit=5 mirrored=True`.
    - The four removed identifiers are absent from the parsed `deny.toml` ignore array, asserted by the parse above rather than by a text grep, so a commented-out leftover cannot pass.
    - The verbatim `Cargo.lock` liveness transcript for all fourteen crate names is recorded in the SUMMARY, showing zero for the four removed and at least one for each of the ten retained.
    - `python3 -c "import tomllib;d=tomllib.load(open('deny.toml','rb'));assert d['graph']['all-features'] is True;print('graph intact')"` exits 0.
    - `git diff -- deny.toml | grep -cE '^[-+]\s*\"?(MIT|Apache|BSD|ISC|Zlib|Unicode|0BSD|CC0|CDLA)'` returns `0` — the licence allow-list was not touched.
    - `grep -c 'SECURITY-EXCEPTIONS' deny.toml` returns at least `1` and `grep -c 'SECURITY-EXCEPTIONS' .cargo/audit.toml` returns at least `1`.
    - `git diff --numstat -- .cargo/audit.toml` shows no deleted line containing an advisory identifier — that file's entries are unchanged.
    - `git diff --stat -- '*.rs' | wc -l` returns `0`.
  </acceptance_criteria>
  <done>`deny.toml` holds ten live suppressions in two labelled classes, `.cargo/audit.toml` holds its unchanged five, both point at the register, and the liveness evidence for every identifier is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Write the advisory-register guard and prove every clause can fail</name>
  <read_first>
    - `scripts/check-changelogs.sh` and `scripts/check-crate-names.sh` — the guard shape proven by plans 09-01 and 09-04: shebang, `set -euo pipefail`, python3 presence-check, heredoc, exactly one success exit
    - `scripts/check-api-surface.sh` lines 15-19 — the missing-input shape, for the missing-register case
    - `SECURITY-EXCEPTIONS.md` — the delimited fenced block and the eleven row keys
    - `deny.toml` and `.cargo/audit.toml` (post-task-1)
    - `Cargo.lock` lines 1-6 — the generated-file header and the format version, which is why an anchored grep is safe here and a hand-rolled parser is unnecessary
    - `.planning/phases/09-release-security-gate-integrity/09-RESEARCH.md` → §2 in full (the three-assertion design, the parsing strategy, and the negative-path instruction) and "Pitfall 3"
    - `.planning/phases/09-release-security-gate-integrity/09-CONTEXT.md` → D-02, D-19
  </read_first>
  <files>scripts/check-advisory-register.sh</files>
  <action>
Create `scripts/check-advisory-register.sh` following the guard shape plans 09-01 and 09-04 proved:
`#!/usr/bin/env bash`, `set -euo pipefail`, a `command -v python3` presence-check that exits non-zero
with a message naming python3, then a `python3 - <<'PY'` heredoc, closing with a two-branch
conditional in which exactly one branch exits 0 and neither falls through.

Missing-input handling comes first: if `SECURITY-EXCEPTIONS.md`, `deny.toml`, `.cargo/audit.toml` or
`Cargo.lock` is absent, exit non-zero naming the missing file. Never default a missing input to an
empty set — an absent file that silently becomes an empty comparison is a gate that certifies
nothing while reporting success.

Extract the register's payload by locating the explicit BEGIN and END marker comments and taking the
fenced TOML block between them; parse it with `tomllib`. Fail if the markers are absent, if they
appear more than once, or if no fenced block sits between them. Parse `deny.toml` and
`.cargo/audit.toml` with `tomllib` too — never with a regex over comments, and never by scraping
either file's inline comment text to recover class information. The register is the single source of
class information; the TOML comments stay human-readable prose and are never parsed.

Then assert three things, reporting every failure found rather than stopping at the first:

1. **Class-set equality, exactly.** Partition register rows by their class field into a
   vulnerability set and an unmaintained set. Assert the vulnerability set equals the
   `.cargo/audit.toml` ignore set exactly — equality, not containment, so an identifier present in
   one and not the other fails loudly in either direction. Assert the union of both register sets
   equals the `deny.toml` ignore set exactly. Compare as sets so array order is irrelevant, and
   compare raw strings so a case-only difference fails rather than matching.
2. **Register coverage, both directions.** For every identifier appearing in either configuration
   file, assert a register row exists whose governance fields are all present and non-empty. For
   every register row, assert its identifier appears in at least one configuration file — a row with
   no live suppression is stale bookkeeping and must fail rather than pass quietly.
3. **Crate liveness.** For each register row, take the affected-crate field, and assert at least one
   named crate is present in `Cargo.lock` using the anchored per-crate form
   `grep -c '^name = "<crate>"$'`. Some rows name more than one candidate crate along a transitive
   path; treat the row as live if any candidate is present, and fail naming the exact row if none
   is. This is the check nothing previously performed, and it is what would have caught the four
   orphaned suppressions automatically.

Also fail, with its own distinct message, if the register parses to zero rows while either
configuration file holds a non-empty ignore array — the one-sided-empty case. Zero rows and two
empty arrays is a legitimate pass and must exit 0.

The script writes nothing and creates no temporary file outside `mktemp`-managed scratch it removes;
running it twice must produce identical output and the same exit code.

Then demonstrate each clause failing, restoring the tree after every fixture:

- Add a fabricated identifier to `deny.toml`'s array with no register row — expect non-zero, naming
  the uncovered identifier.
- Remove one identifier from `.cargo/audit.toml` while leaving its register row and its `deny.toml`
  entry — expect non-zero on clause 1 in the audit direction.
- Add a register row for an identifier in neither configuration file — expect non-zero on clause 2's
  reverse direction.
- Blank one governance field on one register row — expect non-zero naming the row and the field.
- Point one register row's affected-crate field at a crate absent from `Cargo.lock` — expect
  non-zero on clause 3, naming the row.
- Change one identifier's letter case in `deny.toml` — expect non-zero, proving comparison is exact.
- Reorder both ignore arrays without changing their sets — expect exit 0, proving order-insensitivity.
- Run the guard twice with no change between runs and diff the two outputs — expect identical output
  and the same exit code, proving idempotency.
- Move `SECURITY-EXCEPTIONS.md` aside — expect non-zero with the missing-input message.

Record every invocation and its exact exit code verbatim in the SUMMARY under a "Negative-path
evidence" heading. A guard observed only passing has not been validated.
  </action>
  <verify>
    <automated>bash -c 'set -e; chmod +x scripts/check-advisory-register.sh; ./scripts/check-advisory-register.sh > /tmp/r1.out 2>&1; ./scripts/check-advisory-register.sh > /tmp/r2.out 2>&1; diff /tmp/r1.out /tmp/r2.out && echo "IDEMPOTENT"; cp deny.toml /tmp/deny.bak; python3 -c "
s=open(\"deny.toml\",encoding=\"utf-8\").read()
i=s.rindex(\"]\")
open(\"deny.toml\",\"w\",encoding=\"utf-8\").write(s[:i]+\"    \\\"RUSTSEC-9999-0000\\\",\n\"+s[i:])
"; set +e; ./scripts/check-advisory-register.sh > /tmp/r3.out 2>&1; echo "UNCOVERED_EXIT=$?"; set -e; cp /tmp/deny.bak deny.toml; grep -q "RUSTSEC-9999-0000" /tmp/r3.out && echo "NAMED=yes"; ./scripts/check-advisory-register.sh'</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/check-advisory-register.sh` exits 0 against the reconciled tree.
    - The verify command prints `IDEMPOTENT`, `UNCOVERED_EXIT=1` and `NAMED=yes`.
    - All nine demonstrations listed in the action are recorded in the SUMMARY with their exact commands and exit codes; eight produce non-zero and the reorder case produces zero.
    - `git status --porcelain deny.toml .cargo/audit.toml SECURITY-EXCEPTIONS.md Cargo.lock` is empty after the demonstrations — every fixture was reverted.
    - `bash -n scripts/check-advisory-register.sh` exits 0 and `grep -c 'exit 0' scripts/check-advisory-register.sh` returns `1`.
    - `grep -c 'unmaintained' scripts/check-advisory-register.sh` returns `0` when comment lines are excluded via `grep -v '^#' scripts/check-advisory-register.sh | grep -c 'unmaintained'` — class information comes from the register's class field, never from scraping a configuration file's comment wording. A literal class name appearing only as a register field value is acceptable; a `grep`/regex against `deny.toml`'s comments is not.
    - `git diff --stat -- '*.rs' | wc -l` returns `0`.
  </acceptance_criteria>
  <done>The guard asserts all three clauses, has been observed failing eight distinct ways and passing under reordering and repetition, and derives class information only from the register.</done>
</task>

<task type="auto">
  <name>Task 3: Collapse the two CI audit jobs to one and wire the guard</name>
  <read_first>
    - `.github/workflows/ci.yml` — the surviving audit job at roughly `:61-78` (bare invocation under a comment declaring the authoritative file), the duplicate at roughly `:466-482`, and the `cargo-deny:` job with the guard steps plans 09-01 and 09-04 added. Every one of these numbers has moved; re-derive all of them by reading the file.
    - `.github/rulesets/protect-main-branch.json` lines 31-46 — the three required status-check contexts, keyed by context string
    - `docs/src/appendix/branch-protection.md` around line 70 — the same three contexts documented in prose
    - `Makefile` lines 240-265 — the existing `audit`, `deny` and `security` targets and their comment style
    - `.planning/phases/09-release-security-gate-integrity/09-RESEARCH.md` → §6 (the blast-radius answer and the line-drift warning)
    - `.planning/phases/09-release-security-gate-integrity/09-CONTEXT.md` → D-07, D-19
  </read_first>
  <files>.github/workflows/ci.yml, Makefile</files>
  <action>
(a) Delete the duplicate audit job block from `.github/workflows/ci.yml`. It is the second of the two
jobs that both render under the display name `Security Audit`; its job id is `security`, it sits at
roughly `:466-482`, and its final `run:` step invokes the auditor with two advisory identifiers
passed inline rather than reading the authoritative configuration file. Delete the whole block
including its leading `# Security audit` comment line and the blank-line separator, roughly eighteen
lines. Re-derive the exact range by reading the file first — plans 09-01 and 09-04 inserted steps
above it.

The job that **survives** is the first one, whose id is `security-audit`, whose display name is also
`Security Audit`, and whose step runs the auditor bare so it reads all five advisories from the
authoritative file. Do not touch it. Deleting the duplicate is what makes ROADMAP criterion 3 true
and simultaneously closes the duplicate-display-name defect.

Before and after the deletion, assert the required-status-check position: the repository ruleset
requires the context string `Security Audit`, not a job id, and the surviving job posts exactly that
context — so the required check keeps resolving. Record both the pre- and post-edit counts of jobs
carrying that display name in the SUMMARY, along with the ruleset citation, so the risk is recorded
as assessed rather than assumed.

Note in the SUMMARY that this deletion shifts every line number below it upward by the deleted
count, so any later citation into this file must be re-derived and not carried forward.

Do not opportunistically modernise anything else in this file. Two deprecated action references sit
inside jobs this edit is adjacent to; Phase 15 / PIPE-04 owns that sweep and this plan leaves them
exactly as found.

(b) Append one step to the `cargo-deny:` job — display name `License & Dependency Policy`, one of
the three required contexts — immediately after the guard step plan 09-04 added. Name it
`Check advisory exception register` and run `./scripts/check-advisory-register.sh`. Do not add
another `Make scripts executable` step; plan 09-01's already covers the directory.

(c) Add two `Makefile` targets in the shape already established: `check-advisory-register`, a thin
wrapper over the new script, placed beside `check-crate-names`; and `check-gates`, an aggregate that
runs all three of this phase's guards in sequence so a developer has one command. Add `check-gates`
as a prerequisite-style target listing the three guard targets, matching how the existing `security`
target composes `audit` and `deny`. Do not modify the existing `audit`, `deny` or `security`
targets — those wrap tools that cannot run in this environment, and folding offline guards into them
would make a target that currently fails for a known reason start failing for two.
  </action>
  <verify>
    <automated>bash -c 'set -e; python3 -c "
import re
s=open(\".github/workflows/ci.yml\",encoding=\"utf-8\").read()
jobs=re.findall(r\"^  ([a-zA-Z0-9_-]+):\n    name: (.*)$\", s, re.M)
sec=[j for j,n in jobs if n.strip()==\"Security Audit\"]
assert len(sec)==1, sec
assert sec[0]==\"security-audit\", sec
runs=re.findall(r\"run: cargo audit.*\", s)
assert len(runs)==1, runs
assert \"--ignore\" not in runs[0], runs
assert \"check-advisory-register.sh\" in s
print(\"jobs_named_security_audit=1 audit_invocations=1 inline_ignores=0\")
"; make -n check-advisory-register; make -n check-gates'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 and prints `jobs_named_security_audit=1 audit_invocations=1 inline_ignores=0`.
    - Exactly one job in `ci.yml` carries the display name `Security Audit`, and its job id is `security-audit`.
    - `grep -c 'run: cargo audit' .github/workflows/ci.yml` returns `1`, and that line passes no advisory identifiers.
    - `git diff --numstat -- .github/workflows/ci.yml` shows at least 17 deleted lines from the job removal.
    - The pre-edit and post-edit counts of jobs bearing the required display name are recorded in the SUMMARY together with the `.github/rulesets/protect-main-branch.json` line citation, so the required-check risk is recorded as assessed.
    - `git diff -- .github/workflows/ci.yml | grep -c 'actions-rs\|dtolnay\|actions/cache\|codecov'` returns `0` — no deprecated action reference was bumped.
    - `make -n check-advisory-register` and `make -n check-gates` both resolve and exit 0; `make -n check-gates` names all three guard targets.
    - `git diff -- Makefile | grep -cE '^\-[^-]'` returns `0` — the Makefile change is additive; `audit`, `deny` and `security` are unmodified.
    - `./scripts/check-advisory-register.sh && ./scripts/check-crate-names.sh && ./scripts/check-changelogs.sh` exits 0 — all three guards pass together.
    - `git diff --stat -- '*.rs' | wc -l` returns `0`.
  </acceptance_criteria>
  <done>CI holds one audit job reading one configuration, the three guards run in a required-context job and from one make target, and the required-status-check risk is recorded as assessed against the committed ruleset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor commit → `deny.toml` / `.cargo/audit.toml` | Anyone with commit rights can add an advisory identifier to either file; until this plan lands, nothing requires an accountable record or checks the two files agree. |
| upstream dependency graph → `Cargo.lock` | Dependencies enter and leave the graph without any suppression being revisited; a suppression outliving its dependency keeps masking that identifier for whatever arrives under it next. |
| repository ruleset → merge permission | The `Security Audit` required context is what stops an unaudited change reaching main; deleting a job that posts that context is a change to the merge gate itself. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-24 | Spoofing | two CI jobs sharing one display name | high | mitigate | Two jobs configured to reach different verdicts on the same lockfile, both rendering as `Security Audit`, made the divergence invisible in the UI — a green check that was not testing what a reader assumed. Task 3 deletes the duplicate and asserts exactly one job carries that display name. |
| T-09-25 | Denial of service | the surviving audit gate | high | mitigate | A deletion that removed the wrong job, or both, would leave the merge gate reporting on nothing. The verify assertion pins the surviving job id and requires exactly one audit invocation reading the authoritative file; the prohibition block forbids any edit that leaves fewer advisories scanned than before. |
| T-09-26 | Elevation of privilege | self-granted suppressions | high | mitigate | Adding an identifier to either configuration file today grants a permanent exception with no owner and no review. Clause 2 of the guard fails any identifier without a fully-populated register row, in a required-context CI job, so the exception process cannot be bypassed by editing TOML. |
| T-09-27 | Tampering | a suppression outliving its dependency | high | mitigate | An orphaned identifier keeps masking that advisory for any future crate that carries it, while appearing to be governed live risk — this already happened four times. Clause 3 asserts every register row's crate is present in `Cargo.lock`, making the state unreachable rather than merely corrected. |
| T-09-28 | Repudiation | the guard's own error paths | high | mitigate | A guard that treats a missing or unparseable input as an empty set passes vacuously on exactly the tampering it exists to detect. Missing-input and marker-extraction failures are explicit non-zero exits; task 2 demonstrates nine failure modes with recorded exit codes. |
| T-09-29 | Tampering | class information scraped from comments | medium | mitigate | Recovering class from `deny.toml`'s inline comment wording would make the gate depend on prose nobody knows is load-bearing, and it would break into a silent pass. Class comes only from the register's class field; an acceptance criterion greps the script for comment-scraping. |
| T-09-30 | Repudiation | closure claimed on tools that cannot run here | high | mitigate | Neither auditor is installed and neither can be, because crates.io returns HTTP 403 here. Both tool-level passes are recorded as `backstop` truths and restated in the close-out as CI-only; no acceptance criterion in this plan claims a tool pass it did not observe. |
| T-09-SC | Tampering | package-manager installs | high | accept | This plan installs nothing: the guard is `bash` plus stdlib `tomllib`, and the two CI jobs' existing `cargo install` steps are neither added nor modified here. `git diff -- Cargo.toml Cargo.lock crates/*/Cargo.toml` is empty for this plan, so the Package Legitimacy Gate does not fire (`09-RESEARCH.md` § "Package Legitimacy Audit"). |
</threat_model>

<verification>
- `python3` parse assertion prints `deny=10 audit=5 mirrored=True`.
- `./scripts/check-advisory-register.sh` exits 0; the nine negative-path transcripts are in the SUMMARY with their exit codes.
- Exactly one job named `Security Audit` remains, its id is `security-audit`, and exactly one audit invocation exists with no inline advisory identifiers.
- `make -n check-gates` resolves and names all three guards; all three guards pass together.
- `git diff -- .github/workflows/ci.yml | grep -c 'actions-rs\|dtolnay\|actions/cache\|codecov'` → `0`.
- `git diff --stat 49ad74c..HEAD -- '*.rs' | wc -l` → `0`.
- CI-only, restated honestly in the close-out rather than inferred: `cargo audit` and `cargo deny check` passing against the reconciled configuration, and the `Security Audit` required context resolving on the first post-merge run.
</verification>

<success_criteria>
Asking "which advisories does this project suppress, and why?" returns one answer with one owner
from one file; `make audit`, the one CI audit job and `cargo deny` read one configuration rather
than three; and the invariant that keeps them aligned is a script with nine demonstrated failure
modes rather than a sentence in a comment header.
</success_criteria>

<output>
Create `.planning/phases/09-release-security-gate-integrity/09-06-SUMMARY.md` when done, including
the verbatim liveness transcript, the nine negative-path transcripts, and the pre/post job-count
record for the required status check.
</output>
