---
phase: 09-release-security-gate-integrity
plan: 07
type: execute
wave: 4
depends_on: ["09-01", "09-02", "09-03", "09-04", "09-05", "09-06"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/codebase/CONCERNS.md
  - .planning/decisions/PROMOTION.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/phases/09-release-security-gate-integrity/09-COVERAGE.md
autonomous: true
requirements: [SEC-01, SEC-02, SEC-03, SEC-04, SEC-05]

must_haves:
  truths:
    - "Each of SEC-01 through SEC-05 is marked closed in REQUIREMENTS.md only behind an exact command or file:line citation recorded verbatim, never behind an inference"
    - "The five traceability rows read Complete rather than Pending, and match the checkbox state above them"
    - "The corpus's stale suppression arithmetic is corrected at source with the original text retained and marked superseded, in every place it appears"
    - "Phase 12 inherits SUPPLY-01 and SUPPLY-02 as closed-by-Phase-9 items with a commit reference, rather than re-planning work already executed"
    - "Phase 10 receives an explicit hand-off naming every requirement row its ledger must record as already closed, and the pdf-extract reachability finding as an input to its own open question"
    - "The ADR numbering line advances exactly once, from 0024 to 0028, with a dated note explaining the four-number jump"
    - "The claims this phase could not verify locally are listed as unverified with the reason and the CI command that would verify them, rather than being reported as passing"
    - "This phase changed no Rust source, and the coverage floor is confirmed unmoved rather than re-measured"
  artifacts:
    - path: ".planning/phases/09-release-security-gate-integrity/09-COVERAGE.md"
      provides: "The reasoned no-external-API declaration for this phase"
      contains: "No external API integration"
    - path: ".planning/decisions/PROMOTION.md"
      provides: "Numbering index advanced to 0028 with rows for ADRs 0024-0027"
      contains: "0028"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/decisions/0024-rustsec-exception-governance.md"
      via: "the SEC-01 closure citation points at the ADR that records the decision"
      pattern: "0024"
    - from: ".planning/REQUIREMENTS.md"
      to: "the Phase 12 SUPPLY-01 and SUPPLY-02 entries"
      via: "closed-by-Phase-9 notes carrying the commit reference"
      pattern: "SUPPLY-0"
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the Phase 12 scope note points at the requirement rows this phase closed on its behalf"
      pattern: "Phase 9"
  prohibitions:
    - "A closure must not be claimed on evidence that was not executed in this environment. Four of this phase's five requirements concern gates that cannot run here — the two dependency auditors and the container build — and marking any of them closed on the strength of a plausible reading rather than a recorded command is prohibited, however green the change looks."
    - "The stale figures must not be silently overwritten. Corrections are annotations with the original text retained and dated, never a rewrite that leaves no trace of what the record used to claim — a corrected document that hides its own correction teaches the next reader to trust a number nobody re-checked."
---

<objective>
Close Phase 9 in the planning record: flip the five SEC requirements behind recorded evidence,
correct the stale figures at source, hand Phase 10 and Phase 12 what this phase settled on their
behalf, advance the ADR numbering line once, and state plainly which claims a CI runner still has to
confirm.

Purpose: this phase's own discipline is that no closure is claimed without the exact command or
`file:line` that produced it. It is also the phase where four of five requirements concern gates
that cannot be executed locally, so the honest scoping of what was and was not verified is a
deliverable rather than a caveat.
Output: an amended REQUIREMENTS.md, a corrected CONCERNS.md, PROMOTION.md at 0028, PROJECT.md
decision rows, a Phase 12 scope note, and the phase's coverage declaration.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-release-security-gate-integrity/09-CONTEXT.md
@.planning/phases/09-release-security-gate-integrity/09-01-SUMMARY.md
@.planning/phases/09-release-security-gate-integrity/09-02-SUMMARY.md
@.planning/phases/09-release-security-gate-integrity/09-03-SUMMARY.md
@.planning/phases/09-release-security-gate-integrity/09-04-SUMMARY.md
@.planning/phases/09-release-security-gate-integrity/09-05-SUMMARY.md
@.planning/phases/09-release-security-gate-integrity/09-06-SUMMARY.md
</context>

## Artifacts this phase produces

Newly-created symbols and files across all seven Phase 9 plans. Nothing on this list exists in the
tree today, so none of it can be a drift regression against a prior phase.

| Artifact | Plan | Kind |
|---|---|---|
| `crates/paladin-herald/CHANGELOG.md` | 09-01 | new file |
| `scripts/check-changelogs.sh` | 09-01 | new guard script |
| `Makefile` target `check-changelogs` | 09-01 | new target |
| CI step `Check per-crate changelogs` | 09-01 | new `ci.yml` step |
| `SECURITY-EXCEPTIONS.md` | 09-02 | new root file |
| `.planning/decisions/0024-rustsec-exception-governance.md` | 09-02 | new ADR |
| `.planning/decisions/0027-dockerfile-chef-planner-stage.md` | 09-03 | new ADR |
| `.crate-names.txt` | 09-04 | new allow-list file |
| `scripts/check-crate-names.sh` | 09-04 | new guard script |
| `Makefile` target `check-crate-names` | 09-04 | new target |
| CI step `Check crates.io package names` | 09-04 | new `ci.yml` step |
| `.planning/decisions/0026-crate-name-collision-guard.md` | 09-04 | new ADR |
| `LICENSE-MIT`, `LICENSE-APACHE` | 09-05 | new files (option-a branch only) |
| `.planning/decisions/0025-licence-posture.md` | 09-05 | new ADR |
| `scripts/check-advisory-register.sh` | 09-06 | new guard script |
| `Makefile` targets `check-advisory-register`, `check-gates` | 09-06 | new targets |
| CI step `Check advisory exception register` | 09-06 | new `ci.yml` step |
| `.planning/phases/09-release-security-gate-integrity/09-COVERAGE.md` | 09-07 | new declaration file |

<tasks>

<task type="auto">
  <name>Task 1: Close the five SEC rows in REQUIREMENTS.md behind recorded evidence</name>
  <precondition>All six sibling plans have produced SUMMARY files under this phase directory; each carries the verbatim command transcripts this task cites. A missing SUMMARY means the evidence does not exist and this task does not run.</precondition>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 1055-1204 — the SEC block in full, including the run-5 correction banner at 1060-1081 and each requirement's Done-when clause
    - `.planning/REQUIREMENTS.md` lines 3022-3038 — the Milestone 7-8 as-shipped ledger rows this phase's outcomes change
    - `.planning/REQUIREMENTS.md` lines 3755-3759 — the five SEC traceability rows
    - `.planning/REQUIREMENTS.md` lines 3843-3848 — the cross-phase coupling table rows for HARD-06 → SEC-01 and SEC-01 → SUPPLY-01/SUPPLY-02
    - All six sibling SUMMARY files in this phase directory — the source of every command transcript cited here
    - `.planning/phases/09-release-security-gate-integrity/09-CONTEXT.md` → D-00c, D-00d, D-00e, D-05, D-07, D-17, D-19, D-20
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Amend `REQUIREMENTS.md` in place, dated, with superseded text retained — never a rewrite, never a
separate corrections file.

(a) **Flip the five checkboxes**, each with a closure note carrying the exact command or `file:line`
that produced it, lifted verbatim from the sibling SUMMARYs rather than paraphrased. SEC-04 and
SEC-05 close on locally-executed evidence. SEC-03 closes on locally-executed evidence plus a named
accepted residual cost. SEC-02 closes on the recorded checkpoint response, naming the approver and
the branch. SEC-01 closes on the reconciled configuration, the register, the guard's demonstrated
failure modes and the job deletion — and its closure note must state, in the same breath, that the
two dependency auditors themselves were not run here.

(b) **Correct the stale arithmetic at source.** The SEC-01 block and the run-5 correction banner
both carry the older entry counts and the older unmaintained count, and both name an advisory whose
crate is absent from every surface. Correct each figure inline with the tree-verified number,
retaining the original text and marking it superseded with today's date and a pointer to ADR-0024.
The corrections are: the total ignore-array size as read from the file rather than from prose, the
unmaintained-class count, and the fact that the `gcc` advisory was already absent from both
configuration files and from the lockfile with no record of its removal. Also correct the two stale
`ci.yml` line citations in the SEC-01 block — the record cites a range roughly seventy-seven lines
above where the job actually sat before plan 09-06 deleted it — and add a note that after the
deletion any citation into that file must be re-derived rather than carried forward.

(c) **Amend the Milestone 7-8 ledger rows** whose verdicts this phase changes, at lines 3022-3038:
the RustSec risk-acceptance row, the RustSec hardening-actions row, the licence policy sign-off row,
the crate-metadata-completion row, the per-crate changelog row, the Docker workspace-build row, and
the paladin-ports publish-verification row. Each gets a dated amendment naming the Phase 9 outcome
and the ADR that records it, with the original verdict text retained.

(d) **Update the five traceability rows** at 3755-3759 from Pending to Complete. The rows and the
checkboxes must agree; a reader who checks one and not the other must not get two answers.

(e) **Record the Phase 12 closure**, per D-07. SUPPLY-01's concrete deletion and all three of
SUPPLY-02's clauses were executed here. Mark both closed by Phase 9 with the commit reference for
plan 09-06, so Phase 12 inherits closed items rather than re-planning them, and update the
SEC-01 → SUPPLY coupling row at 3843-3848 to say so. State what remains in Phase 12 afterwards:
SUPPLY-03 plus verification of what this phase landed.

(f) **Write the Phase 10 hand-off block**, per D-20. No Milestone 7-8 ledger exists yet — Phase 10 /
HARD-01 builds it — so this phase cannot amend one in place. Instead add an explicit, clearly
headed hand-off naming every requirement row Phase 10's ledger must record as already closed by
Phase 9: the RustSec risk acceptance, the RustSec hardening actions, the licence policy sign-off,
the crate metadata completion, the per-crate changelog, the Docker workspace build, and the
paladin-ports publish verification.

(g) **Hand HARD-06 its input**, per D-17, and decline to answer it. Update the HARD-06 → SEC-01
coupling row at 3843-3848 to record that the coupling is discharged in the SEC-01 direction on tree
evidence: `crates/paladin-content/Cargo.toml:41` declares its PDF dependency unconditionally, with
no optional marker, while `:18`'s feature entry gates nothing — so the advisory's suppression is
warranted regardless of how the capability question resolves. State explicitly that this phase does
not answer whether PDF extraction is supported, that the contradiction between a mandatory
dependency and an empty feature is real, and that it remains Phase 10's subject.
  </action>
  <verify>
    <automated>bash -c 'set -e; f=.planning/REQUIREMENTS.md; for id in SEC-01 SEC-02 SEC-03 SEC-04 SEC-05; do grep -q "\[x\] \*\*$id\*\*" "$f" || { echo "UNCHECKED $id"; exit 1; }; done; for id in SEC-01 SEC-02 SEC-03 SEC-04 SEC-05; do grep -qE "^\| $id \| Phase 9 \| Complete \|" "$f" || { echo "TRACE_PENDING $id"; exit 1; }; done; grep -c "closed by Phase 9\|Closed by Phase 9" "$f"; grep -c "HARD-06" "$f"'</automated>
  </verify>
  <acceptance_criteria>
    - All five SEC checkboxes read checked; the verify command exits 0.
    - All five traceability rows read `Complete`; no row still reads `Pending` for a Phase 9 requirement.
    - Each of the five closure notes contains at least one backticked command or `file:line` citation lifted verbatim from a sibling SUMMARY — checkable by grepping each note for a backtick.
    - SEC-01's closure note names both dependency auditors as not run in this environment.
    - `git diff -- .planning/REQUIREMENTS.md | grep -cE '^\-[^-]'` returns `0` for the SEC block and the ledger rows — corrections are added inline with original text retained, never deleted. Where a checkbox character itself changes, that single-character line is the only permitted deletion; record the count of permitted deletions in the SUMMARY.
    - `grep -c 'SUPPLY-01' .planning/REQUIREMENTS.md` and `grep -c 'SUPPLY-02' .planning/REQUIREMENTS.md` each return at least `2`, and each carries a closed-by-Phase-9 note with a commit reference.
    - The Phase 10 hand-off block names all seven requirement rows listed in the action, verifiable by grepping the block for each row name.
    - The HARD-06 coupling row records the tree evidence with its `file:line` citations and states that this phase does not answer the capability question.
    - `git diff --stat -- '*.rs' | wc -l` returns `0`.
  </acceptance_criteria>
  <done>The five SEC rows are closed behind verbatim evidence, the stale figures are corrected with their originals retained, and Phases 10 and 12 have explicit hand-offs rather than inherited guesswork.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the codebase map, advance the ADR line, and re-scope Phase 12</name>
  <read_first>
    - `.planning/codebase/CONCERNS.md` lines 255-290 — the two advisory sections, dated 2026-07-30: the unmaintained list naming ten advisories including four whose crates Phase 8 removed and one that was already gone, and the three-2026-advisory section citing a `deny.toml` line range
    - `deny.toml` (post plan 09-06) — re-derive the current line numbers for the 2026-advisory block; `CONCERNS.md`'s cited range was already one to two lines stale before this phase, and plan 09-06 moved it again
    - `.planning/decisions/PROMOTION.md` lines 30-60 — the index table, the next-free line at `:47`, the five-step append procedure, and the two prior dated notes explaining earlier jumps
    - `.planning/PROJECT.md` → the Key Decisions table and its evidence note
    - `.planning/ROADMAP.md` § "Phase 12: Supply-Chain Gate Integrity" (around line 667) — its goal, dependency note and five success criteria
    - The four ADRs written this phase, for their exact filenames and titles
  </read_first>
  <files>.planning/codebase/CONCERNS.md, .planning/decisions/PROMOTION.md, .planning/PROJECT.md, .planning/ROADMAP.md</files>
  <action>
(a) **Correct `CONCERNS.md`'s advisory sections** per D-21, using the D-00c pattern: a dated
annotation naming what was wrong, each defective claim corrected inline with the original text
retained and marked superseded. This file sits at the codebase-map tier of the precedence order —
above PRDs — so leaving it wrong actively misleads. Correct the unmaintained count and list to the
five that survive, mark the four Phase-8-orphaned entries as removed by this phase with a pointer to
ADR-0024, mark the already-absent `gcc` entry as removed before this phase with no record of when,
and re-derive and correct the `deny.toml` line citation in the 2026-advisory section rather than
propagating the stale range. Add a pointer from both sections to `SECURITY-EXCEPTIONS.md` as the
authoritative register.

(b) **Advance `PROMOTION.md` exactly once**, per D-18. Append four index rows — 0024 for the RustSec
exception governance, 0025 for the licence posture, 0026 for the crates.io name-collision guard,
0027 for the `Dockerfile.chef` planner-stage supersession — each naming Phase 9 as the executing
phase, following the existing row format exactly. Move the next-free line from 0024 to 0028. Add a
dated note in the same style as the two notes already there, explaining that Phase 9 consumed four
numbers in one phase and why, so a reader who expects a single-number advance finds the explanation
rather than assuming numbers were skipped. No sibling plan touched this file; confirm that by
checking its history before editing, so a concurrent edit cannot be silently overwritten.

(c) **Add PROJECT.md Key Decisions rows** for the four ADRs. That table is documented as empty by
evidence, with a note recording that no locked decision existed anywhere in the corpus and that the
first entries were expected from Phases 1, 5, 7 and 9-10. Add the four rows in the table's existing
format. Do not remove or rewrite the evidence note — amend it to record that Phase 9 supplied its
expected entries, retaining the original text.

(d) **Re-scope Phase 12 in `ROADMAP.md`**, per D-07. Add a dated note under the Phase 12 section
recording that SUPPLY-01's deletion and all three of SUPPLY-02's clauses were executed by Phase 9,
naming the commit, and that what remains is SUPPLY-03 plus verification. Amend the two Phase 12
success criteria that are now already true — the one requiring a single audit invocation with no
inline advisory flags and no shared display name, and the one requiring an owner and a review date
for every suppression — marking each as satisfied by Phase 9 with the original text retained. Do not
delete the criteria and do not renumber anything; a later phase reading a criterion needs to see
both what was asked and that it is done. Correct the criterion that cites the older suppression
count, since the live set is smaller. Leave the Phase 12 goal line, requirement list and every other
phase section untouched.
  </action>
  <verify>
    <automated>bash -c 'set -e; grep -q "Next free ADR number: 0028" .planning/decisions/PROMOTION.md || { echo "COUNTER NOT ADVANCED"; exit 1; }; for n in 0024 0025 0026 0027; do grep -q "| $n |" .planning/decisions/PROMOTION.md || { echo "MISSING ROW $n"; exit 1; }; test -f .planning/decisions/$n-*.md || { echo "MISSING ADR $n"; exit 1; }; done; grep -c "SECURITY-EXCEPTIONS" .planning/codebase/CONCERNS.md; grep -c "Phase 9" .planning/ROADMAP.md'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Next free ADR number: 0028' .planning/decisions/PROMOTION.md` returns `1`, and `grep -c 'Next free ADR number: 0024' .planning/decisions/PROMOTION.md` returns `0`.
    - `ls .planning/decisions/002[4-7]-*.md | wc -l` returns `4`, and each has a matching index row in `PROMOTION.md`.
    - `PROMOTION.md` carries a new dated note explaining the four-number advance, in the same style as the two notes already present.
    - `grep -c 'SECURITY-EXCEPTIONS' .planning/codebase/CONCERNS.md` returns at least `1`, and the corrected `deny.toml` line citation matches the file's current contents — verifiable by opening the cited range and confirming the three 2026 advisory entries are there.
    - `git diff -- .planning/codebase/CONCERNS.md | grep -cE '^\-[^-]'` returns `0` — corrections are annotations, not deletions.
    - `.planning/PROJECT.md` gains four Key Decisions rows and its evidence note is amended rather than replaced; `git diff -- .planning/PROJECT.md | grep -cE '^\-[^-]'` returns `0`.
    - The ROADMAP Phase 12 section carries the dated Phase 9 closure note with a commit reference, and its two now-satisfied criteria are marked with their original text retained; `git diff -- .planning/ROADMAP.md` touches no section other than Phase 12.
    - `git diff --stat -- '*.rs' | wc -l` returns `0`.
  </acceptance_criteria>
  <done>The codebase map no longer misstates the suppression set, the ADR line reads 0028 with the jump explained, PROJECT.md records its first four decisions, and Phase 12 knows what is left of it.</done>
</task>

<task type="auto">
  <name>Task 3: Run the phase gate, declare coverage, and state what CI still has to prove</name>
  <read_first>
    - All six sibling SUMMARY files — the source of the local-evidence and CI-only lists
    - `.planning/phases/09-release-security-gate-integrity/09-VALIDATION.md` → "Manual-Only Verifications" and "Validation Sign-Off" (every manual-only row must be restated in the close-out as explicitly unverified-here)
    - `.planning/decisions/0006-coverage-gate.md` — the 84% workspace line-coverage floor and its scope
    - `CLAUDE.md` → the workspace gate ordering
    - `.planning/phases/09-release-security-gate-integrity/09-CONTEXT.md` → D-19
  </read_first>
  <files>.planning/phases/09-release-security-gate-integrity/09-COVERAGE.md</files>
  <action>
(a) **Run the workspace gate and record it verbatim.** In CLAUDE.md's order:
`cargo test --offline --workspace`, then `cargo fmt --check`, then
`cargo clippy --offline --workspace -- -D warnings`. Prefer the offline flag throughout; crates.io
returns HTTP 403 here while the tree builds and tests offline. Record each command and its result in
the SUMMARY. The test pass count is expected unchanged from the phase base, because this phase
changed no Rust source — record the base and the current counts side by side rather than asserting
they match.

(b) **Prove the no-Rust-change claim** rather than asserting it: run the diff of Rust files between
the phase base commit and the current head and record that it is empty. Then confirm the ADR-0006
coverage floor is unmoved by the argument that no covered line changed, and record that this is a
confirmation rather than a re-measurement — do not run the coverage pipeline for a phase that
touched no source, and do not report a coverage number this phase did not measure.

(c) **Run all three guards together** and record the transcript:
`./scripts/check-advisory-register.sh`, `./scripts/check-crate-names.sh`,
`./scripts/check-changelogs.sh`, and `make check-gates`.

(d) **Re-verify every count this phase depended on**, as the wave-boundary drift check: the
suppression-array sizes in both configuration files, the register row count, the publishable-name
count, the per-crate changelog count, and the per-crate manifest copy count in the container planner
stage. Record each command and output verbatim. Any drift means something moved mid-phase and must
be investigated before the phase is called done.

(e) **Write the coverage declaration** at
`.planning/phases/09-release-security-gate-integrity/09-COVERAGE.md`. The deterministic API-coverage
detector was run at plan time over this phase's scope and returned no detection, so no coverage
matrix is required and none is fabricated. Write the reasoned declaration instead, opening with the
literal phrase `No external API integration` and giving the required one-line reason: this phase
touches release and CI configuration — TOML, workflow YAML, shell guards, a container build file and
Cargo manifest metadata — and integrates against no external service. Add one sentence recording
that crates.io appears throughout this phase's scope as a publishing registry this project uploads
to, not as an API surface this phase integrates against, and that the chosen name-guard design is
offline precisely because that registry is unreachable from this environment. Do not create or touch
`.planning/COVERAGE.md`; that is a different, unrelated project-level register.

(f) **State what CI still has to prove.** Write an explicit, clearly headed section in the SUMMARY
listing every claim this phase could not verify locally, each with the reason and the exact command
a runner will execute: the two dependency auditors passing against the reconciled configuration,
neither being installable because the registry returns HTTP 403 here; the container build's
dependency-compilation layer reporting as cached when only Rust source changes, the container
tooling being absent from this environment; the required status check still resolving after the
duplicate job deletion, which only the platform can evaluate — noting that the committed ruleset
keys on a context string the surviving job still posts, so the risk is assessed rather than open;
and, if the licence branch that changes files was taken, the registry accepting the new expression
at the next real publish, which is out of this phase's scope. Every one of these is recorded as
unverified-here. None is reported as passing.
  </action>
  <verify>
    <automated>bash -c 'set -e; test -f .planning/phases/09-release-security-gate-integrity/09-COVERAGE.md; grep -q "No external API integration" .planning/phases/09-release-security-gate-integrity/09-COVERAGE.md; test ! -e .planning/COVERAGE.md || git diff --quiet -- .planning/COVERAGE.md; git diff --stat 49ad74c..HEAD -- "*.rs" | wc -l; ./scripts/check-advisory-register.sh; ./scripts/check-crate-names.sh; ./scripts/check-changelogs.sh; cargo fmt --check'</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat 49ad74c..HEAD -- '*.rs' | wc -l` returns `0`, recorded verbatim in the SUMMARY.
    - `cargo test --offline --workspace` passes, and the pass count is recorded alongside the phase-base count rather than asserted equal.
    - `cargo fmt --check` exits 0 and `cargo clippy --offline --workspace -- -D warnings` exits 0.
    - All three guards and `make check-gates` exit 0, transcripts recorded.
    - The five drift re-verification counts are recorded verbatim and match the values the sibling plans landed.
    - `test -f .planning/phases/09-release-security-gate-integrity/09-COVERAGE.md` exits 0 and the file opens with the literal phrase `No external API integration` followed by a reason.
    - `git status --porcelain .planning/COVERAGE.md` is empty — the project-level register was not touched.
    - The SUMMARY carries a headed section listing at least four CI-only claims, each with its reason and the command that would verify it, and none of them is described as passing.
    - The ADR-0006 floor is recorded as confirmed-unmoved-by-argument, with no coverage percentage this phase did not measure appearing anywhere in the SUMMARY.
  </acceptance_criteria>
  <done>The workspace gate is green and recorded, the no-Rust-change claim is proven rather than asserted, the coverage declaration is written, and every claim a CI runner still owns is listed as unverified-here rather than inferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase's evidence → the planning record | A closure note in REQUIREMENTS.md becomes the fact every later phase reasons from; an unearned one is indistinguishable from an earned one to the next reader. |
| Phase 9 → Phases 10 and 12 | Both phases plan against what this record says was closed; a missing hand-off makes them re-plan executed work, and a wrong one makes them skip open work. |
| `.planning/codebase/` map → every future plan | The map sits above PRDs in the precedence order, so a stale advisory list there misroutes work at higher authority than the documents it contradicts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-31 | Repudiation | closure claimed on unexecuted evidence | high | mitigate | Four of five requirements concern gates that cannot run here; a closure note that reads as verified when nothing was executed is the failure this phase's own discipline exists to prevent. Every closure note must carry a verbatim command or `file:line` from a sibling SUMMARY, and task 3 lists every CI-only claim explicitly as unverified-here. |
| T-09-32 | Tampering | silent overwriting of stale figures | medium | mitigate | Correcting a number without leaving a trace teaches the next reader that the record was always right, which is how the same arithmetic went uncorrected across five ingest runs. Corrections are annotations with original text retained; the acceptance criteria assert zero deleted lines in the corrected blocks. |
| T-09-33 | Denial of service | Phase 12 re-planning executed work | medium | mitigate | Without an explicit closure note carrying a commit reference, Phase 12 would re-plan a deletion that already happened and re-derive governance that already exists. The SUPPLY-01 and SUPPLY-02 rows and the ROADMAP Phase 12 section are both amended, and the remaining scope is stated. |
| T-09-34 | Spoofing | a coverage floor reported without measurement | medium | mitigate | Reporting a coverage percentage this phase did not measure would put a fabricated number into the record at planning-document authority. The floor is recorded as confirmed-unmoved by the no-source-change argument, and an acceptance criterion forbids any unmeasured percentage appearing in the SUMMARY. |
| T-09-35 | Elevation of privilege | the ADR numbering line | low | mitigate | Two concurrent edits to a single shared counter would produce duplicate ADR numbers with no way to tell which is authoritative. Every sibling plan is forbidden from touching the index; the counter advances exactly once, here, in the last wave, with the jump explained in a dated note. |
| T-09-SC | Tampering | package-manager installs | high | accept | This plan installs nothing and touches no manifest or lockfile. `cargo test`, `cargo fmt`, `cargo clippy` and `cargo metadata` all run with the offline flag against the existing lockfile. The Package Legitimacy Gate does not fire. |
</threat_model>

<verification>
- All five SEC checkboxes checked and all five traceability rows read `Complete`, each behind a verbatim citation.
- `PROMOTION.md` reads `Next free ADR number: 0028` with four new index rows and a dated explanatory note.
- `CONCERNS.md`, `PROJECT.md` and the ROADMAP Phase 12 section are annotated with zero deleted lines.
- `git diff --stat 49ad74c..HEAD -- '*.rs' | wc -l` → `0`.
- `cargo test --offline --workspace`, `cargo fmt --check` and `cargo clippy --offline --workspace -- -D warnings` all pass, recorded verbatim.
- All three guards and `make check-gates` pass.
- `09-COVERAGE.md` exists and opens with the reasoned no-external-API declaration; `.planning/COVERAGE.md` is untouched.
- The CI-only list names at least four claims, each with its reason and its verifying command, none described as passing.
</verification>

<success_criteria>
A developer opening `.planning/` after this phase finds five closed requirements each traceable to a
command someone actually ran, a corrected account of what the suppression set really contains, two
downstream phases holding explicit hand-offs instead of guesses, and an honest list of the four
things only a CI runner can confirm.
</success_criteria>

<output>
Create `.planning/phases/09-release-security-gate-integrity/09-07-SUMMARY.md` when done, including
the workspace-gate transcript, the drift re-verification counts, and the headed CI-only claims list.
</output>
