---
phase: 10-milestone-7-8-ground-truth-recorded-account
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ledgers/milestone-07-08.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [HARD-01, HARD-03, HARD-05, HARD-07]

must_haves:
  truths:
    - "`.planning/ledgers/milestone-07-08.md` exists and carries exactly 86 rows keyed by `REQ-*`, across twelve sections in REQUIREMENTS.md's own run-4 order (D-01, D-27)"
    - "A reader can find every `superseded by outcome` row without scanning 86 rows, because a dedicated summary table at the head of the ledger lists each one by `REQ-*` ID with a one-line what-the-tree-says-instead (D-02)"
    - "The supersession summary table is built from the 13 data rows at `intel/code-verification.md:365-381`, not from the '14-row' figure HARD-01's own text asserts (D-05)"
    - "The ledger head note states the workspace shape once — ten library crates plus `doc-examples` plus the root facade package `paladin-ai` — and records that `paladin-herald` traces to the 2026-06-04 reconciliation rather than to any PRD (D-06)"
    - "The head note states the evidence bar and its manifest carve-out, so no later plan can upgrade a row on an ingest-era status word alone (D-03)"
    - "`REQUIREMENTS.md`'s `## Milestone 7-8 as-shipped ledger` section is a pointer to the ledger file and is not a second, diverging copy of it (D-01)"
    - "Milestone 8 Epic 4's four rows are fully derived end-to-end — verdict, `file:line` citation, and the artefact that exercises it — proving the head-note → legend → section → row → summary-table mechanism before the 86-row fan-out runs"
    - "Two requirements citing the same artefact keep separate rows and separate verdicts; the `REQ-*` ID is the primary key and no two of the 86 rows are merged because they share a citation (D-00f)"
    - "Every one of the 86 rows carries a non-empty Verdict cell and a non-empty Evidence cell; a requirement with no locatable evidence is recorded `genuinely outstanding` or `present, unproven`, never as a blank cell"
    - "The twelve epic sections appear in REQUIREMENTS.md's own run-4 order and rows appear within each section in the ID order REQUIREMENTS.md lists them; no row is ever inserted, deleted, or reordered after this scaffold lands"
    - "Re-running this scaffold against an already-scaffolded ledger amends in place per D-00d and produces no duplicate row and no second head note"
    - "The four wave-3 fan-out plans own disjoint, contiguous epic ranges and replace only Verdict and Evidence cells in place, so `grep -c '^| REQ-'` still reads 86 after each of them"
    - "The '14-row' figure is corrected in place wherever it appears in `REQUIREMENTS.md` and `ROADMAP.md`, with the original text retained per D-00d (D-05)"
    - "HARD-03's stale current-state clause, HARD-05's stale `0.6.0` pin and mis-cited `§4.4`, and HARD-07's stale `ci.yml:225` citation are corrected in place at source with the original text retained (D-11, D-15, D-21)"
  artifacts:
    - path: ".planning/ledgers/milestone-07-08.md"
      provides: "The fourth sibling ledger — head notes, verdict legend, superseded-by-outcome summary table, twelve epic sections, 86 REQ-keyed rows"
      contains: "## Superseded by outcome"
    - path: ".planning/REQUIREMENTS.md"
      provides: "A pointer section replacing the inline 86-row ledger, plus D-00d in-place corrections to HARD-01/03/05/07's own text"
      contains: "milestone-07-08.md"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ledgers/milestone-07-08.md"
      via: "The `## Milestone 7-8 as-shipped ledger` section reduced to a pointer naming the ledger file by path"
      pattern: "ledgers/milestone-07-08\\.md"
    - from: ".planning/ledgers/milestone-07-08.md"
      to: ".planning/intel/code-verification.md"
      via: "The superseded-by-outcome summary table transcribed from the 13-row table at :365-381"
      pattern: "code-verification\\.md"
  prohibitions:
    - "A ledger verdict must not be upgraded on the strength of an ingest-era status word. Carrying `Shipped` forward as `satisfied` without re-deriving the citation is the exact false-positive class this ledger exists to reject, and it is prohibited even where it would let a plan finish faster."
    - "A `.planning/` correction must not delete or rewrite the original text. The superseded claim is the evidence; a clean-looking document that silently loses what it used to assert destroys the record the amendment convention exists to preserve."
    - "The supersession class must not be built from a figure quoted in prose. The class is built by counting the table, and recording a count this plan did not itself run is prohibited."
---

<objective>
Create `.planning/ledgers/milestone-07-08.md` — the fourth sibling ledger — proved end-to-end on one
complete epic section, then reduce `REQUIREMENTS.md`'s inline 86-row section to a pointer and correct
the stale figures inside HARD-01/03/05/07's own requirement text.

Purpose: this is the tracer for the whole phase. Every later ledger plan writes rows into a structure
this plan defines; if the head notes, the legend, the summary table, the section ordering and one
fully-derived epic section do not hold together, the 86-row fan-out multiplies the defect by four.
D-27 names this the wave-1 scaffold.
Output: a ledger file with 86 `REQ-*` rows (four of them fully derived), a pointer section in
REQUIREMENTS.md, and four dated in-place corrections to requirement text that the tree has moved past.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-RESEARCH.md
</context>

## Artifacts this phase produces

Every artefact Phase 10 creates or modifies, across all eleven plans. The plan-review-convergence
source-grounding pass reads this section.

| Artifact | Plan | Kind |
|---|---|---|
| `.planning/ledgers/milestone-07-08.md` | 10-01 (scaffold), 10-07…10-10 (fan-out), 10-11 (close-out amendment) | new file |
| `.planning/decisions/0028-m8-reconciliation-authoritative.md` | 10-02 | new ADR |
| `.planning/decisions/0029-version-trajectory-history.md` | 10-03 | new ADR |
| `.planning/decisions/0030-milestone-7-self-numbering.md` | 10-03 | new ADR |
| `.planning/decisions/0031-extracted-crate-dependency-rule.md` | 10-04 | new ADR |
| `.planning/decisions/0032-pdf-extraction-capability.md` | 10-05 | new ADR |
| `.planning/decisions/0033-cargo-doc-warning-bar.md` | 10-06 | new ADR |
| `.planning/REQUIREMENTS.md` | 10-01 (pointer + text corrections), 10-11 (checkboxes, traceability, hand-offs) | modified |
| `.planning/ROADMAP.md` | 10-01 (criterion-1 figure), 10-11 (phase close-out) | modified |
| `.planning/PROJECT.md` | 10-11 | modified |
| `.planning/decisions/PROMOTION.md` | 10-11 | modified (index + next-free → 0034) |
| `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_1/facade-audit.md` | 10-02 | annotated |
| `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md` | 10-02 | annotated |
| `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/prd-relocate-remaining-misplaced-modules.md` | 10-02 | annotated |
| `.project/Milestone_7-Production-Hardening/overview/Milestone_7-Tier_4-Production_Hardening.md` | 10-03 | annotated |
| `.project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md` | 10-04 (§6.1, Goal 2), 10-05 (§4.4.1, §4.4.6) | annotated |
| `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_5/prd-document-facade-crate-role.md` | 10-06 | annotated |
| `.project/Milestone_7-Production-Hardening/Epic_4/epic-4-completion-summary.md` | 10-06 | annotated |
| `crates/paladin-content/Cargo.toml` | 10-05 | modified (one feature line deleted) |
| `crates/paladin-content/CHANGELOG.md` | 10-05 | modified (one entry) |
| `.cargo/audit.toml` | 10-05 | modified (one comment corrected) |
| `Makefile` | 10-06 | modified (one flag and one echo deleted) |

**No `.rs` file appears on this list, and none may be added to it (D-23).**

## Ledger file contention — the rule every ledger-writing plan obeys

`.planning/ledgers/milestone-07-08.md` is created by this plan and then written by five others. The
contention rule, stated once here and restated in each of those plans:

| Plan | Wave | Owns | May |
|---|---|---|---|
| 10-01 | 1 | the whole file | create head notes, legend, summary table, twelve section headings, all 86 row stubs; fully derive M8 Epic 4's four rows |
| 10-07 | 3 | M7 Epic 1 (12 rows) + M7 Epic 2 (13 rows) | replace Verdict and Evidence cells **in place** inside its own two sections only |
| 10-08 | 3 | M7 Epic 3 (10 rows) + M7 Epic 4 (12 rows) | same, its own two sections only |
| 10-09 | 3 | M8 Epic 1 (4) + M8 Epic 2 (4) + M8 Epic 3 (6) | same, its own three sections only |
| 10-10 | 3 | M8 Epic 5 (6) + M8 Epic 6 (4) + M8 Epic 7 (6) + Cross-milestone (5) | same, its own four sections only |
| 10-11 | 4 | a dated close-out amendment section appended at the foot | append only |

M8 Epic 4's four rows are derived by this plan and are owned by no fan-out plan. The four wave-3
plans run in parallel over **disjoint, contiguous** section ranges and perform **cell replacement
only** — never row insertion, deletion or reordering — so the four diffs are non-adjacent hunks in
one file and merge without conflict. `grep -c '^| REQ-'` reads `86` before and after every one of
them, and `git diff --numstat` on the ledger shows added lines equal to deleted lines.

<tasks>

<task type="tracer">
  <name>Task 1: Create the Milestone 7-8 ledger — head notes, legend, supersession summary table, 86 row stubs, and M8 Epic 4 fully derived</name>
  <read_first>
    - `.planning/ledgers/milestone-04-06.md` lines 1-110 in full — the head-note skeleton, the verdict legend table, the ROADMAP-criterion mapping and tie-break callouts, and the "Row order and amendment convention" section. This is the shape to copy; it is the file whose own head note names `milestone-07-08.md` as this phase's deliverable.
    - `.planning/ledgers/milestone-01.md` lines 21-38 — the dated in-place amendment section pattern (`## Phase N amendments (YYYY-MM-DD)`).
    - `.planning/REQUIREMENTS.md` lines 3121-3317 in full — the existing 86-row run-4 ledger. This is the row inventory: the twelve section headings, the `REQ-*` ID order within each, and the current verdict text. Its Phase-9 amendments at `:3211-3222` are the newest text in it.
    - `.planning/intel/code-verification.md` lines 263-414 — the run-4 verification block. In particular the `### Superseded by shipped outcome — do not plan these as written` table at `:365-381`.
    - `.planning/REQUIREMENTS.md` lines 1320-1355 — Phase 9's hand-off block naming the seven `REQ-*` rows already closed.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md` → the `.planning/ledgers/milestone-07-08.md` section (head-note skeleton, legend wording difference, summary-table shape, the row-count re-verification command).
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-RESEARCH.md` → "Code Examples" → the confirmed 86-row count and per-epic breakdown table, and the confirmed 13-row supersession count.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-00c, D-00d, D-00e, D-00f, D-01 through D-06, D-27.
    - `.planning/codebase/STRUCTURE.md` lines 51-71 — the corrected workspace shape (ten library crates plus `doc-examples`), so D-06's head-note paragraph cites a map that is already right.
  </read_first>
  <files>.planning/ledgers/milestone-07-08.md</files>
  <action>
Create `.planning/ledgers/milestone-07-08.md`, titled `# Milestone 7-8 cited status ledger`, following
`milestone-04-06.md`'s structure section-for-section. Do not invent a new format.

**Head notes**, in this order:

1. A supersession paragraph stating that this file supersedes `REQUIREMENTS.md`'s
   `## Milestone 7-8 as-shipped ledger` section per D-01, that the section becomes a pointer, and that
   Phase 13 adds `milestone-09-12.md` as the fifth sibling.
2. The primary-key paragraph (D-00f): the `REQ-*` ID is the primary key; outstanding task items nest
   under the requirement they belong to; the same `file:line` may legitimately appear in more than one
   row, and two rows are never merged because they cite the same artefact. Copy the substance of
   `milestone-04-06.md`'s paragraph.
3. The evidence bar (D-03): `satisfied` requires a `file:line` citation **plus** a named passing test,
   example or command that exercises it; a citation with nothing exercising it is `present, unproven`.
   State that the bar applies to all 86 rows without exception, including every row the run-4 ledger
   already marked with an ingest-era status word, and that such a status word **is** the bare
   code-exists claim the bar exists to reject.
4. The manifest carve-out (D-03): Milestone 7 is a structural milestone like 4-6, so for manifest
   requirements the manifest line plus a named CI job or build leg that consumes it is the exercising
   artefact. Name the M7/M8-specific anchors and re-grep each before writing it: the `lint` job's
   `cargo doc` step and the workspace test/clippy steps in `.github/workflows/ci.yml`, the
   feature-matrix and library-only isolation steps in `.github/workflows/feature-flags.yml`, and the
   per-crate publish dry-run leg in `.github/workflows/release.yml`. State the line numbers you
   actually observed, not the ones any earlier document quotes.
5. Path caveats (D-00d): state that six Milestone 7-8 deliverables ship as mdbook chapters under
   `docs/src/` rather than at their PRD paths — `docs/PERFORMANCE_BASELINE.md`,
   `docs/RELEASE_CHECKLIST.md`, `docs/VERSIONING_POLICY.md`, `docs/BUILD_BASELINES.md`,
   `docs/INTEGRATION_TESTS.md` and the root `STABLE_API.md` — and that a row whose only divergence is
   this caveat is `relocated`, not a gap.
6. Workspace shape and provenance (D-06): state once, authoritatively, that the workspace is ten
   library crates plus `doc-examples` plus the root facade package `paladin-ai`, citing
   `.planning/codebase/STRUCTURE.md:51-71` and `ls crates/`. Then give the five previously
   provenance-pending crates their provenance: `paladin-storage`, `paladin-notifications`,
   `paladin-content` and `paladin-web` trace to M7 Epic 1's extraction PRD and its four-Go
   cost-benefit gate; **`paladin-herald` traces to the 2026-06-04 reconciliation, not to any PRD** —
   which is why no ingested requirement described it before run 4 and why the earlier nine-crate
   figure was wrong.
7. Per-milestone checkbox corroboration: Milestone 7 at 98.8% with 3 open items (all in
   `tasks-production-build-infra-adaptation.md`) and Milestone 8 at 99.1% with 3 open, from
   `intel/task-completion-state.md`. State explicitly that run 4 established Milestone 8's three as
   contradicted by code and Milestone 7's three as plausible but uncorroborated, so the counts are
   recorded as inputs and not trusted as verdicts.
8. A dated provenance line naming this phase, this plan, and the date.

**Verdict legend.** A table using D-02's seven classes exactly — `satisfied`, `present, unproven`,
`genuinely outstanding`, `relocated`, `superseded by outcome`, `deferred with register`, `diverged`.
Note in the head note that this wording differs deliberately from `milestone-04-06.md`'s
`superseded by shipped code` / `deferred with reason`, and copy that file's shape, columns and its two
following callouts but not its legend text. Follow the legend with (a) a HARD-01 mapping callout
showing that HARD-01's four required dispositions — `Shipped`, `Superseded by outcome`, `Relocated`,
`Deferred with register` — map onto `satisfied`, `superseded by outcome`, `relocated` and
`deferred with register`, and that `present, unproven`, `genuinely outstanding` and `diverged` are this
series' own finer vocabulary; and (b) a tie-break rule stating that a row qualifying for both
`relocated` and `superseded by outcome` takes `relocated`, because the mdbook relocations are this
corpus's largest false-gap generator and collapsing the class destroys the moved-not-missing signal.

**The `## Superseded by outcome — do not plan these as written` summary table.** Place it immediately
after the legend, at the head of the file, so a planner never scans 86 rows to find these. Columns:
`REQ-* ID` · `What the requirement says` · `What the tree does instead` · `Citation`. Build it by
reading and counting `intel/code-verification.md:365-381` directly. Before transcribing, run
`sed -n '365,381p' .planning/intel/code-verification.md | grep -c '^|'` and record the number you got
in the table's own preamble; the table holds **13** data rows, not the fourteen HARD-01 and
`REQUIREMENTS.md:3136` both assert. State that discrepancy in the preamble with today's date, per D-05
and D-00d, and note that the figure is corrected at source by this same plan's task 3.

**Row order and amendment convention.** A section copying `milestone-04-06.md:82-91` in structure:
the twelve sections appear in REQUIREMENTS.md's own run-4 order and are never re-sorted; rows within a
section appear in the ID order REQUIREMENTS.md lists them; later plans replace a row's Verdict and
Evidence cells in place and never insert, delete or reorder rows. Then reproduce the contention table
from this plan's own "Ledger file contention" section above, naming which plan owns which epic range
and stating that the four wave-3 plans run in parallel over disjoint contiguous ranges with cell
replacement only. Amendments follow D-00d: edit in place, retain superseded text, date every
amendment, never a separate corrections file.

**The twelve epic sections and 86 rows.** Create the sections with the exact headings and ID counts
the run-4 ledger uses: M7 Epic 1 — Extended Workspace Decomposition (12); M7 Epic 2 — Production Build
Infrastructure (13); M7 Epic 3 — Benchmark Suite Migration (10); M7 Epic 4 — API Stabilization &
Pre-Release Preparation (12); M8 Epic 1 — Facade Crate Audit (4); M8 Epic 2 — Dead Shim & Empty Module
Removal (4); M8 Epic 3 — Relocate Remaining Misplaced Modules (6); M8 Epic 4 — `use_cases` → `services`
Rename (4); M8 Epic 5 — Facade Role Documentation & v0.2.0 Finalization (6); M8 Epic 6 —
`paladin-content` Services Rename (4); M8 Epic 7 — `paladin-web` Single Framework (axum) (6);
Cross-milestone entries carried by DOCs rather than PRDs (5). Total 86.

Each section carries a three-column table with header `| ID | Verdict | Evidence |` and one row per
`REQ-*` ID, in REQUIREMENTS.md's order. Under each section heading, before the table, place a single
line beginning `**Epic note:**` reserved for the owning fan-out plan to fill in place.

For every row this plan does not derive, the Verdict cell reads `pending — plan 10-NN` naming the
owning fan-out plan from the contention table, and the Evidence cell carries the run-4 ledger's
current text verbatim prefixed with `run-4 input (not yet re-derived):`. **No cell is ever left
blank** — a stub is an explicit pending marker, not an empty cell, so the empty-input case cannot be
mistaken for a finished verdict.

**Fully derive Milestone 8 Epic 4's four rows** — `REQ-use-cases-services-rename`,
`REQ-rename-clean-break`, `REQ-rename-doc-updates`, `REQ-rename-changelog-breaking`. This is the
end-to-end proof of the whole mechanism, so treat it as production work, not a sample. For each row:
assign one of the seven verdicts; cite `file:line` from the tree, re-read this session, never copied
from the run-4 text; and name the artefact that exercises it. The workspace-wide zero-match grep for
`use_cases` across `src/`, `crates/`, `tests/`, `examples/` and `benches/` is the exerciser for the
rename rows — run it and record the exact command and its result. Where the run-4 ledger says
`Verify → HARD-01` and nothing in the tree exercises the claim, the correct verdict is
`present, unproven` or `genuinely outstanding`, with the reason stated; do not reach for `satisfied`.
Where a row's evidence genuinely cannot be located, record `genuinely outstanding` with what was
searched and the command that returned nothing.

Selection reason for this section, to state in the SUMMARY: M8 Epic 4 is the only complete section
whose rows cite no ADR this phase has not yet written, so it proves the mechanism without a forward
reference.

Write no ADR cross-reference in this plan's derived rows. Write no `.rs` change of any kind.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/ledgers/milestone-07-08.md; test -f "$f" && echo "rows=$(grep -c "^| REQ-" "$f")" && echo "uniq=$(grep -o "^| REQ-[a-z0-9-]*" "$f" | sort -u | wc -l)" && echo "sections=$(grep -c "^### " "$f")" && echo "epicnotes=$(grep -c "^\*\*Epic note:\*\*" "$f")" && echo "blankcells=$(grep -c "^| REQ-[a-z0-9-]* *| *|" "$f")" && echo "supertable=$(grep -c "^## Superseded by outcome" "$f")"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| REQ-' .planning/ledgers/milestone-07-08.md` returns exactly `86`.
    - `grep -o '^| REQ-[a-z0-9-]*' .planning/ledgers/milestone-07-08.md | sort -u | wc -l` returns exactly `86` — every `REQ-*` ID is distinct, so no two rows were merged for sharing a citation.
    - `grep -c '^### ' .planning/ledgers/milestone-07-08.md` returns exactly `12` and the twelve headings appear in the same relative order as `awk 'NR>=3121 && NR<=3317' .planning/REQUIREMENTS.md | grep '^### '`, verified by diffing the two heading lists with the ID counts stripped.
    - `grep -c '^\*\*Epic note:\*\*' .planning/ledgers/milestone-07-08.md` returns exactly `12`.
    - `grep -c '^| REQ-[a-z0-9-]* *| *|' .planning/ledgers/milestone-07-08.md` returns `0` — no row has an empty Verdict cell.
    - `grep -c '^## Superseded by outcome' .planning/ledgers/milestone-07-08.md` returns `1`, and the table beneath it holds exactly `13` data rows, matching `sed -n '365,381p' .planning/intel/code-verification.md | grep -c '^|'` minus its header and separator.
    - `grep -c 'paladin-herald' .planning/ledgers/milestone-07-08.md` returns at least `1` and the head note states that its provenance is the 2026-06-04 reconciliation rather than a PRD.
    - `grep -c 'present, unproven' .planning/ledgers/milestone-07-08.md` returns at least `1` — the seven-class legend is present in the file, not only described.
    - The four Milestone 8 Epic 4 rows each carry a `file:line` citation and a named exercising artefact: `grep -A6 'use_cases. → .services. Rename' .planning/ledgers/milestone-07-08.md | grep -c 'pending — plan'` returns `0`.
    - `git status --porcelain -- '*.rs'` is empty.
  </acceptance_criteria>
  <done>The ledger file exists with 86 distinct `REQ-*` rows across twelve correctly-ordered sections, a 13-row supersession summary table at its head, a stated evidence bar and workspace shape, and one complete epic section derived end-to-end with citations and exercisers.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce REQUIREMENTS.md's inline Milestone 7-8 ledger to a pointer</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 3115-3320 — the `## Milestone 7-8 as-shipped ledger` section in full, plus the headings immediately before and after it, so the replacement lands inside the right boundaries.
    - `.planning/REQUIREMENTS.md` — the existing `## Milestone 4-6 as-shipped ledger` pointer that Phase 7 wrote. Locate it by searching for `milestone-04-06.md` and copy its exact pointer prose shape.
    - `.planning/ledgers/milestone-07-08.md` (post-task-1) — confirm the file exists and the row count before writing a pointer that claims it does.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md` → the `REQUIREMENTS.md:3121-3317 (reduced to pointer)` section.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-01 in full.
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Replace the body of `## Milestone 7-8 as-shipped ledger` — currently `REQUIREMENTS.md:3121-3317`,
re-derive the exact boundaries by reading rather than trusting these numbers — with a pointer, per
D-01. Match the pointer prose Phase 7 wrote for the Milestone 4-6 section: state that the section is
superseded by `.planning/ledgers/milestone-07-08.md`, name this phase and plan and today's date, state
that the ledger holds the full 86-row `file:line`-cited verdict table, and state that this section is
retained as a pointer only.

Do **not** leave the 86 rows in place beside the pointer. D-01 is explicit that a second, diverging
copy is the failure mode this move exists to prevent. Retain the section heading itself and the
existing status-key preamble sentence if one is present, so a reader arriving by line number still
lands on a recognisable section.

Add one sentence to the pointer stating that the Phase 9 amendment notes previously carried inside
this section — the `Amended by Phase 9 (plan 09-07), dated 2026-08-08` notes on the seven closed rows —
are carried forward into the ledger's own rows and are not lost by this reduction.

Touch nothing else in `REQUIREMENTS.md` in this task; the requirement-text corrections are task 3.
  </action>
  <verify>
    <automated>bash -c 'echo "rows_left=$(awk "/^## Milestone 7-8 as-shipped ledger/,/^## /" .planning/REQUIREMENTS.md | grep -c "^| REQ-")"; echo "pointer=$(grep -c "ledgers/milestone-07-08.md" .planning/REQUIREMENTS.md)"; echo "total_lines=$(wc -l < .planning/REQUIREMENTS.md)"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ledgers/milestone-07-08.md' .planning/REQUIREMENTS.md` returns at least `1`.
    - The `## Milestone 7-8 as-shipped ledger` section contains zero `REQ-*` table rows: extracting the section with `awk '/^## Milestone 7-8 as-shipped ledger/{f=1;next}/^## /{f=0}f' .planning/REQUIREMENTS.md | grep -c '^| REQ-'` returns `0`.
    - `grep -c '^## Milestone 7-8 as-shipped ledger' .planning/REQUIREMENTS.md` returns exactly `1` — the heading survives, and no duplicate section was created.
    - `wc -l < .planning/REQUIREMENTS.md` is at least 150 lines smaller than before the edit, confirmed against the pre-edit count recorded in the SUMMARY.
    - `grep -c 'ledgers/milestone-04-06.md' .planning/REQUIREMENTS.md` is unchanged from its pre-edit value — the Milestone 4-6 pointer was not disturbed.
    - `git diff --stat -- .planning/REQUIREMENTS.md` shows this file as the only change in this task's commit.
  </acceptance_criteria>
  <done>`REQUIREMENTS.md`'s Milestone 7-8 section names the ledger file and carries no requirement rows, so no second diverging copy of the 86 verdicts exists.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the stale figures inside HARD-01, HARD-03, HARD-05 and HARD-07's own text, and in ROADMAP criterion 1</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 1357-1482 — HARD-01 through HARD-07 in full. The four defective clauses are at approximately `:1362` (the "14-row table" figure), `:1416` (HARD-03's current-tree clause), `:1437` and `:1441` (HARD-05's `§4.4` mis-citation and its `version = "0.6.0"` quote), and `:1474` (HARD-07's `ci.yml:225` citation). Re-derive every line number by reading.
    - `.planning/ROADMAP.md` around line 655 — Phase 10's Success Criteria, criterion 1, which carries the same "fourteen" figure.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-RESEARCH.md` → Common Pitfall 3 (the `cost-benefit-assessment.md:118` correction) and the Summary's confirmed-facts paragraph.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-00d, D-05, D-10, D-11, D-15, D-21.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md` → the two annotation shapes; use the inline strike-and-correct shape (Shape A) here, since these are clause-level fixes inside otherwise-live requirement text.
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Apply four dated in-place corrections inside `REQUIREMENTS.md`'s HARD block and one in `ROADMAP.md`,
using the inline strike-and-correct shape from `10-PATTERNS.md`: strike the defective phrase with
`~~` and follow it immediately with a `**Corrected (dated 2026-08-08, HARD-NN):**` sentence naming the
observed fact and the command or `file:line` that produced it. **Retain every original word** — this is
annotation, not rewriting (D-00c/D-00d).

1. **HARD-01's "14-row table" figure** (`REQUIREMENTS.md` ≈ `:1362`). Corrected value: the
   `### Superseded by shipped outcome — do not plan these as written` table at
   `.planning/intel/code-verification.md:365-381` holds **13** data rows. Cite the counting command
   and its output. Per D-05, correct the figure wherever it appears — check for and correct any second
   occurrence in the same file, and record in the SUMMARY how many occurrences you found and fixed.

2. **HARD-03's current-tree clause** (≈ `:1416`, the sentence asserting `Cargo.toml` `0.6.0`, branch
   `release/v0.7.0`, latest tag `v0.5.1`). Corrected values: `Cargo.toml:34` reads `version = "0.7.0"`
   (Phase 4, commit `c2e20a1`, ADR-0008); `git tag --sort=-v:refname | head -3` returns `v0.7.1`,
   `v0.7.0`, `v0.5.1` in that order; the branch is still `release/v0.7.0`. In the same correction,
   record that HARD-03's "Feeds REL-01" clause has already fired: REL-01 is `[x]` at
   `REQUIREMENTS.md:358` with a `Phase 4 | Complete` traceability row at `:3913`, so HARD-03's live job
   is backwards-looking confirmation and REL-01 must not be re-planned (D-10). Leave REL-01's own
   checkbox untouched.

3. **HARD-05's two defects** (≈ `:1437` and `:1441`). First, the "same PRD's §4.4 complexity
   assessment" citation does not resolve: `prd-extract-infrastructure-crates.md` §4.4 is Task 1.4 and
   contains no mention of `paladin-llm`. The sentence lives in the sibling document
   `.project/Milestone_7-Production-Hardening/Epic_1/cost-benefit-assessment.md:118`, inside the
   `paladin-content` row's `Extraction complexity` cell. Second, the quoted manifest line pins
   `version = "0.6.0"`; the tree reads `version = "0.7.0"` at
   `crates/paladin-content/Cargo.toml:28`, converged by Phase 4's commit `c2e20a1`. Correct both,
   citing the grep for each. Note that HARD-05's substance is unchanged by either correction.

4. **HARD-07's `ci.yml:225` citation** (≈ `:1474`). Corrected value: `crates/paladin-ports/Cargo.toml`
   has no `[lib]` section at all — `git log --oneline -- crates/paladin-ports/Cargo.toml` shows
   `2bffe22 feat(08-03): re-enable paladin-ports doctests` — and `.github/workflows/ci.yml:238` is a
   bare workspace doc-test invocation carrying no crate exclusion. State that DEBT-03 and the
   "unwritten Task 7.0" are therefore discharged by Phase 8, that the surviving residue is one line in
   the `Makefile`'s `release-check` target, and that plan 10-06 executes that fix. Re-derive `:238` by
   reading `ci.yml` rather than trusting this number.

5. **ROADMAP criterion 1** (`.planning/ROADMAP.md`, Phase 10's Success Criteria, the phrase "the
   fourteen 'superseded by outcome' entries"). Apply the same strike-and-correct treatment with the
   13-row count and a pointer to `.planning/ledgers/milestone-07-08.md`'s summary table. Change nothing
   else in `ROADMAP.md` — its Phase 10 checkbox and plan list belong to plan 10-11 and to the planner.

Every corrected figure must be one this task itself produced by running the command it cites. Do not
transcribe a number from `10-CONTEXT.md` or `10-RESEARCH.md` without re-running its command; those
documents record a tree state at commit `9550299` and the tree has moved since.
  </action>
  <verify>
    <automated>bash -c 'echo "corrections=$(grep -c "Corrected (dated 2026-08-08, HARD-" .planning/REQUIREMENTS.md)"; echo "strikes=$(sed -n "1357,1500p" .planning/REQUIREMENTS.md | grep -c "~~")"; echo "roadmap=$(grep -c "Corrected (dated 2026-08-08" .planning/ROADMAP.md)"; echo "rel01=$(sed -n "358p" .planning/REQUIREMENTS.md | grep -c "\[x\]")"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Corrected (dated 2026-08-08, HARD-' .planning/REQUIREMENTS.md` returns at least `4` — one correction per defective clause.
    - `sed -n '1357,1500p' .planning/REQUIREMENTS.md | grep -c '~~'` returns at least `4` — original text is struck, not deleted.
    - `grep -c 'cost-benefit-assessment.md' .planning/REQUIREMENTS.md` returns at least `1` — HARD-05's citation now names the document that actually holds the sentence.
    - `sed -n '358p' .planning/REQUIREMENTS.md | grep -c '\[x\]'` returns `1` — REL-01's own checkbox is untouched.
    - `grep -c 'Corrected (dated 2026-08-08' .planning/ROADMAP.md` returns at least `1`, and `grep -c 'ledgers/milestone-07-08.md' .planning/ROADMAP.md` returns at least `1`.
    - `grep -n '0\.6\.0' .planning/REQUIREMENTS.md | grep -c 'paladin-llm'` returns `0` outside struck text — verified by confirming every surviving `0.6.0` occurrence near HARD-05 sits inside a `~~…~~` span.
    - `git status --porcelain -- '*.rs' Cargo.toml Makefile .cargo/audit.toml` is empty — this task touches planning documents only.
  </acceptance_criteria>
  <done>Four defective clauses inside HARD-01/03/05/07's own text and ROADMAP criterion 1's row count each carry a dated inline correction with its producing command, and every original phrase is retained struck rather than deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingested `.project/` and `intel/` prose → `.planning/` records at ledger precedence | Figures and citations copied forward from documents that this corpus has already caught lying about themselves five times inside HARD-01…07's own text. |
| four parallel wave-3 executors → one ledger file | Disjoint epic ranges are the only thing preventing concurrent writes from dropping or duplicating rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | the ledger's row inventory | high | mitigate | A dropped or duplicated row is invisible in a 300-line markdown table and would silently retire a requirement. The scaffold creates all 86 stubs up front and every subsequent plan asserts `grep -c '^\| REQ-'` equals `86`, so the count is a gate rather than an assumption. |
| T-10-02 | Repudiation | figures transcribed from CONTEXT.md / RESEARCH.md | high | mitigate | Both documents record a tree state at commit `9550299`; HEAD has moved. Task 3 forbids transcribing any figure without re-running the command that produced it, and each acceptance criterion names the command rather than the expected prose. |
| T-10-03 | Information disclosure | superseded requirement text | low | accept | The corrections retain original text by design (D-00c/D-00d). Nothing sensitive is exposed; the retained text is the evidence the record exists to preserve. |
| T-10-04 | Denial of service | `REQUIREMENTS.md` section boundaries | medium | mitigate | Task 2 deletes ~200 lines from a 4,136-line file. The boundaries are re-derived by reading rather than trusted from a line number, and the acceptance criteria assert that exactly one `## Milestone 7-8 as-shipped ledger` heading survives and that the Milestone 4-6 pointer count is unchanged. |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing, adds no dependency, and touches no manifest. `10-RESEARCH.md`'s Package Legitimacy Audit records the phase as not-applicable: the only manifest touch in the whole phase removes a declaration. The Package Legitimacy Gate does not fire. |
</threat_model>

<verification>
- `grep -c '^| REQ-' .planning/ledgers/milestone-07-08.md` → `86`; distinct IDs → `86`.
- `grep -c '^### ' .planning/ledgers/milestone-07-08.md` → `12`, in REQUIREMENTS.md's run-4 order.
- The supersession summary table holds 13 data rows, and its preamble records the counting command's output.
- `awk '/^## Milestone 7-8 as-shipped ledger/{f=1;next}/^## /{f=0}f' .planning/REQUIREMENTS.md | grep -c '^| REQ-'` → `0`.
- `grep -c 'Corrected (dated 2026-08-08, HARD-' .planning/REQUIREMENTS.md` → at least `4`.
- `git status --porcelain -- '*.rs'` → empty. D-23's boundary holds for this plan.
</verification>

<success_criteria>
A reader opening `.planning/ledgers/milestone-07-08.md` finds the evidence bar, the workspace shape,
the seven-class legend and every `superseded by outcome` row before reaching row one; all 86 rows
exist with an explicit verdict cell and none is blank; one epic section is fully derived with
citations and exercisers; `REQUIREMENTS.md` points at the ledger instead of duplicating it; and four
figures inside the requirement text that the tree has moved past carry dated corrections with the
commands that produced them.
</success_criteria>

<output>
Create `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-01-SUMMARY.md` when done.
</output>
