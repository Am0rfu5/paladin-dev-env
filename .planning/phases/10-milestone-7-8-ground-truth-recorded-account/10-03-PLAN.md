---
phase: 10-milestone-7-8-ground-truth-recorded-account
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/decisions/0029-version-trajectory-history.md
  - .planning/decisions/0030-milestone-7-self-numbering.md
  - .project/Milestone_7-Production-Hardening/overview/Milestone_7-Tier_4-Production_Hardening.md
autonomous: true
requirements: [HARD-03, HARD-04]

must_haves:
  truths:
    - "Reading the version record teaches history, not current state: ADR-0029 records `v0.1.0-rc.1` at commit `a9530fc` on 2026-05-28 with all ten crates published at `0.1.0`, a GO sign-off, docs.rs verification and the external smoke project, as closed history (D-10)"
    - "REL-01 is not re-opened: ADR-0029 records that Phase 4 already converged the version disagreement on `0.7.0` via ADR-0008, that REL-01 is `[x]` at `REQUIREMENTS.md:358` with a `Phase 4 | Complete` traceability row, and that it did not converge on any rc.1 figure (D-10)"
    - "ADR-0029 is the single home for the whole trajectory: it carries a `## Trajectory` table that Phase 13 / ORCH-05 appends rows to rather than writing a second version ADR, and says so in `Downstream Consumers` (D-12)"
    - "The trajectory table gives each tag its own row even where one milestone targeted two figures — a row is keyed by the tag, not by the milestone — so `v0.1.0-rc.1` and the lockstep `0.2.0` target are never merged"
    - "A trajectory entry with no tag in the tree — the lockstep `0.2.0` target M7 Epic 4's PRD and the overview Appendix C anchored on — is recorded with an explicit targeted-not-shipped cell rather than left blank or silently dropped"
    - "Trajectory rows appear in ascending version order, and ADR-0029 states in `Downstream Consumers` that ORCH-05 appends rather than re-sorts"
    - "ADR-0030 records the fourth milestone-numbering collision under the same convention that closed the first three, citing ADR-0010 and ADR-0014 explicitly and in that order as its two precedents (D-13)"
    - "ADR-0030 keeps the self-title collision and the Prerequisites mis-credit as two separately stated defects even though one document carries both"
    - "Each of the six mis-credited Prerequisites items is named individually with its `milestone-04-06.md` citation, so none is left as an unenumerated etcetera (D-13)"
    - "The Roadmap Extension Protocol's predicted fifth instance is recorded closed by ADR-0030 — run 5 found none — so no later phase inherits a standing prediction to check (D-14)"
    - "The Milestone 7 overview carries a dated banner naming ADR-0030, and every original word of its self-title line and its six Prerequisites bullets is retained (D-00c)"
  artifacts:
    - path: ".planning/decisions/0029-version-trajectory-history.md"
      provides: "The version trajectory as closed history, with a Trajectory table ORCH-05 extends"
      contains: "## Trajectory"
    - path: ".planning/decisions/0030-milestone-7-self-numbering.md"
      provides: "The fourth milestone-numbering collision, citing ADR-0010 and ADR-0014"
      contains: "ADR-0014"
    - path: ".project/Milestone_7-Production-Hardening/overview/Milestone_7-Tier_4-Production_Hardening.md"
      provides: "A dated supersession banner on the self-title and Prerequisites, original text retained"
      contains: "ADR-0030"
  key_links:
    - from: ".planning/decisions/0029-version-trajectory-history.md"
      to: ".planning/decisions/0008-workspace-version-0-7-0.md"
      via: "Context citing Phase 4's convergence as the landing point this ADR does not re-decide"
      pattern: "ADR-0008"
    - from: ".planning/decisions/0030-milestone-7-self-numbering.md"
      to: ".planning/ledgers/milestone-04-06.md"
      via: "Code Locations citing the ledger that already assigns the six mis-credited items to Milestones 4-6"
      pattern: "milestone-04-06\\.md"
  prohibitions:
    - "A closed requirement must not be reopened by an ADR that records its history. REL-01 is complete; writing ADR-0029 in a way that reads as a competing convergence, or that leaves REL-01's status ambiguous, is prohibited even though HARD-03's own text still describes REL-01 as downstream work."
    - "A `.project/` correction must not delete or rewrite the original text. The self-title and the six mis-credited Prerequisites are the evidence of the collision; a document quietly retitled to `Milestone 7` destroys the record the fourth ADR exists to make."
    - "A prediction must not be silently carried forward. The Roadmap Extension Protocol's expected fifth instance is closed; leaving it unstated so a later phase re-checks it is prohibited even though omission is cheaper than recording a negative."
---

<objective>
Write ADR-0029 (the version trajectory as closed history, with a table Phase 13 extends) and ADR-0030
(the fourth milestone-numbering collision, citing ADR-0010 and ADR-0014), and annotate the Milestone 7
overview that titles itself "Milestone 4" and credits Milestones 1-3 with Milestone 4-6 work.

Purpose: HARD-03's forward coupling has already fired — REL-01 converged on `0.7.0` in Phase 4 — so
this is backwards-looking confirmation, and a planner who reads it as a hand-off re-plans a closed
requirement. HARD-04 is the third application of a convention whose value is entirely in the
cross-reference: without citing 0010 and 0014, `REQ-*` provenance keys stop resolving uniformly across
four ledgers.
Output: two ADRs and one dated supersession banner.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-RESEARCH.md
</context>

## Artifacts this phase produces

| Artifact | Plan | Kind |
|---|---|---|
| `.planning/ledgers/milestone-07-08.md` | 10-01, 10-07…10-10, 10-11 | new file |
| `.planning/decisions/0028-m8-reconciliation-authoritative.md` | 10-02 | new ADR |
| `.planning/decisions/0029-version-trajectory-history.md` | **10-03** | new ADR |
| `.planning/decisions/0030-milestone-7-self-numbering.md` | **10-03** | new ADR |
| `.planning/decisions/0031-extracted-crate-dependency-rule.md` | 10-04 | new ADR |
| `.planning/decisions/0032-pdf-extraction-capability.md` | 10-05 | new ADR |
| `.planning/decisions/0033-cargo-doc-warning-bar.md` | 10-06 | new ADR |
| `.planning/REQUIREMENTS.md` | 10-01, 10-11 | modified |
| `.planning/ROADMAP.md` | 10-01, 10-11 | modified |
| `.planning/PROJECT.md` | 10-11 | modified |
| `.planning/decisions/PROMOTION.md` | 10-11 | modified (next free → 0034) |
| `.project/…/Epic_1/facade-audit.md` | 10-02 | annotated |
| `.project/…/Epic_3/infrastructure-adapter-disposition.md` | 10-02 | annotated |
| `.project/…/Epic_3/prd-relocate-remaining-misplaced-modules.md` | 10-02 | annotated |
| `.project/Milestone_7-Production-Hardening/overview/Milestone_7-Tier_4-Production_Hardening.md` | **10-03** | annotated |
| `.project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md` | 10-04, 10-05 | annotated |
| `.project/…/Epic_5/prd-document-facade-crate-role.md` | 10-06 | annotated |
| `.project/Milestone_7-Production-Hardening/Epic_4/epic-4-completion-summary.md` | 10-06 | annotated |
| `crates/paladin-content/Cargo.toml` | 10-05 | modified |
| `crates/paladin-content/CHANGELOG.md` | 10-05 | modified |
| `.cargo/audit.toml` | 10-05 | modified |
| `Makefile` | 10-06 | modified |

**No `.rs` file appears on this list, and none may be added to it (D-23).**

<tasks>

<task type="auto">
  <name>Task 1: Write ADR-0029 — the version trajectory as closed history, with a table ORCH-05 extends</name>
  <read_first>
    - `.planning/decisions/0027-dockerfile-chef-planner-stage.md` in full — the seven-heading, no-frontmatter shape, the `## Status` / `**Date:**` block, and the precedence-citing opening of `## Decision`.
    - `.planning/decisions/PROMOTION.md` lines 75-106 — the required heading set in exact order and the bullet-list rule on `## Code Locations` and `## Considered Options`.
    - `.planning/decisions/0008-workspace-version-0-7-0.md` in full — Phase 4's convergence on `0.7.0`. This ADR cites it as the landing point; it does not re-decide it.
    - `.planning/REQUIREMENTS.md` line 358 (REL-01's checkbox) and line 3913 (its traceability row) — confirm both by reading, and record what you observed.
    - `.planning/REQUIREMENTS.md` lines 1405-1420 — HARD-03 in full, including the current-state clause plan 10-01 corrected in place.
    - `.project/Milestone_7-Production-Hardening/Epic_4/prd-api-stabilization-pre-release-preparation.md` and `.project/Milestone_7-Production-Hardening/overview/` Appendix C — the lockstep `0.2.0` target that was anchored and never shipped.
    - `.project/Milestone_7-Production-Hardening/Epic_4/` release-readiness audit and post-release verification documents — every gate PASS, the GO sign-off, tag `v0.1.0-rc.1` at `a9530fc`, docs.rs verification of all ten crates including the `paladin-ai-core` / `paladin_core` package-lib split, and the external smoke project compiling against `paladin-ai = "0.1.0"`. Locate these by `ls` in that directory rather than guessing filenames.
    - `.planning/REQUIREMENTS.md` lines 4024-4040 — the cross-phase coupling table, which states the whichever-runs-second-applies rule ADR-0029 names the artefact for.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-00b, D-00e, D-10, D-11, D-12, D-24.
    - Run `git tag --sort=-v:refname | head -6`, `grep -n '^version' Cargo.toml`, and `git log -1 --format=%H%n%cs a9530fc` to establish the current and historical anchors from the tree this session.
  </read_first>
  <files>.planning/decisions/0029-version-trajectory-history.md</files>
  <action>
Write `.planning/decisions/0029-version-trajectory-history.md`, titled
`# ADR-0029: Version trajectory — `v0.1.0-rc.1` as closed history`, in the exact seven-heading order
with no frontmatter, matching ADR-0027's file shape. `## Status` body is `Accepted` plus a bold
`**Date:**` line.

`## Context` establishes three things with citations. First, what the documents anchored: M7 Epic 4's
PRD and the overview's Appendix C both anchored the first publishable release at a lockstep `0.2.0`.
Second, what actually shipped: all ten crates published at `0.1.0`, tagged `v0.1.0-rc.1` at commit
`a9530fc` on 2026-05-28, with a release-readiness audit recording every gate PASS, a GO sign-off, and
post-release verification confirming all ten crates resolve on docs.rs — including the package/lib
split visible as `docs.rs/paladin-ai-core/latest/paladin_core/` and `docs.rs/paladin-ai/latest/paladin/`
— plus an external smoke project compiling against `paladin-ai = "0.1.0"`. Third, and stated as the
sharper finding: **HARD-03's forward coupling has already fired.** REL-01 is `[x]` at
`REQUIREMENTS.md:358` with a `Phase 4 | Complete` traceability row at `:3913`, converged by Phase 4 on
`0.7.0` via ADR-0008 and commit `c2e20a1`. Record the current tree anchors you measured this session —
the root `Cargo.toml` version line, the three newest tags in descending order, and the branch — and
state explicitly that HARD-03's own current-state clause was two releases behind and was corrected at
source by plan 10-01.

`## Decision` opens by citing the D-00b precedence order verbatim, then states the one-sentence
verdict: the `v0.1.0-rc.1` trajectory is recorded as **closed history**, no rc.1 artefact is treated as
current state, and REL-01 is not re-opened by this ADR because Phase 4 already converged it. Then two
sub-decisions:

  (i) **REL-01's status is confirmed, not re-decided.** State that REL-01 did not converge on any rc.1
      figure and that a planner treating it as open will re-plan a closed requirement. Cite ADR-0008 as
      the decision that owns the landing at `0.7.0`.

  (ii) **This ADR is the single home for the whole trajectory.** ORCH-05 (Phase 13) extends it rather
      than writing a second version ADR; three ADRs for one unbroken line would guarantee the third
      contradicts the first.

Immediately after `## Decision`, add a `## Trajectory` section holding one table. Columns:
`Version / tag` · `Status in the tree` · `Date` · `What it was` · `Evidence`. Rows in **ascending
version order**, one row per version figure, keyed by the tag rather than by the milestone — so the
lockstep `0.2.0` target and `v0.1.0-rc.1` are two separate rows even though one milestone carried both.
Populate the rows this ADR owns: the lockstep `0.2.0` target (Status: `targeted, not shipped` — an
explicit cell, never blank, because the figure was anchored by two documents and no tag exists for it);
`0.1.0` / `v0.1.0-rc.1` (shipped, 2026-05-28, commit `a9530fc`); and `v0.2.0` (shipped — Milestone 8
targeted it throughout, and its Epic 7 written 2026-06-06 targets post-v0.5.1, so v0.3.0 through
v0.5.1 all shipped between Epic 5 and Epic 7). Add a `v0.3.0` … `v0.6.0` row marked
`owned by Phase 13 / ORCH-05 — to be appended`, so the gap is a labelled placeholder rather than an
omission, and a final `0.7.0` / `v0.7.0`, `v0.7.1` row citing ADR-0008 and the tags you measured.
Above the table, state the append rule in one sentence: rows are added in ascending version order and
existing rows are never re-sorted or re-keyed.

`## Considered Options` is a bullet list with `(accepted)`/`(rejected)` tags: one trajectory ADR that
later phases append to (accepted); a separate ADR per version range (rejected — three ADRs for one
unbroken line guarantee the third contradicts the first); re-opening REL-01 to converge with the whole
trajectory in view (rejected — REL-01 is `[x]` and Phase 4's ADR-0008 already owns the landing; HARD-03
confirms rather than converges); recording the trajectory only as a ledger row (rejected — the
`0.2.0`-versus-`0.1.0` disagreement is a contested position between two documents and the tree, which
D-00g routes to an ADR).

`## Code Locations` is a bullet list of exact `file:line` citations: the root `Cargo.toml` version
line; `REQUIREMENTS.md:358` and `:3913`; `.planning/decisions/0008-workspace-version-0-7-0.md`; the
Epic 4 PRD and overview Appendix C lines carrying the `0.2.0` target; the release-readiness audit and
post-release verification documents by path; and `REQUIREMENTS.md:4036`'s coupling rule.

`## Code Conformance` is the standalone word `conforms`.

`## Downstream Consumers` is a bullet list naming: **Phase 13 / ORCH-05** — the `## Trajectory` table
to append v0.3.0 through v0.6.0 rows to, in ascending order, without re-sorting or re-keying existing
rows, plus the note that REL-01 is already converged so ORCH-05 applies rather than re-decides;
**Phase 10 / HARD-01** — the ledger rows for `REQ-versioning-policy`, `REQ-release-readiness-audit` and
`REQ-changelog-v020-cut` that cite this ADR, written by plans 10-08 and 10-10.

Every version figure, tag and commit in this ADR must be one this task observed by running the command
it cites. Do not transcribe a tag list or a version from `10-CONTEXT.md` — it records a tree state at
commit `9550299` and the tree has moved since.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/decisions/0029-version-trajectory-history.md; test -f "$f" && head -1 "$f" | grep -q "^# ADR-0029" && ! head -1 "$f" | grep -q "^---" && echo "headings_ok=$(diff <(grep "^## " "$f" | grep -v "^## Trajectory") <(printf "## Status\n## Context\n## Decision\n## Considered Options\n## Code Locations\n## Code Conformance\n## Downstream Consumers\n") >/dev/null && echo yes || echo no)" && echo "trajectory=$(grep -c "^## Trajectory" "$f")" && echo "orch05=$(grep -c "ORCH-05" "$f")" && echo "rel01=$(grep -c "REL-01" "$f")" && echo "conforms=$(grep -cx "conforms" "$f")"'</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 .planning/decisions/0029-version-trajectory-history.md` starts with `# ADR-0029`, and `sed -n '1p'` does not match `^---`.
    - `grep '^## ' .planning/decisions/0029-version-trajectory-history.md | grep -v '^## Trajectory'` yields exactly the seven canonical headings in order.
    - `grep -c '^## Trajectory' .planning/decisions/0029-version-trajectory-history.md` returns `1`, and its table holds at least `5` data rows in ascending version order.
    - No cell in the `## Trajectory` table is empty: `grep -c '| *|' <the table region>` returns `0`, and the lockstep `0.2.0` row carries an explicit targeted-not-shipped status.
    - `grep -c 'ADR-0008' .planning/decisions/0029-version-trajectory-history.md` returns at least `1` and `grep -c 'REL-01' …` returns at least `2`.
    - `grep -c 'a9530fc' .planning/decisions/0029-version-trajectory-history.md` returns at least `1`.
    - `grep -c 'ORCH-05' .planning/decisions/0029-version-trajectory-history.md` returns at least `2` — named both in the trajectory placeholder row and in `## Downstream Consumers`.
    - `grep -cx 'conforms' .planning/decisions/0029-version-trajectory-history.md` returns `1`.
    - The version and tags recorded match this session's tree: `grep -n '^version' Cargo.toml` and `git tag --sort=-v:refname | head -3` reproduce the figures the ADR states.
    - `git status --porcelain -- '*.rs'` is empty.
  </acceptance_criteria>
  <done>ADR-0029 records the rc.1 trajectory as closed history, confirms REL-01 complete without re-opening it, and carries an ascending, no-blank-cell `## Trajectory` table with a labelled placeholder row that names ORCH-05 as its owner.</done>
</task>

<task type="auto">
  <name>Task 2: Write ADR-0030 — the fourth milestone-numbering collision, citing ADR-0010 and ADR-0014</name>
  <read_first>
    - `.planning/decisions/0010-milestone-3-epic-numbering.md` in full and `.planning/decisions/0014-milestone-4-6-tier-numbering.md` in full — the two precedents this ADR must cite by number, and the shape of the convention they state.
    - `.planning/decisions/0027-dockerfile-chef-planner-stage.md` — the current ADR file shape.
    - `.planning/decisions/PROMOTION.md` lines 75-106 — the heading set and the bullet-list rule.
    - `.project/Milestone_7-Production-Hardening/overview/Milestone_7-Tier_4-Production_Hardening.md` — line 1 (the self-title reading "Milestone 4: Production Hardening and Extended Workspace Decomposition") and the `### Prerequisites (Completed in Milestones 1–3)` section at approximately `:25`, plus each of its bullets. Re-derive line numbers.
    - `.planning/ledgers/milestone-04-06.md` — locate the rows for the six mis-credited items: feature flags and the CI matrix, the core workspace crates, `application_settings.rs` decomposition, manager-service relocation, Maneuver DSL co-location, and `CircuitBreaker` relocation. Record each row's `REQ-*` ID and its section.
    - `.planning/ROADMAP.md` lines 108-118 — the Roadmap Extension Protocol statement that the fifth instance was predicted and that run 5 found none.
    - `.planning/REQUIREMENTS.md` lines 1422-1432 — HARD-04 in full.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-00b, D-00g, D-13, D-14, D-24.
  </read_first>
  <files>.planning/decisions/0030-milestone-7-self-numbering.md</files>
  <action>
Write `.planning/decisions/0030-milestone-7-self-numbering.md`, titled
`# ADR-0030: Milestone 7's self-numbering collision — directory numbering is authoritative`, in the
exact seven-heading order with no frontmatter. `## Status` body is `Accepted` plus a bold `**Date:**`
line.

`## Context` states **two separate defects**, kept visually distinct even though one document carries
both:

  **Defect 1 — the self-title.** The overview at
  `.project/Milestone_7-Production-Hardening/overview/Milestone_7-Tier_4-Production_Hardening.md:1`
  titles itself "Milestone 4: Production Hardening and Extended Workspace Decomposition" while its own
  path reads `Milestone_7-Production-Hardening`. Quote the title line verbatim with its `file:line`.

  **Defect 2 — the Prerequisites mis-credit.** The `### Prerequisites (Completed in Milestones 1–3)`
  section credits Milestones 1-3 with work that directory numbering assigns to Milestones 4-6. Name
  **each of the six items individually** — feature flags and the CI matrix; the core workspace crates;
  `application_settings.rs` decomposition; manager-service relocation; Maneuver DSL co-location;
  `CircuitBreaker` relocation — and give each one its `milestone-04-06.md` row citation. Do not
  collapse any of them into an unenumerated etcetera; the enumeration is the evidence, and a reader
  checking one item must be able to resolve it without re-deriving the mapping.

`## Decision` opens by citing the D-00b precedence order verbatim, then states the verdict:
**directory and task-list numbering is authoritative**, so this document describes Milestone 7 and its
Prerequisites describe Milestones 4-6. State that this is the **third application** of the convention
and the **fourth instance** of the collision class, and cite **ADR-0010 first and ADR-0014 second**, in
that chronological precedent order, by number and by the milestone each covered. State why the
cross-reference is mandatory rather than decorative: it is what makes `REQ-*` provenance keys resolve
uniformly across four ledgers, and it is the whole reason the convention is worth an ADR each time
instead of a footnote.

Add a second sub-decision recording that the ADR **cites `milestone-04-06.md` rather than re-asserting
the mapping** — all six items are already ledgered there, and restating the assignment in a fifth place
is how a fifth version of it starts to diverge.

Add a third sub-decision closing the prediction: the Roadmap Extension Protocol predicted a fifth
instance in run 5; run 5 found none. Record the protocol item as **discharged with this fourth
instance**, so no later phase inherits a standing prediction to check. Cite the ROADMAP lines that
state it.

`## Considered Options` is a bullet list with `(accepted)`/`(rejected)` tags: annotate at source and
record the convention in a fourth ADR citing its two precedents (accepted); a footnote in the ledger
instead of an ADR (rejected — the cross-reference is what makes provenance keys resolve, and a footnote
is not citable by number); retitling the overview to match its directory (rejected — D-00c forbids
rewriting, and the self-title is the evidence of the collision); carrying the fifth-instance prediction
forward unclosed (rejected — run 5 found none, and an unclosed prediction costs every later phase a
check that has no possible finding).

`## Code Locations` is a bullet list of exact `file:line` citations: the overview's title line; the
Prerequisites heading and each of the six bullets; the six `milestone-04-06.md` rows;
`.planning/decisions/0010-milestone-3-epic-numbering.md` and
`.planning/decisions/0014-milestone-4-6-tier-numbering.md`; and the ROADMAP lines carrying the
prediction.

`## Code Conformance` is the standalone word `conforms`.

`## Downstream Consumers` names **Phase 10 / HARD-01** (the ledger's M7 epic sections, whose section
headings use directory numbering on this ADR's authority) and **Phase 13 / ORCH-05** (which records the
prediction closed and must not re-open it).
  </action>
  <verify>
    <automated>bash -c 'f=.planning/decisions/0030-milestone-7-self-numbering.md; test -f "$f" && head -1 "$f" | grep -q "^# ADR-0030" && ! head -1 "$f" | grep -q "^---" && diff <(grep "^## " "$f") <(printf "## Status\n## Context\n## Decision\n## Considered Options\n## Code Locations\n## Code Conformance\n## Downstream Consumers\n") && echo "adr0010=$(grep -c "ADR-0010" "$f") adr0014=$(grep -c "ADR-0014" "$f") ledger=$(grep -c "milestone-04-06" "$f") rejected=$(grep -c "(rejected)" "$f")"'</automated>
  </verify>
  <acceptance_criteria>
    - `diff <(grep '^## ' .planning/decisions/0030-milestone-7-self-numbering.md) <(printf '## Status\n## Context\n## Decision\n## Considered Options\n## Code Locations\n## Code Conformance\n## Downstream Consumers\n')` produces no output, and `sed -n '1p'` does not match `^---`.
    - `grep -c 'ADR-0010' .planning/decisions/0030-milestone-7-self-numbering.md` returns at least `1` and `grep -c 'ADR-0014' …` returns at least `1`; the first occurrence of `ADR-0010` precedes the first occurrence of `ADR-0014` in the file, verified with `grep -n`.
    - `grep -c 'milestone-04-06' .planning/decisions/0030-milestone-7-self-numbering.md` returns at least `6` — each of the six mis-credited items carries its own ledger citation rather than sharing one.
    - `grep -ci 'application_settings\|CircuitBreaker\|Maneuver' .planning/decisions/0030-milestone-7-self-numbering.md` returns at least `3` — the six items are named individually.
    - `grep -ci 'fifth' .planning/decisions/0030-milestone-7-self-numbering.md` returns at least `1` and the sentence records the prediction discharged.
    - `grep -c '(rejected)' .planning/decisions/0030-milestone-7-self-numbering.md` returns at least `3`.
    - `grep -cx 'conforms' .planning/decisions/0030-milestone-7-self-numbering.md` returns `1`.
    - `git status --porcelain -- '*.rs'` is empty.
  </acceptance_criteria>
  <done>ADR-0030 states the self-title and the Prerequisites mis-credit as two separate defects, names all six mis-credited items with individual ledger citations, cites ADR-0010 before ADR-0014, and records the fifth-instance prediction discharged.</done>
</task>

<task type="auto">
  <name>Task 3: Annotate the Milestone 7 overview's self-title and Prerequisites at source</name>
  <read_first>
    - `.project/Milestone_7-Production-Hardening/overview/Milestone_7-Tier_4-Production_Hardening.md` in full — the title line at `:1`, the `### Prerequisites (Completed in Milestones 1–3)` heading at approximately `:25`, and each bullet beneath it. Re-derive every line number.
    - `.planning/decisions/0030-milestone-7-self-numbering.md` (post-task-2) — so the banner and the ADR make the same claim in the same words.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md` → the `.project/` annotation banners section: **Shape B** (compact blockquote naming an ADR) for the document-level supersession, and **Shape A** (inline strike-and-correct) for the Prerequisites heading and its bullets.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-00c, D-13.
    - Run `git show 74a05fe -- '*.md' | head -60` and `git show 94814ff -- '*.md' | head -60` to read both live precedent banners as committed.
  </read_first>
  <files>.project/Milestone_7-Production-Hardening/overview/Milestone_7-Tier_4-Production_Hardening.md</files>
  <action>
Apply two annotations to this one document, both dated 2026-08-08 and both naming ADR-0030 by relative
path.

1. **A Shape B banner immediately beneath the title line**, opening with a bold
   `TITLE AND PREREQUISITES SUPERSEDED BY [ADR-0030](<relative path>) — 2026-08-08.` line. Its body
   states: this document's own title reads "Milestone 4" while its path is
   `Milestone_7-Production-Hardening`; directory and task-list numbering is authoritative under
   ADR-0030, which cites ADR-0010 and ADR-0014 as its two precedents; and this is the fourth instance
   of the collision class in this corpus. Close with the sentence that the original title and
   Prerequisites text below are retained unmodified except for the inline correction markers described
   in the banner.

   Do **not** edit the title line itself. The self-title is the evidence.

2. **A Shape A inline correction on the Prerequisites section.** Strike the section heading's
   parenthetical with `~~` markers, preserving its wording, and follow the heading with a
   `**Corrected (dated 2026-08-08, HARD-04):**` paragraph stating that the six items below were
   delivered by Milestones 4-6 under directory numbering, and that each is ledgered in
   `.planning/ledgers/milestone-04-06.md`. Then, for each of the six bullets — feature flags and the CI
   matrix; the core workspace crates; `application_settings.rs` decomposition; manager-service
   relocation; Maneuver DSL co-location; `CircuitBreaker` relocation — append an inline
   `**Corrected:**` clause naming the milestone that actually delivered it and the
   `milestone-04-06.md` row that records it. Strike nothing inside the bullets themselves; the credit
   is in the heading, so the heading is where the strike belongs.

   If the section's bullets do not map one-to-one onto the six items — read them and check rather than
   assume — annotate the ones that do, and record in the SUMMARY exactly which bullets you found, how
   many, and which of the six each corresponds to. Do not invent a bullet to reach six.

Every original word is retained. `git diff --numstat` on this file must show at most one deleted line
per struck element, and no bullet may be removed or reordered.
  </action>
  <verify>
    <automated>bash -c 'f=".project/Milestone_7-Production-Hardening/overview/Milestone_7-Tier_4-Production_Hardening.md"; echo "banner=$(grep -c "SUPERSEDED BY" "$f")"; echo "adr=$(grep -c "ADR-0030" "$f")"; echo "corrections=$(grep -c "Corrected" "$f")"; echo "title=$(sed -n "1p" "$f")"; echo "numstat=$(git diff --numstat -- "$f")"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SUPERSEDED BY' .project/Milestone_7-Production-Hardening/overview/Milestone_7-Tier_4-Production_Hardening.md` returns exactly `1` — one banner, never two stacked.
    - `sed -n '1p' .project/Milestone_7-Production-Hardening/overview/Milestone_7-Tier_4-Production_Hardening.md` still reads `# Milestone 4: Production Hardening and Extended Workspace Decomposition` — the self-title is untouched evidence.
    - `grep -c 'ADR-0030' <that file>` returns at least `2` — the banner and the Prerequisites correction each name it — and the relative link resolves to an existing file.
    - `grep -c 'Corrected (dated 2026-08-08, HARD-04)' <that file>` returns exactly `1`.
    - `grep -c 'milestone-04-06' <that file>` returns at least `1`.
    - `git diff --numstat -- <that file>` shows at most `2` deleted lines, and the SUMMARY records the exact bullet inventory found under the Prerequisites heading.
    - `git status --porcelain -- '*.rs' Cargo.toml Makefile .cargo/audit.toml` is empty.
  </acceptance_criteria>
  <done>The overview opens with one dated banner naming ADR-0030 and its two precedents, its self-title survives verbatim as the evidence, and the Prerequisites section carries a dated correction mapping each item it found to the milestone that delivered it and the ledger row recording it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| historical release artefacts (`.project/` audits, tags, docs.rs) → an ADR at top precedence | Version figures quoted in three documents disagree with each other and with the tree; whichever is transcribed becomes the record. |
| a closed requirement's status → a new ADR describing the same subject | ADR-0029 covers the ground REL-01 already converged; a careless framing re-opens a completed requirement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-09 | Repudiation | ADR-0029's version and tag figures | high | mitigate | `10-CONTEXT.md` records a tree state at commit `9550299`; HEAD has moved. The task forbids transcribing a version or tag list without re-running `grep -n '^version' Cargo.toml` and `git tag --sort=-v:refname`, and an acceptance criterion re-runs both and compares. |
| T-10-10 | Tampering | REL-01's completed status | high | mitigate | An ADR that reads as a competing convergence would silently re-open a `[x]` requirement and cost Phase 13 a re-plan. The ADR states REL-01 complete with both its citations, lists re-opening it as an explicitly rejected option, and the plan's prohibitions forbid ambiguous framing. |
| T-10-11 | Tampering | the overview's self-title | medium | mitigate | Retitling the document to match its directory would be the intuitive fix and would destroy the evidence the fourth ADR exists to record. The acceptance criterion asserts line 1 is byte-unchanged. |
| T-10-12 | Information disclosure | the six mis-credited Prerequisites | low | accept | The correction retains the mis-credit by design. Nothing sensitive; the retained text is the evidence. |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing and modifies no manifest. `10-RESEARCH.md`'s Package Legitimacy Audit records the phase as not-applicable; `git status --porcelain -- Cargo.toml Cargo.lock` is empty for this plan. |
</threat_model>

<verification>
- ADR-0029 and ADR-0030 each carry the seven canonical headings in order, no frontmatter, and `## Code Conformance` reading `conforms`.
- ADR-0029's `## Trajectory` table is ascending, has no empty cell, carries an explicit targeted-not-shipped row for the lockstep `0.2.0` figure and a labelled ORCH-05 placeholder row.
- The version and tags in ADR-0029 reproduce against `grep -n '^version' Cargo.toml` and `git tag --sort=-v:refname | head -3` run at verification time.
- ADR-0030 cites ADR-0010 before ADR-0014 and gives all six mis-credited items individual `milestone-04-06.md` citations.
- The overview carries exactly one supersession banner; its line 1 is byte-unchanged.
- `git status --porcelain -- '*.rs'` → empty. D-23's boundary holds for this plan.
</verification>

<success_criteria>
A developer reading the version record learns what shipped and when, sees `v0.1.0-rc.1` dated and
closed, finds REL-01 recorded complete rather than pending, and finds one table with a named owner for
the rows it does not yet have. A developer landing on the Milestone 7 overview sees on the first screen
that it is Milestone 7, that the ADR recording this cites the two earlier applications of the same
convention, and that the prediction of a fifth instance is closed — while every original word of the
mis-numbering survives as the evidence.
</success_criteria>

<output>
Create `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-03-SUMMARY.md` when done.
</output>
