---
phase: 10-milestone-7-8-ground-truth-recorded-account
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/decisions/0031-extracted-crate-dependency-rule.md
  - .project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md
autonomous: false
requirements: [HARD-05]

must_haves:
  truths:
    - "The extracted-crate dependency rule reads the same way as the tree behaves: ADR-0031 states it as a default-build invariant — no extracted crate may depend on another extracted crate or on the facade in its default build; a non-default optional feature may declare such an edge only where the facade opts in explicitly and the dependent code is `cfg`-gated (D-15)"
    - "The invariant is checkable by a command, not merely asserted: ADR-0031 names `cargo tree --no-default-features` per crate as the mechanism and hands enforcement to Phase 15 alongside ADR-0015's queued allowlist check (D-15)"
    - "ADR-0031 leads with the measured fact that makes the restatement a description rather than an amnesty: the default build of `paladin-content` carries no leaf-to-leaf edge at all, because `llm` is not in any default feature set and gates exactly one `cfg`-guarded module"
    - "ADR-0031 records that the anchor moved deliberately — the enforceable invariant is promoted from the absolute no-edge-at-all form to the default-build form, with the absolute form retained as the special case of a crate with no non-default features — so a later reader sees a promotion rather than an exception added alongside a rule"
    - "ADR-0031 cites `cost-benefit-assessment.md:118` for the `paladin-llm` complexity note, not `prd-extract-infrastructure-crates.md` §4.4, because the §4.4 citation carried by both CONTEXT.md and `REQUIREMENTS.md:1438` does not resolve"
    - "ADR-0031 records that an ADR is the promotion — the position is restated inside `.planning/decisions/`, which sits at the top of the precedence order by construction — and that re-tagging the PRD via `--manifest` and re-running ingest is not attempted, there being no run 6 (Phase 7 D-11)"
    - "ADR-0031 names Phase 11 / FACADE-02 and Phase 15 in `Downstream Consumers`, so FACADE-02's D2/D3/D4 relocation targets have a numbered decision to plan against (D-15)"
    - "M7 Epic 1 PRD §6.1 and Goal 2 each carry a dated inline correction naming ADR-0031, with the original absolute wording retained struck rather than deleted (D-16)"
    - "Applying the annotation twice would stack two banners; this plan asserts that `prd-extract-infrastructure-crates.md` carries exactly one head-level correction blockquote written by it, and exactly two struck clauses"
    - "`prd-extract-infrastructure-crates.md` is edited by this plan (§6.1, Goal 2) and by plan 10-05 (§4.4.1, §4.4.6) in a later wave over a disjoint clause set; 10-05 re-derives its line numbers because this plan's insertions move them"
    - statement: "`cargo tree -p paladin-content --no-default-features` shows no `paladin-llm` edge, confirming the invariant ADR-0031 states is true of the tree today. The command needs no network if the lockfile's crates are cached; the executor attempts it and records the exact output. If it cannot resolve offline, ADR-0031 scopes the check as CI-only and states the manifest-level evidence it rests on instead, never inferring a pass."
      verification: backstop
  artifacts:
    - path: ".planning/decisions/0031-extracted-crate-dependency-rule.md"
      provides: "The dependency rule restated as a default-build invariant, in ADR-0015's multi-part shape, with the enforcement mechanism named and handed to Phase 15"
      contains: "--no-default-features"
    - path: ".project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md"
      provides: "Dated inline corrections on §6.1 and Goal 2 naming ADR-0031, original absolute wording retained"
      contains: "ADR-0031"
  key_links:
    - from: ".planning/decisions/0031-extracted-crate-dependency-rule.md"
      to: ".planning/decisions/0015-core-ports-dependency-allowlist.md"
      via: "Decision section copying ADR-0015's separate-the-invariant-from-the-list shape and joining its Phase 15 enforcement queue"
      pattern: "ADR-0015"
    - from: ".planning/decisions/0031-extracted-crate-dependency-rule.md"
      to: "crates/paladin-content/Cargo.toml"
      via: "Code Locations citing the non-default `llm` feature, the optional `paladin-llm` dependency, and the facade's explicit opt-in"
      pattern: "paladin-content/Cargo\\.toml"
  prohibitions:
    - "A citation must not be copied forward without being re-read. `REQUIREMENTS.md:1438` and CONTEXT.md D-15 both attribute the `paladin-llm` complexity note to a §4.4 that does not contain it; reproducing that citation inside an ADR — where it sits at top precedence and becomes the thing later phases trust — is prohibited."
    - "The restated rule must not read as an amnesty for a violation. The default build carries no leaf-to-leaf edge at all; framing ADR-0031 as permitting an existing breach, rather than as describing what the tree does, is prohibited because it would license the next crate to declare a default-build edge."
    - "The three-file config boundary must not be widened silently. A plan that finds it needs a `.rs` edit must stop and say so rather than absorb the scope — D-23 is the boundary and an unannounced `.rs` change in a ground-truth phase is prohibited."
---

<objective>
Take the three flagged branch decisions at one blocking checkpoint, then write ADR-0031 restating the
extracted-crate dependency rule as a default-build invariant and annotate M7 Epic 1 PRD §6.1 and Goal 2
at source.

Purpose: this is the answer Phase 11 plans FACADE-02's D2/D3/D4 relocation targets against, and it is
one of only two decisions in this phase a human was never asked to confirm. The checkpoint also
resolves the `pdf` feature branch that plan 10-05 executes and the `cargo doc` branch that plan 10-06
records, so all three flagged choices are settled once, before any of them lands.
Output: three recorded branch selections, ADR-0031, and two dated inline corrections.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-RESEARCH.md
</context>

## Artifacts this phase produces

| Artifact | Plan | Kind |
|---|---|---|
| `.planning/ledgers/milestone-07-08.md` | 10-01, 10-07…10-10, 10-11 | new file |
| `.planning/decisions/0028-m8-reconciliation-authoritative.md` | 10-02 | new ADR |
| `.planning/decisions/0029-version-trajectory-history.md` | 10-03 | new ADR |
| `.planning/decisions/0030-milestone-7-self-numbering.md` | 10-03 | new ADR |
| `.planning/decisions/0031-extracted-crate-dependency-rule.md` | **10-04** | new ADR |
| `.planning/decisions/0032-pdf-extraction-capability.md` | 10-05 | new ADR |
| `.planning/decisions/0033-cargo-doc-warning-bar.md` | 10-06 | new ADR |
| `.planning/REQUIREMENTS.md` | 10-01, 10-11 | modified |
| `.planning/ROADMAP.md` | 10-01, 10-11 | modified |
| `.planning/PROJECT.md` | 10-11 | modified |
| `.planning/decisions/PROMOTION.md` | 10-11 | modified (next free → 0034) |
| `.project/…/Epic_1/facade-audit.md` | 10-02 | annotated |
| `.project/…/Epic_3/infrastructure-adapter-disposition.md` | 10-02 | annotated |
| `.project/…/Epic_3/prd-relocate-remaining-misplaced-modules.md` | 10-02 | annotated |
| `.project/Milestone_7-Production-Hardening/overview/Milestone_7-Tier_4-Production_Hardening.md` | 10-03 | annotated |
| `.project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md` | **10-04** (§6.1, Goal 2), 10-05 (§4.4.1, §4.4.6) | annotated |
| `.project/…/Epic_5/prd-document-facade-crate-role.md` | 10-06 | annotated |
| `.project/Milestone_7-Production-Hardening/Epic_4/epic-4-completion-summary.md` | 10-06 | annotated |
| `crates/paladin-content/Cargo.toml` | 10-05 | modified |
| `crates/paladin-content/CHANGELOG.md` | 10-05 | modified |
| `.cargo/audit.toml` | 10-05 | modified |
| `Makefile` | 10-06 | modified |

**No `.rs` file appears on this list.** If the checkpoint selects branch (b) on question 3, that
changes — and the checkpoint's own resolution instructions say what to do about it rather than letting
a plan absorb the scope silently (D-23).

## The `cargo doc` finding this plan's checkpoint exists to resolve

`10-CONTEXT.md` D-20 states that a zero-warning `cargo doc --workspace --no-deps` gate ships today and
that the tree already enforces it. `10-RESEARCH.md` ran the exact CI command from `.github/workflows/ci.yml`
against HEAD twice: **it exits 1.** The tree produces **20 rustdoc warnings** across four crates.
Re-measured by the planner at commit `11e9bdb`, still exiting 1, with the same four-crate split:
`paladin-web` 13, `paladin-battalion` 3, `paladin-ai` 3, `paladin-herald` 1 (24 lines match
`^warning:`; four of them are the per-crate summary lines).

D-20's premise describes the CI **configuration text**, not the **build state**. This collides with
D-23, which fixes this phase's boundary at three config files and no `.rs` file — clearing 20 rustdoc
warnings means editing doc comments in `.rs` files. The checkpoint below puts that choice in front of a
human rather than letting the plan pick silently.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Blocking decision — the three flagged branches (D-15, D-18, and the `cargo doc` boundary)</name>
  <decision>
Three branch selections this phase cannot make on its own authority. All three were auto-resolved to
their recommended option during `/gsd-discuss-phase --auto` and **none was confirmed by a human**; two
are flagged ⚠ HUMAN REVIEW in `10-CONTEXT.md` and the third was discovered after CONTEXT.md was
written.

**Question 1 (D-15 — ⚠ HUMAN REVIEW, `costly`): what is the extracted-crate dependency rule?**
This is the answer Phase 11 plans FACADE-02's D2/D3/D4 relocation targets against.

**Question 2 (D-18 — ⚠ HUMAN REVIEW, `costly`): what happens to the inert `pdf` feature?**
Executed by plan 10-05, on a published crate's public feature surface.

**Question 3 (new, from `10-RESEARCH.md`): does this phase ratify the `cargo doc` bar and record the
20-warning debt, or widen D-23 to clear the warnings?**
Recorded by plan 10-06.
  </decision>
  <context>
**Question 1.** M7 Epic 1 PRD §6.1 (`prd-extract-infrastructure-crates.md:257`) states the rule
absolutely: no extracted crate may depend on another extracted crate or on the facade. Goal 2 (`:30`)
restricts each new crate to `paladin-core`, `paladin-ports` and workspace-shared dependencies. The tree
carries one edge that touches the absolute form: `crates/paladin-content/Cargo.toml:23` declares
`llm = ["dep:paladin-llm"]`, `:28` declares the dependency `optional = true`, `src/services/mod.rs:7`
`cfg`-gates exactly one module behind it, and the facade's `content-processing` feature at root
`Cargo.toml:275` opts in explicitly. **The default build of `paladin-content` therefore carries no
leaf-to-leaf edge at all.** Undo cost of getting this wrong: Phase 11 re-plans three relocations and
removes a shipped, facade-exposed capability.

**Question 2.** `crates/paladin-content/Cargo.toml:18` declares `pdf = []`. `:41` declares
`pdf-extract` unconditionally, with no `optional = true`. A workspace search for a `pdf` feature gate
under `crates/paladin-content/src/` returns zero matches, and `adapters/document/mod.rs` declares and
re-exports `PdfExtractor` unconditionally, held as a plain struct field in `document_adapter.rs` and
called at two sites. So the feature gates nothing in either direction — while `news-api = []` in the
same manifest legitimately gates a dependency-free module, proving an empty feature is not itself the
defect. Undo cost: a published feature name, restorable in one line but visible to every consumer of
`paladin-content 0.1.0`+.

**Question 3.** `.github/workflows/ci.yml` runs `cargo doc --workspace --no-deps`, pipes it to a file,
and fails the required `lint` job if the output contains a warning. The command was run against HEAD
during planning: **exit 1, 20 warnings, four crates** (`paladin-web` 13, `paladin-battalion` 3,
`paladin-ai` 3, `paladin-herald` 1). Six of those twenty are the same six Phase 7's own
`milestone-04-06.md` recorded on 2026-08-06; fourteen are newer. Clearing them means editing `.rs` doc
comments, which D-23 places outside this phase. Undo cost: low for the record either way — but branch
(b) turns a record-writing phase into a code-change phase with its own review surface.
  </context>
  <options>
    <option id="q1-restate">
      <name>Q1 (recommended): restate the rule as a default-build invariant, ADR-0031</name>
      <pros>The invariant that has teeth is the default-build one, and it is checkable with `cargo tree --no-default-features` — the same Phase 15 mechanism ADR-0015 is already waiting on. It describes what the tree does rather than excusing what it does. Milestone 8's reconciliation kept this edge while deleting ~10,250 LOC of everything else, so it was looked at and left. Keeps the phase inside D-23.</pros>
      <cons>The rule's absolute form is weakened in wording, and a future crate could read the exception as broader than it is unless the `cfg`-gating and explicit-facade-opt-in conditions are stated tightly.</cons>
    </option>
    <option id="q1-remove">
      <name>Q1: keep "never" and remove the `paladin-content` → `paladin-llm` edge</name>
      <pros>The rule stays absolute and needs no conditions. No future reader can widen an exception that does not exist.</pros>
      <cons>Not a record change — architecture work. It means deleting a shipped, facade-exposed capability or inverting it through a port: real design, real `.rs` churn, outside a ground-truth phase's boundary. `10-CONTEXT.md` records it as deferred to its own phase if selected.</cons>
    </option>
    <option id="q2-delete">
      <name>Q2 (recommended): delete the inert `pdf = []` line</name>
      <pros>Makes the manifest tell the truth in one line. Cannot change any build's behaviour except `--features pdf`, which today enables nothing. Retires exactly the defect class this milestone close-out exists to retire.</pros>
      <cons>`cargo build -p paladin-content --features pdf` begins to fail where it previously succeeded and did nothing — a minor public-contract change on a pre-1.0 crate family, which must be recorded in `crates/paladin-content/CHANGELOG.md`.</cons>
    </option>
    <option id="q2-keep">
      <name>Q2: keep `pdf = []` and add it to the facade's `content-processing` list</name>
      <pros>Literal compliance with M7 Epic 1 §4.4.6, and no public-contract change at all. Also closes HARD-06.</pros>
      <cons>Leaves a manifest that lies quietly — a declared feature that gates nothing, recorded in the ADR as a documentation marker. That is the defect class this close-out exists to retire.</cons>
    </option>
    <option id="q2-wire">
      <name>Q2: wire `pdf` to gate `pdf-extract`</name>
      <pros>Makes the feature mean what §4.4.1 says it means.</pros>
      <cons>Requires `cfg`-gating a struct field, its constructor and two call sites — a `.rs` change outside D-23 — and turns PDF extraction into an opt-out capability for every existing consumer of the published crate.</cons>
    </option>
    <option id="q3-ratify">
      <name>Q3 (recommended): ADR-0033 ratifies the bar and records the 20-warning debt with a named owner</name>
      <pros>Keeps a ground-truth phase writing ground truth. The bar is honest rather than aspirational: one bar, ratified, plus a dated, counted, per-crate debt entry naming Phase 16 / DOCS-03 as owner. D-23's boundary holds and no `.rs` file is touched.</pros>
      <cons>The gate stays red after this phase. Someone reading only ADR-0033's headline could mistake ratification for compliance unless the measured state is stated in the same breath.</cons>
    </option>
    <option id="q3-widen">
      <name>Q3: widen D-23 for this one item and clear the 20 warnings in this phase</name>
      <pros>The gate goes green and the record and the tree agree completely, which is what ROADMAP criteria 5 and 6 ask for.</pros>
      <cons>A 20-warning sweep across four crates is code work with its own review surface: broken intra-doc links, `redundant_explicit_link` and `private_intra_doc_links` lints in `paladin-web`, the facade's web adapters, `paladin-battalion` and `paladin-herald`. It needs an added plan, the full CLAUDE.md workspace gate (`cargo test` → `cargo fmt --check` → `cargo clippy -- -D warnings`), and it converts this phase's `.rs` change count from zero to non-zero, which every other plan's acceptance criteria currently assert against.</cons>
    </option>
  </options>
  <resume-signal>
Answer all three, for example: `q1-restate, q2-delete, q3-ratify` (the recommended set), or name the
alternatives you want. Record the selections and the reasoning in `10-04-SUMMARY.md`; plans 10-05 and
10-06 read that SUMMARY before executing questions 2 and 3.

If **q1-remove** is selected: stop this plan after recording the answer. Removing the edge is
architecture work with its own phase, and ADR-0031 must then be written against the absolute rule with
the removal named as outstanding work rather than executed here.

If **q3-widen** is selected: this phase's `.rs`-change count stops being zero. Say so explicitly in the
SUMMARY, and flag that plans 10-01 through 10-11 each carry a `git status --porcelain -- '*.rs'` empty
assertion that must be relaxed for the added plan only, plus that the added plan runs the full CLAUDE.md
gate and re-confirms ADR-0006's 84% coverage floor.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write ADR-0031 — the extracted-crate dependency rule as a default-build invariant</name>
  <reversibility rating="costly">Phase 11 chooses FACADE-02's D2/D3/D4 relocation targets against this answer; reversing it means re-planning three relocations and removing a shipped facade capability (D-15).</reversibility>
  <precondition>Task 1's checkpoint has been answered and the selected branch for question 1 is recorded in `10-04-SUMMARY.md`. If the answer was `q1-remove`, this task does not run in its recommended form — write the ADR against the absolute rule and record the edge removal as outstanding architecture work with its own phase.</precondition>
  <read_first>
    - `.planning/decisions/0015-core-ports-dependency-allowlist.md` in full — the explicit structural model. Lines 38-92 hold the multi-part `## Decision` shape ADR-0031 must imitate: the enforceable invariant stated independent of any fact list, then the measured current state accepted as baseline. Lines 122-129 hold the `## Downstream Consumers` shape and the Phase 15 enforcement hand-off this ADR joins.
    - `.planning/decisions/0027-dockerfile-chef-planner-stage.md` — the current ADR file shape and tone.
    - `.planning/decisions/PROMOTION.md` lines 75-106 — the heading set in exact order and the bullet-list rule on `## Code Locations` and `## Considered Options`.
    - `crates/paladin-content/Cargo.toml` lines 14-45 in full — the `[features]` block, the `llm` feature, the optional `paladin-llm` dependency and its version pin. Re-derive every line number.
    - `crates/paladin-content/src/services/mod.rs` and `crates/paladin-content/src/services/content_llm_analysis_service.rs` — the `cfg` gate and the single consumer.
    - Root `Cargo.toml` around line 275 — the facade's `content-processing` feature list and its explicit `paladin-content/llm` opt-in.
    - `.project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md` lines 30 (Goal 2) and 257 (§6.1) — the two absolute statements, quoted verbatim.
    - `.project/Milestone_7-Production-Hardening/Epic_1/cost-benefit-assessment.md` line 118 — the **actual** home of the `paladin-llm` complexity note, inside the `paladin-content` row's extraction-complexity cell. Read it and quote it; do not cite §4.4 of the PRD, which does not contain it.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-RESEARCH.md` → Common Pitfall 3 in full.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-00b, D-00g, D-15, D-16, D-23, D-24.
    - `.planning/phases/07-workspace-ground-truth-recorded-answers/07-CONTEXT.md` → its D-11, the ADR-is-the-promotion rule.
    - `10-04-SUMMARY.md`'s recorded checkpoint answers.
  </read_first>
  <files>.planning/decisions/0031-extracted-crate-dependency-rule.md</files>
  <action>
Write `.planning/decisions/0031-extracted-crate-dependency-rule.md`, titled
`# ADR-0031: The extracted-crate dependency rule as a default-build invariant`, in the exact seven-heading
order with no frontmatter. `## Status` is `Accepted` plus a bold `**Date:**` line.

`## Context` **leads with the measured fact**, not with the PRD's wording: the default build of
`paladin-content` carries no leaf-to-leaf edge at all. Establish it with four citations you re-read
this session — the non-default `llm` feature declaration, the `optional = true` dependency line with
its actual version pin, the single `cfg`-gated module and its one consumer, and the facade's explicit
opt-in one level up. Note that the version pin in the manifest is `0.7.0`, not the `0.6.0`
`REQUIREMENTS.md`'s HARD-05 text quotes, and that Phase 4's commit `c2e20a1` converged it. Only then
state the absolute form the PRD carries at §6.1 and Goal 2, quoted verbatim with their `file:line`.
Close `## Context` by recording that the PRD's sibling document anticipated this exact case without
amending the rule — quote the extraction-complexity sentence and cite
`cost-benefit-assessment.md:118`. State explicitly that both `10-CONTEXT.md` D-15 and
`REQUIREMENTS.md:1438` attribute that sentence to the PRD's §4.4, that the citation does not resolve,
and that this ADR uses the correct one.

`## Decision` opens by citing the D-00b precedence order verbatim, then states the restated rule as a
single quotable sentence: *no extracted crate may depend on another extracted crate or on the facade
in its default build; a non-default optional feature may declare such an edge only where the facade
opts in explicitly and the dependent code is `cfg`-gated.* Then three sub-decisions in ADR-0015's
multi-part shape, kept visually separate:

  (i) **The enforceable invariant**, stated independent of any specific crate or fact list, exactly as
      ADR-0015 separates its invariant from its allowlist. Add the mechanism in the same breath:
      `cargo tree --no-default-features` on each extracted crate, showing no other extracted crate and
      no facade in the resulting tree.

  (ii) **The measured current state accepted as the baseline**: `paladin-content`'s `llm` feature and
      its single `cfg`-gated module, satisfying the invariant because the feature is non-default and
      the opt-in is explicit and lives one level up in the facade.

  (iii) **The anchor moved deliberately.** Record this in its own sub-decision so a later reader sees a
      promotion rather than an exception bolted onto a rule: the enforceable invariant is promoted from
      "no leaf-to-leaf edge at all" to "no leaf-to-leaf edge **in the default build**"; the absolute
      form is not abandoned but becomes the special case of a crate with no non-default features. State
      that the general form was chosen as primary because it is the one a command can check, and that
      the previous absolute form is recoverable from it, not contradicted by it.

Add a fourth paragraph inside `## Decision` recording that **an ADR is the promotion**. HARD-05's own
text proposes re-tagging `prd-extract-infrastructure-crates.md` via `--manifest` and re-running ingest;
that is not done. Phase 7's D-11 settled the pattern — an ADR restates the position inside
`.planning/decisions/`, which sits at the top of the precedence order by construction, and cites the
PRD as provenance — and `STATE.md` records that there is no ingest run 6. Say both.

`## Considered Options` is a bullet list with `(accepted)`/`(rejected)` tags: restate as a default-build
invariant with the `cfg`-gate and explicit-opt-in conditions (accepted); keep the absolute "never" and
remove the `paladin-content` → `paladin-llm` edge (rejected — not a record change but architecture
work, requiring either deletion of a shipped facade-exposed capability or inversion through a port,
outside D-23; record it as deferred to its own phase); re-tag the PRD via `--manifest` and re-run
ingest to promote it above PRD precedence (rejected — no run 6 exists, and re-typing a `.project/` file
changes how five completed ingest runs classified their corpus for an outcome an ADR achieves
natively); leave the rule stated in two contradictory forms (rejected — HARD-05 exists because it is,
and FACADE-02 cannot plan D2/D3/D4 against an ambiguous invariant).

`## Code Locations` is a bullet list of exact `file:line` citations: the four `paladin-content` and
facade manifest/source lines; `prd-extract-infrastructure-crates.md:30` and `:257`;
`cost-benefit-assessment.md:118`; `.planning/decisions/0015-core-ports-dependency-allowlist.md`; and
the `REQUIREMENTS.md` line carrying HARD-05's mis-citation, noted as corrected in place by plan 10-01.

Attempt `cargo tree -p paladin-content --no-default-features` and record its exact output in
`## Code Locations` as the invariant's check. If it cannot resolve offline in this environment, say so
plainly, scope the check as CI-only, and state the manifest-level evidence the ADR rests on instead.
Do not infer a pass.

`## Code Conformance` is the standalone word `conforms` — the tree already satisfies the restated
invariant; this ADR instructs no code change.

`## Downstream Consumers` is a bullet list naming: **Phase 11 / FACADE-02** — D2/D3/D4's relocation
targets are chosen against this rule, and a leaf-to-leaf target is legal only under the stated
conditions; **Phase 15** — the `cargo tree --no-default-features` check joins the ADR-0015 allowlist
check already queued there, as a second clause of the same mechanism; **Phase 10 / HARD-01** — the
ledger row for `REQ-extracted-crate-dependency-rule`, currently reading `Code diverges → HARD-05`,
which plan 10-07 upgrades to `satisfied` citing this ADR. State explicitly, for that row's benefit,
that the divergence was in the rule's **wording** and not in the code, so the verdict flip is
explained rather than bare.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/decisions/0031-extracted-crate-dependency-rule.md; test -f "$f" && head -1 "$f" | grep -q "^# ADR-0031" && ! head -1 "$f" | grep -q "^---" && diff <(grep "^## " "$f") <(printf "## Status\n## Context\n## Decision\n## Considered Options\n## Code Locations\n## Code Conformance\n## Downstream Consumers\n") && echo "cbassess=$(grep -c "cost-benefit-assessment" "$f") tree=$(grep -c "no-default-features" "$f") adr15=$(grep -c "ADR-0015" "$f") facade02=$(grep -c "FACADE-02" "$f") rejected=$(grep -c "(rejected)" "$f")"'</automated>
  </verify>
  <acceptance_criteria>
    - `diff <(grep '^## ' .planning/decisions/0031-extracted-crate-dependency-rule.md) <(printf '## Status\n## Context\n## Decision\n## Considered Options\n## Code Locations\n## Code Conformance\n## Downstream Consumers\n')` produces no output, and `sed -n '1p'` does not match `^---`.
    - `grep -c 'cost-benefit-assessment' .planning/decisions/0031-extracted-crate-dependency-rule.md` returns at least `1`, and `grep -c 'prd-extract-infrastructure-crates.md §4.4\|prd-extract-infrastructure-crates.md.4\.4' …` returns `0` — the mis-citation is not reproduced.
    - `grep -c 'no-default-features' .planning/decisions/0031-extracted-crate-dependency-rule.md` returns at least `2` — the invariant names its checking command and hands it to Phase 15.
    - `grep -c 'ADR-0015' .planning/decisions/0031-extracted-crate-dependency-rule.md` returns at least `1` and `grep -c 'Phase 15' …` returns at least `1`.
    - `grep -c 'FACADE-02' .planning/decisions/0031-extracted-crate-dependency-rule.md` returns at least `1`.
    - `grep -c '(rejected)' .planning/decisions/0031-extracted-crate-dependency-rule.md` returns at least `3`, and the removal option and the `--manifest` re-tag option both appear among them.
    - `grep -ci 'special case\|promot' .planning/decisions/0031-extracted-crate-dependency-rule.md` returns at least `1` — the anchor-moved-deliberately reasoning is recorded, not implied.
    - `grep -c '0\.7\.0' .planning/decisions/0031-extracted-crate-dependency-rule.md` returns at least `1` — the manifest pin is quoted as it reads today, not as HARD-05 quotes it.
    - `grep -cx 'conforms' .planning/decisions/0031-extracted-crate-dependency-rule.md` returns `1`.
    - `git status --porcelain -- '*.rs' Cargo.toml crates/paladin-content/Cargo.toml` is empty — this task reads the manifests and changes none of them.
  </acceptance_criteria>
  <done>ADR-0031 leads with the no-leaf-edge-in-the-default-build measurement, states a single quotable invariant with a command that checks it, records the anchor as deliberately promoted, cites `cost-benefit-assessment.md:118` rather than the mis-cited §4.4, and names FACADE-02 and Phase 15 downstream.</done>
</task>

<task type="auto">
  <name>Task 3: Annotate M7 Epic 1 PRD §6.1 and Goal 2 at source</name>
  <precondition>ADR-0031 exists at `.planning/decisions/0031-extracted-crate-dependency-rule.md`, so the relative link the annotation writes resolves.</precondition>
  <read_first>
    - `.project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md` — the whole `## 2. Goals` section around line 30 and the whole `### 6.1 Crate Dependency Rules` section around line 257, so both strikes land on the right sentences and the surrounding clauses are untouched. Re-derive both line numbers.
    - `.planning/decisions/0031-extracted-crate-dependency-rule.md` (post-task-2) — quote the restated invariant in the same words the ADR uses.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md` → the `.project/` annotation banners section, **Analog B / Shape A (inline strike-and-correct)** from commit `94814ff`.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-00c, D-16.
    - Run `git show 94814ff -- '*.md' | head -60` to read the live precedent as committed.
  </read_first>
  <files>.project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md</files>
  <action>
Apply the Shape A inline strike-and-correct treatment to two clauses in this document, plus one
document-head correction blockquote.

1. **Head blockquote**, dated 2026-08-08, tagged HARD-05, linking ADR-0031 by relative path. It states
   that two clauses below (Goal 2 and §6.1, both struck) state the extracted-crate dependency rule in an
   absolute form the tree satisfies only in its default build, that ADR-0031 restates the enforceable
   invariant against the default build, and that the original text is retained below with inline
   corrections and nothing is deleted. Say explicitly in the banner that this is a promotion of the
   invariant to its general form and not permission for a default-build edge.

   **This document is annotated by two plans in this phase.** Say so in the banner: plan 10-04 owns
   Goal 2 and §6.1 (HARD-05); plan 10-05 owns §4.4.1 and §4.4.6 (HARD-06) and adds its own separate
   dated blockquote in a later wave. This banner covers only the two clauses named in it.

2. **Goal 2** (approximately `:30`): wrap the sentence in `~~` markers, word for word, and follow it
   with a `**Corrected (dated 2026-08-08, HARD-05):**` paragraph giving ADR-0031's restated invariant
   verbatim and naming the ADR by number and relative link.

3. **§6.1's rule sentence** (approximately `:257`): the same treatment — struck original, then a
   `**Corrected (dated 2026-08-08, HARD-05):**` paragraph with the restated invariant, the two
   conditions (non-default optional feature, explicit facade opt-in, `cfg`-gated dependent code), and
   the checking command.

Touch no other clause. Do not renumber, reflow, or re-word anything. Leave §4.4.1 and §4.4.6 alone
entirely — plan 10-05 owns them, in a later wave, and will re-derive their line numbers after this
plan's insertions move them.

Record in the SUMMARY the exact post-edit line numbers of §4.4.1 and §4.4.6, so plan 10-05 has a
starting point it must still re-verify.
  </action>
  <verify>
    <automated>bash -c 'f=".project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md"; echo "hard05=$(grep -c "Corrected (dated 2026-08-08, HARD-05)" "$f")"; echo "hard06=$(grep -c "Corrected (dated 2026-08-08, HARD-06)" "$f")"; echo "strikes=$(grep -c "~~" "$f")"; echo "adr=$(grep -c "ADR-0031" "$f")"; echo "numstat=$(git diff --numstat -- "$f")"; grep -n "4.4.1 Create\|4.4.6 The facade" "$f"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Corrected (dated 2026-08-08, HARD-05)' .project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md` returns exactly `2` — one per corrected clause, no more.
    - `grep -c 'Corrected (dated 2026-08-08, HARD-06)' <that file>` returns `0` — plan 10-05's corrections have not been pre-empted.
    - `grep -c '~~' <that file>` returns at least `4` — both original sentences survive struck.
    - `grep -c 'ADR-0031' <that file>` returns at least `3` and the relative link resolves: the referenced path exists.
    - `git diff --numstat -- <that file>` shows exactly `2` deleted lines — the two struck clauses replaced in place; every other change is additive.
    - `grep -n '4.4.1 Create' <that file>` and `grep -n '4.4.6 The facade' <that file>` each return exactly one line, and both line numbers are recorded in the SUMMARY for plan 10-05.
    - `grep -c 'no-default-features\|default build' <that file>` returns at least `1` — the corrected clause states the invariant against the default build, not merely that the old wording was wrong.
    - `git status --porcelain -- '*.rs' Cargo.toml crates/paladin-content/Cargo.toml Makefile .cargo/audit.toml` is empty.
  </acceptance_criteria>
  <done>Goal 2 and §6.1 each survive struck and intact beneath one dated head blockquote, each followed by a correction stating ADR-0031's restated invariant with its two conditions and its checking command; §4.4.1 and §4.4.6 are untouched and their post-edit line numbers are recorded for plan 10-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an auto-resolved architecture answer → Phase 11's relocation plan | D-15 was auto-selected with no human confirmation, and FACADE-02 chooses D2/D3/D4's targets against it. |
| a mis-cited source sentence → an ADR at top precedence | The `paladin-llm` complexity note is attributed by two documents to a section that does not contain it; reproducing that citation makes it authoritative. |
| two plans → one `.project/` document | 10-04 and 10-05 annotate disjoint clause sets in `prd-extract-infrastructure-crates.md` across two waves; overlapping edits would stack banners or move each other's anchors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-13 | Repudiation | ADR-0031's restated invariant | high | mitigate | An auto-resolved answer recorded at ADR precedence with no human confirmation would bind Phase 11 to a choice nobody made. Task 1 is a blocking `checkpoint:decision` presenting both permitted branches with their undo costs, and the SUMMARY records which was selected and why. |
| T-10-14 | Spoofing | the `paladin-llm` complexity-note citation | high | mitigate | A §4.4 citation that does not resolve, written into an ADR, becomes the version every later phase trusts. The task requires reading `cost-benefit-assessment.md:118` directly, an acceptance criterion greps for that filename, and a second criterion asserts the mis-citation does not appear. |
| T-10-15 | Tampering | `prd-extract-infrastructure-crates.md` line anchors | medium | mitigate | This plan's insertions move §4.4.1 and §4.4.6 downward before plan 10-05 edits them. The banner names the two-plan split, this task records the post-edit line numbers, and plan 10-05 re-derives them rather than trusting either number. |
| T-10-16 | Elevation of privilege | a widened invariant | medium | mitigate | A loosely worded exception licenses the next crate to declare a default-build leaf-to-leaf edge. The invariant is stated as one quotable sentence with three conjunctive conditions, is checkable by a named command, and the anchor-moved sub-decision records the absolute form as the recoverable special case rather than as abandoned. |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing, adds no dependency, and modifies no manifest — it reads `crates/paladin-content/Cargo.toml` and the root `Cargo.toml` and changes neither. `10-RESEARCH.md`'s Package Legitimacy Audit records the phase as not-applicable. |
</threat_model>

<verification>
- The checkpoint's three answers are recorded in `10-04-SUMMARY.md` with reasoning, and plans 10-05 and 10-06 can read them.
- ADR-0031 carries the seven canonical headings in order, no frontmatter, `conforms`, and a single quotable default-build invariant with `cargo tree --no-default-features` named as its check.
- `grep -c 'cost-benefit-assessment' .planning/decisions/0031-extracted-crate-dependency-rule.md` → at least `1`; the §4.4 mis-citation appears nowhere in the file.
- `prd-extract-infrastructure-crates.md` carries exactly two HARD-05 corrections and zero HARD-06 corrections, with exactly two deleted lines in the diff.
- The post-edit line numbers of §4.4.1 and §4.4.6 are recorded in the SUMMARY for plan 10-05.
- `git status --porcelain -- '*.rs'` → empty, unless the checkpoint selected `q3-widen`, in which case the SUMMARY says so explicitly and names the added plan.
</verification>

<success_criteria>
The extracted-crate dependency rule has one stated form, chosen by a human at a blocking checkpoint,
expressed against the default build, checkable by a named command, handed to Phase 15 for enforcement
and to Phase 11 by number — and the PRD clauses it supersedes say so on their own page without losing
a word. The two other flagged branches are settled in the same conversation, before either lands.
</success_criteria>

<output>
Create `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-04-SUMMARY.md` when done.
It must record all three checkpoint answers verbatim, because plans 10-05 and 10-06 read them.
</output>
