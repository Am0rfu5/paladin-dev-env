---
phase: 10-milestone-7-8-ground-truth-recorded-account
plan: 10
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-03", "10-04", "10-05", "10-06"]
files_modified:
  - .planning/ledgers/milestone-07-08.md
autonomous: true
requirements: [HARD-01, HARD-02, HARD-06, HARD-07]

must_haves:
  truths:
    - "All 21 Milestone 8 Epic 5, Epic 6, Epic 7 and cross-milestone rows carry a `file:line`-cited verdict from one of the seven classes, re-derived from the tree rather than carried forward from an ingest-era status word (D-03)"
    - "`REQ-m8-final-quality-gate` records FR-19's warnings-acceptable bar as superseded by outcome, citing ADR-0033, and states the measured state of the gate rather than implying it passes (D-20)"
    - "`REQ-content-processing-build-gate` is resolved by ADR-0032: PDF extraction ships unconditionally, the omitted capability feature gated nothing and has been removed, so the row is no longer a narrowed-with-a-defect verdict (D-17, D-18)"
    - "`REQ-paladin-content-services-rename` records Milestone 8 Epic 6 complete despite the reconciliation rating it not verified and the deferred-items register omitting it, with the zero-match search as evidence and ADR-0028 as the citation (D-09)"
    - "The five cross-milestone rows carried by DOCs rather than PRDs are derived like any other rows, and the two deferred-register rows name their FACADE requirement so Phase 11 inherits them by number rather than by search"
    - "`REQ-deferred-tensorflow-ml-adapter-v3` records the non-goal split — overridden for `paladin-herald`, still holding for `paladin-ml` — citing ADR-0028, because FACADE-03(b) depends on that distinction (D-09)"
    - "Two requirements citing the same artefact keep separate rows and separate verdicts; the `REQ-*` ID is the primary key (D-00f)"
    - "Every one of the 21 rows carries a non-empty Verdict cell and a non-empty Evidence cell; a requirement whose evidence cannot be located is `genuinely outstanding` with the search that returned nothing, never a blank cell"
    - "This plan writes only its own four epic sections and replaces Verdict and Evidence cells in place, so `grep -c '^| REQ-'` still reads 86 and the ledger's added and deleted line counts are equal"
  artifacts:
    - path: ".planning/ledgers/milestone-07-08.md"
      provides: "Milestone 8 Epic 5 (6), Epic 6 (4), Epic 7 (6) and the five cross-milestone rows fully derived, with epic notes"
      contains: "REQ-m8-deferred-items-register"
  key_links:
    - from: ".planning/ledgers/milestone-07-08.md"
      to: ".planning/decisions/0032-pdf-extraction-capability.md"
      via: "The REQ-content-processing-build-gate row citing the ADR that resolved the capability question and the manifest change that followed"
      pattern: "ADR-0032"
    - from: ".planning/ledgers/milestone-07-08.md"
      to: ".planning/decisions/0028-m8-reconciliation-authoritative.md"
      via: "The Epic 6 completeness row and the deferred ML-adapter row citing the ADR for the completeness finding and the non-goal split"
      pattern: "ADR-0028"
  prohibitions:
    - "A ledger verdict must not be upgraded on the strength of an ingest-era status word. Carrying `Shipped` or `Verify` forward without re-deriving the citation is the exact false-positive class this ledger exists to reject, and it is prohibited even where it would let a plan finish faster."
    - "A record must not claim a bar is met when the measured state says otherwise. The final-quality-gate row must not imply the workspace documentation gate currently passes; ratification and compliance are two different claims and conflating them is prohibited."
    - "A deferred item must not be recorded without its register. The two deferred-register rows exist so Phase 11 inherits them by requirement ID; recording them as merely open, without naming the FACADE requirement that owns them, is prohibited."
---

<objective>
Derive all 21 Milestone 8 Epic 5, Epic 6, Epic 7 and cross-milestone rows in
`.planning/ledgers/milestone-07-08.md`, closing the ledger's row inventory.

Purpose: this range holds the two rows that three of this phase's ADRs resolve — the final quality gate
and the content build gate — plus the deferred registers Phase 11 inherits and the Epic 6 completeness
finding that stops it being planned as outstanding. It is the last fan-out range, so after it the ledger
has no pending stubs left.
Output: 21 cited rows and four epic notes.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-PATTERNS.md
@.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-RESEARCH.md
</context>

## Artifacts this phase produces

| Artifact | Plan | Kind |
|---|---|---|
| `.planning/ledgers/milestone-07-08.md` | 10-01 (scaffold), 10-07 (M7 E1-E2), 10-08 (M7 E3-E4), 10-09 (M8 E1-E3), **10-10 (M8 E5-E7 + cross-milestone)**, 10-11 (close-out amendment) | new file |
| `.planning/decisions/0028-m8-reconciliation-authoritative.md` | 10-02 | new ADR |
| `.planning/decisions/0029-version-trajectory-history.md` | 10-03 | new ADR |
| `.planning/decisions/0030-milestone-7-self-numbering.md` | 10-03 | new ADR |
| `.planning/decisions/0031-extracted-crate-dependency-rule.md` | 10-04 | new ADR |
| `.planning/decisions/0032-pdf-extraction-capability.md` | 10-05 | new ADR |
| `.planning/decisions/0033-cargo-doc-warning-bar.md` | 10-06 | new ADR |
| `.planning/REQUIREMENTS.md` | 10-01, 10-11 | modified |
| `.planning/ROADMAP.md` | 10-01, 10-11 | modified |
| `.planning/PROJECT.md` | 10-11 | modified |
| `.planning/decisions/PROMOTION.md` | 10-11 | modified (next free → 0034) |
| `.project/…/Epic_1/facade-audit.md`, `…/Epic_3/infrastructure-adapter-disposition.md`, `…/Epic_3/prd-relocate-remaining-misplaced-modules.md` | 10-02 | annotated |
| `.project/Milestone_7-Production-Hardening/overview/Milestone_7-Tier_4-Production_Hardening.md` | 10-03 | annotated |
| `.project/Milestone_7-Production-Hardening/Epic_1/prd-extract-infrastructure-crates.md` | 10-04, 10-05 | annotated |
| `.project/…/Epic_5/prd-document-facade-crate-role.md`, `.project/Milestone_7-Production-Hardening/Epic_4/epic-4-completion-summary.md` | 10-06 | annotated |
| `crates/paladin-content/Cargo.toml`, `crates/paladin-content/CHANGELOG.md`, `.cargo/audit.toml` | 10-05 | modified |
| `Makefile` | 10-06 | modified |

**No `.rs` file appears on this list, and none may be added to it (D-23).**

## Ledger contention contract — read before editing

Four plans write this file in wave 3, in parallel, over **disjoint contiguous** section ranges:

| Plan | Sections it owns | Rows |
|---|---|---|
| 10-07 | M7 Epic 1, M7 Epic 2 | 25 |
| 10-08 | M7 Epic 3, M7 Epic 4 | 22 |
| 10-09 | M8 Epic 1, M8 Epic 2, M8 Epic 3 | 14 |
| **10-10 (this plan)** | **M8 Epic 5, M8 Epic 6, M8 Epic 7, Cross-milestone** | **21** |

M8 Epic 4's four rows were derived by plan 10-01 and are owned by no fan-out plan. **M8 Epic 4 sits
immediately above this plan's first section — do not edit it.**

**Cell replacement only.** Replace the `Verdict` and `Evidence` cells of rows inside your own sections,
and fill your own sections' `**Epic note:**` lines. **Never** insert, delete, reorder or re-key a row;
**never** add or remove a section heading; **never** touch a row outside your range or edit the head
notes, the legend, the supersession summary table or the row-order section. Keep every row on one line.
This is what makes the four parallel diffs non-adjacent hunks, and it is why every plan asserts
`grep -c '^| REQ-'` equals `86` and that added and deleted line counts are equal.

## Requirement IDs in this plan's range

**M8 Epic 5 — Facade Role Documentation & v0.2.0 Finalization (6):** `REQ-facade-role-lib-docs`,
`REQ-facade-readme`, `REQ-stable-api-v020-sync`, `REQ-changelog-v020-cut`,
`REQ-api-surface-baseline-v020`, `REQ-m8-final-quality-gate`.

**M8 Epic 6 — `paladin-content` Services Rename (4):** `REQ-paladin-content-services-rename`,
`REQ-paladin-content-readme-update`, `REQ-paladin-content-changelog-fix`,
`REQ-content-processing-build-gate`.

**M8 Epic 7 — `paladin-web` Single Framework (axum) (6):** `REQ-delivery-endpoints-axum`,
`REQ-actix-removal`, `REQ-actix-deny-ban`, `REQ-delivery-handler-tests`,
`REQ-web-api-baseline-changelog`, `REQ-web-quality-gate`.

**Cross-milestone entries carried by DOCs rather than PRDs (5):** `REQ-storage-nonoptional-v2`,
`REQ-m8-reconciliation-relocations`, `REQ-m8-deferred-items-register`,
`REQ-deferred-cli-user-commands`, `REQ-deferred-tensorflow-ml-adapter-v3`.

<tasks>

<task type="auto">
  <name>Task 1: Derive Milestone 8 Epic 5's six rows and Epic 6's four rows</name>
  <read_first>
    - `.planning/ledgers/milestone-07-08.md` — the head notes, the legend, the supersession summary table and the M8 Epic 5 and Epic 6 sections as the scaffold left them.
    - `.planning/ledgers/milestone-04-06.md` lines 108-160 — worked example rows in the target shape.
    - `.planning/decisions/0033-cargo-doc-warning-bar.md` — the ratified bar, the measured warning count and crate split, and FR-19's supersession, for the final-quality-gate row.
    - `.planning/decisions/0032-pdf-extraction-capability.md` — the capability answer and the manifest change, for the content build-gate row.
    - `.planning/decisions/0028-m8-reconciliation-authoritative.md` — sub-decision (iii)'s Epic 6 completeness finding.
    - `.planning/decisions/0029-version-trajectory-history.md` — for the v0.2.0 changelog row.
    - `src/lib.rs` and `src/README.md` — the facade role documentation and readme; record sizes and line numbers.
    - `docs/src/api-reference/` — confirm the stable-API chapter exists, by `ls`, for the relocated sync row.
    - `crates/paladin-content/src/services/`, `crates/paladin-content/src/lib.rs`, `crates/paladin-content/README.md`, `crates/paladin-content/CHANGELOG.md` (post-plan-10-05) — the Epic 6 deliverables. Run the workspace-wide search for the pre-rename module name across `src/`, `crates/`, `tests/`, `examples/` and `benches/` and record the command and its zero result.
    - `crates/paladin-content/Cargo.toml` (post-plan-10-05) and root `Cargo.toml` — the features block after the deletion and the facade's capability feature list.
    - `.github/workflows/` — the api-surface job the baseline row depends on; confirm its current state and whether it has worked since the directory rename.
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_5/prd-document-facade-crate-role.md` (post-annotation) — FR-19 and its dated correction.
    - `.planning/intel/code-verification.md` lines 263-414 — the run-4 verification block and its supersession-table rows for this range.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-00e, D-00f, D-02, D-03, D-09, D-17, D-18, D-20.
  </read_first>
  <files>.planning/ledgers/milestone-07-08.md</files>
  <action>
Replace the Verdict and Evidence cells of the six M8 Epic 5 rows and the four M8 Epic 6 rows in place,
and fill both sections' `**Epic note:**` lines.

For each row: one of the seven legend classes, a `file:line` citation re-read at execution time, and a
named exerciser. No cell is left blank.

Specific rows needing more than a mechanical re-derivation:

- **`REQ-m8-final-quality-gate`** — the requirement's FR-19 documentation bar contradicts the M7 bar on
  the same command. Record the row as `superseded by outcome` for that clause, citing ADR-0033, and
  state the measured state of the gate as ADR-0033 records it: the count, the crate split and the
  measurement date. **Do not write a cell that implies the workspace clears this gate today.** Ratification and
  compliance are two different claims and the row must keep them apart. Also record that FR-19 now
  carries a dated correction at source, added by plan 10-06.
- **`REQ-content-processing-build-gate`** — currently `Shipped, narrowed → HARD-06`. ADR-0032 resolves
  it: PDF extraction ships unconditionally in every build of the crate, the omitted capability feature
  gated nothing in either direction, and it has been removed from the manifest. Give the verdict that
  reflects the resolved state, cite ADR-0032 and the post-deletion manifest, and record the feature
  count the facade's capability list now covers against the crate's surviving features. State
  explicitly that the previously-recorded narrowing was a defect in the manifest rather than a
  capability gap.
- **`REQ-paladin-content-services-rename`** — Epic 6 is complete despite the reconciliation rating it
  not verified and the deferred-items register omitting it. Cite ADR-0028, the services directory, the
  crate root's module declaration, and the zero-match search you ran. This is the row that stops Epic 6
  being planned as outstanding.
- **`REQ-changelog-v020-cut`** — `satisfied` as history: the version shipped and the tree is several
  minors past it. Cite ADR-0029's trajectory table rather than re-deriving the version story.
- **`REQ-stable-api-v020-sync`** — `relocated`: the deliverable applies to the mdbook chapter after the
  documentation overhaul. Confirm the chapter exists and cite it.
- **`REQ-api-surface-baseline-v020`** — an open defect: regenerating the baseline depends on a CI job
  that has not worked since the directory rename. Check the job's current state and record what you
  found; if it is still broken, the verdict is `genuinely outstanding` with the evidence, and the cell
  names the requirement that owns fixing it.
- **`REQ-paladin-content-readme-update`** and **`REQ-paladin-content-changelog-fix`** — both marked
  `Verify → HARD-01`, never checked. Check them against the crate's readme and changelog as they stand
  after plan 10-05's entry, and give the honest verdict.

Both `**Epic note:**` lines state their epic's shape in one sentence, including which rows this phase's
own ADRs resolved and the per-class verdict counts.

Touch nothing outside the M8 Epic 5 and M8 Epic 6 sections. The M8 Epic 4 section immediately above
Epic 5 belongs to plan 10-01 and must be byte-unchanged.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/ledgers/milestone-07-08.md; echo "rows=$(grep -c "^| REQ-" "$f")"; echo "sections=$(grep -c "^### " "$f")"; echo "e5=$(awk "/^### Milestone 8 Epic 5/{p=1;next}/^### /{p=0}p" "$f" | grep -c "^| REQ-")"; echo "e6=$(awk "/^### Milestone 8 Epic 6/{p=1;next}/^### /{p=0}p" "$f" | grep -c "^| REQ-")"; echo "adr32=$(grep -c "ADR-0032" "$f")"; echo "adr33=$(grep -c "ADR-0033" "$f")"; echo "numstat=$(git diff --numstat -- "$f")"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| REQ-' .planning/ledgers/milestone-07-08.md` returns exactly `86` and `grep -c '^### '` returns exactly `12`.
    - The M8 Epic 5 section holds exactly `6` `REQ-*` rows and the M8 Epic 6 section exactly `4`, both with `0` matching `pending — plan` and `0` with an empty Verdict cell.
    - `grep -c 'ADR-0032' .planning/ledgers/milestone-07-08.md` returns at least `1` and `grep -c 'ADR-0033' …` returns at least `1` after this task.
    - The `REQ-m8-final-quality-gate` row contains a numeric warning count matching the figure in `.planning/decisions/0033-cargo-doc-warning-bar.md`, and `grep -ciE 'gate (currently )?passes|already enforces|already satisfies' <that row>` returns `0`.
    - The `REQ-paladin-content-services-rename` row names the zero-match search command, and re-running it returns zero matches.
    - The `REQ-content-processing-build-gate` row cites ADR-0032 and reflects the post-deletion manifest: re-running `grep -cE '^pdf +=' crates/paladin-content/Cargo.toml` returns `0`, consistent with what the row states.
    - Every `docs/src/` path named in this range satisfies `test -f`.
    - `git diff --numstat -- .planning/ledgers/milestone-07-08.md` shows added lines equal to deleted lines.
    - The M8 Epic 4 section is byte-unchanged: `git diff -- .planning/ledgers/milestone-07-08.md` touches no line between the M8 Epic 4 and M8 Epic 5 headings.
    - `git status --porcelain` lists only `.planning/ledgers/milestone-07-08.md` for this task's commit.
  </acceptance_criteria>
  <done>All ten M8 Epic 5 and Epic 6 rows carry a re-derived verdict; the final-quality-gate row records the ratified bar and the measured state without implying the gate passes; the content build-gate row is resolved against the post-deletion manifest; and the Epic 6 completeness row names the search that proved it.</done>
</task>

<task type="auto">
  <name>Task 2: Derive Milestone 8 Epic 7's six rows and the five cross-milestone rows</name>
  <read_first>
    - `.planning/ledgers/milestone-07-08.md` — the M8 Epic 7 and Cross-milestone sections as the scaffold left them, and the head notes' verdict legend including `deferred with register`.
    - `crates/paladin-web/src/delivery_controller.rs` and `crates/paladin-web/src/app.rs` — the delivery endpoints, their route registration and the merge that mounts them. Record line numbers for the import and the merge, because mounted-versus-merely-ported is the distinction this row exists to make.
    - Search `crates/paladin-web/` for the removed web framework and record the zero result, for the removal row.
    - `deny.toml` — the ban entry for that framework and its stated reason. Record its line range.
    - `crates/paladin-web/` tests — for the handler-tests row; confirm what exists and what exercises it.
    - `.github/workflows/feature-flags.yml` — the web feature-matrix entry, for the web quality-gate row.
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_7/prd-paladin-web-single-framework-axum.md` — FR-10 and the dated correction Phase 8 already applied to it, for the baseline-changelog row.
    - `.planning/decisions/0028-m8-reconciliation-authoritative.md` — sub-decisions (iii) and (iv): the relocation figures and the non-goal split, for the cross-milestone rows.
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/` — the deferred-items register document; record its D1 through D5 items, their counts and whether each still holds against the tree.
    - Root `Cargo.toml` — whether the storage crate is optional, for the storage-nonoptional row.
    - Search the tree for the CLI user command module and for the ML adapter and its feature flag, recording each zero result; and confirm by `ls crates/` which of the two crates the non-goal named actually exists.
    - `.planning/ROADMAP.md` §"Phase 11" around line 670 — the FACADE requirement IDs the deferred rows must name.
    - `.planning/intel/code-verification.md` lines 263-414 — the run-4 verification block.
    - `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-CONTEXT.md` → D-02, D-03, D-08, D-09.
  </read_first>
  <files>.planning/ledgers/milestone-07-08.md</files>
  <action>
Replace the Verdict and Evidence cells of the six M8 Epic 7 rows and the five cross-milestone rows in
place, and fill both sections' `**Epic note:**` lines. Same rules: one of the seven classes, a citation
re-read at execution time, a named exerciser, no blank cells.

Specific rows needing more than a mechanical re-derivation:

- **`REQ-delivery-endpoints-axum`** — the distinction the row exists to make is that the endpoints are
  **mounted**, not merely ported. Cite the controller's documented routes, the import line and the
  merge line that mounts them, all re-read. A row citing only the controller file would lose the
  distinction.
- **`REQ-actix-removal`** and **`REQ-actix-deny-ban`** — two requirements about the same subject with
  two different mechanisms. They keep **two rows and two verdicts**: one is proved by a zero-match
  search, the other by a manifest ban entry with its stated reason and a build leg that enforces it.
  Never merged.
- **`REQ-web-api-baseline-changelog`** — an open defect whose requirement text names a path that has
  been stale since a directory rename, and which Phase 8 already annotated at source. Record the
  defect, cite Phase 8's dated correction on the PRD, and name the requirement that owns fixing the
  tooling half.
- **`REQ-delivery-handler-tests`** and **`REQ-web-quality-gate`** — both marked `Verify → HARD-01`.
  Check each and give the honest verdict; `present, unproven` where the sub-clause was never audited
  and nothing exercises it.
- **`REQ-m8-reconciliation-relocations`** — the cross-milestone row for the relocation work. Cite
  ADR-0028's measured commit count and net LOC figure, and state that every relocation target is
  confirmed in the tree. Record what you re-verified rather than restating the ADR wholesale.
- **`REQ-m8-deferred-items-register`** — `deferred with register`. The cell must name the register
  document, state which of D1 through D5 still hold against the tree as you found them, and name
  **both** FACADE requirement IDs that own them — the one that owns D5 and the one that owns D1
  through D4 — so Phase 11 inherits them by number rather than by search. Record that the register
  names no owners and assigns no target milestone; that is the defect the FACADE requirements exist to
  close.
- **`REQ-deferred-cli-user-commands`** — `deferred with register`, naming its FACADE requirement. Cite
  the search proving the command module is absent, note that the backend is intact, and record that
  the surface is recoverable from git history. That last fact is why this row must not read as a loss.
- **`REQ-deferred-tensorflow-ml-adapter-v3`** — `deferred with register`, naming its FACADE
  requirement. Cite the searches proving both the adapter and the flag are absent, and record the
  non-goal split from ADR-0028: overridden for the crate that was created, still holding for the one
  that was not. Confirm both halves by `ls crates/` and record the result. FACADE-03(b) depends on
  this distinction.
- **`REQ-storage-nonoptional-v2`** — confirm the current optionality in the root manifest and cite the
  line; this row and the Epic 3 storage row in plan 10-09's range describe the same change from
  different angles and keep separate verdicts.

Both `**Epic note:**` lines state their section's shape in one sentence. The cross-milestone note must
also state that these five entries are carried by DOCs rather than PRDs, which is why no PRD citation
exists for them, and that this is a provenance fact rather than a gap.

Touch nothing outside the M8 Epic 7 and Cross-milestone sections. After this task the ledger has no
`pending — plan` markers left anywhere; assert that.
  </action>
  <verify>
    <automated>bash -c 'f=.planning/ledgers/milestone-07-08.md; echo "rows=$(grep -c "^| REQ-" "$f")"; echo "sections=$(grep -c "^### " "$f")"; echo "e7=$(awk "/^### Milestone 8 Epic 7/{p=1;next}/^### /{p=0}p" "$f" | grep -c "^| REQ-")"; echo "cross=$(awk "/^### Cross-milestone/{p=1;next}/^### /{p=0}p" "$f" | grep -c "^| REQ-")"; echo "pending_total=$(grep -c "pending — plan" "$f")"; echo "facade=$(grep -c "FACADE-0" "$f")"; echo "numstat=$(git diff --numstat -- "$f")"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| REQ-' .planning/ledgers/milestone-07-08.md` returns exactly `86` and `grep -c '^### '` returns exactly `12`.
    - The M8 Epic 7 section holds exactly `6` `REQ-*` rows and the Cross-milestone section exactly `5`, both with `0` with an empty Verdict cell.
    - `grep -c 'pending — plan' .planning/ledgers/milestone-07-08.md` returns `0` across the **whole file** — this is the last fan-out plan, so no stub survives anywhere.
    - `grep -c 'FACADE-0' .planning/ledgers/milestone-07-08.md` returns at least `3` — each deferred-register row names the FACADE requirement that owns it.
    - The `REQ-delivery-endpoints-axum` row cites both the route-registration import and the merge line, not the controller file alone.
    - `REQ-actix-removal` and `REQ-actix-deny-ban` appear as two distinct rows with two distinct verdicts and two distinct citations.
    - The `REQ-deferred-tensorflow-ml-adapter-v3` row records both halves of the non-goal split, and `ls crates/` at verification time is consistent with what it states.
    - Every absence claim in this range names the search that produced it, and re-running each returns zero matches.
    - `git diff --numstat -- .planning/ledgers/milestone-07-08.md` shows added lines equal to deleted lines.
    - `git status --porcelain` lists only `.planning/ledgers/milestone-07-08.md` for this task's commit.
  </acceptance_criteria>
  <done>All eleven M8 Epic 7 and cross-milestone rows carry a re-derived verdict, the three deferred-register rows each name their owning FACADE requirement, the endpoints row proves mounting rather than porting, and the ledger has no pending stub left anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a ratified standard → a claim of current compliance | ADR-0033 ratifies the documentation bar while the tree does not clear it; a ledger row that conflates the two records a passing gate. |
| a deferred register with no owners → Phase 11's plan | Five deferred items and two removed features are recorded with ratings and no owner; a ledger row that omits the FACADE requirement ID leaves them to be found by search. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-41 | Repudiation | the final-quality-gate row | high | mitigate | Recording FR-19 superseded without recording the measured state reads as the stricter bar being met. The row must carry ADR-0033's count and crate split, and an acceptance criterion greps the row for zero occurrences of gate-passes phrasing while requiring the numeric count to match the ADR. |
| T-10-42 | Repudiation | the content build-gate row | high | mitigate | The row previously recorded a narrowing defect that ADR-0032 resolved and plan 10-05 executed. Recording the resolution without checking the post-deletion manifest would assert a state nobody confirmed; an acceptance criterion re-runs the manifest grep and requires the row to be consistent with it. |
| T-10-43 | Tampering | the deferred registers' onward ownership | medium | mitigate | Three rows carry work Phase 11 inherits. Each must name its FACADE requirement ID, and an acceptance criterion counts the occurrences so a row cannot silently drop its pointer. |
| T-10-44 | Tampering | the ledger's row inventory during parallel writes | high | mitigate | Four executors write one file concurrently. Cell replacement inside disjoint contiguous sections; this plan additionally asserts that zero `pending — plan` markers survive anywhere in the file, which is the whole-file completeness check no earlier fan-out plan can make. |
| T-10-45 | Repudiation | absence claims | medium | mitigate | Five rows in this range assert that something is gone. Each names the search that produced the zero result, and every such command is re-runnable at verification time. |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing, adds no dependency and modifies no manifest — it reads them, including the one plan 10-05 edited. `10-RESEARCH.md`'s Package Legitimacy Audit records the phase as not-applicable; `git status --porcelain -- Cargo.toml Cargo.lock` is empty. |
</threat_model>

<verification>
- `grep -c '^| REQ-' .planning/ledgers/milestone-07-08.md` → `86`; `grep -c '^### '` → `12`.
- `grep -c 'pending — plan' .planning/ledgers/milestone-07-08.md` → `0` across the whole file.
- The M8 Epic 5, 6, 7 and Cross-milestone sections hold 6, 4, 6 and 5 rows, with zero blank Verdict cells.
- The final-quality-gate row's warning count matches ADR-0033's, and no phrasing implies the gate passes.
- Each deferred-register row names its FACADE requirement ID; `grep -c 'FACADE-0'` → at least `3`.
- `git diff --numstat` on the ledger shows added lines equal to deleted lines, and the M8 Epic 4 section is byte-unchanged.
- `git status --porcelain -- '*.rs'` → empty. D-23's boundary holds for this plan.
</verification>

<success_criteria>
The ledger has no pending stub anywhere: all 86 rows carry a cited verdict. The three rows this phase's
own ADRs resolved say what changed and on whose authority, without turning a ratified standard into a
claim of compliance. The three deferred registers name the Phase 11 requirement that owns them, so
nothing in this range has to be rediscovered by search.
</success_criteria>

<output>
Create `.planning/phases/10-milestone-7-8-ground-truth-recorded-account/10-10-SUMMARY.md` when done.
Record the per-class verdict counts for all four sections, the whole-file pending-stub count, and the
absence-search commands used.
</output>
