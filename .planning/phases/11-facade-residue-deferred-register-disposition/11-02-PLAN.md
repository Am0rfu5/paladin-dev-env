---
phase: 11-facade-residue-deferred-register-disposition
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - .planning/decisions/0034-d1-d4-facade-relocation-disposition.md
  - .project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-items.md
autonomous: true
requirements: [FACADE-02]

must_haves:
  truths:
    - "Each of D1, D2, D3 and D4 carries a verdict in the three-part shape D-04 requires — a verb (`do` / `defer` / `withdraw`), a named owner, and where deferred a concrete named trigger — so none of the four reads as an effort rating restated"
    - "D1 defers with the facade-wide no-alias sweep as its stated trigger, and the ADR records why ADR-0018 does not already settle it: ADR-0018 retired old paths for *relocated types*, which is a different construct from `src/core/`'s surviving re-export layer (D-05)"
    - "D2's `user_service.rs` split half is withdrawn with its reason recorded, and `content_service.rs` and `event_manager.rs` each carry their own independent verdict rather than being folded into the withdrawal (D-06)"
    - "D3 and D4 defer with triggers, gated explicitly on ADR-0031's per-edge test — is each proposed edge non-default, facade-gated and `cfg`-scoped — and not on HARD-05 being unanswered, because HARD-05 is answered (D-07, D-08, D-00i)"
    - "No relocation executes in this phase and no candidate list re-plans anything inside ADR-0028's `e5b2011~1..a1e4901` range (D-04, D-00j)"
    - "ADR-0031's unratified status is carried forward, not laundered: the ADR states that it was authored under Phase 10 `--auto`, was flagged `⚠ HUMAN REVIEW`, and should be human-confirmed by any future phase that executes a D3 or D4 edge (D-00m)"
    - "The three-owner split on `user_service.rs` is readable from the ADR alone: split withdrawn, full relocation owned by the run-3 v2 tech-debt item, tests owned by DEFER-02 / Phase 15 — which is what ROADMAP criterion 2's 'nothing in that set is planned twice' asks for"
    - "Withdrawing the `user_service.rs` split does not empty D2: the ADR's D2 sub-decision names all three files and gives each a verdict"
    - "The ADR presents D1, D2, D3, D4 in the register's own numeric order and never re-sorts them by verdict, so it can be read side by side with `deferred-items.md`"
    - "No Rust source file is modified by this plan (D-13)"
    - statement: "No file named in D1–D4 is left with two registers proposing incompatible next actions. `user_service.rs` in particular resolves to test-only — the split is withdrawn, the full relocation remains the run-3 v2 tech-debt item, and DEFER-02 / Phase 15 owns the tests. Confirming this requires reading D2, the run-3 v2 item and Deferred-QA Epic 28 together; 11-VALIDATION.md classes it manual-only because no single command proves a cross-document non-collision. A verifier that cannot produce explicit evidence that all three owners are named and non-overlapping must abstain rather than pass."
      verification: backstop
  artifacts:
    - path: ".planning/decisions/0034-d1-d4-facade-relocation-disposition.md"
      provides: "The D1-D4 disposition set — four verdicts with owners and triggers, tested against ADR-0031 and bounded by ADR-0028's executed range"
      contains: "## Downstream Consumers"
    - path: ".project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-items.md"
      provides: "Dated corrections on the D1-D4 clauses pointing at the ADR, original ratings retained"
      contains: "ADR-0034"
  key_links:
    - from: ".planning/decisions/0034-d1-d4-facade-relocation-disposition.md"
      to: ".planning/decisions/0031-extracted-crate-dependency-rule.md"
      via: "Code Locations citing ADR-0031's restated invariant as the test D3's and D4's targets are measured against, together with its unratified status"
      pattern: "0031-extracted-crate-dependency-rule"
    - from: ".planning/decisions/0034-d1-d4-facade-relocation-disposition.md"
      to: ".project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-items.md"
      via: "Code Locations citing each of D1-D4's clause by file:line, and each clause's banner pointing back at the ADR"
      pattern: "deferred-items.md"
  prohibitions:
    - "A correction must not delete or rewrite the text it corrects. Every original effort/risk rating and recommendation in `deferred-items.md`'s D1, D2, D3 and D4 clauses must still be readable after this plan; the ratings are the evidence for why FACADE-02 exists, and a document that quietly loses them destroys the record the annotation preserves."
    - "An inherited decision that no human ratified must not be presented as settled authority. ADR-0031 was authored by Phase 10 under `--auto` and flagged `⚠ HUMAN REVIEW`; citing it as the governing test for D3's and D4's targets without carrying that status forward launders an unratified position into precedence, which is invisible to any test and is exactly what a future phase executing one of those edges would be misled by."
---

<objective>
Give D1, D2, D3 and D4 each a real verdict with a named owner — and, where deferred, a concrete
trigger — in a single ADR, and correct their clauses at source. No relocation executes.

Purpose: the ROADMAP goal is "a decision rather than a rating". `deferred-items.md` states effort and
risk for all four and names no owner and no target milestone for any of them, which is the defect
FACADE-02 exists to close. A verdict without an owner, or a deferral without a stated trigger, is a
rating wearing a verdict's clothes and does not close the item.

Output: ADR-0034 holding four verdicts, and dated corrections on the four clauses it disposes of.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-facade-residue-deferred-register-disposition/11-CONTEXT.md
@.planning/phases/11-facade-residue-deferred-register-disposition/11-PATTERNS.md
@.planning/phases/11-facade-residue-deferred-register-disposition/11-RESEARCH.md
</context>

## Artifacts this phase produces

| Artifact | Plan | Kind |
|---|---|---|
| `.planning/registers/facade-01-rustdoc-stdout-disposition.md` | 11-01 | new file |
| `.planning/registers/facade-03-removed-features.md` | 11-03 | new file |
| `.planning/registers/facade-04-m9-candidate-triage.md` | 11-04 | new file |
| `.planning/decisions/0034-d1-d4-facade-relocation-disposition.md` | **11-02** | new ADR |
| `.planning/decisions/0035-paladin-ml-leaf-crate-placement.md` | 11-03 | new ADR |
| `.planning/decisions/PROMOTION.md` | 11-05 | modified — 2 index rows, 1 dated note, next free → 0036 |
| `.planning/ledgers/milestone-07-08.md` | 11-01 (D5 half of one row), 11-05 (all remaining rows) | modified |
| `.planning/ROADMAP.md` | 11-01 (§Phase 11 criterion 1) | modified |
| `.planning/REQUIREMENTS.md` | 11-05 (FACADE-01 and FACADE-03(a) source corrections) | modified |
| `.planning/PROJECT.md` | 11-05 (2 `## Key Decisions` rows) | modified |
| `.project/…/deferred-items.md` | 11-01 (D5 clause), **11-02** (D1–D4 clauses) | annotated |
| `.project/…/deferred-features.md` | 11-03 | annotated |

New headings and rows added to existing records: two `PROMOTION.md` Numbering-index rows (0034, 0035)
plus one dated note plus the `Next free ADR number` line; two `PROJECT.md` `## Key Decisions` rows;
dated amendments to five named ledger rows; a dated amendment to ROADMAP §Phase 11 criterion 1;
source corrections inside REQUIREMENTS.md's FACADE-01 and FACADE-03 blocks.

**No `.rs` file appears on this list, and none may be added to it (D-13).**

## Annotation ownership

This plan annotates **only the D1, D2, D3 and D4 clauses** of `deferred-items.md`, plus the
`## Suggested grouping` bullets that name D1, D2, D3 and D4. Plan 11-01 owns the D5 clause and the
`- **Quick wins:** D5 (println residue).` bullet in the same file and has already written them; do
not touch, re-word or re-date them. No clause receives two banners.

## Flagged assumptions carried into this plan

- **ADR-0031 is inherited, not ratified (D-00m).** Phase 10 ran `--auto`; none of its nine decisions
  were human-confirmed, and ADR-0031 is one of two it explicitly flagged `⚠ HUMAN REVIEW`. This plan
  proceeds on it because D-04 defers every relocation, so no code depends on its precise form. The
  ADR this plan writes must say so rather than build on it silently.
- **The ADR number is 0034 only under option-a** of plan 11-01's decision checkpoint. If option-b or
  option-c was selected, re-read `PROMOTION.md`'s `Next free ADR number` line, take the number(s) it
  gives, and record in the SUMMARY which numbers were actually taken.
- **This plan's `one-way` reversibility gate is satisfied across plans, by design.** `verify
  plan-structure` warns that Task 1 carries `rating="one-way"` with no `checkpoint:decision` earlier
  in *this* file. That is intentional: the whole phase's ADR allocation is one decision, so it is
  gated once, at the end of plan 11-01 in wave 1, which every wave-2 plan depends on. Duplicating the
  checkpoint here and in plan 11-03 would ask the same question twice (checkpoint fatigue) and would
  race — 11-02 and 11-03 run in parallel, so a gate inside either one cannot stop the other from
  taking a number first. The rating stays `one-way` because the door is real; only the gate's location
  moved.

<tasks>

<task type="auto">
  <name>Task 1: Write ADR-0034 — the D1–D4 disposition set, four verdicts with owners and triggers</name>
  <reversibility rating="one-way">An ADR number is never reused (`PROMOTION.md:141`) and an ADR tops the precedence order (D-00b), so undoing this is authoring a superseding ADR rather than a delete. The allocation was confirmed by plan 11-01's checkpoint.</reversibility>
  <read_first>
    - `.planning/decisions/0031-extracted-crate-dependency-rule.md` in full — the seven-heading no-frontmatter shape to copy exactly; the `**(i)** / **(ii)** / **(iii)**` numbered sub-decision layout; the "restates rather than instructs a change" posture; the single-quotable-sentence convention at lines 65-67; the inline citation correction at lines 50-56; and the restated invariant this ADR tests D3 and D4 against.
    - `.planning/decisions/0018-m6-facade-reexport-policy.md` in full — the no-re-export-alias posture ARCH-04 settled, and precisely which construct it covers, so D1's sub-decision can state the distinction rather than assume it.
    - `.planning/decisions/0028-m8-reconciliation-authoritative.md` `## Decision (iii)` — the 15-commit, net-10,252-LOC range `e5b2011~1..a1e4901` that bounds what may be re-planned.
    - `.planning/decisions/PROMOTION.md` lines 95-128 — the required heading set in order, the bullet-list requirement on `## Considered Options` and `## Code Locations`, and the supersession mechanism.
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-items.md` in full — the D1, D2, D3 and D4 clauses with their exact `file:line`s, their effort/risk ratings, their recommendations, and the `## Suggested grouping` section.
    - `.planning/REQUIREMENTS.md` lines 1570-1592 (the Phase 10 hand-off to FACADE-02) and lines 1636-1666 (FACADE-02's full text including the Epic 1 audit's recommendations for `content_service.rs` and `event_manager.rs`).
    - `.planning/phases/11-facade-residue-deferred-register-disposition/11-CONTEXT.md` → D-00g, D-00i, D-00j, D-00l, D-00m, D-04, D-05, D-06, D-07, D-08, D-13, D-14.
    - `.planning/phases/11-facade-residue-deferred-register-disposition/11-RESEARCH.md` → "Pattern 3: D1-D4 verdict shape", "Pitfall 5: Confusing D2's withdrawal scope", and Open Question 2.
  </read_first>
  <files>.planning/decisions/0034-d1-d4-facade-relocation-disposition.md</files>
  <action>
Re-measure the three D1/D2 facts before writing, and quote their output verbatim in `## Code
Locations` per D-00e: `find src/core -name "*.rs" | wc -l` (expect 6),
`grep -rl "crate::core::" src/ | wc -l` (expect 49), and `ls src/core/platform/manager/` (expect
`mod.rs`, `content_service.rs`, `event_manager.rs`, `user_service.rs`). If any differs, record the
measured value and say so; do not carry a stale figure forward.

Write `.planning/decisions/0034-d1-d4-facade-relocation-disposition.md` with no YAML frontmatter and
exactly these seven H2 headings in this order: `## Status`, `## Context`, `## Decision`,
`## Considered Options`, `## Code Locations`, `## Code Conformance`, `## Downstream Consumers`.
Title line: `# ADR-0034: D1–D4 facade relocation disposition — four verdicts with owners`.
`## Status` body is `Accepted` followed by a bold `**Date:** 2026-08-08` line.

`## Context` states what `deferred-items.md` records for each of D1–D4 (an effort/risk rating and a
recommendation, no owner, no target milestone) with each clause's `file:line`, then what this session
measured against the tree. Name the two governing ADRs it is tested against — ADR-0031's restated
invariant and ADR-0028's executed range — and, in the same breath, record that **ADR-0031 was
authored by Phase 10 under `--auto` and is one of two decisions Phase 10 flagged `⚠ HUMAN REVIEW`;
it has never been human-ratified.** State that this ADR is safe to write on it because D-04 defers
every relocation so no code depends on its precise form here, and that any future phase which
actually executes a D3 or D4 edge should confirm it with a human first.

`## Decision` opens with the disposition rule as one quotable sentence: every item resolves to a verb
— *do*, *defer with a stated trigger*, or *withdraw* — plus a named owner, and no relocation executes
in this phase. Then four numbered sub-decisions in the register's own order, never re-sorted by
verdict:

  **(i) D1 — `src/core/` re-export shims: DEFER. Trigger: a facade-wide no-alias sweep. Owner: the
  phase that runs that sweep; named here as unassigned-pending-that-sweep rather than left blank.**
  Give the measured blast radius: `src/core/` is exactly six files, and 49 files import through
  `crate::core::…`. State why removal is not a pure path rewrite: `src/core/platform/mod.rs` carries
  maneuver/parser path injection, which is real logic rather than re-exports. Then record the
  distinction D-05 requires and the register does not make: **ADR-0018 does not settle D1.** ADR-0018
  retired old paths for *relocated types* (`application::use_cases::*`, `CircuitBreaker`) — a
  different construct from `src/core/`'s surviving re-export layer, which still fronts live code. D1
  therefore does not follow automatically from ARCH-04, and a reader who assumes it does will
  mis-scope the sweep.

  **(ii) D2 — mis-layered `src/core/platform/manager/` services: three files, three verdicts.**
  - `user_service.rs`: the **split is WITHDRAWN**, with the reason recorded. Three facts narrow it to
    nothing: reconciliation commit `6704807` already found "no user-service split was needed" for the
    controller case, because `UserServiceTrait` and the DTOs already live in `paladin-core`; the
    *full* `user_service` relocation is already carried as a run-3 v2 tech-debt item; and Deferred-QA
    Epic 28 (DEFER-02, Phase 15) plans to test the same file to ≥ 80%. Withdrawing resolves the
    collision REQUIREMENTS.md flags ("do not schedule independently") rather than sequencing it, and
    frees Phase 15 to size against the unsplit file. State the resulting three-owner split explicitly
    and in one place, because this is what ROADMAP criterion 2 is asking for: **the split is owned by
    nobody (withdrawn), the full relocation is owned by the run-3 v2 tech-debt item, and the tests are
    owned by DEFER-02 / Phase 15.**
  - `content_service.rs` and `event_manager.rs`: each gets its **own independent verdict** in the
    same three-part shape — verb, owner, and a concrete trigger if deferred. Do not fold them into
    `user_service.rs`'s withdrawal and do not give the pair one shared verdict; D-06 says they
    "remain genuine D2 items and get their own verdicts", plural. Derive each verdict on merit from:
    the Epic 1 audit's recommendation (`ContentItemService` → `paladin-core`; `EventService` →
    `paladin-core` or a facade app-service); whether the proposed target is legal under ADR-0031's
    per-edge test; and whether anything in ADR-0028's `e5b2011~1..a1e4901` range already moved it —
    if it did, the verdict is that the item is already resolved by outcome, not that it should move
    again. Record the measurement that produced each verdict.

  **(iii) D3 — entangled Paladin services: DEFER. Trigger: the broader builder/execution refactor the
  register itself names. Owner: recorded.** The four files are
  `src/application/services/paladin/{planning_service,prompt_generation_service,temperature_service,handoff_service}.rs`,
  ~2,750 LOC, tightly coupled to `paladin_builder.rs` and `paladin_execution_service.rs`. State
  plainly that **HARD-05 is answered** — ADR-0031 restated the rule — so D3's `paladin-battalion`
  (planning/handoff) and `paladin-llm` (prompt/temperature) targets are legal on exactly the same
  terms `paladin-content`'s existing `llm` feature already satisfies. The remaining question is
  per-edge, not categorical: is each proposed edge non-default, facade-gated and `cfg`-scoped? Record
  that question as the thing the trigger event must answer, and do not answer it here.

  **(iv) D4 — `content_ingestion_service.rs` placement: DEFER. Trigger: the dependency-coupling review
  the register already names as the precondition. Owner: recorded.** The file is
  `src/application/services/content/content_ingestion_service.rs`, ~1,211 LOC. M7 Epic 1's extraction
  PRD listed it as moving to `paladin-content`; the facade kept its own copy. Legal under ADR-0031 on
  the same terms as D3. The review is the trigger and is not performed here.

Close `## Decision` with the boundary statement: this ADR records verdicts, not moves. Nothing in
D1–D4 executes in Phase 11 (D-04), and no item's verdict re-plans a relocation inside ADR-0028's
`e5b2011~1..a1e4901` range (D-00j).

`## Considered Options` is a bullet list, each tagged `(accepted)` or `(rejected)` with a one-clause
reason. Include at minimum: four verdicts with owners and triggers, no execution (accepted);
executing the cheap items now (rejected — a disposition phase that also refactors has two jobs and a
much larger blast radius); a uniform "defer all" (rejected — reads as a rating by another name and
does not close the ROADMAP goal); withdrawing all of D2 rather than only the `user_service.rs` split
(rejected — `content_service.rs` and `event_manager.rs` are genuine items and D-06 requires their own
verdicts); sequencing the `user_service.rs` split ahead of DEFER-02 instead of withdrawing it
(rejected — that preserves the collision instead of removing it and leaves Phase 15 sizing against a
moving target); treating D1 as already answered by ADR-0018 (rejected — different construct, D-05).

`## Code Locations` is a bullet list of exact `file:line` citations covering: each of D1–D4's clause
in `deferred-items.md`; `src/core/platform/mod.rs` for the path-injection logic; the three files under
`src/core/platform/manager/`; the four D3 files; the D4 file; `.planning/decisions/0031-…md` and
`.planning/decisions/0028-…md`; `.planning/decisions/0018-…md`; and the three re-run commands with
their verbatim output.

`## Code Conformance` is the standalone word `conforms` plus one justifying line: every verdict is
defer or withdraw, so the tree already satisfies this ADR and it instructs no code change (D-13).

`## Downstream Consumers` is a bullet list naming, at minimum: **Phase 15 / DEFER-02** — inherits the
withdrawn split and may size its estimate and mock set against the unsplit `user_service.rs`; **the
phase that runs the facade-wide no-alias sweep** — inherits D1 and the 49-importer measurement; **any
future phase executing a D3 or D4 edge** — inherits the per-edge ADR-0031 test *and* the instruction
to human-confirm ADR-0031 first; and **plan 11-05** — amends the `REQ-m8-deferred-items-register`
ledger row against this ADR.
  </action>
  <verify>
    <automated>A=.planning/decisions/0034-d1-d4-facade-relocation-disposition.md; test -f "$A" || { echo "ADR missing"; exit 1; }; diff <(grep '^## ' "$A") <(printf '## Status\n## Context\n## Decision\n## Considered Options\n## Code Locations\n## Code Conformance\n## Downstream Consumers\n') && echo "seven headings, correct order"</automated>
  </verify>
  <acceptance_criteria>
    - The `<verify>` diff returns clean: exactly the seven required H2 headings, in `PROMOTION.md`'s order, with no YAML frontmatter (`head -1` of the file is the `# ADR-0034:` title line, not `---`).
    - `## Considered Options` and `## Code Locations` are bulleted, not prose: `awk '/^## Considered Options/,/^## Code Locations/' "$A" | grep -c '^- '` and `awk '/^## Code Locations/,/^## Code Conformance/' "$A" | grep -c '^- '` each return at least `4`.
    - All four items appear with a decision verb: `grep -Eic 'defer|withdraw' "$A"` is non-zero, and the strings `D1`, `D2`, `D3` and `D4` each appear inside `## Decision`.
    - D2 names all three files: `grep -c 'user_service.rs' "$A"`, `grep -c 'content_service.rs' "$A"` and `grep -c 'event_manager.rs' "$A"` are each non-zero, and `content_service.rs` and `event_manager.rs` each appear with their own verb and owner rather than only inside the withdrawal sentence.
    - The three-owner split on `user_service.rs` is stated in one place: the ADR contains all three of `6704807`, `run-3 v2` and `DEFER-02` within the D2 sub-decision.
    - `grep -c '0031-extracted-crate-dependency-rule' "$A"` is non-zero and `grep -Ec 'HUMAN REVIEW|never been human-ratified|not human-ratified' "$A"` is non-zero — ADR-0031's unratified status is carried forward, not laundered.
    - `grep -c 'e5b2011' "$A"` is non-zero — ADR-0028's executed range is named as the bound on what may be re-planned.
    - `grep -c '0018-m6-facade-reexport-policy' "$A"` is non-zero and D1's sub-decision states that ADR-0018 does not settle D1.
    - `find src/core -name "*.rs" | wc -l` returns `6` and `grep -rl "crate::core::" src/ | wc -l` returns `49`; both figures appear in `## Code Locations` with the commands that produced them.
    - `grep -c '^conforms' "$A"` returns `1` — `## Code Conformance` carries the single verdict word.
    - `grep -Ec 'Phase 15|DEFER-02' "$A"` is non-zero inside `## Downstream Consumers`.
    - `git diff --name-only HEAD~1 HEAD | grep -c 'rs$'` returns `0` — no Rust source file was modified (D-13).
  </acceptance_criteria>
  <done>ADR-0034 exists with the seven required headings in order, carries four verdicts each with a verb, an owner and (where deferred) a concrete trigger, gives `content_service.rs` and `event_manager.rs` independent verdicts, states the three-owner split on `user_service.rs`, and records ADR-0031's unratified status rather than relying on it silently.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the D1–D4 clauses of `deferred-items.md` at source</name>
  <reversibility rating="costly">Withdrawing the `user_service.rs` split is recorded here as well as in the ADR; Phase 15 sizes DEFER-02 against the unsplit file, so reinstating the split later invalidates that sizing and re-creates the coupling this withdrawal removes (D-06).</reversibility>
  <precondition>`.planning/decisions/0034-d1-d4-facade-relocation-disposition.md` exists and carries all seven required headings (Task 1 of this plan).</precondition>
  <read_first>
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-items.md` in full **as it stands after plan 11-01** — plan 11-01 has already added a dated Shape-A correction to the D5 clause and to the `- **Quick wins:** D5 (println residue).` bullet. Read those to match their visual convention exactly, and to confirm which lines are already annotated so this task does not touch them.
    - `.planning/decisions/0034-d1-d4-facade-relocation-disposition.md` (just written) — the four verdicts, so each clause's correction states the same verdict the ADR does.
    - `.planning/phases/11-facade-residue-deferred-register-disposition/11-PATTERNS.md` → the "Dated correction banner, two shapes" section, and the Shape-A description.
    - `.planning/phases/11-facade-residue-deferred-register-disposition/11-CONTEXT.md` → D-00c.
  </read_first>
  <files>.project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-items.md</files>
  <action>
Annotate the D1, D2, D3 and D4 clauses in place, Shape A, dated 2026-08-08, naming plan 11-02 and
citing `.planning/decisions/0034-d1-d4-facade-relocation-disposition.md`. This is annotation, never
rewriting (D-00c): every original word — each clause's `- **Effort / risk:**` rating, each
`- **Recommendation:**` line, and the `## Suggested grouping` bullets naming D2, D3, D4 and D1 — must
remain readable after the edit.

For each clause insert a dated correction stating the same thing the ADR's corresponding sub-decision
states, in one or two lines: D1 defers with the facade-wide no-alias sweep as its trigger, and
ADR-0018 does not settle it; D2's `user_service.rs` split is withdrawn with `content_service.rs` and
`event_manager.rs` carrying their own verdicts; D3 defers on the per-edge ADR-0031 test with the
broader refactor as trigger; D4 defers pending the dependency-coupling review. Each correction names
FACADE-02 and points at the ADR path so a reader landing on the clause is routed to the verdict
without having to search.

Also annotate the `## Suggested grouping` section's three surviving bullets — the architecture-pass
bullet naming D2 and D4, and the broader-refactor bullet naming D3 and D1 — with one dated line
recording that the grouping is now superseded by four individual verdicts with owners and triggers,
and that the grouping's own text is retained as the input it was.

Do not touch the `### D5` section or the `- **Quick wins:** D5 (println residue).` bullet; plan 11-01
owns both and has already annotated them. Do not add a second document-level banner: this file
already carries plan 11-01's dated correction. Commit as one atomic commit.
  </action>
  <verify>
    <automated>D=.project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-items.md; for s in "D1 —" "D2 —" "D3 —" "D4 —"; do grep -q "$s" "$D" || { echo "MISSING clause $s"; exit 1; }; done; grep -q '0034-d1-d4-facade-relocation-disposition' "$D" || { echo "ADR pointer missing"; exit 1; }; grep -q 'Effort / risk' "$D" || { echo "original ratings lost"; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Effort / risk' .project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-items.md` returns `5` — every original effort/risk rating for D1 through D5 is still present and none was deleted.
    - `grep -c 'Recommendation:' .project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-items.md` returns the same value it had before this task ran — no recommendation line was removed.
    - `grep -c '0034-d1-d4-facade-relocation-disposition' .project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-items.md` returns at least `4` — one pointer per disposed clause.
    - `grep -c 'FACADE-02' .project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-items.md` is non-zero.
    - `grep -n 'Quick wins:\*\* D5' .project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-items.md` returns a match and the lines around it are byte-identical to their state after plan 11-01 — verified by `git diff HEAD~1 HEAD -- .project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-items.md` showing no hunk touching the D5 section or the quick-wins bullet.
    - `grep -c 'Suggested grouping' .project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-items.md` returns `1` and a dated 2026-08-08 line appears within that section.
    - `git diff --name-only HEAD~1 HEAD` lists exactly `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/deferred-items.md`, and `git diff --name-only HEAD~1 HEAD | grep -c 'rs$'` returns `0`.
  </acceptance_criteria>
  <done>Each of the four D1–D4 clauses and the `## Suggested grouping` section carries a dated correction naming its verdict and pointing at ADR-0034, with every original rating and recommendation still readable and plan 11-01's D5 annotations untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.project/` register (fallible) → `.planning/decisions/` ADR (top of precedence) | A Milestone-8-era rating is converted into a governing verdict that later phases inherit without re-deriving |
| Phase 10's `--auto` output → this phase's ADR | ADR-0031 crosses in as the governing test for D3 and D4 without ever having been human-ratified |
| ADR-0028's measured commit range → this phase's candidate list | The boundary that stops FACADE-02 re-planning relocations that already executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07 | Elevation of Privilege | ADR-0034's citation of ADR-0031 | high | mitigate | An unratified `--auto` decision cited as settled authority acquires precedence it was never granted, and the next phase to execute a D3/D4 edge inherits it as fact. ADR-0034 must state ADR-0031's `⚠ HUMAN REVIEW` status and instruct human confirmation before any edge executes; the acceptance criteria grep for that statement. |
| T-11-08 | Tampering | `deferred-items.md`'s D1–D4 clauses | medium | mitigate | Annotation only, never rewriting (D-00c). The `Effort / risk` count is asserted at `5` after the edit, so deleting any original rating fails the gate. |
| T-11-09 | Spoofing | The 6-file / 49-importer / three-manager-file measurements | medium | mitigate | All three commands are re-run at execution time and their verbatim output recorded in `## Code Locations` per D-00e; a stale figure carried from CONTEXT.md without re-measurement fails the acceptance criteria. |
| T-11-10 | Repudiation | The four verdicts | medium | mitigate | Each verdict carries a verb, a named owner and (where deferred) a concrete trigger, so a later reader can identify who owes the work and what event starts it — the exact property a bare effort rating lacks. |
| T-11-11 | Tampering | Scope of the D2 withdrawal | medium | mitigate | Withdrawing all of D2 rather than only the `user_service.rs` split would silently drop two genuine items (Pitfall 5). Acceptance criteria require `content_service.rs` and `event_manager.rs` to appear with their own verb and owner. |
| T-11-12 | Information Disclosure | — | low | accept | Not applicable. Records contain repository-local paths, line numbers and commit SHAs already in the checkout; no secret or personal datum is read or written. |
| T-11-13 | Denial of Service | — | low | accept | Not applicable. No runtime surface is touched; the plan's execution is file reads, three greps and two writes. |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable, truthfully: this plan installs, upgrades and removes nothing. D-13 confines it to one new ADR and one `.project/` annotation, so no manifest or lockfile is in reach. The `git diff --name-only` criteria prove it. |
</threat_model>

<verification>
- ADR-0034 carries exactly the seven required headings in `PROMOTION.md`'s order, with bulleted `## Considered Options` and `## Code Locations`.
- All four items carry a verb, an owner and a trigger where deferred; `content_service.rs` and `event_manager.rs` have independent verdicts.
- ADR-0031's unratified status and ADR-0028's executed range both appear.
- `deferred-items.md` retains all five effort/risk ratings and every recommendation line.
- Neither commit touches a Rust source file.
</verification>

<success_criteria>
- Reading ADR-0034 alone answers, for each of D1 through D4, what happens, who owns it, and what event starts it.
- Reading `user_service.rs`'s entry answers "who owns the split, the relocation and the tests" with three distinct names and no overlap.
- A reader landing on any of the four clauses in `deferred-items.md` is routed to the verdict without searching.
- No relocation executed and no item inside ADR-0028's range was re-planned.
</success_criteria>

<output>
Create `.planning/phases/11-facade-residue-deferred-register-disposition/11-02-SUMMARY.md` when done.
</output>
