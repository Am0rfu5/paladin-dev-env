---
phase: 11-facade-residue-deferred-register-disposition
plan: 04
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - .planning/registers/facade-04-m9-candidate-triage.md
  - .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md
autonomous: true
requirements: [FACADE-04]

must_haves:
  truths:
    - "Every surviving row of `infrastructure-adapter-disposition.md`'s 20-row table is marked `done` / `not a candidate` / `still open`, so nobody re-plans relocations the reconciliation already executed (ROADMAP criterion 5)"
    - "`paladin-arsenal` and `paladin-sanctum` are recorded as artefacts of a mis-written table rather than as future crates, with `ls crates/` as the evidence and PROJECT.md's `### Out of Scope` entry as the binding statement (D-11)"
    - "The triage runs against ADR-0028's `e5b2011~1..a1e4901` range, not against the disposition record's own claims: any row whose relocation falls inside that range is `done` by outcome regardless of what the record says (D-12, D-00j)"
    - "The list is not blanket-marked superseded: the `arsenal/` rows were never acted on either way and are marked `not a candidate` on the governing PRD's authority, not written off (D-11)"
    - "Rows 1 (`arsenal/`) and 19 (`sanctum/mod.rs`) each record the disagreement with `prd-relocate-remaining-misplaced-modules.md` explicitly — the PRD's verdict, the row's own verdict, and which one this triage follows — rather than silently picking one"
    - "Every one of the 20 rows carries a live-tree evidence cell produced by a named command run this session; no row's evidence reads only 'per the record' and none is blank"
    - "The triage defines its own row identity before counting — the source table's 20 rows with the `input/*` fetcher group expanded to its five named files — and its tally arithmetic sums to the stated row count with no internal contradiction"
    - "The triage lists rows in the source document's own order 1 through 20 and never re-sorts by disposition, so it can be diffed against `infrastructure-adapter-disposition.md` row by row"
    - "Zero rows landing in `still open` is stated plainly as a finding in its own right, not left to be read as an incomplete triage"
    - "Row 14's inventory defect is corrected rather than absorbed: the row describes two log adapters and only `system_log_adapter.rs` survives, `error_log_adapter.rs` having been deleted as an orphan"
    - "`infrastructure-adapter-disposition.md` ends this phase with exactly one banner — Phase 10's, extended in place — never two"
    - "No Rust source file is modified by this plan (D-13)"
  artifacts:
    - path: ".planning/registers/facade-04-m9-candidate-triage.md"
      provides: "The 20-row triage with per-row disposition and live-tree evidence, the two artefact crate names, and the zero-still-open finding"
      contains: "not a candidate"
    - path: ".project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md"
      provides: "The existing Phase-10 banner extended with a pointer to the new triage file"
      contains: "facade-04-m9-candidate-triage"
  key_links:
    - from: ".planning/registers/facade-04-m9-candidate-triage.md"
      to: ".planning/decisions/0028-m8-reconciliation-authoritative.md"
      via: "each `done` row cites ADR-0028's commit range as the authority that resolved it, rather than citing the disposition record"
      pattern: "e5b2011"
    - from: ".project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md"
      to: ".planning/registers/facade-04-m9-candidate-triage.md"
      via: "the extended Phase-10 banner names the triage file so a reader of the source lands on the corrected record"
      pattern: "facade-04-m9-candidate-triage"
  prohibitions:
    - "A correction must not delete or rewrite the text it corrects. `infrastructure-adapter-disposition.md`'s original 20-row table, its `Date: 2025-01` header, its `paladin-arsenal` and `paladin-sanctum` target cells and Phase 10's existing banner must all still be readable after this plan. Those cells are the evidence that the two crate names are artefacts; a document tidied of them proves nothing."
---

<objective>
Triage every row of `infrastructure-adapter-disposition.md`'s 20-row Milestone 9 candidate list into
`done` / `not a candidate` / `still open`, against ADR-0028's executed commit range rather than
against the record's own claims, and settle whether `paladin-arsenal` and `paladin-sanctum` are real
future crates or artefacts of a mis-written table.

Purpose: the Epic 3 PRD §6 designates this document "the authoritative cross-reference for the §4.3
M9 flags" — the document Milestone 9 was meant to read. All 20 of its rows say "Stays / No change"
while the reconciliation executed most of its List B; it names two crates that do not exist; and it
disagrees with its own governing PRD on two rows. Ingest run 5 happened to bypass it, so the trap did
not spring — but the list is still uncorrected at source, and the next reader gets the same wrong
answer.

Output: one `.planning/` triage register with per-row dispositions and live-tree evidence, plus a
pointer added to the banner the source document already carries.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-facade-residue-deferred-register-disposition/11-CONTEXT.md
@.planning/phases/11-facade-residue-deferred-register-disposition/11-PATTERNS.md
@.planning/phases/11-facade-residue-deferred-register-disposition/11-RESEARCH.md
</context>

## Artifacts this phase produces

| Artifact | Plan | Kind |
|---|---|---|
| `.planning/registers/facade-01-rustdoc-stdout-disposition.md` | 11-01 | new file |
| `.planning/registers/facade-03-removed-features.md` | 11-03 | new file |
| `.planning/registers/facade-04-m9-candidate-triage.md` | **11-04** | new file |
| `.planning/decisions/0034-d1-d4-facade-relocation-disposition.md` | 11-02 | new ADR |
| `.planning/decisions/0035-paladin-ml-leaf-crate-placement.md` | 11-03 | new ADR |
| `.planning/decisions/PROMOTION.md` | 11-05 | modified — 2 index rows, 1 dated note, next free → 0036 |
| `.planning/ledgers/milestone-07-08.md` | 11-01 (D5 half of one row), 11-05 (all remaining rows) | modified |
| `.planning/ROADMAP.md` | 11-01 (§Phase 11 criterion 1) | modified |
| `.planning/REQUIREMENTS.md` | 11-05 (FACADE-01 and FACADE-03(a) source corrections) | modified |
| `.planning/PROJECT.md` | 11-05 (2 `## Key Decisions` rows) | modified |
| `.project/…/deferred-items.md` | 11-01 (D5 clause), 11-02 (D1–D4 clauses) | annotated |
| `.project/…/deferred-features.md` | 11-03 | annotated |
| `.project/…/Epic_3/infrastructure-adapter-disposition.md` | **11-04** (existing Phase-10 banner extended, never duplicated) | annotated |

New headings and rows added to existing records: two `PROMOTION.md` Numbering-index rows (0034, 0035)
plus one dated note plus the `Next free ADR number` line; two `PROJECT.md` `## Key Decisions` rows;
dated amendments to five named ledger rows; a dated amendment to ROADMAP §Phase 11 criterion 1;
source corrections inside REQUIREMENTS.md's FACADE-01 and FACADE-03 blocks.

**No `.rs` file appears on this list, and none may be added to it (D-13).**

## Annotation ownership

`infrastructure-adapter-disposition.md` **already carries a Shape-B banner** written by Phase 10 plan
10-02 at lines 1-12, dated 2026-08-08 and citing ADR-0028. This plan **extends that banner in place**
with one pointer line and adds nothing else at the head of the file (Pitfall 2). Under no
circumstances stack a second blockquote. No other plan in this phase touches this document.

<tasks>

<task type="auto">
  <name>Task 1: Re-measure all 20 candidate paths and write the triage table</name>
  <read_first>
    - `.planning/phases/11-facade-residue-deferred-register-disposition/11-RESEARCH.md` → the `## FACADE-04 Verification Table` section in full. Every one of its 20 rows was existence-checked against the tree; use it as the starting position and re-run each check rather than re-deriving the verdicts from scratch (Pitfall 4). **Note that its own tallies disagree with its own rows** — the Summary paragraph says "11 of the 20", the tally paragraph says "13 rows" and lists thirteen numbers omitting row 12, while row 12's own cell reads `done`. Task 2 resolves that.
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md` in full — the 20-row table as it stands, each row's original "Stays / No change" verdict, each row's M9 target-crate cell, the `Date: 2025-01` header, and Phase 10's existing banner at lines 1-12.
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/prd-relocate-remaining-misplaced-modules.md` line 145 (the `arsenal/` "Stays… No" verdict) and §8 Q2 around lines 293-299 (sanctum folding into `paladin-memory`, naming no `paladin-sanctum`) — the governing PRD both disputed rows disagree with.
    - `.planning/decisions/0028-m8-reconciliation-authoritative.md` — `## Decision (iii)`'s commit range `e5b2011~1..a1e4901`, and its Context section's orphan-deletion list (Category 1), which is the authority for rows 3, 7-11, 14 and 16.
    - `.planning/ledgers/milestone-07-08.md` line 280 (`REQ-adapter-disposition-record`) and line 282 (`REQ-garrison-sanctum-bridges-kept`) — the already-derived findings on the two crate names and on the deliberately-kept garrison/sanctum bridges, including the do-not-re-delete marker on `paladin_registry.rs`.
    - `.planning/phases/11-facade-residue-deferred-register-disposition/11-CONTEXT.md` → D-00b, D-00e, D-00j, D-11, D-12, D-13.
  </read_first>
  <files>.planning/registers/facade-04-m9-candidate-triage.md</files>
  <action>
Re-run the existence check for every path before writing, and record each result with the command that
produced it per D-00e. A single loop over the paths is sufficient — for each, `[ -e <path> ] && echo
EXISTS || echo GONE` — supplemented by `ls` on the surviving directories (`arsenal/`, `logs/`,
`input/`, `document/`, `herald/`, `notifications/`, `citadel/`, `file_storage/`, `output/`, `queue/`)
so a directory reduced to a bare `mod.rs` is distinguished from one that still holds real files. Also
run `ls crates/` (expect 11 entries, neither `paladin-arsenal` nor `paladin-sanctum` among them). If
any path's state differs from the expectation below, record what you measured and flag the drift;
never write a disposition against a stale check.

Create `.planning/registers/facade-04-m9-candidate-triage.md`. Dated header naming FACADE-04 as its
requirement and `REQ-adapter-disposition-record` / `REQ-m8-epic3-no-extractions` as its ledger keys
per D-00f, and stating the triage baseline explicitly: **dispositions are derived against ADR-0028's
`e5b2011~1..a1e4901` range and the live tree, not against the disposition record's own claims**
(D-12). State the row-identity definition immediately after, before any counting: the unit of triage
is the source table's twenty rows, with the `input/*` fetcher group expanded to its five named files
(rows 7 through 11) so that a grouped source cell does not collapse five separate facts into one.

Then one table, rows in the source document's own order 1 through 20 and never re-sorted by
disposition, with columns `| # | Adapter path (as the source row names it) | Live-tree status
(command + result) | Disposition | Reason and authority |`. The disposition cell holds exactly one of
the three words `done`, `not a candidate`, `still open` — the reason goes in the next column, never
mixed into the disposition cell. Expected results, all confirmed against the tree at planning time:

  1. `adapters/arsenal/` — EXISTS, six files, still in the facade — **not a candidate**. The governing
     PRD says "Stays… No" at `prd-relocate-remaining-misplaced-modules.md:145` while this row marks it
     an M9 candidate targeting `paladin-arsenal`. Record both verdicts and state that this triage
     follows the PRD, per D-00b. Never acted on either way, so it is not superseded — it is decided.
  2. `adapters/citadel/file_citadel.rs` — GONE, directory holds only `mod.rs` re-exporting from
     `paladin_memory` — **done** (relocated to `paladin-memory`, facade keeps a stability re-export).
  3. `adapters/document/` — GONE, directory holds only `mod.rs` — **done** (deleted as an orphaned
     duplicate, ADR-0028 Category 1, commit `e5b2011`).
  4. `adapters/file_storage/minio.rs` — GONE, directory holds only `mod.rs` — **done** (relocated to
     `paladin-storage`).
  5. `adapters/garrison/mod.rs` — EXISTS as a re-export shim — **not a candidate** (kept deliberately;
     `REQ-garrison-sanctum-bridges-kept` is already `satisfied` with named multi-file consumer
     evidence).
  6. `adapters/herald/` — GONE as separate files, directory holds only `mod.rs` — **done**, and
     note that this contradicts the row's own "No" verdict: extracted to a brand-new `paladin-herald`
     crate by commit `66f6c4e`, despite the M8 Epic 3 §5 non-goal naming that crate as out of scope.
  7.–11. `adapters/input/{file_content_fetcher,file_content_list_fetcher,http_content_fetcher,local_file_fetcher,news_api_fetcher}.rs`
     — ALL GONE, `input/` holds only `mod.rs` — **done** for each of the five rows individually
     (deleted as orphaned duplicates, ADR-0028 Category 1, commit `e5b2011`).
  12. `adapters/input/tensorflow_adapter.rs` — GONE entirely — **done**, resolved differently from
     what the row's own "Action in Epic 3" cell describes: deleted outright by commit `3d48768`, not
     feature-gated. Note that this row's subject is FACADE-03(b)'s, not a relocation, and point at
     `.planning/registers/facade-03-removed-features.md`.
  13. `adapters/llm/` (`config_bridge.rs`) — EXISTS — **not a candidate** (stays; config mapping is
     facade-level, matching the row's own verdict).
  14. `adapters/logs/` — PARTIAL: `system_log_adapter.rs` EXISTS, `error_log_adapter.rs` GONE —
     **not a candidate, with an inventory correction.** Logging stays facade-level per the row's
     original "No", but the two-file inventory the row describes is now one file:
     `error_log_adapter.rs` was orphaned and deleted (ADR-0028 Category 1), not "stays" as the row
     claims. State the correction on the row rather than absorbing it silently.
  15. `adapters/notifications/` — GONE as separate files, directory holds only `mod.rs` — **done**
     (relocated to `paladin-notifications`).
  16. `adapters/output/api_content_deliverer.rs` — GONE — **done** (deleted as an orphaned duplicate,
     commit `e5b2011`). Cite the ledger's own correction of its LOC to 724, the 629 figure belonging
     to `tensorflow_adapter.rs`.
  17. `adapters/paladin_registry.rs` — GONE entirely — **done**, contradicting the row's own "No"
     verdict: consolidated into `paladin-battalion` by commit `ca7e4e8`, the richer 418-LOC facade
     implementation replacing battalion's thinner 67-LOC copy. **Carry the do-not-re-delete marker
     forward** — this was not an orphan deletion and its logic is now battalion's registry.
  18. `adapters/queue/redis.rs` — GONE, directory holds only `mod.rs` — **done** (relocated to
     `paladin-storage`).
  19. `adapters/sanctum/mod.rs` — EXISTS as a re-export shim — **not a candidate; its target name is
     an artefact.** Kept deliberately (`REQ-garrison-sanctum-bridges-kept` `satisfied`); the row's
     "future `paladin-sanctum` (M9)" target disagrees with the governing PRD §8 Q2, which folds
     sanctum into `paladin-memory` — a crate that already exists and already owns it. Record both
     verdicts, as with row 1.
  20. `adapters/scheduling/tokio_cron_adapter.rs` — EXISTS — **not a candidate** (stays; single
     concrete scheduler implementation, matching the row's own verdict).

Do not blanket-mark the table superseded (D-11): rows 1, 5, 13, 19 and 20 are live, deliberate
decisions and rows 1 and 19 were never acted on either way.
  </action>
  <verify>
    <automated>T=.planning/registers/facade-04-m9-candidate-triage.md; test -f "$T" || { echo "triage missing"; exit 1; }; awk -F'\|' '/^\| *[0-9]+ / {n++} END {print "numbered rows:", n; exit (n==20?0:1)}' "$T" && echo "20 rows present"</automated>
  </verify>
  <acceptance_criteria>
    - The `<verify>` awk count returns `numbered rows: 20` — every source row is present, with the `input/*` fetcher group expanded to rows 7 through 11 rather than collapsed into one.
    - Every row's disposition cell holds exactly one of the three words: `awk -F'\|' '/^\| *[0-9]+ /' "$T"` yields 20 lines and each contains one of `done`, `not a candidate`, `still open`.
    - No numbered row's disposition cell reads `still open`: `awk -F'\|' '/^\| *[0-9]+ /{print $5}' "$T" | grep -c 'still open'` returns `0` — the third bucket is genuinely empty, which Task 2 then states as a finding in prose.
    - Rows appear in ascending source order: `awk -F'\|' '/^\| *[0-9]+ /{gsub(/ /,"",$2); print $2}' "$T" | sort -c -n` succeeds, proving the table was not re-sorted by disposition.
    - Every row's live-tree status cell names a command: `awk -F'\|' '/^\| *[0-9]+ /' "$T" | grep -cE '\-e |ls |EXISTS|GONE'` returns `20` — no row's evidence is blank or reads only as a claim from the record.
    - `ls crates/ | wc -l` returns `11`, and `ls crates/ | grep -cE 'paladin-(arsenal|sanctum)'` returns `0`; both results appear in the register with the command that produced them.
    - Row 1 and row 19 each cite `prd-relocate-remaining-misplaced-modules.md` and state both the PRD's verdict and the row's own: `grep -c 'prd-relocate-remaining-misplaced-modules' "$T"` returns at least `2`.
    - Row 14 states the inventory correction: `grep -c 'error_log_adapter' "$T"` is non-zero and `grep -c 'system_log_adapter' "$T"` is non-zero.
    - Row 17 carries the do-not-re-delete marker: `grep -qi 'do not re-delete' "$T"` succeeds and `ca7e4e8` appears.
    - `grep -c 'e5b2011' "$T"` is non-zero — the triage baseline is ADR-0028's range, cited on the rows it resolves.
    - `git diff --name-only HEAD~1 HEAD` lists exactly `.planning/registers/facade-04-m9-candidate-triage.md`, and `git diff --name-only HEAD~1 HEAD | grep -c 'rs$'` returns `0`.
  </acceptance_criteria>
  <done>All 20 rows are triaged into one of the three words, in source order, each with a live-tree evidence cell produced by a named command and an authority cited from ADR-0028 or the governing PRD; rows 1, 14, 17 and 19 carry their specific corrections; no row is blank and the table is not blanket-marked superseded.</done>
</task>

<task type="auto">
  <name>Task 2: Write the triage's findings and extend the source document's existing banner</name>
  <precondition>`.planning/registers/facade-04-m9-candidate-triage.md` exists and carries 20 numbered rows (Task 1 of this plan).</precondition>
  <read_first>
    - `.planning/registers/facade-04-m9-candidate-triage.md` as just written — the 20 dispositions, so the tally is derived by counting the table rather than copied from anywhere.
    - `.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md` lines 1-12 — Phase 10's existing banner in full, including its closing sentence, so the extension matches its voice and is appended inside the same blockquote rather than stacked beneath it.
    - `.planning/PROJECT.md` around line 570 — the `### Out of Scope` entry that already records `paladin-arsenal` and `paladin-sanctum` as "none exists… named only by a superseded disposition record that contradicts its own governing PRD".
    - `.planning/phases/11-facade-residue-deferred-register-disposition/11-RESEARCH.md` → the `## FACADE-04 Verification Table`'s closing tally paragraph and Open Question 1.
    - `.planning/phases/11-facade-residue-deferred-register-disposition/11-CONTEXT.md` → D-00c, D-11, D-12.
  </read_first>
  <files>.planning/registers/facade-04-m9-candidate-triage.md, .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md</files>
  <action>
**(1) Append four findings sections to the triage register.**

`## Tally` — derive the counts by counting the table's own disposition cells, not by copying a figure
from any other document. The expected result, confirmed against the tree at planning time, is
**14 `done`, 6 `not a candidate`, 0 `still open`, summing to 20**: `done` on rows 2, 3, 4, 6, 7, 8, 9,
10, 11, 12, 15, 16, 17 and 18; `not a candidate` on rows 1, 5, 13, 14, 19 and 20. If your count of
your own table differs from 14/6/0, stop and reconcile the table before writing the tally — a stated
sum that disagrees with the rows above it is the exact defect this section exists to prevent. Record
explicitly that `11-RESEARCH.md`'s two internal figures are both superseded by this count: its Summary
paragraph says "11 of the 20" and its tally paragraph says "13 rows" while listing thirteen numbers
that omit row 12, whose own cell reads `done`. State the resolved figure once and name the row-identity
definition it was counted under.

`## Zero rows are still open — a finding, not an omission` — state plainly that FACADE-04's own third
bucket lands empty, and why that is a true and useful result rather than an incomplete triage: every
row's disposition is settled either by what already shipped inside ADR-0028's range or by a deliberate
decision to keep the code in the facade. A reader who expected some rows to remain open should read
this section rather than assume the triage stopped early.

`## The two crate names are artefacts` — record `paladin-arsenal` (named at rows 1, 36 and 81 of the
source document) and `paladin-sanctum` (rows 10 and 54) as artefacts of a mis-written table rather
than as future crates, on three grounds stated with their evidence: neither appears in `ls crates/`
(11 entries, quoted verbatim); each disagrees with the governing PRD on the same row that names it —
`arsenal/` is "Stays… No" at `prd-relocate-remaining-misplaced-modules.md:145`, and sanctum folds into
`paladin-memory` per §8 Q2, which already exists and already owns it; and PROJECT.md's `### Out of
Scope` already records exactly this. State that this record creates no crate and authorises none.

`## What this triage does not do` — state that no relocation is planned or executed here, and that no
row inside ADR-0028's `e5b2011~1..a1e4901` range is re-planned regardless of what the source record
says about it (D-12). Also record that the source document's `Date: 2025-01` header remains
inconsistent with every other Milestone 8 document — noted as residue, not corrected, since correcting
it would be a rewrite rather than an annotation.

**(2) Extend the existing banner on `infrastructure-adapter-disposition.md`.** The document already
carries a Phase-10 blockquote at lines 1-12 ending "The original text below is retained unmodified."
Add **one** dated 2026-08-08 line **inside that same blockquote**, immediately before its closing
sentence, naming plan 11-04 and stating that every one of the 20 rows is now triaged at
`.planning/registers/facade-04-m9-candidate-triage.md`, that the two crate names are recorded there as
artefacts, and that no row is still open. Do not create a second blockquote, do not re-date Phase 10's
line, and do not modify anything below the banner — the 20-row table, its target-crate cells and the
`Date: 2025-01` header all stay exactly as they are, because those cells are the evidence the triage
cites.
  </action>
  <verify>
    <automated>T=.planning/registers/facade-04-m9-candidate-triage.md; S=.project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md; grep -q 'Tally' "$T" || { echo "tally section missing"; exit 1; }; grep -q 'paladin-arsenal' "$T" || { echo "arsenal finding missing"; exit 1; }; grep -q 'facade-04-m9-candidate-triage' "$S" || { echo "banner pointer missing"; exit 1; }; B=$(grep -c 'The original text below is retained unmodified' "$S"); [ "$B" = 1 ] || { echo "FAIL: expected exactly one banner, found $B"; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'The original text below is retained unmodified' .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md` returns `1` — Phase 10's banner was extended, not duplicated (Pitfall 2).
    - `grep -c 'facade-04-m9-candidate-triage' .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md` returns at least `1`.
    - `git diff HEAD~1 HEAD -- .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md --numstat` shows deletions of `0`, and every added line falls within the first 14 lines of the file — the 20-row table and the `Date: 2025-01` header are untouched.
    - `grep -c 'Date: 2025-01' .project/Milestone_8-Facade-Cleanup-Shim-Resolution/Epic_3/infrastructure-adapter-disposition.md` is unchanged from before this task.
    - The register's `## Tally` states 14 / 6 / 0 summing to 20, and those three figures agree with a live count of the table's own disposition cells — re-run `awk -F'\|' '/^\| *[0-9]+ /' "$T" | grep -c 'not a candidate'` (expect `6`) and confirm the remaining 14 rows read `done`.
    - `grep -Eic 'still open' .planning/registers/facade-04-m9-candidate-triage.md` is non-zero **only** in the findings heading and prose, never as a disposition cell value.
    - The register names RESEARCH.md's two superseded figures: `grep -c '11-RESEARCH' .planning/registers/facade-04-m9-candidate-triage.md` is non-zero and both `11 of the 20` and `13` are addressed.
    - `ls crates/ | grep -cE 'paladin-(arsenal|sanctum)'` returns `0` after this task — no crate was created — and `ls crates/ | wc -l` is unchanged at `11`.
    - `grep -c 'Out of Scope' .planning/registers/facade-04-m9-candidate-triage.md` is non-zero — PROJECT.md's binding entry is cited.
    - `git diff --name-only HEAD~1 HEAD | grep -c 'rs$'` returns `0`.
  </acceptance_criteria>
  <done>The triage register states a tally that agrees with its own rows, records zero-still-open as a finding, records both crate names as artefacts with three cited grounds, and states what it deliberately does not do; the source document carries exactly one banner, extended in place with a pointer to the triage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| A superseded `.project/` record → a forward plan | The document the Epic 3 PRD designates "the authoritative cross-reference for the §4.3 M9 flags" asserts twenty things the tree contradicts; a reader who trusts it plans relocations that already happened |
| ADR-0028's measured commit range → each row's disposition | The boundary that decides `done` by outcome rather than by claim |
| the source record's target-crate cells → the crate namespace | Two names that appear authoritative but correspond to nothing in `crates/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-20 | Spoofing | The 20 per-row live-tree status cells | high | mitigate | A disposition that misstates the tree is the precise failure FACADE-04 exists to prevent, and it would be inherited as settled. Every path is re-checked at execution time with `[ -e … ]` plus `ls` on surviving directories, the command and result recorded per D-00e, and the task halts on any drift from the stated expectation. |
| T-11-21 | Spoofing | `paladin-arsenal` / `paladin-sanctum` as target crates | high | mitigate | Two names that read as future crates would send a future phase to build them, against PROJECT.md's `### Out of Scope`. Recorded as artefacts on three cited grounds — `ls crates/` output, the governing PRD's contrary verdict on the same rows, and PROJECT.md — with acceptance criteria asserting no crate was created. |
| T-11-22 | Repudiation | The tally | medium | mitigate | A stated sum that disagrees with the rows above it cannot be challenged coherently, and RESEARCH.md already carries two such figures. The tally is derived by counting the table under a stated row-identity definition, both superseded figures are named, and the acceptance criteria re-count independently. |
| T-11-23 | Tampering | `infrastructure-adapter-disposition.md` | medium | mitigate | Annotation only, and extension of one existing banner rather than a second (Pitfall 2). Acceptance criteria assert `--numstat` deletions of `0`, exactly one banner sentence, and additions confined to the head of the file. |
| T-11-24 | Elevation of Privilege | The triage's authority over the source record | medium | mitigate | The triage sits in `.planning/` and outranks the `.project/` record under D-00b, so a careless blanket "superseded" would retire five live, deliberate decisions along with the wrong ones. D-11 forbids blanket-marking; rows 1, 5, 13, 19 and 20 are decided individually and `arsenal/` is explicitly recorded as never acted on either way. |
| T-11-25 | Information Disclosure | — | low | accept | Not applicable. The triage contains repository-local adapter paths, commit SHAs and directory listings already present in the checkout; no secret or personal datum is read or written. |
| T-11-26 | Denial of Service | — | low | accept | Not applicable. No runtime surface is touched; execution is roughly thirty existence checks and two writes. |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable, truthfully: this plan installs, upgrades and removes nothing, and creates no crate. No manifest or lockfile is in its `<files>`; the `ls crates/` and `git diff --name-only` criteria prove it. |
</threat_model>

<verification>
- All 20 numbered rows present, in source order, each with one of the three disposition words and a command-backed evidence cell.
- The tally 14 / 6 / 0 agrees with a live count of the table.
- `ls crates/` returns 11 entries with neither disputed name, before and after.
- `infrastructure-adapter-disposition.md` carries exactly one banner and zero deleted lines.
- No commit in this plan touches a Rust source file.
</verification>

<success_criteria>
- ROADMAP criterion 5 is met: every row is marked, and both crate names are recorded as artefacts of a table that contradicts its own governing PRD.
- Nobody reading `.planning/` re-plans a relocation inside ADR-0028's `e5b2011~1..a1e4901` range.
- The empty `still open` bucket is stated as a finding, so the triage cannot be mistaken for incomplete.
- The source document still holds every original word that the triage cites as evidence.
</success_criteria>

<output>
Create `.planning/phases/11-facade-residue-deferred-register-disposition/11-04-SUMMARY.md` when done.
</output>
