---
phase: 11-facade-residue-deferred-register-disposition
plan: 05
type: execute
wave: 3
depends_on: ["11-01", "11-02", "11-03", "11-04"]
files_modified:
  - .planning/ledgers/milestone-07-08.md
  - .planning/REQUIREMENTS.md
  - .planning/decisions/PROMOTION.md
  - .planning/PROJECT.md
autonomous: true
requirements: [FACADE-01, FACADE-02, FACADE-03, FACADE-04]

must_haves:
  truths:
    - "All five named ledger rows close against this phase's artefacts by citation — `REQ-m8-deferred-items-register`, `REQ-deferred-cli-user-commands`, `REQ-deferred-tensorflow-ml-adapter-v3`, `REQ-adapter-disposition-record` and `REQ-m8-epic3-no-extractions` — each with the exact command or `file:line` that produced its verdict (D-00e)"
    - "The `pending — plan 11-05` marker plan 11-01 left on the D1–D4 half of `REQ-m8-deferred-items-register` is resolved, not abandoned: the row now records verdicts for all five deferred items"
    - "Ledger amendment is in place: `grep -c '^| REQ-'` reads 86 before and after, and no row is inserted, deleted or reordered (D-00d, `milestone-07-08.md:144-150`)"
    - "REQUIREMENTS.md's FACADE-01 'Done when' clause is corrected at source so it no longer implies a `log::*` conversion should happen, with the original retained (D-02, D-00c)"
    - "REQUIREMENTS.md's FACADE-03(a) recovery sentence is corrected at source: one commit `3d48768` removed both features, so attributing the CLI removal to a branch and the ML removal to a commit splits one event across two pointer kinds (D-10)"
    - "`PROMOTION.md` is updated as this phase's last act, in the six-step order its own procedure at lines 139-151 prescribes, with a dated note explaining the advance rather than a silent renumber (D-14)"
    - "`PROJECT.md`'s `## Key Decisions` table gains one row per new ADR, per step 6 of the promotion procedure"
    - "Every ADR number this phase took is contiguous, none skipped and none reused, proven by `ls` before the numbering line is advanced"
    - "No Rust source file is modified by this plan (D-13)"
  artifacts:
    - path: ".planning/ledgers/milestone-07-08.md"
      provides: "Five amended rows closing FACADE-01 through FACADE-04 against this phase's registers and ADRs"
      contains: "plan 11-05"
    - path: ".planning/decisions/PROMOTION.md"
      provides: "Two Numbering-index rows, a dated advance note, and the advanced Next free ADR number line"
      contains: "Next free ADR number"
    - path: ".planning/PROJECT.md"
      provides: "One `## Key Decisions` row per ADR this phase authored"
      contains: "## Key Decisions"
  key_links:
    - from: ".planning/ledgers/milestone-07-08.md"
      to: ".planning/registers/facade-01-rustdoc-stdout-disposition.md"
      via: "the REQ-m8-deferred-items-register Evidence cell cites the register for D5 and ADR-0034 for D1-D4"
      pattern: "facade-01-rustdoc-stdout-disposition"
    - from: ".planning/decisions/PROMOTION.md"
      to: ".planning/decisions/0034-d1-d4-facade-relocation-disposition.md"
      via: "a Numbering-index row per ADR, and a dated note whose ls check confirms the files exist with contiguous numbers"
      pattern: "0034"
    - from: ".planning/PROJECT.md"
      to: ".planning/decisions/0035-paladin-ml-leaf-crate-placement.md"
      via: "a `## Key Decisions` row linking to the ADR file, per promotion-procedure step 6"
      pattern: "0035-paladin-ml-leaf-crate-placement"
  prohibitions:
    - "A correction must not delete or rewrite the text it corrects. REQUIREMENTS.md's original FACADE-01 'Done when' clause and its original FACADE-03(a) recovery sentence, and every superseded ledger Evidence cell, must all still be readable after this plan. These are governing documents; a correction that erases what it corrects leaves a later auditor unable to tell a fixed defect from one that never existed."
---

<objective>
Close the phase: amend the five ledger rows FACADE-01 through FACADE-04 answer to, correct the two
defective claims in REQUIREMENTS.md at source, and advance the ADR numbering index — in that order,
with `PROMOTION.md` last.

Purpose: the registers and ADRs written in waves 1 and 2 are only findable if the ledger rows that
own the `REQ-*` keys point at them, and the ADR numbering line is shared state that every later phase
reads before writing. D-14 makes advancing it this phase's last act, after the ADR files exist and
have been counted.

Output: five amended ledger rows, two source corrections in REQUIREMENTS.md, two `PROMOTION.md` index
rows plus a dated note plus the advanced next-free line, and two `PROJECT.md` `## Key Decisions` rows.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-facade-residue-deferred-register-disposition/11-CONTEXT.md
@.planning/phases/11-facade-residue-deferred-register-disposition/11-PATTERNS.md
@.planning/phases/11-facade-residue-deferred-register-disposition/11-RESEARCH.md
</context>

## Artifacts this phase produces

| Artifact | Plan | Kind |
|---|---|---|
| `.planning/registers/facade-01-rustdoc-stdout-disposition.md` | 11-01 | new file |
| `.planning/registers/facade-03-removed-features.md` | 11-03 | new file |
| `.planning/registers/facade-04-m9-candidate-triage.md` | 11-04 | new file |
| `.planning/decisions/0034-d1-d4-facade-relocation-disposition.md` | 11-02 | new ADR |
| `.planning/decisions/0035-paladin-ml-leaf-crate-placement.md` | 11-03 | new ADR |
| `.planning/decisions/PROMOTION.md` | **11-05** | modified — 2 index rows, 1 dated note, next free → 0036 |
| `.planning/ledgers/milestone-07-08.md` | 11-01 (D5 half of one row), **11-05** (all remaining rows) | modified |
| `.planning/ROADMAP.md` | 11-01 (§Phase 11 criterion 1) | modified |
| `.planning/REQUIREMENTS.md` | **11-05** (FACADE-01 and FACADE-03(a) source corrections) | modified |
| `.planning/PROJECT.md` | **11-05** (2 `## Key Decisions` rows) | modified |
| `.project/…/deferred-items.md` | 11-01 (D5 clause), 11-02 (D1–D4 clauses) | annotated |
| `.project/…/deferred-features.md` | 11-03 | annotated |
| `.project/…/Epic_3/infrastructure-adapter-disposition.md` | 11-04 (existing banner extended) | annotated |

New headings and rows added to existing records: two `PROMOTION.md` Numbering-index rows (0034, 0035)
plus one dated note plus the `Next free ADR number` line; two `PROJECT.md` `## Key Decisions` rows;
dated amendments to five named ledger rows; a dated amendment to ROADMAP §Phase 11 criterion 1;
source corrections inside REQUIREMENTS.md's FACADE-01 and FACADE-03 blocks.

**No `.rs` file appears on this list, and none may be added to it (D-13).**

## Ledger contention

This plan is the only Phase-11 plan that writes `.planning/ledgers/milestone-07-08.md` in wave 3.
Plan 11-01 wrote the D5 half of `REQ-m8-deferred-items-register` in wave 1 and left an explicit
`pending — plan 11-05` marker on the D1–D4 half; this plan resolves that marker and amends the other
four rows. No other plan in any wave touches this file.

## Flagged assumptions carried into this plan

- **The ADR numbers are 0034 and 0035 only under option-a** of plan 11-01's decision checkpoint. Read
  the SUMMARYs of plans 11-02 and 11-03 and `ls .planning/decisions/` before writing any index row;
  record the numbers actually taken. If option-c was selected, plan 11-02 wrote no ADR and the D1–D4
  verdicts live in the ledger row instead — amend accordingly and say so in the dated note.
- **Neither new ADR closes an entry in `PROMOTION.md`'s Part B inventory.** The eleven candidates
  listed there do not include `deferred-features.md`, so no Part B entry gains a "Closed by" note this
  phase. Confirm before writing rather than assuming, and do not invent a closure.
- **This plan's `one-way` reversibility gate is satisfied across plans, by design.** `verify
  plan-structure` warns that Task 3 carries `rating="one-way"` with no `checkpoint:decision` earlier
  in *this* file. That is intentional: the ADR allocation this task records was gated once, at the end
  of plan 11-01 in wave 1, and by the time this plan runs the numbers are already spent — the files
  exist. Re-asking here would gate a door already walked through, which the reversibility taxonomy
  names as an anti-pattern. The rating stays `one-way` because advancing the numbering line is
  genuinely irreversible; only the gate's location moved.

<tasks>

<task type="auto">
  <name>Task 1: Amend the five ledger rows in place</name>
  <read_first>
    - `.planning/ledgers/milestone-07-08.md` lines 144-171 — the row-order and amendment convention ("Later plans replace a row's Verdict and Evidence cells in place; they never insert, delete, or reorder rows"), the ledger-file-contention table shape, and the `pending — plan NN-NN` convention.
    - `.planning/ledgers/milestone-07-08.md` lines 79-89 — the verdict legend and its exact vocabulary (`satisfied`, `present, unproven`, `genuinely outstanding`, `relocated`, `superseded by outcome`, `deferred with register`, `diverged`), and lines 16-22 for the evidence bar these rows must clear.
    - `.planning/ledgers/milestone-07-08.md` line 280 (`REQ-adapter-disposition-record`), line 282 (`REQ-garrison-sanctum-bridges-kept`, for context only — this plan does not amend it), line 283 (`REQ-m8-epic3-no-extractions`), line 345 (`REQ-m8-deferred-items-register`, carrying plan 11-01's amendment and its pending marker), line 346 (`REQ-deferred-cli-user-commands`) and line 347 (`REQ-deferred-tensorflow-ml-adapter-v3`) — each row's current Verdict and Evidence cells in full.
    - `.planning/ledgers/milestone-07-08.md` line 135 — the head-of-file "Superseded by outcome" summary-table entry for `REQ-m8-epic3-no-extractions`, to confirm the section row's amendment leaves the summary entry still true.
    - The five artefacts this phase produced: `.planning/registers/facade-01-rustdoc-stdout-disposition.md`, `.planning/registers/facade-03-removed-features.md`, `.planning/registers/facade-04-m9-candidate-triage.md`, `.planning/decisions/0034-d1-d4-facade-relocation-disposition.md` and `.planning/decisions/0035-paladin-ml-leaf-crate-placement.md` — read each one's headline claims so the ledger cells cite them accurately rather than paraphrasing.
    - `.planning/phases/11-facade-residue-deferred-register-disposition/11-CONTEXT.md` → D-00d, D-00e, D-00f, D-13.
  </read_first>
  <files>.planning/ledgers/milestone-07-08.md</files>
  <action>
Record `grep -c '^| REQ-' .planning/ledgers/milestone-07-08.md` before starting (expect `86`) and
re-run it after; the two must match. Replace Verdict and Evidence cell content only. Never insert,
delete or reorder a row.

Amend these five rows, each with a dated `**Amended 2026-08-08 (plan 11-05):**` passage appended to
its Evidence cell, retaining the cell's existing text including plan 11-01's earlier amendment:

  - **`REQ-m8-deferred-items-register`** (line ~345). Remove the D1–D4 pending marker plan 11-01 left
    on this row — the one naming this plan as its owner — and do not restate that marker's wording
    anywhere in the replacement prose, since a later gate asserts the marker string is gone from the
    file. Record instead that all five deferred items now carry a disposition — D5's
    per-occurrence rustdoc dispositions at
    `.planning/registers/facade-01-rustdoc-stdout-disposition.md`, and D1 through D4's verdicts,
    owners and triggers in `.planning/decisions/0034-d1-d4-facade-relocation-disposition.md`. Name
    each of the four verdicts in one clause each so the row is readable without opening the ADR. The
    Verdict cell stays in the legend's vocabulary; `deferred with register` remains correct because
    every item is deferred or withdrawn with a register behind it — if you judge a different legend
    word more accurate, use one from lines 79-89 and say why, never invent one.
  - **`REQ-deferred-cli-user-commands`** (line ~346). Cite
    `.planning/registers/facade-03-removed-features.md` §1 as the `.planning`-native home of the
    record, and give the recovery pointer in its runnable form
    `git show 3d48768^:src/application/cli/commands/user.rs`. Record that the recovery instruction the
    register carries is a commit SHA rather than the branch name this row's own register text names,
    and why.
  - **`REQ-deferred-tensorflow-ml-adapter-v3`** (line ~347). Cite
    `.planning/registers/facade-03-removed-features.md` §2 and
    `.planning/decisions/0035-paladin-ml-leaf-crate-placement.md`, and record that the placement
    condition is now promoted out of DOC precedence into an ADR — which is what FACADE-03's "survives
    outside a single DOC" clause asked for. Restate the asymmetric non-goal split in one clause.
  - **`REQ-adapter-disposition-record`** (line ~280). Cite
    `.planning/registers/facade-04-m9-candidate-triage.md` and record the tally the triage derived
    (14 `done`, 6 `not a candidate`, 0 `still open`, summing to 20) plus the finding that
    `paladin-arsenal` and `paladin-sanctum` are artefacts of a mis-written table, with `ls crates/`
    as evidence.
  - **`REQ-m8-epic3-no-extractions`** (line ~283). Cite the triage as the row-level confirmation that
    no relocation inside ADR-0028's `e5b2011~1..a1e4901` range is re-planned by this phase, and that
    every row resolved by that range is marked `done` by outcome rather than by claim.

After amending, confirm the head-of-file summary-table entry for `REQ-m8-epic3-no-extractions` at line
135 still states something true; if the section row's amendment has made it inconsistent, amend that
entry in place too rather than leaving the two cells disagreeing, and say so in the SUMMARY.
  </action>
  <verify>
    <automated>L=.planning/ledgers/milestone-07-08.md; N=$(grep -c '^| REQ-' "$L"); [ "$N" = 86 ] || { echo "FAIL: REQ row count is $N, expected 86"; exit 1; }; for r in REQ-m8-deferred-items-register REQ-deferred-cli-user-commands REQ-deferred-tensorflow-ml-adapter-v3 REQ-adapter-disposition-record REQ-m8-epic3-no-extractions; do grep -q "^| $r " "$L" || { echo "MISSING row $r"; exit 1; }; done; C=$(grep -c 'plan 11-05' "$L"); [ "$C" -ge 5 ] || { echo "FAIL: only $C rows carry a plan 11-05 amendment"; exit 1; }; echo "86 rows intact, 5 amended"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| REQ-' .planning/ledgers/milestone-07-08.md` returns `86` — identical to its value before this task; no row inserted, deleted or reordered.
    - `grep -c 'plan 11-05' .planning/ledgers/milestone-07-08.md` returns at least `5` — one dated amendment per row.
    - `grep -c 'pending — plan 11-05' .planning/ledgers/milestone-07-08.md` returns `0` — plan 11-01's marker is resolved, not left dangling.
    - `grep -c 'plan 11-01' .planning/ledgers/milestone-07-08.md` is non-zero — plan 11-01's earlier amendment text is retained, not overwritten.
    - Each of the five rows cites its artefact: `grep -c 'facade-01-rustdoc-stdout-disposition' "$L"`, `grep -c '0034-d1-d4-facade-relocation-disposition' "$L"`, `grep -c 'facade-03-removed-features' "$L"`, `grep -c '0035-paladin-ml-leaf-crate-placement' "$L"` and `grep -c 'facade-04-m9-candidate-triage' "$L"` are each non-zero.
    - `grep -qF 'git show 3d48768^:src/application/cli/commands/user.rs' "$L"` succeeds — the recovery pointer is in the ledger in runnable form.
    - Every amended Verdict cell uses a word from the legend at lines 79-89: `awk -F'\|' '/^\| REQ-/{print $3}' "$L" | sort -u` yields only legend vocabulary, with no newly invented verdict word.
    - `git diff HEAD~1 HEAD -- .planning/ledgers/milestone-07-08.md --numstat` shows added lines equal to deleted lines, consistent with pure cell replacement rather than row insertion.
    - `git diff --name-only HEAD~1 HEAD` lists exactly `.planning/ledgers/milestone-07-08.md`, and `| grep -c 'rs$'` returns `0`.
  </acceptance_criteria>
  <done>All five rows carry a dated plan 11-05 amendment citing this phase's artefacts by path, the D1–D4 pending marker is resolved, the row count is unchanged at 86, and every verdict word comes from the ledger's own legend.</done>
</task>

<task type="auto">
  <name>Task 2: Correct FACADE-01's and FACADE-03(a)'s defective claims in REQUIREMENTS.md at source</name>
  <reversibility rating="costly">REQUIREMENTS.md is a governing document every later phase reads; a correction here changes what a future auditor of FACADE-01 and FACADE-03 measures the phase against, which is why the superseded original must stay visible (D-00c).</reversibility>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 1616-1635 — FACADE-01's full text, in particular its closing `**Done when** each of the 17 is either converted to `log::*` or annotated as deliberate stdout, with the disposition recorded per file.` clause.
    - `.planning/REQUIREMENTS.md` lines 1668-1694 — FACADE-03's full text, in particular the sentence directing recovery to "the Milestone 8 removal commit on branch `chore/facade-cleanup-m8-finish`" and the separate attribution of the ML removal to commit `3d48768`.
    - `.planning/decisions/0031-extracted-crate-dependency-rule.md` around line 149 — the precedent for correcting a REQUIREMENTS.md citation error in place rather than silently, which is the convention to match.
    - `.planning/registers/facade-01-rustdoc-stdout-disposition.md` and `.planning/registers/facade-03-removed-features.md` — the two records these corrections point at, and (for FACADE-03) the branch state actually measured by plan 11-03.
    - `.planning/ROADMAP.md` §Phase 11 criterion 1 as amended by plan 11-01 — so the REQUIREMENTS.md correction says the same thing in the same terms rather than a third variant.
    - `.planning/phases/11-facade-residue-deferred-register-disposition/11-CONTEXT.md` → D-00c, D-02, D-10.
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Two in-place corrections, each dated 2026-08-08 and naming plan 11-05, each leaving the original
sentence readable beneath or beside it. This is annotation, not rewriting (D-00c).

**(1) FACADE-01's framing.** The requirement's `**Done when**` clause offers a conversion branch that
should not be taken, and the surrounding text is premised on these being runtime stdout in library
code. Append a `**Corrected 2026-08-08 (plan 11-05):**` passage immediately after the clause stating
the measured finding: the grep returns exactly 17 occurrences across exactly 6 files, every one is a
`///` or `//!` doc-comment line inside a fenced code block, the same grep filtered to non-doc-comment
lines returns nothing, so all 17 resolve to the annotate branch and the conversion branch is not
available. Point at `.planning/registers/facade-01-rustdoc-stdout-disposition.md` and at the amended
ROADMAP criterion 1, and use the same figures and the same two commands both of those carry, so the
three statements agree word for word on the numbers. Do not delete or reword the original clause.

**(2) FACADE-03(a)'s recovery attribution.** The requirement points recovery at a branch for the CLI
removal while attributing the ML removal to commit `3d48768`. Append a
`**Corrected 2026-08-08 (plan 11-05):**` passage stating that **one commit did both** — `3d48768`,
2026-06-04, "chore(facade): remove half-built user CLI + tensorflow ML stub (M8)" — so the two
removals are one event and should carry one pointer; and that the durable pointer is the immutable
SHA in its runnable form `git show 3d48768^:src/application/cli/commands/user.rs`, because a branch
ref is mutable and deletable while a SHA is not. Record the branch state exactly as plan 11-03
measured it rather than asserting an absence: at planning time no *local* branch of that name existed
while the remote-tracking ref `refs/remotes/origin/chore/facade-cleanup-m8-finish` did resolve, which
contradicts the "not present as a local or remote ref" framing carried in `11-CONTEXT.md`'s D-10.
Point at `.planning/registers/facade-03-removed-features.md` §1. Do not delete or reword the original
sentence — it is the evidence for the correction.
  </action>
  <verify>
    <automated>R=.planning/REQUIREMENTS.md; grep -qF 'chore/facade-cleanup-m8-finish' "$R" || { echo "original branch reference lost"; exit 1; }; grep -qF 'converted to `log::*`' "$R" || { echo "original FACADE-01 clause lost"; exit 1; }; C=$(grep -c 'Corrected 2026-08-08 (plan 11-05)' "$R"); [ "$C" -ge 2 ] || { echo "FAIL: expected 2 dated corrections, found $C"; exit 1; }; grep -qF 'facade-01-rustdoc-stdout-disposition' "$R" && grep -qF 'facade-03-removed-features' "$R" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF 'chore/facade-cleanup-m8-finish' .planning/REQUIREMENTS.md` succeeds — the original branch reference is retained as the evidence for the correction.
    - `grep -c 'Corrected 2026-08-08 (plan 11-05)' .planning/REQUIREMENTS.md` returns at least `2` — one dated correction per defective claim.
    - `grep -qF 'git show 3d48768^:src/application/cli/commands/user.rs' .planning/REQUIREMENTS.md` succeeds.
    - The FACADE-01 correction carries the same figures as the amended ROADMAP criterion 1 and the register: `grep -c '17' .planning/REQUIREMENTS.md` is non-zero within the FACADE-01 block, and the two deciding commands appear verbatim.
    - `grep -c 'facade-01-rustdoc-stdout-disposition' .planning/REQUIREMENTS.md` and `grep -c 'facade-03-removed-features' .planning/REQUIREMENTS.md` are each non-zero.
    - The FACADE-03 correction records the measured branch state and does **not** assert that no remote ref exists unless `git rev-parse --verify refs/remotes/origin/chore/facade-cleanup-m8-finish` actually fails at execution time.
    - `git diff HEAD~1 HEAD -- .planning/REQUIREMENTS.md --numstat` shows deletions of `0` — both corrections are purely additive, so no original requirement text was removed.
    - `git diff --name-only HEAD~1 HEAD` lists exactly `.planning/REQUIREMENTS.md`, and `| grep -c 'rs$'` returns `0`.
  </acceptance_criteria>
  <done>Both defective claims carry a dated in-place correction pointing at this phase's registers, both originals are still readable, the FACADE-01 figures agree word for word with the ROADMAP amendment and the register, and the FACADE-03 correction states the branch state that was actually measured.</done>
</task>

<task type="auto">
  <name>Task 3: Advance the ADR numbering index and add the Key Decisions rows — the phase's last act</name>
  <reversibility rating="one-way">`PROMOTION.md:141` states that ADR numbers are never reused, even if a candidate is later rejected; advancing the next-free line spends the numbers permanently. The allocation was confirmed by plan 11-01's decision checkpoint.</reversibility>
  <precondition>Every ADR file this phase authored exists on disk — verify with `ls .planning/decisions/00*-*.md` and read plans 11-02's and 11-03's SUMMARYs for the numbers actually taken before writing any index row.</precondition>
  <read_first>
    - `.planning/decisions/PROMOTION.md` in full — the Numbering index table's exact `| Number | Slug | Subject |` row shape (lines 21-55), the four existing dated notes (lines 59-91) and their voice, the `**Next free ADR number: 0034**` line at line 57, the required heading set at lines 95-116, and the six-step promotion procedure at lines 139-151.
    - `.planning/PROJECT.md` lines 1103-1140 — the `## Key Decisions` table's column shape (`| Decision | Rationale | Outcome |`), its `[title](.planning/decisions/NNNN-slug.md) (ADR-NNNN)` link convention in the first column, and the vocabulary its Outcome column uses (`conforms`, `✓ Good — applied in Phase N`, `— Pending — …`).
    - `.planning/decisions/0034-d1-d4-facade-relocation-disposition.md` and `.planning/decisions/0035-paladin-ml-leaf-crate-placement.md` — each ADR's title line and `## Code Conformance` verdict, so the index subject line and the Key Decisions Outcome cell state what the ADR actually says.
    - `.planning/phases/11-facade-residue-deferred-register-disposition/11-PATTERNS.md` → the `PROMOTION.md` section, which gives the dated-note shape and the index-row shape to copy.
    - `.planning/phases/11-facade-residue-deferred-register-disposition/11-CONTEXT.md` → D-14.
  </read_first>
  <files>.planning/decisions/PROMOTION.md, .planning/PROJECT.md</files>
  <action>
Run `ls .planning/decisions/0034-*.md .planning/decisions/0035-*.md` first and record its output; the
dated note quotes it as proof the files exist with contiguous numbers. Then follow the six-step
procedure at `PROMOTION.md:139-151`, with step 5 last.

**(1) Add two rows to the Numbering index table**, in the same `| Number | Slug | Subject |` shape as
the 33 rows above them, appended after the `| 0033 | …` row and never re-sorting or renumbering any
existing row:
  - `| 0034 | `d1-d4-facade-relocation-disposition` | D1–D4 verdicts — `src/core/` shims defer, the `user_service.rs` split withdrawn, D3/D4 defer-with-trigger under ADR-0031 (Phase 11) |`
  - `| 0035 | `paladin-ml-leaf-crate-placement` | The `paladin-ml` leaf-crate placement condition for a future ML adapter, promoted out of DOC precedence (Phase 11) |`

**(2) Add a dated note** beneath the next-free line, above the existing 2026-08-08 (plan 10-11) note,
matching the voice of the four notes already there. It states: the line advances by **two** in one
phase, from 0034 to 0036, because Phase 11 authored ADR-0034 (plan 11-02, the D1–D4 disposition set)
and ADR-0035 (plan 11-03, the `paladin-ml` placement condition) across its own plans; both carry
`conforms` verdicts, so unlike Phase 10 neither instructs a code change (D-13 — this phase changed
zero executable Rust); the `ls` output above confirms both files exist with contiguous numbers, none
skipped or reused; and no existing index row was renumbered, reworded or reordered. Add one further
sentence recording that **neither ADR closes an entry in Part B's eleven-candidate inventory** —
`deferred-features.md` is not among them — so no Part B "Closed by" note is added. If plan 11-01's
checkpoint selected option-b or option-c, state the numbers actually taken and why the advance differs
from two.

**(3) Add two rows to `.planning/PROJECT.md`'s `## Key Decisions` table**, per step 6, appended after
the existing rows in the table's own `| Decision | Rationale | Outcome |` shape and using its
established link convention:
  - one for ADR-0034, whose Rationale states in one sentence that each of D1–D4 carries a verb, a
    named owner and a trigger where deferred, and that no relocation executes; Outcome `conforms`.
  - one for ADR-0035, whose Rationale states the placement condition in one sentence — a dedicated
    `paladin-ml` leaf crate with the `ml` flag on that crate, never the facade, `MlPort` staying in
    the workspace; Outcome `conforms`.

**(4) Update the `Next free ADR number` line to `**Next free ADR number: 0036**` — last, after every
other edit in this task and in this phase** (D-14, step 5 of the procedure). Under option-b or
option-c, set it to whatever the actual allocation reached instead.
  </action>
  <verify>
    <automated>P=.planning/decisions/PROMOTION.md; J=.planning/PROJECT.md; ls .planning/decisions/0034-*.md .planning/decisions/0035-*.md >/dev/null 2>&1 || { echo "ADR files missing"; exit 1; }; N=$(grep -c '^| 00' "$P"); [ "$N" = 35 ] || { echo "FAIL: index has $N rows, expected 35"; exit 1; }; grep -q 'Next free ADR number: 0036' "$P" || { echo "next-free line not advanced"; exit 1; }; grep -q '0034-d1-d4-facade-relocation-disposition' "$J" && grep -q '0035-paladin-ml-leaf-crate-placement' "$J" && echo "index 35 rows, next free 0036, both Key Decisions rows present"</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/decisions/0034-*.md .planning/decisions/0035-*.md` lists exactly two files, and their names match the slugs written into the index rows.
    - `grep -c '^| 00' .planning/decisions/PROMOTION.md` returns `35` — two rows added to the 33 that existed, and none renumbered: `awk -F'\|' '/^\| 00/{gsub(/ /,"",$2); print $2}' .planning/decisions/PROMOTION.md | sort -c` succeeds, proving the index is still in ascending contiguous order.
    - `grep -c 'Next free ADR number' .planning/decisions/PROMOTION.md` returns `1` and that line reads `**Next free ADR number: 0036**`.
    - `grep -c 'Dated note, 2026-08-08 (plan 11-05)' .planning/decisions/PROMOTION.md` returns `1`, and that note contains the `ls` output, the word `two` for the advance, and the Part B sentence.
    - Every previously existing index row is byte-identical: `git diff HEAD~1 HEAD -- .planning/decisions/PROMOTION.md` shows no hunk modifying a `| 00` row numbered 0001 through 0033.
    - `.planning/PROJECT.md`'s `## Key Decisions` table gained exactly two rows: its row count rises from `29` to `31` (`sed -n '/^## Key Decisions/,/^## /p' .planning/PROJECT.md | grep -c '^| \['`).
    - Both new `PROJECT.md` rows link to the ADR files using the table's existing `[title](.planning/decisions/NNNN-slug.md) (ADR-NNNN)` convention, and both Outcome cells read `conforms`, matching each ADR's own `## Code Conformance` verdict.
    - The `Next free ADR number` line is the last edit in the commit sequence for this task — the commit that advances it contains no other Phase-11 artefact change (D-14).
    - `git diff --name-only HEAD~1 HEAD | grep -c 'rs$'` returns `0`.
  </acceptance_criteria>
  <done>`PROMOTION.md` carries two new contiguous index rows, a dated note quoting the `ls` proof and recording that no Part B candidate was closed, and an advanced next-free line written last; `PROJECT.md`'s `## Key Decisions` table carries one row per new ADR with matching `conforms` outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase's registers and ADRs → the `REQ-*` ledger keys | The ledger is the index every later phase queries by requirement ID; an artefact not cited from a row is effectively unfindable |
| this phase → `PROMOTION.md`'s shared numbering state | A single line that every future ADR-writing phase reads and trusts without re-deriving |
| a measured finding → REQUIREMENTS.md, a governing document | A correction here changes what future audits of FACADE-01 and FACADE-03 measure against |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-27 | Tampering | `.planning/ledgers/milestone-07-08.md` | high | mitigate | Row insertion, deletion or reordering would silently break the 86-row inventory every Milestone 7-8 lookup depends on. Mitigated by cell-replacement only, an explicit `^| REQ-` count of `86` asserted before and after, and a `--numstat` check that added lines equal deleted lines. |
| T-11-28 | Tampering | `PROMOTION.md`'s numbering index | high | mitigate | Renumbering, reusing or skipping a number corrupts shared state for every later phase, and `PROMOTION.md:141` forbids reuse outright. Mitigated by appending only, an `ls` proof quoted in the dated note, an ascending-order `sort -c` check across all 35 rows, and a `git diff` assertion that no row 0001-0033 was modified. |
| T-11-29 | Repudiation | The five amended ledger cells | medium | mitigate | Each amendment is dated, names plan 11-05, cites the artefact by path, and retains the superseded text including plan 11-01's earlier amendment — so a later reader can see what changed, when, and on what evidence (D-00d, D-00e). |
| T-11-30 | Spoofing | REQUIREMENTS.md's FACADE-03 correction | high | mitigate | Recording a branch state that was never measured would replace one false claim with another. The task forbids asserting an absence unless `git rev-parse --verify` actually fails at execution time, and requires the measured result from plan 11-03 to be carried through verbatim. |
| T-11-31 | Elevation of Privilege | The advanced next-free line | medium | mitigate | Advancing the line before the ADR files exist would grant numbers to artefacts that were never written. Mitigated by the `<precondition>` requiring `ls .planning/decisions/00*-*.md` first, and by making the advance the last edit of the phase (D-14, procedure step 5). |
| T-11-32 | Information Disclosure | — | low | accept | Not applicable. All four files hold repository-local paths, requirement IDs, commit SHAs and ADR numbers already present in the checkout; no secret or personal datum is read or written. |
| T-11-33 | Denial of Service | — | low | accept | Not applicable. No runtime surface is touched; execution is file reads, a handful of greps and four writes. |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable, truthfully: this plan installs, upgrades and removes nothing. Its four files are all `.planning/` documents, so no manifest or lockfile is in reach; the `git diff --name-only` criteria on each task prove it. |
</threat_model>

<verification>
- `grep -c '^| REQ-' .planning/ledgers/milestone-07-08.md` returns `86` before and after.
- All five named rows carry a dated plan 11-05 amendment citing an artefact path, and no `pending — plan 11-05` marker survives.
- REQUIREMENTS.md's two corrections are additive (`--numstat` deletions of `0`) with both originals readable.
- `PROMOTION.md` holds 35 contiguous ascending index rows and the next-free line reads `0036`, advanced last.
- `PROJECT.md`'s `## Key Decisions` table rises from 29 to 31 rows.
- No commit in this plan touches a Rust source file.
</verification>

<success_criteria>
- Every artefact this phase produced is reachable from a `REQ-*` ledger row by path.
- The two defective claims in REQUIREMENTS.md carry dated corrections with their originals intact.
- The ADR numbering line is advanced exactly once, last, with a note explaining the advance rather than a silent renumber.
- Phase 11 ends having changed zero executable Rust — by finding, not by fiat (D-13).
</success_criteria>

<output>
Create `.planning/phases/11-facade-residue-deferred-register-disposition/11-05-SUMMARY.md` when done.
</output>
