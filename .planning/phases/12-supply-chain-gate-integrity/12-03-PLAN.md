---
phase: 12-supply-chain-gate-integrity
plan: 03
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - .planning/decisions/0036-audit-suppression-single-source-topology.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
autonomous: true
requirements: [SUPPLY-03]

must_haves:
  truths:
    - "ADR-0036 exists at `.planning/decisions/0036-audit-suppression-single-source-topology.md`, carries all seven required H2 headings in `PROMOTION.md`'s prescribed order, has no frontmatter, and parses cleanly through `adr-parser.cjs` with `status` reading `accepted` (D-00a)"
    - "`## Code Locations` and `## Considered Options` are genuine one-item-per-line bulleted lists, so `splitEntries` yields real citations and real options rather than one opaque blob or a shred of per-line sentence fragments"
    - "`## Code Conformance` reads `conforms` — the tree already satisfies the invariant, so the ADR ratifies a true state rather than instructing a change (D-03), unlike ADR-0024's `must change`"
    - "ADR-0036 governs suppression **topology** — which mechanical surfaces may legally carry a suppression — and cites ADR-0024, which governs suppression **contents**, as a related decision without superseding it; ADR-0024's `## Status` is untouched and ADR-0036 carries no `## Supersedes` line (D-05)"
    - "The restated invariant uses the source PRD §8's two-file framing (`.cargo/audit.toml` **and** `deny.toml`) rather than FR-1's narrower single-file framing, and names the distinction explicitly rather than silently picking one"
    - "`## Downstream Consumers` names Phase 13 / ORCH-01 and Phase 15 / PIPE-01, and does not duplicate ADR-0024's existing `Phase 12 / SUPPLY-01 and SUPPLY-02` consumer entry"
    - "Every `ci.yml` line citation in ADR-0036 is re-derived against the tree as plan 12-02 left it, not copied from an artifact written before the guard step was inserted — a line-number citation that was correct yesterday is the exact defect three of this phase's own correction banners exist to fix"
    - "The three passages claiming promotion is impossible from inside a planning artefact each carry a dated correction citing `PROMOTION.md` §Part A and the four prior promotions, with the original text retained — the override is authorised by the precedence order, under which an ADR-class document plus shipped practice outrank PRD/DOC-tier requirement text (D-01, D-00b, D-00c)"
    - "SUPPLY-03's `two supply-chain ADR candidates` phrasing carries a dated correction recording that candidate 3 was discharged by ADR-0024 on 2026-08-08 and naming candidate 7 as the surviving subject, with the original retained (D-02)"
    - "No second ADR is written on ADR-0024's subject — exactly one live ADR answers each question, and the candidate-count correction is recorded as a banner rather than as a competing promotion, because only the contested position earns an ADR while a code-settled defect earns a dated record (D-00g)"
  artifacts:
    - path: ".planning/decisions/0036-audit-suppression-single-source-topology.md"
      provides: "The promoted audit-suppression single-source topology invariant, `Accepted`, `conforms`, citing ADR-0024 as related and naming the D-08 guard as its enforcement point"
      contains: "## Code Conformance"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Dated corrections on SUPPLY-03's `does not act` clause, its `two candidates` phrasing, and the eleven-candidate/zero-locked framing"
      contains: "Corrected by Phase 12 (plan 12-03)"
    - path: ".planning/PROJECT.md"
      provides: "Dated corrections on the §Out of Scope promotion bullet and the §Context eleven-candidate passage"
      contains: "Corrected by Phase 12 (plan 12-03)"
  key_links:
    - from: ".planning/decisions/0036-audit-suppression-single-source-topology.md"
      to: "scripts/check-workflow-suppressions.sh"
      via: "`## Code Locations` cites the D-08 guard and its two wiring points as the mechanism that makes the invariant a gate rather than an observation"
      pattern: "check-workflow-suppressions"
    - from: ".planning/decisions/0036-audit-suppression-single-source-topology.md"
      to: ".planning/decisions/0024-rustsec-exception-governance.md"
      via: "`## Context` cites ADR-0024 by number as the owner of suppression contents, scoping ADR-0036 to topology; no supersession"
      pattern: "ADR-0024"
    - from: ".planning/decisions/0036-audit-suppression-single-source-topology.md"
      to: ".project/Milestone_10-CI-Hardening-Release-Automation/Epic_2/prd-dependency-security-license-compliance.md"
      via: "`## Code Locations` cites the source document's full resolved path so a reader can trace the promoted decision to the corpus document it came from, per `PROMOTION.md` §Part A step 4"
      pattern: "prd-dependency-security-license-compliance"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/decisions/PROMOTION.md"
      via: "each promotion-viability correction cites §Part A's `Promotion is now an ordinary write to a directory plus a table row` passage as the superseding authority"
      pattern: "PROMOTION.md"
  prohibitions:
    - "Do not restate, re-derive or second-guess ADR-0024's suppression-content decisions. ADR-0036 names ADR-0024 by number and cites its decisions; it never explains which advisories are suppressed or why. Draft text naming a specific `RUSTSEC-*` identifier's rationale has drifted into ADR-0024's territory (D-00i, and this phase's named pitfall 2)."
    - "Do not touch `.planning/decisions/0024-rustsec-exception-governance.md`. ADR-0036 does not supersede it, its `## Status` stays `Accepted`, and it is not edited by this phase in any way (D-05, D-00i)."
    - "Do not write a second ADR on ADR-0024's subject. `REQUIREMENTS.md:1801` prohibits it by precedent and `PROMOTION.md` §Supersession requires exactly one live ADR per question; candidate 3 is closed and the correction is a banner, not a promotion (D-02)."
    - "Do not delete or rewrite any text this plan corrects. All four original passages — the `does not act` clause, the `two candidates` phrasing, and both eleven-candidate framings — must still be readable afterward (D-00c)."
    - "Do not promote any other `PROMOTION.md` Part B candidate. Candidates 4, 6, 8, 9, 10 and 11 keep their owning phases."
    - "Do not write `## Code Locations` or `## Considered Options` as prose. Every line in both sections starts with a bullet marker and carries exactly one citation or one option; a paragraph collapses into one opaque blob or shreds into per-line fragments under `splitEntries`."
    - "Do not modify any `.rs` file, and do not create `.planning/ledgers/milestone-09-12.md` (D-09)."
---

<objective>
Promote `PROMOTION.md` Part B candidate 7 — the audit-suppression single-source invariant — as
ADR-0036, and correct at source the four passages that claim this promotion is impossible.

Purpose: SUPPLY-03 is the only genuinely open work in this phase. The invariant it names sits at PRD
precedence, where SUPPLY-03's own text says "any future document can override them", one phase after
the project paid for the violation. Its owning phase is explicitly Phase 12, and the tree now satisfies
it — which makes this the cheapest possible moment to lock it, because the ADR ratifies a true state
rather than mandating a change.

Output: `.planning/decisions/0036-audit-suppression-single-source-topology.md` with a `conforms`
verdict citing the D-08 guard as its enforcement point, and four dated correction banners with every
original retained.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-supply-chain-gate-integrity/12-CONTEXT.md
@.planning/phases/12-supply-chain-gate-integrity/12-RESEARCH.md
@.planning/phases/12-supply-chain-gate-integrity/12-PATTERNS.md
@.planning/phases/12-supply-chain-gate-integrity/12-VALIDATION.md
</context>

## Artifacts this phase produces

Every symbol below is created by Phase 12 and does not exist in the tree before it. A drift check that
flags one of these as an unknown reference is reading a not-yet-written artifact, not a defect.

| Artifact | Plan | Kind |
|---|---|---|
| `scripts/check-workflow-suppressions.sh` | 12-02 | new file — the D-08 regression guard |
| `WORKFLOWS_DIR`, `CARGO_GATE_RE`, `IGNORE_FLAG_RE`, `violates()`, `CLAUSE1_INLINE_SUPPRESSION`, `CLAUSE2_AUDIT_INVOCATION_COUNT` | 12-02 | new identifiers inside that script |
| `check-workflow-suppressions` | 12-02 | new `Makefile` target, added to the `check-gates` prerequisite list |
| `Check workflow files for inline advisory suppressions` | 12-02 | new `ci.yml` step name in the `cargo-deny:` job |
| `.planning/decisions/0036-audit-suppression-single-source-topology.md` | **12-03** | new ADR |
| `#### Hand-off to Phase 13 / ORCH-01 — dated 2026-08-09 (plan 12-04)` | 12-04 | new `REQUIREMENTS.md` heading |
| `PROMOTION.md` index row `0036`, its dated note, `**Next free ADR number: 0037**`, Part B candidate 7 closure note | 12-04 | new rows/lines in an existing record |
| `PROJECT.md` `## Key Decisions` row for ADR-0036 | 12-04 | new row in an existing table |

**No `.rs` file appears on this list, and none may be added to it.**

## Executor notes

- `.planning/config.json` sets `worktree_skip_hooks: true`, and both Rust pre-commit hooks carry
  `always_run: true`. This plan touches only `.planning/` markdown — commit with `--no-verify`.
- **Line-number hygiene.** Plan 12-02 inserted a step into `ci.yml`'s `cargo-deny:` job, so every line
  citation below `:101` has shifted. Re-derive each `ci.yml` citation against the tree as it stands
  before writing it into the ADR. Citing a range that was correct yesterday is precisely the defect
  three of this phase's own correction banners exist to fix.
- **Confirm the checkpoint first.** Read `12-01-SUMMARY.md` and confirm the decision checkpoint selected
  option-a. Under option-b, task 1 runs but the guard citations in `## Code Locations` do not exist;
  under option-c this plan does not run at all.

## ⚠ HUMAN REVIEW flag carried into this plan

**D-01 overrides a governing requirement's own explicit self-limitation.** `REQUIREMENTS.md:1937-1939`
states "**This requirement does not act.**", and `PROJECT.md` §Out of Scope and
`REQUIREMENTS.md:103-109` say the same in their own words. This plan acts anyway, on the authority of
`PROMOTION.md` §Part A ("Promotion is now an ordinary write to a directory plus a table row") and four
prior promotions — candidates 1, 2, 3 and 5, closed by ADR-0016, ADR-0021, ADR-0024 and ADR-0025 as
ordinary writes with no re-ingest. Three documents say this requirement cannot act; a fourth says it
can, and four ADRs prove it. Under D-00b, shipped practice plus the higher-precedence ADR-class document
outrank the PRD/DOC-tier requirement text. That reasoning is worth stating plainly inside each of the
four correction banners, because it is a fact about the corpus and not only about SUPPLY-03: the rule
that "nothing is locked" was itself superseded when Phase 1 built `.planning/decisions/`, and the
requirement text never caught up. The gate for this decision fired at the end of plan 12-01.

<tasks>

<task type="auto">
  <name>Task 1: Author ADR-0036 — the audit-suppression single-source topology invariant</name>
  <reversibility rating="costly">An ADR promoted into `.planning/decisions/` tops the precedence order; a later phase that disagrees must supersede it through the `## Status` / `## Supersedes` mechanism rather than delete it, and the four correction banners in task 2 would each have to be re-superseded rather than reverted. The decision was gated by the blocking checkpoint at the end of plan 12-01.</reversibility>
  <precondition>Plan 12-01's decision checkpoint selected option-a, confirmed by reading `12-01-SUMMARY.md`. `.planning/decisions/0036-*.md` does not already exist (`ls .planning/decisions/0036-*.md` fails), and `PROMOTION.md:59` still reads `Next free ADR number: 0036` — plan 12-04, not this plan, advances it.</precondition>
  <read_first>
    - `.planning/decisions/PROMOTION.md` lines 107-128 — the required H2 heading set in its prescribed order, the bulleted-list constraint on `## Code Locations` and `## Considered Options` and the `splitEntries` reason for it, and the note that `## Code Conformance` and `## Downstream Consumers` land in the parser's `unmapped_headers` bucket and are required anyway. Lines 130-140 for the supersession mechanism — read it to confirm ADR-0036 needs no `## Supersedes` line. Lines 149-170 for the six-step promotion procedure. Lines 203-205 for Part B candidate 7's entry and its `**Owner phase: Phase 12**` marker.
    - `.planning/decisions/0031-extracted-crate-dependency-rule.md` in full — the closest shape model: an ADR that *restates* an invariant the tree already satisfies rather than instructing a change. Note its `## Status` two-line form (`Accepted`, then a bold `**Date:**` line), its `## Decision` structure (precedence restatement, then the rule as a single bold quotable sentence, then labelled sub-decisions), its `## Considered Options` four bullets each ending `(accepted)` or `(rejected)` with a one-sentence reason, its three-line `## Code Conformance`, and its three-bullet `## Downstream Consumers`. Its total length, roughly 180 lines, is the budget to target. Note also its one structural wrinkle to **avoid**: an un-bulleted transcript paragraph trailing after the `## Code Locations` bullets, which `splitEntries` shreds into ~18 spurious fragment entries.
    - `.planning/decisions/0024-rustsec-exception-governance.md` — read `## Status`, `## Decision` (its five numbered decisions, in particular decisions 1 and 2 on which files are authoritative for what), `## Code Conformance` (`must change`, and why ADR-0036's verdict differs) and `## Downstream Consumers` (its existing `Phase 12 / SUPPLY-01 and SUPPLY-02` entry, which ADR-0036 must not duplicate). This is the ADR to cite and never restate.
    - `.project/Milestone_10-CI-Hardening-Release-Automation/Epic_2/prd-dependency-security-license-compliance.md` lines 69-73 (the group heading `### \`cargo audit\` Integration & Exception Process (FR 1–4)` and item 1 beneath it, which is FR 1) and lines 180-189 (§8 Success Metrics, in particular the two bullets naming the distinct passing checks and the "only places policy/exceptions are defined" claim). This is the source text being promoted; quote it accurately rather than paraphrasing.
    - `.github/workflows/ci.yml` — the `security-audit:` job with its `name: Security Audit`, its rationale comment and its bare `cargo audit`; the `cargo-deny:` job's guard steps including the one plan 12-02 added; and the `cargo deny check` step. **Re-derive every line number** — plan 12-02 shifted them.
    - `scripts/check-workflow-suppressions.sh` as plan 12-02 wrote it, plus `Makefile`'s `check-gates` target and the new `ci.yml` step — the enforcement mechanism ADR-0036 cites as what makes this invariant a gate.
    - `.planning/phases/12-supply-chain-gate-integrity/12-PATTERNS.md` § `.planning/decisions/0036-<slug>.md` — the section-by-section shape guide with the exact `## Status`, `## Code Conformance`, `## Considered Options` and `## Code Locations` forms.
  </read_first>
  <files>.planning/decisions/0036-audit-suppression-single-source-topology.md</files>
  <action>
Write the file at `.planning/decisions/0036-audit-suppression-single-source-topology.md`. **No
frontmatter.** An H1 title line, then the seven required H2 headings in `PROMOTION.md`'s order:
`## Status`, `## Context`, `## Decision`, `## Considered Options`, `## Code Locations`,
`## Code Conformance`, `## Downstream Consumers`. No `## Supersedes` line — this ADR supersedes nothing.
Target roughly ADR-0031's length.

**`## Status`** — the bare word `Accepted` on its own line, then a bold `**Date:** 2026-08-09` line,
matching ADR-0031's and ADR-0024's shape exactly. The parser's reject set does an exact match after
normalisation, so the status line must read plainly `Accepted` and nothing else.

**`## Context`** — state the measured fact first, then the PRD claim, then the relationship to
ADR-0024. Cover: the invariant's source and its two framings; that FR 1 names only `.cargo/audit.toml`
as the single source of truth for `cargo audit` while §8 names both `.cargo/audit.toml` and `deny.toml`
as the two authorised surfaces, and that this ADR adopts §8's broader two-file framing and says so
rather than silently picking one; that `PROMOTION.md` Part B recorded candidate 7 as "currently
violated by the tree" and that this is **no longer true**, with the measurement that establishes it;
and that ADR-0024 owns the *contents* of the exception set — which advisories, whose, reviewed when,
under what schema — while this ADR owns the *topology*. Make the division quotable: a reader asking
"who owns this advisory?" goes to ADR-0024; a reader asking "may I add an ignore flag to a workflow
step?" goes here. Cite ADR-0024 by number and by file path. **Never restate what ADR-0024 decided.**

**`## Decision`** — prose is fine here; only the two list-shaped sections need bullets. Restate the
precedence order, then give the invariant as a single bold quotable sentence in §8's two-file framing:
an advisory suppression may be expressed only in `.cargo/audit.toml` or `deny.toml`, and a workflow
file may not carry one inline. Then three labelled sub-decisions: (i) the enforceable invariant and
what a violation looks like; (ii) that this ADR is scoped to topology and explicitly does not touch
ADR-0024's contents, schema, owner or review dates; (iii) that the invariant is now enforced rather
than asserted, naming the D-08 guard, the `make check-gates` target and the CI step. Close with a short
paragraph citing the ADR-0016 / ADR-0021 / ADR-0024 / ADR-0025 precedent for why an ADR — rather than a
re-tagged `.project/` document — is the promotion mechanism, matching ADR-0031's own closing move.

**`## Considered Options`** — a bulleted list, one option per line, each ending `(accepted)` or
`(rejected)` with a one-sentence reason. Four options: promote as a standalone ADR with a `conforms`
verdict (accepted); decline and leave the invariant at PRD precedence, where SUPPLY-03's own text says
any future document can override it (rejected); fold the invariant into ADR-0024 as an amendment, which
would make one ADR answer two questions and blur the supersession mechanism (rejected); mark ADR-0024
superseded, which is simply false since it is live and its review dates are in force (rejected). The
decline branch is reproduced as a full rejected option rather than merely named, so a future reader
does not have to re-derive why it lost.

**`## Code Locations`** — a bulleted list, one citation per line, and **no trailing un-bulleted
paragraph**. Every `ci.yml` line number re-derived against the current tree. Cite: the `security-audit:`
job's range and its rationale comment line; the bare `cargo audit` line; the new guard step's line in
the `cargo-deny:` job; the `cargo deny check` line; `.cargo/audit.toml`; `deny.toml`;
`SECURITY-EXCEPTIONS.md`; `scripts/check-workflow-suppressions.sh`; the `Makefile` `check-gates` line;
and the source document's full resolved path with its `FR 1` and `§8` locations, per `PROMOTION.md`
§Part A step 4.

**`## Code Conformance`** — the bare word `conforms`, a blank line, then the explanation. This heading
is unmapped by the parser, so prose here is harmless — **put the verification transcript here**, not
trailing after `## Code Locations`, deliberately improving on ADR-0031's one structural wrinkle. Record
the two measurements that establish conformance, re-run in this execution: the count of `cargo audit`
invocations across every workflow file, and the guard's own exit-0 run against the real tree. State
explicitly that the only ignore-family tokens anywhere under the workflows directory are a MinIO client
flag and Rust's own test-harness opt-in flag, neither of which is an advisory suppression.

**`## Downstream Consumers`** — three bullets. Phase 13 / ORCH-01, which must carry into the Milestone
9-12 ledger the verdict class "100% complete with one false acceptance criterion — and, as of
2026-08-08, no longer false". Phase 15 / PIPE-01, because new CI jobs must not reintroduce inline
suppressions and the guard is what will tell them. And the milestone close-out, which receives the
unapplied-rulesets finding plan 12-01 recorded. **Do not duplicate ADR-0024's existing
`Phase 12 / SUPPLY-01 and SUPPLY-02` entry.**

**Then run the structural check** from `12-VALIDATION.md` against the finished file and record its
output. Assert `unmapped_headers` membership as a subset test, never as a length equality — the parser
treats the H1 title as a section boundary too, so a length check spuriously fails on every ADR in the
corpus including the four already accepted.
  </action>
  <verify>
    <automated>set -e; A=$(ls .planning/decisions/0036-*.md); echo "ADR: $A"; head -1 "$A" | grep -q '^# ' || { echo "FAIL: no H1 title"; exit 1; }; head -1 "$A" | grep -q '^---' && { echo "FAIL: ADR must have no frontmatter"; exit 1; }; for h in '## Status' '## Context' '## Decision' '## Considered Options' '## Code Locations' '## Code Conformance' '## Downstream Consumers'; do grep -qF "$h" "$A" || { echo "FAIL: missing $h"; exit 1; }; done; grep -qF '## Supersedes' "$A" && { echo "FAIL: ADR-0036 supersedes nothing"; exit 1; }; node .claude/gsd-core/bin/lib/adr-parser.cjs --input "$A" | python3 -c "
import json, sys
d = json.load(sys.stdin)
assert d['status'] == 'accepted', f\"status={d['status']!r}, expected 'accepted'\"
assert len(d['key_files']) > 0, 'Code Locations parsed to zero entries'
assert len(d['options_considered']) > 0, 'Considered Options parsed to zero entries'
assert len(d['decisions']) > 0, 'Decision section empty'
unmapped = set(d['unmapped_headers'])
assert 'Code Conformance' in unmapped, 'Code Conformance heading missing or misnamed'
assert 'Downstream Consumers' in unmapped, 'Downstream Consumers heading missing or misnamed'
print('ADR-0036 STRUCTURAL CHECK: PASS')
"; grep -qF 'ADR-0024' "$A" || { echo "FAIL: ADR-0024 not cited"; exit 1; }; grep -qF 'check-workflow-suppressions' "$A" || { echo "FAIL: guard not cited in Code Locations"; exit 1; }; grep -qF 'prd-dependency-security-license-compliance' "$A" || { echo "FAIL: source document path not cited"; exit 1; }; git status --porcelain -- .planning/decisions/0024-rustsec-exception-governance.md | grep -q . && { echo "FAIL: ADR-0024 was modified"; exit 1; }; echo "ADR-0036 structurally valid, cites ADR-0024 and the guard, ADR-0024 untouched"</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/decisions/0036-*.md` returns exactly one file named `0036-audit-suppression-single-source-topology.md` — one ADR, numbered 0036, advancing the line by one (D-04) — and it begins with an H1 title line and carries no YAML frontmatter.
    - All seven required H2 headings are present in `PROMOTION.md`'s prescribed order, and no `## Supersedes` line exists.
    - The structural check passes: `adr-parser.cjs` reports `status` as `accepted`, non-empty `key_files`, non-empty `options_considered`, non-empty `decisions`, and both `Code Conformance` and `Downstream Consumers` present in `unmapped_headers` — asserted as subset membership, never as a length equality.
    - `## Code Conformance`'s first non-blank line after the heading is the bare word `conforms`, and the verification transcript sits inside this section rather than trailing after `## Code Locations`.
    - `## Code Locations`'s `key_files` entry count is within a factor of two of the number of bullet lines in that section — proving no un-bulleted prose paragraph is being shredded into fragment entries, the wrinkle ADR-0031 carries.
    - Every line in `## Code Locations` and `## Considered Options` begins with a bullet marker: `sed -n '/^## Considered Options/,/^## Code Conformance/p'` and `sed -n '/^## Code Locations/,/^## Code Conformance/p'` yield only heading lines, blank lines and bullet lines.
    - Every `ci.yml` citation resolves against the current tree: for each cited line or range, the content at that location is what the ADR says it is — re-checked after plan 12-02's step insertion, not copied from `12-CONTEXT.md` or `12-PATTERNS.md`.
    - The invariant is stated in §8's two-file framing naming both `.cargo/audit.toml` and `deny.toml`, and the ADR states explicitly that FR 1's narrower single-file framing was the alternative and why the broader one was taken.
    - `grep -qF 'ADR-0024' .planning/decisions/0036-*.md` succeeds, and no `RUSTSEC-` identifier appears anywhere in the file with a rationale attached to it — `grep -c 'RUSTSEC-' .planning/decisions/0036-*.md` may be non-zero only where the identifier is part of an illustrative flag form, never where a suppression is justified.
    - `## Downstream Consumers` names Phase 13 / ORCH-01 and Phase 15 / PIPE-01 and does not contain the string `SUPPLY-01 and SUPPLY-02`, which is ADR-0024's entry, not this one's.
    - `git status --porcelain -- .planning/decisions/0024-rustsec-exception-governance.md` is empty, and `PROMOTION.md` is unmodified by this plan (`git status --porcelain -- .planning/decisions/PROMOTION.md` is empty — plan 12-04 owns it).
    - `git diff --name-only <base>..HEAD -- '*.rs' | wc -l` returns `0`.
  </acceptance_criteria>
  <done>ADR-0036 exists with all seven headings in order and no frontmatter, passes the structural check with `status=accepted` and both unmapped headers present, carries a `conforms` verdict with its transcript inside `## Code Conformance`, cites ADR-0024 without superseding it, cites the D-08 guard and the source PRD's full path, states the invariant in the two-file framing, and left ADR-0024 and `PROMOTION.md` untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the four passages claiming this promotion is impossible</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 1929-1941 — SUPPLY-03's full text. The `two supply-chain ADR candidates` phrasing opens the block at `:1929`; the `**This requirement does not act.** Promotion requires re-tagging the source documents and re-running ingest…` clause is at `:1937-1939`. One banner covers both defects, since they are the same block.
    - `.planning/REQUIREMENTS.md` lines 103-109 — the `**Eleven ADR candidates exist and none is promoted.**` passage. It carries **three** stale claims at once: that none is promoted; that promotion requires `--manifest` re-tagging and re-ingest; and that the tree currently violates the single-source invariant. One banner addresses all three.
    - `.planning/PROJECT.md` lines 587-588 — the §Out of Scope bullet `**Promoting the two ADR candidates into locked decisions** — doing so requires re-tagging the source documents via \`--manifest\` and re-running ingest, not an edit here.`
    - `.planning/PROJECT.md` lines 623-627 — the §Context passage `**Eleven ADR candidates have accumulated, and none is promoted.**` This site is **not** named in `12-CONTEXT.md`'s canonical refs but carries the identical stale claim in the same document, feeding directly into the `## Key Decisions` table that plan 12-04 adds a row to. Correct both `PROJECT.md` sites in the same pass.
    - `.planning/decisions/PROMOTION.md` lines 165-170 — the `**Why this is viable now, where it previously was not.**` passage, the superseding authority every banner cites. Lines 177-199 — Part B candidates 1, 2, 3 and 5 with their `Closed by ADR-00NN` notes, the four prior promotions that prove the practice. Lines 185-189 specifically — candidate 3's closure note, the citation for D-02's candidate-count correction.
    - `.planning/codebase/CONCERNS.md` lines 9 and 276 — the two verbatim banner examples and the parenthetical shape.
    - `.planning/decisions/0036-audit-suppression-single-source-topology.md` as written by task 1 — so each banner points at the ADR that now exists rather than at one that is planned.
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/PROJECT.md</files>
  <action>
Four dated corrections, one per site, each in the inline-parenthetical shape
`(**Corrected by Phase 12 (plan 12-03), dated 2026-08-09, citing …:** …)` appended inside or
immediately after the original sentence. Not a blockquote, not a separate section. **Every original
sentence stays exactly where it is and is never reworded** — it is the evidence for the correction.

**(1) `REQUIREMENTS.md:1929-1939` — SUPPLY-03's own block, one banner covering both defects.**
  - The **does-not-act** defect (D-01): the clause is superseded by `PROMOTION.md` §Part A, which
    states plainly that the re-tagging path no longer exists and is not needed, because ADRs live in
    `.planning/decisions/` as their own document class independent of the ingest manifest and top the
    precedence order — promotion is now an ordinary write to a directory plus a table row. Cite the
    four prior promotions by candidate number and ADR number as proof the practice already settles it,
    and state the precedence reasoning explicitly: under the project's own precedence order, shipped
    practice plus a higher-precedence ADR-class document outrank PRD/DOC-tier requirement text. Name
    ADR-0036 as this requirement's own act.
  - The **candidate-count** defect (D-02): "the two supply-chain ADR candidates" is now one. Candidate
    3, `Milestone_7/Epic_4/rustsec-remediation-plan.md` — the corpus's only dated item, review target
    2026-09-30 — was discharged on 2026-08-08 by ADR-0024, which renewed it to per-advisory 2026-12-31
    review dates and reassigned the owner; `PROMOTION.md:185-189` records the closure. Candidate 7 is
    the surviving subject and is what ADR-0036 promotes. State that no second ADR is written on
    ADR-0024's subject, and why: exactly one live ADR answers each question.

**(2) `REQUIREMENTS.md:103-109` — the eleven-candidate framing, three stale claims in one passage.**
One banner correcting all three: four of the eleven are now promoted, and this phase adds a fifth —
name them as candidates 1, 2, 3 and 5 closed by ADR-0016, ADR-0021, ADR-0024 and ADR-0025, plus
candidate 7 closed by ADR-0036; the re-tagging requirement is superseded by `PROMOTION.md` §Part A; and
the tree no longer violates the single-source invariant, pointing at ADR-0036's `## Code Conformance`
verdict for the measurement rather than restating it.

**(3) `PROJECT.md:587-588` — the §Out of Scope bullet.** The same correction as (1)'s first half: the
`--manifest` requirement is superseded, both counts are stale, and the promotion this bullet declares
out of scope has now happened. Point at ADR-0036 and at the `## Key Decisions` row plan 12-04 adds.

**(4) `PROJECT.md:623-627` — the §Context eleven-candidate passage.** The same correction as (2), in
this document's own register.

Say the thing plainly somewhere in banners (1) and (2), because it is a fact about the corpus and not
only about SUPPLY-03: **three documents say this requirement cannot act; a fourth says it can, and four
ADRs prove it.** The corpus's rule that "nothing is locked" was itself superseded when Phase 1 built
`.planning/decisions/`, and the requirement text never caught up.

**Do not** flip SUPPLY-03's checkbox or its traceability row here — plan 12-04 owns those, together
with the hand-off block, so the phase's closure record lands in one place.
  </action>
  <verify>
    <automated>set -e; R=.planning/REQUIREMENTS.md; P=.planning/PROJECT.md; grep -qF 'This requirement does not act' "$R" || { echo "FAIL: original does-not-act clause deleted"; exit 1; }; grep -qF 'Eleven ADR candidates exist and none is promoted' "$R" || { echo "FAIL: original eleven-candidate passage deleted"; exit 1; }; grep -qF 'Promoting the two ADR candidates into locked decisions' "$P" || { echo "FAIL: original Out of Scope bullet deleted"; exit 1; }; grep -qF 'Eleven ADR candidates have accumulated, and none is promoted' "$P" || { echo "FAIL: original Context passage deleted"; exit 1; }; C=$(grep -rc 'Corrected by Phase 12 (plan 12-03), dated 2026-08-09' "$R" "$P" | awk -F: '{s+=$2} END {print s}'); [ "$C" -ge 4 ] || { echo "FAIL: expected 4 dated corrections, found $C"; exit 1; }; A=$(grep -rc 'ADR-0036' "$R" "$P" | awk -F: '{s+=$2} END {print s}'); [ "$A" -ge 4 ] || { echo "FAIL: only $A corrections cite ADR-0036"; exit 1; }; grep -qF 'ADR-0024' "$R" || { echo "FAIL: candidate-count correction does not cite ADR-0024"; exit 1; }; grep -q '^| SUPPLY-03 | Phase 12 | Pending |' "$R" || { echo "FAIL: SUPPLY-03 traceability row must stay Pending until plan 12-04"; exit 1; }; echo "4 dated corrections, 4 originals retained, ADR-0036 cited, SUPPLY-03 row untouched"</automated>
  </verify>
  <acceptance_criteria>
    - All four original passages are still readable verbatim: `grep -qF 'This requirement does not act'` and `grep -qF 'Eleven ADR candidates exist and none is promoted'` succeed in `REQUIREMENTS.md`; `grep -qF 'Promoting the two ADR candidates into locked decisions'` and `grep -qF 'Eleven ADR candidates have accumulated, and none is promoted'` succeed in `PROJECT.md`.
    - `grep -c 'Corrected by Phase 12 (plan 12-03), dated 2026-08-09'` summed across both files returns at least `4` — one per site, and no site carries two.
    - Every banner cites ADR-0036 by number, and each of the two candidate-count-bearing banners also cites ADR-0024 and `PROMOTION.md`'s candidate-3 closure.
    - The four-prior-promotions argument is present by name: `grep -c 'ADR-0016'`, `grep -c 'ADR-0021'`, `grep -c 'ADR-0025'` in `REQUIREMENTS.md` are each non-zero within the corrected passages.
    - Each banner states the original claim's status explicitly — superseded, stale or corrected — rather than only asserting the new fact.
    - `git diff --numstat` for both files shows deletions of `0`; every correction is purely additive.
    - SUPPLY-03's checkbox is still `- [ ]` and its traceability row still reads `Pending` — plan 12-04 owns both, and `grep -q '^| SUPPLY-03 | Phase 12 | Pending |'` succeeds.
    - Plan 12-01's own corrections in these two files are intact: `grep -c 'Corrected by Phase 12 (plan 12-01), dated 2026-08-09'` is unchanged from its post-12-01 value.
    - `git status --porcelain -- .planning/decisions/PROMOTION.md .planning/decisions/0024-rustsec-exception-governance.md` is empty.
    - `git diff --name-only <base>..HEAD -- '*.rs' | wc -l` returns `0`.
  </acceptance_criteria>
  <done>All four passages carry one dated correction each, citing ADR-0036 and — where the candidate count is at issue — ADR-0024 and `PROMOTION.md:185-189`; every original is retained verbatim; the precedence reasoning and the four prior promotions are named; and SUPPLY-03's checkbox, traceability row and `PROMOTION.md` are all left for plan 12-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADR-0036 → the top of the precedence order | An ADR outranks the shipped tree, the codebase map, PRDs and DOCs; an inaccuracy here propagates further than an inaccuracy anywhere else in the corpus |
| ADR-0036 ↔ ADR-0024 | Two supply-chain suppression ADRs sitting close together; blurring their scopes would put two live ADRs on one question and break the supersession mechanism |
| a `file:line` citation → the tree it names | Plan 12-02 shifted `ci.yml`'s line numbers; a citation copied from an artifact written before that insertion is stale on arrival |
| a correction banner → the original claim it corrects | The original is the evidence; deleting it converts a repaired defect into one that appears never to have existed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-17 | Spoofing | ADR-0036's `## Code Locations` | high | mitigate | A stale line citation asserts a fact about a file that is not there — and this phase exists partly to fix exactly that defect elsewhere. Mitigated by an executor note requiring re-derivation after plan 12-02's step insertion, and by an acceptance criterion requiring each cited location's content to be checked against the current tree rather than copied from `12-CONTEXT.md` or `12-PATTERNS.md`. |
| T-12-18 | Tampering | `.planning/decisions/0024-rustsec-exception-governance.md` | high | mitigate | Marking ADR-0024 superseded, or amending its `## Status`, would retire a live governance register whose review dates are in force and whose suppression set the whole supply chain depends on. Mitigated by a prohibition, by ADR-0036 carrying no `## Supersedes` line (asserted in the verify block), and by a `git status --porcelain` criterion on that exact file. |
| T-12-19 | Tampering | corrected source text | high | mitigate | A correction that overwrites what it corrects destroys the audit trail (D-00c). Mitigated by the paired-grep protocol on all four sites — banner present **and** original retained — and by a `--numstat` deletions-of-zero assertion across both files. |
| T-12-20 | Elevation of Privilege | ADR-0036's scope | medium | mitigate | An ADR that drifts from topology into contents would silently claim authority over the suppression set that ADR-0024 holds, giving two documents a say over one question. Mitigated by a prohibition, by the `## Context` instruction to cite ADR-0024's decisions by number and never restate them, and by an acceptance criterion that no `RUSTSEC-` identifier appears with a justification attached. |
| T-12-21 | Repudiation | the four correction banners | medium | mitigate | Each is dated, names plan 12-03, cites the superseding authority (`PROMOTION.md` §Part A) and the ADRs that prove the practice, and states the original claim's status explicitly — so a later reader can reconstruct what changed, when, and on what basis (D-00c, D-00e). |
| T-12-22 | Tampering | `adr-parser.cjs`'s view of ADR-0036 | medium | mitigate | Prose under `## Code Locations` or `## Considered Options` degrades into one opaque blob or a shred of per-line fragments, silently defeating every downstream consumer that reads citable entries. Mitigated by the bullets-only prohibition, by running the structural check in the verify block, and by a criterion bounding the `key_files` entry count against the visible bullet count. |
| T-12-23 | Information Disclosure | — | low | accept | Not applicable. ADR-0036 and the four banners contain repository-local paths, requirement IDs, ADR numbers, advisory identifiers and commit SHAs already present in the checkout. No secret or personal datum is read or written. |
| T-12-24 | Denial of Service | — | low | accept | Not applicable. No runtime surface is touched; execution is file reads, one file write, two in-place annotations and a handful of greps. |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable, truthfully: this plan installs, upgrades and removes nothing. Its three files are `.planning/` markdown; no manifest or lockfile is in reach, and no package manager is invoked. |
</threat_model>

<verification>
- `ls .planning/decisions/0036-*.md` returns one file; it has no frontmatter and carries all seven headings in order with no `## Supersedes`.
- The `adr-parser.cjs` structural check prints `ADR-0036 STRUCTURAL CHECK: PASS` with `status=accepted` and both unmapped headers present.
- `## Code Conformance` reads `conforms` and holds the verification transcript; `## Code Locations` and `## Considered Options` are bullets only.
- Every `ci.yml` citation resolves against the tree as plan 12-02 left it.
- Four dated `plan 12-03` corrections exist, all four originals are retained, and `--numstat` deletions are `0`.
- ADR-0024 and `PROMOTION.md` are unmodified; SUPPLY-03's checkbox and traceability row are untouched.
- No `.rs` file is modified.
</verification>

<success_criteria>
- The audit-suppression single-source invariant is out of PRD precedence and into an ADR that tops the precedence order, with a `conforms` verdict that ratifies a true state rather than mandating a change.
- ADR-0036 and ADR-0024 answer two different questions and a reader can tell which to open, without either ADR contradicting the other.
- A future reader of SUPPLY-03, `REQUIREMENTS.md`'s eleven-candidate framing or `PROJECT.md`'s §Out of Scope finds the correction beside the stale claim instead of re-deriving the contradiction.
- `PROMOTION.md` Part B candidate 7's owning phase closes it rather than spending itself and leaving it open.
</success_criteria>

<output>
Create `.planning/phases/12-supply-chain-gate-integrity/12-03-SUMMARY.md` when done. Record in it: the
ADR structural-check output verbatim, the re-derived `ci.yml` line citations with what each resolves to,
the four correction sites with both greps per site, and confirmation that plan 12-01's checkpoint
selected option-a.
</output>
