---
phase: 13-milestone-9-12-ground-truth-recorded-account
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ledgers/milestone-09-12.md
autonomous: true
requirements: [ORCH-01, ORCH-02]
must_haves:
  truths:
    - "`.planning/ledgers/milestone-09-12.md` exists and holds exactly 120 `| REQ-` rows across 29 section headers, matching REQUIREMENTS.md:3638-3931 one-for-one"
    - "Epic sections appear in REQUIREMENTS.md's own order and are never re-sorted; a later plan replaces a row's Verdict cell in place and never inserts, deletes or reorders a row (ORCH-01 ordering edge)"
    - "Two requirements citing the same artefact keep separate rows and separate verdicts; no row is merged for sharing a citation (D-00f, ORCH-01 duplicates edge)"
    - "The five ORCH-02 checkbox verdicts appear in milestone order M9 -> M10 -> M11 -> M12 -> project-management in the head note (ORCH-02 ordering edge)"
    - "The five-run pattern understated -> accurate -> overstated -> contradicted -> vacuous is written in exactly one place in the corpus — this ledger's head note — and nowhere else (D-10, ORCH-02 duplicates edge)"
    - statement: "project-management's single `open` checkbox is recorded as a formatting example inside a template file rather than as a zero-item or omitted block (ORCH-02 empty edge)"
      verification: backstop
    - "Milestone 9 Epic 1's six rows carry real verdicts with a `file:line` re-read this session, so no row in that section reads `Verify` or `pending`"
    - "A reader finds the single `Shipped, one acceptance criterion false` row and the `Verified open` block from head-note tables without scanning 120 rows (D-02)"
    - "The head note states the precedence order this ledger's verdicts are resolved under — ADR, then shipped tree, then `.planning/codebase/` map, then `intel/code-verification.md`, then PRD, then DOC, then task-list checkbox — so a reader knows which source wins when two disagree (D-00b)"
  artifacts:
    - .planning/ledgers/milestone-09-12.md
  key_links:
    - "ledger head note -> milestone-07-08.md:5's naming of milestone-09-12.md as the fifth and final sibling (D-01)"
    - "ledger legend -> REQUIREMENTS.md:3632-3636's eleven-class run-5 status key, mapped onto the series' seven classes (D-02)"
    - "ledger contention table -> plans 13-02..13-07's disjoint section ranges (D-23)"
  prohibitions:
    - "No `.rs` file is modified by this plan (D-19)"
    - "No ledger row is left as a bare `Verify` in a section this plan derives; `Verify` is retired as a verdict (D-02)"
    - "No `Shipped` verdict is written without a `file:line` citation plus something that exercises it; an ingest-era status word is not evidence (D-03)"
    - "No superseded text is deleted; corrections are dated and retain the original (D-00c, D-00d)"
    - "No new verdict vocabulary is invented; the run-5 eleven-class key is mapped onto the series' seven, never re-keyed (D-02)"
    - "No third `Evidence` column is introduced; rows are two-column `Requirement | Verdict` with the citation as prose inside the Verdict cell"
    - "No item from 13-CONTEXT.md's `<deferred>` block is planned or actioned here"
---

<objective>
Create the fifth and final as-shipped ledger, `.planning/ledgers/milestone-09-12.md`, with every head
note, legend, highlight table and contention table the sibling series uses — and prove the whole
derivation pipeline end-to-end by deriving Milestone 9 Epic 1's six rows for keeps before any fan-out
plan starts.

Purpose: the ledger is the artifact a planner opens. Everything Phases 14, 15 and 16 inherit from
Milestones 9-12 arrives through this file. Getting its spine right once, with one section proven
against real commands, is what makes six parallel fan-out plans safe.

Output: `.planning/ledgers/milestone-09-12.md` — 29 section headers, 120 `| REQ-` rows, Milestone 9
Epic 1 fully derived, the other 114 rows carrying either their transcribed run-5 input or a
`pending — plan 13-NN` marker naming their owner.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-CONTEXT.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-RESEARCH.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Create the ledger and derive Milestone 9 Epic 1 end-to-end</name>
  <files>.planning/ledgers/milestone-09-12.md</files>
  <read_first>
    - `.planning/ledgers/milestone-07-08.md` — the structural template and the closest-analogue sibling; read its opening supersession line, primary-key note, evidence-bar paragraph, manifest carve-out, verdict legend and its two-column row shape. Its `:5` already names `milestone-09-12.md`.
    - `.planning/REQUIREMENTS.md:3607-3650` — the existing draft ledger's head note, its eleven-class status key at `:3632-3636`, and the Milestone 9 Epic 1 section at `:3638-3648`.
    - `.planning/intel/requirements.md` — the acceptance-criteria text for `REQ-taskservice-dispatch`, `REQ-default-task-services-real-logic` and `REQ-workflow-lifecycle-integration-test`; grep each ID with `grep -n '^## REQ-taskservice-dispatch' .planning/intel/requirements.md` and read that block. A `Verify` row cannot be turned into a verdict without knowing what "done" means for it.
  </read_first>
  <action>
Create `.planning/ledgers/milestone-09-12.md`. Write the spine in the order `milestone-07-08.md` uses:

Opening supersession line: this file supersedes `REQUIREMENTS.md`'s `## Milestone 9-12 as-shipped
ledger` section, which plan 13-10 reduces to a pointer (D-01), and state that this is the **fifth and
final** sibling — the series is complete, there is no sixth name to forward. Name the four existing
siblings `milestone-01.md`, `milestone-02-03.md`, `milestone-04-06.md`, `milestone-07-08.md`.

Primary-key note: the `REQ-*` ID is the key; outstanding task items nest under the requirement they
belong to rather than getting invented identifiers; two requirements citing the same artefact keep
separate rows and separate verdicts (D-00f).

Evidence-bar paragraph: no row gets a `Shipped` verdict without a `file:line` citation plus something
that exercises it, and the bar applies to all 120 rows without exception — including the 53 rows an
earlier ingest already marked with the bare status word `Shipped`, because an ingest status word is
precisely the "the code exists" claim the bar exists to reject (D-03, D-00e).

Verdict legend: a `Verdict | Meaning` table for the eleven run-5 classes exactly as written at
`REQUIREMENTS.md:3632-3636` — `Shipped`, `Shipped (relocated)`, `Shipped, superseded`,
`Shipped, one acceptance criterion false`, `Superseded by outcome`, `Verified open`, `Variant`,
`Contract diverges`, `Open defect → X`, `Provenance only` — with `Verify` recorded as **retired**:
it was never a verdict, only a marker meaning ORCH-01 owes one here, and no row ships carrying it
(D-02). Follow the table with a mapping callout reconciling those eleven onto the series' seven-class
vocabulary, the way Phase 10's D-02 mapped four onto seven. If a row qualifies for two classes at
once, state the tie-break explicitly and follow `milestone-07-08.md`'s precedent that `relocated`
wins.

Then write `### Milestone 9 Epic 1 — Orchestrator End-to-End Workflow Execution (6 IDs)` and a
two-column `| Requirement | Verdict |` table with these six rows, in this order, each verdict derived
this session and each citation re-run before it is written down (D-00e, D-21):
`REQ-workflow-execution-loop`, `REQ-taskservice-dispatch`, `REQ-default-task-services-real-logic`,
`REQ-workflow-repository-port`, `REQ-workflow-crash-recovery`, `REQ-workflow-lifecycle-integration-test`.

For the three currently-bare `Verify` rows — `REQ-taskservice-dispatch`,
`REQ-default-task-services-real-logic`, `REQ-workflow-lifecycle-integration-test` — read the
acceptance criteria in `.planning/intel/requirements.md`, locate the implementing symbol and its
exerciser with `grep -rn` over `src/` and `crates/`, and write a real verdict from the eleven-class
key with the command and its output recorded verbatim. If no evidence can be found for a row, the
verdict is `Verified open` with the search command that found nothing — an honest verdict, never a
silent omission and never a carried-over `Verify`.

For `REQ-workflow-repository-port` re-run the citation rather than transcribing it: confirm
`crates/paladin-ports/src/output/workflow_repository_port.rs` exists and name the consumer that
exercises it. For `REQ-workflow-execution-loop` re-read `src/application/services/orchestration/mod.rs`
and confirm `execute_workflow` and `execute_workflow_inner` are still at the cited lines before
repeating them; the draft's own caveat that per-variant behaviour was not individually re-checked is
either discharged with evidence or carried forward as a stated limitation of the verdict, not
dropped. For `REQ-workflow-crash-recovery` keep the Epic 1 Open Question 4 resolution-by-outcome note
and re-confirm `crates/paladin-storage/src/sqlite_workflow_repository.rs`.

This section is the tracer: it is written for keeps, at the same evidence bar as every fan-out plan,
and plans 13-02 through 13-07 copy its shape.
  </action>
  <verify>
    <automated>test -f .planning/ledgers/milestone-09-12.md && grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/ledgers/milestone-09-12.md` → exit 0
    - `grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md` → `6`
    - `grep -c '^### Milestone 9 Epic 1 — Orchestrator End-to-End Workflow Execution (6 IDs)$' .planning/ledgers/milestone-09-12.md` → `1`
    - `grep -c 'Verify' .planning/ledgers/milestone-09-12.md` → counts only the legend line retiring the class; `grep -c '| Verify' .planning/ledgers/milestone-09-12.md` → `0`
    - `grep -c 'milestone-07-08.md' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c 'fifth and final' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `awk -F'|' '/^\| REQ-/ {print NF}' .planning/ledgers/milestone-09-12.md | sort -u` → `4` (a two-column row; no third Evidence column)
  </acceptance_criteria>
  <done>The ledger file exists with its supersession line, primary-key note, evidence-bar paragraph and verdict legend, and Milestone 9 Epic 1's six rows carry real verdicts whose citations were re-run this session.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the head notes, the two highlight tables and the contention table</name>
  <files>.planning/ledgers/milestone-09-12.md</files>
  <read_first>
    - `.planning/ledgers/milestone-09-12.md` — the file as task 1 left it.
    - `.planning/ledgers/milestone-07-08.md` — its manifest carve-out paragraph, its per-milestone checkbox corroboration paragraph, its `Superseded by outcome` summary table (the model for both highlight tables), its row-order/amendment convention paragraph and its ledger-file-contention table.
    - `.planning/REQUIREMENTS.md:2084-2202` — the two inherited hand-off blocks; item 2 of the Phase 12 block is the verbatim source of the both-halves verdict class.
  </read_first>
  <action>
Append the remaining head-note material to `.planning/ledgers/milestone-09-12.md`, above the epic
sections.

**Manifest carve-out paragraph.** Milestones 10 and 12 are structural in large part, so a manifest
line, workflow job or `Makefile` target plus a named consumer is the exercising artefact (Phase 7's
D-01, carried by D-03). Name the specific job or target that exercises a manifest-only row rather
than asserting "CI runs it".

**Path-caveat / factual-anchor paragraphs**, each re-grepped this session rather than trusted:
(1) every agent path in `crates/paladin-web/openapi.json` carries the `/v1` prefix (D-11);
(2) `.github/workflows/ci.yml` has **15** job ids, not the 14 that `intel/code-verification.md:539-540`
records — `lint`, `security-audit`, `cargo-deny`, `osv-scanner`, `api-surface`, `test`, `examples`,
`crate-isolation`, `integration-tests`, `docker`, `kubernetes-smoke`, `e2e-tests`, `benchmark`,
`benchmark-regression-signal`, `publish-dry-run` — measured with
`grep -nE '^  [a-z][a-z0-9-]*:$' .github/workflows/ci.yml`, `security` gone per Phase 9, and the list
handed to Phase 15 / PIPE-01 (D-08);
(3) `scripts/check-api-surface.sh:6` reads the dotted `.project/current-exports.txt` and that file
exists, so run-5 finding 8's "fails on every run" consequence clause is closed in the script and open
only in four Milestone 12 requirement texts (D-09);
(4) the tree is at `Cargo.toml:34` `version = "0.7.0"` on branch `release/v0.7.0` with `v0.7.1` and
`v0.7.0` the newest tags — ORCH-05's own text says `0.6.0`/`v0.5.1` and is two releases stale, the
same sentence Phase 10's D-11 already corrected once in HARD-03 (D-18).

**Corrected arithmetic paragraph (D-04).** ORCH-01's "sixteen entries already carry `settled-by`
pointers … and the remaining 104 need the same treatment" counts two different populations: the
sixteen are variant-register entries (`intel/SYNTHESIS.md:335`, `:546`) and
`grep 'settled-by'` across the draft ledger's own rows returns zero. **All 120 rows need a verdict.**
The measured split is **35** bare `Verify`, **53** bare `Shipped`, **32** already carrying a richer
verdict. Record the counting commands verbatim.

**Per-milestone checkbox corroboration paragraph (D-10, ORCH-02).** The five verdicts, once, here, in
milestone order and none converted into a task: M9 0 open — corroborated; M10 0 open — corroborated in
artefacts and contradicted in one acceptance criterion; M11 26 open — the only genuinely open count in
run 5 and the only one of all 542 across 75 task lists that survives verification, carried to DOCS-01
in Phase 16 and settleable only by content; M12 3 open — vacuous, all three Task 0.0 feature-branch
scaffolding while the Epic 5 work ships as `crates/paladin-web/src/agent_auth.rs`;
project-management 1 open — nonexistent, a formatting example inside a template file. Then the
five-run pattern in exactly one place: understated → accurate → overstated → contradicted → vacuous,
with the corpus position from `intel/code-verification.md:647-659` that checkbox arithmetic is not a
backlog. State explicitly that this paragraph is the only home for the pattern so a sixth
rediscovery has somewhere to land.

**Precedence paragraph (D-00b).** State the order every verdict in this file is resolved under — ADR,
then shipped tree, then `.planning/codebase/` map, then `intel/code-verification.md`, then PRD, then
DOC, then task-list checkbox — and that an ADR contradicting shipped code is an instruction to change
the code. Add the note this phase's own material makes necessary: a published page under `docs/src/`
sits between the tree and the maps, because a reader treats it as the contract, which is why the
`sidecar.md` and `http-service-host.md` defects are corrections rather than annotations.

**Inherited-inventory paragraph (D-07).** Phase 12's plan 12-01 measured 87 stale-citation hits across
25 files, corrected 8 sites in four canonical governance documents, and excluded the rest by a stated
class-by-class scoping rule recorded in `12-01-SUMMARY.md` §Grep Inventory. Those exclusions are
deliberate historical record; re-running the grep and "fixing" them would undo a decision.

**Provenance paragraph.** Name plan 13-01 as the scaffold, name Milestone 9 Epic 1 as the section
derived fully here, and state that every other row's Verdict cell is replaced in place by the fan-out
plan named in the contention table.

**Highlight table 1 — `Shipped, one acceptance criterion false` (D-05).** One row, both halves,
dated: Milestone 10 is recorded 100% complete, ships every artefact it promised, and failed M10 Epic
2 §8's criterion that `audit.toml` and `deny.toml` are the only places policy/exceptions are defined
with no inline advisory-ignore flags remaining in CI — and as of 2026-08-08 no longer does. Phase 9
made it true (plan 09-06, commit `cb75b2b`, deleting the duplicate `security:` job at pre-deletion
`ci.yml:465-482`); Phase 12 promoted it to ADR-0036 and put `scripts/check-workflow-suppressions.sh`
behind it. A row recording only the failure, or only the fix, is wrong.

**Highlight table 2 — the `Verified open` block.** The Deferred-QA Epic 25-29 and related rows, the
highest-confidence forward-work signal in the corpus and the direct input to Phases 14, 15 and 16.
List each `REQ-*` ID with its owning phase so a planner never scans 120 rows to find them.

**Row-order and amendment convention paragraph (D-00d).** Epic sections are in `REQUIREMENTS.md`'s
own order and are never re-sorted; a later plan replaces a row's Verdict cell **in place** and never
inserts, deletes or reorders a row; amendments are appended as dated sections at the foot, never by
rewriting the sections above.

**Ledger-file contention table (D-23).** Columns `Plan | Wave | Owns | May`, one row per plan touching
this file: 13-01 wave 1 owns the whole file and Milestone 9 Epic 1; 13-02 wave 2 owns Milestone 9
Epics 2-6; 13-03 wave 2 owns Milestone 10 Epics 1-5; 13-04 wave 2 owns Milestone 11 Epics 1-2, 3, 4, 6
and 5&7; 13-05 wave 2 owns Milestone 12 Epics 1-4; 13-06 wave 2 owns Milestone 12 Epics 5-7; 13-07
wave 2 owns Deferred-QA Epics 25, 26, 27, 28-29 and project-management; 13-13 wave 4 owns the appended
close-out amendment section only. The `May` column reads "replace Verdict cells inside its own
sections; nothing else" for 13-02 through 13-07.
  </action>
  <verify>
    <automated>grep -c 'understated' .planning/ledgers/milestone-09-12.md && grep -c '35' .planning/ledgers/milestone-09-12.md && grep -c 'kubernetes-smoke' .planning/ledgers/milestone-09-12.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'understated → accurate → overstated → contradicted → vacuous' .planning/ledgers/milestone-09-12.md` → `1`
    - `grep -c 'kubernetes-smoke' .planning/ledgers/milestone-09-12.md` → `≥1` and `grep -c 'examples' .planning/ledgers/milestone-09-12.md` → `≥1` (the D-08 15-job list is present)
    - `grep -c 'cb75b2b' .planning/ledgers/milestone-09-12.md` → `≥1` (D-05's second half is dated and cited)
    - `grep -c 'Shipped, one acceptance criterion false' .planning/ledgers/milestone-09-12.md` → `≥2` (legend plus highlight table)
    - `grep -c 'Verified open' .planning/ledgers/milestone-09-12.md` → `≥2` (legend plus highlight table)
    - `grep -cE '13-0[2-7]' .planning/ledgers/milestone-09-12.md` → `≥6` (the contention table names every fan-out plan)
    - `grep -c '0\.7\.0' .planning/ledgers/milestone-09-12.md` → `≥1` and `grep -c 'v0\.7\.1' .planning/ledgers/milestone-09-12.md` → `≥1` (D-18's corrected current state)
    - `grep -c 'check-api-surface.sh:6' .planning/ledgers/milestone-09-12.md` → `≥1` (D-09)
    - `grep -c '12-01-SUMMARY.md' .planning/ledgers/milestone-09-12.md` → `≥1` (D-07's inherited inventory)
    - `grep -ci 'precedence' .planning/ledgers/milestone-09-12.md` → `≥1` and `grep -c 'code-verification.md' .planning/ledgers/milestone-09-12.md` → `≥1` (D-00b's order is stated)
  </acceptance_criteria>
  <done>Every head-note element the sibling series carries is present, both highlight tables are placed above the epic sections, and the contention table names the disjoint section range of each of plans 13-02 through 13-07.</done>
</task>

<task type="auto">
  <name>Task 3: Transcribe the remaining 28 section headers and 114 row stubs</name>
  <files>.planning/ledgers/milestone-09-12.md</files>
  <read_first>
    - `.planning/ledgers/milestone-09-12.md` — the file as task 2 left it.
    - `.planning/REQUIREMENTS.md:3649-3931` — the 28 remaining section headers and their 114 rows, in the order they appear. This is the transcription source; do not re-order, re-key or re-word any row.
  </read_first>
  <action>
Append the 28 remaining section headers, verbatim including their `(N IDs)` counts, in
`REQUIREMENTS.md`'s own order, each followed by a two-column `| Requirement | Verdict |` table
carrying its rows in source order. The headers and their line anchors in `REQUIREMENTS.md` are:
Milestone 9 Epic 2 (`:3649`), Epic 3 (`:3659`), Epic 4 (`:3668`), Epic 5 (`:3682`), Epic 6 (`:3692`);
Milestone 10 Epic 1 (`:3698`), Epic 2 (`:3707`), Epic 3 (`:3729`), Epic 4 (`:3740`), Epic 5 (`:3746`);
Milestone 11 Epics 1-2 (`:3758`), Epic 3 (`:3767`), Epic 4 (`:3782`), Epic 6 (`:3791`), Epics 5 & 7
(`:3800`); Milestone 12 Epic 1 (`:3809`), Epic 2 (`:3820`), Epic 3 (`:3829`), Epic 4 (`:3838`), Epic 5
(`:3848`), Epic 6 (`:3864`), Epic 7 (`:3873`); Deferred-QA Epic 25 (`:3883`), Epic 26 (`:3899`),
Epic 27 (`:3908`), Epics 28-29 (`:3915`); project-management (`:3924`). Re-read each anchor before
transcribing — the corpus's own records cite stale lines repeatedly, and this plan must not add
another instance.

Row interim-state contract, following `milestone-07-08.md`'s own convention:
- A row whose current verdict is a bare `Verify` gets the Verdict cell `pending — plan 13-NN`,
  naming its owning fan-out plan from the contention table. Nothing is ever left blank.
- Every other row is transcribed with its existing verdict text prefixed
  `run-5 input (not yet re-derived): ` so a reader can tell an inherited claim from a derived one.
  The prefix is removed by the owning fan-out plan when it re-derives the citation (D-03).

Do not fill in any verdict outside Milestone 9 Epic 1 — those belong to plans 13-02 through 13-07 and
filling them here would produce the file contention the contention table exists to prevent.
  </action>
  <verify>
    <automated>grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md && grep -c '^### ' .planning/ledgers/milestone-09-12.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md` → `120`
    - `grep -oE '^\| REQ-[a-z0-9-]+' .planning/ledgers/milestone-09-12.md | sort -u | wc -l` → `120` (no duplicated or dropped ID)
    - `grep -c '^### ' .planning/ledgers/milestone-09-12.md` → `29`
    - `diff <(grep -oE '^\| REQ-[a-z0-9-]+' .planning/ledgers/milestone-09-12.md) <(sed -n '3638,3931p' .planning/REQUIREMENTS.md | grep -oE '^\| REQ-[a-z0-9-]+')` → no output (same IDs, same order)
    - `grep -c 'pending — plan 13-' .planning/ledgers/milestone-09-12.md` → `32` (the 35 bare `Verify` rows minus the 3 derived in Milestone 9 Epic 1)
    - `grep -cE '^\| REQ-[a-z0-9-]+ \|\s*$' .planning/ledgers/milestone-09-12.md` → `0` (no blank Verdict cell)
  </acceptance_criteria>
  <done>All 120 rows and all 29 section headers are present in source order, Milestone 9 Epic 1 is derived, 32 rows carry a `pending — plan 13-NN` marker naming their owner, and every other row carries its transcribed run-5 input with the not-yet-re-derived prefix.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Created by this plan:
- `.planning/ledgers/milestone-09-12.md`

New identifiers a later reader will grep for, introduced here:
- Section headers `### Milestone 9 Epic 1 — Orchestrator End-to-End Workflow Execution (6 IDs)` through `### project-management (1 ID)` (29 total)
- Head-note section names: `Evidence bar`, `Manifest carve-out`, `Corrected arithmetic (D-04)`, `Per-milestone checkbox corroboration`, `The five-run pattern`, `Inherited stale-citation inventory (D-07)`, `Provenance`, `Verdict legend and the seven-class mapping`, `Shipped, one acceptance criterion false — the single instance`, `Verified open — the Deferred-QA block`, `Row order and amendment convention`, `Ledger-file contention`
- Interim row markers `pending — plan 13-NN` and `run-5 input (not yet re-derived):`
- Retired verdict marker: `Verify` recorded as retired in the legend
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingest record -> ledger | run-5 status words and `file:line` citations cross into a document later phases treat as ground truth |
| ledger -> Phases 14/15/16 | forward-work signal crosses out of this phase with no further review |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | ledger Verdict cells | high | mitigate | D-03 evidence bar: no `Shipped` without a `file:line` plus an exerciser; the 53 bare-`Shipped` rows carry the `run-5 input (not yet re-derived)` prefix until a fan-out plan re-runs the citation |
| T-13-02 | Tampering | transcribed `file:line` citations | high | mitigate | Every task's `read_first` names the cited file; task 3 re-reads each of the 28 section anchors in `REQUIREMENTS.md` before transcribing |
| T-13-03 | Repudiation | head-note corrections (D-04, D-08, D-18) | medium | mitigate | D-00c/D-00d: the superseded figure is quoted alongside the corrected one with the measuring command recorded verbatim; nothing is silently overwritten |
| T-13-04 | Elevation of privilege | locked decisions D-01..D-23 | medium | mitigate | Tasks cite decisions by `D-NN` token; the executor implements, never re-decides; any conflict is surfaced, not absorbed |
| T-13-05 | Denial of service | parallel wave-2 writes to one file | medium | mitigate | The contention table fixes disjoint section ranges before any fan-out plan runs; task 3 leaves no verdict outside Milestone 9 Epic 1 |
| T-13-SC | Tampering | package installs | low | accept | N/A — this plan installs no packages and modifies no manifest (D-19). `13-RESEARCH.md` §Package Legitimacy Audit records the gate as skipped for this reason rather than silently omitted |
</threat_model>

<verification>
- `grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md` → `120`
- `grep -c '^### ' .planning/ledgers/milestone-09-12.md` → `29`
- `grep -c '| Verify' .planning/ledgers/milestone-09-12.md` → `0`
- `git diff --name-only -- '*.rs' | wc -l` → `0`
</verification>

<success_criteria>
The ledger exists with 120 rows, 29 sections, complete head notes, both highlight tables and a
contention table; Milestone 9 Epic 1 is derived to the D-00e bar; the six fan-out plans can start in
parallel without touching each other's rows.
</success_criteria>

<output>
Create `.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-01-SUMMARY.md` when done.
</output>
