---
phase: 13-milestone-9-12-ground-truth-recorded-account
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - .planning/ledgers/milestone-09-12.md
autonomous: true
requirements: [ORCH-01]
must_haves:
  truths:
    - "All 19 rows in Milestone 9 Epics 2-6 carry a verdict from the eleven-class key with a `file:line` re-read this session; none reads `Verify` and none reads `pending — plan 13-02`"
    - "The 10 rows that arrived as a bare `Verify` carry a real verdict derived from their acceptance criteria in `.planning/intel/requirements.md`, including an honest `Verified open` where the search found nothing"
    - "The 9 rows that arrived as `run-5 input (not yet re-derived)` have had their citations re-run and the prefix removed (D-03)"
    - "`REQ-opaque-bearer-token-adapter-v1` records the Milestone 9 mechanism half of the JWT-vs-opaque contradiction and points at Phase 14 / WEB-01 without resolving it"
  artifacts:
    - .planning/ledgers/milestone-09-12.md
  key_links:
    - "Milestone 9 Epic 2-6 rows -> `.planning/intel/requirements.md` acceptance criteria for each bare-`Verify` ID"
    - "`REQ-m9-quality-gate-v030` -> ADR-0029's `v0.3.0` trajectory row appended by plan 13-12 (D-16)"
  prohibitions:
    - "No `.rs` file is modified by this plan (D-19)"
    - "No row outside Milestone 9 Epics 2-6 is touched; the contention table in the ledger head note is the authority (D-23)"
    - "No `Shipped` verdict without a `file:line` plus an exerciser; an ingest status word is not evidence (D-03)"
    - "No row is inserted, deleted or reordered; only Verdict cells are replaced in place (D-00d)"
    - "No bare `Verify` survives; it is a marker, not a verdict (D-02)"
    - "No item from 13-CONTEXT.md's `<deferred>` block is planned or actioned here"
---

<objective>
Derive real, cited verdicts for the 19 requirement IDs in Milestone 9 Epics 2-6 — the densest
bare-`Verify` cluster in the ledger, 10 of the 35.

Purpose: Milestone 9 is the classic orchestrator completion. The ledger must state plainly that the
whole subsystem ships, so none of it is re-planned, and it can only say that with evidence.

Output: 19 Verdict cells replaced in place in `.planning/ledgers/milestone-09-12.md`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-CONTEXT.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive Milestone 9 Epics 2-4 (13 rows)</name>
  <files>.planning/ledgers/milestone-09-12.md</files>
  <read_first>
    - `.planning/ledgers/milestone-09-12.md` — the head notes (evidence bar, manifest carve-out, verdict legend, contention table) and the Milestone 9 Epic 1 section plan 13-01 derived, which is the shape to copy.
    - `.planning/intel/requirements.md` — the acceptance-criteria block for each of the six bare-`Verify` IDs in this task's range; locate each with `grep -n '^## REQ-<id>' .planning/intel/requirements.md` and read its block. A `Verify` row cannot become a verdict without knowing what "done" means for it.
    - `.planning/intel/code-verification.md:417-471` — run 5's 37-row Verified SHIPPED table, for the rows it already covers.
  </read_first>
  <action>
Replace the Verdict cell of these 13 rows in place, in the sections
`### Milestone 9 Epic 2 — Scheduler, Queue & Event Operational Validation (5 IDs)`,
`### Milestone 9 Epic 3 — Content Processing Pipeline (4 IDs)` and
`### Milestone 9 Epic 4 — Agent / Orchestrator Bridge (4 IDs)`.

Epic 2 — `REQ-scheduler-tick-validation`, `REQ-cron-adapter-validation`,
`REQ-queueport-contract-parity`, `REQ-queue-retry-dead-letter`, `REQ-event-trigger-job-pipeline`.
Four of these five arrived as a bare `Verify`; this is the single densest cluster in the ledger.

Epic 3 — `REQ-paladin-content-processor`, `REQ-battalion-content-processor`,
`REQ-content-processor-orchestrator-wiring`, `REQ-content-ingestion-e2e-validation`.

Epic 4 — `REQ-orchestrator-port`, `REQ-bridge-policy-guardrails`, `REQ-orchestrator-bridge-adapter`,
`REQ-execution-service-bridge-wiring`.

For each row: read the acceptance criteria, locate the implementing symbol with `grep -rn` over
`src/` and `crates/`, locate its exerciser (a test, an example, a `Makefile` target or a named CI job
from the head note's 15-job list), and write the verdict as one of the eleven classes with the
command and its output recorded verbatim (D-00e, D-21). Behavioural requirements need a test,
example or command; a manifest-only claim takes the head note's carve-out and must name its consumer
(D-03).

Where a row already carried `run-5 input (not yet re-derived):`, re-run the citation before repeating
it and remove the prefix once it holds. If the cited path has moved, record it as
`Shipped (relocated)` with both paths — the relocation signal is the corpus's largest false-gap
generator. If no evidence can be found, the verdict is `Verified open` with the search command that
returned nothing; an unfindable exerciser is an honest verdict, never a silent omission.

On `REQ-orchestrator-bridge-adapter` and `REQ-bridge-policy-guardrails`, note in the Verdict cell that
`.project/Milestone_9/Epic_4/prd-agent-orchestrator-bridge.md` §6.1 is `PROMOTION.md` Part B candidate
9 and is **not** promoted to an ADR by this phase — D-20 allocates exactly 0037-0039 and no ORCH
requirement's `Derives` list reaches that PRD section. Its disposition is stated by plan 13-13's
advancing note; record the pointer here so the two documents do not disagree silently.
  </action>
  <verify>
    <automated>awk '/^### Milestone 9 Epic 2/,/^### Milestone 9 Epic 5/' .planning/ledgers/milestone-09-12.md | grep -c 'pending — plan 13-02\|^| REQ-[a-z-]* | Verify'</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^### Milestone 9 Epic 2/,/^### Milestone 9 Epic 5/' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'` → `13`
    - `awk '/^### Milestone 9 Epic 2/,/^### Milestone 9 Epic 5/' .planning/ledgers/milestone-09-12.md | grep -c 'pending — plan'` → `0`
    - `awk '/^### Milestone 9 Epic 2/,/^### Milestone 9 Epic 5/' .planning/ledgers/milestone-09-12.md | grep -c 'run-5 input (not yet re-derived)'` → `0`
    - `awk '/^### Milestone 9 Epic 2/,/^### Milestone 9 Epic 5/' .planning/ledgers/milestone-09-12.md | grep -cE '^\| REQ-[a-z0-9-]+ \| Verify'` → `0`
    - `awk '/^### Milestone 9 Epic 2/,/^### Milestone 9 Epic 5/' .planning/ledgers/milestone-09-12.md | grep -cE '\.rs:[0-9]+|\.toml:[0-9]+|\.yml:[0-9]+'` → `≥10` (each verdict carries a line-addressed citation)
    - `grep -c 'candidate 9' .planning/ledgers/milestone-09-12.md` → `≥1`
  </acceptance_criteria>
  <done>All 13 rows in Milestone 9 Epics 2-4 carry a verdict from the eleven-class key with a citation re-run this session, and the Part B candidate-9 pointer is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Derive Milestone 9 Epics 5-6 (6 rows)</name>
  <files>.planning/ledgers/milestone-09-12.md</files>
  <read_first>
    - `.planning/ledgers/milestone-09-12.md` — the rows this task owns and the Epic 2-4 rows task 1 just wrote.
    - `src/infrastructure/adapters/auth/in_memory_token_auth_adapter.rs` — the only `AuthPort` implementation, and the Milestone 9 half of the JWT-vs-opaque contradiction.
    - `.planning/intel/requirements.md` — the acceptance-criteria blocks for `REQ-user-role-rbac`, `REQ-auth-middleware-rbac-guards`, `REQ-user-crud-completeness` and `REQ-m9-quality-gate-v030`.
  </read_first>
  <action>
Replace the Verdict cell of these 6 rows in place, in
`### Milestone 9 Epic 5 — User / Admin System Completion (5 IDs)` and
`### Milestone 9 Epic 6 — Finalization & Release (1 ID)`:
`REQ-user-role-rbac`, `REQ-auth-port`, `REQ-opaque-bearer-token-adapter-v1`,
`REQ-auth-middleware-rbac-guards`, `REQ-user-crud-completeness`, `REQ-m9-quality-gate-v030`.

`REQ-opaque-bearer-token-adapter-v1` is the highest-value row in this plan. Record that the shipped
mechanism is Milestone 9's opaque, in-process, hashed token store at
`src/infrastructure/adapters/auth/in_memory_token_auth_adapter.rs`, chosen deliberately with JWT as
an explicit non-goal, and that `grep -rn 'jsonwebtoken' Cargo.toml crates/*/Cargo.toml` returns
nothing — re-run that grep and record its output. State that Milestone 12's `agent_auth.rs`
documents its verifier as JWT throughout, that this is variant group 29 and the only variant in five
runs shipped code cannot settle, and that **Phase 14 / WEB-01 owns the resolution**. Verdict class:
`Contract diverges`. Do not resolve it here — WEB-01 is out of this phase's scope.

`REQ-m9-quality-gate-v030` is the Milestone 9 lockstep release gate. Record the version bump and tag
with the mechanism citation (`REQ-lockstep-versioning`), and note that plan 13-12 transcribes this
row into ADR-0029's `## Trajectory` table as the `v0.3.0` row (D-16). Do not append the trajectory
row here — ADR-0029 belongs to plan 13-12.

Apply the same evidence rule as task 1 to the remaining four rows: citation re-run, exerciser named,
`Verified open` with its search command where nothing is found.
  </action>
  <verify>
    <automated>awk '/^### Milestone 9 Epic 5/,/^### Milestone 10 Epic 1/' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^### Milestone 9 Epic 5/,/^### Milestone 10 Epic 1/' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'` → `6`
    - `awk '/^### Milestone 9 Epic 5/,/^### Milestone 10 Epic 1/' .planning/ledgers/milestone-09-12.md | grep -c 'pending — plan'` → `0`
    - `awk '/^### Milestone 9 Epic 5/,/^### Milestone 10 Epic 1/' .planning/ledgers/milestone-09-12.md | grep -c 'run-5 input (not yet re-derived)'` → `0`
    - `grep -c 'Contract diverges' .planning/ledgers/milestone-09-12.md` → `≥2` (legend plus this row)
    - `grep -c 'WEB-01' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c 'in_memory_token_auth_adapter.rs' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md` → `120` (nothing added or dropped)
  </acceptance_criteria>
  <done>All 6 rows in Milestone 9 Epics 5-6 carry a cited verdict, the JWT-vs-opaque contradiction is recorded as `Contract diverges` and handed to Phase 14 / WEB-01 unresolved, and the ledger still holds exactly 120 rows.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Modified by this plan (no new files):
- `.planning/ledgers/milestone-09-12.md` — Verdict cells for 19 IDs

Identifiers a later reader will grep for, finalized here:
- `REQ-scheduler-tick-validation`, `REQ-cron-adapter-validation`, `REQ-queueport-contract-parity`, `REQ-queue-retry-dead-letter`, `REQ-event-trigger-job-pipeline`
- `REQ-paladin-content-processor`, `REQ-battalion-content-processor`, `REQ-content-processor-orchestrator-wiring`, `REQ-content-ingestion-e2e-validation`
- `REQ-orchestrator-port`, `REQ-bridge-policy-guardrails`, `REQ-orchestrator-bridge-adapter`, `REQ-execution-service-bridge-wiring`
- `REQ-user-role-rbac`, `REQ-auth-port`, `REQ-opaque-bearer-token-adapter-v1`, `REQ-auth-middleware-rbac-guards`, `REQ-user-crud-completeness`
- `REQ-m9-quality-gate-v030`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped tree -> ledger verdict | grep output crosses into a claim later phases treat as settled |
| ledger -> Phase 14 | the `Contract diverges` row is WEB-01's entire inherited framing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | Verdict cells for 19 IDs | high | mitigate | Every verdict names the command run this session and its output; the `run-5 input (not yet re-derived)` prefix is removed only after the citation is re-run (D-03) |
| T-13-02 | Tampering | inherited `file:line` citations | high | mitigate | `read_first` names the cited file; a moved path becomes `Shipped (relocated)` with both paths rather than a silently updated citation |
| T-13-06 | Repudiation | the WEB-01 hand-off | medium | mitigate | The row records the contradiction and names Phase 14 / WEB-01 as owner; it does not pick a mechanism, so Phase 14 inherits the question rather than a fabricated answer |
| T-13-04 | Elevation of privilege | D-20's three-ADR allocation | medium | mitigate | The Part B candidate-9 pointer is recorded as a pointer only; no fourth ADR is authored here |
| T-13-05 | Denial of service | parallel wave-2 writes | medium | mitigate | This plan touches only Milestone 9 Epics 2-6; the `grep -c '^| REQ-'` → `120` check proves nothing was added or dropped |
| T-13-SC | Tampering | package installs | low | accept | N/A — no package install, no manifest change (D-19) |
</threat_model>

<verification>
- `awk '/^### Milestone 9 Epic 2/,/^### Milestone 10 Epic 1/' .planning/ledgers/milestone-09-12.md | grep -c 'pending — plan'` → `0`
- `grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md` → `120`
- `git diff --name-only -- '*.rs' | wc -l` → `0`
</verification>

<success_criteria>
Milestone 9 Epics 2-6 are fully derived at the D-00e bar; the ledger can state plainly that the whole
Milestone 9 orchestrator subsystem ships.
</success_criteria>

<output>
Create `.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-02-SUMMARY.md` when done.
</output>
