---
phase: 13-milestone-9-12-ground-truth-recorded-account
plan: 04
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - .planning/ledgers/milestone-09-12.md
autonomous: true
requirements: [ORCH-01, ORCH-02]
must_haves:
  truths:
    - "All 20 rows in Milestone 11 Epics 1-2, 3, 4, 6 and 5&7 carry a verdict from the eleven-class key with a `file:line` re-read this session; none reads `Verify` and none reads `pending — plan 13-04`"
    - "The three requirements behind Milestone 11's 26 open items — `REQ-user-guides-rewrite`, `REQ-deployment-operations-docs-update`, `REQ-doc-link-repair-linkcheck` — record that all fourteen target files exist so file existence settles nothing, that the count is the only genuinely open one in run 5, and that it is carried to DOCS-01 in Phase 16 rather than settled here (D-10)"
    - "`REQ-doc-link-repair-linkcheck` records the measured `mdbook build docs/` result from this session — including that it fails today for two pre-existing broken links in `deployment/docker.md` and `user-guides/tool-integration.md`, neither of which this phase touches"
    - "`REQ-deployment-topologies-section` records M11 Epic 6 FR-8's `single source of routing` framing and names it as ADR-0039's placement target without pre-empting the plan 13-09 checkpoint"
  artifacts:
    - .planning/ledgers/milestone-09-12.md
  key_links:
    - "`REQ-user-guides-rewrite` / `REQ-deployment-operations-docs-update` -> Phase 16 / DOCS-01"
    - "`REQ-deployment-topologies-section` -> ADR-0039 and `docs/src/deployment-topologies/overview.md` (plan 13-09)"
    - "`REQ-m11-v050-release` -> ADR-0029's `v0.5.0` trajectory row appended by plan 13-12 (D-16)"
  prohibitions:
    - "No `.rs` file is modified by this plan (D-19)"
    - "No row outside Milestone 11 Epics 1-2, 3, 4, 6 and 5&7 is touched (D-23)"
    - "Milestone 11's 26 open items are not converted into a task or a phase here; they are carried to DOCS-01 (D-10)"
    - "No file under `docs/src/` is edited by this plan; the three permitted documentation edits belong to plans 13-08 and 13-09 (D-19)"
    - "No `Shipped` verdict without a `file:line` plus an exerciser (D-03); no bare `Verify` survives (D-02)"
    - "A failing `mdbook build` is not attributed to this phase and not reinterpreted as a pass"
    - "No item from 13-CONTEXT.md's `<deferred>` block is planned or actioned here"
---

<objective>
Derive real, cited verdicts for the 20 requirement IDs in Milestone 11, and record the one genuinely
open checkbox count in the corpus for what it is — settleable only by content, and owned by Phase 16.

Purpose: Milestone 11 shipped the mdbook. Its 26 open items are the only count of all 542 across 75
task lists that survives verification, and the ledger is where a Phase 16 planner will find out that
all fourteen target files already exist.

Output: 20 Verdict cells replaced in place in `.planning/ledgers/milestone-09-12.md`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-CONTEXT.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive Milestone 11 Epics 1-2 and 3 (11 rows) with the 26-open verdict</name>
  <files>.planning/ledgers/milestone-09-12.md</files>
  <read_first>
    - `.planning/ledgers/milestone-09-12.md` — the head notes, the per-milestone checkbox corroboration paragraph plan 13-01 wrote, and the 11 rows this task owns.
    - `.project/Milestone_11-Documentation-Overhaul-Publish/Epic_3/tasks-content-rewrite.md` — the 26 open items themselves: task 6.0 (six user-guide updates), task 7.0 (eight deployment/operations updates), task 1.2 (linkcheck review).
    - `docs/book.toml` — `[output.linkcheck] warning-policy = "error"`, `follow-web-links = false`, the gate `REQ-doc-link-repair-linkcheck` is measured against.
    - `.planning/intel/requirements.md` — acceptance criteria for the bare-`Verify` IDs in this range.
  </read_first>
  <action>
Replace the Verdict cell of these 11 rows in place, in
`### Milestone 11 Epics 1-2 — mdbook Scaffold & Chapter Hierarchy (4 IDs)` and
`### Milestone 11 Epic 3 — Content Rewrite (7 IDs)`.

Epics 1-2 — `REQ-mdbook-scaffold`, `REQ-mdbook-chapter-hierarchy`, `REQ-docs-ci-pages-deploy`,
`REQ-docs-migration-log`.

Epic 3 — `REQ-doc-link-repair-linkcheck`, `REQ-doc-example-compile-gate`,
`REQ-getting-started-rewrite`, `REQ-architecture-docs-update`, `REQ-user-guides-rewrite`,
`REQ-deployment-operations-docs-update`, `REQ-api-reference-contributing-rewrite`.

`REQ-user-guides-rewrite`, `REQ-deployment-operations-docs-update` and `REQ-doc-link-repair-linkcheck`
are the three requirements behind Milestone 11's 26 open items (D-10). For each: confirm by `ls` that
its target files exist and record the count, then state that file existence settles nothing —
whether the content is current is settleable only by reading it. Verdict class `Verified open` with
the owner named as **Phase 16 / DOCS-01**. Do not convert any of the 26 into a task here; ORCH-02's
verdict is that this is the only genuinely open count in run 5, and carrying it is the deliverable.

`REQ-doc-link-repair-linkcheck` additionally records the measured build. Run `mdbook build docs/`
and record its exit code and error list verbatim. 13-RESEARCH.md established the pre-phase baseline
on 2026-08-10: exit `101`, one `Linking outside of the "root" directory is forbidden` error at
`deployment/docker.md:118` and one `Potential incomplete link` error at
`user-guides/tool-integration.md:324`, both in files this phase does not touch, both post-dating the
last successful `docs.yml` run of 2026-07-06. State the result honestly: the linkcheck gate would
fail today for reasons that predate Phase 13. Do not attribute it to this phase and do not
reinterpret it as a pass. If `mdbook` is not installed, install it offline with the versions
`.github/workflows/docs.yml` pins — `mdbook` 0.4.40, `mdbook-mermaid` 0.13.0, `mdbook-linkcheck`
0.7.7, each via `cargo install --version <v> --offline` — and if that fails, record the failure
verbatim and scope the claim CI-only (D-21).

`REQ-architecture-docs-update` touches the same document as `REQ-arch-doc-modernization` (plan
13-07's row) but is a different requirement with a different verdict — keep them separate (D-00f) and
cross-reference rather than merge.

Apply the D-03 evidence bar to the remaining rows: `file:line` plus an exerciser, the manifest
carve-out where the claim is a workflow job or `book.toml` setting, `Verified open` with its search
command where nothing is found.
  </action>
  <verify>
    <automated>awk '/^### Milestone 11 Epics 1-2/,/^### Milestone 11 Epic 4/' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^### Milestone 11 Epics 1-2/,/^### Milestone 11 Epic 4/' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'` → `11`
    - `awk '/^### Milestone 11 Epics 1-2/,/^### Milestone 11 Epic 4/' .planning/ledgers/milestone-09-12.md | grep -c 'pending — plan'` → `0`
    - `awk '/^### Milestone 11 Epics 1-2/,/^### Milestone 11 Epic 4/' .planning/ledgers/milestone-09-12.md | grep -c 'run-5 input (not yet re-derived)'` → `0`
    - `grep -c 'DOCS-01' .planning/ledgers/milestone-09-12.md` → `≥3` (one per carried requirement)
    - `grep -c 'tool-integration.md:324' .planning/ledgers/milestone-09-12.md` → `≥1` and `grep -c 'docker.md:118' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c 'exit 101\|exit code 101' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `git diff --name-only -- 'docs/*' | wc -l` → `0` (this plan edits no documentation file)
  </acceptance_criteria>
  <done>All 11 rows carry a cited verdict; the 26 open items are recorded as the corpus's only genuine open count with all fourteen files confirmed present and Phase 16 / DOCS-01 named as owner; the measured `mdbook build` result is recorded with its pre-existing failures attributed correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Derive Milestone 11 Epics 4, 6 and 5&7 (9 rows)</name>
  <files>.planning/ledgers/milestone-09-12.md</files>
  <read_first>
    - `.planning/ledgers/milestone-09-12.md` — the rows this task owns and the Epic 1-3 rows task 1 just wrote.
    - `docs/src/deployment-topologies/overview.md` — the five-topology comparison table and the routing flowchart; `REQ-deployment-topologies-section`'s subject and ADR-0039's placement target. Read it, do not edit it.
    - `README.md` — 193 lines; `REQ-readme-landing-page`'s subject and the other half of D-13(e).
    - `.planning/intel/requirements.md` — acceptance criteria for the bare-`Verify` IDs in this range.
  </read_first>
  <action>
Replace the Verdict cell of these 9 rows in place, in
`### Milestone 11 Epic 4 — New Subsystem Guides (4 IDs)`,
`### Milestone 11 Epic 6 — Deployment Topologies (1 ID)` and
`### Milestone 11 Epics 5 & 7 — README, Version Sync, Final Review, v0.5.0 (4 IDs)`.

Epic 4 — `REQ-orchestration-guide`, `REQ-content-processing-guide`,
`REQ-agent-orchestrator-bridge-guide`, `REQ-crate-map-feature-flag-reference`. Three of these four
arrived as a bare `Verify`; for each, confirm the guide page exists under `docs/src/`, name the
`SUMMARY.md` entry that reaches it (that is the exerciser for a documentation deliverable), and cite
both.

Epic 6 — `REQ-deployment-topologies-section`. Record that the section ships as
`docs/src/deployment-topologies/` with `overview.md` as the routing matrix M11 Epic 6 FR-8 designates
"the single source of routing", and that this page is where ADR-0039's Garrison/Arsenal limitation
lands (plan 13-09, gated on a blocking human checkpoint). Record the pointer only — do not state the
limitation here and do not pre-empt the checkpoint's outcome.

Epics 5 & 7 — `REQ-mdbook-final-review`, `REQ-doc-version-sync`, `REQ-readme-landing-page`,
`REQ-m11-v050-release`.
`REQ-readme-landing-page`: re-measure the README with `wc -l README.md` and
`grep -ic 'asciinema\|demo' README.md` and record both outputs; the rewrite into a concise landing
page is what removed the demos section, which is the context for `REQ-asciinema-demos` (plan 13-07's
row, D-13(e)). Cross-reference; do not duplicate that row's verdict here.
`REQ-doc-version-sync`: the version figures the docs are synced against are `Cargo.toml:34`
`version = "0.7.0"` and tags `v0.7.1`/`v0.7.0` — re-run `grep -n '^version' Cargo.toml` and
`git tag --sort=-v:refname | head -5` and cite the outputs, not ORCH-05's stale `0.6.0`/`v0.5.1`
figures (D-18).
`REQ-m11-v050-release`: record the lockstep bump and tag, and note that plan 13-12 transcribes it as
ADR-0029's `v0.5.0` row (D-16). Do not touch ADR-0029 here.
  </action>
  <verify>
    <automated>awk '/^### Milestone 11 Epic 4/,/^### Milestone 12 Epic 1/' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^### Milestone 11 Epic 4/,/^### Milestone 12 Epic 1/' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'` → `9`
    - `awk '/^### Milestone 11 Epic 4/,/^### Milestone 12 Epic 1/' .planning/ledgers/milestone-09-12.md | grep -c 'pending — plan'` → `0`
    - `awk '/^### Milestone 11 Epic 4/,/^### Milestone 12 Epic 1/' .planning/ledgers/milestone-09-12.md | grep -c 'run-5 input (not yet re-derived)'` → `0`
    - `grep -c 'single source of routing' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c 'ADR-0039' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c '193' .planning/ledgers/milestone-09-12.md` → `≥1` (the re-measured README line count)
    - `grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md` → `120`
  </acceptance_criteria>
  <done>All 9 rows carry a cited verdict, the routing-matrix pointer to ADR-0039 is recorded without pre-empting the checkpoint, and the ledger still holds exactly 120 rows.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Modified by this plan (no new files):
- `.planning/ledgers/milestone-09-12.md` — Verdict cells for 20 IDs

Identifiers a later reader will grep for, finalized here:
- `REQ-mdbook-scaffold`, `REQ-mdbook-chapter-hierarchy`, `REQ-docs-ci-pages-deploy`, `REQ-docs-migration-log`
- `REQ-doc-link-repair-linkcheck`, `REQ-doc-example-compile-gate`, `REQ-getting-started-rewrite`, `REQ-architecture-docs-update`, `REQ-user-guides-rewrite`, `REQ-deployment-operations-docs-update`, `REQ-api-reference-contributing-rewrite`
- `REQ-orchestration-guide`, `REQ-content-processing-guide`, `REQ-agent-orchestrator-bridge-guide`, `REQ-crate-map-feature-flag-reference`
- `REQ-deployment-topologies-section`
- `REQ-mdbook-final-review`, `REQ-doc-version-sync`, `REQ-readme-landing-page`, `REQ-m11-v050-release`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `mdbook build` exit status -> ledger claim | a tool result whose failure has a cause outside this phase crosses into a recorded verdict |
| ledger -> Phase 16 | the 26-open verdict is DOCS-01's entire inherited framing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-10 | Repudiation | the `mdbook build` result | high | mitigate | The exit code and both error sites are recorded verbatim and their files named, so the failure cannot later be read as caused by this phase's three-line documentation edit; the acceptance criteria require `docker.md:118` and `tool-integration.md:324` to appear |
| T-13-01 | Tampering | Verdict cells for 20 IDs | high | mitigate | D-03 evidence bar; a documentation deliverable's exerciser is its `SUMMARY.md` entry, cited by `file:line` |
| T-13-11 | Information disclosure | the 26-open count | medium | mitigate | Recorded as `Verified open` with all fourteen files confirmed present, so Phase 16 inherits "settleable only by content" rather than "files missing" |
| T-13-03 | Repudiation | stale version figures | medium | mitigate | `REQ-doc-version-sync` cites re-run `Cargo.toml` and `git tag` output, not ORCH-05's two-release-stale text (D-18) |
| T-13-12 | Elevation of privilege | pre-empting the plan 13-09 checkpoint | medium | mitigate | `REQ-deployment-topologies-section` records a pointer to ADR-0039 only; the limitation statement itself is written after the blocking human checkpoint |
| T-13-SC | Tampering | package installs | low | accept | The three mdbook tools install from the pre-cached local registry with `--offline` at versions pinned by `.github/workflows/docs.yml`; no new dependency enters any manifest and no `.rs` or `Cargo.toml` changes (D-19) |
</threat_model>

<verification>
- `awk '/^### Milestone 11 Epics 1-2/,/^### Milestone 12 Epic 1/' .planning/ledgers/milestone-09-12.md | grep -c 'pending — plan'` → `0`
- `grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md` → `120`
- `git diff --name-only -- '*.rs' 'docs/*' | wc -l` → `0`
</verification>

<success_criteria>
Milestone 11's 20 rows are derived at the D-00e bar; the 26 open items are carried to Phase 16 with
the evidence that file existence settles nothing; the pre-existing mdbook failure is recorded
honestly.
</success_criteria>

<output>
Create `.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-04-SUMMARY.md` when done.
</output>
