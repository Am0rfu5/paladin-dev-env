---
phase: 13-milestone-9-12-ground-truth-recorded-account
plan: 06
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - .planning/ledgers/milestone-09-12.md
autonomous: true
requirements: [ORCH-01, ORCH-02]
must_haves:
  truths:
    - "All 15 rows in Milestone 12 Epics 5-7 carry a verdict from the eleven-class key with a `file:line` re-read this session; none reads `Verify` and none reads `pending — plan 13-06`"
    - "`REQ-jwt-bearer-auth-v2` records the Milestone 12 documentation half of the JWT-vs-opaque contradiction, cross-references the Milestone 9 mechanism half at `REQ-opaque-bearer-token-adapter-v1`, keeps both as separate rows with separate verdicts (D-00f), and hands the resolution to Phase 14 / WEB-01"
    - "Milestone 12's three open checkboxes are shown to be Epic 5 Task 0.0 feature-branch scaffolding while the Epic 5 work ships as `crates/paladin-web/src/agent_auth.rs` — the row-level evidence behind ORCH-02's vacuous verdict (D-10)"
    - "`REQ-k8s-manifests` records the multi-replica token-verification problem as `Verified open` and names Phase 14 / WEB-02 as owner without proposing a fix or a replica pin"
    - "`REQ-api-v1-versioning` and `REQ-openapi-drift-guard` record the `/v1` prefix confirmed against `crates/paladin-web/openapi.json` and name ADR-0037 as the recorded answer (D-11)"
  artifacts:
    - .planning/ledgers/milestone-09-12.md
  key_links:
    - "`REQ-jwt-bearer-auth-v2` <-> `REQ-opaque-bearer-token-adapter-v1` (variant group 29) -> Phase 14 / WEB-01"
    - "`REQ-k8s-manifests` -> Phase 14 / WEB-02"
    - "`REQ-m12-v060-release` -> ADR-0029's `v0.6.0` trajectory row appended by plan 13-12 (D-16)"
  prohibitions:
    - "No `.rs` file is modified by this plan (D-19)"
    - "No row outside Milestone 12 Epics 5-7 is touched (D-23)"
    - "WEB-01's token mechanism and WEB-02's shared store are recorded, not decided; both are Phase 14's"
    - "ORCH-03 does not grow to a sixth item; the Epic 5, 6 and 7 `project/current-exports.txt` references stay ledger rows (D-09)"
    - "No `.project/Milestone_12-Web-API/Epic_{5,6,7}` file is edited for the `current-exports.txt` defect; Phase 8 already annotated them on 2026-08-06"
    - "No `Shipped` verdict without a `file:line` plus an exerciser (D-03); no bare `Verify` survives (D-02)"
    - "No item from 13-CONTEXT.md's `<deferred>` block is planned or actioned here"
---

<objective>
Derive real, cited verdicts for the 15 requirement IDs in Milestone 12 Epics 5-7 — API security and
authorization, OpenAPI and interactive docs, and deployment artefacts — and record the two Phase 14
inputs that only this range can produce.

Purpose: Epic 5 is where the corpus's only unsettleable variant lives and where Milestone 12's three
"open" checkboxes turn out to be scaffolding. Epic 7 is where the shipped Kubernetes manifests meet
an in-process token store. Both are Phase 14's inheritance and must arrive cited.

Output: 15 Verdict cells replaced in place in `.planning/ledgers/milestone-09-12.md`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-CONTEXT.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive Milestone 12 Epic 5 (6 rows) with the WEB-01 contradiction and the vacuous-checkbox evidence</name>
  <files>.planning/ledgers/milestone-09-12.md</files>
  <read_first>
    - `.planning/ledgers/milestone-09-12.md` — the head notes, the per-milestone checkbox corroboration paragraph plan 13-01 wrote, and the 6 rows this task owns.
    - `crates/paladin-web/src/agent_auth.rs` — the Milestone 12 verifier and the documentation half of the contradiction.
    - `.project/Milestone_12-Web-API/Epic_5/tasks-api-security-authorization.md` — the three "open" Task 0.0 items, ORCH-02's vacuous verdict evidence.
    - `.planning/intel/requirements.md` — acceptance criteria for the three bare-`Verify` IDs in this range.
  </read_first>
  <action>
Replace the Verdict cell of these 6 rows in place, in
`### Milestone 12 Epic 5 — API Security & Authorization (6 IDs)`:
`REQ-api-key-auth`, `REQ-jwt-bearer-auth-v2`, `REQ-fail-closed-auth-posture`,
`REQ-per-agent-role-authorization`, `REQ-admin-gated-registration`, `REQ-secret-hygiene-redaction`.

`REQ-jwt-bearer-auth-v2` is the Milestone 12 half of variant group 29. Record that
`crates/paladin-web/src/agent_auth.rs` documents its verifier as JWT throughout, that
`grep -rn 'jsonwebtoken' Cargo.toml crates/*/Cargo.toml` returns nothing — re-run it and record the
output — and that Epic 5's **Open Question 4** is unanswerable for the shipped adapter because an
opaque store has no signing secret and no algorithm. Verdict class `Contract diverges`.
Cross-reference `REQ-opaque-bearer-token-adapter-v1` (plan 13-02's row) as the mechanism half and
keep the two as separate rows with separate verdicts (D-00f). Name **Phase 14 / WEB-01** as owner.
Do not pick a mechanism.

Record ORCH-02's vacuous-checkbox evidence on this Epic's rows (D-10): the three items
`intel/task-completion-state.md` counts open in `Epic_5/tasks-api-security-authorization.md` are
Task 0.0 scaffolding — create a feature branch, check out
`feature/m12-epic5-api-security-authorization`, confirm a clean baseline — while the Epic 5 work
itself ships. Quote the three item texts from the task file and cite the shipped module. The verdict
is that the count is vacuous, and none of the three becomes a task.

`REQ-fail-closed-auth-posture`, `REQ-per-agent-role-authorization`, `REQ-admin-gated-registration`
and `REQ-secret-hygiene-redaction`: locate the implementing symbol and its test with `grep -rn` over
`crates/paladin-web/src/` and `crates/paladin-web/tests/`, and cite both. A security posture claim
needs a test, not a manifest line — the head note's carve-out does not apply to behavioural
requirements (D-03). Where no exerciser exists, the verdict is `Verified open` with the search
command that returned nothing.

The Milestone 12 Epic 5 requirement text that names the undotted `project/current-exports.txt` gets
the D-09 pointer in its row: `scripts/check-api-surface.sh:6` reads the dotted path, the baseline
file exists, and Phase 8's dated `Corrected (dated 2026-08-06, DEBT-01)` banner already sits in the
source document — confirm the banner with a grep before citing it. Hand the residue to Phase 15. Do
not edit the `.project/` file and do not grow ORCH-03 to a sixth item.
  </action>
  <verify>
    <automated>awk '/^### Milestone 12 Epic 5/,/^### Milestone 12 Epic 6/' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^### Milestone 12 Epic 5/,/^### Milestone 12 Epic 6/' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'` → `6`
    - `awk '/^### Milestone 12 Epic 5/,/^### Milestone 12 Epic 6/' .planning/ledgers/milestone-09-12.md | grep -c 'pending — plan'` → `0`
    - `awk '/^### Milestone 12 Epic 5/,/^### Milestone 12 Epic 6/' .planning/ledgers/milestone-09-12.md | grep -c 'run-5 input (not yet re-derived)'` → `0`
    - `grep -c 'agent_auth.rs' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c 'jsonwebtoken' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c 'feature/m12-epic5-api-security-authorization' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c 'REQ-opaque-bearer-token-adapter-v1' .planning/ledgers/milestone-09-12.md` → `≥2` (its own row plus this cross-reference)
    - `git diff --name-only -- '.project/*' | wc -l` → `0`
  </acceptance_criteria>
  <done>All 6 Epic 5 rows carry a cited verdict, the JWT-vs-opaque contradiction is recorded as `Contract diverges` and handed to Phase 14 / WEB-01 unresolved, and the three vacuous checkboxes are shown to be Task 0.0 scaffolding with their texts quoted.</done>
</task>

<task type="auto">
  <name>Task 2: Derive Milestone 12 Epics 6-7 (9 rows) with the WEB-02 hand-off</name>
  <files>.planning/ledgers/milestone-09-12.md</files>
  <read_first>
    - `.planning/ledgers/milestone-09-12.md` — the rows this task owns and the Epic 5 rows task 1 just wrote.
    - `crates/paladin-web/openapi.json` — the drift-guard baseline and the `/v1` evidence for `REQ-api-v1-versioning` and `REQ-openapi-drift-guard`.
    - `k8s/deployment.yaml` — the shipped manifest with liveness and readiness probes; the WEB-02 subject.
    - `docs/src/deployment-topologies/` — the pages `REQ-deployment-topology-doc-update` covers, including `sidecar.md` and `http-service-host.md`. Read them; do not edit them.
    - `.planning/intel/requirements.md` — acceptance criteria for the bare-`Verify` ID in this range.
  </read_first>
  <action>
Replace the Verdict cell of these 9 rows in place, in
`### Milestone 12 Epic 6 — OpenAPI & Interactive Docs (4 IDs)` and
`### Milestone 12 Epic 7 — Deployment Artefacts, Examples & Docs (5 IDs)`.

Epic 6 — `REQ-openapi-spec-generation`, `REQ-swagger-ui-docs-endpoint`, `REQ-api-v1-versioning`,
`REQ-openapi-drift-guard`.

Epic 7 — `REQ-dockerfile-server-compose`, `REQ-k8s-manifests`,
`REQ-deployment-topology-doc-update`, `REQ-server-e2e-tests`, `REQ-m12-v060-release`.

`REQ-api-v1-versioning` and `REQ-openapi-drift-guard` are the evidence ADR-0037 rests on. Re-run the
grep over `crates/paladin-web/openapi.json` for the six agent paths and record that every one carries
the `/v1` prefix; record that the committed spec is the drift guard and that it locks in whichever
form actually shipped, and name ADR-0037 (plan 13-08) as the recorded answer. Verdict class `Shipped`
with the citation and the drift-guard job as exerciser.

`REQ-k8s-manifests` carries the WEB-02 hand-off. Record that `k8s/deployment.yaml` ships liveness and
readiness probes for multi-process serving against Milestone 9's in-process token store, so under
more than one replica a token issued by one pod will not verify on another; that Milestone 9 Epic 5
§6.1 recorded the trade-off in its own words and no requirement in the corpus covers the shared
store. Verdict class `Verified open`, owner **Phase 14 / WEB-02**. Do not propose a fix and do not
propose a `replicas: 1` pin — that is Phase 14's call.

`REQ-deployment-topology-doc-update`: record which pages shipped under
`docs/src/deployment-topologies/` and note the two defects this phase corrects elsewhere —
`sidecar.md:29`'s unprefixed `POST /agents/{id}/execute` (D-12, plan 13-08) and
`http-service-host.md:54`'s `run (LLM + tools + memory)` line (D-15, plan 13-09, gated on a blocking
human checkpoint). Re-read both lines before citing them. Record the pointers only; the edits belong
to those plans and stating ADR-0039's answer here would pre-empt the checkpoint.

`REQ-m12-v060-release`: record the lockstep bump and tag, and note that plan 13-12 transcribes it as
ADR-0029's `v0.6.0` row (D-16). Do not touch ADR-0029 here.

The Milestone 12 Epic 6 and Epic 7 requirement texts naming the undotted `project/current-exports.txt`
get the same D-09 pointer as Epic 5's: script closed at `scripts/check-api-surface.sh:6`, Phase 8's
dated banner already present, residue handed to Phase 15, no `.project/` edit, no sixth ORCH-03 item.

Apply the D-03 evidence bar and the head note's manifest carve-out to the remaining rows.
  </action>
  <verify>
    <automated>awk '/^### Milestone 12 Epic 6/,/^### Deferred-QA Epic 25/' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^### Milestone 12 Epic 6/,/^### Deferred-QA Epic 25/' .planning/ledgers/milestone-09-12.md | grep -c '^| REQ-'` → `9`
    - `awk '/^### Milestone 12 Epic 6/,/^### Deferred-QA Epic 25/' .planning/ledgers/milestone-09-12.md | grep -c 'pending — plan'` → `0`
    - `awk '/^### Milestone 12 Epic 6/,/^### Deferred-QA Epic 25/' .planning/ledgers/milestone-09-12.md | grep -c 'run-5 input (not yet re-derived)'` → `0`
    - `grep -c 'WEB-02' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c 'k8s/deployment.yaml' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c 'sidecar.md:29' .planning/ledgers/milestone-09-12.md` → `≥1`
    - `grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md` → `120`
    - `git diff --name-only -- '*.rs' 'docs/*' '.project/*' | wc -l` → `0`
  </acceptance_criteria>
  <done>All 9 rows carry a cited verdict, the multi-replica token problem is handed to Phase 14 / WEB-02 as `Verified open`, the two documentation defects are recorded as pointers to plans 13-08 and 13-09, and the ledger still holds exactly 120 rows.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Modified by this plan (no new files):
- `.planning/ledgers/milestone-09-12.md` — Verdict cells for 15 IDs

Identifiers a later reader will grep for, finalized here:
- `REQ-api-key-auth`, `REQ-jwt-bearer-auth-v2`, `REQ-fail-closed-auth-posture`, `REQ-per-agent-role-authorization`, `REQ-admin-gated-registration`, `REQ-secret-hygiene-redaction`
- `REQ-openapi-spec-generation`, `REQ-swagger-ui-docs-endpoint`, `REQ-api-v1-versioning`, `REQ-openapi-drift-guard`
- `REQ-dockerfile-server-compose`, `REQ-k8s-manifests`, `REQ-deployment-topology-doc-update`, `REQ-server-e2e-tests`, `REQ-m12-v060-release`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped auth code vs. its own documentation | the corpus's only variant shipped code cannot settle crosses into a recorded verdict |
| ledger -> Phase 14 | WEB-01's and WEB-02's entire inherited framing leaves this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-15 | Spoofing | the recorded auth mechanism | high | mitigate | The row records both the documented mechanism and the implemented one as `Contract diverges` with the `jsonwebtoken` grep output; picking one here would publish an authentication claim the tree does not support |
| T-13-16 | Information disclosure | the multi-replica token edge | high | mitigate | Recorded as `Verified open` with the `k8s/deployment.yaml` citation and Phase 14 / WEB-02 named, so a deployer reading the ledger learns the constraint rather than discovering it in production |
| T-13-01 | Tampering | Verdict cells for 15 IDs | high | mitigate | D-03 evidence bar; behavioural security requirements need a test, not a manifest line |
| T-13-12 | Elevation of privilege | pre-empting the plan 13-09 checkpoint | high | mitigate | `REQ-deployment-topology-doc-update` records the `http-service-host.md:54` defect as a pointer only; ADR-0039's answer is not stated before ratification |
| T-13-11 | Repudiation | the vacuous-checkbox verdict | medium | mitigate | The three Task 0.0 item texts are quoted from the task file and the shipped module is cited, so the verdict is checkable rather than asserted |
| T-13-SC | Tampering | package installs | low | accept | N/A — no package install, no manifest change (D-19) |
</threat_model>

<verification>
- `awk '/^### Milestone 12 Epic 5/,/^### Deferred-QA Epic 25/' .planning/ledgers/milestone-09-12.md | grep -c 'pending — plan'` → `0`
- `grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md` → `120`
- `git diff --name-only -- '*.rs' | wc -l` → `0`
</verification>

<success_criteria>
Milestone 12 Epics 5-7 are derived at the D-00e bar; WEB-01 and WEB-02 leave this phase as cited
`Contract diverges` and `Verified open` rows with named owners and no fabricated answers.
</success_criteria>

<output>
Create `.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-06-SUMMARY.md` when done.
</output>
