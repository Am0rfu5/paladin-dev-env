---
phase: 13-milestone-9-12-ground-truth-recorded-account
plan: 09
type: execute
wave: 3
depends_on: ["13-01", "13-04", "13-05"]
files_modified:
  - .planning/decisions/0038-agent-provisioner-placement.md
  - .planning/decisions/0039-http-topology-no-garrison-no-arsenal.md
  - docs/src/deployment-topologies/http-service-host.md
  - docs/src/deployment-topologies/overview.md
autonomous: false
requirements: [ORCH-04]
must_haves:
  truths:
    - "A blocking `checkpoint:decision` is resolved by a human before ADR-0038 or ADR-0039 is authored, and its outcome — including how it was obtained — is recorded verbatim in the plan summary (D-14, D-15, D-23)"
    - "`.planning/decisions/0038-agent-provisioner-placement.md` and `.planning/decisions/0039-http-topology-no-garrison-no-arsenal.md` each exist with the seven required H2 headings in order, no frontmatter, and bulleted `## Code Locations` and `## Considered Options` (D-00a, D-20)"
    - "ADR-0038 records that `AgentProvisioner` stays in `paladin-web` because `AgentSpec` is an OpenAPI-annotated HTTP request DTO and promotion would put `utoipa` into `paladin-ports` against ADR-0015(i), not because either placement is clean (D-14)"
    - "ADR-0039 records the absence of Garrison and Arsenal on HTTP-served agents as a permanent property of the shipped topology, states what a consumer needing them should use instead, and declines to schedule the capability (D-15)"
    - "`docs/src/deployment-topologies/http-service-host.md` no longer promises tools or memory in its sequence diagram, and `overview.md` states the limitation where a reader chooses a topology (D-15)"
    - "The two ADRs remain two separately citable files because Phase 14 depends on each by number (D-20)"
    - statement: "If a reader-facing surface for the limitation cannot be placed in `overview.md`'s comparison table without distorting it, the limitation is still stated in a note directly under the table — never omitted from the routing page (ORCH-04 empty edge)"
      verification: backstop
    - "The corrected `http-service-host.md` capability wording and `overview.md`'s limitation statement describe the same capability set in the same terms, so the two adjacent pages do not disagree (ORCH-04 adjacency edge)"
    - "`embedded-library.md`'s existing Garrison/Arsenal advertisement is left untouched and is cross-referenced rather than duplicated, so the corpus states the capability difference once per page role (ORCH-04 duplicates edge)"
    - "Both edited pages and both ADRs name the subsystems in the project's medieval-military ubiquitous language — Garrison for memory and Arsenal for tools — matching `embedded-library.md`'s existing wording rather than substituting generic terms (D-00h)"
  artifacts:
    - .planning/decisions/0038-agent-provisioner-placement.md
    - .planning/decisions/0039-http-topology-no-garrison-no-arsenal.md
  key_links:
    - "ADR-0038 -> `crates/paladin-web/src/agent_registry.rs`, `crates/paladin-ports/Cargo.toml`, ADR-0015 §Decision (i), ADR-0031"
    - "ADR-0039 -> `docs/src/deployment-topologies/overview.md` (M11 Epic 6 FR-8's single source of routing) and Phase 14 / WEB-04"
  prohibitions:
    - "No `.rs` file is modified by this plan; the `AgentProvisioner` trait is not moved (D-14, D-19)"
    - "No ADR is authored before the blocking checkpoint is resolved by a human (D-23)"
    - "No documentation file other than `docs/src/deployment-topologies/http-service-host.md` and `overview.md` is edited by this plan (D-19)"
    - "Garrison or Arsenal wiring for HTTP-served agents is not built, scheduled or given a target; D-15 declined it and it is a milestone, not a clause"
    - "`AgentSpec` is not split, `ProvisionError`/`ProvisionedAgent` are not moved, and no deprecated re-export is added; that is Phase 14's work if a human overturns D-14"
    - "No ADR number other than 0038 and 0039 is taken; `PROMOTION.md`'s next-free line is advanced once, by plan 13-13 (D-20)"
    - "No item from 13-CONTEXT.md's `<deferred>` block is planned or actioned here"
---

<objective>
Turn Milestone 12's two remaining defaults into decisions with reasoning — where `AgentProvisioner`
lives, and whether Garrison and Arsenal for HTTP-served agents are planned scope or a permanent
property of the topology — and make the routing page a reader is sent to tell the truth about what
that topology can do.

Purpose: both seams are load-bearing. The first determines whether the queue/worker and sidecar
topologies can reuse the trait or must duplicate it. The second decides whether a reader routed to
the HTTP host by the decision matrix is told what they are giving up. Both were auto-resolved and
neither has been ratified, which is why this plan opens with a blocking checkpoint.

Output: `.planning/decisions/0038-agent-provisioner-placement.md`,
`.planning/decisions/0039-http-topology-no-garrison-no-arsenal.md`, and two corrected pages under
`docs/src/deployment-topologies/`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-CONTEXT.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-RESEARCH.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Ratify D-14 and D-15, and dispose PROMOTION.md Part B candidate 9</name>
  <decision>
Three dispositions are needed before any ADR in this plan is written:
(1) **D-14** — `AgentProvisioner` stays in `crates/paladin-web`;
(2) **D-15** — the absence of Garrison and Arsenal on HTTP-served agents is recorded as a permanent
property of the shipped topology and published in the routing matrix, with the capability not
scheduled;
(3) **PROMOTION.md Part B candidate 9** — the Milestone 9 Epic 4 agent/orchestrator bridge decision,
which `PROMOTION.md`'s own inventory assigns to Phase 13 but which D-20's three-ADR allocation does
not cover.
  </decision>
  <context>
D-14 and D-15 were auto-resolved under `--auto` and are both flagged `⚠ HUMAN REVIEW` in
13-CONTEXT.md, both rated reversibility **costly**. No human has confirmed either.

D-14 retires a "promote when a second consumer appears" default by denying its premise. The evidence:
the trait signature is `provision(&self, spec: &AgentSpec) -> Result<ProvisionedAgent, ProvisionError>`
at `crates/paladin-web/src/agent_registry.rs:103-110`; `AgentSpec` at `:55-56` derives
`utoipa::ToSchema` and is doc-commented "Sent in the body of `POST /agents`"; `paladin-ports` carries
no `utoipa`; ADR-0015 §Decision (i) bars web-framework dependencies from `paladin-core` and
`paladin-ports`. The apparent second consumer, `FacadeProvisioner` at
`src/infrastructure/web/facade_provisioner.rs:70`, is `#[cfg(feature = "web-server")]`-gated — the
HTTP composition root, not a new topology. Reversing later means splitting `AgentSpec` into a domain
spec and an HTTP DTO with a `From` conversion, moving `ProvisionError` and `ProvisionedAgent`, and
deprecating the `crates/paladin-web/src/lib.rs:46` re-export, across two published crates.

D-15 writes a capability limitation into published user documentation and declines to schedule the
capability. `docs/src/deployment-topologies/http-service-host.md:54` currently advertises the
opposite (`Service->>Agent: run (LLM + tools + memory)`), and `overview.md` — which M11 Epic 6 FR-8
makes "the single source of routing" — says nothing either way. Reversing means retracting a
published capability statement and re-opening a seam two Epics closed.

Candidate 9 is a gap between two independently-written documents, found by 13-RESEARCH.md. No ORCH
requirement's `Derives` list reaches that PRD section, so there is no requirement-level mandate to
promote it — but leaving its disposition silent is what the corpus's own advancing-note convention
exists to prevent.
  </context>
  <options>
    <option id="option-a">
      <name>Ratify D-14 and D-15 as written; record candidate 9 as not promoted this phase with a named future owner</name>
      <pros>Matches the locked decisions and the evidence; keeps the phase's edit surface at three documentation lines and zero `.rs`; candidate 9's disposition is stated rather than silent</pros>
      <cons>Two costly decisions are ratified on documentary evidence rather than on a design review; candidate 9's "Owner phase: Phase 13" assignment is redirected rather than honoured</cons>
    </option>
    <option id="option-b">
      <name>Overturn D-14 — promote `AgentProvisioner` to `paladin-ports`</name>
      <pros>Makes the trait reusable by a non-HTTP topology without depending on the HTTP adapter crate</pros>
      <cons>Requires splitting `AgentSpec`, moving two more types and deprecating a public re-export across two published crates — `.rs` work outside this phase's D-19 boundary; the work moves to Phase 14 and this plan writes ADR-0038 recording the reversal instead</cons>
    </option>
    <option id="option-c">
      <name>Overturn D-15 — make Garrison and Arsenal for HTTP-served agents planned scope with a target</name>
      <pros>Closes the capability gap rather than publishing it</pros>
      <cons>`AgentSpec` has no fields for memory or tools; expressing an MCP server, its credentials and its lifetime in a JSON request body is genuine API design that no milestone has scheduled, so this commits an unplanned milestone; `http-service-host.md:54` still needs correcting in the interim</cons>
    </option>
    <option id="option-d">
      <name>Promote PROMOTION.md Part B candidate 9 as a fourth ADR (0040) this phase</name>
      <pros>Honours `PROMOTION.md`'s own "Owner phase: Phase 13" assignment and closes the inventory entry</pros>
      <cons>Widens D-20's locked three-ADR allocation; no ORCH requirement's `Derives` list reaches the PRD section, so the promotion has no requirement behind it</cons>
    </option>
  </options>
  <resume-signal>
Reply with a disposition for **all three** items — for example `option-a` to take the recommended
package, or a combination such as `option-a with option-d`. A reply that leaves any of D-14, D-15 or
candidate 9 undisposed is not a resume signal. Record the selected option, the date, and **how the
decision was obtained** (which mechanism produced the human's answer) in `13-09-SUMMARY.md` under a
`Checkpoint Status` heading, following the provenance convention Phase 12's plan 12-01 established
(D-00i).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author ADR-0038 and ADR-0039</name>
  <files>.planning/decisions/0038-agent-provisioner-placement.md, .planning/decisions/0039-http-topology-no-garrison-no-arsenal.md</files>
  <read_first>
    - The task 1 checkpoint outcome as recorded in this plan's working notes — if a human overturned D-14 or D-15, the ADR records the reversal and its consequence, not the recommended position.
    - `.planning/decisions/PROMOTION.md` — re-read the `Next free ADR number` line, the file-shape rules at `:119-140`, and the promotion procedure at `:161-182`.
    - `.planning/decisions/0036-audit-suppression-single-source-topology.md` — the shape to match.
    - `.planning/decisions/0015-core-ports-dependency-allowlist.md` §Decision (i) — the invariant ADR-0038 turns on. Read it; do not paraphrase it from CONTEXT.md.
    - `.planning/decisions/0031-extracted-crate-dependency-rule.md` — the same principle applied to a manifest edge.
    - `crates/paladin-web/src/agent_registry.rs` — re-read `AgentSpec` and the `AgentProvisioner` trait, and re-confirm the line numbers before citing them.
    - `crates/paladin-ports/Cargo.toml` — confirm `utoipa` is absent and list what is present.
    - `docs/src/deployment-topologies/queue-worker.md` — the worker running the agent through `PaladinExecutionService` and being itself an embedded agent host; re-grep for `provision` and `AgentSpec` and record that it returns nothing.
    - `docs/src/deployment-topologies/embedded-library.md` — the only page under `deployment-topologies/` that advertises Garrison and Arsenal; ADR-0039 routes there.
  </read_first>
  <action>
Author two separate files — two questions, two ADRs, both separately citable because Phase 14 depends
on each by number (D-20).

**`.planning/decisions/0038-agent-provisioner-placement.md`** — seven H2 headings in order, no
frontmatter.
`## Status` — `Accepted`, blank line, `**Date:**` today.
`## Context` — Epic 1 Open Question 2 recorded the placement as a default with the escape clause
"promote only if a second consumer appears", and §7 justified calling it a coin-flip on the grounds
that the trait references only `Paladin` and `PaladinExecutorPort`. State that §7 omits `AgentSpec`,
the type that decides the question, and give the re-read evidence: the trait signature, `AgentSpec`'s
`utoipa::ToSchema` derive and its `#[schema(value_type = Vec<String>)]` field, its doc comment naming
it a request body, `paladin-ports`' dependency list without `utoipa`, and ADR-0015 §Decision (i).
State that the escape clause would have fired on a false signal: `FacadeProvisioner` is
`#[cfg(feature = "web-server")]`-gated, so the "second consumer" is the HTTP composition root, and the
shipped topology pages describe no spec-driven provisioning at all.
`## Decision` — `AgentProvisioner` stays in `crates/paladin-web`, because its parameter type is an
OpenAPI-annotated HTTP request DTO and promotion would move that schema into the core-tier ports
crate. The correct shape for a second topology is its own spec type over the same `PaladinBuilder`
path.
`## Considered Options` — bulleted, one physical line each: keep in `paladin-web` (chosen); promote
to `paladin-ports` for reuse (rejected — drags `utoipa` into a crate ADR-0015(i) forbids it in, and
requires splitting `AgentSpec`); leave it as a default (rejected — ORCH-04's done-when requires a
recorded placement with reasoning).
`## Code Locations` — bulleted, one physical line each, every citation re-read this session, plus the
source `.project/` document path per `PROMOTION.md` step 4.
`## Code Conformance` — `conforms`, with the re-run command showing the trait is where the decision
says it is. This ADR changes no file; it ratifies where the trait already lives.
`## Downstream Consumers` — bulleted, each leading with a bold `Phase N / REQ-ID`: **Phase 14 /
WEB-03, WEB-04** already opens `crates/paladin-ports/src/output/llm_port.rs` and
`crates/paladin-web/src/agent_auth.rs`, and is the phase that would execute a move if this is
overturned. State the cost of the chosen placement explicitly: a future non-HTTP consumer needing
spec-driven provisioning either takes `paladin-web` and `axum`, or writes its own trait — and the
second is correct — so a later reader does not re-derive the question.

**`.planning/decisions/0039-http-topology-no-garrison-no-arsenal.md`** — same seven headings.
`## Context` — Milestone 12 Epic 2's non-goal and Epic 3's restatement exclude Garrison and Arsenal
wiring for HTTP-served agents. State that the situation is worse than an omission:
`http-service-host.md:54`'s sequence diagram advertises tools and memory in the same words
`embedded-library.md` uses for the topology that actually has them, and `overview.md` — M11 Epic 6
FR-8's single source of routing — says nothing either way, so a routed reader is actively
misinformed. Re-grep for Garrison and Arsenal across `docs/src/deployment-topologies/` and record
that only `embedded-library.md` matches.
`## Decision` — the absence is a permanent property of the shipped topology, stated in the decision
matrix; a consumer needing Garrison or Arsenal uses the embedded-library topology, and each
queue/worker is itself an embedded agent host, so the routing story is coherent without new work.
`## Considered Options` — bulleted: property of the topology, stated in the matrix (chosen); planned
scope with a target (rejected — `AgentSpec` has no fields for memory or tools and expressing an MCP
server, its credentials and its lifetime in a JSON request body is genuine API design no milestone
has scheduled); leave it in the Epic 2 non-goal (rejected — one line in a non-goal is not enough
surface, and the routing page contradicts it).
`## Code Locations` — bulleted, re-read citations for `AgentSpec`'s field set, the two documentation
sites, `embedded-library.md`'s correct advertisement, `queue-worker.md`'s embedded-host statement, and
the source `.project/` non-goal documents.
`## Code Conformance` — `must change`, naming plan 13-09 task 3 as the executor of the
`http-service-host.md` and `overview.md` edits, with the before and after strings quoted.
`## Downstream Consumers` — **Phase 14 / WEB-04**, whose own text says Arsenal/MCP and LLM tool
calling "would need a stated relationship"; this ADR is half of it.

Do not add a `## Supersedes` line to either file — neither corrects an earlier answer.
  </action>
  <verify>
    <automated>ls .planning/decisions/0038-*.md .planning/decisions/0039-*.md && grep -c '^## ' .planning/decisions/0038-*.md .planning/decisions/0039-*.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/decisions/0038-*.md` → exactly one file; `ls .planning/decisions/0039-*.md` → exactly one file
    - `grep -c '^## ' .planning/decisions/0038-*.md` → `7`; `grep -c '^## ' .planning/decisions/0039-*.md` → `7`
    - `head -1 .planning/decisions/0038-*.md | grep -c '^---'` → `0`; same for `0039`
    - `awk '/^## Code Locations/,/^## Code Conformance/' .planning/decisions/0038-*.md | grep -cv '^- \|^## \|^$'` → `0` (bulleted, no prose paragraphs); same for `0039`
    - `awk '/^## Code Conformance/,/^## Downstream Consumers/' .planning/decisions/0038-*.md | grep -c 'conforms'` → `1`
    - `awk '/^## Code Conformance/,/^## Downstream Consumers/' .planning/decisions/0039-*.md | grep -c 'must change'` → `1`
    - `grep -c 'Phase 14' .planning/decisions/0038-*.md` → `≥1`; `grep -c 'WEB-04' .planning/decisions/0039-*.md` → `≥1`
    - `grep -c 'utoipa' .planning/decisions/0038-*.md` → `≥2` and `grep -c 'ADR-0015' .planning/decisions/0038-*.md` → `≥1`
    - `grep -c '^## Supersedes' .planning/decisions/0038-*.md .planning/decisions/0039-*.md` → `0` for both
    - `ls .planning/decisions/0040-*.md 2>/dev/null | wc -l` → `0` unless the checkpoint selected `option-d`
  </acceptance_criteria>
  <done>Both ADRs exist as separately citable files in the corpus's exact shape, ADR-0038 carries `conforms` and ADR-0039 carries `must change` naming its executing task, and both record the checkpoint's ratified position rather than the pre-checkpoint recommendation.</done>
</task>

<task type="auto">
  <name>Task 3: Correct http-service-host.md and state the limitation in overview.md</name>
  <files>docs/src/deployment-topologies/http-service-host.md, docs/src/deployment-topologies/overview.md</files>
  <read_first>
    - `docs/src/deployment-topologies/http-service-host.md` — read the mermaid sequence block and locate the `Service->>Agent: run (...)` line before editing; 13-RESEARCH.md read it at line 54 on 2026-08-10, but re-read rather than trusting the line number.
    - `docs/src/deployment-topologies/overview.md` — read the five-topology comparison table, its `Use when` and `Avoid when` columns, and the routing flowchart, so the limitation lands where a reader chooses.
    - `docs/src/deployment-topologies/embedded-library.md` — the correct Garrison/Arsenal advertisement; the wording the two edits must be consistent with, and the page `overview.md` routes a Garrison/Arsenal-needing reader to.
    - `.planning/decisions/0039-http-topology-no-garrison-no-arsenal.md` — the ADR task 2 just wrote, whose `## Code Conformance` names this edit.
    - `docs/book.toml` — `[output.linkcheck] warning-policy = "error"`; a cross-link added to `overview.md` must resolve.
  </read_first>
  <action>
In `docs/src/deployment-topologies/http-service-host.md`, correct the sequence-diagram line that
currently promises `run (LLM + tools + memory)` so it stops advertising Arsenal tools and Garrison
memory on a topology that has neither, keeping the diagram's flow intact — for example
`run (LLM + prompt)`. Then state the limitation in the page's prose so a reader who skips the diagram
still learns it, and point them at the embedded-library topology.

In `docs/src/deployment-topologies/overview.md`, surface the limitation where a reader chooses a
topology. Placement is your discretion — the HTTP-service-host row's `Avoid when` cell, a note
directly under the comparison table, or both — provided a reader choosing a topology cannot miss it.
If the table cell cannot carry it without distorting the table, put it in a note under the table
rather than omitting it. Use the same capability terms in both files and in ADR-0039 so the two
adjacent pages do not describe the same limitation two ways.

Name the subsystems in the project's medieval-military ubiquitous language throughout — Garrison for
memory, Arsenal for tools and MCP — matching the wording `embedded-library.md` already uses, so a
reader comparing the two pages sees one vocabulary rather than two (D-00h, CLAUDE.md's standing
convention).

Do not edit `embedded-library.md`. Its existing advertisement is correct for its own topology;
cross-reference it rather than restating the capability set a third time.

Do not touch `sidecar.md` — that is plan 13-08's file.

Run `mdbook build docs/` and compare against the pre-phase baseline recorded in 13-RESEARCH.md: exit
`101`, a broken link at `deployment/docker.md:118` and an incomplete reference link at
`user-guides/tool-integration.md:324`, both pre-existing, both in files this phase does not touch. The
expected outcome is both errors persisting unchanged and no new error. Record the comparison in the
summary; do not attribute the pre-existing failure to these edits and do not reinterpret it as a
pass. If a cross-link added to `overview.md` produces a **new** linkcheck error, fix the link — that
one is this plan's. If `mdbook` is unavailable, install it offline at `.github/workflows/docs.yml`'s
pinned versions; if that fails, record the failure verbatim and scope the claim CI-only (D-21).
  </action>
  <verify>
    <automated>grep -cE 'tools.*memory|memory.*tools' docs/src/deployment-topologies/http-service-host.md; grep -icE 'garrison|arsenal' docs/src/deployment-topologies/overview.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'tools \+ memory|memory \+ tools' docs/src/deployment-topologies/http-service-host.md` → `0`
    - `grep -icE 'garrison|arsenal' docs/src/deployment-topologies/http-service-host.md` → `≥1` (the limitation is stated in prose, not merely removed from the diagram)
    - `grep -icE 'garrison|arsenal' docs/src/deployment-topologies/overview.md` → `≥1`
    - `grep -c 'embedded-library.md' docs/src/deployment-topologies/overview.md` → `≥1` (the reader is routed somewhere)
    - `git diff --name-only -- 'docs/*' | sort` → exactly `docs/src/deployment-topologies/http-service-host.md` and `docs/src/deployment-topologies/overview.md`
    - `git diff --name-only -- 'docs/src/deployment-topologies/embedded-library.md' | wc -l` → `0`
    - `mdbook build docs/ ; echo "exit=$?"` → `exit=101` with exactly the two pre-existing errors and no error naming `http-service-host.md` or `overview.md`
    - `git diff --name-only -- '*.rs' | wc -l` → `0`
  </acceptance_criteria>
  <done>The HTTP topology page no longer promises tools and memory and states the limitation in prose, the routing page states it where a reader chooses and links to the embedded topology, `embedded-library.md` is untouched, and the mdbook result matches the pre-phase baseline.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Created by this plan:
- `.planning/decisions/0038-agent-provisioner-placement.md`
- `.planning/decisions/0039-http-topology-no-garrison-no-arsenal.md`

Modified by this plan:
- `docs/src/deployment-topologies/http-service-host.md` (sequence-diagram capability line plus a prose limitation statement)
- `docs/src/deployment-topologies/overview.md` (the limitation in the routing matrix)

New identifiers a later reader will grep for:
- `ADR-0038`, `0038-agent-provisioner-placement.md`
- `ADR-0039`, `0039-http-topology-no-garrison-no-arsenal.md`
- The `Checkpoint Status` heading in `13-09-SUMMARY.md` recording the human disposition of D-14, D-15 and PROMOTION.md Part B candidate 9
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unratified `--auto` decision -> Accepted ADR | a position no human confirmed would enter the top of the precedence order |
| ADR / docs -> framework consumers | a published capability statement is what a reader chooses a deployment topology on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-24 | Elevation of privilege | D-14 and D-15 entering ADRs unratified | high | mitigate | Task 1 is a blocking `checkpoint:decision` that must resolve before task 2 runs; the selected option, its date and how it was obtained are recorded in `13-09-SUMMARY.md` under `Checkpoint Status` (D-00i, D-23) |
| T-13-25 | Information disclosure | publishing a capability limitation to user documentation | high | mitigate | The same checkpoint gates it; the limitation lands on the routing page M11 Epic 6 FR-8 designates the single source of routing, with a named alternative topology, so a reader is redirected rather than merely warned |
| T-13-26 | Spoofing | `http-service-host.md`'s diagram advertising Arsenal and Garrison | high | mitigate | The acceptance criteria assert the `tools + memory` claim is gone and that the page states the limitation in prose, so removing the words alone does not pass |
| T-13-02 | Tampering | the `agent_registry.rs` and `Cargo.toml` citations in ADR-0038 | high | mitigate | `read_first` requires re-reading `agent_registry.rs`, `paladin-ports/Cargo.toml` and ADR-0015 §(i) rather than paraphrasing CONTEXT.md |
| T-13-27 | Repudiation | candidate 9's disposition left silent | medium | mitigate | The resume signal is invalid unless all three items are disposed; plan 13-13's advancing note records the outcome |
| T-13-10 | Repudiation | the `mdbook build` result after two edits | medium | mitigate | Compared against the recorded pre-phase baseline; a new error naming either edited file fails the criteria |
| T-13-28 | Tampering | scope creep into `.rs` | high | mitigate | `git diff --name-only -- '*.rs' | wc -l` → `0`; the trait is not moved, `AgentSpec` is not split, no re-export is deprecated (D-19) |
| T-13-SC | Tampering | package installs | low | accept | Only the pre-cached, `docs.yml`-pinned mdbook toolchain may be installed with `--offline`; no manifest is modified (D-19) |
</threat_model>

<verification>
- `ls .planning/decisions/0038-*.md .planning/decisions/0039-*.md` → two files, 7 H2 headings each, no frontmatter
- `grep -cE 'tools \+ memory' docs/src/deployment-topologies/http-service-host.md` → `0`
- `grep -icE 'garrison|arsenal' docs/src/deployment-topologies/overview.md` → `≥1`
- `git diff --name-only -- '*.rs' | wc -l` → `0`
- `13-09-SUMMARY.md` contains a `Checkpoint Status` section recording the selected option, its date and how the decision was obtained
</verification>

<success_criteria>
Both Milestone 12 seams are decisions with reasoning, ratified by a human before being written down;
the routing matrix tells a reader what the HTTP topology cannot do; no `.rs` file moved.
</success_criteria>

<output>
Create `.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-09-SUMMARY.md` when done,
including a `Checkpoint Status` section.
</output>
