---
phase: 13-milestone-9-12-ground-truth-recorded-account
plan: 10
type: execute
wave: 3
depends_on: ["13-01", "13-02", "13-03", "13-04", "13-05", "13-06", "13-07"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [ORCH-01, ORCH-05]
must_haves:
  truths:
    - "ORCH-01's `sixteen entries already carry settled-by pointers … and the remaining 104 need the same treatment` sentence carries a dated correction recording that the sixteen are variant-register entries, that zero ledger rows carry `settled-by`, and that the measured split is 35 bare `Verify` / 53 bare `Shipped` / 32 already-rich with all 120 needing a verdict — with the original text retained (D-04, D-00d)"
    - "ORCH-05's `root Cargo.toml at version 0.6.0, branch release/v0.7.0, latest tag v0.5.1` clause carries a dated correction to the measured `0.7.0` / `v0.7.1` state, with the original retained and the historical M9-M12 lockstep gates left untouched (D-18)"
    - "PIPE-01's verbatim 14-job `ci.yml` list carries a dated correction to the measured 15-job list, so Phase 15 does not plan against a pipeline that no longer exists (D-08)"
    - "`.planning/REQUIREMENTS.md`'s `## Milestone 9-12 as-shipped ledger` section is reduced to a pointer at `.planning/ledgers/milestone-09-12.md` and holds zero `| REQ-` rows, so the ledger and REQUIREMENTS.md are not two diverging copies of the same 120 rows (D-01, ORCH-01 adjacency edge)"
    - "The pointer section names the ledger by path and states that the ledger is authoritative, matching how `REQUIREMENTS.md` already points at the four sibling ledgers"
  artifacts: []
  key_links:
    - "`REQUIREMENTS.md` §Milestone 9-12 as-shipped ledger -> `.planning/ledgers/milestone-09-12.md`"
    - "PIPE-01's corrected job list -> Phase 15 / PIPE-01"
  prohibitions:
    - "No `.rs` file is modified by this plan (D-19)"
    - "No superseded sentence is deleted; every correction is dated and retains the original text (D-00c, D-00d)"
    - "The historical facts ORCH-05 records — the four lockstep gates M9 to v0.3.0, M10 to v0.4.0, M11 to v0.5.0, M12 to v0.6.0 — are unchanged; only the current-state clause is corrected (D-18)"
    - "The 120 ledger rows are not deleted without the ledger already holding them; the pointer replaces content that has been transcribed and derived, never content that exists only here"
    - "No requirement checkbox, traceability row or hand-off block is touched by this plan; those are plan 13-13's (D-22)"
    - "`.planning/intel/` is not edited by this plan; those corrections are plan 13-11's"
    - "No item from 13-CONTEXT.md's `<deferred>` block is planned or actioned here"
---

<objective>
Correct at source the three places `REQUIREMENTS.md` states something about itself that measurement
contradicts, and reduce the 120-row draft ledger to a pointer at the file that now holds it.

Purpose: this corpus's characteristic defect is a document that lies about itself and is then quoted
verbatim by a later requirement. ORCH-01's arithmetic, ORCH-05's version figures and PIPE-01's job
list are three live instances, and two of them are already quoted downstream.

Output: four dated corrections in `.planning/REQUIREMENTS.md`, the last of which is the ledger
pointer.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-CONTEXT.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct ORCH-01's arithmetic and ORCH-05's current-state figures</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md:2204-2315` — ORCH-01 through ORCH-05 in full. Locate ORCH-01's `settled-by` sentence and ORCH-05's current-state clause by reading, not by trusting the `:2210-2212` and `:2300-2301` anchors from another document.
    - `.planning/ledgers/milestone-09-12.md` — the corrected-arithmetic head note plan 13-01 wrote; the correction here must state the same 35/53/32 figures with the same counting commands.
    - `.planning/intel/SYNTHESIS.md:330-340` and `:546` — the `settled-by` mechanism and the sixteen variant-register entries that carry it.
    - `Cargo.toml` — line 34's `version` value, re-read.
    - Any existing dated correction banner elsewhere in `REQUIREMENTS.md` — copy its shape so the corpus has one annotation style.
  </read_first>
  <action>
Apply two dated in-place corrections, each retaining the original text and marking it superseded
(D-00c, D-00d).

**Correction 1 — ORCH-01's arithmetic (D-04).** The sentence claiming sixteen entries already carry
`settled-by` pointers and that the remaining 104 need the same treatment counts two different
populations. Record that the sixteen are **variant-register** entries — `intel/SYNTHESIS.md:335`
defines the mechanism as applying where the shipped tree settles a *variant*, and `:546` counts them
under the variants section — and that no ledger row carries one. Record the counting commands and
their outputs: `grep -c "settled-by" .planning/REQUIREMENTS.md` and a `settled-by` grep restricted to
the ledger's own line range returning zero. Record the measured split — **35** bare `Verify`, **53**
bare `Shipped`, **32** already carrying a richer verdict — and that **all 120 rows need a verdict**.
Note that this is the same class of error as Phase 10's D-05, sitting inside the requirement that
exists to retire it, and that a planner budgeting 104 rows would be wrong in both directions at once.

**Correction 2 — ORCH-05's current-state figures (D-18).** The clause stating that the chain
terminates at root `Cargo.toml` `version = "0.6.0"`, branch `release/v0.7.0`, latest tag `v0.5.1` is
two releases stale. Re-run `grep -n '^version' Cargo.toml`, `git tag --sort=-v:refname | head -8` and
`git branch --show-current`, and record their outputs: `0.7.0`, the newest tags, and the branch,
which is the one accurate clause of the three. State that
`intel/code-verification.md:469` carries the same stale figure, correct as of the 2026-07-30 ingest
and superseded since, and that plan 13-11 corrects it there. State that this is the identical defect
Phase 10's D-11 corrected once in HARD-03, regrown one requirement later.

**Leave the historical facts alone.** The four lockstep gates M9 to `v0.3.0`, M10 to `v0.4.0`, M11 to
`v0.5.0` and M12 to `v0.6.0` are unchanged and are exactly what plan 13-12 transcribes into ADR-0029.
Only the current-state clause is corrected.

Do not touch ORCH-01..ORCH-05's checkboxes — plan 13-13 owns those.
  </action>
  <verify>
    <automated>grep -c '35' .planning/REQUIREMENTS.md && grep -n 'version = "0.7.0"' .planning/REQUIREMENTS.md | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^- \[ \] \*\*ORCH-01\*\*/,/^- \[ \] \*\*ORCH-02\*\*/' .planning/REQUIREMENTS.md | grep -c 'variant-register\|variant register'` → `≥1`
    - `awk '/^- \[ \] \*\*ORCH-01\*\*/,/^- \[ \] \*\*ORCH-02\*\*/' .planning/REQUIREMENTS.md | grep -c '104'` → `≥1` (the superseded figure is retained, not deleted)
    - `awk '/^- \[ \] \*\*ORCH-01\*\*/,/^- \[ \] \*\*ORCH-02\*\*/' .planning/REQUIREMENTS.md | grep -cE '\b35\b'` → `≥1`
    - `awk '/\*\*ORCH-05\*\*/,/^### API contract truthfulness/' .planning/REQUIREMENTS.md | grep -c '0\.7\.0'` → `≥1`
    - `awk '/\*\*ORCH-05\*\*/,/^### API contract truthfulness/' .planning/REQUIREMENTS.md | grep -c 'v0\.5\.1'` → `≥1` (the superseded figure is retained)
    - `awk '/\*\*ORCH-05\*\*/,/^### API contract truthfulness/' .planning/REQUIREMENTS.md | grep -cE 'v0\.3\.0|v0\.4\.0|v0\.6\.0'` → `≥1` (the historical gates are untouched)
    - `git diff -- .planning/REQUIREMENTS.md | grep -c '^-[^-]'` → `0` (annotation only; no original line removed by this task)
  </acceptance_criteria>
  <done>ORCH-01's arithmetic and ORCH-05's current-state figures each carry a dated correction with the measuring commands recorded and the original text retained, and the historical lockstep gates are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Correct PIPE-01's ci.yml job list at source</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — locate PIPE-01's verbatim quotation of the 14-job list by grepping for `benchmark-regression-signal` or `crate-isolation`; 13-CONTEXT.md read it at `:2434-2436`, but re-read rather than trusting the anchor.
    - `.github/workflows/ci.yml` — re-run `grep -nE '^  [a-z][a-z0-9-]*:$' .github/workflows/ci.yml` and use its live output, not the list transcribed in CONTEXT.md.
    - `.planning/ledgers/milestone-09-12.md` — the measured job list in the head note plan 13-01 wrote; this correction must match it exactly.
  </read_first>
  <action>
Apply a dated in-place correction to PIPE-01's quoted `ci.yml` job list, retaining the original
(D-00c, D-00d).

Record that `intel/code-verification.md:539-540`'s 14-job list, which PIPE-01 quotes verbatim, is
stale: `security` was deleted by Phase 9 and two jobs the run-5 list does not name are present.
Record the live measured list with each job's line number from the grep run in this session, and the
grep command itself. State that the measured count is 15 and note that the two additions are recorded
as present without attributing them to a commit unless `git log` is consulted — record what is there,
not a guess about how it got there (D-08).

State the hand-off explicitly: **Phase 15 / PIPE-01** inherits the measured list so it does not plan
against a 14-job pipeline that no longer exists. Also record here, as part of the same hand-off, that
`scripts/check-api-surface.sh:6` reads the dotted `.project/current-exports.txt` and that the file
exists, so run-5 finding 8's "the `api-surface` CI job fails on every run" consequence clause is
closed in the script and open only in four Milestone 12 requirement texts already carrying Phase 8's
dated banners (D-09). Re-run both checks and record their outputs.

Do not grow ORCH-03 to a sixth item and do not edit any `.project/` file here.
  </action>
  <verify>
    <automated>grep -c 'kubernetes-smoke' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'kubernetes-smoke' .planning/REQUIREMENTS.md` → `≥1`
    - `grep -c 'benchmark-regression-signal' .planning/REQUIREMENTS.md` → `≥2` (the retained original list plus the corrected one)
    - `awk '/\*\*PIPE-01\*\*/,/\*\*PIPE-02\*\*/' .planning/REQUIREMENTS.md | grep -cE '\b15\b'` → `≥1`
    - `grep -c 'check-api-surface.sh:6' .planning/REQUIREMENTS.md` → `≥1`
    - `diff <(grep -oE '^  [a-z][a-z0-9-]*:' .github/workflows/ci.yml | tr -d ' :' | sort) <(awk '/\*\*PIPE-01\*\*/,/\*\*PIPE-02\*\*/' .planning/REQUIREMENTS.md | grep -oE '`[a-z][a-z0-9-]*`' | tr -d '`' | sort -u | grep -xFf <(grep -oE '^  [a-z][a-z0-9-]*:' .github/workflows/ci.yml | tr -d ' :'))` → no output (every measured job id appears in the corrected list)
    - `git diff --name-only -- '.project/*' '*.rs' | wc -l` → `0`
  </acceptance_criteria>
  <done>PIPE-01's job list carries a dated correction naming all 15 measured jobs with their line numbers and the grep that produced them, the original 14-job quotation is retained, and the D-09 residue is handed to Phase 15 in the same block.</done>
</task>

<task type="auto">
  <name>Task 3: Reduce the Milestone 9-12 as-shipped ledger section to a pointer</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — locate `## Milestone 9-12 as-shipped ledger` and its full extent by reading; 13-CONTEXT.md records it as `:3607-3931`, but re-read to find the current start and end after tasks 1 and 2 have shifted line numbers.
    - `.planning/REQUIREMENTS.md` — the three existing pointer sections for `milestone-01.md`, `milestone-02-03.md`, `milestone-04-06.md` and `milestone-07-08.md`; grep for `.planning/ledgers/` and copy the established pointer shape.
    - `.planning/ledgers/milestone-09-12.md` — confirm it holds all 120 rows with zero `pending — plan` markers and zero `run-5 input (not yet re-derived)` prefixes **before** removing anything from `REQUIREMENTS.md`. If either count is non-zero, stop and report: a fan-out plan has not finished and the content exists only here.
  </read_first>
  <action>
Replace the body of `## Milestone 9-12 as-shipped ledger` with a pointer, exactly as Phase 7's D-26
did for Milestone 4-6 and Phase 10's D-01 did for Milestone 7-8.

Preconditions, asserted before any deletion: `grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md`
returns `120`; `grep -c 'pending — plan' .planning/ledgers/milestone-09-12.md` returns `0`;
`grep -c 'run-5 input (not yet re-derived)' .planning/ledgers/milestone-09-12.md` returns `0`; and
the ID list in the ledger diffs clean against the ID list in this section. If any check fails, make
no deletion and report the failure — the 120 rows must exist in the ledger before they stop existing
here.

The pointer keeps the section heading and replaces its 325-line body with: a one-line statement that
the ledger for Milestone 9-12 and the Deferred-QA and project-management registers lives at
`.planning/ledgers/milestone-09-12.md` and is authoritative; the dated note that this section was
reduced to a pointer by Phase 13 plan 13-10 (D-01, D-00d); the row count and section count so a
reader can check the ledger is intact; and the note that this is the **fifth and final** sibling, so
the series is complete. Match the shape of the four existing sibling pointers.

Do not leave the 120 rows in place as a second copy — a diverging duplicate is exactly what D-01
forbids. Do not delete the section heading itself; downstream documents reference it by name.
  </action>
  <verify>
    <automated>awk '/^## Milestone 9-12 as-shipped ledger/,/^## [A-Z]/' .planning/REQUIREMENTS.md | grep -c '^| REQ-'</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^## Milestone 9-12 as-shipped ledger/,/^## [A-Z]/' .planning/REQUIREMENTS.md | grep -c '^| REQ-'` → `0`
    - `grep -c '^## Milestone 9-12 as-shipped ledger' .planning/REQUIREMENTS.md` → `1` (the heading survives)
    - `grep -c '.planning/ledgers/milestone-09-12.md' .planning/REQUIREMENTS.md` → `≥1`
    - `grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md` → `120` (asserted before the deletion and again after)
    - `grep -c 'pending — plan' .planning/ledgers/milestone-09-12.md` → `0`
    - `grep -c 'fifth and final' .planning/REQUIREMENTS.md` → `≥1`
    - `git diff --numstat -- .planning/REQUIREMENTS.md | awk '{print ($3 > 300) ? "body-removed" : "body-retained"}'` → `body-removed`
    - `git diff --name-only -- '*.rs' | wc -l` → `0`
  </acceptance_criteria>
  <done>The section is a pointer at `.planning/ledgers/milestone-09-12.md` holding zero `| REQ-` rows, its heading survives, the ledger was verified to hold all 120 derived rows before anything was removed, and the pointer matches the four sibling pointers' shape.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Modified by this plan (no new files):
- `.planning/REQUIREMENTS.md` — four dated corrections: ORCH-01's arithmetic (D-04), ORCH-05's current-state figures (D-18), PIPE-01's `ci.yml` job list plus the D-09 residue (D-08, D-09), and the `## Milestone 9-12 as-shipped ledger` section reduced to a pointer (D-01)

Identifiers a later reader will grep for, introduced here:
- The pointer string `.planning/ledgers/milestone-09-12.md` inside `REQUIREMENTS.md`
- The corrected 15-job `ci.yml` list including `examples` and `kubernetes-smoke`
- The corrected split figures `35` / `53` / `32` inside ORCH-01
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requirement text -> downstream phase plan | PIPE-01 already quotes a stale list verbatim; a requirement's own text is what a later planner budgets against |
| REQUIREMENTS.md body -> ledger file | 120 rows move out of one document and must exist in the other first |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-29 | Denial of service | deleting the 325-line ledger body | high | mitigate | Task 3 asserts `120` rows, zero `pending — plan` markers and zero not-yet-re-derived prefixes in the ledger, and a clean ID diff, **before** any deletion; a failed precondition stops the task with no edit |
| T-13-30 | Tampering | two diverging copies of the same 120 rows | high | mitigate | D-01: the section body is replaced by a pointer, and the acceptance criteria assert zero `| REQ-` rows remain in that section |
| T-13-03 | Repudiation | corrections overwriting superseded text | high | mitigate | D-00c/D-00d: the acceptance criteria require the superseded figures `104`, `v0.5.1` and the 14-job list to still be present alongside the corrected ones |
| T-13-02 | Tampering | stale anchors `:2210-2212`, `:2300-2301`, `:2434-2436` | high | mitigate | Every task's `read_first` requires locating the sentence by reading or grepping, not by trusting an anchor from another document — and tasks 1 and 2 shift the line numbers task 3 would otherwise rely on, which is why the pointer is last |
| T-13-31 | Elevation of privilege | correcting historical facts along with current state | medium | mitigate | The four lockstep gates are explicitly out of scope for the D-18 correction, and an acceptance criterion asserts they survive |
| T-13-SC | Tampering | package installs | low | accept | N/A — no package install, no manifest change (D-19) |
</threat_model>

<verification>
- `awk '/^## Milestone 9-12 as-shipped ledger/,/^## [A-Z]/' .planning/REQUIREMENTS.md | grep -c '^| REQ-'` → `0`
- `grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md` → `120`
- `grep -c 'kubernetes-smoke' .planning/REQUIREMENTS.md` → `≥1`
- `git diff --name-only -- '*.rs' | wc -l` → `0`

## Precondition

The ledger must already hold all 120 derived rows with no interim markers before task 3 removes the
section body. Plan ordering places every fan-out plan in wave 2 and this plan in wave 3, but ordering
is not a guarantee — task 3 asserts the precondition itself and halts if it is unmet.
</verification>

<success_criteria>
`REQUIREMENTS.md` states nothing about itself that measurement contradicts, the three downstream-quoted
figures are corrected at source with their originals retained, and the ledger has exactly one home.
</success_criteria>

<output>
Create `.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-10-SUMMARY.md` when done.
</output>
