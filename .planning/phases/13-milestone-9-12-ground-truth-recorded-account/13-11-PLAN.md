---
phase: 13-milestone-9-12-ground-truth-recorded-account
plan: 11
type: execute
wave: 3
depends_on: ["13-01", "13-07"]
files_modified:
  - .project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md
  - .project/Deferred-QA-CICD-Completion/DEFERRED_COVERAGE.md
  - .planning/intel/requirements.md
  - .planning/intel/code-verification.md
autonomous: true
requirements: [ORCH-03]
must_haves:
  truths:
    - "Each of the four ORCH-03(b)-(e) stale paths carries a dated correction banner at source naming the absent path, the shipped path and the milestone that moved it, with the original path text retained inline and marked superseded (D-13, D-00c)"
    - "`.planning/intel/code-verification.md` carries three dated corrections: the 14-job `ci.yml` list at its job-list statement (D-08), the `v0.6.0` current-state figure (D-18), and finding 8's `fails on every run` consequence clause (D-09) — each retaining the original"
    - "Every correction banner is appended below any banner already present in the same document and carries its own date, so banners accumulate in chronological order rather than replacing one another (ORCH-03 ordering edge)"
    - statement: "A stale path whose successor cannot be located in the tree is annotated as absent with no successor found, naming the search command, rather than being left uncorrected or given a guessed replacement (ORCH-03 empty edge)"
      verification: backstop
    - "The corrections agree with the four ledger rows plan 13-07 derived; the ledger row and the source annotation state the same relocation with the same two paths"
  artifacts: []
  key_links:
    - "`.project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md` and `DEFERRED_COVERAGE.md` -> the ledger rows for `REQ-listener-service-test-coverage`, `REQ-llm-tool-calling-port`, `REQ-arch-doc-modernization`, `REQ-asciinema-demos`"
    - "`.planning/intel/code-verification.md` corrections -> Phase 15 (D-08 job list, D-09 baseline path) and plan 13-10's matching REQUIREMENTS.md corrections"
  prohibitions:
    - "No `.rs` file is modified by this plan (D-19)"
    - "No source document is rewritten; corrections are dated banners plus inline annotation with the original retained (D-00c)"
    - "No stale path is given a replacement that was not verified present in the tree this session"
    - "ORCH-03 is not grown beyond its five named items; the four Milestone 12 `current-exports.txt` references stay ledger rows (D-09)"
    - "`.project/Milestone_12-Web-API/` is not edited by this plan; Phase 8 already annotated it and plan 13-08 owns the route annotations"
    - "`.planning/REQUIREMENTS.md` is not edited by this plan; plan 13-10 owns it"
    - "Phase 12's 87-hit stale-citation grep is not re-run and its deliberately excluded classes are not touched (D-07)"
    - "No item from 13-CONTEXT.md's `<deferred>` block is planned or actioned here"
---

<objective>
Correct at source the four stale module and document paths ORCH-03(b)-(e) names, and the three
statements in `.planning/intel/code-verification.md` that later measurement superseded.

Purpose: ORCH-03's done-when is that anyone applying a run-5 requirement literally cannot write to a
path that does not exist. A ledger row records the relocation; only a source annotation stops the
next reader of the source document from following the dead path.

Output: dated correction banners in two `.project/` documents and two `.planning/intel/` documents,
with every superseded string retained.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-CONTEXT.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate the Deferred-QA source documents for the four relocations</name>
  <files>.project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md, .project/Deferred-QA-CICD-Completion/DEFERRED_COVERAGE.md</files>
  <read_first>
    - `.project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md` — locate every stale path with a grep for `listener_service.rs`, for the `application/ports/output/llm_port.rs` path, for the `Design/Design_and_Architecture.md` path and for `asciinema`, and read each surrounding block. Do not trust a line number from another document.
    - `.project/Deferred-QA-CICD-Completion/DEFERRED_COVERAGE.md` — the same grep for `listener_service.rs`; the module-2 section names the absent path.
    - `.planning/ledgers/milestone-09-12.md` — the four rows plan 13-07 derived; the annotation must state the same two paths per relocation.
    - `.project/Milestone_12-Web-API/Epic_1/prd-agent-registry-execution-api.md` — read Phase 8's existing dated `DEBT-01` banner and copy its shape, so the corpus has one annotation style.
  </read_first>
  <action>
Verify each relocation in the tree before writing anything, and record both commands per pair:
(b) the `src/core/platform/manager/listener_service.rs` path against
`src/application/services/orchestration/listener.rs` (`ListenerOrchestrator`, Milestone 6 Epic 2);
(c) the `src/application/ports/output/llm_port.rs` path — the whole `src/application/ports/`
directory was deleted by Milestone 5 Epic 2 — against `crates/paladin-ports/src/output/llm_port.rs`;
(d) the `docs/Design/Design_and_Architecture.md` path against
`docs/src/appendix/design-and-architecture.md` (Milestone 11 overhaul);
(e) `REQ-asciinema-demos`'s README embedding clause against the current README, `docs/assets/` and
`docs/DEMOS.md`.

Then annotate both documents per D-00c: a dated correction banner near the top of each, naming what
was wrong and pointing at the ORCH-03 item and the ledger row, plus each stale path corrected inline
with the **original path text retained and marked superseded**. Never rewrite a document, never
delete a superseded path string.

If a document already carries a dated banner, append this one below it with its own date — banners
accumulate in chronological order and are never merged or replaced.

Per-relocation content:
(b) record the relocation and that the register's 602 LOC / ~57.83% baseline dated 2026-02-14 is
stale in both path and number because Milestone 9 Epic 2 added tests against the module; scope real,
arithmetic not; owner Phase 15 / DEFER-03.
(c) record the relocation and note for Phase 14 that this is the same file WEB-03 and WEB-04 act on.
(d) record the relocation **and** that it hid a gap rather than closing one: the file is still at the
line count its own PRD cites as the pre-rewrite state, with none of the seven newer subsystems and no
diagrams, because Milestone 11 Epic 3's non-goals exempt the appendix chapter Epic 2 moved it into.
Two facts, two labels: the move is closed, the rewrite is open and owned by Phase 16 / DOCS-02.
(e) record that the clause targets a document that changed shape — the README was rewritten by
Milestone 11 Epic 5 into a concise landing page with no demos section — and that `docs/assets/`
exists and is empty while `docs/DEMOS.md` does not exist. Owner Phase 16 / DOCS-04.

If any successor path cannot be located in the tree, annotate the entry as absent with no successor
found and record the search command that returned nothing. Do not guess a replacement.

Do not edit anything under `.project/Milestone_12-Web-API/` — Phase 8 already annotated it for the
baseline-path defect and plan 13-08 owns the route annotations.
  </action>
  <verify>
    <automated>grep -c 'src/application/services/orchestration/listener.rs' .project/Deferred-QA-CICD-Completion/DEFERRED_COVERAGE.md .project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'src/application/services/orchestration/listener.rs' .project/Deferred-QA-CICD-Completion/DEFERRED_COVERAGE.md` → `≥1`
    - `grep -c 'src/core/platform/manager/listener_service.rs' .project/Deferred-QA-CICD-Completion/DEFERRED_COVERAGE.md` → `≥1` (the superseded path is retained)
    - `grep -c 'crates/paladin-ports/src/output/llm_port.rs' .project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md` → `≥1`
    - `grep -c 'docs/src/appendix/design-and-architecture.md' .project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md` → `≥1`
    - `grep -c 'DOCS-04' .project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md` → `≥1`
    - `grep -cE 'ORCH-03|milestone-09-12.md' .project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md` → `≥1`
    - `git diff -- .project/Deferred-QA-CICD-Completion/ | grep -c '^-[^-]'` → `0` (annotation only; no original line removed)
    - `git diff --name-only -- '.project/*' | sort` → exactly the two Deferred-QA files
  </acceptance_criteria>
  <done>Both Deferred-QA source documents carry dated banners and inline annotations for all four relocations, every superseded path string survives, no original line is deleted, and no other `.project/` file is touched.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate the four requirement blocks in .planning/intel/requirements.md</name>
  <files>.planning/intel/requirements.md</files>
  <read_first>
    - `.planning/intel/requirements.md` — locate the `REQ-listener-service-test-coverage`, `REQ-llm-tool-calling-port`, `REQ-arch-doc-modernization` and `REQ-asciinema-demos` blocks with `grep -n '^## REQ-<id>'` and read each in full. Two of them already carry `note:` lines recording a relocation; read those first so this plan adds only what is missing.
    - `.planning/ledgers/milestone-09-12.md` — the four rows plan 13-07 derived.
    - Task 1's annotations in the `.project/` documents — the wording must agree.
  </read_first>
  <action>
For each of the four requirement blocks, add a dated correction annotation recording the relocation
and its owner, in the block's own `note:` style so the file stays machine-readable in the shape it
already uses.

Read before writing: the `REQ-llm-tool-calling-port` block and the `REQ-listener-service-test-coverage`
block already carry `note:` or `settled-by:` lines recording their relocations. Where a correction is
already present and still accurate, **record that it is already correct in the plan summary and add
nothing** — a duplicate annotation is churn, and the corpus already demonstrates what happens when
the same fact is written in two places that then diverge. Where it is present but incomplete, extend
it with a dated addition rather than replacing it.

Add what is genuinely missing: the shipped path for each stale path, dated; for
`REQ-arch-doc-modernization` the split into a closed relocation and an open rewrite with Phase 16 /
DOCS-02 named; for `REQ-asciinema-demos` the README's changed shape and Phase 16 / DOCS-04; for
`REQ-listener-service-test-coverage` the stale baseline figure and Phase 15 / DEFER-03.

Retain every original path string. Do not delete, re-key or re-order any block.
  </action>
  <verify>
    <automated>grep -c 'crates/paladin-ports/src/output/llm_port.rs' .planning/intel/requirements.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'crates/paladin-ports/src/output/llm_port.rs' .planning/intel/requirements.md` → `≥1`
    - `grep -c 'src/application/services/orchestration/listener.rs' .planning/intel/requirements.md` → `≥1`
    - `grep -c 'docs/src/appendix/design-and-architecture.md' .planning/intel/requirements.md` → `≥1`
    - `grep -c 'src/core/platform/manager/listener_service.rs' .planning/intel/requirements.md` → `≥1` (superseded path retained)
    - `grep -cE 'DOCS-02|DOCS-04|DEFER-03' .planning/intel/requirements.md` → `≥3`
    - `git diff -- .planning/intel/requirements.md | grep -c '^-[^-]'` → `0`
    - `grep -c '^## REQ-' .planning/intel/requirements.md` → unchanged from the pre-edit count (no block added or removed)
  </acceptance_criteria>
  <done>All four requirement blocks record their relocation and owner with the original path retained, blocks that were already correct are left alone and reported as such, and no block is added, removed or re-keyed.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the three superseded statements in .planning/intel/code-verification.md</name>
  <files>.planning/intel/code-verification.md</files>
  <read_first>
    - `.planning/intel/code-verification.md` — locate the three statements by content, not by line number: the `ci.yml` 14-job list, the workspace-version current-state figure, and finding 8's consequence clause about the `api-surface` job. 13-CONTEXT.md records them at `:539-540`, `:469` and `:619-620`, but the file has been amended before and the anchors must be re-verified.
    - `.github/workflows/ci.yml` — re-run `grep -nE '^  [a-z][a-z0-9-]*:$' .github/workflows/ci.yml` for the live job list.
    - `Cargo.toml` and `git tag --sort=-v:refname | head -8` — the live version state.
    - `scripts/check-api-surface.sh` — line 6's `BASELINE` default, and confirm the dotted baseline file exists.
    - `.planning/REQUIREMENTS.md` — plan 13-10's matching corrections; the wording here must agree with them.
  </read_first>
  <action>
Apply three dated in-place corrections, each retaining the original statement and marking it
superseded (D-00c, D-00d). This is a verification record, so each correction records the command,
its output and the date it was run.

**(1) The `ci.yml` job list (D-08).** The recorded 14-job list is stale. Record the live measured
list with each job's line number and the grep that produced it, note that `security` was deleted by
Phase 9 and that two jobs the recorded list does not name are present, and state that the measured
count is 15. Record what is there; do not attribute the two additions to a commit without consulting
`git log`. Note that PIPE-01 quotes the stale list verbatim and that plan 13-10 corrects it there.

**(2) The workspace-version current-state figure (D-18).** The recorded figure was correct as of the
2026-07-30 ingest and is superseded. Record the live `Cargo.toml` version, the newest tags and the
current branch, with their commands.

**(3) Finding 8's consequence clause (D-09).** The clause concluding that the `api-surface` CI job
fails on every run because the baseline file is absent is **no longer true**:
`scripts/check-api-surface.sh:6` reads the dotted `.project/current-exports.txt` and that file
exists. Record both checks and their outputs. State precisely what remains true — the documentation
half, four Milestone 12 requirement texts naming the undotted path, already carrying Phase 8's dated
banners — and that it is handed to Phase 15 rather than becoming a sixth ORCH-03 item.

Do not delete any original statement, do not renumber findings, and do not re-run or revise Phase
12's 87-hit stale-citation inventory (D-07).
  </action>
  <verify>
    <automated>grep -c 'kubernetes-smoke' .planning/intel/code-verification.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'kubernetes-smoke' .planning/intel/code-verification.md` → `≥1`
    - `grep -c 'benchmark-regression-signal' .planning/intel/code-verification.md` → `≥2` (superseded list retained alongside the corrected one)
    - `grep -c '0\.7\.0' .planning/intel/code-verification.md` → `≥1` and `grep -c '0\.6\.0' .planning/intel/code-verification.md` → `≥1` (corrected and superseded figures both present)
    - `grep -c 'check-api-surface.sh:6' .planning/intel/code-verification.md` → `≥1`
    - `git diff -- .planning/intel/code-verification.md | grep -c '^-[^-]'` → `0`
    - `diff <(grep -oE '^  [a-z][a-z0-9-]*:' .github/workflows/ci.yml | tr -d ' :' | sort) <(grep -oE '`[a-z][a-z0-9-]*`' .planning/intel/code-verification.md | tr -d '`' | sort -u | grep -xFf <(grep -oE '^  [a-z][a-z0-9-]*:' .github/workflows/ci.yml | tr -d ' :'))` → no output (every measured job id appears)
    - `git diff --name-only -- '*.rs' | wc -l` → `0`
  </acceptance_criteria>
  <done>All three superseded statements carry a dated correction with the command, output and date recorded, every original statement survives, and no finding is renumbered.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Modified by this plan (no new files):
- `.project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md` — dated banners for ORCH-03(c), (d), (e)
- `.project/Deferred-QA-CICD-Completion/DEFERRED_COVERAGE.md` — dated banner for ORCH-03(b)
- `.planning/intel/requirements.md` — dated relocation annotations on four `REQ-*` blocks
- `.planning/intel/code-verification.md` — three dated corrections (job list, version figure, finding 8's consequence clause)

Identifiers a later reader will grep for, introduced here:
- The shipped-path strings `src/application/services/orchestration/listener.rs`, `crates/paladin-ports/src/output/llm_port.rs`, `docs/src/appendix/design-and-architecture.md` inside the Deferred-QA sources
- The corrected 15-job `ci.yml` list inside `code-verification.md`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source document -> next implementer | a `.project/` PRD is what someone picking up deferred work reads first |
| verification record -> later phases | `code-verification.md` sits third in the precedence order and is quoted verbatim by PIPE-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-21 | Repudiation | annotation deleting superseded text | high | mitigate | D-00c: `git diff … grep -c '^-[^-]'` → `0` on every edited file; superseded path strings are asserted still present by positive greps |
| T-13-02 | Tampering | the `:539-540`, `:469`, `:619-620` anchors | high | mitigate | `read_first` requires locating each statement by content; the file has been amended before and the anchors are treated as hints, not facts |
| T-13-32 | Spoofing | a guessed successor path | high | mitigate | Every replacement path is verified present in the tree this session with the command recorded; an unlocatable successor is annotated as absent with no successor found rather than filled in |
| T-13-33 | Tampering | duplicate annotations diverging | medium | mitigate | Task 2 reads existing `note:` lines first and adds nothing where a correction is already present and accurate, reporting that in the summary instead |
| T-13-08 | Tampering | Phase 12's excluded stale-citation sites | medium | mitigate | D-07: the 87-hit grep is not re-run and the excluded classes are untouched |
| T-13-14 | Elevation of privilege | ORCH-03 growing to a sixth item | medium | mitigate | D-09: the four Milestone 12 references stay ledger rows and are handed to Phase 15; `.project/Milestone_12-Web-API/` is outside this plan's `files_modified` |
| T-13-SC | Tampering | package installs | low | accept | N/A — no package install, no manifest change (D-19) |
</threat_model>

<verification>
- `git diff -- .project/ .planning/intel/ | grep -c '^-[^-]'` → `0`
- `grep -c 'crates/paladin-ports/src/output/llm_port.rs' .planning/intel/requirements.md` → `≥1`
- `grep -c 'kubernetes-smoke' .planning/intel/code-verification.md` → `≥1`
- `git diff --name-only -- '*.rs' | wc -l` → `0`
</verification>

<success_criteria>
Nobody reading a run-5 source document can follow a path that does not exist, the four relocations
are recorded as relocations at source and in the ledger with the same two paths, and the three
superseded verification statements say what measurement says.
</success_criteria>

<output>
Create `.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-11-SUMMARY.md` when done.
</output>
