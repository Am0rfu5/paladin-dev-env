---
phase: 13-milestone-9-12-ground-truth-recorded-account
plan: 12
type: execute
wave: 3
depends_on: ["13-01", "13-02", "13-03", "13-04", "13-06"]
files_modified:
  - .planning/decisions/0029-version-trajectory-history.md
  - .planning/decisions/0030-milestone-7-self-numbering.md
autonomous: true
requirements: [ORCH-05]
must_haves:
  truths:
    - "ADR-0029's `## Trajectory` table carries four new rows — `v0.3.0` (M9), `v0.4.0` (M10), `v0.5.0` (M11), `v0.6.0` (M12) — each with its own Date, What-it-was and Evidence cells sourced from the corresponding ledger row (D-16)"
    - "The four new rows sit in ascending order between the existing `v0.2.0` row and the existing `0.7.0` row, and no pre-existing row is re-sorted or re-keyed (D-16, ORCH-05 ordering edge)"
    - "The spanning placeholder row reserved for Phase 13 / ORCH-05 is superseded in place with a dated marker retaining its text, not deleted (D-00d)"
    - "The version line runs unbroken from `v0.1.0-rc.1` through v0.3.0, v0.4.0, v0.5.0 and v0.6.0 to the tree's `0.7.0`, with no gap and no duplicated tag key"
    - "The run-5 provenance-key confirmation ORCH-05 owns is run in this environment and its command and output are recorded verbatim; ADR-0030:79-84 is cited as the record that already closed the fifth-collision prediction (D-17)"
    - statement: "If the provenance-key confirmation finds a fifth numbering collision, ADR-0030 is amended in place with a dated section and no rival ADR is created; if it finds none, ADR-0030 is cited unchanged (ORCH-05 empty edge)"
      verification: backstop
    - statement: "A milestone that targeted or shipped two figures gets two rows keyed by tag, and a tag that appears in both a milestone gate and a later release row is keyed once per row context without either row being merged (ORCH-05 adjacency and duplicates edges)"
      verification: backstop
  artifacts: []
  key_links:
    - "ADR-0029 `## Trajectory` -> ledger rows `REQ-m9-quality-gate-v030`, `REQ-m10-v040-release`, `REQ-m11-v050-release`, `REQ-m12-v060-release`, `REQ-lockstep-versioning`"
    - "ORCH-05 -> ADR-0030:79-84 (the numbering prediction, already discharged) and ADR-0008 (REL-01, already converged)"
  prohibitions:
    - "No `.rs` file is modified by this plan (D-19)"
    - "No rival version ADR and no rival numbering ADR is created; ADR-0029 is appended to and ADR-0030 is cited (D-16, D-17, D-20)"
    - "REL-01 is not re-opened; it is `[x] Complete` at Phase 4 via ADR-0008 and ORCH-05 applies the converged result (D-16)"
    - "No existing `## Trajectory` row is re-sorted, re-keyed or deleted; the placeholder is superseded in place with its text retained (D-00d)"
    - "No commit hash is invented per intermediate tag; the four new rows cite the ledger rows and the lockstep mechanism (D-16)"
    - "`.planning/REQUIREMENTS.md` is not edited by this plan; plan 13-10 corrected ORCH-05's figures and plan 13-13 flips its checkbox"
    - "No item from 13-CONTEXT.md's `<deferred>` block is planned or actioned here"
---

<objective>
Complete the version trajectory through the tree and close ORCH-05's numbering half by citation
rather than by re-deciding it.

Purpose: HARD-03 records `v0.1.0-rc.1` as history, REL-01 converged on `0.7.0`, and ADR-0029 is the
single home for the whole line with a placeholder row explicitly reserved for this phase. Four rows
close the gap so REL-01 converges with the whole trajectory in view.

Output: four appended rows in `.planning/decisions/0029-version-trajectory-history.md`, its
placeholder row superseded in place, and a recorded provenance-key confirmation citing ADR-0030.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-CONTEXT.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the four lockstep-gate rows to ADR-0029's Trajectory table</name>
  <files>.planning/decisions/0029-version-trajectory-history.md</files>
  <read_first>
    - `.planning/decisions/0029-version-trajectory-history.md` — read the whole file: the `## Trajectory` table's exact column set, its five existing rows including the spanning placeholder whose What-it-was cell reserves the range for Phase 13 / ORCH-05, its stated keying rule, and its `## Downstream Consumers` naming Phase 13.
    - `.planning/ledgers/milestone-09-12.md` — the derived rows for `REQ-m9-quality-gate-v030` (plan 13-02), `REQ-m10-v040-release` and `REQ-lockstep-versioning` (plan 13-03), `REQ-m11-v050-release` (plan 13-04) and `REQ-m12-v060-release` (plan 13-06). These are the evidence the four new rows cite.
    - `.planning/decisions/0008-workspace-version-0-7-0.md` — Phase 4's convergence on `0.7.0`; REL-01 is closed by it and ORCH-05 applies it.
  </read_first>
  <action>
Append four rows to ADR-0029's `## Trajectory` table using its existing column set exactly:
`v0.3.0` (Milestone 9), `v0.4.0` (Milestone 10), `v0.5.0` (Milestone 11), `v0.6.0` (Milestone 12).

Place them in **ascending order between** the existing `v0.2.0` row and the existing `0.7.0` row, so
the table's own ascending-order invariant survives. Do not re-sort, re-key or delete any existing
row.

Each row is a lockstep bump: the milestone's finalization Epic bumps the root crate and every
workspace member together and cuts a tag. Source each row's Date, What-it-was and Evidence cells from
the corresponding ledger row named in `read_first`, and cite `REQ-lockstep-versioning` as the
mechanism. Do not derive a fresh commit hash per intermediate tag — D-16 asks for an unbroken chain
citing the mechanism, not a per-tag archaeology.

Resolve the spanning placeholder row whose What-it-was cell reserves the `v0.3.0`-to-`v0.5.1` range
for this phase. Do not delete it: mark it superseded in place with a dated note stating that the four
rows below now carry the range, retaining its original text, per D-00d. Its Evidence cell's
`git tag --sort=-v:refname` citation should be re-run and its live output recorded in the dated note.

Apply the table's own stated keying rule: a row is keyed by the tag or target figure, not by the
milestone, so a milestone that targeted two figures gets two rows. Where the same tag string appears
in both a milestone-gate row and a later release row, keep both rows — they answer different
questions and merging them would lose one. Re-run `git tag --sort=-v:refname | head -8` and confirm
each of the four new tags against its output before writing the row; the tree state is `Cargo.toml`
at `0.7.0` on branch `release/v0.7.0` with `v0.7.1` and `v0.7.0` newest, not the `0.6.0`/`v0.5.1`
figures ORCH-05's own text carried (D-18, corrected at source by plan 13-10).

Do not create a second version ADR. ADR-0029 is the single home for the whole line, HARD-03 covers
rc.1 through `v0.2.0`, and REL-01 (ADR-0008, Phase 4, complete) covers the landing at `0.7.0` — this
plan extends the same table and re-opens nothing (D-16).
  </action>
  <verify>
    <automated>grep -cE '^\| ?`?v0\.[3-6]\.0' .planning/decisions/0029-version-trajectory-history.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^\| ?`?v0\.[3-6]\.0' .planning/decisions/0029-version-trajectory-history.md` → `4`
    - `grep -nE '^\| ?`?v0\.[0-9]+\.[0-9]+' .planning/decisions/0029-version-trajectory-history.md` → the `v0.3.0`, `v0.4.0`, `v0.5.0`, `v0.6.0` rows appear on consecutive increasing line numbers, after the `v0.2.0` row and before the `0.7.0` row
    - `grep -c 'v0.1.0-rc.1' .planning/decisions/0029-version-trajectory-history.md` → `≥1` (the head of the chain is untouched)
    - `grep -c 'REQ-lockstep-versioning' .planning/decisions/0029-version-trajectory-history.md` → `≥1`
    - `grep -cE 'REQ-m9-quality-gate-v030|REQ-m10-v040-release|REQ-m11-v050-release|REQ-m12-v060-release' .planning/decisions/0029-version-trajectory-history.md` → `≥4`
    - `grep -ci 'superseded' .planning/decisions/0029-version-trajectory-history.md` → `≥1` (the placeholder is marked, not deleted)
    - `git diff -- .planning/decisions/0029-version-trajectory-history.md | grep -c '^-[^-]'` → `≤1` (at most the placeholder row's single line rewritten in place with its text retained)
    - `ls .planning/decisions/ | grep -cE '^00(4[0-9])-.*version' ` → `0` (no rival version ADR)
  </acceptance_criteria>
  <done>The Trajectory table runs unbroken from rc.1 to `0.7.0` with four new ascending rows citing their ledger rows and the lockstep mechanism, the placeholder is superseded in place with its text retained, and no existing row moved.</done>
</task>

<task type="auto">
  <name>Task 2: Run the provenance-key confirmation and cite ADR-0030</name>
  <files>.planning/decisions/0029-version-trajectory-history.md, .planning/decisions/0030-milestone-7-self-numbering.md</files>
  <read_first>
    - `.planning/decisions/0030-milestone-7-self-numbering.md` — read the whole file, and lines 79-84 in particular, where the Roadmap Extension Protocol's predicted fifth instance is already recorded closed so that no later phase inherits a standing prediction. Re-read; do not repeat the anchor without confirming it.
    - `.planning/decisions/0029-version-trajectory-history.md` — the file task 1 just amended, including its `## Downstream Consumers` section.
    - `.project/` — the `Milestone_9-*`, `Milestone_10-*`, `Milestone_11-*` and `Milestone_12-*` directory names, which are what the run-5 provenance keys must resolve against.
  </read_first>
  <action>
Run the one check ORCH-05 owns and record it verbatim: confirm that the run-5 `REQ-*` provenance keys
resolve directly against directory numbering. Grep the run-5 requirement provenance for its milestone
references and compare each against the actual `.project/Milestone_N-*` directory numbers, so that a
requirement provenanced to Milestone 9 lives under the Milestone 9 directory and not under a
directory whose overview titles itself with a different number. Record the exact commands and their
output, including the count of keys checked (D-00e, D-21).

Two outcomes, both explicit:

- **No fifth collision found** — the expected result. Record the commands, the count and the null
  result. Cite `.planning/decisions/0030-milestone-7-self-numbering.md` lines 79-84 as the record
  that already closed the prediction, deliberately, so no later phase inherits a standing check.
  Add the citation to ADR-0029's `## Downstream Consumers` as a one-line bullet noting that the
  numbering half of ORCH-05 is discharged by ADR-0030 and is not re-recorded here — recording a
  closure a second time is how the numbering convention starts disagreeing with itself, which is the
  defect ADR-0030 exists to fix. Make no change to ADR-0030 itself in this branch.

- **A fifth collision found** — amend `.planning/decisions/0030-milestone-7-self-numbering.md`
  **in place** with a dated section naming the collision, its two conflicting numbers and its
  `file:line`, retaining the existing closure text and marking it superseded (D-00d, D-17). Do **not**
  create a rival numbering ADR under any circumstances, and do not renumber anything in `.project/`.
  Record the finding in the plan summary and flag it for plan 13-13's close-out.

Whichever branch runs, the plan summary states which one ran, the commands, the outputs and the date.
Do not record the outcome as "confirmed" without the command that confirmed it.
  </action>
  <verify>
    <automated>grep -c '0030-milestone-7-self-numbering' .planning/decisions/0029-version-trajectory-history.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '0030-milestone-7-self-numbering|ADR-0030' .planning/decisions/0029-version-trajectory-history.md` → `≥1`
    - `awk '/^## Downstream Consumers/,0' .planning/decisions/0029-version-trajectory-history.md | grep -cE 'ADR-0030|0030-'` → `≥1`
    - `ls .planning/decisions/ | grep -icE 'numbering' ` → `1` (only `0030-milestone-7-self-numbering.md`; no rival numbering ADR)
    - `13-12-SUMMARY.md` records the provenance-key confirmation command, its output, the number of keys checked and the date
    - If no collision: `git diff --name-only -- .planning/decisions/0030-milestone-7-self-numbering.md | wc -l` → `0`
    - If a collision: `git diff -- .planning/decisions/0030-milestone-7-self-numbering.md | grep -c '^-[^-]'` → `0` (amended in place, existing text retained)
    - `git diff --name-only -- '*.rs' | wc -l` → `0`
  </acceptance_criteria>
  <done>The provenance-key confirmation is run with its command and output recorded, ADR-0030:79-84 is cited as the existing closure, and either ADR-0030 is untouched (no collision) or amended in place (collision) — with no rival numbering ADR either way.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Modified by this plan (no new files):
- `.planning/decisions/0029-version-trajectory-history.md` — four appended `## Trajectory` rows (`v0.3.0`, `v0.4.0`, `v0.5.0`, `v0.6.0`), the Phase 13 placeholder row superseded in place, and an ADR-0030 citation in `## Downstream Consumers`
- `.planning/decisions/0030-milestone-7-self-numbering.md` — untouched unless the provenance-key confirmation finds a fifth collision, in which case a dated in-place amendment

Identifiers a later reader will grep for, introduced here:
- The Trajectory row keys `v0.3.0`, `v0.4.0`, `v0.5.0`, `v0.6.0`
- The dated superseded marker on ADR-0029's former Phase 13 placeholder row
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger row -> ADR row | evidence crosses from a ledger into the ADR that is the single home for the whole version line |
| ADR-0029 / ADR-0030 -> future phases | two ADRs recording adjacent facts must not start disagreeing with each other |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-34 | Tampering | the existing Trajectory rows | high | mitigate | Rows are inserted between two named existing rows; the acceptance criteria assert `v0.1.0-rc.1` survives and that at most one line was rewritten in place — the placeholder, with its text retained |
| T-13-35 | Repudiation | recording a closure a second time | high | mitigate | D-17: ADR-0030 is cited, not duplicated; the acceptance criteria assert exactly one numbering ADR exists and that ADR-0030 is untouched unless a collision is actually found |
| T-13-01 | Tampering | the four new rows' evidence cells | high | mitigate | Each cites its derived ledger row and the lockstep mechanism, and each tag is confirmed against a re-run `git tag --sort=-v:refname` |
| T-13-03 | Repudiation | deleting the placeholder row | medium | mitigate | D-00d: superseded in place with a dated marker retaining its text |
| T-13-36 | Elevation of privilege | re-opening REL-01 or forking a rival version ADR | high | mitigate | D-16: REL-01 is `[x] Complete` via ADR-0008 and ORCH-05 applies it; the acceptance criteria assert no additional version ADR file exists |
| T-13-SC | Tampering | package installs | low | accept | N/A — no package install, no manifest change (D-19) |
</threat_model>

<verification>
- `grep -cE '^\| ?`?v0\.[3-6]\.0' .planning/decisions/0029-version-trajectory-history.md` → `4`
- `ls .planning/decisions/ | grep -icE 'version-trajectory'` → `1`
- `git diff --name-only -- '*.rs' | wc -l` → `0`
</verification>

<success_criteria>
The version story runs unbroken from `v0.1.0-rc.1` through v0.3.0, v0.4.0, v0.5.0 and v0.6.0 to the
tree, in one ADR, with the numbering prediction closed by citation and no rival ADR anywhere.
</success_criteria>

<output>
Create `.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-12-SUMMARY.md` when done.
</output>
