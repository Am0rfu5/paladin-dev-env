---
phase: 13-milestone-9-12-ground-truth-recorded-account
plan: 13
type: execute
wave: 4
depends_on: ["13-01", "13-02", "13-03", "13-04", "13-05", "13-06", "13-07", "13-08", "13-09", "13-10", "13-11", "13-12"]
files_modified:
  - .planning/ledgers/milestone-09-12.md
  - .planning/REQUIREMENTS.md
  - .planning/decisions/PROMOTION.md
  - .planning/PROJECT.md
autonomous: true
requirements: [ORCH-01, ORCH-02, ORCH-03, ORCH-04, ORCH-05]
must_haves:
  truths:
    - "The ledger carries a dated close-out amendment section appended at the foot recording a whole-file integrity re-check — 120 `| REQ-` rows, 120 distinct IDs, 29 section headers, zero interim stub markers, zero blank Verdict cells, and every `ADR-NNNN` citation resolving to a file that exists"
    - "The close-out records a per-class verdict-distribution table counted directly from the Verdict column that session, plus the list of rows cited rather than re-derived and why, plus every claim that is CI-only with the exact command a CI runner executes (D-21)"
    - "`git diff --name-only <base>..HEAD -- '*.rs' | wc -l` returns `0` with the base SHA recorded verbatim, so the D-19 boundary is checkable rather than claimed"
    - "`mdbook build docs/` is re-run and compared against the pre-phase baseline of exit 101 with two pre-existing broken links in files this phase does not touch; the result is recorded honestly, neither reinterpreted as a pass nor attributed to this phase's three documentation edits"
    - "ORCH-01 through ORCH-05 are flipped to `[x]` in REQUIREMENTS.md, each with the evidence that closes it, and the five traceability rows read `Phase 13 | Complete`"
    - "Three forward hand-off blocks are written in the shape Phases 9, 10 and 12 used — Phase 14 / WEB-01..WEB-04, Phase 15 / PIPE-01.., Phase 16 / DOCS-01..DOCS-04 (D-22)"
    - "`PROMOTION.md`'s next-free line reads `0040`, and its dated advancing note names the authoring plan for each of 0037, 0038 and 0039 and states the disposition of Part B candidates 8 and 9 explicitly rather than leaving candidate 9 silent"
    - "`PROJECT.md`'s Key Decisions table carries one row per new ADR (0037, 0038, 0039)"
  artifacts:
    - .planning/ledgers/milestone-09-12.md
  key_links:
    - "ledger close-out amendment -> the `## Phase 10 close-out amendments` section of `milestone-07-08.md`, the shape it copies"
    - "the three hand-off blocks -> Phases 14, 15 and 16"
    - "`PROMOTION.md` advancing note -> ADR-0037, ADR-0038, ADR-0039 and the plan 13-09 checkpoint's candidate-9 disposition"
  prohibitions:
    - "No `.rs` file is modified by this plan, and the phase-wide assertion must return zero (D-19)"
    - "No requirement checkbox is flipped without the evidence that closes it named in the same edit"
    - "A failing `mdbook build` is neither attributed to this phase's three documentation edits nor reinterpreted as a pass"
    - "`PROMOTION.md`'s next-free line is advanced exactly once, to 0040, and no fourth ADR is authored here unless plan 13-09's checkpoint selected it (D-20)"
    - "Part B candidate 9's disposition is stated, never left silent"
    - "No new scope is absorbed; anything discovered goes to 13-CONTEXT.md's `<deferred>` register with a named owner"
    - "No claim resting on `cargo audit`, `cargo deny`, `cargo llvm-cov` or Docker is inferred as passing; each is scoped CI-only with its exact command (D-21)"
---

<objective>
Close Phase 13: prove the ledger is internally whole, prove the code-change boundary held, flip the
five requirements with their evidence, hand three explicit blocks forward, and advance the ADR
numbering index.

Purpose: this is the last ground-truth phase in the corpus. The hand-offs it writes are the final
forward-work signal Phases 14, 15 and 16 receive, and the boundary assertion is what makes "a records
phase changed no code" checkable instead of merely asserted.

Output: a dated close-out amendment section in the ledger, five closed requirements with three
hand-off blocks in `REQUIREMENTS.md`, `PROMOTION.md` at `0040` with a complete advancing note, and
three Key Decisions rows in `PROJECT.md`.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-CONTEXT.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-RESEARCH.md
@.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the dated close-out amendment to the ledger and run the phase gate</name>
  <files>.planning/ledgers/milestone-09-12.md</files>
  <read_first>
    - `.planning/ledgers/milestone-09-12.md` — the whole file as plans 13-01 through 13-07 left it, including the contention table and both highlight tables.
    - `.planning/ledgers/milestone-07-08.md` — its `## Phase 10 close-out amendments (2026-08-08)` section, the exact shape this one copies: integrity re-check, verdict distribution, cited-not-re-derived list, CI-only claims with their commands, phase gate.
    - `.planning/ledgers/milestone-01.md` — the stacked dated amendment convention: each section appended after the last, none rewriting another.
    - `.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-RESEARCH.md` §Environment Availability — the pre-phase `mdbook build` baseline and the list of tools that cannot run here.
  </read_first>
  <action>
Append `## Phase 13 close-out amendments (<today's date>)` at the **foot** of the ledger. Never
rewrite an epic section above it; this is the file's first amendment section and it establishes the
stacking convention for any future one.

**Whole-file integrity re-check**, each with its command and output recorded verbatim:
`grep -c '^| REQ-'` returns 120; the distinct-ID count returns 120; the `^### ` section-header count
returns 29; the interim markers `pending — plan` and `run-5 input (not yet re-derived)` both return
zero; no Verdict cell is blank; and every `ADR-NNNN` string cited anywhere in the ledger resolves to
a file that exists under `.planning/decisions/` — build that last check as a loop over the grepped
ADR numbers and record any that does not resolve.

**Per-class verdict distribution**, counted directly from the Verdict column in this session, not
assumed from the 35/53/32 input split. Present it as a table of verdict class against count, with
the counting command. Reconcile it against the head note's corrected arithmetic and against the two
highlight tables: the `Shipped, one acceptance criterion false` count must be 1 and the
`Verified open` count must match the highlight table's row list. If a count disagrees with an earlier
figure in the file, reconcile it in prose rather than leaving the discrepancy for a reader to find —
Phase 10's close-out did exactly this for its own 12-versus-13 supersession count.

**Rows cited rather than re-derived, and why** — principally the three SUPPLY closures, cited with
their provenance per D-06, and Phase 12's 87-hit stale-citation inventory inherited whole per D-07.
Name each and give its reason.

**Claims that are CI-only** — every claim this environment cannot measure, each with the exact
command a CI runner executes. `cargo audit`, `cargo deny`, `cargo llvm-cov` and anything Docker are
unrunnable here (crates.io returns HTTP 403, `docker` is absent); re-confirm that and record the
confirmation rather than repeating it from precedent (D-21).

**Phase gate**, recorded with commands and outputs:
- Determine the phase base SHA — the parent of this phase's first commit — and record it verbatim.
  Then run `git diff --name-only <base>..HEAD -- '*.rs' | wc -l` and record that it returns `0`
  (D-19). Any non-zero result is a boundary breach: stop and report rather than explaining it away.
- Run `mdbook build docs/` and record its exit code and full error list. Compare against the
  pre-phase baseline established 2026-08-10: exit `101`, a link-outside-root error at
  `deployment/docker.md:118`, and an incomplete reference link at `user-guides/tool-integration.md:324`
  — both pre-existing, both in files this phase does not touch, both post-dating the last successful
  `docs.yml` run of 2026-07-06. State that the failure predates Phase 13 and that this means
  `docs.yml`'s gate would fail today independently of this phase. Do not reinterpret it as a pass and
  do not attribute it to the three documentation edits. If a **new** error naming `sidecar.md`,
  `http-service-host.md` or `overview.md` appears, that one is this phase's — fix it and re-run.
- Record the workspace gate's disposition: a phase with a zero `.rs` diff cannot move coverage or
  clippy state, so `cargo test`, `cargo fmt --check`, `cargo clippy -- -D warnings` and ADR-0006's
  84% floor are recorded as unaffected **by reasoning from the measured zero diff**, with that
  reasoning stated, following Phase 10's precedent.

Close the section by stating that `milestone-09-12.md` is the fifth and final sibling and the series
is complete.
  </action>
  <verify>
    <automated>grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md && grep -c 'pending — plan' .planning/ledgers/milestone-09-12.md && git diff --name-only -- '*.rs' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md` → `120`
    - `grep -oE '^\| REQ-[a-z0-9-]+' .planning/ledgers/milestone-09-12.md | sort -u | wc -l` → `120`
    - `grep -c '^### ' .planning/ledgers/milestone-09-12.md` → `29`
    - `grep -c 'pending — plan' .planning/ledgers/milestone-09-12.md` → `0`
    - `grep -c 'run-5 input (not yet re-derived)' .planning/ledgers/milestone-09-12.md` → `0`
    - `grep -cE '^\| REQ-[a-z0-9-]+ \|\s*\|?\s*$' .planning/ledgers/milestone-09-12.md` → `0`
    - `for n in $(grep -oE 'ADR-[0-9]{4}' .planning/ledgers/milestone-09-12.md | sort -u | sed 's/ADR-//'); do ls .planning/decisions/$n-*.md >/dev/null 2>&1 || echo "MISSING $n"; done` → no output
    - `grep -c '^## Phase 13 close-out amendments' .planning/ledgers/milestone-09-12.md` → `1`
    - `git diff --name-only <base>..HEAD -- '*.rs' | wc -l` → `0`, with `<base>` recorded verbatim in the section
    - `mdbook build docs/ ; echo "exit=$?"` → `exit=101`, error list identical to the recorded baseline, with no error naming `sidecar.md`, `http-service-host.md` or `overview.md`
  </acceptance_criteria>
  <done>The ledger carries a dated close-out amendment with a passing integrity re-check, a counted verdict distribution reconciled against the head note, the cited-not-re-derived and CI-only lists, the zero-`.rs`-diff assertion with its base SHA, and an honest mdbook comparison.</done>
</task>

<task type="auto">
  <name>Task 2: Close ORCH-01..ORCH-05 and write the three forward hand-off blocks</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md:2204-2315` — ORCH-01 through ORCH-05 as plan 13-10's corrections left them; locate the five checkboxes by reading.
    - `.planning/REQUIREMENTS.md` — the five ORCH traceability rows, currently `Phase 13 | Pending`; locate them by grepping for `| ORCH-01 | Phase 13 |`.
    - `.planning/REQUIREMENTS.md:2084-2202` — the two inherited hand-off blocks from Phases 10 and 12; the exact shape the three new blocks copy, including the numbered-item structure and the "points at the evidence rather than repeating it" convention.
    - `.planning/ledgers/milestone-09-12.md` — the close-out amendment task 1 wrote and the `Verified open` highlight table; the hand-offs cite rows from it.
    - `.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-09-SUMMARY.md` — the checkpoint's recorded disposition of D-14, D-15 and Part B candidate 9.
  </read_first>
  <action>
Flip the five ORCH checkboxes from `[ ]` to `[x]`, each accompanied by the evidence that closes it,
recorded in the requirement's own block in the shape prior phases used:
ORCH-01 — the ledger exists at `.planning/ledgers/milestone-09-12.md` with 120 cited verdicts and
`REQUIREMENTS.md`'s section reduced to a pointer;
ORCH-02 — the five checkbox verdicts and the five-run pattern in the ledger head note, none converted
into a task;
ORCH-03 — ADR-0037, the `sidecar.md` correction and the four relocations corrected at source;
ORCH-04 — ADR-0038 and ADR-0039 with the two documentation corrections, ratified at plan 13-09's
blocking checkpoint on the recorded date and by the recorded mechanism (D-00i);
ORCH-05 — ADR-0029's four appended Trajectory rows and the ADR-0030 citation with the
provenance-key confirmation's recorded result.

Update the five traceability rows to `Phase 13 | Complete`.

Write three forward hand-off blocks in the shape of the Phase 10 and Phase 12 blocks — dated, naming
the authoring plan, pointing at evidence rather than repeating it (D-22):

**Hand-off to Phase 14 / WEB-01 … WEB-04.** The `Contract diverges` row for the JWT-vs-opaque token
mechanism (variant group 29, the only variant in five runs shipped code cannot settle), the
`Verified open` row for the multi-replica token problem against the shipped `k8s/deployment.yaml`,
and the `ProviderCapabilities` over-report and Epic 27 rows. ADR-0038's placement answer and its
stated cost if a non-HTTP consumer ever needs spec-driven provisioning. ADR-0039 as half of WEB-04's
required "stated relationship" between Arsenal/MCP and a future `LlmPort` tool surface. Cite each by
`REQ-*` ID and ADR number.

**Hand-off to Phase 15 / PIPE-01 … .** The measured 15-job `ci.yml` list replacing the stale 14, with
the grep that produced it and the note that PIPE-01's own text is now corrected at source. The
`scripts/check-api-surface.sh:6` finding — closed in the script, open in four Milestone 12 requirement
texts that already carry Phase 8's dated banners. The Deferred-QA Epic 25 rows with the
coverage-threshold variant recorded on both sides and unresolved. The eight deprecated GitHub Action
references with the note that run 5's line numbers for them are stale.

**Hand-off to Phase 16 / DOCS-01 … DOCS-04.** ORCH-02's verdict that Milestone 11's 26 open items are
the corpus's only genuine open count, that all fourteen target files exist so file existence settles
nothing, and that they are settleable only by content. D-13(d)'s split of the architecture document
into a closed relocation and an open rewrite, with the re-measured line count and the seven missing
subsystems. D-13(e)'s README and demos finding with the empty `docs/assets/` and absent
`docs/DEMOS.md`. The pre-existing `mdbook build` failure at `deployment/docker.md:118` and
`user-guides/tool-integration.md:324` — two broken links no phase owns yet, discovered by this phase's
research and unrelated to its edits.

State in the Phase 16 block that this is the **last** ground-truth phase, so these three blocks are
the corpus's final forward-work signal. If plan 13-12's provenance-key confirmation found a fifth
numbering collision, record it here too.
  </action>
  <verify>
    <automated>grep -cE '^- \[x\] \*\*ORCH-0[1-5]\*\*' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^- \[x\] \*\*ORCH-0[1-5]\*\*' .planning/REQUIREMENTS.md` → `5`
    - `grep -cE '^- \[ \] \*\*ORCH-0[1-5]\*\*' .planning/REQUIREMENTS.md` → `0`
    - `grep -cE '^\| ORCH-0[1-5] \| Phase 13 \| Complete \|' .planning/REQUIREMENTS.md` → `5`
    - `grep -cE '^\| ORCH-0[1-5] \| Phase 13 \| Pending \|' .planning/REQUIREMENTS.md` → `0`
    - `grep -cE '^#### Hand-off to Phase 1[456]' .planning/REQUIREMENTS.md` → `3`
    - `grep -c 'WEB-04' .planning/REQUIREMENTS.md` → `≥1` inside the Phase 14 block; `grep -c 'kubernetes-smoke' .planning/REQUIREMENTS.md` → `≥1`; `grep -c 'DOCS-02' .planning/REQUIREMENTS.md` → `≥1`
    - `grep -c 'tool-integration.md:324' .planning/REQUIREMENTS.md` → `≥1`
    - `grep -cE 'ADR-0037|ADR-0038|ADR-0039' .planning/REQUIREMENTS.md` → `≥3`
  </acceptance_criteria>
  <done>All five ORCH requirements are `[x]` with their closing evidence named, the five traceability rows read Complete, and three dated hand-off blocks name Phases 14, 15 and 16 with per-requirement citations.</done>
</task>

<task type="auto">
  <name>Task 3: Advance PROMOTION.md to 0040 with a complete advancing note, and add the PROJECT.md Key Decisions rows</name>
  <files>.planning/decisions/PROMOTION.md, .planning/PROJECT.md</files>
  <read_first>
    - `.planning/decisions/PROMOTION.md` — the `Next free ADR number` line, the advancing-note convention and its per-phase dated paragraphs, and the Part B candidate inventory including entries 8 and 9. Read Phase 12's plan 12-04 advancing note as the template, and Phase 11's note as the precedent for explicitly stating when an ADR does **not** close an inventory entry.
    - `.planning/PROJECT.md` — the `## Key Decisions` table and its column set.
    - `.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-09-SUMMARY.md` §Checkpoint Status — the human's disposition of Part B candidate 9.
    - `.planning/decisions/0037-agent-route-surface-v1.md`, `0038-agent-provisioner-placement.md`, `0039-http-topology-no-garrison-no-arsenal.md` — the three new ADRs, to confirm each exists before advancing the index.
  </read_first>
  <action>
Confirm all three new ADR files exist before touching the index. Then update `PROMOTION.md`'s
`Next free ADR number` line from `0037` to `0040` — exactly once, and only this line's state value.
Do not edit the Part A procedural prose that describes the step; Phase 12 recorded that editing it
produces a spurious grep-count mismatch, and the state line is the single source.

Add a dated advancing note in the file's established shape: three numbers advance, from 0036 to 0040;
plan 13-08 authored ADR-0037 (the agent route surface), plan 13-09 authored ADR-0038 (the
`AgentProvisioner` placement) and ADR-0039 (Garrison and Arsenal on the HTTP topology).

State both Part B dispositions explicitly:
- **Candidate 8** — the `AgentProvisioner` placement — is closed by ADR-0038.
- **Candidate 9** — the Milestone 9 Epic 4 agent/orchestrator bridge decision at that PRD's §6.1 — is
  dispositioned as the plan 13-09 checkpoint decided. If the human declined to promote it, record that
  it is **not** promoted this phase, that no ORCH requirement's `Derives` list reaches that PRD
  section, and name the owner its "Owner phase: Phase 13" assignment is redirected to. If the human
  selected promotion, record the ADR number it took and adjust the next-free line accordingly. Either
  way the disposition is stated — `PROMOTION.md`'s own convention is to say when an entry is not
  closed rather than leave a reader to notice the gap.

Note in the same paragraph that ORCH-01, ORCH-02 and ORCH-05 deliberately produced no ADR: a ledger,
a set of checkbox verdicts and an append to an existing trajectory table are not contested positions,
and ORCH-05 amended ADR-0029 and cited ADR-0030 (D-00g, D-16, D-17, D-20).

Then add one row per new ADR to `PROJECT.md`'s `## Key Decisions` table, matching its existing column
set and the way Phase 12's ADR-0036 row is written.
  </action>
  <verify>
    <automated>grep -n 'Next free ADR number' .planning/decisions/PROMOTION.md && grep -cE 'ADR-003[789]' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Next free ADR number: 0040' .planning/decisions/PROMOTION.md` → `1`
    - `grep -c 'Next free ADR number: 0037' .planning/decisions/PROMOTION.md` → `0`
    - `grep -cE '0037|0038|0039' .planning/decisions/PROMOTION.md` → `≥3`
    - `grep -ciE 'candidate 9' .planning/decisions/PROMOTION.md` → `≥1`
    - `grep -cE '13-08|13-09' .planning/decisions/PROMOTION.md` → `≥2` (the authoring plans are named)
    - `grep -cE 'ADR-0037|ADR-0038|ADR-0039' .planning/PROJECT.md` → `≥3`
    - `ls .planning/decisions/003[789]-*.md | wc -l` → `3`
    - `git diff --name-only -- '*.rs' | wc -l` → `0`
  </acceptance_criteria>
  <done>`PROMOTION.md` reads `0040`, its dated advancing note names the authoring plan for each ADR and states both Part B dispositions with candidate 9 explicit, and `PROJECT.md` carries a Key Decisions row for each of 0037, 0038 and 0039.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Modified by this plan (no new files):
- `.planning/ledgers/milestone-09-12.md` — `## Phase 13 close-out amendments (<date>)` appended at the foot
- `.planning/REQUIREMENTS.md` — five `[x]` ORCH checkboxes, five `Phase 13 | Complete` traceability rows, and three `#### Hand-off to Phase 14 / 15 / 16` blocks
- `.planning/decisions/PROMOTION.md` — next free `0040` plus a dated advancing note
- `.planning/PROJECT.md` — three Key Decisions rows

Identifiers a later reader will grep for, introduced here:
- `## Phase 13 close-out amendments`
- `#### Hand-off to Phase 14 / WEB-01`, `#### Hand-off to Phase 15 / PIPE-01`, `#### Hand-off to Phase 16 / DOCS-01`
- `Next free ADR number: 0040`
- The recorded phase base SHA used by the `git diff --name-only <base>..HEAD -- '*.rs'` assertion

Full phase artifact set (all thirteen plans):
- `.planning/ledgers/milestone-09-12.md`
- `.planning/decisions/0037-agent-route-surface-v1.md`
- `.planning/decisions/0038-agent-provisioner-placement.md`
- `.planning/decisions/0039-http-topology-no-garrison-no-arsenal.md`
- New ledger verdict-class usage: `Shipped, one acceptance criterion false` (one row), `Verified open` (the Deferred-QA block), and `Verify` recorded retired
- Ledger head-note section names: `Evidence bar`, `Manifest carve-out`, `Corrected arithmetic (D-04)`, `Per-milestone checkbox corroboration`, `The five-run pattern`, `Inherited stale-citation inventory (D-07)`, `Provenance`, `Verdict legend and the seven-class mapping`, `Shipped, one acceptance criterion false — the single instance`, `Verified open — the Deferred-QA block`, `Row order and amendment convention`, `Ledger-file contention`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase work -> closed requirement | a `[x]` is what a later reader treats as settled, and nothing re-opens it |
| this phase -> Phases 14, 15, 16 | the three hand-off blocks are the corpus's final forward-work signal |
| claimed boundary -> measured boundary | "this phase changed no code" must be a command, not a statement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-37 | Repudiation | flipping a checkbox without evidence | high | mitigate | Each of the five flips names the artefact that closes it in the same edit, and the ledger's integrity re-check must pass first; ORCH-04's flip additionally records the checkpoint date and how the decision was obtained (D-00i) |
| T-13-38 | Tampering | an unverifiable D-19 boundary claim | high | mitigate | The base SHA is recorded verbatim and `git diff --name-only <base>..HEAD -- '*.rs' | wc -l` must return `0`; a non-zero result stops the task rather than being explained away |
| T-13-10 | Repudiation | the `mdbook build` result | high | mitigate | Compared error-for-error against the recorded pre-phase baseline; a new error naming any of the three edited pages fails the criteria, and the pre-existing failure is stated as pre-existing with its two file paths |
| T-13-27 | Repudiation | Part B candidate 9 left silent | high | mitigate | The advancing note must contain a candidate-9 disposition; the acceptance criteria grep for it |
| T-13-39 | Spoofing | a ledger row citing an ADR that does not exist | medium | mitigate | The integrity re-check resolves every `ADR-NNNN` string in the ledger against `.planning/decisions/` and reports any that does not |
| T-13-40 | Tampering | inferring an unrunnable gate as passing | high | mitigate | D-21: `cargo audit`, `cargo deny`, `cargo llvm-cov` and Docker claims are scoped CI-only with their exact commands; the workspace gate is recorded as unaffected **by reasoning from the measured zero diff**, with the reasoning stated |
| T-13-41 | Elevation of privilege | absorbing new scope at close-out | medium | mitigate | Anything discovered goes to the `<deferred>` register with a named owner; no plan grows here |
| T-13-SC | Tampering | package installs | low | accept | Only the pre-cached, `docs.yml`-pinned mdbook toolchain may be installed with `--offline`; no manifest is modified and the `.rs` diff assertion is zero (D-19) |
</threat_model>

<verification>
- `grep -c '^| REQ-' .planning/ledgers/milestone-09-12.md` → `120`; distinct IDs → `120`; `^### ` → `29`
- `grep -c 'pending — plan' .planning/ledgers/milestone-09-12.md` → `0`
- `grep -cE '^- \[x\] \*\*ORCH-0[1-5]\*\*' .planning/REQUIREMENTS.md` → `5`
- `grep -cE '^\| ORCH-0[1-5] \| Phase 13 \| Complete \|' .planning/REQUIREMENTS.md` → `5`
- `grep -c 'Next free ADR number: 0040' .planning/decisions/PROMOTION.md` → `1`
- `git diff --name-only <base>..HEAD -- '*.rs' | wc -l` → `0`
- `git diff --name-only <base>..HEAD -- 'docs/*'` → exactly `sidecar.md`, `http-service-host.md`, `overview.md`
</verification>

<success_criteria>
The ledger is internally whole and says so with commands; the code-change boundary is measured at
zero; all five ORCH requirements are closed with evidence; Phases 14, 15 and 16 have explicit,
cited hand-offs; the ADR index is at 0040 with no disposition left silent.
</success_criteria>

<output>
Create `.planning/phases/13-milestone-9-12-ground-truth-recorded-account/13-13-SUMMARY.md` when done.
</output>
