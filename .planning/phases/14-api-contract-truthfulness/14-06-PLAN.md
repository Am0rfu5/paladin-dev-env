---
phase: 14-api-contract-truthfulness
plan: 06
type: execute
wave: 2
depends_on: ["14-02", "14-03"]
files_modified:
  - .planning/decisions/0042-llm-native-tool-calling-deferred.md
  - .project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md
autonomous: true
requirements: [WEB-04]

must_haves:
  truths:
    - "Asking whether Paladin supports LLM-native tool calling returns one recorded answer with its reasoning — ADR-0042, recording it as a future capability improvement with a named reintroduction trigger and an owner, not built (D-10, D-16)"
    - "The next reader of Deferred-QA Epic 27 meets the decision rather than an unbuilt epic: each of the five Epic 27 sites D-11 names carries or is directly reachable from a dated correction banner pointing at ADR-0042 (D-11)"
    - "The correction is annotation, not rewriting: every line of original Epic 27 text is retained and marked superseded where it is now false, and no line is deleted or renumbered (D-00c)"
    - "WEB-04's closure is not a fourth deferred-register entry — no new register row, no new deferred-items file, no fifth mention (D-11)"
    - "Both of Epic 27's open questions are recorded as still unanswered and named as part of the reason the capability is not built now, rather than silently inherited (D-10)"
    - "ADR-0042 states the Arsenal relationship the requirement asks for, pairing with ADR-0039's already-recorded HTTP half: Arsenal/MCP provides tool execution through a working seam, and what is deferred is the LLM-initiated entry into it"
    - "The ADR numbers land in sequence and do not collide: 0040 and 0041 are authored by plan 14-05, 0042 here, and PROMOTION.md's next-free line advances once, in plan 14-07"
    - statement: "Applying the D-11 correction banner a second time does not duplicate it — the Epic 27 sites carry exactly one dated 2026 Phase-14 banner block each, and a re-run detects the existing marker instead of appending another"
      verification: backstop
  artifacts:
    - path: ".planning/decisions/0042-llm-native-tool-calling-deferred.md"
      provides: "The WEB-04 decision record: LLM-native tool calling deferred with a named trigger and an owner"
      contains: "## Downstream Consumers"
    - path: ".project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md"
      provides: "Dated correction banner on the Epic 27 source, pointing every defective claim at ADR-0042"
      contains: "ADR-0042"
  key_links:
    - from: ".project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md"
      to: ".planning/decisions/0042-llm-native-tool-calling-deferred.md"
      via: "the dated banner names what was wrong and points at the decision record"
      pattern: "ADR-0042"
    - from: ".planning/decisions/0042-llm-native-tool-calling-deferred.md"
      to: ".planning/decisions/0039-http-topology-no-garrison-no-arsenal.md"
      via: "the Arsenal relationship the requirement asks for, whose HTTP half ADR-0039 already records"
      pattern: "ADR-0039"
  prohibitions:
    - requirement_id: "WEB-04"
      category: "transparency"
      statement: "MUST NOT delete, rewrite, renumber or reflow any original Epic 27 text — the correction is additive annotation with the superseded text retained and marked, so a future reader can still see exactly what the source claimed and judge the correction against it"
---

<objective>
Record WEB-04's answer once, in the one place that changes what the next reader meets.

D-10 sets the frame from the user's own words: the deferred register is a source of future version
improvements, not a description of current functionality — and every surface describing current
functionality must actually describe it. Plans 14-02 and 14-03 did the second half in code and docs.
This plan does the first: **ADR-0042** records LLM-native tool calling (Deferred-QA Epic 27) as a
future capability improvement with a named reintroduction trigger and an owner, not built.

D-11 is the part that makes this different from the three previous times Epic 27 was deferred.
WEB-04's own text warns it is "not done by leaving it as a deferred register entry for a fourth
time" — so the record is paired with a **dated correction banner on the Epic 27 source**, applied at
the five sites D-11 names, so the next reader of that epic meets the decision instead of an unbuilt
plan. The banner annotates; it never rewrites (D-00c). The document already carries a 2026-08-10
correction banner from Phase 13, and that one stays exactly as it is.

Purpose: convert the corpus's most-deferred item from an open plan into a closed decision with a
trigger.
Output: one ADR and one annotated source document.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-api-contract-truthfulness/14-CONTEXT.md
@.planning/phases/14-api-contract-truthfulness/14-02-SUMMARY.md
@.planning/phases/14-api-contract-truthfulness/14-03-SUMMARY.md
</context>

## Artifacts this phase produces

| Kind | Path |
|---|---|
| ADR | `.planning/decisions/0042-llm-native-tool-calling-deferred.md` |
| annotation | new dated Phase-14 banner blocks inside `.project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md` (the file exists; the blocks do not) |

`.planning/decisions/PROMOTION.md` is advanced once, by plan 14-07. Do not edit it here.
Plan 14-05 authors ADR-0040 and ADR-0041 against the adjacent reserved numbers; this plan must use
`0042` exactly so the three do not collide.

## ADR file shape (D-00a — matches 0001 through 0039)

No frontmatter. First line `# ADR-0042: <title>`. Sections in order: `## Status` · `## Context` ·
`## Decision` · `## Considered Options` · `## Code Locations` · `## Code Conformance` ·
`## Downstream Consumers`.

**Parser constraint carried forward from Phase 12 (plan 12-03):** write every bullet under
`## Code Locations` and `## Considered Options` as a **single physical line**; the repo's ADR parser
splits those two sections per physical line and a wrapped bullet inflates the count.

<tasks>

<task type="auto">
  <name>Task 1: ADR-0042 — LLM-native tool calling recorded as a future capability, with a trigger and an owner</name>
  <read_first>
    - `.planning/decisions/0035-paladin-ml-leaf-crate-placement.md` — the deferred-with-trigger shape D-10 and D-16 follow: a reintroduction condition promoted into a decision record without building the thing
    - `.planning/decisions/0039-http-topology-no-garrison-no-arsenal.md` — the section shape, and the already-recorded HTTP half of the Arsenal relationship this record must pair with (D-00j)
    - `.project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md` — Epic 27's user stories and functional requirements, the breaking-change/phased-approach risk note, the epic-priority ordering table, and the two open questions (DeepSeek API support; whether the canonical tool schema is a vendor's or a provider-agnostic one)
    - `.planning/phases/14-api-contract-truthfulness/14-CONTEXT.md` — D-10's verbatim user framing in `<specifics>` finding 5, plus D-11 and D-16
    - `.planning/phases/14-api-contract-truthfulness/14-02-SUMMARY.md` and `14-03-SUMMARY.md` — what already landed on the current-functionality side, so the record cites shipped fact rather than intent
    - `crates/paladin-ports/src/output/llm_port.rs` — the request type with no tool-definition field, and the response field no shipped adapter populates
    - `crates/paladin-llm/src/mock.rs` — read to confirm it already declares both capability flags false and emits no function call, since the record notes the option of changing that as a possible future step
  </read_first>
  <files>.planning/decisions/0042-llm-native-tool-calling-deferred.md</files>
  <action>
Author ADR-0042 in the shape above.

Status: `Accepted`, dated the day the plan executes.

Context: state what Epic 27 proposes and what the tree actually contains, each fact with the command
or `file:line` that produced it, re-verified this session. Record that the epic is entirely unbuilt:
the request type carries no field a tool definition could travel through; no tool-definition or
tool-call type exists in either the ports or the adapter crate; and every producer of a populated
function call in the workspace is a test double under `tests/`. Record the cost the epic's own PRD
flags — it modifies the `LlmPort` trait, which is a breaking change to a published port interface
requiring all three shipped adapters and the mock to change together. Record that both of Epic 27's
open questions are still unanswered, naming each: whether DeepSeek's API supports tool calling at all,
and whether a vendor's JSON Schema is canonical or a provider-agnostic schema is needed. Record the
Arsenal relationship WEB-04 asks for, pairing with ADR-0039's already-recorded half (D-00j): Arsenal
and MCP already provide tool execution through a working seam, ADR-0039 records the absence of Arsenal
on HTTP-served agents as a permanent property of that topology, and this record supplies the other
half — what is missing is not tool execution but the LLM-initiated entry into it.

Decision (D-10): LLM-native tool calling is recorded as a **future capability improvement, not built**.
Quote the user's framing verbatim from `<specifics>` finding 5 as the recorded rationale — it is the
frame for the whole phase, not just a verdict on this epic, and paraphrasing it loses the second
instruction it carries. Give the decision a **named reintroduction trigger** and an **owner**, both of
which D-10 requires: the trigger is the point at which a consumer needs a shipped adapter to initiate
a tool call rather than supplying their own `LlmPort` implementation, conditioned on both open
questions being answered first, since the schema question determines the type design and the DeepSeek
question determines the scope. Name the owning surface for the future work concretely (the `LlmPort`
trait in `paladin-ports` plus the three adapters and the mock in `paladin-llm`). State explicitly that
nothing is deleted — the trigger is what brings it back — so this is a deferral, not a withdrawal, and
not the permanent-property treatment ADR-0039 gives a different question.

Note as a possible future step, without planning it, the option CONTEXT.md raised and left to
discretion: letting the bundled mock emit a function call so the tool path is demonstrable without a
custom adapter. Record the decision taken for this phase — the mock is left unchanged because it
already declares both flags false and emits none, so it is already truthful and needs no correction.

Considered Options, as single-physical-line bullets: build Epic 27 now following the PRD's own four-step
phased approach (rejected — a breaking change across three adapters and the mock, with two unanswered
open questions that determine the type design); withdraw the capability entirely (rejected — the user
explicitly wants it recorded as a future improvement, not deleted); record it as deferred with a named
trigger and an owner (chosen, on the ADR-0035 precedent); add a fourth entry to a deferred register
(rejected under D-11 — WEB-04's own text names that as the failure mode).

Code Locations, as single-physical-line bullets: the port file WEB-03 and WEB-04 both act on; the
reachability rustdoc plan 14-03 added; the four documentation pages plan 14-03 corrected; the
correspondence test plan 14-02 extended; the reasoning-loop tool branch and the specialist-handoff path
in the execution service; and the Epic 27 sites in the source PRD that Task 2 annotates. Re-derive every
line number against the post-14-02/14-03 tree.

Code Conformance: `conforms` — no code change is made by this record; the current-functionality
corrections that make the tree match it were landed by plans 14-02 (the capability flag and its
correspondence test) and 14-03 (the reachability rustdoc and the four pages), with their commands
recorded verbatim.

Downstream Consumers: the `REQ-llm-tool-calling-port` and `REQ-llm-tool-calling-adapters` ledger rows
plan 14-07 amends; the Epic 27 sites Task 2 annotates; ADR-0039, whose Arsenal half this record
completes; and any future phase picking up the trigger.
  </action>
  <verify>
    <automated>test -f .planning/decisions/0042-llm-native-tool-calling-deferred.md &amp;&amp; head -1 .planning/decisions/0042-llm-native-tool-calling-deferred.md | grep -q '^# ADR-0042:' &amp;&amp; for s in "Status" "Context" "Decision" "Considered Options" "Code Locations" "Code Conformance" "Downstream Consumers"; do grep -q "^## $s\$" .planning/decisions/0042-llm-native-tool-calling-deferred.md || exit 1; done &amp;&amp; grep -q 'ADR-0039' .planning/decisions/0042-llm-native-tool-calling-deferred.md</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 .planning/decisions/0042-llm-native-tool-calling-deferred.md` matches `^# ADR-0042:`
    - `grep -c '^## ' .planning/decisions/0042-llm-native-tool-calling-deferred.md` is 7 and the file has no YAML frontmatter (D-00a)
    - The Decision section quotes the user's framing verbatim as a block quote, not paraphrased
    - The Decision section names both a reintroduction trigger and an owner: `grep -ci 'trigger' .planning/decisions/0042-llm-native-tool-calling-deferred.md` is at least 1 and `grep -ci 'owner' .planning/decisions/0042-llm-native-tool-calling-deferred.md` is at least 1
    - Both open questions are recorded as unanswered, each named by its subject (DeepSeek API support; canonical versus provider-agnostic tool schema)
    - `grep -c 'ADR-0039' .planning/decisions/0042-llm-native-tool-calling-deferred.md` is at least 1 — the Arsenal relationship pairs with the recorded HTTP half
    - `grep -c 'ADR-0035' .planning/decisions/0042-llm-native-tool-calling-deferred.md` is at least 1 — the deferred-with-trigger precedent is cited
    - The record states the mock is left unchanged and why, so the discretion item CONTEXT.md raised is closed rather than dropped
    - Every bullet under `## Code Locations` and `## Considered Options` occupies exactly one physical line — the ADR parser self-check reports counts equal to the literal `- ` bullet counts in those sections
    - Every file path cited in `## Code Locations` resolves: each path extracted from that section passes `test -e`
  </acceptance_criteria>
  <done>ADR-0042 records one decision — LLM-native tool calling deferred as a future capability with a named trigger and an owner — quoting the user's framing verbatim, pairing with ADR-0039 on the Arsenal relationship, and citing only paths that resolve (D-10, D-16).</done>
</task>

<task type="auto">
  <name>Task 2: Dated correction banner on the Deferred-QA Epic 27 source</name>
  <read_first>
    - `.project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md` — read in full enough to locate all five sites D-11 names and to read the **existing 2026-08-10 correction banner at the top of the document**, which is the in-repo precedent for the banner's voice, its dated-heading form, its lettered sub-points and its retain-the-original discipline
    - `.planning/phases/14-api-contract-truthfulness/14-CONTEXT.md` — D-11's site list and D-00c's annotation rule
    - `.planning/decisions/0042-llm-native-tool-calling-deferred.md` — the record just written, so the banner cites its exact filename and title
  </read_first>
  <files>.project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md</files>
  <action>
Annotate the Epic 27 source per D-00c and D-11: a dated correction banner naming what was wrong and
pointing at ADR-0042, with each defective claim corrected inline, the original text retained and marked
superseded.

Add a **new, separately dated Phase-14 banner** rather than editing the existing 2026-08-10
ORCH-03 banner at the top of the document — that one records a different correction and stays
byte-identical. Follow its form: a block-quoted heading naming the date and the owning requirement,
then the correction body. The new top-level banner states the single fact a reader needs before
reading any of Epic 27: the epic is **recorded as a future capability improvement, not current
functionality and not scheduled work**, by ADR-0042, and the deferred register it lives in is a source
of future improvements rather than a description of what ships today.

Then annotate each of the five sites D-11 names, inline and in place, each pointing at ADR-0042:

1. The Epic 27 user-story block — note that neither story describes current functionality.
2. The Epic 27 functional-requirements section — note that the whole section is unbuilt and is now a
   recorded future capability, and that the flag-honesty half of the original problem statement was
   separately closed (the adapters' declared capability now matches their behaviour, pinned by a
   correspondence test).
3. The breaking-change and phased-approach risk note — note that the four-step approach it proposes is
   preserved as the shape the future work would take, and that the breaking-change cost it flags is
   part of the recorded reason for deferring.
4. The epic-priority ordering row that places this epic second — note that the ordering is superseded
   for this epic specifically, and that the ordering of the remaining epics is untouched.
5. Both open questions — mark each as still Open and record that their being unanswered is part of the
   deferral reasoning, so a future reader knows answering them is the precondition, not an afterthought.

Discipline (D-00c, and the prohibition in this plan's `must_haves`): every annotation is **additive**.
Retain every line of original text; mark superseded claims as superseded rather than editing them;
delete nothing; renumber nothing; do not reflow surrounding paragraphs, because a reflow makes the
retain-the-original guarantee unverifiable in the diff. Create **no** new deferred-register row, no new
deferred-items file, and no fifth mention anywhere — the banner plus ADR-0042 is the whole record
(D-11).

Make the annotation idempotent: give each inserted block a distinctive dated marker string, and before
inserting, check whether that marker is already present at the site. A second run of this task must
leave the document unchanged rather than stacking a duplicate banner.
  </action>
  <verify>
    <automated>grep -c 'ADR-0042' .project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md | grep -qE '^([5-9]|[1-9][0-9]+)$' &amp;&amp; grep -q 'ORCH-03' .project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md &amp;&amp; git diff --numstat -- .project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md | awk '{ if ($2 != 0) { print "deleted lines: " $2; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ADR-0042' .project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md` is at least 5 — one pointer per D-11 site, plus the top-level banner
    - `git diff --numstat -- .project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md` reports **0 deleted lines** — the annotation is purely additive, proving no original text was removed or reflowed (D-00c)
    - The pre-existing Phase 13 banner is untouched: `grep -c 'ORCH-03' .project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md` is unchanged from its pre-task value, and the region containing it appears in the diff only as context
    - The new banner carries a Phase-14 date distinct from the existing banner's, and names WEB-04 as the owning requirement
    - Each of the five sites carries an annotation: the user-story block, the functional-requirements section, the breaking-change/phased-approach note, the epic-priority row, and both open questions
    - Both open-question rows still read `Open` and are annotated rather than closed — answering them is recorded as the precondition for the trigger
    - No new deferred-register row or file was created: `git status --porcelain .project/` lists only the modified PRD and no new file, and `git diff -- .project/` adds no new row to any priority or register table other than the annotations themselves
    - The inserted blocks are idempotent: re-running the task's insertion logic detects the dated marker and leaves `git diff --exit-code` clean on a second pass
  </acceptance_criteria>
  <done>The next reader of Epic 27 meets the recorded decision at every one of the five sites, the original text is fully retained with zero deleted lines, the Phase 13 banner is untouched, and no fourth register entry was created (D-00c, D-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ingested source document -> future planner | `.project/**` sits below ADRs and shipped code in the precedence order, but is what a future planner reads to size unbuilt work; an unannotated Epic 27 invites re-planning already-decided scope |
| Decision record -> future implementer | ADR-0042 outranks the source PRD, so what it records about the tree becomes the working truth for later phases |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-22 | Tampering | `.project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md` | medium | mitigate | A correction that silently rewrites source text destroys the audit trail this corpus depends on and makes the correction itself unauditable. The acceptance criteria require zero deleted lines in the diff, which mechanically proves the annotation was additive (D-00c) |
| T-14-23 | Repudiation | ADR-0042's recorded rationale | medium | mitigate | The user's framing is the load-bearing input to this decision; paraphrasing it would let a later reader attribute a position the user did not take. The record must quote it verbatim as a block quote, and the acceptance criteria check for that rather than for a summary |
| T-14-24 | Spoofing | ADR numbering collision | low | mitigate | Three records are authored against reserved numbers across two plans in different waves while `PROMOTION.md` still reads 0040 as next-free. This plan pins `0042` in both the filename and the heading, and plan 14-07 advances the next-free line exactly once |
| T-14-25 | Information Disclosure | banner and ADR content | low | accept | Both artefacts discuss capability scope and type design; neither quotes a credential, an endpoint or a key name |
| T-14-SC | Tampering | cargo dependency graph | low | accept | Documentation-only plan; no manifest is touched and no package is installed. Recording a deferral explicitly avoids adding the tool-schema dependencies Epic 27 would require |
</threat_model>

<verification>
- `.planning/decisions/0042-llm-native-tool-calling-deferred.md` exists with all seven mandated sections, no frontmatter, and `# ADR-0042:` as its first line
- Every path cited in the ADR's `## Code Locations` resolves on disk
- `git diff --numstat -- .project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md` reports 0 deleted lines
- `git status --porcelain` for this plan lists exactly one new file (the ADR) and one modified file (the PRD)
- `git diff --exit-code -- .planning/decisions/PROMOTION.md` is clean (14-07 owns that file)
</verification>

<success_criteria>
One decision record answers "does Paladin support LLM tool calling?" with reasoning, a verbatim user
framing, a named trigger and an owner; the Epic 27 source carries a dated additive banner at five
sites pointing at that record with zero deleted lines; the Phase 13 banner is untouched; and no fourth
deferred-register entry exists anywhere.
</success_criteria>

<output>
Create `.planning/phases/14-api-contract-truthfulness/14-06-SUMMARY.md` when done. Record the
`git diff --numstat` output proving zero deleted lines, the ADR parser self-check, and the
`test -e` sweep over every path cited in `## Code Locations`.
</output>
