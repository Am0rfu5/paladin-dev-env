---
phase: 15-coverage-ci-quality-gates
plan: 10
type: execute
wave: 8
depends_on: [15-04, 15-07, 15-09]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ledgers/milestone-09-12.md
  - .planning/ROADMAP.md
  - .project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md
  - .project/Deferred-QA-CICD-Completion/Epic_25/prd-cicd-pipeline-enhancement.md
  - .project/Deferred-QA-CICD-Completion/DEFERRED_COVERAGE.md
autonomous: true
requirements: [PIPE-01, PIPE-02, PIPE-03, PIPE-04, PIPE-05, DEFER-01, DEFER-02, DEFER-03]

must_haves:
  truths:
    - "Every false premise this phase found in a requirement text is corrected at its source, dated, with the original text retained and marked superseded — never in a separate corrections file."
    - "Each of DEFER-01's five named mocks carries a recorded verdict — built, replaced by a named alternative, or unnecessary for a named reason — and none is silently dropped."
    - "Epic 25's Open Question 3 and the parent PRD's Open Question 3 are both recorded as answered, with the answer and its source named, rather than left Open."
    - "The two Claude's-Discretion items this phase declined — the native-arm64 CI rework and a second feature-scoped coverage measurement — carry a recorded decline with a reason and an owner, rather than being absent."
    - "ROADMAP's Phase 15 success criteria that rest on superseded figures are amended at source with the original text retained, so the criteria can be judged true or false against what actually shipped."
    - "No requirement is marked complete without the exact command or file:line that proves it, recorded verbatim."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "the eight requirement texts, amended at source and checked off against evidence"
    - path: ".planning/ledgers/milestone-09-12.md"
      provides: "the ten ledger rows, amended in place with closure evidence"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/decisions/0006-coverage-gate.md"
      via: "PIPE-02's settled threshold cites the ADR amendment rather than restating a number"
      pattern: "ADR-0006"
    - from: ".planning/ledgers/milestone-09-12.md"
      to: ".planning/phases/15-coverage-ci-quality-gates"
      via: "each closed row cites the plan summary and the command that proved it"
      pattern: "15-0"
---

<objective>
Make the record true. This phase found eleven false premises in the documents it was planned from,
and left decisions, verdicts and declines scattered across nine plan summaries. This plan lands them
at source.

Everything here follows the house conventions for corrections. Requirement and ledger texts are
**amended in place**, dated, with superseded text retained and marked — never a separate corrections
file (D-00d). Source documents under `.project/` are **annotated, not rewritten**: a dated correction
banner naming what was wrong and pointing at the ADR or requirement, with each defective claim
corrected inline and the original retained (D-00c). No claim of closure is recorded without the exact
command or `file:line` that produced it, verbatim (D-00e). Outstanding items nest under the
requirement they belong to rather than getting invented identifiers (D-00f).

Purpose: the next phase reads a record that matches the tree.
Output: eight amended requirements, ten amended ledger rows, corrected ROADMAP criteria, and three
annotated source documents.

**Executor note:** `.planning/config.json` sets `workflow.worktree_skip_hooks: true`. Honor it.

**Re-grep everything.** This phase's own opening finding was that line numbers, counts and paths
across this requirement set are stale. Every citation you write must be re-verified against the tree
at execution time, not copied from a planning document.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-coverage-ci-quality-gates/15-CONTEXT.md
@.planning/phases/15-coverage-ci-quality-gates/15-02-SUMMARY.md
@.planning/phases/15-coverage-ci-quality-gates/15-03-SUMMARY.md
@.planning/phases/15-coverage-ci-quality-gates/15-05-SUMMARY.md
@.planning/phases/15-coverage-ci-quality-gates/15-07-SUMMARY.md
@.planning/phases/15-coverage-ci-quality-gates/15-09-SUMMARY.md
</context>

<!-- planner-discipline-allow: Owner: Phase 15 / PIPE -->

<tasks>

<task type="auto">
  <name>Task 1: Amend the eight requirement texts at source</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` around lines 2780-3005 — the full PIPE-01 through PIPE-05 and DEFER-01 through DEFER-03 texts, including the dated correction banners plan 13-10 already added to PIPE-01, so this pass matches their format rather than inventing a second one.
    - `.planning/REQUIREMENTS.md` around line 2528 — the "Hand-off to Phase 15 / PIPE-01" block and its five numbered items, each of which needs a disposition.
    - All five referenced plan summaries — the figures, commands, verdicts and findings this task transcribes.
    - `.planning/decisions/0006-coverage-gate.md` § Phase 15 amendment — the settled threshold, which PIPE-02 cites rather than restates.
    - `.planning/decisions/0034-d1-d4-facade-relocation-disposition.md` § "(ii) D2" — the withdrawn split, which DEFER-02's collision text must stop describing as live.
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Amend each of the eight requirement texts in place, dated, with the original retained and marked
    superseded in the format plan 13-10's PIPE-01 banner already established. Then check the box only
    where the evidence in the summaries supports it, citing the exact command or `file:line`.

    **PIPE-01.** The "43 total" CLI snapshot figure is stale: re-count at execution time
    (`ls tests/cli/snapshots | wc -l` and `grep -c '#\[test\]' tests/cli/*.rs` summed) and record the
    real counts — the last measurement found 86 snapshot files and 97 test functions across seven
    files, against a requirement text and a ledger row that both say 43 because they counted four
    files. Record that `cargo test --test cli` as written compiles nothing: `required-features = ["cli"]`
    makes Cargo skip the target silently, which is why the suite never ran in CI, and the job passes
    `--features cli`. Cite the shipped `cli-tests` and `bench-check` job ids.

    **PIPE-02.** Record the threshold as settled, citing the ADR-0006 Phase 15 amendment rather than
    restating the number — one number, one home. Record that `--all-features` was rejected in favour
    of `--features integration-tests` and why. Record that the gate is
    `cargo llvm-cov --fail-under-lines` in the workflow and that Codecov reports without gating, and
    that the `taiki-e/install-action` pin named in the original text (`cargo-llvm-cov@0.7.1`) was
    stale and was replaced with the version verified at execution time. Record **Epic 25's Open
    Question 3 as answered**: the `integration-tests.yml` coverage step was removed, matching the
    parent PRD's own recommendation. Record the two outward couplings the ROADMAP names — that
    PIPE-02's threshold lands on ADR-0006's single binding number, and that the CI figure differs from
    RECON-07's and VERIFY-05's recorded figures **by scope**, with that difference stated rather than
    left to be discovered.

    **PIPE-03.** Correct the two flag references — the targets carry `--features integration-tests`,
    not `--all-features`, and `test-cli` carries `--features cli` without which it runs nothing.
    Record the five shipped targets and the extended `ci-test`.

    **PIPE-04.** Correct all three defects: the cited line numbers were stale in every case (record
    the real ones from 15-02's summary, and note that any line number written here is stale the moment
    the file next changes); the count of references to *upgrade* is **seven**, not eight, because D-03
    removed one by deletion rather than upgrade; and "all three workflows" is wrong — six exist and
    all six are linted, by a standing `actionlint` job rather than a one-time check. Record that
    `codecov/codecov-action` was deleted rather than upgraded to v4 as the original text directs, and
    that a v5 upload landed in the new coverage job instead.

    **PIPE-05.** Correct the `CONTRIBUTING.md` premise as **relocated-by-outcome** — the file does not
    exist; Milestone 11 relocated contributor documentation into the mdbook, and the section landed in
    `docs/src/contributing/testing-guide.md`. Correct the second premise too: the `cargo tarpaulin`
    references the requirement says to update are not in contributor documentation at all; the only
    ones in the tree were in `.planning/codebase/TESTING.md`, which ADR-0006 already flagged. Record
    the "done when" outcome from plan 15-04's human-verify checkpoint, including the two figures and
    whether they agreed.

    **DEFER-01.** Correct the `tests/common/` placement as **stale-by-structure**, not merely
    stale-by-path: both coverage targets test from inside `src/` via co-located `#[cfg(test)]` modules,
    and `tests/` is a separate crate `src/` cannot import from, so that placement cannot serve the two
    requirements this one exists to unblock. Record that Open Question 2 is **answered explicitly**:
    hand-written mocks, `mockall` not adopted, with the reason and the named reintroduction trigger.
    Then record the **five-name verdict table** D-10 requires — one row per name, each reading built,
    replaced by a named alternative, or unnecessary for a named reason, written from what plans 15-06
    through 15-09 actually consumed. Note in particular that the ledger's existing row already
    corrects the register on one of the five, recording `MockLogPort` as present-but-unshared at two
    module-local sites rather than absent; reconcile the verdict table with that finding rather than
    contradicting it.

    **DEFER-02.** Correct the recorded profile: the file ships at 583 lines, not 488, and the ~4.23%
    coverage figure is stale — cite the measured entry and exit figures from 15-07's summary with
    their exact command and scope, and state that they are not comparable with the ADR-0006 gate
    figure. Correct the collision text: it describes a live sequencing question that **ADR-0034
    dissolved** — the split is withdrawn, owned by nobody; the full relocation is owned by the run-3
    v2 tech-debt item; the tests are this requirement. Record every observed-versus-assumed difference
    15-06 and 15-07 found, and any production finding they surfaced (a race, an unimplemented path the
    register names) as a named item with an owner.

    **DEFER-03.** Correct both stale halves: the path is
    `src/application/services/orchestration/listener.rs`, not
    `src/core/platform/manager/listener_service.rs`; and the 57.83% baseline dated 2026-02-14 is
    superseded by the entry figure 15-08 measured, with the delta framed as a re-measurement across a
    relocation and four months of added tests. Record the exit figure, the re-derived effort (in plans
    and tasks, not the inherited 20-25 h), and every justification entry. Record any production
    finding 15-09's concurrency suite surfaced as a named item with an owner.

    **The Phase 15 hand-off block** (around line 2528). Give each of its five numbered items a
    disposition: the corrected 15-job `ci.yml` list (now 19 with this phase's four additions — re-count
    and record), the `check-api-surface.sh` closure, the threshold variant (settled by ADR-0006), the
    stale action line numbers (corrected here), and the mock prerequisite with ADR-0034's resolution.
  </action>
  <verify>
    <automated>bash -c 'S=$(ls tests/cli/snapshots | wc -l); T=$(grep -c "#\[test\]" tests/cli/*.rs | awk -F: "{s+=\$2} END {print s}"); echo "snapshots=$S tests=$T"; grep -q "$S" .planning/REQUIREMENTS.md &amp;&amp; grep -q "$T" .planning/REQUIREMENTS.md &amp;&amp; echo "real counts present in REQUIREMENTS.md"'</automated>
    <automated>python3 -c "
t=open('.planning/REQUIREMENTS.md').read()
for rid in ['PIPE-01','PIPE-02','PIPE-03','PIPE-04','PIPE-05','DEFER-01','DEFER-02','DEFER-03']:
    assert rid in t, rid
for tok in ['ADR-0006','ADR-0034','docs/src/contributing/testing-guide.md','src/test_support','src/application/services/orchestration/listener.rs','actionlint']:
    assert tok in t, 'missing correction anchor: '+tok
print('all eight ids and every correction anchor present')"</automated>
    <automated>bash -c 'N=$(python3 -c "import yaml;print(len(yaml.safe_load(open(\".github/workflows/ci.yml\"))[\"jobs\"]))"); echo "ci.yml job count=$N"; grep -q "$N" .planning/REQUIREMENTS.md &amp;&amp; echo "job count recorded"'</automated>
  </verify>
  <acceptance_criteria>
    - All eight requirement texts carry a dated Phase 15 amendment in the same format as plan 13-10's existing PIPE-01 banner, with the superseded original retained and marked.
    - The real snapshot-file and test-function counts, measured at execution time, appear in PIPE-01's text; the "43 total" figure is marked superseded rather than deleted.
    - PIPE-02 cites `ADR-0006` for the threshold and does not restate a second threshold number.
    - PIPE-02 records Epic 25's Open Question 3 as answered, naming the removal and the parent PRD's own recommendation as the basis.
    - PIPE-04 records seven upgraded plus one deleted, the real line numbers found at execution time, and six workflows rather than three.
    - PIPE-05 records the `CONTRIBUTING.md` premise as relocated-by-outcome and names `docs/src/contributing/testing-guide.md`.
    - DEFER-01 contains a five-row verdict table covering `MockUserRepository`, `MockLogPort`, `MockNotificationService`, `MockEventSource` and `MockTriggerExecutor`, each row reading built / replaced by a named alternative / unnecessary for a named reason. No name is absent.
    - DEFER-01's verdict for `MockLogPort` is consistent with the ledger's existing present-but-unshared finding rather than contradicting it.
    - DEFER-02 records 583 lines, cites ADR-0034 for the withdrawn split, and carries the entry and exit figures with their exact command and scope.
    - DEFER-03 records the correct path and marks the 57.83% / 2026-02-14 baseline superseded by the measured entry figure.
    - Every checked box carries an exact command or `file:line` as its evidence; `grep -c '^      - \[x\] \*\*PIPE\|^- \[x\] \*\*PIPE\|^- \[x\] \*\*DEFER'` matches only requirements whose text contains a command string or a `file:line` citation.
    - The current `ci.yml` job count, re-counted at execution time, appears in the hand-off block, and each of its five numbered items has a disposition.
    - No separate corrections file is created anywhere.
  </acceptance_criteria>
  <done>Every false premise this phase found is corrected where the next reader will encounter it, and no requirement is closed without the command that proves it.</done>
</task>

<task type="auto">
  <name>Task 2: Amend the ledger rows and the ROADMAP criteria</name>
  <read_first>
    - `.planning/ledgers/milestone-09-12.md` — the ten rows this phase closes: `REQ-ci-cli-snapshot-job`, `REQ-ci-bench-check-job`, `REQ-ci-combined-coverage-job`, `REQ-codecov-config-thresholds`, `REQ-makefile-coverage-targets`, `REQ-modernize-github-actions`, `REQ-contributing-coverage-docs`, `REQ-mock-infrastructure`, `REQ-user-service-test-coverage`, `REQ-listener-service-test-coverage`. Also the head-note paragraph carrying the measured `ci.yml` job list. Note the `REQ-ci-cli-snapshot-job` row's own "sums to 43" count, which is itself stale — it counted four of the seven files.
    - `.planning/ledgers/milestone-02-03.md` — the Epic 24 block verdict, the cluster `8.0` row, and the `### Phase 6 CLOSE-02 scope` section. These carry the bidirectional deferral pointers into this phase and need the return pointer now that the work shipped.
    - `.planning/ROADMAP.md` § Phase 15 — the six success criteria. Criteria 1, 3 and 6 rest on superseded figures.
    - All five referenced plan summaries.
  </read_first>
  <files>.planning/ledgers/milestone-09-12.md, .planning/ROADMAP.md</files>
  <action>
    Amend the ten ledger rows in place, dated, with the superseded verdict text retained. Each closed
    row records: what shipped, the exact command or `file:line` that proves it, and the plan summary
    it came from. Correct `REQ-ci-cli-snapshot-job`'s own stale "sums to 43" count in the same pass —
    it enumerated four of the seven `tests/cli/*.rs` files, and the row's arithmetic is wrong for that
    reason rather than because the tree changed. Update the head-note's measured `ci.yml` job list to
    the count and ids present after this phase's four additions, re-counted at execution time.

    Add the return pointer in `.planning/ledgers/milestone-02-03.md`: the Epic 24 cluster `8.0` row
    and the `### Phase 6 CLOSE-02 scope` section deferred `cli-tests`, `bench-check`, the `coverage`
    job and `.codecov.yml` into this phase with a written reason and a bidirectional pointer. Record
    that all four shipped, naming the plans, so the deferral closes from both ends rather than leaving
    a dangling forward reference.

    Then amend ROADMAP's Phase 15 entry at source, original text retained:

    **Criterion 1** cites "43 snapshot tests" — superseded by the real count measured at execution time.

    **Criterion 3** frames the threshold decision as a choice "between a 78% hard gate and a phased
    70 → 74 → 78 ramp against a measured 76-77% baseline". All three figures are superseded:
    ADR-0006 rejected both positions and the 76-77% baseline, and the number this phase landed is
    derived from a fresh measurement under a scope neither position contemplated. Replace the framing
    with a pointer to the ADR-0006 Phase 15 amendment and record why the earlier framing was wrong,
    rather than silently swapping the numbers.

    **Criterion 6** says "covered to the gate", which is ambiguous, and describes the
    split-versus-test collision as needing resolution "by sequence". Record that it resolves to the
    ≥ 80% module bar as a **phase acceptance criterion, not a standing CI gate** (D-12), that the
    collision was dissolved by ADR-0034 rather than sequenced, and that the listener's stale baseline
    was re-measured with the delta stated.

    Update the Phase 15 `**Plans:**` line and plan list to the ten plans this phase actually
    produced, with each marked complete. Do not renumber, reword or touch any other phase entry, and
    do not rewrite the ROADMAP as a whole — scoped edits only.
  </action>
  <verify>
    <automated>python3 -c "
t=open('.planning/ledgers/milestone-09-12.md').read()
rows=['REQ-ci-cli-snapshot-job','REQ-ci-bench-check-job','REQ-ci-combined-coverage-job','REQ-codecov-config-thresholds','REQ-makefile-coverage-targets','REQ-modernize-github-actions','REQ-contributing-coverage-docs','REQ-mock-infrastructure','REQ-user-service-test-coverage','REQ-listener-service-test-coverage']
for r in rows: assert r in t, r
print('all ten ledger rows present')"</automated>
    <automated>bash -c 'grep -q "cli-tests" .planning/ledgers/milestone-02-03.md &amp;&amp; grep -q "bench-check" .planning/ledgers/milestone-02-03.md &amp;&amp; echo "return pointer present"'</automated>
    <automated>bash -c 'awk "/^### Phase 15:/,/^### Phase 16:/" .planning/ROADMAP.md > /tmp/p15.md; grep -q "ADR-0006" /tmp/p15.md &amp;&amp; grep -q "15-10-PLAN.md" /tmp/p15.md &amp;&amp; echo ok; test "$(grep -c "^### Phase " .planning/ROADMAP.md)" -eq "$(git show HEAD:.planning/ROADMAP.md | grep -c "^### Phase ")" &amp;&amp; echo "no phase added or removed"'</automated>
  </verify>
  <acceptance_criteria>
    - All ten ledger rows carry a dated Phase 15 closure verdict naming what shipped, the exact command or `file:line` that proves it, and the originating plan summary; the superseded verdict text is retained.
    - `REQ-ci-cli-snapshot-job`'s "sums to 43" arithmetic is corrected in place with the reason (four of seven files enumerated) and the original retained.
    - The ledger head-note's `ci.yml` job list matches the count and ids present at execution time.
    - `.planning/ledgers/milestone-02-03.md`'s Epic 24 cluster `8.0` row and `### Phase 6 CLOSE-02 scope` section each record that the four deferred items shipped, naming the plans.
    - ROADMAP Phase 15 criteria 1, 3 and 6 each carry a dated amendment with the original text retained; criterion 3 points at the ADR-0006 Phase 15 amendment instead of restating a threshold.
    - ROADMAP Phase 15's `**Plans:**` line reads ten plans and the plan list names `15-01-PLAN.md` through `15-10-PLAN.md`.
    - `grep -c '^### Phase ' .planning/ROADMAP.md` equals the same count in `git show HEAD:` — no phase was added, removed or renumbered.
    - `git diff .planning/ROADMAP.md` shows changes only inside the Phase 15 entry.
  </acceptance_criteria>
  <done>The ledger and the ROADMAP describe what shipped, with every superseded figure marked rather than quietly replaced.</done>
</task>

<task type="auto">
  <name>Task 3: Annotate the source documents and record the declines</name>
  <read_first>
    - `.project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md` — FR-25.3 item 10's 78% hard gate, and Open Question 3.
    - `.project/Deferred-QA-CICD-Completion/Epic_25/prd-cicd-pipeline-enhancement.md` (locate with `ls .project/Deferred-QA-CICD-Completion/Epic_25/`) — FR-25.1 through FR-25.10, Appendix B's pre-M10 seven-job pipeline table, Appendix C's phased ramp, and its own Open Question 3 on the integration-tests coverage step.
    - `.project/Deferred-QA-CICD-Completion/DEFERRED_COVERAGE.md` — Module 1, Module 2, and the three unchecked prerequisites: "create reusable mock infrastructure patterns", "document testing best practices", "establish concurrency testing patterns".
    - `.github/workflows/ci.yml` around the Docker build-time budget comment naming "Owner: Phase 15 / PIPE" — locate it with `grep -n 'Owner: Phase 15' .github/workflows/ci.yml`.
    - `.planning/phases/15-coverage-ci-quality-gates/15-CONTEXT.md` § Deferred Ideas.
  </read_first>
  <files>.project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md, .project/Deferred-QA-CICD-Completion/Epic_25/prd-cicd-pipeline-enhancement.md, .project/Deferred-QA-CICD-Completion/DEFERRED_COVERAGE.md, .github/workflows/ci.yml</files>
  <action>
    Annotate, do not rewrite. Each `.project/` document gets a dated correction banner at its head
    naming what was wrong and pointing at the ADR or requirement that settles it, with each defective
    claim corrected inline, the original text retained and marked superseded.

    **The parent PRD.** FR-25.3 item 10's 78% hard gate and the "measured 76-77%" baseline are both
    superseded by ADR-0006's Phase 15 amendment. Its **Open Question 3** — hard-fail versus
    soft-warning during ramp-up — is answered: `cargo llvm-cov --fail-under-lines` hard-fails in the
    workflow from the first gated run, and the construction guarantees the gate cannot be red on the
    run that sets it, which is what made a ramp unnecessary. Record it as answered with its source.

    **The Epic 25 PRD.** Appendix C's phased 70 → 74 → 78 ramp is superseded for the same reason.
    Its own **Open Question 3** — remove or retain `integration-tests.yml`'s coverage step — is
    answered by removal, which is the parent PRD's own recommendation; record it. Appendix B's
    seven-job pipeline table is long superseded by the tree; note the current job count measured at
    execution time rather than editing the historical table.

    **DEFERRED_COVERAGE.md.** Correct Module 2's dead path and stale baseline, pointing at DEFER-03's
    re-measurement. Then close the three unchecked prerequisites, each with its evidence:
    "create reusable mock infrastructure patterns" is met by `src/test_support/` (name the module and
    the doubles); "document testing best practices" is met by the Code Coverage section in
    `docs/src/contributing/testing-guide.md` (name the file); and "establish concurrency testing
    patterns" is met by the four timeout-guarded, exact-assertion scenarios in `listener.rs`'s test
    module (name them). These get **explicit closure records** rather than being left implicitly
    satisfied — an unchecked box with satisfied content is exactly the class of stale signal this
    phase exists to remove.

    **Record the two declines**, each with a reason and an owner, so neither is silently absent:

    The **native-arm64 CI rework**. The advisory Docker build-time budget comment in `ci.yml` names
    "Owner: Phase 15 / PIPE" and proposes `ubuntu-24.04-arm` runners in place of QEMU emulation so the
    budget can be hard rather than advisory. No requirement ID covers it, and CONTEXT.md's Deferred
    Ideas places it out of scope for this phase. Edit that comment in place to reassign the owner —
    naming a future infrastructure phase rather than this one — and record the reassignment in the
    ledger so the pointer does not go on naming a phase that has closed. Change nothing else about
    the Docker job or either budget.

    A **second, feature-scoped coverage measurement**. ADR-0006 D-14b's open question on `minio.rs` is
    answered by D-01 choosing one scope; a second measurement is not added, because two recorded
    numbers is the failure RECON-07 exists to close. Record the decline with the condition that would
    reopen it: a feature-gated subsystem needing its own recorded number.
  </action>
  <verify>
    <automated>bash -c 'for f in .project/Deferred-QA-CICD-Completion/prd-deferred-qa-completion.md .project/Deferred-QA-CICD-Completion/DEFERRED_COVERAGE.md; do grep -q "ADR-0006" "$f" || { echo "no ADR-0006 pointer in $f"; exit 1; }; done; echo ok'</automated>
    <automated>bash -c 'grep -q "src/test_support" .project/Deferred-QA-CICD-Completion/DEFERRED_COVERAGE.md &amp;&amp; grep -q "testing-guide.md" .project/Deferred-QA-CICD-Completion/DEFERRED_COVERAGE.md &amp;&amp; grep -q "listener.rs" .project/Deferred-QA-CICD-Completion/DEFERRED_COVERAGE.md &amp;&amp; echo "all three prerequisites have evidence"'</automated>
    <automated>bash -c 'test "$(grep -c "Owner: Phase 15 / PIPE" .github/workflows/ci.yml)" -eq 0 &amp;&amp; python3 -c "import yaml; yaml.safe_load(open(\".github/workflows/ci.yml\"))" &amp;&amp; echo "owner reassigned, workflow still parses"'</automated>
    <automated>bash -c 'for f in .github/workflows/*.yml; do python3 -c "import yaml; yaml.safe_load(open(\"$f\"))" || exit 1; done; cargo test --workspace --lib >/dev/null &amp;&amp; echo "tree still green"'</automated>
  </verify>
  <acceptance_criteria>
    - Each of the three `.project/` documents carries a dated correction banner at its head naming what was wrong and pointing at ADR-0006 or the settling requirement, with the original text of each corrected claim retained and marked superseded.
    - The parent PRD's Open Question 3 and the Epic 25 PRD's Open Question 3 are each recorded as **answered**, with the answer and its source named.
    - `DEFERRED_COVERAGE.md`'s Module 2 path and baseline are corrected, pointing at DEFER-03's re-measurement.
    - All three of `DEFERRED_COVERAGE.md`'s prerequisites carry an explicit closure record naming its evidence: `src/test_support`, `docs/src/contributing/testing-guide.md`, and the named concurrency tests in `listener.rs`.
    - `grep -c 'Owner: Phase 15 / PIPE' .github/workflows/ci.yml` returns `0`, the comment names a future owner instead, and the workflow still parses.
    - `git diff .github/workflows/ci.yml` in this task shows only comment-line changes — no step, budget, `run:` block or job structure altered.
    - The two declines — the native-arm64 rework and a second feature-scoped measurement — each appear with a reason, an owner and a reopening condition.
    - All six workflow files still parse; `cargo test --workspace --lib` exits 0.
    - No `.project/` document was rewritten wholesale: `git diff --stat` shows additions and marked inline corrections, with the pre-existing body retained.
  </acceptance_criteria>
  <done>Every source document this phase contradicted now says so at its head, every open question it answered is recorded as answered, and every item it declined carries a reason, an owner and a reopening condition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning record → every future phase | A stale figure in a requirement or ledger is what the next planner plans against |
| Documentation edit → executable CI config | This plan edits a comment inside `ci.yml`, a file whose parse validity gates every build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-27 | Repudiation | A correction that erases the defective claim, destroying the audit trail of what was believed and when | medium | mitigate | D-00c/D-00d annotation rather than rewriting is an acceptance criterion on every file: a dated banner naming the defect, the original text retained and marked superseded, and `git diff --stat` asserted to show additions and inline corrections rather than a wholesale rewrite. |
| T-15-28 | Spoofing | A requirement checked off without the evidence that proves it | high | mitigate | D-00e is enforced as an acceptance criterion: no box is checked unless the requirement text carries the exact command string or a `file:line` citation. Every ledger closure names the command and the originating plan summary. |
| T-15-29 | Tampering | A documentation edit inside `ci.yml` breaking the workflow | medium | mitigate | Only comment lines are changed in that file, asserted by a `git diff` criterion; every workflow file is re-parsed with `yaml.safe_load` and `cargo test --workspace --lib` is re-run before the plan closes. |
| T-15-30 | Tampering | A scoped ROADMAP edit turning into a whole-file rewrite that drops other phase entries | high | mitigate | The phase-header count is asserted equal to `git show HEAD:`, and `git diff` is asserted to show changes only inside the Phase 15 entry. No structural ROADMAP mutation is performed by hand. |
| T-15-31 | Repudiation | A declined item vanishing rather than being recorded as declined, so the next reader cannot tell whether it was considered | medium | mitigate | Both declines require a reason, an owner and a reopening condition as acceptance criteria; the `ci.yml` owner comment is reassigned in place rather than deleted, so the pointer stays live and does not name a closed phase. |
</threat_model>

<verification>
- All eight requirement texts amended at source, dated, originals retained.
- DEFER-01 carries a five-row verdict table with no name absent.
- All ten ledger rows closed with command-level evidence; the `43` arithmetic corrected.
- The Phase 6 deferral closes bidirectionally in `milestone-02-03.md`.
- ROADMAP Phase 15 criteria 1, 3 and 6 amended; no other phase entry touched; phase count unchanged.
- Three `.project/` documents annotated; both Open Question 3s recorded as answered; all three DEFERRED_COVERAGE prerequisites closed with named evidence.
- The `Owner: Phase 15 / PIPE` comment reassigned; `ci.yml` still parses; all six workflows parse.
- `cargo test --workspace --lib` exits 0.
</verification>

<success_criteria>
- No false premise this phase found survives at its source.
- No requirement closed without its command or `file:line`.
- No named mock, open question, or declined item silently dropped.
- No source document rewritten; every correction annotated with the original retained.
- No phase entry other than 15 touched in the ROADMAP.
</success_criteria>

## Artifacts this phase produces

Created by this plan:

| Kind | Symbol |
|---|---|
| Requirement amendments | dated Phase 15 correction banners on PIPE-01 … PIPE-05 and DEFER-01 … DEFER-03 |
| Verdict table | the DEFER-01 five-name mock disposition table (`MockUserRepository`, `MockLogPort`, `MockNotificationService`, `MockEventSource`, `MockTriggerExecutor`) |
| Ledger closures | dated Phase 15 verdicts on the ten `REQ-*` rows in `milestone-09-12.md`, plus the return pointer in `milestone-02-03.md` |
| ROADMAP amendments | dated corrections to Phase 15 criteria 1, 3 and 6; the `**Plans:**` line and ten-item plan list |
| Source-document banners | dated correction banners on the parent PRD, the Epic 25 PRD, and `DEFERRED_COVERAGE.md` |
| Closure records | the three `DEFERRED_COVERAGE.md` prerequisites, each with named evidence |
| Recorded answers | parent-PRD Open Question 3; Epic 25 Open Question 3; DEFER-01 Open Question 2 |
| Recorded declines | the native-arm64 CI rework (owner reassigned in the `ci.yml` comment); a second feature-scoped coverage measurement |

Deliberately **not** created: any separate corrections file, any new ADR, any change to
`PROMOTION.md`, any renumbered or reworded phase entry other than 15.

<output>
Create `.planning/phases/15-coverage-ci-quality-gates/15-10-SUMMARY.md` when done. List every
correction applied with its file and the claim it replaced, every verdict recorded, and every finding
handed forward with its owner — this is the phase's hand-off, and Phase 16's DOCS-01 planner reads it
to know what this phase already touched and what it deliberately left alone.
</output>
