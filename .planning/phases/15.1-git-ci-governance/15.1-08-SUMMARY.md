---
phase: 15.1-git-ci-governance
plan: 08
subsystem: infra
tags: [github, rulesets, branch-protection, ci-governance]

# Dependency graph
requires:
  - phase: 15.1-git-ci-governance (plan 07)
    provides: origin/main fast-forwarded to d87d11ea2968ebc185afddf79a34cb0a200eff85 and both retired branches deleted — the ordering precondition for applying protection to the single trunk
  - phase: 15.1-git-ci-governance (plan 05)
    provides: disambiguation of the `Integration Tests` context slot plan 15.1-04 had deliberately left open
provides:
  - "Three GitHub rulesets applied to the live repository: 'Protect main branch' (id 20868126, target refs/heads/main, 0 bypass actors), 'Protect release branches' (id 20868128, target refs/heads/release/*), 'Protect release tags' (id 20868099, target refs/tags/v*, 1 bypass actor)"
  - "15.1-RULESET-EVIDENCE.md — the verbatim D-00e evidence record SC2 closes against"
  - "protect-main-branch.json's required-check set pinned to 44 contexts (Integration Tests added per 15.1-05's hand-off, committed at e99ac9a)"
affects: ["15.1-09 (trigger-policy documentation and guard now protect a live gate, not a committed-but-unapplied one)", "15.1-10 (ADR-0043/0044 can cite protection as applied, not merely decided)"]

# Tech tracking
tech-stack:
  added: []
  patterns: ["Applying repository-configuration payloads via gh api POST against the rulesets endpoint, then reading the live state back separately (collection, per-ruleset detail, branch-rules evaluation) rather than inferring success from request exit status alone"]

key-files:
  created:
    - .planning/phases/15.1-git-ci-governance/15.1-RULESET-EVIDENCE.md
  modified: []

key-decisions:
  - "Order of application: tag ruleset first (cheapest permission smoke test), then release-branch, then trunk last — so a scope failure surfaces on the least consequential payload before the merge gate is touched"
  - "Integration Tests added to protect-main-branch.json's required-check set before application (43 -> 44 contexts), applying the hand-off plan 15.1-05 recorded, committed separately at e99ac9a"
  - "All three requests were creates, not updates — no ruleset with the same name pre-existed under any of the three names"

requirements-completed: [SC2]

coverage:
  - id: D1
    description: "The repository's ruleset collection is non-empty and contains a ruleset targeting the trunk ref, a ruleset targeting the release-branch ref pattern, and a ruleset targeting the release-tag ref pattern"
    requirement: "SC2"
    verification:
      - kind: other
        ref: "gh api /repos/DF3NDR/paladin-dev-env/rulesets --jq collection read-back, transcribed verbatim in 15.1-RULESET-EVIDENCE.md §3 — three active rulesets returned (ids 20868126, 20868128, 20868099), up from [] at §1's before-state"
        status: pass
    human_judgment: false
  - id: D2
    description: "The applied trunk ruleset's required-check list, as returned by the server, is set-equal to the committed payload's list — the server's normalisation is recorded, not assumed"
    requirement: "SC2"
    verification:
      - kind: other
        ref: "15.1-RULESET-EVIDENCE.md §5 — server: 44, committed: 44, identical: True, server-only: [], committed-only: []; §2's response head additionally names every server-defaulted pull_request field (dismiss_stale_reviews_on_push, required_reviewers, require_code_owner_review, dismissal_restriction, require_last_push_approval, required_review_thread_resolution, allowed_merge_methods)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Every request's response, including status, is recorded verbatim so a permission failure cannot be mistaken for success"
    requirement: "SC2"
    verification:
      - kind: other
        ref: "15.1-RULESET-EVIDENCE.md §2 — all three requests recorded with exit=0 and full/head response body transcribed byte-identical"
        status: pass
    human_judgment: false
  - id: D4
    description: "The tag ruleset's bypass entry and the trunk ruleset's absence of one both survive application, verified against the live server"
    requirement: "SC2"
    verification:
      - kind: other
        ref: "15.1-RULESET-EVIDENCE.md §6 — gh api rulesets/20868126 detail read returns bypass:0; gh api rulesets/20868099 detail read returns bypass:1; both transcribed verbatim with the reasoning for each"
        status: pass
    human_judgment: false
  - id: D5
    description: "A ruleset applied to the release-branch ref class exists, applied after the retirement of the branch (release/v0.7.0) that had matched that pattern"
    requirement: "SC2"
    verification:
      - kind: other
        ref: "15.1-RULESET-EVIDENCE.md §2 REQUEST 3/3 and §3 collection read-back — 'Protect release branches' (id 20868128, target refs/heads/release/*) present; ordering satisfied by plan 15.1-07 completing both retirements before this plan's checkpoint was presented"
        status: pass
    human_judgment: false
  - id: D6
    description: "The rollback command for each applied ruleset is written down, executable rather than theoretical"
    requirement: "SC2"
    verification: []
    human_judgment: true
    rationale: "Backstop-verification truth per the plan's must_haves (verification: backstop) — the three gh api -X DELETE commands with server-assigned ids are recorded in 15.1-RULESET-EVIDENCE.md §7, but their success is only provable by running them, which would undo the very state this plan applies. Recording is the verification the plan specifies; execution is intentionally not performed here."

duration: n/a (record-only execution; Task 1 checkpoint approved and Task 2's three POST requests were already issued by the orchestrator before this executor was spawned)
completed: 2026-08-14
status: complete
---

# Phase 15.1 Plan 08: Ruleset Application Evidence Summary

**Three GitHub rulesets (trunk, release-branches, release-tags) applied live to DF3NDR/paladin-dev-env via `gh api POST`, with a verbatim D-00e evidence record confirming the 44-context required-check match, the D-08b bypass asymmetry, and a written rollback path.**

## Performance

- **Duration:** n/a — Task 1's checkpoint was approved by the human operator ("Apply now") and Task 2's three mutating `gh api -X POST` requests were already issued by the orchestrator before this executor was spawned. This executor's role was to write `15.1-RULESET-EVIDENCE.md` and this summary from the verbatim request/response data supplied by the orchestrator, not to re-issue any mutating command.
- **Completed:** 2026-08-14
- **Tasks:** 2 (Task 1 `checkpoint:decision`, `gate="blocking"` — approved; Task 2 `auto` — executed by the orchestrator, recorded here)
- **Files modified:** 1 created (`15.1-RULESET-EVIDENCE.md`)

## Accomplishments

- Three rulesets created on the live repository, all as first-time creates (no pre-existing ruleset of the same name):
  - `Protect release tags` — id `20868099`, target `refs/tags/v*`, `bypass_actors` length 1
  - `Protect main branch` — id `20868126`, target `refs/heads/main`, `bypass_actors` length 0
  - `Protect release branches` — id `20868128`, target `refs/heads/release/*`
- Collection read-back confirms all three `enforcement: active`, up from an empty `[]` at the pre-application baseline
- Trunk-ref rule evaluation (`gh api /repos/DF3NDR/paladin-dev-env/rules/branches/main`) returns all four expected rule types (`deletion`, `non_fast_forward`, `pull_request`, `required_status_checks`) — the ruleset does not merely exist, it evaluates against `main`
- Server's required-check context list for the trunk ruleset is set-equal (44/44, no server-only or committed-only entries) to the committed `protect-main-branch.json` payload
- D-08b's bypass asymmetry verified live: trunk ruleset carries 0 bypass actors, tag ruleset carries 1 — both transcribed with the reasoning for why each is correct
- Rollback commands recorded for all three rulesets, with their server-assigned ids filled in
- The accepted open risk recorded plainly: no complete green PR run has been observed against the post-consolidation `ci.yml`, since plans `15.1-05`, `15.1-06`, and `15.1-07` all touched that file after the context list was captured at `15.1-CI-EVIDENCE.md`

## Task Commits

1. **Task 1: Approve the exact payloads and required-check list before anything is applied** — checkpoint approved by the human operator ("Apply now"); no commit (decision only)
2. **Task 2: Apply the rulesets and transcribe the responses into the evidence record** — the pre-application payload amendment was committed separately by the orchestrator at `e99ac9a feat(15.1-08): pin Integration Tests now that 15.1-05 disambiguated it`; this executor's contribution is the evidence record commit below

**This executor's commit:** `63629ce` — `docs(15.1-08): record verbatim ruleset application evidence`

**Plan metadata:** (this commit, below — `docs(15.1-08): complete ruleset application evidence plan`)

## Files Created/Modified

- `.planning/phases/15.1-git-ci-governance/15.1-RULESET-EVIDENCE.md` — the verbatim before/request/response/read-back/asymmetry/rollback record SC2 closes against

## Decisions Made

- **Order of application: tag ruleset, then release-branch, then trunk.** The tag payload is the cheapest permission smoke test (least consequential if it fails) and the trunk payload is applied last so nothing else is left half-applied behind the merge gate.
- **`Integration Tests` added to `protect-main-branch.json`'s required-check set (43 -> 44 contexts) before any request was issued**, applying the explicit hand-off recorded at `15.1-05-SUMMARY.md` line 157 — committed separately at `e99ac9a`, prior to and independent of this executor's evidence-record commit.
- **All three requests were creates, not updates.** No ruleset with any of the three names existed on the repository before this plan (before-state: `[]`).

## Deviations from Plan

None — plan executed exactly as written, with the noted exception that the mutating operations (Task 1 approval, Task 2's three POST requests) were performed by the orchestrator before this executor was spawned; this executor's job, per its prompt, was record-only: write the evidence file and SUMMARY.md from the verbatim data supplied, and commit them. No state-mutating `gh api` command was run by this executor.

## Known Stubs

None.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **`15.1-09`**'s trigger-policy documentation and workflow-trigger guard now protect a *live* gate rather than a committed-but-unapplied one — the ordering the phase's decisions (D-02, D-06) required has been completed.
- **`15.1-10`**'s ADR-0043/0044 can now cite branch protection as *applied*, not merely decided, referencing this plan's evidence record.
- **Open item carried forward, stated plainly per §8 of `15.1-RULESET-EVIDENCE.md`:** no complete green PR run has been observed against the post-consolidation `ci.yml`. The first PR into `main` under the newly applied trunk ruleset is the actual test of whether all 44 pinned contexts still resolve to `success` under the current workflow shape. If any do not, merges will block until the ruleset is deleted with the rollback command recorded in the evidence record's §7.
- **This evidence commit itself must reach `main` through a pull request with all required checks green**, per this plan's executor note — that observation, when it occurs, is the phase's first piece of dogfooding evidence and is not yet available at the time this summary was written.

---
*Phase: 15.1-git-ci-governance*
*Completed: 2026-08-14*

## Self-Check: PASSED

- FOUND: `.planning/phases/15.1-git-ci-governance/15.1-RULESET-EVIDENCE.md` exists (confirmed via prior Write tool call in this session)
- FOUND: commit `63629ce` exists in `git log --oneline` (this executor's evidence-record commit)
- FOUND: commit `e99ac9a` exists in `git log --oneline` (the pre-application payload amendment cited in the evidence record and this summary)
- No mutating `gh api` command was run by this executor; all request/response data transcribed verbatim from the orchestrator-supplied application record, matching the instruction not to issue any state-mutating call
