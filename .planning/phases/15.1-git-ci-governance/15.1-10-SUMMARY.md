---
phase: 15.1-git-ci-governance
plan: 10
subsystem: infra
tags: [git, github, branch-protection, adr, documentation, roadmap, ledger]

# Dependency graph
requires:
  - phase: 15.1-git-ci-governance (plan 07)
    provides: "15.1-07-SUMMARY.md — the fast-forward transcript (994 commits, d87d11e), cited by ADR-0043 and the ROADMAP criterion-4 amendment"
  - phase: 15.1-git-ci-governance (plan 08)
    provides: "15.1-RULESET-EVIDENCE.md — the applied ruleset read-back (44 contexts, bypass asymmetry), cited by ADR-0044 and the appendix page"
  - phase: 15.1-git-ci-governance (plan 09)
    provides: "docs/src/contributing/branching-model.md and scripts/check-workflow-triggers.sh — cross-linked and cited by this plan, not duplicated"
provides:
  - "ADR-0043 (.planning/decisions/0043-github-flow-trunk-and-trigger-surface.md) — the GitHub Flow trunk model, the match-all trigger surface, and the register-plus-guard control, D-01 through D-04/D-09/D-09b"
  - "ADR-0044 (.planning/decisions/0044-branch-protection-posture.md) — the 44-context required-check set and its two measured exclusions, the review-count/bypass decisions, D-05 through D-08b"
  - "PROMOTION.md's next-free-ADR-number line advanced 0043 -> 0045"
  - "docs/src/appendix/branch-protection.md brought current and rewritten clean (D-12): applied ruleset IDs/status, the 44-context list, the bypass asymmetry, the fast-forward account replacing the obsolete v0.4.0 reconciliation section, the mislabelled link relabelled"
  - "Three stale docs/BRANCH_PROTECTION.md pointers redirected to docs/src/appendix/branch-protection.md (release.yml x1, Makefile x2)"
  - "ROADMAP.md's Requirements line and four false-premise success criteria (1, 3, 4, 6) amended in place, dated, superseded text retained (D-00d); criterion 5 annotated separately as understated framing"
  - "milestone-09-12.md ledger gains a dated close-out section citing both ADRs and both evidence records, sharpening the native-arm64 CI rework's reopening condition, and recording the two unpinned toolchain references as deliberate"
affects: ["Phase 16 (documentation currency work inherits a current appendix page and a clean ADR pair rather than open questions)", "A future infrastructure phase picking up the native-arm64 CI rework (inherits the sharpened reopening condition: Docker Build is now the only correctness-relevant excluded job)"]

# Tech tracking
tech-stack:
  added: []
  patterns: ["ADR citation inside docs/src/ uses plain-text `ADR-NNNN` with the path in backticks, never a markdown link outside docs/src/ — mdbook's linkcheck forbids linking outside its root"]

key-files:
  created:
    - ".planning/decisions/0043-github-flow-trunk-and-trigger-surface.md"
    - ".planning/decisions/0044-branch-protection-posture.md"
  modified:
    - ".planning/decisions/PROMOTION.md"
    - "docs/src/appendix/branch-protection.md"
    - ".github/workflows/release.yml"
    - "Makefile"
    - ".planning/ROADMAP.md"
    - ".planning/ledgers/milestone-09-12.md"

key-decisions:
  - "Two decision records, not one and not five — the branching model/trigger surface and the protection posture are distinct supersession units (D-00g)"
  - "No requirement identifiers minted for this phase (D-00f) — the requirements document is corpus-wide, an inserted phase's retroactive IDs would have no upstream text or consumer, and the seven success criteria already carry the traceability"
  - "The ROADMAP's original text is retained verbatim beneath each amendment per D-00d, even where that means the word 'TBD' remains in the retained original Requirements line — see 'Verification Notes' below for the resulting known conflict with the plan's own <verify> script"
  - "docs/src/ book pages are rewritten clean per D-12 (no dated banner, no retained superseded text) — a different convention from D-00d's ROADMAP/ledger retention, applied to the right file class in each case"

requirements-completed: [SC3, SC4, SC7]

coverage:
  - id: D1
    description: "ADR-0043 and ADR-0044 authored in the house shape (no frontmatter, seven required sections), and PROMOTION.md's next-free-ADR-number line advanced from 0043 to 0045 with no other line changed"
    requirement: "SC4"
    verification:
      - kind: other
        ref: "python3 -c \"import pathlib; [assert-house-shape over both files]\" -> 'both records have the house shape'; grep -c \"Next free ADR number: 0045\" .planning/decisions/PROMOTION.md -> 1; git diff .planning/decisions/PROMOTION.md shows exactly one changed line"
        status: pass
    human_judgment: false
  - id: D2
    description: "Three stale docs/BRANCH_PROTECTION.md pointers redirected to docs/src/appendix/branch-protection.md (release.yml's verify-tag-source failure message; Makefile's release-branch guard comment and override hint), with release.yml's git merge-base ancestry assertion unchanged"
    requirement: "SC3"
    verification:
      - kind: other
        ref: "python3 -c \"assert 'docs/BRANCH_PROTECTION.md' not in Makefile/release.yml text and 'docs/src/appendix/branch-protection.md' in both\" -> 'all three pointers redirected'; grep -n 'git merge-base --is-ancestor' .github/workflows/release.yml:60 present unchanged; test ! -e docs/BRANCH_PROTECTION.md succeeds"
        status: pass
    human_judgment: false
  - id: D3
    description: "docs/src/appendix/branch-protection.md brought current (44-context required-check list, two exclusions with the 3762s measurement, applied-status dated 2026-08-14, bypass asymmetry stated plainly, obsolete v0.4.0 reconciliation section replaced, mislabelled [CONTRIBUTING.md] link relabelled), cross-linked with branching-model.md, and mdbook build reports no broken links"
    requirement: "SC3"
    verification:
      - kind: other
        ref: "cd docs && mdbook build -> exit 0, 'No broken links found' (2026-08-14T20:06:27Z run); grep -c bypass docs/src/appendix/branch-protection.md -> 13; grep -n CONTRIBUTING.md docs/src/appendix/branch-protection.md -> no matches"
        status: pass
    human_judgment: false
  - id: D4
    description: "ROADMAP.md's Requirements line and success criteria 1, 3, 4, 6 amended in place, dated, superseded text retained; criterion 5 annotated separately as understated framing, not counted among the four"
    requirement: "SC3"
    verification:
      - kind: other
        ref: "python3 -c \"section-scan: 'superseded' in sec.lower() -> True; sec.count('2026-') -> 6; sec.count('**Amended') -> 5; sec.count('**Noted') -> 1\""
        status: pass
    human_judgment: false
  - id: D5
    description: "milestone-09-12.md ledger gains a dated close-out section referencing both ADR numbers, both evidence records, and the sharpened native-arm64 rework reopening condition"
    requirement: "SC3"
    verification:
      - kind: other
        ref: "python3 -c \"for s in ['15.1','0043','0044']: assert s in ledger_text\" -> 'ledger row present and cross-referenced'; git diff --name-only -> exactly .planning/ROADMAP.md and .planning/ledgers/milestone-09-12.md"
        status: pass
    human_judgment: false
  - id: D6
    description: "The ROADMAP verify script's literal 'no TBD substring anywhere in the section' assertion fails, because D-00d's own retention requirement keeps the original Requirements line text (which reads 'TBD — minted during...') and because the out-of-scope Plans-section checkbox list also carries the token 'TBD' for an unrelated reason"
    verification: []
    human_judgment: true
    rationale: "This is a genuine internal contradiction between the plan's own <action> (retain original text per D-00d) and its own <verify> script (assert no 'TBD' substring anywhere in the section) — not a defect in the delivered work. A human should confirm the resolution documented in 'Verification Notes' below is the correct reading rather than silently trusting an automated pass/fail."

duration: 55min
completed: 2026-08-14
status: complete
---

# Phase 15.1 Plan 10: Decision Records and the Roadmap Made True Summary

**Authored ADR-0043 (trunk model + trigger surface) and ADR-0044 (branch-protection posture), redirected the three stale `docs/BRANCH_PROTECTION.md` pointers to the current appendix page and brought that page current, and amended ROADMAP.md's four false premises and its Requirements line in place — closing SC3 as the tree actually needs it closed.**

## Performance

- **Duration:** ~55 min
- **Started:** 2026-08-14T19:16:00Z (approx.)
- **Completed:** 2026-08-14T20:11:37Z
- **Tasks:** 3
- **Files modified:** 8 (2 created, 6 modified)

## Accomplishments

- **ADR-0043** (`.planning/decisions/0043-github-flow-trunk-and-trigger-surface.md`) records the GitHub Flow trunk model, the match-all `push: branches: ['**']` trigger surface with its two named exceptions (`docs.yml`, `release.yml`), the `integration-tests.yml` consolidation, and the trigger-policy register plus guard (D-01 through D-04, D-09, D-09b). 11 bullets in Code Locations, 4 in Considered Options, each a single physical line.
- **ADR-0044** (`.planning/decisions/0044-branch-protection-posture.md`) records the 44-context required-check set and its two measured exclusions (`Docker Build` 3762s, `Kubernetes Smoke Test`), the zero-review-count/mandatory-PR posture, the bypass removal on the trunk and release-branch rulesets, and the deliberate bypass retention on the tag ruleset (D-05 through D-08b). 4 bullets in Code Locations, 4 in Considered Options, each a single physical line.
- `PROMOTION.md`'s "Next free ADR number" line advanced from `0043` to `0045` — exactly one line changed, procedural prose untouched (`git diff` confirms a single-line diff).
- Three stale `docs/BRANCH_PROTECTION.md` pointers redirected to `docs/src/appendix/branch-protection.md`: `.github/workflows/release.yml:65` (the `verify-tag-source` failure message) and `Makefile:511`/`:519` (the release-branch guard comment and override hint). `release.yml`'s `git merge-base --is-ancestor` ancestry assertion is untouched — only the message string changed.
- `docs/src/appendix/branch-protection.md` rewritten clean (D-12: no dated banner, no retained superseded text): the three applied ruleset IDs and their status, the full 44-context required-check list, the two exclusions with the 3762s measurement, the bypass asymmetry stated plainly (trunk/release-branch rulesets carry no bypass, the tag ruleset retains one because it gates ref creation not a merge), the obsolete "Reconciling the existing `v0.4.0` tag" section replaced with a short account of this phase's own fast-forward citing ADR-0043, and the mislabelled `[CONTRIBUTING.md]` link relabelled to `[Contributing to Paladin]`. Cross-linked both directions with `docs/src/contributing/branching-model.md` (which already linked to the appendix page from plan `15.1-09`).
- `.planning/ROADMAP.md`'s Phase 15.1 entry amended in place: the Requirements line now states plainly that no requirement identifier exists for this phase (D-00f), and success criteria 1, 3, 4 and 6 each carry a dated 2026-08-14 amendment stating what was claimed, what is actually true, and the evidence — all four retaining the original text above, marked superseded. Criterion 5 carries a separate "Noted" annotation (not counted among the four) stating its framing understated the defect's scope.
- `.planning/ledgers/milestone-09-12.md` gains a "Phase 15.1 close-out amendments (2026-08-14)" section citing both ADR numbers and both evidence records, sharpening the "Phase 15 declines" native-arm64 CI rework entry's reopening condition (`Docker Build` is now the only correctness-relevant job excluded from the merge gate), and recording the two remaining unpinned `dtolnay/rust-toolchain@master` references as deliberate (they pass an explicit `toolchain:` input the `@stable` tag cannot accept), not residue.
- `cd docs && mdbook build` reports `No broken links found`, exit 0, after all `docs/src/` edits.
- `./scripts/check-workflow-triggers.sh` reports all clauses pass, exit 0, after the `release.yml` message-string edit.

## Task Commits

Each task was committed atomically:

1. **Task 1: Author the two decision records and advance the promotion register** - `4d7ae51` (docs)
2. **Task 2: Redirect the three stale pointers and bring the appendix page current** - `8c6b93d` (docs)
3. **Task 3: Amend the ROADMAP at source and write the ledger row** - `e20226e` (docs)

_Note: no TDD tasks in this plan — it changes no Rust, per the plan's own executor note._

## Files Created/Modified

- `.planning/decisions/0043-github-flow-trunk-and-trigger-surface.md` - New ADR: trunk model, trigger surface, register-plus-guard control
- `.planning/decisions/0044-branch-protection-posture.md` - New ADR: required-check set, review count, bypass posture
- `.planning/decisions/PROMOTION.md` - Next-free-ADR-number line advanced 0043 -> 0045 (single-line change)
- `docs/src/appendix/branch-protection.md` - Rewritten clean: applied ruleset state, 44-context list, bypass asymmetry, fast-forward account, cross-link, relabelled link
- `.github/workflows/release.yml` - One message-string pointer redirected; `verify-tag-source`'s ancestry guard untouched
- `Makefile` - Two message-string pointers redirected (guard comment, override hint)
- `.planning/ROADMAP.md` - Requirements line and four success criteria amended in place, dated, superseded text retained; criterion 5 annotated separately
- `.planning/ledgers/milestone-09-12.md` - Dated close-out section appended, citing both ADRs and both evidence records

## Decisions Made

- **Two decision records, not one and not five** (D-00g) — the branching model/trigger surface and the protection posture are genuinely distinct supersession units; someone revisiting the trunk model should not have to supersede the required-check set.
- **No requirement identifiers minted for this phase** (D-00f) — the requirements document is corpus-wide, an inserted phase's retroactive identifiers would have no upstream requirement text and no consumer, and the seven success criteria already carry the traceability identifiers would add. Recorded as an answer in the ROADMAP's Requirements line, not left open.
- **The ROADMAP's original text is retained verbatim beneath every amendment**, per D-00d and this plan's own `<action>` — including where the retained original Requirements line still reads "TBD — minted during...". This produces a documented, expected conflict with the plan's own `<verify>` script; see Verification Notes below.
- **docs/src/ book pages are rewritten clean, not annotated** (D-12) — a deliberately different convention from D-00d's ROADMAP/ledger retention-with-amendment. Applied correctly to each file class: the appendix page carries no dated banner or superseded text; the ROADMAP and ledger both retain original text with dated amendments appended.
- **ADR citations inside `docs/src/` use plain-text `ADR-NNNN` with the path in backticks, not a markdown link.** The first draft of the appendix page used `[ADR-0044](../../../.planning/decisions/...)`, which mdbook's linkcheck rejects as "Linking outside of the root directory is forbidden" — exactly the failure mode named in this plan's phase-specific notes as having broken `Build MDBook` earlier in this phase. Caught and fixed before committing, matching `docs/src/api-reference/stable-api.md`'s own established citation style.

## Deviations from Plan

None of Rules 1-4 — no bug fix, missing-functionality addition, blocking-issue fix, or architectural change was required. One self-corrected drafting mistake (the ADR markdown-link error above) and one documented, unresolvable conflict between the plan's own `<action>` and `<verify>` instructions (below), neither of which is a deviation from the plan's specified behavior.

## Issues Encountered

**The plan's own `<verify>` script for Task 3 cannot pass while also satisfying the plan's own `<action>` instruction and the orchestrator's explicit scope boundary.** The Task 3 `<verify>` block asserts:

```python
assert 'TBD' not in sec, 'requirements line still records pending identifiers'
```

run against the full Phase 15.1 ROADMAP section text. This assertion fails, and cannot be made to pass without violating one of two other explicit instructions:

1. The plan's own `<action>` for Task 3 requires "retaining the original text marked superseded and dated" for the Requirements line — and the original text is literally `TBD — minted during '/gsd-discuss-phase 15.1'`. D-00d's own established precedent elsewhere in this corpus (e.g. `milestone-09-12.md`'s `REQ-modernize-github-actions` row: "The fifteen-job figure above is retained, marked superseded, per D-00d") confirms full verbatim retention, not paraphrase-without-quotation, is the correct reading of D-00d for this file class.
2. The orchestrator's own task prompt states explicitly: "just do not touch its plan-progress checkboxes" — and the Phase 15.1 entry's `Plans:` section (`- [ ] TBD (run /gsd-plan-phase 15.1 to break down)`) also carries the literal substring `TBD`, for a reason unrelated to the Requirements-line question this task closes.

Verified directly (`python3` re-run against the current section text): both occurrences are exactly these two — the retained-original Requirements line and the untouched `Plans:` checkbox line — and no other `TBD` occurrence exists in the section. All of the Task 3 `<verify>` block's other assertions pass independently: `'superseded' in sec.lower()` is `True`, and `sec.count('2026-')` is `6` (well above the required `>= 4`), and the section carries 5 `**Amended` markers plus 1 `**Noted` marker matching the plan's own required structure (four criteria amendments plus the Requirements-line amendment, plus criterion 5's separate note). Resolved by following the two more specific, more recently-stated instructions (D-00d's retention requirement, the orchestrator's explicit scope boundary) over the verify script's blunt substring check, and recording the conflict here per this corpus's established practice for plan-authoring/verify-script defects (e.g. the `milestone-09-12.md` ledger's own recorded 29-vs-28 heading-count defect, and ADR-0033's Makefile-only conformance note) rather than mangling retained source text or touching out-of-scope checkboxes to force a false green light.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

**SC3 is closed.** The stale pointers redirect to real content, the appendix page describes the applied protection state rather than an intended one, and both pages cross-link so neither is a dead end.

**Phase 16 (Documentation Currency & the Architecture Gap) inherits a current appendix page and a clean ADR pair**, not open questions. This plan deliberately does not pre-commit Phase 16's own DOCS-02 question of whether the appendix as a whole is archive material (per D-12's own stated scope limit), and does not extend the currency pass to the stale required-check list on the deployment page beyond what this plan's redirect needed.

**A future infrastructure phase taking up the native-arm64 CI rework** inherits the sharpened reopening condition recorded in this plan's ledger addition: `Docker Build` is now the only correctness-relevant job excluded from the merge gate, so closing that gap removes exactly one exclusion rather than requiring a fresh assessment of the required-check set's shape.

**This plan's own pull request has not yet been opened or observed against the live ruleset by this executor.** Per this plan's own text ("This plan lands under the gate it helped build... Run the local gates before pushing"), the dogfooding observation the plan's `<output>` asks for — "whether this plan's own pull request passed every required check on the first attempt" — is not yet available: PR creation and push are outside this worktree-isolated executor's role (per the standard GSD wave/ship separation), and are the orchestrator's or `/gsd-ship`'s next step, not this plan's own commits. This is recorded here rather than silently omitted, since the plan's `<output>` names it explicitly. No blocker — the local gates this plan's own commits pass are: `mdbook build` (exit 0, no broken links), `scripts/check-workflow-triggers.sh` (exit 0, all clauses pass), and `git diff --name-only` scoped exactly to the plan's declared `files_modified`.

---

*Phase: 15.1-git-ci-governance*
*Completed: 2026-08-14*

## Self-Check: PASSED

- FOUND: `.planning/decisions/0043-github-flow-trunk-and-trigger-surface.md`
- FOUND: `.planning/decisions/0044-branch-protection-posture.md`
- FOUND: `docs/src/appendix/branch-protection.md`
- FOUND: `.planning/phases/15.1-git-ci-governance/15.1-10-SUMMARY.md`
- FOUND commit `4d7ae51` (Task 1: ADR-0043, ADR-0044, PROMOTION.md)
- FOUND commit `8c6b93d` (Task 2: pointer redirects, appendix currency pass)
- FOUND commit `e20226e` (Task 3: ROADMAP amendments, ledger row)

All claimed artifacts and commits verified present against the tree and `git log` this session.
