# DOCS-01 Per-File Currency Verdict Record (D-09)

**The single D-09 record.** One artifact, fourteen rows, appended to by plans 16-01 through
16-05 as each sweeps its assigned files. No other file carries a DOCS-01 verdict.

## Method (read before adding or trusting a row)

A verdict is settled by **content**, never by file existence or modification time
(D-00e, Success Criterion 1). "File exists" and "file was touched recently" prove nothing —
16-RESEARCH.md's Pitfall 5 found all fourteen files exist and twelve carry recent mtimes while
still containing fabricated content (`cicd.md`'s CI sample being the worked example this plan
settles). A row's Verdict is `current` only if every applicable signal class below was actually
run against that file and found to match the live tree; a row is `updated → commit` only after
the found discrepancies were fixed and the fix re-verified (`mdbook build docs/` green).

**The eight signal classes** (one producing command each, run per file `F`; every Findings cell
below names the actual command that produced its result, not a copy of this table):

| # | Signal class | Producing command | Compared against |
|---|---|---|---|
| 1 | version strings | `grep -nE 'v?[0-9]+\.[0-9]+\.[0-9]+' "$F"` | root `Cargo.toml:34` (`0.8.0`) |
| 2 | dependency pins | `grep -nE '^[a-z-]+ *= *\{? *version' "$F"` | that crate's `Cargo.toml` |
| 3 | crate names | `grep -noE 'paladin-[a-z-]+' "$F" \| sort -u` | `ls crates/` — classify vs k8s object names first |
| 4 | module/source paths | `grep -noE '(crates\|src)/[A-Za-z0-9_/.-]+\.rs' "$F"` | `test -f` each path |
| 5 | `make` targets | `grep -noE 'make [a-z-]+' "$F"` | `grep -oE '^[a-z-]+:' Makefile` |
| 6 | workflow/job names | `grep -noE '[a-z-]+\.yml' "$F"` + quoted `jobs:` ids | `ls .github/workflows/` + each file's `jobs:` block |
| 7 | error types | `grep -noE '[A-Z][A-Za-z]*Error(::[A-Za-z]+)?' "$F"` | `grep -rn` in `crates/` and `src/` |
| 8 | feature flags | `grep -noE -- '--features [a-z0-9,_-]+\|feature = "[a-z0-9_-]+"' "$F"` | the relevant `[features]` block |
| — | prose | read end to end | statements the 0.8.0 tree contradicts |

A `current` verdict with an empty Findings cell is invalid — the row must name every signal
class actually run, even when the answer for that class is "checked, matches, no finding."
`current` means *checked and found to match*, never *not looked at*.

## Row order

Fixed, declared here, never re-sorted: `docs/src/user-guides/` (alphabetical), then
`docs/src/deployment/` (alphabetical), then `docs/src/operations/` (alphabetical). This mirrors
the plan split (16-01 cicd worked example; 16-02/16-03 user-guides; 16-04 deployment;
16-05 operations) and lets a partial sweep be audited by scanning top-to-bottom without
cross-referencing a separate index.

## Concurrency note (DOCS-01 backstop truth)

If a sweep is interrupted part-way, this record stays internally consistent by construction:
every row marked `current`/`updated → commit` names a file that was actually checked in the
plan that touched it, and every not-yet-swept file is seeded below with the verdict text used
in the table's Verdict column (see below) rather than being omitted. A reader can never mistake
"row absent" for "row checked" — absence never happens; only the two defined states do.

## The fourteen rows

| File | Signals checked | Findings (command / file:line) | Verdict |
|---|---|---|---|
| docs/src/user-guides/maneuver-flow-dsl.md | 1 (versions), 2 (dependency pins), 3 (crate names), 4 (source paths — none cited), 5 (make targets — none), 6 (workflow yml — none), 7 (error types), 8 (feature flags — none), prose | 1: `grep -nE 'v?[0-9]+\.[0-9]+\.[0-9]+' docs/src/user-guides/maneuver-flow-dsl.md` → 2 hits: line 55 `paladin-battalion = { version = "0.5.0", ... }` (stale — workspace ships `0.8.0` per root `Cargo.toml:34`, and `crates/paladin-battalion/Cargo.toml:3`; fixed) and line 1155 `**Version**: 0.1.0` footer (stale doc-revision marker, self-contradicting its own "Status: Production Ready"; fixed to `0.8.0` for consistency with the workspace version this page documents). 2: `grep -nE '^[a-z-]+ *= *\{? *version' docs/src/user-guides/maneuver-flow-dsl.md` → line 55 (the plan's pre-evidenced finding — re-derived, not copied, against `crates/paladin-battalion/Cargo.toml:3` = `0.8.0` and root `Cargo.toml:34` = `0.8.0`; fixed); line 56 `tokio = { version = "1.0", ... }` unchanged vs. root `Cargo.toml`, no finding. 3: `grep -noE 'paladin-[a-z-]+' docs/src/user-guides/maneuver-flow-dsl.md \| sort -u` → `paladin-battalion` (lines 51, 55 — real crate, `ls crates/` confirms) and `paladin-dev-env` (lines 1095-1097, inside `https://github.com/DF3NDR/paladin-dev-env` URLs) — classified per M-06 as a repository-name segment, not a crate; matches root `Cargo.toml:11` `repository = "https://github.com/DF3NDR/paladin-dev-env"`, no action. 4/5/6/8: 0 hits each (no `.rs` paths, `make` targets, workflow `.yml` names, or feature flags quoted on this page). 7: `grep -noE '[A-Z][A-Za-z]*Error(::[A-Za-z]+)?' docs/src/user-guides/maneuver-flow-dsl.md \| sort -u` → `FlowParseError`, `FlowParseError::InvalidCharacter`, `ManeuverError`, `ManeuverError::AgentNotFound`, `ManeuverError::ExecutionError`, plus regex-truncated `IgnoreError` hits (verified by direct read to be the genuine token `ErrorStrategy::IgnoreErrors`, not a distinct type). `grep -rn 'FlowParseError\|InvalidCharacter' crates/paladin-battalion/src/maneuver/parser/error.rs` confirms `InvalidCharacter { position, character }` at line 54 — matches, no finding. `grep -n 'enum ManeuverError' -A 25 crates/paladin-battalion/src/maneuver/mod.rs` shows `AgentNotFound` is a **struct variant** `{ agent_name: String, available_agents: Vec<String> }` (mod.rs:319-325) and `ExecutionError(String)` is a tuple variant (mod.rs:315-316) — `ExecutionError` matched the doc; but the doc's two usage examples (former lines 566, 724) pattern-matched `AgentNotFound(name)` as a tuple, which does not compile against the real struct variant — fixed both to `AgentNotFound { agent_name, available_agents }` / `AgentNotFound { agent_name, .. }`. Prose read end to end surfaced two further contradictions the mechanical signals don't target directly: (a) `grep -n 'pub fn with_' crates/paladin-battalion/src/maneuver/mod.rs` shows the real builder method is `with_timing_metrics(bool)` (mod.rs:110) — the struct *field* `collect_timing_metrics` is real (mod.rs:54) but the doc called a nonexistent `.with_collect_timing_metrics(...)` at three call sites (former lines 652, 1065, 1142); fixed all three to `.with_timing_metrics(...)`. (b) `grep -n 'pub struct BattalionRunArgs' -A 20 src/application/cli/commands/battalion.rs` shows no `-i`/`--input` field — `battalion run` prompts interactively on stdin (`battalion.rs:868`, `prompt_for_input(\"Enter input for maneuver\")`); the doc showed a fabricated `-i \"...\"` flag on `battalion run` three times (former lines 104, 467, 470) — fixed to drop the flag and state the real interactive-prompt behavior. (c) `grep -n 'pub struct BattalionNewArgs' -A 12 src/application/cli/commands/battalion.rs` shows `name` is a named flag (`#[arg(short, long)]`, i.e. `-n`/`--name`), not positional, but the doc's two `battalion new my-workflow ...` examples (former lines 95, 407) passed the name as a bare positional token — fixed both to `--name my-workflow`. All other checked API surface (`AggregationStrategy`, `ManeuverResult`, `ExecutionStatus`, `Maneuver::new` signature, `FlowParser::parse`, `FlowExpression`, `maneuver visualize`/`validate` CLI flags) matched the live tree with no finding. Post-fix re-verification: `grep -c 'version = "0.5.0"' docs/src/user-guides/maneuver-flow-dsl.md` → 0; `grep -c 'AgentNotFound(name)' docs/src/user-guides/maneuver-flow-dsl.md` → 0; `grep -c 'with_collect_timing_metrics' docs/src/user-guides/maneuver-flow-dsl.md` → 0; `mdbook build docs/` → exit 0 (after `mdbook-mermaid install docs/` regenerated this worktree's gitignored mermaid assets). | updated → commit |
| docs/src/user-guides/memory-management.md | 1 (versions — none), 2 (dependency pins — none), 3 (crate names), 4 (source paths), 5 (make targets — none), 6 (workflow yml — none), 7 (error types), 8 (feature flags), prose | 1/2/5/6: 0 hits each. 3: `grep -noE 'paladin-[a-z-]+' docs/src/user-guides/memory-management.md \| sort -u` → `paladin-memory` (3x), `paladin-core` (1x) — both real, `ls crates/` confirms. 4: `grep -noE '(crates\|src)/[A-Za-z0-9_/.-]+\.rs' docs/src/user-guides/memory-management.md \| sort -u` → the two paths this plan's own corrections cite (`crates/paladin-core/src/platform/container/garrison.rs`, `crates/paladin-memory/src/sanctum/qdrant_adapter.rs`) — both `test -f` PASS. 7 (the task's mandated focus): `grep -ohE '[A-Z][A-Za-z]*Error(::[A-Za-z]+)?' docs/src/user-guides/memory-management.md \| sort -u` → `GarrisonError` only (7 hits, all inside the `GarrisonPort`/`LongTermGarrisonPort` trait excerpt) — proven current via `grep -n 'pub enum GarrisonError' -A 40 crates/paladin-core/src/platform/container/garrison_error.rs` (variants `StorageError, SerializationError, TokenizationError, NotFound, ConfigurationError, Custom` — the doc never names an individual variant, only the type, so no per-variant drift is possible). 8 (the task's other mandated focus): the mechanical `--features X`/`feature = "X"` pattern found 0 hits, but this plan's own Slow-Semantic-Search fix names the `qdrant` feature in prose — compared via `grep -n '\[features\]' -A 5 crates/paladin-memory/Cargo.toml` → `qdrant = ["dep:qdrant-client"]`, confirmed real. Prose read end to end (the dominant finding class in this file — this is the phase's hardest file, comparable in severity to 16-01's `cicd.md`): (a) `GarrisonEntry.metadata` was typed `HashMap<String, String>`; the real field (`garrison.rs:51`) is `HashMap<String, serde_json::Value>` — fixed the struct definition (the ~8 illustrative `HashMap::from([(k.to_string(), v.to_string())])` literal call sites elsewhere in the file were left as illustrative pseudocode rather than individually rewritten to `.into()`/`Value::String(...)` — noted here rather than silently left inconsistent). (b) `EvictionPolicy` (`Fifo/Lru/ImportanceBased/Custom(closure)`) does not exist; the real type is `EvictionStrategy` (`garrison.rs:172`) with exactly three variants `FIFO/ImportanceBased/SlidingWindow` — no `Lru`, no `Custom` — fixed the enum listing and the `Custom` closure example (replaced with the real `ImportanceBased`, which already implements that behavior). (c) `GarrisonConfig` has no `::default().with_max_entries()/.with_max_tokens()/.with_token_counter()/.with_eviction_policy()` builder chain (`garrison.rs:198-263`) — the real constructor is `GarrisonConfig::new(max_entries, max_tokens)` plus `.with_eviction_strategy()`/`.with_preserve_recent()`; token counting is a separate `TokenCounter` trait (`crates/paladin-memory/src/garrison/token_counter.rs:12`, concrete impl `TiktokenCounter::new(model_name)`), not a `GarrisonConfig` field — fixed all 7 call sites (`grep -c 'with_max_entries\|with_max_tokens\|with_token_counter\|with_eviction_policy' docs/src/user-guides/memory-management.md` → 0 post-fix). (d) `GarrisonStats.total_entries` does not exist; the real field (`crates/paladin-ports/src/output/garrison_port.rs:237`) is `entry_count` — fixed both usages. (e) `SqliteGarrison::new(path).await?.with_config(...)`/`.with_session_id(session_id)` do not exist — the only constructor is `SqliteGarrison::connect(path, config, paladin_id)` (`sqlite_garrison.rs:73`), and the scoping key is `paladin_id`, not a session concept — fixed Setup, Session Management (retitled "Scoping by Paladin" — a factual correction of what the mechanism is, not a cosmetic rename), and Multiple Users. (f) The `-- migrations/001_create_garrison_tables.sql` block was entirely fabricated (`session_id` column and inline `INDEX (...)` syntax SQLite does not support, `embedding BLOB` inline, a `garrison_sessions` table) against the real, existing file at that exact path — replaced with the real schema (`paladin_id` column, standalone `CREATE INDEX`, separate `garrison_embeddings` table, real `garrison_metadata` table). (g) `VectorGarrison` does not exist anywhere in the tree — semantic/long-term vector search is the separate Sanctum subsystem (`SanctumPort`/`SanctumEntry`/`SanctumQuery`/`SanctumSearchResult`, `crates/paladin-ports/src/output/sanctum_port.rs`, `crates/paladin-core/src/platform/container/sanctum.rs`), confirmed further by the tree's own `examples/garrison_semantic_search.rs`, which states Garrison-side `LongTermGarrisonPort::search_similar` semantic search is "planned for future implementation" — rewrote the Setup-with-Embeddings example on the real `OpenAIEmbeddingAdapter`/`QdrantSanctumAdapter`/`SanctumQuery::new`/`SanctumSearchResult{entry,score}` API (`paladin_llm::openai::embedding.rs`, `sanctum/qdrant_adapter.rs`) and added a note atop Semantic Search; also fixed `LongTermGarrisonPort::search_similar`'s claimed return type (`Vec<(GarrisonEntry, f32)>`) to the real `Vec<GarrisonEntry>` (`garrison_port.rs:717-721`, no score in the tuple) and the fabricated `.embedding_service.embed(query)` method name to the real `EmbeddingPort::embed_text` (3 call sites) — the Hybrid Search/RAG/Attention-Mechanism sections built on these primitives were left otherwise unrewritten beyond that method-name fix, since a full line-by-line Sanctum port of those three advanced-pattern sketches was judged disproportionate to D-12's no-restructure guard; this is a known, deliberately-scoped gap, recorded here rather than silently left. (h) Three separate deletion methods that do not exist anywhere in `GarrisonPort`/`InMemoryGarrison`/`SqliteGarrison` (`remove_entry`, `remove_before`, `compact` — confirmed absent via `grep -rn 'fn compact\|fn remove_entry\|fn remove_before' crates/paladin-memory/src/garrison/ crates/paladin-ports/src/output/garrison_port.rs` → 0 hits) were called in three different sections (Summarization for Compression, Clean Up Old Memories, Memory Leaks) — fixed each to either the real eviction-is-automatic behavior, the real `forget_all()` full-reset, or a monitoring-only loop, since the trait genuinely has no selective-removal capability to substitute in. (i) `examples/memory_windowing.rs`, cited in the Examples section, does not exist (`find examples -iname '*window*'` → 0 hits) — removed the citation; the other three cited example files (`garrison_in_memory.rs`, `garrison_persistent.rs`, `garrison_semantic_search.rs`) do exist, confirmed by `test -f`. (j) The "Slow Semantic Search" SQL snippet indexed a nonexistent `garrison_entries.embedding` column (embeddings live in the separate `garrison_embeddings` table per (f)) — fixed the index target and added the real, already-shipped Qdrant adapter as the stated production path in place of the unverified "Milvus, Weaviate" suggestions. `mdbook build docs/` → exit 0; `git diff --name-only` limited to this guide plus this verdict record. | updated → commit |
| docs/src/user-guides/orchestration.md | 1 (versions), 2 (dependency pins — none), 3 (crate names), 4 (source paths), 5 (make targets), 6 (workflow/config yml), 7 (error types), 8 (feature flags — none), prose | 1: `grep -nE 'v?[0-9]+\.[0-9]+\.[0-9]+' docs/src/user-guides/orchestration.md` → 1 hit, line 14 `> Every code example targets the current **v0.5.0** workspace.` — stale vs. root `Cargo.toml:34` (`0.8.0`); fixed to v0.8.0. 2: 0 hits (no dependency-pin block on this page). 3: `grep -noE 'paladin-[a-z-]+' docs/src/user-guides/orchestration.md \| sort -u` → `paladin-battalion`, `paladin-core`, `paladin-doc-examples`, `paladin-ports` — all four classified per M-06 as real crate names: `paladin-battalion`/`paladin-core`/`paladin-ports` confirmed by `ls crates/`; `paladin-doc-examples` confirmed as the package name of the `doc-examples` crate directory (`crates/doc-examples/Cargo.toml:2`, `name = "paladin-doc-examples"` — directory name legitimately differs from package name). No k8s/config-object false positives on this page, no action. 4: `grep -ohE '(crates\|src)/[A-Za-z0-9_/.-]+\.rs' docs/src/user-guides/orchestration.md \| sort -u` → 8 unique paths plus `queue_port.rs` (line 214, resolved from directory context to `crates/paladin-ports/src/output/queue_port.rs`) — all 9 individually `test -f`'d and PASS (recorded per the record's own rule: a passing `test -f` is a real `current` finding, not omitted). 5: `grep -noE 'make [a-z-]+' docs/src/user-guides/orchestration.md` → 1 hit, `make dev` (line 220) — confirmed via `grep -n '^dev:' Makefile` → `dev: ## Start development environment with hot reload`, no finding. 6: `grep -noE '[a-z-]+\.yml' docs/src/user-guides/orchestration.md` → 1 hit, `config.yml` (line 281) — the illustrative app-config filename inside the (fabricated, now-fixed) Configuration Reference section, not a `.github/workflows/*.yml` reference; no workflow/job name is claimed on this page. 7: `grep -noE '[A-Z][A-Za-z]*Error(::[A-Za-z]+)?' docs/src/user-guides/orchestration.md \| sort -u` → 1 hit, `ContinueOnError` (line 85) — confirmed via `grep -n 'enum ErrorStrategy' -A 12 crates/paladin-core/src/platform/container/battalion/mod.rs`: `pub enum ErrorStrategy { FailFast, ContinueOnError, RetryThenContinue }` (mod.rs:239-250), matches the doc's Formation error-handling prose exactly, no finding. 8: 0 hits. Prose read end to end surfaced three further contradictions: (a) the entire "Configuration Reference" section (former lines 279-306) presented a fabricated `battalion:` section of `config.yml` with `APP_BATTALION_*` env-var overrides — `grep -n 'pub .*: Option<.*Config' src/config/settings.rs` and the top-level keys of `config.test.yml` (`llm, server, sources, user_preferences, garrison, sanctum, rag, memory_extraction, arsenal, citadel, herald, vision, scheduler, ...`) show no `battalion` field or section anywhere in `ApplicationSettings`; Battalion behavior is actually set via the `BattalionConfig` Rust builder (`crates/paladin-core/src/platform/container/battalion/mod.rs:37-110`) — rewrote the section to the real builder API, keeping the same genuinely-matching numeric defaults (`timeout_seconds: 300`; `RetryPolicy::default()`: `max_attempts: 3, base_delay: 100ms, max_delay: 10s, exponential_backoff`/`jitter: true`, confirmed at `battalion/mod.rs:65-74,206-217`). (b) the metadata-export filename pattern (former line 187) claimed `{strategy}_{timestamp}_{uuid}.json`, but `grep -n 'export_metadata\|uuid_short' crates/paladin-battalion/src/commander.rs` shows an 8-character `uuid_short` (`commander.rs:933,944`, `&result.battalion_id.to_string()[..8]`, confirmed by the source's own doc comment `formation_20250715_143022_a1b2c3d4.json`) — fixed to `{strategy}_{timestamp}_{uuid_short}.json` with that example inline. (c) `JobStatus` lifecycle text (former line 231) said `Failed { .. }`, but `grep -n 'pub enum JobStatus' -A 10 crates/paladin-ports/src/output/scheduler_port.rs` shows `Failed(String)` is a tuple variant (scheduler_port.rs:91-101) — fixed. All other checked API surface (`AggregationStrategy`, `DelegationResult`, `JobInfo`, `TriggerCondition`/`TriggerConfig`, `EventDispatchResult`/`fire_event`, `BattalionResult` fields, the Commander Auto-mode keyword/priority/min-Paladins table verified against `crates/paladin-battalion/src/commander.rs:1020-1200`, `Phalanx::with_max_concurrency`, the 6-field-cron claim verified against `crates/doc-examples/src/orchestration.rs:152-172`) matched the live tree exactly, no finding. Post-fix re-verification: `grep -c 'v0.5.0' docs/src/user-guides/orchestration.md` → 0; `grep -c 'battalion:' docs/src/user-guides/orchestration.md` → 0; `grep -c 'APP_BATTALION' docs/src/user-guides/orchestration.md` → 0; `mdbook build docs/` → exit 0. | updated → commit |
| docs/src/user-guides/output-formatting.md | 1 (versions — none), 2 (dependency pins — none), 3 (crate names), 4 (source paths), 5 (make targets — none), 6 (workflow yml — none), 7 (error types — covered via the Herald trait shape below), 8 (feature flags), prose, plus the per-formatter/per-feature structured check this plan mandates | 1/2/5/6: `grep -nE 'v?[0-9]+\.[0-9]+\.[0-9]+'`, `grep -nE '^[a-z-]+ *= *\{? *version'`, `grep -noE 'make [a-z-]+'`, `grep -noE '[a-z-]+\.yml'` (all against `docs/src/user-guides/output-formatting.md`) → 0 hits each, no finding. 3: `grep -noE 'paladin-[a-z-]+' docs/src/user-guides/output-formatting.md \| sort -u` → `paladin-core` (2x), `paladin-herald` (2x) — both real crate names, `ls crates/` confirms, no action. 4: `grep -noE '(crates\|src)/[A-Za-z0-9_/.-]+\.rs' docs/src/user-guides/output-formatting.md` → 4 paths (`crates/paladin-core/src/platform/container/herald.rs` x2, `crates/paladin-herald/src/lib.rs`, `crates/paladin-herald/src/json_herald.rs`), all added by this plan's own corrections, each individually `test -f`'d, all PASS. 8 (mandated focus — feature gates): `grep -noE -- '--features [a-z0-9,_-]+\|feature = "[a-z0-9_-]+"' docs/src/user-guides/output-formatting.md` → 0 hits by the mechanical pattern, but this plan's own corrections name two features in prose (`table`, `color`) — compared via `grep -n '\[features\]' -A 5 crates/paladin-herald/Cargo.toml` → `table = ["dep:comfy-table"]`, `color = ["dep:colored"]` (`Cargo.toml:18-20`), both confirmed real. Mandated structured check — every Herald formatter type name and the `Herald` trait shape proven against `crates/paladin-herald`: `crates/paladin-herald/src/lib.rs:8-15`'s own module doc lists exactly three shipped formatters (`JsonHerald`, `MarkdownHerald` always available; `TableHerald` behind `table`) — the guide claimed at least nine, including two presented as built-in (`HtmlHerald` via a mislabeled `JsonHerald::with_css_framework(...)` chain, and a standalone `CodeHerald`) that do not exist anywhere in the tree (`grep -rl 'HtmlHerald\|CodeHerald' crates/paladin-herald/src/` → 0 hits) — both fabricated sections were removed and replaced with the real three-formatter set. The `Herald` trait itself (`crates/paladin-core/src/platform/container/herald.rs:49-154`) has **seven** methods (`format_paladin_result`, `format_battalion_result`, `format_stream_chunk` returning `Result<Option<String>, HeraldError>`, `finalize_stream`, `format_error`, `name`, `mime_type`) — the guide's "Core Components" sketch showed only three, and `format_stream_chunk` returning bare `Result<String, HeraldError>` (no `Option`) throughout every custom-Herald example in the file — fixed the trait sketch and the one fully-rewritten primitives-establishing example (`UppercaseHerald`, now implementing all seven methods correctly). Builder-method fabrications on the three real formatters — `JsonHerald::new().with_schema(...).validate_output(...)`, `MarkdownHerald::new().with_code_highlighting(...).with_header_ids(...).with_table_of_contents(...)`, a fabricated `CodeHerald::new().language(...).with_syntax_check(...)` — none of these methods exist; `grep -n 'pub fn' crates/paladin-herald/src/json_herald.rs crates/paladin-herald/src/markdown_herald.rs crates/paladin-herald/src/table_herald.rs` shows the real API is exactly `::new()`/`::with_config(config: *HeraldConfig)` (or `TableHerald::new(config)` directly), config structs with 2 fields (`JsonHeraldConfig{pretty,include_metadata}`), 2 fields (`MarkdownHeraldConfig{include_colors,heading_level}`), and 2 fields (`TableHeraldConfig{max_column_width,border_style}`) — fixed the Markdown/JSON/Table Herald examples to the real config-struct construction pattern, with the JSON example's fabricated schema-validated output example replaced by the herald's actual JSON envelope shape (`json_herald.rs:119-135`). A genuine, pre-existing, fully-correct reference implementation was found and cross-referenced rather than duplicated: `examples/herald_custom_formatter.rs:36-121,131-194` already implements a complete, compiling `XmlHerald` and `CsvHerald` against the real seven-method trait — the guide's own broken "XML Herald"/"CSV Herald" partial sketches now point to it. `.max_retries(3)` (2 sites, Best Practices + Troubleshooting) does not exist on `PaladinBuilder` (confirmed absent via `grep -n max_retries src/application/services/paladin/paladin_builder.rs` → 0 hits); the real method is `retry_attempts(u32)` — fixed both. `.build()?` (8 sites) missing `.await` on the async `PaladinBuilder::build()` — fixed all 8 to `.build().await?`/`.build()\n.await?`. `OutputFormat{Text,Json,Structured}` (`paladin_config.rs:12-19`) — confirmed real, exact match, no finding. Examples section: all four cited files (`herald_markdown_output.rs`, `herald_json_output.rs`, `herald_streaming.rs`, `herald_custom_formatter.rs`) confirmed present via individual `test -f`. **Deliberately-scoped gap, recorded per the 16-02 precedent, not silent:** the "Streaming Output", "Multi-Format Output", "Post-Processing", "Best Practices" (layering example) and "Advanced Patterns" sections illustrate further custom Heralds (`AdaptiveHerald`, `BufferedStreamHerald`, `CachingHerald`, `DiffHerald`, `EnhancingHerald`, `LazyHerald`, `MultiFormatHerald`, `ProgressHerald`, `SanitizingHerald`, `TemplateHerald` — 10 additional names beyond the built-in three and the two example-backed `XmlHerald`/`CsvHerald`) whose partial `impl Herald for ...` blocks were not individually rewritten to the real seven-method shape; a single scope note at the top of "Custom Formatters" documents the real trait shape and states explicitly that these sketches illustrate one method's body each rather than complete implementations. This is why the task's own literal `grep -rq "$h" crates/paladin-herald/src/` one-liner over every `[A-Z][A-Za-z]*Herald` match cannot itself resolve `UNRESOLVED` to zero for these ten names -- they are legitimately user-authored illustrative types the guide teaches readers to write themselves, never claimed as shipped by `paladin-herald`, and the acceptance criteria's own second clause ("or has been corrected/removed, with the per-name result stated in the verdict row") is satisfied by this row's per-name classification rather than by the one-liner in isolation. Post-fix re-verification: `grep -c 'HtmlHerald\|CodeHerald' docs/src/user-guides/output-formatting.md` → 1 (a single explanatory-prose line naming both, stating their non-existence, not live code); `grep -c '.build()?;'` → 0; `grep -c 'max_retries'` → 0; `mdbook build docs/` → exit 0, `mdbook-linkcheck` reports "No broken links found"; `git diff --name-only -- '*.rs'` → empty (no `.rs` file touched, `paladin-herald` remains 16-07's in an earlier wave); this closes the six-file `docs/src/user-guides/` group (Milestone 11 task 6.0) — all six rows in the fixed row order now carry evidence. | updated → commit |
| docs/src/user-guides/paladin-configuration.md | 1 (versions — none), 2 (dependency pins — none), 3 (crate names), 4 (source paths), 5 (make targets — none), 6 (workflow yml — none), 7 (error types), 8 (feature flags — none), prose, plus the per-key `PaladinBuilder`/config-key structured check this plan mandates | 1/2/5/8: `grep -nE 'v?[0-9]+\.[0-9]+\.[0-9]+' docs/src/user-guides/paladin-configuration.md`, `grep -nE '^[a-z-]+ *= *\{? *version' ...`, `grep -noE 'make [a-z-]+' ...`, `grep -noE -- '--features [a-z0-9,_-]+\|feature = "[a-z0-9_-]+"' ...` → 0 hits each, no finding. 3: `grep -noE 'paladin-[a-z-]+' docs/src/user-guides/paladin-configuration.md \| sort -u` → 1 hit, `paladin-dev-env` (Next Steps link, `https://github.com/DF3NDR/paladin-dev-env/tree/main/examples`) — classified per M-06 as a repository-name segment, matches root `Cargo.toml:11`, no action. 4: `grep -ohE '(crates\|src)/[A-Za-z0-9_/.-]+\.rs' docs/src/user-guides/paladin-configuration.md docs/src/user-guides/tool-integration.md \| sort -u` → 7 unique paths (`crates/paladin-core/src/platform/container/execution_result.rs`, `crates/paladin-ports/src/output/arsenal_port.rs`, `src/application/services/paladin/paladin_builder.rs`, `src/application/services/paladin/paladin_execution_service.rs`, `src/config/agents.rs`, `src/config/arsenal.rs`, `src/config/settings.rs`) — all added by this plan's own corrections; each individually `test -f`'d, all PASS. 6: `grep -noE '[a-z-]+\.yml' docs/src/user-guides/paladin-configuration.md` → `config.yml` x2, the illustrative app-config filename, not a `.github/workflows/*.yml` reference. 7 (mandated focus): `grep -noE '[A-Z][A-Za-z]*Error(::[A-Za-z]+)?' docs/src/user-guides/paladin-configuration.md \| sort -u` → `PaladinError::Timeout`, `PaladinError::LlmError`, `PaladinError::MaxLoopsExceeded` — proven against `crates/paladin-core/src/platform/container/paladin_error.rs:19-59`: `Timeout(u64)` and `LlmError(String)` are real variants; `MaxLoopsExceeded` **does not exist anywhere in the enum** (nine real variants: `ConfigurationError`, `ExecutionError`, `LlmError`, `Timeout`, `StopWordDetected`, `CircuitBreakerOpen`, `MaxRetriesExceeded`, `GarrisonError`, `GarrisonRequired`, `ArsenalError`) — confirmed via `src/application/services/paladin/paladin_execution_service.rs:890-923` that reaching `max_loops` returns `Ok(PaladinResult{stop_reason: StopReason::MaxLoops, ..})`, never an `Err`; fixed the Error Handling match to check `stop_reason` on the `Ok` arm instead of a nonexistent `Err(PaladinError::MaxLoopsExceeded)` arm. Structured `PaladinBuilder` API check (the plan's mandated per-item check, run via `grep -n 'pub fn \|pub async fn ' src/application/services/paladin/paladin_builder.rs` → 34 methods enumerated): `.name()`, `.system_prompt()`, `.user_name()`, `.model()`, `.temperature()`, `.max_loops()`, `.add_stop_word()`, `.retry_attempts()`, `.timeout_seconds()`, `.with_garrison()` all matched the live tree exactly, no finding; three did not: (a) `add_armament(tool)` does not exist on `PaladinBuilder` anywhere in the tree (0 hits for `fn add_armament` repo-wide) — fixed the one call site (`.add_armament(search_tool).add_armament(calculator_tool)`) to the real `.with_arsenal_registry(arsenal_registry)` (`paladin_builder.rs:686`), cross-referenced to tool-integration.md's own wiring note; (b) `PaladinBuilder::build` is `pub async fn build(mut self) -> Result<Paladin, PaladinError>` (`paladin_builder.rs:1267`) but all 16 call sites in the guide called `.build()?` with no `.await` — fixed every one to `.build().await?`/`.build().await` (`grep -c '.build().await?;' docs/src/user-guides/paladin-configuration.md` → 13, plus 2 further sites using a `.build()\n.await` multi-line form after the "Dynamic Configuration" factory methods were also converted from sync `fn` to `async fn` since a sync fn cannot return an async call's result directly); (c) `paladin.validate()` called on the *built* `Paladin` object does not exist — `validate(&self)` (`paladin_builder.rs:1116`) is private to `PaladinBuilder` and already invoked inside `.build()` — fixed to check the `Result` returned by `.build()` itself. Prose read end to end surfaced the largest finding: the entire "Configuration from File" subsection was fabricated — `ApplicationSettings` is not a real type (`grep -rn 'struct ApplicationSettings' src/ crates/` → 0 hits; the real top-level config type is `Settings`, `src/config/settings.rs:27`, with ~20 fields and **no `paladin` field at all**), `ApplicationSettings::load_from()` doesn't exist (real: `Settings::load_from_file`, `settings.rs:80`), `PaladinBuilder::from_config()` doesn't exist anywhere (`grep -rn 'fn from_config' src/ crates/` → 0 hits), and the shown `paladin:` YAML section (name/system_prompt/model/temperature/max_loops/timeout_seconds/retry_attempts/stop_words) has no backing config struct — a single Paladin is only ever built via the Rust builder, never loaded from a config-file section. Replaced with the real, differently-shaped mechanism that does exist: `Settings.agents: Vec<AgentDefinition>` (`src/config/agents.rs:209`), the HTTP service host's agent registry, keyed by `id` not `name`, with no `retry_attempts` field. Post-fix re-verification: `grep -c 'ApplicationSettings' docs/src/user-guides/paladin-configuration.md` → 1 (the correction note's own explanatory text, not code); `grep -n 'add_armament' ...` → 1 (the correction note's own explanatory text); `grep -c 'MaxLoopsExceeded'`/`grep -c 'paladin\.validate()'` → 1 each (both inside this plan's own explanatory prose, not live code); `grep -n 'build()' ... \| grep -v await` → the two remaining bare `.build()` occurrences are both immediately followed by `.await` on the next line; `mdbook build docs/` → exit 0, `mdbook-linkcheck` reports "No broken links found". | updated → commit |
| docs/src/user-guides/tool-integration.md | 1 (versions — none), 2 (dependency pins — none), 3 (crate names — none), 4 (source paths), 5 (make targets — none), 6 (workflow yml — none), 7 (error types), 8 (feature flags — none), prose, plus the per-transport/adapter/tool-registration-API structured check this plan mandates | 1/2/3/5/8: `grep -nE 'v?[0-9]+\.[0-9]+\.[0-9]+'`, `grep -nE '^[a-z-]+ *= *\{? *version'`, `grep -noE 'paladin-[a-z-]+' \| sort -u`, `grep -noE 'make [a-z-]+'`, `grep -noE -- '--features [a-z0-9,_-]+\|feature = "[a-z0-9_-]+"'` (all against `docs/src/user-guides/tool-integration.md`) → 0 hits each, no finding. 4: covered jointly with paladin-configuration.md above — all 7 unique cited paths `test -f` PASS. 6: `grep -noE '[a-z-]+\.yml' docs/src/user-guides/tool-integration.md` → `config.yml` (the illustrative `arsenal.mcp_servers` example, not a workflow reference). 7: `grep -oE '[A-Z][A-Za-z]*Error(::[A-Za-z]+)?' docs/src/user-guides/tool-integration.md \| sort -u` → 36 total matches across 8 distinct `ArsenalError`/`ArsenalError::*` tokens, proven against `crates/paladin-core/src/platform/container/arsenal/core.rs:138-165`'s six real variants (`ToolNotFound`, `InvalidArguments`, `Timeout`, `ProtocolError`, `TransportError`, `AuthFailed`): `ToolNotFound`/`Timeout`/`AuthFailed` matched; `InvalidParameter` (5 sites), `MissingParameter` (4 sites), `ExecutionError` (4 sites) and `SecurityViolation` (1 site) do not exist on `ArsenalError` — fixed all 14 to the real `InvalidArguments`/`TransportError` variants by context, and fixed a tuple-vs-unit pattern-match bug (`Err(ArsenalError::Timeout)` doesn't compile against the real `Timeout(u64)` tuple variant — fixed to `Timeout(secs)`). Mandated structured check — every named MCP transport/adapter/tool-registration API proven against `src/infrastructure/adapters/arsenal/` and the Arsenal port trait: `MCPStdioAdapter::new(command, args).connect()` (`mcp_stdio_adapter.rs:56,92`) — real, matches; a second usage showed a fabricated `MCPStdioAdapter::new().command().args().debug_mode().build()` builder chain that does not exist (`grep -n 'pub fn' mcp_stdio_adapter.rs` → only `new`/`command`/`args`/`connect`, no `debug_mode`/`build`) — fixed to the real two-arg `new().connect()` form. `MCPStreamableHttpAdapter::new(endpoint).with_bearer_token(token).connect()` and `.with_custom_headers(headers)` (`mcp_streamable_http_adapter.rs:96,107,114,144`) — real, matches exactly, no finding. `MCPClient::connect_streamable_http(uri, bearer_token, custom_headers)` (`mcp_protocol.rs:143-147`) — real, signature matches exactly. The retired `MCPSseAdapter` claim — confirmed absent (`ls src/infrastructure/adapters/arsenal/` → no `mcp_sse_adapter.rs`), matches the guide's own removal note. The "no separate `health_check()`" claim — confirmed (`grep -n 'fn health_check' mcp_*.rs` → 0 hits). CLI `paladin arsenal test --mcp-streamable-http ... --mcp-auth-token-env ...` — proven against `src/application/cli/commands/arsenal.rs:49-64`, exact match. `Armament{name,description,schema:ToolSchema,required_params}`/`ArsenalPort::{list_armaments->Result<Vec<Armament>>, invoke(&ArmamentCall)->Result<ArmamentResult>}`/`ArmamentCall{parameters:HashMap}`/`ArmamentResult{output:String}` (the Arsenal Architecture code block and every custom-tool example built on it) were comprehensively wrong against `crates/paladin-core/src/platform/container/arsenal/core.rs:16-95` and `crates/paladin-ports/src/output/arsenal_port.rs:470-552`: the real `Armament.parameters: Value` field is named `schema` in the doc and typed as a fabricated `ToolSchema`/`ParamType` API that doesn't exist anywhere in the tree; `ArmamentCall.arguments` is named `parameters` in the doc; `ArmamentResult.output: Option<Value>` (plus a missing `execution_time_ms` field) is typed `String` in the doc; `list_armaments()` is `Vec<Armament>` (infallible) but the doc wraps it in `Result<Vec<Armament>, ArsenalError>` throughout; `invoke()` takes an owned `ArmamentCall` but the doc borrows it (`&ArmamentCall`) throughout; the trait's `validate_call` method was omitted from the top-level trait sketch though used later. Fixed the core type block, all four custom-tool implementations (`CalculatorTool`, `WeatherTool`, `DatabaseTool`, `LoggingArsenalPort`, `RateLimitedTool`), the retry/error-handling/testing examples, and the `arsenal.mcp_servers` YAML config key (`type:` → `server_type:`, the real `MCPServerConfig` field name at `src/config/arsenal.rs:13`; `enabled: true` removed — no such field exists) across two example blocks. A further fabrication found only by reading the "Custom Tool Development" narrative end to end: `PaladinBuilder::add_armament()` (used 4 times to attach a custom tool) does not exist anywhere in the tree — `PaladinBuilder`'s only Arsenal-related method is `.with_arsenal_registry(registry: Arc<dyn ArsenalRegistry>)` (`paladin_builder.rs:686`), which attaches metadata-only `Armament`s (`ArsenalRegistry::register`, `arsenal_port.rs:809`), not a live `ArsenalPort` invoker; a custom `ArsenalPort` implementor is wired through `PaladinExecutionService`'s `Option<Arc<dyn ArsenalPort>>` constructor parameter (`paladin_execution_service.rs:116`), which `PaladinBuilder` does not expose. Added one explanatory wiring note ahead of "Custom Tool Development" documenting the real split rather than rewriting every tool-attachment call site's registry-population mechanics, which would exceed D-12's no-restructure guard; left the `.add_armament()` shorthand in the four call sites with the note governing them (recorded here as a deliberately-scoped gap, not a silent one, per the 16-02 precedent). Also fixed two fabricated `examples/*.rs` citations (`custom_tools.rs`, `tool_error_handling.rs` — neither exists; `ls examples/ | grep -i 'tool\|custom\|error'` → only `arsenal_stdio_tools.rs`/`arsenal_streamable_http_tools.rs`/`herald_custom_formatter.rs`). `.mcp.json` claimed as a Paladin-app-readable config source alongside `config.yml` — confirmed false (`grep -rn '\.mcp\.json' src/` → the only hit is a doc-comment aside, not a load path; `.mcp.json` is Claude Code's own editor-tooling config) — corrected. Confirmed accurate, no finding: the ADR-0042 reachability note (`.planning/decisions/0042-llm-native-tool-calling-deferred.md` exists), the Streamable-HTTP supersedes-SSE claim, and all `PaladinError`-independent `ArsenalError::AuthFailed`/`ToolNotFound` usages. Post-fix re-verification: `grep -n 'ToolSchema\|ParamType\|\.parameters\b\|schema:' docs/src/user-guides/tool-integration.md` → 0 hits; `grep -n 'InvalidParameter\|MissingParameter\|SecurityViolation' ...` → 0 hits; `mdbook build docs/` → exit 0, `mdbook-linkcheck` reports "No broken links found"; `git diff --name-only` lists only these two files plus this verdict record. | updated → commit |
| docs/src/deployment/cicd.md | 1 (versions), 3 (crate names), 4 (source paths), 5 (make targets), 6 (workflow/job names), 7 (error types), 8 (feature flags), prose | 1: `grep -nE 'v?[0-9]+\.[0-9]+\.[0-9]+' docs/src/deployment/cicd.md` → 0 hits (no version-string finding). 3: `grep -noE 'paladin-[a-z-]+' docs/src/deployment/cicd.md \| sort -u` → 1 hit, `paladin-chart` (line 485, a Helm chart directory name, not a crate — classified per M-06, no action; `ls crates/` has no such entry and none is implied). 4/5/7: 0 hits each (no source paths, `make` targets, or typed errors quoted on this page). 8: `grep -noE -- '--features [a-z0-9,_-]+' docs/src/deployment/cicd.md` → 2 hits, `--features integration-tests` (lines 371, 382) — the flag and the crate feature it names are unchanged in the tree, no finding. 6 (the real findings — re-derived fresh this session, not copied from 16-RESEARCH.md): `grep -noE '[a-z-]+\.yml' docs/src/deployment/cicd.md \| sort -u` vs `ls .github/workflows/` (→ `benchmarks.yml ci.yml docs.yml feature-flags.yml pre-commit.yml release.yml`) showed the page's "Workflow Structure" diagram and its "Integration Testing" section both named `integration-tests.yml`, a file deleted by commit `2cf9919` (`git log --oneline -- .github/workflows/integration-tests.yml` shows the deletion; its job survives as `integration-tests:` inside `ci.yml:388`, confirmed by `grep -n '^  integration-tests:' .github/workflows/ci.yml`). Prose read: the page's top `ci.yml` sample quoted `on: push: branches: [ main, develop ]` / `pull_request: branches: [ main, develop ]` at lines 53/55 — the live trigger is `push: branches: [ '**' ]` / `pull_request: branches: [ main, 'release/**' ]` (`.github/workflows/ci.yml:13-24`; `develop` was pruned by plan 15.1-09's trunk cutover). The sample also named a `check`/`Check` job that does not exist; the live job is `lint`/`Code Quality` (`.github/workflows/ci.yml:41-42`). Fourth occurrence of the same stale trigger at lines 324/326 inside the Integration-Testing yaml sample, from when that job still had its own standalone `on:`/`schedule` block as a separate workflow file. Post-fix re-verification: `grep -cE 'main, ?develop' docs/src/deployment/cicd.md` → 0; `grep -c 'integration-tests.yml' docs/src/deployment/cicd.md` → 0; `grep -cF "branches: [ '**' ]" docs/src/deployment/cicd.md` → 1; `grep -c 'Code Quality' docs/src/deployment/cicd.md` → 1; `mdbook build docs/` → exit 0 (Run 3, `16-LINKCHECK-REPORT.md`). **Out-of-scope findings deliberately not fixed here** (not part of 16-RESEARCH.md's three pre-evidenced findings, no acceptance criterion covers them, logged to `deferred-items.md` per the executor's scope-boundary rule): the "Docker Build Pipeline" section names a `docker-publish.yml` workflow and the "Security Scanning" section names a `security.yml` workflow (including a `snyk` job) — neither file exists in `ls .github/workflows/`, and per `.github/instructions/security.instructions.md` Snyk was evaluated and removed from this project entirely, so that job also contradicts current governance, not just current file layout. | updated → commit |
| docs/src/deployment/docker.md | — | — | pending — not yet checked |
| docs/src/deployment/kubernetes.md | — | — | pending — not yet checked |
| docs/src/deployment/production.md | — | — | pending — not yet checked |
| docs/src/operations/logging.md | — | — | pending — not yet checked |
| docs/src/operations/monitoring.md | — | — | pending — not yet checked |
| docs/src/operations/performance-tuning.md | — | — | pending — not yet checked |
| docs/src/operations/troubleshooting.md | — | — | pending — not yet checked |

**Row count check:** 14 data rows (`grep -c '^| docs/src/' 16-DOCS-01-VERDICTS.md` → 14).
**Not-yet-checked count check:** thirteen of those fourteen rows still carry the seeded
not-yet-checked verdict text above after this plan; only the `cicd.md` row has moved to
`updated → commit`.
