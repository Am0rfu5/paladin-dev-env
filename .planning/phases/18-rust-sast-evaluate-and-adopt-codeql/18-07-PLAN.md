---
phase: 18-rust-sast-evaluate-and-adopt-codeql
plan: 07
type: execute
wave: 7
depends_on: [18-06]
files_modified:
  - .github/instructions/security.instructions.md
  - CLAUDE.md
  - .github/copilot-instructions.md
  - .planning/STATE.md
  - .planning/MILESTONES.md
  - .github/workflows/codeql.yml
  - docs/src/contributing/branching-model.md
  - .planning/phases/18-rust-sast-evaluate-and-adopt-codeql/18-CODEQL-EVIDENCE.md
autonomous: true
requirements: [SAST-04]

must_haves:
  truths:
    - "After the rewrite, `.github/instructions/security.instructions.md` still contains the Snyk verdict section verbatim — it is a standing prohibition, not stale text — and the Rust-SAST section is narrowed by evidence rather than removed."
    - "On a zero-finding outcome the Rust-SAST section records which tool was measured, on what date, with what finding count and what analysed-file count, and still states that no static taint analysis of first-party Rust is in place; it is never deleted to imply coverage."
    - "The three manual credential-handling checks remain listed in their existing order, and any one of them removed carries a per-check evidence line naming the CodeQL query that covers it."
    - "No document in the repository claims automated Rust coverage the probe did not demonstrate: every claim in the rewritten section maps to a number recorded in the evidence document."
    - "Every place that currently asserts the gap moves in the same change — the security instructions, the project guide's security bullet, the shared agent instructions, the project state's known-limitations row and the milestone record."
    - "If the tool was disqualified, the workflow and its trigger-policy register row are removed together so no scan remains that reads as assurance while analysing nothing, and the fixture stays in the tree so any successor tool is measured against the identical probe."
  prohibitions:
    - statement: "Documentation must not claim coverage the probe did not demonstrate, and the Rust-SAST section must not be deleted in a way that implies a gap was closed when it was only measured."
      status: resolved
      verification: judgment
    - statement: "The standing Snyk prohibition must not be softened, relocated or removed while adjacent text is rewritten — it records a measured verdict and a rule, not stale history."
      status: resolved
      verification: judgment
  artifacts:
    - path: ".github/instructions/security.instructions.md"
      provides: "The rewritten Rust-SAST section, narrowed by measured evidence, with the manual-review ownership restated"
      contains: "Snyk was evaluated and removed"
    - path: ".planning/STATE.md"
      provides: "Known-limitations row updated to the settled verdict"
    - path: ".planning/MILESTONES.md"
      provides: "Milestone record updated to the settled verdict"
  key_links:
    - from: ".github/instructions/security.instructions.md"
      to: ".planning/phases/18-rust-sast-evaluate-and-adopt-codeql/18-CODEQL-EVIDENCE.md"
      via: "every coverage claim in the rewritten section cites a number recorded in the evidence document"
      pattern: "CodeQL"
    - from: "CLAUDE.md"
      to: ".github/instructions/security.instructions.md"
      via: "CLAUDE.md imports the security instructions and restates its security bullet; both must agree"
      pattern: "security.instructions.md"
---

<objective>
Close the phase by making every record of the Rust-SAST gap say what was actually measured — and
nothing more.

The section is narrowed by evidence, never deleted. If CodeQL qualified, the section states what it
covers, what it does not, and what the manual credential-handling review still owns. If it was
disqualified, the section records which tool was measured, on what date, with what finding count and
what analysed-file count, so the next person to ask does not repeat the evaluation blind — and the
workflow comes back out of the tree rather than sitting there reading as assurance.

The Snyk section stays. It is a standing prohibition, not stale text.

Purpose: SAST-04, plus the phase's own carried state.
Output: a rewritten Rust-SAST section, a project guide and shared agent instructions that agree
with it, and a project state and milestone record carrying the settled verdict.
</objective>

<execution_context>
@/workspace/.claude/gsd-core/workflows/execute-plan.md
@/workspace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-rust-sast-evaluate-and-adopt-codeql/18-CONTEXT.md
@.planning/phases/18-rust-sast-evaluate-and-adopt-codeql/18-03-SUMMARY.md
@.planning/phases/18-rust-sast-evaluate-and-adopt-codeql/18-06-SUMMARY.md
@.github/instructions/security.instructions.md
@CLAUDE.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Created by plan |
|---|---|---|
| `.github/workflows/codeql.yml` + its `branching-model.md` register row | new workflow and register row | 18-01 (removed here only on a disqualified verdict) |
| `scripts/codeql-analysed-files.sh` | new script | 18-01 |
| `18-CODEQL-EVIDENCE.md` under the phase directory | new evidence document | 18-01, closed here |
| `fixtures/codeql-probe/` — standalone crate, five planted defect classes | new fixture crate | 18-02 (kept in every branch) |
| `exclude` key in the root `Cargo.toml` `[workspace]` table | new key | 18-02 |
| `.gitleaks.toml` allowlist path scoped to the fixture | new allowlist path | 18-02 |
| `.github/codeql/codeql-config.yml` and `.github/codeql/codeql-config-probe.yml` | new CodeQL configs | 18-02 |
| `scan_probe_fixture` dispatch input on `codeql.yml` | new workflow input | 18-02 |
| `CODEQL-DISMISSALS.md` | new governed alert-dismissal register | 18-04 |
| `scripts/check-codeql-dismissals.sh` | new offline guard script | 18-04 |
| `tests/scripts/check-codeql-dismissals_test.sh` | new regression harness | 18-04 |
| `check-codeql-dismissals` Makefile target (+ `check-gates`, `test-shell-guards`) | new make target | 18-04 |
| "Check CodeQL dismissal register" step in `ci.yml`'s `License & Dependency Policy` job | new CI step | 18-04 |
| `{"context": "CodeQL Analysis (Rust)"}` entry in `.github/rulesets/protect-main-branch.json` | new ruleset entry (promotion branch only) | 18-06 |
| Corrected ruleset re-application procedure in `docs/src/appendix/branch-protection.md` | corrected procedure | 18-06 |
| Rewritten Rust-SAST section in `.github/instructions/security.instructions.md` | rewritten section | 18-07 |

<!-- planner-discipline-allow: No tool above performs taint analysis of first-party Rust -->

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the Rust-SAST section to match the measured outcome</name>
  <files>.github/instructions/security.instructions.md, .planning/phases/18-rust-sast-evaluate-and-adopt-codeql/18-CODEQL-EVIDENCE.md</files>
  <read_first>
    - `.github/instructions/security.instructions.md` (whole file, 63 lines) — the tool table, the "Manual review still matters" preamble and its three concrete credential-handling checks, the Snyk verdict section with its "Measured, not assumed:" structure and concrete counts, and the Rust-SAST section at the end
    - `.planning/phases/18-rust-sast-evaluate-and-adopt-codeql/18-CODEQL-EVIDENCE.md` — `## Probe Result`, `## Baseline Comparison`, `## Analysis Coverage`, `## Verdict`, `## Observation Window` → `### Metrics`, `## Promotion Status`
    - `.planning/phases/18-rust-sast-evaluate-and-adopt-codeql/18-CONTEXT.md` — D-21 (the blast radius) and D-22 (narrowed by evidence, never deleted; the three manual checks stay human-owned unless the probe proved otherwise for a specific one)
    - `.planning/REQUIREMENTS.md` §SAST-04
  </read_first>
  <action>
    Rewrite the Rust-SAST section of `.github/instructions/security.instructions.md` so it states
    the measured outcome and nothing beyond it. Echo the Snyk section's rhetorical structure — a
    dated verdict followed by "Measured, not assumed:" and concrete bullet evidence — because that
    is what makes a verdict readable as evidence rather than as opinion.

    Every claim must map to a number already recorded in `18-CODEQL-EVIDENCE.md`: the per-class
    probe result, the analysed-file count against the 385 denominator, the false-positive rate with
    its raw counts, and whether the scan gates or annotates. Cite the evidence document by path so
    a reader can check the working.

    On a qualifying outcome, state what the tool covers and — with equal prominence — what it does
    not: name the query suite in use, state whether feature-gated code was shown to be reached
    (the two-signal answer from 18-03), and state whether the scan gates merges or only annotates
    them, matching whatever `## Promotion Status` records. If the promotion was held, say advisory
    and name the open item; do not describe an annotating scan as a gate.

    On a zero-finding outcome, the section is narrowed, not deleted (D-22). It must record which
    tool was measured, on what date, with what finding count and what analysed-file count, and it
    must still state that no static taint analysis of first-party Rust is in place — the gap was
    measured, not closed. Also record that the Semgrep contingency's trigger condition is now met
    and that any successor tool is measured against the identical fixture at
    `fixtures/codeql-probe/` (D-20), so the next evaluation produces a comparable number rather
    than a fresh one.

    Update the "Manual review still matters" preamble so its standing claim about first-party Rust
    taint analysis matches the outcome. Keep all three concrete credential-handling checks —
    redact-before-truncate, no key interpolation in logs, no redirect-following on credentialed
    clients — listed in their existing order and stated as human-owned. Remove one only if the
    probe specifically demonstrated that a named CodeQL query covers it, and in that case replace
    the bullet with a line naming that query and the evidence.

    Leave the Snyk verdict section exactly as it is. It records a measured verdict and a standing
    prohibition; adjacent rewriting is not a licence to touch it.

    Finally, set `## Verdict` in the evidence document to its closed form: the verdict, the date,
    and a pointer to the section it now backs.
  </action>
  <acceptance_criteria>
    - `.github/instructions/security.instructions.md` still contains the heading `## Snyk was evaluated and removed (2026-08-18)` and its section body is unchanged in this commit's diff
    - A section covering Rust static analysis is still present — the file's heading count did not decrease
    - The rewritten section contains the string `CodeQL`, a date, and at least two integers that also appear in `18-CODEQL-EVIDENCE.md`
    - The rewritten section references `18-CODEQL-EVIDENCE.md` by path
    - The three manual credential-handling checks are still present in their existing order, or each removed one is replaced by a line naming the CodeQL query that covers it
    - On a disqualified verdict, the rewritten section states that no static taint analysis of first-party Rust is in place, and names `fixtures/codeql-probe/` as the fixture a successor tool is measured against
    - On a qualified verdict where the promotion was held, the section describes the scan as advisory and does not describe it as gating merges
    - `pre-commit run --all-files` exits 0
  </acceptance_criteria>
  <verify>
    <automated>F=.github/instructions/security.instructions.md; EV=.planning/phases/18-rust-sast-evaluate-and-adopt-codeql/18-CODEQL-EVIDENCE.md; grep -qF '## Snyk was evaluated and removed (2026-08-18)' "$F" && grep -q 'CodeQL' "$F" && grep -q '18-CODEQL-EVIDENCE.md' "$F" && python3 -c "
import re,subprocess,pathlib
f=pathlib.Path('$F').read_text()
for k in ('redact','interpolat','redirect'):
    assert re.search(k, f, re.I), 'manual credential-handling check missing: '+k
heads=[l for l in f.splitlines() if l.startswith('## ')]
assert len(heads)>=3, 'a whole section was deleted rather than narrowed: %r' % heads
snyk=subprocess.check_output(['git','diff','HEAD~1','HEAD','--','$F'],text=True)
assert 'Snyk Code (SAST)' not in [l[1:].strip() for l in snyk.splitlines() if l.startswith('-')], 'the Snyk verdict section was modified'
ev=pathlib.Path('$EV').read_text()
nums=set(re.findall(r'\b[0-9]{1,6}\b', f)) & set(re.findall(r'\b[0-9]{1,6}\b', ev))
assert len(nums)>=2, 'rewritten section cites fewer than two numbers that appear in the evidence document'
print('SAST-04 rewrite consistent with the evidence: shared figures %s' % sorted(nums)[:6])
" && pre-commit run --all-files</automated>
  </verify>
  <done>The Rust-SAST section states the measured outcome with its numbers, the Snyk prohibition is untouched, and the manual credential-handling review's ownership is restated rather than implied away.</done>
</task>

<task type="auto">
  <name>Task 2: Propagate the settled verdict to every place that asserts the gap</name>
  <files>CLAUDE.md, .github/copilot-instructions.md, .planning/STATE.md, .planning/MILESTONES.md</files>
  <read_first>
    - `CLAUDE.md` lines 60-75 — the Security working-agreement bullet, which restates the tool set and the standing Snyk prohibition
    - `.github/copilot-instructions.md` — its Security section and its coverage-requirements subsection, which mirror `CLAUDE.md`'s claims for the shared agent audience
    - `.planning/STATE.md` — the known-limitations table row recording "No static taint analysis for first-party Rust" and naming Phase 18 as its owner, and the position/current-focus block near the top
    - `.planning/MILESTONES.md` — the "No Rust SAST" entry naming Phase 18 as its owner
    - `.github/instructions/security.instructions.md` (rewritten in Task 1) — the single source the other three must agree with
  </read_first>
  <action>
    Bring every other place that asserts the gap into agreement with Task 1's rewritten section
    (D-21). These are separate documents restating one fact, so they move together or the record
    contradicts itself.

    `CLAUDE.md`'s Security bullet: update it to name whatever automated Rust analysis now exists
    and whether it gates or annotates, and to keep the manual credential-handling review named as
    a distinct, still-required step. Keep the standing instruction not to reintroduce Snyk and not
    to record a phase as blocked on one — that prohibition is unaffected by this phase's outcome.

    `.github/copilot-instructions.md`: apply the same correction to its Security section so the
    shared agent instructions and the project guide do not disagree.

    `.planning/STATE.md`: rewrite the known-limitations row for first-party Rust taint analysis to
    the settled verdict, with its date and a pointer to `18-CODEQL-EVIDENCE.md`. If 18-06 held the
    promotion, mirror that open item here with the same owner and revisit date rather than
    inventing new ones. Update the position and current-focus block to reflect the phase's close.

    `.planning/MILESTONES.md`: update the "No Rust SAST" entry the same way — settled verdict,
    date, evidence pointer — rather than deleting it, so the milestone record shows what changed
    and when.

    Do not restate numbers here that are not in the evidence document, and do not describe an
    annotating scan as a gate in any of the four.
  </action>
  <acceptance_criteria>
    - `CLAUDE.md`'s Security bullet names the current automated Rust analysis state and still contains the standing instruction not to reintroduce Snyk
    - `.github/copilot-instructions.md`'s Security section makes the same claim as `CLAUDE.md` — neither describes a gate the other calls advisory
    - `.planning/STATE.md`'s known-limitations row for first-party Rust taint analysis names the verdict, a date, and `18-CODEQL-EVIDENCE.md`
    - `.planning/MILESTONES.md`'s Rust-SAST entry names the verdict, a date, and `18-CODEQL-EVIDENCE.md`, and is updated rather than deleted
    - If 18-06 recorded a held promotion, `.planning/STATE.md` carries the same owner and revisit date as `18-CODEQL-EVIDENCE.md`'s open item
    - All four files and `.github/instructions/security.instructions.md` agree on whether the scan gates or annotates
    - `pre-commit run --all-files` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import re,pathlib
sec=pathlib.Path('.github/instructions/security.instructions.md').read_text()
claude=pathlib.Path('CLAUDE.md').read_text()
copilot=pathlib.Path('.github/copilot-instructions.md').read_text()
state=pathlib.Path('.planning/STATE.md').read_text()
miles=pathlib.Path('.planning/MILESTONES.md').read_text()
assert 'Snyk' in claude and 'do not' in claude.lower(), 'the standing Snyk prohibition left CLAUDE.md'
for name,txt in (('STATE.md',state),('MILESTONES.md',miles)):
    assert '18-CODEQL-EVIDENCE.md' in txt, '%s does not point at the evidence document' % name
gating = lambda t: bool(re.search(r'required (status )?check|gates? (the )?merge', t, re.I))
claims={'security.instructions.md':gating(sec),'CLAUDE.md':gating(claude),'copilot-instructions.md':gating(copilot)}
assert len(set(claims.values()))==1, 'documents disagree on whether the scan gates: %s' % claims
print('verdict propagated consistently; gating claim = %s' % list(claims.values())[0])
" && pre-commit run --all-files</automated>
  </verify>
  <done>The project guide, the shared agent instructions, the project state and the milestone record all say the same thing the security instructions say, with a date and a pointer to the evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Settle the workflow's own fate to match the verdict</name>
  <files>.github/workflows/codeql.yml, docs/src/contributing/branching-model.md, .github/codeql/codeql-config.yml, .github/codeql/codeql-config-probe.yml, .planning/phases/18-rust-sast-evaluate-and-adopt-codeql/18-CODEQL-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/18-rust-sast-evaluate-and-adopt-codeql/18-CODEQL-EVIDENCE.md` — `## Verdict` and `## Promotion Status`
    - `docs/src/contributing/branching-model.md` — the trigger-policy table and the paragraph above it explaining that a workflow with no row fails a required check
    - `scripts/check-workflow-triggers.sh` lines 20-40 — Clause 1 (every workflow file needs a row) and Clause 2 (drift), which together govern what removing a workflow requires
    - `.planning/phases/18-rust-sast-evaluate-and-adopt-codeql/18-CONTEXT.md` — D-07 (the fixture is kept for reproducibility) and D-11 (no adoption work proceeds past a failed probe)
  </read_first>
  <action>
    Execute exactly one branch, determined by `## Verdict`.

    **If the verdict was `qualified` or `qualified-with-coverage-gap`:** the workflow stays.
    Confirm `.github/workflows/codeql.yml`, its two configuration files and its trigger-policy row
    are all still present and mutually consistent, and that the trigger guard passes. Record in
    `18-CODEQL-EVIDENCE.md` that the scan remains in the tree in whichever posture
    `## Promotion Status` names. Change nothing else.

    **If the verdict was `disqualified`:** remove the scan rather than leaving it running. A
    workflow that analyses a fraction of the tree while reporting green is the failure this whole
    phase exists to prevent, and keeping it "just in case" reintroduces it. In one commit, delete
    `.github/workflows/codeql.yml`, both files under `.github/codeql/`, and the `codeql.yml` row
    from the trigger-policy table — the row and the file are governed together, so removing one
    without the other breaks Clause 1 or leaves a row describing nothing.

    Keep `fixtures/codeql-probe/`, its workspace exclusion, its secret-scanning accommodation and
    `scripts/codeql-analysed-files.sh` in every branch. They are the reproducible apparatus any
    successor tool is measured with, and discarding them would reduce this verdict to the
    unrepeatable kind the Snyk episode already produced once.

    Then run `scripts/check-workflow-triggers.sh` and `make check-gates` and leave both green.
  </action>
  <acceptance_criteria>
    - `18-CODEQL-EVIDENCE.md` records which branch was executed and why, naming the verdict it followed
    - **Qualified branch:** `.github/workflows/codeql.yml`, `.github/codeql/codeql-config.yml` and `.github/codeql/codeql-config-probe.yml` all exist, and the trigger-policy table still has exactly one `` `codeql.yml` `` row
    - **Disqualified branch:** none of `.github/workflows/codeql.yml`, `.github/codeql/codeql-config.yml`, `.github/codeql/codeql-config-probe.yml` exists, the trigger-policy table has zero `` `codeql.yml` `` rows, and both removals are in the same commit
    - Either branch: `fixtures/codeql-probe/Cargo.toml` and `scripts/codeql-analysed-files.sh` still exist
    - Either branch: `bash scripts/check-workflow-triggers.sh` exits 0
    - Either branch: `make check-gates` exits 0 and `pre-commit run --all-files` exits 0
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-workflow-triggers.sh && make check-gates && test -f fixtures/codeql-probe/Cargo.toml && test -x scripts/codeql-analysed-files.sh && python3 -c "
import os,pathlib,re
ev=pathlib.Path('.planning/phases/18-rust-sast-evaluate-and-adopt-codeql/18-CODEQL-EVIDENCE.md').read_text()
v=ev.split('## Verdict',1)[1].split('##',1)[0]
disq='disqualified' in v
wf=os.path.exists('.github/workflows/codeql.yml')
cfg=os.path.exists('.github/codeql/codeql-config.yml') or os.path.exists('.github/codeql/codeql-config-probe.yml')
rows=len(re.findall(r'^\| \`codeql\.yml\` \|', pathlib.Path('docs/src/contributing/branching-model.md').read_text(), re.M))
if disq:
    assert not wf and not cfg, 'disqualified verdict but the workflow or its config survives'
    assert rows==0, 'disqualified verdict but a trigger-policy row survives'
    print('disqualified: scan removed, fixture and evidence retained')
else:
    assert wf and cfg, 'qualified verdict but the workflow or its config is missing'
    assert rows==1, 'expected exactly one codeql.yml register row, found %d' % rows
    print('qualified: scan retained with its register row')
" && pre-commit run --all-files</automated>
  </verify>
  <done>The tree matches the verdict: the scan is retained in its recorded posture, or removed together with its register row and configuration, with the probe apparatus kept either way.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured evidence → documented claim | The evidence document holds numbers; the instructions hold what readers and agents believe. Any gap between them is an unearned assurance claim |
| security instructions → agent behaviour | `CLAUDE.md` and `.github/copilot-instructions.md` are always-on instructions; a wrong coverage claim there changes what every future agent skips |
| workflow presence → perceived coverage | A workflow left in the tree after a disqualifying verdict reads as a working control to anyone who does not open the evidence document |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-33 | Repudiation | Documentation claiming unearned coverage | high | mitigate | Every claim in the rewritten section maps to a number recorded in `18-CODEQL-EVIDENCE.md`, and the verify asserts at least two shared figures between the two files plus a path reference to the evidence. |
| T-18-34 | Repudiation | The Rust-SAST section deleted rather than narrowed | high | mitigate | The section is narrowed by evidence and the verify asserts the file's heading count did not fall; a zero-finding outcome must still state that no static taint analysis of first-party Rust is in place. |
| T-18-35 | Tampering | The standing Snyk prohibition softened while adjacent text is rewritten | high | mitigate | The Snyk heading is asserted present verbatim and the commit diff is checked for edits inside that section; `CLAUDE.md`'s prohibition line is asserted intact. |
| T-18-36 | Repudiation | Four documents disagreeing about whether the scan gates | high | mitigate | The verify compares the gating claim across the security instructions, the project guide and the shared agent instructions and fails when they disagree. |
| T-18-37 | Repudiation | A disqualified scanner left running as apparent assurance | high | mitigate | On a disqualified verdict the workflow, both configurations and the trigger-policy row are removed in one commit, asserted by file-absence and row-count checks; the fixture and the evidence apparatus are retained so the verdict stays reproducible. |
| T-18-38 | Repudiation | Manual credential-handling review implied away | high | mitigate | All three concrete checks are asserted present in their existing order; removing one requires a replacement line naming the specific query that covers it. |
| T-18-SC | Tampering | Package-manager installs | high | mitigate | This plan installs nothing. RESEARCH.md's Package Legitimacy Audit records no `[SLOP]` and no `[SUS]` verdicts for this phase. |
</threat_model>

<verification>
- `.github/instructions/security.instructions.md` retains the Snyk verdict section verbatim and its Rust-SAST section cites numbers that appear in `18-CODEQL-EVIDENCE.md`
- `CLAUDE.md`, `.github/copilot-instructions.md`, `.planning/STATE.md` and `.planning/MILESTONES.md` all agree with it
- `bash scripts/check-workflow-triggers.sh` exits 0
- `make check-gates` exits 0
- `pre-commit run --all-files` exits 0
- `fixtures/codeql-probe/` and `scripts/codeql-analysed-files.sh` survive in every branch
</verification>

<success_criteria>
No record in this repository still asserts a gap that was measured, and none claims coverage the
probe did not demonstrate. The manual credential-handling review keeps exactly the ground the
evidence leaves it, the Snyk prohibition stands unchanged, and the tree matches the verdict rather
than hedging between outcomes.
</success_criteria>

<output>
Create `.planning/phases/18-rust-sast-evaluate-and-adopt-codeql/18-07-SUMMARY.md` when done
</output>
